---
slug: snapshot-shot-flow-mismatch
status: resolved
trigger: "Snapshot shot flow doesn't match other shot flows: snapshot path, path color, and goalie dive behavior are inconsistent with regular/headed shots, and the goalie is not getting the ball on a snapshot save."
created: '2026-06-16'
updated: '2026-06-19'
resolved: '2026-06-19'
---

## FINAL HUMAN VERIFICATION (2026-06-19)

User confirmed in the browser, after restarting both dev server processes (the
stale-process theory documented in ROUND 2's Current Focus / Evidence was correct):

1. Out-of-range GK dive now auto-skips to GOAL for both snapshot and header shots
   (the GK-range auto-GOAL gate, including the round-2 header fallback, fires correctly
   instead of leaving the defending team stuck in GK_DIVE with no selectable dive hexes).
2. A legitimate in-range save now correctly transfers ball possession to the GK
   (the premature-deflection GAME_SHOT fix from round 1 holds up live — the snapshot's
   fate is no longer resolved on pre-repositioning positions, so SHOT/GK_DIVE's SAVE
   branch is actually reached and sets carrierId: gk.id).

Both previously-failing symptoms reported at the top of ROUND 2 are now resolved.
**Root cause of the round-1-vs-round-2 discrepancy**: the running dev server processes
(server PID 56828/24900, client PID 22692/26484) had been started on 2026-06-14 —
before EITHER round of code fixes existed (round 1 on 2026-06-16, round 2 on
2026-06-19) — and had not been restarted, so the browser was exercising stale code
the entire time the symptoms were reported as "still broken." All server-side logic
(GK-range gate, SAVE→carrierId transfer, snapshot path color, single-click GK/defender
movement) was already correct and deterministically proven by test before this
verification; restarting both dev servers was sufficient to resolve the live behavior.

## ROUND 2 — human re-test after round-1 fix (2026-06-19)

Human verified round 1 in the browser:

- Path color: CONFIRMED FIXED.
- GK dive UX: STILL BROKEN — "only path is selectable but it is out of range. dive phase
  should skip if goalie not in range. this needs to be fixed for the header shot progression too."
- Save possession: STILL BROKEN — "ball didnt go to keeper on save and keeper saved a shot
  that was out of range. both are wrong behaviors"

A `gsd-debugger` agent was spawned to continue and hit the weekly usage limit mid-investigation
(77 tool calls, no final report). Its uncommitted work-in-progress was recovered from the working
tree (still present, never committed — same as round 1's fixes). Orchestrator (this session)
verified the recovered state before re-delegating:

- `pnpm typecheck` clean on server + client.
- Full server suite: 293/299 pass; the 4 failures are the SAME pre-existing unrelated RED stubs
  documented since phase 17.1 (FREE_MOVE transition, FIRST_TIME_PASS delivery x2, SNAP_DEFLECT
  end-turn PASS-02) — confirmed NOT caused by this work.
- Full client suite: 95/95 pass.
- `gameHandlers.phase10.test.ts`: 16/16 pass (includes round-1's 4 new regression tests).

**What the limit-hit agent had already added (uncommitted, in `gameHandlers.ts`):**

- A GK-range auto-GOAL fallback in the `GAME_HEADER_TARGET` handler (after
  `applyResolveHeaderTarget`, when result phase is `GK_DIVE`): mirrors the existing
  out-of-range auto-GOAL check already present in `GAME_SHOT` (step 7) and in the
  `SNAPSHOT_DEFLECT` end-of-turn handler (lines ~906-914) — if no shot-path hex is within
  3 hexes of the defending GK, auto-resolves to GOAL instead of entering `GK_DIVE` with zero
  selectable dive hexes. **This new header code path has NO test coverage yet** and was
  never manually verified — added on the hypothesis that header shares the same
  "GK_DIVE entered despite being unreachable" defect the user reported, but this is UNCONFIRMED.

**Orchestrator's own static read of the SNAPSHOT_DEFLECT path (NOT yet empirically verified
via a live reproduction):**

- `gameHandlers.ts:906-914` (SNAPSHOT_DEFLECT end-of-turn GK-range gate) and the client's
  `gkDiveTargetSet` computation (`HexGrid.tsx:113-126`) both independently filter shot-path
  hexes by `hexDistance(gk.position, h) <= 3`, using the same `hexLine(shooter, target)` path —
  these two computations LOOK consistent on paper (same formula, same threshold).
- `applyGKDive` (`gameEngine.ts:2882-2929`) independently re-validates `TOO_FAR` server-side
  (`hexDistance(gk.position, to) > 3`) on every dive click — so an out-of-range click SHOULD be
  rejected even if the client incorrectly highlighted it as selectable.
- Despite all of the above appearing correct in isolation, the user's live playtest still
  observed: a selectable-looking out-of-range path, an accepted out-of-range save, AND the ball
  not transferring to the GK on that save. **No reproduction test has been written yet to
  empirically confirm or refute any of this — all evidence above is static code reading only.**
  The discrepancy between "code looks correct" and "live behavior is wrong" is itself a key
  clue: check for state desync (client showing stale `gkDivePosition`/`shotTargetHex` from a
  PRIOR action), an alternate/legacy code path being hit instead of the one read above, or a
  build/HMR staleness issue, in addition to a logic bug not yet spotted by static reading.

# Debug Session: snapshot-shot-flow-mismatch

## Symptoms

- **Expected:** Snapshot shot flow should match regular/headed shot flow: same path-highlight color, GK selects a save spot directly on the shot path within range (not moved hex-by-hex), and a successful save transfers ball possession to the GK.
- **Actual:**
  1. Snapshot shot path is highlighted with the wrong color compared to regular/headed shots.
  2. GK dive UX differs: for snapshot, the GK must be moved hex-by-hex toward the shot path instead of selecting a spot in range directly on the path (as regular/headed shots allow).
  3. On a successful snapshot save, the GK does not receive the ball — ball possession incorrectly remains with the shooter/attacking team.
- **Error messages:** None reported
- **Timeline:** Always been broken (not a regression) — snapshot shot flow has never matched regular/headed shot behavior
- **Reproduction:** Trigger a snapshot shot (SNAPSHOT/SNAP_DEFLECT path) in-game; compare path color, GK dive interaction, and post-save ball possession against a regular or headed shot

## Current Focus

next_action: "DONE. Human re-verified in the browser after restarting both dev server processes — both previously-failing symptoms (out-of-range GK dive not auto-skipping to GOAL; legitimate in-range save not transferring the ball to the GK) are now resolved. No further action needed."
hypothesis: "ROUND 2 RESOLVED for server-side logic — deterministic testing (forced dice via vi.mock) proves the GK-range auto-GOAL gate and SAVE->carrierId transfer are both correct for all three GK_DIVE entry points (regular shot, snapshot, header). The header auto-GOAL fallback left uncommitted by the limit-hit agent is correct and now has test coverage. No remaining server logic defect was found despite deliberately adversarial deterministic testing (forced SAVE outcome, forced out-of-range GK, both end-to-end through real socket handlers with real squad data). The leading explanation for the user's live-play contradiction is that their dev server processes were started days before any of these fixes (round 1 on 2026-06-16, round 2 header fix today) and may not have been restarted/reloaded since — this is the single most likely explanation for 'code says X, browser shows not-X' and must be ruled out by the human before further server-side investigation is warranted."
test: "n/a — root cause investigation complete for server logic; remaining uncertainty is environmental (stale running process), which only the human can confirm/rule out by restarting `pnpm --filter @counter-attack/server dev` and `pnpm --filter @counter-attack/client dev` and hard-refreshing the browser before re-testing."
expecting: "If the user restarts both dev servers, hard-refreshes, and re-tests: symptom 1 (out-of-range path selectable, dive phase doesn't skip) and symptom 2 (ball not transferring to GK on save) should both now be resolved, since the gate/transfer logic is proven correct deterministically. If either symptom STILL reproduces after a confirmed clean restart, that would be strong evidence of an actual remaining client-side defect not yet found by static reading of HexGrid.tsx/useGameStore.ts, and investigation should resume there specifically (not server-side, which is now proven correct)."

reasoning_checkpoint:
hypothesis: "Three independent, falsifiable root causes: (1) HexGrid.tsx classifies the SNAPSHOT_DEFLECT shot-path hexes as highlightType 'risk' (orange) via the isRisk union (line 329) while the regular/headed GK_DIVE shot path uses lastShotPathSet classified as 'shot-path' (white, line 339) -- a hardcoded, inconsistent classification, not a computed/legitimate difference. (2) gameHandlers.ts's GAME_SHOT handler executes its deflection-check (step 6) and GK-range-check (step 7) unconditionally after applyDeclareShot, even when the resulting phase is SNAPSHOT_DEFLECT (snapshot path) instead of GK_DIVE (regular path) -- causing the snapshot's fate to be resolved using pre-repositioning-move positions, bypassing the correct post-move checks already implemented in the SNAPSHOT_DEFLECT end-of-turn handler (gameHandlers.ts:815-976). (3) Symptom 2 (hex-by-hex GK movement) is caused by the SNAPSHOT_DEFLECT phase requiring strict adjacency-only movement (hexDistance===1 per click) for the defending team's repositioning, instead of letting the GK click a final spot directly the way GK_DIVE's applyGKDive does."
confirming_evidence: - "HexGrid.tsx line 329: isRisk = (...) || isShotPath, where isShotPath = snapDeflectPathSet.has(hexId) (line 272) -- directly observed in source, not inferred." - "gameHandlers.ts GAME_SHOT handler (line 1352) has a single phase guard permitting both 'PASS' and 'SNAPSHOT_TARGET' (line 1367), with no branch after applyDeclareShot to skip steps 6 (1398-1463) and 7 (1465-1529) when declaredState.phase === 'SNAPSHOT_DEFLECT' -- directly observed; these steps reference shotShooter/shotPathTarget computed from declaredState which, for the snapshot case, is still pre-repositioning state." - "gameHandlers.ts lines 368-379 (SNAPSHOT_DEFLECT move handler) requires hexDistance(sdPiece.position, to) === 1 -- contrasted directly against applyGKDive (gameEngine.ts:2911-2915) which allows hexDistance(gk.position, to) <= 3 in one click."
falsification_test: "If, after instrumenting/running the actual game, a snapshot shot's path renders white (not orange) without code changes, hypothesis 1 is false. If the SNAPSHOT_DEFLECT phase is observed to wait for the defending team's full repositioning turn before any deflection/auto-goal resolution happens (i.e., steps 6/7 of GAME_SHOT are actually gated and never reached for SNAPSHOT_TARGET), hypothesis 2 is false. If a snapshot GK can be observed clicking a single far hex directly (not adjacency-locked) during the deflect-move turn, hypothesis 3 is false. None of these were observed; all are directly contradicted by the code paths read above with no remaining ambiguity."
fix_rationale: "Each fix targets the actual mechanism, not a symptom patch: (1) change the highlight classification so SNAPSHOT_DEFLECT's shot path renders with the same 'shot-path' (white) classification as regular shots, removing the special-cased 'risk' (orange) override. (2) Gate GAME_SHOT's steps 6/7 behind a check that the resulting phase is GK_DIVE (i.e., NOT SNAPSHOT_DEFLECT) -- for the snapshot case, simply enter SNAPSHOT_DEFLECT and let the existing (already correct) end-of-turn handler in gameHandlers.ts run the deflection/GK-range logic exactly once, after the repositioning turn. (3) Change the SNAPSHOT_DEFLECT defending-piece movement handler to allow a direct one-click target (any pitch hex within 2 hexes of the piece's current position, mirroring GK_DIVE's distance-based click instead of strict per-step adjacency) OR, more consistent with the rulebook intent (SNAP-02: 'opponent gets a 2-hex deflection move'), keep multi-hex total budget but remove the per-click adjacency=1 restriction so a single click of a 2-hex-distant hex is valid -- eliminating the need to click through each intermediate hex one at a time."
blind_spots: "Have not run the test suite yet to see if any existing tests assert the current (buggy) SNAPSHOT_DEFLECT adjacency-only behavior or the orange path-color, which would need updating alongside the fix. Have not manually played through the UI to visually confirm the orange/white color discrepancy in a live browser (relying on source-level color/classification logic only). Have not fully verified whether removing the duplicate deflection check in GAME_SHOT could leave some other regular-shot-only invariant unintentionally broken, since steps 6/7 are shared code -- the safest fix is an early-return/guard specifically for the SNAPSHOT_DEFLECT phase rather than restructuring the shared logic."

## Evidence

- timestamp: "2026-06-16T00:00:00Z"
  checked: "packages/shared/src/snapshotValidator.ts (validateSnapshot) vs packages/server/src/gameEngine.ts (applySnapshot)"
  found: "validateSnapshot() in snapshotValidator.ts is dead code — never called anywhere in gameEngine.ts or gameHandlers.ts (grep for 'validateSnapshot(' and 'shootingPenalty' in both files returns no call sites). The actual snapshot flow is implemented entirely by applySnapshot (gameEngine.ts:2315) and applyDeclareShot's SNAPSHOT_TARGET branch (gameEngine.ts:2796)."
  implication: "The documented SNAP-02 '-1 shooting penalty' on the shooter is never applied anywhere — shooterPenalties in the SHOT case (gameEngine.ts:1445) only ever pushes the generic outside-penalty-area -1, never a snapshot-specific one."

- timestamp: "2026-06-16T00:05:00Z"
  checked: "gameHandlers.ts SNAPSHOT_DEFLECT block (lines 341-411) vs gameEngine.ts applyGKDive (lines 2882-2920)"
  found: "Regular/headed shots go PASS -> GK_DIVE directly (applyDeclareShot, gameEngine.ts:2840-2851), where applyGKDive lets the GK click ANY hex on the shot path within 3 hexes of their current position in one move (hexDistance(gk.position, to) <= 3, no adjacency requirement). Snapshot shots instead go SNAPSHOT_TARGET -> SNAPSHOT_DEFLECT (gameEngine.ts:2796-2814), and the SNAPSHOT_DEFLECT handler in gameHandlers.ts requires hexDistance(piece.position, to) === 1 (strict adjacency) per click, up to 2 hexes total (lines 368-379). Only after the defending team explicitly ends their deflection turn (gameHandlers.ts:817) does the engine check GK range and transition to GK_DIVE (gameHandlers.ts:965-975) for the _actual_ dive — meaning the GK is forced through an extra hex-by-hex 'deflection' pre-phase that regular shots never have."
  implication: "Symptom 2 (GK must be moved hex-by-hex for snapshot, but can click a spot directly for regular/headed shots) is caused by the unnecessary SNAPSHOT_DEFLECT phase using adjacency-only single-step movement for ANY defending piece (not just GK), before ever reaching GK_DIVE. This phase also recomputes 'snapshotGkPenalty' from GK position (gameHandlers.ts:396-401) — a value that is set but never consumed."

- timestamp: "2026-06-16T00:10:00Z"
  checked: "snapshotGkPenalty lifecycle: set in gameEngine.ts:2349/2373, recomputed in gameHandlers.ts:396-401, read in gameEngine.ts SHOT case"
  found: "grep for 'snapshotGkPenalty' across gameEngine.ts shows it is only ever set to 0 (applySnapshot) or null (every SHOT-case outcome branch: lines 1424, 1506, 1548, 1588, 1606) — it is NEVER read/added into gkPenalties in the SHOT case. gkPenalties at line 1442 is always just '[gkSavingPenalty]' from validateGKDive's dive-distance calc. The comment at gameEngine.ts:1439-1440 explicitly states 'Snapshot shots go through GK_DIVE same as regular shots — no special-case needed,' confirming the snapshotGkPenalty field is vestigial/orphaned from an earlier design."
  implication: "This particular orphaned field is NOT the cause of symptom 3 (GK not receiving ball) since it's just an unused penalty value — but its presence is a code smell tied to the same flow as symptom 2. Root cause of symptom 3 must be elsewhere; the SHOT-case SAVE branch (gameEngine.ts:1576-1591, 1592-1609) DOES correctly set carrierId: gk.id for both clean catch and spill-to-GK_RESTART outcomes. Need to check if snapshot's SHOT case is even reached the same way, or if LOOSE_BALL/deflection in SNAPSHOT_DEFLECT's own end-of-turn handler (gameHandlers.ts:883-898) intercepts before SHOT case runs, leaving ball.carrierId null in a path that the UI/symptom-reporter perceives as 'GK not getting the ball.'"

- timestamp: "2026-06-16T00:15:00Z"
  checked: "gameHandlers.ts SNAPSHOT_DEFLECT end-of-turn handler (lines 815-976), specifically the deflected-by-outfield-defender branch (883-898) vs GK-catches-save branch"
  found: "When a defender (not GK) deflects the snapshot on the path (computeShotPathDeflection, line 883), the result transitions straight to phase 'LOOSE_BALL' with ball carrierId: null (line 888) — this is correct, mirrors regular-shot deflection (D-03), and is NOT a bug (loose ball after an outfield deflection should have no carrier). When GK is in range (no deflection), it transitions to GK_DIVE (line 966-975) and the actual save resolution happens later in applyRoll's SHOT case, which DOES set carrierId: gk.id on a clean SAVE per the SAVE branch confirmed above. This means the documented bug ('GK does not receive ball on save') is not reproduced by reading the SAVE-success code path of SHOT case directly."
  implication: "Re-scoping: the SHOT case's SAVE outcome looks correct in isolation. The likely actual root cause is upstream — applyDeclareShot's SNAPSHOT_TARGET branch (gameEngine.ts:2796-2814) sets shotTargetHex AND lastShotPath, but critically does NOT set lastActionType beyond what's already 'SNAPSHOT' from applySnapshot, and the eventual SHOT case at gameEngine.ts:1356 looks up shooter via state.ball.carrierId. Need to verify state.ball.carrierId is still the shooter going into SNAPSHOT_DEFLECT and through to SHOT/GK_DIVE — if SNAPSHOT_DEFLECT's piece-move handler (gameHandlers.ts:402-408) ever moves/clears the shooter or the ball position itself stays stale, the SHOT case's shooter lookup or GK lookup could silently produce a different/incorrect outcome. Need one more read: confirm ball.carrierId/ball.position are untouched through SNAPSHOT_TARGET -> SNAPSHOT_DEFLECT -> GK_DIVE -> SHOT for the save path specifically, and check client-side handling of GK_RESTART phase rendering for snapshot to rule out a client display-only bug masking a correct server state."

- timestamp: "2026-06-19T06:00:00Z"
  checked: "Ran the prior limit-hit agent's scratch file (headerGkRange.scratch.test.ts, 3 tests) as-is against the current uncommitted gameHandlers.ts/gameEngine.ts: (1) HEADER with GK >3 hexes from every path hex via GAME_HEADER_TARGET, (2) HEADER with GK on the goal hex (distance 0) via GAME_HEADER_TARGET -> GAME_GK_DIVE, (3) SNAPSHOT_DEFLECT with GK >3 hexes from every path hex via GAME_END_TURN."
  found: "All 3 PASS. (1) and (3) correctly auto-resolve to KICK_OFF_SETUP with the attacking team's score incremented, never entering GK_DIVE. (2) correctly transitions HEADER -> GK_DIVE -> (after a real, unmocked dice roll) sometimes GK_RESTART with carrierId set to the GK. The prior agent's uncommitted GAME_HEADER_TARGET auto-GOAL fallback (mirroring GAME_SHOT step 7 and the SNAPSHOT_DEFLECT end-of-turn gate) works correctly and is structurally consistent: it looks up the GK via the same `pieces.find(p => p.teamId === defTeam && p.role === 'GK')` pattern, against the same `hexLine(shooter.position, shotTargetHex)` path, as `applyResolveHeaderTarget`'s own `gkDivePosition: gk?.position ?? state.ball.position` seed (gameEngine.ts:2643/2657) — both lookups are guaranteed to agree because they read the identical broadcast GameState fields."
  implication: "The prior agent's HEADER fix is correct and should be committed. The GK-range auto-GOAL gate is now verified at the integration level for ALL THREE GK_DIVE entry points (regular shot, snapshot, header). The static-read concern in the ROUND 2 preamble (code looks correct but user's live play contradicts it) is NOT explained by a gate-logic defect — need a more rigorous deterministic test (real save resolution, not just gate skip) to fully close out symptom 2 (ball not transferring to GK on save), since round-1's only SAVE test was conditional on a random dice roll landing on SAVE."

- timestamp: "2026-06-19T06:10:00Z"
  checked: "Wrote packages/server/src/**tests**/shotGkRange.test.ts using vi.mock('../diceUtils.js', () => ({ rollDice: () => 3 })) (precedent: gameHandlers.rule11.test.ts) plus piece stats (gk.saving=10, gk.handling=10, shooter.shooting=1) to force a deterministic SAVE+clean-catch outcome. Covers GK-range auto-GOAL gate AND deterministic SAVE->carrierId transfer for all 3 flows: regular GAME_SHOT, snapshot (SNAPSHOT_TARGET->SNAPSHOT_DEFLECT->GK_DIVE), and header (GAME_HEADER_TARGET->GK_DIVE)."
  found: "Initial run: 2 of 6 tests FAILED non-deterministically depending on timing — NOT a server logic bug. Root cause of the test failures: the test only awaited clientA's broadcast after a declare-shot/declare-target action, then immediately emitted the next action from clientB and registered a fresh `.once()` GAME_STATE listener on clientB. clientB's OWN copy of the just-broadcast prior-phase state was still in flight on the wire/event-loop at that moment, so the freshly-registered `.once()` listener consumed that STALE prior-phase broadcast instead of the new one produced by the next action — producing a result that looked exactly like 'stuck in GK_DIVE' / 'stuck in SNAPSHOT_DEFLECT' (e.g. received GK_DIVE when GK_RESTART was expected, by one event). Confirmed via Date.now()-stamped event logging on a persistent (non-once) listener: the correct final broadcast (GK_RESTART, carrierId set) DID arrive ~2ms after the stale one — the test's `.once()` simply grabbed the wrong one. Fixed by awaiting BOTH clientA's and clientB's copy of every intermediate broadcast (Promise.all pattern, same convention round-1's own end-to-end save test already used in one place but not consistently) before emitting the next action. After this fix, all 6 tests pass deterministically and repeatably."
  implication: "This is itself an important, generalizable finding: any test (or, by extension, any REAL client) that reacts to a 'next' broadcast via a fire-and-forget single-shot listener registered immediately after sending an action — without having first fully drained/processed the PREVIOUS broadcast that is still in flight to that same socket — can observe a stale intermediate phase. The real client (App.tsx) does NOT have this exact bug: it registers ONE persistent `socket.on(GAME_STATE, onGameState)` listener for the component's lifetime (not a `.once()`), and Socket.io delivers events to a given listener strictly in arrival order, so the real client cannot 'skip ahead' and miss an intermediate broadcast the way a freshly-registered `.once()` test listener can. This rules out the exact mechanism as a CLIENT bug, but it is strong supporting evidence that ordering/timing assumptions are easy to get wrong in this codebase's test harnesses specifically (worth flagging for future debug sessions touching multi-client socket tests)."

- timestamp: "2026-06-19T06:25:00Z"
  checked: "Re-ran shotGkRange.test.ts after fixing the test-harness race (Promise.all on both clients' broadcasts before each next emit)."
  found: "All 6 tests pass deterministically: (a) regular-shot GK-range auto-GOAL gate, (b) regular-shot GK-in-range -> GK_DIVE -> forced SAVE -> carrierId===gk.id, (c) snapshot GK-range auto-GOAL gate at end-of-turn, (d) snapshot GK-in-range -> GK_DIVE -> forced SAVE -> carrierId===gk.id, (e) header GK-range auto-GOAL gate, (f) header GK-in-range -> GK_DIVE -> forced SAVE -> carrierId===gk.id. Re-ran the full server suite: 299/305 pass (4 pre-existing unrelated RED stubs unchanged: gameEngine.phase17.test.ts FREE_MOVE transition x2 + FIRST_TIME_PASS delivery x1, gameHandlers.phase17.test.ts SNAP_DEFLECT end-turn PASS-02 x1 — confirmed same failures as the ROUND 2 baseline, none newly introduced). pnpm typecheck clean on server+client. Client suite 95/95 pass. Shared suite 242/242 pass."
  implication: "Server-side logic for the GK-range gate and SAVE->carrierId transfer is now PROVEN correct, deterministically, for all three GK_DIVE entry points (regular/snapshot/header) — not just 'looks correct on paper' as the ROUND 2 preamble flagged. This strongly suggests the user's live-play contradiction is NOT a server logic defect that exists in the current uncommitted code. Two remaining explanations were investigated: (1) a stale running dev-server process not picking up the uncommitted fixes, (2) a client-side rendering/highlight defect not yet found by static reading."

- timestamp: "2026-06-19T06:35:00Z"
  checked: "Whether the currently-running dev server processes on this machine could be serving pre-fix code. `Get-CimInstance Win32_Process -Filter \"name='node.exe'\"` command lines, and process start times via Get-Process."
  found: "Two long-lived node processes are running: `pnpm --filter @counter-attack/server dev` (PID 56828, started 2026-06-14) which spawns `tsx watch src/main.ts` (PID 24900), and `pnpm --filter @counter-attack/client dev` (PID 22692, started 2026-06-14) which spawns vite (PID 26484). Both predate ALL of round-1's fixes (2026-06-16) and round-2's uncommitted header fix (2026-06-19) by multiple days. `tsx watch` is supposed to auto-restart on src file changes, and there is only one copy of gameHandlers.ts/gameEngine.ts in the repo (no duplicate/legacy file found), so this is not confirmed as the actual cause -- but the timing is suspicious enough that it cannot be ruled out without the user confirming their browser tab was hard-refreshed and pointed at a server process that picked up the file changes after they were written today."
  implication: "Flagging as a concrete, actionable item for the user: restart both dev server processes (or at minimum hard-refresh the browser and confirm via a trivial visible change that the client picked up the latest build) before re-testing in the browser, since the current processes have been running since before EITHER round of fixes existed. This does not rule out a genuine remaining client-side bug, but it must be eliminated first since it would fully explain 'code says X, live play shows not-X' without requiring any further code changes."

- timestamp: "2026-06-16T00:25:00Z"
  checked: "gameHandlers.ts GAME_SHOT handler (lines 1352-1540) end-to-end — confirmed it is a single unbranched handler used for BOTH 'PASS'->declare-shot AND 'SNAPSHOT_TARGET'->declare-snapshot-target (phase guard at line 1367 allows both)."
  found: "applyDeclareShot (engine) correctly branches: from SNAPSHOT_TARGET it transitions to phase 'SNAPSHOT_DEFLECT' (gameEngine.ts:2796-2814) intending the defending team's 2-hex repositioning turn to happen BEFORE any deflection/GK-range check (the correct check for that is implemented separately in gameHandlers.ts's SNAPSHOT_DEFLECT end-of-turn block, lines 815-976, using POST-move positions). However, back in the GAME_SHOT handler, steps 6 ('Deflection check', lines 1398-1463) and 7 ('GK range check', lines 1465-1529) run UNCONDITIONALLY right after applyDeclareShot returns — regardless of whether the resulting phase is GK_DIVE (regular shot) or SNAPSHOT_DEFLECT (snapshot). For snapshot, this means the deflection roll and the 'no reachable GK hex -> auto GOAL' check both execute IMMEDIATELY using PRE-deflection-move positions, completely bypassing/duplicating the snapshot's own correct logic in the SNAPSHOT_DEFLECT end-of-turn handler. If this premature check rolls a deflection, the snapshot is resolved to LOOSE_BALL (ball.carrierId: null, line 1454) before the defending team ever gets their repositioning turn — the SNAPSHOT_DEFLECT phase is entered and then immediately short-circuited away, OR (if hasReachableHex is false using stale positions) the match jumps straight to KICK_OFF_SETUP with an auto-GOAL (line 1501-1526), skipping SNAPSHOT_DEFLECT and GK_DIVE entirely."
  implication: "ROOT CAUSE of symptom 3 confirmed: the GAME_SHOT handler's steps 6/7 (designed only for the regular PASS->GK_DIVE flow) incorrectly also execute for the SNAPSHOT_TARGET->SNAPSHOT_DEFLECT flow, resolving the shot's fate (deflection / out-of-range auto-goal) using pre-repositioning positions and bypassing the correct post-repositioning checks in the SNAPSHOT_DEFLECT end-of-turn handler. When this premature deflection check 'succeeds' (rolls a deflect), the ball goes LOOSE_BALL with carrierId:null instead of reaching GK_DIVE/SHOT's SAVE branch which would correctly assign carrierId: gk.id. This reads as 'GK does not receive the ball' to the user, because the GK never even got the chance to dive/save — the shot's outcome was already decided by stale-position logic immediately after target declaration."

## Eliminated

- hypothesis: "snapshotGkPenalty is silently dropped and that under-penalizes/over-penalizes the GK causing wrong SAVE/GOAL/LOOSE_BALL outcome which masks as 'GK not getting ball'"
  evidence: "snapshotGkPenalty is read nowhere in the SHOT case (confirmed via grep across gameEngine.ts) — its absence doesn't change which branch (GOAL/LOOSE_BALL/SAVE) is taken differently than a regular shot would; it's simply unused dead state, not a logic-altering bug for ball possession."
  timestamp: "2026-06-16T00:12:00Z"

- hypothesis: "SAVE branch of SHOT case fails to assign carrierId to GK for snapshot shots specifically (different code path)"
  evidence: "SHOT case is phase-agnostic — it does not branch on lastActionType/SNAPSHOT vs regular. Both handling.caught===true and ===false branches set ball: { position: gkEffectivePos, carrierId: gk.id } identically regardless of how the GK_DIVE phase was entered (gameEngine.ts:1573-1591, 1597-1609). So the SAVE branch itself is not where symptom 3 originates."
  timestamp: "2026-06-16T00:16:00Z"

## Resolution

root_cause: "Three independent root causes, one per symptom. (1) Path color: HexGrid.tsx classified SNAPSHOT_DEFLECT's shot path (snapDeflectPathSet) as highlightType 'risk' (orange) via the isRisk union, while regular/headed shot paths (lastShotPathSet, used in GK_DIVE) were classified 'shot-path' (white) — a hardcoded inconsistency, not a computed difference. (2) GK dive UX: the SNAPSHOT_DEFLECT defending-piece movement handler (gameHandlers.ts GAME_MOVE block) required strict adjacency (hexDistance===1 per click), forcing hex-by-hex movement, whereas regular/headed shots' GK_DIVE phase (applyGKDive) lets the GK click any reachable path hex directly in one move. (3) GK ball possession on save: the GAME_SHOT handler ran its regular-shot deflection check (step 6) and GK-range check (step 7) unconditionally after applyDeclareShot — even when the resulting phase was SNAPSHOT_DEFLECT (snapshot) rather than GK_DIVE (regular shot). This resolved the snapshot's fate (deflection to LOOSE_BALL, or auto-GOAL) using PRE-repositioning positions immediately upon target declaration, completely bypassing the defending team's 2-hex deflection-move turn and the SNAPSHOT_DEFLECT end-of-turn handler's own (correct) post-repositioning checks. When this premature deflection fired, the ball went to LOOSE_BALL (carrierId: null) instead of ever reaching GK_DIVE/SHOT's SAVE branch — the only place carrierId: gk.id is correctly assigned. Confirmed deterministically via a regression test with a defender guaranteed (tackling:10) to deflect: without the fix, GAME_SHOT alone (before any repositioning) produced phase 'LOOSE_BALL'; with the fix, it correctly stayed in 'SNAPSHOT_DEFLECT' awaiting the defending team's turn."
fix: "(1) packages/client/src/components/HexGrid.tsx: removed isShotPath from the isRisk (orange) union; added it to isShotPathTint (white) instead, so the snapshot's shot path renders identically to a regular/headed shot path. (2) packages/server/src/gameHandlers.ts SNAPSHOT_DEFLECT GAME_MOVE block: replaced the strict adjacency check (hexDistance===1) with a remaining-budget check (1 <= hexDistance <= paceRemaining), consuming the full clicked distance from the 2-hex budget in one move; mirrored the equivalent fix in packages/client/src/store/useGameStore.ts selectPiece (SNAPSHOT_DEFLECT branch) using hexesInRange(piece.position, paceRemaining) instead of hexesInRange(piece.position, 1), so the client's highlighted valid-move set matches what the server now accepts. (3) packages/server/src/gameHandlers.ts GAME_SHOT handler: added an early return immediately after applyDeclareShot — if declaredState.phase === 'SNAPSHOT_DEFLECT', broadcast that state and return, skipping the regular-shot-only deflection check (step 6) and GK-range check (step 7). The snapshot's own correct deflection/GK-range logic in the SNAPSHOT_DEFLECT end-of-turn handler (triggered by GAME_END_TURN) already runs these checks correctly using post-repositioning positions — it just needed steps 6/7 in GAME_SHOT to stop running redundantly/prematurely."
verification: "Self-verified: (a) full server test suite (290 passed, same 4 pre-existing unrelated failures confirmed present on baseline via git stash comparison — FREE_MOVE transition and FIRST_TIME_PASS delivery, untouched by this fix); (b) full client test suite (95 passed) and shared test suite (242 passed); (c) tsc --noEmit clean on both server and client packages; (d) added 4 new regression tests in gameHandlers.phase10.test.ts covering all 3 symptoms, including a deterministic red/green proof for the premature-deflection bug (defender with tackling:10 on the shot path guarantees a deflect roll — confirmed test fails with phase 'LOOSE_BALL' when the GAME_SHOT fix is disabled, passes with 'SNAPSHOT_DEFLECT' when enabled) and a single-click multi-hex SNAPSHOT_DEFLECT movement test. Awaiting human verification of visual path color and dive UX feel in the actual browser UI, which cannot be confirmed from source/test analysis alone."
files_changed:

- "packages/client/src/components/HexGrid.tsx"
- "packages/client/src/store/useGameStore.ts"
- "packages/server/src/gameHandlers.ts"
- "packages/server/src/**tests**/gameHandlers.phase10.test.ts"

## ROUND 2 Resolution (2026-06-19)

root_cause: "No additional server-side root cause was found beyond what round 1 already fixed and what the limit-hit agent had already (correctly, but untested) added. The GAME_HEADER_TARGET handler's GK-range auto-GOAL fallback (left uncommitted by the limit-hit agent, mirroring GAME_SHOT step 7 and the SNAPSHOT_DEFLECT end-of-turn gate) is correct and now has deterministic test coverage proving it works. Deterministic, dice-mocked, end-to-end socket tests (packages/server/src/**tests**/shotGkRange.test.ts) prove BOTH the GK-range auto-GOAL gate AND the SAVE -> carrierId=gk.id transfer are correct for all three GK_DIVE entry points (regular shot via GAME_SHOT, snapshot via SNAPSHOT_TARGET->SNAPSHOT_DEFLECT, header via GAME_HEADER_TARGET) — closing the gap left by round 1's only SAVE test, which was conditional on a random dice roll happening to land on SAVE and therefore never actually proved the carrierId transfer deterministically. The leading remaining explanation for the user's live-play report contradicting this is environmental: both the server dev process (tsx watch, PID 24900) and client dev process (vite, PID 26484) were started on 2026-06-14 — before EITHER round of fixes (2026-06-16 and 2026-06-19) — and there is no confirmation they were restarted before the user's live re-test. This must be ruled out before further server-side investigation is warranted."
fix: "(1) Confirmed (no changes needed) the limit-hit agent's uncommitted GAME_HEADER_TARGET GK-range auto-GOAL fallback in packages/server/src/gameHandlers.ts — added test coverage, no logic changes required. (2) Added packages/server/src/**tests**/shotGkRange.test.ts: 6 deterministic end-to-end socket tests (regular/snapshot/header x {out-of-range auto-GOAL, in-range forced SAVE with carrierId assertion}) using vi.mock('../diceUtils.js') to force dice=3 and explicit piece-stat overrides (gk.saving=10, gk.handling=10, shooter.shooting=1) to guarantee a SAVE+clean-catch outcome deterministically rather than conditionally. (3) Removed packages/server/src/**tests**/headerGkRange.scratch.test.ts (prior agent's scratch reproduction file, explicitly marked DELETE-after-fix in its own header comment) — its 3 cases are now superseded/covered by shotGkRange.test.ts plus the existing SNAPSHOT_DEFLECT-out-of-range coverage."
verification: "Self-verified: (a) pnpm typecheck clean on server + client; (b) full server suite 299/305 pass (4 pre-existing unrelated RED stubs unchanged from the ROUND 2 baseline — gameEngine.phase17.test.ts FREE_MOVE transition x2 + FIRST_TIME_PASS delivery x1, gameHandlers.phase17.test.ts SNAP_DEFLECT end-turn PASS-02 x1 — confirmed NOT caused by this work); (c) full client suite 95/95 pass; (d) full shared suite 242/242 pass; (e) shotGkRange.test.ts 6/6 pass deterministically (re-ran multiple times — no flake) after fixing a test-harness-only race condition (see Evidence 2026-06-19T06:10:00Z) that initially produced 2 false failures. NOT verified: the actual browser UI symptoms reported by the user (out-of-range path appearing selectable; ball not visually transferring to GK on save) — these require a human re-test in the real game UI AFTER confirming both dev server processes have been restarted since the fixes were written (see Current Focus). Server-side logic cannot be proven 'wrong' any further without either a fresh live reproduction or a description of exactly what the user observes on screen after a confirmed clean restart."
files_changed_round2:

- "packages/server/src/**tests**/shotGkRange.test.ts (new)"
- "packages/server/src/**tests**/headerGkRange.scratch.test.ts (deleted — superseded scratch file)"
- "packages/server/src/gameHandlers.ts (uncommitted GAME_HEADER_TARGET fix from prior agent — confirmed correct, no further changes)"

## FINAL Resolution — human-verified (2026-06-19)

root_cause: "All server-side logic (GK-range auto-GOAL gate for regular/snapshot/header shots, SAVE -> carrierId=gk.id transfer, snapshot shot-path color classification, single-click GK_DIVE-style movement for SNAPSHOT_DEFLECT) was already correct and deterministically proven by test as of the ROUND 2 resolution above. The reason the user's live browser testing continued to reproduce both symptoms after round 2's fix was committed-in-working-tree was environmental, not a code defect: both the server dev process (tsx watch, started 2026-06-14) and the client dev process (vite, started 2026-06-14) predated every fix written in this debug arc (round 1 on 2026-06-16, round 2 on 2026-06-19) and had never been restarted, so the browser was exercising stale pre-fix code the entire time."
fix: "No further code changes were required. The user restarted both dev server processes (`pnpm --filter @counter-attack/server dev` and `pnpm --filter @counter-attack/client dev`) and hard-refreshed the browser, then re-tested."
verification: "Human-verified in the browser on 2026-06-19, post-restart: (1) an out-of-range GK dive now auto-skips to GOAL for both snapshot and header shots; (2) a legitimate in-range save now correctly transfers ball possession to the GK. Both previously-failing symptoms are resolved. This is the terminal verification for the snapshot-shot-flow-mismatch session — no further investigation or fixes are pending."
human_verification: "Confirmed by user on 2026-06-19: out-of-range GK dive auto-skips to GOAL (snapshot + header); in-range save correctly transfers ball to GK."
