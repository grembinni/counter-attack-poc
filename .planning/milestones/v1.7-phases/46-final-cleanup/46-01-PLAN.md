---
phase: 46-final-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/BallLocationRing.tsx
  - packages/client/src/components/BallLocationRing.test.tsx
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/ActionPanel.test.tsx
  - docs/HIGHLIGHT-REFERENCE.md
autonomous: true
requirements: [CLEANUP-06, CLEANUP-08, CLEANUP-13]
must_haves:
  truths:
    - "During FREE_KICK_SETUP the ball's hex shows the same white ball-location ring that every other restart-setup phase already shows"
    - "During FREE_MOVE_ATTACK / FREE_MOVE_DEFENSE the ball's hex shows the white ball-location ring (the ball is provably stationary in those phases)"
    - "The Free Move action panel heading names the trigger (final third) instead of the generic 'Free Move!'"
    - "docs/HIGHLIGHT-REFERENCE.md's stated BALL_MARKER_PHASES list matches the real Set exactly"
  artifacts:
    - path: "packages/client/src/components/BallLocationRing.tsx"
      provides: "BALL_MARKER_PHASES with 35 members including FREE_KICK_SETUP, FREE_MOVE_ATTACK, FREE_MOVE_DEFENSE"
      contains: "FREE_KICK_SETUP"
    - path: "packages/client/src/components/ActionPanel.tsx"
      provides: "Final-Third Movement! heading for the FREE_MOVE_ATTACK/FREE_MOVE_DEFENSE panel"
      contains: "Final-Third Movement!"
    - path: "docs/HIGHLIGHT-REFERENCE.md"
      provides: "Corrected BALL_MARKER_PHASES prose list and exclusion list"
  key_links:
    - from: "packages/client/src/components/BallLocationRing.test.tsx"
      to: "BALL_MARKER_PHASES.size"
      via: "literal count assertion pinning doc-vs-code drift"
      pattern: "BALL_MARKER_PHASES\\.size"
    - from: "docs/HIGHLIGHT-REFERENCE.md"
      to: "packages/client/src/components/BallLocationRing.tsx"
      via: "phase list prose mirrors the Set literal"
      pattern: "BALL_MARKER_PHASES"
---

<objective>
Close the two mechanically-confirmed dead-ball consistency divergences that live in a single constant and a single string literal: the ball-location ring's phase gate (CLEANUP-06) and the Free Move panel's trigger-less heading (CLEANUP-08). Bundle each fix's stale-documentation correction (CLEANUP-13).

Purpose: These are the two highest-confidence, lowest-blast-radius findings in 46-RESEARCH.md — each is a one-constant or one-literal change with an existing sibling pattern in the same file and an existing test file to extend. This plan also establishes the corrected `docs/HIGHLIGHT-REFERENCE.md` that plan 46-05's movement-tint audit builds on, which is why 46-05 depends on it.
Output: The ball-location ring renders during every dead-ball repositioning phase; the Free Move heading names its trigger; the highlight reference doc's stated phase list matches the real Set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-final-cleanup/46-CONTEXT.md
@.planning/phases/46-final-cleanup/46-RESEARCH.md
@.planning/phases/46-final-cleanup/46-PATTERNS.md
@.planning/phases/46-final-cleanup/46-UI-SPEC.md
</context>

## Artifacts this phase produces

Plan 46-01 adds no new files. It adds or changes the following symbols and values in existing files:

| Artifact | File | Kind |
|---|---|---|
| `'FREE_KICK_SETUP'` member of `BALL_MARKER_PHASES` | `packages/client/src/components/BallLocationRing.tsx` | new Set member |
| `'FREE_MOVE_ATTACK'` member of `BALL_MARKER_PHASES` | `packages/client/src/components/BallLocationRing.tsx` | new Set member |
| `'FREE_MOVE_DEFENSE'` member of `BALL_MARKER_PHASES` | `packages/client/src/components/BallLocationRing.tsx` | new Set member |
| `"Final-Third Movement!"` `helperLine1` string for `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` | `packages/client/src/components/ActionPanel.tsx` | changed literal |
| "Phase 46 CLEANUP-06 addition" doc note | `docs/HIGHLIGHT-REFERENCE.md` | new doc section |

No new exports, no new files, no new CSS classes, no new colors.

<tasks>

<task type="auto">
  <name>Task 1: Add the three dead-ball repositioning phases to BALL_MARKER_PHASES and resync the highlight doc</name>
  <files>packages/client/src/components/BallLocationRing.tsx, packages/client/src/components/BallLocationRing.test.tsx, docs/HIGHLIGHT-REFERENCE.md</files>
  <read_first>
    - `packages/client/src/components/BallLocationRing.tsx` — the whole file; the `BALL_MARKER_PHASES` Set literal and the module doc comment above it explaining that the Set is exported precisely so the doc can be checked against the real Set rather than drifting (it already drifted once, after Plan 37-02)
    - `packages/client/src/components/BallLocationRing.test.tsx` — the `BALL_MARKER_PHASES.size` literal-count assertion near line 78 (currently `32`) and the surrounding per-phase render assertions
    - `docs/HIGHLIGHT-REFERENCE.md` lines ~130–165 — the ball-location-marker table row, the "17-phase list" prose paragraph (line ~138), the "It does not render during…" exclusion paragraph (line ~149), and the "`KICK_OFF_SETUP` addition (Plan 33-07 Task 3 human-verify feedback)" note that is the exact voice and shape to mirror
    - `docs/HIGHLIGHT-REFERENCE.md` — the "Adding a New Highlight" checklist section, whose step 3 names `BALL_MARKER_PHASES` explicitly
    - `packages/server/src/gameEngine.ts` — `applyFreeMove` (starts ~line 655) and its `moveEvent` construction, which carries the inline comment `Ball unchanged during FREE_MOVE` and builds `ballAfter` from `state.ball.position` unchanged; this is the evidence backing the FREE_MOVE decision below
  </read_first>
  <action>
Add exactly three new members to the `BALL_MARKER_PHASES` Set in `BallLocationRing.tsx`: `'FREE_KICK_SETUP'`, `'FREE_MOVE_ATTACK'`, `'FREE_MOVE_DEFENSE'`. Append them after the existing `'TACKLE_STEAL_PROMPT'` entry, under a single new block comment following the exact voice already used by the Phase 37 / Phase 38 / Phase 39 / TACKLE-02 entry comments in the same Set: cite the phase and requirement (`Phase 46, CLEANUP-06`) and state *why* the ball is fixed during those phases. Do not add a bare literal with no comment; the file's own convention forbids it.

The premise to confirm before writing that comment: the ball is stationary throughout all three phases. `FREE_KICK_SETUP` holds the ball at `freeKickHex` for every stage. `applyFreeMove` in `packages/server/src/gameEngine.ts` never writes `state.ball` — its own emitted `MOVE` event comment reads `Ball unchanged during FREE_MOVE`. This resolves 46-RESEARCH.md Open Question 1 / Assumption A2 in favour of inclusion, because CLEANUP-06's requirement text is "when the ball is dead and players are being selected/moved" and all three phases are exactly that. If reading `applyFreeMove` shows the ball position IS mutated during free move, STOP and report the contradiction rather than deviating silently — in that case only `FREE_KICK_SETUP` gets added.

Do not introduce a new stroke color, a new marker component, or a new tint type. The existing `BALL_MARKER_STROKE` (`#ffffff`, `strokeWidth: 2.5`) renders unchanged for the new phases, per 46-UI-SPEC.md's binding rule for CLEANUP-06.

In `BallLocationRing.test.tsx`: update the literal size assertion from `32` to `35`, and add render assertions for the three new phases mirroring the existing per-phase assertions in that file (render with `phase` set to each new value, assert the ring element is present at the ball hex).

In `docs/HIGHLIGHT-REFERENCE.md`: (a) change the heading text `**\`BallLocationRing.tsx\` visibility gate — 17-phase list**` to state the true post-change count of 35; (b) replace the inline prose list of phase names with the complete current membership of `BALL_MARKER_PHASES`, transcribed from the Set literal in declaration order — do not hand-author it from memory; (c) remove `FREE_KICK_SETUP` and `FREE_MOVE_*` from the "It does not render during…" exclusion paragraph, leaving `MOVE` / `PASS` / `KICK_OFF` / `LOOSE_BALL` / `HIGH_PASS_MOVE` / `FIRST_TIME_PASS_MOVE` / `HALF_TIME` / `FULL_TIME` / `REPLAY`; (d) add a new note directly after the existing `KICK_OFF_SETUP addition` note, titled for the Phase 46 CLEANUP-06 addition and written in that note's voice, recording that free kick was the only restart-setup family missing the marker and that free move qualifies because the engine provably leaves `ball.position` untouched.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- BallLocationRing</automated>
  </verify>
  <acceptance_criteria>
    - `BALL_MARKER_PHASES` contains the exact strings `'FREE_KICK_SETUP'`, `'FREE_MOVE_ATTACK'`, `'FREE_MOVE_DEFENSE'`.
    - `pnpm --filter @counter-attack/client test -- BallLocationRing` exits 0 with the size assertion reading `toBe(35)`.
    - `docs/HIGHLIGHT-REFERENCE.md` no longer contains the string `17-phase list`.
    - Reading the "It does not render during…" paragraph shows no occurrence of `FREE_KICK_SETUP` or `FREE_MOVE`.
    - The prose phase list in `docs/HIGHLIGHT-REFERENCE.md` contains all 35 phase names present in the Set literal (spot-check at minimum `TACKLE_STEAL_PROMPT`, `PENALTY_KICK`, `CORNER_KICK_REPOSITION`, plus the 3 new ones).
    - Each of the 3 new Set members is covered by a comment naming `Phase 46` and `CLEANUP-06`.
    - No new color literal, no new `strokeWidth` value, and no new exported symbol were introduced in `BallLocationRing.tsx`.
  </acceptance_criteria>
  <done>The white ball-location ring renders during FREE_KICK_SETUP, FREE_MOVE_ATTACK, and FREE_MOVE_DEFENSE; the test passes with a 35-member assertion; the highlight reference doc's stated list matches the real Set with no stale count and no stale exclusion entry.</done>
</task>

<task type="auto">
  <name>Task 2: Rename the Free Move panel heading to name its final-third trigger</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/ActionPanel.test.tsx</files>
  <read_first>
    - `packages/client/src/components/ActionPanel.tsx` lines ~700–745 — the `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` branch, including the block comment above it stating the actual game rule ("triggered when the ball enters a final third") and the in-branch comment at ~719–727 recording the prior checkpoint 45-05-04 rename of this exact string
    - `packages/client/src/components/ActionPanel.tsx` — the sibling `helperLine1` headings this file already uses (`High Pass Aerial Challenge!` ~line 372, `First-Time Pass!` ~412, `Attempt Save!` ~450, `Snapshot — Deflection Attempt!` ~475, `Ball in Air!` ~682) to match the short-noun-phrase-plus-exclamation convention
    - `packages/client/src/components/ActionPanel.test.tsx` — any assertion referencing the string `Free Move!` or the FREE_MOVE branch
    - `.planning/phases/46-final-cleanup/46-UI-SPEC.md` — the Copywriting Contract row locking the replacement string
  </read_first>
  <action>
Change the `helperLine1` text rendered by the `FREE_MOVE_ATTACK` / `FREE_MOVE_DEFENSE` branch of `ActionPanel.tsx` from `Free Move!` to exactly `Final-Third Movement!`. This exact string is locked by 46-UI-SPEC.md's Copywriting Contract — do not paraphrase it.

Leave `helperLine2` byte-for-byte unchanged: it already reads `{remaining} players still eligible to move — up to 6 hexes each, regardless of remaining pace.` and already names the mechanic. Only the heading was the confirmed gap (CLEANUP-08).

Extend the existing in-branch comment (the one documenting the checkpoint 45-05-04 rename) with a short Phase 46 / CLEANUP-08 line recording why this second rename happened: the heading previously gave the player no indication of what triggered the phase, unlike every restart panel whose heading names the restart by definition. Do not delete the existing 45-05-04 comment — append to it.

No layout change, no CSS change, no new class name.

In `ActionPanel.test.tsx`: update any assertion matching `Free Move!` to `Final-Third Movement!`. If no such assertion exists, add one — render the panel in `FREE_MOVE_ATTACK` for the acting player and assert the heading text is present.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- ActionPanel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Final-Third Movement!" packages/client/src/components/ActionPanel.tsx` returns 1.
    - `grep -n "Free Move!" packages/client/src/components/ActionPanel.tsx` returns no match inside a JSX text node (a mention inside a comment is acceptable only where it describes the old value).
    - `packages/client/src/components/ActionPanel.test.tsx` contains an assertion on the literal `Final-Third Movement!`.
    - `pnpm --filter @counter-attack/client test -- ActionPanel` exits 0.
    - The `helperLine2` template string is unchanged (still contains `regardless of remaining pace.`).
    - `ActionPanel.module.css` was not modified.
  </acceptance_criteria>
  <done>The Free Move panel heading reads "Final-Third Movement!", the body copy is untouched, and the ActionPanel test suite passes with an assertion pinning the new string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | This plan changes only a client-side render gate and a display string. No new data crosses any boundary; no server code, no socket event, and no user-supplied input is touched. |

Confirmed against 46-RESEARCH.md's Security Domain section: this phase touches no authentication, session management, access control, cryptography, or externally-facing input-validation surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01-I | Information Disclosure | `BALL_MARKER_PHASES` render gate | accept | The ball's hex is already rendered unconditionally by `BallMarker.tsx` for both players in all phases, so adding a ring on the same hex discloses nothing not already visible. This is unlike valid-move tints, which are gated on `isActivePlayer` elsewhere and are untouched here. |
| T-46-01-T | Tampering | `ActionPanel` display copy | accept | Purely presentational read of server-broadcast `GameState`. No client-authored value is emitted and no validation path is altered. |
| T-46-01-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages. 46-RESEARCH.md's Package Legitimacy Audit records that this phase adds zero dependencies; if any task appears to require an install, stop and escalate rather than installing. |
</threat_model>

<verification>
Run in order:

1. `pnpm --filter @counter-attack/client test -- BallLocationRing`
2. `pnpm --filter @counter-attack/client test -- ActionPanel`
3. `pnpm -w typecheck`
4. `pnpm knip`

All four must exit 0. `pnpm knip` must report zero findings — this plan adds no new exports, so any new knip finding indicates an accidental export.
</verification>

<success_criteria>
- `BALL_MARKER_PHASES.size === 35`, including `FREE_KICK_SETUP`, `FREE_MOVE_ATTACK`, `FREE_MOVE_DEFENSE`, each covered by a Phase 46 / CLEANUP-06 comment.
- `docs/HIGHLIGHT-REFERENCE.md` states a 35-phase list, transcribes it fully, no longer excludes free kick or free move, and carries a Phase 46 addition note in the existing note's voice.
- `ActionPanel.tsx` renders `Final-Third Movement!` for `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE`; `helperLine2` unchanged.
- Both client test files pass; `pnpm -w typecheck` and `pnpm knip` both clean.
</success_criteria>

<output>
Create `.planning/phases/46-final-cleanup/46-01-SUMMARY.md` when done. Record the final `BALL_MARKER_PHASES` member count so plan 46-05's doc work can cite it.
</output>
