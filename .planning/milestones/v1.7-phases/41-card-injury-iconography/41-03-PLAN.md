---
phase: 41-card-injury-iconography
plan: 03
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - packages/client/src/components/PieceOverlay.tsx
  - packages/client/src/components/PlayerStatsPanel.tsx
  - packages/client/src/components/PlayerStatsPanel.module.css
  - packages/client/src/components/PlayerStatsPanel.test.tsx
autonomous: true
requirements: [ICON-01, ICON-02]

must_haves:
  truths:
    - 'The pitch token renders its card/injury badge through the shared component with no visual change (D-03)'
    - 'The scoreboard player-stats card shows the same shape+colour glyph as the pitch token instead of YELLOW/RED/INJ text (D-01)'
    - 'The scoreboard glyph sits immediately after the jersey number in the flag-role-number row (D-02)'
    - 'A booked-and-injured player shows both glyphs side by side on the scoreboard card (D-04)'
    - 'No copy of the card-colour ternary remains in either component (ICON-01)'
  artifacts:
    - path: 'packages/client/src/components/PieceOverlay.tsx'
      provides: 'Pitch token badge rendered via CardInjuryBadgeGroup'
      contains: 'CardInjuryBadgeGroup'
    - path: 'packages/client/src/components/PlayerStatsPanel.tsx'
      provides: 'Scoreboard card badge rendered via the standalone CardInjuryBadge'
      contains: 'CardInjuryBadge'
    - path: 'packages/client/src/components/PlayerStatsPanel.module.css'
      provides: 'Dead cardChip/injuryChip rules removed'
  key_links:
    - from: 'packages/client/src/components/PieceOverlay.tsx'
      to: 'packages/client/src/components/CardInjuryBadge.tsx'
      via: 'import of CardInjuryBadgeGroup + cardColorFor'
      pattern: "from './CardInjuryBadge.js'"
    - from: 'packages/client/src/components/PlayerStatsPanel.tsx'
      to: 'packages/client/src/components/CardInjuryBadge.tsx'
      via: 'import of CardInjuryBadge + cardColorFor'
      pattern: "from './CardInjuryBadge.js'"
---

<objective>
Migrate the first two of the four surfaces onto the shared badge: the on-pitch SVG token (which was
the glyph source of truth and must come out visually identical) and the scoreboard player-stats card
(which loses its `YELLOW`/`RED`/`INJ`/`INJ x2` text chips in favour of the glyph, per D-01).

Purpose: ICON-01 requires the three independent treatments to collapse into one component; this plan
retires two of the three.
Output: two migrated components, dead CSS removed, and the scoreboard test assertions rewritten from
text to glyph.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-card-injury-iconography/41-CONTEXT.md
@.planning/phases/41-card-injury-iconography/41-UI-SPEC.md
@.planning/phases/41-card-injury-iconography/41-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by Phase 41 (do not treat as drift):

| Symbol                                                           | Kind                      | Location                                       |
| ---------------------------------------------------------------- | ------------------------- | ---------------------------------------------- |
| `CardInjuryBadge.tsx` / `CardInjuryBadge.test.tsx`               | new files                 | `packages/client/src/components/` (plan 41-01) |
| `CardColor`, `BenchCardStatus`                                   | exported type aliases     | `CardInjuryBadge.tsx` (plan 41-01)             |
| `cardColorFor`, `cardColorForBenchEntry`, `cardInjuryLabel`      | exported functions        | `CardInjuryBadge.tsx` (plan 41-01)             |
| `CardInjuryBadgeGroup`, `CardInjuryBadge`                        | exported React components | `CardInjuryBadge.tsx` (plan 41-01)             |
| `data-testid="card-injury-badge"`                                | new test hook             | standalone `<svg>` wrapper (plan 41-01)        |
| `BenchEntry.yellowCards`, `BenchEntry.injuryCount`               | new optional fields       | `packages/shared/src/types.ts` (plan 41-02)    |
| `DraftCardBodyProps.cardColor`, `DraftCardBodyProps.injuryCount` | new optional props        | `DraftPackCarousel.tsx` (plan 41-05)           |
| `BenchCarouselProps.benchCardStatus`                             | new optional prop         | `BenchCarousel.tsx` (plan 41-05)               |

<tasks>

<task type="auto">
  <name>Task 1: Point the pitch token at the shared badge (no visual change)</name>
  <files>packages/client/src/components/PieceOverlay.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx (lines 200-295 — the ball-dot geometry that defines `dotOffsetX`/`dotOffsetY`, and the badge IIFE at 233-292 being replaced; the comment block at 227-232 explains the negated-offset corner anchor that must survive verbatim)
    - packages/client/src/components/CardInjuryBadge.tsx (the shared module from plan 41-01 — exact prop names of `CardInjuryBadgeGroup` and `cardColorFor`)
    - packages/client/src/components/PieceOverlay.test.tsx (lines 271-347 — the badge assertions that must stay green WITHOUT edits; they are this task's behaviour-preservation proof)
  </read_first>
  <action>
    Replace the inline badge IIFE in `PieceOverlay.tsx` (lines 233-292) with a single
    `<CardInjuryBadgeGroup>` element, keeping the surrounding comment block that documents the
    negated-ball-dot corner anchor (D-03: this surface's position rule is explicitly exempt from the
    name-row rule and is unchanged by this phase).

    Add `import { CardInjuryBadgeGroup, cardColorFor } from './CardInjuryBadge.js';` alongside the
    existing relative imports (`.js` extension, `verbatimModuleSyntax` applies to type-only imports).

    Pass exactly: `cx={cx - dotOffsetX}`, `cy={cy - dotOffsetY}`, `r={PIECE_RADIUS * 0.59}`,
    `cardColor={cardColorFor(piece)}`, `injuryCount={piece.injuryCount ?? 0}`. Delete the now-dead
    local `badgeCx`/`badgeCy`/`badgeR`/`cardColor`/`hasInjury`/`cardWidth`/`cardHeight`/`barLength`/
    `barThickness` locals and the local `redCarded === true ?` ternary — the shared module owns them
    now (ICON-01).

    Do not touch the ball-carrier dot, the offside ring, the number label, `PIECE_RADIUS`, or the
    element ordering around the badge. The rendered SVG output must be byte-equivalent apart from the
    stroke colour now resolving through `var(--color-card-badge-border)` (same computed
    `rgba(0, 0, 0, 0.5)`) and the new `role`/`aria-label` attributes.

    Do NOT edit `PieceOverlay.test.tsx`. An unchanged, still-green test file is the acceptance signal
    that the extraction preserved behaviour.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- PieceOverlay</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- PieceOverlay` exits 0 with 0 failures.
    - `git diff --exit-code packages/client/src/components/PieceOverlay.test.tsx` exits 0 (the test file was not modified).
    - `grep -c "CardInjuryBadgeGroup" packages/client/src/components/PieceOverlay.tsx` is at least 2 (import + usage).
    - `grep -c "redCarded === true ?" packages/client/src/components/PieceOverlay.tsx` is 0.
    - `grep -cE "badgeCx|badgeCy|barThickness|cardHeight" packages/client/src/components/PieceOverlay.tsx` is 0.
    - `pnpm --filter @counter-attack/client typecheck` exits 0.
  </acceptance_criteria>
  <done>The pitch token draws its badge from the shared component with its original geometry, corner anchor, and test file untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the scoreboard card's text chips with the shared glyph</name>
  <files>packages/client/src/components/PlayerStatsPanel.tsx, packages/client/src/components/PlayerStatsPanel.module.css, packages/client/src/components/PlayerStatsPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/PlayerStatsPanel.tsx (lines 129-175 — the `playerMeta` row: `NationFlag size={20}` -> `roleChip` -> `jerseyNum` -> the two chip IIFEs at 146-170 being replaced)
    - packages/client/src/components/PlayerStatsPanel.module.css (lines 55-110 — `.playerMeta` already supplies `gap: 12px`; `.cardChip`/`.injuryChip` rules at 80-109 become dead)
    - packages/client/src/components/PlayerStatsPanel.test.tsx (lines 120-180 — the `overridePiece` helper and the six chip assertions being rewritten)
    - packages/client/src/components/CardInjuryBadge.tsx (the shared module from plan 41-01)
  </read_first>
  <action>
    Swap the scoreboard card's text chips for the shared glyph, in one commit with the test rewrite
    (the `.textContent` assertions test a design D-01 deliberately removes, so they cannot be left
    passing unchanged).

    In `PlayerStatsPanel.tsx`:
    - import `{ CardInjuryBadge, cardColorFor }` from `./CardInjuryBadge.js`;
    - delete both chip IIFEs (the `stats-card-chip` span and the `stats-injury-chip` span, lines
      146-170) including the local `redCarded === true ?` ternary, and render in their place, at the
      same anchor point immediately after `<span className={styles.jerseyNum}>` (D-02):
      `<CardInjuryBadge cardColor={cardColorFor(piece)} injuryCount={piece.injuryCount ?? 0} size={20} />`;
    - `size={20}` because this row is already built around 20px icons (`NationFlag size={20}`,
      `MiniTokenBadge` 20x20) per the UI-SPEC Spacing Scale note;
    - no wrapper span and no new class — `.playerMeta`'s existing `gap: 12px` provides the spacing
      the old `margin-left: 8px` used to add on top of it.

    In `PlayerStatsPanel.module.css`: delete the `.cardChip, .injuryChip` rule block and the
    `.cardChip[data-card='yellow']`, `.cardChip[data-card='red']`, `.injuryChip` rules (lines 80-109),
    plus their now-stale "Plan 39-06 (D-04)" comment. Leave `.playerMeta`, `.roleChip`, `.jerseyNum`,
    `.tokenBadge` and everything below `.statGrid` untouched.

    In `PlayerStatsPanel.test.tsx`, rewrite the `describe` block's six assertions against the glyph
    contract (keep the existing `overridePiece` helper and piece ids):
    - clean player: `container.querySelector('[data-testid="card-injury-badge"]')` is `null`;
    - `yellowCards: 1`: `container.querySelector('[data-testid="piece-card-badge"]')` has
      `data-card="yellow"`; `redCarded: true`: `data-card="red"`;
    - `injuryCount: 1`: exactly one `[data-testid="piece-injury-badge"]` element and the wrapper's
      `aria-label` is `'Injured'`;
    - `injuryCount: 2`: still exactly one injury glyph, wrapper `aria-label` is `'Injured x2'`
      (U+00D7) — the binary-glyph rule, with the count preserved only in the label;
    - booked-and-injured: both glyphs present and horizontally non-overlapping (card rect
      `x + width` &lt;= injury cross's first rect `x`), matching D-04.
    Rename the test titles so they describe glyphs rather than chips, and reference `ICON-02` in at
    least one title.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- PlayerStatsPanel</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- PlayerStatsPanel` exits 0 with 0 failures.
    - `grep -cE "stats-card-chip|stats-injury-chip" packages/client/src/components/PlayerStatsPanel.tsx` is 0.
    - `grep -cE "stats-card-chip|stats-injury-chip" packages/client/src/components/PlayerStatsPanel.test.tsx` is 0.
    - `grep -cE "'INJ'|INJ ×2|INJ x2" packages/client/src/components/PlayerStatsPanel.test.tsx` is 0.
    - `grep -cE "^\.cardChip|^\.injuryChip|\.cardChip\[" packages/client/src/components/PlayerStatsPanel.module.css` is 0.
    - `grep -c "redCarded === true ?" packages/client/src/components/PlayerStatsPanel.tsx` is 0.
    - `grep -c "CardInjuryBadge" packages/client/src/components/PlayerStatsPanel.tsx` is at least 2 (import + usage) and the usage passes `size={20}`.
    - `pnpm --filter @counter-attack/client typecheck` and `pnpm stylelint` both exit 0.
  </acceptance_criteria>
  <done>The scoreboard card shows the shared glyph after the jersey number, its dead chip CSS is gone, and its tests assert glyphs instead of text.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                                                             |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| server -> client (`GameState` broadcast) | Pre-existing. Both components already read `pieces[].redCarded/yellowCards/injuryCount` from the broadcast snapshot; this plan changes only how those values are drawn. |

## STRIDE Threat Register

| Threat ID | Category                 | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                             |
| --------- | ------------------------ | ------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-41-06   | Information Disclosure   | `PieceOverlay.tsx`, `PlayerStatsPanel.tsx` | accept      | Identical data to today, redrawn. Both components already displayed card/injury status for the selected piece; no field becomes newly visible.                                              |
| T-41-07   | Tampering                | migrated render paths                      | accept      | Presentational swap only: no store write, no socket emit, no `dangerouslySetInnerHTML`. The new `aria-label` is a React-escaped text node built from an enum-ish `CardColor` plus a number. |
| T-41-SC   | Tampering (supply chain) | package installs                           | n/a         | Zero packages installed this plan (`41-RESEARCH.md`: Package Legitimacy Audit not applicable).                                                                                              |

**Phase-level statement (applies to every Phase 41 plan):** this phase introduces NO new attack
surface — no new user-controllable input, no new server endpoint or socket event, no auth/session
change, no new network or trust boundary. This plan is pure client rendering consolidation. Per
`41-RESEARCH.md` Security Domain, ASVS V2/V3/V4/V5/V6 are non-applicable at L1 for this scope. This
is the expected, complete threat model for this phase, not an omission.
</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test -- PieceOverlay` and `-- PlayerStatsPanel` both green.
- `pnpm --filter @counter-attack/client test` (full client suite) green — `HexGrid`/`GameBoard` tests
  render `PieceOverlay` transitively and must not regress.
- `pnpm --filter @counter-attack/client typecheck` and `pnpm stylelint` green.
- `git diff --exit-code packages/client/src/components/PieceOverlay.test.tsx` clean.
</verification>

<success_criteria>

- Pitch token: identical visual output and an untouched, green test file (D-03 position rule intact).
- Scoreboard card: shape+colour glyph immediately after the jersey number (D-01/D-02), both glyphs
  side by side when booked and injured (D-04), no `YELLOW`/`RED`/`INJ` text anywhere.
- Zero card-colour ternary copies remain in either file (ICON-01 progress).
  </success_criteria>

<output>
Create `.planning/phases/41-card-injury-iconography/41-03-SUMMARY.md` when done
</output>
