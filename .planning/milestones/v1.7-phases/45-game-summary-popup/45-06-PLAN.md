---
phase: 45-game-summary-popup
plan: 06
type: execute
wave: 4
depends_on: ["45-02", "45-03", "45-05"]
files_modified:
  - packages/server/src/__tests__/matchStats.integration.test.ts
  - .planning/phases/45-game-summary-popup/45-VALIDATION.md
autonomous: true
requirements:
  [
    STATS-01,
    STATS-02,
    STATS-03,
    STATS-04,
    STATS-05,
    STATS-06,
    STATS-07,
    STATS-08,
    STATS-09,
  ]

must_haves:
  truths:
    - "A real socket-connected match accumulates match statistics end to end, from a client action through the engine to the broadcast GameState both players receive"
    - "Both connected clients receive byte-identical matchStats in the same broadcast"
    - "No statistic resets when the match crosses the half-time boundary"
    - "The Referee Leniency manual/auto distinction survives from room settings all the way to the broadcast GameState"
    - "The phase's validation contract reflects the plans and task IDs that were actually built"
  artifacts:
    - path: "packages/server/src/__tests__/matchStats.integration.test.ts"
      provides: "Full-room socket round-trip coverage of match-stat accumulation, symmetry and half-time persistence"
      min_lines: 200
    - path: ".planning/phases/45-game-summary-popup/45-VALIDATION.md"
      provides: "Per-task verification map matching the delivered plan and task breakdown"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "packages/server/src/__tests__/matchStats.integration.test.ts"
      to: "packages/server/src/createServer.ts buildServer"
      via: "real socket server lifecycle, mirroring refereeLeniency.integration.test.ts"
      pattern: "buildServer"
    - from: "packages/server/src/__tests__/matchStats.integration.test.ts"
      to: "GameState.matchStats"
      via: "assertions on the broadcast snapshot rather than on internal engine state"
      pattern: "matchStats"
---

<objective>
Close the phase with an end-to-end socket integration suite proving the whole chain works together — a client emits an action, the engine resolves it, the reducer folds it, and both connected browsers receive the updated statistics in the same full-state broadcast — and refresh `45-VALIDATION.md` so the phase's validation contract matches what was actually built.

Plans 45-01 through 45-05 each verified their own hop in isolation: the formula, the per-site capture, the reducer's counting rules, the broadcast wiring, and the rendering. Nothing so far has asserted that a player taking a shot in one browser produces an updated xG figure in the other browser's summary. That is the gap this plan closes, at the socket layer, in the same style as `refereeLeniency.integration.test.ts` did for Phase 44.

Purpose: prove the seams hold, and leave the phase's validation documentation accurate rather than stale.
Output: `matchStats.integration.test.ts` and an updated `45-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-game-summary-popup/45-CONTEXT.md
@.planning/phases/45-game-summary-popup/45-VALIDATION.md
@.planning/phases/45-game-summary-popup/45-02-PLAN.md
@.planning/phases/45-game-summary-popup/45-03-PLAN.md
</context>

<tasks>

<task type="auto" id="45-06-01" tdd="true">
  <name>Task 45-06-01: End-to-end socket integration suite for match-stat accumulation</name>
  <files>packages/server/src/__tests__/matchStats.integration.test.ts</files>
  <read_first>
    - `packages/server/src/__tests__/refereeLeniency.integration.test.ts` in full — the server lifecycle block, the `createClient` / `oncePromise` / `waitForConnect` helpers, and the local setup helper that drives create room, confirm settings, join, team pick, uniform confirm and lineup confirm before returning the first broadcast `GameState`. Its header comment documents the per-file self-contained convention: copy these helpers verbatim rather than importing them from a shared module.
    - `packages/server/src/__tests__/testHelpers.ts` lines 1-15 (the comment establishing that convention)
    - `packages/server/src/__tests__/penaltyKick.integration.test.ts` and `packages/server/src/__tests__/gkDiveAtFeet.integration.test.ts` — how an existing suite drives a match forward far enough to reach a duel or a shot over real sockets
    - `packages/server/src/__tests__/gameHandlers.halfTime.test.ts` — how the half-time boundary is driven, needed for the never-reset assertion
  </read_first>
  <behavior>
    - A freshly built match broadcasts a `GameState` whose `matchStats` is present and all-zero across all nine counters.
    - `refereeCard.wasManualOverride` is true in the broadcast state when the room was created with a manual Leniency override, and false or absent when it was left to the random roll — proving the STATS-03 distinction survives the full six-hop chain from room settings to broadcast.
    - Driving a completed pass over real sockets increases `passesCompleted` for the passing team by exactly 1 in the next broadcast, and leaves the other team's count unchanged.
    - Driving a tackle or steal duel increases `tackleStealAttempts` for the defending team by exactly 1, and increases `tackleStealSuccesses` by 1 only when the duel resolved as a success.
    - Driving a shot increases `shots` for the attacking team by exactly 1 and increases `xg` for that team by a value strictly greater than 0 and no greater than 1.
    - Elapsed match time accrues into `possessionActionCount`: after several actions, the two team counters sum to a value greater than 0 and no greater than the broadcast `actionCount`.
    - Both connected clients receive the same broadcast payload: the `matchStats` object each client observes for a given action is deep-equal.
    - Crossing the half-time boundary leaves every one of the nine counters at or above its pre-half-time value — nothing resets to zero (STATS-04 through STATS-09, ROADMAP Success Criterion 4, D-06). Assert per-counter, not just on one representative counter.
    - `subsUsed` and `addedTimeBonus` still behave as before across the same half-time boundary: `subsUsed` persists and `addedTimeBonus` resets, confirming the new counters joined the whole-match family without disturbing the per-half one.
  </behavior>
  <action>
    Create `packages/server/src/__tests__/matchStats.integration.test.ts` modelled structurally on `refereeLeniency.integration.test.ts`. Open it with a header comment in the same style: name the phase and the requirement IDs, state which chain it covers end to end, and state explicitly which hops the earlier plans already verified in isolation so a reader understands why this file exists and what it is NOT re-testing.

    Copy the server lifecycle block and the client helpers verbatim from `refereeLeniency.integration.test.ts` per the documented per-file self-contained convention. Do not extract them into a shared helper module; that convention is deliberate and predates this phase.

    Write a local setup helper that brings a room to a live, playable `GameState` with two connected clients and returns handles to both clients plus the latest broadcast state, parameterised by the Referee Leniency override setting so the same helper serves both the manual and auto assertions.

    Cover every bullet in the behavior block with a named test carrying its requirement ID. For the action-driving tests, prefer the shortest real socket path to each event type; where a fully realistic path would need a long scripted sequence, seed the room's `gameState` directly to the immediately-preceding position and then drive the final action over the socket, matching how `penaltyKick.integration.test.ts` and `gkDiveAtFeet.integration.test.ts` already do it. Assert on the broadcast `GameState` the clients receive, never on internal engine return values — the point of this file is that the numbers reach the browser.

    The half-time persistence test is the single most important case here and must assert all nine counters individually rather than sampling one. ROADMAP Success Criterion 4 and D-06 both hinge on it, and the failure mode (one counter accidentally rebuilt rather than spread across the boundary) would otherwise ship silently.

    If any assertion fails, fix the production code rather than weakening the assertion — a failure here means one of the earlier plans has a real seam defect, which is precisely what this suite exists to surface.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared build && pnpm --filter @counter-attack/server test -- matchStats.integration</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/server test -- matchStats.integration` passes with one named test per behavior bullet, each naming its requirement ID.
    - The half-time test asserts all nine `matchStats` counters individually.
    - A named test asserts both clients observe deep-equal `matchStats` for the same action.
    - A named test asserts `refereeCard.wasManualOverride` for both the manual and the auto room-settings paths.
    - The file contains its own server lifecycle and client helpers rather than importing them from a shared module.
    - No test weakens an assertion to a range where an exact value is knowable.
  </acceptance_criteria>
  <done>A real two-client socket match provably accumulates all nine statistics, delivers them identically to both players, and carries every one of them across half-time without reset.</done>
</task>

<task type="auto" id="45-06-02">
  <name>Task 45-06-02: Refresh 45-VALIDATION.md to match the delivered task breakdown</name>
  <files>.planning/phases/45-game-summary-popup/45-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/45-game-summary-popup/45-VALIDATION.md` in full — the current Per-Task Verification Map assumes a two-plan breakdown (`45-01-01` through `45-01-05` and `45-02-01`/`45-02-02`) that the delivered six-plan breakdown supersedes
    - The `<tasks>` blocks and `<verify>` commands of `45-01-PLAN.md` through `45-06-PLAN.md`, which are the authoritative source for the refreshed map
    - `.planning/STATE.md` "Deferred Items (acknowledged at milestone close 2026-08-17, v1.6)" — the `nyquist` row recording that Phases 38, 39 and 40 all shipped with stale VALIDATION.md files. This task exists so Phase 45 does not repeat that.
  </read_first>
  <action>
    Rewrite the Per-Task Verification Map so its rows are the sixteen task IDs actually delivered across the six plans (`45-01-01`, `45-01-02`, `45-02-01` through `45-02-03`, `45-03-01`, `45-03-02`, `45-04-01` through `45-04-03`, `45-05-01` through `45-05-04`, `45-06-01`, `45-06-02`), with each row carrying its plan number, wave, requirement IDs, threat reference where the task's plan declares one, the secure behaviour or verified behaviour in one line, the test type, and the exact `<automated>` command from that task's `<verify>` block. Mark every row's status as green only where the task's suite is actually passing at the time of writing; leave any row whose suite is not green as red with a one-line note.

    Update the Wave 0 Requirements checklist to reflect what was actually created: `packages/shared/src/matchStats.test.ts`, `packages/server/src/__tests__/gameEngine.matchStats.test.ts`, `packages/server/src/__tests__/matchStatsReducer.test.ts`, the new `describe` block added to `packages/server/src/__tests__/roomStore.test.ts`, `packages/server/src/__tests__/matchStats.integration.test.ts`, `packages/client/src/components/MatchSummaryContent.test.tsx`, and `packages/client/src/components/GameBoard.matchSummary.test.tsx`. Tick each item that exists.

    Update the Manual-Only Verifications table so it points at the human-verify checkpoint that actually shipped (task `45-05-04`) and reflects its ten-step script rather than the earlier two-row placeholder.

    Set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter, set `status: complete`, and tick the Validation Sign-Off checklist, but only if each of those statements is genuinely true against the delivered suites. If any sign-off item is not satisfiable — for example if a task's automated command is not green — leave it unticked and add a one-line note stating why, rather than ticking it optimistically. A stale-but-honest validation document is far more useful than a green-but-wrong one, which is exactly the failure recorded against Phases 38, 39 and 40.

    Set the Approval line to reference the human-verify checkpoint's outcome.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/45-game-summary-popup/45-VALIDATION.md','utf8');const ids=['45-01-01','45-01-02','45-02-01','45-02-02','45-02-03','45-03-01','45-03-02','45-04-01','45-04-02','45-04-03','45-05-01','45-05-02','45-05-03','45-05-04','45-06-01','45-06-02'];const missing=ids.filter(i=>!t.includes(i));if(missing.length){console.error('MISSING TASK ROWS: '+missing.join(', '));process.exit(1)}if(t.includes('45-01-05')){console.error('STALE ROW 45-01-05 STILL PRESENT');process.exit(1)}console.log('validation map covers all 16 delivered task ids')"</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the sixteen delivered task IDs appears as a row in the Per-Task Verification Map, and the superseded `45-01-03` through `45-01-05` placeholder rows are gone.
    - Every row's automated command matches the `<verify><automated>` command in the corresponding plan task, verbatim.
    - The Wave 0 checklist lists all seven test artifacts actually created and each existing one is ticked.
    - The Manual-Only Verifications table references checkpoint `45-05-04`.
    - Frontmatter flags and the sign-off checklist are set truthfully — anything not satisfiable is left unticked with a stated reason rather than ticked.
    - The verify script above exits 0.
  </acceptance_criteria>
  <done>`45-VALIDATION.md` describes the phase that was actually built, with every delivered task mapped to its real automated command, closing the stale-validation-document gap that Phases 38, 39 and 40 each shipped with.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `packages/server/src/__tests__/matchStats.integration.test.ts` (new file)
- Rewritten Per-Task Verification Map, Wave 0 checklist and Manual-Only Verifications table in `.planning/phases/45-game-summary-popup/45-VALIDATION.md`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket client → server handlers → broadcast | This suite exercises the real boundary rather than mocking past it — that is its entire purpose. |
| test process → in-memory room store | Tests seed and inspect room state directly; `clearAllRooms` between cases prevents cross-test leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-22 | Information disclosure | One client receiving statistics the other does not, or a divergent payload between the two sockets | mitigate | A named test asserts both clients observe deep-equal `matchStats` for the same action, directly exercising the ASVS V4 symmetric-broadcast expectation from 45-RESEARCH.md's Security Domain rather than assuming it. |
| T-45-23 | Repudiation | A statistic silently resetting at half-time, understating the whole-match total the summary claims to show | mitigate | The half-time test asserts all nine counters individually across the boundary, and additionally re-asserts the existing `subsUsed` persist / `addedTimeBonus` reset behaviours so a regression in either family is caught. |
| T-45-24 | Repudiation | The phase shipping with a validation document that claims coverage it does not have | mitigate | Task 45-06-02 rebuilds the map from the delivered plans and its verify script fails if any delivered task ID is missing or a superseded placeholder row remains. The task explicitly instructs leaving unsatisfiable sign-off items unticked rather than ticking optimistically. |
| T-45-25 | Denial of service | Flaky integration tests destabilising the suite | accept | The suite reuses the established server-lifecycle and client-helper pattern already proven across seven existing integration files in this repo. STATE.md records a known vitest worker-crash flake on Windows whose documented workaround is rerunning with `--pool=forks`; that is an environment quirk, not a defect in this suite. |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed. No install task exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared build && pnpm --filter @counter-attack/server test` — the full server suite is green including the new integration file. On Windows, if a vitest worker crash appears, rerun with `--pool=forks` per STATE.md's documented environment quirk before treating it as a real failure.
- `pnpm --filter @counter-attack/client test` — the full client suite is green.
- `pnpm --filter @counter-attack/shared test` — the full shared suite is green.
- `pnpm typecheck` — all three packages exit 0.
- `pnpm lint`, `pnpm stylelint`, `pnpm knip` — all exit clean.
- `pnpm build` — the whole monorepo builds.
- The `45-VALIDATION.md` coverage script exits 0.
</verification>

<success_criteria>
- A real two-client socket match accumulates all nine statistics end to end and both players receive identical values.
- Every counter survives half-time, asserted individually, without disturbing the existing `subsUsed` / `addedTimeBonus` split.
- The Referee Leniency manual/auto distinction is proven to survive the full room-settings-to-broadcast chain.
- `45-VALIDATION.md` maps every delivered task to its real automated command, with truthful sign-off.
- The whole monorepo builds, typechecks, lints and tests clean.
</success_criteria>

<output>
Create `.planning/phases/45-game-summary-popup/45-06-SUMMARY.md` when done.
</output>
