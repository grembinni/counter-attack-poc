---
phase: 42-substitution-ux-overhaul
plan: 03
type: execute
wave: 2
depends_on: ['42-01']
files_modified:
  - packages/server/src/gameEngine.ts
autonomous: true
requirements: [BUG-38]

must_haves:
  truths:
    - 'Every eligibility helper in gameEngine.ts that already excluded red-carded pieces now does so through the one shared predicate, with identical behavior.'
    - "No pitch-occupancy check in the first half of gameEngine.ts treats a red-carded piece's frozen hex as blocked."
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'shared-predicate-based red-card exclusion across eligibility and occupancy sites'
      contains: 'isActivePiece'
  key_links:
    - from: 'packages/server/src/gameEngine.ts'
      to: '@counter-attack/shared isActivePiece'
      via: 'named import from the shared package barrel'
      pattern: 'isActivePiece'
---

<objective>
Retrofit every already-correct hand-written `redCarded` filter in `packages/server/src/gameEngine.ts` onto the shared `isActivePiece` predicate (D-10 item 2), and apply that predicate to every pitch-occupancy check in the file's first half (lines ~337–4171).

Purpose: D-09 exists because this exclusion has been hand-written inline at least three separate times and two sites were still missed. Converging every site on one predicate is the structural fix that prevents a fourth recurrence — and the retrofit half is behavior-identical, so it is safe to do in the same pass as the genuinely-new occupancy fixes. BUG-38's requirement text names "movement-path blocking" and "any other pitch-occupancy check" explicitly, so the occupancy sweep is in scope, not optional polish.

Output: `gameEngine.ts` lines ~337–4171 fully red-card aware, with every retrofit proven behavior-preserving by the existing suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md
@.planning/phases/42-substitution-ux-overhaul/42-PATTERNS.md
@.planning/phases/42-substitution-ux-overhaul/42-01-SUMMARY.md
@.planning/debug/resolved/red-card-bench-removal-scope.md
</context>

## Artifacts this phase produces

See the table in `42-01-PLAN.md` under "Artifacts this phase produces". This plan creates no new exported symbol; it consumes `isActivePiece` from 42-01.

<tasks>

<task type="auto">
  <name>Task 1: Retrofit the eight existing redCarded filters onto isActivePiece</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 1066-1080 (`applyMove`'s red-card rejection plus the CARD-02/CARD-04 comment that documents D-08's exclude-by-flag convention — this comment is the canonical rationale and must be preserved, not deleted)
    - packages/server/src/gameEngine.ts lines 1805-1820 and 2300-2315 (the two GK `redCarded === true` early-return guards)
    - packages/server/src/gameEngine.ts lines 2895-2940 (`applyFreeMoveZoneCheck`'s eligible-pieces filter and its Debug-session doc comment)
    - packages/server/src/gameEngine.ts lines 5735-5755 (`computeCornerKickEligibleIds` / corner-kick-taker filter)
    - packages/server/src/gameEngine.ts lines 6320-6340 (`computeGoalKickEligibleIds` eligible-ids filter)
    - packages/server/src/gameEngine.ts lines 6950-6975 (`computePenaltyKickEligibleIds` — the original correct pattern every other site was told to mirror)
    - packages/server/src/gameEngine.ts lines 7375-7412 (`applyPenaltyKickTaker`'s `TAKER_INVALID` red-card rejection)
    - packages/server/src/gameEngine.ts lines 1-60 (the existing `@counter-attack/shared` named-import statement)
    - .planning/phases/42-substitution-ux-overhaul/42-01-SUMMARY.md (the exact `isActivePiece` semantics — it checks BOTH `redCarded !== true` AND `onPitch !== false`, which is a strict widening of every site below)
  </read_first>
  <action>
Add `isActivePiece` to the existing `@counter-attack/shared` named-import statement in `gameEngine.ts`.

Replace the inline flag comparison at each of these eight sites with the shared predicate, preserving every other clause and every surrounding doc comment verbatim:

1. `applyMove` (~1075): `if (piece.redCarded === true)` becomes `if (!isActivePiece(piece))`. Keep the `{ ok: false, reason: 'MOVE_INVALID', detail: 'RED_CARDED' }` return and the CARD-02/CARD-04 comment above it unchanged.
2. GK guard (~1813): `if (gk === undefined || gk.redCarded === true) return null;` becomes `if (gk === undefined || !isActivePiece(gk)) return null;`.
3. GK guard (~2309): identical change to the sibling guard.
4. `applyFreeMoveZoneCheck` (~2934): `(p) => p.redCarded !== true && isInRegion(p.position, oppositeThird)` becomes `(p) => isActivePiece(p) && isInRegion(p.position, oppositeThird)`.
5. Corner-kick-taker filter (~5749): `(p) => p.role !== 'GK' && p.id !== cornerKickTakerId && p.redCarded !== true` becomes `(p) => p.role !== 'GK' && p.id !== cornerKickTakerId && isActivePiece(p)`.
6. `computeGoalKickEligibleIds` (~6334): replace the `p.redCarded !== true &&` conjunct with `isActivePiece(p) &&`.
7. `computePenaltyKickEligibleIds` (~6967): `pieces.filter((p) => p.redCarded !== true && !excludeIds.includes(p.id))` becomes `pieces.filter((p) => isActivePiece(p) && !excludeIds.includes(p.id))`.
8. `applyPenaltyKickTaker` (~7406): `if (piece.redCarded === true)` becomes `if (!isActivePiece(piece))`, keeping the `TAKER_INVALID` return.

Then update each of the four Debug-session doc comments that currently say "mirroring `computePenaltyKickEligibleIds`'s existing `redCarded !== true` filter" (at ~2898-2900, ~5738-5739, ~6323-6324, ~6953) so they instead reference the shared `isActivePiece` helper and cite D-09/BUG-38. Do not delete the historical rationale in those comments — amend the mechanism sentence only.

This task must be behavior-preserving except for the strict widening introduced by `isActivePiece`'s second clause (a piece with `onPitch: false` but `redCarded` unset is now also excluded). No test should need editing; if one fails, stop and report it rather than adjusting the assertion.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- --pool=forks</automated>
</verify>
<acceptance_criteria> - `grep -vn "^\s*[*/]" packages/server/src/gameEngine.ts | grep -c "redCarded === true\|redCarded !== true"` returns 0 — no live (non-comment) inline flag comparison remains anywhere in the file. - `grep -c "isActivePiece" packages/server/src/gameEngine.ts` >= 9 (1 import + 8 retrofit sites). - `pnpm --filter @counter-attack/server test -- --pool=forks` passes the full suite with zero failures and zero test-file edits (confirm with `git status --short packages/server/src/__tests__` showing no modified test files from this task). - `pnpm --filter @counter-attack/server typecheck` exits 0.
</acceptance_criteria>
<done>Every pre-existing red-card exclusion in `gameEngine.ts` runs through the shared predicate, with the whole server suite still green and no test edited.</done>
</task>

<task type="auto">
  <name>Task 2: Apply isActivePiece to occupancy checks in gameEngine.ts lines 337-2432</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts around each of these lines, reading the enclosing function before editing: 337, 526, 623, 1879, 1897, 2432
    - packages/server/src/gameEngine.ts lines 271-345 (`buildSquadPieces` — line 337's `defendingFwdLine` filter lives here; this function builds a fresh squad from the pool, so classify carefully)
    - packages/shared/src/types.ts lines 60-80 (`onPitch`'s doc comment — the authority on why `position` stays a real HexCoord for a dismissed piece)
    - .planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md decisions D-08/D-09/D-10 (exclude-by-flag, never remove-by-splice; the audit mandate in D-10 item 4)
  </read_first>
  <action>
For each of the six listed lines, classify the predicate as one of:
- **OCCUPANCY** — it answers "is a piece physically standing on this hex" (blocking a destination, finding the occupant of a hex, checking a restart hex is clear). Add `isActivePiece(p) &&` as a leading conjunct, preserving every existing clause (`p.id !== excludeId`, team filters, ball-position clauses) exactly.
- **CONSTRUCTION** — it operates on a freshly-built squad array that cannot contain a dismissed piece (e.g. `buildSquadPieces`'s own `pieces` array, built from the player pool before any match state is overlaid). Leave unchanged and record the line number plus the reason in the summary.

Expected classification, to be confirmed by reading (do not apply blindly — if the code disagrees with this list, follow the code and say so in the summary):

- 337 — CONSTRUCTION (`buildSquadPieces` operates on the array it is currently building; `applyRosterContinuity` overlays match state afterwards). Leave unchanged, add a one-line comment recording that this was audited for BUG-38 and deliberately skipped.
- 526, 623, 1879, 1897, 2432 — OCCUPANCY. Apply the conjunct.

Add a single block comment above the first changed occupancy site in the file explaining the BUG-38 sweep: a red-carded piece keeps a live on-pitch `position` by design (D-08), so occupancy predicates must exclude it by flag.

Do not change any function signature, return type, or control flow. Conjunct addition only.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- --pool=forks</automated>
</verify>
<acceptance_criteria> - `grep -c "isActivePiece" packages/server/src/gameEngine.ts` increased by at least 5 relative to the count after Task 1. - Every line in {526, 623, 1879, 1897, 2432} (or its post-edit equivalent, located by the enclosing function name rather than raw line number) contains `isActivePiece`. - Line 337's predicate is unchanged and carries an audit comment naming BUG-38. - `pnpm --filter @counter-attack/server test -- --pool=forks` passes the full suite with zero failures. - The summary lists each of the six sites with its classification and enclosing function name.
</acceptance_criteria>
<done>Occupancy predicates in the first quarter of `gameEngine.ts` no longer treat a dismissed piece's frozen hex as blocked, and the one deliberate skip is documented in code.</done>
</task>

<task type="auto">
  <name>Task 3: Apply isActivePiece to occupancy checks in gameEngine.ts lines 3865-4171</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts around each of these lines, reading the enclosing function before editing: 3865, 4075, 4135, 4171
    - packages/server/src/gameEngine.ts lines 4300-4330 and 4400-4425 (the goal-reset / half-time-reset `applyRosterContinuity` call sites — these run near the 4075/4135/4171 sites and must NOT be altered; read them so the edits stay clear of the reset overlay logic)
    - .planning/phases/42-substitution-ux-overhaul/42-03-SUMMARY.md is not yet written; instead re-read the block comment added in Task 2 so this task's edits match its wording
  </read_first>
  <action>
Apply the same OCCUPANCY/CONSTRUCTION classification and the same `isActivePiece(p) &&` leading-conjunct treatment to the target-hex occupancy predicates at approximately lines 3865, 4075, 4135 and 4171.

All four are expected to be OCCUPANCY (target-hex occupancy tests inside restart/reposition apply-functions). Confirm each by reading its enclosing function; record any that turn out otherwise, with the reason, in the summary.

Explicitly do NOT touch `applyRosterContinuity` or either of the reset call sites near lines 4320 and 4416 — that helper matches by piece `id` and deliberately preserves `redCarded`/`onPitch` from the live roster. Changing it would undo D-08's permanent headcount cap.

After the edits, run a verification grep for any pitch-occupancy predicate remaining un-narrowed in lines 1-4200 of the file and list them in the summary; the remaining occupancy sweep from line 5103 onward is plan 42-05's scope and must be left alone here so the two plans do not collide.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- --pool=forks && pnpm --filter @counter-attack/server typecheck</automated>
</verify>
<acceptance_criteria> - Each of the four target predicates (located by enclosing function name) contains `isActivePiece`. - `git diff --stat packages/server/src/gameEngine.ts` shows changes only within the line ranges covered by Tasks 1-3; no hunk touches `applyRosterContinuity` (verify with `git diff packages/server/src/gameEngine.ts | grep -c "applyRosterContinuity"` returning 0). - `pnpm --filter @counter-attack/server test -- --pool=forks` passes the full suite with zero failures. - `npx eslint packages/server/src/gameEngine.ts` and `npx prettier --check packages/server/src/gameEngine.ts` both exit 0.
</acceptance_criteria>
<done>All occupancy predicates in `gameEngine.ts` lines 337-4171 are red-card aware; `applyRosterContinuity` is provably untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                                                                                       |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| handler → `gameEngine` pure functions        | The engine is the final authority; every apply-function re-validates independently of the handler that called it.                                 |
| `state.pieces` → eligibility/occupancy lists | Pieces are server-owned, but `redCarded` pieces carry stale on-pitch coordinates by design (D-08), which is the whole hazard this plan addresses. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                         |
| --------- | ---------------------- | ------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-08   | Tampering              | Retrofit of 8 existing `redCarded` filters | mitigate    | The retrofit is behavior-preserving except for a strict widening. Guarded by requiring the full pre-existing server suite to pass with zero test-file edits — any narrowing of an existing guard shows up as a failing test rather than a silent regression.            |
| T-42-09   | Elevation of Privilege | `applyMove` red-card rejection (~1075)     | mitigate    | This is the primary server-side gate stopping a modified client moving a dismissed piece. The retrofit must keep the `MOVE_INVALID`/`RED_CARDED` return and the CARD-02/CARD-04 comment; acceptance criteria assert the comparison is gone but the return shape is not. |
| T-42-10   | Tampering              | Occupancy predicate widening               | mitigate    | Narrowing occupancy could in principle let two pieces stack on one hex if a live piece were wrongly excluded. Mitigated by the conjunct-only edit rule (no existing clause removed) and by the full-suite green requirement.                                            |
| T-42-11   | Denial of Service      | `applyRosterContinuity` accidental edit    | mitigate    | Altering it would resurrect dismissed players at every goal/half-time reset. Explicit prohibition plus a `git diff` acceptance criterion asserting zero diff hunks mention it.                                                                                          |
| T-42-SC   | Tampering              | npm/pnpm installs                          | accept      | No packages installed by this plan. No Package Legitimacy Gate applies.                                                                                                                                                                                                 |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test -- --pool=forks` — full server suite green, no test file modified.
- `pnpm -r typecheck` — clean.
- `grep -vn "^\s*[*/]" packages/server/src/gameEngine.ts | grep -c "redCarded === true\|redCarded !== true"` returns 0.
- `git diff packages/server/src/gameEngine.ts | grep -c "applyRosterContinuity"` returns 0.
- `npx eslint packages/server/src/gameEngine.ts` — clean.
</verification>

<success_criteria>

- Zero live inline `redCarded` comparisons remain in `gameEngine.ts` — all eight are shared-predicate calls.
- Occupancy predicates at lines ~526, 623, 1879, 1897, 2432, 3865, 4075, 4135, 4171 are red-card aware.
- The one deliberate skip (line ~337, `buildSquadPieces`) is documented in code and in the summary.
- The full pre-existing server suite passes with no test assertion edited.
  </success_criteria>

<output>
Create `.planning/phases/42-substitution-ux-overhaul/42-03-SUMMARY.md` when done. Record: the eight retrofit sites with their enclosing function names, the classification table for all ten occupancy candidates, and the list of any un-narrowed occupancy predicates found in lines 1-4200 that were left for a follow-up.
</output>
