---
phase: 42-substitution-ux-overhaul
plan: 02
type: execute
wave: 2
depends_on: ['42-01']
files_modified:
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/gameHandlers.redCardExclusion.test.ts
autonomous: true
requirements: [BUG-38]

must_haves:
  truths:
    - 'A red-carded defender standing on or beside a shot path never rolls a deflection die.'
    - 'A red-carded piece is rejected server-side if a modified client tries to move it in any of the five response-move phases.'
    - "A red-carded piece's frozen hex never returns OCCUPIED to a response-move destination check."
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'red-card-aware deflection defender sets, response-move guard, and occupancy checks'
      contains: 'isActivePiece'
    - path: 'packages/server/src/__tests__/gameHandlers.redCardExclusion.test.ts'
      provides: 'BUG-38 regression coverage for both deflection builders and validateResponseMoveStep'
      min_lines: 60
  key_links:
    - from: 'packages/server/src/gameHandlers.ts'
      to: '@counter-attack/shared isActivePiece'
      via: 'named import from the shared package barrel'
      pattern: 'isActivePiece'
---

<objective>
Close BUG-38's two confirmed live defect sites in `packages/server/src/gameHandlers.ts` — the SNAPSHOT_DEFLECT and SHOT-phase deflection defender-set builders — plus the secondary `validateResponseMoveStep` defense-in-depth gap and every pitch-occupancy predicate in the same file.

Purpose: this is the exact code path the user-reported bug travels. `gameHandlers.ts:1286` and `:2289` build their eligible-defender list as `(p) => p.teamId === X && p.role !== 'GK'` with no red-card exclusion, operating on raw `state.pieces` positions with no rendering gate at all. A sent-off defender whose frozen `position` happens to sit on a later shot's `hexLine` gets rolled a real deflection die and can block a goal while being invisible on both clients' boards. Research confirmed this root cause with file:line citations; it needs no further investigation, only a mechanical fix plus regression coverage.

Output: every eligibility, defender-set and occupancy list in `gameHandlers.ts` routed through the shared `isActivePiece` predicate, with a dedicated regression test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md
@.planning/phases/42-substitution-ux-overhaul/42-PATTERNS.md
@.planning/phases/42-substitution-ux-overhaul/42-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

## Artifacts this phase produces

See the table in `42-01-PLAN.md` under "Artifacts this phase produces" — it lists every symbol Phase 42 creates across all nine plans. This plan creates no new exported symbol; it consumes `isActivePiece` from 42-01.

<tasks>

<task type="auto">
  <name>Task 1: Exclude red-carded defenders from both deflection defender-set builders</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts lines 1275-1320 (the SNAPSHOT_DEFLECT deflection defender-set builder and the `rollDice()` call it feeds)
    - packages/server/src/gameHandlers.ts lines 2280-2320 (the SHOT-phase deflection defender-set builder, commented "same pattern as GAME_SHOT")
    - packages/server/src/gameHandlers.ts lines 1-60 (the existing import block — find where `@counter-attack/shared` named imports live)
    - .planning/research/ARCHITECTURE.md lines 130-166 (§4c — the confirmed root cause, the exact filter shape, and why this filter is purely positional with no rendering gate)
    - .planning/research/PITFALLS.md lines 91-103 (Pitfall 6 — the required regression test shape)
    - .planning/phases/42-substitution-ux-overhaul/42-01-SUMMARY.md (the exact `isActivePiece` signature/semantics landed in 42-01)
  </read_first>
  <action>
Add `isActivePiece` to the existing `@counter-attack/shared` named-import statement in `gameHandlers.ts` (do not create a second import statement from the same module).

Site 1 — SNAPSHOT_DEFLECT defender-set builder (~line 1285): change the filter from
`(p) => p.teamId === defendingTeam && p.role !== 'GK'`
to
`(p) => p.teamId === defendingTeam && p.role !== 'GK' && isActivePiece(p)`.

Site 2 — SHOT-phase defender-set builder (~line 2288): change the filter from
`(p) => p.teamId === defTeam && p.role !== 'GK'`
to
`(p) => p.teamId === defTeam && p.role !== 'GK' && isActivePiece(p)`.

Add a one-line comment above each citing `BUG-38` and stating that a sent-off piece keeps a live on-pitch `position` by design (D-08) and must therefore be excluded by flag here, not assumed absent.

Do not restructure the loops, the `pathSet`/`nearPath` logic, or the `DefenderDeflectionInput` push shape — this is a filter-conjunct addition only. Do not change the `role !== 'GK'` clause (BUG-32's GK exclusion is a separate, still-required rule).
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- --pool=forks gameHandlers</automated>
</verify>
<acceptance_criteria> - `grep -c "isActivePiece" packages/server/src/gameHandlers.ts` >= 3 (import + 2 call sites) after this task. - `grep -n "p.teamId === defendingTeam && p.role !== 'GK'," packages/server/src/gameHandlers.ts` returns no match (the un-narrowed SNAPSHOT filter is gone). - `grep -n "p.teamId === defTeam && p.role !== 'GK'," packages/server/src/gameHandlers.ts` returns no match (the un-narrowed SHOT filter is gone). - The pre-existing server suite passes unchanged: `pnpm --filter @counter-attack/server test -- --pool=forks` exits 0.
</acceptance_criteria>
<done>Neither deflection defender-set builder can include a red-carded or off-pitch piece.</done>
</task>

<task type="auto">
  <name>Task 2: Add the red-card guard to validateResponseMoveStep and the file's occupancy predicates</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts lines 255-330 (`validateResponseMoveStep` in full — its guard sequence is ownership → carrier-exclusion → lock → pace → distance → boundary → occupancy; the new guard's position matters)
    - packages/server/src/gameHandlers.ts lines 520-540 (the sibling BUG-32 GK-exclusion guard whose documented rationale is "a modified client bypassing the client-side gate" — the precedent for adding a server-side check that is currently masked client-side)
    - packages/server/src/gameHandlers.ts around lines 1082, 1162, 2512, 2591, 2687 (the five remaining pitch-occupancy predicates outside `validateResponseMoveStep`; read each surrounding function to classify what its predicate answers before editing)
    - packages/server/src/gameEngine.ts lines 1066-1080 (`applyMove`'s existing red-card rejection — the shape and the `MOVE_INVALID`/`RED_CARDED` reason this new guard mirrors)
    - .planning/research/ARCHITECTURE.md line 164 (the secondary finding: `validateResponseMoveStep` is used by HIGH_PASS_MOVE, GK_KICK_MOVE, FIRST_TIME_PASS_MOVE, SNAPSHOT_DEFLECT and GOAL_KICK_MOVE and has no red-card check at all)
  </read_first>
  <action>
Part A — `validateResponseMoveStep` guard (D-10 item 3). Insert a new guard immediately AFTER the ownership check (the `if (!piece || piece.teamId !== config.actingTeam) return fail('WRONG_TEAM');` block, ~line 292-294) and BEFORE the carrier-exclusion check: `if (!isActivePiece(piece)) return fail('RED_CARDED');`.

Use the literal reason string `'RED_CARDED'` — `ServerEvents.GAME_ERROR`'s payload is typed as a plain `string` (`packages/shared/src/events.ts:401`), and `'RED_CARDED'` mirrors `applyMove`'s existing `detail: 'RED_CARDED'` so the two rejection paths read identically. Add a comment recording that this is defense-in-depth against a modified client: the client's `HexGrid.tsx` render-skip masks it today, and this handler is shared by all five response-move phases named above.

Do not reorder any existing guard. Ownership must still be checked first (an opponent's piece id must leak `WRONG_TEAM`, not `RED_CARDED`).

Part B — the file's occupancy predicates. Add `isActivePiece(p) &&` as a leading conjunct to each pitch-occupancy predicate in this file:

- `validateResponseMoveStep`'s destination-occupancy check (~line 324, `state.pieces.some((p) => p.id !== pieceId && p.position.q === to.q && ...)`)
- ~line 1082 and ~line 1162 (target-hex occupancy checks)
- ~line 2512 and ~line 2687 (destination-occupancy checks with an `id !== pieceId` exclusion)
- ~line 2591 (kick-off-hex occupancy check)

For each of the six, first read the surrounding function and confirm the predicate answers "is a piece physically standing on this hex". If any one of them is instead a roster/identity lookup rather than a pitch-occupancy test, leave it unchanged and record the skip with its line number and reason in the summary — do not silently skip.

Preserve every existing conjunct (`p.id !== pieceId`, team filters, role filters) exactly. This is an added conjunct at each site, never a replacement.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- --pool=forks</automated>
</verify>
<acceptance_criteria> - `grep -c "isActivePiece" packages/server/src/gameHandlers.ts` >= 9 (1 import + 2 deflection sites + 1 response-move guard + up to 6 occupancy sites; the exact count is >= 9 when all six occupancy sites apply). - `grep -n "fail('RED_CARDED')" packages/server/src/gameHandlers.ts` returns exactly 1 match, and it appears at a lower line number than the `carrierExclusionKey` check inside `validateResponseMoveStep`. - `grep -n "state.pieces.some((p) => p.id !== pieceId && p.position.q === to.q" packages/server/src/gameHandlers.ts` returns no match at line ~324 (the un-narrowed form is gone). - `pnpm --filter @counter-attack/server test -- --pool=forks` passes the full existing suite with zero failures. - `pnpm --filter @counter-attack/server typecheck` exits 0.
</acceptance_criteria>
<done>A modified client cannot move a sent-off piece through any response-move phase, and no red-carded piece's frozen hex blocks a destination in this file.</done>
</task>

<task type="auto">
  <name>Task 3: BUG-38 regression tests for gameHandlers</name>
  <files>packages/server/src/__tests__/gameHandlers.redCardExclusion.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameHandlers.substitution.test.ts (the closest existing handler-level test file — copy its room/socket harness setup, mock shape and `describe` naming conventions)
    - packages/server/src/__tests__/testHelpers.ts (shared fixture builders for rooms, sockets and game state)
    - packages/server/src/gameHandlers.ts (the two deflection builders and `validateResponseMoveStep` as edited in Tasks 1-2 — needed to know which handler events drive each path)
    - .planning/research/PITFALLS.md lines 91-103 (Pitfall 6 specifies the required regression: "a red-carded defender positioned on a shot's path must never appear in the DEFLECT_ATTEMPT defender-input list")
  </read_first>
  <action>
Create `packages/server/src/__tests__/gameHandlers.redCardExclusion.test.ts` with a `describe('BUG-38 — red-carded pieces excluded from gameHandlers eligibility', ...)` block containing at minimum:

1. SHOT-phase deflection: seed a SHOT-phase state where a defending outfielder with `redCarded: true, onPitch: false` sits exactly ON the `hexLine` between shooter and `shotTargetHex`. Drive the shot-resolution handler and assert no `DEFLECT_ATTEMPT` event is appended for that piece id and no deflection die is attributed to it.
2. SHOT-phase positive control: the identical scenario with the same defender NOT red-carded — assert the deflection path DOES engage that defender, proving the filter was narrowed rather than broken.
3. SNAPSHOT_DEFLECT deflection: the same pair of assertions against the SNAPSHOT_DEFLECT builder path.
4. `validateResponseMoveStep` rejection: for at least two of the five response-move phases (use `SNAPSHOT_DEFLECT` and `HIGH_PASS_MOVE`), emit a response-move for an own-team piece with `redCarded: true` and assert the socket receives `GAME_ERROR` with `'RED_CARDED'` and that `room.gameState` is unchanged.
5. Ownership precedence: an OPPONENT-owned red-carded piece id in the same response-move still yields `'WRONG_TEAM'`, not `'RED_CARDED'` — proving guard order was preserved.
6. Occupancy: a response-move destination hex occupied only by a red-carded piece is accepted (no `'OCCUPIED'`), while the same hex occupied by a live piece is still rejected with `'OCCUPIED'`.

Every test must set BOTH `redCarded: true` and `onPitch: false` on the dismissed piece, matching how `gameEngine.ts`'s booking branch sets them atomically. Add one extra case that sets ONLY `onPitch: false` and asserts the same exclusion, proving the two-clause predicate from 42-01.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- --pool=forks gameHandlers.redCardExclusion</automated>
</verify>
<acceptance_criteria> - The file `packages/server/src/__tests__/gameHandlers.redCardExclusion.test.ts` exists and `pnpm --filter @counter-attack/server test -- --pool=forks gameHandlers.redCardExclusion` reports >= 8 passing assertions and 0 failures. - Each of the six numbered scenarios above has at least one assertion, and every negative case has its positive control in the same file. - Reverting either deflection filter to its pre-fix form makes at least one test in this file fail (verify by temporarily reverting one filter, running the file, observing the failure, then restoring the fix — report the observed failing test name in the summary). - `pnpm --filter @counter-attack/server test -- --pool=forks` (whole suite) exits 0.
</acceptance_criteria>
<done>The confirmed live defect has an executable regression test that fails if the fix is reverted.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                                             |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| client socket → `gameHandlers`        | Every `ClientEvents` payload is untrusted: piece ids, hex coordinates and team claims all originate from a potentially modified client. |
| handler → `gameEngine` pure functions | Handlers gate first; the engine re-validates independently (defense-in-depth convention established at `gameHandlers.ts:526-533`).      |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                                         | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                 |
| --------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-04   | Elevation of Privilege | `validateResponseMoveStep` (HIGH_PASS_MOVE, GK_KICK_MOVE, FIRST_TIME_PASS_MOVE, SNAPSHOT_DEFLECT, GOAL_KICK_MOVE) | mitigate    | Add `if (!isActivePiece(piece)) return fail('RED_CARDED')` after the ownership check. Today the only barrier is `HexGrid.tsx:761`'s render-skip — a client-side gate a modified client bypasses entirely. Regression test 4 drives the socket event directly, bypassing the UI. |
| T-42-05   | Tampering              | Deflection defender-set builders (`:1286`, `:2289`)                                                               | mitigate    | Not client-triggerable directly, but a server-side data-integrity defect that silently alters shot outcomes. Fixed by the `isActivePiece` conjunct; regression tests 1-3 assert both the exclusion and its positive control.                                                    |
| T-42-06   | Information Disclosure | `GAME_ERROR: 'RED_CARDED'` reason string                                                                          | accept      | Discloses only that a piece the caller already owns is sent off — information the caller's own snapshot already contains. No new leakage.                                                                                                                                       |
| T-42-07   | Elevation of Privilege | Guard ordering in `validateResponseMoveStep`                                                                      | mitigate    | The new guard is inserted AFTER the ownership check so an opponent's piece id can never probe red-card status. Regression test 5 asserts `'WRONG_TEAM'` still wins for opponent-owned ids.                                                                                      |
| T-42-SC   | Tampering              | npm/pnpm installs                                                                                                 | accept      | No packages installed by this plan; all edits are to existing files. No Package Legitimacy Gate applies.                                                                                                                                                                        |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test -- --pool=forks` — full server suite green (1340+ pre-existing tests must all still pass).
- `pnpm -r typecheck` — clean (run `pnpm --filter @counter-attack/shared build` first if `isActivePiece` is not resolving).
- `npx eslint packages/server/src/gameHandlers.ts packages/server/src/__tests__/gameHandlers.redCardExclusion.test.ts` — clean.
- Grep audit for residual inline flag checks in this file: `grep -n "redCarded" packages/server/src/gameHandlers.ts | grep -v "^[0-9]*: *[*/]"` should return only comment-free lines that are part of test fixtures or the new comments — no live inline `p.redCarded !== true` predicate.
</verification>

<success_criteria>

- Both deflection defender-set builders exclude red-carded/off-pitch pieces.
- `validateResponseMoveStep` rejects a sent-off piece with `RED_CARDED` for all five response-move phases, after (not before) the ownership check.
- All six pitch-occupancy predicates in `gameHandlers.ts` are red-card aware, or a documented skip with rationale exists for any that are not occupancy tests.
- A dedicated regression test file fails if either deflection filter is reverted.
  </success_criteria>

<output>
Create `.planning/phases/42-substitution-ux-overhaul/42-02-SUMMARY.md` when done. Record: every changed line with file:line, any occupancy predicate deliberately skipped with its reason, and the name of the test observed failing during the revert check.
</output>
