---
phase: 42-substitution-ux-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/stoppagePhases.ts
  - packages/shared/src/stoppagePhases.test.ts
  - packages/shared/src/moveValidator.ts
  - packages/shared/src/moveValidator.test.ts
  - packages/shared/src/passValidator.ts
  - packages/shared/src/passValidator.test.ts
  - packages/shared/src/outOfBounds.ts
  - packages/shared/src/outOfBounds.test.ts
autonomous: true
requirements: [BUG-38]

must_haves:
  truths:
    - "A red-carded opponent standing on a hex adjacent to the ball-carrier's destination no longer triggers a STEAL_ATTEMPT."
    - "A red-carded piece's frozen hex no longer reports as OCCUPIED to validateMove."
    - "A red-carded piece no longer appears in a pass's interceptor list nor blocks a pass path."
    - 'Every packages/shared eligibility/occupancy list is built through one shared predicate, not a hand-written inline clause.'
  artifacts:
    - path: 'packages/shared/src/stoppagePhases.ts'
      provides: 'isActivePiece predicate exported from the shared package'
      contains: 'export function isActivePiece'
    - path: 'packages/shared/src/moveValidator.ts'
      provides: 'red-card-aware OCCUPIED check and ZoI opponent list'
      contains: 'isActivePiece'
    - path: 'packages/shared/src/passValidator.ts'
      provides: 'red-card-aware interceptor/blocker lists'
      contains: 'isActivePiece'
  key_links:
    - from: 'packages/shared/src/moveValidator.ts'
      to: 'packages/shared/src/stoppagePhases.ts'
      via: "import { isActivePiece } from './stoppagePhases.js'"
      pattern: "from '\\./stoppagePhases\\.js'"
    - from: 'packages/shared/src/index.ts'
      to: 'isActivePiece'
      via: "existing `export * from './stoppagePhases.js'` barrel re-export"
      pattern: "export \\* from '\\./stoppagePhases\\.js'"
---

<objective>
Create the single shared `isActivePiece` predicate that BUG-38's whole fix converges on (D-09), and apply it to every eligibility/occupancy list inside `packages/shared` — the package the previous red-card audit structurally missed (Pitfall 7).

Purpose: `packages/shared` validators run on BOTH server and client. `moveValidator.ts`'s ZoI opponent list is a confirmed live defect: a sent-off opponent still projects a Zone of Influence and still triggers STEAL_ATTEMPT against the ball carrier purely because `position` is deliberately never nulled (D-08). Every downstream plan in this phase imports the helper this plan creates, so it must land first and alone.

Output: `isActivePiece` exported from `packages/shared`, plus red-card-aware occupancy/ZoI/interception in `moveValidator.ts`, `passValidator.ts`, and `outOfBounds.ts`, each with a regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md
@.planning/phases/42-substitution-ux-overhaul/42-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

## Artifacts this phase produces

New symbols created by Phase 42 (all plans — listed here so drift verification does not flag them as unresolved references):

| Symbol                                                                              | Kind                           | Created in | File                                                               |
| ----------------------------------------------------------------------------------- | ------------------------------ | ---------- | ------------------------------------------------------------------ |
| `isActivePiece(piece)`                                                              | exported function              | 42-01      | `packages/shared/src/stoppagePhases.ts`                            |
| `'ROSTER_REPOSITION'`                                                               | `ActionEventType` union member | 42-05      | `packages/shared/src/types.ts`                                     |
| `ROSTER_REPOSITION` `ActionEvent` variant                                           | discriminated-union member     | 42-05      | `packages/shared/src/types.ts`                                     |
| `RosterRepositionPayload`                                                           | exported type                  | 42-05      | `packages/shared/src/events.ts`                                    |
| `GAME_ROSTER_REPOSITION`                                                            | `ClientEvents` key             | 42-05      | `packages/shared/src/events.ts`                                    |
| `applyRosterReposition()`                                                           | exported function              | 42-05      | `packages/server/src/gameEngine.ts`                                |
| `ApplyRosterRepositionResult`                                                       | exported type                  | 42-05      | `packages/server/src/gameEngine.ts`                                |
| `RosterRepositionRejection`                                                         | type                           | 42-05      | `packages/server/src/gameEngine.ts`                                |
| `emitRosterReposition()`                                                            | Zustand store action           | 42-08      | `packages/client/src/store/useGameStore.ts`                        |
| `MidmatchSubMode` (`'reposition' \| 'substitute'`)                                  | local type                     | 42-06      | `packages/client/src/components/LineupAssignmentScreen.tsx`        |
| `MidmatchDragState`                                                                 | local type                     | 42-06      | `packages/client/src/components/LineupAssignmentScreen.tsx`        |
| `onReposition` prop                                                                 | component prop                 | 42-06      | `packages/client/src/components/LineupAssignmentScreen.tsx`        |
| `actionPending` prop                                                                | component prop                 | 42-06      | `packages/client/src/components/LineupAssignmentScreen.tsx`        |
| `.statCardSentOff`                                                                  | CSS module class               | 42-06      | `packages/client/src/components/LineupAssignmentScreen.module.css` |
| `.sentOffBadge`                                                                     | CSS module class               | 42-06      | `packages/client/src/components/LineupAssignmentScreen.module.css` |
| `.subModeButton` / `.subModeButtonActive`                                           | CSS module classes             | 42-07      | `packages/client/src/components/LineupAssignmentScreen.module.css` |
| `.subConfirmOverlay` / `.subConfirmCard` / `.subConfirmText` / `.subConfirmActions` | CSS module classes             | 42-07      | `packages/client/src/components/LineupAssignmentScreen.module.css` |
| `.subButtonStripActive`                                                             | CSS module class               | 42-08      | `packages/client/src/components/GameBoard.module.css`              |
| `.resumeCtaRow` / `.resumeCta`                                                      | CSS module classes             | 42-08      | `packages/client/src/components/GameBoard.module.css`              |

<tasks>

<task type="auto">
  <name>Task 1: Add the shared isActivePiece predicate</name>
  <files>packages/shared/src/stoppagePhases.ts, packages/shared/src/stoppagePhases.test.ts</files>
  <read_first>
    - packages/shared/src/stoppagePhases.ts (the whole file — `maxOnPitchFor` at ~75-78 is the same-file, same-idiom analog to copy for export style and doc-comment discipline)
    - packages/shared/src/types.ts lines 14-90 (`PlayerPiece`, specifically the `redCarded` and `onPitch` field doc comments — `onPitch`'s comment explicitly warns it is a client-rendering signal, which decides the helper's semantics)
    - packages/shared/src/stoppagePhases.test.ts (existing test style for this file)
    - packages/shared/src/index.ts (confirm `export * from './stoppagePhases.js'` already re-exports this module, so no barrel edit is needed)
  </read_first>
  <action>
Add an exported `isActivePiece(piece: PlayerPiece): boolean` to `packages/shared/src/stoppagePhases.ts`, placed after `maxOnPitchFor` and using the same `export function` style.

Semantics (D-09, locked here for every downstream consumer): return `piece.redCarded !== true && piece.onPitch !== false`. Both clauses, not one. Rationale to record in the doc comment: `gameEngine.ts`'s booking-resolution red-card branch sets `redCarded: true` and `onPitch: false` atomically in the same `pieces.map()` spread, so either clause alone is sufficient TODAY — but `onPitch`'s own field comment documents it as an independently-settable rendering signal, and checking both makes the predicate correct for any future caller that sets only one. Do NOT collapse it to a single clause.

The doc comment must state: (a) this is the single shared exclude-by-flag check for "is this piece eligible for gameplay computations", (b) D-08's constraint that `position` is never nulled and a red-carded piece is never spliced out of `state.pieces`, so every consumer must exclude by flag, (c) that hand-writing `p.redCarded !== true` inline is the exact bug class BUG-38 exists to close (it happened at 3+ sites and 2 were missed), and (d) that new eligibility/occupancy/defender-list sites must call this rather than re-deriving it.

No barrel edit: `packages/shared/src/index.ts` already does `export * from './stoppagePhases.js'`.

Add tests to `packages/shared/src/stoppagePhases.test.ts`: a plain piece returns true; `redCarded: true` returns false; `onPitch: false` returns false; both set returns false; `redCarded: false`/`onPitch: true` returns true; `yellowCards: 1` (booked but not sent off) returns true.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared test -- --pool=forks stoppagePhases</automated>
</verify>
<acceptance_criteria> - `grep -n "export function isActivePiece" packages/shared/src/stoppagePhases.ts` returns exactly 1 match. - The helper body references both `redCarded` and `onPitch`: `grep -A4 "export function isActivePiece" packages/shared/src/stoppagePhases.ts | grep -c "redCarded"` >= 1 AND the same range contains `onPitch`. - `pnpm --filter @counter-attack/shared test -- --pool=forks stoppagePhases` passes with at least 6 new assertions covering: default piece true, redCarded true→false, onPitch false→false, both→false, explicit redCarded:false/onPitch:true→true, yellowCards:1→true. - `pnpm --filter @counter-attack/shared typecheck` exits 0.
</acceptance_criteria>
<done>`isActivePiece` is exported from `@counter-attack/shared` and its truth table is covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Apply isActivePiece to moveValidator's OCCUPIED check and ZoI opponent list</name>
  <files>packages/shared/src/moveValidator.ts, packages/shared/src/moveValidator.test.ts</files>
  <read_first>
    - packages/shared/src/moveValidator.ts (the whole file, 130 lines — line 66 is the OCCUPIED check, line 95 is the ZoI opponent list, line 110 resolves the carrier for the TACKLE_ATTEMPT branch)
    - packages/shared/src/stoppagePhases.ts (as edited in Task 1 — the exact exported name and signature)
    - packages/shared/src/moveValidator.test.ts (existing state/piece fixture builders to reuse for the new tests)
    - .planning/research/PITFALLS.md lines 106-118 (Pitfall 7 — the confirmed defect, its blast radius, and why a `packages/server`-scoped grep audit structurally missed this file)
  </read_first>
  <action>
Import `isActivePiece` into `packages/shared/src/moveValidator.ts` from `'./stoppagePhases.js'` (a one-way import; `stoppagePhases.ts` imports only types from `./types.js`, so no cycle is created — confirm this before adding the import).

Apply it at three predicates:

1. Guard 3, the OCCUPIED check (~line 66): `state.pieces.some((p) => p.position.q === to.q && p.position.r === to.r)` becomes `state.pieces.some((p) => isActivePiece(p) && p.position.q === to.q && p.position.r === to.r)`. BUG-38's requirement text names movement-path blocking / pitch occupancy explicitly — a sent-off player's frozen hex must stop returning OCCUPIED.

2. Guard 6, the ZoI opponent list (~line 95): `state.pieces.filter((p) => p.teamId !== piece.teamId)` becomes `state.pieces.filter((p) => p.teamId !== piece.teamId && isActivePiece(p))`. This is Pitfall 7's confirmed live defect. Keep the existing `stealAttemptedByIds` exclusion applied AFTER `getZoIDefenders`, unchanged — the two filters are independent and must not be merged.

3. Guard 7, the TACKLE_ATTEMPT branch (~line 109-124): the carrier lookup `state.pieces.find((p) => p.id === state.ball.carrierId)` is left alone (a red-carded piece can never be the carrier), but add `isActivePiece(piece)`-awareness is NOT needed for the moving piece here — `applyMove` already rejects a red-carded mover by id. Do not add a redundant guard; instead add a one-line comment at guard 7 recording that the moving piece's own red-card rejection lives in `applyMove` (`gameEngine.ts` CARD-02/CARD-04 guard) and is deliberately not duplicated here.

Update the guard-precedence doc comment at the top of `validateMove` (lines 43-57) to note the red-card exclusion now applied at guards 3 and 6.

Add tests to `packages/shared/src/moveValidator.test.ts`:

- A red-carded opponent occupying the destination hex: `validateMove` returns `{ ok: true }` (not `OCCUPIED`).
- A non-red-carded opponent occupying the destination hex still returns `{ ok: false, reason: 'OCCUPIED' }` (proves the guard was narrowed, not removed).
- A red-carded opponent adjacent to the ball-carrier's destination produces NO `STEAL_ATTEMPT` effect.
- A non-red-carded opponent in the same adjacency still produces `STEAL_ATTEMPT` with that defender in `defenders`.
- An `onPitch: false` opponent (without `redCarded`) is excluded from both, proving the two-clause predicate.
  </action>
  <verify>
  <automated>pnpm --filter @counter-attack/shared test -- --pool=forks moveValidator</automated>
  </verify>
  <acceptance_criteria> - `grep -c "isActivePiece" packages/shared/src/moveValidator.ts` >= 3 (1 import + 2 predicate call sites). - `grep -n "state.pieces.filter((p) => p.teamId !== piece.teamId)" packages/shared/src/moveValidator.ts` returns no match (the bare unfiltered opponent list is gone). - `pnpm --filter @counter-attack/shared test -- --pool=forks moveValidator` passes, including 5 new assertions named for BUG-38. - The existing `moveValidator.test.ts` suite passes unchanged (no pre-existing assertion edited to accommodate the new behavior).
  </acceptance_criteria>
  <done>A red-carded opponent neither blocks a destination hex nor triggers a steal attempt; a live opponent still does both.</done>
  </task>

<task type="auto">
  <name>Task 3: Apply isActivePiece to passValidator and outOfBounds occupancy lists</name>
  <files>packages/shared/src/passValidator.ts, packages/shared/src/passValidator.test.ts, packages/shared/src/outOfBounds.ts, packages/shared/src/outOfBounds.test.ts</files>
  <read_first>
    - packages/shared/src/passValidator.ts (the whole file — the piece-position predicates at ~104, ~113, ~146 are the interceptor / path-blocker / destination-occupancy checks; read the surrounding function so each one's role is understood before editing)
    - packages/shared/src/outOfBounds.ts (~line 191, the `pieces.some((p) => p.position.q === hex.q && p.position.r === hex.r)` occupancy predicate and the function that owns it)
    - packages/shared/src/stoppagePhases.ts (the exported `isActivePiece` signature from Task 1)
    - packages/shared/src/passValidator.test.ts and packages/shared/src/outOfBounds.test.ts (fixture builders and naming conventions)
    - .planning/research/PITFALLS.md lines 106-118 (Pitfall 7 names `passValidator.ts` explicitly as the un-audited pass-side sibling of the movement-side gap)
  </read_first>
  <action>
Import `isActivePiece` from `'./stoppagePhases.js'` into both `passValidator.ts` and `outOfBounds.ts`.

In `passValidator.ts`, add `isActivePiece(p) &&` to each of the three piece-position predicates (~104 opponent-on-path interception, ~113 adjacent-to-path interception, ~146 destination-occupied-by-opponent). Preserve each predicate's existing team/role clauses exactly — this is an added conjunct, never a replacement. If any of the three is fed by an already-filtered `opponentPieces` array built earlier in the function, apply `isActivePiece` at the array-construction site instead and leave the downstream predicate untouched; record which shape you chose in the summary.

In `outOfBounds.ts`, add `isActivePiece(p) &&` to the occupancy predicate at ~line 191.

Add tests:

- `passValidator.test.ts`: a red-carded opponent sitting on the pass path is not returned as an interceptor; a live opponent in the identical position still is; a red-carded opponent on the destination hex no longer makes the pass destination occupied.
- `outOfBounds.test.ts`: a red-carded piece on the candidate hex no longer reports it as occupied; a live piece still does.

Do NOT change any function signature or exported name in either file — this is a predicate-narrowing change only.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared test -- --pool=forks && pnpm --filter @counter-attack/shared typecheck</automated>
</verify>
<acceptance_criteria> - `grep -c "isActivePiece" packages/shared/src/passValidator.ts` >= 2 and `grep -c "isActivePiece" packages/shared/src/outOfBounds.ts` >= 2. - `grep -rn "redCarded" packages/shared/src --include=*.ts | grep -v "\.test\.ts" | grep -v stoppagePhases.ts | grep -v types.ts` returns no match — proving no shared-package consumer hand-writes the flag check inline (D-09). - `pnpm --filter @counter-attack/shared test -- --pool=forks` passes the full shared suite with zero failures. - `pnpm --filter @counter-attack/shared build && pnpm -r typecheck` exits 0 (the shared `dist/` must be rebuilt before server/client typecheck picks up the new export).
</acceptance_criteria>
<done>Every eligibility, interception and occupancy list inside `packages/shared` excludes red-carded pieces through the one shared predicate.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                                                                                                   |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client → shared validator | `packages/shared` validators are bundled into the client AND imported by the server. Client-side results are advisory; the server re-runs the same functions authoritatively. |
| game state → validator    | `GameState.pieces` is server-authoritative and never client-supplied at this layer.                                                                                           |

## STRIDE Threat Register

| Threat ID | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                            |
| --------- | ---------------------- | ---------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-01   | Elevation of Privilege | `moveValidator.validateMove` ZoI opponent list | mitigate    | Narrowing the opponent list removes a phantom defender; the server calls the identical function, so a modified client cannot widen it. Regression test asserts a live opponent still triggers STEAL_ATTEMPT (guard narrowed, not removed).                 |
| T-42-02   | Tampering              | `moveValidator` OCCUPIED guard                 | mitigate    | Removing red-carded pieces from occupancy could in principle let a client claim a hex is free. Mitigated because the server re-runs `validateMove` on `GAME_MOVE` and `applyMove` re-checks occupancy independently (`gameEngine.ts:623`, fixed in 42-03). |
| T-42-03   | Spoofing               | `isActivePiece` semantics                      | accept      | The predicate reads only server-set fields (`redCarded`/`onPitch`); no client input reaches it. Low risk, no mitigation needed.                                                                                                                            |
| T-42-SC   | Tampering              | npm/pnpm installs                              | accept      | This plan installs no packages — every change is to an existing file in an existing workspace package. No Package Legitimacy Gate applies.                                                                                                                 |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test -- --pool=forks` — full shared suite green.
- `pnpm --filter @counter-attack/shared build && pnpm -r typecheck` — shared `dist/` rebuilt, all three packages typecheck.
- `pnpm --filter @counter-attack/server test -- --pool=forks` and `pnpm --filter @counter-attack/client test -- --pool=forks` — no downstream regressions from the narrowed shared predicates. (If a vitest worker crashes, re-run the same command; `--pool=forks` is already the workaround.)
- `npx eslint packages/shared/src/stoppagePhases.ts packages/shared/src/moveValidator.ts packages/shared/src/passValidator.ts packages/shared/src/outOfBounds.ts` — clean.
</verification>

<success_criteria>

- `isActivePiece` is exported from `@counter-attack/shared` and checks both `redCarded` and `onPitch`.
- No file in `packages/shared` outside `stoppagePhases.ts`/`types.ts` hand-writes a `redCarded` comparison.
- A red-carded opponent no longer triggers STEAL_ATTEMPT, no longer blocks a destination hex, and no longer intercepts a pass — each proven by a named regression test.
- The equivalent live-opponent case still fails/triggers exactly as before, proving no guard was removed rather than narrowed.
  </success_criteria>

<output>
Create `.planning/phases/42-substitution-ux-overhaul/42-01-SUMMARY.md` when done. Record: the exact `isActivePiece` body, the list of predicates changed with file:line, and (for Task 3) whether `passValidator` was narrowed at the predicate or at the array-construction site.
</output>
