---
phase: 34-visual-theme-restyle
plan: 03
type: execute
wave: 3
depends_on: [34-01, 34-02]
files_modified:
  - packages/client/scripts/check-contrast.ts
  - packages/client/package.json
  - knip.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [THEME-04, THEME-02]
must_haves:
  truths:
    - '`pnpm check-contrast` iterates every TEAM_CONFIGS team, derives its AA-safe accent via the same deriveAaAccentColor used at runtime, and exits 0 when all clear (non-zero on any failure)'
    - 'The script reads the charcoal/white reference values from tokens.css at runtime (not hardcoded), so it stays correct after the 34-04 value swap'
    - 'CI runs `pnpm stylelint` and `pnpm check-contrast` as required-green steps'
    - '`pnpm knip` does not flag the new script file as unused'
  artifacts:
    - path: 'packages/client/scripts/check-contrast.ts'
      provides: 'tsx CI script asserting all team accents clear AA after derivation'
      contains: 'deriveAaAccentColor'
    - path: '.github/workflows/ci.yml'
      provides: 'stylelint + check-contrast CI gate steps'
      contains: 'pnpm check-contrast'
    - path: 'knip.json'
      provides: 'packages/client scripts entry + project glob'
      contains: 'scripts/check-contrast.ts'
  key_links:
    - from: 'packages/client/scripts/check-contrast.ts'
      to: 'packages/client/src/hooks/useTeamColors.ts'
      via: 'imports the same deriveAaAccentColor + threshold constants (no re-implementation)'
      pattern: "from '\\.\\./src/hooks/useTeamColors"
    - from: '.github/workflows/ci.yml'
      to: 'package.json scripts'
      via: 'pnpm stylelint / pnpm check-contrast run steps'
      pattern: 'pnpm (stylelint|check-contrast)'
---

<objective>
Add the automated WCAG contrast-check CI gate (THEME-04 success criterion #3) and wire both new gates — `pnpm stylelint` (THEME-02) and `pnpm check-contrast` (THEME-04) — into `.github/workflows/ci.yml`. The check-contrast script imports the exact same `deriveAaAccentColor()` and threshold constants the browser runtime uses (34-02), reads the charcoal/white reference values out of `tokens.css` at runtime, and fails the build if any team's derived accent does not clear the policy thresholds. Registering the new script with `knip.json` keeps the existing dead-code CI gate (Phase 32) green.

Purpose: Make THEME-04 continuously enforced in CI with zero drift between what the browser renders and what CI verifies.
Output: `check-contrast.ts`, client `check-contrast` script, updated `knip.json`, updated `ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-visual-theme-restyle/34-RESEARCH.md
@.planning/phases/34-visual-theme-restyle/34-PATTERNS.md
@packages/client/src/hooks/useTeamColors.ts
@packages/shared/scripts/seed-rosters.ts
@.github/workflows/ci.yml
@knip.json
</context>

<artifacts_this_phase_produces>
Symbols/files this plan creates or modifies (source-grounding exclusion list):

- `packages/client/scripts/check-contrast.ts` — NEW tsx CI script
- `packages/client/package.json` scripts: `check-contrast` (= `tsx scripts/check-contrast.ts`) — NEW
- `knip.json` `packages/client` block: adds `scripts/check-contrast.ts` to `entry` and `scripts/**/*.ts` to `project`
- `.github/workflows/ci.yml`: adds `- run: pnpm stylelint` and `- run: pnpm check-contrast` steps
- Consumes (does not create): `deriveAaAccentColor`, `AA_TEXT_MIN_RATIO`, `AA_UI_MIN_RATIO` from 34-02
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create check-contrast.ts + wire client script + knip registration</name>
  <files>packages/client/scripts/check-contrast.ts, packages/client/package.json, knip.json</files>
  <read_first>
    - packages/shared/scripts/seed-rosters.ts (ESM `import.meta.url` + `fileURLToPath`/`dirname`/`join` idiom, header-comment convention, `tsx scripts/x.ts` package-script pattern)
    - packages/client/src/hooks/useTeamColors.ts (the deriveAaAccentColor + AA_TEXT_MIN_RATIO/AA_UI_MIN_RATIO exports to import)
    - packages/client/package.json (scripts block to extend)
    - knip.json (packages/shared block is the exact shape to mirror for packages/client)
    - .planning/phases/34-visual-theme-restyle/34-RESEARCH.md (Code Examples: extractToken regex + contrast loop; Pitfall 4: knip)
  </read_first>
  <action>
    Create `packages/client/scripts/check-contrast.ts` as a tsx-run ESM script following seed-rosters.ts's header-comment + `import.meta.url` path-resolution idiom. It must: import `deriveAaAccentColor`, `AA_TEXT_MIN_RATIO`, and `AA_UI_MIN_RATIO` from `../src/hooks/useTeamColors.js` (the SAME functions/constants the runtime uses — never a re-implementation; RESEARCH.md Don't Hand-Roll), import `hex` from `wcag-contrast`, and import `TEAM_CONFIGS` from `@counter-attack/shared`. Resolve `tokens.css` relative to the script via `fileURLToPath(import.meta.url)` → `../src/styles/tokens.css` (do NOT rely on `process.cwd()`), read it, and regex-extract `--color-bg-page` (charcoal reference) and `--color-text-inverse` (white reference) with an `extractToken(cssText, name)` helper that throws if a token is missing. Reading the values from tokens.css (not hardcoding) is required so the gate stays correct after 34-04 tunes those values.

    Loop over `Object.keys(TEAM_CONFIGS)`: for each team, `raw = TEAM_CONFIGS[teamId].palette.uiColor`, `adjusted = deriveAaAccentColor(raw, bgPage, textInverse)`; if `hex(adjusted, bgPage) < AA_TEXT_MIN_RATIO || hex(adjusted, textInverse) < AA_UI_MIN_RATIO`, `console.error` a `FAIL: <teamId> (<raw> -> <adjusted>)` line and set a `failed` flag. After the loop, `process.exit(1)` if any failed, else print a success summary and exit 0. Use the exported threshold constants — do NOT hardcode 4.5/3.0 literals in the script (single source of truth is 34-02).

    Add `"check-contrast": "tsx scripts/check-contrast.ts"` to `packages/client/package.json` scripts (the root `check-contrast` alias delegating here was already added in 34-01).

    Update `knip.json` `packages/client` block: append `"scripts/check-contrast.ts"` to `entry` and `"scripts/**/*.ts"` to `project` (mirror the existing `packages/shared` block's two-array shape exactly; add array entries only, do not restructure). This prevents the Phase-32 knip gate from flagging the new script as dead code (RESEARCH.md Pitfall 4).

  </action>
  <verify>
    <automated>pnpm check-contrast && pnpm knip</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check-contrast` exits 0 and its output shows every TEAM_CONFIGS team clearing the thresholds.
    - `check-contrast.ts` imports `deriveAaAccentColor`, `AA_TEXT_MIN_RATIO`, `AA_UI_MIN_RATIO` from `../src/hooks/useTeamColors.js` (no local copy of the derivation or the numeric thresholds).
    - The charcoal/white references are regex-extracted from `tokens.css`, not string literals in the script.
    - `pnpm knip` exits 0 (the new script is registered, not flagged unused).
    - `packages/client/package.json` scripts contains `check-contrast`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire stylelint + check-contrast into CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (existing flat `- run:` step list, lines 20-25; no `name:` keys used anywhere)
    - .planning/phases/34-visual-theme-restyle/34-PATTERNS.md (CI step addition convention)
  </read_first>
  <action>
    Append two new sibling steps to the `ci` job's step list, after the existing `- run: pnpm -r build` line, using the exact flat single-line-per-step convention already used for `pnpm knip` (no `name:` keys): `- run: pnpm stylelint` and `- run: pnpm check-contrast`. Keep the existing `- run: pnpm knip` step where it is (it must continue to run and pass now that `knip.json` registers the new script). Do not reorder or rename existing steps. Final order: install → shared build → knip → typecheck → test → build → stylelint → check-contrast.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); if(!/pnpm stylelint/.test(y)||!/pnpm check-contrast/.test(y)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a `- run: pnpm stylelint` step and a `- run: pnpm check-contrast` step, both after `pnpm -r build`.
    - The existing `- run: pnpm knip` step is retained unchanged.
    - No `name:` keys were introduced; step style matches the existing flat convention.
    - Locally, the full gate sequence passes: `pnpm knip && pnpm stylelint && pnpm check-contrast`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
No meaningful attack surface: this plan adds a dev-only CI script and CI config — no auth, session, user input, cryptography, or network runtime code (per 34-RESEARCH.md Security Domain). No package installs occur here (done in 34-01, already gated by the T-34-SC legitimacy checkpoint). No STRIDE threat applicable.
</threat_model>

<verification>
- `pnpm check-contrast` green across all teams.
- `pnpm knip` green (script registered).
- CI yaml contains both new gate steps.
</verification>

<success_criteria>

- THEME-04 continuously enforced by an automated contrast-check that shares the runtime derivation function (no drift).
- THEME-02 + THEME-04 gates both wired into CI as required-green steps.
- Phase-32 knip gate remains green.
  </success_criteria>

<output>
Create `.planning/phases/34-visual-theme-restyle/34-03-SUMMARY.md` when done.
</output>
