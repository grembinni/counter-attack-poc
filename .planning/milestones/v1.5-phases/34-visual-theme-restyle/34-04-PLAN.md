---
phase: 34-visual-theme-restyle
plan: 04
type: execute
wave: 4
depends_on: [34-01, 34-02, 34-03]
files_modified:
  - packages/client/src/styles/tokens.css
  - packages/client/src/components/GameBoard.tsx
autonomous: false
requirements: [THEME-01, THEME-04]
must_haves:
  truths:
    - 'Lobby, settings, team/draft selection, and the in-game board render the charcoal/graphite base with crisp white text — no deep-blue chrome surfaces remain'
    - 'No chrome token in tokens.css carries a blue-channel bias (the residual #808090 tertiary text and #1a56b0 accent fallback are gone)'
    - 'GameBoard injects AA-derived accent values into --team-accent/--home-accent/--away-accent (THEME-04 wiring)'
    - 'The full client test suite, typecheck, build, stylelint, and contrast-check all remain green after the swap'
  artifacts:
    - path: 'packages/client/src/styles/tokens.css'
      provides: 'Charcoal/graphite chrome palette (3-tier bg + borders + text + accent fallback)'
      contains: '--color-bg-page: #121212'
    - path: 'packages/client/src/components/GameBoard.tsx'
      provides: 'Runtime AA-safe accent injection via useTeamAccentColorAA'
      contains: 'useTeamAccentColorAA'
  key_links:
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: 'useTeamAccentColorAA'
      via: '3 accent call sites feed --team-accent/--home-accent/--away-accent'
      pattern: "useTeamAccentColorAA\\(selectedTeams"
    - from: 'packages/client/src/styles/tokens.css'
      to: '17 chrome .module.css files'
      via: 'var(--token) consumption of the new charcoal values'
      pattern: '--color-bg-(page|surface)'
---

<objective>
Perform the actual palette value swap in `tokens.css` (THEME-01) — replacing the deep-blue chrome theme with the broadcast-sports charcoal/graphite base, crisp white text, and a theme-neutral accent fallback — and wire `GameBoard.tsx`'s three accent call sites through the new AA-safe derivation hook `useTeamAccentColorAA` (THEME-04). Because Phase 33 already routed all 17 chrome CSS Modules through `var(--token)`, this is a pure value substitution plus a one-line-per-site hook swap: no `.module.css` structural edits. A human-verify checkpoint confirms the charcoal look across every screen at the end.

Purpose: Land the visible outcome of the phase — the deep-blue theme is gone app-wide, replaced by the contrast-verified charcoal palette.
Output: Recolored `tokens.css`, `GameBoard.tsx` wired to the AA hook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-visual-theme-restyle/34-CONTEXT.md
@.planning/phases/34-visual-theme-restyle/34-UI-SPEC.md
@.planning/phases/34-visual-theme-restyle/34-PATTERNS.md
@packages/client/src/styles/tokens.css
@packages/client/src/components/GameBoard.tsx
</context>

<artifacts_this_phase_produces>
This plan changes existing values/wiring; it introduces no new exported symbols. Modifications (source-grounding exclusion list):

- `tokens.css` value changes: `--color-bg-page` `#1a1a2e`→`#121212`, `--color-bg-surface` `#16213e`→`#1c1c1c`, `--color-bg-surface-alt` `#0f3460`→`#262626`, `--color-border` `#0f3460`→`#262626`, `--color-text-primary` `#e0e0e0`→`#f5f5f5`, `--color-text-tertiary` `#808090`→`#808080`, `--team-accent` `#1a56b0`→`#f5c518`; file-header comment updated to record Phase 34 as the swap phase
- `GameBoard.tsx`: import + 3 call sites changed from `useTeamAccentColor` to `useTeamAccentColorAA`
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Swap tokens.css chrome palette to charcoal/graphite (THEME-01)</name>
  <files>packages/client/src/styles/tokens.css</files>
  <read_first>
    - packages/client/src/styles/tokens.css (current values + comment structure + the extended-token blocks to leave untouched)
    - .planning/phases/34-visual-theme-restyle/34-UI-SPEC.md (Color → "Chrome token value swap" table = the exact target values; "Explicitly unchanged" tables = do-not-touch list)
    - .planning/phases/34-visual-theme-restyle/34-CONTEXT.md (D-01 no-blue-tint hard constraint, D-02 preserve 3-tier structure, D-05 functional colors untouched)
  </read_first>
  <action>
    Edit values in place; preserve the file's `:root` grouping, comment headers, and every extended-token block byte-for-byte except the seven tokens below. Set exactly these new values (per 34-UI-SPEC Color table):
    - `--color-bg-page`: `#121212` (darkest tier, D-02 tier 1)
    - `--color-bg-surface`: `#1c1c1c` (mid tier, D-02 tier 2)
    - `--color-bg-surface-alt`: `#262626` (lightest chrome tier, D-02 tier 3)
    - `--color-border`: `#262626` (preserve the existing "reuses --color-bg-surface-alt's value" reuse pattern + its comment — update the comment's referenced value if it names the old hex)
    - `--color-text-primary`: `#f5f5f5` (crisp near-white, success criterion #1; deliberately short of pure #ffffff to avoid glare on #121212)
    - `--color-text-tertiary`: `#808080` (removes the residual `0x90` blue-channel bias in `#808090` — D-01 hard constraint)
    - `--team-accent` (static pre-team-select fallback only): `#f5c518` (reuse `--color-accent-gold`'s value — removes the saturated blue `#1a56b0` without inventing a new hue; this fallback is only ever visible on Lobby/Settings before a match, since GameBoard overrides it at runtime)

    Do NOT change any other token. Leave unchanged: `--color-border-muted` `#a0a0a0`, `--color-text-secondary` `#a0a0a0`, `--color-text-inverse` `#ffffff`; all D-05 functional colors (`--color-cta-ready-bg`/`-hover`, `--color-cta-pending-bg`/`-hover`, `--color-danger`, `--color-success`/`-hover`); and every ambiguous/functional/extended token (`--color-accent-gold`, `--color-accent-gold-glow`, `--color-accent-purple`, `--color-stat-badge-medium`, `--color-confirm-pending-bg`, `--color-border-subtle`, all `--color-card-*`, `--color-divider-subtle`, `--color-overlay-backdrop`, `--color-banner-backdrop`, `--color-bg-pitch`, and the entire `--color-speed-*` family — the speed picker's blue is a semantic indicator, NOT chrome theme, do not repaint it). Update the file-header comment (currently records Phase 33's freeze rationale) to record that Phase 34 performs the value swap, following the same multi-line `/* ... */` convention.

  </action>
  <verify>
    <automated>pnpm stylelint && node -e "const c=require('fs').readFileSync('packages/client/src/styles/tokens.css','utf8'); const need=['--color-bg-page: #121212','--color-bg-surface: #1c1c1c','--color-bg-surface-alt: #262626','--color-text-primary: #f5f5f5','--color-text-tertiary: #808080']; const bad=['#1a1a2e','#16213e','#0f3460','#1a56b0','#808090']; if(need.some(n=>!c.includes(n))||bad.some(b=>c.includes(b))) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - tokens.css contains all seven new values exactly as listed and none of the old blue literals `#1a1a2e`, `#16213e`, `#0f3460`, `#1a56b0`, `#808090` remain anywhere in the file.
    - The 3-tier background structure and all comment-headed groups are intact (D-02 not flattened).
    - All D-05 functional colors and the `--color-speed-*` family are byte-for-byte unchanged.
    - `pnpm stylelint` still exits 0 (tokens.css is outside the lint glob; the run confirms no chrome .module.css regressed).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire GameBoard accent injection through the AA hook (THEME-04)</name>
  <files>packages/client/src/components/GameBoard.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (import line 7; the three call sites at lines 180, 181, 185; the rootStyle CSS-var injection at lines 224-228)
    - packages/client/src/hooks/useTeamColors.ts (the `useTeamAccentColorAA` export from 34-02)
    - .planning/phases/34-visual-theme-restyle/34-PATTERNS.md (GameBoard.tsx pattern — swap hook, no structural change; do NOT touch ActionLog.tsx)
  </read_first>
  <action>
    Change the import on line 7 to bring in `useTeamAccentColorAA` (either replacing `useTeamAccentColor` or adding it — `useTeamAccentColor` may become unused in this file, in which case drop it from the import to keep the no-unused-vars lint green). Change the three call sites: `const homeColor = useTeamAccentColorAA(selectedTeams['home'])`, `const awayColor = useTeamAccentColorAA(selectedTeams['away'])`, and `const teamColor = useTeamAccentColorAA(selectedTeams[activeTeam])`. These feed the existing `rootStyle` object's `--team-accent`/`--home-accent`/`--away-accent` custom properties unchanged. Keep the hook calls in the component body only (never inside `.map()`/conditionals — Rules of Hooks). Update the adjacent comment block's decision-ID reference to note the THEME-04 AA-derivation change where accurate; do not alter `resultText`/`resultColor`/`rootStyle` structure otherwise. Do NOT apply this swap to `ActionLog.tsx` — its `teamAccentColor()` calls stay raw/unadjusted (D-04 carve-out).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client typecheck && pnpm --filter @counter-attack/client test && pnpm -r build && pnpm check-contrast</automated>
  </verify>
  <acceptance_criteria>
    - GameBoard.tsx's three accent resolutions call `useTeamAccentColorAA(...)`; no `useTeamAccentColor(` call remains in GameBoard.tsx.
    - The import line resolves with no unused-import lint error (`pnpm lint` clean for GameBoard.tsx).
    - `pnpm --filter @counter-attack/client typecheck`, `pnpm --filter @counter-attack/client test`, and `pnpm -r build` all pass.
    - `pnpm check-contrast` still exits 0.
    - ActionLog.tsx is unchanged (still imports/uses raw `teamAccentColor`).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification — charcoal theme across all screens</name>
  <action>After Tasks 1-2 land and all automated gates are green, pause for human visual confirmation of the charcoal theme across lobby, settings, team/draft selection, and the in-game board. Do not mark the phase complete until the human returns "approved". This is the only THEME-01 property with no automated surface (a perceptual "does it read charcoal, no blue chrome" check).</action>
  <what-built>
    tokens.css now carries the charcoal/graphite chrome palette and GameBoard injects AA-safe team accents. All automated gates (stylelint, contrast-check, tests, typecheck, build) are green. This checkpoint is the one property with no automated surface: whether the app actually reads as a neutral broadcast-sports charcoal theme with no residual blue chrome (THEME-01 success criterion #1 — a perceptual property).
  </what-built>
  <how-to-verify>
    1. Run the client dev server (`pnpm --filter @counter-attack/client dev`) and open the app.
    2. Lobby / landing: confirm the base is neutral charcoal (not deep blue), text is crisp near-white, and the pre-team accent (gold `#f5c518`) shows where an accent appears — no `#1a56b0` blue anywhere.
    3. Game settings screen: same charcoal base, no blue chrome panels/borders.
    4. Team/draft selection + uniform selection: chrome is charcoal; per-team brand swatches (intentionally multi-colored, driven by inline styles, out of scope) may still show brand colors — that is expected and correct.
    5. In-game board (start or resume a match): top band, panels, ActionPanel/ActionLog surfaces all charcoal; the active team's accent (team name/score text) is readable against the charcoal base; hover a CTA button to confirm its team-accent hover background reads acceptably with white text.
    6. Confirm no screen still shows the old deep-blue (`#1a1a2e`/`#16213e`/`#0f3460`) surfaces.
  </how-to-verify>
  <resume-signal>Type "approved" if every screen reads charcoal with no blue chrome, or describe which surface still looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
No meaningful attack surface: this plan changes CSS custom-property values and swaps a hook call — no auth, session, user input, cryptography, or network runtime code (per 34-RESEARCH.md Security Domain). No package installs. No STRIDE threat applicable.
</threat_model>

<verification>
- `pnpm -r typecheck && pnpm -r test && pnpm -r build` green.
- `pnpm stylelint && pnpm check-contrast` green.
- No old blue chrome literals remain in tokens.css.
- Human-verify checkpoint approved.
</verification>

<success_criteria>

- Deep-blue chrome theme replaced app-wide by the charcoal/graphite palette (THEME-01).
- GameBoard accent injection routed through the AA-safe derivation (THEME-04 end-to-end wiring).
- Full automated suite + human visual check all green.
  </success_criteria>

<output>
Create `.planning/phases/34-visual-theme-restyle/34-04-SUMMARY.md` when done.
</output>
