---
phase: 35-actionpanel-log-standardization
plan: 04
type: execute
wave: 2
depends_on: ['35-01']
files_modified:
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/ActionPanel.module.css
  - packages/client/src/components/ActionPanel.test.tsx
  - packages/client/src/components/GameBoard.test.tsx
autonomous: true
requirements: [PANEL-01, PANEL-02, PANEL-04]
must_haves:
  truths:
    - 'ActionPanel shows a heading in every phase state, so all four panels sharing its slot present the same structural pattern'
    - 'ActionPanel''s confirm-and-advance button reads "Confirm" in every phase'
    - "ActionPanel's confirm dialog is not framed by a border"
    - 'ActionPanel never calls the goalkeeper "Goalie" or "Goalkeeper"'
  artifacts:
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'PanelShell heading wrapper, Confirm verb on every CTA, Keeper terminology'
      contains: 'PanelShell'
    - path: 'packages/client/src/components/ActionPanel.module.css'
      provides: 'panelHeading class matching sibling panels; border-free confirmCard'
      contains: '.panelHeading'
    - path: 'packages/client/src/components/ActionPanel.test.tsx'
      provides: 'Assertions locking the heading, the Confirm verb, and Keeper wording'
  key_links:
    - from: 'packages/client/src/components/ActionPanel.tsx'
      to: 'packages/client/src/components/ActionPanel.module.css'
      via: 'PanelShell renders styles.panelHeading inside styles.panel'
      pattern: 'styles\\.panelHeading'
    - from: 'packages/client/src/components/GameBoard.test.tsx'
      to: 'packages/client/src/components/ActionPanel.tsx'
      via: 'GameBoard integration test queries the renamed Confirm CTA'
      pattern: 'confirm'
---

<objective>
Give `ActionPanel` the structural heading its three slot siblings already have (D-07), unify
its confirm-and-advance button verb to "Confirm" across every phase (D-08), remove the
`.confirmCard` container border (D-01), and standardize its goalkeeper wording to "Keeper"
(D-03).

Purpose: `ActionPanel` is the only panel in the D-05 render-slot set with no heading at all,
while `KickOffSetupPanel` ("Kick-Off Setup"), `FreeKickSetupPanel` ("Offside — Free Kick"),
and `ReplayPanel` ("Replay") each show one — a structural inconsistency directly under
PANEL-01. It is also the last holder of the "End Turn" verb (PANEL-04) and of the last
container border in the area (PANEL-02).

Output: a single `PanelShell` wrapper that makes the heading impossible to omit from any of
ActionPanel's 18 render sites, one canonical CTA verb, one border removed, and two
goalkeeper strings corrected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-actionpanel-log-standardization/35-CONTEXT.md
@.planning/phases/35-actionpanel-log-standardization/35-PATTERNS.md
@.planning/phases/35-actionpanel-log-standardization/35-01-SUMMARY.md
</context>

## Artifacts this phase produces

Consumed by **this plan** (created in plan 35-01): the `ctaClass` adapter and
`ctaColorClass` import already present in `ActionPanel.tsx`.

Created by **this plan**:

| Symbol                            | Location                                                | Kind                             |
| --------------------------------- | ------------------------------------------------------- | -------------------------------- |
| `ACTION_PANEL_HEADING`            | `packages/client/src/components/ActionPanel.tsx`        | module-scope const (`'Actions'`) |
| `PanelShell({ wide?, children })` | `packages/client/src/components/ActionPanel.tsx`        | module-scope local component     |
| `.panelHeading`                   | `packages/client/src/components/ActionPanel.module.css` | CSS class                        |

Removed by **this plan**:

| Symbol                                                        | Location                                                           |
| ------------------------------------------------------------- | ------------------------------------------------------------------ |
| `border: 1px solid var(--color-border)` on `.confirmCard`     | `packages/client/src/components/ActionPanel.module.css` (line 128) |
| `'End Turn'` key in `ACTION_SUMMARY` (renamed to `'Confirm'`) | `packages/client/src/components/ActionPanel.tsx` (line 37)         |

Sibling plans in this phase produce: `ctaColorClass` (plan 35-01);
`RESULT_LABEL` / `SHOT_OUTCOME_LABEL` / `HEADER_RESULT_LABEL` (plan 35-02);
`.panelHeading` in `ReplayPanel.module.css` (plan 35-03). Plan 35-05 adds
`waitingHelperBlock` / `waitingPanel(detail)` / `actingSideLabel` on top of `PanelShell`.

<tasks>

<task type="auto">
  <name>Task 1: Add a panelHeading class, remove the confirmCard border, and route every render site through PanelShell (D-01, D-07)</name>
  <files>packages/client/src/components/ActionPanel.module.css, packages/client/src/components/ActionPanel.tsx, packages/client/src/components/ActionPanel.test.tsx</files>
  <read_first>
    - `packages/client/src/components/FreeKickSetupPanel.module.css` (lines 1-38) — the exact
      `.panelHeading` declarations to replicate (`font-size: 12px; font-weight: 700;
      color: var(--color-text-primary); text-align: center;`) and the `flex: 0 0 100%`
      full-width group it belongs to. Copy these values; do not invent new ones.
    - `packages/client/src/components/KickOffSetupPanel.tsx` (line 91) and
      `packages/client/src/components/FreeKickSetupPanel.tsx` (lines 75, 178) — the
      `<span className={styles.panelHeading}>…</span>` structural element to imitate, always
      rendered as the panel's first child.
    - `packages/client/src/components/ActionPanel.module.css` (whole file, 177 lines) — the
      full-width group at lines 13-18, the `.confirmCard` border at line 128, and confirmation
      that `.backButton`'s `var(--color-border)` border at line 58 is a BUTTON border that D-01
      explicitly exempts.
    - `packages/client/src/components/ActionPanel.tsx` — post-plan-35-01 state. Every
      `<div className={styles.panel}>` occurrence (17 after plan 35-01 deletes the two inside
      the FREE_KICK_SETUP block) plus the one composed
      `<div className={`${styles.panel} ${actionCount >= 5 ? styles.wide : ''}`}>` in the
      PASS/KICK_OFF chooser. Confirm the counts yourself with
      `grep -c '<div className={styles.panel}>' packages/client/src/components/ActionPanel.tsx`
      before starting.
    - `packages/client/src/components/ActionPanel.module.css` (lines 108-111) — `.wide .ctaButton`
      proves `.wide` is applied to the panel element itself, which is why `PanelShell` needs a
      `wide` prop rather than a wrapper div.
  </read_first>
  <action>
    In `packages/client/src/components/ActionPanel.module.css`:
    1. Add a `.panelHeading` rule with the same four declarations
       `FreeKickSetupPanel.module.css` uses, and add `.panelHeading` to the existing full-width
       selector group so it reads `.panelHeading, .phaseLabel, .gkLabel, .errorText { flex: 0 0 100%; }`.
       Placing it in that group is required: `.panel` is a `flex-wrap: wrap` row, so without
       `flex: 0 0 100%` the heading would sit inline beside the first button.
    2. Remove the `border: 1px solid var(--color-border);` declaration from `.confirmCard`
       (line 128), keeping `background`, `border-radius`, `padding`, `display`,
       `flex-direction`, `gap`, `max-width`, and `width` byte-for-byte. Do NOT touch
       `.ctaButton`'s border (line 26) or `.backButton`'s border (line 58) — D-01 exempts
       buttons, and `.backButton` is the documented case of a button that happens to use
       `var(--color-border)`.

    In `packages/client/src/components/ActionPanel.tsx`:
    3. Add a module-scope const `const ACTION_PANEL_HEADING = 'Actions';` with a docblock
       naming D-07 and recording the discretionary choice: a static `'Actions'` label is used
       rather than a phase-derived one because each phase block already carries its own
       contextual title in `helperLine1` (`Move!`, `Attempt Save!`, `Kick-Off!`, …); a
       phase-derived heading would duplicate that line, whereas `'Actions'` reads naturally
       above all of the roughly fifteen phase states and gives the panel the same
       heading-then-content structure its three siblings have.
    4. Add a module-scope local component:
       `function PanelShell({ wide = false, children }: { wide?: boolean; children: React.ReactNode })`
       which renders a single `div` whose className is `styles.panel` plus `styles.wide` when
       `wide` is true, with `<span className={styles.panelHeading}>{ACTION_PANEL_HEADING}</span>`
       as its first child followed by `{children}`. Import the `ReactNode` type from `react`
       (this file already imports from `react`). Preserve the existing className composition
       semantics exactly: today the chooser produces `styles.panel` plus either `styles.wide` or
       an empty string, so `PanelShell` must not emit a stray trailing space that would change
       any `className` snapshot — build it so a non-wide shell yields exactly `styles.panel`.
    5. Replace all 17 `<div className={styles.panel}>` … `</div>` render sites with
       `<PanelShell>` … `</PanelShell>`, and replace the composed chooser site with
       `<PanelShell wide={actionCount >= 5}>` … `</PanelShell>`. The sites are: the
       `waitingPanel` const, `HIGH_PASS_MOVE`, `FIRST_TIME_PASS_MOVE`, `GK_DIVE`,
       `SNAPSHOT_DEFLECT`, HEADER's duel-won branch, HEADER's contest branch,
       `SNAPSHOT_TARGET`, `GK_RESTART`, `GK_QUICK_THROW`, `GK_KICK_TARGET`, `GK_KICK_MOVE`,
       the shared `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` block, the PASS loose-ball branch, the
       PASS/KICK_OFF chooser (the `wide` one), the PASS step-2 target prompt, `MOVE`, and the
       final error-only fallback. Every one of them gets a heading — there is no exception.
    6. Do NOT wrap `confirmOverlay` or `confirmCard` in `PanelShell` — the confirm dialog is a
       modal, not a slot panel, and must not gain an "Actions" heading. Leave its JSX shape
       untouched in this task.

    In `packages/client/src/components/ActionPanel.test.tsx` add a
    `describe('ActionPanel — D-07: every phase state renders the panel heading', …)` that
    renders the panel in at least six representative states — the waiting panel (non-active
    player), `MOVE`, `GK_RESTART`, `HEADER` contest, the `PASS` chooser, and the `PASS` step-2
    target prompt — and in each asserts `screen.getByText('Actions')` is defined. Add one test
    asserting the heading is NOT rendered inside the confirm dialog's card by opening the
    dialog (click the CTA with remaining > 0) and asserting `screen.getAllByText('Actions')`
    still has length 1.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- ActionPanel && pnpm stylelint && pnpm --filter @counter-attack/client typecheck && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '<div className={styles.panel}>' packages/client/src/components/ActionPanel.tsx` returns `0`.
    - Source assertion: `grep -c 'styles.panel}' packages/client/src/components/ActionPanel.tsx` returns `1` — the only remaining reference is inside `PanelShell`.
    - Source assertion: `grep -c '<PanelShell' packages/client/src/components/ActionPanel.tsx` returns `18`, and `grep -c 'wide={actionCount >= 5}' packages/client/src/components/ActionPanel.tsx` returns `1`.
    - Source assertion: `grep -c 'styles.panelHeading' packages/client/src/components/ActionPanel.tsx` returns `1` — the heading is emitted from exactly one place, so it cannot drift per phase.
    - Source assertion: `grep -c '^\.panelHeading' packages/client/src/components/ActionPanel.module.css` returns `1`, and `grep -c 'panelHeading,' packages/client/src/components/ActionPanel.module.css` returns `1` (it joined the full-width group).
    - Source assertion: `grep -c 'border: 1px solid var(--color-border);' packages/client/src/components/ActionPanel.module.css` returns `1` — only `.backButton`'s exempt button border remains; `.confirmCard`'s is gone. Confirm the surviving one is `.backButton` by inspecting the match context.
    - Behavior assertion (test): `screen.getByText('Actions')` resolves in all six tested phase states.
    - Behavior assertion (test): with the confirm dialog open, exactly one `Actions` heading is in the DOM.
    - `pnpm stylelint`, `pnpm --filter @counter-attack/client typecheck`, and `pnpm -r build` exit 0.
  </acceptance_criteria>
  <done>Every ActionPanel render site emits the heading through a single `PanelShell`, `.panelHeading` matches its siblings' declarations, and `.confirmCard` is border-free.</done>
</task>

<task type="auto">
  <name>Task 2: Unify the confirm-and-advance verb to "Confirm" across every ActionPanel phase (D-08)</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/ActionPanel.test.tsx, packages/client/src/components/GameBoard.test.tsx</files>
  <read_first>
    - `packages/client/src/components/ActionPanel.tsx` — post-Task-1 state. Every `End Turn`
      button label (6 after plan 35-01's FREE_KICK_SETUP deletion: `HIGH_PASS_MOVE`,
      `FIRST_TIME_PASS_MOVE`, `SNAPSHOT_DEFLECT`, `GK_KICK_MOVE`, the shared `FREE_MOVE_*`
      block, and `MOVE`), the `ACTION_SUMMARY` map at lines 26-38, the HEADER contest button's
      `Confirm Selection` / `Decline (no contestant)` ternary, and the `confirmDialog` affirm
      button currently labelled `Confirm`.
    - `.planning/phases/35-actionpanel-log-standardization/35-03-SUMMARY.md` if it exists (this
      plan's sibling in the same wave) — `FreeKickSetupPanel` makes the identical
      CTA-`Confirm` / dialog-affirm-`Yes, end turn` split. Match its wording exactly so the two
      dialogs read the same. If the SUMMARY is not yet written, follow the wording specified
      below, which is what 35-03 was planned against.
    - `packages/client/src/components/ActionPanel.test.tsx` — every
      `screen.getByRole('button', { name: /end turn/i })` query (at minimum lines 143, 229,
      244, 256, and the four in the BUG-25/BUG-31 blocks plus the D-02 block added by plan
      35-01), and the dialog-affirm query `{ name: /^confirm$/i }` at line 261 which must be
      requeried once the CTA also reads `Confirm`.
    - `packages/client/src/components/GameBoard.test.tsx` (lines 174-182) — the integration
      assertion `expect(screen.getByText(/End Turn/i)).toBeDefined();` that this rename breaks.
  </read_first>
  <action>
    In `packages/client/src/components/ActionPanel.tsx`:
    1. Change all 6 remaining phase CTA button labels from `End Turn` to `Confirm`. Do not
       change any button's `onClick`, `disabled`, `title`, or `className` composition — plan
       35-01 already set the color logic and it must be preserved.
    2. Rename the `ACTION_SUMMARY` key `'End Turn'` to `'Confirm'` and reword its tooltip to
       `'Confirm your actions and end your turn, passing control to the opponent.'` Update all
       6 `title={ACTION_SUMMARY['End Turn']}` references to `title={ACTION_SUMMARY['Confirm']}`.
       Leave every other `ACTION_SUMMARY` entry untouched in this task (`Punt (High Pass)` and
       `Quick Throw` are reworded in Task 3).
    3. Change the HEADER contest button's positive label from `'Confirm Selection'` to
       `'Confirm'`, keeping the `'Decline (no contestant)'` negative branch and the
       `headerContestantIds.length > 0` condition unchanged. `Confirm Selection` is the same
       confirm-and-advance action under a longer name; collapsing it to `Confirm` is what makes
       the verb genuinely single-valued across the panel.
    4. Change the `confirmDialog` affirm button's label from `Confirm` to `Yes, end turn`,
       keeping `Cancel` unchanged. This is mandatory, not cosmetic: with the CTAs now reading
       `Confirm`, leaving the modal affirm as `Confirm` would put two identically-named buttons
       in the DOM whenever the dialog is open, making the UI ambiguous and every
       accessible-name query non-deterministic. `Yes, end turn` directly answers the dialog's
       own prompt ("…are you sure you want to end your turn?") and matches the wording
       `FreeKickSetupPanel` adopts in plan 35-03.

    In `packages/client/src/components/ActionPanel.test.tsx`: replace every
    `screen.getByRole('button', { name: /end turn/i })` with
    `screen.getByRole('button', { name: /^confirm$/i })` — the `^…$` anchors are required so
    the query cannot also match `Yes, end turn` or `Decline (no contestant)`. Change the
    dialog-affirm query from `{ name: /^confirm$/i }` to `{ name: /yes, end turn/i }`. Update
    `it` descriptions mentioning "End Turn" to say "Confirm". Then add a
    `describe('ActionPanel — D-08: single Confirm verb', …)` asserting:
    - in `MOVE` phase, `screen.queryByRole('button', { name: /end turn/i })` is `null` and
      `screen.getByRole('button', { name: /^confirm$/i })` is defined;
    - in `HEADER` contest with a contestant selected, the button reads exactly `Confirm` (not
      `Confirm Selection`);
    - in `HEADER` contest with no contestant selected, the button still reads
      `Decline (no contestant)`;
    - with the confirm dialog open in `MOVE` phase, the DOM contains buttons named `Cancel`,
      `Yes, end turn`, and `Confirm` (the CTA behind the overlay), and clicking `Yes, end turn`
      calls `emitEndTurn`.

    In `packages/client/src/components/GameBoard.test.tsx`: change
    `expect(screen.getByText(/End Turn/i)).toBeDefined();` to
    `expect(screen.getByRole('button', { name: /^confirm$/i })).toBeDefined();` and update the
    `it` description and its inline comment from "End Turn button" to "Confirm button". Change
    nothing else in that file — `GameBoard.module.css` is owned by plan 35-03 and
    `GameBoard.tsx` is not modified in this phase.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- ActionPanel && pnpm --filter @counter-attack/client test -- GameBoard && pnpm --filter @counter-attack/client typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "End Turn" packages/client/src/components/ActionPanel.tsx` returns `0`.
    - Source assertion: `grep -c "Confirm Selection" packages/client/src/components/ActionPanel.tsx` returns `0`.
    - Source assertion: `grep -c "ACTION_SUMMARY\['Confirm'\]" packages/client/src/components/ActionPanel.tsx` returns `6`, and `grep -c "ACTION_SUMMARY\['End Turn'\]" packages/client/src/components/ActionPanel.tsx` returns `0`.
    - Source assertion: `grep -c "Yes, end turn" packages/client/src/components/ActionPanel.tsx` returns `1`.
    - Source assertion: `grep -c "/end turn/i" packages/client/src/components/ActionPanel.test.tsx` returns `1` — the single remaining occurrence is the negative `queryByRole` assertion in the new D-08 block.
    - Source assertion: `grep -c "End Turn" packages/client/src/components/GameBoard.test.tsx` returns `0`.
    - Behavior assertion (test): in `MOVE` phase no button is named `End Turn` and exactly one is named `Confirm`.
    - Behavior assertion (test): with the dialog open, `Cancel`, `Yes, end turn`, and `Confirm` are all present and unambiguous; clicking `Yes, end turn` calls `emitEndTurn`.
    - Behavior assertion (test): the BUG-25, BUG-31, and plan-35-01 D-02 color tests all still pass after requerying by `/^confirm$/i` — the color-state behavior set in plan 35-01 is unchanged.
    - `pnpm lint` and `pnpm --filter @counter-attack/client typecheck` exit 0.
  </acceptance_criteria>
  <done>Every confirm-and-advance button in ActionPanel reads `Confirm`; the modal affirm reads `Yes, end turn`; no `End Turn` string remains in ActionPanel or its GameBoard integration test.</done>
</task>

<task type="auto">
  <name>Task 3: Standardize ActionPanel goalkeeper wording to "Keeper" (D-03)</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/ActionPanel.test.tsx</files>
  <read_first>
    - `packages/client/src/components/ActionPanel.tsx` (lines 26-38 `ACTION_SUMMARY`) — the two
      tooltip strings using "Goalkeeper".
    - `packages/client/src/components/ActionPanel.tsx` (the `GK_RESTART` block, around line 512)
      — the `helperLine1` reading `Goalie Restart!`.
    - `packages/client/src/components/ActionPanel.tsx` — scan the whole file for any other
      user-facing occurrence of `Goalie`, `Goalkeeper`, or a bare `GK` rendered as text (as
      opposed to identifiers like `isGKTeamPlayer`, `gkPiece`, `gkTeam`, `GK_RESTART`,
      `GK_DIVE`, which are code and stay).
    - `.planning/phases/35-actionpanel-log-standardization/35-02-SUMMARY.md` if it exists —
      plan 35-02 made the matching ActionLog-side changes (`prefix="K"`, "keeper out of
      range"); align wording so both components read the same. If it is not yet written,
      follow the wording below.
    - `packages/client/src/components/ActionPanel.test.tsx` (lines 415-460) — the existing
      `GK_RESTART` tests, including the `{ name: /punt \(high pass\)/i }` button query, so you
      know which assertions exist for that block.
  </read_first>
  <action>
    Three edits in `packages/client/src/components/ActionPanel.tsx`, all user-facing strings only:
    1. `GK_RESTART` block's `helperLine1`: `Goalie Restart!` → `Keeper Restart!`. Keep the
       trailing `!` — it matches the phase's own title-line convention (`Attempt Save!`,
       `Quick Throw!`, `Punt!`, `Move!`).
    2. `ACTION_SUMMARY['Punt (High Pass)']`: `'Goalkeeper clears with a long kick.'` →
       `'Keeper clears with a long kick.'`
    3. `ACTION_SUMMARY['Quick Throw']`: `'Goalkeeper throws the ball back into play.'` →
       `'Keeper throws the ball back into play.'`

    Do NOT rename the button labels `Punt (High Pass)`, `Quick Throw`, or `Move` in the
    `GK_RESTART` block — they name the action, not the actor, and the `ACTION_SUMMARY` keys are
    those labels, so renaming a key would silently break its `title` lookup.

    Do NOT rename any identifier: `isGKTeamPlayer`, `gkPiece`, `gkTeam`, `gkPieceForRestart`,
    `gkTeamForRestart`, `emitGKRestart`, `gkKickMovedPieceId`, `gkmEligibleRemaining`, and the
    `GK_RESTART` / `GK_DIVE` / `GK_QUICK_THROW` / `GK_KICK_TARGET` / `GK_KICK_MOVE` phase
    literals are code and protocol values, not user-facing text. D-03 governs displayed
    strings only.

    Per D-03's scope guard, do NOT touch `PlayerStatsPanel` or any component outside
    `ActionPanel.tsx` / `ActionLog.tsx`, even if it uses "GK" — that is explicitly out of scope
    for this phase.

    If the whole-file scan turns up an additional user-facing `Goalie` / `Goalkeeper` / bare-`GK`
    string beyond the three above, fix it under the same rule and record it in the SUMMARY.

    In `packages/client/src/components/ActionPanel.test.tsx` add a
    `describe('ActionPanel — D-03: Keeper terminology', …)` asserting:
    - in `GK_RESTART` for the keeper's team, `screen.getByText('Keeper Restart!')` is defined and
      `screen.queryByText(/Goalie/i)` is `null`;
    - the `Punt (High Pass)` button's `title` attribute matches `/^Keeper clears/` and does not
      match `/Goalkeeper/`;
    - the `Quick Throw` button's `title` attribute matches `/^Keeper throws/` and does not match
      `/Goalkeeper/`.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- ActionPanel && pnpm --filter @counter-attack/client test && pnpm --filter @counter-attack/client typecheck && pnpm lint && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -ci "goalie" packages/client/src/components/ActionPanel.tsx` returns `0`.
    - Source assertion: `grep -c "Goalkeeper" packages/client/src/components/ActionPanel.tsx` returns `0`.
    - Source assertion: `grep -c "Keeper Restart!" packages/client/src/components/ActionPanel.tsx` returns `1`.
    - Source assertion: `grep -c "'Keeper clears with a long kick.'" packages/client/src/components/ActionPanel.tsx` returns `1` and `grep -c "'Keeper throws the ball back into play.'" packages/client/src/components/ActionPanel.tsx` returns `1`.
    - Source assertion: `grep -c "emitGKRestart" packages/client/src/components/ActionPanel.tsx` is unchanged from its pre-task value — no identifier was renamed.
    - Source assertion: `grep -rci "goalie\|goalkeeper" packages/client/src/components/ActionLog.tsx packages/client/src/components/ActionPanel.tsx` returns `0` for both files, confirming the two-component scope of D-03 is fully satisfied.
    - Behavior assertion (test): the `GK_RESTART` panel renders `Keeper Restart!` and no `Goalie` text; both keeper action buttons carry Keeper-worded tooltips.
    - Full client suite, `pnpm lint`, `pnpm --filter @counter-attack/client typecheck`, and `pnpm -r build` all exit 0.
  </acceptance_criteria>
  <done>ActionPanel's user-facing goalkeeper wording is "Keeper" everywhere, no identifier was renamed, and the two-file D-03 scope (ActionPanel + ActionLog) is provably clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                                                                                                                                               |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client→server socket emit | The renamed CTAs still call the same `emitEndTurn` / `emitHeaderContestant` / `emitGKRestart` store actions with the same arguments. Only labels, tooltips, and one wrapper element change; no emit signature is touched. |
| server→client game state  | No new `gameState` field is read and no phase gate is altered.                                                                                                                                                            |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------- | ---------------------- | ------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-35-10   | Elevation of Privilege | `PanelShell` refactor in `ActionPanel.tsx` | mitigate    | Replacing 18 render-site wrappers risks accidentally relocating a phase's `!isActivePlayer` early return or `myTeam === null` guard, which would render another player's controls. Task 1 changes only the wrapping element — the phase conditionals, `isActivePlayer` / `isGKTeamPlayer` / `isDefendingTeamPlayer` guards, and early returns are untouched. The full pre-existing UNDO-03 active-player-gating suite plus the per-phase waiting-panel tests re-run in this task's verify command and must all pass. Server-side handlers remain the authoritative gate. |
| T-35-11   | Spoofing               | HEADER contest button rename               | mitigate    | Collapsing `Confirm Selection` to `Confirm` must not change which team may confirm or what payload is sent. Task 2 explicitly preserves the `headerContestantIds.length > 0` condition, the `withEndTurnConfirm` wrapper, and the `emitHeaderContestant(headerContestantIds)` call. A test asserts the `Decline (no contestant)` branch still renders when no contestant is selected.                                                                                                                                                                                    |
| T-35-12   | Tampering              | `ActionPanel.module.css`                   | accept      | Adding a `.panelHeading` rule and removing a `border` declaration cannot affect behavior or authorization. `pnpm stylelint` gates the added declarations against hardcoded color literals; `pnpm -r build` proves the new class resolves.                                                                                                                                                                                                                                                                                                                                |
| T-35-SC   | Tampering              | npm/pnpm installs                          | accept      | No package is added, removed, or upgraded in this plan — `package.json` and `pnpm-lock.yaml` are not in `files_modified`. No Package Legitimacy Gate applies.                                                                                                                                                                                                                                                                                                                                                                                                            |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test` (full client suite) passes, including the GameBoard integration test requeried for `Confirm`.
- `pnpm --filter @counter-attack/client typecheck` exits 0.
- `pnpm lint` exits 0.
- `pnpm stylelint` exits 0.
- `pnpm -r build` exits 0.
- `grep -c '<div className={styles.panel}>' packages/client/src/components/ActionPanel.tsx` returns `0`.
- `grep -c 'styles.panelHeading' packages/client/src/components/ActionPanel.tsx` returns `1`.
- `grep -cE "End Turn|Confirm Selection|Goalie|Goalkeeper" packages/client/src/components/ActionPanel.tsx` returns `0`.
- `grep -c 'border: 1px solid var(--color-border);' packages/client/src/components/ActionPanel.module.css` returns `1` (the exempt `.backButton`).
</verification>

<success_criteria>

- All 18 ActionPanel render sites go through `PanelShell` and therefore show the `Actions` heading; the heading string and its class are each written exactly once in the file.
- `.panelHeading` exists in `ActionPanel.module.css` with the same declarations as its siblings and belongs to the `flex: 0 0 100%` group.
- `.confirmCard` has no border; `.ctaButton` and `.backButton` borders are retained per D-01's button exemption.
- Every confirm-and-advance button in ActionPanel reads `Confirm`; the modal affirm reads `Yes, end turn`; `End Turn` and `Confirm Selection` no longer appear.
- No `Goalie` or `Goalkeeper` string remains in `ActionPanel.tsx`; combined with plan 35-02, both D-03-scoped files are clean.
- Full client test suite, typecheck, lint, stylelint, and build are green, including every pre-existing active-player-gating and CTA-color test.
  </success_criteria>

<output>
Create `.planning/phases/35-actionpanel-log-standardization/35-04-SUMMARY.md` when done.
</output>
