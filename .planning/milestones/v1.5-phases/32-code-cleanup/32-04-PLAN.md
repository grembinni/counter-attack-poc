---
phase: 32-code-cleanup
plan: 04
type: execute
wave: 2
depends_on: ['32-01', '32-02']
files_modified:
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/FreeKickSetupPanel.tsx
  - packages/client/src/components/KickOffSetupPanel.tsx
  - packages/client/src/store/useGameStore.ts
autonomous: true

requirements: [CLEANUP-02]

must_haves:
  truths:
    - 'The null-safe inline `myTeam` derivations in HexGrid.tsx (line 134) and ActionPanel.tsx (line 118) are replaced by `useMyTeam()` — no inline `playerSlot === 1 ? ...` remains in either component.'
    - 'The 7 non-null-safe inline `myTeam` derivations in useGameStore.ts (lines 400/435/494/528/566/598/620) are replaced by `deriveMyTeam(playerSlot)` (the PURE fn — hooks are illegal in store action bodies, Pitfall 2), each with an explicit, recorded null-handling decision (Pitfall 4).'
    - "FreeKickSetupPanel.tsx (line 53) and KickOffSetupPanel.tsx (line 28) migrate from the non-null-safe `'home' | 'away'` form to the canonical helper, with the new `null` case explicitly guarded or documented (Pitfall 4) — never a silent `?? 'away'` coercion."
    - 'ALL myTeam/team-slot duplication call sites named in D-04 are consolidated (not just the worst offenders).'
  artifacts:
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: 'myTeam via useMyTeam()'
      contains: 'useMyTeam'
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'myTeam via useMyTeam()'
      contains: 'useMyTeam'
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: '7 inline myTeam derivations replaced by deriveMyTeam(playerSlot)'
      contains: 'deriveMyTeam'
    - path: 'packages/client/src/components/FreeKickSetupPanel.tsx'
      provides: 'Canonical myTeam derivation with explicit null handling'
    - path: 'packages/client/src/components/KickOffSetupPanel.tsx'
      provides: 'Canonical myTeam derivation with explicit null handling'
  key_links:
    - from: 'HexGrid.tsx / ActionPanel.tsx'
      to: 'hooks/useMyTeam.ts'
      via: 'import { useMyTeam }'
      pattern: "import.*useMyTeam.*from '\\.\\./hooks/useMyTeam"
    - from: 'useGameStore.ts action bodies'
      to: 'hooks/useMyTeam.ts deriveMyTeam'
      via: 'deriveMyTeam(playerSlot) — pure fn, callable outside React'
      pattern: 'deriveMyTeam\\('
---

<objective>
CLEANUP-02 (part 3 of 3): Consolidate every remaining `myTeam` / team-slot derivation onto Plan 32-02's canonical helper. Component bodies (`HexGrid.tsx`, `ActionPanel.tsx`) use the `useMyTeam()` hook; the Zustand store's action bodies (`useGameStore.ts`, 7 sites) MUST use the pure `deriveMyTeam(playerSlot)` because hooks are uncallable there (Pitfall 2); and the two non-null-safe panels (`FreeKickSetupPanel.tsx`, `KickOffSetupPanel.tsx`) migrate to the canonical null-safe form with an explicit, recorded decision for the newly-introduced `null` case (Pitfall 4).

Purpose: These are the 8 `myTeam`-duplication files named in D-04. Two subtly-different semantics coexist today (null-safe vs. the `?? 'away'`-equivalent non-null-safe form); consolidating them onto one helper eliminates the drift — but the reconciliation must be explicit, never a silent coercion.
Output: HexGrid.tsx / ActionPanel.tsx call `useMyTeam()`; useGameStore.ts calls `deriveMyTeam(...)` at all 7 sites; both panels use the canonical form with documented null handling.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-code-cleanup/32-CONTEXT.md
@.planning/phases/32-code-cleanup/32-RESEARCH.md
@.planning/phases/32-code-cleanup/32-PATTERNS.md
@packages/client/src/hooks/useMyTeam.ts
</context>

<artifacts_produced>

## Artifacts this phase produces (new symbols — Plan 04)

No new exported symbols. This plan migrates existing inline `myTeam` derivations onto Plan 32-02's `useMyTeam` (component bodies) and `deriveMyTeam` (store action bodies). Local `const myTeam` variables retain their names; their derivation source changes.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the null-safe component sites (HexGrid, ActionPanel) to useMyTeam()</name>
  <files>packages/client/src/components/HexGrid.tsx, packages/client/src/components/ActionPanel.tsx</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx line 134 (`const myTeam: 'home' | 'away' | null = playerSlot === 1 ? 'home' : playerSlot === 2 ? 'away' : null;`) and where `playerSlot` is currently obtained (the `useGameStore((s) => s.playerSlot)` selector that becomes redundant if only used for myTeam)
    - packages/client/src/components/ActionPanel.tsx line 118 (identical null-safe form)
    - packages/client/src/hooks/useMyTeam.ts (exact `useMyTeam` signature from Plan 32-02)
    - .planning/phases/32-code-cleanup/32-PATTERNS.md (HexGrid is the canonical null-safe analog — exact form match)
  </read_first>
  <action>
    In HexGrid.tsx, replace the inline `const myTeam ... = playerSlot === 1 ? 'home' : playerSlot === 2 ? 'away' : null;` at line 134 with `const myTeam = useMyTeam();`. In ActionPanel.tsx, do the same at line 118. Import `useMyTeam` from `../hooks/useMyTeam.js` in both. If `playerSlot` was selected via `useGameStore((s) => s.playerSlot)` ONLY to compute `myTeam`, remove that now-redundant selector (verify `playerSlot` has no other use in the file first — if it is used elsewhere, keep the selector). The resulting `myTeam` type is `'home' | 'away' | null`, identical to the previous inline type — no downstream changes needed. Call `useMyTeam()` at the top level of the component body only (never in a loop/conditional).
  </action>
  <verify>
    <automated>grep -n "playerSlot === 1" packages/client/src/components/HexGrid.tsx packages/client/src/components/ActionPanel.tsx ; pnpm --filter @counter-attack/client test HexGrid ActionPanel 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "playerSlot === 1" packages/client/src/components/HexGrid.tsx` returns 0 and same for ActionPanel.tsx.
    - `grep -n "useMyTeam" packages/client/src/components/HexGrid.tsx` and `... ActionPanel.tsx` both match with a `../hooks/useMyTeam` import.
    - `pnpm --filter @counter-attack/client test HexGrid ActionPanel` passes (existing myTeam-guard tests, e.g. HexGrid.test.tsx BUG-10 myTeam-guard, still hold).
    - `pnpm --filter @counter-attack/client typecheck` clean; `pnpm lint` shows no react-hooks violation for these files.
  </acceptance_criteria>
  <done>HexGrid.tsx and ActionPanel.tsx derive myTeam via useMyTeam() with identical semantics and passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the non-null-safe sites (useGameStore ×7, FreeKickSetupPanel, KickOffSetupPanel) to deriveMyTeam with explicit null handling</name>
  <files>packages/client/src/store/useGameStore.ts, packages/client/src/components/FreeKickSetupPanel.tsx, packages/client/src/components/KickOffSetupPanel.tsx</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts lines 395-405, 430-440, 489-499, 523-533, 561-571, 593-603, 615-625 (the 7 `const myTeam = playerSlot === 1 ? 'home' : 'away';` sites at 400/435/494/528/566/598/620) — read each site's surrounding action body to see how `myTeam` is consumed and where `playerSlot` comes from (`const { ..., playerSlot } = get();`)
    - packages/client/src/components/FreeKickSetupPanel.tsx line 53 (`const myTeam: 'home' | 'away' = playerSlot === 1 ? 'home' : 'away';`) and the surrounding early-return/phase guards that establish whether playerSlot can be null at render
    - packages/client/src/components/KickOffSetupPanel.tsx line 28 (identical non-null-safe form) and its render guards
    - packages/client/src/hooks/useMyTeam.ts (`deriveMyTeam` pure fn — returns `'home' | 'away' | null`) and `useMyTeam` hook
    - .planning/phases/32-code-cleanup/32-RESEARCH.md § Common Pitfalls 2 (hooks illegal in store action bodies) and § Common Pitfalls 4 (two semantics; explicit decision required; Assumption A3 — panels only render mid-game after room join)
  </read_first>
  <action>
    In useGameStore.ts, replace each of the 7 `const myTeam = playerSlot === 1 ? 'home' : 'away';` with `const myTeam = deriveMyTeam(playerSlot);` (import `deriveMyTeam` from `../hooks/useMyTeam.js` — the PURE fn; do NOT call `useMyTeam()` here, it is uncallable in action bodies per Pitfall 2). Because `deriveMyTeam` returns `'home' | 'away' | null` (canonical), each of the 7 sites gains a `null` branch the old `?? 'away'`-equivalent form did not. For EACH site, choose ONE and record it in the SUMMARY: (a) if the action already cannot run with a null playerSlot (an earlier guard/return proves it), add a narrowing guard or non-null handling consistent with that flow; OR (b) if the site genuinely must preserve the old "treat null as away" behavior, write `deriveMyTeam(playerSlot) ?? 'away'` with an inline comment stating WHY — do not leave a silent coercion. In FreeKickSetupPanel.tsx (component body) replace line 53 with `const myTeam = useMyTeam();` and handle the resulting `'home' | 'away' | null`: confirm via the component's existing render guards that playerSlot is non-null at that point and add an explicit guard (e.g. early return / narrowing) OR document the `?? 'away'` fallback with justification. Do the same in KickOffSetupPanel.tsx at line 28. Verify A3 by tracing when each panel renders (mid-game, post room-join) rather than assuming. Ensure the result compiles under exactOptionalPropertyTypes.
  </action>
  <verify>
    <automated>grep -rn "playerSlot === 1 ? 'home' : 'away'" packages/client/src ; pnpm --filter @counter-attack/client test useGameStore FreeKickSetupPanel KickOffSetupPanel 2>&1 | tail -30 ; pnpm --filter @counter-attack/client typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "playerSlot === 1 ? 'home' : 'away'" packages/client/src` returns no matches (all 9 non-null-safe sites reconciled).
    - `grep -c "deriveMyTeam" packages/client/src/store/useGameStore.ts` returns at least 7 (plus the import).
    - FreeKickSetupPanel.tsx and KickOffSetupPanel.tsx import `useMyTeam` from `../hooks/useMyTeam` and no longer contain the inline non-null-safe derivation.
    - Each of the 7 store sites and the 2 panels has a recorded null-handling decision in the SUMMARY (guard vs. documented `?? 'away'`).
    - `pnpm --filter @counter-attack/client test useGameStore FreeKickSetupPanel KickOffSetupPanel` passes (including useGameStore.rule11.test.ts, one of the D-04-named files) and `pnpm --filter @counter-attack/client typecheck` reports zero errors.
  </acceptance_criteria>
  <done>All 9 non-null-safe myTeam sites use the canonical deriveMyTeam/useMyTeam with explicit, recorded null handling — no silent coercion, tests green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                                                                                                                                                            |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | Client-side team-slot derivation refactor; no new input surface, network path, or trust boundary. The `myTeam` value gates which pieces a player may act on, but that authorization is already enforced server-side (server-authoritative model); this is a client-UX derivation only. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                     |
| --------- | ---------------------- | ------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-32-05   | Elevation of privilege | client myTeam gating in useGameStore/panels | accept      | myTeam is a client-side UX affordance; the server independently validates every action against the socket's team (server-authoritative). A null-handling regression would at worst mis-enable a client control, which the server still rejects. Behavior guarded by existing store/component tests. |
| T-32-SC   | Tampering              | npm/pip/cargo installs                      | mitigate    | No package installs in this plan — no new dependencies introduced.                                                                                                                                                                                                                                  |

</threat_model>

<verification>
- `grep -rn "playerSlot === 1" packages/client/src` returns no matches in any of the 5 files (all inline derivations consolidated).
- `pnpm --filter @counter-attack/client test HexGrid ActionPanel useGameStore FreeKickSetupPanel KickOffSetupPanel` passes.
- `pnpm --filter @counter-attack/client typecheck` and `pnpm lint` (react-hooks) clean.
</verification>

<success_criteria>

- Every myTeam/team-slot duplication site named in D-04 is consolidated onto one canonical helper — component bodies via `useMyTeam()`, store action bodies via `deriveMyTeam()` (D-04, CLEANUP-02).
- The null-safe/non-null-safe semantic split is resolved with an explicit recorded decision at each of the 9 non-null-safe sites (Pitfall 4) — no silent coercion.
- Hooks are never called in store action bodies (Pitfall 2).
  </success_criteria>

<output>
Create `.planning/phases/32-code-cleanup/32-04-SUMMARY.md` when done. Record the per-site null-handling decision for all 7 store sites and both panels (guard vs. documented fallback), and confirm the A3 assumption was verified (not assumed) for each panel.
</output>
