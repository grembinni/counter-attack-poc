---
phase: 27-game-creation-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
autonomous: true
requirements: [DRAFT-01]
must_haves:
  truths:
    - 'Both client and server can import TeamType, DraftPoolId, and SELECTABLE_DRAFT_POOLS from @counter-attack/shared'
    - 'The ROOM_SETTINGS_CONFIRM / ROOM_SETTINGS_CONFIRMED typed event pair exists and compiles'
    - 'SELECTABLE_DRAFT_POOLS is narrower (3 values) than the DraftPoolId type (5 values) — the divergence is intentional per D-04'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'TeamType, DraftPoolId, SELECTABLE_DRAFT_POOLS'
      contains: 'SELECTABLE_DRAFT_POOLS'
    - path: 'packages/shared/src/events.ts'
      provides: 'ROOM_SETTINGS_CONFIRM (client) + ROOM_SETTINGS_CONFIRMED (server) event names and typed maps'
      contains: 'ROOM_SETTINGS_CONFIRM'
  key_links:
    - from: 'packages/shared/src/events.ts'
      to: 'packages/shared/src/types.ts'
      via: 'import type { GameSpeed, TeamType, DraftPoolId }'
      pattern: 'TeamType.*DraftPoolId'
---

<objective>
Introduce the shared vocabulary this phase needs: the `TeamType` and `DraftPoolId` types plus the `SELECTABLE_DRAFT_POOLS` allow-list in `packages/shared/src/types.ts`, and the `ROOM_SETTINGS_CONFIRM` / `ROOM_SETTINGS_CONFIRMED` typed Socket.io event pair in `packages/shared/src/events.ts`.

Purpose: This is the interface-first foundation. The server settings-confirm handler (27-02) and the client settings screen + subheaders (27-03, 27-04) all import these symbols. Defining them once, first, lets the server and client plans proceed in parallel in Wave 2 without a scavenger hunt for contracts.

Output: Two shared-package files extended with new exported symbols and typed event contracts. No runtime behavior changes yet — this plan only widens the type surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-game-creation-settings/27-CONTEXT.md
@.planning/phases/27-game-creation-settings/27-RESEARCH.md
@.planning/phases/27-game-creation-settings/27-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add TeamType, DraftPoolId, and SELECTABLE_DRAFT_POOLS to shared types</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts (read lines ~430-445: existing `GameSpeed` union at line 433 and `GAME_SPEED_MINUTES` map at line 439 — mirror this "type union + adjacent allow-list const" shape exactly)
    - .planning/phases/27-game-creation-settings/27-RESEARCH.md (§Room / Type Additions and Pitfall 3 — the type/allow-list divergence rationale)
  </read_first>
  <action>
    Immediately after the `GAME_SPEED_MINUTES` const block (ends ~line 445) add three exported symbols. Add `export type TeamType = 'standard' | 'draft';`. Add `export type DraftPoolId = 'original' | 'mls' | 'international' | 'legends' | 'icons';` — the type intentionally includes all five values for forward-compat with DRAFT-11 (Legends/Icons), even though only three are selectable this phase (D-04). Add `export const SELECTABLE_DRAFT_POOLS: readonly DraftPoolId[] = ['original', 'mls', 'international'] as const;`. Add a doc comment on each citing DRAFT-01 and D-04, and explicitly note on `SELECTABLE_DRAFT_POOLS` that it is deliberately NARROWER than `DraftPoolId` (server-side allow-list must reject `'legends'`/`'icons'` per Pitfall 3 / ASVS V5) — this is the first place in the codebase where a type and its validation allow-list intentionally diverge. Do NOT add any 'legends'/'icons' handling logic; they exist only as type members here.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `packages/shared/src/types.ts` exports `TeamType`, `DraftPoolId`, and `SELECTABLE_DRAFT_POOLS`.
    - Source assertion: `SELECTABLE_DRAFT_POOLS` has exactly the three entries `'original'`, `'mls'`, `'international'` (Legends/Icons excluded).
    - `DraftPoolId` union contains all five ids including `'legends'` and `'icons'`.
    - `pnpm --filter @counter-attack/shared typecheck` exits 0.
  </acceptance_criteria>
  <done>The three symbols are exported from shared types and typecheck passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add ROOM_SETTINGS_CONFIRM / ROOM_SETTINGS_CONFIRMED typed event pair</name>
  <files>packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/events.ts (read the whole file: `ClientEvents` const line 10, `ServerEvents` const line 84, `ClientToServerEvents` interface line 123, `ServerToClientEvents` interface line 208, and the `TEAM_SPEED_SET`/`TEAM_SPEED_CHANGED` pair at const lines 60/95 and interface lines 178/220 — the exact naming and typing convention to mirror)
    - .planning/phases/27-game-creation-settings/27-PATTERNS.md (§events.ts pattern assignment — object payload client→server, positional args server→client)
  </read_first>
  <action>
    Extend the top-of-file import from `'./types.js'` (currently `import type { HexCoord, GameState, GameSpeed } from './types.js';`) to also import `TeamType` and `DraftPoolId`. In the `ClientEvents` const object add `ROOM_SETTINGS_CONFIRM: 'room:settings-confirm',` (place it near `ROOM_CREATE`/`ROOM_JOIN` since it is room-lifecycle-adjacent) with a doc comment citing DRAFT-01/D-03: host confirms speed + team type + draft pools atomically. In the `ServerEvents` const object add `ROOM_SETTINGS_CONFIRMED: 'room:settings-confirmed',` (near `TEAM_SPEED_CHANGED`) with a doc comment: broadcast to room members carrying confirmed settings. In the `ClientToServerEvents` interface add `[ClientEvents.ROOM_SETTINGS_CONFIRM]: (settings: { speed: GameSpeed; teamType: TeamType; draftPools: DraftPoolId[] }) => void;` — single OBJECT payload (three always-sent-together fields, consistent with `LINEUP_CONFIRM`'s object payload). In the `ServerToClientEvents` interface add `[ServerEvents.ROOM_SETTINGS_CONFIRMED]: (speed: GameSpeed, teamType: TeamType, draftPools: DraftPoolId[]) => void;` — POSITIONAL args (consistent with `TEAM_SPEED_CHANGED(speed)`/`UNIFORM_HOME_CONFIRMED(...)`). Do NOT modify or extend `TEAM_SPEED_SET`/`TEAM_SPEED_CHANGED` — the three settings must lock together in one new event, not layer onto the speed-only event (explicit anti-pattern in RESEARCH.md).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ClientEvents.ROOM_SETTINGS_CONFIRM === 'room:settings-confirm'` and `ServerEvents.ROOM_SETTINGS_CONFIRMED === 'room:settings-confirmed'`.
    - Source assertion: `ClientToServerEvents` has a `ROOM_SETTINGS_CONFIRM` entry whose single param is an object `{ speed; teamType; draftPools }`.
    - Source assertion: `ServerToClientEvents` has a `ROOM_SETTINGS_CONFIRMED` entry with three positional params `(speed, teamType, draftPools)`.
    - `TEAM_SPEED_SET`/`TEAM_SPEED_CHANGED` are unchanged.
    - `pnpm --filter @counter-attack/shared typecheck` exits 0.
  </acceptance_criteria>
  <done>The event pair is defined in both the name consts and both typed event-map interfaces, importing TeamType/DraftPoolId from types.ts; shared typecheck passes.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (owned by this plan)

- Type `TeamType = 'standard' | 'draft'` — `packages/shared/src/types.ts`
- Type `DraftPoolId = 'original' | 'mls' | 'international' | 'legends' | 'icons'` — `packages/shared/src/types.ts`
- Const `SELECTABLE_DRAFT_POOLS: readonly DraftPoolId[]` (`['original','mls','international']`) — `packages/shared/src/types.ts`
- Client event `ClientEvents.ROOM_SETTINGS_CONFIRM = 'room:settings-confirm'` + `ClientToServerEvents` map entry (object payload `{ speed, teamType, draftPools }`) — `packages/shared/src/events.ts`
- Server event `ServerEvents.ROOM_SETTINGS_CONFIRMED = 'room:settings-confirmed'` + `ServerToClientEvents` map entry (positional `speed, teamType, draftPools`) — `packages/shared/src/events.ts`

Consumed downstream by: 27-02 (server handler + Room fields), 27-03 (settings screen + App wiring), 27-04 (subheaders).
</artifacts_produced>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                                           |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| client → server (ROOM_SETTINGS_CONFIRM) | This plan defines the wire contract only; the untrusted payload `{ speed, teamType, draftPools }` crosses here and is validated server-side in 27-02. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                                 | Disposition         | Mitigation Plan                                                                                                                                          |
| --------- | ---------------------------------- | ----------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-27-01   | Tampering / Elevation of Privilege | `ROOM_SETTINGS_CONFIRM` contract          | mitigate (in 27-02) | Type surface only here; the object payload is fully re-validated server-side (host-only guard + allow-lists) in 27-02. Typing is NOT a security control. |
| T-27-02   | Tampering                          | `DraftPoolId` vs `SELECTABLE_DRAFT_POOLS` | mitigate (in 27-02) | The 5-value type must never be used as the server allow-list; `SELECTABLE_DRAFT_POOLS` (3 values) is the validation source. Enforced in 27-02.           |
| T-27-SC   | Tampering                          | npm/pip/cargo installs                    | accept              | No new dependencies introduced this phase (RESEARCH.md Package Legitimacy Audit: N/A). No install task exists.                                           |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared typecheck` passes.
- `pnpm -r typecheck` still passes (no downstream consumer broken by the additive type/event surface).
</verification>

<success_criteria>

- `TeamType`, `DraftPoolId`, `SELECTABLE_DRAFT_POOLS` exported from shared types.
- `ROOM_SETTINGS_CONFIRM` / `ROOM_SETTINGS_CONFIRMED` present in both event name consts and both typed event-map interfaces.
- No changes to existing `TEAM_SPEED_SET`/`TEAM_SPEED_CHANGED`.
- Workspace typecheck green.
  </success_criteria>

<output>
Create `.planning/phases/27-game-creation-settings/27-01-SUMMARY.md` when done.
</output>
