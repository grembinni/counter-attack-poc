---
phase: 29-draft-ui-pick-and-swap-flow
plan: 02
type: execute
wave: 2
depends_on: ['29-01']
files_modified:
  - packages/server/src/draftSession.ts
  - packages/server/src/draftSession.test.ts
autonomous: true
requirements: [DRAFT-07, DRAFT-08, DRAFT-10]

must_haves:
  truths:
    - "A draft session can be bootstrapped from 8 generated packs with each player's 4-pack open-order chosen by an independent CSPRNG-driven shuffle (not a fixed 0-3 / 4-7 split, D-04)"
    - "Applying a pick removes the card from the player's current pack, places it on the requested slot or bench, bumps a displaced slot occupant to the bench (D-07), and decrements that player's picksRemaining"
    - "The 1+2+1 sub-step machine advances only when BOTH players have exhausted their picksRemaining, swapping packs on PICK1->PICK2, swapping back on PICK2->PICK3, discarding the 3 leftovers and opening each player's next pack on PICK3->cycle+1, and marking draftComplete after cycle 4 (D-01/D-02); there is no separate manual 'ready' action — making a pick is itself the readiness signal for that sub-step, same mutual-wait pattern as LINEUP_CONFIRM's home/away flags (D-03)"
    - 'On cycle 4, a player who has drafted no keeper gets one auto-selected from their own pack as their second pick, placed in the empty GK slot else bench, with their PICK2 requirement reduced to 1 (DRAFT-08)'
    - 'An already-drafted card can be rearranged between a lineup slot and the bench (applyRearrange) without changing cycle/sub-step or picksRemaining (D-08/D-10), displacing any slot occupant to the bench (D-07)'
    - 'At draft completion, bench cards receive distinct random jersey numbers in 15-99 via CSPRNG; starters are numbered by their formation slot, not repositioned (D-15)'
  artifacts:
    - path: 'packages/server/src/draftSession.ts'
      provides: 'Pure, io/socket-free draft state-machine helpers: assignPackOrders, createDraftSession, openNextPack, applyPick, applyRearrange, advanceSubStep, checkKeeperSafety, assignBenchNumbers, buildDraftView'
      min_lines: 120
    - path: 'packages/server/src/draftSession.test.ts'
      provides: 'Unit tests for pack assignment fairness, pick application, sub-step advancement, keeper safety, and bench numbering'
      min_lines: 120
  key_links:
    - from: 'packages/server/src/draftSession.ts'
      to: 'packages/shared/src/types.ts'
      via: 'import type { DraftSession, DraftClientView, DraftSubStep, DraftDestination }'
      pattern: 'DraftSession'
    - from: 'packages/server/src/draftSession.ts'
      to: 'packages/shared/src/draftEngine.ts (RandomIntFn)'
      via: 'injected rng parameter of type RandomIntFn — never Math.random'
      pattern: 'RandomIntFn|rng'
---

<objective>
Build the pure, side-effect-free draft state machine that powers the whole pick-and-swap flow. It has zero `io`/`socket` references — mirroring how `gameEngine.ts` is kept separate from `roomHandlers.ts` — so every branch (pack assignment fairness, sub-step advancement, keeper safety, bench numbering) is unit-testable in isolation. Plan 04 wires these functions into socket handlers with `crypto.randomInt`.

Purpose: Concentrate all draft rules in one tested module; keep the socket layer a thin orchestration shell.
Output: `packages/server/src/draftSession.ts` and its unit test file `packages/server/src/draftSession.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-RESEARCH.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-PATTERNS.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-VALIDATION.md
@packages/shared/src/draftEngine.ts
@packages/server/src/draftPacks.ts
@packages/server/src/__tests__/draftPacks.test.ts
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by THIS plan (new — exclude from drift/source-grounding checks):

- `packages/server/src/draftSession.ts` exports: `assignPackOrders(packCount: number, rng: RandomIntFn): { homePackOrder: number[]; awayPackOrder: number[] }`, `createDraftSession(packs: DraftPack[], rng: RandomIntFn): DraftSession`, `openNextPack(session: DraftSession): DraftSession` (advances cycle, sets each player's currentPack from their next pack index, resets picksRemaining), `applyPick(session, side, cardId, destination): { session: DraftSession; ok: boolean; error?: string }`, `applyRearrange(session, side, from, to): { session: DraftSession; ok: boolean; error?: string }` (moves an already-drafted card between slot/bench without touching cycle/sub-step, D-08/D-10), `advanceSubStep(session): DraftSession`, `checkKeeperSafety(session, rng): DraftSession`, `assignBenchNumbers(benchIds: string[], rng: RandomIntFn): Record<string, number>`, `buildDraftView(session, side): DraftClientView`
- Local type alias `DraftSide = 'home' | 'away'` (or reuse existing `'home' | 'away'` literal)
- Sort helper for tier-order (rarest-first) if placed here rather than client — decide during implementation; if server sorts the pack before sending, document it

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pack assignment + session bootstrap + pick & rearrange application</name>
  <files>packages/server/src/draftSession.ts, packages/server/src/draftSession.test.ts</files>
  <read_first>
    - packages/shared/src/draftEngine.ts (lines 148-182 — RandomIntFn type and the Fisher-Yates `shuffle` helper to mirror; DraftPack shape lines 159-166; TieredPoolPlayer lines 33-40)
    - packages/server/src/draftPacks.ts (how crypto.randomInt is bound; the RandomIntFn contract min inclusive / max exclusive)
    - packages/shared/src/types.ts (the DraftSession / DraftClientView / DraftDestination types added in Plan 01)
    - packages/server/src/__tests__/draftPacks.test.ts (the seeded-vs-real RNG testing style and structural-invariant looping to mirror)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-RESEARCH.md (Common Pitfalls #5 — pack-to-player assignment must be an independent shuffle, not a slice of generateMatchPacks' own order)
  </read_first>
  <behavior>
    - assignPackOrders(8, seededRng): returns homePackOrder + awayPackOrder, each length 4, together a permutation of 0..7 with no overlap (union === {0..7}).
    - assignPackOrders is NOT the identity split: across many seeded runs it produces home orders other than [0,1,2,3] (assert with a controllable rng that at least one non-identity assignment occurs; the shuffle uses the injected rng exclusively — no Math.random).
    - createDraftSession(packs, rng): cycle=0, subStep='PICK1', draftComplete=false, empty drafted/bench arrays, homeLineupSlots/awayLineupSlots are length-11 all-null, homeHasKeeper/awayHasKeeper=false, draftPacks stored, pack orders assigned via assignPackOrders.
    - applyPick(session,'home',cardId,{type:'slot',slotIndex:5}): moves the card id out of homeCurrentPack, sets homeLineupSlots[5]=cardId, adds to homeDraftedIds, decrements homePicksRemaining by 1, sets homeHasKeeper=true if the card's tier==='keeper'; returns ok:true.
    - applyPick onto an occupied slot: the previous occupant id is pushed onto homeBenchIds (D-07), then the new card takes the slot.
    - applyPick with destination {type:'bench'}: appends cardId to homeBenchIds.
    - applyPick with a cardId not present in the side's currentPack: returns ok:false, error:'INVALID_CARD', session unchanged (no mutation).
    - applyPick returns a NEW session object (input not mutated in place) — matches the engine module's no-side-effects convention.
    - applyRearrange(session,'home',{type:'slot',slotIndex:3},{type:'bench'}): moves the card at homeLineupSlots[3] to homeBenchIds, clears the slot, and does NOT change cycle/subStep/picksRemaining (D-08/D-10); returns a NEW session.
    - applyRearrange from a bench card onto an occupied slot: displaces the current occupant to homeBenchIds (D-07) and places the moved card in the slot.
    - applyRearrange with a `from` ref that holds no card: returns ok:false, error:'INVALID_REARRANGE', session unchanged.
  </behavior>
  <action>
    Create `packages/server/src/draftSession.ts` as a pure module: a top doc-comment stating no `io`/`socket` imports (mirrors gameEngine.ts separation, RESEARCH Open Question 2). Import `RandomIntFn`, `DraftPack`, `TieredPoolPlayer` from `@counter-attack/shared`, and the `DraftSession`/`DraftClientView`/`DraftDestination` types from Plan 01.

    Implement `assignPackOrders(packCount, rng)` using a Fisher-Yates shuffle over `[0..packCount-1]` driven solely by `rng` (copy the `shuffle` idiom from draftEngine.ts; do NOT import the private one — reimplement locally or export a shared helper, executor's call), then slice `[0..3]` to home and `[4..7]` to away. This is the independent second shuffle that satisfies D-04 / Pitfall 5 — it must run over pack INDICES regardless of the order generateMatchPacks returned them in.

    Implement `createDraftSession(packs, rng)` returning a fully-initialized `DraftSession` (cycle 0, subStep 'PICK1', all per-player arrays empty, 11-null lineup slots, picksRemaining 0 until the first `openNextPack`, hasKeeper false, keeperAutoPickedThisCycle {home:false,away:false}, draftComplete false).

    Implement `applyPick(session, side, cardId, destination)` returning `{ session, ok, error? }`. Validate `cardId` is in `session[side+'CurrentPack']`; if not, return `{ session, ok:false, error:'INVALID_CARD' }`. Otherwise produce a NEW session (shallow-clone the arrays you mutate): remove the card from the current pack, add id to `${side}DraftedIds`, set hasKeeper if tier==='keeper', decrement `${side}PicksRemaining`, and apply the destination: for `slot` — if `lineupSlots[slotIndex]` is non-null push that occupant onto `${side}BenchIds` (D-07), then set the slot; for `bench` — append to `${side}BenchIds`. Do NOT perform GK-slot role validation here — that is the handler's allow-list job in Plan 04 (this function trusts a pre-validated destination); document that boundary in a comment.

    Implement `applyRearrange(session, side, from, to)` returning `{ session, ok, error? }` for moving an ALREADY-drafted card between a lineup slot and/or the bench: slot -> slot (swap or fill-empty), slot -> bench (append + clear the slot), bench -> slot (fill an empty slot, or displace the occupant to the bench per D-07), bench -> bench (reorder). It must NEVER change cycle/subStep/picksRemaining (D-10) and returns a NEW session; if the `from` ref holds no card, return `{ session, ok:false, error:'INVALID_REARRANGE' }`. Like `applyPick`, it does NOT perform GK-slot role validation — Plan 04's DRAFT_REARRANGE handler does that allow-list check before calling this; document that boundary in a comment.

    In the same commit, add `packages/server/src/draftSession.test.ts` with the RED-then-GREEN tests described in <behavior>, using a deterministic injected rng (a counter/array-backed fake matching the `(min,max)` contract) so assignment/order assertions are reproducible. Follow the looping structural-invariant style of draftPacks.test.ts where randomness is involved.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- draftSession</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/server test -- draftSession` runs and all Task-1 tests pass.
    - A test asserts `assignPackOrders` output is a no-overlap permutation of 0..7 (home ∪ away === all 8, intersection empty).
    - A test asserts a non-identity home order is producible under a controlling rng (proves it is a real shuffle, not a slice).
    - A test asserts `applyPick` with an out-of-pack cardId returns `ok:false, error:'INVALID_CARD'` and leaves the input session unmodified (deep-equal to a pre-call snapshot).
    - A test asserts occupied-slot replacement pushes the prior occupant to the bench.
    - A test asserts `applyRearrange` moves a card slot<->bench and that a bench -> occupied-slot move displaces the prior occupant to the bench, all without changing cycle/subStep/picksRemaining.
    - `grep -v '^\s*//' packages/server/src/draftSession.ts | grep -c 'Math.random'` returns 0.
  </acceptance_criteria>
  <done>Session bootstrap, fair pack assignment, and pick/rearrange placement-with-displacement are implemented and unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sub-step advancement state machine (1+2+1 x4)</name>
  <files>packages/server/src/draftSession.ts, packages/server/src/draftSession.test.ts</files>
  <read_first>
    - packages/server/src/draftSession.ts (the Task-1 functions and DraftSession field names — build advanceSubStep/openNextPack against them)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-RESEARCH.md (Architecture Patterns §3 — the exact PICK1->SWAP->PICK2->SWAP_BACK->PICK3->NEW_PACK sequence and the 4-cycle/16-card cross-check; Common Pitfalls #1 / Assumption A1 — phase-boundary-only gating for the 2-card PICK2)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md (D-01 cycle math, D-02 discard-3)
  </read_first>
  <behavior>
    - openNextPack(session): increments cycle, sets homeCurrentPack from draftPacks[homePackOrder[cycle-1]].cards (copy) and away likewise, subStep='PICK1', homePicksRemaining=1, awayPicksRemaining=1, resets keeperAutoPickedThisCycle to {home:false,away:false}.
    - advanceSubStep is a no-op (returns session unchanged) while EITHER player still has picksRemaining > 0 in the current sub-step (phase-boundary-only gating, A1).
    - When both picksRemaining===0 in PICK1: swap homeCurrentPack <-> awayCurrentPack, set subStep='PICK2', picksRemaining=2 each (unless keeper-safety already reduced a side — see Task 3 ordering note).
    - PICK2 both done: swap packs back to original owners, subStep='PICK3', picksRemaining=1 each.
    - PICK3 both done: discard the leftover current-pack cards (D-02 — they simply are not carried forward), then if cycle < 4 call openNextPack (cycle++, PICK1, picksRemaining 1 each); if cycle === 4 set draftComplete=true and leave subStep as-is.
    - Cross-check invariant: driving a full session PICK1(1)+PICK2(2)+PICK3(1) across 4 cycles for each player yields exactly 16 drafted ids per player (plus any auto-keeper, still 16 total picks/player).
  </behavior>
  <action>
    Implement `openNextPack(session)` and `advanceSubStep(session)` per <behavior>, each returning a NEW session (no in-place mutation of the caller's object). Keep the swap operations as reference exchanges of the two `*CurrentPack` arrays. Gate strictly on `homePicksRemaining === 0 && awayPicksRemaining === 0` (A1 phase-boundary gating) — do NOT gate per individual card within PICK2. Do the PICK3 discard by simply not copying the leftover cards forward (openNextPack overwrites currentPack from the next pack index).

    Add tests to `draftSession.test.ts`: a helper that simulates both players making their required picks per sub-step and calls advanceSubStep after each side's picks, asserting the sub-step/cycle transitions in order and that a mid-PICK2 state (one card picked, one remaining) does NOT advance. Add the 16-cards-per-player end-to-end cross-check test (drive all 4 cycles, assert drafted count). Assert draftComplete flips true only after cycle-4 PICK3 resolves.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- draftSession</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove the sub-step order PICK1 -> (swap) PICK2 -> (swap back) PICK3 -> (open next) PICK1 for cycles 1-3, and PICK3 -> draftComplete on cycle 4.
    - A test proves advanceSubStep is a no-op while one side still has picksRemaining > 0 (the "waiting for opponent" state) and does not advance mid-PICK2.
    - A test drives a full 4-cycle session and asserts each player ends with 16 drafted ids.
    - A test asserts the PICK1->PICK2 transition swaps the two players' current packs (home ends holding what away held).
  </acceptance_criteria>
  <done>The full 1+2+1 x4 cycle machine advances correctly under phase-boundary gating.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keeper safety (cycle 4) + bench numbering + buildDraftView</name>
  <files>packages/server/src/draftSession.ts, packages/server/src/draftSession.test.ts</files>
  <read_first>
    - packages/server/src/draftSession.ts (Task 1 + 2 functions and field names)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-RESEARCH.md (Architecture Patterns §4 keeper-safety trigger point + Assumption A3 — auto-keeper drawn from the player's OWN cycle-4 pack; the Phase 28 invariant that every pack contains exactly one keeper)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md (D-15 bench random 15-99; D-16 REQUIREMENTS.md conflict note; UI-SPEC Component Note 5 auto-place keeper into empty GK slot else bench)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-UI-SPEC.md (Component Note 5)
  </read_first>
  <behavior>
    - checkKeeperSafety(session, rng): only acts when cycle===4 and subStep==='PICK1' with both players' PICK1 picks resolved (picksRemaining 0). For each side where hasKeeper is false: find the keeper-tier card in that side's current pack, remove it, add to that side's draftedIds, set hasKeeper true, set keeperAutoPickedThisCycle[side]=true, auto-place it into lineupSlots[0] if that slot is null else append to benchIds (Component Note 5), and reduce that side's next PICK2 requirement so picksRemaining will be 1 not 2 when PICK2 opens.
    - checkKeeperSafety leaves a side untouched if it already hasKeeper (no-op for that side).
    - A player who reaches cycle 4 with no keeper and whose own cycle-4 pack still holds its guaranteed keeper always gets exactly one auto-keeper (invariant from Phase 28: every pack has 1 keeper).
    - assignBenchNumbers(benchIds, rng): returns a Record mapping each bench id to a DISTINCT integer in [15,99]; no duplicates; length === benchIds.length; uses rng only.
    - buildDraftView(session,'home'): returns a DraftClientView with currentPack = homeCurrentPack, picksRemaining = homePicksRemaining, benchIds = homeBenchIds, lineupSlots = homeLineupSlots, benchNumbers = homeBenchNumbers, keeperAutoPickedThisCycle = keeperAutoPickedThisCycle.home, draftComplete = session.draftComplete, and waitingForOpponent = (homePicksRemaining===0 && awayPicksRemaining>0 && !draftComplete). It must NOT include away* fields.
  </action>
  <action>
    Implement `checkKeeperSafety(session, rng)`, `assignBenchNumbers(benchIds, rng)`, and `buildDraftView(session, side)` per <behavior>. Sequence keeper-safety in the cycle-4 PICK1 boundary BEFORE the PICK1->PICK2 swap (the handler in Plan 04 calls checkKeeperSafety, then advanceSubStep) so the reduced picksRemaining is applied to the correct player's PICK2. For bench numbering, draw distinct values by shuffling the 15..99 range (85 values, always >= max bench size of ~5) with rng and taking the first N — never `Math.random`. `buildDraftView` is the single place that projects the private per-side view (mirrors D-14); assert structurally it exposes no opponent fields.

    Add tests to `draftSession.test.ts`: (a) a cycle-4 session where 'home' has drafted no keeper — after checkKeeperSafety, home has exactly one keeper-tier drafted id, keeperAutoPickedThisCycle.home is true, it landed in lineupSlots[0] (empty GK case), and home's PICK2 requirement is 1; (b) a cycle-4 session where 'away' already has a keeper — checkKeeperSafety leaves away unchanged; (c) assignBenchNumbers over a 6-id list returns 6 distinct 15-99 numbers across repeated seeded runs; (d) buildDraftView('home') has no `awayCurrentPack`/`away`-prefixed keys and sets waitingForOpponent correctly for a home-done/away-pending state.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- draftSession</automated>
  </verify>
  <acceptance_criteria>
    - Keeper-safety test proves auto-selection only fires on cycle 4 for a keeperless player, places into empty GK slot, and reduces that player's PICK2 to 1 pick.
    - Keeper-safety test proves a player who already has a keeper is untouched.
    - `assignBenchNumbers` test proves N distinct integers, all within [15,99], across repeated runs.
    - `buildDraftView` test proves the returned object has no away-prefixed / opponent keys and computes waitingForOpponent from picksRemaining.
    - Full file: `pnpm --filter @counter-attack/server test -- draftSession` green; `grep -c 'Math.random' packages/server/src/draftSession.ts` (excluding comments) === 0.
  </acceptance_criteria>
  <done>Keeper safety, bench numbering, and the private per-player view projection are implemented and tested.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                                |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| injected RNG → draft outcomes | Pack-to-player assignment and bench numbering are fairness-affecting; a weak/predictable source undermines draft integrity |
| private view projection       | `buildDraftView` is the choke point that must never emit the opponent's pack                                               |

## STRIDE Threat Register

| Threat ID | Category                          | Component                                | Disposition | Mitigation Plan                                                                                                                                                                                                                                              |
| --------- | --------------------------------- | ---------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-29-03   | Information Disclosure / Fairness | `assignPackOrders`, `assignBenchNumbers` | mitigate    | Both take an injected `RandomIntFn` and use it exclusively (no `Math.random`); Plan 04 binds `crypto.randomInt`. The independent pack-index shuffle (Pitfall 5) prevents a predictable fixed 0-3/4-7 split. Unit tests assert distinctness and non-identity. |
| T-29-PRIV | Information Disclosure            | `buildDraftView`                         | mitigate    | `buildDraftView(side)` returns a `DraftClientView` containing only that side's pack/lineup/bench; a test asserts the object carries no opponent-prefixed keys.                                                                                               |
| T-29-SC   | Tampering (supply chain)          | package installs                         | accept      | No npm packages installed this plan.                                                                                                                                                                                                                         |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test -- draftSession` all green.
- `pnpm --filter @counter-attack/server typecheck` green.
- No `Math.random` anywhere in `draftSession.ts` (comment-stripped grep === 0).
</verification>

<success_criteria>

- draftSession.ts implements every exported helper listed in "Artifacts this phase produces" with no io/socket dependency.
- All DRAFT-07 (cycle machine), DRAFT-08 (keeper safety), DRAFT-10 (numbering) behaviors are covered by passing unit tests.
- 16 drafted cards per player is proven by an end-to-end cycle-drive test.
  </success_criteria>

<output>
Create `.planning/phases/29-draft-ui-pick-and-swap-flow/29-02-SUMMARY.md` when done.
</output>
