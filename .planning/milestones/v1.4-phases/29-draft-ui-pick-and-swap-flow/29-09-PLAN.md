---
phase: 29-draft-ui-pick-and-swap-flow
plan: 09
type: execute
wave: 2
depends_on: [29-07, 29-08]
files_modified:
  - .planning/phases/29-draft-ui-pick-and-swap-flow/29-VERIFICATION.md
autonomous: false
gap_closure: true
requirements: [DRAFT-06, DRAFT-07, DRAFT-08, DRAFT-09, DRAFT-10]

must_haves:
  truths:
    - 'Full automated gate is green after the gap fixes: pnpm -r typecheck, pnpm -r test (full suite), pnpm -r build all exit 0 (DRAFT-06..10 regression safety)'
    - 'In a live two-browser session, after a completed draft a player performs multiple consecutive lineup↔bench rearrangements before Confirm without any drag failing (DRAFT-09/DRAFT-10 — gap 1 closed)'
    - 'The bench navigates left/right as a carousel with ‹/› controls, and draft/bench cards show all player stats legibly (DRAFT-09/DRAFT-06 — gap 2 + non-critical card width closed)'
    - 'After both players confirm the draft lineup and the match starts, all 22 on-pitch pieces render with correct stats and board positions (DRAFT-10 — gap 3, the most severe, closed)'
    - 'The cycle-4 keeper-safety scenario is DELIBERATELY exercised (a player withholds a keeper until cycle 4) and a keeper is auto-selected and correctly placed and carried into game start (DRAFT-08 re-test)'
    - 'Mid-draft disconnect/reconnect within the grace window resumes the draft exactly where it left off (D-13 re-test)'
  artifacts:
    - path: '.planning/phases/29-draft-ui-pick-and-swap-flow/29-VERIFICATION.md'
      provides: 'Updated re-verification report recording gap closure or any remaining gaps'
      contains: 'Status'
  key_links:
    - from: 'gap-closure plans 29-07/29-08'
      to: 'human two-browser walkthrough'
      via: "re-run of 29-06 Task 2's 8 steps plus the keeper-safety and reconnect scenarios"
      pattern: 'walkthrough'
---

<objective>
Re-verify Phase 29 end-to-end after the 29-07 (server lifecycle) and 29-08 (client carousel/robustness) gap fixes land. This is the gate that decides whether Phase 29 can finally be marked complete.

Purpose: Confirm all three critical gaps from 29-VERIFICATION.md are closed under real human two-browser testing, exercise the two items that were never deliberately tested in the 29-06 session (DRAFT-08 cycle-4 keeper safety and D-13 reconnect), and prove no regression to the working DRAFT-07 pick-and-swap flow.
Output: A refreshed verification verdict written back into 29-VERIFICATION.md — either "phase complete" or a fresh, specific gap list for another closure cycle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-VERIFICATION.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-06-PLAN.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-06-SUMMARY.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gate — typecheck, full test suite, build</name>
  <files>(no source changes — runs the monorepo gate)</files>
  <read_first>
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-06-PLAN.md — Task 1 defines the exact automated gate this re-run repeats (typecheck + full suite + build).
    - packages/server/src/__tests__/draftSession.integration.test.ts and packages/client/src/components/BenchCarousel.test.tsx / LineupAssignmentScreen.test.tsx — the new/updated tests from 29-07/29-08 that must be green.
  </read_first>
  <action>
    Run the full monorepo automated gate from the repo root and confirm all three commands exit 0: `pnpm -r typecheck`, `pnpm -r test`, `pnpm -r build`. Capture the total test count and confirm it is at least the prior 1527 (it should grow with the new draftSession.integration + BenchCarousel tests). If any command fails, STOP and record the failure in the verification notes — do not proceed to the human checkpoint on a red gate.
  </action>
  <verify>
    <automated>pnpm -r typecheck && pnpm -r test && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `pnpm -r typecheck` exits 0.
    - CLI: `pnpm -r test` exits 0 with a total test count >= 1527 (includes the new 29-07/29-08 tests).
    - CLI: `pnpm -r build` exits 0.
  </acceptance_criteria>
  <done>The full automated gate is green across shared/server/client.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Two-browser human walkthrough — re-verify all gaps + keeper safety + reconnect</name>
  <files>.planning/phases/29-draft-ui-pick-and-swap-flow/29-VERIFICATION.md</files>
  <action>
    Conduct a live two-browser draft session (per the how-to-verify steps below) to re-verify all three critical gaps are closed and the two previously-unexercised scenarios pass, then record the verdict in 29-VERIFICATION.md. This is a blocking human-verify checkpoint: do not auto-advance — pause for the human to run the walkthrough and report the result.
  </action>
  <what-built>
    The 29-07 server fixes (post-draft rearrange allowed before Confirm; drafted roster resolved into game start) and the 29-08 client fixes (bench carousel chrome, wider legible cards, drag-state robustness, Confirm-gated on a full lineup). This checkpoint confirms they actually close the three critical gaps and that the two never-exercised scenarios pass.
  </what-built>
  <how-to-verify>
    Start the dev servers (client on :5173, server on :3001) and open two browser windows as two players in the same room, choosing Draft mode with a pool that guarantees keepers (Original + at least one more), following the 8-step walkthrough from 29-06-PLAN.md Task 2. Verify, in order:

    1. Draft screen shows the 7-card carousel above the lineup grid; cards are wide enough to read every stat (no truncation/crowding) — DRAFT-06.
    2. Complete all 4 pick-and-swap cycles (16 cards each). The in-draft pick/swap flow still works end-to-end — DRAFT-07 non-regression.
    3. Keeper-safety re-test (DRAFT-08): in ONE browser, DELIBERATELY avoid drafting any keeper through cycle 4's first pick. Confirm a keeper is auto-selected as that player's second pick of cycle 4, the "Keeper auto-selected — cycle 4 safety net" banner shows, the passed pack has one fewer card, and that player then picks 1 (not 2) on the following pick phase.
    4. Bench carousel (DRAFT-09): once overflow cards land on the bench, confirm the bench navigates left/right with ‹/› controls (a real carousel, not a static row) and uses the same card style as the draft-pack row.
    5. Post-draft rearrange (DRAFT-09/DRAFT-10 — gap 1): after the draft completes and BEFORE clicking Confirm, drag cards between lineup and bench MULTIPLE consecutive times (at least 3 distinct moves, including bench→lineup and lineup→bench). Every drag must apply — no "first one works then it wedges" behavior.
    6. Reconnect (D-13): mid-draft, refresh/reconnect one browser within the grace window; confirm the draft resumes exactly where it left off (current pack, drafted lineup/bench intact).
    7. Confirm-gating: confirm the Confirm button is unavailable until all 11 starting slots are filled, then both players confirm.
    8. Hand-off (DRAFT-10 — gap 3): after both confirm, the match starts and EVERY on-pitch player (all 22) shows correct stats and is in a valid board position — no blank/statless pieces.

    Then write the outcome into 29-VERIFICATION.md: update `status` to `verified` (all truths pass) or `gaps_found` with a fresh specific gap list, and refresh the Observable Truths / Requirements Coverage tables for DRAFT-06..10.

  </how-to-verify>
  <verify>
    <human-check>All 8 walkthrough checks pass in the live two-browser session; 29-VERIFICATION.md updated with the verdict.</human-check>
  </verify>
  <acceptance_criteria>
    - All 8 walkthrough checks pass in the live two-browser session (each of the 3 critical gaps closed; DRAFT-08 keeper safety and D-13 reconnect deliberately exercised and passing; DRAFT-07 not regressed).
    - 29-VERIFICATION.md is updated with the re-verification verdict and refreshed truth/requirement tables.
    - If any check fails, the specific failure (truth #, observed behavior, suspected file) is recorded for a follow-up gap-closure cycle rather than silently passing.
  </acceptance_criteria>
  <done>All 8 checks pass and 29-VERIFICATION.md records a `verified` status, or a fresh specific gap list is captured for another cycle.</done>
  <resume-signal>Type "approved" if all 8 checks pass, or describe which checks failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
- Automated gate (Task 1) green: `pnpm -r typecheck`, `pnpm -r test`, `pnpm -r build` all exit 0.
- Human walkthrough (Task 2) confirms the 3 critical gaps closed plus DRAFT-08 keeper safety and D-13 reconnect exercised.
- 29-VERIFICATION.md updated with the new verdict.
</verification>

<success_criteria>

- Phase 29's five observable truths (DRAFT-06..10) all pass under live human testing, or a fresh precise gap list is produced.
- The two previously-unexercised scenarios (cycle-4 keeper safety, mid-draft reconnect) are deliberately tested.
- No regression to the confirmed-working DRAFT-07 pick-and-swap flow.
  </success_criteria>

<artifacts>
## Artifacts this plan produces

- Updated `.planning/phases/29-draft-ui-pick-and-swap-flow/29-VERIFICATION.md` — re-verification verdict (status verified or a fresh gap list) with refreshed truth/requirement tables.
- No source code artifacts (verification-only plan).
  </artifacts>

<output>
Create `.planning/phases/29-draft-ui-pick-and-swap-flow/29-09-SUMMARY.md` when done
</output>
