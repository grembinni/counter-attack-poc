---
phase: 29-draft-ui-pick-and-swap-flow
plan: 03
type: execute
wave: 2
depends_on: ['29-01']
files_modified:
  - packages/client/src/components/DraftPackCarousel.tsx
  - packages/client/src/components/BenchCarousel.tsx
  - packages/client/src/components/LineupAssignmentScreen.module.css
  - packages/client/src/components/DraftPackCarousel.test.tsx
autonomous: true
requirements: [DRAFT-06, DRAFT-09]

must_haves:
  truths:
    - 'A left-right navigable draft-pack carousel renders any number of cards (5, 6, or 7) — never hardcoded to 7 — sorted rarest-first (Chase, Rare, Uncommon, Common, Keeper) and starts scrolled to the leftmost card on each new pack (D-20)'
    - 'Each card shows the same LineupStatCard content plus a 3px tier-colored border/frame (Chase gold, Rare silver, Uncommon bronze, Common blue, Keeper green) — no corner badge (D-17/D-18/D-19)'
    - 'Cards in the draft-pack row are drag sources only — they expose a drag-start carrying the card id and cannot be drop targets (one-way out, D-06)'
    - 'A dynamically sized bench carousel renders 0..16 cards using the identical card style as the draft-pack row and is both a drag source and a drop target (D-08/D-09/D-21)'
  artifacts:
    - path: 'packages/client/src/components/DraftPackCarousel.tsx'
      provides: 'DraftPackCarousel component: variable-size, tier-sorted, left/right nav, drag-source-only cards with tier borders'
      min_lines: 80
    - path: 'packages/client/src/components/BenchCarousel.tsx'
      provides: 'BenchCarousel component: dynamic-length card row, drag source + drop target'
      min_lines: 50
    - path: 'packages/client/src/components/LineupAssignmentScreen.module.css'
      provides: 'tier-border classes and carousel/waiting/keeper-banner styles'
      contains: 'cardTierChase'
    - path: 'packages/client/src/components/DraftPackCarousel.test.tsx'
      provides: 'component tests for tier-sort, variable pack size, left/right nav, drag-source dataTransfer'
      min_lines: 60
  key_links:
    - from: 'packages/client/src/components/DraftPackCarousel.tsx'
      to: 'packages/shared/src/draftEngine.ts (TieredPoolPlayer)'
      via: 'import type — cards are TieredPoolPlayer[] with a tier field'
      pattern: 'TieredPoolPlayer|tier'
    - from: 'packages/client/src/components/DraftPackCarousel.tsx'
      to: 'packages/client/src/components/LineupAssignmentScreen.module.css'
      via: 'tier border class lookup by card.tier'
      pattern: 'cardTier'
---

<objective>
Build the two new client card carousels — the one genuinely new UI primitive in this phase. The draft-pack carousel is a variable-size, tier-sorted, left/right-navigable row of drag-source-only cards; the bench carousel is a dynamic-length row that is both drag source and drop target. Both reuse the existing `LineupStatCard` visual body and the dark-theme palette; the only new visual is a 3px tier-colored border. All new CSS (tier borders, carousel chrome, waiting/disabled/keeper-banner states) lands here so the screen-integration plan (Plan 05) only wires .tsx.

Purpose: Isolate the new visual primitive and its styles into one plan with clean file ownership, unblocking Plan 05.
Output: `DraftPackCarousel.tsx`, `BenchCarousel.tsx`, extended `LineupAssignmentScreen.module.css`, and `DraftPackCarousel.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-UI-SPEC.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-PATTERNS.md
@packages/client/src/components/LineupAssignmentScreen.tsx
@packages/client/src/components/LineupAssignmentScreen.module.css
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by THIS plan (new — exclude from drift/source-grounding checks):

- `packages/client/src/components/DraftPackCarousel.tsx`: `DraftPackCarousel` component; a `TIER_ORDER` rarest-first constant; a `TIER_CARD_CLASS: Record<DraftTier, string>` map; a shared inner card renderer (extracted from / mirroring `LineupStatCard`)
- `packages/client/src/components/BenchCarousel.tsx`: `BenchCarousel` component
- `packages/client/src/components/LineupAssignmentScreen.module.css`: new classes `cardTierChase`, `cardTierRare`, `cardTierUncommon`, `cardTierCommon`, `cardTierKeeper`, `draftPackRow`, `carouselViewport`, `carouselTrack`, `carouselNav`, `carouselNavDisabled`, `draftRowDisabled`, `waitingIndicator`, `cyclePickCounter`, `keeperBanner`, `benchCarousel`

<tasks>

<task type="auto">
  <name>Task 1: Tier border CSS + shared draft card renderer + DraftPackCarousel</name>
  <files>packages/client/src/components/LineupAssignmentScreen.module.css, packages/client/src/components/DraftPackCarousel.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx (lines 22-27 statTier; lines 65-142 LineupStatCard — the exact TeamBadge + cardBody + cardHeader + statGrid structure to reuse; lines 19-20 PLAYER_MAP pattern)
    - packages/client/src/components/LineupAssignmentScreen.module.css (lines 122-169 statCardBase/statCard/statCardDropTarget; lines 254-269 statBadge tiers; the dark palette comment lines 1-5)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-UI-SPEC.md (New Tier Border Colors table; Component Note 1 carousel; Spacing/Typography/Color sections; Carousel nav copy `‹`/`›`, aria-labels)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md (D-17 tier colors, D-18 content unchanged, D-19 3px border, D-20 carousel behavior)
    - packages/shared/src/draftEngine.ts (TieredPoolPlayer has `tier: DraftTier` and `totalStat`)
  </read_first>
  <action>
    In `LineupAssignmentScreen.module.css`, add the five tier-border classes exactly per UI-SPEC: `.cardTierChase { composes: statCardBase; border: 3px solid #f5c518; }`, `.cardTierRare` `#c0c0c0`, `.cardTierUncommon` `#cd7f32`, `.cardTierCommon` `#3b82f6`, `.cardTierKeeper` `#22c55e`. Add carousel chrome classes reusing the existing dark tokens (`#1a1a2e`/`#16213e`/`#0f3460`): `.draftPackRow` (section wrapper), `.carouselViewport` (overflow-x hidden window), `.carouselTrack` (horizontal flex, gap 8px per Spacing sm), `.carouselNav` (32px-square nav-arrow hit target per Spacing exception), `.carouselNavDisabled` (dimmed/disabled arrow at list ends), `.draftRowDisabled` (opacity 0.85 matching statCardConfirmed, pointer-events none — the D-12 waiting state), `.waitingIndicator` (14px/700 subheading), `.cyclePickCounter` (12px/700 label), `.keeperBanner` (brief fade banner surface). Do not introduce a new base palette.

    Create `DraftPackCarousel.tsx`. Export a `TIER_ORDER: DraftTier[] = ['chase','rare','uncommon','common','keeper']` (rarest-first, D-20) and `TIER_CARD_CLASS: Record<DraftTier, string>` mapping each tier to its CSS class. Extract or mirror `LineupStatCard`'s JSX body into a reusable inner card (same TeamBadge/cardBody/cardHeader/statGrid/statChip markup, same STAT_LABELS role-filter). The component props: `cards: TieredPoolPlayer[]`, `teamId: TeamId`, `disabled: boolean`, `onCardDragStart: (cardId: string) => void`. Behavior: sort a copy of `cards` by `TIER_ORDER.indexOf(tier)` (stable within tier); render them in a `.carouselViewport`/`.carouselTrack`; each card is `draggable={!disabled}`, sets `e.dataTransfer.setData('text/plain', 'pack:' + cardId)` and `effectAllowed='move'` on drag start (drag source ONLY — no onDragOver/onDrop handlers on these cards, D-06), and calls `onCardDragStart(cardId)`. Add left/right nav buttons (`‹`/`›` unicode, `aria-label="Previous card"`/`"Next card"`) that scroll the viewport; disable the appropriate arrow at each end via `.carouselNavDisabled`. Reset the scroll position to the leftmost card whenever the `cards` prop identity/length changes (a `useEffect` on the cards array), so each new pack opens scrolled-left (D-20). Do not hardcode a 7-slot grid — render `cards.map(...)` so a 5- or 6-card keeper-safety pack renders correctly. Keep any drag state local `useState` (never Zustand — Pitfall 7).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client typecheck` exits 0.
    - `grep` confirms all five `.cardTier*` classes and each carousel class (`draftPackRow`, `carouselTrack`, `carouselNav`, `draftRowDisabled`, `waitingIndicator`) exist in the CSS module.
    - `DraftPackCarousel.tsx` renders `cards.map(...)` (no literal `[0,1,2,3,4,5,6]` or `Array(7)` grid) and applies `TIER_CARD_CLASS[card.tier]` to each card.
    - Draft-pack cards have `draggable` and an `onDragStart` that writes a `pack:`-prefixed id, and have NO `onDrop`/`onDragOver` handler (one-way-out).
  </acceptance_criteria>
  <done>Tier-bordered, variable-size, tier-sorted, drag-source-only draft carousel exists and typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: BenchCarousel + DraftPackCarousel component tests</name>
  <files>packages/client/src/components/BenchCarousel.tsx, packages/client/src/components/DraftPackCarousel.test.tsx</files>
  <read_first>
    - packages/client/src/components/DraftPackCarousel.tsx (the shared inner card renderer + TIER_CARD_CLASS from Task 1 — reuse them in BenchCarousel)
    - packages/client/src/components/LineupAssignmentScreen.tsx (lines 202-232 handleDragStart/Over/Drop/End — bench must be a drop target using this same native pattern; lines 283-291 the placeholder block being conceptually replaced)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md (D-08 bench<->lineup both directions, D-09 dynamic size, D-21 same card style)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-VALIDATION.md (DRAFT-09 component test row; test framework = Vitest + @testing-library/react jsdom)
    - packages/client/vite.config.ts (the embedded test block / jsdom environment — confirm test setup)
  </read_first>
  <action>
    Create `BenchCarousel.tsx` reusing DraftPackCarousel's inner card renderer + TIER_CARD_CLASS. Props: `cards: TieredPoolPlayer[]`, `teamId: TeamId`, `benchNumbers?: Record<string, number>` (shown once draft completes), `onCardDragStart: (benchIndex: number) => void`, `onDropToBench: () => void`. Render `cards.map(...)` (dynamic length, 0..16, D-09) in a `.benchCarousel` track using the same card body + tier border. Each card is `draggable`, calls `onCardDragStart(benchIndex)` on drag start so the parent records the drag source, and also sets `e.dataTransfer.setData('text/plain', 'bench:' + benchIndex)` + `effectAllowed='move'` purely to satisfy native HTML5 drag initiation — this payload is NOT the resolution channel. The bench container is a drop target: `onDragOver` calls `preventDefault()` and `onDrop` calls `onDropToBench()` so a slot- or pack-sourced card can be dragged onto the bench (D-08). `onDropToBench` is payload-free BY DESIGN (`() => void`): it only signals that a drop landed on the bench. The parent (`LineupAssignmentScreen`, Plan 05 Task 1) resolves WHICH card was dropped and its origin from a single parent-owned drag-state variable set via the `onCardDragStart` callbacks — `BenchCarousel.onDrop` must NOT read `dataTransfer` to decide anything. When `benchNumbers` is provided, render each card's `#{benchNumbers[cardId]}` in the card header slot where LineupStatCard shows the jersey number. Empty bench (0 cards) renders the existing dashed placeholder style (reuse `.benchSlot`, D-22) — no new empty-state component.

    Create `DraftPackCarousel.test.tsx` (Vitest + @testing-library/react). Tests: (1) rendering a mixed-tier pack sorts cards rarest-first — assert DOM order has the chase-tier card before the common-tier card; (2) a 6-card pack renders exactly 6 cards (variable size, not 7) — assert the rendered card count equals the input length; (3) each card carries the correct tier border class via `TIER_CARD_CLASS`; (4) clicking `Next card` / `Previous card` nav buttons is wired (buttons present with correct aria-labels; the end arrow is disabled at the list start); (5) a card's `dragStart` populates dataTransfer with a `pack:`-prefixed id (fire a dragStart event with a mock dataTransfer and assert setData called with the expected string). Follow the jsdom render/screen/fireEvent style; mock dataTransfer as needed.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- DraftPackCarousel</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- DraftPackCarousel` runs and all tests pass.
    - A test proves rarest-first sort (chase before common in DOM order).
    - A test proves a 6-card input renders exactly 6 cards (variable pack size).
    - A test proves the drag-start writes a `pack:`-prefixed cardId to dataTransfer.
    - `BenchCarousel.tsx` renders `cards.map(...)` with no fixed-length array and typechecks (`pnpm --filter @counter-attack/client typecheck` exits 0).
  </acceptance_criteria>
  <done>Dynamic bench carousel and the draft carousel's component tests exist and pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                           |
| -------------------- | ----------------------------------------------------------------------------------------------------- |
| client render → user | Carousels are pure display + drag sources; they hold no authoritative game state and validate nothing |

## STRIDE Threat Register

| Threat ID           | Category                 | Component                         | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                              |
| ------------------- | ------------------------ | --------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-29-CLIENT-DISPLAY | Tampering                | DraftPackCarousel / BenchCarousel | accept      | These components only render server-supplied `TieredPoolPlayer[]` and emit drag intents; they perform no authoritative validation. All pick legality (card membership, GK-slot rule, cycle gating) is enforced server-side in Plan 04, so a tampered client render cannot change game outcome — the server rejects any illegal `DRAFT_PICK`. |
| T-29-SC             | Tampering (supply chain) | package installs                  | accept      | No npm packages installed (native HTML5 drag-and-drop; no DnD library per RESEARCH).                                                                                                                                                                                                                                                         |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client typecheck` green.
- `pnpm --filter @counter-attack/client test -- DraftPackCarousel` green.
- CSS grep confirms all five tier classes + carousel classes present.
</verification>

<success_criteria>

- DraftPackCarousel renders variable-size, tier-sorted, drag-source-only cards with tier borders and left/right nav (DRAFT-06, D-20).
- BenchCarousel renders a dynamic 0..16-card row that is both drag source and drop target (DRAFT-09, D-08/D-21).
- All new draft CSS classes live in LineupAssignmentScreen.module.css (Plan 05 adds no CSS).
- Standard-mode LineupAssignmentScreen visuals are untouched (only additive CSS classes).
  </success_criteria>

<output>
Create `.planning/phases/29-draft-ui-pick-and-swap-flow/29-03-SUMMARY.md` when done.
</output>
