---
phase: 29-draft-ui-pick-and-swap-flow
plan: 05
type: execute
wave: 3
depends_on: ['29-03']
files_modified:
  - packages/client/src/components/LineupAssignmentScreen.tsx
  - packages/client/src/App.tsx
  - packages/client/src/components/LineupAssignmentScreen.test.tsx
autonomous: true
requirements: [DRAFT-06, DRAFT-09, DRAFT-10]

must_haves:
  truths:
    - 'In draft mode the LineupAssignmentScreen renders the DraftPackCarousel above the formation grid and the BenchCarousel below it, over the same empty formation shell used by Standard mode (D-05/D-11/D-22)'
    - 'Dragging a card off the draft-pack row onto a lineup slot or the bench emits DRAFT_PICK{cardId,destination}; dragging an already-drafted card between slot and bench emits DRAFT_REARRANGE (D-05/D-06/D-08)'
    - "While waiting on the opponent's pick, the draft-pack row is disabled/dimmed and a 'Waiting for {Home/Visitor} Player to pick…' indicator plus the 'Cycle n of 4 · Pick k of {2 or 1}' counter are shown (D-12)"
    - 'The keeper-safety auto-pick shows a brief non-blocking banner; when the draft completes the carousel row disappears and the existing Confirm/LINEUP_CONFIRM flow takes over unchanged (DRAFT-08 banner, D-23)'
    - 'App routes draft-mode rooms to the draft screen on the initial DRAFT_STATE_UPDATED and keeps its DraftClientView in local (non-Zustand) state, updating on each DRAFT_STATE_UPDATED (D-14)'
  artifacts:
    - path: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      provides: 'draftMode branch: renders DraftPackCarousel + BenchCarousel, wires DRAFT_PICK/DRAFT_REARRANGE, waiting/counter/banner UI, GK reverse-lock messaging'
      contains: 'DraftPackCarousel'
    - path: 'packages/client/src/App.tsx'
      provides: 'DRAFT_STATE_UPDATED handler, draftView local state, handleDraftPick/handleDraftRearrange, draft routing'
      contains: 'DRAFT_STATE_UPDATED'
    - path: 'packages/client/src/components/LineupAssignmentScreen.test.tsx'
      provides: 'component tests for draft-mode rendering, drag-to-pick emit, waiting-state disable, draft-complete carousel hide, and Standard-mode non-regression'
      min_lines: 90
  key_links:
    - from: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      to: 'packages/client/src/components/DraftPackCarousel.tsx / BenchCarousel.tsx'
      via: 'import + render when draftMode'
      pattern: 'DraftPackCarousel|BenchCarousel'
    - from: 'packages/client/src/App.tsx'
      to: 'packages/shared/src/events.ts (DRAFT_STATE_UPDATED / DRAFT_PICK)'
      via: 'socket.on(DRAFT_STATE_UPDATED) + socket.emit(DRAFT_PICK)'
      pattern: 'DRAFT_STATE_UPDATED|DRAFT_PICK'
---

<objective>
Assemble the full draft interaction on the client: extend `LineupAssignmentScreen` with a `draftMode` branch that overlays the draft-pack carousel and dynamic bench onto the existing empty formation shell, wire the drag gestures to `DRAFT_PICK`/`DRAFT_REARRANGE`, render the waiting indicator / cycle-pick counter / keeper banner, and route App to the draft screen off `DRAFT_STATE_UPDATED`. This is the last implementation plan — after it, the phase is fully playable end-to-end.

Purpose: Deliver the combined draft+lineup+bench single-screen interaction (D-05) that is the heart of the phase.
Output: extended `LineupAssignmentScreen.tsx`, extended `App.tsx`, and a new `LineupAssignmentScreen.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-UI-SPEC.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-PATTERNS.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-VALIDATION.md
@packages/client/src/components/LineupAssignmentScreen.tsx
@packages/client/src/App.tsx
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-01-SUMMARY.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-03-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by THIS plan (new — exclude from drift/source-grounding checks):

- `packages/client/src/components/LineupAssignmentScreen.tsx`: new props `draftMode?: boolean`, `draftView?: DraftClientView`, `onDraftPick?: (cardId: string, destination: DraftDestination) => void`, `onDraftRearrange?: (from: DraftSlotRef, to: DraftSlotRef) => void`; draft-mode render branch; GK reverse-lock rejection messages
- `packages/client/src/App.tsx`: `onDraftStateUpdated` handler, `draftView` local state, `handleDraftPick`, `handleDraftRearrange`, draft-mode routing to `LINEUP_ASSIGNMENT`
- `packages/client/src/components/LineupAssignmentScreen.test.tsx`: new test file (also covers Standard-mode non-regression)

<tasks>

<task type="auto">
  <name>Task 1: Extend LineupAssignmentScreen with the draftMode branch</name>
  <files>packages/client/src/components/LineupAssignmentScreen.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx (whole file — Props type lines 29-44, LineupStatCard, handleDragStart/Over/Drop/End lines 202-232, renderColumn lines 236-261, the bench section lines 283-291, rejection-message effect lines 163-172, the JSX return)
    - packages/client/src/components/DraftPackCarousel.tsx and BenchCarousel.tsx (their prop contracts from Plan 03 — DraftPackCarousel: cards/teamId/disabled/onCardDragStart; BenchCarousel: cards/teamId/benchNumbers/onCardDragStart/onDropToBench; TIER_CARD_CLASS)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-UI-SPEC.md (Copywriting Contract — screen heading "STEP 4", waiting indicator, cycle/pick counter, keeper banner, GK rejection copy; Component Notes 3/4/5)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md (D-05, D-06, D-07, D-08, D-09, D-10, D-12, D-22, D-23; UI-SPEC Component Note 5 keeper auto-place)
    - packages/shared/src/types.ts (DraftClientView / DraftDestination / DraftSlotRef shapes from Plan 01)
  </read_first>
  <action>
    Extend the `Props` type with optional `draftMode?: boolean`, `draftView?: DraftClientView`, `onDraftPick?: (cardId: string, destination: DraftDestination) => void`, `onDraftRearrange?: (from: DraftSlotRef, to: DraftSlotRef) => void`. Keep all existing Standard-mode props/behavior intact; the draft branch is additive and only active when `draftMode` is true.

    When `draftMode` is true, drive the formation grid from `draftView.lineupSlots` (a `(string|null)[]` of 11) instead of the Standard `assignment` — resolve each non-null id via the existing PLAYER_MAP; render an empty-slot placeholder (reuse the dashed `.benchSlot`/existing empty style, D-22) for null slots so the empty formation shell shows role-labeled empty slots (D-11). Render `DraftPackCarousel` ABOVE the formation columns (cards = `draftView.currentPack`, teamId = myTeamId, disabled = `draftView.waitingForOpponent`, onCardDragStart records the dragged pack card id in local state). Render `BenchCarousel` BELOW the grid (cards resolved from `draftView.benchIds`, benchNumbers = `draftView.benchNumbers` when `draftComplete`).

    Resolve every drag via a SINGLE parent-owned drag-state variable — never via `dataTransfer` reads inside child `onDrop` handlers. Declare one local `dragState` in `LineupAssignmentScreen` (a `useRef` or `useState` holding `{ cardId: string; source: 'pack' | 'slot' | 'bench'; slotIndex?: number; benchIndex?: number } | null`). Set it from each drag-start callback: `DraftPackCarousel.onCardDragStart(cardId)` -> `{ cardId, source: 'pack' }`; `BenchCarousel.onCardDragStart(benchIndex)` -> `{ cardId: draftView.benchIds[benchIndex], source: 'bench', benchIndex }`; and the lineup slots' OWN `onDragStart` (parent-owned in this component) -> `{ cardId, source: 'slot', slotIndex }`. Then resolve every drop from `dragState` ALONE: dropping onto a lineup slot -> if `dragState.source === 'pack'` call `onDraftPick(dragState.cardId, { type:'slot', slotIndex })`, else call `onDraftRearrange({ from-ref derived from dragState }, { type:'slot', slotIndex })` (D-05/D-08/D-10); a bench drop arrives as `BenchCarousel.onDropToBench()` (payload-free) and the parent reads `dragState` -> if `source === 'pack'` call `onDraftPick(dragState.cardId, { type:'bench' })`, else `onDraftRearrange({ from-ref from dragState }, { type:'bench' })`. Clear `dragState` in `onDragEnd`. The `pack:`/`bench:`/`slot:` `text/plain` payloads that Plan 03's cards set exist ONLY to satisfy native HTML5 drag initiation — they are never read at drop time; the dropped card's id/source is read exclusively from `dragState`. Enforce, client-side for UX only, the GK-slot rule both directions and show the UI-SPEC rejection copy via the existing `setRejectionMessage`/2000ms pattern: "Swap rejected — only a goalkeeper card can be placed here." (non-GK onto GK slot) and "Swap rejected — goalkeeper slot requires a GK card." (GK card onto outfield slot). Extend the existing `gameError` effect to also map the server reasons `GK_SLOT_REQUIRES_GK` / `NON_GK_SLOT_REJECTS_GK` / `INVALID_CARD` to their messages (server remains the authority; client message is UX). Cards leaving the draft-pack row can never be dropped back into it (D-06 — the carousel exposes no drop target, enforced in Plan 03).

    Above the carousel, render the cycle/pick counter `Cycle {draftView.cycle} of 4 · Pick {picksRemaining} of {picksRemaining <= 1 ? 1 : 2}` and, when `draftView.waitingForOpponent`, the disabled-row treatment plus `Waiting for {Home/Visitor} Player to pick…`. When `draftView.keeperAutoPickedThisCycle` is true, show the brief `Keeper auto-selected — cycle 4 safety net.` banner (reuse the EventBanner fade pattern or a simple timed banner using the `.keeperBanner` class from Plan 03). Use the heading `MATCH SETUP: STEP 4 — {HOME/VISITOR} PLAYER (YOU)`. When `draftView.draftComplete` is true, DO NOT render the DraftPackCarousel row at all (D-23) — leave only the finalized lineup + bench, and render the existing Confirm button so the unchanged LINEUP_CONFIRM flow takes over. In Standard mode (draftMode falsy), render exactly as today.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client typecheck` exits 0.
    - `grep` confirms `DraftPackCarousel` and `BenchCarousel` are imported and conditionally rendered under a `draftMode` guard.
    - The draft branch renders lineup slots from `draftView.lineupSlots` and hides the DraftPackCarousel when `draftView.draftComplete` is true.
    - Drag drops resolve the dropped card's id/source from a single parent-owned `dragState` variable (NOT from `dataTransfer` reads in child `onDrop` handlers): a `dragState.source==='pack'` drop calls `onDraftPick`, a `'slot'`/`'bench'` drop calls `onDraftRearrange` (grep confirms both callback invocations and the `dragState` variable).
    - Standard-mode render path (draftMode falsy) is unchanged — the existing return branch still renders the formation columns from `assignment`.
  </acceptance_criteria>
  <done>LineupAssignmentScreen renders and wires the full draft interaction in draft mode without altering Standard mode.</done>
</task>

<task type="auto">
  <name>Task 2: App routing + DRAFT_STATE_UPDATED wiring</name>
  <files>packages/client/src/App.tsx</files>
  <read_first>
    - packages/client/src/App.tsx (whole file — the useEffect socket registration block lines 54-187, the lineup local-state pattern lines 47-52, onLineupAssignmentReady lines 145-149, the LINEUP_ASSIGNMENT render branch lines 234-243, teamType local state line 43, handleLineupSwap/handleLineupConfirm lines 219-228)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md (D-13 reconnect resume, D-14 unicast private, D-23 draft-complete hand-off)
    - packages/shared/src/events.ts (DRAFT_STATE_UPDATED / DRAFT_PICK / DRAFT_REARRANGE signatures from Plan 01)
  </read_first>
  <action>
    Add local (non-Zustand, Pitfall 7) state `const [draftView, setDraftView] = useState<DraftClientView | null>(null)`. Register a handler `onDraftStateUpdated(view)` in the existing useEffect that `setDraftView(view)` and, if the screen is not already `LINEUP_ASSIGNMENT`, `setScreen('LINEUP_ASSIGNMENT')` (draft rooms route to the draft screen on the FIRST DRAFT_STATE_UPDATED, which the server sends from the UNIFORM_CONFIRM away-branch — Plan 04; also covers reconnect resume, D-13). Add matching `socket.on(ServerEvents.DRAFT_STATE_UPDATED, onDraftStateUpdated)` and the corresponding `socket.off` in the cleanup (Pitfall 9 — every on has a matching off). Add `handleDraftPick(cardId, destination)` emitting `ClientEvents.DRAFT_PICK` and `handleDraftRearrange(from, to)` emitting `ClientEvents.DRAFT_REARRANGE`.

    In the `LINEUP_ASSIGNMENT` render branch, pass the draft props when `teamType === 'draft'`: `draftMode={teamType === 'draft'}`, `draftView={draftView}`, `onDraftPick={handleDraftPick}`, `onDraftRearrange={handleDraftRearrange}`. Keep the existing Standard-mode props (assignment/onSwap/onConfirm/lineupConfirmed) passed as today. Note: in draft mode the empty `assignment` array (Array(11).fill(null)) sent by the server is not the render source — the draftView.lineupSlots is; still pass `assignment={lineupAssignment ?? []}` so the component's Standard-path props stay type-satisfied. Confirm the existing `onLineupAssignmentReady` remains the Standard-mode route (draft mode never receives LINEUP_ASSIGNMENT_READY — Plan 04). When `draftView.draftComplete` becomes true, the same LINEUP_ASSIGNMENT screen stays mounted and the existing Confirm/handleLineupConfirm flow finalizes the match (D-23) — no new route needed.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client typecheck` exits 0.
    - `grep` confirms `socket.on(ServerEvents.DRAFT_STATE_UPDATED` AND a matching `socket.off(ServerEvents.DRAFT_STATE_UPDATED` in App.tsx (listener cleanup).
    - `handleDraftPick` emits `ClientEvents.DRAFT_PICK` and `handleDraftRearrange` emits `ClientEvents.DRAFT_REARRANGE`.
    - The LINEUP_ASSIGNMENT branch passes `draftMode`, `draftView`, `onDraftPick`, `onDraftRearrange` to LineupAssignmentScreen gated on `teamType === 'draft'`.
  </acceptance_criteria>
  <done>App routes draft rooms to the draft screen, threads the private draftView, and wires pick/rearrange emits with proper listener cleanup.</done>
</task>

<task type="auto">
  <name>Task 3: LineupAssignmentScreen component tests (draft + Standard non-regression)</name>
  <files>packages/client/src/components/LineupAssignmentScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx (the extended props + draft branch from Task 1)
    - packages/client/src/components/DraftPackCarousel.test.tsx (Plan 03's test style — render/screen/fireEvent, dataTransfer mocking — mirror it)
    - packages/client/vite.config.ts (jsdom test environment + any setup file)
    - packages/client/src/store/useGameStore.ts (the store shape — draft branch reads gameError; tests may need to seed it or mock the selector)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-VALIDATION.md (DRAFT-06 / DRAFT-09 component test rows — `-t "draft"`)
  </read_first>
  <action>
    Create `LineupAssignmentScreen.test.tsx` (Vitest + @testing-library/react, jsdom). Draft-mode tests (construct a representative `DraftClientView`: cycle 2, subStep 'PICK1', a 7-card currentPack of TieredPoolPlayers, picksRemaining 1, waitingForOpponent false, lineupSlots with a couple filled + rest null, benchIds with 1-2 ids, draftComplete false): (1) the DraftPackCarousel renders above the formation grid and the BenchCarousel below (assert both present and DOM order carousel-before-grid); (2) dropping a `pack:`-sourced card onto a lineup slot calls `onDraftPick` with `{type:'slot',slotIndex}` (fire drag/drop with a mock dataTransfer carrying the `pack:` payload); (3) when `waitingForOpponent` is true the draft-pack row is disabled (carousel receives `disabled` / the `.draftRowDisabled` treatment) and the "Waiting for … to pick" text shows; (4) the "Cycle 2 of 4 · Pick 1 of 1" counter renders; (5) when `draftComplete` is true the DraftPackCarousel is NOT rendered and the Confirm button is present (D-23); (6) `keeperAutoPickedThisCycle` true renders the keeper banner text. Standard-mode non-regression test: rendering with `draftMode` falsy and a Standard `assignment` still renders the 4 formation columns and the Confirm button exactly as before (this is the first-ever test for this component per the Wave-0 gap — it also guards Plan 04's server gate did not change the client's Standard path).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- LineupAssignmentScreen</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- LineupAssignmentScreen` runs and all tests pass.
    - A draft-mode test proves DraftPackCarousel renders above the grid and BenchCarousel below.
    - A test proves a pack-sourced drop calls `onDraftPick` with a slot destination.
    - A test proves the waiting state disables the row and shows the waiting text.
    - A test proves draftComplete hides the carousel and shows Confirm.
    - A Standard-mode test proves the non-draft render path is unchanged (4 columns + Confirm).
  </acceptance_criteria>
  <done>Draft-mode rendering, drag-to-pick, waiting-state, completion hand-off, and Standard-mode non-regression are all covered by passing component tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                                           |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| user drag gesture → client emit | The client translates drags into DRAFT_PICK/DRAFT_REARRANGE emits; it performs UX-only validation and never holds authoritative state |

## STRIDE Threat Register

| Threat ID                          | Category                 | Component                                  | Disposition                 | Mitigation Plan                                                                                                                                                                                                                                                                                     |
| ---------------------------------- | ------------------------ | ------------------------------------------ | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-29-01 / T-29-06 (client surface) | Tampering                | LineupAssignmentScreen draft drag handlers | mitigate (defense-in-depth) | Client-side GK-slot and pack-membership checks are UX-only; the server (Plan 04, T-29-01/T-29-06) re-validates every DRAFT_PICK/DRAFT_REARRANGE and is the sole authority. The client renders only its own server-supplied `DraftClientView` (T-29-PRIV upheld — App stores only the private view). |
| T-29-SC                            | Tampering (supply chain) | package installs                           | accept                      | No npm packages installed (native drag-and-drop; carousels from Plan 03).                                                                                                                                                                                                                           |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client typecheck` green.
- `pnpm --filter @counter-attack/client test` (full client suite) green — new component tests plus existing tests unaffected.
</verification>

<success_criteria>

- Draft mode renders the carousel-over-lineup single-screen interaction (DRAFT-06, D-05) with dynamic bench (DRAFT-09) and correct waiting/counter/banner UI (D-12, DRAFT-08).
- Drag-to-pick emits DRAFT_PICK; rearrangement emits DRAFT_REARRANGE (D-05/D-08); one-way-out is enforced (D-06).
- Draft completion hides the carousel and hands off to the existing Confirm flow (D-23); numbering/badge/colors display via existing props (DRAFT-10).
- Standard mode is fully non-regressed (proven by the new component test).
  </success_criteria>

<output>
Create `.planning/phases/29-draft-ui-pick-and-swap-flow/29-05-SUMMARY.md` when done.
</output>
