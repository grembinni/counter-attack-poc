---
phase: 29-draft-ui-pick-and-swap-flow
verified: 2026-07-22T01:00:00Z
status: human_needed
score: 6/7 truths verified (Truth 7 excluded from denominator — out of Phase 29 scope); 1 human verification item still outstanding (unrelated to this plan)
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: '6/7 must-have truths verified (Truth 7 excluded); 1 in-scope truth (Truth 4 / DRAFT-09) failed'
  gaps_closed:
    - 'Truth 4 / DRAFT-09: bench carousel scroll-reset-on-every-re-render — closed by 29-12 (benchCards useMemo in LineupAssignmentScreen.tsx keyed on [draftView?.benchIds, cardCache]; BenchCarousel scroll-reset effect re-keyed from [cards] identity to content-derived [benchKey]). Independently confirmed by direct code read AND a negative-control test (reverted the effect dependency back to [cards], confirmed the new regression test fails exactly as predicted, then restored and reconfirmed green).'
  gaps_remaining: []
  regressions: []
human_verification:
  - test: 'Live two-browser lineup-slot <-> lineup-slot swap confirmation'
    expected: "The dragged card takes the destination slot; the previously-occupying card takes the dragged card's now-vacated slot. Repeat for both an outfield<->outfield swap and an attempted GK<->outfield swap (the latter should be rejected with an error, not silently applied)."
    why_human: "Carried forward unresolved from the prior two verification passes. STATE.md still shows no completion of this walkthrough (last_activity references 'Phase 29 execution started' 2026-07-22, current stopped_at text is stale/mid-execution and does not mention this item being closed). No UAT artifact exists in the phase directory. The 29-10 fix remains proven correct at the protocol layer only (unit + real socket.io integration tests, re-run and confirmed green by this verifier) — the live DOM drag-and-drop confirmation required by 29-10-PLAN.md's own success criteria has still not been performed, and this plan (29-12) did not touch slot<->slot swap code, so it cannot close this item."
---

# Phase 29: Draft UI + Pick-and-Swap Flow Verification Report

**Phase Goal:** Players can complete a full draft session in real time — a 7-card carousel screen appears between team selection and lineup; 4 pick-and-swap cycles deliver 16 cards per player; keeper safety triggers automatically on the 4th cycle if needed; overflow drafted players appear on a dynamic bench carousel; post-draft, players are NOT auto-repositioned (the lineup/bench arrangement made during the draft stands, D-15) — jersey numbers are applied automatically (starters keep their role-appropriate slot numbers; bench players receive a random unused number in the 15-99 range, D-15/D-16) and team colors are applied.
**Verified:** 2026-07-22T01:00:00Z
**Status:** human_needed
**Re-verification:** Yes — after 29-12 gap-closure plan (bench carousel scroll-reset instability, DRAFT-09)

## Goal Achievement

### Observable Truths

| #                                                              | Truth                                                                                                                                                                                                   | Status                                                                  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1                                                              | Draft screen appears with 7-card carousel above lineup grid (DRAFT-06)                                                                                                                                  | VERIFIED (regression)                                                   | `LineupAssignmentScreen.tsx:564` renders `<DraftPackCarousel cards={draftView.currentPack} .../>` when `!draftView.draftComplete`. Confirmed passing in full client suite I ran directly (370/370 green).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2                                                              | Two players complete 4 pick-and-swap cycles delivering 16 cards each, via real-time WebSocket coordination (DRAFT-07)                                                                                   | VERIFIED (regression)                                                   | `draftSession.test.ts` "full 4-cycle drive" test passes; full server suite re-run by me (611/611 green, 1 skipped, 1 todo). Real-time coordination invariants (CR-01/CR-02/CR-03) remain closed per Truth 6.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 3                                                              | Cycle-4 keeper safety auto-selects a keeper (DRAFT-08)                                                                                                                                                  | VERIFIED (regression)                                                   | `checkKeeperSafety (DRAFT-08)` tests pass in the full server run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 4                                                              | Overflow drafted players appear on a dynamically-sized bench carousel using the same card display as the draft stage, and the carousel remains usable during normal draft-screen interaction (DRAFT-09) | **VERIFIED — gap closed**                                               | Direct code read confirms the fix exactly as 29-12-SUMMARY.md describes: `LineupAssignmentScreen.tsx:291-296` — `benchCards` is now a top-level `useMemo(() => ..., [draftView?.benchIds, cardCache])`, placed before the `if (!formation) return null;` early return (rules-of-hooks compliant), replacing the previously-unmemoized inline `.map()/.filter()`. `BenchCarousel.tsx:66,70-75` — scroll-reset `useEffect` now keys on a content-derived `benchKey = cards.map(c => c.id).join('                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ')`instead of`[cards]`reference identity. I independently ran a negative control: reverted the effect's dependency array back to`[cards]`, re-ran `BenchCarousel.test.tsx`, and confirmed the new regression test ("does NOT reset scroll on an unrelated re-render") fails exactly as predicted (`expected +0 to be 150`) — proving the test is a genuine regression guard, not a tautology. Restored the fix and re-confirmed 8/8 green. Full client suite also re-run clean (370/370, up from 368/368 pre-fix, matching the 2 new tests exactly). |
| 5                                                              | Post-draft: no auto-repositioning, jersey numbers + team colors applied (DRAFT-10)                                                                                                                      | VERIFIED (regression)                                                   | `assignBenchNumbers (D-15/D-16)` tests pass; draft-mode `LINEUP_CONFIRM` roster-resolution integration tests pass in the full server run. No code in this plan touched this area.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 6                                                              | Real-time WebSocket coordination enforces draft-completion invariants consistently (CR-01/CR-02/CR-03)                                                                                                  | VERIFIED (regression, previously closed by 29-11)                       | `draftSession.integration.test.ts` and `draftReconnect.integration.test.ts` CR-01/CR-02/CR-03 tests pass in the full server run (611/611). No code in this plan touched `roomHandlers.ts`/`createServer.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 7 (out of Phase 29 SC scope, carried forward for completeness) | Reconnect handler correctly falls through to fresh-connection registration for a still-valid session token whose room is not (or no longer) `'playing'`                                                 | FAILED (pre-existing, NOT introduced by Phase 29, NOT touched by 29-12) | Carried forward unchanged from the prior verification pass. 29-REVIEW.md's "CR-01" finding (`createServer.ts:99-167`): an unconditional `return;` at line 166 sits outside the `room.status === 'playing'` inner `if` but inside the outer session-token check, so a socket whose room is `'waiting'` (or gone) never falls through to fresh-connection registration. Traced via `git log -p` to commit `0b65df4` (Phase 07), well before Phase 29 existed. `roomStore.ts` confirms `room.status` flips to `'playing'` the instant the second player joins — before team selection, formation selection, or the draft screen — so this bug's trigger window sits entirely outside the draft flow Phase 29's success criteria describe. Not counted against Phase 29's score; flagged because Phase 29 is the last phase in the current roadmap and there is no later phase to catch it. This plan (29-12) is client-only and did not touch `createServer.ts`, so it cannot have closed or reopened this item — confirmed unchanged by direct read. |

**Score:** 6/7 must-have truths verified (Truth 7 excluded from the denominator — out of Phase 29's success-criteria scope). All in-scope truths now VERIFIED; the one remaining item is a human-verification requirement (see below), not a code gap.

### Required Artifacts

| Artifact                                                                                  | Expected                                                                                  | Status                                     | Details                                                                                                                                                                                                                                                                                                            |
| ----------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `packages/client/src/components/LineupAssignmentScreen.tsx`                               | `benchCards` memoized on `[draftView?.benchIds, cardCache]`, hoisted above early return   | VERIFIED                                   | Read directly at lines 283-296; matches 29-12-SUMMARY.md and 29-12-REVIEW.md exactly. Only one `benchCards` binding remains in the file (confirmed via grep).                                                                                                                                                      |
| `packages/client/src/components/BenchCarousel.tsx`                                        | Scroll-reset effect keyed on content-derived `benchKey`, not `[cards]` identity           | VERIFIED                                   | Read directly at lines 61-75; `benchKey` computed unconditionally each render, effect depends on `[benchKey]` only. Grep confirms no remaining `[cards]` dependency array in the file.                                                                                                                             |
| `packages/client/src/components/BenchCarousel.test.tsx`                                   | New regression tests proving scroll stability on identity churn + reset on content change | VERIFIED                                   | Read both new tests (lines 155-234) directly; ran them myself (8/8 pass) and independently proved genuineness via negative control (see Truth 4).                                                                                                                                                                  |
| `packages/server/src/roomHandlers.ts` / `createServer.ts` (29-11 CR-01/CR-02/CR-03 fixes) | Unchanged, still passing                                                                  | VERIFIED (regression)                      | Full server suite re-run (611/611); not touched by 29-12 (client-only plan).                                                                                                                                                                                                                                       |
| `.planning/REQUIREMENTS.md`                                                               | DRAFT-06..10 checked off and correctly mapped to Phase 29                                 | ⚠️ STALE (unchanged from prior two passes) | DRAFT-06 still unchecked (`- [ ]`, line 36); traceability table (lines 88-92) still maps all five requirements to a nonexistent "Phase 30". Documentation-only, not a functional gap.                                                                                                                              |
| `.planning/STATE.md`                                                                      | Reflects Phase 29 / Plan 12 completion status                                             | ⚠️ STALE                                   | `git diff` shows STATE.md was updated at the _start_ of 29-12 execution (`status: executing`, `Plan 1 of 12`, `last_updated: 2026-07-22T00:01:48Z`) but was never updated again after 29-12 completed at `00:08:48Z` per its own SUMMARY. Tracking/orchestration artifact issue, not a code gap — flagged as info. |

### Key Link Verification

| From                                                | To                           | Via                                                                        | Status | Details                                                                                                                                                                 |
| --------------------------------------------------- | ---------------------------- | -------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `LineupAssignmentScreen.tsx` `benchCards` (useMemo) | `BenchCarousel` `cards` prop | Direct prop pass-through, now stable reference across unrelated re-renders | WIRED  | Confirmed by direct read; dependency array `[draftView?.benchIds, cardCache]` does not include `setDraftDropTargetIndex`/`setRejectionMessage` state.                   |
| `BenchCarousel` `cards` prop                        | Scroll-reset `useEffect`     | `benchKey` content-derived dependency                                      | WIRED  | Confirmed by direct read at lines 66, 75; independently verified via negative-control test (reverting to `[cards]` reproduces the original bug and fails the new test). |

### Data-Flow Trace (Level 4)

| Artifact        | Data Variable                     | Source                                                                                                                     | Produces Real Data                             | Status    |
| --------------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | --------- |
| `BenchCarousel` | `cards` (= memoized `benchCards`) | `draftView.benchIds.map(resolveTieredCard).filter(...)` sourced from server-authoritative `DRAFT_STATE_UPDATED` broadcasts | Yes — real drafted-card data, not static/empty | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior                                                                    | Command                                                                                     | Result                                                                    | Status                                         |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------- |
| BenchCarousel test file (targeted)                                          | `pnpm --filter @counter-attack/client exec vitest run BenchCarousel`                        | 8/8 passed                                                                | PASS                                           |
| Negative control — revert fix, confirm regression test fails                | Manually reverted `[benchKey]` → `[cards]` in `BenchCarousel.tsx`, re-ran the same command  | 7/8 passed; 1 failed exactly as predicted (`expected +0 to be 150`)       | PASS (proves test is genuine, not a tautology) |
| Restore fix, re-confirm green                                               | `git checkout -- packages/client/src/components/BenchCarousel.tsx`, re-ran the same command | 8/8 passed                                                                | PASS                                           |
| Full client suite green (regression safety)                                 | `pnpm --filter @counter-attack/client test -- --run`                                        | 370/370 passed (up from 368/368 pre-fix; matches SUMMARY's claim exactly) | PASS                                           |
| Client typecheck                                                            | `pnpm --filter @counter-attack/client typecheck`                                            | exit 0                                                                    | PASS                                           |
| Full server suite green (regression safety, unaffected by client-only plan) | `pnpm --filter @counter-attack/server test`                                                 | 611/611 passed, 1 skipped, 1 todo                                         | PASS                                           |
| Debt-marker scan on all 3 files modified by 29-12                           | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` on the 3 files                      | No matches                                                                | PASS                                           |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes declared in any Phase 29 plan or found in the repository. SKIPPED (no runnable entry points of this kind).

### Requirements Coverage

| Requirement | Source Plan(s)          | Description                                                             | Status                                                                                     | Evidence                                                                                                                                                                                                                      |
| ----------- | ----------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DRAFT-06    | 29-01/03/05/06/08/09    | Draft carousel screen between team/formation selection and lineup       | SATISFIED (functionally) — ⚠️ REQUIREMENTS.md checkbox stale (carried forward, unresolved) | Truth 1. Checkbox still `[ ]`, traceability still points to nonexistent Phase 30.                                                                                                                                             |
| DRAFT-07    | 29-02/04/06/09/11       | 4-cycle pick-and-swap protocol, 16 cards/player, real-time coordination | SATISFIED                                                                                  | Truth 2 + Truth 6                                                                                                                                                                                                             |
| DRAFT-08    | 29-02/04/06/09          | Cycle-4 keeper safety                                                   | SATISFIED                                                                                  | Truth 3                                                                                                                                                                                                                       |
| DRAFT-09    | 29-03/07/08/09/10/12    | Dynamic bench carousel                                                  | **SATISFIED — gap closed by 29-12**                                                        | Truth 4. Scroll-reset defect (29-REVIEW.md CR-02 / prior verification's Critical Gap #1) is now closed and independently confirmed via direct code read plus a negative-control test proving the regression guard is genuine. |
| DRAFT-10    | 29-02/04/05/07/09/10/11 | Post-draft numbering, no auto-reposition, colors                        | SATISFIED                                                                                  | Truth 5                                                                                                                                                                                                                       |

**Orphaned requirements:** None — all five requirements appear in at least one Phase 29 plan's `requirements:` frontmatter field, including 29-12's `[DRAFT-09]`.

### Anti-Patterns Found

None in the 29-12 diff (grep confirmed no TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER in `LineupAssignmentScreen.tsx`, `BenchCarousel.tsx`, or `BenchCarousel.test.tsx`).

### Warnings (Non-Blocking, Carried From 29-12-REVIEW.md)

- **WR-01 (29-12-REVIEW.md)**: The two new regression tests cover "identical ids, new reference" (no reset) and "id added" (reset), but not "same ids reordered" or "id removed" — both plausible real events per the fix's own documented contract (order-significant `benchKey`). The underlying implementation is correct by construction (a reorder produces a different joined string, so the effect will still fire), but test coverage of this specific claim is incomplete. Non-blocking; recommend adding the two additional test cases 29-12-REVIEW.md proposes.
- **IN-01 (29-12-REVIEW.md)**: `benchKey`'s `'|'`-joined id string has a theoretical (not live) delimiter-collision edge case if card ids ever contained `|`. Not exploitable today (`PoolPlayer.id` values are internally generated, e.g. `p001`, and never contain `|`).
- **Carried forward, unchanged from prior two verification passes** (non-blocking, out of Phase 29 scope):
  - `createServer.ts` reconnect misplaced `return` (pre-existing Phase 07 defect, see Truth 7) — recommend a dedicated remediation task since Phase 29 is the last phase in the current roadmap.
  - WR-01 (prior pass, `roomHandlers.ts:965-975`): `DRAFT_REARRANGE` bench-index bounds validation gap, not currently exploitable.
  - WR-02 (prior pass, `roomHandlers.ts`/`draftSession.ts`): bench jersey numbers never assigned for cards benched via post-`draftComplete` `DRAFT_REARRANGE` — cosmetic only.
  - WR-03 (prior pass): client-side tier-color cache does not survive reconnect/reload — cosmetic only.
  - REQUIREMENTS.md traceability staleness (DRAFT-06 checkbox, Phase 30 references).
  - STATE.md not updated after 29-12 completed (tracking artifact issue, not a code gap).
  - Uncommitted local changes (`LineupAssignmentScreen.module.css`, `player-pool.csv`) — still present, still unrelated to any Phase 29 plan's `files_modified`, not evaluated as part of this verification.

## Human Verification Required

1. **Live two-browser lineup-slot <-> lineup-slot swap confirmation**
   - **Test:** Complete a draft in a two-browser session, then drag a card from one filled lineup slot onto another filled lineup slot.
   - **Expected:** Both cards trade places; neither lands on the bench. A GK<->outfield attempted swap is rejected with an error.
   - **Why human:** Carried forward unresolved from the prior two verification passes. This is unrelated to plan 29-12 (client-only bench-carousel scroll fix) and no code change in this plan could resolve it. The fix is proven correct at the protocol layer (unit + real socket.io integration tests, re-run and confirmed green by this verifier) but the live DOM drag-and-drop confirmation required by 29-10-PLAN.md's own success criteria has still not been performed. No UAT artifact exists in the phase directory confirming it was done.

## Gaps Summary

No open code-level gaps remain against Phase 29's 5 roadmap success criteria. The only previously in-scope defect (bench carousel scroll instability, DRAFT-09) is closed and independently re-confirmed by direct code reading plus a self-run negative-control test (not accepted on SUMMARY.md's or 29-12-REVIEW.md's word). Status is `human_needed` solely because one pre-existing human-verification item (live two-browser slot<->slot swap) remains unperformed and is outside this plan's scope to close. A second item (Truth 7, pre-existing Phase 07 reconnect defect) is flagged for visibility but explicitly excluded from Phase 29's score since it sits outside this phase's draft-flow trigger window and this plan did not touch it.

## Verification Metadata

**Verification approach:** Direct code inspection of the 29-12 fix (read, not trusted from SUMMARY/REVIEW) in both `LineupAssignmentScreen.tsx` and `BenchCarousel.tsx` + a self-run negative-control test (temporarily reverted the fix, confirmed the new regression test fails exactly as predicted, restored and reconfirmed green) + full client suite re-run (370/370) + full server suite re-run (611/611, 1 skipped, 1 todo, unaffected by this client-only plan) + client typecheck (exit 0) + requirements cross-reference against REQUIREMENTS.md + STATE.md/git-log check for the still-outstanding human-verification item.
**Must-haves source:** ROADMAP.md Phase 29 Success Criteria (5) + prior 29-VERIFICATION.md must-haves (carried forward) + 29-12-PLAN.md frontmatter must_haves.
**Automated checks:** targeted BenchCarousel test (8/8, plus negative-control run), full client suite (370/370), full server suite (611/611), client typecheck (exit 0), debt-marker grep (clean) — all run directly by this verifier in this session.
**Human checks:** 1 outstanding (live two-browser slot<->slot swap confirmation) — carried forward, unrelated to 29-12, still pending per STATE.md and absence of any UAT artifact.
**Total verification time:** ~20 minutes (code reads, negative-control test cycle, 6 test/build command executions, git status/log checks).

---

_Verified: 2026-07-22T01:00:00Z_
_Verifier: Claude (gsd-verifier, re-verification pass after 29-12 gap-closure)_
