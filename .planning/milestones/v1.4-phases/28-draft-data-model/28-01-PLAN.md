---
phase: 28-draft-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/data/player-pool.csv
  - packages/shared/scripts/seed-rosters.ts
  - packages/shared/src/teams.ts
  - packages/shared/src/teams.test.ts
  - packages/shared/src/types.ts
autonomous: true
requirements: [DRAFT-04, DRAFT-05]

must_haves:
  truths:
    - "Exactly 10 free-agent players carry a poolTag (5 'legend', 5 'icon'); every other player has poolTag undefined"
    - "No player's name (firstName or lastName) contains a '(L)' or '(M)' suffix after regeneration"
    - "The 'original' pool derivation (free-agent AND no poolTag) yields exactly 46 players"
    - 'DraftTier, TIER_PERCENTILE_BOUNDS, PACKS_PER_MATCH, and PACK_COMPOSITION are exported from the shared barrel as configurable constants'
  artifacts:
    - path: 'packages/shared/src/teams.ts'
      provides: 'Regenerated PLAYER_POOL with poolTag field on PoolPlayer'
      contains: 'poolTag'
    - path: 'packages/shared/src/data/player-pool.csv'
      provides: 'PoolTag column + suffix-stripped Player names'
      contains: 'PoolTag'
    - path: 'packages/shared/scripts/seed-rosters.ts'
      provides: 'poolTag threaded through RawPlayer, PlayerEntry, parseRow, serializePlayer, emitted interface'
      contains: 'poolTag'
    - path: 'packages/shared/src/types.ts'
      provides: 'DraftTier type + TIER_PERCENTILE_BOUNDS/PACKS_PER_MATCH/PACK_COMPOSITION constants'
      contains: 'PACK_COMPOSITION'
  key_links:
    - from: 'packages/shared/src/data/player-pool.csv'
      to: 'packages/shared/src/teams.ts'
      via: 'pnpm run seed:rosters codegen'
      pattern: 'poolTag'
---

<objective>
Establish the shared data-model foundation for Draft mode: add the `poolTag` field to the player pool (D-01, D-02, D-03) and add the draft tier / pack configuration vocabulary to `types.ts` (DRAFT-04 tier type + boundary constants, DRAFT-05 pack constants — D-05, D-10, D-11).

Purpose: Everything downstream (the tier-classification and pack-generation engine in plans 28-02/28-03/28-04) reads from this data and these constants. `poolTag` lets the engine exclude the 10 reserved Legends/Icons superstars from the `'original'` pool (D-04) without runtime regex, and the exported constants make all tier boundaries and pack composition counts configurable per DRAFT-04/DRAFT-05 criterion 3.
Output: Regenerated `teams.ts` with `PoolPlayer.poolTag`, an updated CSV + seed script, poolTag data assertions in `teams.test.ts`, and four new exported symbols in `types.ts`.
</objective>

<artifacts_this_phase_produces>
Symbols/paths this plan creates (exclude from source-drift verification — they are new here):

- CSV column: `PoolTag` (in `player-pool.csv`)
- `PoolPlayer.poolTag` field (optional `'legend' | 'icon'`) — emitted into generated `teams.ts`
- `RawPlayer.poolTag`, `PlayerEntry.poolTag`, and poolTag parse/emit logic in `seed-rosters.ts`
- `types.ts` exports: `DraftTier` (type), `TIER_PERCENTILE_BOUNDS` (const), `PACKS_PER_MATCH` (const), `PACK_COMPOSITION` (const)
  </artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-draft-data-model/28-CONTEXT.md
@.planning/phases/28-draft-data-model/28-PATTERNS.md
@packages/shared/src/data/player-pool.csv
@packages/shared/scripts/seed-rosters.ts
@packages/shared/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread poolTag through the CSV → seed → teams.ts codegen pipeline (D-01/D-02/D-03)</name>
  <files>packages/shared/src/data/player-pool.csv, packages/shared/scripts/seed-rosters.ts, packages/shared/src/teams.ts</files>
  <read_first>
    - packages/shared/src/data/player-pool.csv (current header has NO SourceTeam column; the `Team` column holds slugs directly — do not assume a SourceTeam column exists)
    - packages/shared/scripts/seed-rosters.ts (RawPlayer interface ~L91-107; parseRow ~L129-188; PlayerEntry interface ~L190-208; buildSquadEntries map ~L234-251; free-agent inline push ~L325-349; serializePlayer ~L256-276; the EMITTED PoolPlayer interface template string ~L387-415)
    - packages/shared/src/teams.ts (AUTO-GENERATED header L1-10 — never hand-edit; PoolPlayer interface L17-41)
    - .planning/phases/28-draft-data-model/28-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Edit the CSV and seed script ONLY, then regenerate `teams.ts` — never hand-edit `teams.ts` (it is AUTO-GENERATED).

    CSV (`player-pool.csv`): Append a new trailing header column named exactly `PoolTag` (single word, no comma — satisfies the seed script's CR-02 header-comma check). For the 10 rows whose `Player` cell ends with a space + `(L)` or space + `(M)` suffix, (a) remove that exact ` (L)`/` (M)` suffix from the `Player` cell and (b) append the tag value as the trailing PoolTag cell: `legend` for the 5 `(L)` rows, `icon` for the 5 `(M)` rows. The 10 rows are: `Diego Maradona (L)`→`Diego Maradona`/legend, `Paolo Maldini (L)`→`Paolo Maldini`/legend, `Pelé (L)`→`Pelé`/legend, `Ronaldinho (L)`→`Ronaldinho`/legend, `Zinedine Zidane (L)`→`Zinedine Zidane`/legend, `Cristiano Ronaldo (M)`→`Cristiano Ronaldo`/icon, `Erling Haaland (M)`→`Erling Haaland`/icon, `Kevin De Bruyne (M)`→`Kevin De Bruyne`/icon, `Neymar Jr (M)`→`Neymar Jr`/icon, `Virgil van Dijk (M)`→`Virgil van Dijk`/icon. DECOYS — do NOT tag `Neymar Andre` or `Cristiano Ribeiro` (they have no `(L)`/`(M)` suffix). Untagged rows need no trailing PoolTag cell — the seed parser reads `row[idx['PoolTag']] ?? ''`, so a missing trailing cell yields undefined (ordinary player).

    Edge case (D-03, per PATTERNS.md): `Pelé (L)` and `Ronaldinho (L)` carry the suffix as the ENTIRE name-remainder. After stripping, their `Player` cell is a single mononym (`Pelé` / `Ronaldinho`). The existing `parseRow` splits on the first space, so a mononym yields `firstName='Pelé'`, `lastName=''`. Empty lastName is intended and display-safe — `ActionLog.tsx` renders `piece.lastName ? \`${firstName} ${lastName}\` : firstName`. Do NOT special-case a leading space in the CSV (the parser trims every cell).

    `seed-rosters.ts`: (1) Add `poolTag?: 'legend' | 'icon';` to the `RawPlayer` interface. (2) In `parseRow`, read the column and whitelist it: derive `poolTagRaw` from `(row[idx['PoolTag']] ?? '').trim()`, then set `poolTag` to that value only when it is exactly `'legend'` or `'icon'`, otherwise `undefined`; include `poolTag` in the returned object. (3) Add `poolTag?: 'legend' | 'icon';` to the `PlayerEntry` interface. (4) In the free-agent inline push, pass `poolTag: p.poolTag`. (5) In `buildSquadEntries`'s per-player map, pass `poolTag: p.poolTag` (always undefined for squads — harmless). (6) In `serializePlayer`, after the `highPass` line and before the closing brace, emit a guarded line that outputs `poolTag: '<value>',` ONLY when `p.poolTag` is defined (mirror the all-fields style; do not emit the line when undefined). (7) In the EMITTED PoolPlayer interface template string, add the field after `highPass: number;` with a doc comment `/** D-02 (Phase 28): reserved Legends/Icons tag; undefined = ordinary free agent, included in the 'original' pool. */` then `poolTag?: 'legend' | 'icon';`.

    Regenerate: run `pnpm --filter @counter-attack/shared seed:rosters`. Commit the regenerated `teams.ts` (it is a committed artifact, not a build step). The count assertion (EXPECTED_TOTAL=188) must still pass — poolTag is additive, the pool stays 188.

  </action>
  <verify>
    <automated>cd packages/shared && pnpm run seed:rosters && cd ../.. && test "$(grep -cE "poolTag: '(legend|icon)'" packages/shared/src/teams.ts)" = "10" && test "$(grep -cE "poolTag: 'legend'" packages/shared/src/teams.ts)" = "5" && test "$(grep -cE "poolTag: 'icon'" packages/shared/src/teams.ts)" = "5" && test "$(grep -cE "\((L|M)\)" packages/shared/src/teams.ts)" = "0" && pnpm --filter @counter-attack/shared test teams</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/shared seed:rosters` exits 0 and logs `Total players: 188`
    - `grep -cE "poolTag: '(legend|icon)'" packages/shared/src/teams.ts` returns exactly `10`
    - `grep -cE "poolTag: 'legend'" packages/shared/src/teams.ts` returns exactly `5`; `poolTag: 'icon'` returns exactly `5`
    - `grep -cE "\((L|M)\)" packages/shared/src/teams.ts` returns `0` (no `(L)`/`(M)` remnants in names or comments' emitted data)
    - The generated `teams.ts` Pelé entry has `firstName: 'Pelé'`, `lastName: ''`, `poolTag: 'legend'`; the Ronaldinho entry has `firstName: 'Ronaldinho'`, `lastName: ''`, `poolTag: 'legend'`
    - The generated `teams.ts` PoolPlayer interface declares `poolTag?: 'legend' | 'icon';`
    - `pnpm --filter @counter-attack/shared test teams` passes (existing 188-count and sequential-ID tests still green)
  </acceptance_criteria>
  <done>The CSV, seed script, and regenerated teams.ts all carry poolTag; exactly 10 tagged (5 legend / 5 icon); no name suffixes remain; the two mononyms parse to empty lastName; the pool is still 188.</done>
</task>

<task type="auto">
  <name>Task 2: Add draft tier + pack configuration vocabulary to types.ts (D-05/D-10/D-11)</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts (Phase 27 draft vocabulary L445-467 — TeamType, DraftPoolId, SELECTABLE_DRAFT_POOLS; copy this JSDoc+union+const house style)
    - .planning/phases/28-draft-data-model/28-CONTEXT.md (D-05 tier bands, D-10 PACKS_PER_MATCH, D-11 composition)
  </read_first>
  <action>
    Immediately after the `SELECTABLE_DRAFT_POOLS` const (ends ~L467), add four exported declarations. Do NOT import `PoolPlayer` into `types.ts` — `teams.ts` imports `HexCoord` FROM `types.ts`, so a `PoolPlayer` import here would create a circular dependency. Player-referencing output types (`TieredPoolPlayer`, `DraftPack`) are intentionally deferred to `draftEngine.ts` in plans 28-02/28-03.

    (1) `export type DraftTier = 'chase' | 'rare' | 'uncommon' | 'common' | 'keeper';` with JSDoc citing DRAFT-04 (Phase 28), D-13 (tier value only, no display constants this phase).

    (2) `export const TIER_PERCENTILE_BOUNDS` typed `Readonly<Record<'chase' | 'rare' | 'uncommon', number>>` with values `{ chase: 90, rare: 80, uncommon: 60 }`. JSDoc (cite D-05, D-06): these are percentile FLOORS on a 0–100 scale where 100 = highest total stat; an outfield player's rank-based percentile is tested `>= chase → 'chase'`, else `>= rare → 'rare'`, else `>= uncommon → 'uncommon'`, else `'common'`. Configurable per DRAFT-04.

    (3) `export const PACKS_PER_MATCH = 8;` with JSDoc citing DRAFT-05, D-10 (configurable packs generated per match).

    (4) `export const PACK_COMPOSITION` typed `Readonly<Record<DraftTier, number>>` with values `{ chase: 1, rare: 1, uncommon: 1, common: 3, keeper: 1 }` (sums to 7). JSDoc cites DRAFT-05, D-11 (confirmed 1/1/1/3/1, configurable).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared typecheck && test "$(grep -cE "export (type DraftTier|const TIER_PERCENTILE_BOUNDS|const PACKS_PER_MATCH|const PACK_COMPOSITION)" packages/shared/src/types.ts)" = "4"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/shared typecheck` passes
    - `types.ts` exports all four: `DraftTier`, `TIER_PERCENTILE_BOUNDS`, `PACKS_PER_MATCH`, `PACK_COMPOSITION`
    - `PACK_COMPOSITION` values are `chase:1, rare:1, uncommon:1, common:3, keeper:1` (sum 7)
    - `TIER_PERCENTILE_BOUNDS` values are `chase:90, rare:80, uncommon:60`
    - `types.ts` does NOT import from `./teams.js` (no circular import)
  </acceptance_criteria>
  <done>Four configurable draft constants/types are exported from types.ts and reachable via the shared barrel; no circular import introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Assert poolTag data integrity in teams.test.ts (D-01/D-02/D-03)</name>
  <files>packages/shared/src/teams.test.ts</files>
  <read_first>
    - packages/shared/src/teams.test.ts (vitest describe/it style, grouped by requirement ID — copy this shape)
    - packages/shared/src/teams.ts (regenerated from Task 1 — the poolTag data to assert)
  </read_first>
  <action>
    Add a new `describe('PoolPlayer poolTag — DRAFT-04 / D-01 / D-02 / D-03: reserved Legends/Icons tagging', ...)` block with `it` cases asserting:
    - Exactly 10 players have a defined `poolTag`; exactly 5 equal `'legend'` and 5 equal `'icon'`.
    - The 5 legends match the expected players (Maradona, Maldini, Pelé, Ronaldinho, Zidane) and the 5 icons match (Ronaldo, Haaland, De Bruyne, Neymar Jr, van Dijk) — identify each by firstName/lastName.
    - No player's `firstName` or `lastName` contains `(L)` or `(M)` (regex `/\((L|M)\)/`).
    - The two mononyms: the player with `firstName === 'Pelé'` has `lastName === ''` and `poolTag === 'legend'`; likewise `firstName === 'Ronaldinho'`, `lastName === ''`, `poolTag === 'legend'`.
    - Decoys stay untagged: the player named `Neymar Andre` and the player named `Cristiano Ribeiro` both have `poolTag === undefined`.
    - Original-pool derivation: `PLAYER_POOL.filter(p => p.sourceTeamId === 'free-agent' && !p.poolTag)` has length 46 (56 free agents − 10 tagged); of those, exactly 4 have `role === 'GK'`.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test teams</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/shared test teams` passes with the new poolTag describe block present
    - Tests fail if any of: tagged count ≠ 10, legend/icon split ≠ 5/5, a name still contains `(L)`/`(M)`, a mononym has non-empty lastName, a decoy is tagged, or the original-pool count ≠ 46
  </acceptance_criteria>
  <done>teams.test.ts encodes the poolTag contract (counts, split, suffix removal, mononym edge, decoys, original-pool size) and passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                                                                                                                                                   |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CSV/seed build-time input → generated teams.ts | Data-integrity boundary: a malformed `PoolTag` cell or botched suffix strip would corrupt the pool consumed by the entire draft engine. No runtime/user input crosses here — this is a developer-authored, committed dataset. |

## STRIDE Threat Register

| Threat ID   | Category  | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                             |
| ----------- | --------- | ----------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-28-DATA   | Tampering | player-pool.csv `PoolTag` column + suffix strip | mitigate    | Whitelist parse in `parseRow` (only `'legend'`/`'icon'` accepted, else `undefined`); seed `EXPECTED_TOTAL=188` fail-fast assertion; `teams.test.ts` asserts exact 10 tagged / 5-5 split / no `(L)`/`(M)` remnants / mononym handling / decoys untagged / original-pool = 46 |
| T-28-DATA-2 | Tampering | Circular import types.ts↔teams.ts               | mitigate    | Player-referencing types kept out of `types.ts`; typecheck gate confirms no cycle                                                                                                                                                                                           |
| T-28-SC     | Tampering | package installs                                | accept      | No npm/pip/cargo packages installed this phase (only existing `tsx`/`vitest`); no supply-chain surface — no legitimacy checkpoint required                                                                                                                                  |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared seed:rosters` regenerates a 188-player teams.ts with 10 poolTag rows.
- `pnpm --filter @counter-attack/shared typecheck` passes (new constants compile, no circular import).
- `pnpm --filter @counter-attack/shared test teams` passes (existing + new poolTag assertions).
</verification>

<success_criteria>

- `PoolPlayer.poolTag` exists on the regenerated pool; exactly 10 free agents tagged (5 legend / 5 icon); no name suffixes remain; mononyms handled.
- `DraftTier`, `TIER_PERCENTILE_BOUNDS`, `PACKS_PER_MATCH`, `PACK_COMPOSITION` are exported configurable constants (DRAFT-04/DRAFT-05 criterion 3 foundation).
- Original-pool derivation (free-agent AND no poolTag) = 46 players, verified by test.
  </success_criteria>

<output>
Create `.planning/phases/28-draft-data-model/28-01-SUMMARY.md` when done.
</output>
