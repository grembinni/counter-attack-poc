---
phase: 30-recalibrate-draft
plan: 06
type: execute
wave: 4
depends_on: [30-02, 30-04, 30-05]
files_modified: []
autonomous: false
requirements: [DRAFT-05, DRAFT-08, DRAFT-11]
must_haves:
  truths:
    - 'The full 3-package test suite is green and the whole workspace typechecks'
    - 'No superseded draft identifier survives anywhere: keeper (as DraftTier/field), PACKS_PER_MATCH, PACK_COMPOSITION, TIER_PERCENTILE_BOUNDS, checkKeeperSafety, cardTierKeeper'
    - 'A live two-browser draft plays through all 6 rounds with GK-only round 1, tiered rounds 2-6, and tier-colored borders visible on carousel, bench, and starting-11'
  artifacts: []
  key_links: []
---

<objective>
Final phase gate: prove the recalibrated draft is complete and green across all three packages, confirm no superseded identifier lingers (tests passing does not guarantee a clean purge), and human-verify the full 6-round draft flow in a live two-browser session.

Purpose: This is the convergence checkpoint. It catches partial-rename residue (RESEARCH Pitfalls 4/6/7 warn that `tsc` + tests passing is not sufficient evidence the old model was fully purged) and validates the round-flow UX end-to-end, which component/integration tests cannot fully capture across round boundaries.

Output: A recorded full-suite + grep-purge result and a human-verified draft walkthrough.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-recalibrate-draft/30-VALIDATION.md
@.planning/phases/30-recalibrate-draft/30-UI-SPEC.md
@.planning/phases/30-recalibrate-draft/30-02-SUMMARY.md
@.planning/phases/30-recalibrate-draft/30-04-SUMMARY.md
@.planning/phases/30-recalibrate-draft/30-05-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 06)

None — this plan is verification-only (no source changes).

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + typecheck gate + superseded-identifier purge grep</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/30-recalibrate-draft/30-VALIDATION.md (Sampling Rate → "Before /gsd-verify-work" grep pass)
    - .planning/phases/30-recalibrate-draft/30-RESEARCH.md (Common Pitfalls 4/6/7 — the exact identifiers to grep for)
  </read_first>
  <action>
    Run the full workspace test suite (`pnpm test`) and full typecheck (`pnpm typecheck`) — both must be green. Then run a superseded-identifier purge grep across `packages/` (excluding `.planning/` and `node_modules/`) for each of these old-model tokens and confirm zero functional matches (a match only survives as an intentional historical note in a SUMMARY, never in `packages/` source/tests): the `'keeper'` DraftTier literal and `cardTierKeeper`/`.cardTierKeeper`/`keeperBanner`/`keeperAutoPickedThisCycle`/`homeHasKeeper`/`awayHasKeeper`/`checkKeeperSafety`/`autoSelectKeeperIfMissing`; `PACKS_PER_MATCH`; `PACK_COMPOSITION`; `TIER_PERCENTILE_BOUNDS`; `FALLBACK_POOL_ORDER` with an `international` member; `openNextPack`; `assignPackOrders`; and stray `.cycle`/`cycle:` draft-session references (distinguish from unrelated non-draft uses). Any surviving functional match in `packages/` is a purge failure that must be fixed before the human checkpoint. Record the pass/fail of the suite, typecheck, and each grep in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` and `pnpm typecheck` both exit 0
    - `grep -rIl "PACKS_PER_MATCH\|PACK_COMPOSITION\|TIER_PERCENTILE_BOUNDS\|checkKeeperSafety\|cardTierKeeper\|keeperAutoPickedThisCycle\|openNextPack\|assignPackOrders" packages/` returns no files
    - No `FALLBACK_POOL_ORDER` definition includes an `international` member
  </acceptance_criteria>
  <done>Full suite and typecheck are green and every superseded draft identifier is purged from packages/ source and tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Two-browser full 6-round draft human verification</name>
  <files>(human verification — no files modified)</files>
  <what-built>The recalibrated 6-round draft: fixed-threshold tiers, GK-only round 1, tiered rounds 2-6 with position-bucket caps and unbiased chase-or-rare slots, Legends/Icons pools, and the 4-color tier borders extended to bench and starting-11 lineup slots.</what-built>
  <action>
    Guide the user through a live two-browser draft walkthrough and collect their approval. Steps for the user:
    1. Start the app locally (`pnpm dev` or the project `/run` flow) and open two browser windows sharing one room code.
    2. On the settings pre-step, select Draft mode and confirm Legends and Icons checkboxes are now selectable (no "(coming soon)"); start a draft (any valid pool selection).
    3. Round 1: confirm each player's pack shows 4 GK-only cards and the label reads "GK Round" / "Pick 1 of 2"; complete round 1 (2 picks, draft then swap then draft, no third pick).
    4. Rounds 2-6: confirm packs are 4 cards with no GK, the label reads "Round N of 6" / "Pick X of 3", and each round runs draft-swap-draft-swap-draft (3 picks).
    5. Confirm tier-colored borders (purple chase, red rare, green uncommon, white common) appear on the draft carousel, the bench carousel, AND the starting-11 lineup slot cards after placement — no keeper-safety banner ever appears.
    6. Complete the draft, confirm the lineup, and confirm the match starts with the drafted roster (17 cards drafted per player).
  </action>
  <verify>
    <human-check>User confirms all six walkthrough steps pass in a live two-browser session.</human-check>
  </verify>
  <done>User types "approved" after confirming round structure, tier colors, pool availability, and border coverage.</done>
  <resume-signal>Type "approved" or describe any issues (round structure, tier colors, pool availability, missing borders).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                              |
| -------- | -------------------------------------------------------- |
| n/a      | Verification-only plan; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category  | Component                  | Disposition | Mitigation Plan                                                                                                                                            |
| --------- | --------- | -------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-30-01   | Tampering | End-to-end pool allow-list | mitigate    | The human walkthrough exercises the live Legends/Icons path; the server allow-list (Plan 05) remains the authoritative gate confirmed by integration tests |
| T-30-SC   | Tampering | npm/pnpm installs          | accept      | Zero new packages this phase (RESEARCH Package Legitimacy Audit: Not applicable)                                                                           |

</threat_model>

<verification>
- Full suite green, full typecheck green, purge grep clean, human walkthrough approved.
</verification>

<success_criteria>

- All automated gates pass; superseded identifiers purged; the live 6-round draft is human-verified.
  </success_criteria>

<output>
Create `.planning/phases/30-recalibrate-draft/30-06-SUMMARY.md` when done.
</output>
