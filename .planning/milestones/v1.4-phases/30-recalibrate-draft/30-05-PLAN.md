---
phase: 30-recalibrate-draft
plan: 05
type: execute
wave: 3
depends_on: [30-02, 30-03]
files_modified:
  - packages/server/src/roomHandlers.ts
  - packages/server/src/draftPacks.ts
  - packages/server/src/__tests__/draftPacks.test.ts
  - packages/server/src/__tests__/draftSession.integration.test.ts
  - packages/server/src/__tests__/draftReconnect.integration.test.ts
  - packages/server/src/__tests__/lineupAssignment.integration.test.ts
autonomous: true
requirements: [DRAFT-05, DRAFT-08, DRAFT-11]
must_haves:
  truths:
    - 'roomHandlers wires the round-aware session: openNextRound replaces openNextPack, and the checkKeeperSafety call block is removed (D-21)'
    - 'The DRAFT_PICK handler drives the 6-round flow to completion end-to-end over real crypto RNG'
    - 'Legends and Icons are accepted by the server ROOM_SETTINGS_CONFIRM allow-list; a draft using them generates valid packs via backfill (D-08/D-10)'
    - 'generateMatchPacks binds crypto.randomInt and returns the round-structured packs unchanged (fairness boundary preserved)'
    - 'A forged draftPools payload containing an unknown pool id is rejected server-side (INVALID_DRAFT_POOL)'
  artifacts:
    - path: 'packages/server/src/roomHandlers.ts'
      provides: 'Round-aware draft wiring with keeper-safety call removed and legends/icons accepted via the shared allow-list'
      contains: 'openNextRound'
  key_links:
    - from: 'packages/server/src/roomHandlers.ts'
      to: 'packages/server/src/draftSession.ts'
      via: 'openNextRound / advanceSubStep / applyPick calls'
      pattern: 'openNextRound'
    - from: 'packages/server/src/roomHandlers.ts'
      to: 'packages/shared/src/types.ts'
      via: 'SELECTABLE_DRAFT_POOLS allow-list validation (now 5 values)'
      pattern: 'SELECTABLE_DRAFT_POOLS'
---

<objective>
Wire the round-aware draft session into the server socket layer and rewrite the server integration tests for the recalibrated model: remove the keeper-safety call, rename the pack-opening call, confirm the Legends/Icons allow-list, and prove the full 6-round flow end-to-end over the real CSPRNG (DRAFT-05/DRAFT-08/DRAFT-11).

Purpose: This is the final integration layer — it connects the pure state machine (Plan 03) and the round-structured pack generator (Plan 02) to the live WebSocket handlers, and it greens the server package (which has been in an accepted cross-plan RED window since the shared type contract landed). It also confirms the server-authoritative pool allow-list now admits Legends/Icons, closing the DRAFT-11 server side.

Output: A keeper-safety-free, round-aware `roomHandlers.ts`; a verified `draftPacks.ts` pass-through; and rewritten `draftPacks`, `draftSession.integration`, `draftReconnect.integration`, and `lineupAssignment.integration` test suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-recalibrate-draft/30-CONTEXT.md
@.planning/phases/30-recalibrate-draft/30-RESEARCH.md
@.planning/phases/30-recalibrate-draft/30-PATTERNS.md
@packages/server/src/roomHandlers.ts
@packages/server/src/draftPacks.ts
@.planning/phases/30-recalibrate-draft/30-02-SUMMARY.md
@.planning/phases/30-recalibrate-draft/30-03-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 05)

- Round-aware `roomHandlers.ts` draft wiring (openNextRound call, keeper-safety block removed)
- Verified `draftPacks.ts` pass-through (signature unchanged; forwards round-tagged packs)
- Rewritten server integration test suites for the 6-round model

Removed (intentional): the `checkKeeperSafety` import + the cycle-4 keeper-safety call block in roomHandlers; the `openNextPack` call site.

Consumes: Plan 02 `generateDraftPacks`/`generateMatchPacks` (round packs), Plan 03 `openNextRound`/`advanceSubStep`/`applyPick`/`assignBenchNumbers`, Plan 01 widened `SELECTABLE_DRAFT_POOLS`.

<tasks>

<task type="auto">
  <name>Task 1: Round-aware roomHandlers wiring + draftPacks pass-through + allow-list confirm</name>
  <files>packages/server/src/roomHandlers.ts, packages/server/src/draftPacks.ts</files>
  <read_first>
    - packages/server/src/roomHandlers.ts (draft imports ~44-61; ROOM_SETTINGS_CONFIRM allow-list ~410-449; openNextPack call ~577; DRAFT_PICK handler ~855-914 including the checkKeeperSafety block ~895-904 and the draftComplete/assignBenchNumbers block ~908-914)
    - packages/server/src/draftPacks.ts (full — the 12-line generateMatchPacks wrapper binding crypto.randomInt)
    - .planning/phases/30-recalibrate-draft/30-RESEARCH.md (Security Domain — V4 Access Control allow-list; State of the Art)
    - .planning/phases/30-recalibrate-draft/30-PATTERNS.md (roomHandlers section — confirm SELECTABLE_DRAFT_POOLS is read dynamically, not a hardcoded 3-pool literal)
  </read_first>
  <action>
    Remove the `checkKeeperSafety` import from the draft-session import block and DELETE the entire cycle-4 keeper-safety call block in the DRAFT_PICK handler (the `if (room.draftSession.cycle === 4 && ... ) { room.draftSession = checkKeeperSafety(...) }` guard) — the round-1 GK pack round supersedes it (D-21). Rename the `openNextPack(room.draftSession!)` call site to `openNextRound(...)` (and its import) per Plan 03's renamed export. Update any `room.draftSession.cycle` references in this handler to `room.draftSession.round`. Keep the `advanceSubStep` call and the draftComplete→`assignBenchNumbers` block; keep the D-09/Pitfall-4 GK-slot role validation (`card.role === 'GK'` vs `FormationSlot.slotRole`) unchanged. Confirm the ROOM_SETTINGS_CONFIRM allow-list at ~418-425 validates `draftPools` against `SELECTABLE_DRAFT_POOLS` (imported from shared, now 5 values) rather than a hardcoded `['original','mls','international']` literal — if a hardcoded literal is present anywhere in this handler, replace it with the imported const so Legends/Icons are admitted through the single source of truth (D-08, V4 Access Control); the duplicate-rejection (IN-03) and DRAFT_POOL_REQUIRED guards stay. Verify `draftPacks.ts` needs no change: `generateMatchPacks` forwards `crypto.randomInt` into `generateDraftPacks` and returns its `{ pool, packs }` (round-tagged) unchanged — the fairness boundary (T-28-04-FAIR) is preserved; only confirm the return type still flows through.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server exec tsc --noEmit -p tsconfig.json 2>&1 | grep -c "error TS" | grep -qx 0 && echo SERVER_TYPECHECK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "checkKeeperSafety\|openNextPack\|\.cycle" packages/server/src/roomHandlers.ts` returns 0
    - `grep -c "openNextRound\|SELECTABLE_DRAFT_POOLS" packages/server/src/roomHandlers.ts` >= 1 each
    - No hardcoded `['original','mls','international']` pool literal remains in the ROOM_SETTINGS_CONFIRM handler
    - The full server package typechecks (`tsc --noEmit` reports 0 errors) — closing the cross-plan RED window
  </acceptance_criteria>
  <done>roomHandlers drives the round-aware session with no keeper-safety call, the pool allow-list admits Legends/Icons via the shared const, and the server package typechecks green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite draftPacks + draftSession integration tests</name>
  <files>packages/server/src/__tests__/draftPacks.test.ts, packages/server/src/__tests__/draftSession.integration.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/draftPacks.test.ts (generateMatchPacks end-to-end structural invariants; per-pool no-throw loop)
    - packages/server/src/__tests__/draftSession.integration.test.ts (the DRAFT_PICK socket flow, cycle/keeper assertions to rewrite)
    - packages/server/src/roomHandlers.ts (the Plan-05 wiring under test)
    - .planning/phases/30-recalibrate-draft/30-RESEARCH.md (Common Pitfall 2 — legends/icons-only must succeed via backfill; Pitfall 8 — remove keeper-safety tests)
  </read_first>
  <action>
    Rewrite `draftPacks.test.ts` for the round model: assert `generateMatchPacks(pools)` returns 12 round-tagged 4-card packs with GK-only round 1 and no-GK rounds 2-6, and extend the per-pool no-throw loop to include `['legends']` and `['icons']` (D-08/D-10 — must NOT throw; backfill supplies round-1 GKs). Rewrite `draftSession.integration.test.ts` to drive the DRAFT_PICK socket handler through the full 6-round flow: 2 picks in round 1 (no PICK3), 3 picks in rounds 2-6, both sides reaching draftComplete with 17 drafted each (D-16), bench numbers assigned at completion. DELETE all cycle-4 keeper-safety integration assertions (Pitfall 8 — the mechanic no longer exists; do not leave them referencing removed code). Add an allow-list test: a ROOM_SETTINGS_CONFIRM with a Legends/Icons selection is accepted, and one with an unknown pool id is rejected with INVALID_DRAFT_POOL (T-30-01). Update every `cycle` reference in these tests to `round`.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test draftPacks draftSession.integration</automated>
  </verify>
  <acceptance_criteria>
    - Both suites pass
    - `grep -ci "keeper\|checkKeeperSafety" packages/server/src/__tests__/draftSession.integration.test.ts` returns 0
    - A test proves `generateMatchPacks(['legends'])` and `(['icons'])` do not throw
    - A test proves a Legends/Icons ROOM_SETTINGS_CONFIRM is accepted and an unknown pool id is rejected
  </acceptance_criteria>
  <done>Pack-generation and draft-session integration tests exercise the 6-round flow end-to-end over real crypto RNG, admit Legends/Icons, and contain zero keeper-safety references.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite draftReconnect + lineupAssignment integration tests</name>
  <files>packages/server/src/__tests__/draftReconnect.integration.test.ts, packages/server/src/__tests__/lineupAssignment.integration.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/draftReconnect.integration.test.ts (reconnect draft-resume flow, cycle/keeper references)
    - packages/server/src/__tests__/lineupAssignment.integration.test.ts (draft→lineup handoff into game start)
    - packages/server/src/roomHandlers.ts (reconnect re-sync + LINEUP_CONFIRM draft-resolution paths)
  </read_first>
  <action>
    Rewrite `draftReconnect.integration.test.ts` so its mid-draft reconnect assertions use the round model (round-based progress, round-tagged currentPack re-sync) and remove any `cycle`/keeper references. Rewrite `lineupAssignment.integration.test.ts` so the draft→lineup→game-start handoff drives a full 6-round draft to completion (17 cards per side) before LINEUP_CONFIRM, asserting the resolved roster (draftSession lineup slots + bench) flows into game start unchanged. Update `cycle`→`round` throughout. Do not weaken any existing D-13/D-15/D-24 (post-draft rearrange, reconnect, hand-off stats) integration coverage carried over from Phase 29 — port it to the round model rather than dropping it.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test draftReconnect lineupAssignment</automated>
  </verify>
  <acceptance_criteria>
    - Both suites pass
    - `grep -ci "keeper" packages/server/src/__tests__/draftReconnect.integration.test.ts packages/server/src/__tests__/lineupAssignment.integration.test.ts` returns 0
    - The lineup handoff test drives a full 6-round draft (17 cards/side) before game start
  </acceptance_criteria>
  <done>Reconnect and lineup-handoff integration tests exercise the round model end-to-end and carry forward the prior draft→game lifecycle coverage.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                         |
| --------------------------------------- | ----------------------------------------------------------------------------------- |
| client → server (ROOM_SETTINGS_CONFIRM) | Untrusted draftPools; server allow-list is authoritative                            |
| client → server (DRAFT_PICK)            | Untrusted card id + destination; validated against server pool + GK-slot role rules |

## STRIDE Threat Register

| Threat ID | Category  | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                            |
| --------- | --------- | ------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-30-01   | Tampering | ROOM_SETTINGS_CONFIRM pool allow-list | mitigate    | Validate `draftPools` against the imported SELECTABLE_DRAFT_POOLS (now 5 values) plus the existing duplicate-rejection; reject unknown ids with INVALID_DRAFT_POOL. Integration test asserts both the accept (legends/icons) and reject (unknown id) paths |
| T-30-03   | Tampering | DRAFT_PICK GK-slot placement          | mitigate    | Keep the existing GK-slot role validation (`card.role === 'GK'` vs slotRole) unchanged; combined with Plan 02's generation-time GK exclusion from rounds 2-6, no GK card can be dealt into or placed outside its valid slot                                |
| T-30-02   | Tampering | Pack randomness                       | mitigate    | generateMatchPacks remains the sole CSPRNG binding (crypto.randomInt); the shared engine never sources randomness — confirmed unchanged pass-through                                                                                                       |
| T-30-SC   | Tampering | npm/pnpm installs                     | accept      | Zero new packages this phase (RESEARCH Package Legitimacy Audit: Not applicable)                                                                                                                                                                           |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` is fully green.
- `pnpm --filter @counter-attack/server exec tsc --noEmit` reports 0 errors (server RED window closed).
</verification>

<success_criteria>

- roomHandlers wires the round-aware session with keeper-safety removed and Legends/Icons admitted.
- All four server integration suites pass on the 6-round model.
- Server package fully green.
  </success_criteria>

<output>
Create `.planning/phases/30-recalibrate-draft/30-05-SUMMARY.md` when done.
</output>
