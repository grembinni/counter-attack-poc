---
phase: 30-recalibrate-draft
plan: 04
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - packages/client/src/components/DraftPackCarousel.tsx
  - packages/client/src/components/DraftPackCarousel.test.tsx
  - packages/client/src/components/BenchCarousel.tsx
  - packages/client/src/components/BenchCarousel.test.tsx
  - packages/client/src/components/LineupAssignmentScreen.tsx
  - packages/client/src/components/LineupAssignmentScreen.module.css
  - packages/client/src/components/LineupAssignmentScreen.test.tsx
  - packages/client/src/components/GameSettingsScreen.test.tsx
autonomous: true
requirements: [DRAFT-08, DRAFT-11]
must_haves:
  truths:
    - 'Tier borders use the 4-color scheme: chase #a855f7, rare #ef4444, uncommon #22c55e (green), common #ffffff (white); no keeper color exists (D-22/D-05)'
    - 'The tier-colored border appears on the starting-11 lineup slot cards, the bench carousel, AND the draft carousel (D-23)'
    - "resolveTieredCard resolves any card's tier via classifyTier(computeTotalStat(player)) with no role-based 'keeper' fallback (D-05)"
    - 'The keeper auto-selected banner (state, effect, JSX, and .keeperBanner CSS) is removed entirely (D-21)'
    - "The draft progress label reads 'Round N of 6' with a per-round pick count derived from DRAFT_ROUNDS, not a hardcoded 'of 4'/'of 2' (D-20)"
    - "Legends and Icons pool checkboxes render enabled and clickable with no '(coming soon)' label (D-08)"
  artifacts:
    - path: 'packages/client/src/components/DraftPackCarousel.tsx'
      provides: '4-value TIER_ORDER/TIER_CARD_CLASS (keeper dropped)'
      contains: 'TIER_CARD_CLASS'
    - path: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      provides: 'classifyTier-based resolveTieredCard, tier-bordered LineupStatCard, round-aware progress label, keeper-banner removed'
      contains: 'classifyTier'
  key_links:
    - from: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      to: 'packages/shared/src/draftEngine.ts'
      via: 'resolveTieredCard imports classifyTier + computeTotalStat'
      pattern: 'classifyTier'
    - from: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      to: 'packages/client/src/components/DraftPackCarousel.tsx'
      via: 'LineupStatCard reuses TIER_CARD_CLASS for the tier border'
      pattern: 'TIER_CARD_CLASS'
---

<objective>
Extend the recalibrated tier system across the client: narrow the tier-color scheme to 4 values with the corrected D-22 hexes, extend the tier border from the draft carousel to the starting-11 lineup slots and bench (D-23), replace the role-based keeper fallback with exact classifyTier resolution (D-05), remove the dead keeper-safety banner (D-21), make the draft progress label round-aware (D-20), and invert the Legends/Icons checkbox tests now that those pools are selectable (D-08/DRAFT-11).

Purpose: Tier color must be visible everywhere a drafted card appears — draft carousel, bench, and starting-11 — with the correct 4-color mapping and no leftover keeper artifacts. This is the client half of the recalibration; it consumes only the Plan 01 shared contract (narrowed DraftTier, classifyTier, round-named DraftClientView), so it runs in parallel with the shared/server Wave 2 plans.

Output: 4-color tier borders on all three card surfaces, a round-aware progress label, a keeper-banner-free LineupAssignmentScreen, and inverted Legends/Icons checkbox tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-recalibrate-draft/30-CONTEXT.md
@.planning/phases/30-recalibrate-draft/30-PATTERNS.md
@.planning/phases/30-recalibrate-draft/30-UI-SPEC.md
@packages/client/src/components/LineupAssignmentScreen.tsx
@packages/client/src/components/DraftPackCarousel.tsx
@.planning/phases/30-recalibrate-draft/30-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 04)

- 4-entry `TIER_ORDER` / `TIER_CARD_CLASS` (keeper dropped) in DraftPackCarousel.tsx
- Tier-border-aware `LineupStatCard` (D-23) in LineupAssignmentScreen.tsx
- classifyTier-based `resolveTieredCard` (D-05)
- Round-aware draft progress label (D-20)

Removed (intentional, not drift): `.cardTierKeeper` CSS class, `keeper` entries in TIER_ORDER/TIER_CARD_CLASS, `showKeeperBanner` state/effect/JSX, `.keeperBanner` CSS, the `role === 'GK' ? 'keeper'` fallback, and `draftView.keeperAutoPickedThisCycle` reads.

Consumes (from Plan 01): 4-value `DraftTier`, `classifyTier`, `computeTotalStat`, `DRAFT_ROUNDS`, round-named `DraftClientView` (no `keeperAutoPickedThisCycle`).

<tasks>

<task type="auto">
  <name>Task 1: Narrow tier classes and apply the D-22 4-color scheme</name>
  <files>packages/client/src/components/DraftPackCarousel.tsx, packages/client/src/components/DraftPackCarousel.test.tsx, packages/client/src/components/BenchCarousel.tsx, packages/client/src/components/BenchCarousel.test.tsx, packages/client/src/components/LineupAssignmentScreen.module.css</files>
  <read_first>
    - packages/client/src/components/DraftPackCarousel.tsx (TIER_ORDER ~26, TIER_CARD_CLASS ~33-39, DraftCardBody ~61-119, rarest-first sort ~147-149)
    - packages/client/src/components/LineupAssignmentScreen.module.css (tier-border block ~320-351: .cardTierChase/Rare/Uncommon/Common/Keeper)
    - packages/client/src/components/BenchCarousel.tsx (imports DraftCardBody/TIER_CARD_CLASS; stale "cycle 4/16 cards" doc comment)
    - .planning/phases/30-recalibrate-draft/30-UI-SPEC.md (Color → Tier border colors table; Additional Contract Notes 3)
  </read_first>
  <action>
    In DraftPackCarousel.tsx, narrow `TIER_ORDER` to `['chase','rare','uncommon','common']` and remove the `keeper: styles.cardTierKeeper!` entry from `TIER_CARD_CLASS` (now `Record<DraftTier,string>` over 4 keys — typechecks once Plan 01's DraftTier lands). In LineupAssignmentScreen.module.css, set `.cardTierUncommon` border to `3px solid #22c55e` (green, correcting the current #eab308 yellow) and `.cardTierCommon` border to `3px solid #ffffff` (white, correcting the current #22c55e green) per D-22, and DELETE the `.cardTierKeeper` rule entirely (D-05 — no 5th color). Leave `.cardTierChase` (#a855f7) and `.cardTierRare` (#ef4444) unchanged. Update the stale "cycle 4/16 cards" doc comment in BenchCarousel.tsx to reference the 6-round/17-card model (non-functional clarity fix). Update DraftPackCarousel.test.tsx and BenchCarousel.test.tsx to drop any `TIER_CARD_CLASS.keeper` / `cardTierKeeper` reference and assert the corrected color classes still map. Do NOT change the `min-width: 320px` tier-card sizing or introduce new spacing/typography (UI-SPEC: border color/width only).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test DraftPackCarousel BenchCarousel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cardTierKeeper\|keeper:" packages/client/src/components/DraftPackCarousel.tsx packages/client/src/components/LineupAssignmentScreen.module.css` returns 0
    - `.cardTierUncommon` uses `#22c55e` and `.cardTierCommon` uses `#ffffff` in the module CSS
    - DraftPackCarousel and BenchCarousel tests pass with the 4-color map
  </acceptance_criteria>
  <done>Tier classes are a 4-value map with the corrected D-22 hexes; the keeper CSS class and all keeper class references are gone.</done>
</task>

<task type="auto">
  <name>Task 2: LineupStatCard tier border + classifyTier fallback + round label + keeper-banner removal</name>
  <files>packages/client/src/components/LineupAssignmentScreen.tsx, packages/client/src/components/LineupAssignmentScreen.module.css, packages/client/src/components/LineupAssignmentScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx (LineupStatCard ~99-177, showKeeperBanner state ~235 + effect ~257-264 + JSX ~567-569, resolveTieredCard ~266-275, cyclePickCounter JSX ~560-565)
    - packages/client/src/components/LineupAssignmentScreen.module.css (.statCardBase ~126, .statCardLocked ~148, .statCardConfirmed ~154, .keeperBanner ~436)
    - .planning/phases/30-recalibrate-draft/30-UI-SPEC.md (Additional Contract Notes 1/2/5; Copywriting Contract rows for round label + keeper banner)
    - .planning/phases/30-recalibrate-draft/30-RESEARCH.md (Common Pitfall 5 — the fallback becomes exact under classifyTier)
  </read_first>
  <action>
    Replace `resolveTieredCard`'s heuristic tier line (`player.role === 'GK' ? 'keeper' : 'common'`) with `classifyTier(computeTotalStat(player))` — no role branch (D-05/Pitfall 5). Import `classifyTier` and `computeTotalStat` from `@counter-attack/shared` (same import source already used for `TieredPoolPlayer`/`DraftTier`). Extend `LineupStatCard` (D-23): resolve the card's tier the same way (via `resolveTieredCard(player.id)` or `classifyTier(computeTotalStat(player))`) and compose the matching `TIER_CARD_CLASS[tier]` onto the existing state-based class (`statCard`/`statCardLocked`/`statCardConfirmed`/`statCardDragging`/`statCardDropTarget`) so the tier border shows alongside drag/lock/confirm state — reconcile the two borders (base `.statCardBase` 1px vs tier 3px) exactly as `DraftPackCarousel`/`BenchCarousel` already combine tier class + interaction state; import `TIER_CARD_CLASS` from `./DraftPackCarousel.js`. Do NOT change the starting-11 grid card dimensions (UI-SPEC Note 1: border color/width only, not the 320px min-width rule). Remove the keeper-safety banner completely (D-21/Pitfall 7): delete the `showKeeperBanner` `useState`, the `useEffect` keyed on `draftView?.keeperAutoPickedThisCycle`, the `{showKeeperBanner && ...}` JSX block, and the `.keeperBanner` CSS rule. Make the draft progress label round-aware (D-20): replace the `Cycle {draftView.cycle} of 4 · Pick {...} of {picksRemaining <= 1 ? 1 : 2}` text with `Round {draftView.round} of {DRAFT_ROUND_COUNT}` and a pick count derived from `DRAFT_ROUNDS[draftView.round - 1].picks` (round 1 → "of 2", rounds 2-6 → "of 3"); for round 1's GK packs use the "GK Round" label per UI-SPEC Copywriting Contract; keep the existing `.cyclePickCounter` styling (12px/700/#a0a0a0 — do not introduce a new size/weight). Update LineupAssignmentScreen.test.tsx: remove/invert the "renders the keeper auto-selected banner" test (the banner no longer exists), update the `makeDraftView` fixture to the round-named/keeper-free DraftClientView shape, and ADD test cases asserting a tier-color class (e.g. `TIER_CARD_CLASS.chase`) is present on a filled starting-11 lineup slot card (the D-23 gap).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test LineupAssignmentScreen</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "keeper" packages/client/src/components/LineupAssignmentScreen.tsx` returns 0
    - `grep -c "classifyTier" packages/client/src/components/LineupAssignmentScreen.tsx` >= 1
    - `grep -c "keeperBanner" packages/client/src/components/LineupAssignmentScreen.module.css` returns 0
    - LineupAssignmentScreen.test.tsx passes and includes a starting-11 lineup-slot tier-color assertion and a "Round N of 6" progress-label assertion
  </acceptance_criteria>
  <done>Starting-11 cards show tier borders, the fallback resolves tiers exactly via classifyTier, the keeper banner is gone, and the progress label is round-aware.</done>
</task>

<task type="auto">
  <name>Task 3: Invert the Legends/Icons checkbox tests (D-08 unlock)</name>
  <files>packages/client/src/components/GameSettingsScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/GameSettingsScreen.test.tsx (~lines 57-78 — the two tests asserting Legends/Icons are disabled + "(coming soon)")
    - packages/client/src/components/GameSettingsScreen.tsx (data-driven: `disabled = !SELECTABLE_DRAFT_POOLS.includes(poolId)` — no component code change needed once the shared const widens)
    - .planning/phases/30-recalibrate-draft/30-PATTERNS.md (GameSettingsScreen section — invert, don't delete, the two named tests)
  </read_first>
  <action>
    The GameSettingsScreen component needs NO code change — its disabled/"(coming soon)" state is derived from `SELECTABLE_DRAFT_POOLS`, which Plan 01 widened to include legends/icons (confirm this rather than adding a special case). INVERT the two existing tests (Pitfall 8 — flip assertions, do not delete): the test titled "Legends and Icons checkboxes are disabled and labelled (coming soon)" becomes an assertion that both checkboxes are ENABLED with no "(coming soon)" suffix; the test titled "clicking a disabled Legends/Icons checkbox does not check it" becomes an assertion that clicking Legends/Icons DOES toggle it checked. Rename the test titles to match the new behavior so test-name-to-behavior traceability is preserved. If GameSettingsScreen.tsx is found to contain any hardcoded 3-pool literal gating legends/icons (rather than reading SELECTABLE_DRAFT_POOLS), note it — but per the data-driven pattern none is expected.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test GameSettingsScreen</automated>
  </verify>
  <acceptance_criteria>
    - GameSettingsScreen tests pass and now assert Legends/Icons are enabled, unlabelled, and clickable
    - No remaining test asserts Legends/Icons are disabled or "(coming soon)"
  </acceptance_criteria>
  <done>The settings-screen tests reflect the unlocked Legends/Icons pools without deleting the traceable test cases.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                            |
| ------------------ | -------------------------------------------------------------------------------------- |
| client UI → server | Client checkbox disabling is UX-only; the server allow-list (Plan 05) is authoritative |

## STRIDE Threat Register

| Threat ID | Category  | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                           |
| --------- | --------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-30-01   | Tampering | GameSettingsScreen pool checkboxes | mitigate    | Client enabling of Legends/Icons is presentation-only; the server ROOM_SETTINGS_CONFIRM handler independently validates against SELECTABLE_DRAFT_POOLS (Plan 05) — a forged client cannot smuggle an invalid pool past the server allow-list regardless of checkbox state |
| T-30-SC   | Tampering | npm/pnpm installs                  | accept      | Zero new packages this phase (RESEARCH Package Legitimacy Audit: Not applicable)                                                                                                                                                                                          |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test DraftPackCarousel BenchCarousel LineupAssignmentScreen GameSettingsScreen` is green.
- Manual grep: 0 matches for `keeper`/`cardTierKeeper`/`keeperBanner` across the touched client files.
- Client tests run under esbuild transform; full-workspace `pnpm typecheck` greens after the server Wave 3 plan lands.
</verification>

<success_criteria>

- 4-color tier borders (D-22) on draft carousel, bench, and starting-11 (D-23).
- classifyTier-based resolveTieredCard (D-05); keeper banner removed (D-21).
- Round-aware progress label (D-20); Legends/Icons checkbox tests inverted (D-08).
  </success_criteria>

<output>
Create `.planning/phases/30-recalibrate-draft/30-04-SUMMARY.md` when done.
</output>
