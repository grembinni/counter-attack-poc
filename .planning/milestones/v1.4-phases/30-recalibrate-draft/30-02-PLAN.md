---
phase: 30-recalibrate-draft
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - packages/shared/src/draftEngine.ts
  - packages/shared/src/draftEngine.test.ts
autonomous: true
requirements: [DRAFT-05]
must_haves:
  truths:
    - 'generateDraftPacks produces 12 packs (2 per round × 6 rounds), each tagged with its round number and carrying exactly 4 cards (D-12..D-15)'
    - 'Round-1 packs contain only GK-role cards; rounds 2-6 packs contain no GK cards (D-07/D-12/D-17)'
    - "Each round's pack composition matches DRAFT_ROUNDS: rounds 2-3 all common; round 4 = 2 uncommon + 2 common; rounds 5-6 = 1 chase-or-rare + 1 uncommon + 2 common (D-13/D-14/D-15)"
    - 'Within any non-GK pack, at most 2 cards come from DEF, at most 2 from MID, and at most 2 from the combined {FWD, ST} bucket (D-17)'
    - 'The chase-or-rare slot draws from a merged, shuffled chase+rare pool — an even mix, not chase-preferred (D-25)'
    - "Selecting only 'legends' or only 'icons' still yields full round-1 GK packs via the MLS→Original backfill chain (D-10/D-11)"
  artifacts:
    - path: 'packages/shared/src/draftEngine.ts'
      provides: 'Round-structured generateDraftPacks with per-round composition, GK-only round 1, position-bucket cap, chase-or-rare merged draw, and reordered FALLBACK_POOL_ORDER'
      contains: 'DRAFT_ROUNDS'
  key_links:
    - from: 'packages/shared/src/draftEngine.ts'
      to: 'packages/shared/src/types.ts'
      via: 'generateDraftPacks iterates DRAFT_ROUNDS and reads PackSlot composition'
      pattern: 'DRAFT_ROUNDS'
    - from: 'packages/shared/src/draftEngine.ts'
      to: 'classifyTier'
      via: 'tier partitioning of the resolved pool uses classifyTier'
      pattern: 'classifyTier'
---

<objective>
Rewrite `generateDraftPacks` from the flat 8-pack / uniform-composition model to the 6-round, position-and-GK-constrained, per-round-variable-composition model (DRAFT-05, superseding the Phase 28 design per D-12..D-19/D-25).

Purpose: The pack generator is the pure, RNG-agnostic heart of the draft. It must express the literal round-by-round spec the user handed down (non-negotiable structure) — GK-only round 1, tiered rounds 2-6, per-pack position-bucket caps, and an unbiased chase-or-rare draw — while preserving the fairness boundary (randomness is an injected parameter, bound to the CSPRNG only server-side).

Output: A round-driven `generateDraftPacks` that returns 12 round-tagged 4-card packs plus the tiered pool, with per-round backfill (MLS→Original), and a full pack-generation test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-recalibrate-draft/30-CONTEXT.md
@.planning/phases/30-recalibrate-draft/30-RESEARCH.md
@.planning/phases/30-recalibrate-draft/30-PATTERNS.md
@packages/shared/src/draftEngine.ts
@.planning/phases/30-recalibrate-draft/30-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 02)

- Re-introduced `FALLBACK_POOL_ORDER` (const, draftEngine.ts) reordered to `['mls', 'original']` (D-11)
- Round-structured `generateDraftPacks` implementation (replaces the Plan 01 stub)
- Internal per-round dealing helpers (executor's choice of names; e.g. a per-pack position-bucket counter) — pure, RNG-agnostic

Consumes (from Plan 01): `DRAFT_ROUNDS`, `RoundConfig`, `PackSlot`, `TIER_STAT_THRESHOLDS`, 4-value `DraftTier`, `classifyTier`, `DraftPack.round`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement round-structured generateDraftPacks</name>
  <files>packages/shared/src/draftEngine.ts</files>
  <read_first>
    - packages/shared/src/draftEngine.ts (the Plan 01 state — classifyTier, assignTiers, isInPool, resolvePoolPlayers, shuffle, the CR-01 input guard, the generateDraftPacks stub, DraftPack with round field)
    - .planning/phases/30-recalibrate-draft/30-RESEARCH.md (Patterns 2/3/4/5; Anti-Patterns; Common Pitfalls 2/3; Open Questions 1/2/3; Assumptions A2/A3/A4)
    - .planning/phases/30-recalibrate-draft/30-PATTERNS.md (draftEngine.ts section — backfill/dealing loop shapes to re-scope; FALLBACK reorder)
  </read_first>
  <behavior>
    - Returns `{ pool, packs }` where `packs.length === DRAFT_ROUND_COUNT * PACKS_PER_ROUND` (12) and every pack has `cards.length === 4` and a `round` in 1..6.
    - Exactly 2 packs carry each round number; round-1 packs are GK-only (every card role === 'GK'); rounds 2-6 packs contain zero GK cards.
    - Per-round tier composition equals the round's DRAFT_ROUNDS slots (e.g. round 4 pack = 2 cards classifyTier uncommon + 2 common).
    - Position-bucket invariant per non-GK pack: count(DEF) <= 2, count(MID) <= 2, count(FWD)+count(ST) <= 2 (D-17).
    - The single chaseOrRare slot per rounds 5-6 pack draws one card from a merged shuffled chase+rare candidate pool (D-25) — not "chase first".
    - No card id appears in more than one pack of the same round (D-09 invariant, re-scoped per round).
    - Selecting `['legends']` or `['icons']` alone does not throw — round-1 GK supply is met via backfill.
  </behavior>
  <action>
    Re-introduce `const FALLBACK_POOL_ORDER: readonly DraftPoolId[] = ['mls', 'original']` (D-11 — International removed as a backfill SOURCE entirely; still directly selectable). Keep the existing CR-01 fail-closed input guard verbatim at the top of `generateDraftPacks`. Replace the flat 8-pack loop with a per-round loop over `DRAFT_ROUNDS`. Overall structure: (1) resolve the selected-pool union via `resolvePoolPlayers`; compute the active fallback list as `FALLBACK_POOL_ORDER` minus already-selected pools. (2) For each round, deal `PACKS_PER_ROUND` (2) packs of `cardsPerPack` (4). For `kind: 'gk'` rounds, the candidate supply is GK-role players from the selected union, backfilled from the fallback chain when short (Pitfall 2 — do NOT special-case round 1 to skip backfill); shuffle and deal 4 distinct GK cards per pack with no cross-pack duplication within the round. For `kind: 'tiered'` rounds, partition the resolved-and-backfilled non-GK union by `classifyTier`, and deal each pack slot-by-slot in rarest-first order (chaseOrRare → uncommon → common), maintaining a per-pack `Record<'DEF'|'MID'|'FWD_ST', number>` bucket counter: when drawing the next candidate from a tier's shuffled cursor, skip any candidate whose bucket already sits at 2 (D-17; FWD and ST share the `FWD_ST` bucket) and advance the cursor forward, only relaxing the cap if a tier's array is exhausted (RESEARCH Pattern 4). Implement the `chaseOrRare` slot by merging the chase and rare candidate arrays into ONE shuffled pool and drawing from it (D-25 unbiased mix), never "prefer chase". Tag every produced pack with its `round` and keep `packNumber` 1-based across all 12 packs. Preserve the per-round loud-throw supply assertions (adapt the existing WR-01 "insufficient supply" throws to per-round-need scope rather than match-wide) so a genuinely unfillable selection fails fast instead of silently duplicating. The module stays pure and RNG-agnostic — `rng: RandomIntFn` remains the only randomness source; never import crypto or Math.random. Update the `generateDraftPacks` doc comment to describe the round model and cite D-07/D-10..D-18/D-25.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test draftEngine</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Math.random\|require('crypto')\|from 'crypto'" packages/shared/src/draftEngine.ts` returns 0 (fairness boundary preserved)
    - `grep -c "FALLBACK_POOL_ORDER" packages/shared/src/draftEngine.ts` >= 1 and the const literal is `['mls', 'original']`
    - generateDraftPacks no longer throws the Plan-01 stub error; returns 12 round-tagged 4-card packs
    - `grep -c "international" packages/shared/src/draftEngine.ts` shows International only in isInPool/selectable logic, never in FALLBACK_POOL_ORDER
  </acceptance_criteria>
  <done>generateDraftPacks yields the exact 6-round structure with GK-only round 1, per-round tiered composition, per-pack position-bucket caps, unbiased chase-or-rare draw, and MLS→Original backfill — pure and RNG-agnostic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-scoped pack-generation test suite</name>
  <files>packages/shared/src/draftEngine.test.ts</files>
  <read_first>
    - packages/shared/src/draftEngine.test.ts (the Plan 01 state — classification/isInPool blocks present, old pack block deleted)
    - packages/shared/src/draftEngine.ts (the new generateDraftPacks — the contract under test)
    - .planning/phases/30-recalibrate-draft/30-RESEARCH.md (Open Question 3 — assert the no-cross-pack-duplication invariant per round, not just match-wide)
  </read_first>
  <behavior>
    - A test proves 12 packs, 2 per round, each 4 cards, round tags 1..6.
    - A test proves round-1 packs are all GK-role and rounds 2-6 packs contain no GK.
    - A per-round composition test proves round 4 = 2 uncommon + 2 common and rounds 5-6 = 1 chaseOrRare + 1 uncommon + 2 common by classifyTier.
    - A position-bucket test proves DEF<=2, MID<=2, {FWD,ST}<=2 per non-GK pack.
    - A backfill test proves `generateDraftPacks(['legends'], rng)` and `(['icons'], rng)` succeed (do not throw) and still fill round-1 GK packs.
    - A chase-or-rare distribution test proves both chase and rare cards appear across many seeded runs of the round-5/6 slot (even mix, D-25) — not chase-only.
    - A no-cross-pack-duplication test asserts, per round, that the round's two packs share no card id (D-09).
  </behavior>
  <action>
    Add a `describe('generateDraftPacks — 6-round structure', ...)` block (replacing the deleted flat-model block). Drive it with a deterministic injected rng for structural assertions and, where distribution is asserted (chase-or-rare even mix), loop many runs with `crypto.randomInt` (mirroring the existing draftPacks.test pattern of real-RNG structural checks) and assert both tiers appear. Assert every behavior listed above. Do NOT write a test that expects `['legends']`/`['icons']` alone to throw for round 1 (Pitfall 2 — backfill makes it succeed by design). Do NOT assert any GK-tier property (GK tier is cosmetic per D-07/D-24). Use `classifyTier`/`computeTotalStat` from the shared package to derive expected tiers rather than hardcoding player ids.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test</automated>
  </verify>
  <acceptance_criteria>
    - The full shared test suite passes, including the new 6-round generateDraftPacks block
    - Tests cover: 12/2-per-round/4-card structure, GK-only round 1, per-round tier composition, position-bucket cap, legends/icons backfill success, chase-or-rare even mix, per-round no-duplication
    - `pnpm --filter @counter-attack/shared exec tsc --noEmit -p tsconfig.json` reports 0 errors
  </acceptance_criteria>
  <done>The shared package is fully green (typecheck + tests) with a round-scoped pack-generation suite proving every D-12..D-18/D-25 invariant.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                       |
| --------------------------------- | ------------------------------------------------------------------------------------------------- |
| client → server (pack generation) | Pack contents are gameplay-affecting; must be server-authoritative and uninfluenceable by clients |

## STRIDE Threat Register

| Threat ID | Category  | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                |
| --------- | --------- | ------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-30-02   | Tampering | generateDraftPacks randomness  | mitigate    | Shared engine stays RNG-agnostic (rng injected as `RandomIntFn`); it never imports crypto/Math.random, so pack contents cannot be influenced client-side — the CSPRNG binding lives only in the server draftPacks.ts (Plan 05). Grep gate asserts no crypto/Math.random import |
| T-30-03   | Tampering | GK card placed outside round 1 | mitigate    | Rounds 2-6 dealing structurally excludes GK-role players from candidate pools (D-17) — no code path can deal a GK into a non-GK pack, enforced at generation time, not just at placement time                                                                                  |
| T-30-SC   | Tampering | npm/pnpm installs              | accept      | Zero new packages this phase (RESEARCH Package Legitimacy Audit: Not applicable)                                                                                                                                                                                               |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test` and `pnpm --filter @counter-attack/shared exec tsc --noEmit` both green.
- generateDraftPacks throws only on genuinely unfillable input, never on legends/icons-only selections.
</verification>

<success_criteria>

- generateDraftPacks yields the literal 6-round structure (D-12..D-15) with per-pack bucket caps (D-17), unbiased chase-or-rare (D-25), and MLS→Original backfill (D-11).
- Round tagging on every DraftPack; 12 packs total.
- Shared package fully green.
  </success_criteria>

<output>
Create `.planning/phases/30-recalibrate-draft/30-02-SUMMARY.md` when done.
</output>
