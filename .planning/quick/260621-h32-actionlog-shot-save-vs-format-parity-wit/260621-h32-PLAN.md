---
phase: quick-260621-h32
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/__tests__/kickoffDebug.test.ts
  - packages/shared/src/types.ts
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/ActionLog.test.tsx
autonomous: true
requirements: []
---

<objective>
Five independent ActionLog / log-noise fixes, each committed atomically as its own task.

Purpose:

1. Remove leftover diagnostic `console.log` noise (and the now-dead vars feeding it) from a regression test.
2. Give SHOT_ATTEMPT "vs" lines the same structural format as TACKLE_ATTEMPT — both duel sides as named `<PNamed>` labels — by adding `gkId` to the SHOT_ATTEMPT event variant (mirrors the prior `scorerId`-on-GOAL precedent).
3. Rename `[GK_KICK_K]` → `[KEEPER KICK RESULT]` (KICKER side only).
4. Rename `[GK KICK ✓/✗]` → `[KEEPER KICK TARGET ✓/✗]`.
5. Triple retained log entries from 10 → 30.

Output: cleaner test file + restructured/renamed ActionLog branches, with the SHOT_ATTEMPT change threaded through shared types and all 7 server emission sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# The files being modified (already read during planning):

@packages/server/src/**tests**/kickoffDebug.test.ts
@packages/shared/src/types.ts
@packages/server/src/gameEngine.ts
@packages/server/src/gameHandlers.ts
@packages/client/src/components/ActionLog.tsx
@packages/client/src/components/ActionLog.test.tsx
</context>

<critical_build_note>
**packages/shared is consumed via its BUILT `dist/` output, NOT `src/`** (see
packages/shared/package.json `main`/`types` → `./dist/index.*`).

Any task that edits `packages/shared/src/types.ts` MUST run
`pnpm --filter @counter-attack/shared build` BEFORE `pnpm -r typecheck` or any
server/client test — otherwise client/server typecheck against the stale
`dist/index.d.ts` and either show false errors (gkId missing) or silently pass
without catching real ones. This bit a prior quick task. Task 2's verify step
enforces this ordering explicitly.
</critical_build_note>

<tasks>

<task type="auto">
  <name>Task 1: Remove debug console.log noise + dead vars from kickoffDebug.test.ts</name>
  <files>packages/server/src/__tests__/kickoffDebug.test.ts</files>
  <action>
Pure deletion of debug scaffolding — NO behavior change.

Delete all 6 `console.log(...)` calls:

- line 12 (`'attackingTeam:'...`)
- line 22 (`'Attacking team has piece on centre hex:'...`)
- lines 48-51 (`'Away in circle after seeding:'...` — the .map() arg)
- lines 52-55 (`'Home in circle after seeding:'...` — the .map() arg)
- line 60 (`'Attacking GAME_READY result:'...`)
- line 61 (`'Defending GAME_READY result:'...`)

Then delete the two now-unused `const` declarations whose ONLY consumers were the
deleted console.logs:

- `const awayInCircleAfter = seededState.pieces.filter(...)` (lines 42-44)
- `const homeInCircleAfter = seededState.pieces.filter(...)` (lines 45-47)

These become dead/unused once their console.logs are gone; leaving them in trips
eslint no-unused-vars / @typescript-eslint and fails the pre-commit hook (as already
happened once in this file's history).

DO NOT touch the actual test logic: the `buildInitialGameState` setup, the
`hasCentreHex` check and the `pieces.map(...)` repositioning, the `seededState`
construction, the two `applyKickOffReady(...)` calls, or the two
`expect(...).toBe(true)` assertions. The seededState const must remain (it feeds
applyKickOffReady).
</action>
<verify>
<automated>cd packages/server && pnpm test -- kickoffDebug</automated>
</verify>
<done>kickoffDebug.test.ts has zero `console.log` and zero `awayInCircleAfter`/`homeInCircleAfter` references; the test still passes; eslint reports no unused-vars in the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SHOT_ATTEMPT gains gkId + TACKLE-parity "vs" rendering</name>
  <files>packages/shared/src/types.ts, packages/server/src/gameEngine.ts, packages/server/src/gameHandlers.ts, packages/client/src/components/ActionLog.tsx, packages/client/src/components/ActionLog.test.tsx</files>
  <behavior>
    - ActionLog.test.tsx: the existing "SHOT_ATTEMPT GOAL renders [SHOT ✓] and the shooter ... label" test (or the SAVE test) additionally asserts the GK's resolved name appears in output — e.g. `expect(screen.getByText(/<away-0 name>/)).toBeDefined()` — proving the GK is now rendered as a named PNamed label, not a bare stat string.
    - All 8 existing SHOT_ATTEMPT fixtures still typecheck (gkId is now required) and their existing Shooting/Saving/`- 0`/prefix assertions still pass.
    - Server tests still pass (gkId populated at all 7 emission sites).
  </behavior>
  <action>
**Step A — shared type (packages/shared/src/types.ts):**
In the `SHOT_ATTEMPT` ActionEvent variant (starts line 181), add a required field
`gkId: string;` (place it next to `shooterId` for symmetry, e.g. right after line 182).
This mirrors the precedent set by `scorerId: string` on the `GOAL` variant (line 133),
added by a prior quick task for the exact same "duel side has stats but no identity" reason.

**Step B — gameEngine.ts `case 'SHOT':` block (4 sites):**
`const gk = state.pieces.find((p) => p.teamId === opposingTeam && p.role === 'GK')`
is at line 1675, guarded non-null at line 1676, and in scope for all four
SHOT_ATTEMPT object literals in the case. Add `gkId: gk.id,` to each:

- line 1709-1724 literal (unsaveable / out-of-range auto-goal)
- line 1792-1807 literal (regular GOAL outcome)
- line 1838-1853 literal (LOOSE_BALL outcome)
- line 1874-1891 literal (SAVE-with-handling-check outcome)

**Step C — gameHandlers.ts (3 "outOfRangeEvent" sites):**
Each is preceded by a GK lookup (`Piece | undefined`) used for the range check.
Add `gkId: <thatVar>?.id ?? '',` to each object literal, matching the existing
`shooterId: ...ball.carrierId ?? ''` fallback style in the same literal:

- site ~994-1009: GK var is `snapGk` (line 980) → `gkId: snapGk?.id ?? '',`
- site ~1607-1622: GK var is `gkForRange` (line 1593) → `gkId: gkForRange?.id ?? '',`
- site ~2368-2383: GK var is `headerGk` (line 2352) → `gkId: headerGk?.id ?? '',`

**Step D — ActionLog.tsx `case 'SHOT_ATTEMPT'` (restructure 2 duel branches to TACKLE shape):**
TACKLE's shape (lines 366-373) is: `{result/outcome} -> <PNamed/> ({statStr}) vs <PNamed/> ({statStr})`.

1. `handlingDie !== null` branch's `subKind === 'duel'` return (currently lines 475-485):
   change content from `{shooterLabel} {event.outcome} — {duelStr}` to outcome-leads
   TACKLE shape: `{outcomeLabel} -> {shooterLabel} ({shooterStr}) vs <PNamed pieceId={event.gkId} /> ({gkStr})`.
   The `duelStr` local (line 457, plain "shooterStr vs gkStr" with no GK name) becomes
   unused for this branch — remove it if nothing else references it, or leave only if
   still used (it is not used elsewhere; remove to avoid unused-var).
2. `else` branch ("Regular duel outcome", currently lines 487-511): change content from
   `{shooterLabel} {outcomeLabel} — {shooterStr} vs {gkStr}` to the same shape:
   `{outcomeLabel} -> {shooterLabel} ({shooterStr}) vs <PNamed pieceId={event.gkId} /> ({gkStr})`.
3. `outcomeLabel` (the `LOOSE_BALL → "LOOSE BALL (tie)"` remap, currently line 490) is
   used by both "vs" branches now. Hoist it into a shared local visible to both branches,
   OR duplicate the one-line ternary in the duel branch — your call — so both branches
   print the outcome word identically.

DO NOT touch:

- the `shooterScore === null` branch (no duel ran — no "vs" to format; leave prose unchanged)
- the `subKind === 'handling'` branch (the `[HANDLING]` sub-roll line — not a player-vs-player compare)
- `fmtStatRoll` itself (output unchanged; just re-wrapped in parens with an added GK PNamed)

**Step E — ActionLog.test.tsx (add gkId to all 8 SHOT_ATTEMPT fixtures + 1 GK-name guard):**
All 8 SHOT_ATTEMPT fixtures must add `gkId: 'away-0',` (away-0 is the away GK in
mockMovementState — confirmed in packages/client/src/mock/mockMovementState.ts line 26).
The fixtures are at approx lines: 73, 98, 265, 308, 331, 405, 440, 466.
Then, for ONE real-duel SHOT_ATTEMPT test (the "SHOT_ATTEMPT GOAL renders [SHOT ✓]..."
test ~line 262, or the SAVE test ~line 305), add an assertion that the GK's resolved
name now appears in output — mirror the existing shooter assertion
`expect(screen.getByText(/Nicolae Rusu/)).toBeDefined()`; resolve away-0's seeded name
from the mock squad and assert it via `screen.getByText(/<that name>/)`. This is the
regression guard for the feature.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared build && pnpm -r typecheck && pnpm --filter @counter-attack/server test && pnpm --filter @counter-attack/client test</automated>
</verify>
<done>SHOT_ATTEMPT variant has required `gkId: string`; all 7 server emission sites populate it (4 gameEngine, 3 gameHandlers); both ActionLog "vs" duel branches render the GK as a named PNamed label in TACKLE's `{outcome} -> shooter (stats) vs GK (stats)` shape; the shared package is rebuilt BEFORE typecheck/tests; all typecheck + server + client tests pass; the new GK-name assertion passes.</done>
</task>

<task type="auto">
  <name>Task 3: Rename [GK_KICK_K] → [KEEPER KICK RESULT] (KICKER side only)</name>
  <files>packages/client/src/components/ActionLog.tsx</files>
  <action>
Two locations share the ternary `event.slot === 'KICKER' ? '[GK_KICK_K]' : '[GK_KICK_O]'`:
- `consolidateEvents`'s GK_KICK_MOVE branch (line 182)
- `formatEvent`'s `case 'GK_KICK_MOVE'` (line 690)

In BOTH, change ONLY the KICKER (true) side `'[GK_KICK_K]'` → `'[KEEPER KICK RESULT]'`.

**Scoping decision (explicit, not an oversight):** leave `'[GK_KICK_O]'` (the OPP/false
side) completely untouched in both locations. The user asked only to rename the KICKER
variant; OPP is a different role and was not mentioned. This mirrors the FTP_MOVE scoping
note from a prior quick task — record the decision, change nothing on the OPP side.
</action>
<verify>
<automated>cd packages/client && pnpm test -- ActionLog</automated>
</verify>
<done>Both KICKER-side ternary branches read `'[KEEPER KICK RESULT]'`; both `'[GK_KICK_O]'` occurrences are unchanged; ActionLog tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Rename [GK KICK ✓/✗] → [KEEPER KICK TARGET ✓/✗]</name>
  <files>packages/client/src/components/ActionLog.tsx</files>
  <action>
In `formatEvent`'s `case 'GK_KICK':` (line 670 — the kick-target accuracy check, a
DIFFERENT event type from GK_KICK_MOVE), the prefix is currently
`accurate ? '[GK KICK ✓]' : '[GK KICK ✗]'` (line 674). Change the stem to
`accurate ? '[KEEPER KICK TARGET ✓]' : '[KEEPER KICK TARGET ✗]'`, preserving the
existing ✓/✗ suffix pattern.
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- ActionLog</automated>
  </verify>
  <done>GK_KICK prefix reads `[KEEPER KICK TARGET ✓]` / `[KEEPER KICK TARGET ✗]`; ActionLog tests pass.</done>
</task>

<task type="auto">
  <name>Task 5: Triple retained log entries (10 → 30)</name>
  <files>packages/client/src/components/ActionLog.tsx</files>
  <action>
In the exported `ActionLog()` component, change
`const recent = [...consolidated].reverse().slice(0, 10);` (line 737) to `.slice(0, 30)`.
Also update the component JSDoc above it (line 726, currently "last 10 display items in
reverse-chronological order") to say "last 30" instead.
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- ActionLog</automated>
  </verify>
  <done>The slice uses 30 and the JSDoc says "last 30"; ActionLog tests pass.</done>
</task>

</tasks>

<verification>
Full sweep after all tasks: `pnpm --filter @counter-attack/shared build && pnpm -r typecheck && pnpm -r test`.
The shared build MUST run first because Task 2 changed shared types.
</verification>

<success_criteria>

- kickoffDebug.test.ts: no console.log, no dead vars, test still green.
- SHOT_ATTEMPT renders GK as named PNamed label in TACKLE-parity "vs" shape; gkId populated at all 7 server sites; shared rebuilt before typecheck.
- [KEEPER KICK RESULT] (KICKER only) and [KEEPER KICK TARGET ✓/✗] renames applied; [GK_KICK_O] untouched.
- Log retains 30 entries; JSDoc updated.
- All typecheck + server + client tests pass.
  </success_criteria>

<output>
Create `.planning/quick/260621-h32-actionlog-shot-save-vs-format-parity-wit/260621-h32-SUMMARY.md` when done.
</output>
