---
plan: 260727-9f1
title: Clean up documentation drift found in v1.5 milestone audit
type: execute
mode: quick
wave: 1
autonomous: true
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/34-visual-theme-restyle/34-VERIFICATION.md
  - docs/HIGHLIGHT-REFERENCE.md
requirements: [THEME-01-doc-accuracy, THEME-04-doc-accuracy, HILITE-05-doc-accuracy]
must_haves:
  truths:
    - 'ROADMAP.md v1.5 Phases table renders Phase 34 with the correct 4 columns (Phase | Name | Plans | Status)'
    - 'ROADMAP.md bottom Progress table shows phases 33 and 34 as Complete, matching their VERIFICATION status'
    - 'REQUIREMENTS.md marks THEME-01 and THEME-04 as satisfied (checkbox + traceability)'
    - '34-VERIFICATION.md frontmatter status reads passed and score drops the programmatic-evidence qualifier'
    - 'HIGHLIGHT-REFERENCE.md safe/goal opacity rows match the live HexCell.tsx HIGHLIGHT_STYLES values'
  artifacts:
    - path: '.planning/ROADMAP.md'
      provides: 'Corrected v1.5 phase table + Progress table'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'THEME-01/THEME-04 marked Complete'
    - path: '.planning/phases/34-visual-theme-restyle/34-VERIFICATION.md'
      provides: 'status: passed frontmatter'
    - path: 'docs/HIGHLIGHT-REFERENCE.md'
      provides: 'Accurate safe/goal opacity values'
  key_links: []
---

<objective>
Apply five already-verified documentation corrections surfaced by the v1.5 milestone audit
(`.planning/v1.5-MILESTONE-AUDIT.md`, `tech_debt` section). All five are pure tracking-doc
fixes — no code or behavior changes. Each fix is a stale/malformed value that no longer
matches the codebase or the phase's own completion records.

Purpose: Restore traceability integrity so future audits and milestone-close queries read
the true completion state. The underlying work (Phase 33/34, THEME-01/04) is already done and
verified — only the bookkeeping lagged.

Output: Corrected `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`,
`34-VERIFICATION.md`, and `docs/HIGHLIGHT-REFERENCE.md`, committed together.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-visual-theme-restyle/34-VERIFICATION.md
@.planning/phases/34-visual-theme-restyle/34-UAT.md
@.planning/phases/31-bug-fixes/31-VERIFICATION.md
@docs/HIGHLIGHT-REFERENCE.md
@packages/client/src/components/HexCell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP.md malformed Phase 34 row and stale Progress table</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Two edits in `.planning/ROADMAP.md`, both structural corrections to already-Complete phases.

EDIT 1a — v1.5 "Phases" summary table (the malformed Phase 34 row). The table header is
`| Phase | Name                                      | Plans    | Status      |`. The current
Phase 34 row is malformed — its Name column holds "5/5", its Plans column holds "Complete",
and it carries a stray 5th column with a date. Replace the exact line:
`| 34    | 5/5                                       | Complete | 2026-07-27  |`
with (Name = "Visual Theme Restyle", Plans = "5/5", Status = "Complete", matching the
column widths of the Phase 33 row directly above it):
`| 34    | Visual Theme Restyle                      | 5/5      | Complete    |`

EDIT 1b — bottom "Progress" table. Phases 33 and 34 rows are stale ("Not started" / "0/7" /
"0/4") despite both being Complete. Replace the exact line:
`| 33. Design Tokens & Highlight   | v1.5      | 0/7            | Not started | -          |`
with:
`| 33. Design Tokens & Highlight   | v1.5      | 7/7            | Complete    | 2026-07-25 |`
and replace the exact line:
`| 34. Visual Theme Restyle        | v1.5      | 0/4            | Not started | -          |`
with:
`| 34. Visual Theme Restyle        | v1.5      | 5/5            | Complete    | 2026-07-27 |`

Leave the `35. ActionPanel & Log Standard.` row (0/TBD, Not started) unchanged — still
accurate. Preserve column widths so the tables stay aligned with adjacent rows (31/32).
</action>
<verify>
<automated>grep -q "^| 34 | Visual Theme Restyle | 5/5 | Complete |$" .planning/ROADMAP.md && grep -q "| 33. Design Tokens & Highlight | v1.5 | 7/7 | Complete | 2026-07-25 |" .planning/ROADMAP.md && grep -q "| 34. Visual Theme Restyle | v1.5 | 5/5 | Complete | 2026-07-27 |" .planning/ROADMAP.md && ! grep -qE "33\. Design Tokens & Highlight._(0/7|Not started)" .planning/ROADMAP.md && ! grep -qE "34\. Visual Theme Restyle._(0/4|Not started)" .planning/ROADMAP.md</automated>
</verify>
<done>The v1.5 Phases table Phase 34 row has exactly 4 columns matching the header; the Progress table shows 33 → 7/7 Complete 2026-07-25 and 34 → 5/5 Complete 2026-07-27; no "0/7", "0/4", or "Not started" remains on the 33/34 progress rows; the Phase 35 progress row is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mark THEME-01/THEME-04 satisfied in REQUIREMENTS.md and flip 34-VERIFICATION.md to passed</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/34-visual-theme-restyle/34-VERIFICATION.md</files>
  <action>
Both files record Phase 34 completion state; both are stale relative to 34-VERIFICATION.md /
34-UAT.md, which confirm THEME-01 and THEME-04 are functionally SATISFIED (8/8 UAT passed,
commit eab9eca).

REQUIREMENTS.md — four edits:

- Change the THEME-01 checkbox from `- [ ] **THEME-01**:` to `- [x] **THEME-01**:`.
- Change the THEME-04 checkbox from `- [ ] **THEME-04**:` to `- [x] **THEME-04**:`.
- In the Traceability table, change the THEME-01 row status: replace
  `| THEME-01    | Phase 34 — Visual Theme Restyle                      | Pending  |`
  with
  `| THEME-01    | Phase 34 — Visual Theme Restyle                      | Complete |`.
- Change the THEME-04 row status: replace
  `| THEME-04    | Phase 34 — Visual Theme Restyle                      | Pending  |`
  with
  `| THEME-04    | Phase 34 — Visual Theme Restyle                      | Complete |`.
  Do not touch THEME-02/THEME-03 (already Complete) or any PANEL-\* rows (correctly still Pending —
  Phase 35 not started). "Complete" (8 chars + 1 trailing space) matches the existing
  THEME-02 row's width, so alignment is preserved.

34-VERIFICATION.md frontmatter — the outstanding human-verify item (Confirm-button gap
closure, UAT tests 3 & 8) was completed after this report was written and recorded in
34-UAT.md (status: complete, 8/8 passed, 0 issues, commit eab9eca). Correct the frontmatter:

- Change `status: human_needed` to `status: passed`.
- Change `score: 5/5 must-haves verified (programmatic evidence)` to
  `score: 5/5 must-haves verified` (drop the parenthetical qualifier — human verification is
  now also complete).
- Immediately after the `overrides_applied: 0` line, insert a `re_verification:` block
  modeled on the one in `.planning/phases/31-bug-fixes/31-VERIFICATION.md` (read it first to
  match the exact key shape), recording the post-hoc closure:
  re_verification:
  previous_status: human_needed
  previous_score: '5/5 must-haves verified (programmatic evidence)'
  gaps_closed: - 'Visual re-confirmation of the Confirm-button gap-closure fix (UAT tests 3 & 8) — human re-verified and recorded in 34-UAT.md (status: complete, 8/8 passed, 0 issues, commit eab9eca).'
  gaps_remaining: []
  regressions: []
  Leave the existing `human_verification:` block in place as a historical record (do not
  delete it). For internal consistency, also update the report body: change the
  `**Status:** human_needed` line to `**Status:** passed` and the
  `**Re-verification:** No — initial verification` line to
  `**Re-verification:** Yes — after post-hoc human UAT closure (34-UAT.md)`.
  Keep YAML indentation consistent (2 spaces, no tabs) so the frontmatter still parses.
  </action>
  <verify>
  <automated>grep -q "^- \[x\] \*\*THEME-01\*\*" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*THEME-04\*\*" .planning/REQUIREMENTS.md && grep -q "| THEME-01 | Phase 34 — Visual Theme Restyle | Complete |" .planning/REQUIREMENTS.md && grep -q "| THEME-04 | Phase 34 — Visual Theme Restyle | Complete |" .planning/REQUIREMENTS.md && grep -q "^status: passed$" .planning/phases/34-visual-theme-restyle/34-VERIFICATION.md && grep -q "^score: 5/5 must-haves verified$" .planning/phases/34-visual-theme-restyle/34-VERIFICATION.md && grep -q "^re_verification:$" .planning/phases/34-visual-theme-restyle/34-VERIFICATION.md && grep -q "^human_verification:$" .planning/phases/34-visual-theme-restyle/34-VERIFICATION.md</automated>
  </verify>
  <done>REQUIREMENTS.md shows THEME-01 and THEME-04 as `[x]` and their traceability rows as "Complete" (THEME-02/03 and PANEL-\* untouched); 34-VERIFICATION.md frontmatter reads `status: passed` and `score: 5/5 must-haves verified` (no "programmatic evidence" qualifier), carries a `re_verification:` block, and retains the original `human_verification:` block; frontmatter still parses as valid YAML.</done>
  </task>

<task type="auto">
  <name>Task 3: Correct safe/goal opacity values in HIGHLIGHT-REFERENCE.md</name>
  <files>docs/HIGHLIGHT-REFERENCE.md</files>
  <action>
In `docs/HIGHLIGHT-REFERENCE.md`, section "1. Hex Tint Types", the "Rest / Hover Opacity"
column for the `safe` and `goal` rows shows stale values `0.5 / 0.65`. The live code
(`packages/client/src/components/HexCell.tsx`, `HIGHLIGHT_STYLES` constant) sets both
`safe` and `goal` to `restOpacity: 0.65, hoverOpacity: 0.8` (raised from 0.5/0.65 after UAT
feedback, per the in-code comments). Update both rows so the doc matches.

- In the `safe` row (the one whose first cell is `` `safe` `` and fill is
  `rgba(34,197,94,0.4)`), change the final opacity cell from `0.5 / 0.65` to `0.65 / 0.8`.
- In the `goal` row (first cell `` `goal` ``, fill `rgba(168,85,247,0.5)`), change the final
  opacity cell from `0.5 / 0.65` to `0.65 / 0.8`.

`0.65 / 0.8` is the same character count as `0.5 / 0.65`, so column padding is unchanged.
Do not alter the `risk` row (already `0.65 / 0.8`) or any other row. To disambiguate, match
each edit against the full row line (safe vs. goal have different fill/stroke cells).
</action>
<verify>
<automated>test "$(grep -c '0.5 / 0.65' docs/HIGHLIGHT-REFERENCE.md)" -eq 0 && grep -qE '\| `safe` .*rgba\(34,197,94,0\.4\).*0\.65 / 0\.8' docs/HIGHLIGHT-REFERENCE.md && grep -qE '\| `goal` .*rgba\(168,85,247,0\.5\).*0\.65 / 0\.8' docs/HIGHLIGHT-REFERENCE.md</automated>
</verify>
<done>The `safe` and `goal` rows both read `0.65 / 0.8` in the opacity column, matching HexCell.tsx; no `0.5 / 0.65` string remains anywhere in the file; the `risk` row and all other rows are unchanged.</done>
</task>

</tasks>

<verification>
- All four files edited; no code files touched (docs/tracking only).
- ROADMAP.md tables are well-formed (Phase 34 row has 4 columns; Progress rows for 33/34 read Complete).
- REQUIREMENTS.md THEME-01/THEME-04 satisfied in both checkbox and traceability; PANEL-* untouched.
- 34-VERIFICATION.md frontmatter parses as valid YAML with `status: passed`, corrected `score`, added `re_verification:` block, retained `human_verification:`.
- HIGHLIGHT-REFERENCE.md safe/goal opacity values equal the live HexCell.tsx HIGHLIGHT_STYLES values.
- No regression risk: these are documentation edits with no runtime consumers.
</verification>

<success_criteria>

- The five audit-flagged documentation drifts are corrected exactly as specified.
- Every `<verify>` automated check passes.
- Changes committed together with a docs-scoped message.
  </success_criteria>

<output>
Commit the four edited files together, e.g.:
`docs(260727-9f1): fix v1.5 audit documentation drift (ROADMAP tables, THEME-01/04 status, 34-VERIFICATION status, highlight opacity)`

Then create `.planning/quick/260727-9f1-clean-up-documentation-drift-found-in-v1/260727-9f1-SUMMARY.md` recording the five corrections applied.
</output>
