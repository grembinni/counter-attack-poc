---
phase: quick-260727-o85
plan: 01
subsystem: ui
tags: [react, vitest, kick-off-setup, copy]

requires:
  - phase: 35-actionpanel-log-standardization
    provides: UAT feedback identifying the placement-valid copy as reading like a validation verdict rather than a setup instruction
provides:
  - Updated placement-valid constraint copy in KickOffSetupPanel ("Setup your players for kickoff.")
  - Regression tests locking both branches of the placementValid ternary
affects: []

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - packages/client/src/components/KickOffSetupPanel.tsx
    - packages/client/src/components/KickOffSetupPanel.test.tsx

key-decisions:
  - "Test asserting absence of the retired copy uses a partial regex (/legal position/i) instead of embedding the full deprecated sentence, so the plan's own zero-occurrence grep check for the old string doesn't false-positive on the regression test itself"

patterns-established: []

requirements-completed: [UAT-35-KICKOFF-COPY]

duration: ~15min
completed: 2026-07-27
---

# Quick Task 260727-o85: Change KickOffSetupPanel placement-valid copy Summary

**Replaced the kick-off setup sidebar's valid-placement text ("All your players are in a legal position.") with a forward-looking setup prompt ("Setup your players for kickoff."), with regression tests locking both branches of the ternary.**

## Performance

- **Duration:** ~15 min
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments

- `KickOffSetupPanel.tsx` valid-placement branch now renders "Setup your players for kickoff." instead of the old validation-verdict phrasing
- Three new regression tests lock both branches of the `placementValid` ternary (new copy present, old copy absent, invalid-branch copy unchanged)
- Full client test suite (475 tests) and `tsc --noEmit` both green after the change

## Task Commits

Each task was committed atomically:

1. **Task 1: Add failing regression tests for both placement-status branches** - `549545d` (test)
2. **Task 2: Replace the placement-valid string and confirm green** - `7f7e6e7` (feat)

_TDD cycle: RED (549545d, 2 of 3 new tests failing as expected) → GREEN (7f7e6e7, all 475 tests passing)._

## Files Created/Modified

- `packages/client/src/components/KickOffSetupPanel.tsx` - One-line string replacement in the `placementValid` ternary (line 119)
- `packages/client/src/components/KickOffSetupPanel.test.tsx` - New `describe('KickOffSetupPanel — placement status copy', ...)` block with 3 tests

## Decisions Made

- The plan's Task 2 verify step greps `packages/client/src` for zero non-comment occurrences of the retired string "All your players are in a legal position", but Task 1's design required a test asserting that exact string is no longer rendered — which, if written as a literal, would itself be a non-comment match and always keep the grep count at 1 (or more), permanently failing the plan's own verification. Resolved by writing the negative assertion as a partial case-insensitive regex (`/legal position/i`) rather than embedding the retired sentence verbatim. This still fails before the fix (old copy contains "legal position") and passes after (new copy doesn't), while keeping the grep check honestly at zero.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Verify command was self-contradicting given Task 1's own instructions**

- **Found during:** Task 2 (running the plan's verify grep after Task 1's tests were written)
- **Issue:** Task 1 explicitly instructed writing a test asserting `queryByText('All your players are in a legal position.')` is null. Doing so literally embeds the retired sentence in a non-comment line of `KickOffSetupPanel.test.tsx`, which the Task 2 verify grep (`grep -rn ... "All your players are in a legal position" packages/client/src | grep -v '^\s*//' | wc -l | grep -qx '0'`) would then always find and count as 1, meaning the plan's own success criterion ("the old string returns zero non-comment matches") could never be satisfied as literally written.
- **Fix:** Rewrote the negative-assertion test to use a shorter partial regex (`/legal position/i`) instead of the full retired sentence, and reworded the test's `it()` description to avoid quoting the full old sentence. Functionally equivalent regression coverage (fails before the source fix, passes after), but no longer trips the grep check.
- **Files modified:** `packages/client/src/components/KickOffSetupPanel.test.tsx`
- **Verification:** Ran `grep -rn --include=*.tsx --include=*.ts "All your players are in a legal position" packages/client/src | grep -v '^\s*//' | wc -l` — returns `0`. Full client suite (475 tests) and `tsc --noEmit` both pass.
- **Committed in:** `7f7e6e7` (Task 2 commit; the test-file edit was folded into the Task 2 commit alongside the source change since it was discovered during Task 2's verification step)

---

**Total deviations:** 1 auto-fixed (1 bug — self-contradicting verify script)
**Impact on plan:** Necessary to satisfy the plan's own stated success criteria; no scope creep, no behavior change beyond what the plan specified.

## Issues Encountered

- Fresh worktree had no `node_modules` installed at all (neither root nor `packages/client`) and `packages/shared` had no built `dist/`, causing `vitest`/`tsc` invocations to fail with "not recognized" / "Failed to resolve entry for package '@counter-attack/shared'" errors. Resolved by running `pnpm install` (safe — no existing `node_modules` to conflict with or overwrite) followed by `pnpm --filter @counter-attack/shared build`. Not a plan deviation — standard worktree environment bootstrap, no code changes involved.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- No blockers. This was an isolated copy change with no dependencies for future work.

---

_Phase: quick-260727-o85_
_Completed: 2026-07-27_

## Self-Check: PASSED

- FOUND: packages/client/src/components/KickOffSetupPanel.tsx
- FOUND: packages/client/src/components/KickOffSetupPanel.test.tsx
- FOUND: 549545d (Task 1 commit)
- FOUND: 7f7e6e7 (Task 2 commit)
