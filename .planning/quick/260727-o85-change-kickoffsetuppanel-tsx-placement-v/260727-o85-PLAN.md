---
phase: quick-260727-o85
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/KickOffSetupPanel.tsx
  - packages/client/src/components/KickOffSetupPanel.test.tsx
autonomous: true
requirements: [UAT-35-KICKOFF-COPY]

must_haves:
  truths:
    - 'During KICK_OFF_SETUP, a player whose pieces are all legally placed sees "Setup your players for kickoff." in the constraint row'
    - 'The out-of-position message ("N players out of position — move into ...") is unchanged'
    - 'The string "All your players are in a legal position." no longer exists anywhere in packages/client/src'
    - 'The placement row still renders in --color-text-secondary when valid and --color-danger when invalid'
  artifacts:
    - path: 'packages/client/src/components/KickOffSetupPanel.tsx'
      provides: 'Kick-off setup constraint rows and Confirm CTA'
      contains: 'Setup your players for kickoff.'
    - path: 'packages/client/src/components/KickOffSetupPanel.test.tsx'
      provides: 'Regression coverage for both placement-status branches'
      contains: 'Setup your players for kickoff'
  key_links:
    - from: 'packages/client/src/components/KickOffSetupPanel.tsx'
      to: 'placementValid ternary'
      via: 'valid branch renders the new copy, invalid branch keeps piecesLabel/zoneGuidance template'
      pattern: 'placementValid'
---

<objective>
Change the placement-valid status text in the kick-off setup sidebar from
"All your players are in a legal position." to "Setup your players for kickoff."

Purpose: Phase 35 UAT feedback — the valid-state copy reads as a validation
verdict ("legal position") rather than an instruction telling the player what to
do on this screen. The new copy frames the row as a setup prompt.

Output: One string literal changed in `KickOffSetupPanel.tsx` plus regression
tests locking both branches of the `placementValid` ternary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@packages/client/src/components/KickOffSetupPanel.tsx
@packages/client/src/components/KickOffSetupPanel.test.tsx
</context>

<interface_notes>
Facts confirmed by reading the source before planning — do not re-derive:

- The target literal is at `KickOffSetupPanel.tsx:119`, inside the
  `placementValid ? ... : ...` ternary that starts at line 118.
- The invalid branch is a template literal:
  `` `${piecesLabel} out of position — move into ${zoneGuidance}.` `` (line 120).
  It stays as-is; `piecesLabel` (line 89) and `zoneGuidance` (lines 86-88) remain
  used, so no unused-variable lint fallout.
- The wrapping `<span>` uses inline `style={{ color: placementValid ? 'var(--color-text-secondary)' : 'var(--color-danger)' }}`
  (line 116). Colour logic is out of scope — leave it untouched.
- A separate attacker-only centre-hex constraint row (lines 101-112) has its own
  copy ("A player is on the centre hex — ready to go." / "Place a player on the
  centre hex to start the kick-off."). Out of scope — do not touch.
- No existing test asserts on the old string (grep confirmed: the only other
  occurrences are historical planning artifacts under
  `.planning/phases/35-actionpanel-log-standardization/`, which are point-in-time
  records and MUST NOT be edited).
- `KickOffSetupPanel.test.tsx` already has the fixture helpers this work needs:
  `kickOffSetupState(overrides)` (line 24) and `KICK_OFF_HEX = { q: 18, r: 13 }`
  (line 21). `beforeEach` seeds `playerSlot: 1` (home, attacking).
  Default `mockMovementState` positions are all in-zone, so `placementValid` is
  already `true` with no overrides — the valid branch needs no piece surgery.
- Test commands: `pnpm --filter @counter-attack/client test` (vitest run),
  `pnpm --filter @counter-attack/client typecheck` (tsc --noEmit).
  </interface_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression tests for both placement-status branches</name>
  <files>packages/client/src/components/KickOffSetupPanel.test.tsx</files>
  <behavior>
    - Test 1 (RED until Task 2): with the default `kickOffSetupState()` fixture (all
      home pieces in-zone, `placementValid === true`), the panel renders the text
      `Setup your players for kickoff.`
    - Test 2 (RED until Task 2): the same valid-state render does NOT contain the
      text `All your players are in a legal position.`
    - Test 3 (passes before and after): when at least one home piece is moved
      out of zone (e.g. override `home-9` position to `{ q: 30, r: 5 }`, which is
      `q > 18` and outside the centre circle), the panel renders text matching
      `/out of position — move into/i`, proving the invalid branch is untouched.
  </behavior>
  <action>
    Append a new `describe('KickOffSetupPanel — placement status copy', ...)` block
    at the end of `KickOffSetupPanel.test.tsx`, after the existing D-09 describe
    block. Reuse the existing module-level helpers `kickOffSetupState` and
    `KICK_OFF_HEX` rather than defining new fixtures; `beforeEach` already seeds
    `playerSlot: 1` and the KICK_OFF_SETUP gameState, so the valid-state tests
    call `render(<KickOffSetupPanel />)` directly with no `setState` override.

    For the invalid-state test, call
    `useGameStore.setState({ gameState: kickOffSetupState({ pieces: ... }) })`
    following the exact `mockMovementState.pieces.map(...)` override shape already
    used at lines 72-75, but relocating `home-9` to an out-of-zone hex instead of
    `KICK_OFF_HEX`.

    Assertions use `screen.getByText` / `screen.queryByText` with regex matchers,
    matching the style of the existing tests. Do not use snapshot assertions.

    Run the suite after writing and confirm Tests 1 and 2 FAIL and Test 3 PASSES
    before moving to Task 2 — that failure is the RED signal proving the tests
    actually bind to the string being changed.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client exec vitest run src/components/KickOffSetupPanel.test.tsx</automated>
  </verify>
  <done>Three new tests exist; the two asserting the new copy fail with a "unable to find an element with the text" error, and the out-of-position test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the placement-valid string and confirm green</name>
  <files>packages/client/src/components/KickOffSetupPanel.tsx</files>
  <behavior>
    - The three tests from Task 1 all pass.
    - No other assertion in the client suite regresses.
  </behavior>
  <action>
    In `packages/client/src/components/KickOffSetupPanel.tsx`, replace the string
    literal on line 119 — `'All your players are in a legal position.'` — with
    `'Setup your players for kickoff.'`. This is the only production change.

    Do NOT alter: the `placementValid` computation (line 76), the inline colour
    style (line 116), the invalid-branch template literal (line 120), the
    `zoneGuidance`/`piecesLabel`/`disabledTitle` consts (lines 82-89), the
    attacker centre-hex constraint row (lines 101-112), or the CTA block
    (lines 134-148).

    Do NOT edit the historical planning artifacts that quote the old string
    (`.planning/phases/35-actionpanel-log-standardization/35-VERIFICATION.md` and
    `35-06-PLAN.md`) — those are point-in-time records of what was verified then;
    rewriting them would falsify the audit trail.

    After the edit, run the full client test suite and typecheck. Prettier
    formatting is enforced by lint-staged on commit; keep the existing indentation
    of the ternary so the diff stays a single line.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test && pnpm --filter @counter-attack/client typecheck && grep -rn --include=*.tsx --include=*.ts "All your players are in a legal position" packages/client/src | grep -v '^\s*//' | wc -l | grep -qx '0'</automated>
  </verify>
  <done>Full client vitest suite passes, `tsc --noEmit` is clean, and the old string returns zero non-comment matches under `packages/client/src`.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                         |
| --------------- | ----------------------------------------------------------------------------------- |
| none introduced | Change is a hardcoded client-side display string; no new input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                            |
| --------- | ---------------------- | ---------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-o85-01  | Information disclosure | `KickOffSetupPanel` constraint row | accept      | New copy is a static literal containing no game state, player identity, or opponent placement data — strictly less information than the string it replaces |
| T-o85-02  | Tampering              | dependency tree                    | accept      | No package installs; no `pnpm add`, no lockfile change. Package Legitimacy Gate not applicable                                                             |

</threat_model>

<verification>
1. `pnpm --filter @counter-attack/client test` — full client suite green (includes the 3 new tests plus the 5 pre-existing `KickOffSetupPanel` tests).
2. `pnpm --filter @counter-attack/client typecheck` — clean.
3. `grep -rn "All your players are in a legal position" packages/client/src` — zero matches.
4. `grep -rn "out of position — move into" packages/client/src` — still exactly one match (invalid branch preserved).
5. `git diff --stat` — exactly two files changed; `KickOffSetupPanel.tsx` shows a 1-line replacement.
</verification>

<success_criteria>

- `KickOffSetupPanel.tsx:119` renders `Setup your players for kickoff.` when `placementValid` is true.
- The out-of-position branch, its danger colour, the centre-hex constraint row, and the Confirm CTA are byte-identical to before.
- Three new tests in `KickOffSetupPanel.test.tsx` lock both branches; they failed before the source edit and pass after.
- Client test suite and typecheck are green; no planning artifacts were rewritten.
  </success_criteria>

<output>
Create `.planning/quick/260727-o85-change-kickoffsetuppanel-tsx-placement-v/260727-o85-SUMMARY.md` when done
</output>
