---
phase: quick-260620-9ql
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-rule-bugs/17-05-PLAN.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - packages/server/src/__tests__/gameEngine.phase17.test.ts
  - packages/server/src/__tests__/gameHandlers.phase17.test.ts
autonomous: true
requirements: [PASS-02]
user_setup: []
must_haves:
  truths:
    - 'Plan 17-05 (the abandoned single-step PASS-02 design) no longer exists on disk'
    - 'ROADMAP Phase 17 lists 4 plans and no longer references 17-05-PLAN.md'
    - 'REQUIREMENTS PASS-02 stays [x] Complete but is attributed to Phase 17.1 FIRST_TIME_PASS_MOVE redesign'
    - 'The 2 stale abandoned-design test stubs (firstTimePassStep/firstTimePassPath single-step + SNAP_DEFLECT FIRST_TIME_PASS) are deleted'
    - 'Server typecheck is clean and the phase17 suites show exactly 2 fewer failing tests (only the 2 MOVE-06 FREE_MOVE failures remain)'
  artifacts:
    - path: '.planning/ROADMAP.md'
      provides: 'Phase 17 plan list and count corrected to 4 plans'
      contains: '**Plans**: 4 plans'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'PASS-02 attribution corrected to Phase 17.1'
      contains: 'PASS-02'
  key_links:
    - from: 'gameEngine.phase17.test.ts'
      to: 'abandoned firstTimePassStep/firstTimePassPath design'
      via: 'deleted describe/it block'
      pattern: 'firstTimePassStep'
    - from: 'gameHandlers.phase17.test.ts'
      to: 'abandoned SNAP_DEFLECT FIRST_TIME_PASS design'
      via: 'deleted describe/it block'
      pattern: 'lastActionType=FIRST_TIME_PASS'
---

<objective>
Cancel the never-executed Phase 17 plan 17-05 and remove the stale abandoned-design test stubs it left behind. Plan 17-05 described the OLD single-step PASS-02 design (attacker moves 1 non-passer, defender moves 1 player onto the pass path, reusing SNAP_DEFLECT). It was superseded by Phase 17.1's shipped two-slot FIRST_TIME_PASS_MOVE redesign, which already satisfies PASS-02 (verified across 5 cycles, including undo and self-pass-reclaim exclusion).

Purpose: Clean up planning artifacts and dead Wave-0 tests so the codebase reflects what was actually built. Correct the PASS-02 attribution from Phase 17 to Phase 17.1.
Output: 17-05-PLAN.md deleted; ROADMAP + REQUIREMENTS corrected; 2 stale test stubs deleted; server typecheck clean and phase17 suites down to only the 2 unrelated MOVE-06 failures.

This is a docs + dead-test cleanup task ONLY. Do NOT modify gameEngine.ts, gameHandlers.ts, useGameStore.ts, or any other production source file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete plan 17-05 and correct ROADMAP + REQUIREMENTS attribution</name>
  <files>.planning/phases/17-rule-bugs/17-05-PLAN.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    1. Delete the file `.planning/phases/17-rule-bugs/17-05-PLAN.md` (it has no SUMMARY — never executed). Use `rm` via Bash, or the platform delete; confirm it is gone.

    2. In `.planning/ROADMAP.md` Phase 17 section (around lines 105-111):
       - Change the `**Plans**: 5 plans` line (line ~105) to `**Plans**: 4 plans`.
       - Delete the entire `- [ ] 17-05-PLAN.md — PASS-02 First-time Pass mid-flight repositioning (attacker step + SNAP_DEFLECT reuse + path highlight)` bullet (line ~111).
       - Do NOT touch the Phase 17 Goal line or any of the numbered Success Criteria (criterion 5 mentions first-time-pass repositioning — it remains TRUE, just delivered by Phase 17.1). Do NOT touch the 17-01..17-04 bullets.

    3. In `.planning/REQUIREMENTS.md`:
       - Keep the PASS-02 entry (line ~39) marked `[x]` Complete. Update its note so it reflects the actual delivery: it was delivered by Phase 17.1's two-slot `FIRST_TIME_PASS_MOVE` redesign, NOT Phase 17's originally-planned single-step design (plan 17-05, cancelled/superseded). Suggested text (adapt wording, keep the requirement intent): `**PASS-02**: During a First-time Pass flight, each team may move one player up to 1 hex. Delivered by Phase 17.1 two-slot FIRST_TIME_PASS_MOVE redesign (Phase 17 plan 17-05 single-step design cancelled/superseded).`
       - In the Traceability table, change the PASS-02 row `Phase` cell from `Phase 17` to `Phase 17.1` (keep Status `Complete`). Leave MOVE-06 and all other rows untouched.

  </action>
  <verify>
    <automated>test ! -f .planning/phases/17-rule-bugs/17-05-PLAN.md && grep -q "4 plans" .planning/ROADMAP.md && ! grep -q "17-05-PLAN.md" .planning/ROADMAP.md && grep -q "17.1" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>17-05-PLAN.md is gone; ROADMAP shows 4 plans with no 17-05 bullet and unchanged success criteria; REQUIREMENTS PASS-02 stays Complete with Phase 17.1 attribution in both the entry note and the traceability table.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 2 stale abandoned-design test stubs</name>
  <files>packages/server/src/__tests__/gameEngine.phase17.test.ts, packages/server/src/__tests__/gameHandlers.phase17.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.phase17.test.ts (lines 539-561: the describe block "Phase 17 PASS-02: FIRST_TIME_PASS delivery enters attacker repositioning step" + its section banner comment; the `firstTimePassState` fixture at line ~177; the comment at line ~757 that references `firstTimePassState` by name)
    - packages/server/src/__tests__/gameHandlers.phase17.test.ts (lines 284-308: the describe block "Phase 17 PASS-02: SNAP_DEFLECT end-turn resolves as pass when lastActionType=FIRST_TIME_PASS"; the `seedSnapDeflectFirstTimePass` helper at lines 194-243 — confirm it is used ONLY by this describe block)
  </read_first>
  <action>
    Delete ONLY the abandoned single-step design tests. Do NOT touch any other test.

    gameEngine.phase17.test.ts:
    - Delete the describe block "Phase 17 PASS-02: FIRST_TIME_PASS delivery enters attacker repositioning step" (the describe at line ~543 and its single `it`, roughly lines 543-561) along with its section-banner comment immediately above (the `// ---- PASS-02: applyRoll FIRST_TIME_PASS → attacker repositioning sub-state ----` block, ~lines 539-541).
    - The `firstTimePassState` fixture (line ~177) is used by the deleted test AND referenced by name in the comment at line ~757. Do NOT delete the fixture in this task — removing it would orphan the line-757 comment which is part of a still-valid Phase 17.1 CR-02-new test. Leave `firstTimePassState` in place. (Confirm via grep that after deleting the describe block, `firstTimePassState` is still referenced at line ~548? No — that reference is inside the deleted block. After deletion `firstTimePassState` is referenced only by the line-757 comment. An unused const will trigger a lint/typecheck `no-unused-vars` error. Therefore: keep the fixture ONLY if it remains referenced by live code; if the typecheck/lint flags it as unused, instead delete the `firstTimePassState` fixture AND update the line-757 comment to remove the now-dangling "unlike the out-of-range firstTimePassState fixture above" clause, rewording it to stand alone, e.g. "(distance 4, within the FIRST_TIME cap of 6)". Run typecheck to decide which path applies — do not guess.)

    gameHandlers.phase17.test.ts:
    - Delete the describe block "Phase 17 PASS-02: SNAP_DEFLECT end-turn resolves as pass when lastActionType=FIRST_TIME_PASS" (the describe at line ~289 and its single `it`, roughly lines 284-308 including the section-banner comment above it at ~lines 284-287).
    - Delete the now-orphaned `seedSnapDeflectFirstTimePass` helper (lines ~194-243, including its doc comment) — confirm via grep it has zero remaining references after the describe block is removed, then delete it.

    Do NOT touch the MOVE-06 FREE_MOVE tests, the BUG-02 cancel-movement tests, the smoke/regression guards, or any FIRST_TIME_PASS_MOVE / FTP_MOVE / CR-02-new tests (those are Phase 17.1, current and correct).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server typecheck</automated>
  </verify>
  <done>Both stale describe blocks are deleted; orphaned helpers/fixtures handled per the typecheck-driven rule (no unused-var errors, no dangling comment references); no other test or production file modified; server typecheck is clean.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @counter-attack/server typecheck` exits 0 (clean — no unused-var or dangling-reference errors).
- `pnpm --filter @counter-attack/server test -- gameEngine.phase17 gameHandlers.phase17` shows exactly 2 fewer failing tests than the pre-task baseline (the 2 deleted abandoned-design stubs). The ONLY remaining failures must be the 2 MOVE-06 FREE_MOVE tests (plan 17-04, still unexecuted, out of scope). If any other test fails or any FIRST_TIME_PASS_MOVE test breaks, the deletion was too aggressive — revert and re-scope.
- `git diff --stat` touches only the 5 files in `files_modified` (plus the deletion of 17-05-PLAN.md). No production source (gameEngine.ts, gameHandlers.ts, useGameStore.ts, etc.) appears in the diff.
</verification>

<success_criteria>

- 17-05-PLAN.md deleted; ROADMAP Phase 17 = 4 plans, no 17-05 bullet, success criteria untouched.
- REQUIREMENTS PASS-02 stays Complete, attributed to Phase 17.1 in both the entry and traceability table.
- 2 stale abandoned-design test stubs deleted; server typecheck clean; phase17 suites down to only the 2 unrelated MOVE-06 failures.
- Zero production source files modified.
  </success_criteria>

<output>
Create `.planning/quick/260620-9ql-cancel-phase-17-plan-17-05-superseded-by/260620-9ql-SUMMARY.md` when done
</output>
