---
phase: quick-260612-l7d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/GameBoard.module.css
  - packages/client/src/components/ActionPanel.module.css
autonomous: true
requirements:
  - LAYOUT-TOP-BAND-REDESIGN

must_haves:
  truths:
    - 'Top band has exactly three CSS grid tracks: 1fr auto 1fr'
    - 'Scoreboard (auto track) is visually distinct with a background/border and is horizontally centred on screen'
    - 'Scoreboard internal grid is: home-shield+score | clock+connection+phaseInfo | score+away-shield'
    - 'Player card is centred horizontally within the 1fr left zone'
    - 'Action panel is centred horizontally within the 1fr right zone'
    - 'ActionPanel buttons render in a 2-column grid (1fr 1fr), no scroll'
    - 'centreSection track is eliminated — its content lives inside the scoreboard centre cell'
  artifacts:
    - path: packages/client/src/components/GameBoard.tsx
      provides: Restructured top-band JSX with 3-track grid and scoreboard absorbing centreSection
    - path: packages/client/src/components/GameBoard.module.css
      provides: Updated CSS for topBand 3-track, scoreboard block, left/right flex zones
    - path: packages/client/src/components/ActionPanel.module.css
      provides: 2-column button grid inside .panel
  key_links:
    - from: GameBoard.tsx topBand div
      to: scoreboard centre cell
      via: scoreboardCentreCell div containing clockDisplay, ConnectionStatus, phaseSummary
      pattern: 'scoreboardCentreCell'
---

<objective>
Redesign the GameBoard top-band from a 4-track layout into a 3-track layout where the
scoreboard (auto width) is the visual centrepiece. The player card occupies the left 1fr zone
centred within it, and the action panel occupies the right 1fr zone centred within it.
The existing centreSection track is eliminated: its clock, connection status, and phase
summary content moves inside the scoreboard's centre cell.

Purpose: Creates a clear visual hierarchy where the score is the focal point, flanked
symmetrically by player info and action controls.

Output: Updated GameBoard.tsx (JSX restructure), GameBoard.module.css (new grid + scoreboard
styles), ActionPanel.module.css (2-column button layout).
</objective>

<execution_context>
@D:/Users/jerem/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@D:/dev/repo/counter-attack-poc/.planning/STATE.md
@D:/dev/repo/counter-attack-poc/packages/client/src/components/GameBoard.tsx
@D:/dev/repo/counter-attack-poc/packages/client/src/components/GameBoard.module.css
@D:/dev/repo/counter-attack-poc/packages/client/src/components/ActionPanel.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure top-band JSX in GameBoard.tsx</name>
  <files>packages/client/src/components/GameBoard.tsx</files>
  <action>
Replace the topBand div's child structure. The current layout has four children:
- .playerCardSection (track 1)
- .centreSection (track 2, contains scoreRow + connectionLine + phaseSummary)
- .topBandSection wrapping .actionSection (track 3)
- (implicit empty track 4)

The new layout has three children inside .topBand:

CHILD 1 — Left zone (.topBandLeft):
A div with className={styles.topBandLeft}.
Inside it, render the existing player card JSX verbatim (the displayPiece conditional
rendering the .playerCard3Col or the .playerCardPlaceholder span). No other changes to
the player card content.

CHILD 2 — Scoreboard (.scoreboard):
A div with className={styles.scoreboard}.
Inside it, an internal 3-cell grid (.scoreboardGrid):

Cell A (.scoreboardHomeCell): flex row, align-items center, gap 6px - TeamShieldIcon color="#1a56b0" - score numeral: className={styles.scoreNumeral} style={{ color: '#1a56b0' }}, text {score.home}

Cell B (.scoreboardCentreCell): flex column, align-items center, justify-content center, gap 2px - span className={styles.clockDisplay} → {clockDisplay} - div className={styles.connectionLine} → <ConnectionStatus /> - div className={styles.phaseSummary}:
span className={styles.teamName} style={{ color: teamColor }} → {teamName}
conditional phaseLabel span (phase !== 'REPLAY' && phaseLabel): className={styles.phaseLabel}, text "&nbsp;{phaseLabel}"
conditional movesRemaining span (phase === 'MOVEMENT' && movementSlot != null && remaining != null):
className={styles.movesRemaining}, text "&nbsp;·&nbsp;{movementSlot}&nbsp;·&nbsp;{remaining} moves remaining"

Cell C (.scoreboardAwayCell): flex row, align-items center, gap 6px - score numeral: className={styles.scoreNumeral} style={{ color: '#c0392b' }}, text {score.away} - TeamShieldIcon color="#c0392b"

CHILD 3 — Right zone (.topBandRight):
A div with className={styles.topBandRight}.
Inside it, the action panel switcher:
{phase === 'KICK_OFF_SETUP' ? <KickOffSetupPanel /> : phase === 'REPLAY' ? <ReplayPanel /> : <ActionPanel />}

Remove the old .topBandSection wrapper div entirely (the action content now sits directly
inside .topBandRight). Remove the empty track 4 comment. The existing variable declarations
(clockDisplay, teamName, teamColor, phaseLabel, remaining, displayPiece, etc.) are unchanged.
</action>
<verify>
<automated>cd D:\dev\repo\counter-attack-poc && pnpm --filter client tsc --noEmit 2>&1 | tail -20</automated>
</verify>
<done>GameBoard.tsx compiles with no TypeScript errors. topBand has exactly 3 direct children: .topBandLeft, .scoreboard, .topBandRight. centreSection div and .topBandSection div are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite top-band CSS and add 2-column ActionPanel button grid</name>
  <files>packages/client/src/components/GameBoard.module.css, packages/client/src/components/ActionPanel.module.css</files>
  <action>
--- GameBoard.module.css changes ---

Replace the .topBand rule:
grid-template-columns: 1fr auto 1fr
(height, flex-shrink, background, border-bottom remain the same)

Remove the .topBandSection rule entirely (no longer used).

Remove the .centreSection rule entirely (no longer used).

Remove the .scoreRow rule (the scoreRow div inside centreSection is gone; score cells are
now inside .scoreboardHomeCell and .scoreboardAwayCell).

Add new rules:

.topBandLeft
display: flex
align-items: center
justify-content: center
padding: 4px 8px
background: #16213e
border-right: 1px solid #0f3460
overflow: hidden

.topBandRight
display: flex
align-items: center
justify-content: center
padding: 4px 8px
background: #16213e
border-left: 1px solid #0f3460
overflow: hidden

.scoreboard
display: flex
align-items: stretch
background: #0f3460
border-left: 1px solid #1a56b0
border-right: 1px solid #1a56b0

.scoreboardGrid
display: grid
grid-template-columns: auto 1fr auto
align-items: center

.scoreboardHomeCell
display: flex
align-items: center
gap: 6px
padding: 0 10px

.scoreboardAwayCell
display: flex
align-items: center
gap: 6px
padding: 0 10px

.scoreboardCentreCell
display: flex
flex-direction: column
align-items: center
justify-content: center
gap: 2px
padding: 4px 12px
border-left: 1px solid rgba(255,255,255,0.1)
border-right: 1px solid rgba(255,255,255,0.1)

Keep .playerCardSection if it still exists in JSX; otherwise it can remain as dead CSS (no
harm). The .playerCard3Col, .playerCardInfoCol, .playerCardStatsCol, .statRow, .statLabel,
.statBubble\*, .playerCardName, .playerCardRole, .playerCardPlaceholder rules are all unchanged.

Keep .clockDisplay, .connectionLine, .phaseSummary, .teamName, .phaseLabel, .movesRemaining,
.scoreNumeral — these are now used inside .scoreboardCentreCell and .scoreboardHomeCell /
.scoreboardAwayCell, so their values need no changes.

Keep .actionSection rule as-is — it is still referenced if any panel wraps itself in it,
though .topBandSection is removed. (If actionSection is no longer referenced by any JSX
after Task 1, it becomes harmless dead CSS.)

--- ActionPanel.module.css changes ---

In the .panel rule, change flex-direction: column and gap: 3px to:
display: grid
grid-template-columns: 1fr 1fr
gap: 3px
align-items: start

Add an explicit rule so .phaseLabel, .errorText, and .gkLabel span the full 2 columns (they
are status text, not action buttons):
.phaseLabel, .gkLabel, .errorText {
grid-column: 1 / -1;
}

The .ctaButton and .backButton height/font-size do not need changing (already 11px font,
~20-22px effective height with padding: 3px 8px).
</action>
<verify>
<automated>cd D:\dev\repo\counter-attack-poc && pnpm --filter client build 2>&1 | tail -30</automated>
</verify>
<done>Build succeeds with no errors. .topBand has grid-template-columns: 1fr auto 1fr. .scoreboard rule exists with background: #0f3460. .panel in ActionPanel.module.css uses display: grid with grid-template-columns: 1fr 1fr. .phaseLabel / .gkLabel / .errorText have grid-column: 1 / -1.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                         |
| ---------------------- | ------------------------------------------------------------------- |
| CSS module class names | Renaming/removing classes must not leave dangling references in TSX |

## STRIDE Threat Register

| Threat ID | Category  | Component            | Disposition | Mitigation Plan                                                                                   |
| --------- | --------- | -------------------- | ----------- | ------------------------------------------------------------------------------------------------- |
| T-l7d-01  | Tampering | CSS class references | mitigate    | TypeScript build (tsc --noEmit) catches missing module exports; task verify runs tsc before build |

</threat_model>

<verification>
1. TypeScript check: `pnpm --filter client tsc --noEmit` — zero errors
2. Vite build: `pnpm --filter client build` — zero errors/warnings
3. Browser check (manual): top band shows 3 zones; scoreboard background #0f3460 is visually distinct and centred; player card is centred in left 1fr zone; action buttons render in 2-column grid without scrolling
</verification>

<success_criteria>

- top-band grid has exactly 3 tracks: 1fr auto 1fr
- Scoreboard (auto) is the visual centrepiece with background: #0f3460 and side borders
- Scoreboard left cell: home shield + home score numeral (#1a56b0)
- Scoreboard centre cell: clockDisplay (gold) + ConnectionStatus dot + phaseSummary (team name, phase label, moves remaining)
- Scoreboard right cell: away score numeral (#c0392b) + away shield
- Player card is flex-centred inside topBandLeft (1fr)
- ActionPanel / KickOffSetupPanel / ReplayPanel is flex-centred inside topBandRight (1fr)
- ActionPanel .panel uses display: grid, grid-template-columns: 1fr 1fr; text-only spans span full width via grid-column: 1 / -1
- Build passes, zero TypeScript errors
  </success_criteria>

<output>
No SUMMARY.md required for quick tasks. Return confirmation to the orchestrator on completion.
</output>
