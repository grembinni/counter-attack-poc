---
phase: 19-data-model-team-palette
plan: 03
type: execute
wave: 2
depends_on: ['19-01']
files_modified:
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/PlayerStatsPanel.tsx
  - packages/client/src/components/TeamSelectionScreen.tsx
  - packages/client/src/components/PieceOverlay.tsx
  - packages/client/src/components/TeamBadge.tsx
  - packages/client/src/mock/mockMovementState.ts
  - packages/client/src/components/ActionLog.test.tsx
  - packages/client/src/components/GameBoard.test.tsx
  - packages/client/src/components/PlayerStatsPanel.test.tsx
  - packages/client/src/components/TeamSelectionScreen.test.tsx
autonomous: true
requirements: [PALETTE-02, TEAM-07]

must_haves:
  truths:
    - 'All client reads of TeamConfig.primaryColor are replaced by palette.primary (PALETTE-02, D-10)'
    - 'No client file references TeamConfig.primaryColor or .secondaryColor (PALETTE-02)'
    - 'TeamId-keyed Record maps (TeamBadge BADGE_MAP/BADGE_MAP_FULL, TeamSelectionScreen FULL_BADGE_MAP) and ALL_TEAMS contain only city and crew (TEAM-07, D-04)'
    - "mockMovementState uses PLAYER_POOL filtered by sourceTeamId and selectedTeams {home:'city', away:'crew'} (TEAM-07)"
    - 'Client test suite passes with player-name assertions updated to city/crew squad members'
  artifacts:
    - path: 'packages/client/src/mock/mockMovementState.ts'
      provides: 'Mock GameState built from PLAYER_POOL'
      contains: 'PLAYER_POOL'
    - path: 'packages/client/src/components/TeamBadge.tsx'
      provides: 'Badge maps keyed to shrunk TeamId'
      contains: 'Record<TeamId, string>'
  key_links:
    - from: 'packages/client/src/mock/mockMovementState.ts'
      to: '@counter-attack/shared PLAYER_POOL'
      via: 'filter by sourceTeamId'
      pattern: 'PLAYER_POOL.filter'
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: 'TEAM_CONFIGS[...].palette.primary'
      via: 'field access'
      pattern: "palette\\.primary"
---

<objective>
Update all client consumers to the new palette model and the shrunk TeamId union: swap every `.primaryColor` read to `.palette.primary`, shrink the TeamId-keyed badge/team Record maps to city/crew, rebuild mockMovementState from PLAYER_POOL, and update the client tests whose assertions depended on cosmos/xolos squads or team ids.

Purpose: After Plan 19-01 removes primaryColor and shrinks TeamId, every client file that reads primaryColor or keys a Record by TeamId fails to compile. The mock and its dependent tests assume cosmos(home)/xolos(away) squads, which no longer exist as selectable teams.

Output: All 5 field-swap consumers plus TeamBadge and TeamSelectionScreen compile; mockMovementState draws from PLAYER_POOL; the client test suite passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-data-model-team-palette/19-PATTERNS.md
@.planning/phases/19-data-model-team-palette/19-01-SUMMARY.md
</context>

<artifacts_produced>

## Artifacts this phase produces (Plan 19-03)

This plan creates no new exported symbols. It rewires existing client components, one mock, and four test files to consume symbols created by Plan 19-01 (`PLAYER_POOL`, `PoolPlayer.sourceTeamId`, `TeamConfig.palette`) and the shrunk `TeamId` union. The maps `BADGE_MAP`, `BADGE_MAP_FULL`, `FULL_BADGE_MAP`, and the `ALL_TEAMS` array are existing symbols being narrowed, not new. All referenced shared symbols are declared in Plan 19-01's Artifacts section — exclude from drift verification.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Swap primaryColor → palette.primary across the 5 consumer components</name>
  <files>packages/client/src/components/ActionLog.tsx, packages/client/src/components/GameBoard.tsx, packages/client/src/components/PlayerStatsPanel.tsx, packages/client/src/components/TeamSelectionScreen.tsx, packages/client/src/components/PieceOverlay.tsx</files>
  <read_first>
    - packages/client/src/components/ActionLog.tsx (lines 12, 37 — the .primaryColor reads)
    - packages/client/src/components/GameBoard.tsx (lines 180, 198, 205, 207, 261, 298, 341, 370, 396, 412 — the .primaryColor reads)
    - packages/client/src/components/PlayerStatsPanel.tsx (line 61)
    - packages/client/src/components/TeamSelectionScreen.tsx (lines 119–120)
    - packages/client/src/components/PieceOverlay.tsx (lines 97–98)
    - .planning/phases/19-data-model-team-palette/19-PATTERNS.md (Shared Pattern lines 585–589 — the exact mechanical rule)
    - .planning/phases/19-data-model-team-palette/19-CONTEXT.md (D-10 — field access only; no SVG pattern rewrite in Phase 19)
  </read_first>
  <action>
    In all 5 files, replace every occurrence of `.primaryColor` with `.palette.primary`. The reads are of the form `TEAM_CONFIGS[<id>].primaryColor` and `teamConfig.primaryColor` — the substitution is `.primaryColor` → `.palette.primary` with no structural change. This is exactly 12 occurrences total (ActionLog ×2, GameBoard ×8, PlayerStatsPanel ×1, TeamSelectionScreen ×2 at lines 119–120, PieceOverlay ×2). Do NOT change PieceOverlay's SVG pattern/url(#…) logic — only the two field-access reads at lines 97–98 (per D-10, the pattern rewrite is Phase 20). Do not touch any `secondaryColor` reference unless present (RESEARCH confirms none in these display reads).
  </action>
  <verify>
    <automated>! grep -rn "primaryColor\|secondaryColor" packages/client/src/components/ActionLog.tsx packages/client/src/components/GameBoard.tsx packages/client/src/components/PlayerStatsPanel.tsx packages/client/src/components/TeamSelectionScreen.tsx packages/client/src/components/PieceOverlay.tsx && echo SWAPS_DONE</automated>
  </verify>
  <acceptance_criteria>
    - None of the 5 component files contain the strings `primaryColor` or `secondaryColor`
    - GameBoard.tsx contains `palette.primary` at 8 locations (grep -c `palette.primary` >= 8)
    - PieceOverlay.tsx still contains its `url(#` pattern references (SVG logic untouched)
  </acceptance_criteria>
  <done>All 12 primaryColor reads across the 5 consumers now read palette.primary; no SVG pattern logic changed.</done>
</task>

<task type="auto">
  <name>Task 2: Shrink TeamId-keyed badge/team maps in TeamBadge and TeamSelectionScreen</name>
  <files>packages/client/src/components/TeamBadge.tsx, packages/client/src/components/TeamSelectionScreen.tsx</files>
  <read_first>
    - packages/client/src/components/TeamBadge.tsx (BADGE_MAP and BADGE_MAP_FULL Record<TeamId,string> with cosmos/xolos keys + their imports lines 9–31)
    - packages/client/src/components/TeamSelectionScreen.tsx (ALL_TEAMS line 22; FULL_BADGE_MAP Record<TeamId,string> lines 25–30 with cosmos/xolos imports lines 16–17)
    - .planning/phases/19-data-model-team-palette/19-CONTEXT.md (D-04 — TeamId shrinks; Phase 21 re-adds teams)
  </read_first>
  <action>
    TeamId is now `'city' | 'crew'`, so any `Record<TeamId, string>` must have exactly the keys city and crew. In TeamBadge.tsx, remove the `cosmos`/`xolos` keys from both `BADGE_MAP` and `BADGE_MAP_FULL`, and remove the now-unused cosmos/xolos badge imports (cosmosBadge, xolosBadge, cosmosBadgeFull, xolosBadgeFull). Keep city/crew entries.

    In TeamSelectionScreen.tsx, change `ALL_TEAMS` from `['cosmos', 'xolos', 'city', 'crew']` to `['city', 'crew']` (Phase 19 has 2 selectable teams; Phase 21 re-expands per LEAGUE-01). Remove the `cosmos`/`xolos` keys from `FULL_BADGE_MAP` and remove the unused cosmosFullBadge/xolosFullBadge imports (lines 16–17). Update the PLAY-03 "exactly 4 teams" comment to reflect the transitional 2-team state with a note that Phase 21 restores the full grid.

  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "error TS" | grep -q "^0$" && echo CLIENT_TYPES_OK</automated>
  </verify>
  <acceptance_criteria>
    - TeamBadge.tsx BADGE_MAP and BADGE_MAP_FULL contain only city and crew keys
    - TeamBadge.tsx does NOT import cosmosBadge or xolosBadge (regular or full)
    - TeamSelectionScreen.tsx ALL_TEAMS is `['city', 'crew']`
    - TeamSelectionScreen.tsx FULL_BADGE_MAP contains only city and crew keys
    - `cd packages/client && npx tsc --noEmit` reports 0 errors (this task also depends on Task 1's swaps compiling)
  </acceptance_criteria>
  <done>All TeamId-keyed Record maps and the ALL_TEAMS array contain only city/crew; unused retired-team badge imports removed; client compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild mockMovementState from PLAYER_POOL and update dependent test assertions</name>
  <files>packages/client/src/mock/mockMovementState.ts, packages/client/src/components/ActionLog.test.tsx, packages/client/src/components/GameBoard.test.tsx, packages/client/src/components/PlayerStatsPanel.test.tsx, packages/client/src/components/TeamSelectionScreen.test.tsx</files>
  <read_first>
    - packages/client/src/mock/mockMovementState.ts (current TEAM_SQUADS.cosmos/xolos usage + selectedTeams — the file to rewrite)
    - packages/client/src/components/ActionLog.test.tsx (lines 189, 194, 238–239, 286–288 — hardcoded player-name assertions "Nicolae Rusu" / "Oliver Walker" tied to cosmos/xolos squads)
    - packages/client/src/components/GameBoard.test.tsx (lines 198–210 — badge alt assertions "cosmos badge" / "xolos badge")
    - packages/client/src/components/PlayerStatsPanel.test.tsx (line 27 — selectedTeams override to cosmos/xolos)
    - packages/client/src/components/TeamSelectionScreen.test.tsx (lines 98, 127, 140, 148 — homePickedTeam="cosmos" and calledWith expectations)
    - .planning/phases/19-data-model-team-palette/19-01-SUMMARY.md (city/crew squad player names by slot — use these to set the new assertions; do NOT re-derive)
    - .planning/phases/19-data-model-team-palette/19-PATTERNS.md (target mock lines 555–577; Pitfall-3 warning about test names lines 579)
  </read_first>
  <action>
    Rewrite mockMovementState.ts per 19-PATTERNS.md lines 555–577: import `PLAYER_POOL` (not TEAM_SQUADS); build home pieces from `PLAYER_POOL.filter(p => p.sourceTeamId === 'city').map((p, i) => ({ ...p, teamId: 'home' as const, id: `home-${i}`, position: HOME_POSITIONS[`home-${i}`] ?? p.position }))` and away from `sourceTeamId === 'crew'` with `id: `away-${i}`` and `position: AWAY_POSITIONS[`away-${i}`] ?? { q: 36 - p.position.q, r: p.position.r }`. Set `selectedTeams: { home: 'city', away: 'crew' }`. Keep HOME_POSITIONS/AWAY_POSITIONS maps and ball.carrierId 'home-9' unchanged.

    Update ActionLog.test.tsx: replace the hardcoded cosmos/xolos player-name assertions with the actual city (home) and crew (away) player names at the referenced slots (home-9, and the away id used), sourced from 19-01-SUMMARY.md. Update the code comments that say "in the cosmos squad" to reference city/crew.

    Update GameBoard.test.tsx lines 198–210: change `getByAltText('cosmos badge')` → `getByAltText('city badge')` and `getByAltText('xolos badge')` → `getByAltText('crew badge')`, and update the test descriptions/comments from cosmos/xolos to city/crew.

    Update PlayerStatsPanel.test.tsx line 27: change the selectedTeams override from `{ home: 'cosmos', away: 'xolos' }` to `{ home: 'city', away: 'crew' }`.

    Update TeamSelectionScreen.test.tsx: change `homePickedTeam="cosmos"` occurrences (lines 98, 127, 148) to `homePickedTeam="city"`, and change the `expect(['xolos', 'city', 'crew']).toContain(calledWith)` (line 140) to reflect the shrunk selectable set — since ALL_TEAMS is now ['city','crew'] and 'city' is struck out, the only pickable option is 'crew'; assert `expect(calledWith).toBe('crew')` (or `expect(['crew']).toContain(calledWith)`), and adjust any count-of-cards assertion to expect 2 cards.

  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "error TS" | grep -q "^0$" && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - mockMovementState.ts contains `PLAYER_POOL.filter` and `selectedTeams: { home: 'city', away: 'crew' }`
    - mockMovementState.ts does NOT import or reference `TEAM_SQUADS`
    - GameBoard.test.tsx contains `getByAltText('city badge')` and `getByAltText('crew badge')` and does NOT contain `cosmos badge` or `xolos badge`
    - PlayerStatsPanel.test.tsx line 27 uses `{ home: 'city', away: 'crew' }`
    - No client test file contains a `homePickedTeam="cosmos"` prop
    - `cd packages/client && npx vitest run` exits 0
    - `cd packages/client && npx tsc --noEmit` reports 0 errors
  </acceptance_criteria>
  <done>mockMovementState builds from PLAYER_POOL with city/crew squads; all dependent tests assert city/crew names, badges, and pick behavior; the client test suite is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                                     |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------- |
| server → client (rendered team data) | Server-authoritative selectedTeams/pieces rendered by client; no untrusted client input introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category  | Component                    | Disposition | Mitigation Plan                                                                                                                                                                                      |
| --------- | --------- | ---------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-19-05   | Tampering | Client Record<TeamId,…> maps | accept      | Display-only lookups; TeamId is compile-time narrowed so an out-of-union key cannot be constructed; no runtime validation needed client-side (server allow-list in Plan 19-02 is the trust boundary) |
| T-19-SC   | Tampering | npm/pip/cargo installs       | mitigate    | No packages installed this phase; no install tasks present                                                                                                                                           |

</threat_model>

<verification>
- `cd packages/client && npx tsc --noEmit` reports 0 errors
- `pnpm --filter @counter-attack/client test --run` passes
- `grep -rn "primaryColor\|secondaryColor" packages/client/src` returns nothing
- `pnpm -r test --run` passes across all packages (phase gate)
</verification>

<success_criteria>

- All client primaryColor reads migrated to palette.primary (PALETTE-02)
- TeamId-keyed maps and ALL_TEAMS contain only city/crew; retired teams removed from client surfaces (TEAM-07)
- mockMovementState and its dependent tests run on city/crew squads; full client suite green
  </success_criteria>

<output>
Create `.planning/phases/19-data-model-team-palette/19-03-SUMMARY.md` when done. Record which test assertions changed (old cosmos/xolos value → new city/crew value) and note the TeamSelectionScreen transitional 2-team state for Phase 21 to restore.
</output>
