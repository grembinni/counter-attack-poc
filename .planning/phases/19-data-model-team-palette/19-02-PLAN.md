---
phase: 19-data-model-team-palette
plan: 02
type: execute
wave: 2
depends_on: ['19-01']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/roomHandlers.ts
autonomous: true
requirements: [DATA-02, TEAM-07]

must_haves:
  truths:
    - 'buildSquadPieces resolves squads via getSquadPlayers(teamId) from PLAYER_POOL, not TEAM_SQUADS (DATA-02, D-03)'
    - "Home pieces get teamId 'home' and ids home-0..home-10; away pieces get teamId 'away', ids away-0..away-10, mirrored positions (q=36-q)"
    - "VALID_TEAM_IDS allow-list is ['city', 'crew'] — cosmos/xolos rejected server-side (TEAM-07, ASVS V5)"
    - 'buildInitialGameState produces a 22-piece GameState with striker positioning unchanged'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'buildSquadPieces rewritten to use getSquadPlayers'
      contains: 'getSquadPlayers'
    - path: 'packages/server/src/roomHandlers.ts'
      provides: 'VALID_TEAM_IDS shrunk to active teams'
      contains: "['city', 'crew']"
  key_links:
    - from: 'packages/server/src/gameEngine.ts'
      to: '@counter-attack/shared getSquadPlayers'
      via: 'import + call in buildSquadPieces'
      pattern: "getSquadPlayers\\(selectedTeams"
---

<objective>
Update the server to consume the new PLAYER_POOL data model: rewrite buildSquadPieces to resolve squads through getSquadPlayers instead of the deleted TEAM_SQUADS, and shrink the VALID_TEAM_IDS allow-list to the active teams so a client can no longer pick a retired team.

Purpose: TEAM_SQUADS no longer exists after Plan 19-01, so gameEngine.ts will not compile until buildSquadPieces switches sources. The allow-list must shrink in lockstep with the TeamId union or a `team:pick 'cosmos'` would pass validation and crash on TEAM_CONFIGS lookup.

Output: gameEngine.ts and roomHandlers.ts compile against the new shared exports; server tests pass; the 22-piece build behavior is unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-data-model-team-palette/19-PATTERNS.md
@.planning/phases/19-data-model-team-palette/19-01-SUMMARY.md
</context>

<artifacts_produced>

## Artifacts this phase produces (Plan 19-02)

This plan creates no new exported symbols. It rewires two existing server functions (`buildSquadPieces`, and the `VALID_TEAM_IDS` constant) to consume symbols created by Plan 19-01 (`getSquadPlayers`, `PLAYER_POOL`). The symbols `getSquadPlayers` and `PLAYER_POOL` are declared in Plan 19-01's Artifacts section — exclude from drift verification.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite buildSquadPieces to use getSquadPlayers from PLAYER_POOL</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (import block ~line 29 with TEAM_SQUADS; buildSquadPieces lines 112–136; the ST-positioning logic that must remain unchanged)
    - .planning/phases/19-data-model-team-palette/19-PATTERNS.md (target buildSquadPieces lines 394–415; TypeScript validation note lines 415)
    - .planning/phases/19-data-model-team-palette/19-RESEARCH.md (Pitfall 7 — PoolPlayer vs PlayerPiece spread produces valid PlayerPiece)
    - .planning/phases/19-data-model-team-palette/19-01-SUMMARY.md (confirms PLAYER_POOL shape and getSquadPlayers signature)
  </read_first>
  <action>
    In the shared import block (~line 29), remove `TEAM_SQUADS` and add `getSquadPlayers` (and `PLAYER_POOL` only if referenced elsewhere in the file; otherwise just `getSquadPlayers`).

    Rewrite buildSquadPieces: replace `TEAM_SQUADS[selectedTeams.home].map((p) => ({ ...p, teamId: 'home' as const }))` with `getSquadPlayers(selectedTeams.home).map((p, i) => ({ ...p, teamId: 'home' as const, id: \`home-${i}\` }))`. Replace the away mapping with `getSquadPlayers(selectedTeams.away).map((p, i) => ({ ...p, teamId: 'away' as const, id: \`away-${i}\`, position: { q: 36 - p.position.q, r: p.position.r } }))`. The away branch now derives ids from the index (`away-${i}`) rather than `p.id.replace('home-', 'away-')`, because PoolPlayer ids are now `p###` not `home-#`.

    Leave the ST-positioning block (lines 124–134: homeST/awayST find + kickOffHex assignment) and the return statement exactly as they are. The spread `{ ...poolPlayer, teamId, id }` satisfies PlayerPiece because PoolPlayer carries every stat field and teamId+id are supplied by the override — verify TypeScript accepts it with no cast.

  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "error TS" | grep -q "^0$" && echo SERVER_TYPES_OK</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts import block does NOT contain `TEAM_SQUADS`
    - gameEngine.ts contains `getSquadPlayers(selectedTeams.home)` and `getSquadPlayers(selectedTeams.away)`
    - away mapping sets `id: `away-${i}`` and `position: { q: 36 - p.position.q, r: p.position.r }`
    - `cd packages/server && npx tsc --noEmit` reports 0 errors
  </acceptance_criteria>
  <done>buildSquadPieces resolves both squads via getSquadPlayers with correct home-/away- id assignment and mirror positions; server compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Shrink VALID_TEAM_IDS allow-list to active teams and run server test suite</name>
  <files>packages/server/src/roomHandlers.ts</files>
  <read_first>
    - packages/server/src/roomHandlers.ts (line 40 VALID_TEAM_IDS; confirm it is typed `readonly TeamId[]`)
    - .planning/phases/19-data-model-team-palette/19-RESEARCH.md (Pitfall 4 and Security Domain — VALID_TEAM_IDS must be ['city','crew'])
    - .planning/phases/19-data-model-team-palette/19-CONTEXT.md (D-04)
  </read_first>
  <action>
    Change line 40 from `const VALID_TEAM_IDS: readonly TeamId[] = ['cosmos', 'xolos', 'city', 'crew'] as const;` to `const VALID_TEAM_IDS: readonly TeamId[] = ['city', 'crew'] as const;`. After the shrink of the TeamId union in Plan 19-01, 'cosmos'/'xolos' are no longer assignable to TeamId, so TypeScript enforces this. Do not change any other logic.

    Then run the server test suite to confirm nothing else in the server references the removed exports or retired team ids.

  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "error TS" | grep -q "^0$" && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - roomHandlers.ts contains `['city', 'crew']` as the VALID_TEAM_IDS literal
    - roomHandlers.ts does NOT contain `'cosmos'` or `'xolos'`
    - `cd packages/server && npx tsc --noEmit` reports 0 errors
    - `cd packages/server && npx vitest run` exits 0
  </acceptance_criteria>
  <done>Server allow-list rejects retired teams; server package compiles clean and its full test suite passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                         |
| --------------------------- | ----------------------------------------------------------------------------------- |
| client → server `team:pick` | Untrusted client-supplied teamId string crosses into server game-state construction |

## STRIDE Threat Register

| Threat ID | Category          | Component                           | Disposition | Mitigation Plan                                                                                                                                          |
| --------- | ----------------- | ----------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-19-03   | Tampering         | roomHandlers.ts `team:pick` handler | mitigate    | VALID_TEAM_IDS allow-list narrowed to ['city','crew']; a `team:pick` with a retired or arbitrary id is rejected before any TEAM_CONFIGS lookup (ASVS V5) |
| T-19-04   | Denial of Service | buildSquadPieces getSquadPlayers    | mitigate    | getSquadPlayers throws on unknown player id; only reachable with valid TeamId post allow-list, so a crash-on-lookup path is not client-triggerable       |
| T-19-SC   | Tampering         | npm/pip/cargo installs              | mitigate    | No packages installed this phase; no install tasks present                                                                                               |

</threat_model>

<verification>
- `cd packages/server && npx tsc --noEmit` reports 0 errors
- `pnpm --filter @counter-attack/server test --run` passes
- roomHandlers.ts allow-list is exactly `['city', 'crew']`
</verification>

<success_criteria>

- Server resolves squads from PLAYER_POOL via getSquadPlayers (DATA-02)
- Retired teams rejected by VALID_TEAM_IDS (TEAM-07)
- Server compiles clean and all server tests pass
  </success_criteria>

<output>
Create `.planning/phases/19-data-model-team-palette/19-02-SUMMARY.md` when done. Record any server test assertions that referenced cosmos/xolos and were updated, and confirm the 22-piece build still positions the striker at kickOffHex.
</output>
