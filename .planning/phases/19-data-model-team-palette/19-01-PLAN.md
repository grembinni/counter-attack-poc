---
phase: 19-data-model-team-palette
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/teamConfig.ts
  - packages/shared/src/data/city_players.csv
  - packages/shared/src/data/crew_players.csv
  - packages/shared/src/data/cosmos_players.csv
  - packages/shared/src/data/xolos_players.csv
  - packages/shared/src/data/fa_players.csv
  - packages/shared/src/data/mls.csv
  - packages/shared/src/data/national.csv
  - packages/shared/scripts/seed-rosters.ts
  - packages/shared/src/teams.ts
  - packages/shared/src/teamConfig.test.ts
  - packages/shared/src/teams.test.ts
autonomous: true
requirements: [PALETTE-01, PALETTE-02, PALETTE-03, TEAM-07, DATA-01, DATA-02, DATA-03, LEAGUE-03]

must_haves:
  truths:
    - 'TeamPalette type has exactly 4 fields: primary, primaryLight, secondary1, secondary2 (PALETTE-01, PALETTE-03)'
    - "TeamId union is 'city' | 'crew' only; cosmos and xolos are removed (TEAM-07, D-04)"
    - 'COLOR_SCHEME_REGISTRY has entries for cosmos, xolos, city, crew (DATA-03, D-06)'
    - 'PLAYER_POOL is a single flat readonly array replacing TEAM_SQUADS and FREE_AGENTS (DATA-01, D-12)'
    - 'TEAM_CONFIGS.city.playerIds and TEAM_CONFIGS.crew.playerIds reference valid PLAYER_POOL ids (DATA-02, D-03)'
    - "getSquadPlayers('city') returns 11 PoolPlayer objects resolved from PLAYER_POOL (DATA-02)"
    - "TeamConfig.league is 'mls' for both city and crew (LEAGUE-03, D-14)"
    - "All 7 CSV headers use 'Aerial Ability' (typo fixed) and seed script reads that key (D-13)"
  artifacts:
    - path: 'packages/shared/src/teamConfig.ts'
      provides: 'TeamId, ColorSchemeId, TeamPalette, ColorScheme, TeamConfig types; COLOR_SCHEME_REGISTRY, TEAM_CONFIGS, getSquadPlayers'
      contains: 'export function getSquadPlayers'
    - path: 'packages/shared/src/teams.ts'
      provides: 'PoolPlayer interface + PLAYER_POOL constant (auto-generated)'
      contains: 'export const PLAYER_POOL'
    - path: 'packages/shared/scripts/seed-rosters.ts'
      provides: 'Multi-CSV seed pipeline emitting PLAYER_POOL with sequential p001.. ids'
      contains: 'PLAYER_POOL'
  key_links:
    - from: 'packages/shared/src/teamConfig.ts'
      to: 'packages/shared/src/teams.ts'
      via: 'getSquadPlayers references PLAYER_POOL'
      pattern: 'PLAYER_POOL'
    - from: 'packages/shared/scripts/seed-rosters.ts'
      to: 'packages/shared/src/teams.ts'
      via: 'writeFileSync output'
      pattern: "PLAYER_POOL: readonly PoolPlayer\\[\\]"
---

<objective>
Establish the Phase 19 shared data foundation: the 4-color palette model, the split of TeamId vs ColorSchemeId, the COLOR_SCHEME_REGISTRY, and the unified PLAYER_POOL that replaces TEAM_SQUADS + FREE_AGENTS. Fix the "Arial Ability" CSV typo across all 7 files and extend the seed script to ingest mls.csv + national.csv with sequential p001.. IDs. Regenerate teams.ts and populate TEAM_CONFIGS.playerIds from the seed output.

Purpose: Every downstream consumer (server buildSquadPieces, all 5 client components, mocks, TeamBadge) depends on these types and exports existing with their final shape. This plan is the sole Wave 1 blocker for the entire phase.

Output: Updated teamConfig.ts (types + constants + getSquadPlayers), fixed CSV headers, rewritten seed-rosters.ts, regenerated teams.ts (PLAYER_POOL), and two shared unit tests. TypeScript compiles clean within packages/shared.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-data-model-team-palette/19-CONTEXT.md
@.planning/phases/19-data-model-team-palette/19-PATTERNS.md
</context>

<artifacts_produced>

## Artifacts this phase produces (Plan 19-01)

New symbols created by this plan (exclude from drift verification):

- Types: `ColorSchemeId`, `TeamPalette`, `ColorScheme`, `PoolPlayer`
- Constants: `COLOR_SCHEME_REGISTRY`, `PLAYER_POOL`
- Functions: `getSquadPlayers`
- New `TeamConfig` fields: `colorSchemeId`, `palette`, `playerIds`, `league`
- Seed script helpers: `toSlug` (team-name normalizer), extended `CSV_FILES` list, `PLAYER_POOL` output template

Removed symbols (no longer exist after this plan): `TEAM_SQUADS`, `FREE_AGENTS`, `TeamConfig.primaryColor`, `TeamConfig.secondaryColor`, `TeamId` members `'cosmos'` and `'xolos'`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite teamConfig.ts types, registry, TEAM_CONFIGS, and getSquadPlayers; author shared unit tests</name>
  <files>packages/shared/src/teamConfig.ts, packages/shared/src/teamConfig.test.ts</files>
  <read_first>
    - packages/shared/src/teamConfig.ts (current TeamId union, TeamConfig, TEAM_CONFIGS — the file being replaced)
    - packages/shared/src/teamConfig.test.ts (current assertions — must be updated, not left asserting old shape)
    - packages/shared/src/types.ts (PlayerPiece type — PoolPlayer must share all its stat fields; note PlayerPiece has NO `heading` field)
    - .planning/phases/19-data-model-team-palette/19-PATTERNS.md (target type shapes and COLOR_SCHEME_REGISTRY color values, lines 46–164)
    - .planning/phases/19-data-model-team-palette/19-CONTEXT.md (decisions D-04, D-06, D-08, D-09, D-14 and Claude's Discretion on color values)
  </read_first>
  <behavior>
    - COLOR_SCHEME_REGISTRY has exactly 4 keys: cosmos, xolos, city, crew
    - Each registry entry's palette has all 4 string fields (primary, primaryLight, secondary1, secondary2)
    - TEAM_CONFIGS has exactly 2 keys: city, crew
    - TEAM_CONFIGS.city.league === 'mls' and TEAM_CONFIGS.crew.league === 'mls'
    - TEAM_CONFIGS.city.palette deep-equals COLOR_SCHEME_REGISTRY.city.palette
    - getSquadPlayers('city') returns an array of length 11 (asserted after PLAYER_POOL is regenerated in Task 3 — this test may RED until then; that is the Nyquist signal)
  </behavior>
  <action>
    Replace the type block per D-04/D-06/D-08/D-14: `export type TeamId = 'city' | 'crew'`; add `export type ColorSchemeId = 'cosmos' | 'xolos' | 'city' | 'crew'`; add `TeamPalette { primary; primaryLight; secondary1; secondary2 }` (all string); add `ColorScheme { id: ColorSchemeId; name: string; palette: TeamPalette; badgeFile: string }`; replace `TeamConfig` to `{ id: TeamId; name: string; colorSchemeId: ColorSchemeId; palette: TeamPalette; playerIds: readonly string[]; league: 'mls' | 'international'; badgeFile: string }` — DELETE primaryColor and secondaryColor.

    Author `COLOR_SCHEME_REGISTRY: Record<ColorSchemeId, ColorScheme>` with entries for cosmos, xolos, city, crew. Map each team's existing `primaryColor` → `palette.primary` and existing `secondaryColor` → `palette.secondary1`; author `primaryLight` and `secondary2` per the values in 19-PATTERNS.md lines 87–132 (cosmos primaryLight '#93c5fd' / secondary2 '#1e3a5f'; xolos '#fcd34d' / '#1f2937'; city '#f87171' / '#1e1e2e'; crew '#fde68a' / '#14532d'). Keep badgeFile filename-only per the existing comment convention.

    Author `TEAM_CONFIGS: Record<TeamId, TeamConfig>` with city and crew only; set `colorSchemeId`, `palette: COLOR_SCHEME_REGISTRY.<id>.palette`, `league: 'mls'`, `badgeFile`. Leave `playerIds: []` for now with a comment noting Task 3 populates it from seed output.

    Add `export function getSquadPlayers(teamId: TeamId): PoolPlayer[]` that maps `TEAM_CONFIGS[teamId].playerIds` through `PLAYER_POOL.find(p => p.id === id)`, throwing `Error(\`Player ${id} not found in PLAYER_POOL\`)` on miss. Import `PoolPlayer` and `PLAYER_POOL` from `'./teams.js'`.

    Rewrite teamConfig.test.ts to assert the behaviors listed above (registry has 4 entries, TEAM_CONFIGS has 2, each palette has 4 fields, league values, palette identity, getSquadPlayers length 11).

  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "PLAYER_POOL\|teams.js" | grep -c "error TS" | grep -q "^0$" && echo TYPES_OK</automated>
  </verify>
  <acceptance_criteria>
    - teamConfig.ts contains `export type TeamId = 'city' | 'crew'`
    - teamConfig.ts contains `export type ColorSchemeId = 'cosmos' | 'xolos' | 'city' | 'crew'`
    - teamConfig.ts contains `export interface TeamPalette` with fields primary, primaryLight, secondary1, secondary2
    - teamConfig.ts contains `export function getSquadPlayers`
    - teamConfig.ts does NOT contain the strings `primaryColor` or `secondaryColor`
    - COLOR_SCHEME_REGISTRY object literal contains keys cosmos, xolos, city, crew
    - TEAM_CONFIGS object literal contains only keys city and crew
    - teamConfig.test.ts asserts COLOR_SCHEME_REGISTRY has 4 entries and TEAM_CONFIGS has 2 entries
  </acceptance_criteria>
  <done>teamConfig.ts holds the final Phase 19 type shapes and constants; getSquadPlayers exists; teamConfig.test.ts asserts the new shape. TypeScript errors are limited to the not-yet-regenerated PLAYER_POOL (resolved in Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Fix "Arial Ability" typo in all 7 CSV headers and update the seed script header key</name>
  <files>packages/shared/src/data/city_players.csv, packages/shared/src/data/crew_players.csv, packages/shared/src/data/cosmos_players.csv, packages/shared/src/data/xolos_players.csv, packages/shared/src/data/fa_players.csv, packages/shared/src/data/mls.csv, packages/shared/src/data/national.csv, packages/shared/scripts/seed-rosters.ts</files>
  <read_first>
    - packages/shared/scripts/seed-rosters.ts (line 130 `row[idx['Arial Ability']]` — the key that must change in lockstep with the CSV headers)
    - packages/shared/src/data/city_players.csv (header row — confirm exact column ordering before editing)
    - .planning/phases/19-data-model-team-palette/19-CONTEXT.md (D-13)
  </read_first>
  <action>
    In each of the 7 CSV files, change ONLY the header cell `Arial Ability` to `Aerial Ability` (header row only; do not touch data rows). The header column order is: `Player,Team,Nationality,Position,Pace,Dribbling,Heading,Highpass,Resilience,Shooting,Tackling,Arial Ability,Saving,Handling` → the 12th column label becomes `Aerial Ability`.

    In seed-rosters.ts, change the hardcoded key on line 130 from `row[idx['Arial Ability']]` to `row[idx['Aerial Ability']]`, and update the adjacent D-05 comment to note the typo has been fixed (D-13). Do NOT change any other logic in this task.

  </action>
  <verify>
    <automated>grep -L "Aerial Ability" packages/shared/src/data/city_players.csv packages/shared/src/data/crew_players.csv packages/shared/src/data/cosmos_players.csv packages/shared/src/data/xolos_players.csv packages/shared/src/data/fa_players.csv packages/shared/src/data/mls.csv packages/shared/src/data/national.csv | grep -c . | grep -q "^0$" && grep -q "Aerial Ability" packages/shared/scripts/seed-rosters.ts && ! grep -q "Arial Ability" packages/shared/scripts/seed-rosters.ts && echo TYPO_FIXED</automated>
  </verify>
  <acceptance_criteria>
    - All 7 CSV files contain the string `Aerial Ability` in their header row
    - No CSV file header contains the string `Arial Ability`
    - seed-rosters.ts contains `idx['Aerial Ability']`
    - seed-rosters.ts does NOT contain `idx['Arial Ability']`
  </acceptance_criteria>
  <done>Every CSV header and the seed script key use the corrected spelling "Aerial Ability" atomically; no remaining "Arial Ability" occurrences in CSVs or the script.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite seed-rosters.ts to emit PLAYER_POOL; run seed; populate TEAM_CONFIGS.playerIds; author PLAYER_POOL test</name>
  <files>packages/shared/scripts/seed-rosters.ts, packages/shared/src/teams.ts, packages/shared/src/teamConfig.ts, packages/shared/src/teams.test.ts</files>
  <read_first>
    - packages/shared/scripts/seed-rosters.ts (current main() flow, CSV_FILES list, buildSquadEntries, serializePlayer, TEAM_ID_MAP, output template — the file being extended)
    - packages/shared/src/teams.ts (current TEAM_SQUADS output format and header comment — the output being replaced)
    - packages/shared/src/types.ts (PlayerPiece field set — PoolPlayer must match minus teamId; PlayerPiece has NO `heading` field, so PoolPlayer and serializePlayer must NOT emit `heading`)
    - .planning/phases/19-data-model-team-palette/19-PATTERNS.md (PoolPlayer target shape lines 200–218; seed script diffs lines 259–331; ID sequencing lines 278–331)
    - .planning/phases/19-data-model-team-palette/19-CONTEXT.md (D-01, D-03, D-07, D-11, D-12; free-agent sourceTeamId slug)
    - .planning/phases/19-data-model-team-palette/19-RESEARCH.md (Pitfall 6 — position values; Open Question A4 — free-agent slug)
  </read_first>
  <action>
    Extend CSV_FILES to the 7 files in this exact order: cosmos_players.csv, xolos_players.csv, city_players.csv, crew_players.csv, fa_players.csv, mls.csv, national.csv. Add `function toSlug(name)` = `name.toLowerCase().replace(/\s+/g,'-').replace(/[^a-z0-9-]/g,'')`.

    Determine sourceTeamId per row: for the 4 legacy squad CSVs use their fixed ColorSchemeId ('cosmos','xolos','city','crew'); for fa_players.csv (Team col 'FA') use the literal slug `'free-agent'`; for mls.csv and national.csv use `toSlug(row[idx['Team']])`.

    Assign global sequential IDs `p${String(counter++).padStart(3,'0')}` starting at 1, iterating CSV_FILES in the order above and rows in file order (keep the existing per-squad ROLE_ORDER sort for jersey numbering within each squad, but the global p-counter increments across all players). Keep `number` = jersey number and `position` = FORMATION_POSITIONS slot per role (D-02 placeholder retained).

    Define the `PoolPlayer` interface in the OUTPUT template (written into teams.ts) with fields in this order: `id, sourceTeamId, firstName, lastName, number, nationality, role, position, pace, shooting, tackling, dribbling, saving, handling, resilience, aerialAbility, highPass`. Import `ColorSchemeId` and `HexCoord`; type `sourceTeamId` as `ColorSchemeId | string` (wide, per A4 — tightened in Phase 21). Update `serializePlayer` to emit these fields and to DROP `teamId` and DROP `heading`. Replace the output template's exports and header comment: single `export const PLAYER_POOL: readonly PoolPlayer[] = [ ... ]`; NO TEAM_SQUADS, NO FREE_AGENTS. Update the RawPlayer/PlayerEntry interfaces and buildSquadEntries as needed to carry sourceTeamId and drop heading from output.

    Run `pnpm --filter @counter-attack/shared run seed:rosters`. Read the regenerated teams.ts. Extract the ordered `id` values whose `sourceTeamId === 'city'` (11 ids) and `=== 'crew'` (11 ids). Paste those id arrays into `TEAM_CONFIGS.city.playerIds` and `TEAM_CONFIGS.crew.playerIds` in teamConfig.ts.

    Author teams.test.ts asserting: PLAYER_POOL length equals total player count (44 legacy squads + free agents + 44 mls + 66 national — assert the actual counted length, not a guessed number); all ids unique; ids match /^p\d{3}$/; getSquadPlayers('city') and getSquadPlayers('crew') each return 11; every PoolPlayer has a defined aerialAbility (>= 0) confirming the header-key fix produced non-zero values for outfielders.

  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "error TS" | grep -q "^0$" && npx vitest run src/teams.test.ts src/teamConfig.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - teams.ts contains `export const PLAYER_POOL: readonly PoolPlayer[]`
    - teams.ts does NOT contain `TEAM_SQUADS` or `FREE_AGENTS`
    - teams.ts PoolPlayer entries do NOT contain a `heading:` field
    - teams.ts contains at least one entry with `sourceTeamId: 'free-agent'`
    - teamConfig.ts TEAM_CONFIGS.city.playerIds and crew.playerIds are non-empty arrays of 11 `p###` ids each
    - `npx vitest run src/teams.test.ts` exits 0
    - `cd packages/shared && npx tsc --noEmit` reports 0 errors
  </acceptance_criteria>
  <done>PLAYER_POOL is generated and committed; TEAM_CONFIGS.playerIds is populated with real seeded ids; shared package compiles clean and both shared data tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                         |
| ----------------------- | --------------------------------------------------------------------------------------------------- |
| CSV files → seed script | Developer-authored static input; only read at dev time by `pnpm run seed:rosters`, never at runtime |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                                                                        |
| --------- | ---------------------- | --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------ |
| T-19-01   | Tampering              | seed-rosters.ts CSV parsing | accept      | Dev-only build tool; CSV input is committed to the repo and reviewed; no runtime or network exposure   |
| T-19-02   | Information Disclosure | PLAYER_POOL contents        | accept      | Player stat data is non-sensitive game content; no PII beyond public roster names                      |
| T-19-SC   | Tampering              | npm/pip/cargo installs      | mitigate    | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: N/A); no install tasks present |

</threat_model>

<verification>
- `cd packages/shared && npx tsc --noEmit` reports 0 errors (barrel still exports teams.js; PLAYER_POOL replaces TEAM_SQUADS)
- `pnpm --filter @counter-attack/shared test --run` passes (teams.test.ts + teamConfig.test.ts + all existing shared tests)
- `grep -r "Arial Ability" packages/shared` returns nothing
</verification>

<success_criteria>

- TeamConfig has exactly 4 palette values + league field; primaryColor/secondaryColor gone from teamConfig.ts (PALETTE-01/02/03, LEAGUE-03)
- TeamId = 'city' | 'crew'; COLOR_SCHEME_REGISTRY retains cosmos/xolos/city/crew (TEAM-07, DATA-03)
- PLAYER_POOL replaces TEAM_SQUADS + FREE_AGENTS; TEAM_CONFIGS references player ids; getSquadPlayers resolves them (DATA-01, DATA-02)
- Shared package TypeScript compiles clean and shared tests pass
  </success_criteria>

<output>
Create `.planning/phases/19-data-model-team-palette/19-01-SUMMARY.md` when done. Record: final PLAYER_POOL length, the city/crew player-id ranges, the player names at home-9 / away-N slots for city and crew squads (so Plan 19-03 can update ActionLog test assertions without re-deriving), and any color values authored.
</output>
