---
phase: 24-auto-assignment-lineup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/events.ts
autonomous: true
requirements: [ASSIGN-02, ASSIGN-03, ASSIGN-05]
must_haves:
  truths:
    - 'Server and client can reference LINEUP_SWAP and LINEUP_CONFIRM as typed client-to-server events'
    - 'Server and client can reference LINEUP_ASSIGNMENT_READY and LINEUP_ASSIGNMENT_UPDATED as typed server-to-client events'
    - 'TypeScript compiles clean across all three packages with the new event contracts'
  artifacts:
    - path: 'packages/shared/src/events.ts'
      provides: 'Four Phase 24 lineup event names + typed payload signatures'
      contains: 'LINEUP_SWAP'
  key_links:
    - from: 'packages/shared/src/events.ts'
      to: 'packages/shared/src/index.ts barrel'
      via: "export * from './events.js'"
      pattern: 'LINEUP_(SWAP|CONFIRM|ASSIGNMENT_READY|ASSIGNMENT_UPDATED)'
---

<objective>
Add the four Phase 24 lineup socket event contracts to the shared events module: two
client-to-server events (`LINEUP_SWAP`, `LINEUP_CONFIRM`) and two server-to-client events
(`LINEUP_ASSIGNMENT_READY`, `LINEUP_ASSIGNMENT_UPDATED`), each with a typed payload
signature. This is the interface-first foundation both the server handlers (Plan 03) and the
client screen (Plan 04) consume.

Purpose: Establish the typed event surface before any producer or consumer is written, so no
downstream plan explores the codebase to learn payload shapes.
Output: Extended `ClientEvents` / `ServerEvents` const objects and their
`ClientToServerEvents` / `ServerToClientEvents` typed interfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-auto-assignment-lineup/24-CONTEXT.md
@.planning/phases/24-auto-assignment-lineup/24-RESEARCH.md
@.planning/phases/24-auto-assignment-lineup/24-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (new — exclude from drift verification):

- `ClientEvents.LINEUP_SWAP` = `'lineup:swap'`
- `ClientEvents.LINEUP_CONFIRM` = `'lineup:confirm'`
- `ServerEvents.LINEUP_ASSIGNMENT_READY` = `'lineup:assignment-ready'`
- `ServerEvents.LINEUP_ASSIGNMENT_UPDATED` = `'lineup:assignment-updated'`
- `ClientToServerEvents[ClientEvents.LINEUP_SWAP]` signature `(payload: { slotIndexA: number; slotIndexB: number }) => void`
- `ClientToServerEvents[ClientEvents.LINEUP_CONFIRM]` signature `(payload: { confirmedOrder: string[] }) => void`
- `ServerToClientEvents[ServerEvents.LINEUP_ASSIGNMENT_READY]` signature `(assignment: string[]) => void`
- `ServerToClientEvents[ServerEvents.LINEUP_ASSIGNMENT_UPDATED]` signature `(assignment: string[]) => void`
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add four lineup events + typed payload signatures to events.ts</name>
  <files>packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/events.ts (the file being modified — study the existing const-object and typed-interface patterns, e.g. UNIFORM_CONFIRM and BOTH_FORMATIONS_CONFIRMED)
    - packages/shared/src/index.ts (confirm `export * from './events.js'` re-exports the new symbols with no extra work)
    - .planning/phases/24-auto-assignment-lineup/24-RESEARCH.md (Pattern 3: exact event names and payload shapes)
    - .planning/phases/24-auto-assignment-lineup/24-PATTERNS.md (events.ts analog — const object + typed interface extension pattern)
  </read_first>
  <action>
    Extend the `ClientEvents` const object with `LINEUP_SWAP: 'lineup:swap'` and
    `LINEUP_CONFIRM: 'lineup:confirm'`. Extend the `ServerEvents` const object with
    `LINEUP_ASSIGNMENT_READY: 'lineup:assignment-ready'` and
    `LINEUP_ASSIGNMENT_UPDATED: 'lineup:assignment-updated'`. Add matching entries to the
    typed interfaces: in `ClientToServerEvents`, `[ClientEvents.LINEUP_SWAP]` takes
    `(payload: { slotIndexA: number; slotIndexB: number })` and `[ClientEvents.LINEUP_CONFIRM]`
    takes `(payload: { confirmedOrder: string[] })`; in `ServerToClientEvents`,
    `[ServerEvents.LINEUP_ASSIGNMENT_READY]` and `[ServerEvents.LINEUP_ASSIGNMENT_UPDATED]`
    each take `(assignment: string[])`. The `assignment: string[]` is a `PlayerId[]` of 11
    entries where index i maps to `FORMATIONS[formationId].slots[i]` (per D-06). Give each new
    entry a JSDoc comment matching the existing style, referencing Phase 24 and the decision ID
    (D-08 for swap, D-10 for confirm, D-07 for assignment-ready, D-12 for assignment-updated).
    Do NOT add any new `GameState` fields — per RESEARCH.md, Phase 24 assignment is pre-game
    room state, not game state.
  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/events.ts` contains `LINEUP_SWAP: 'lineup:swap'` and `LINEUP_CONFIRM: 'lineup:confirm'` in the `ClientEvents` object
    - `packages/shared/src/events.ts` contains `LINEUP_ASSIGNMENT_READY: 'lineup:assignment-ready'` and `LINEUP_ASSIGNMENT_UPDATED: 'lineup:assignment-updated'` in the `ServerEvents` object
    - `ClientToServerEvents` declares `[ClientEvents.LINEUP_SWAP]` with a `{ slotIndexA: number; slotIndexB: number }` payload and `[ClientEvents.LINEUP_CONFIRM]` with a `{ confirmedOrder: string[] }` payload
    - `ServerToClientEvents` declares `[ServerEvents.LINEUP_ASSIGNMENT_READY]` and `[ServerEvents.LINEUP_ASSIGNMENT_UPDATED]`, each `(assignment: string[]) => void`
    - `npx tsc --noEmit` in packages/shared exits 0
  </acceptance_criteria>
  <done>All four events and their typed signatures exist; shared package type-checks clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                                                                          |
| ------------- | ---------------------------------------------------------------------------------------------------- |
| client→server | Lineup swap/confirm payloads cross from untrusted client into server handlers (validated in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category  | Component              | Disposition | Mitigation Plan                                                                                                                                                       |
| --------- | --------- | ---------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-24-05   | Tampering | Event payload typing   | mitigate    | Typed payload signatures constrain the compile-time contract; runtime validation of `slotIndexA/B` and `confirmedOrder` is enforced in the Plan 03 handlers, not here |
| T-24-SC   | Tampering | npm/pip/cargo installs | accept      | No packages installed this plan (contract-only edit to existing file); RESEARCH Package Legitimacy Audit confirms zero new dependencies                               |

</threat_model>

<verification>
- `packages/shared` type-checks clean (`npx tsc --noEmit` exits 0)
- Barrel re-export confirmed: importing `ClientEvents.LINEUP_SWAP` from `@counter-attack/shared` resolves
</verification>

<success_criteria>
Four new lineup events with typed payload signatures exist in `events.ts`, re-exported through
the shared barrel, and the shared package compiles without error.
</success_criteria>

<output>
Create `.planning/phases/24-auto-assignment-lineup/24-01-SUMMARY.md` when done.
</output>
