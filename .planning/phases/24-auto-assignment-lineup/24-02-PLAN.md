---
phase: 24-auto-assignment-lineup
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.phase24.test.ts
autonomous: true
requirements: [ASSIGN-01]
must_haves:
  truths:
    - 'computeAutoAssignment places the GK player in slot index 0 before any scoring'
    - 'Anchor slot roles (DEF-center, MID-central, FWD-central) are filled before flex roles from remaining players'
    - 'Ties are broken deterministically by lower source-team index (no randomness)'
    - 'buildInitialGameState can position pieces from an explicit confirmed player ordering while defaulting to getSquadPlayers when none is supplied'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'computeAutoAssignment + scoreForRole pure functions; confirmed-order params on buildSquadPieces/buildInitialGameState'
      contains: 'export function computeAutoAssignment'
    - path: 'packages/server/src/__tests__/gameEngine.phase24.test.ts'
      provides: 'Unit coverage for ASSIGN-01 (priority order, scoring, tie-break)'
      contains: 'computeAutoAssignment'
  key_links:
    - from: 'packages/server/src/gameEngine.ts computeAutoAssignment'
      to: '@counter-attack/shared FormationSlot / SlotRole'
      via: 'slots parameter drives scoreForRole per D-04'
      pattern: 'scoreForRole'
---

<objective>
Implement the deterministic auto-assignment algorithm (ASSIGN-01) as pure, unit-tested
functions in `gameEngine.ts`, and extend `buildSquadPieces` / `buildInitialGameState` to accept
an explicit confirmed player ordering so Plan 03 can position pieces from the player-chosen
lineup. TDD: failing unit tests first, then implementation, then the signature threading.

Purpose: The scoring algorithm is the highest-complexity new logic in this phase; verifying it
in isolation before any Socket.io wiring is the lowest-risk sequencing.
Output: Exported `computeAutoAssignment` + `scoreForRole` functions; optional
`confirmedHomeOrder` / `confirmedAwayOrder` parameters threaded through
`buildSquadPieces` and `buildInitialGameState`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-auto-assignment-lineup/24-CONTEXT.md
@.planning/phases/24-auto-assignment-lineup/24-RESEARCH.md
@.planning/phases/24-auto-assignment-lineup/24-PATTERNS.md
@.planning/phases/24-auto-assignment-lineup/24-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (new — exclude from drift verification):

- `export function computeAutoAssignment(squad: PoolPlayer[], slots: readonly FormationSlot[]): PoolPlayer[]` in gameEngine.ts
- `function scoreForRole(player: PoolPlayer, slotRole: SlotRole): number` (exported for unit tests) in gameEngine.ts
- New optional params `confirmedHomeOrder?: PoolPlayer[]`, `confirmedAwayOrder?: PoolPlayer[]` on `buildSquadPieces`
- New optional params `confirmedHomeOrder?: PoolPlayer[]`, `confirmedAwayOrder?: PoolPlayer[]` on `buildInitialGameState`
- New test file `packages/server/src/__tests__/gameEngine.phase24.test.ts`
- `ANCHOR_ROLES` module-level const `['DEF-center','MID-central','FWD-central']` in gameEngine.ts
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing unit tests for computeAutoAssignment and scoreForRole</name>
  <files>packages/server/src/__tests__/gameEngine.phase24.test.ts</files>
  <read_first>
    - packages/server/src/__tests__ (read one existing unit test file, e.g. an existing gameEngine test, for the Vitest describe/it/expect style and import conventions)
    - packages/shared/src/formations.ts (FORMATIONS registry, FormationSlot, SlotRole; slot index 0 is always GK; jerseyNumber and slotRole per slot)
    - packages/shared/src/teams.ts (PoolPlayer interface — role union is 'GK'|'DEF'|'MID'|'FWD'|'ST'; stat fields pace/shooting/tackling/dribbling/aerialAbility/highPass)
    - packages/shared/src/teamConfig.ts (getSquadPlayers — returns PoolPlayer[] in source-team order)
    - .planning/phases/24-auto-assignment-lineup/24-RESEARCH.md (Pattern 1 reference implementation; D-04 scoring formulas)
    - .planning/phases/24-auto-assignment-lineup/24-VALIDATION.md (tasks 24-01-01/02/03 test map)
  </read_first>
  <behavior>
    - Test: `computeAutoAssignment(getSquadPlayers('city'), FORMATIONS['4-4-2'].slots)` returns an 11-element array whose element at index 0 has `role === 'GK'` (GK locked to slot 0).
    - Test: for every formation ('4-4-2','5-3-2','4-3-3','3-4-3'), the result length equals the slots length and contains each squad player exactly once (no duplicates, no nulls).
    - Test: anchor roles are filled before flex — construct a small synthetic squad where one player has the top score for both an anchor and a flex slot; assert the top player lands in the anchor slot, not the flex slot.
    - Test: tie-break — two synthetic players with identical stats/roles produce identical scores; assert the one earlier in the input array is chosen for the contested slot.
    - Test: `scoreForRole(player, 'FWD-central')` equals `shooting + aerialAbility + (2 if role==='FWD') + (4 if role==='ST')` for representative players.
    - Test: `scoreForRole(player, 'MID-central')` equals `dribbling + tackling + pace + shooting + (3 if role==='MID')`.
    - Test: `scoreForRole(player, 'DEF-back')` equals `tackling + pace + (2 if role==='DEF')`.
    - Test: `scoreForRole(player, 'FWD-wing')` equals `dribbling + highPass + (3 if role==='FWD') + (2 if role==='MID')` — role==='ST' receives NO wing bonus (literal D-04; the +4 FWD-central bonus is what pulls strikers central).
  </behavior>
  <action>
    Create `packages/server/src/__tests__/gameEngine.phase24.test.ts` importing
    `computeAutoAssignment` and `scoreForRole` from `../gameEngine.js` and `FORMATIONS`,
    `getSquadPlayers` from `@counter-attack/shared`. Write the tests described in `<behavior>`
    using Vitest. Encode the exact D-04 numeric formulas as assertions (compute expected values
    inline from the player's stat fields, not by re-calling scoreForRole). For synthetic-squad
    tests, build minimal `PoolPlayer` objects (all required fields present) so anchor-before-flex
    and tie-break behavior is unambiguous. The tests MUST fail now because the functions do not
    yet exist — this is the RED state.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server run test -- --run src/__tests__/gameEngine.phase24.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/server/src/__tests__/gameEngine.phase24.test.ts` exists and imports `computeAutoAssignment` and `scoreForRole` from `../gameEngine.js`
    - Running the test command reports failures (RED) — either compile failure on the missing import or failing assertions
    - Test cases exist for: GK-in-slot-0, no-duplicates/full-coverage per formation, anchor-before-flex, tie-break-by-index, and scoreForRole numeric values for FWD-central, MID-central, DEF-back, and FWD-wing
  </acceptance_criteria>
  <done>Test file present with the eight behavior cases; suite is RED pending implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement computeAutoAssignment + scoreForRole</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (the file being modified — study existing pure-function style, e.g. buildSquadPieces; the import block from @counter-attack/shared at the top)
    - packages/shared/src/formations.ts (SlotRole union values; slot index 0 = GK)
    - packages/shared/src/teams.ts (PoolPlayer fields and role union)
    - .planning/phases/24-auto-assignment-lineup/24-RESEARCH.md (Pattern 1 full reference implementation and D-04 formulas)
  </read_first>
  <action>
    Add `computeAutoAssignment(squad: PoolPlayer[], slots: readonly FormationSlot[]): PoolPlayer[]`
    as an exported pure function using the three-pass greedy strategy: Pass 1 locks the
    `role === 'GK'` player into result index 0 (D-01); Pass 2 fills anchor slots whose slotRole is
    in `ANCHOR_ROLES = ['DEF-center','MID-central','FWD-central']` from remaining players (D-02);
    Pass 3 fills all remaining (flex) slots (D-03). Track each candidate's original squad index and
    break ties by lower original index (D-05) — never `Math.random()`. Add a `pickBest` helper that
    scores all available candidates for a slotRole and sorts by score desc then origIdx asc, and a
    `scoreForRole(player, slotRole)` helper implementing the D-04 formulas EXACTLY as literal text:
    FWD-central = shooting + aerialAbility + (2 if role==='FWD') + (4 if role==='ST');
    FWD-wing = dribbling + highPass + (3 if role==='FWD') + (2 if role==='MID');
    DEF-center = tackling + aerialAbility + (2 if role==='DEF');
    DEF-back = tackling + pace + (2 if role==='DEF');
    MID-central = dribbling + tackling + pace + shooting + (3 if role==='MID');
    MID-wing = dribbling + highPass + (3 if role==='FWD') + (2 if role==='MID');
    GK = 0. Note the resolved interpretation of RESEARCH assumption A1: role==='ST' does NOT receive
    the wing +3 bonus (literal D-04 says `role=FWD`); strikers are pulled central by the FWD-central
    +4 bonus instead. Import `FormationSlot`, `SlotRole`, `PoolPlayer` types from `@counter-attack/shared`
    if not already imported. Export `scoreForRole` so the unit tests can call it directly.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server run test -- --run src/__tests__/gameEngine.phase24.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/gameEngine.ts` contains `export function computeAutoAssignment(`
    - `scoreForRole` is exported and returns the exact D-04 numeric values (verified by the Task 1 tests)
    - `ANCHOR_ROLES` includes exactly `'DEF-center'`, `'MID-central'`, `'FWD-central'`
    - `src/__tests__/gameEngine.phase24.test.ts` passes (GREEN)
    - No `Math.random` or `crypto` call inside `computeAutoAssignment` / `pickBest` / `scoreForRole`
  </acceptance_criteria>
  <done>All Task 1 unit tests pass; algorithm is deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Thread confirmed-order params through buildSquadPieces and buildInitialGameState</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (buildSquadPieces lines ~115-181 and buildInitialGameState lines ~183+; also buildKickOffPieces which wraps buildSquadPieces)
    - .planning/phases/24-auto-assignment-lineup/24-RESEARCH.md (Pattern 4 signature change; Pitfall 8 — buildKickOffPieces must NOT receive confirmed order)
  </read_first>
  <action>
    Add optional trailing parameters `confirmedHomeOrder?: PoolPlayer[]` and
    `confirmedAwayOrder?: PoolPlayer[]` to `buildSquadPieces`. In the home mapping use
    `(confirmedHomeOrder ?? getSquadPlayers(selectedTeams.home))`; in the away mapping use
    `(confirmedAwayOrder ?? getSquadPlayers(selectedTeams.away))`. The mapped array element at
    index i still takes its `position` and `number` from `slots[i]` exactly as today — only the
    player source list changes. Add the same two optional parameters to `buildInitialGameState`
    and pass them through to its `buildSquadPieces(attackingTeam, selectedTeams, selectedFormation,
    confirmedHomeOrder, confirmedAwayOrder)` call. Do NOT add these parameters to
    `buildKickOffPieces` and do NOT forward them from any post-goal reset path — post-goal resets
    intentionally use default `getSquadPlayers` ordering (Pitfall 8 / assumption A2). Add one unit
    test in gameEngine.phase24.test.ts asserting that passing a reversed confirmed order to
    `buildInitialGameState` produces `home-*` pieces whose player identities follow the supplied
    order (position/number still from the formation slot).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `buildSquadPieces` signature includes `confirmedHomeOrder?: PoolPlayer[]` and `confirmedAwayOrder?: PoolPlayer[]`
    - `buildSquadPieces` uses `confirmedHomeOrder ?? getSquadPlayers(...)` (and away equivalent) for the player source
    - `buildInitialGameState` accepts and forwards both optional params to `buildSquadPieces`
    - `buildKickOffPieces` does NOT declare or pass `confirmedHomeOrder`/`confirmedAwayOrder`
    - A new test confirms confirmed-order threading; full server suite (`pnpm --filter @counter-attack/server run test -- --run`) is green (506+ passing)
  </acceptance_criteria>
  <done>Confirmed order can drive initial piece identities; existing behavior and full suite unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                                       |
| --------------- | ------------------------------------------------------------------------------------------------- |
| server-internal | computeAutoAssignment runs server-only on trusted squad data; no client input reaches it directly |

## STRIDE Threat Register

| Threat ID | Category    | Component                     | Disposition | Mitigation Plan                                                                                                                     |
| --------- | ----------- | ----------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-24-06   | Tampering   | Non-determinism in assignment | mitigate    | Algorithm forbids `Math.random`; tie-break uses source-team index; identical squad always yields identical assignment (unit-tested) |
| T-24-07   | Repudiation | Server-only computation       | accept      | Assignment is computed from trusted PLAYER_POOL/getSquadPlayers, not client-supplied data; low risk                                 |
| T-24-SC   | Tampering   | npm/pip/cargo installs        | accept      | No packages installed this plan; RESEARCH Package Legitimacy Audit confirms zero new dependencies                                   |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server run test -- --run src/__tests__/gameEngine.phase24.test.ts` green
- Full server suite `pnpm --filter @counter-attack/server run test -- --run` green (506+ tests)
</verification>

<success_criteria>
`computeAutoAssignment` and `scoreForRole` are exported, deterministic, and unit-tested for
priority order, scoring, and tie-break; `buildSquadPieces`/`buildInitialGameState` can position
pieces from a confirmed ordering while defaulting to `getSquadPlayers`; full server suite green.
</success_criteria>

<output>
Create `.planning/phases/24-auto-assignment-lineup/24-02-SUMMARY.md` when done.
</output>
