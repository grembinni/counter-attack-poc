---
phase: 24-auto-assignment-lineup
plan: 04
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - packages/client/src/components/PlayerStatsPanel.tsx
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/components/LineupAssignmentScreen.tsx
  - packages/client/src/components/LineupAssignmentScreen.module.css
  - packages/client/src/App.tsx
autonomous: false
requirements: [ASSIGN-02, ASSIGN-03, ASSIGN-04]
must_haves:
  truths:
    - 'After both formations confirm, each player sees a lineup screen with all 11 slots mapped to player names in GK|DEF|MID|FWD columns; GK is locked and never empty'
    - 'A player can drag one outfield card onto another to request a swap; the GK card is not draggable'
    - "Cards re-render from the server's LINEUP_ASSIGNMENT_UPDATED payload (server-authoritative), and a rejected swap shows an auto-clearing message"
    - 'Confirming the lineup emits LINEUP_CONFIRM and shows the waiting-for-opponent state'
  artifacts:
    - path: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      provides: 'Lineup display + drag-to-swap + confirm UI'
      contains: 'LineupAssignmentScreen'
    - path: 'packages/client/src/components/LineupAssignmentScreen.module.css'
      provides: 'Horizontal formation-column + stat-card styling'
      contains: 'formationColumns'
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: "'LINEUP_ASSIGNMENT' Screen union member"
      contains: 'LINEUP_ASSIGNMENT'
  key_links:
    - from: 'packages/client/src/App.tsx'
      to: 'LineupAssignmentScreen'
      via: "screen === 'LINEUP_ASSIGNMENT' render branch"
      pattern: 'LineupAssignmentScreen'
    - from: 'LineupAssignmentScreen onDrop'
      to: 'server LINEUP_SWAP'
      via: 'onSwap prop -> socket.emit(ClientEvents.LINEUP_SWAP)'
      pattern: 'onSwap'
---

<objective>
Build the client lineup experience: a new `LineupAssignmentScreen` that renders the server's
assignment as a horizontal GK|DEF|MID|FWD formation grid of full stat cards, supports HTML5
drag-to-swap on outfield cards (GK locked), reflects server-authoritative updates and rejections,
and confirms via `LINEUP_CONFIRM`. Wire it into `App.tsx` and the Zustand `Screen` union, and
export `STAT_LABELS` for reuse.

Purpose: Deliver the player-facing half of ASSIGN-02/03/04 per the locked UI-SPEC contract.
Output: New component + CSS module; `STAT_LABELS` export; `'LINEUP_ASSIGNMENT'` screen; App.tsx
socket handlers, local state, emit helpers, and render branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-auto-assignment-lineup/24-CONTEXT.md
@.planning/phases/24-auto-assignment-lineup/24-RESEARCH.md
@.planning/phases/24-auto-assignment-lineup/24-PATTERNS.md
@.planning/phases/24-auto-assignment-lineup/24-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (new — exclude from drift verification):

- `export const STAT_LABELS` (add `export` to existing const in PlayerStatsPanel.tsx)
- `'LINEUP_ASSIGNMENT'` member added to the `Screen` union in useGameStore.ts
- New component `LineupAssignmentScreen` (packages/client/src/components/LineupAssignmentScreen.tsx)
- Inline sub-component `LineupStatCard` (in LineupAssignmentScreen.tsx)
- New stylesheet `LineupAssignmentScreen.module.css`
- App.tsx local state: `lineupAssignment`, `lineupConfirmed`, `myFormationId`
- App.tsx handlers: `onLineupAssignmentReady`, `onLineupAssignmentUpdated`, `handleLineupSwap`, `handleLineupConfirm`
  Consumes (already exist — do NOT flag): `LINEUP_*` events (Plan 01); `FORMATIONS`, `PLAYER_POOL`, `PoolPlayer`, `FormationSlot` (shared).
  Removed by this plan: `formationsLocked` / `setFormationsLocked` state and the
  `screen === 'UNIFORM_SELECTION' && formationsLocked` placeholder render branch in App.tsx.
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Export STAT_LABELS, add Screen member, build LineupAssignmentScreen + CSS</name>
  <files>packages/client/src/components/PlayerStatsPanel.tsx, packages/client/src/store/useGameStore.ts, packages/client/src/components/LineupAssignmentScreen.tsx, packages/client/src/components/LineupAssignmentScreen.module.css</files>
  <read_first>
    - packages/client/src/components/PlayerStatsPanel.tsx (STAT_LABELS at line 11; MiniTokenBadge SVG pattern to reproduce inline for a static PoolPlayer)
    - packages/client/src/components/UniformSelectionScreen.tsx (heading/status/confirm messaging and class usage to mirror)
    - packages/client/src/components/UniformSelectionScreen.module.css (.screen, .matchSetupHeading, .statusActive, .statusWaiting, .confirmButtonGreen — copy verbatim into the new module)
    - packages/client/src/store/useGameStore.ts (Screen union lines 23-31; playerSlot field/selector)
    - packages/shared/src/formations.ts (FORMATIONS, FormationSlot, slotRole values, jerseyNumber; slot 0 is GK)
    - packages/shared/src/teams.ts (PoolPlayer fields for card content)
    - .planning/phases/24-auto-assignment-lineup/24-UI-SPEC.md (full visual + interaction contract — exact CSS values, spacing tokens, copy strings, drag states)
    - .planning/phases/24-auto-assignment-lineup/24-PATTERNS.md (LineupAssignmentScreen analog + drag-and-drop card pattern)
  </read_first>
  <action>
    (a) In PlayerStatsPanel.tsx add the `export` keyword to `const STAT_LABELS` (one-word change;
    no other edits). (b) In useGameStore.ts insert `'LINEUP_ASSIGNMENT'` into the `Screen` union
    between `'UNIFORM_SELECTION'` and `'GAME_BOARD'` with a Phase 24 comment. (c) Create
    `LineupAssignmentScreen.tsx` with `Props = { assignment: string[]; formationId: FormationId;
    playerSlot: 1 | 2; onSwap: (a: number, b: number) => void; onConfirm: (order: string[]) => void;
    lineupConfirmed: boolean }`. Resolve `assignment[i]` to a `PoolPlayer` via a
    `Map<string, PoolPlayer>` built once from `PLAYER_POOL`. Read slot metadata from
    `FORMATIONS[formationId].slots[i]`. Group the 11 slots into four columns by `slotRole` prefix:
    GK (slotRole 'GK'), DEF ('DEF-*'), MID ('MID-*'), FWD ('FWD-*'), preserving slot index order
    within each column. Render heading `MATCH SETUP: STEP 3 — {HOME|VISITOR} PLAYER ({YOU|OPPONENT})`
    (HOME for playerSlot 1, VISITOR for 2; always YOU since only own lineup is shown per D-16) and
    the status line using the EXACT strings `Make your selections now!` (when `!lineupConfirmed`) and
    `` `Waiting for ${waitingForLabel} Player to Lock in their Selection.` `` (when confirmed).
    Implement an inline `LineupStatCard` receiving `{ player, slotMeta, isDraggable, isDragSource,
    isDropTarget, isConfirmed, slotIndex }`; display first name, last name, source `player.role`, and
    `#${slotMeta.jerseyNumber}` (NOT `player.number` — Pitfall 5), an inline 20x20 mini token, and all
    9 stats via imported `STAT_LABELS`. Wire HTML5 drag-and-drop per card: `draggable={slotIndex !== 0
    && !lineupConfirmed}`; `onDragStart` sets source index + `dataTransfer`; `onDragOver` calls
    `e.preventDefault()` ONLY when `slotIndex !== 0` (GK is never a drop target — Pitfall 3); `onDrop`
    reads the source index and calls `onSwap(source, slotIndex)` when both are outfield and differ;
    `onDragLeave`/`onDragEnd` clear local drag state. Keep `dragSourceIndex`/`dropTargetIndex` as
    `useState` local to the component (never in Zustand — Pitfall 7). Render a BENCH section below the
    columns with 5 empty `.benchSlot` placeholders, each with `data-bench-index` and an
    `// v1.4: bench drop target` comment on stubbed onDragOver/onDrop (no behavior in v1.3). Render a
    `Confirm Lineup` green button when `!lineupConfirmed` that calls `onConfirm(assignment)`. Render a
    swap-rejection `<p>` (copy: `Swap rejected — GK cannot be moved.`) driven by a local
    `rejectionMessage` state that a parent-provided signal or a GAME_ERROR listener can set and that
    auto-clears via `setTimeout(..., 2000)`. (d) Create `LineupAssignmentScreen.module.css` copying
    `.screen`, `.matchSetupHeading`, `.statusActive`, `.statusWaiting`, `.confirmButtonGreen` verbatim
    from UniformSelectionScreen.module.css, and adding `.formationColumns`, `.column`, `.columnHeader`,
    `.statCard`, `.statCardLocked`, `.statCardDragging`, `.statCardDropTarget`, `.statCardConfirmed`,
    `.cardHeader`, `.cardHeaderText`, `.lockedBadge`, `.statGrid`, `.statLabel`, `.statValue`,
    `.benchSection`, `.benchLabel`, `.benchPlaceholders`, `.benchSlot`, `.swapRejection` using the
    exact values in 24-UI-SPEC.md (900px grid max-width, #16213e card bg, #0f3460 borders, #f5c518
    stat value + drop-target accent, 12px card-inner padding, 4px stat-row gap). GK card always uses
    `.statCardLocked` with a `LOCKED` badge regardless of confirm state (D-09/D-24).
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `PlayerStatsPanel.tsx` line declares `export const STAT_LABELS`
    - `useGameStore.ts` Screen union contains `'LINEUP_ASSIGNMENT'`
    - `LineupAssignmentScreen.tsx` exists, exports `LineupAssignmentScreen`, imports `STAT_LABELS` from `./PlayerStatsPanel.js`, and resolves players via a `Map` over `PLAYER_POOL`
    - Cards display `slotMeta.jerseyNumber` (prefixed `#`), not `player.number`; GK column card renders with a `LOCKED` badge and is not draggable
    - `onDragOver` calls `e.preventDefault()` only for non-GK cards; `onDrop` calls `onSwap` with source and target slot indices
    - `LineupAssignmentScreen.module.css` defines `.formationColumns` (grid, 4 columns, max-width 900px) plus the stat-card and bench classes with the 24-UI-SPEC.md values
    - `npx tsc --noEmit` in packages/client exits 0
  </acceptance_criteria>
  <done>Screen renders the 4-column lineup with draggable outfield cards, locked GK, bench row, and confirm/rejection UI; client type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx — handlers, local state, own-formation tracking, screen route, emit helpers</name>
  <files>packages/client/src/App.tsx</files>
  <read_first>
    - packages/client/src/App.tsx (the file being modified — socket handler registration/cleanup block lines ~38-143; local-state pattern lines ~28-36; render switch lines ~167-195; handleUniformConfirm)
    - packages/client/src/store/useGameStore.ts (playerSlot selector; setScreen)
    - .planning/phases/24-auto-assignment-lineup/24-PATTERNS.md (App.tsx analog — handler pattern, local state, screen routing BEFORE/AFTER, emit helpers)
    - .planning/phases/24-auto-assignment-lineup/24-RESEARCH.md (Pattern 8 screen routing; Pitfall 1 — remove formationsLocked placeholder)
  </read_first>
  <action>
    Add local state `const [lineupAssignment, setLineupAssignment] = useState<string[] | null>(null)`,
    `const [lineupConfirmed, setLineupConfirmed] = useState(false)`, and
    `const [myFormationId, setMyFormationId] = useState<FormationId | null>(null)`. Read `playerSlot`
    from the store. Change `onBothFormationsConfirmed(homeFormation, awayFormation)` to set
    `myFormationId` = `playerSlot === 1 ? homeFormation : awayFormation` (it no longer sets a locked
    flag). Add `onLineupAssignmentReady(assignment: string[])` → `setLineupAssignment(assignment);
    setLineupConfirmed(false); setScreen('LINEUP_ASSIGNMENT')` and
    `onLineupAssignmentUpdated(assignment: string[])` → `setLineupAssignment(assignment)`. Register
    both with `socket.on(ServerEvents.LINEUP_ASSIGNMENT_READY, ...)` /
    `socket.on(ServerEvents.LINEUP_ASSIGNMENT_UPDATED, ...)` and add matching `socket.off` entries in
    the cleanup return (every on has an off). Add emit helpers `handleLineupSwap(a, b)` →
    `socket.emit(ClientEvents.LINEUP_SWAP, { slotIndexA: a, slotIndexB: b })` and
    `handleLineupConfirm(order)` → `socket.emit(ClientEvents.LINEUP_CONFIRM, { confirmedOrder: order });
    setLineupConfirmed(true)`. In the render switch, DELETE the
    `screen === 'UNIFORM_SELECTION' && formationsLocked` placeholder branch and add
    `screen === 'LINEUP_ASSIGNMENT'` rendering `<LineupAssignmentScreen assignment={lineupAssignment!}
    formationId={myFormationId!} playerSlot={playerSlot!} onSwap={handleLineupSwap}
    onConfirm={handleLineupConfirm} lineupConfirmed={lineupConfirmed} />`. Remove the now-dead
    `formationsLocked` / `setFormationsLocked` state declaration. Import `LineupAssignmentScreen`.
    Leave the existing `GAME_STATE` handler untouched — when both players confirm, the server
    broadcast routes the client to GAME_BOARD as it already does.
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit && pnpm --filter @counter-attack/client run build</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx declares `lineupAssignment`, `lineupConfirmed`, and `myFormationId` local state and no longer declares `formationsLocked`
    - `onBothFormationsConfirmed` sets `myFormationId` from `playerSlot`; `onLineupAssignmentReady` sets assignment and calls `setScreen('LINEUP_ASSIGNMENT')`
    - `LINEUP_ASSIGNMENT_READY` and `LINEUP_ASSIGNMENT_UPDATED` each have a `socket.on` registration and a matching `socket.off` in cleanup
    - Render switch has a `screen === 'LINEUP_ASSIGNMENT'` branch and no `formationsLocked` placeholder branch
    - `handleLineupSwap` emits `LINEUP_SWAP` and `handleLineupConfirm` emits `LINEUP_CONFIRM` then sets `lineupConfirmed`
    - `npx tsc --noEmit` and the client build both succeed
  </acceptance_criteria>
  <done>App routes to the lineup screen on assignment-ready, relays swaps/confirms, tracks own formation, and the placeholder is gone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser UAT — lineup display, drag-swap, GK lock, confirm-to-kickoff</name>
  <what-built>
    The full auto-assignment + lineup flow: after both players confirm formation, each browser tab
    shows its own lineup as a GK|DEF|MID|FWD stat-card grid; outfield cards drag-to-swap (server
    validates), the GK card is locked, and confirming both lineups starts the match at KICK_OFF_SETUP.
  </what-built>
  <how-to-verify>
    1. Start the server and client dev servers; open two browser tabs and create/join the same room.
    2. Complete team, uniform, and formation selection in both tabs until both confirm formation.
    3. Verify BOTH tabs now show the lineup screen (heading "MATCH SETUP: STEP 3"), each with all 11
       slots filled — GK in the GK column with a LOCKED badge, and correct player names in each column.
    4. In one tab, drag an outfield card onto another outfield card; verify the two cards swap after
       the server responds, and that the same swap does NOT appear in the other tab's lineup.
    5. Attempt to drag the GK card; verify it does not move (no-drop cursor) — and if any GK swap is
       forced, verify the "Swap rejected — GK cannot be moved." message appears and auto-clears.
    6. Click "Confirm Lineup" in both tabs (in either order); verify each tab shows the waiting state
       after its own confirm, and that once BOTH have confirmed the game advances to the board with
       pieces at their formation positions.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue (wrong names, swap not reflected, GK movable, game not starting).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary         | Description                                                                                 |
| ---------------- | ------------------------------------------------------------------------------------------- |
| client UI→server | Drag/confirm intents cross to the server, which re-validates (client drag guard is UX-only) |

## STRIDE Threat Register

| Threat ID | Category               | Component                 | Disposition | Mitigation Plan                                                                                                                       |
| --------- | ---------------------- | ------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-24-09   | Tampering              | Client-side GK drag guard | mitigate    | GK card `draggable={false}` is UX-only; authoritative GK lock is enforced server-side in Plan 03 (T-24-01)                            |
| T-24-10   | Information Disclosure | Own-lineup-only display   | mitigate    | Client renders only its own assignment (D-16); opponent assignment never sent to this socket (Plan 03 T-24-08)                        |
| T-24-SC   | Tampering              | npm/pip/cargo installs    | accept      | No packages installed this plan; HTML5 drag-and-drop is native browser API (RESEARCH Package Legitimacy Audit: zero new dependencies) |

</threat_model>

<verification>
- `packages/client` type-checks clean and builds (`npx tsc --noEmit`, `pnpm --filter @counter-attack/client run build`)
- Browser UAT (Task 3) confirms lineup render, drag-swap, GK lock, and confirm-to-kickoff
</verification>

<success_criteria>
Each player sees their own 4-column lineup of stat cards after formations confirm, can drag-to-swap
outfield players with server-authoritative updates, cannot move the GK, and confirming both lineups
starts the match at KICK_OFF_SETUP with pieces at formation positions.
</success_criteria>

<output>
Create `.planning/phases/24-auto-assignment-lineup/24-04-SUMMARY.md` when done.
</output>
