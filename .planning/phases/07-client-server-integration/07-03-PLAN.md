---
phase: 07-client-server-integration
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/ActionPanel.module.css
  - packages/client/src/components/ActionPanel.test.tsx
  - packages/client/src/components/ConnectionStatus.tsx
  - packages/client/src/components/ConnectionStatus.module.css
  - packages/client/src/components/DisconnectBanner.tsx
  - packages/client/src/components/DisconnectBanner.module.css
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/HexCell.tsx
autonomous: true
requirements: [UNDO-01, UNDO-02, UNDO-03, UNDO-04]
must_haves:
  truths:
    - "The Undo button appears only in the active player's tab during MOVEMENT and is hidden entirely for the opposing player"
    - 'The Undo button is disabled when gameState.lastDiceRoll is set'
    - 'Clicking Undo emits game:undo, which the server applyUndo reverses (piece restored, counter decremented)'
    - 'Roll / End Turn / Start Movement / GK restart buttons render per phase for the active player and emit the correct events'
    - 'A green/amber/red connection status indicator reflects live socket state'
    - 'A disconnect banner shows when game:disconnect-warning is received and dismisses on the next game:state'
    - 'During SHOT phase, opponent goal hexes are clickable and store a local shot target; Roll resolves the duel'
  artifacts:
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'Phase-gated action buttons + PassTypeSelector + undo gating (UNDO-01/02/03)'
      contains: 'emitUndo'
    - path: 'packages/client/src/components/ConnectionStatus.tsx'
      provides: 'Three-state socket connection indicator (D-12)'
      contains: 'reconnect_attempt'
    - path: 'packages/client/src/components/DisconnectBanner.tsx'
      provides: 'Conditional opponent-disconnect banner (D-13)'
      contains: 'disconnectWarning'
    - path: 'packages/client/src/components/ActionPanel.test.tsx'
      provides: 'Undo visibility/disabled tests (UNDO-02, UNDO-03)'
      contains: 'Undo'
  key_links:
    - from: 'packages/client/src/components/ActionPanel.tsx'
      to: 'packages/client/src/store/useGameStore.ts'
      via: 'emitUndo / emitRoll / emitEndTurn / emitGKRestart / emitStartMovement'
      pattern: 'emitUndo|emitRoll|emitGKRestart'
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'packages/client/src/store/useGameStore.ts'
      via: 'emitMove replaces movePiece on highlighted-hex click'
      pattern: "emitMove\\("
    - from: 'packages/client/src/components/ConnectionStatus.tsx'
      to: 'packages/client/src/socket.ts'
      via: 'socket.on connect/disconnect + socket.io.on reconnect_attempt'
      pattern: "socket\\.io\\.on\\('reconnect_attempt'"
---

<objective>
Build the live game-board interaction layer: the ActionPanel (Roll / End Turn / Undo / Start Movement / GK restart buttons + PassTypeSelector, all phase-gated and active-player-gated), the ConnectionStatus indicator, the DisconnectBanner, and the HexGrid/HexCell upgrades that replace local movePiece with server emitMove and add SHOT-phase goal-hex click routing. Wire all of these into GameBoard.

Purpose: This delivers all four UNDO requirements end-to-end on the client (UNDO-01 emit, UNDO-02 disabled-on-dice, UNDO-03 hidden-for-opponent, UNDO-04 server-reversal via emit) plus the action controls, connection feedback, and shot interaction that make a two-tab session playable.
Output: ActionPanel + PassTypeSelector (in ActionPanel), ConnectionStatus, DisconnectBanner (new); GameBoard, HexGrid, HexCell (modified); ActionPanel.test.tsx (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-client-server-integration/07-CONTEXT.md
@.planning/phases/07-client-server-integration/07-UI-SPEC.md
@.planning/phases/07-client-server-integration/07-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ActionPanel (buttons + PassTypeSelector + undo gating) with tests</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/ActionPanel.module.css, packages/client/src/components/ActionPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/TurnIndicator.tsx (per-field selector pattern to copy)
    - packages/client/src/components/TurnIndicator.module.css (.panel container style)
    - packages/client/src/components/LobbyScreen.module.css (.ctaButton + :disabled style to copy)
    - packages/client/src/store/useGameStore.ts (emitters + playerSlot from Plan 01)
    - packages/shared/src/types.ts (GameState fields: phase, activeTeam, attackingTeam, movementSlot, lastDiceRoll, ball.carrierId, pieces)
    - .planning/phases/07-client-server-integration/07-UI-SPEC.md (ActionPanel + PassTypeSelector full spec: visibility table, copy, colors, GK group)
    - .planning/phases/07-client-server-integration/07-PATTERNS.md (ActionPanel section: active-player derivation, undo gating, GK team derivation)
    - .planning/REQUIREMENTS.md (UNDO-01..04 exact wording)
  </read_first>
  <behavior>
    - Test: when playerSlot=2 and activeTeam='home', ActionPanel renders null (non-active player sees nothing — UNDO-03)
    - Test: when playerSlot=1, activeTeam='home', phase='MOVEMENT', lastDiceRoll undefined → Undo button is present and enabled
    - Test: when playerSlot=1, activeTeam='home', phase='MOVEMENT', lastDiceRoll set → Undo button is present but disabled (UNDO-02)
    - Test: when playerSlot=2 (opponent), phase='MOVEMENT', activeTeam='home' → queryByRole('button', { name: /undo/i }) is null (UNDO-03)
    - Test: clicking Undo (active player, no dice) calls emitUndo (UNDO-01)
    - Test: phase='PASS' active player → Roll Dice button present; clicking it calls emitRoll
    - Test: phase='KICK_OFF' active player → Start Movement button present
  </behavior>
  <action>
    Create `ActionPanel.tsx` as a named-export function component using per-field `useGameStore` selectors (copy TurnIndicator pattern — one selector per field, never destructure the whole store). Read: `playerSlot`, `gameState.phase`, `gameState.activeTeam`, `gameState.lastDiceRoll`, `gameState.ball.carrierId`, `gameState.pieces`, and emitters `emitRoll`, `emitEndTurn`, `emitUndo`, `emitStartMovement`, `emitGKRestart`.

    Active-player derivation (RESEARCH Open Q2 resolution — use `activeTeam` directly; the server maintains it across all transitions): `const myTeam = playerSlot === 1 ? 'home' : playerSlot === 2 ? 'away' : null; const isActivePlayer = myTeam !== null && myTeam === activeTeam;`. If `!isActivePlayer`, return `null` (UI-SPEC: ActionPanel renders nothing for the non-active player — this is the UNDO-03 mechanism).

    GK team derivation (mirror server controlsGKTeam in gameHandlers.ts): `const gkPiece = pieces.find((p) => p.id === ball.carrierId); const gkTeam = gkPiece?.teamId ?? null; const isGKTeam = myTeam !== null && myTeam === gkTeam;`. (For GK_RESTART, gate the three GK buttons on `isGKTeam` rather than the generic isActivePlayer.)

    Render buttons per the UI-SPEC visibility table, each using the `.ctaButton` class (copied into ActionPanel.module.css):
    - Roll ("Roll Dice"): when `phase` is one of `'PASS' | 'SHOT' | 'HEADER' | 'LOOSE_BALL'` AND isActivePlayer → `onClick={emitRoll}`.
    - End Turn ("End Turn"): when `phase === 'MOVEMENT'` AND isActivePlayer → `onClick={emitEndTurn}`.
    - Undo ("Undo Move"): when `phase === 'MOVEMENT'` AND isActivePlayer → conditionally RENDERED (D-10/UNDO-03 — only active player), `disabled={!!lastDiceRoll}` (D-09/UNDO-02 — dice committed are final), `onClick={emitUndo}` (UNDO-01 — emit reverses last movement; server applyUndo restores piece + decrements counter per UNDO-04). Render Undo ABOVE End Turn in the same column (UI-SPEC).
    - Start Movement ("Start Movement"): when `phase === 'KICK_OFF'` AND isActivePlayer → `onClick={emitStartMovement}`.
    - GK restart group: when `phase === 'GK_RESTART'` AND isGKTeam → a "GK Restart — choose:" label (13px/400/#a0a0a0) above three buttons: "Kick (High Pass)" → `emitGKRestart('kick')`, "Quick Throw" → `emitGKRestart('throw')`, "Start Movement" → `emitGKRestart('movement')`. Render all three simultaneously (no radio).

    PassTypeSelector: implement INSIDE ActionPanel (a local sub-component or inline block) rendered when `phase === 'PASS'` AND isActivePlayer, ABOVE the Roll button. Local React state `const [passType, setPassType] = useState<'STANDARD'|'FIRST_TIME'|'HIGH'|'LONG'>('STANDARD')`. Toggle button group of 4 (Standard/First-time/High/Long) styled per UI-SPEC (selected: bg #1a56b0, color #fff, weight 700; unselected: bg #0f3460, color #a0a0a0, weight 400). Label "Pass Type" above. NOTE: the current server `game:roll` for PASS resolves a HIGH-pass accuracy check and does not consume a pass-type payload, so passType is UI selection state only for Phase 7; do NOT invent a new event. Document this inline as a Phase 8 extension point.

    Game error display: read `gameState`-adjacent `gameError` via `useGameStore((s) => s.gameError)`; if set, render it below the buttons as `color: #ef4444` 13px/400 text (auto-clears when the next game:state arrives because App.tsx does not re-set it — acceptable for v1; no manual clear needed).

    "No spinner / no retry" — do NOT add loading state or debounce (UI-SPEC: isProcessing mutex drops duplicates server-side).

    Create `ActionPanel.module.css`: copy `.panel` from TurnIndicator.module.css (bg #16213e, 1px solid #0f3460, radius 4px, padding 16px, display flex column gap 8px); copy `.ctaButton` + `.ctaButton:disabled` (opacity 0.5; cursor default) from LobbyScreen.module.css; add PassTypeSelector classes per UI-SPEC tokens.

    Create `ActionPanel.test.tsx` using `@testing-library/react` (`render`, `screen`) and `vi.mock('../store/useGameStore.js', ...)` OR drive the real store via `useGameStore.setState({...})` before render. Mock `'../socket.js'` if importing the store transitively pulls socket. Cover every case in the behavior list. For undo-disabled use `screen.getByRole('button', { name: /undo/i })` and assert `.toBeDisabled()`; for opponent-hidden use `screen.queryByRole('button', { name: /undo/i })` is null.

  </action>
  <verify>
    <automated>cd packages/client && npx vitest run src/components/ActionPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ActionPanel.tsx contains `emitUndo` and `disabled={!!lastDiceRoll}` (UNDO-02)
    - ActionPanel.tsx returns `null` when not the active player (UNDO-03 mechanism)
    - ActionPanel.tsx contains `emitGKRestart('kick')`, `emitGKRestart('throw')`, `emitGKRestart('movement')`
    - ActionPanel.tsx contains all four pass-type values `'STANDARD'`, `'FIRST_TIME'`, `'HIGH'`, `'LONG'`
    - ActionPanel.module.css contains `.ctaButton` and `opacity: 0.5`
    - ActionPanel.test.tsx asserts Undo is disabled when lastDiceRoll is set and absent for the opposing player
    - `npx vitest run src/components/ActionPanel.test.tsx` exits 0
  </acceptance_criteria>
  <done>ActionPanel renders phase-gated, active-player-gated controls; undo gating satisfies UNDO-01/02/03; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build ConnectionStatus and DisconnectBanner components</name>
  <files>packages/client/src/components/ConnectionStatus.tsx, packages/client/src/components/ConnectionStatus.module.css, packages/client/src/components/DisconnectBanner.tsx, packages/client/src/components/DisconnectBanner.module.css</files>
  <read_first>
    - packages/client/src/components/TurnIndicator.tsx (display-component + selector pattern)
    - packages/client/src/components/TurnIndicator.module.css (.panel tokens)
    - packages/client/src/components/LobbyScreen.module.css (panel color tokens for banner)
    - packages/client/src/socket.ts (singleton — connect/disconnect events + socket.io manager)
    - packages/client/src/store/useGameStore.ts (disconnectWarning field from Plan 01)
    - .planning/phases/07-client-server-integration/07-UI-SPEC.md (ConnectionStatus + DisconnectBanner specs: colors, copy, placement)
    - .planning/phases/07-client-server-integration/07-PATTERNS.md (ConnectionStatus + DisconnectBanner sections; Pitfall 6 socket.io.on)
  </read_first>
  <action>
    Create `ConnectionStatus.tsx` (named export) using local React state `useState<'connected'|'reconnecting'|'disconnected'>('disconnected')`. In a `useEffect` (empty deps) declare named handlers `onConnect`/`onDisconnect`/`onReconnectAttempt`, register `socket.on('connect', onConnect)`, `socket.on('disconnect', onDisconnect)`, and `socket.io.on('reconnect_attempt', onReconnectAttempt)` — note `reconnect_attempt` is a MANAGER event on `socket.io`, NOT on `socket` (RESEARCH Pitfall 6; using `socket.on('reconnect_attempt')` silently never fires). Cleanup returns `socket.off('connect', onConnect)`, `socket.off('disconnect', onDisconnect)`, `socket.io.off('reconnect_attempt', onReconnectAttempt)`. Render a 10px circle (`border-radius: 50%`) with fill `#22c55e` (connected) / `#eab308` (reconnecting) / `#ef4444` (disconnected) and a 13px/400 `#a0a0a0` label "Connected" / "Reconnecting…" / "Disconnected", dot+label gap 4px, flex row align-items center. These three semantic colours appear ONLY here (UI-SPEC). Create `ConnectionStatus.module.css` with the container flex row; the dot fill may be an inline style driven by status.

    Create `DisconnectBanner.tsx` (named export): read `const disconnectWarning = useGameStore((s) => s.disconnectWarning)`. If `!disconnectWarning` return `null` (conditional render — unmount, not display:none; D-13). When shown, render a fixed full-width banner with copy "Opponent disconnected. Waiting for them to reconnect… (90s)". Create `DisconnectBanner.module.css`: `position: fixed; top: 0; left: 0; right: 0; z-index: 100; height: 40px; background: #0f3460; color: #f5c518; font-size: 13px; font-weight: 700;` centred (flex center). The banner auto-dismisses because App.tsx's game:state handler calls setDisconnectWarning(false) on the next broadcast (Plan 02).

  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - ConnectionStatus.tsx contains `socket.io.on('reconnect_attempt'` and `socket.io.off('reconnect_attempt'`
    - ConnectionStatus.tsx contains all three fills `#22c55e`, `#eab308`, `#ef4444`
    - ConnectionStatus.tsx contains labels "Connected", "Reconnecting…", "Disconnected"
    - DisconnectBanner.tsx contains `useGameStore((s) => s.disconnectWarning)` and `return null`
    - DisconnectBanner.module.css contains `position: fixed` and `z-index: 100`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ConnectionStatus reflects the three socket states via correct socket vs manager events; DisconnectBanner conditionally renders the warning; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire components into GameBoard and upgrade HexGrid/HexCell click routing</name>
  <files>packages/client/src/components/GameBoard.tsx, packages/client/src/components/HexGrid.tsx, packages/client/src/components/HexCell.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (current layout — additive insertion points)
    - packages/client/src/components/HexGrid.tsx (current — movePiece→emitMove + SHOT routing + movedPieceIds clickability)
    - packages/client/src/components/HexCell.tsx (current — add highlightColor prop)
    - packages/client/src/store/useGameStore.ts (emitMove, playerSlot from Plan 01)
    - packages/shared/src/pitch.ts (isInRegion; goal region names 'homeGoal' (q=0,r∈[10,16]) and 'awayGoal' (q=36,r∈[10,16]))
    - packages/shared/src/types.ts (GameState.movedPieceIds, activeTeam, phase)
    - .planning/phases/07-client-server-integration/07-UI-SPEC.md (Board Click Routing Contract; Hex Interaction State Extensions; Layout Spec)
    - .planning/phases/07-client-server-integration/07-PATTERNS.md (GameBoard, HexGrid, HexCell sections)
  </read_first>
  <action>
    GameBoard.tsx: import `ConnectionStatus`, `DisconnectBanner`, `ActionPanel`. Add `<ConnectionStatus />` at the end of the `<header>` (after the score span; header is flex, gap 16px per UI-SPEC). Insert `<DisconnectBanner />` immediately after the `</header>` and before `<main>`. Insert `<ActionPanel />` into the `<aside className={styles.sidebar}>` between `<TurnIndicator />` and `<ActionLog />`. Do not otherwise change the layout.

    HexCell.tsx: add an optional `highlightColor?: string` prop to `Props` (D-06: undefined → gold `#f5c518` for valid-move; a passed color e.g. `#ef4444` → red for SHOT goal hexes). In the highlight `<polygon>`, set `fill={highlightColor ?? '#f5c518'}` and `stroke={highlightColor ? '#cc2222' : '#d4a017'}` (keep existing fillOpacity hover logic 0.55/0.75 and strokeWidth 1.5/2). Leave the base polygon, difficult-angle dot, and goal-fill logic unchanged.

    HexGrid.tsx: replace `const movePiece = useGameStore((s) => s.movePiece);` with `const emitMove = useGameStore((s) => s.emitMove);`. Add selectors `const movedPieceIds = useGameStore((s) => s.gameState.movedPieceIds);` and `const playerSlot = useGameStore((s) => s.playerSlot);`. Add a local React state `const [shotTarget, setShotTarget] = useState<HexCoord | null>(null);` for the SHOT-phase goal click (RESEARCH Open Q1 resolution: the server SHOT resolution via game:roll does NOT consume a target hex — applyRoll resolves shooter-vs-GK from dice only. There is NO game:shot server event and none is to be created. The goal-hex click is a local UX selection; the Roll button in ActionPanel drives resolution via game:roll). Compute per hex whether it is an opponent goal hex during SHOT: `const isGoalHex = phase === 'SHOT' && (isInRegion(hex, 'homeGoal') || isInRegion(hex, 'awayGoal'));`. For the HexCell mapping:
    - Pass `highlightColor={isGoalHex ? '#ef4444' : undefined}` and treat `isHighlighted = validMoveHexSet.has(hexId) || isGoalHex`.
    - `onClick`: if a valid-move hex → `() => emitMove(selectedPieceId!, hex)` (replaces movePiece; emitMove clears selection in the store); else if `isGoalHex` → `() => setShotTarget(hex)` (local selection; cosmetic for v1); else `undefined`.
    Update piece clickability (Pitfall 8) — change `isClickable={phase === 'MOVEMENT' && piece.teamId === activeTeam}` to also exclude already-moved pieces: `isClickable={phase === 'MOVEMENT' && piece.teamId === activeTeam && !movedPieceIds.includes(piece.id)}`. Import `useState` from react, `isInRegion` and `type HexCoord` from `@counter-attack/shared`.

  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit -p tsconfig.json && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - GameBoard.tsx contains `<ConnectionStatus />`, `<DisconnectBanner />`, and `<ActionPanel />`
    - HexCell.tsx Props contains `highlightColor?: string` and the highlight polygon uses `highlightColor ?? '#f5c518'`
    - HexGrid.tsx contains `emitMove(` and does NOT contain `movePiece` (grep returns 0)
    - HexGrid.tsx contains `isInRegion(hex, 'homeGoal')` and `'awayGoal'`
    - HexGrid.tsx contains `!movedPieceIds.includes(piece.id)`
    - HexGrid.tsx does NOT reference a `game:shot` / `GAME_SHOT` event (none exists; grep returns 0)
    - `npx tsc --noEmit` exits 0 and `npx vitest run` exits 0 (full client suite green)
  </acceptance_criteria>
  <done>GameBoard renders the new components; HexGrid uses emitMove and SHOT goal-hex routing; HexCell supports the red highlight; full client typecheck + test suite pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| action buttons → server               | Every button emits a client event into the server-authoritative boundary; server re-validates phase + team |
| hex click → emitMove/local shotTarget | Board clicks cross to the server (move) or stay local (shot target UX)                                     |

## STRIDE Threat Register

| Threat ID | Category               | Component                                          | Disposition | Mitigation Plan                                                                                                                                                                    |
| --------- | ---------------------- | -------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-07-07   | Tampering              | Undo emitted by opposing player via crafted client | mitigate    | Client hides Undo for non-active (UNDO-03, UX only); server applyUndo guards isActivePlayer + MOVEMENT phase (gameHandlers GAME_UNDO) — render gating is not the security boundary |
| T-07-08   | Tampering              | Roll/EndTurn/GKRestart emitted out of phase/team   | mitigate    | Server WRONG_PHASE / WRONG_TEAM guards on every handler (gameHandlers); client phase-gating is UX only                                                                             |
| T-07-09   | Tampering              | shotTarget hex click                               | accept      | Shot target is local UI state only; never emitted; server SHOT duel resolves from dice via game:roll (no target consumed) — no attack surface                                      |
| T-07-10   | Information disclosure | gameError reason rendered in ActionPanel           | accept      | React JSX escapes the string; reason is a server enum, not user HTML (RESEARCH Security V5)                                                                                        |
| T-07-SC   | Tampering              | npm/pip/cargo installs                             | mitigate    | No new packages in Phase 7 (RESEARCH Package Legitimacy Audit: none); no install step in this plan                                                                                 |

</threat_model>

<verification>
- `cd packages/client && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd packages/client && npx vitest run` exits 0 (full client suite including ActionPanel + store tests)
- `grep -c movePiece packages/client/src/components/HexGrid.tsx` returns 0
- ActionPanel renders null for the non-active player (UNDO-03), disables Undo when lastDiceRoll set (UNDO-02)
</verification>

<success_criteria>

- All four UNDO requirements satisfied on the client: emit (UNDO-01), disabled-on-dice (UNDO-02), hidden-for-opponent (UNDO-03), server-reversal via emit (UNDO-04)
- ActionPanel delivers phase-gated Roll/EndTurn/StartMovement/GK-restart controls + PassTypeSelector
- ConnectionStatus shows live green/amber/red; DisconnectBanner shows + auto-dismisses
- HexGrid emits moves to the server and routes SHOT goal-hex clicks; HexCell supports red highlight
- Full client typecheck and test suite green
  </success_criteria>

<output>
Create `.planning/phases/07-client-server-integration/07-03-SUMMARY.md` when done
</output>
