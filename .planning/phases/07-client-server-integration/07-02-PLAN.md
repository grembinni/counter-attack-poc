---
phase: 07-client-server-integration
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - packages/client/src/App.tsx
  - packages/client/src/components/LobbyScreen.tsx
autonomous: true
requirements: [UNDO-03]
must_haves:
  truths:
    - 'On app mount the socket connects exactly once via a single central useEffect'
    - 'All five server events (game:state, room:joined, room:error, game:error, game:disconnect-warning) are registered with named handlers and cleaned up with socket.off using the same references'
    - 'Receiving game:state while on WAITING advances to GAME_BOARD'
    - 'room:joined stores the session token in localStorage and sets playerSlot + roomCode'
    - 'Create Room emits room:create on mount and shows the real server code; Join Room emits room:join with the typed code and shows inline errors'
  artifacts:
    - path: 'packages/client/src/App.tsx'
      provides: 'Central socket listener hub (D-03) with connect-on-mount (D-01)'
      contains: 'socket.connect()'
    - path: 'packages/client/src/components/LobbyScreen.tsx'
      provides: 'Real room:create / room:join wiring replacing MOCK42'
      contains: 'ClientEvents.ROOM_CREATE'
  key_links:
    - from: 'packages/client/src/App.tsx'
      to: 'packages/client/src/socket.ts'
      via: 'socket.on/off registration in mount useEffect'
      pattern: "socket\\.on\\(ServerEvents\\."
    - from: 'packages/client/src/components/LobbyScreen.tsx'
      to: 'packages/client/src/socket.ts'
      via: 'socket.emit room events'
      pattern: "socket\\.emit\\(ClientEvents\\.ROOM_"
---

<objective>
Wire App.tsx as the single central Socket.io event hub (connect on mount, register all server listeners with named handlers, clean up on unmount) and replace LobbyScreen's MOCK42 placeholder with real room:create / room:join socket calls driven by the server's room:joined and room:error responses.

Purpose: This is the lobby + event-routing half of Phase 7 — it makes two browser tabs able to create/join a room and routes every server broadcast into the Zustand store. Without App.tsx the board never receives state; without LobbyScreen wiring there is no way to start a real session.
Output: App.tsx (central useEffect added), LobbyScreen.tsx (mock replaced with socket events).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-client-server-integration/07-CONTEXT.md
@.planning/phases/07-client-server-integration/07-PATTERNS.md
@.planning/phases/07-client-server-integration/07-UI-SPEC.md
@packages/shared/src/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add central socket listener useEffect to App.tsx</name>
  <files>packages/client/src/App.tsx</files>
  <read_first>
    - packages/client/src/App.tsx (current — screen routing only; add useEffect)
    - packages/client/src/socket.ts (singleton; created in Plan 01)
    - packages/client/src/store/useGameStore.ts (extended store; setGameState/setScreen/setPlayerSlot/setRoomCode/setDisconnectWarning/setRoomError/setGameError from Plan 01)
    - packages/shared/src/events.ts (ServerEvents constants + ServerToClientEvents signatures — room:joined is (roomCode, slot, sessionToken); game:error is (reason); game:disconnect-warning is ())
    - .planning/phases/07-client-server-integration/07-PATTERNS.md (App.tsx section — named-handler pattern, screen-advance logic, token storage)
  </read_first>
  <action>
    Add a single `useEffect(() => { ... }, [])` (empty deps — runs once) to the `App` component. Subscribe to the store setters via individual `useGameStore((s) => s.setX)` selectors at the top of the component: setGameState, setScreen, setPlayerSlot, setRoomCode, setDisconnectWarning, setRoomError, setGameError.

    Inside the effect, call `socket.connect()` first (D-01: explicit connect on mount). Then declare FIVE NAMED function handlers (NOT inline arrows — RESEARCH Pitfall 1; inline arrows cannot be removed by socket.off):
    - `onGameState(state: GameState)`: call `setGameState(state)`, then `setDisconnectWarning(false)` (D-13 auto-dismiss banner on any fresh state), then if `useGameStore.getState().screen === 'WAITING'` call `setScreen('GAME_BOARD')` (Pitfall 7: advance from WAITING on game:state, NOT on room:joined, because broadcastState fires right after room:joined in the same tick).
    - `onRoomJoined(code: string, slot: 1 | 2, token: string)`: if `token` is truthy, `localStorage.setItem('ca_session_token', token)` (the slot-2 broadcast to the existing player carries an empty token — guard against storing it); call `setRoomCode(code)` and `setPlayerSlot(slot)`; read `const screen = useGameStore.getState().screen`; if `screen === 'CREATE_ROOM'` call `setScreen('WAITING')`. Do NOT advance JOIN_ROOM → GAME_BOARD here — that advance is handled by onGameState (Pitfall 7).
    - `onRoomError(reason: string)`: `setRoomError(reason)`.
    - `onGameError(reason: string)`: `setGameError(reason)`.
    - `onDisconnectWarning()`: `setDisconnectWarning(true)` (D-13, UNDO-03 context: opponent disconnect is surfaced; the opposing player cannot block the active player's undo — this banner is informational only).

    Register all five with `socket.on(ServerEvents.GAME_STATE, onGameState)`, `socket.on(ServerEvents.ROOM_JOINED, onRoomJoined)`, `socket.on(ServerEvents.ROOM_ERROR, onRoomError)`, `socket.on(ServerEvents.GAME_ERROR, onGameError)`, `socket.on(ServerEvents.GAME_DISCONNECT_WARNING, onDisconnectWarning)`. Return a cleanup function that calls `socket.off(...)` for each, passing the SAME named references. Import `useEffect` from react, `socket` from `./socket.js`, `ServerEvents` and `type GameState` from `@counter-attack/shared`. Keep the existing screen-routing JSX unchanged.

  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `socket.connect()`
    - App.tsx contains all five: `ServerEvents.GAME_STATE`, `ServerEvents.ROOM_JOINED`, `ServerEvents.ROOM_ERROR`, `ServerEvents.GAME_ERROR`, `ServerEvents.GAME_DISCONNECT_WARNING`
    - App.tsx contains `socket.off(` exactly five times (one cleanup per listener)
    - App.tsx contains `localStorage.setItem('ca_session_token'`
    - App.tsx advances WAITING→GAME_BOARD inside the game:state handler (contains `=== 'WAITING'` and `setScreen('GAME_BOARD')` within onGameState)
    - App.tsx uses named `function on...` handlers, not inline arrows in socket.on calls
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>App.tsx connects once on mount, routes all five server events into the store with named handlers, and cleans up on unmount; client typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace MOCK42 in LobbyScreen with real room socket wiring</name>
  <files>packages/client/src/components/LobbyScreen.tsx</files>
  <read_first>
    - packages/client/src/components/LobbyScreen.tsx (current — MOCK_CODE + setScreen mock buttons to replace)
    - packages/client/src/socket.ts (singleton; for emit)
    - packages/client/src/store/useGameStore.ts (roomCode, roomError, setRoomError from Plan 01)
    - packages/shared/src/events.ts (ClientEvents.ROOM_CREATE takes no args; ClientEvents.ROOM_JOIN takes a plain string roomCode — NOT an object)
    - .planning/phases/07-client-server-integration/07-UI-SPEC.md (Lobby Flow Wiring Contract; Copywriting Contract for error copy)
    - .planning/phases/07-client-server-integration/07-PATTERNS.md (LobbyScreen section)
  </read_first>
  <action>
    DELETE the `const MOCK_CODE = 'MOCK42';` line. Add imports: `socket` from `../socket.js`, `useEffect` from react, `ClientEvents` from `@counter-attack/shared`.

    `CopyButton`: change it to read the real code from the store — accept the code via a `code: string` prop OR read `useGameStore((s) => s.roomCode)` directly. Copy `roomCode ?? ''` to clipboard instead of MOCK_CODE.

    `CreateRoomScreen`: read `const roomCode = useGameStore((s) => s.roomCode)`. Add `useEffect(() => { socket.emit(ClientEvents.ROOM_CREATE); }, [])` to emit room:create on mount (no payload). The App.tsx room:joined handler sets roomCode + navigates to WAITING. Render the room code display showing `roomCode ?? 'Generating…'` (the 'Generating…' fallback in `#a0a0a0` weight 400 per UI-SPEC). DELETE the dev-shortcut `<button ... onClick={() => setScreen('GAME_BOARD')}>View Game Board</button>`. Keep the "Or join an existing room" sub-link (setScreen('JOIN_ROOM')).

    `JoinRoomScreen`: read `const roomError = useGameStore((s) => s.roomError)` and `const setRoomError = useGameStore((s) => s.setRoomError)`. In `handleSubmit`: if input is empty return; call `setRoomError(null)` to clear stale errors, then `socket.emit(ClientEvents.ROOM_JOIN, input)` — pass the raw string positionally (the server normalises case). REMOVE the `setScreen('WAITING')` call (navigation is now server-driven via App.tsx). Below the input, conditionally render an inline error when `roomError` is set: map the actual server reason strings — `'NOT_FOUND'` and `'INVALID_CODE'` → "Room not found. Check the code and try again."; `'NOT_WAITING'`, `'FULL'`, `'ALREADY_IN_ROOM'` → "This room is already in progress."; any other → "Could not join room. Try again." Style the error span `color: #ef4444`, font-size 13px, font-weight 400 (inline style or a new `.errorText` class in LobbyScreen.module.css — adding the class is permitted; the existing CSS file is owned by this plan via this component change).

    `WaitingScreen`: read `const roomCode = useGameStore((s) => s.roomCode)`. Replace `{MOCK_CODE}` with `{roomCode ?? 'Loading…'}`. Keep CopyButton and the animated dots.

  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - LobbyScreen.tsx does NOT contain `MOCK_CODE` or `MOCK42` (grep returns 0)
    - LobbyScreen.tsx contains `socket.emit(ClientEvents.ROOM_CREATE)`
    - LobbyScreen.tsx contains `socket.emit(ClientEvents.ROOM_JOIN, input)` (positional string, not object)
    - LobbyScreen.tsx does NOT contain `setScreen('WAITING')` (navigation is server-driven)
    - LobbyScreen.tsx maps `'NOT_FOUND'` to a "Room not found" message
    - LobbyScreen.tsx contains `'Generating…'`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>LobbyScreen emits real room events, shows the server-generated code, displays inline join errors mapped from actual server reason strings, and no longer references MOCK42.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                     |
| --------------------------------- | ------------------------------------------------------------------------------- |
| lobby input → room:join           | User-typed room code crosses to the server; server validates existence + status |
| server room:joined → localStorage | Session token from server is persisted client-side                              |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-07-04   | Spoofing               | room:join with arbitrary code                   | mitigate    | Server joinRoom guards NOT_FOUND/NOT_WAITING/FULL; client only displays mapped error, never bypasses (existing CONN-04 guards) |
| T-07-05   | Information disclosure | empty-token slot-2 broadcast to existing player | mitigate    | onRoomJoined stores token only when truthy; the existing-player notification carries `''` and is skipped (server CR-01 fix)    |
| T-07-06   | Tampering              | room:error reason rendered as inline text       | accept      | React JSX escapes the reason string; mapped to fixed copy, no HTML injection (RESEARCH Security V5)                            |
| T-07-SC   | Tampering              | npm/pip/cargo installs                          | mitigate    | No new packages in Phase 7 (RESEARCH Package Legitimacy Audit: none); no install step in this plan                             |

</threat_model>

<verification>
- `cd packages/client && npx tsc --noEmit -p tsconfig.json` exits 0
- `grep -c MOCK LobbyScreen.tsx` returns 0
- App.tsx registers and cleans up exactly five server-event listeners
</verification>

<success_criteria>

- App.tsx is the single socket event hub: connect on mount, five named listeners, full cleanup
- WAITING→GAME_BOARD advance is driven by game:state (not room:joined)
- LobbyScreen drives a real create/join flow with the server-generated code and inline error mapping
- No MOCK42 reference remains
  </success_criteria>

<output>
Create `.planning/phases/07-client-server-integration/07-02-SUMMARY.md` when done
</output>
