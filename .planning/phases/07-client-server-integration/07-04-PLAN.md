---
phase: 07-client-server-integration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/events.ts
  - packages/server/src/gameHandlers.ts
  - packages/server/src/roomStore.ts
  - packages/server/src/__tests__/game.integration.test.ts
autonomous: true
requirements: [CONN-02]
must_haves:
  truths:
    - "ClientEvents exposes GAME_SHOT = 'game:shot' and ClientToServerEvents types game:shot as (targetHex: HexCoord) => void (D-06)"
    - "The server registers a game:shot handler that guards roomCode, room existence, and phase === 'SHOT'"
    - 'The game:shot handler records the target on the room (room.shotTarget) but does NOT change dice resolution and does NOT call broadcastState'
    - 'applyRoll for SHOT continues to resolve the shooter-vs-GK duel from dice only; the target is recorded for UX/broadcast, never consumed by resolution'
    - 'A malformed game:shot payload (non-{q,r} target) or out-of-phase emit is rejected without mutating room state'
  artifacts:
    - path: 'packages/shared/src/events.ts'
      provides: 'GAME_SHOT client event constant + ClientToServerEvents game:shot signature'
      contains: "GAME_SHOT: 'game:shot'"
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'game:shot handler with SHOT-phase + HexCoord-shape guards'
      contains: 'ClientEvents.GAME_SHOT'
    - path: 'packages/server/src/roomStore.ts'
      provides: 'Room.shotTarget field to hold the recorded shot target'
      contains: 'shotTarget'
  key_links:
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'packages/server/src/gameHandlers.ts'
      via: 'socket.emit(ClientEvents.GAME_SHOT, hex) → game:shot handler'
      pattern: "ClientEvents\\.GAME_SHOT"
    - from: 'packages/server/src/gameHandlers.ts'
      to: 'packages/server/src/roomStore.ts'
      via: 'handler writes room.shotTarget'
      pattern: 'room\\.shotTarget'
---

<objective>
Add the `game:shot` event end-to-end on the shared + server side so D-06 is honoured: when a goal hex is clicked during the SHOT phase the client emits the target coordinate to the server, the server records it for UX/broadcast, and the existing dice-only duel resolution (via `game:roll` → `applyRoll`) is left unchanged.

Purpose: D-06 in CONTEXT.md states "the client emits the target coord; server transitions to the saving duel." Phase 7's prior plans stored the target only in local React state and never emitted it — a context-compliance gap flagged by the checker. This plan supplies the shared-type and server contract that Plan 03 Task 3 wires the client click handler against. It is Wave 1 (no dependencies) so it lands before or in parallel with 07-01, giving 07-03 a stable `GAME_SHOT` symbol to import.
Output: `events.ts` (GAME_SHOT added), `gameHandlers.ts` (game:shot handler added), `roomStore.ts` (Room.shotTarget field added), `game.integration.test.ts` (game:shot coverage added).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-client-server-integration/07-CONTEXT.md
@.planning/phases/07-client-server-integration/07-RESEARCH.md
@packages/shared/src/events.ts
@packages/server/src/gameHandlers.ts
@packages/server/src/roomStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GAME_SHOT to shared events and Room.shotTarget to the store</name>
  <files>packages/shared/src/events.ts, packages/server/src/roomStore.ts</files>
  <read_first>
    - packages/shared/src/events.ts (ClientEvents const object + ClientToServerEvents interface — additive only; mirror GAME_MOVE's HexCoord import and JSDoc style)
    - packages/shared/src/types.ts (HexCoord = { q: number; r: number } — the payload shape)
    - packages/server/src/roomStore.ts (Room type — add an optional shotTarget field; do NOT touch GameState in shared types)
  </read_first>
  <action>
    In `packages/shared/src/events.ts`: add `GAME_SHOT: 'game:shot',` to the `ClientEvents` const object (place it adjacent to `GAME_ROLL` so the SHOT pair reads together). Add a corresponding entry to the `ClientToServerEvents` interface: `[ClientEvents.GAME_SHOT]: (targetHex: HexCoord) => void;` with a JSDoc line citing D-06 (e.g. "D-06: client emits the SHOT target coord; server records it for UX/broadcast — duel still resolves from dice via game:roll."). `HexCoord` is already imported at the top of the file — reuse it, do not re-import.

    In `packages/server/src/roomStore.ts`: add an optional field `shotTarget?: HexCoord | null;` to the exported `Room` type (after `disconnectTimers`). Import `type HexCoord` from `@counter-attack/shared` alongside the existing `GameState` type import. Do NOT initialise it in `createRoom`/`joinRoom` (optional field, defaults to undefined) and do NOT add it to `broadcastState` — it is server-side UX bookkeeping, not part of the GameState snapshot. This keeps the shared GameState type untouched (no client snapshot churn) while giving the handler a place to record the target.

  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit -p tsconfig.json && cd ../server && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - events.ts contains `GAME_SHOT: 'game:shot'`
    - events.ts ClientToServerEvents contains `[ClientEvents.GAME_SHOT]: (targetHex: HexCoord) => void`
    - roomStore.ts Room type contains `shotTarget?: HexCoord | null`
    - GameState in packages/shared/src/types.ts is NOT modified (grep `shotTarget` in types.ts returns 0)
    - `npx tsc --noEmit` exits 0 in both packages/shared and packages/server
  </acceptance_criteria>
  <done>GAME_SHOT exists in the shared contract with a typed HexCoord payload; Room has a shotTarget field; both shared and server typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add the game:shot handler to gameHandlers.ts with phase + payload guards and integration tests</name>
  <files>packages/server/src/gameHandlers.ts, packages/server/src/__tests__/game.integration.test.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (handler pattern: roomCode guard → getRoom → isProcessing → phase guard → team guard → mutate; the GAME_GK_RESTART handler's payload-validation block is the closest analog for input validation)
    - packages/server/src/roomStore.ts (getRoom, Room.shotTarget from Task 1; broadcastState is NOT used here)
    - packages/shared/src/types.ts (HexCoord shape for the validation guard)
    - packages/server/src/__tests__/game.integration.test.ts (server lifecycle + typed-client + setupRoom helpers to reuse; copy the oncePromise/WRONG_PHASE assertion idioms)
  </read_first>
  <action>
    In `packages/server/src/gameHandlers.ts`, register a new `socket.on(ClientEvents.GAME_SHOT, (targetHex: HexCoord) => { ... })` handler inside `registerGameHandlers`, placed after the GAME_ROLL handler and before GAME_GK_RESTART. Implement the guard ladder, mirroring the existing handlers but with two deliberate departures noted below:
    - `const { roomCode } = socket.data; if (roomCode === undefined) return;`
    - `const room = getRoom(roomCode); if (!room || room.isProcessing) return;` (SC-5 duplicate-drop). Set `room.isProcessing = true;` and wrap the body in `try { ... } finally { room.isProcessing = false; }` per Pitfall 5.
    - Phase guard (T-07-11): `if (room.gameState === null || room.gameState.phase !== 'SHOT') { socket.emit(ServerEvents.GAME_ERROR, 'WRONG_PHASE'); return; }`. NOTE the first departure: do NOT call `broadcastState` here — unlike the move/roll handlers, game:shot records UX-only data; a rejection should not trigger a state snapshot (per revision instruction: the handler does NOT broadcast).
    - Team guard: `if (!isActivePlayer(socket, room)) { socket.emit(ServerEvents.GAME_ERROR, 'WRONG_TEAM'); return; }` (the active player in SHOT is the shooter; reuse the existing `isActivePlayer` helper — no broadcast on rejection).
    - Payload validation (T-07-12, ASVS V5 — never trust client input): guard the HexCoord shape: `if (typeof targetHex !== 'object' || targetHex === null || typeof targetHex.q !== 'number' || typeof targetHex.r !== 'number') { socket.emit(ServerEvents.GAME_ERROR, 'INVALID_TARGET'); return; }`. Mirror the GAME_GK_RESTART INVALID_CHOICE validation style.
    - Record the target: `room.shotTarget = { q: targetHex.q, r: targetHex.r };`. Second departure: do NOT call `broadcastState` and do NOT mutate `room.gameState`. The duel still resolves from dice when the Roll button later fires `game:roll` → `applyRoll`; `applyRoll`'s SHOT branch is unchanged and never reads `room.shotTarget`. Add a comment block at the top of the handler documenting: "D-06: game:shot records the shooter's target hex for UX/broadcast. Dice resolution is unchanged — applyRoll resolves shooter-vs-GK from dice only and does not consume shotTarget. This handler records intent; the Roll button's game:roll handler performs (and broadcasts) the resolution." Optionally emit a lightweight ack (`socket.emit(ServerEvents.GAME_ERROR, ...)` is NOT an ack — do not abuse it); recording server-side state with no emit is acceptable and matches the revision instruction ("emit an ack or update server-side state only").

    In `packages/server/src/__tests__/game.integration.test.ts`, add a `describe('game:shot (D-06)')` block reusing the existing server lifecycle + setupRoom helpers. Cover: (a) emitting `game:shot` with a valid `{q,r}` while phase is NOT 'SHOT' results in a `GAME_ERROR` with `'WRONG_PHASE'` and leaves `getRoom(code)!.shotTarget` undefined; (b) driving the room to SHOT phase (reuse the existing FSM-advance helpers; if reaching SHOT in-test is impractical, set `getRoom(code)!.gameState!.phase = 'SHOT'` directly as the integration tests already manipulate room state) then emitting `game:shot` from the active (shooter) client records `getRoom(code)!.shotTarget` deep-equal to the sent hex and does NOT emit a `game:state` to the shooter (assert no game:state fires within a short window, OR assert shotTarget is set — whichever is stable); (c) a malformed payload (e.g. `socket.emit('game:shot', { q: 'x' } as never)`) yields `GAME_ERROR` `'INVALID_TARGET'`. Follow the oncePromise / WRONG_PHASE assertion idioms already in the file.

  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/__tests__/game.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - gameHandlers.ts contains `socket.on(ClientEvents.GAME_SHOT`
    - gameHandlers.ts game:shot handler contains `phase !== 'SHOT'` and `'INVALID_TARGET'`
    - gameHandlers.ts game:shot handler sets `room.shotTarget` and does NOT call `broadcastState` (grep the handler body: broadcastState count inside the GAME_SHOT handler is 0)
    - gameHandlers.ts does NOT modify `applyRoll` or the GAME_ROLL handler's SHOT resolution
    - game.integration.test.ts contains a `describe('game:shot` block asserting WRONG_PHASE, recorded shotTarget, and INVALID_TARGET
    - `npx vitest run src/__tests__/game.integration.test.ts` exits 0
  </acceptance_criteria>
  <done>The server accepts game:shot, guards phase + team + payload shape, records room.shotTarget without broadcasting, and leaves dice resolution untouched; integration tests prove all three branches.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                       |
| --------------------------- | ------------------------------------------------------------------------------------------------- |
| client click → game:shot    | The shooter's goal-hex click crosses into the server-authoritative boundary as a HexCoord payload |
| game:shot → room.shotTarget | Recorded server-side; never fed into dice resolution, so it cannot alter the duel outcome         |

## STRIDE Threat Register

| Threat ID | Category  | Component                                | Disposition | Mitigation Plan                                                                                                                                        |
| --------- | --------- | ---------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-07-11   | Tampering | game:shot emitted out of SHOT phase/team | mitigate    | Handler guards `phase === 'SHOT'` + `isActivePlayer`; out-of-phase/wrong-team emits return GAME_ERROR and never write room.shotTarget                  |
| T-07-12   | Tampering | malformed HexCoord payload on game:shot  | mitigate    | Shape validation rejects non-{q:number,r:number} payloads with INVALID_TARGET before any state write (ASVS V5, mirrors GK_RESTART INVALID_CHOICE)      |
| T-07-13   | Elevation | shotTarget used to bias duel resolution  | accept      | room.shotTarget is UX/broadcast bookkeeping only; applyRoll resolves SHOT from dice and never reads shotTarget — recording it grants no game advantage |
| T-07-SC   | Tampering | npm/pip/cargo installs                   | mitigate    | No new packages in Phase 7 (RESEARCH Package Legitimacy Audit: none); no install step in this plan                                                     |

</threat_model>

<verification>
- `cd packages/shared && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd packages/server && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd packages/server && npx vitest run src/__tests__/game.integration.test.ts` exits 0
- game:shot rejects WRONG_PHASE and INVALID_TARGET without mutating room.shotTarget; records the target on the valid SHOT path without broadcasting
</verification>

<success_criteria>

- GAME_SHOT is a typed shared event with a HexCoord payload (D-06 contract)
- The server records the shot target on a valid SHOT-phase emit and rejects out-of-phase/malformed emits
- Dice resolution is unchanged — applyRoll still resolves SHOT from dice only
- Integration tests cover the phase guard, payload guard, and recorded-target path
  </success_criteria>

<output>
Create `.planning/phases/07-client-server-integration/07-04-SUMMARY.md` when done
</output>
