---
phase: 18.3-bug-bash-rule-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/roomStore.ts
files_touched_note: 'gameEngine.ts sections: applyMove (~399-500, BUG-13) and applyFreeMoveZoneCheck (~1022, BUG-20) — distinct, non-overlapping regions. This is the FIRST wave-1 owner of gameEngine.ts; plans 03/04 depend on it to serialize gameEngine.ts edits.'
autonomous: true
requirements: [BUG-13, BUG-20]
must_haves:
  truths:
    - 'When an attacker moves into a hex within ZoI of two defenders and retains the ball after the first tackle, a second TACKLE_ATTEMPT fires against the second contesting defender'
    - 'The free-move offer never overlays FREE_MOVE_ATTACK/FREE_MOVE_DEFENSE in the middle of a MOVE slot sequence or during an in-progress HEADER flow'
    - 'A ball crossing a final-third boundary still triggers the free-move offer — but only at the next clean phase boundary'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'multi-tackle sequencing in applyMove; deferred free-move interrupt guard in applyFreeMoveZoneCheck'
      contains: 'applyFreeMoveZoneCheck'
  key_links:
    - from: 'packages/server/src/roomStore.ts broadcastState'
      to: 'applyFreeMoveZoneCheck'
      via: 'unchanged call site; the deferral guard lives inside the function'
      pattern: 'applyFreeMoveZoneCheck'
---

<objective>
Fix two server-engine rule/flow bugs that touch distinct, non-overlapping regions of `gameEngine.ts`:
- BUG-13 (investigation-first): when an attacker steps into a hex contested by two ZoI defenders and retains the ball after the first TACKLE_ATTEMPT resolves, sequence a second TACKLE_ATTEMPT against the second defender before the move fully resolves.
- BUG-20: defer the MOVE-06 free-move interrupt so `applyFreeMoveZoneCheck` never overlays FREE_MOVE_ATTACK/DEFENSE mid-MOVE-slot or mid-HEADER; the in-progress slot/header resolves to its natural next phase boundary first, then the free-move offer is evaluated. Preserve D-33's zone-crossing trigger condition and D-34/D-35/D-36 eligibility/resume mechanism.

Purpose: This plan is the first wave-1 owner of `gameEngine.ts`. Batching BUG-13 (applyMove) and BUG-20 (applyFreeMoveZoneCheck) here — two distinct engine sections — lets the offside/header plan (03) and the movedPieceIds/replay/undo plan (04) serialize behind it, avoiding same-file edit conflicts (RESEARCH Pitfall 3).
Output: Multi-defender tackle sequencing + deferred free-move interrupt timing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-CONTEXT.md
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-RESEARCH.md
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUG-13 Wave-0 investigation — add failing two-defender tackle fixture</name>
  <files>packages/server/src/gameEngine.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (`applyMove` ~399-500 — the TACKLE_ATTEMPT resolution; how the single injected `dice?.tackleDie ?? 3` is consumed; how the mover is added to `tackleAttemptedByIds` after a FAIL)
    - packages/shared/src/moveValidator.ts (`validateMove` — the ZoI/TACKLE_ATTEMPT effect; the `tackleAttemptedByIds` exclusion filter at line ~120 that already skips already-attempted defenders)
    - packages/server/src/gameEngine.test.ts (existing tackle test fixtures — the established pattern for seeding attacker + defender positions with real HOME_SQUAD/AWAY_SQUAD coords, hexDistance=1 adjacency)
  </read_first>
  <action>
    Add a RED test to `gameEngine.test.ts` that seeds one attacking carrier moving into a hex that is simultaneously within ZoI (adjacency) of TWO defenders, forces the first tackle die so the attacker RETAINS the ball (tackle FAILs), and asserts that TWO TACKLE_ATTEMPT events are ultimately produced (one per contesting defender) with both defender ids appearing in `tackleAttemptedByIds` after resolution. Document in a comment that this test captures current behavior (only ONE TACKLE fires today) and is expected RED until Task 2 lands the sequencing loop. Use real squad positions (per the locked convention: placeholder coords break moves silently). Name the test descriptively so `-t "tackle"` selects it.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- --run -t "tackle"</automated>
  </verify>
  <acceptance_criteria>
    - `gameEngine.test.ts` contains a new test seeding two ZoI defenders around one destination hex
    - The test asserts two TACKLE_ATTEMPT events and both defender ids in `tackleAttemptedByIds`
    - Running `-t "tackle"` shows this new test RED (documenting the current single-tackle behavior) — this is the intended Nyquist RED signal
  </acceptance_criteria>
  <done>A failing regression test exists proving only one tackle currently fires against two contesting defenders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BUG-13 — sequence a second TACKLE_ATTEMPT via inline re-validate loop</name>
  <files>packages/server/src/gameEngine.ts</files>
  <behavior>
    - After the first TACKLE_ATTEMPT resolves FAIL (attacker retains ball), re-run validateMove with tackleAttemptedByIds updated to include the first defender; if it returns another TACKLE_ATTEMPT, resolve it too
    - The loop terminates when validateMove returns no further TACKLE_ATTEMPT (all contesting defenders attempted, or a tackle succeeds and possession changes)
    - A successful tackle (possession lost) short-circuits — no further attempts after the ball is won
    - The two-defender fixture from Task 1 now passes: two TACKLE_ATTEMPT events, both ids in tackleAttemptedByIds
  </behavior>
  <read_first>
    - packages/server/src/gameEngine.ts (`applyMove` TACKLE branch ~399-500; the `abandonedIds` loop-then-continue structure ~453-457 as the structural analog for iterate-with-updated-state; the injected `dice` param shape)
    - packages/shared/src/moveValidator.ts (`validateMove` `tackleAttemptedByIds` exclusion at ~line 120 — re-calling with updated state naturally yields the next un-attempted defender)
  </read_first>
  <action>
    In `applyMove`'s TACKLE branch, after the first TACKLE_ATTEMPT resolves with the attacker retaining the ball, add `tackleAttemptedByIds`-updated state and re-call `validateMove` (or the inline ZoI check that produces the TACKLE_ATTEMPT effect). If a second TACKLE_ATTEMPT effect is returned, resolve it inline before committing the final return; loop until `validateMove` returns no further TACKLE_ATTEMPT. Terminate immediately if any attempt succeeds and possession transfers (the ball is won — no more attempts). For the dice injection: use the existing `dice?.tackleDie ?? 3` convention for each attempt; if the injected `dice` param only supplies one tackle die, default subsequent attempts to `3` (mid-range fallback, matching the existing single-tackle default) — do not introduce `Math.random` in the engine. Do NOT change `moveValidator.ts` (its exclusion filter already supports re-calling with updated ids).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- --run -t "tackle"</automated>
  </verify>
  <acceptance_criteria>
    - `applyMove` re-checks ZoI/tackle against updated `tackleAttemptedByIds` after a retained-ball FAIL and sequences a second TACKLE_ATTEMPT when a second defender contests
    - The loop short-circuits on possession change (successful tackle)
    - No `Math.random` introduced in `gameEngine.ts`
    - The Task-1 two-defender fixture now passes (two TACKLE_ATTEMPT events)
    - `pnpm --filter @counter-attack/server test -- --run -t "tackle"` exits 0 (all tackle tests green)
  </acceptance_criteria>
  <done>Two contesting defenders each get a tackle attempt when the attacker retains the ball after the first; single-defender behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: BUG-20 — defer free-move interrupt past mid-MOVE-slot and mid-HEADER</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/roomStore.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (`applyFreeMoveZoneCheck` ~1022 — the existing early-return guard block that already returns unchanged state for HALF_TIME/FULL_TIME/FREE_MOVE_ATTACK/FREE_MOVE_DEFENSE; the D-33 zone-crossing trigger; the `freeMoveResume`/`freeMoveEligibleIds` D-34/D-35/D-36 mechanism it feeds)
    - packages/server/src/roomStore.ts (`broadcastState` ~244-248 — the single ARCH-04 call site `room.gameState = applyFreeMoveZoneCheck(room.gameState);`; note this fires after EVERY resolved action, so the guard must live inside the function, not at this call site, to avoid affecting unrelated action types)
    - packages/shared/src/types.ts (`GameState.phase`, `movementSlot`, `movedPieceIds` fields; whether a `ballZone`/`computeBallZone` mechanism exists that must keep updating even when the interrupt is deferred)
  </read_first>
  <action>
    Extend `applyFreeMoveZoneCheck`'s early-return guard so it defers the free-move overlay while a MOVE slot is in progress or a HEADER is in progress, WITHOUT changing D-33's zone-crossing trigger detection. Add two conditions after the existing phase checks: (1) if `state.phase === 'MOVE' && state.movementSlot !== null`, do not overlay FREE_MOVE_ATTACK/DEFENSE — return state with only any ball-zone tracking field updated (if such tracking exists; otherwise return `state` unchanged so the interrupt is simply skipped this broadcast); (2) if `state.phase === 'HEADER'`, likewise defer. The deferred interrupt fires naturally on the next `broadcastState` call after the slot/header resolves to a new phase (at which point the phase is no longer MOVE-mid-slot / HEADER, so the existing zone-check logic engages). Do NOT change the `broadcastState` call site in `roomStore.ts` beyond confirming the guard is inside the pure function; if `roomStore.ts` needs no edit, remove it from files_modified in the SUMMARY. Preserve `freeMoveEligibleIds` (D-34) and `freeMoveResume` (D-35/D-36) unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- --run -t "free.move"</automated>
  </verify>
  <acceptance_criteria>
    - `applyFreeMoveZoneCheck` returns without overlaying FREE_MOVE_ATTACK/DEFENSE when `phase === 'MOVE' && movementSlot !== null`
    - `applyFreeMoveZoneCheck` returns without overlaying FREE_MOVE_ATTACK/DEFENSE when `phase === 'HEADER'`
    - The D-33 zone-crossing trigger condition and the `freeMoveResume`/`freeMoveEligibleIds` fields are unchanged (no edits to those computations)
    - `pnpm --filter @counter-attack/server test -- --run -t "free.move"` exits 0
    - `pnpm -r test -- --run` still green (guard does not regress unrelated action flows through broadcastState)
  </acceptance_criteria>
  <done>Free-move offer no longer breaks up an in-progress MOVE slot or HEADER; it is evaluated at the next clean phase boundary; zone-crossing trigger unchanged.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new exported symbols or files. This plan modifies existing functions:

- `gameEngine.ts`: `applyMove` (multi-tackle loop added inside), `applyFreeMoveZoneCheck` (early-return guard extended)
- `roomStore.ts`: `broadcastState` call site inspected; edited only if a ball-zone tracking field must be threaded (likely unchanged)
- `gameEngine.test.ts`: one new tackle test (test file, not a production symbol)
  If BUG-20's deferral requires a small internal helper (e.g. a ball-zone recompute), it is a local, non-exported addition to `gameEngine.ts` — document it in the SUMMARY.
  </artifacts_this_phase_produces>

<verification>
- `pnpm -r test -- --run` full suite green
- `pnpm -r tsc --noEmit` clean typecheck
- BUG-13: two-defender fixture green; BUG-20: free-move deferral tests green; no unrelated broadcastState regressions
</verification>

<success_criteria>
BUG-13 and BUG-20 are fixed and covered by regression tests; the full test suite and typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/18.3-bug-bash-rule-correctness/18.3-02-SUMMARY.md` when done
</output>
