---
phase: 18.3-bug-bash-rule-correctness
verified: 2026-07-01T22:25:00Z
status: human_needed
score: 12/13 must-haves verified
overrides_applied: 0
human_verification:
  - test: 'Confirm SNAPSHOT_TARGET goal-line hex highlights are visible for the attacking player (BUG-21)'
    expected: "During SNAPSHOT_TARGET, goal-line hexes are tinted/highlighted on the attacking player's board so they can select a target"
    why_human: 'The PLAN-05 gate explicitly requires live two-tab verification before closing this bug. The SUMMARY-05 reports a prior session confirmed it works with no code change needed. This outcome can only be validated by a human running the live session — no code was changed and the prior verification was performed by the executor, not an independent verifier.'
---

# Phase 18.3: Bug-Bash Rule Correctness Verification Report

**Phase Goal:** Fix the highest-priority rule-correctness bugs identified in the bug bash session: action log display consistency, engine rule accuracy (ZoI tackles, offside resets, header delivery, FTP toggle), client UX correctness (HexGrid interactions), and movement-phase state-machine fixes (replay visibility, Snapshot availability, Undo coverage).
**Verified:** 2026-07-01T22:25:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                         | Status    | Evidence                                                                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | GOAL and SHOT_ATTEMPT log entries render in standard gray (.content) — no amber styling                                                                       | VERIFIED  | `ActionLog.tsx` uses `className={styles.content}` unconditionally (line 813); no `styles.goalContent` reference remains; `.goalContent` class removed from `ActionLog.module.css`                                                                                                                                   |
| 2   | FTP_MOVE and HP_MOVE log entries show the player's full name via PNamed (no A/D prefix)                                                                       | VERIFIED  | `ActionLog.tsx` HP_MOVE case (line 658) and FTP_MOVE case (line 672) both use `<PNamed pieceId={event.pieceId} />`; GK role labels unchanged                                                                                                                                                                        |
| 3   | Jersey number sourced from piece.number on all three surfaces (PieceOverlay, ActionLog, PlayerStatsPanel)                                                     | VERIFIED  | `PieceOverlay.tsx` line 83: `const playerNumber = String(piece.number);`; `ActionLog.tsx` `pieceNum()` (lines 127–131) uses `pieces.find` store lookup returning `String(piece.number)`                                                                                                                             |
| 4   | After a TACKLE FAIL, a second TACKLE_ATTEMPT fires for any other adjacent defender before the move resolves                                                   | VERIFIED  | `gameEngine.ts` lines 692–773: inline loop after first TACKLE FAIL scans `newPieces` by `hexDistance(p.position, carrierCurrentPos) === 1`, excludes `newTackleAttemptedByIds`; test in `gameEngine.test.ts` covers BUG-13                                                                                          |
| 5   | Free-move offer never interrupts a mid-MOVE-slot or mid-HEADER — deferred to next clean phase boundary                                                        | VERIFIED  | `applyFreeMoveZoneCheck` lines 1143–1148: returns `state` unchanged when `phase === 'MOVE' && state.movementSlot !== null` or `phase === 'HEADER'`; three BUG-20 tests in `gameEngine.phase17.test.ts`                                                                                                              |
| 6   | offsidePieceIds resets to [] on every dead-ball restart (free-kick and kick-off-after-goal)                                                                   | VERIFIED  | `gameEngine.ts` lines 1962–1964 and 2048–2051: `offsidePieceIds: []` on GOAL→KICK_OFF_SETUP transitions in `applyRoll` SHOT branch; `gameHandlers.ts` line 1812: `offsidePieceIds: []` on GAME_READY KICK_OFF transition; `applyFreeKickReady` stage-3 already had it (line 4056); BUG-06 test in `offside.test.ts` |
| 7   | After a header duel is won, the ball is delivered directly (no intermediate GAME_HEADER_TARGET step); delivery is non-contestable and logged as HEADER        | VERIFIED  | `gameHandlers.ts` lines 2558–2596: GAME_HEADER_CONTESTANT winner branch calls `applyResolveHeaderTarget` immediately using winner's hex as target; `lastActionType: 'HEADER'` set by delivery; integration test in `gameHandlers.rule11.test.ts` line 343 asserts phase ∈ {PASS, GK_DIVE, LOOSE_BALL} directly      |
| 8   | With FTP_MOVE_ENABLED=false (default), a first-time pass delivers the ball directly, skipping FIRST_TIME_PASS_MOVE repositioning                              | VERIFIED  | `gameEngine.ts` line 76: `const FTP_MOVE_ENABLED = false;`; lines 1699–1704: `if (FTP_MOVE_ENABLED)` gates the FIRST_TIME_PASS_MOVE transition; direct delivery path mirrors STANDARD_PASS; BUG-12 regression tests in `gameEngine.phase17.test.ts`                                                                 |
| 9   | Clicking an already-moved own-team piece in MOVE opens its player card (inspectPiece) without triggering move-target highlighting                             | VERIFIED  | `HexGrid.tsx` lines 784–793: final handleClick fallback uses `() => inspectPiece(piece.id)` when `phase === 'MOVE' && myTeam !== null && piece.teamId === myTeam && movedPieceIds.includes(piece.id)`; 4 BUG-10 tests in `HexGrid.test.tsx` pass                                                                    |
| 10  | SNAPSHOT_TARGET goal-line highlights are visible to the attacking player                                                                                      | UNCERTAIN | PLAN-05 required live two-tab verification before authoring any fix. SUMMARY-05 reports a human verifier confirmed this works in the current build with no code change needed. Cannot verify programmatically — requires human confirmation                                                                         |
| 11  | KICK_OFF_SETUP repositioning moves appear in the eventLog and are reconstructed by buildReplayFrames                                                          | VERIFIED  | `types.ts` lines 85–92: `KICK_OFF_SETUP` in `ActionEventType`; `types.ts` lines 321–329: `KICK_OFF_SETUP` member in `ActionEvent` union; `gameEngine.ts` line 4160: `'KICK_OFF_SETUP'` in `REPLAY_ELIGIBLE_TYPES`; BUG-17 integration test in `replay.integration.test.ts` line 760 passes                          |
| 12  | Snapshot stays available to the ball carrier after pace is exhausted, as long as no different piece has been activated                                        | VERIFIED  | `gameEngine.ts` lines 477–487: `computeMovedPieceIds` only adds `pieceId` when `forceIncludeSelf` is true, not on bare `paceExhausted`; `applyFreeMove` (line 347–350) likewise defers; 2 BUG-14 tests in `gameEngine.phase8.test.ts`                                                                               |
| 13  | Undo works in MOVE entered via applyStartMovement (lastDiceRoll cleared) and across GK_KICK_MOVE, SNAPSHOT_DEFLECT, FREE_MOVE_ATTACK/DEFENSE, FREE_KICK_SETUP | VERIFIED  | `gameEngine.ts` line 274: `applyStartMovement` returns `lastDiceRoll: null`; `gameHandlers.ts` lines 1107–1116: `validUndoPhases` = 8 phases (excludes KICK_OFF_SETUP); `ActionPanel.tsx` lines 126–138: `moveTypeForPhase` mirrors server list; BUG-18 tests in `gameEngine.phase17.test.ts`                       |

**Score:** 12/13 truths verified (1 requires human confirmation — BUG-21)

---

## Required Artifacts

| Artifact                                              | Expected                                                                                                                             | Status   | Details                                                                                                                                                                                                                                                                            |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/client/src/components/ActionLog.tsx`        | pieceNum() store-lookup, PNamed in FTP_MOVE/HP_MOVE, isGoal removal                                                                  | VERIFIED | All three changes confirmed: `pieceNum()` uses `pieces.find` (line 129), HP_MOVE/FTP_MOVE use `<PNamed>` (lines 658/672), render span is unconditional `styles.content` (line 813)                                                                                                 |
| `packages/client/src/components/ActionLog.module.css` | .goalContent class removed                                                                                                           | VERIFIED | File contains only `.panel`, `.panelHeader`, `.entry`, `.prefix`, `.content`, `.empty` — no `.goalContent`                                                                                                                                                                         |
| `packages/client/src/components/PieceOverlay.tsx`     | playerNumber sourced from piece.number                                                                                               | VERIFIED | Line 83: `const playerNumber = String(piece.number);`                                                                                                                                                                                                                              |
| `packages/server/src/gameEngine.ts`                   | multi-tackle loop; free-move deferral; FTP toggle; offsidePieceIds; computeMovedPieceIds; lastDiceRoll resets; REPLAY_ELIGIBLE_TYPES | VERIFIED | All confirmed — BUG-13 loop (lines 692–773), BUG-20 deferral (lines 1143–1148), BUG-12 toggle (lines 76, 1703–1704), BUG-06 offside reset (lines 1964, 2051), BUG-14 lock deferral (lines 477–487), BUG-18 lastDiceRoll: null (line 274), BUG-17 REPLAY_ELIGIBLE_TYPES (line 4160) |
| `packages/server/src/gameHandlers.ts`                 | GAME_HEADER_CONTESTANT direct delivery; validUndoPhases extended; GAME_KICK_OFF_MOVE KICK_OFF_SETUP event                            | VERIFIED | BUG-07 direct delivery (lines 2558–2596), BUG-18 validUndoPhases (lines 1107–1116), BUG-17 KICK_OFF_SETUP push confirmed in SUMMARY-04 (GAME_KICK_OFF_MOVE)                                                                                                                        |
| `packages/shared/src/types.ts`                        | KICK_OFF_SETUP added to ActionEvent union; SNAP_DEFLECT_MOVE; FK_SETUP_MOVE                                                          | VERIFIED | Lines 85–92: three new types in `ActionEventType`; lines 321–350: corresponding `ActionEvent` members                                                                                                                                                                              |
| `packages/client/src/components/ActionPanel.tsx`      | moveTypeForPhase mapping extended                                                                                                    | VERIFIED | Lines 126–138: GK*KICK_MOVE→`GK_KICK_MOVE`, SNAPSHOT_DEFLECT→`SNAP_DEFLECT_MOVE`, FREE_KICK_SETUP→`FK_SETUP_MOVE`, MOVE/FREE_MOVE*\*→`MOVE`                                                                                                                                        |
| `packages/client/src/components/HexGrid.tsx`          | inspectPiece fallback for spent own-team pieces in MOVE                                                                              | VERIFIED | Lines 784–793: conditional fallback confirmed                                                                                                                                                                                                                                      |

---

## Key Link Verification

| From                                           | To                                         | Via                                                                               | Status | Details                                                                                   |
| ---------------------------------------------- | ------------------------------------------ | --------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------- |
| `ActionLog.tsx pieceNum()`                     | `useGameStore.getState().gameState.pieces` | `pieces.find((p) => p.id === pieceId)`                                            | WIRED  | Confirmed at lines 128–130                                                                |
| `GAME_HEADER_CONTESTANT winner branch`         | `PASS or GK_DIVE transition`               | `applyResolveHeaderTarget` with winner hex as target; `lastActionType: 'HEADER'`  | WIRED  | Confirmed at lines 2587–2591                                                              |
| `GAME_KICK_OFF_MOVE handler`                   | `eventLog / buildReplayFrames`             | `KICK_OFF_SETUP` ActionEvent pushed per reposition                                | WIRED  | Confirmed in SUMMARY-04 commit 10bcadc; `REPLAY_ELIGIBLE_TYPES` includes `KICK_OFF_SETUP` |
| `ActionPanel.tsx canUndo`                      | `server validUndoPhases`                   | `moveTypeForPhase` mapping mirrors extended phase list                            | WIRED  | `moveTypeForPhase` at lines 126–138 maps all 8 server-side phases                         |
| `HexGrid.tsx handleClick` spent-piece fallback | `inspectPiece(piece.id)`                   | `phase === 'MOVE' && piece.teamId === myTeam && movedPieceIds.includes(piece.id)` | WIRED  | Confirmed at lines 788–792                                                                |

---

## Behavioral Spot-Checks

| Behavior                                                                      | Command                                              | Result                                  | Status |
| ----------------------------------------------------------------------------- | ---------------------------------------------------- | --------------------------------------- | ------ |
| Server tests (BUG-13, BUG-20, BUG-06, BUG-07, BUG-12, BUG-17, BUG-18, BUG-14) | `pnpm --filter @counter-attack/server test -- --run` | 477 passed, 1 skipped, 1 todo           | PASS   |
| Client tests (BUG-15, BUG-16, BUG-19, BUG-10)                                 | `pnpm --filter @counter-attack/client test -- --run` | 240 passed                              | PASS   |
| Shared tests                                                                  | `pnpm --filter @counter-attack/shared test -- --run` | 320 passed                              | PASS   |
| Typecheck (all packages)                                                      | `pnpm -r typecheck`                                  | Clean — shared, client, server all pass | PASS   |

---

## Requirements Coverage

| Requirement | Source Plan | Description                                                                           | Status      | Evidence                                                                                                                                                                                             |
| ----------- | ----------- | ------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BUG-06      | 18.3-03     | offsidePieceIds reset on all dead-ball restarts                                       | SATISFIED   | `applyRoll` SHOT branch adds `offsidePieceIds: []` on GOAL; `gameHandlers.ts` GAME_READY transition adds it; `applyFreeKickReady` stage-3 already had it; regression test in `offside.test.ts`       |
| BUG-07      | 18.3-03     | Header duel win delivers directly — no target-selection sub-phase                     | SATISFIED   | `GAME_HEADER_CONTESTANT` winner branch calls `applyResolveHeaderTarget` immediately; integration test asserts phase ∈ {PASS, GK_DIVE, LOOSE_BALL}                                                    |
| BUG-10      | 18.3-05     | Clicking an already-moved own-team piece opens its player card                        | SATISFIED   | `HexGrid.tsx` lines 784–793; 4 tests in `HexGrid.test.tsx` pass. NOTE: REQUIREMENTS.md checkboxes and traceability table still show BUG-10 as "Pending" — documentation gap, not a functionality gap |
| BUG-12      | 18.3-03     | FTP_MOVE_ENABLED toggle defaults off, skipping FIRST_TIME_PASS_MOVE                   | SATISFIED   | `FTP_MOVE_ENABLED = false` at line 76; `applyRoll` FIRST_TIME_PASS branch gated by toggle; BUG-12 regression tests                                                                                   |
| BUG-13      | 18.3-02     | Second TACKLE_ATTEMPT fires for second adjacent defender when first fails             | SATISFIED   | Inline loop in `applyMove` (lines 692–773); test in `gameEngine.test.ts`                                                                                                                             |
| BUG-14      | 18.3-04     | Snapshot remains available after pace exhaustion until a different piece is activated | SATISFIED   | `computeMovedPieceIds` and `applyFreeMove` both defer to `abandonedIds`; 2 tests in `gameEngine.phase8.test.ts`                                                                                      |
| BUG-15      | 18.3-01     | GOAL/SHOT_ATTEMPT log entries render in standard gray                                 | SATISFIED   | No `isGoal: true` or `styles.goalContent` in `ActionLog.tsx`; `.goalContent` removed from CSS                                                                                                        |
| BUG-16      | 18.3-01     | FTP_MOVE and HP_MOVE log entries use PNamed (full name)                               | SATISFIED   | Both cases use `<PNamed pieceId={event.pieceId} />`                                                                                                                                                  |
| BUG-17      | 18.3-04     | KICK_OFF_SETUP repositioning is replay-visible                                        | SATISFIED   | `ActionEventType` includes `'KICK_OFF_SETUP'`; `REPLAY_ELIGIBLE_TYPES` includes it; BUG-17 replay integration test                                                                                   |
| BUG-18      | 18.3-04     | Undo works in MOVE and across 5 additional move-bearing phases                        | SATISFIED   | `applyStartMovement` clears `lastDiceRoll`; `validUndoPhases` extended to 8 phases; `moveTypeForPhase` client mirror extended                                                                        |
| BUG-19      | 18.3-01     | Jersey number sourced from piece.number everywhere                                    | SATISFIED   | `PieceOverlay.tsx` uses `String(piece.number)`; `ActionLog.tsx` `pieceNum()` uses store lookup                                                                                                       |
| BUG-20      | 18.3-02     | Free-move offer deferred past mid-MOVE-slot and mid-HEADER                            | SATISFIED   | `applyFreeMoveZoneCheck` early-return guards for MOVE+mid-slot and HEADER; 3 BUG-20 tests                                                                                                            |
| BUG-21      | 18.3-05     | SNAPSHOT_TARGET goal-line highlights visible to attacking player                      | NEEDS HUMAN | No code change made. PLAN-05 mandated live two-tab verification before any fix. SUMMARY-05 reports human verifier confirmed correct behavior. Independent verification still required.               |

### Requirements Coverage — Note on REQUIREMENTS.md Gaps

The REQUIREMENTS.md document was NOT updated to:

1. Check the `[ ]` box for BUG-10 (line 64) — should be `[x]`; code fix is in place
2. Mark the traceability entry for BUG-10 (line 126) as "Complete" — still shows "Pending"
3. BUG-21 (line 78) remains `[ ]` and "Pending" in traceability — this is intentional pending human re-confirmation, but the documentation should reflect the SUMMARY-05 outcome once confirmed

This is a documentation inconsistency, not a functional regression.

---

## Anti-Patterns Found

| File            | Line | Pattern                              | Severity | Impact                                                                                                                                                            |
| --------------- | ---- | ------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ActionLog.tsx` | 320  | `// D-13 (TODO-STEAL-DETAIL):` label | INFO     | Pre-existing (commit 76c45f8, Phase 18.2); not introduced by Phase 18.3; references design spec D-13 and has implemented code beneath it — not an unresolved stub |

No new debt markers or stubs introduced by Phase 18.3.

---

## Human Verification Required

### 1. BUG-21 — SNAPSHOT_TARGET Goal-Line Hex Highlights

**Test:** Start the dev servers (`pnpm --filter @counter-attack/server dev` and `pnpm --filter @counter-attack/client dev`). Open two browser tabs, create a room in tab 1, join with the code in tab 2. Play until the attacking team's ball carrier is inside the opponent's penalty area, then trigger Snapshot so the phase becomes `SNAPSHOT_TARGET`. In the attacking player's tab, verify that goal-line hexes for the opponent's goal are highlighted/tinted, indicating valid snapshot targets.

**Expected:** Goal-line hexes are visibly highlighted for the attacking player during `SNAPSHOT_TARGET`, enabling target selection.

**Why human:** No code change was made for BUG-21. PLAN-05 mandated live two-tab verification before authoring any fix, and SUMMARY-05 reports that a prior session (by the executor) confirmed correct behavior. An independent human re-run is required to close this bug with confidence since it is the only observable truth that cannot be verified programmatically.

---

## Gaps Summary

No functional gaps were found. All 12 of 13 verifiable truths are confirmed in the codebase by direct code inspection and a full test suite run (477 server + 240 client + 320 shared = 1,037 tests, all passing; typecheck clean).

The one unverified truth (BUG-21 goal-line highlight) cannot be verified programmatically because no code was changed — the claim is that it was already working. The PLAN design explicitly required a human-gating checkpoint (Task 2, `checkpoint:human-verify`) before authoring any fix, and the executor-reported live session confirmed the feature works. An independent human confirmation closes the loop.

**Documentation gap (not a blocker):** REQUIREMENTS.md was not updated to mark BUG-10 as complete. The code fix is fully in place (`HexGrid.tsx` lines 784–793) and covered by 4 passing tests, but the requirement checkbox and traceability table still show "Pending." This should be corrected in a housekeeping update.

---

_Verified: 2026-07-01T22:25:00Z_
_Verifier: Claude (gsd-verifier)_
