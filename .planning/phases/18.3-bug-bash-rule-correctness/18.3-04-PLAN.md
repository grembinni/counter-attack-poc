---
phase: 18.3-bug-bash-rule-correctness
plan: 04
type: execute
wave: 3
depends_on: ['18.3-03']
files_modified:
  - packages/shared/src/types.ts
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
  - packages/client/src/components/ActionPanel.tsx
files_touched_note: 'Depends on 18.3-03 to serialize gameEngine.ts edits (no same-wave overlap with 02/03). Ordered internally: BUG-17 (replay eventLog) MUST land before BUG-18 Undo coverage (Pitfall 2 — Undo scans eventLog). BUG-14 and BUG-18 touch adjacent computeMovedPieceIds/applyStartMovement sections — batched here.'
autonomous: true
requirements: [BUG-14, BUG-17, BUG-18]
must_haves:
  truths:
    - 'KICK_OFF_SETUP repositioning moves appear in the eventLog and are reconstructed by buildReplayFrames, so post-goal formation resets are visible in replay'
    - 'Snapshot stays available to the ball carrier in MOVE after their pace is exhausted, as long as no different piece has been activated yet'
    - 'Undo works in the MOVE phase entered via applyStartMovement (lastDiceRoll no longer blocks it) and across all move-bearing phases (GK_KICK_MOVE, SNAPSHOT_DEFLECT, FREE_MOVE_ATTACK/DEFENSE, FREE_KICK_SETUP)'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'KICK_OFF_SETUP added to ActionEvent union'
      contains: 'KICK_OFF_SETUP'
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'computeMovedPieceIds defers paceExhausted lock; applyStartMovement clears lastDiceRoll; REPLAY_ELIGIBLE_TYPES includes KICK_OFF_SETUP'
      contains: 'lastDiceRoll: null'
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'GAME_KICK_OFF_MOVE logs KICK_OFF_SETUP event; validUndoPhases extended'
      contains: 'validUndoPhases'
  key_links:
    - from: 'GAME_KICK_OFF_MOVE handler'
      to: 'eventLog / buildReplayFrames'
      via: 'push KICK_OFF_SETUP ActionEvent per reposition'
      pattern: 'KICK_OFF_SETUP'
    - from: 'ActionPanel.tsx canUndo'
      to: 'server validUndoPhases'
      via: 'moveTypeForPhase mapping mirrors the extended phase list'
      pattern: 'moveTypeForPhase'
---

<objective>
Fix three interlocking movement-phase state-machine bugs in `gameEngine.ts`/`gameHandlers.ts`. Depends on plan 18.3-03 to serialize `gameEngine.ts` edits. Ordered so BUG-17 lands before BUG-18 (Pitfall 2: Undo scans eventLog, so phases wired for Undo must first produce reversible eventLog entries).
- BUG-17: give `buildReplayFrames` visibility into KICK_OFF_SETUP repositioning by adding a `KICK_OFF_SETUP` ActionEvent type (mirroring MOVE's shape), pushing one per reposition in `GAME_KICK_OFF_MOVE`, and adding it to `REPLAY_ELIGIBLE_TYPES`.
- BUG-14: stop `computeMovedPieceIds` (and the FREE_MOVE equivalent) from locking the carrier into `movedPieceIds` purely because their own move step exhausted pace — defer to the existing `abandonedIds` mechanism (lock only when a DIFFERENT piece is activated). This keeps Snapshot available while the exhausted-pace carrier remains selected.
- BUG-18: (1) add `lastDiceRoll: null` to `applyStartMovement`'s return (the MOVE-entry regression); (2) extend `validUndoPhases` to GK_KICK_MOVE, SNAPSHOT_DEFLECT, FREE_MOVE_ATTACK, FREE_MOVE_DEFENSE, FREE_KICK_SETUP, auditing each new phase's transition for the same missing `lastDiceRoll: null` reset; mirror the coverage in the client `canUndo`.

Purpose: BUG-14/17/18 all touch adjacent movement-state sections of `gameEngine.ts` and have a hard ordering dependency (BUG-17 → BUG-18). Batching + ordering here prevents edit conflicts and broken Undo.
Output: Replay-visible kick-off setup, pace-exhaustion-tolerant Snapshot, and consistent Undo across all move-bearing phases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-CONTEXT.md
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-RESEARCH.md
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUG-17 — KICK_OFF_SETUP ActionEvent + replay visibility</name>
  <files>packages/shared/src/types.ts, packages/server/src/gameHandlers.ts, packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/shared/src/types.ts (`ActionEventType` union ~56-85 — confirm `KICK_OFF_SETUP` is NOT present as an event type though it exists as a GamePhase literal ~317; the `ActionEvent` discriminated union ~91 and the shape of the `MOVE` member with `pieceId`/`from`/`to`/`timestamp`)
    - packages/server/src/gameHandlers.ts (`GAME_KICK_OFF_MOVE` handler ~1650-1714 — currently mutates `room.gameState.pieces` and calls `broadcastState` with NO eventLog push; the D-23 "no pace/ZoI limits" free-repositioning behavior that must be preserved)
    - packages/server/src/gameEngine.ts (`REPLAY_ELIGIBLE_TYPES` ~3899; `buildReplayFrames` ~3944 — how it walks `finalState.eventLog` and repositions pieces from MOVE-shaped event data; the MOVE event-shape template ~473-482)
  </read_first>
  <action>
    Add `'KICK_OFF_SETUP'` to the `ActionEventType` union in `types.ts` and add a corresponding member to the `ActionEvent` discriminated union mirroring the `MOVE` shape: `{ type: 'KICK_OFF_SETUP'; pieceId: string; from: HexCoord; to: HexCoord; timestamp: number }` (no `slot`/`ballAfter` needed — kick-off setup does not move the ball). In `GAME_KICK_OFF_MOVE`, after computing `newPieces`, append one `KICK_OFF_SETUP` event to `eventLog` recording the repositioned piece's `from` (its position before the move) and `to`, alongside the existing `pieces: newPieces` spread — do this in the same immutable `room.gameState = { ... }` assignment so the event and the position change commit together. Add `'KICK_OFF_SETUP'` to `REPLAY_ELIGIBLE_TYPES`. Confirm `buildReplayFrames` handles it via the same MOVE-style reposition path (it iterates eligible events and applies `from→to`); add an explicit branch only if the generic handler does not already reposition from a `pieceId`/`to` pair. Preserve D-23 free-repositioning (no pace/ZoI checks added).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- --run -t "replay"</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` `ActionEventType` contains `'KICK_OFF_SETUP'` and the `ActionEvent` union has a `KICK_OFF_SETUP` member with `pieceId`, `from`, `to`, `timestamp`
    - `GAME_KICK_OFF_MOVE` pushes a `KICK_OFF_SETUP` event to `eventLog` on each reposition, in the same state assignment as the piece move
    - `REPLAY_ELIGIBLE_TYPES` includes `'KICK_OFF_SETUP'`
    - A test asserts `buildReplayFrames` produces frames for KICK_OFF_SETUP repositioning moves
    - `pnpm --filter @counter-attack/server test -- --run -t "replay"` exits 0 and `pnpm -r tsc --noEmit` clean (exhaustive ActionEvent switches updated)
  </acceptance_criteria>
  <done>Kick-off formation resets after a goal are replay-visible; the new event type follows the MOVE shape.</done>
</task>

<task type="auto">
  <name>Task 2: BUG-14 — defer paceExhausted lock to the abandonedIds mechanism</name>
  <files>packages/server/src/gameEngine.ts, packages/client/src/components/ActionPanel.tsx</files>
  <read_first>
    - packages/server/src/gameEngine.ts (`computeMovedPieceIds` ~458-463 — the `if (paceExhausted || forceIncludeSelf) ids.add(pieceId)` line; the `abandonedIds` deferral pattern ~453-457; the FREE_MOVE equivalent in `applyFreeMove` ~329-331 with `if (paceExhausted) newMovedPieceIds.add(pieceId)`)
    - packages/client/src/components/ActionPanel.tsx (`canSnapshot` ~613-619 — reads `!movedPieceIds.includes(carrierId)`, no change needed once engine defers; the `movedPieceIds.length` countdown display, Pitfall 4)
  </read_first>
  <action>
    In `computeMovedPieceIds` (~line 458), change the condition so the carrier is added to `movedPieceIds` ONLY when `forceIncludeSelf` is true — remove `paceExhausted` from that add condition. The `abandonedIds` sweep (already merged into `ids` earlier in the function) is the sole mechanism that locks a pace-exhausted piece, and only once a DIFFERENT piece is activated (`isNewActivation` for someone else). Apply the identical change in the FREE_MOVE path (`applyFreeMove` ~line 331): remove the `if (paceExhausted) newMovedPieceIds.add(pieceId)` unconditional lock, relying on `abandonedIds` there too. Do NOT change `canSnapshot` in `ActionPanel.tsx` — verify only (Pitfall 4) that the `movedPieceIds.length` countdown still reflects actually-locked pieces and not pace-exhausted-but-still-selected ones; if the countdown reads wrong, note it in the SUMMARY (do not expand scope). The `validateMove` slot-quota guard (`activatedCount >= slotQuota`) still blocks over-activation independently, so removing the eager lock does not allow extra pieces.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- --run -t "snapshot"</automated>
  </verify>
  <acceptance_criteria>
    - `computeMovedPieceIds` adds `pieceId` only when `forceIncludeSelf` is true (no longer on bare `paceExhausted`)
    - `applyFreeMove` no longer adds the moving piece to `movedPieceIds` purely on `paceExhausted`
    - A test proves Snapshot stays available (`canSnapshot`-relevant: carrier NOT in `movedPieceIds`) after the carrier exhausts pace but before a different piece is activated, and becomes unavailable once a different piece is activated (abandonedIds path)
    - `pnpm --filter @counter-attack/server test -- --run -t "snapshot"` exits 0 and existing MOVE slot-quota tests remain green
  </acceptance_criteria>
  <done>Snapshot remains available to the exhausted-pace carrier until a different piece is activated; slot quotas still enforced.</done>
</task>

<task type="auto">
  <name>Task 3: BUG-18 — fix lastDiceRoll MOVE regression and extend Undo coverage</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/gameHandlers.ts, packages/client/src/components/ActionPanel.tsx</files>
  <read_first>
    - packages/server/src/gameEngine.ts (`applyStartMovement` ~224-262 — the return object missing `lastDiceRoll: null`; the GK_RESTART 'movement' branch ~2470 that already sets `lastDiceRoll: null` as the reference; every function that returns `phase: '<GK_KICK_MOVE|SNAPSHOT_DEFLECT|FREE_MOVE_ATTACK|FREE_MOVE_DEFENSE|FREE_KICK_SETUP>'` to audit for the same omission)
    - packages/server/src/gameHandlers.ts (`validUndoPhases` ~1094 — currently `['MOVE', 'HIGH_PASS_MOVE', 'FIRST_TIME_PASS_MOVE']`; the `applyUndo` scan-and-revert flow it guards)
    - packages/client/src/components/ActionPanel.tsx (`canUndo` ~113-132 — `if (lastDiceRoll) return false;` at line 114; the `moveTypeForPhase` mapping ~125-131 that must gain entries for each newly-covered phase's corresponding event type)
    - packages/shared/src/types.ts (`ActionEventType` — confirm the event type each new phase's moves log: GK_KICK_MOVE→`GK_KICK_MOVE`, SNAPSHOT_DEFLECT→(the deflect move event), FREE_MOVE_*→(the free-move event), FREE_KICK_SETUP→(its reposition event) so `moveTypeForPhase` maps correctly)
  </read_first>
  <action>
    Fix 1 (regression): add `lastDiceRoll: null` to `applyStartMovement`'s returned state (~line 241-261), mirroring the GK_RESTART branch at ~2470. This unblocks `canUndo` for the dominant MOVE-entry path.

    Fix 2 (coverage): extend `validUndoPhases` in `gameHandlers.ts` to `['MOVE', 'HIGH_PASS_MOVE', 'FIRST_TIME_PASS_MOVE', 'GK_KICK_MOVE', 'SNAPSHOT_DEFLECT', 'FREE_MOVE_ATTACK', 'FREE_MOVE_DEFENSE', 'FREE_KICK_SETUP']`. Do NOT add `KICK_OFF_SETUP` here (its Undo is out of scope this phase even though BUG-17 now logs its events). For EACH newly-added phase, grep every `gameEngine.ts` return that transitions into it and confirm it sets `lastDiceRoll: null`; add the reset wherever missing (Pitfall 1). Confirm each newly-covered phase produces a reversible `eventLog` entry for its moves (Pitfall 2) — if any of these five phases share BUG-17's "no eventLog entry" gap, fix that logging as a prerequisite (document in SUMMARY); the five listed phases are expected to already log their moves, but verify before assuming Undo "just works."

    Fix 3 (client mirror): extend `ActionPanel.tsx`'s `moveTypeForPhase` mapping so each newly-covered phase maps to its corresponding move event type (so the eventLog scan at line ~131 finds the right events), and ensure the Undo control renders in those phases. Keep the `if (lastDiceRoll) return false;` guard — it is correct once the engine clears `lastDiceRoll` on entry.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- --run -t "undo"</automated>
  </verify>
  <acceptance_criteria>
    - `applyStartMovement` return object contains `lastDiceRoll: null`
    - `validUndoPhases` equals `['MOVE', 'HIGH_PASS_MOVE', 'FIRST_TIME_PASS_MOVE', 'GK_KICK_MOVE', 'SNAPSHOT_DEFLECT', 'FREE_MOVE_ATTACK', 'FREE_MOVE_DEFENSE', 'FREE_KICK_SETUP']` (no `KICK_OFF_SETUP`)
    - Every `gameEngine.ts` transition into the five newly-covered phases sets `lastDiceRoll: null` (audited; SUMMARY lists the audited sites)
    - `ActionPanel.tsx` `moveTypeForPhase` has an entry for each newly-covered phase mapping to its logged move event type
    - A test proves Undo is enabled in MOVE entered via `applyStartMovement` (previously blocked by stale `lastDiceRoll`)
    - `pnpm --filter @counter-attack/server test -- --run -t "undo"` exits 0
  </acceptance_criteria>
  <done>Undo works in MOVE after applyStartMovement and across all five newly-covered move-bearing phases; client canUndo mirrors the server list.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols created by this phase:

- `types.ts`: `'KICK_OFF_SETUP'` added to the `ActionEventType` union; a new `KICK_OFF_SETUP` member (`{ type; pieceId; from; to; timestamp }`) in the `ActionEvent` discriminated union
  Modified (not new): `gameEngine.ts` `computeMovedPieceIds`/`applyFreeMove` (paceExhausted lock deferred), `applyStartMovement` (adds `lastDiceRoll: null`), `REPLAY_ELIGIBLE_TYPES` (adds `KICK_OFF_SETUP`), phase-transition returns (audited `lastDiceRoll: null`); `gameHandlers.ts` `GAME_KICK_OFF_MOVE` (event push), `validUndoPhases` (extended); `ActionPanel.tsx` `moveTypeForPhase` (extended). No new files.
  </artifacts_this_phase_produces>

<verification>
- `pnpm -r test -- --run` full suite green
- `pnpm -r tsc --noEmit` clean typecheck (exhaustive ActionEvent switches updated for KICK_OFF_SETUP)
- BUG-17: KICK_OFF_SETUP replay-visible; BUG-14: Snapshot survives pace exhaustion; BUG-18: Undo works in MOVE + 5 new phases
</verification>

<success_criteria>
BUG-14, BUG-17, and BUG-18 are fixed and covered by regression tests; the full test suite and typecheck pass. BUG-17 lands before BUG-18's Undo coverage so every Undo-enabled phase has reversible eventLog entries.
</success_criteria>

<output>
Create `.planning/phases/18.3-bug-bash-rule-correctness/18.3-04-SUMMARY.md` when done
</output>
