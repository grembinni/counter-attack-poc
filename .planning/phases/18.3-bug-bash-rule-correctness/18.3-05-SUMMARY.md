---
phase: 18.3-bug-bash-rule-correctness
plan: '05'
subsystem: ui
tags: [react, hexgrid, snapshot, bug-bash]

# Dependency graph
requires:
  - phase: 18.3-bug-bash-rule-correctness
    provides: bug context and patterns for BUG-10 and BUG-21
provides:
  - BUG-10 fix: click-to-inspect fallback for already-moved own-team pieces in MOVE phase
  - BUG-21 verification: SNAPSHOT_TARGET goal-line highlight confirmed working in live session — no code change required
affects: [18.3-03-PLAN, 18.3-04-PLAN]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'spent-piece click fallback: MOVE + myTeam + movedPieceIds.includes(id) → inspectPiece(piece.id) rather than () => undefined'

key-files:
  created: []
  modified:
    - packages/client/src/components/HexGrid.tsx
    - packages/client/src/components/__tests__/HexGrid.test.tsx

key-decisions:
  - 'BUG-21: live two-tab verification confirmed SNAPSHOT_TARGET goal-line hexes highlight correctly in current build — no code change made; marked resolved'
  - 'BUG-10: spent-piece fallback inserted after all positive-selection cases in handleClick to avoid interfering with canSelect / move-target highlighting'

patterns-established: []

requirements-completed: [BUG-10, BUG-21]

# Metrics
duration: ~20min (continuation — Task 1 committed in prior session, Task 2 human-verify in live session, Task 3 documentation only)
completed: 2026-07-01
---

# Phase 18.3 Plan 05: HexGrid Bug-Bash (BUG-10 + BUG-21) Summary

**BUG-10 fixed via spent-piece inspect fallback in HexGrid.tsx handleClick; BUG-21 confirmed resolved in live two-tab session with no code change needed**

## Performance

- **Duration:** ~20 min (continuation agent; Task 1 committed in prior session as 00decb8)
- **Started:** Prior session (Task 1); resumed 2026-07-01
- **Completed:** 2026-07-01
- **Tasks:** 3 (Task 1 code fix, Task 2 human-verify checkpoint, Task 3 documentation close)
- **Files modified:** 2 (HexGrid.tsx, HexGrid.test.tsx)

## Accomplishments

- BUG-10: clicking an already-moved own-team piece in MOVE now opens its player card via `inspectPiece(piece.id)` without re-triggering move-target highlighting; spent pieces remain excluded from `canSelect`/highlight computation
- BUG-21: live two-tab verification (human verifier) confirmed goal-line hexes highlight correctly during SNAPSHOT_TARGET for the attacking player; no client or server code change was needed
- New HexGrid test asserts the spent-piece inspect fallback calls `inspectPiece` with the correct piece id and does NOT call `selectPiece`

## Task Commits

1. **Task 1: BUG-10 — click-to-inspect fallback for spent pieces in MOVE** - `00decb8` (fix)
2. **Task 2: BUG-21 — live two-tab verification** — human-verify checkpoint; no commit (no code change)
3. **Task 3: documentation close** — covered by plan metadata commit

## Files Created/Modified

- `packages/client/src/components/HexGrid.tsx` — BUG-10: final `handleClick` fallback replaced `() => undefined` with conditional `() => inspectPiece(piece.id)` when `phase === 'MOVE' && piece.teamId === myTeam && movedPieceIds.includes(piece.id)`, else `() => undefined`
- `packages/client/src/components/__tests__/HexGrid.test.tsx` — new test: clicking a moved own-team piece in MOVE calls `inspectPiece` with that piece id and does NOT call `selectPiece`

## Decisions Made

- **BUG-21 no-change decision:** The plan explicitly required live verification before authoring a fix (CONTEXT.md forbids guess-fixing). The human verifier ran a two-tab live session and confirmed goal-line hexes highlight as expected during SNAPSHOT_TARGET. Root cause analysis found no defect in the current build. BUG-21 is closed as "resolved in prior work — no further code change required."
- **BUG-10 ordering:** The spent-piece inspect fallback is placed as the last clause in the `handleClick` chain (after all positive-selection cases), ensuring it only fires when the piece is otherwise non-clickable.

## Deviations from Plan

### BUG-21 — No Code Change (Behavior Already Correct)

The plan anticipated that Task 2's live verification would identify a root cause (one of five candidates: `isActivePlayer` false, `snapCarrier` null, wrong `goalLineHexSet` side, CSS/z-order suppression, or phase-name mismatch), and Task 3 would apply the targeted fix.

The human verifier's live session found none of these candidates — goal-line hexes highlight correctly in the current build. Task 3 therefore produced no code change: the SUMMARY documents the verification outcome and closes BUG-21 as already resolved.

This is within plan scope: the plan's Task 3 action explicitly states "if Task 2 revealed a SERVER-side cause … document … and surface to the orchestrator" — the analogous path here is "behavior already correct, no fix needed, document in SUMMARY." No deviation rule needed; the plan already anticipated a no-op outcome.

**Total deviations:** None — plan executed correctly accounting for the live-verify outcome.

## Issues Encountered

None. The human-verify checkpoint returned a clean result (bug already resolved), simplifying Task 3 to a documentation-only step.

## Known Stubs

None.

## Threat Flags

None — this plan only modifies client-side click-handler and test files with no new network endpoints, auth paths, or schema changes.

## Next Phase Readiness

- Plan 18.3-05 is complete. BUG-10 and BUG-21 are both closed.
- Phase 18.3 has 3 remaining open plans: 18.3-03 (BUG-06 offside reset, BUG-07 header delivery, BUG-12 FTP toggle), 18.3-04 (BUG-14/17/18 snapshot/undo/replay), and the Wave 2/3 sequencing can proceed.
- No blockers introduced by this plan.

---

_Phase: 18.3-bug-bash-rule-correctness_
_Completed: 2026-07-01_
