---
phase: 18.3-bug-bash-rule-correctness
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/HexGrid.tsx
autonomous: false
requirements: [BUG-10, BUG-21]
must_haves:
  truths:
    - "Clicking an already-moved (spent) own-team piece in MOVE opens that piece's player card, without re-triggering move-target highlighting"
    - 'During SNAPSHOT_TARGET, the goal-line target hexes are visible to the attacking player so they can select a target'
  artifacts:
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: 'click-to-inspect fallback for spent pieces (BUG-10); BUG-21 highlight fix (root cause TBD from live verification)'
      contains: 'inspectPiece'
  key_links:
    - from: 'HexGrid.tsx handleClick (spent-piece fallback)'
      to: 'inspectPiece(piece.id)'
      via: 'replace () => undefined with conditional inspect for MOVE + myTeam + movedPieceIds'
      pattern: 'inspectPiece'
---

<objective>
Fix two client-rendering bugs isolated to `HexGrid.tsx`:
- BUG-10: clicking an already-moved (`movedPieceIds.includes(id)`) own-team piece in MOVE must open its player card via `inspectPiece` — same as unmoved pieces — but must NOT re-trigger move-target highlighting (the `canSelect` guard already excludes moved pieces).
- BUG-21 (investigation-first): the SNAPSHOT_TARGET goal-line highlights reportedly do not display, blocking target selection. Code inspection found no obvious gap, so this bug is scoped as: (1) a live two-tab verification to identify which layer fails (computation vs. rendering), gated by a human-verify checkpoint, then (2) the targeted fix once the root cause is confirmed.

Purpose: Both bugs touch only `HexGrid.tsx`, keeping them isolated from the engine plans. BUG-21's fix cannot be guess-written — CONTEXT.md forbids guess-fixing without live confirmation — so it carries a blocking human-verify checkpoint.
Output: Click-to-card on spent pieces; a confirmed root cause + fix for the Snapshot goal-line highlight.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-CONTEXT.md
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-RESEARCH.md
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUG-10 — click-to-inspect fallback for spent pieces in MOVE</name>
  <files>packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (the `handleClick` chain ~754-784 — the final `() => undefined` fallback for spent pieces at ~780-784; the `canSelect` guard ~632 that excludes `movedPieceIds`; the `onInspect={() => inspectPiece(piece.id)}` wiring ~792; the `inspectPiece` store selector ~69; how `phase`, `myTeam`, and `movedPieceIds` are read in this component)
  </read_first>
  <action>
    In the `handleClick` chain, replace the final `() => undefined` fallback with a conditional: when `phase === 'MOVE'` and `piece.teamId === myTeam` and `movedPieceIds.includes(piece.id)`, return `() => inspectPiece(piece.id)`; otherwise keep `() => undefined`. This adds an inspect-only path for spent own-team pieces while leaving the `canSelect` guard intact (no move-target highlighting is triggered — the piece is still excluded from selection). Do not alter `canSelect`, `onInspect`, or any highlight computation. Confirm the ordering: this fallback must come AFTER all positive selection cases so it only catches pieces that were otherwise non-clickable.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- --run -t "HexGrid"</automated>
  </verify>
  <acceptance_criteria>
    - `HexGrid.tsx` `handleClick` final fallback returns `() => inspectPiece(piece.id)` when `phase === 'MOVE' && piece.teamId === myTeam && movedPieceIds.includes(piece.id)`, else `() => undefined`
    - `canSelect` and move-target highlighting logic are unchanged (spent pieces still excluded from selection/highlighting)
    - A test asserts clicking a moved own-team piece in MOVE calls `inspectPiece` with that piece id and does NOT call `selectPiece`
    - `pnpm --filter @counter-attack/client test -- --run -t "HexGrid"` exits 0
  </acceptance_criteria>
  <done>Clicking a spent own-team piece opens its player card; unmoved-piece behavior and move highlighting are unaffected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BUG-21 — live two-tab verification of Snapshot goal-line highlight</name>
  <files>packages/client/src/components/HexGrid.tsx (inspection only — no edit in this task)</files>
  <action>
    This is an investigation checkpoint, not a code-change task. Do NOT modify any file here. Run the live two-tab session described in `<how-to-verify>`, inspect the SNAPSHOT_TARGET highlight state via React DevTools, and confirm which layer (computation vs. rendering) and which of the five candidate causes is responsible. Pause for the human verifier; the confirmed root cause becomes the input to Task 3's targeted fix. CONTEXT.md forbids guess-fixing BUG-21 before this confirmation.
  </action>
  <what-built>
    Nothing new built yet — this is an investigation gate. Code inspection (RESEARCH.md) confirmed `snapCarrier` (~line 318), `goalLineHexSet` (~line 132, keyed on `attackingTeam`), and `isShootingModeGoalHex` (~lines 325-334) appear correct on paper, and the server `GAME_SNAPSHOT` handler already gates on `isActivePlayer` (~gameHandlers.ts:2103). The bug must be reproduced live to determine whether the failing layer is computation (a boolean resolves false) or rendering (boolean true but tint suppressed by highlight-priority/CSS/z-order at HexGrid `isHighlighted` ~lines 360-368).
  </what-built>
  <how-to-verify>
    1. Start the dev servers: `pnpm --filter @counter-attack/server dev` and `pnpm --filter @counter-attack/client dev`.
    2. Open two browser tabs, create a room in tab 1, join with the code in tab 2 (two players).
    3. Play until the attacking team's ball carrier is inside the opponent's penalty area, then trigger Snapshot so the phase becomes `SNAPSHOT_TARGET`.
    4. In the attacking player's tab, open React DevTools and inspect the `HexGrid` render for a goal-line hex: record whether `isActivePlayer` is true, whether `snapCarrier` is non-null, whether `goalLineHexSet` contains the goal-line hex ids for the correct side, and whether `isShootingModeGoalHex` computes true.
    5. If the booleans are all true but no tint is visible, inspect the DOM/CSS to see if another overlay layer or highlight-priority ternary (`isHighlighted` combination ~360-368) is suppressing the goal tint (rendering-layer failure).
    6. Report which of the five candidate causes matches: (a) `isActivePlayer` false, (b) `snapCarrier` null, (c) wrong `attackingTeam`/`goalLineHexSet` side, (d) CSS/z-order suppression, (e) client/server phase-name mismatch — OR a different cause.
  </how-to-verify>
  <verify>
    <human-check>Human confirms the root cause of the missing SNAPSHOT_TARGET goal-line highlight (which layer + which candidate) via the live two-tab session before Task 3 authors the fix.</human-check>
  </verify>
  <resume-signal>Report the confirmed root cause (which layer/candidate), then type "proceed" to author the targeted fix, or describe what you observed if it doesn't match any candidate.</resume-signal>
  <done>The failing layer (computation vs. rendering) and the specific root cause of the missing SNAPSHOT_TARGET goal-line highlight are confirmed and recorded, ready to drive Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: BUG-21 — apply the targeted fix for the confirmed root cause</name>
  <files>packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (the specific lines identified during Task 2's live verification: `snapCarrier` ~318, `goalLineHexSet` ~130-132, `isShootingModeGoalHex` ~325-334, `isHighlighted` ~360-368, the highlight-priority ternary that assigns tint ~402/459)
    - packages/server/src/gameHandlers.ts (`GAME_SNAPSHOT` handler `isActivePlayer` gate ~2103 — only if Task 2 identified a server-side/computation cause; note server files are owned by other plans, so if the root cause is server-side, flag it for coordination rather than editing here)
  </read_first>
  <action>
    Apply the minimal targeted fix for the root cause confirmed in Task 2. If the failure is in the CLIENT rendering/computation layer (the expected case per the file-ownership scope of this plan — `HexGrid.tsx`), fix it there: e.g. correct the `goalLineHexSet` side keying, the `isShootingModeGoalHex` boolean composition, or the highlight-priority ternary so the goal-line tint is actually emitted during SNAPSHOT_TARGET. Do NOT guess — implement only the fix matching the confirmed cause. If Task 2 revealed a SERVER-side cause (e.g. `isActivePlayer`/`attackingTeam` wrong for the attacking team during SNAPSHOT_TARGET, or a phase-name mismatch originating server-side), STOP: this plan owns only `HexGrid.tsx`; document the server-side root cause in the SUMMARY and surface it to the orchestrator as a gap requiring a server plan, rather than editing server files here. Add or extend a `HexGrid.test.tsx` case covering `SNAPSHOT_TARGET` goal-line highlight visibility to lock the fix (if the cause was client-side computation).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- --run -t "HexGrid"</automated>
  </verify>
  <acceptance_criteria>
    - The fix directly addresses the root cause confirmed in Task 2 (SUMMARY states the cause and the change)
    - If client-side: a `HexGrid.test.tsx` case asserts the goal-line hexes are highlighted/marked during `SNAPSHOT_TARGET` for the attacking player
    - If server-side: no server files edited by this plan; the SUMMARY documents the server root cause and flags it to the orchestrator
    - `pnpm --filter @counter-attack/client test -- --run -t "HexGrid"` exits 0
  </acceptance_criteria>
  <done>The Snapshot goal-line highlight is visible during SNAPSHOT_TARGET (or the server-side root cause is documented and escalated).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new exported symbols or files. This plan modifies existing code:

- `HexGrid.tsx`: `handleClick` spent-piece fallback (BUG-10); the SNAPSHOT_TARGET highlight computation/rendering (BUG-21, exact edit determined by Task 2's live verification)
- `HexGrid.test.tsx`: new/extended test cases (test file, not a production symbol)
  No new types, no new store actions (`inspectPiece` already exists).
  </artifacts_this_phase_produces>

<verification>
- `pnpm -r test -- --run` full suite green
- `pnpm -r tsc --noEmit` clean typecheck
- BUG-10: spent-piece click opens the card without highlighting; BUG-21: confirmed root cause fixed (client) or escalated (server)
</verification>

<success_criteria>
BUG-10 is fixed and covered by a HexGrid test. BUG-21 is either fixed (client-side root cause, with a regression test) or its confirmed server-side root cause is documented and escalated to the orchestrator for a server plan.
</success_criteria>

<output>
Create `.planning/phases/18.3-bug-bash-rule-correctness/18.3-05-SUMMARY.md` when done
</output>
