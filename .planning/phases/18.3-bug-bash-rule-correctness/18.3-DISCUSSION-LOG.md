# Phase 18.3: Bug-Bash (Rule Correctness) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-22
**Phase:** 18.3-bug-bash-rule-correctness
**Areas discussed:** Update existing context — Pending todo fold-in, Snapshot/pace fix, goal-log color, Undo scope

---

## Existing Context Disposition

**User's choice:** Update it (vs. View it / Skip)
**Notes:** CONTEXT.md already existed from the original 2026-06-20 Phase 18 split (BUG-06/07/10 only). User chose to run a fresh discuss-phase session to add to it rather than leave it as-is.

---

## Pending Todo Fold-In

| Todo                                                                                                               | Score                                        | Selected            |
| ------------------------------------------------------------------------------------------------------------------ | -------------------------------------------- | ------------------- |
| Add toggle to skip move phase after first-time pass (tagged `resolves_phase: "18.3"`)                              | 0.6                                          | ✓ (BUG-12)          |
| Bug - double tackle attempt when attacker enters hex challenged by two defenders (tagged `resolves_phase: "18.3"`) | 0.4                                          | ✓ (BUG-13)          |
| Bug - GK_KICK ball delivery invisible during post-game replay (REPLAY-06 gap)                                      | 0.6 (keyword match only, not tagged to 18.3) | ✗ (left as backlog) |

**User's choice:** Folded the 2 tagged todos (BUG-12, BUG-13). Also used the "Other" freeform option to surface 5 brand-new, previously untracked bugs in the same response:

1. Snapshot should remain an option when the carrier hits max pace, if still in range
2. Shot/goal log color format — content text renders in amber instead of white like other log lines
3. First-time-pass move log doesn't use the correct player-name format and retains a stray "D" role-letter prefix
4. Replay needs all player positions at kickoff after a goal, to show the "table reset"
5. Undo is broken; needs to consider replay impacts; needs to apply to all move actions

**Notes:** All 5 were investigated against the live codebase (not taken at face value) before being locked into CONTEXT.md as BUG-14 through BUG-18. REQUIREMENTS.md and ROADMAP.md were updated with a new "Bug Bash Addendum (Phase 18.3)" section, following the same mid-discussion addendum precedent already used for OFFSIDE-01/02 (Phase 17) and BUG-06..11/UX-07..14 (Phase 18).

---

## BUG-14: Snapshot After Pace Exhaustion

| Option                                  | Description                                                                              | Selected |
| --------------------------------------- | ---------------------------------------------------------------------------------------- | -------- |
| Drop the `movedPieceIds` check entirely | Snapshot stays available all MOVE phase as long as carrier is in the penalty-area region |          |
| Allow only once pace is fully exhausted | Snapshot unavailable while carrier still has pace, available once pace hits 0            |          |

**User's choice:** Neither preset option — user gave a third, more precise design: keep the `movedPieceIds` check as the gating mechanism, but change WHEN the carrier is added to `movedPieceIds`. Don't flag the carrier as "activated" (added to `movedPieceIds`) purely because they exhausted pace on their own move step — defer that addition until the player selects and moves a _different_ piece next, mirroring the existing "abandoned piece" deferral pattern already used for pieces that stop moving without exhausting pace.
**Notes:** Investigated `gameEngine.ts`'s `computeMovedPieceIds`/`abandonedIds` mechanism (~lines 329-331, 453-463) to confirm this deferral pattern already exists for the "didn't use full pace, player moved on" case — it just doesn't yet apply to the "did use full pace" case. The fix closes that one remaining asymmetry.

---

## BUG-15: Goal Log Color

| Option                           | Description                                                                         | Selected |
| -------------------------------- | ----------------------------------------------------------------------------------- | -------- |
| Remove `isGoal` styling entirely | GOAL/SHOT_ATTEMPT(GOAL) content uses the same `.content` class as every other event | ✓        |
| Keep distinct but not colored    | Content stays gray but remains visually distinct via weight/style                   |          |

**User's choice:** Remove `isGoal` styling entirely.
**Notes:** Confirmed `.goalContent` (`#e8a020` amber) vs `.content` (`#e0e0e0` gray) in `ActionLog.module.css`, and the `isGoal` flag set at 3 sites in `ActionLog.tsx` (GOAL case, 2 SHOT_ATTEMPT branches).

---

## BUG-16: FTP/HP Move Log Player Format

**User's choice (implicit — only FTP_MOVE was reported):** Fix the player-name format to use the established `PNamed` convention instead of the bare `P` (role-letter + number, no name) component.
**Notes:** Investigation found `HP_MOVE` has the byte-identical defect pattern as `FTP_MOVE` — both were missed by the `260621-hnd` commit's A/D role-letter-prefix sweep. Both are fixed together per the existing "fix inconsistencies found during the sweep inline" precedent (Phase 18 D-04) — not treated as new scope. `GK_KICK`/`GK_KICK_MOVE`'s `P` usage with `"GK"`/`"K"`/`"O"` prefixes was explicitly NOT touched (legitimate role labels, not the redundant A/D pattern, not flagged by the user).

---

## BUG-17: Kickoff-Setup Replay Visibility

**User's choice:** Replay needs to show all player positions resetting at kickoff after a score.
**Notes:** Root-caused to `GAME_KICK_OFF_MOVE` (`gameHandlers.ts`) pushing zero `ActionEvent`s to `eventLog` — confirmed no `KICK_OFF_SETUP`-related event type exists in `shared/src/types.ts`. Since `buildReplayFrames` only walks `eventLog`, this repositioning is structurally invisible to replay — same defect _class_ as the known GK_KICK/LOOSE_BALL_LAND REPLAY-06 gaps, but a distinct root cause (no event at all, vs. event present but incomplete).

---

## BUG-18: Undo Scope

| Option                                           | Description                                                                                                 | Selected |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- | -------- |
| Coverage gap — extend to all move-bearing phases | Add Undo to GK_KICK_MOVE, SNAPSHOT_DEFLECT, FREE_MOVE_ATTACK/DEFENSE, HEADER repositioning, FREE_KICK_SETUP |          |
| Specific regression in an existing Undo phase    | User describes exact broken behavior in one of the 3 already-supported phases                               |          |

**User's choice:** Both, effectively — user clarified Undo is "never enabled in the move phase" (a regression in an already-"supported" phase), AND asked for consistent behavior "across all move actions" (the coverage-gap option).
**Notes:** Investigation found the regression: `ActionPanel.tsx`'s `canUndo` returns `false` whenever `lastDiceRoll` is truthy, and `gameEngine.ts`'s `applyStartMovement` (the dominant PASS/KICK_OFF/LOOSE_BALL → MOVE transition) never resets `lastDiceRoll: null` — unlike the GK_RESTART movement branch, which does. Since most possession changes route through a dice roll before reaching MOVE, this explains "never enabled." Confirmed `validUndoPhases` server-side only covers 3 of the many move-bearing phases. User explicitly flagged "consider replay impacts when fixing" — tied to the BUG-17 finding that some movement paths don't produce eventLog entries at all.

---

## Claude's Discretion (round 1)

- BUG-07's exact header-pass log-line wording (carried from original 2026-06-20 session)
- BUG-12's toggle storage mechanism (env var vs. GameState field vs. config constant)
- BUG-17's exact mechanism for giving replay visibility into KICK_OFF_SETUP repositioning (new ActionEvent type vs. alternative)
- BUG-18's exact set of phase-transition sites needing a `lastDiceRoll: null` audit beyond `applyStartMovement`

## Deferred Ideas (round 1)

- GK_KICK ball-delivery-invisible-during-replay todo — reviewed but not folded; remains a pending backlog item for a future phase (kinship to BUG-17 noted in CONTEXT.md for whenever it's picked up)

---

## Round 2 — Continuation Session (no phase number given)

**Context:** `/gsd-discuss-phase` was invoked with no phase number. Asked the user to choose between 18.3 (this phase, just updated) or 18.4 (next pending phase). User instead used the "Other" freeform option to list 3 more bug reports, all rule/flow-correctness in nature — treated as a continuation of this same Phase 18.3 Bug-Bash session rather than a literal answer to "which phase," since all 3 fit this phase's exact theme.

1. Player numbers are wrong — should match what's on the field; OK to change piece IDs if it simplifies the mapping
2. Free move should be evaluated/offered before standard move options, not mid-MOVE/mid-header
3. Snapshot not displaying the goal-line target

## BUG-19: Player Number Consistency

| Option                                                 | Description                                                                        | Selected |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------- | -------- |
| Make PieceOverlay + ActionLog read `piece.number`      | Mirror PlayerStatsPanel's already-validated D-08 fix; no ID scheme change          | ✓        |
| Change piece-ID generation to encode the jersey number | Larger blast radius — touches roster seeding and ~1000 ID-hardcoding test fixtures |          |

**User's choice:** Make PieceOverlay + ActionLog read `piece.number`.
**Notes:** Investigation found 3 independent, disagreeing number-display implementations: `PlayerStatsPanel.tsx` (already correct, `piece.number`), `PieceOverlay.tsx` (on-pitch token — own idx/role-based formula), `ActionLog.tsx` (id-trailing-digit + 1). User explicitly floated the ID-rename alternative themselves but the lookup-based fix was recommended and chosen given the much smaller blast radius.

---

## BUG-20: Free Move Interrupt Timing

| Option                                  | Description                                                                                                                  | Selected |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------- |
| Defer the interrupt to a clean boundary | Keep the same zone-crossing trigger; never interrupt mid-MOVE-slot or mid-HEADER; offer free move at the next clean boundary | ✓        |
| Only evaluate at MOVE-phase start       | Drop the per-action reactive check; evaluate once on MOVE-phase entry                                                        |          |

**User's choice:** Defer the interrupt to a clean boundary.
**Notes:** `applyFreeMoveZoneCheck` (gameEngine.ts) is invoked from `roomStore.ts`'s `broadcastState` after every resolved action, per Phase 17's literal rulebook-text design (D-33: "any action has come to an end"). This explains the reported flow-breaking — confirmed it can overlay FREE_MOVE_ATTACK/DEFENSE mid-slot or mid-header. The chosen fix preserves the existing trigger condition and eligibility/resume mechanism (D-34/D-35/D-36) — only the timing of when the interrupt is allowed to engage changes.

---

## BUG-21: Snapshot Goal-Line Target Not Displaying

**User's choice:** N/A — single bug report, no design alternatives to choose between.
**Notes:** Investigated `HexGrid.tsx`'s `isShootingModeGoalHex` computation, `applySnapshot`'s state spread (confirms `ball.carrierId` preserved), and the server's `GAME_SNAPSHOT` `isActivePlayer` gate — none showed an obvious gap from code inspection alone. Locked into CONTEXT.md as an explicit investigation-first item (no fix pattern assumed) rather than guessing at a root cause without a live repro. This mirrors the original BUG-06's "find the actual gap" framing from the first 2026-06-20 session.

## Claude's Discretion (round 2)

- BUG-20's exact mechanism for deferring the interrupt (guard flag vs. re-running the check at the slot/header boundary instead of after every action)
- BUG-21's actual root cause and fix — fully open pending live verification

## Deferred Ideas (round 2)

None — BUG-19, BUG-20, and BUG-21 are all in scope for this sub-phase.
