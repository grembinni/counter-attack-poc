---
phase: 18.3-bug-bash-rule-correctness
plan: '03'
subsystem: game-engine
tags: [game-engine, first-time-pass, offside, header, possession-transition]

# Dependency graph
requires:
  - phase: 18.3-bug-bash-rule-correctness
    provides: BUG-06/BUG-07/BUG-12 fix targets in gameEngine.ts and gameHandlers.ts
  - phase: 17.1-action-flow-cleanup
    provides: FIRST_TIME_PASS_MOVE two-slot repositioning sub-phase and firstTimePassCarrierId lifecycle
provides:
  - offsidePieceIds reset on GOAL→KICK_OFF_SETUP transition (applyKickOffReady)
  - direct header delivery from GAME_HEADER_CONTESTANT winner branch (no GAME_HEADER_TARGET wait)
  - FTP_MOVE_ENABLED toggle defaulting false — FTP delivers directly, skipping FIRST_TIME_PASS_MOVE
affects: [18.3-04, 18.4-ux-enhancements]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Feature toggle const at module level (FTP_MOVE_ENABLED) gates sub-phase entry without deleting the sub-phase handler code'
    - 'Direct delivery path mirrors STANDARD_PASS occupant-pickup/BUG-04 possession-transfer logic for parity'

key-files:
  created: []
  modified:
    - packages/server/src/gameEngine.ts
    - packages/server/src/gameHandlers.ts
    - packages/server/src/__tests__/gameEngine.phase17.test.ts
    - packages/server/src/__tests__/gameEngine.test.ts

key-decisions:
  - 'BUG-06: applyKickOffReady was the confirmed bypass — it lacked offsidePieceIds: [] (all other applyFreeKickReady stage returns already had it)'
  - 'BUG-07: GAME_HEADER_CONTESTANT winner branch now delivers directly using winner hex as target; GAME_HEADER_TARGET handler retained as dead-but-intact code (grep confirmed no other caller)'
  - 'BUG-12: FTP_MOVE_ENABLED=false gates the FIRST_TIME_PASS_MOVE transition; direct delivery mirrors STANDARD_PASS occupant-pickup including BUG-04 defender-occupied possession transfer'
  - 'BUG-12: Interception bypass (isFirstTimePass guard, CR-02-new) preserved for the toggle-off direct delivery path'
  - '4 pre-existing tests updated to reflect toggle-off behavior (gameEngine.test.ts x2, gameEngine.phase17.test.ts x2); 3 new regression tests added for BUG-12'

patterns-established:
  - "Toggle-off bypass: existing gate `if (newLastActionType === 'FIRST_TIME_PASS')` is wrapped with `if (FTP_MOVE_ENABLED)` inner guard; fallthrough delivers directly"

requirements-completed: [BUG-06, BUG-07, BUG-12]

# Metrics
duration: ~25min
completed: 2026-07-01
---

# Phase 18.3 Plan 03: Bug-Bash Rule Correctness (BUG-06, BUG-07, BUG-12) Summary

**offsidePieceIds reset on kick-off-after-goal, header duel delivers directly (non-contestable HEADER pass), and first-time-pass repositioning sub-phase gated off by FTP_MOVE_ENABLED=false toggle**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-07-01T21:00:00Z
- **Completed:** 2026-07-01T21:30:00Z
- **Tasks:** 3 (Tasks 1 and 2 from prior session; Task 3 in this session)
- **Files modified:** 4

## Accomplishments

- BUG-06: `applyKickOffReady` now includes `offsidePieceIds: []` — confirmed as the only bypass path; all free-kick stage returns already had the reset
- BUG-07: `GAME_HEADER_CONTESTANT` winner branch skips the `GAME_HEADER_TARGET` wait, delivers directly (winner's current hex as target), routes to `GK_DIVE` or `PASS` via `isGoalLineTarget` check, sets `lastActionType: 'HEADER'` for non-contestable delivery
- BUG-12: `FTP_MOVE_ENABLED = false` at module top of `gameEngine.ts`; `applyRoll` FIRST_TIME_PASS branch gated — toggle-off path delivers ball directly at `targetHex` with full BUG-04 occupant-pickup parity; toggle-on path unchanged; `FIRST_TIME_PASS_MOVE` handler code untouched

## Task Commits

Each task was committed atomically:

1. **Task 1: BUG-06 — offsidePieceIds reset on all dead-ball restart paths** - `b08070e` (fix)
2. **Task 2: BUG-07 — deliver header pass directly after duel win** - `fedfb19` (fix)
3. **Task 3: BUG-12 — FTP_MOVE_ENABLED toggle gates FIRST_TIME_PASS_MOVE transition** - `6e1b53d` (fix)

## Files Created/Modified

- `packages/server/src/gameEngine.ts` — BUG-06: `offsidePieceIds: []` added to `applyKickOffReady`; BUG-07: direct header delivery in `GAME_HEADER_CONTESTANT` winner branch; BUG-12: `FTP_MOVE_ENABLED` const + toggle gate in `applyRoll` FIRST_TIME_PASS branch
- `packages/server/src/gameHandlers.ts` — BUG-07: `GAME_HEADER_CONTESTANT` winner branch rewritten to deliver directly (no `GAME_HEADER_TARGET` wait)
- `packages/server/src/__tests__/gameEngine.phase17.test.ts` — BUG-12: updated CR-02-new test (now expects PASS not FIRST_TIME_PASS_MOVE); added 3 BUG-12 regression tests (teammate delivery, defender-occupied transfer, empty-hex delivery)
- `packages/server/src/__tests__/gameEngine.test.ts` — BUG-12: updated 2 FIRST_TIME_PASS occupied/empty-target tests to expect direct PASS delivery

## Decisions Made

- **BUG-12 toggle-off delivery**: direct path mirrors STANDARD_PASS occupant-pickup logic including BUG-04 defender-occupied-hex possession transfer — a defender at the FTP target hex transfers possession exactly as for a STANDARD_PASS
- **BUG-12 interception suppression**: the `isFirstTimePass` bypass (CR-02-new, Phase 17.1-13) already suppresses interception before the FIRST_TIME_PASS branch is reached; this applies equally to the toggle-off direct delivery path
- **GAME_HEADER_TARGET handler retained**: confirmed via grep that no remaining caller reaches `GAME_HEADER_TARGET` after the BUG-07 change; handler left as dead-but-intact code for safety
- **4 test updates**: existing tests that hard-coded `phase: 'FIRST_TIME_PASS_MOVE'` expectations were updated to reflect the new toggle-off default (PASS phase, direct delivery); their original behavior (toggle-on) remains exercisable by flipping the const

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated 4 pre-existing tests that hard-coded FIRST_TIME_PASS_MOVE expectations**

- **Found during:** Task 3 (BUG-12 implementation)
- **Issue:** gameEngine.test.ts (2 tests) and gameEngine.phase17.test.ts (1 test) expected `phase: 'FIRST_TIME_PASS_MOVE'` — now incorrect with `FTP_MOVE_ENABLED = false`
- **Fix:** Updated test descriptions and assertions to match toggle-off direct delivery; 3 new BUG-12 regression tests added in gameEngine.phase17.test.ts
- **Files modified:** packages/server/src/**tests**/gameEngine.test.ts, packages/server/src/**tests**/gameEngine.phase17.test.ts
- **Verification:** `pnpm --filter @counter-attack/server test -- --run` — 472 passed, 1 skipped, 1 todo
- **Committed in:** 6e1b53d (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — test fixture alignment with new toggle-off default)
**Impact on plan:** Necessary for test suite to reflect the implemented behavior. No scope creep.

## Issues Encountered

None — implementation was straightforward; the main discovery was that 4 existing tests needed updating to reflect `FTP_MOVE_ENABLED = false` behavior.

## Self-Check

### Created files exist

- `.planning/phases/18.3-bug-bash-rule-correctness/18.3-03-SUMMARY.md` — this file (Write tool confirms creation)

### Commits exist

- b08070e — BUG-06 fix (offsidePieceIds reset)
- fedfb19 — BUG-07 fix (direct header delivery)
- 6e1b53d — BUG-12 fix (FTP_MOVE_ENABLED toggle)

## Self-Check: PASSED

## Next Phase Readiness

- BUG-06, BUG-07, BUG-12 all fixed and covered by regression tests
- 472 server tests passing; typecheck clean across shared/server/client
- Phase 18.3 plans 01–05 complete; plan 04 (18.3-04) is the remaining open wave-3 plan

---

_Phase: 18.3-bug-bash-rule-correctness_
_Completed: 2026-07-01_
