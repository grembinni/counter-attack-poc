---
phase: 18.3-bug-bash-rule-correctness
plan: 03
type: execute
wave: 2
depends_on: ['18.3-02']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
files_touched_note: 'Depends on 18.3-02 to serialize gameEngine.ts edits (no same-wave overlap). This plan owns gameEngine.ts sections: applyFreeKickReady/applyKickOffReady (~3347/3742, BUG-06), applyResolveHeaderTarget/applyRoll FIRST_TIME_PASS branch (~2960/1291, BUG-07/BUG-12). gameHandlers.ts sections: GAME_HEADER_CONTESTANT/GAME_HEADER_TARGET (~2245-2560, BUG-07).'
autonomous: true
requirements: [BUG-06, BUG-07, BUG-12]
must_haves:
  truths:
    - 'offsidePieceIds resets to [] for ALL players on every dead-ball restart return path (free-kick and kick-off-after-goal), not only the free-kick staged sequence'
    - 'After a header duel is won, the ball is delivered directly with no intermediate GAME_HEADER_TARGET selection step; the delivery is non-contestable and logged as HEADER'
    - 'With the FTP-move toggle off (default), a first-time pass delivers the ball directly and skips the FIRST_TIME_PASS_MOVE repositioning sub-phase; the phase/handler code remains intact'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'offsidePieceIds reset on all restart paths; direct header delivery; FTP-move toggle gate'
      contains: 'offsidePieceIds'
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'GAME_HEADER_CONTESTANT winner branch delivers directly (no GAME_HEADER_TARGET wait)'
      contains: 'GAME_HEADER_CONTESTANT'
  key_links:
    - from: 'GAME_HEADER_CONTESTANT winner branch'
      to: 'PASS or GK_DIVE transition (via isGoalLineTarget routing)'
      via: "immediate delivery using winner's current hex as target; lastActionType stays HEADER"
      pattern: 'lastActionType.*HEADER'
---

<objective>
Fix three server-engine rule/flow bugs. Depends on plan 18.3-02 to serialize `gameEngine.ts` edits.
- BUG-06: close the code path(s) that bypass the D-43/D-47 full offside reset. Grep every dead-ball restart that transitions to PASS (free-kick staged sequence AND `applyKickOffReady` after a goal) and ensure each sets `offsidePieceIds: []`, mirroring the confirmed pattern in `applyFreeKickReady`'s last-stage return.
- BUG-07: after a header duel is won in `GAME_HEADER_CONTESTANT`, deliver the pass directly — drop the intermediate `GAME_HEADER_TARGET` selection sub-phase. The delivery is non-contestable (`lastActionType === 'HEADER'` already suppresses interception in `applyPass`, the BUG-01 precedent) and logged as a `HEADED_PASS`/HEADER event, not FIRST_TIME_PASS. Replicate the `isGoalLineTarget` routing so a goal-line-adjacent winner routes to GK_DIVE, not PASS.
- BUG-12: add a server-level `FTP_MOVE_ENABLED` const (default `false`) that gates the `applyRoll` FIRST_TIME_PASS transition — when off, deliver the ball directly and skip FIRST_TIME_PASS_MOVE; leave all FIRST_TIME_PASS_MOVE handler code intact.

Purpose: These three engine bugs cluster around possession-transition transitions (offside reset, header delivery, FTP delivery) and share `gameEngine.ts`; batching avoids cross-plan edit conflicts.
Output: Corrected offside reset coverage, direct header delivery, and a working FTP-move toggle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-CONTEXT.md
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-RESEARCH.md
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-PATTERNS.md
@.planning/phases/17-rule-bugs/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUG-06 — offsidePieceIds reset on all dead-ball restart paths</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (`applyFreeKickReady` ~3742 — the CONFIRMED correct last-stage return that sets `offsidePieceIds: []` and `movedPieceIds: []`, per D-43/D-47/D-56; `applyKickOffReady` ~3347 — the kick-off-after-goal path, check whether it sets `offsidePieceIds: []`)
    - packages/shared/src/offside.ts (`triggerOffsideFoul`, `evaluateOffside`, reset patterns; `FREE_KICK_STAGES`)
    - .planning/phases/17-rule-bugs/17-CONTEXT.md (D-43 full offside reset on free-kick taken; D-47 generalized to kick-off — the intended behavior this bug reports as incomplete)
  </read_first>
  <action>
    Grep `gameEngine.ts` for every function/return that transitions to `phase: 'PASS'` (or otherwise concludes a dead-ball restart into live play) from a free-kick-adjacent or kick-off-adjacent phase: at minimum `applyFreeKickReady` (all stage returns, not just stage 3) and `applyKickOffReady`. For each such return that does NOT already include `offsidePieceIds: []`, add it, mirroring the exact spread at `applyFreeKickReady`'s last-stage return (`offsidePieceIds: []`). Per RESEARCH assumption A5, `applyKickOffReady` is the prime suspect — if it is missing the reset, that is the confirmed gap; add it there. Do not narrow the reset to only the offending player — it must clear ALL flags (empty array). Confirm the fix site by tracing which restart path the user's reported regression follows (a kick taken via a transition other than the staged stage-3 return). Document the confirmed bypass path in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- --run --reporter=verbose -t "offside"</automated>
  </verify>
  <acceptance_criteria>
    - Every dead-ball-restart-to-live-play return in `gameEngine.ts` (free-kick stages + `applyKickOffReady`) includes `offsidePieceIds: []`
    - `applyKickOffReady` return contains `offsidePieceIds: []` (the assumed-missing site, per A5) OR the SUMMARY documents the actual confirmed bypass path if different
    - A regression test in `offside.test.ts` asserts `offsidePieceIds` is empty after the confirmed restart path (add one if not already covered)
    - `pnpm --filter @counter-attack/server test -- --run -t "offside"` exits 0
  </acceptance_criteria>
  <done>Offside flags clear for all players on every restart path; the specific bypass path is identified and fixed.</done>
</task>

<task type="auto">
  <name>Task 2: BUG-07 — deliver header pass directly, drop target-selection sub-phase</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (`GAME_HEADER_CONTESTANT` handler ~2414-2560 — the winner branch after `computeHeaderDuelWinner`, which currently leaves phase as HEADER awaiting `GAME_HEADER_TARGET`; the `GAME_HEADER_TARGET` handler ~2257 for the routing logic to replicate)
    - packages/server/src/gameEngine.ts (`applyResolveHeaderTarget` ~2960 — the `isGoalLineTarget` routing check that decides GK_DIVE vs PASS; the ~line 3031 non-goal-line transition to PASS with `lastActionType: 'HEADER'` and a HEADED_PASS event; the `applyPass` HEADER interception-suppression BUG-01 precedent)
    - packages/client/src/components/ActionLog.tsx (the `HEADED_PASS` case ~708-716 — the target event shape/label `[HEADER PASS]`, `isGoal: false`)
    - packages/shared/src/types.ts (confirm `HEADED_PASS` is in `ActionEventType` — it is, added by quick-task 260621-b8f; `REPLAY_ELIGIBLE_TYPES` already includes it)
  </read_first>
  <action>
    In the `GAME_HEADER_CONTESTANT` winner branch (after `computeHeaderDuelWinner` determines a non-null winner), replace the "leave phase as HEADER, wait for GAME_HEADER_TARGET" behavior with immediate delivery: treat the winning contestant's CURRENT position as the target and route exactly as `applyResolveHeaderTarget` would — replicate its `isGoalLineTarget` check (Pitfall 6: if the winner's hex is goal-line-adjacent, route to GK_DIVE; otherwise transition to PASS). Set `lastActionType: 'HEADER'` on the resulting state so `applyPass`'s existing interception suppression makes the delivery non-contestable (no interception roll / ZoI). Emit a `HEADED_PASS` event (NOT `FIRST_TIME_PASS`) for the delivery, matching the `[HEADER PASS]` log-line shape already rendered in `ActionLog.tsx`. The `GAME_HEADER_TARGET` handler may remain as dead-but-intact code, or be removed if the executor confirms via grep it has no remaining reachable caller after this change — document the choice in the SUMMARY. Preserve the high-pass contest (who wins the header) unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- --run -t "header"</automated>
  </verify>
  <acceptance_criteria>
    - `GAME_HEADER_CONTESTANT` winner branch transitions directly to PASS or GK_DIVE (no phase stop awaiting `GAME_HEADER_TARGET`)
    - The delivery state sets `lastActionType: 'HEADER'` (making it non-contestable via the existing `applyPass` suppression)
    - The delivery emits a `HEADED_PASS` event, not `FIRST_TIME_PASS`
    - Goal-line-adjacent winner routes to GK_DIVE (isGoalLineTarget replicated), non-goal-line routes to PASS
    - A regression test asserts no interception fires on the header-derived pass and the phase does not stop in HEADER awaiting a target
    - `pnpm --filter @counter-attack/server test -- --run -t "header"` exits 0
  </acceptance_criteria>
  <done>Header duel win delivers the pass immediately, non-contestable, logged as HEADER; no intermediate target-selection step.</done>
</task>

<task type="auto">
  <name>Task 3: BUG-12 — FTP_MOVE_ENABLED toggle gates the FIRST_TIME_PASS_MOVE transition</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (`applyRoll` ~1291 — the FIRST_TIME_PASS branch, ~line 1545, that always transitions to `phase: 'FIRST_TIME_PASS_MOVE'`; the "deliver ball directly" logic that the no-repositioning path would use — i.e. how a plain STANDARD_PASS delivers the ball at `targetHex` without a repositioning phase; the `firstTimePassCarrierId` lifecycle set at this transition per Phase 17.1-16)
    - packages/server/src/gameHandlers.ts (FIRST_TIME_PASS_MOVE handler — confirm it stays intact/reachable when toggle is on)
  </read_first>
  <action>
    Add a server-level constant near the top of `gameEngine.ts` (or a small config module if the executor finds an existing feature-flag module): `const FTP_MOVE_ENABLED = false;` with a comment `// BUG-12: toggle off to skip the FIRST_TIME_PASS_MOVE repositioning sub-phase`. In `applyRoll`'s FIRST_TIME_PASS branch (~line 1545), gate the `phase: 'FIRST_TIME_PASS_MOVE'` return behind `FTP_MOVE_ENABLED`: when `true`, keep the existing transition (including `firstTimePassCarrierId` setup); when `false`, deliver the ball directly at `targetHex` and proceed to the next phase exactly as the no-repositioning delivery path would (mirror the STANDARD_PASS delivery/occupant-pickup logic — including BUG-04 defender-occupied-hex possession transfer — so a toggled-off FTP behaves like a plain grounded pass on delivery). Do NOT delete or alter any FIRST_TIME_PASS_MOVE phase/handler code — it must work unchanged when the toggle is flipped back on. Keep interception suppression consistent with FIRST_TIME_PASS's existing bypass (Phase 17.1-13) for the toggled-off delivery.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- --run -t "first.time.pass"</automated>
  </verify>
  <acceptance_criteria>
    - `gameEngine.ts` contains `const FTP_MOVE_ENABLED = false;`
    - With `FTP_MOVE_ENABLED === false`, `applyRoll`'s FIRST_TIME_PASS branch does NOT return `phase: 'FIRST_TIME_PASS_MOVE'` — it delivers the ball at `targetHex` and advances to the next phase
    - FIRST_TIME_PASS_MOVE phase/handler code is untouched (no deletions); flipping the const to `true` restores the repositioning transition
    - A regression test covers the toggle-off delivery path (ball delivered, no FIRST_TIME_PASS_MOVE phase entered)
    - `pnpm --filter @counter-attack/server test -- --run -t "first.time.pass"` exits 0
  </acceptance_criteria>
  <done>FTP-move toggle defaults off and skips the repositioning sub-phase; the mechanic remains fully intact for re-enabling.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols created by this phase:

- `gameEngine.ts`: `FTP_MOVE_ENABLED` — server-level `const boolean` (default `false`), the BUG-12 feature toggle
  Modified (not new): `applyFreeKickReady`/`applyKickOffReady` return objects (add `offsidePieceIds: []`), `applyResolveHeaderTarget` routing reused in the header-delivery path, `applyRoll` FIRST_TIME_PASS branch (toggle gate), `GAME_HEADER_CONTESTANT` handler winner branch (direct delivery). No new files, no new ActionEvent types (`HEADED_PASS` already exists).
  </artifacts_this_phase_produces>

<verification>
- `pnpm -r test -- --run` full suite green
- `pnpm -r tsc --noEmit` clean typecheck
- BUG-06: offside reset on all restart paths; BUG-07: direct non-contestable header delivery; BUG-12: toggle-off skips FTP repositioning
</verification>

<success_criteria>
BUG-06, BUG-07, and BUG-12 are fixed and covered by regression tests; the full test suite and typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/18.3-bug-bash-rule-correctness/18.3-03-SUMMARY.md` when done
</output>
