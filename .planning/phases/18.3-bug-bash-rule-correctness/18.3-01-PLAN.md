---
phase: 18.3-bug-bash-rule-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/ActionLog.module.css
  - packages/client/src/components/PieceOverlay.tsx
autonomous: true
requirements: [BUG-15, BUG-16, BUG-19]
must_haves:
  truths:
    - 'GOAL and SHOT_ATTEMPT(GOAL) log entries render in the same gray color as every other event'
    - "FTP_MOVE and HP_MOVE log entries show the player's full name (not an A/D role-letter + number)"
    - 'The on-pitch token, the action log, and the player stats panel all show the same jersey number for a given piece, sourced from piece.number'
  artifacts:
    - path: 'packages/client/src/components/ActionLog.tsx'
      provides: 'pieceNum() store-lookup, PNamed in FTP_MOVE/HP_MOVE, isGoal removal'
      contains: 'pieces.find'
    - path: 'packages/client/src/components/PieceOverlay.tsx'
      provides: 'playerNumber sourced from piece.number'
      contains: 'String(piece.number)'
  key_links:
    - from: 'packages/client/src/components/ActionLog.tsx pieceNum()'
      to: 'useGameStore.getState().gameState.pieces'
      via: 'pieces.find lookup mirroring pieceName()'
      pattern: "pieces\\.find"
---

<objective>
Fix the three Tier-1 (low-risk, self-contained client rendering) bugs in the action log and on-pitch token:
- BUG-15: remove the amber goal-content styling — all events render with the standard gray `.content` class.
- BUG-16: FTP_MOVE and HP_MOVE log entries use `PNamed` (full name) instead of `P` (role-letter + number).
- BUG-19: jersey number is always sourced from `piece.number` (the canonical CSV-seeded roster number) on the on-pitch token (PieceOverlay) and the action log (ActionLog `pieceNum()`), matching PlayerStatsPanel's already-correct approach.

Purpose: These three bugs are mechanical search-and-replace within client components with direct code analogs; batching them into one plan avoids ActionLog.tsx edit conflicts (BUG-16 and BUG-19 both touch it) and PieceOverlay.tsx overlap.
Output: Corrected ActionLog rendering + jersey-number source of truth.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-CONTEXT.md
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-RESEARCH.md
@.planning/phases/18.3-bug-bash-rule-correctness/18.3-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUG-19 — jersey number source of truth (PieceOverlay + ActionLog.pieceNum)</name>
  <files>packages/client/src/components/PieceOverlay.tsx, packages/client/src/components/ActionLog.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx (current `playerNumber` idx/role formula at lines ~83-89)
    - packages/client/src/components/ActionLog.tsx (current `pieceNum()` at lines ~123-126; `pieceName()` lookup template at lines ~20-25; `pieceColorOf()` at lines ~9-13)
    - packages/client/src/components/PlayerStatsPanel.tsx (reference: `const playerNumber = String(piece.number);` at lines ~34-35, 140 — the already-correct D-08 pattern to mirror)
    - packages/shared/src/types.ts (confirm `PlayerPiece.number` field exists — canonical jersey number, D-04 Phase 16)
  </read_first>
  <action>
    In `PieceOverlay.tsx`, replace the entire `idx`/`playerNumber` derivation block (the `Number(piece.id.slice(...))` line plus the `piece.role === 'ST' ? '9' : ...` ternary, lines ~82-89) with a single statement: `const playerNumber = String(piece.number);`. The `piece` prop is a `PlayerPiece` and already carries `number`. Remove the now-unused `idx` local. Do NOT change `isGK` or the `<text>` render at line ~358 (it already consumes `playerNumber`). Do NOT change piece ID generation/format anywhere — IDs remain internal references.

    In `ActionLog.tsx`, rewrite `pieceNum(pieceId: string): string` to resolve via a store lookup mirroring `pieceName()` exactly: read `const pieces = useGameStore.getState().gameState.pieces;`, `const piece = pieces.find((p) => p.id === pieceId);`, then `return piece !== undefined ? String(piece.number) : pieceId;`. The fallback must be the raw `pieceId` string (graceful degradation, matching `pieceName()`'s null-safe fallback) — never throw. This automatically corrects every `PNamed`/`P` usage since both call `pieceNum()` internally.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- --run -t "PieceOverlay"</automated>
  </verify>
  <acceptance_criteria>
    - `PieceOverlay.tsx` contains `const playerNumber = String(piece.number);` and no longer contains `piece.id.slice(piece.id.lastIndexOf('-')` for number derivation
    - `PieceOverlay.tsx` no longer contains the `piece.role === 'ST' ? '9'` number ternary
    - `ActionLog.tsx` `pieceNum()` body contains `pieces.find((p) => p.id === pieceId)` and returns `String(piece.number)` on hit, `pieceId` on miss
    - `pnpm --filter @counter-attack/client test -- --run -t "PieceOverlay"` exits 0
    - `pnpm --filter @counter-attack/client test -- --run -t "ActionLog"` exits 0
  </acceptance_criteria>
  <done>PieceOverlay and ActionLog both derive the displayed number from `piece.number`; PlayerStatsPanel is unchanged and all three surfaces now agree.</done>
</task>

<task type="auto">
  <name>Task 2: BUG-15 + BUG-16 — remove goal amber styling and use PNamed in FTP/HP move logs</name>
  <files>packages/client/src/components/ActionLog.tsx, packages/client/src/components/ActionLog.module.css</files>
  <read_first>
    - packages/client/src/components/ActionLog.tsx (the three `isGoal: true` / `isGoal: event.outcome === 'GOAL'` assignment sites at lines ~386 GOAL case, ~485 SHOT_ATTEMPT snapshot branch, ~518 SHOT_ATTEMPT regular-duel branch; the render ternary at line ~785; the `HP_MOVE` case ~646-660 and `FTP_MOVE` case ~661-675 using `<P pieceId={...} prefix={team|ftpTeam} />`; the `PNamed` definition ~57-65; the `isGoal: boolean` field on the format-result type ~251)
    - packages/client/src/components/ActionLog.module.css (`.content` = `#e0e0e0`; `.goalContent` = `#e8a020`)
  </read_first>
  <action>
    BUG-15: In `ActionLog.tsx` change all three goal-styling assignments to `isGoal: false` — the GOAL case (~line 386, currently `isGoal: true`), the SHOT_ATTEMPT snapshot branch (~line 485, currently `isGoal: event.outcome === 'GOAL'`), and the SHOT_ATTEMPT regular-duel branch (~line 518, same). Change the render ternary at ~line 785 from `<span className={isGoal ? styles.goalContent : styles.content}>{content}</span>` to `<span className={styles.content}>{content}</span>`. In `ActionLog.module.css`, remove the `.goalContent` class entirely (it is unused after this change). Leave the `isGoal` field on the result type in place (still referenced structurally by every case) unless the executor confirms via grep that nothing reads it after the render-ternary change — if fully unreferenced, removing it is acceptable but not required.

    BUG-16: In the `HP_MOVE` case (~line 654) and the `FTP_MOVE` case (~line 669), replace `<P pieceId={event.pieceId} prefix={team} />` and `<P pieceId={event.pieceId} prefix={ftpTeam} />` respectively with `<PNamed pieceId={event.pieceId} />` (no prefix argument — `PNamed` resolves the full name via `pieceName()`). Do NOT touch the `GK_KICK`/`GK_KICK_MOVE` cases' use of `<P prefix="GK"/"K"/"O">` — those are legitimate role labels, not the redundant A/D pattern. Do NOT change the `pieceNum(event.pieceId)` group-label usage inside `consolidateEvents()` — those are internal grouping keys, not displayed.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- --run -t "ActionLog"</automated>
  </verify>
  <acceptance_criteria>
    - `ActionLog.tsx` contains no `isGoal: true` and no `isGoal: event.outcome === 'GOAL'` (all such sites now `isGoal: false`)
    - `ActionLog.tsx` render span uses `className={styles.content}` unconditionally (no `styles.goalContent` reference remains)
    - `ActionLog.module.css` no longer contains a `.goalContent` class
    - `ActionLog.tsx` HP_MOVE and FTP_MOVE cases render `<PNamed pieceId={event.pieceId} />` with no prefix; neither uses `<P ... prefix={team}` / `prefix={ftpTeam}`
    - `ActionLog.tsx` still contains `<P` with `prefix="GK"`/`"K"`/`"O"` in the GK_KICK/GK_KICK_MOVE cases (unchanged)
    - `pnpm --filter @counter-attack/client test -- --run -t "ActionLog"` exits 0
  </acceptance_criteria>
  <done>Goal log entries render gray like all other events; FTP/HP move logs show full player names; GK role labels untouched.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
No new symbols. This plan only modifies existing functions/components:

- `ActionLog.tsx`: `pieceNum()` (rewritten body), `formatEvent` GOAL/SHOT_ATTEMPT/HP_MOVE/FTP_MOVE cases (edited), render span (edited)
- `ActionLog.module.css`: `.goalContent` class removed
- `PieceOverlay.tsx`: `playerNumber` local (re-sourced from `piece.number`)
  No new files, no new exports, no new types, no new CSS classes.
  </artifacts_this_phase_produces>

<verification>
- `pnpm -r test -- --run` full suite green
- `pnpm -r tsc --noEmit` clean typecheck
- BUG-15: no amber `.goalContent` remains; BUG-16: FTP/HP logs use PNamed; BUG-19: all three number-display sites agree on `piece.number`
</verification>

<success_criteria>
BUG-15, BUG-16, and BUG-19 are fixed and covered by the existing ActionLog and PieceOverlay client test suites; full test suite and typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/18.3-bug-bash-rule-correctness/18.3-01-SUMMARY.md` when done
</output>
