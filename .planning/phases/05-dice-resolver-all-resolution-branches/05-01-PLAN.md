---
phase: 05-dice-resolver-all-resolution-branches
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/shared/src/teams.ts
  - packages/shared/src/passValidator.ts
  - packages/shared/src/shotValidator.ts
  - packages/shared/src/teams.test.ts
  - packages/shared/src/shotValidator.test.ts
autonomous: true
requirements: [DICE-01, DICE-02, SHOT-05]
must_haves:
  truths:
    - 'PlayerPiece type has a highPass attribute; GKs are 0, outfielders are non-zero'
    - 'GameState type has a lastDiceRoll field (rolls + context) the server can populate'
    - 'GAME_GK_RESTART client event exists and is typed with a kick/throw/movement payload'
    - 'High Pass accuracy uses the highPass attribute, not aerialAbility'
    - 'A tied shot duel resolves to LOOSE_BALL, not SAVE'
    - 'packages/shared compiles and its full test suite passes'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'highPass on PlayerPiece, lastDiceRoll on GameState'
      contains: 'highPass'
    - path: 'packages/shared/src/events.ts'
      provides: 'GAME_GK_RESTART event + typed payload'
      contains: 'GAME_GK_RESTART'
    - path: 'packages/shared/src/teams.ts'
      provides: 'highPass values on all 22 players'
      contains: 'highPass'
    - path: 'packages/shared/src/passValidator.ts'
      provides: 'D-14 highPass fix in validatePassAccuracy'
      contains: 'piece.highPass'
    - path: 'packages/shared/src/shotValidator.ts'
      provides: 'D-13/D-17 LOOSE_BALL tie outcome'
      contains: 'LOOSE_BALL'
  key_links:
    - from: 'packages/shared/src/passValidator.ts'
      to: 'PlayerPiece.highPass'
      via: 'attribute selection for HIGH pass'
      pattern: "passType === 'HIGH' \\? piece.highPass"
    - from: 'packages/shared/src/shotValidator.ts'
      to: 'ShotDuelResult'
      via: 'tie branch returns LOOSE_BALL'
      pattern: 'shooterScore === gkScore'
---

<objective>
Establish the shared-package foundation for Phase 5 dice resolution: add the `highPass`
player attribute and `lastDiceRoll` game-state field, declare the `game:gk-restart`
event contract, and apply the locked rulebook corrections to the pass and shot validators
(D-04, D-05, D-06, D-07, D-11, D-13, D-14, D-15, D-16, D-17, D-22). Update the affected
test fixtures and assertions so `packages/shared` compiles and all shared tests pass.

Purpose: Every downstream server task (Plans 02 and 03) consumes these types, events, and
validator behaviours. Defining the contracts first prevents the executors of later waves
from guessing field shapes or validator semantics.

Output: Updated shared types/events/teams/validators with passing `packages/shared` tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-PATTERNS.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add highPass + lastDiceRoll types and GAME_GK_RESTART event</name>
  <files>packages/shared/src/types.ts, packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/types.ts (current PlayerPiece interface lines 6-22, GameState interface lines 51-63, GamePhase already includes 'GK_RESTART')
    - packages/shared/src/events.ts (ClientEvents already has GAME_ROLL: 'game:roll'; ClientToServerEvents already has GAME_ROLL typed)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md (D-04, D-05, D-06, D-11, D-22)
  </read_first>
  <behavior>
    - PlayerPiece gains a required numeric `highPass` field
    - GameState gains an optional `lastDiceRoll: { rolls: number[]; context: string } | null` field
    - ClientEvents has `GAME_GK_RESTART: 'game:gk-restart'`
    - ClientToServerEvents maps GAME_GK_RESTART to `(choice: 'kick' | 'throw' | 'movement') => void`
    - packages/shared typechecks once teams.ts and test fixtures are updated (Task 2/3)
  </behavior>
  <action>
    In `packages/shared/src/types.ts`, add a required `highPass: number` field to the
    `PlayerPiece` interface, placed immediately after `aerialAbility: number`. Document
    via JSDoc that outfielders carry a meaningful value and GKs carry 0 (D-04, D-05).
    Add an optional field `lastDiceRoll?: { rolls: number[]; context: string } | null` to
    the `GameState` interface, placed after `pendingFreeMove` (D-11). Document the context
    string vocabulary: 'PASS_ACCURACY' | 'SHOT_DUEL' | 'HEADING_DUEL' | 'LOOSE_BALL' | 'GK_KICK'.
    Do NOT change `GamePhase` — it already contains 'GK_RESTART'.

    In `packages/shared/src/events.ts`, add `GAME_GK_RESTART: 'game:gk-restart'` to the
    `ClientEvents` const object (after GAME_ROLL). Add the mapping
    `[ClientEvents.GAME_GK_RESTART]: (choice: 'kick' | 'throw' | 'movement') => void;` to the
    `ClientToServerEvents` interface (after the GAME_ROLL entry). Leave ServerEvents and
    ServerToClientEvents unchanged — GAME_STATE and GAME_ERROR already cover the responses (D-10).

  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "^#" | grep -E "highPass|teams.ts|shotValidator|test" || true</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `highPass: number` inside the PlayerPiece interface
    - types.ts contains `lastDiceRoll?:` with `rolls: number[]` and `context: string`
    - events.ts contains `GAME_GK_RESTART: 'game:gk-restart'`
    - events.ts ClientToServerEvents contains `(choice: 'kick' | 'throw' | 'movement') => void`
    - GamePhase union is unchanged (still includes 'GK_RESTART', no new members added)
  </acceptance_criteria>
  <done>highPass and lastDiceRoll types exist; GAME_GK_RESTART event is declared and typed.</done>
</task>

<task type="auto">
  <name>Task 2: Add highPass values to all 22 players and update teams.test.ts</name>
  <files>packages/shared/src/teams.ts, packages/shared/src/teams.test.ts</files>
  <read_first>
    - packages/shared/src/teams.ts (HOME_TEAM and AWAY_TEAM arrays; each player object lists 9 attributes ending in aerialAbility; GK is index 0 of each array with role 'GK')
    - packages/shared/src/teams.test.ts (ATTRIBUTES array lines 4-14; the >= 1 assertion at line 27)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md (D-04, D-05, D-06, D-07)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-RESEARCH.md (Pitfalls 2, 6, 7; recommended highPass ranges)
  </read_first>
  <action>
    In `packages/shared/src/teams.ts`, add a `highPass: number` field to every one of the
    22 player objects in HOME_TEAM and AWAY_TEAM, inserted after `aerialAbility`. Use these
    role-based values (A2-flagged balance values, adjustable later): GK → `highPass: 0`;
    DEF → `highPass: 4`; MID → `highPass: 6`; FWD → `highPass: 5`. Per D-05 and D-06 also set
    outfielders (DEF/MID/FWD) to `aerialAbility: 0` and `handling: 0`, and keep GK values as-is
    (GK aerialAbility and handling stay meaningful). Do not change any other attribute values.

    In `packages/shared/src/teams.test.ts`, add `'highPass'` to the `ATTRIBUTES` array. Replace
    the blanket `expect(val).toBeGreaterThanOrEqual(1)` assertion with role-aware minimums: for
    `aerialAbility`, `handling`, and `highPass` the minimum is 0 when the player's `role` is not
    'GK'; for all other attribute/role combinations the minimum stays 1. Keep the `<= 10` upper
    bound and the `Number.isInteger` check for all attributes. This resolves Pitfalls 6 and 7.

  </action>
  <verify>
    <automated>cd packages/shared && npx vitest run teams.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - teams.ts: every player object contains a `highPass:` field (22 occurrences)
    - teams.ts: every GK object has `highPass: 0`
    - teams.test.ts ATTRIBUTES array contains 'highPass'
    - teams.test.ts allows 0 for aerialAbility/handling/highPass on non-GK roles
    - `npx vitest run teams.test.ts` exits 0
  </acceptance_criteria>
  <done>All 22 players have highPass; teams.test.ts passes with role-aware attribute ranges.</done>
</task>

<task type="auto">
  <name>Task 3: Fix validatePassAccuracy (D-14) and validateShotDuel tie outcome (D-13/D-17)</name>
  <files>packages/shared/src/passValidator.ts, packages/shared/src/shotValidator.ts, packages/shared/src/shotValidator.test.ts</files>
  <read_first>
    - packages/shared/src/passValidator.ts (validatePassAccuracy line 52: attribute = passType === 'HIGH' ? piece.aerialAbility : piece.dribbling; AccuracyResult union lines 41-43)
    - packages/shared/src/shotValidator.ts (ShotDuelResult union lines 8-11; tie comment line 32; comparison lines 33-36)
    - packages/shared/src/shotValidator.test.ts (mkPiece fixture lines 5-23 — needs highPass; tie test lines 31-34)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md (D-13, D-14, D-15, D-16, D-17)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-PATTERNS.md (shotValidator + shotValidator.test sections)
  </read_first>
  <behavior>
    - validatePassAccuracy uses piece.highPass for the HIGH case, piece.dribbling for LONG (unchanged)
    - validateShotDuel returns { outcome: 'LOOSE_BALL' } when shooterScore === gkScore
    - validateShotDuel still returns GOAL when shooter > gk, SAVE when gk > shooter, AUTO_MISS on natural 1
    - ShotDuelResult union includes the { outcome: 'LOOSE_BALL' } member
    - shotValidator.test.ts mkPiece includes highPass; the tie test expects LOOSE_BALL
  </behavior>
  <action>
    In `packages/shared/src/passValidator.ts`, change line 52 so the HIGH branch selects
    `piece.highPass` instead of `piece.aerialAbility` (LONG stays `piece.dribbling`) per D-14.
    Update any nearby JSDoc that claims High Pass uses aerialAbility. Leave the `AccuracyResult`
    union and `validatePass` unchanged (D-15, D-16 — inaccurate High and Long both keep
    `{ accurate: false; triggerLooseBall: true }`).

    In `packages/shared/src/shotValidator.ts`, add `| { outcome: 'LOOSE_BALL' }` to the
    `ShotDuelResult` union (D-17). Change the duel comparison so that after the GOAL check
    (`shooterScore > gkScore`), an explicit equality branch `shooterScore === gkScore` returns
    `{ outcome: 'LOOSE_BALL' }`, and only the remaining (gk strictly greater) case returns
    `{ outcome: 'SAVE', needsHandlingCheck: true }` (D-13). Remove the "Ties go to the GK" comment.
    Keep the SHOT-03 natural-1 AUTO_MISS guard and validateGKDive / validateHandlingCheck unchanged (D-18).

    In `packages/shared/src/shotValidator.test.ts`, add `highPass: 5` to the mkPiece fixture
    object (so it satisfies the updated PlayerPiece type). Change the tie test (currently
    "ties go to the GK (SAVE)") to assert `result.outcome` is `'LOOSE_BALL'` and reword its
    description to reference D-13. Add a new test asserting that when gkScore strictly exceeds
    shooterScore the outcome is `'SAVE'` with `needsHandlingCheck === true`.

  </action>
  <verify>
    <automated>cd packages/shared && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - passValidator.ts contains `passType === 'HIGH' ? piece.highPass`
    - passValidator.ts no longer references `piece.aerialAbility` in validatePassAccuracy
    - shotValidator.ts ShotDuelResult union contains `{ outcome: 'LOOSE_BALL' }`
    - shotValidator.ts contains a `shooterScore === gkScore` branch returning LOOSE_BALL
    - shotValidator.test.ts mkPiece contains `highPass`
    - shotValidator.test.ts tie test asserts `'LOOSE_BALL'`
    - `npx vitest run` in packages/shared exits 0 (full shared suite green)
  </acceptance_criteria>
  <done>High Pass accuracy uses highPass; shot ties produce LOOSE_BALL; full shared test suite passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                              |
| ---------- | -------------------------------------------------------------------------------------------------------- |
| (none new) | This plan is pure shared-package type/data/logic; no network or untrusted-input boundary is crossed here |

## STRIDE Threat Register

| Threat ID | Category               | Component                                            | Disposition | Mitigation Plan                                                                                                                                                                                 |
| --------- | ---------------------- | ---------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-01   | Tampering              | ShotDuelResult / AccuracyResult discriminated unions | mitigate    | Tie/accuracy semantics are pure functions with exhaustive union members; covered by shared unit tests (shotValidator.test.ts, passValidator.test.ts) so a regression is caught before broadcast |
| T-05-02   | Information Disclosure | dice/RNG usage                                       | accept      | No RNG is introduced in packages/shared in this plan; dice remain server-only (D-08). Enforced by absence of any crypto/Math.random import in shared (verified by existing shared compile)      |
| T-05-SC   | Tampering              | npm/pip/cargo installs                               | accept      | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: none); nothing to verify                                                                                                |

</threat_model>

<verification>
- `cd packages/shared && npx vitest run` exits 0 (all shared tests green)
- `cd packages/shared && npx tsc --noEmit -p tsconfig.json` reports no errors
- `grep -rn "piece.aerialAbility" packages/shared/src/passValidator.ts` returns nothing in validatePassAccuracy
</verification>

<success_criteria>

- highPass exists on PlayerPiece (all 22 fixtures populated) and lastDiceRoll exists on GameState
- GAME_GK_RESTART event declared and typed
- validatePassAccuracy uses highPass for HIGH; validateShotDuel ties → LOOSE_BALL
- packages/shared compiles and its full test suite passes
  </success_criteria>

<output>
Create `.planning/phases/05-dice-resolver-all-resolution-branches/05-01-SUMMARY.md` when done
</output>
