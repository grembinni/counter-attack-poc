---
phase: 05-dice-resolver-all-resolution-branches
plan: 03
type: execute
wave: 3
depends_on: ['05-02']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/gameEngine.test.ts
  - packages/server/src/__tests__/game.integration.test.ts
autonomous: true
requirements: [SHOT-05]
user_setup: []
must_haves:
  truths:
    - "After a GK catch (phase GK_RESTART), the GK's team can choose kick, throw, or movement"
    - "Kick applies High Pass accuracy using the GK's highPass attribute; inaccurate → Loose Ball"
    - 'Quick throw keeps possession with the GK and starts a Movement Phase (v1: ball delivery is via the subsequent movement phase; targetHex-based 11-hex throw delivery deferred to Phase 7)'
    - "Movement transitions to MOVEMENT with attackingTeam set to the GK's team"
    - "game:gk-restart is rejected unless phase is GK_RESTART, the socket controls the GK's team, and the choice is valid"
    - 'packages/server compiles and its full test suite passes'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applyGKRestart with kick/throw/movement branches'
      contains: 'applyGKRestart'
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'game:gk-restart handler + controlsGKTeam guard'
      contains: 'GAME_GK_RESTART'
  key_links:
    - from: 'packages/server/src/gameHandlers.ts'
      to: 'applyGKRestart'
      via: 'game:gk-restart handler dispatches by choice'
      pattern: "applyGKRestart\\("
    - from: 'packages/server/src/gameHandlers.ts'
      to: 'ball.carrierId GK piece'
      via: 'controlsGKTeam derives GK team from ball carrier'
      pattern: 'controlsGKTeam'
---

<objective>
Implement the goalkeeper restart (SHOT-05): after the GK catches the ball the FSM is in
GK_RESTART, and the GK's team chooses kick (High Pass accuracy with the GK's highPass),
quick throw (Standard Pass distance, uninterceptable), or start a Movement Phase. Add the
`applyGKRestart` engine function and the `game:gk-restart` handler with a GK-team guard and
payload validation (D-22, D-23, D-24, D-25, D-26).

Purpose: Closes the only Phase 5 requirement not covered by game:roll, completing the
movement → pass → shot → save → GK restart action loop on the server.

Output: A working GK restart flow with passing server tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-PATTERNS.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-VALIDATION.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add applyGKRestart engine function with kick/throw/movement branches</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (apply* patterns; applyRoll added in Plan 02; immutable spread + lastDiceRoll convention)
    - packages/shared/src/passValidator.ts (validatePassAccuracy signature; PASS_RANGE STANDARD=11)
    - packages/shared/src/scoreUtils.ts (computeLooseBall)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md (D-24, D-25, D-26)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-RESEARCH.md (GK restart flow diagram; Open Question 3 gkTeam via ball.carrierId)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-PATTERNS.md (ApplyGKRestartResult union; applyGKRestart signature accepts a rollDice fn)
  </read_first>
  <behavior>
    - applyGKRestart(state, choice, rollDie) returns ApplyGKRestartResult
    - choice 'kick': validatePassAccuracy(gk, 'HIGH', rollDie(), []) using the GK piece (ball carrier); accurate → ball at intended target, phase MOVEMENT, attackingTeam = GK team; inaccurate → computeLooseBall from ball.position, phase MOVEMENT; sets lastDiceRoll context 'GK_KICK'
    - choice 'throw': ball remains uninterceptable, phase MOVEMENT, attackingTeam = GK team, no accuracy roll, lastDiceRoll null
    - choice 'movement': phase MOVEMENT, attackingTeam = GK team, ball stays with GK, lastDiceRoll null
    - invalid phase (not GK_RESTART) → { ok: false, reason: 'WRONG_PHASE' }
    - invalid choice → { ok: false, reason: 'INVALID_CHOICE' }
  </behavior>
  <action>
    In `packages/server/src/gameEngine.ts`, export `ApplyGKRestartResult` union:
    `{ ok: false; reason: 'WRONG_PHASE' | 'WRONG_TEAM' | 'INVALID_CHOICE' } | { ok: true; state: GameState }`.
    Export `applyGKRestart(state: GameState, choice: 'kick' | 'throw' | 'movement', rollDie: () => number): ApplyGKRestartResult`.
    The rollDie function is injected (the handler passes rollDice) so the engine stays pure and
    testable. Guard: if `state.phase !== 'GK_RESTART'` return WRONG_PHASE. Determine the GK piece
    via `state.ball.carrierId` lookup; the GK's teamId is the restart team. Validate `choice` is one
    of the three literals, else INVALID_CHOICE.

    - 'kick': call `validatePassAccuracy(gk, 'HIGH', rollDie(), [])`. On accurate, set phase
      'MOVEMENT', attackingTeam = gk.teamId, keep ball with the GK (carrierId = gk.id) — the
      intended-target landing is deferred to the movement/pass flow; set lastDiceRoll
      { rolls: [<the rolled value>], context: 'GK_KICK' }. On inaccurate, compute
      `computeLooseBall(state.ball.position, rollDie(), rollDie())`, set ball.position to landing,
      carrierId null, phase 'MOVEMENT', attackingTeam = gk.teamId, lastDiceRoll context 'GK_KICK'
      with all rolled values included.
    - 'throw': set phase 'MOVEMENT', attackingTeam = gk.teamId, ball stays with the GK
      (carrierId = gk.id), lastDiceRoll = null (uninterceptable, no accuracy check, D-25).
      v1 SCOPE CONSTRAINT (deliberate, do not treat as a bug): in v1 the throw is implemented as a
      movement-phase start with the ball held by the GK; the ball's destination is implicit in the
      subsequent movement phase. The targetHex delivery mechanic — D-25's full intent of placing the
      ball up to 11 hexes away on the throw itself — requires a `targetHex` parameter on
      `game:gk-restart`, which is deferred to Phase 7 client integration when the click-to-target UI
      is built. This means the v1 'throw' branch and the 'movement' branch produce the same engine
      state today; they are kept as distinct choices so the Phase 7 targetHex work only has to extend
      the 'throw' branch, not reintroduce it.
    - 'movement': set phase 'MOVEMENT', attackingTeam = gk.teamId, ball stays with the GK,
      lastDiceRoll = null (D-26).

    Use immutable spread; append no event unless an existing ActionEvent variant fits (do not invent
    new ActionEvent types in this plan — keep eventLog unchanged for restart). Named exports, `.js` imports.

    Note for the executor (do not silently reduce scope): D-24's "GK may not kick into the opposite
    final third" range restriction is listed under Deferred Ideas in 05-CONTEXT.md and is NOT enforced
    in this plan. Implement kick accuracy fully; the directional range restriction is out of scope here.

    In `packages/server/src/__tests__/gameEngine.test.ts`, add applyGKRestart tests: accurate kick
    (high GK highPass + favourable roll) → phase MOVEMENT with attackingTeam = GK team; inaccurate kick
    → ball moved via loose ball, carrierId null; throw → phase MOVEMENT, ball still with GK, lastDiceRoll
    null; movement → phase MOVEMENT, attackingTeam = GK team; WRONG_PHASE when called outside GK_RESTART;
    INVALID_CHOICE for a bad choice value. Inject a deterministic rollDie stub (e.g. `() => 6`) so the
    accuracy outcome is predictable.

  </action>
  <verify>
    <automated>cd packages/server && npx vitest run gameEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts exports `applyGKRestart` and `ApplyGKRestartResult`
    - applyGKRestart accepts an injected `rollDie: () => number` (does not import diceUtils)
    - kick branch calls validatePassAccuracy with 'HIGH' and the GK piece
    - throw and movement branches set phase 'MOVEMENT' and attackingTeam to the GK's team
    - WRONG_PHASE returned outside GK_RESTART; INVALID_CHOICE for bad choice
    - `npx vitest run gameEngine.test.ts` exits 0
  </acceptance_criteria>
  <done>applyGKRestart resolves all three restart choices and is covered by passing engine tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the game:gk-restart handler with GK-team guard and payload validation</name>
  <files>packages/server/src/gameHandlers.ts, packages/server/src/__tests__/game.integration.test.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (existing handler skeleton; socketTeam helper line 20; isActivePlayer line 26; game:roll handler added in Plan 02; import block)
    - packages/server/src/roomStore.ts (Room type, broadcastState)
    - packages/shared/src/events.ts (GAME_GK_RESTART added in Plan 01; payload is choice: 'kick'|'throw'|'movement')
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md (D-22, D-23)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-PATTERNS.md (controlsGKTeam helper; game:gk-restart handler skeleton; payload validation)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-RESEARCH.md (Pitfall 5 GK team guard; Security V4/V5)
  </read_first>
  <action>
    In `packages/server/src/gameHandlers.ts`, import `applyGKRestart` from './gameEngine.js'
    (rollDice is already imported from Plan 02). Add a `controlsGKTeam(socket, room): boolean`
    helper alongside the existing helpers: return false if gameState is null or ball.carrierId is
    null; look up the piece whose id === ball.carrierId; return `socketTeam(socket, room) === gkPiece.teamId`.
    This derives the GK team from ball ownership (Open Question 3) and avoids socket.rooms (Pitfall 2).

    Add a `socket.on(ClientEvents.GAME_GK_RESTART, (choice) => { ... })` handler following the same
    mutex pattern: read roomCode; `if (!room || room.isProcessing) return;` set isProcessing true in
    try / clear in finally. In try, in order: (1) phase guard — if gameState null or
    `room.gameState.phase !== 'GK_RESTART'`, emit GAME_ERROR 'WRONG_PHASE' + broadcastState + return;
    (2) team guard — if `!controlsGKTeam(socket, room)`, emit GAME_ERROR 'WRONG_TEAM' + broadcastState
    + return; (3) payload validation — if choice is not one of 'kick'/'throw'/'movement', emit
    GAME_ERROR 'INVALID_CHOICE' + broadcastState + return (V5 — never trust client input); (4) call
    `applyGKRestart(room.gameState, choice, rollDice)`; if `!result.ok` emit GAME_ERROR result.reason +
    broadcastState + return; else `room.gameState = result.state` and `broadcastState(io, room)`.

    In `packages/server/src/__tests__/game.integration.test.ts`, add scenarios that drive a room
    into GK_RESTART (e.g. via a SHOT that resolves to SAVE+CAUGHT using the game:roll path from Plan 02,
    or by directly seeding room.gameState into GK_RESTART with the GK as ball carrier): (a) the GK
    team's socket emits game:gk-restart 'movement' and both clients receive a game:state with phase
    MOVEMENT and attackingTeam = GK team; (b) the non-GK socket emits game:gk-restart and receives a
    game:error 'WRONG_TEAM' with phase unchanged; (c) the GK socket emits an invalid choice and
    receives game:error 'INVALID_CHOICE'.

  </action>
  <verify>
    <automated>cd packages/server && npx vitest run && cd ../.. && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - gameHandlers.ts registers a GAME_GK_RESTART handler with isProcessing in try/finally
    - a controlsGKTeam helper derives the GK team from ball.carrierId
    - the handler emits WRONG_PHASE (not GK_RESTART), WRONG_TEAM (not GK team), and INVALID_CHOICE (bad payload), each with a snap-back broadcastState
    - the handler calls applyGKRestart(room.gameState, choice, rollDice) and broadcasts on success
    - game.integration.test.ts covers movement choice success, WRONG_TEAM rejection, and INVALID_CHOICE rejection
    - `npx vitest run` in packages/server exits 0 and `pnpm -r build` succeeds
  </acceptance_criteria>
  <done>game:gk-restart resolves all three choices server-side, rejects unauthorized/invalid input, and the full repo builds with green server tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                                                              |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client → server (Socket.io) | The `game:gk-restart` event and its `choice` payload arrive from an untrusted client and must be validated for phase, team ownership, and allowed values |

## STRIDE Threat Register

| Threat ID | Category          | Component                            | Disposition | Mitigation Plan                                                                                                           |
| --------- | ----------------- | ------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-05-07   | Tampering         | game:gk-restart team guard           | mitigate    | controlsGKTeam derives the GK team from ball.carrierId and rejects non-GK sockets with WRONG_TEAM + snap-back (Pitfall 5) |
| T-05-08   | Tampering         | game:gk-restart choice payload       | mitigate    | choice is validated against ['kick','throw','movement'] before routing; invalid → INVALID_CHOICE + snap-back (ASVS V5)    |
| T-05-09   | Spoofing          | game:gk-restart in wrong phase       | mitigate    | phase guard requires GK_RESTART; otherwise WRONG_PHASE + snap-back                                                        |
| T-05-10   | Denial of Service | double-click race on game:gk-restart | mitigate    | room.isProcessing mutex set/cleared in try/finally; second event silently dropped                                         |
| T-05-SC   | Tampering         | npm/pip/cargo installs               | accept      | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: none)                                             |

</threat_model>

<verification>
- `cd packages/server && npx vitest run` exits 0 (all server tests green)
- `pnpm -r build` succeeds across all packages
- `grep -c "GAME_GK_RESTART" packages/server/src/gameHandlers.ts` returns >= 1
- `grep -c "controlsGKTeam" packages/server/src/gameHandlers.ts` returns >= 2 (definition + use)
</verification>

<success_criteria>

- GK restart kick/throw/movement all resolve server-side (SHOT-05)
- unauthorized team, wrong phase, and invalid choice are rejected with game:error + snap-back
- full server test suite passes and the whole repo builds
  </success_criteria>

<output>
Create `.planning/phases/05-dice-resolver-all-resolution-branches/05-03-SUMMARY.md` when done
</output>
