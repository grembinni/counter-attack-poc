---
phase: 05-dice-resolver-all-resolution-branches
plan: 02
type: execute
wave: 2
depends_on: ['05-01']
files_modified:
  - packages/server/src/diceUtils.ts
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/diceUtils.test.ts
  - packages/server/src/__tests__/gameEngine.test.ts
  - packages/server/src/__tests__/game.integration.test.ts
autonomous: true
requirements: [DICE-01, DICE-02]
user_setup: []
must_haves:
  truths:
    - 'All dice come from crypto.randomInt via rollDice(); the stubDice() function no longer exists'
    - 'A game:roll event from the active player rolls dice, applies the phase-correct resolution, and broadcasts one game:state with lastDiceRoll populated'
    - 'PASS, SHOT, HEADER, and LOOSE_BALL phases each route to a correct apply* branch'
    - 'A tied shot duel transitions to LOOSE_BALL; a save with caught ball transitions to GK_RESTART'
    - 'game:roll in a non-dice phase or from the wrong team is rejected with game:error and a snap-back broadcast'
    - 'packages/server compiles and its full test suite passes'
  artifacts:
    - path: 'packages/server/src/diceUtils.ts'
      provides: 'rollDice() crypto wrapper'
      contains: 'randomInt'
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applyRoll dispatcher + resolution branches; stubDice removed'
      contains: 'applyRoll'
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'game:roll handler with isProcessing mutex + guards'
      contains: 'GAME_ROLL'
  key_links:
    - from: 'packages/server/src/gameHandlers.ts'
      to: 'applyRoll'
      via: 'game:roll handler calls applyRoll with rollDice() dice'
      pattern: "applyRoll\\("
    - from: 'packages/server/src/gameEngine.ts'
      to: 'validateShotDuel / validatePassAccuracy / validateHeading / computeLooseBall'
      via: 'branch dispatch by state.phase'
      pattern: 'validateShotDuel|validatePassAccuracy|computeLooseBall'
    - from: 'packages/server/src/gameHandlers.ts'
      to: 'broadcastState'
      via: 'single broadcast after every resolution'
      pattern: "broadcastState\\(io, room\\)"
---

<objective>
Replace the deterministic dice stub with cryptographic dice and wire the `game:roll`
event end-to-end. Add `diceUtils.rollDice()`, an `applyRoll` dispatcher in the game
engine that resolves PASS / SHOT / HEADER / LOOSE_BALL phases using the existing shared
validators with injected dice, and a `game:roll` handler that follows the established
isProcessing-mutex + guard + single-broadcast pattern. Every resolution embeds
`lastDiceRoll` in the broadcast state (D-08, D-09, D-10, D-12, D-13, D-19, D-20, D-21, D-27, D-28).

Purpose: This is the core of DICE-01 and DICE-02 — server-authoritative crypto dice and
a single explicit player-triggered roll that resolves the current stochastic branch.

Output: Live dice resolution for passes, shots, headers, and loose balls with passing
server tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-PATTERNS.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-VALIDATION.md
@.planning/phases/05-dice-resolver-all-resolution-branches/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create diceUtils.rollDice() and remove stubDice()</name>
  <files>packages/server/src/diceUtils.ts, packages/server/src/__tests__/diceUtils.test.ts, packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (stubDice at lines 22-24; sole call site in applyMove steal path lines 77-86; crypto import line 1; computeCombinedScore already imported line 13)
    - packages/server/src/__tests__/gameEngine.test.ts (existing describe/it/expect structure and import style)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md (D-08, D-09)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-RESEARCH.md (Pitfall 1: stubDice call site; Open Question 2: steal threshold)
  </read_first>
  <behavior>
    - rollDice() returns an integer in [1,6] across all calls
    - rollDice() produces at least 3 distinct values across 20 calls (statistical non-flaky)
    - stubDice() is deleted from gameEngine.ts; its single steal-path call site uses rollDice()
    - the steal path keeps the combined >= 10 threshold using rollDice() instead of the constant 3
  </behavior>
  <action>
    Create `packages/server/src/diceUtils.ts` with a named export `rollDice(): number` that
    returns `randomInt(1, 7)` (imported from 'crypto'; min inclusive, max exclusive → 1..6).
    Add JSDoc noting this is the only permitted dice source per D-08 / DICE-01. Use a named
    export, no default.

    Create `packages/server/src/__tests__/diceUtils.test.ts` with two tests: (1) 100 calls all
    return integers in [1,6]; (2) a Set of 20 calls has size >= 3. Follow the import/describe/it
    style already used in gameEngine.test.ts; import rollDice from '../diceUtils.js'.

    In `packages/server/src/gameEngine.ts`, delete the `stubDice` function (lines 22-24). Add
    `import { rollDice } from './diceUtils.js';`. In the applyMove steal block, replace
    `const dice = stubDice();` with `const dice = rollDice();` and keep the existing
    `computeCombinedScore(defender.tackling, dice, [])` and `combined >= 10` threshold per the
    MOVE-04 rule (Open Question 2 resolution). Remove the now-stale "Phase 4 deterministic" comment.

  </action>
  <verify>
    <automated>cd packages/server && npx vitest run diceUtils.test.ts && grep -c "stubDice" src/gameEngine.ts</automated>
  </verify>
  <acceptance_criteria>
    - diceUtils.ts exports `rollDice` and imports `randomInt` from 'crypto'
    - diceUtils.test.ts exits 0
    - `grep -c "stubDice" src/gameEngine.ts` returns 0
    - gameEngine.ts imports rollDice from './diceUtils.js'
    - applyMove steal path calls rollDice() and retains the `>= 10` combined threshold
  </acceptance_criteria>
  <done>rollDice() is the sole dice source; stubDice() is gone; diceUtils tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add applyRoll dispatcher with PASS/SHOT/HEADER/LOOSE_BALL branches</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (apply* patterns: applyStartMovement, applyMove, immutable spread + eventLog append; ApplyMoveResult union shape lines 44-46)
    - packages/shared/src/shotValidator.ts (validateShotDuel — now returns GOAL/MISS/SAVE/LOOSE_BALL; validateGKDive; validateHandlingCheck)
    - packages/shared/src/passValidator.ts (validatePassAccuracy — HIGH/LONG, returns accurate or triggerLooseBall)
    - packages/shared/src/headingValidator.ts (validateHeading — ATTACKER_WINS/DEFENDER_WINS/TIE)
    - packages/shared/src/scoreUtils.ts (computeLooseBall, computeCombinedScore)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md (D-10, D-13, D-19, D-20, D-21, D-27, D-28)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-RESEARCH.md (System Architecture Diagram applyRoll dispatch; Pitfall 4 pre-generate all dice; Pitfall 8 exhaustive default)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-PATTERNS.md (gameEngine apply* extension; lastDiceRoll embedding)
  </read_first>
  <behavior>
    - applyRoll(state, ...dice) dispatches by state.phase to the correct resolution branch
    - PASS: accurate → phase SHOT; inaccurate → computeLooseBall from ball position, phase LOOSE_BALL
    - SHOT: GOAL → score++ for attackingTeam, phase KICK_OFF; LOOSE_BALL (tie) → computeLooseBall, phase MOVEMENT; SAVE → validateHandlingCheck: CAUGHT → phase GK_RESTART, SPILL → computeLooseBall, phase MOVEMENT; AUTO_MISS (natural 1) → phase MOVEMENT
    - HEADER: attacker outfield win → lock score, GK rolls aerialAbility vs locked score: GK wins → GK_RESTART, attacker wins → GOAL (score++); defender wins → MOVEMENT; tie → computeLooseBall, MOVEMENT
    - LOOSE_BALL: computeLooseBall(ball.position, dir, dist), ball.position = landing, carrierId = null, phase MOVEMENT
    - Every successful branch sets lastDiceRoll { rolls: [...all dice generated], context }
    - Unhandled phase → { ok: false, reason: 'WRONG_PHASE' }
    - All dice are generated upfront (Pitfall 4); validators receive dice as arguments only
  </behavior>
  <action>
    In `packages/server/src/gameEngine.ts`, export an `ApplyRollResult` discriminated union
    matching the existing apply* style: `{ ok: false; reason: 'WRONG_PHASE' | 'WRONG_TEAM' } | { ok: true; state: GameState }`.
    Export `applyRoll(state: GameState, ...dice: number[]): ApplyRollResult`. Pre-generate dice
    in the handler (Task 3) and pass them in; applyRoll must NOT call rollDice itself (keep the
    engine pure and deterministic for tests). Dispatch with a switch on `state.phase`:

    - case 'PASS': resolve via validatePassAccuracy using the ball carrier piece. On accurate,
      return new state with `phase: 'SHOT'`. On inaccurate, compute `computeLooseBall(state.ball.position, dice[0], dice[1])`,
      set ball.position to the result, carrierId null, `phase: 'LOOSE_BALL'`, context 'PASS_ACCURACY'.
    - case 'SHOT': identify shooter (ball carrier) and the defending GK (the GK PlayerPiece whose
      teamId is the non-attacking team). Pre-generate shooterDice, gkDice, handlingDice. Call
      validateShotDuel. GOAL → increment score for attackingTeam, `phase: 'KICK_OFF'`. LOOSE_BALL →
      computeLooseBall from ball.position, `phase: 'MOVEMENT'`. SAVE → validateHandlingCheck(gk, handlingDice):
      CAUGHT → set ball.carrierId to gk.id, ball.position to gk.position, `phase: 'GK_RESTART'`;
      SPILL → computeLooseBall, `phase: 'MOVEMENT'`. MISS/AUTO_MISS → `phase: 'MOVEMENT'`. context 'SHOT_DUEL',
      rolls = [shooterDice, gkDice, handlingDice].
    - case 'HEADER': resolve per D-27/D-28 — attacker vs nearest defender via validateHeading; if
      attacker wins, lock combinedScore, GK rolls (use computeCombinedScore(gk.aerialAbility, gkDice)) vs
      locked score: GK higher-or-equal → GK catches (ball.carrierId = gk.id, phase 'GK_RESTART'),
      attacker higher → GOAL (score++, phase 'KICK_OFF'); defender wins → phase 'MOVEMENT';
      tie → computeLooseBall, phase 'MOVEMENT'. context 'HEADING_DUEL'.
    - case 'LOOSE_BALL': landing = computeLooseBall(state.ball.position, dice[0], dice[1]); set
      ball.position = landing, carrierId null, `phase: 'MOVEMENT'`, attackingTeam unchanged. context 'LOOSE_BALL'.
    - default: return { ok: false, reason: 'WRONG_PHASE' } (Pitfall 8 — explicit, no silent no-op).

    Use immutable spread for all new state (never mutate state.pieces / state.ball / state.eventLog).
    Set `lastDiceRoll` on every ok:true return. Use named exports only, `.js` import extensions.

    In `packages/server/src/__tests__/gameEngine.test.ts`, extend with applyRoll tests: a happy
    path for each of PASS (accurate→SHOT, inaccurate→LOOSE_BALL), SHOT (GOAL increments score,
    tie→LOOSE_BALL per D-13, SAVE+caught→GK_RESTART), HEADER (attacker win→GOAL or GK catch),
    LOOSE_BALL (ball.position moves, carrierId null, phase MOVEMENT); and a WRONG_PHASE rejection
    when applyRoll is called in 'MOVEMENT'. Construct GameState fixtures inline; ensure every
    PlayerPiece fixture includes highPass (added in Plan 01).

  </action>
  <verify>
    <automated>cd packages/server && npx vitest run gameEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts exports `applyRoll` and `ApplyRollResult`
    - applyRoll has explicit branches for PASS, SHOT, HEADER, LOOSE_BALL and a default WRONG_PHASE
    - SHOT tie path produces phase 'LOOSE_BALL'; SAVE+CAUGHT produces phase 'GK_RESTART'
    - every ok:true return sets a `lastDiceRoll` with rolls + context
    - applyRoll does not call rollDice (no 'diceUtils' import added for applyRoll itself)
    - `npx vitest run gameEngine.test.ts` exits 0
  </acceptance_criteria>
  <done>applyRoll resolves all four dice phases correctly and is covered by passing engine tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the game:roll handler with mutex, guards, and broadcast</name>
  <files>packages/server/src/gameHandlers.ts, packages/server/src/__tests__/game.integration.test.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (registerGameHandlers; game:move handler lines 37-61 as the canonical mutex+guard+broadcast pattern; isActivePlayer line 26; import block lines 11-16)
    - packages/server/src/roomStore.ts (Room type, getRoom, broadcastState)
    - packages/server/src/__tests__/game.integration.test.ts (existing two-client connect/emit harness and assertion style)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-CONTEXT.md (D-10, D-12)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-PATTERNS.md (Handler skeleton; isProcessing in finally; snap-back broadcast; DICE_PHASES set)
    - .planning/phases/05-dice-resolver-all-resolution-branches/05-RESEARCH.md (Security Domain: V4 access control, double-click DoS)
  </read_first>
  <action>
    In `packages/server/src/gameHandlers.ts`, import `applyRoll` from './gameEngine.js' and
    `rollDice` from './diceUtils.js'. Define a module-level `const DICE_PHASES = new Set(['PASS','SHOT','HEADER','LOOSE_BALL'])`
    (GK_RESTART is handled by the separate gk-restart handler in Plan 03, not game:roll, per D-12/D-22).
    Add a `socket.on(ClientEvents.GAME_ROLL, () => { ... })` handler that mirrors the game:move
    structure exactly: read `socket.data.roomCode`; `if (!room || room.isProcessing) return;`
    (SC-5 double-click drop); set `room.isProcessing = true` inside a try; in `finally` set it false.
    Inside try, in order: (1) phase guard — if gameState is null or `!DICE_PHASES.has(room.gameState.phase)`,
    emit GAME_ERROR 'WRONG_PHASE', broadcastState, return; (2) team guard — if `!isActivePlayer(socket, room)`,
    emit GAME_ERROR 'WRONG_TEAM', broadcastState, return; (3) pre-generate the dice the branch may need
    (call `rollDice()` up to three times) and call `applyRoll(room.gameState, d1, d2, d3)`; if `!result.ok`
    emit GAME_ERROR result.reason + broadcastState + return; else `room.gameState = result.state` and
    `broadcastState(io, room)` (ARCH-04 single entry point). Never call io.to(...).emit directly.

    In `packages/server/src/__tests__/game.integration.test.ts`, add a scenario: drive a room into
    PASS phase, emit game:roll from the active player's socket, and assert both clients receive a
    game:state whose `lastDiceRoll` is populated and whose phase advanced (SHOT or LOOSE_BALL). Add a
    rejection scenario: emit game:roll from the non-active socket and assert a game:error 'WRONG_TEAM'
    is received by that socket and the phase is unchanged.

  </action>
  <verify>
    <automated>cd packages/server && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - gameHandlers.ts registers a GAME_ROLL handler
    - the handler sets room.isProcessing in a try and clears it in finally
    - the handler emits GAME_ERROR 'WRONG_PHASE' for non-dice phases and 'WRONG_TEAM' for the wrong team, each followed by broadcastState
    - the handler calls applyRoll with rollDice()-generated dice and broadcastState on success
    - game.integration.test.ts covers a successful game:roll (lastDiceRoll populated, phase advanced) and a WRONG_TEAM rejection
    - `npx vitest run` in packages/server exits 0 (full server suite green)
  </acceptance_criteria>
  <done>game:roll resolves the current dice phase server-side and broadcasts to both clients; full server suite passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                                       |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| client → server (Socket.io) | The `game:roll` event arrives from an untrusted client; the server must verify phase, team, and processing state before resolving |

## STRIDE Threat Register

| Threat ID | Category               | Component                      | Disposition | Mitigation Plan                                                                                                                                |
| --------- | ---------------------- | ------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-03   | Tampering              | game:roll handler              | mitigate    | isActivePlayer team guard rejects non-active sockets with WRONG_TEAM + snap-back broadcast before any dice are generated                       |
| T-05-04   | Spoofing               | game:roll in wrong phase       | mitigate    | DICE_PHASES set guard emits WRONG_PHASE + snap-back; resolution only runs for PASS/SHOT/HEADER/LOOSE_BALL                                      |
| T-05-05   | Denial of Service      | double-click race on game:roll | mitigate    | room.isProcessing mutex set/cleared in try/finally; the second concurrent event is silently dropped (SC-5)                                     |
| T-05-06   | Information Disclosure | dice randomness                | mitigate    | rollDice() uses crypto.randomInt only; no Math.random anywhere in server dice path (DICE-01, D-08); enforced by grep gate and diceUtils source |
| T-05-SC   | Tampering              | npm/pip/cargo installs         | accept      | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: none)                                                                  |

</threat_model>

<verification>
- `cd packages/server && npx vitest run` exits 0 (all server tests green)
- `grep -rn "Math.random" packages/server/src` returns nothing in the dice path
- `grep -c "stubDice" packages/server/src/gameEngine.ts` returns 0
- `grep -c "crypto" packages/client/src` shows the client introduces no RNG (still no dice generation client-side)
</verification>

<success_criteria>

- rollDice() is the sole dice source; stubDice removed
- game:roll resolves PASS/SHOT/HEADER/LOOSE_BALL and broadcasts lastDiceRoll
- wrong-phase and wrong-team rolls rejected with game:error + snap-back
- full server test suite passes
  </success_criteria>

<output>
Create `.planning/phases/05-dice-resolver-all-resolution-branches/05-02-SUMMARY.md` when done
</output>
