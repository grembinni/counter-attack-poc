---
phase: 01-monorepo-scaffold-shared-types
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - eslint.config.js
  - .prettierrc
  - .prettierignore
  - .gitignore
  - .husky/pre-commit
  - .nvmrc
autonomous: true
requirements:
  - ARCH-02
user_setup:
  - service: pnpm
    why: "pnpm 9.x is the workspace manager and is not installed in this environment (per RESEARCH.md Environment Availability)"
    install_cmd: "npm install -g pnpm@9.15.9   # OR: corepack enable && corepack prepare pnpm@9.15.9 --activate"

must_haves:
  truths:
    - "Running `pnpm install` from the repo root completes with exit code 0"
    - "`pnpm -v` reports a 9.x version"
    - "Running `pnpm exec prettier --check .` succeeds (or reports only expected style differences) without crashing"
    - "Running `pnpm exec eslint .` succeeds without crashing (flat config loads)"
    - "A commit to the repo triggers the `.husky/pre-commit` hook which runs lint-staged"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package globs"
      contains: "packages/*"
    - path: "package.json"
      provides: "Root package metadata, dev dependencies, scripts, lint-staged config"
      contains: "\"packageManager\": \"pnpm@9.15.9\""
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript compiler options for all packages (D-07)"
      contains: "\"strict\": true"
    - path: "eslint.config.js"
      provides: "Flat ESLint v9 config with typescript-eslint + eslint-config-prettier (D-10)"
      contains: "projectService"
    - path: ".prettierrc"
      provides: "Prettier formatting config"
    - path: ".husky/pre-commit"
      provides: "Git pre-commit hook running lint-staged (D-11)"
      contains: "lint-staged"
  key_links:
    - from: "package.json"
      to: ".husky/pre-commit"
      via: "prepare script (`husky`)"
      pattern: "\"prepare\":\\s*\"husky\""
    - from: ".husky/pre-commit"
      to: "lint-staged config"
      via: "pnpm exec lint-staged invocation"
      pattern: "lint-staged"
---

<objective>
Bootstrap the pnpm monorepo root: workspace declaration, shared TypeScript base config, ESLint v9 flat config, Prettier, Husky v9 + lint-staged, and root devDependencies. This wave 0 plan establishes the foundation that the three workspace packages (shared, server, client) will all extend.

Purpose: Phase 1 success criterion 1 requires `pnpm install` from the repo root to succeed. That requires the workspace file, root package.json, and devDependencies to exist before any package can install. Without this foundation, no later plan can compile or test anything (per D-06: resolution via pnpm workspace symlinks + package.json exports).

Output: A working repo root where `pnpm install` succeeds, ESLint/Prettier/Husky run, and a `tsconfig.base.json` exists for downstream packages to extend (per D-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-monorepo-scaffold-shared-types/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold-shared-types/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold-shared-types/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify pnpm is installed and create workspace + root package.json</name>
  <files>pnpm-workspace.yaml, package.json, .gitignore, .nvmrc</files>
  <read_first>
    - .planning/phases/01-monorepo-scaffold-shared-types/01-RESEARCH.md (Environment Availability section, Standard Stack versions, Pattern 1)
    - .planning/phases/01-monorepo-scaffold-shared-types/01-CONTEXT.md (decisions D-06, D-11)
    - CLAUDE.md (stack table — pnpm 9.x, Node 22 LTS)
  </read_first>
  <action>
    First verify pnpm is available: run `pnpm -v` and confirm it reports a 9.x version. If pnpm is not installed, halt and surface a user_setup checkpoint — do NOT attempt to install global tools without user confirmation. (Per D-06, pnpm workspaces are the resolution mechanism — no fallback exists.)

    Create `pnpm-workspace.yaml` declaring a single `packages` entry containing the glob `packages/*`.

    Create `.nvmrc` containing the single line `22` (Node.js 22 LTS, per CLAUDE.md stack table).

    Create `.gitignore` covering: `node_modules/`, `dist/`, `.DS_Store`, `*.log`, `.env`, `.env.local`, `.husky/_/`, and editor folders (`.vscode/`, `.idea/`).

    Create root `package.json` with the following keys:
    - `"name": "counter-attack-poc"`
    - `"private": true`
    - `"version": "0.0.1"`
    - `"packageManager": "pnpm@9.15.9"` (per RESEARCH.md Open Question 3, enables Corepack enforcement)
    - `"type": "module"` (ESM-first per D-08 / RESEARCH.md Pitfall 3)
    - `"scripts"` containing:
      - `"build": "pnpm -r build"` (recursive, NOT --parallel — per RESEARCH.md Anti-Pattern; topological order required)
      - `"test": "pnpm -r test"`
      - `"lint": "eslint ."`
      - `"format": "prettier --write ."`
      - `"format:check": "prettier --check ."`
      - `"typecheck": "pnpm -r typecheck"`
      - `"prepare": "husky"` (per RESEARCH.md Pattern 6 / Pitfall 4)
    - `"lint-staged"` block per RESEARCH.md Pattern 6:
      - `"*.{ts,tsx}": ["eslint --fix", "prettier --write"]`
      - `"*.{json,md,yaml,yml}": ["prettier --write"]`
    - An empty `"devDependencies"` object (populated in Task 2)
    - An `"engines"` block with `"node": ">=22"` and `"pnpm": ">=9"`

    Do NOT yet add workspace packages — those are created in plan 02 / 03.
  </action>
  <verify>
    <automated>pnpm -v &amp;&amp; node -e "const p=require('./package.json'); if(p.packageManager!=='pnpm@9.15.9') process.exit(1); if(!p.scripts.prepare||p.scripts.prepare!=='husky') process.exit(2); if(!p.scripts.build||!/pnpm -r build/.test(p.scripts.build)) process.exit(3); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -v` outputs a version starting with `9.` (exit 0)
    - `pnpm-workspace.yaml` exists and contains the line `  - 'packages/*'` (or equivalent YAML list entry)
    - `package.json` exists with `"packageManager": "pnpm@9.15.9"`, `"private": true`, `"type": "module"`
    - `package.json` `scripts.prepare` equals exactly `"husky"`
    - `package.json` `scripts.build` equals exactly `"pnpm -r build"` (no `--parallel` flag)
    - `package.json` contains a `lint-staged` block keyed on `*.{ts,tsx}` running `eslint --fix` then `prettier --write`
    - `.gitignore` contains lines `node_modules/`, `dist/`, and `.env`
    - `.nvmrc` contains the single line `22`
  </acceptance_criteria>
  <done>Workspace and root package metadata exist; pnpm is confirmed available; no devDeps installed yet but `package.json` declares all required scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Install root devDependencies and create tsconfig.base.json</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.base.json</files>
  <read_first>
    - .planning/phases/01-monorepo-scaffold-shared-types/01-RESEARCH.md (Standard Stack table with exact versions, Pattern 3, Pitfall 2, Pitfall 5)
    - .planning/phases/01-monorepo-scaffold-shared-types/01-CONTEXT.md (decisions D-07, D-09, D-10, D-11)
    - package.json (current state after Task 1)
  </read_first>
  <action>
    Install root devDependencies via pnpm using the `-w` (workspace root) flag and exact versions from RESEARCH.md Standard Stack table:

    Run: `pnpm add -D -w typescript@5.9.3 prettier@3.8.3 eslint@9.39.4 typescript-eslint@8.60.0 eslint-config-prettier@10.1.8 husky@9.1.7 lint-staged@17.0.5`

    Verify the resulting `package.json` `devDependencies` block contains all 7 packages at the pinned versions (use caret prefix `^` to allow patch updates; pin exact for `typescript` if pnpm install resolves to a different patch).

    Create `tsconfig.base.json` at the repo root with the following `compilerOptions` (per RESEARCH.md Pattern 3 and the "Claude's Discretion" note that `typescript-eslint` strict preset is the baseline):
    - `"target": "ES2022"`
    - `"strict": true`
    - `"noUncheckedIndexedAccess": true`
    - `"exactOptionalPropertyTypes": true`
    - `"noImplicitOverride": true`
    - `"verbatimModuleSyntax": true` (per RESEARCH.md Pitfall 5 — forces `import type` discipline from day one)
    - `"isolatedModules": true`
    - `"declaration": true`
    - `"declarationMap": true`
    - `"sourceMap": true`
    - `"skipLibCheck": true`
    - `"forceConsistentCasingInFileNames": true`
    - `"esModuleInterop": true`
    - `"resolveJsonModule": true`

    IMPORTANT: Do NOT set `module` or `moduleResolution` in `tsconfig.base.json`. Per RESEARCH.md Pattern 3 and the moduleResolution split discussion, each package sets these per-package: server uses `NodeNext`, client uses `Bundler`. Setting them at base would force one mode on all packages.

    Do NOT set `include`, `outDir`, or `rootDir` — those are per-package.
  </action>
  <verify>
    <automated>pnpm install &amp;&amp; node -e "const p=require('./package.json'); const need=['typescript','prettier','eslint','typescript-eslint','eslint-config-prettier','husky','lint-staged']; const d=p.devDependencies||{}; for(const k of need){ if(!d[k]){ console.error('missing',k); process.exit(1);} } const t=require('./tsconfig.base.json'); const co=t.compilerOptions; if(!co.strict||!co.verbatimModuleSyntax||!co.noUncheckedIndexedAccess) process.exit(2); if(co.module||co.moduleResolution) process.exit(3); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0 and produces `pnpm-lock.yaml`
    - `package.json` `devDependencies` contains entries for: `typescript`, `prettier`, `eslint`, `typescript-eslint`, `eslint-config-prettier`, `husky`, `lint-staged`
    - `tsconfig.base.json` exists with `compilerOptions.strict === true`, `compilerOptions.verbatimModuleSyntax === true`, `compilerOptions.noUncheckedIndexedAccess === true`
    - `tsconfig.base.json` does NOT define `module` or `moduleResolution` keys (verified by absence — RESEARCH.md Pattern 3 split-resolution requirement)
    - `node_modules/.bin/eslint`, `node_modules/.bin/prettier`, and `node_modules/.bin/tsc` are executable
  </acceptance_criteria>
  <done>All root devDependencies installed; tsconfig.base.json holds shared strict settings; per-package tsconfigs (created in later plans) will extend it without `module`/`moduleResolution` conflicts.</done>
</task>

<task type="auto">
  <name>Task 3: Configure ESLint flat config, Prettier, and Husky pre-commit hook</name>
  <files>eslint.config.js, .prettierrc, .prettierignore, .husky/pre-commit, package.json</files>
  <read_first>
    - .planning/phases/01-monorepo-scaffold-shared-types/01-RESEARCH.md (Pattern 5 ESLint flat config, Pattern 6 Husky+lint-staged, State of the Art table)
    - .planning/phases/01-monorepo-scaffold-shared-types/01-CONTEXT.md (decisions D-09, D-10, D-11)
    - package.json (current state with lint-staged config from Task 1, devDeps from Task 2)
  </read_first>
  <action>
    Create `eslint.config.js` (ESM, flat config — per RESEARCH.md Pattern 5 and State of the Art table). The file uses `import tseslint from 'typescript-eslint'` and `import prettier from 'eslint-config-prettier'`, then exports `tseslint.config(...)` composed of:
    1. `...tseslint.configs.recommendedTypeChecked` (typescript-eslint recommended ruleset with type-aware rules)
    2. `prettier` (eslint-config-prettier — disables rules that conflict with Prettier formatting)
    3. A `languageOptions` block with `parserOptions: { projectService: true }` (per RESEARCH.md Pattern 5 — typescript-eslint v8 zero-config monorepo support)
    4. An `ignores` block listing: `**/dist/**`, `**/node_modules/**`, `**/*.config.js`, `**/*.config.ts`, `.husky/**`

    Create `.prettierrc` (JSON) with community-default values per the "Claude's Discretion" CONTEXT.md note:
    - `"printWidth": 100`
    - `"trailingComma": "all"`
    - `"singleQuote": true`
    - `"semi": true`
    - `"tabWidth": 2`
    - `"useTabs": false`
    - `"endOfLine": "lf"`

    Create `.prettierignore` listing: `node_modules`, `dist`, `pnpm-lock.yaml`, `.husky`, `coverage`.

    Initialize Husky: run `pnpm exec husky init` (per RESEARCH.md Pattern 6 — Husky v9 replaces `husky install`). This creates `.husky/pre-commit` with a default `pnpm test` line.

    Overwrite `.husky/pre-commit` so it contains exactly the single command:
    `pnpm exec lint-staged`

    Do NOT include shell shebang lines or `. "$(dirname -- "$0")/_/husky.sh"` sourcing — Husky v9 hooks are plain shell scripts (per RESEARCH.md State of the Art table).

    Husky `init` also adds `"prepare": "husky"` to `package.json`. Confirm it matches what Task 1 set; if pnpm rewrote it, restore the value `"husky"` exactly.

    Verify ESLint loads by running `pnpm exec eslint --print-config tsconfig.base.json` — it should exit 0 even with no source files yet.

    Verify Prettier loads by running `pnpm exec prettier --check .` — it should exit 0 or report only specific files needing formatting (acceptable; do not fail the task).
  </action>
  <verify>
    <automated>pnpm exec eslint --print-config tsconfig.base.json &gt; /dev/null &amp;&amp; pnpm exec prettier --check package.json &amp;&amp; test -f .husky/pre-commit &amp;&amp; grep -q "lint-staged" .husky/pre-commit &amp;&amp; node -e "const p=require('./package.json'); if(p.scripts.prepare!=='husky') process.exit(1); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` exists, uses ESM `import` syntax, and imports `typescript-eslint` and `eslint-config-prettier`
    - `eslint.config.js` body includes the literal string `projectService` (typescript-eslint v8 monorepo zero-config marker)
    - `pnpm exec eslint --print-config tsconfig.base.json` exits 0 (proves the flat config loads without parse errors)
    - `.prettierrc` exists and contains `"printWidth": 100`, `"singleQuote": true`, `"trailingComma": "all"`
    - `.prettierignore` exists and lists `node_modules`, `dist`, and `pnpm-lock.yaml`
    - `.husky/pre-commit` exists and contains the line `pnpm exec lint-staged` (verified by grep)
    - `.husky/pre-commit` does NOT contain `husky.sh` (Husky v9 hooks are plain scripts — RESEARCH.md State of the Art)
    - `package.json` `scripts.prepare` is the string `"husky"`
    - `pnpm exec prettier --check package.json` exits 0
  </acceptance_criteria>
  <done>ESLint flat config loads, Prettier config + ignore established, Husky v9 pre-commit hook is in place and will run lint-staged on every commit. Pre-commit hook will trigger on next `git commit`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — phase 1 has no runtime, no endpoints, no data) | This is a pure scaffolding phase. No process listens on a port, no user input is parsed at runtime, no secrets exist, no network traffic occurs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm/pnpm package install (typescript, eslint, prettier, husky, lint-staged, typescript-eslint, eslint-config-prettier) | accept | All 7 root devDeps verified on npm registry in RESEARCH.md "Package Legitimacy Audit"; ages range 8–14 years; all listed as Approved. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages in this plan, so no human-verify checkpoints required. |
| T-01-01 | (N/A) | (no application code) | accept | Phase is pure config + tooling; no application logic to threat-model. Threat modelling resumes in Phase 3 (server endpoints) and Phase 5 (server-side dice). |

**Rationale for minimal threat model:** RESEARCH.md "Security Domain" section explicitly states: "This phase has no user-facing endpoints, no authentication, no data storage, and no network traffic. Security controls do not apply to a scaffolding phase." ASVS V2–V6 marked as not applicable.
</threat_model>

<verification>
After all tasks complete:
1. `pnpm -v` reports `9.x` (exit 0)
2. `pnpm install` from repo root exits 0
3. `pnpm exec eslint --print-config tsconfig.base.json` exits 0 (proves flat config loads)
4. `pnpm exec prettier --check package.json` exits 0
5. `.husky/pre-commit` contains `pnpm exec lint-staged`
6. `tsconfig.base.json` defines `strict: true` and does NOT define `module` or `moduleResolution`
7. `package.json` `packageManager` field equals `pnpm@9.15.9`
</verification>

<success_criteria>
- All eight artifact files (`package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json`, `eslint.config.js`, `.prettierrc`, `.prettierignore`, `.gitignore`, `.husky/pre-commit`) exist with the required contents
- `pnpm install` succeeds with all 7 dev-dependencies installed
- ESLint flat config and Prettier both load and run without crashing
- Husky pre-commit hook will execute `lint-staged` on the next `git commit`
- Foundation is ready for plan 02 (`packages/shared`) to extend `tsconfig.base.json` and use `workspace:*` resolution
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-scaffold-shared-types/01-01-SUMMARY.md` when done. Include:
- Confirmed pnpm version
- List of installed devDependency versions actually resolved by pnpm
- Path to `.husky/pre-commit` and its final content
- Any deviations from research (e.g., if pnpm resolved a different patch version of typescript)
- Confirmation that `tsconfig.base.json` does NOT define `module`/`moduleResolution`
</output>
