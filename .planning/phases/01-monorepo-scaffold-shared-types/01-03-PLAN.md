---
phase: 01-monorepo-scaffold-shared-types
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
  - packages/client/package.json
  - packages/client/tsconfig.json
  - packages/client/src/main.ts
autonomous: true
requirements:
  - ARCH-02

must_haves:
  truths:
    - "`packages/server` declares `@counter-attack/shared: workspace:*` and can import from it without path hacks"
    - "`packages/client` declares `@counter-attack/shared: workspace:*` and can import from it without path hacks"
    - "`pnpm -r build` from the repo root exits 0 across all three packages in topological order"
    - "`packages/server/src/index.ts` successfully imports HexCoord and PITCH_HEXES from `@counter-attack/shared` and logs PITCH_HEXES.length to stdout (or no-op, but the import resolves)"
    - "`packages/client/src/main.ts` successfully imports types from `@counter-attack/shared`"
    - "`packages/server/tsconfig.json` uses `moduleResolution: NodeNext`; `packages/client/tsconfig.json` uses `moduleResolution: Bundler`"
  artifacts:
    - path: "packages/server/package.json"
      provides: "Server package metadata with workspace dependency on shared"
      contains: "\"@counter-attack/shared\": \"workspace:*\""
    - path: "packages/server/tsconfig.json"
      provides: "Server TypeScript config — NodeNext resolution"
      contains: "NodeNext"
    - path: "packages/server/src/index.ts"
      provides: "Placeholder server entrypoint that imports from shared (proves resolution works)"
      contains: "@counter-attack/shared"
    - path: "packages/client/package.json"
      provides: "Client package metadata with workspace dependency on shared"
      contains: "\"@counter-attack/shared\": \"workspace:*\""
    - path: "packages/client/tsconfig.json"
      provides: "Client TypeScript config — Bundler resolution, DOM lib"
      contains: "Bundler"
    - path: "packages/client/src/main.ts"
      provides: "Placeholder client entrypoint that imports types from shared"
      contains: "@counter-attack/shared"
  key_links:
    - from: "packages/server/src/index.ts"
      to: "packages/shared/dist/index.js"
      via: "import from '@counter-attack/shared'"
      pattern: "from\\s+['\"]@counter-attack/shared['\"]"
    - from: "packages/client/src/main.ts"
      to: "packages/shared/dist/index.d.ts"
      via: "import from '@counter-attack/shared'"
      pattern: "from\\s+['\"]@counter-attack/shared['\"]"
    - from: "packages/server/package.json"
      to: "packages/shared/package.json"
      via: "workspace:* protocol resolution"
      pattern: "\"@counter-attack/shared\":\\s*\"workspace:\\*\""
---

<objective>
Scaffold the two remaining workspace packages — `packages/server` and `packages/client` — as minimal placeholders that prove the shared-types resolution chain works end-to-end. Each package declares `@counter-attack/shared` as a `workspace:*` dependency, sets its tier-appropriate `moduleResolution` (NodeNext for server, Bundler for client per RESEARCH.md Pattern 3), and contains a single source file that imports from `@counter-attack/shared`. The final phase-level validation is `pnpm -r build` succeeding across all three packages in topological order.

Purpose: Phase 1 success criterion 1 ("`pnpm install` and `pnpm build` succeed from the repo root with zero errors across packages/shared, packages/server, packages/client") cannot be satisfied until both server and client exist as resolvable workspace packages. This plan satisfies ARCH-02 (monorepo structure across shared/server/client) and provides the final cross-package integration proof that the shared-types contract works.

Output: A repo where `pnpm -r build` exits 0 across all three packages with the shared/dist consumed by both server and client without TypeScript path-mapping hacks (per D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-monorepo-scaffold-shared-types/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffold-shared-types/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffold-shared-types/01-01-SUMMARY.md
@.planning/phases/01-monorepo-scaffold-shared-types/01-02-SUMMARY.md
@CLAUDE.md
@tsconfig.base.json
@packages/shared/package.json
@packages/shared/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/server with workspace dependency on shared</name>
  <files>packages/server/package.json, packages/server/tsconfig.json, packages/server/src/index.ts</files>
  <read_first>
    - .planning/phases/01-monorepo-scaffold-shared-types/01-RESEARCH.md (Pattern 1 workspace protocol, Pattern 3 server tsconfig snippet, Pitfall 1 build order, Pitfall 2 moduleResolution, Pitfall 3 ESM/CJS, Pitfall 5 verbatimModuleSyntax)
    - .planning/phases/01-monorepo-scaffold-shared-types/01-CONTEXT.md (D-06 workspace:*, D-07 extends base, D-08 server imports compiled dist)
    - packages/shared/package.json (the file plan 02 created — confirms `@counter-attack/shared` name and `exports` field)
    - packages/shared/tsconfig.json (the file plan 02 created — confirms NodeNext target so server matches)
    - tsconfig.base.json (verify it does NOT set module/moduleResolution so per-package settings take effect)
  </read_first>
  <action>
    Create directory `packages/server/src/`.

    Create `packages/server/package.json`:
    - `"name": "@counter-attack/server"`
    - `"version": "0.0.1"`
    - `"private": true`
    - `"type": "module"` (per RESEARCH.md Pitfall 3 — server must also be ESM since shared is ESM-only; Node.js 22 LTS handles ESM natively)
    - `"main": "./dist/index.js"`
    - `"scripts"`:
      - `"build": "tsc"`
      - `"typecheck": "tsc --noEmit"`
      - `"clean": "rm -rf dist"`
      - `"test": "echo \"No tests yet — added in Phase 3\" &amp;&amp; exit 0"` (per D-02 — server tests come later; pnpm -r test must not fail in Phase 1)
    - `"dependencies"`: `{ "@counter-attack/shared": "workspace:*" }` (per RESEARCH.md Pattern 1, Anti-Pattern "workspace:* only in devDependencies"; the runtime needs this in `dependencies`, NOT devDeps)
    - No `devDependencies` in Phase 1 (Phase 3 adds Express, Socket.io, @types/node — already in shared's devDeps but not server's yet)

    Create `packages/server/tsconfig.json` per RESEARCH.md Pattern 3 (server snippet):
    - `"extends": "../../tsconfig.base.json"`
    - `compilerOptions`:
      - `"module": "NodeNext"`
      - `"moduleResolution": "NodeNext"` (per RESEARCH.md Pitfall 2 — must respect `exports` field of shared's package.json)
      - `"outDir": "./dist"`
      - `"rootDir": "./src"`
      - `"types": ["node"]`
    - `"include": ["src/**/*"]`
    - `"exclude": ["dist", "node_modules"]`

    Create `packages/server/src/index.ts` as a placeholder that proves resolution works (per D-08 — server consumes shared/dist). It must import at least one type and one value from `@counter-attack/shared`:

    The file should:
    - `import type { HexCoord, GameState } from '@counter-attack/shared';` (type-only import per verbatimModuleSyntax / RESEARCH.md Pitfall 5)
    - `import { PITCH_HEXES, ClientEvents } from '@counter-attack/shared';` (value imports)
    - Export a function `bootstrap(): void` that does NOT start any server (Phase 3's job) but logs to console: `console.log('Counter Attack server placeholder. Pitch hexes:', PITCH_HEXES.length, 'first client event:', ClientEvents.ROOM_CREATE);`
    - Include a top-level call `bootstrap();` so running the compiled file emits proof of resolution

    Run `pnpm install` from repo root to refresh workspace symlinks now that server declares a workspace dep.

    Run `pnpm --filter @counter-attack/server build` and confirm it exits 0. `packages/server/dist/index.js` must exist.

    Run `node packages/server/dist/index.js` and confirm it prints a line containing `Pitch hexes:` followed by `400` (25×16 from the placeholder grid) and `first client event:` followed by `room:create`. (This proves runtime ESM resolution of `@counter-attack/shared` works through pnpm symlinks.)
  </action>
  <verify>
    <automated>pnpm install &amp;&amp; pnpm --filter @counter-attack/server build &amp;&amp; test -f packages/server/dist/index.js &amp;&amp; node packages/server/dist/index.js | grep -q "Pitch hexes: 400"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/package.json` exists with `"name": "@counter-attack/server"`, `"type": "module"`, and `dependencies["@counter-attack/shared"] === "workspace:*"`
    - `packages/server/tsconfig.json` extends `"../../tsconfig.base.json"` and sets `"moduleResolution": "NodeNext"`, `"module": "NodeNext"`, `"outDir": "./dist"`
    - `packages/server/src/index.ts` contains the literal string `from '@counter-attack/shared'` at least twice (one type import, one value import) — verified by `grep -c "@counter-attack/shared" packages/server/src/index.ts` returning >= 2
    - `packages/server/src/index.ts` uses `import type` for type-only imports (verified by `grep -E "^import type" packages/server/src/index.ts` returning at least 1 line — RESEARCH.md Pitfall 5)
    - `pnpm --filter @counter-attack/server build` exits 0
    - `packages/server/dist/index.js` exists after build
    - `node packages/server/dist/index.js` prints a line containing `Pitch hexes: 400` (proves @counter-attack/shared resolves at runtime via pnpm symlinks — ARCH-02 end-to-end)
    - `node_modules/@counter-attack/shared` exists as a symlink to `../../packages/shared` (verified by `test -L node_modules/@counter-attack/shared`)
  </acceptance_criteria>
  <done>Server package scaffolded; workspace dependency on shared works at both build-time (tsc resolves types via exports field) and runtime (Node.js resolves the symlinked dist/index.js via the main field). ARCH-02 server side satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold packages/client with workspace dependency on shared</name>
  <files>packages/client/package.json, packages/client/tsconfig.json, packages/client/src/main.ts</files>
  <read_first>
    - .planning/phases/01-monorepo-scaffold-shared-types/01-RESEARCH.md (Pattern 3 client tsconfig snippet, Pitfall 2 Bundler moduleResolution rationale, Architectural Responsibility Map row "TypeScript compilation (client)")
    - .planning/phases/01-monorepo-scaffold-shared-types/01-CONTEXT.md (D-04 pixel code in client only — placeholder, not built here, D-06 workspace:*, D-07 extends base)
    - packages/shared/package.json (confirms exports field is what Bundler resolution will use)
    - packages/server/package.json (use as a structural reference for sibling package shape)
    - tsconfig.base.json
  </read_first>
  <action>
    Create directory `packages/client/src/`.

    Create `packages/client/package.json`:
    - `"name": "@counter-attack/client"`
    - `"version": "0.0.1"`
    - `"private": true`
    - `"type": "module"`
    - `"scripts"`:
      - `"build": "tsc"` (placeholder — Phase 6 swaps this for `vite build`; for Phase 1 we want a tsc emit to prove type resolution works via Bundler mode, even with `noEmit: true`)
      - `"typecheck": "tsc --noEmit"`
      - `"test": "echo \"No tests yet — added in Phase 6\" &amp;&amp; exit 0"`
    - `"dependencies"`: `{ "@counter-attack/shared": "workspace:*" }`
    - No `devDependencies` in Phase 1 (Phase 6 adds React, Vite, honeycomb-grid)

    Create `packages/client/tsconfig.json` per RESEARCH.md Pattern 3 (client snippet — note `noEmit: true` because Phase 6 will hand bundling to Vite, but Phase 1 still typechecks via tsc):
    - `"extends": "../../tsconfig.base.json"`
    - `compilerOptions`:
      - `"module": "ESNext"`
      - `"moduleResolution": "Bundler"` (per RESEARCH.md Pattern 3 — Vite handles extensions, no .js suffix enforcement; respects shared's `exports` field)
      - `"lib": ["ES2022", "DOM", "DOM.Iterable"]` (DOM for browser globals; only client gets these — D-07)
      - `"noEmit": true` (per RESEARCH.md Architectural Responsibility Map — Vite handles transpilation in Phase 6; tsc only typechecks)
      - `"jsx": "react-jsx"` (pre-configured for Phase 6 even though no JSX exists yet)
    - `"include": ["src/**/*"]`
    - `"exclude": ["dist", "node_modules"]`

    Because `noEmit: true`, the `build` script will not produce a `dist/`. That is intentional and matches RESEARCH.md guidance — clients build via Vite later.

    Create `packages/client/src/main.ts` as a placeholder proving the shared-types resolution chain through Bundler mode:
    - `import type { HexCoord, GameState, PlayerPiece } from '@counter-attack/shared';` (type-only)
    - `import { hexDistance, ClientEvents } from '@counter-attack/shared';` (value)
    - Declare a typed constant: `const origin: HexCoord = { q: 0, r: 0 };`
    - Declare another: `const target: HexCoord = { q: 3, r: 0 };`
    - Compute: `const distance: number = hexDistance(origin, target);`
    - Export a function `placeholder(): void` body: `console.log('Counter Attack client placeholder. Distance 0->3:', distance, 'create event:', ClientEvents.ROOM_CREATE);`
    - Include unused `GameState` and `PlayerPiece` type imports (prefixed with `// @ts-expect-error` if needed, OR mark them as used by declaring `const _gs: GameState | null = null; const _pp: PlayerPiece | null = null;`) — the goal is to prove every required Phase 1 type is importable from the client side per ROADMAP success criterion 2

    Run `pnpm install` from repo root to refresh workspace symlinks.

    Run `pnpm --filter @counter-attack/client typecheck` and confirm it exits 0 (proves Bundler-mode resolution succeeds against `packages/shared/dist/`).

    `pnpm --filter @counter-attack/client build` should also exit 0 (tsc with noEmit: true emits no files but still typechecks).
  </action>
  <verify>
    <automated>pnpm install &amp;&amp; pnpm --filter @counter-attack/client typecheck &amp;&amp; pnpm --filter @counter-attack/client build &amp;&amp; node -e "const p=require('./packages/client/package.json'); if(p.dependencies['@counter-attack/shared']!=='workspace:*') process.exit(1); const t=require('./packages/client/tsconfig.json'); if(t.compilerOptions.moduleResolution!=='Bundler') process.exit(2); if(t.compilerOptions.noEmit!==true) process.exit(3); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/client/package.json` exists with `"name": "@counter-attack/client"`, `"type": "module"`, and `dependencies["@counter-attack/shared"] === "workspace:*"`
    - `packages/client/tsconfig.json` extends `"../../tsconfig.base.json"`, sets `"moduleResolution": "Bundler"`, `"module": "ESNext"`, `"noEmit": true`, and includes `"DOM"` in `lib`
    - `packages/client/src/main.ts` imports at least the type names `HexCoord`, `GameState`, `PlayerPiece` and at least the value names `hexDistance`, `ClientEvents` from `@counter-attack/shared` (verified by `grep -c "@counter-attack/shared" packages/client/src/main.ts` returning >= 2)
    - `packages/client/src/main.ts` uses `import type` for the type-only line (verified by `grep -cE "^import type" packages/client/src/main.ts` returning >= 1)
    - `pnpm --filter @counter-attack/client typecheck` exits 0
    - `pnpm --filter @counter-attack/client build` exits 0
    - Because `noEmit: true`, `packages/client/dist/` either does not exist OR is empty — both are acceptable
  </acceptance_criteria>
  <done>Client package scaffolded; Bundler-mode resolution against shared's exports field works; all five required shared types (HexCoord, GameState, PlayerPiece, BallState, GamePhase per ROADMAP criterion 2) are reachable from client code. ARCH-02 client side satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full repo build, lint, and run final phase acceptance checks</name>
  <files>(no files modified — verification-only)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 Success Criteria 1–5 — verify each)
    - .planning/REQUIREMENTS.md (ARCH-02, ARCH-03, ARCH-07 acceptance wording)
    - .planning/phases/01-monorepo-scaffold-shared-types/01-VALIDATION.md (Per-Task Verification Map)
    - .planning/phases/01-monorepo-scaffold-shared-types/01-CONTEXT.md (all 11 decisions D-01 through D-11 — confirm each was implemented)
    - packages/shared/src/index.ts (the barrel)
    - packages/shared/src/pitch.ts (placeholder comment)
    - packages/server/src/index.ts (cross-package import)
    - packages/client/src/main.ts (cross-package import)
  </read_first>
  <action>
    This task runs the full Phase 1 acceptance battery. It modifies no source files; it validates the work of all previous plans.

    Step 1 (clean + install): Run `pnpm install` from repo root. Exit 0 required.

    Step 2 (recursive build, topological order): Run `pnpm -r build` from repo root. Per RESEARCH.md Pitfall 1 and Anti-Pattern note: do NOT pass `--parallel`. pnpm must resolve the topological order (shared → server, shared → client). Confirm exit 0 and that the output contains entries for all three packages.

    Step 3 (lint clean): Run `pnpm exec eslint .` from repo root. Acceptable outcomes are: exit 0 (no errors) OR exit 0 with warnings only. Errors are not acceptable.

    Step 4 (prettier check): Run `pnpm exec prettier --check .` from repo root. Files in `dist/` and `node_modules/` are ignored via `.prettierignore`. Acceptable: exit 0.

    Step 5 (shared tests pass): Run `pnpm --filter @counter-attack/shared test`. Exit 0 with all assertions green.

    Step 6 (cross-package runtime proof): Run `node packages/server/dist/index.js`. Output must contain `Pitch hexes: 400` (proves the symlinked `@counter-attack/shared` resolves at Node.js runtime through ESM).

    Step 7 (isolation grep — ARCH-07): Run `grep -rE "from ['\"](socket\.io|express|honeycomb-grid)" packages/shared/src/` (excluding comments). Must return no matches.

    Step 8 (no pixel code in shared — D-04): Run `grep -rE "(axialToPixel|hexToPixel|toPixel)" packages/shared/src/`. Must return no matches.

    Step 9 (placeholder marker — ROADMAP criterion 5): Run `grep -q "PLACEHOLDER" packages/shared/src/pitch.ts &amp;&amp; grep -q "pending real board measurements" packages/shared/src/pitch.ts`. Must succeed.

    Step 10 (decision audit): Verify each locked decision has visible evidence:
    - D-01 (Vitest): `packages/shared/vitest.config.ts` exists
    - D-02 (Vitest only in shared, per-package config): only `packages/shared/vitest.config.ts` exists; no root vitest workspace file
    - D-03 (pure axial math): `packages/shared/src/hex.ts` has zero non-local imports (only `import type` from `./types`)
    - D-04 (pixel code in client only): server and shared have no `*toPixel*` symbols; client may have placeholder
    - D-05 (single barrel): `packages/shared/src/index.ts` re-exports all four submodules; no sub-path exports declared in `packages/shared/package.json`
    - D-06 (workspace:* + exports field): `packages/server/package.json` and `packages/client/package.json` both contain `"@counter-attack/shared": "workspace:*"`; `packages/shared/package.json` has an `exports` field
    - D-07 (tsconfig.base.json + extends): all three package tsconfigs use `"extends": "../../tsconfig.base.json"`
    - D-08 (shared builds to /dist): `packages/shared/dist/index.js` and `packages/shared/dist/index.d.ts` exist
    - D-09 (ESLint + Prettier present): `eslint.config.js` and `.prettierrc` exist at repo root
    - D-10 (typescript-eslint + eslint-config-prettier): `eslint.config.js` imports both
    - D-11 (Husky + lint-staged): `.husky/pre-commit` contains `lint-staged`; root `package.json` has `lint-staged` config

    If ANY check fails, do not declare success — surface the failing check in the SUMMARY and route back to the relevant plan.

    Step 11 (write the SUMMARY): Once all 10 checks pass, write `01-03-SUMMARY.md` with the audit results.
  </action>
  <verify>
    <automated>pnpm install &amp;&amp; pnpm -r build &amp;&amp; pnpm --filter @counter-attack/shared test &amp;&amp; pnpm exec eslint . &amp;&amp; pnpm exec prettier --check . &amp;&amp; node packages/server/dist/index.js | grep -q "Pitch hexes: 400" &amp;&amp; ! grep -rE "from ['\"](socket\.io|express|honeycomb-grid)" packages/shared/src/ &amp;&amp; ! grep -rE "(axialToPixel|hexToPixel|toPixel)" packages/shared/src/ &amp;&amp; grep -q "PLACEHOLDER" packages/shared/src/pitch.ts &amp;&amp; grep -q "pending real board measurements" packages/shared/src/pitch.ts &amp;&amp; test -f packages/shared/dist/index.js &amp;&amp; test -f packages/shared/dist/index.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0
    - `pnpm -r build` exits 0 (per RESEARCH.md Pitfall 1, topological order: shared builds before server/client; the recursive run reports all three packages succeeded)
    - `pnpm exec eslint .` exits 0 (no errors)
    - `pnpm exec prettier --check .` exits 0
    - `pnpm --filter @counter-attack/shared test` exits 0
    - `node packages/server/dist/index.js` output contains the substring `Pitch hexes: 400`
    - No socket.io/express/honeycomb-grid imports in `packages/shared/src/` (ARCH-07)
    - No `*toPixel*` symbols in `packages/shared/src/` (D-04)
    - `packages/shared/src/pitch.ts` contains both `PLACEHOLDER` and `pending real board measurements` strings (ROADMAP criterion 5)
    - `packages/shared/dist/index.js` and `packages/shared/dist/index.d.ts` both exist
    - All 11 decisions D-01 through D-11 have visible evidence (per Step 10 audit)
    - The five Phase 1 ROADMAP success criteria are all satisfied — verified individually in the SUMMARY
  </acceptance_criteria>
  <done>Phase 1 is verifiably complete. The pnpm monorepo, all three packages, shared types and hex math, Socket.io event constants, placeholder pitch, and the full dev-tooling chain (ESLint, Prettier, Husky, lint-staged) all work together. The repo is ready for Phase 2 (move validator) to consume `@counter-attack/shared` without any setup work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — placeholders) | `packages/server/src/index.ts` and `packages/client/src/main.ts` are scaffolding placeholders. The server placeholder runs `console.log` once and exits; it does NOT bind a port, parse network input, or expose any endpoints. The client placeholder is type-checked but does not run in a browser yet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | (N/A) | placeholder server / client entry files | accept | No application surface area. The server placeholder exits after one `console.log`; the client placeholder is type-checked but never executed in this phase. Threats appear when Phase 3 introduces Socket.io endpoints (server) and Phase 7 wires Socket.io client (client). |
| T-01-SC-03 | Tampering | npm install (no new packages in this plan) | accept | This plan adds zero new third-party packages. All previously installed packages (root devDeps + Vitest in shared) were audited in 01-RESEARCH.md "Package Legitimacy Audit". |

**Rationale:** Same as plan 01 and 02 — Phase 1 scaffolding has no endpoints, no data, no network traffic. The first real attack surface appears in Phase 3 (Socket.io room manager), at which point the threat model will cover client→server input validation, room-code brute-force, disconnect-flood handling, etc.
</threat_model>

<verification>
This plan IS the phase-level verification. All of the following must hold simultaneously:

1. `pnpm install` exits 0
2. `pnpm -r build` exits 0 across all three packages (no --parallel, topological order honored)
3. `pnpm exec eslint .` exits 0
4. `pnpm exec prettier --check .` exits 0
5. `pnpm --filter @counter-attack/shared test` exits 0
6. `node packages/server/dist/index.js` prints `Pitch hexes: 400` (cross-package runtime resolution proof)
7. No socket.io / express / honeycomb-grid imports in `packages/shared/src/`
8. No pixel-conversion symbols in `packages/shared/src/`
9. `packages/shared/src/pitch.ts` contains the required PLACEHOLDER comment strings
10. All five ROADMAP Phase 1 success criteria verified individually
11. All eleven locked decisions D-01 through D-11 have visible evidence
</verification>

<success_criteria>
- ARCH-02 satisfied: monorepo with three packages — shared, server, client — all building successfully via `pnpm -r build`
- ROADMAP Phase 1 success criterion 1 satisfied: `pnpm install` and `pnpm build` both succeed across all three packages
- Cross-package resolution proven at runtime (server importing from `@counter-attack/shared` via pnpm symlink + ESM resolution)
- Cross-package resolution proven at type-check time (client typecheck via Bundler resolution against shared's exports field)
- All five ROADMAP success criteria for Phase 1 confirmed by the final task's audit
- All eleven user decisions implemented and auditable
- Repo is ready for Phase 2 (move validator) to begin consuming `@counter-attack/shared` with zero scaffolding work
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-scaffold-shared-types/01-03-SUMMARY.md` when done. Include:
- Result of every check in Task 3 step list (pass/fail per check)
- Output of `pnpm -r build` showing all three packages built
- Output of `node packages/server/dist/index.js` (the `Pitch hexes: 400` line)
- Confirmation table for each of D-01..D-11 with the artifact path proving implementation
- Confirmation table for each of ROADMAP Phase 1 success criteria 1–5 with the verification command and result
- Any deviations from RESEARCH.md (e.g., if `noEmit: true` on client was rejected by tsc for some reason)
- Final phase status: READY for Phase 2
</output>
