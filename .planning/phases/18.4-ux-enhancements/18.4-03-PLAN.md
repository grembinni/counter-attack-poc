---
phase: 18.4-ux-enhancements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/ActionPanel.tsx
autonomous: true
requirements: [UX-10, UX-11, UX-13]
must_haves:
  truths:
    - 'The Free Move helper text explains the final-third reposition mechanic and shows the count of eligible players still able to move'
    - 'The Movement phase helper text displays the number of players left to move'
    - 'Hovering an action button shows a one-line summary of what that action does'
  artifacts:
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'Rewritten FREE_MOVE + MOVE helper copy and per-action title tooltips'
      contains: 'players left to move'
  key_links:
    - from: 'packages/client/src/components/ActionPanel.tsx MOVE branch'
      to: 'movedPieceIds / slotTotal count'
      via: 'remaining = slotTotal - movedPieceIds.length rendered in helper line 2'
      pattern: 'players left to move'
    - from: 'packages/client/src/components/ActionPanel.tsx action buttons'
      to: 'native title tooltip'
      via: 'title={ACTION_SUMMARY} on each ctaButton'
      pattern: 'title='
---

<objective>
Implement three ActionPanel helper/tooltip enhancements that all live in the same file
(`ActionPanel.tsx`), grouped so they share one edit pass and avoid intra-wave file conflicts:

- UX-11 (movement helper — player count): the MOVE phase helper text tracks and displays
  "{N} players left to move." mirroring the existing header-contestant-selection helper pattern.
- UX-10 (Free Move helper text): rewrite the FREE_MOVE_ATTACK/FREE_MOVE_DEFENSE helper to explain
  the mechanic ("ball entered the opposite final third — your backline can reposition up to 6
  hexes regardless of remaining pace") and show the count of still-eligible players (mirrors
  UX-11's pattern).
- UX-13 (action button tooltip): hovering an action button shows a short one-line summary of what
  that action does, via the native HTML `title` attribute (18-UI-SPEC Interaction Contract).

Purpose: Make the two response-move phases self-explanatory and give every action button a
hover hint. All copy is locked/patterned in 18-CONTEXT.md and 18-UI-SPEC.md.

Output: updated helper strings in the MOVE and FREE_MOVE branches, and `title=` attributes on the
action buttons in ActionPanel.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.4-ux-enhancements/18.4-CONTEXT.md
@.planning/phases/18-design-polish/18-UI-SPEC.md
@.planning/phases/18-design-polish/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MOVE + FREE_MOVE helper text — player-left-to-move counts (UX-11, UX-10)</name>
  <files>packages/client/src/components/ActionPanel.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (read the MOVE branch at lines 615-669 — `slotTotal`/`remaining`/`slotHelperLine2` already compute a "{remaining} of {slotTotal} players left to move." string at lines 630-639; read the FREE_MOVE_ATTACK/FREE_MOVE_DEFENSE branch at lines 437-462 where `eligibleTotal`/`movedCount`/`remaining` are already computed at lines 443-446 and rendered at lines 450-454; read the HEADER contestant helper at lines 293-308 which is the "{N} players selected within range." pattern UX-11 mirrors)
    - .planning/phases/18-UI-SPEC.md → Copywriting Contract: UX-11 = `"{N} players left to move."`; UX-10 line 1 = `"Ball entered the opposite final third — your backline can reposition up to 6 hexes regardless of remaining pace."`, UX-10 line 2 = `"{N} players still eligible to move."`; Typography §UX-10/UX-11: reuse the existing `.helperLine1` (Emphasis) + `.helperLine2` (Micro-label) pair, do NOT invent new classes
  </read_first>
  <action>
    UX-11 (MOVE branch, lines ~630-648): the `remaining`/`slotTotal` count already exists — align
    the helper copy to the locked UX-11 wording `"{N} players left to move."`. Keep the existing
    `.helperLine1`/`.helperLine2` two-line block (do not add classes). The line-1 emphasis label
    stays `"Move!"`; line 2 must read `"{remaining} players left to move."` (retain the
    `(2 hex max)` suffix for the ATTACKER_2 slot). Match the exact UI-SPEC string shape — a bare
    "{N} players left to move." is acceptable; the existing "{remaining} of {slotTotal}" form is a
    superset and may be kept, but the phrase "players left to move" must be present verbatim.
    UX-10 (FREE_MOVE branch, lines ~447-462): replace the current line-1 `"Free Move!"` + line-2
    `"{sideLabel} — move up to 6 hexes ... {remaining} of {eligibleTotal} players left to move."`
    with the locked UX-10 copy: line 1 (Emphasis, `.helperLine1`) = `"Ball entered the opposite
    final third — your backline can reposition up to 6 hexes regardless of remaining pace."`;
    line 2 (Micro-label, `.helperLine2`) = `"{remaining} players still eligible to move."` using
    the already-computed `remaining` value. Keep the existing eligible-count derivation
    (`eligibleIds`/`freeMoveUsedPace`) — only the displayed copy changes.
  </action>
  <verify>
    <automated>cd packages/client && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - The MOVE branch helper line-2 contains the literal substring `players left to move`
    - The FREE_MOVE branch line-1 contains the literal substring `reposition up to 6 hexes regardless of remaining pace`
    - The FREE_MOVE branch line-2 contains the literal substring `players still eligible to move`
    - Both blocks still use `styles.helperLine1` and `styles.helperLine2` (no new CSS classes introduced)
    - `cd packages/client && pnpm typecheck` exits 0 and `pnpm test` passes
  </acceptance_criteria>
  <done>MOVE and FREE_MOVE helper text show the locked UX-10/UX-11 copy with live eligible-player counts; client typecheck + tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Action button tooltips via native title (UX-13)</name>
  <files>packages/client/src/components/ActionPanel.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (read every `<button className={styles.ctaButton}>` call site — Move (lines 481-483, 538-542), Standard Pass (543-550), One-Touch/FIRST_TIME_PASS (551-558), High Pass (559-563), Long Ball (564-568), Snapshot (569-573, 650-654), Shoot (574-582), GK Restart Punt/Quick Throw/Move (355-363), plus the End Turn / Undo buttons; read the existing `PASS_TYPE_LABELS` map at lines 7-12 as the label source)
    - .planning/phases/18-design-polish/18-PATTERNS.md (§Native title tooltip — `KickOffSetupPanel.tsx:115`, `FreeKickSetupPanel.tsx:137`, `GameBoard.tsx:234,333` are the confirmed precedent; add `title="..."` directly on the `<button>`)
    - .planning/phases/18-UI-SPEC.md → Interaction Contract (UX-13 row): one-line summary per action, e.g. Standard Pass → "Pass to a nearby teammate; may be intercepted."; exact per-action wording is executor discretion
  </read_first>
  <action>
    Add a module-level `ACTION_SUMMARY: Record<string, string>` lookup (flat `Record`, D-02 shape)
    keyed by a stable action id/label, mapping each action to a one-line summary, e.g.:
    `Move` → "Move a player; distance limited by pace."; `Standard Pass` → "Pass to a nearby
    teammate; may be intercepted."; `One-Touch` → "First-time pass; both teams reposition before
    it lands."; `High Pass` → "Lofted pass contested by an aerial header."; `Long Ball` → "A long
    downfield pass; less accurate."; `Snapshot` → "A quick shot from inside the box."; `Shoot` →
    "Take a shot at goal (in range only)."; `Punt (High Pass)` → "Goalkeeper clears with a long
    kick."; `Quick Throw` → "Goalkeeper throws the ball back into play."; `Undo` → "Undo your last
    move this phase."; `End Turn` → "End your turn and pass control to the opponent.". Apply
    `title={ACTION_SUMMARY['<label>']}` on each corresponding `<button className={styles.ctaButton}>`
    (native tooltip, no new markup, no CSS change, no new dependency). Do NOT add tooltips to the
    `.backButton` cancel controls unless trivial — scope is the action `ctaButton` set.
  </action>
  <verify>
    <automated>cd packages/client && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - ActionPanel.tsx contains `const ACTION_SUMMARY: Record<string, string>` with entries for at least Move, Standard Pass, One-Touch, High Pass, Long Ball, Snapshot, Shoot, End Turn
    - At least the Standard Pass, High Pass, Long Ball, Shoot, and End Turn `ctaButton` elements carry a `title=` attribute sourced from ACTION_SUMMARY
    - No new npm dependency and no new tooltip component file
    - `cd packages/client && pnpm typecheck` exits 0 and `pnpm test` passes
  </acceptance_criteria>
  <done>Action buttons show a native one-line summary tooltip on hover; client typecheck + tests pass.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by this plan (exclude from source-drift verification):

- `ACTION_SUMMARY` (const `Record<string,string>`, packages/client/src/components/ActionPanel.tsx)
- Rewritten UX-10/UX-11 helper-copy strings (packages/client/src/components/ActionPanel.tsx)
- `title` attributes on ActionPanel `ctaButton` elements (packages/client/src/components/ActionPanel.tsx)
  </artifacts_this_phase_produces>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                         |
| ---------------------- | --------------------------------------------------------------------------------------------------- |
| server → client render | Helper counts and button copy render server-supplied state; no new client→server input crosses here |

## STRIDE Threat Register

| Threat ID   | Category               | Component                       | Disposition | Mitigation Plan                                                                                    |
| ----------- | ---------------------- | ------------------------------- | ----------- | -------------------------------------------------------------------------------------------------- |
| T-18.4.3-01 | Information Disclosure | Free-move eligible-player count | accept      | Count derives from already-broadcast `freeMoveEligibleIds`/`freeMoveUsedPace`; no new data exposed |
| T-18.4.3-02 | Tampering              | ACTION_SUMMARY title strings    | accept      | Static descriptive strings; no state, no input, no rule impact                                     |
| T-18.4.3-SC | Tampering              | npm/pip/cargo installs          | mitigate    | No new packages installed (native title); slopcheck N/A                                            |

</threat_model>

<verification>
- `pnpm -r typecheck` clean; `pnpm -r test` passes (pre-existing RED failures unchanged)
- Manual (optional): enter MOVE phase → helper shows "N players left to move."; trigger a
  final-third free move → helper explains the mechanic + eligible count; hover any action button →
  one-line summary tooltip
</verification>

<success_criteria>

- UX-11: MOVE helper shows live "players left to move" count
- UX-10: Free Move helper explains the 6-hex reposition mechanic + shows eligible-player count
- UX-13: action buttons show native one-line summary tooltips
- Typechecks and tests green
  </success_criteria>

<output>
Create `.planning/phases/18.4-ux-enhancements/18.4-03-SUMMARY.md` when done.
</output>
