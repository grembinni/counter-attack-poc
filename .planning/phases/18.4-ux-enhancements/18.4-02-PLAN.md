---
phase: 18.4-ux-enhancements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/PitchMarkings.tsx
  - packages/client/src/components/GameBoard.tsx
autonomous: true
requirements: [UX-09, UX-12]
must_haves:
  truths:
    - "A red boundary line is rendered at each team's final-third edge, spanning the full board height"
    - "Hovering a stat bubble in the player card shows a tooltip with the stat's full name and what it helps with"
  artifacts:
    - path: 'packages/client/src/components/PitchMarkings.tsx'
      provides: 'Two final-third boundary <line> elements at the q10/q11 and q25/q26 edges'
      contains: '#ef4444'
    - path: 'packages/client/src/components/GameBoard.tsx'
      provides: 'STAT_FULL_NAME lookup + title attribute on StatRow'
      contains: 'STAT_FULL_NAME'
  key_links:
    - from: 'packages/client/src/components/GameBoard.tsx StatRow'
      to: 'native title tooltip'
      via: 'title={STAT_FULL_NAME[label]} on the stat label span'
      pattern: 'title='
    - from: 'packages/client/src/components/PitchMarkings.tsx'
      to: 'final-third boundary geometry'
      via: 'new <line> elements matching the halfway-line x=540 hex-to-pixel convention'
      pattern: 'stroke="#ef4444"'
---

<objective>
Implement two independent visual enhancements that touch non-overlapping files:

- UX-09 (final-third marker): render a red boundary line at each team's final-third edge across
  the top and bottom of the board, reusing the existing `PitchMarkings.tsx` cosmetic SVG layer
  and the `PITCH_REGIONS` zone boundaries already used for `ballZone`/MOVE-06 — no new geometry.
- UX-12 (stat tooltip): hovering a stat bubble in the player card (the `StatRow` component in
  `GameBoard.tsx`) shows the stat's full name AND a short summary of what actions it helps with,
  via the native HTML `title` attribute (18-UI-SPEC Interaction Contract: native `title`, 4x
  existing precedent, zero new dependency).

Purpose: Board-reading clarity (where each final third begins) and stat discoverability (what
`TKL`/`AA` mean and do), both locked in 18-CONTEXT.md and resolved to concrete values in
18-UI-SPEC.md.

Output: two `<line>` elements in `PitchMarkings.tsx` and a `STAT_FULL_NAME` lookup +
`title` attribute in `GameBoard.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.4-ux-enhancements/18.4-CONTEXT.md
@.planning/phases/18-design-polish/18-UI-SPEC.md
@.planning/phases/18-design-polish/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final-third red boundary marker in PitchMarkings.tsx (UX-09)</name>
  <files>packages/client/src/components/PitchMarkings.tsx</files>
  <read_first>
    - packages/client/src/components/PitchMarkings.tsx (whole file — the halfway line `<line x1={540} y1={17.3} x2={540} y2={883.3} .../>` at lines 14-25 is the exact geometry template; note the module doc comment: coordinates are LOCAL to the `<g translate(20, 17.32)>`, do NOT re-add the translate offset)
    - packages/shared/src/pitch.ts (read `PITCH_REGIONS` — `homeThird` (`q<=10`) and `awayThird` (`q>=26`) boundaries confirmed in 18-UI-SPEC §UX-09; also read the hex-to-pixel conversion / HEX_SIZE constant used to derive x=540 for q=18 so the new x-coordinates are COMPUTED, not guessed)
    - .planning/phases/18-design-polish/18-UI-SPEC.md (§Color → UX-09 final-third marker table: stroke `#ef4444`, strokeWidth 3, strokeOpacity 1, full board height y1=17.3 → y2=883.3, position at the q=10/q=11 and q=25/q=26 boundaries)
  </read_first>
  <action>
    Add two `<line>` elements to the `PitchMarkings.tsx` fragment (alongside the existing halfway
    line). Each marks a final-third boundary: one at the x-coordinate of the boundary between
    `q=10` and `q=11` (home final-third edge) and one at the boundary between `q=25` and `q=26`
    (away final-third edge). Derive both x-coordinates from the SAME hex-to-pixel math that
    produces `x=540` for the q=18 halfway line (the halfway line sits at the q=18 column centre =
    540; compute the q=10/q=11 and q=25/q=26 boundary x-values from the column pitch, do NOT
    hardcode a visual guess). Style per 18-UI-SPEC §UX-09: `stroke="#ef4444"`, `strokeWidth={3}`,
    `strokeOpacity={1}`, `fill="none"`, `pointerEvents="none"`, with `y1={17.3}` and `y2={883.3}`
    matching the halfway line's full-height y-span exactly. Add a short comment referencing UX-09
    and the `PITCH_REGIONS.homeThird`/`awayThird` boundaries.
  </action>
  <verify>
    <automated>cd packages/client && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - PitchMarkings.tsx contains exactly two new `<line>` elements with `stroke="#ef4444"` and `strokeWidth={3}`
    - Both new lines use `y1={17.3}` and `y2={883.3}` (full board height)
    - The x-coordinates are derived from the hex column pitch (not the literal 540 halfway value), one near the home final-third edge and one near the away edge
    - `cd packages/client && pnpm typecheck` exits 0 and `pnpm test` passes
  </acceptance_criteria>
  <done>Two red final-third boundary lines render across the full board height at the home and away final-third edges; client typecheck + tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Stat-bubble tooltip via STAT_FULL_NAME lookup in GameBoard.tsx (UX-12)</name>
  <files>packages/client/src/components/GameBoard.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (read the `PHASE_LABEL`/`MOVE_SLOT_SUFFIX` `Record<...>` lookup-table pattern at lines 18-66 — STAT_FULL_NAME follows the same flat-Record shape per D-02; read `StatRow` at lines 75-83 where the `.statLabel` span renders the abbreviation; read the StatRow call sites at lines 211-226 to enumerate every abbreviation actually rendered: PAC, DRB, AA, SAV, SHT, HPS, RES, TAC, HND)
    - .planning/phases/18-UI-SPEC.md → Interaction Contract table (UX-12 row): add `title={fullStatName}` to the `.statLabel` element; new `STAT_FULL_NAME: Record<string,string>` mapping every abbreviation already rendered
    - .planning/phases/18.4-ux-enhancements/18.4-CONTEXT.md (UX-12 decision: "Manual scope add — should also have a summary explaining what actions the stat helps with" — the tooltip text is full name PLUS a short action summary, not just the full name)
    - .planning/phases/18-design-polish/18-PATTERNS.md (§Native title tooltip: zero-dependency precedent, no custom tooltip component)
  </read_first>
  <action>
    Add a module-level `STAT_FULL_NAME: Record<string, string>` lookup to `GameBoard.tsx`
    (same flat-`Record` shape as `PHASE_LABEL`, per D-02 — no templating, no function values).
    Map every abbreviation rendered by the StatRow call sites (PAC, DRB, AA, SAV, SHT, HPS, RES,
    TAC, HND) to a string containing the full stat name AND a short summary of what it helps with,
    e.g. `PAC` → `"Pace — how far this player can move per turn."`, `TAC` →
    `"Tackling — contesting the ball from a carrier."`, `AA` → `"Aerial Ability — winning headers
    and high passes."`, `SHT` → `"Shooting — scoring power on shots."`, `SAV` → `"Saving — a
    goalkeeper's shot-stopping."`, `HND` → `"Handling — a goalkeeper's catch reliability."`,
    `DRB` → `"Dribbling — keeping the ball under pressure."`, `HPS` → `"High Pass — accuracy of
    lofted passes."`, `RES` → `"Resilience — resisting fatigue and knocks."`. In `StatRow`, add
    `title={STAT_FULL_NAME[label] ?? label}` to the existing `.statLabel` span (native browser
    tooltip; no CSS change, no new component, no new dependency).
  </action>
  <verify>
    <automated>cd packages/client && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - GameBoard.tsx contains `const STAT_FULL_NAME: Record<string, string>` with entries for PAC, DRB, AA, SAV, SHT, HPS, RES, TAC, HND
    - Each STAT_FULL_NAME value contains both the full stat name and a short action summary (a `—` or similar separator)
    - StatRow's `.statLabel` span has `title={STAT_FULL_NAME[label] ?? label}`
    - No new npm dependency added and no new tooltip component file created
    - `cd packages/client && pnpm typecheck` exits 0 and `pnpm test` passes
  </acceptance_criteria>
  <done>Hovering any stat abbreviation in the player card shows the full name plus an action summary via native title; client typecheck + tests pass.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by this plan (exclude from source-drift verification):

- Two final-third `<line>` marker elements (packages/client/src/components/PitchMarkings.tsx)
- `STAT_FULL_NAME` (const `Record<string,string>`, packages/client/src/components/GameBoard.tsx)
- `title` attribute on StatRow's `.statLabel` span (packages/client/src/components/GameBoard.tsx)
  </artifacts_this_phase_produces>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                      |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------- |
| server → client render | Both changes render server-supplied state (stat values, board geometry); no new client→server input crosses here |

## STRIDE Threat Register

| Threat ID   | Category               | Component                 | Disposition | Mitigation Plan                                                                                                  |
| ----------- | ---------------------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-18.4.2-01 | Information Disclosure | STAT_FULL_NAME title text | accept      | Tooltip only surfaces stat names already visible on-screen; no sensitive data, purely descriptive static strings |
| T-18.4.2-02 | Tampering              | PitchMarkings SVG lines   | accept      | Cosmetic non-interactive layer (`pointerEvents="none"`); no state, no input, no rule impact                      |
| T-18.4.2-SC | Tampering              | npm/pip/cargo installs    | mitigate    | No new packages installed (native title, no tooltip lib); slopcheck N/A                                          |

</threat_model>

<verification>
- `pnpm -r typecheck` clean; `pnpm -r test` passes (pre-existing RED failures unchanged)
- Manual (optional): board shows two red vertical boundary lines at the final-third edges; hovering a stat bubble shows the full name + summary tooltip
</verification>

<success_criteria>

- UX-09: red final-third boundary lines render at both edges, full board height, `#ef4444`/3px
- UX-12: stat bubbles show a native title tooltip with full name + action summary
- Typechecks and tests green
  </success_criteria>

<output>
Create `.planning/phases/18.4-ux-enhancements/18.4-02-SUMMARY.md` when done.
</output>
