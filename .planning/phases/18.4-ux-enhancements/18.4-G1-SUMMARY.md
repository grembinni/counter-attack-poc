---
phase: 18.4-ux-enhancements
plan: G1
subsystem: ui
tags: [react, hooks, useEffect, eventbanner, zustand]

# Dependency graph
requires:
  - phase: 18.4-ux-enhancements
    provides: EventBanner component (UX-14) that this plan fixes
provides:
  - EventBanner with correct React hook usage — diff-and-trigger in useEffect([eventLog])
affects:
  - any phase that reads EventBanner.tsx for hook patterns

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "useEffect([dep]) for state updates triggered by store changes — never call setState in render body"

key-files:
  created: []
  modified:
    - packages/client/src/components/EventBanner.tsx

key-decisions:
  - "Diff-and-trigger logic relocated from render body into useEffect([eventLog]) — preserves existing ref-advance-always semantics"

patterns-established:
  - "Side-effect pattern: when Zustand store change must fire a setState, use useEffect keyed on the store slice, not synchronous render-body mutation"

requirements-completed: [UX-14]

# Metrics
duration: 11min
completed: 2026-07-02
---

# Phase 18.4 Plan G1: EventBanner useEffect Fix Summary

**React rules-of-hooks violation in EventBanner.tsx fixed: diff-and-trigger logic moved from render body into useEffect([eventLog]), eliminating the synchronous setActive call during render**

## Performance

- **Duration:** 11 min
- **Started:** 2026-07-02T17:50:20Z
- **Completed:** 2026-07-02T18:01:05Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Removed the `if (eventLog.length > lastProcessedLengthRef.current) { setActive(banner); }` block from the EventBanner render function body
- Added `useEffect(() => { ... }, [eventLog])` that performs the identical tail-diff logic after render
- Updated the stale comment that incorrectly described render-phase state updates as "same render cycle"
- All 9 existing EventBanner tests pass unchanged (tests already used `act(...)` wrappers around store mutations, which is the correct testing pattern for effects)

## Task Commits

Each task was committed atomically:

1. **Task 1: Move EventBanner diff-and-trigger logic from render body into useEffect([eventLog])** - `589daf4` (fix)

**Plan metadata:** (docs commit — pending)

## Files Created/Modified
- `packages/client/src/components/EventBanner.tsx` - Diff-and-trigger logic relocated into useEffect([eventLog]); render body no longer mutates state

## Decisions Made
- Preserved the "always advance ref even when banner is null" behavior inside the new effect, matching the existing comment intent and preventing re-processing of non-qualifying events
- Kept effect dependency array as exactly `[eventLog]` — `setActive` identity is stable (React guarantee) and the ref is exempt from dependencies per React rules
- Did not change the auto-dismiss `useEffect([active])` timer, `getBannerMessage`, JSX return, CSS, or Zustand selector

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

The worktree did not have a `node_modules` directory, causing the `pnpm exec lint-staged` pre-commit hook to fail with "lint-staged not found". Resolved by running `pnpm install --frozen-lockfile --ignore-scripts` in the worktree root to create a local `node_modules` from the shared pnpm content-addressable store. All packages resolved from the existing store (0 downloads). The hook then ran successfully on the second commit attempt.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- GAP-1 (CR-02) closed: EventBanner no longer violates React's rules of hooks
- UX-14 single-fire guarantee is now reliable under React Strict Mode — double-invoke will advance the ref only inside the effect (which React runs once per commit in production), not during each render pass
- GAP-2 (CR-01 — clock freeze at half boundary) remains open and is the next gap closure plan

---
*Phase: 18.4-ux-enhancements*
*Completed: 2026-07-02*

## Self-Check: PASSED

- packages/client/src/components/EventBanner.tsx: FOUND
- .planning/phases/18.4-ux-enhancements/18.4-G1-SUMMARY.md: FOUND
- Commit 589daf4: FOUND
