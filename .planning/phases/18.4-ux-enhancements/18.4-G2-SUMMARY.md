---
phase: 18.4-ux-enhancements
plan: G2
subsystem: server/game-engine
tags: [bug-fix, clock, tackle, steal, half-time, game-engine, tdd]
dependency_graph:
  requires: []
  provides: [checkHalfEndOnTackle, half-end-tackle-steal-boundary]
  affects: [applyMove, UX-07-game-speed, match-clock]
tech_stack:
  added: []
  patterns: [module-scope-helper, ?? nullish-coalescing-fallback]
key_files:
  created: []
  modified:
    - packages/server/src/gameEngine.ts
    - packages/server/src/__tests__/gameEngine.phase8.test.ts
decisions:
  - 'Exported checkHalfEndOnTackle as a named export so unit tests can call it directly without constructing full end-to-end tackle scenarios'
  - "Helper reads state.addedTime without re-rolling — 'set once per half' invariant stays exclusively in applyEndTurn"
  - "Used ?? 'PASS' pattern at each call site rather than an if/else so the minimal diff is self-documenting"
metrics:
  duration: 16m 12s
  completed: 2026-07-02T18:07:00Z
  tasks_completed: 1
  files_changed: 2
---

# Phase 18.4 Plan G2: checkHalfEndOnTackle — Half-End Boundary on Tackle/Steal Summary

**One-liner:** Exported `checkHalfEndOnTackle` helper mirrors `applyEndTurn`'s half-end logic in the three tackle/steal success paths of `applyMove`, preventing clock-freeze in Fast mode when a tackle pushes `actionCount` past `halfEnd`.

## What Was Built

### Task 1: Add half-end check to the three tackle/steal success paths in applyMove (TDD)

**RED phase (commit ef08475):** Added 9 failing tests to `gameEngine.phase8.test.ts`:

- 6 unit tests for the (not-yet-exported) `checkHalfEndOnTackle` helper covering null/boundary/half-1/half-2 cases
- 2 end-to-end `applyMove` tests for tackle success returning HALF_TIME (half 1) and FULL_TIME (half 2) when `newActionCount >= halfEnd`
- 1 e2e steal success test for the same boundary

**GREEN phase (commit e1ad201):** Implemented the fix in `gameEngine.ts`:

- Added `export function checkHalfEndOnTackle(state, newActionCount): 'HALF_TIME' | 'FULL_TIME' | null` immediately before the `applyMove` section divider
- Applied at all three tackle/steal success return paths using `phase: endPhase ?? 'PASS'` and `actionCount: newActionCount`
- All 489 server tests pass, 1 skipped, 1 todo; no regressions

## Verification

- `pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts` (from worktree packages/server): 75/75 tests passed
- Full server suite `pnpm exec vitest run` (worktree): 489 passed, 1 skipped, 1 todo; 23/23 test files green
- `grep -c "checkHalfEndOnTackle(" gameEngine.ts` = 4 (1 definition + 3 call sites)
- Helper body: reads only `state.half` and `state.addedTime`; no `randomInt`, no `options`, no re-roll

## Acceptance Criteria Verification

- [x] `checkHalfEndOnTackle` appears >= 4 times in gameEngine.ts (4 confirmed: 1 def + 3 call sites)
- [x] Three tackle/steal success returns use `phase: endPhase ?? 'PASS'` pattern
- [x] Helper does NOT call `randomInt`, does NOT read `options`, does NOT re-assign `state.addedTime`
- [x] New tests exist asserting: (a) Fast-mode tackle past halfEnd in half 1 → `HALF_TIME`; (b) half 2 → `FULL_TIME`; (c) steal success past halfEnd → end phase; (d) `newActionCount < halfEnd` → `PASS`; (e) returned `actionCount === state.actionCount + GAME_SPEED_MINUTES[state.gameSpeed]`
- [x] Full server suite passes with no new failures

## Commits

| Commit  | Type | Description                                                                 |
| ------- | ---- | --------------------------------------------------------------------------- |
| ef08475 | test | RED: 9 failing tests for checkHalfEndOnTackle and e2e tackle/steal half-end |
| e1ad201 | feat | GREEN: checkHalfEndOnTackle helper + 3 call sites in applyMove              |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Steal test scenario used occupied destination hex**

- **Found during:** RED phase test run — `result.ok` was `false` (OCCUPIED) for steal tests
- **Issue:** Initial steal test placed `awayStealDefender` at `{q:11,r:7}` (1 hex from carrier), then had the carrier move TO `{q:11,r:7}`. That hex is already occupied by the defender, so `validateMove` rejected with OCCUPIED before the ZoI steal could fire.
- **Fix:** Moved `awayStealDefender` to `{q:12,r:7}` so the carrier at `{q:10,r:7}` can move to `{q:11,r:7}` (1 hex away from the defender), correctly triggering the ZoI steal.
- **Files modified:** `packages/server/src/__tests__/gameEngine.phase8.test.ts`
- **Commit:** ef08475 (corrected before committing RED)

**2. [Rule 3 - Blocking] pnpm install required in worktree before vitest could resolve @counter-attack/shared**

- **Found during:** First attempt to run tests (vitest resolved the worktree's own node_modules which were empty)
- **Fix:** Ran `pnpm install` (frozen-lockfile, reused local store) and `pnpm --filter @counter-attack/shared build` in the worktree root. Standard worktree bootstrap; no code changes.

## Known Stubs

None - this plan modifies pure logic paths with no UI or data source stubs.

## Threat Flags

No new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

Files created/modified:

- packages/server/src/gameEngine.ts — FOUND
- packages/server/src/**tests**/gameEngine.phase8.test.ts — FOUND
- .planning/phases/18.4-ux-enhancements/18.4-G2-SUMMARY.md — FOUND

Commits:

- ef08475 (RED: failing tests) — FOUND
- e1ad201 (GREEN: implementation) — FOUND
