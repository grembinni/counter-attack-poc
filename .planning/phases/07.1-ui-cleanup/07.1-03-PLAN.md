---
phase: 07.1-ui-cleanup
plan: 03
type: execute
wave: 2
depends_on: ['07.1-01', '07.1-02']
files_modified:
  - packages/client/src/components/PitchMarkings.tsx
  - packages/client/src/components/GoalNets.tsx
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/PieceOverlay.tsx
  - packages/client/src/components/PlayerStatsPanel.tsx
  - packages/client/src/components/PlayerStatsPanel.module.css
  - packages/client/src/components/PlayerStatsPanel.test.tsx
  - packages/client/src/components/GameBoard.tsx
autonomous: true
requirements: [PITCH-02, PITCH-03, TEAM-02]
must_haves:
  truths:
    - 'Halfway line, centre circle, both penalty boxes, both 6-yard boxes, and 4 corner arcs render as SVG overlay inside the HexGrid <g>'
    - 'Two goal nets render with a mesh pattern just outside the pitch boundary at each end'
    - "Clicking any piece (active or non-active player) opens a stats panel showing the piece's name, role, and all 10 attributes"
    - 'Piece inspection is local client state only and never emits to the server'
    - 'PlayerStatsPanel renders between ActionPanel and ActionLog in the sidebar; renders null when no piece is selected'
  artifacts:
    - path: 'packages/client/src/components/PitchMarkings.tsx'
      provides: 'SVG halfway line, centre circle, penalty/6-yard boxes, corner arcs (D-07, D-08, D-12)'
      exports: ['PitchMarkings']
    - path: 'packages/client/src/components/GoalNets.tsx'
      provides: 'Two goal-net rects with mesh pattern (D-09)'
      exports: ['GoalNets']
    - path: 'packages/client/src/components/PlayerStatsPanel.tsx'
      provides: 'Selected-piece attribute display (D-05, D-06, TEAM-02)'
      exports: ['PlayerStatsPanel']
    - path: 'packages/client/src/components/PlayerStatsPanel.test.tsx'
      provides: 'TEAM-02 coverage: renders null + renders stats + all 10 attribute labels'
  key_links:
    - from: 'packages/client/src/components/PieceOverlay.tsx'
      to: 'selectPiece(piece.id)'
      via: 'always-wired onInspect prop'
      pattern: 'onInspect'
    - from: 'packages/client/src/components/PlayerStatsPanel.tsx'
      to: 'useGameStore selectedPieceId + gameState.pieces'
      via: 'per-slice selectors'
      pattern: "useGameStore\\(\\(s\\) => s.selectedPieceId\\)"
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'PitchMarkings + GoalNets'
      via: 'JSX render inside <g>'
      pattern: '<PitchMarkings'
---

<objective>
Add the visual pitch markings and the player stats panel — the two larger client features of Phase 7.1.
Create PitchMarkings (halfway line, centre circle, penalty boxes, 6-yard boxes, corner arcs — D-07, D-08,
D-12) and GoalNets (mesh-pattern rects outside the pitch edge — D-09) and wire them into HexGrid.tsx.
Create PlayerStatsPanel (D-05, D-06, TEAM-02) wired to `selectedPieceId`, add an always-on `onInspect`
prop to PieceOverlay so both players can inspect any piece, and slot the panel into GameBoard.

Purpose: These are the cosmetic-field and stats-display deliverables. They build on the cleaned HexCell
fill (Plan 02, D-10) and the corrected team positions (Plan 01) and are grouped here because both center
on the HexGrid SVG / sidebar rendering layer.

Output: Two new SVG sub-components, the PlayerStatsPanel + its CSS + tests, a PieceOverlay onInspect
prop, and HexGrid/GameBoard wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-ui-cleanup/07.1-CONTEXT.md
@.planning/phases/07.1-ui-cleanup/07.1-RESEARCH.md
@.planning/phases/07.1-ui-cleanup/07.1-PATTERNS.md
@packages/client/src/components/HexGrid.tsx
@packages/client/src/components/PieceOverlay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PitchMarkings and GoalNets SVG sub-components and wire into HexGrid (D-07, D-08, D-09, D-12)</name>
  <files>packages/client/src/components/PitchMarkings.tsx, packages/client/src/components/GoalNets.tsx, packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (confirm the single `<svg>` root, `<defs>` with `clipPath id="pitch-clip"`, the outer `<g transform="translate(translateX, translateY)" clipPath="url(#pitch-clip)">`, the Layer 1 hex map, Layer 2 BallMarker, Layer 3 PieceOverlay, and that translateX=HEX_SIZE=20, translateY=HEX_SIZE*sqrt3/2)
    - packages/client/src/components/HexCell.tsx (SVG element conventions: stroke="white", strokeOpacity, pointerEvents="none")
    - packages/client/src/utils/hexToPixel.ts (confirm axialToPixel formula and HEX_SIZE so coordinate constants below are correct: col step 30px, row step ~34.64px)
    - .planning/phases/07.1-ui-cleanup/07.1-RESEARCH.md (Pattern 1, Pitfall 3, Code Examples — coordinate table lines 360-379)
    - .planning/phases/07.1-ui-cleanup/07.1-PATTERNS.md (HexGrid integration, lines 212-284)
  </read_first>
  <action>
    Create packages/client/src/components/PitchMarkings.tsx as a named-export function `PitchMarkings`
    taking no props. It returns a React fragment of SVG elements using these LOCAL coordinates (already
    inside the `<g translate(20, 17.32)>` — do NOT add the translate offset again, per Pitfall 3):
    - Halfway line: `<line x1={540} y1={17.3} x2={540} y2={883.3} />`
    - Centre circle: `<circle cx={540} cy={450.3} r={103.9} fill="none" />`
    - Home penalty box: `<rect x={-10} y={173.2} width={210} height={536.9} fill="none" />`
    - Away penalty box: `<rect x={880} y={173.2} width={210} height={536.9} fill="none" />`
    - Home 6-yard box: `<rect x={-10} y={294.4} width={80} height={329.1} fill="none" />`
    - Away 6-yard box: `<rect x={1030} y={294.4} width={80} height={329.1} fill="none" />`
    - Four corner arcs (D-12) as `<path>` quarter circles:
      top-left `M -10,57.3 A 40,40 0 0,0 50,17.3`; bottom-left `M -10,843.3 A 40,40 0 0,1 50,883.3`;
      top-right `M 1090,57.3 A 40,40 0 0,1 1030,17.3`; bottom-right `M 1090,843.3 A 40,40 0 0,0 1030,883.3`.
    Every element uses `stroke="white"`, `strokeWidth={1.5}`, `strokeOpacity={0.6}`, `fill="none"`
    (for line/circle/path; rects also `fill="none"`), and `pointerEvents="none"`.

    Create packages/client/src/components/GoalNets.tsx as a named-export function `GoalNets` taking no props.
    It returns a fragment of two `<rect>` elements filled with `url(#goal-net)`:
    home net `x={-30} y={346.4} width={20} height={242.5}`; away net `x={1110} y={346.4} width={20} height={242.5}`.
    Both also `stroke="white"`, `strokeWidth={1}`, `strokeOpacity={0.6}`, `pointerEvents="none"`.

    Wire into HexGrid.tsx:
    1. Import `PitchMarkings` from './PitchMarkings.js' and `GoalNets` from './GoalNets.js'.
    2. Add the goal-net mesh `<pattern id="goal-net" x="0" y="0" width="8" height="8"
       patternUnits="userSpaceOnUse">` containing `<path d="M 0 0 L 8 0 M 0 0 L 0 8"
       stroke="rgba(255,255,255,0.4)" strokeWidth={0.8} />` inside the existing `<defs>`.
    3. Render `<GoalNets />` directly inside the outer `<g transform=translate>` but BEFORE / OUTSIDE the
       `clipPath="url(#pitch-clip)"` group so the nets (at x=-30 and x=1110) are not clipped. This requires
       splitting the current single `<g>` so the clipped layers (hex cells, BallMarker, PieceOverlay) sit
       in an inner `<g clipPath="url(#pitch-clip)">` while `<GoalNets />` is a sibling inside the outer
       translate `<g>`. Preserve all existing Layer 1/2/3 content and behavior unchanged.
    4. Render `<PitchMarkings />` INSIDE the clipped inner `<g>`, after the Layer 1 hex map and before the
       Layer 2 BallMarker (so markings sit over hex fill but under the ball and pieces).

  </action>
  <verify>
    <automated>pnpm --filter client test && pnpm --filter client exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - PitchMarkings.tsx exists, is a named export `export function PitchMarkings`, and contains `x1={540}` and `cx={540}` and `r={103.9}`
    - PitchMarkings.tsx contains four `<path` corner-arc elements with `A 40,40`
    - Every PitchMarkings element has `pointerEvents="none"`
    - GoalNets.tsx exists, named export, contains `fill="url(#goal-net)"`, with rects at `x={-30}` and `x={1110}`
    - HexGrid.tsx imports both `PitchMarkings` and `GoalNets`
    - HexGrid.tsx `<defs>` contains `<pattern id="goal-net"`
    - HexGrid.tsx renders `<GoalNets />` as a sibling of the `clipPath`-bearing `<g>` (not inside it) and `<PitchMarkings />` inside the clipped group between hex cells and BallMarker
    - `pnpm --filter client test` exits 0 and `tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>PitchMarkings and GoalNets render in the correct z-order and coordinate space; goal nets fall outside the clip; client tests and typecheck pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PlayerStatsPanel with onInspect wiring and slot into GameBoard (D-05, D-06, TEAM-02)</name>
  <files>packages/client/src/components/PlayerStatsPanel.tsx, packages/client/src/components/PlayerStatsPanel.module.css, packages/client/src/components/PlayerStatsPanel.test.tsx, packages/client/src/components/PieceOverlay.tsx, packages/client/src/components/HexGrid.tsx, packages/client/src/components/GameBoard.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (analog: per-slice useGameStore selectors, null-guard early return, CSS-module import, named export)
    - packages/client/src/components/ActionPanel.module.css (palette tokens and `.panel` container to copy)
    - packages/client/src/components/ActionPanel.test.tsx (test boilerplate: vi.mock socket, useGameStore.setState in beforeEach, render/screen, afterEach cleanup)
    - packages/client/src/components/PieceOverlay.tsx (current Props type and the base `<circle>` `onClick={isClickable ? onClick : undefined}` on line 41 — this becomes always-fire onInspect)
    - packages/client/src/components/HexGrid.tsx (the PieceOverlay call site on lines 116-123 — add the onInspect prop)
    - packages/client/src/components/GameBoard.tsx (sidebar `<aside>` with TurnIndicator, ActionPanel, ActionLog order)
    - packages/client/src/store/useGameStore.ts (confirm `selectedPieceId`, `selectPiece`, and `gameState.pieces` shapes; confirm selectPiece is local-only and never emits)
    - packages/shared/src/types.ts (PlayerPiece attribute keys: pace, shooting, tackling, dribbling, heading, saving, handling, resilience, aerialAbility, highPass)
    - .planning/phases/07.1-ui-cleanup/07.1-PATTERNS.md (PlayerStatsPanel structure lines 346-414, PieceOverlay edits lines 426-477, GameBoard edits lines 317-342, CSS lines 481-551, tests lines 555-633)
  </read_first>
  <behavior>
    - Renders null (container.firstChild is null) when selectedPieceId is null
    - Renders the selected piece's name and role when selectedPieceId is set (e.g. 'home-0' -> "Home GK")
    - Renders all 10 attribute labels: Pace, Shooting, Tackling, Dribbling, Heading, Saving, Handling, Resilience, Aerial Ability, High Pass
    - Renders the actual attribute value from gameState.pieces (e.g. home-9 has pace 9)
  </behavior>
  <action>
    Create packages/client/src/components/PlayerStatsPanel.tsx as a named export following the ActionPanel
    analog: per-slice selectors `const selectedPieceId = useGameStore((s) => s.selectedPieceId)` and
    `const pieces = useGameStore((s) => s.gameState.pieces)`. Return null when `!selectedPieceId`; find
    the piece by id; return null when not found. Render a `.panel` with a `.header` showing
    `{piece.name}` and a `.role` span showing `{piece.role}`, then a `.statGrid` mapping a
    `STAT_LABELS` array of `[keyof PlayerPiece, string]` pairs in this exact order:
    pace/Pace, shooting/Shooting, tackling/Tackling, dribbling/Dribbling, heading/Heading,
    saving/Saving, handling/Handling, resilience/Resilience, aerialAbility/Aerial Ability,
    highPass/High Pass. Each row is a `.statRow` with `.statLabel` (label) and `.statValue` (`piece[attr]`).
    Import `PlayerPiece` type from '@counter-attack/shared'.

    Create packages/client/src/components/PlayerStatsPanel.module.css copying ActionPanel.module.css
    palette tokens: `.panel` background #16213e, border 1px solid #0f3460, border-radius 4px, padding 16px,
    flex column gap 8px. Add `.header` (flex space-between, baseline, font-size 13px, weight 700, color
    #e0e0e0), `.role` (font-size 11px, weight 400, color #a0a0a0, uppercase, letter-spacing 0.05em),
    `.statGrid` (display grid, grid-template-columns 1fr auto, gap 3px 8px), `.statRow` (display contents),
    `.statLabel` (font-size 12px, color #a0a0a0), `.statValue` (font-size 12px, weight 700, color #f5c518,
    text-align right).

    Modify PieceOverlay.tsx (D-06): add `onInspect: () => void;` to the Props type. Change the base
    `<circle>` handler from `onClick={isClickable ? onClick : undefined}` to an inline handler that calls
    `onInspect()` always and then `if (isClickable) onClick();`. Keep `style={{ cursor: isClickable ?
    'pointer' : 'default' }}` (move cursor to style if it was on the conditional). The selected-ring,
    ball-carrier ring, and label elements are unchanged.

    Modify the PieceOverlay call site in HexGrid.tsx: add `onInspect={() => selectPiece(piece.id)}` so
    any piece click (active or non-active player) sets selectedPieceId locally. Do NOT add any socket.emit
    to this path — inspection is local state only (D-06).

    Modify GameBoard.tsx: import `PlayerStatsPanel` from './PlayerStatsPanel.js' and insert
    `<PlayerStatsPanel />` between `<ActionPanel />` and `<ActionLog />` in the sidebar `<aside>`.

    Create packages/client/src/components/PlayerStatsPanel.test.tsx following the ActionPanel.test.tsx
    analog (vi.mock '../socket.js', mockMovementState seed in beforeEach, afterEach cleanup). Implement the
    four behavior cases in <behavior>: null when no selection; renders name when 'home-0' selected;
    renders all 10 attribute labels; renders a correct attribute value for 'home-9'.

  </action>
  <verify>
    <automated>pnpm --filter client test -- --reporter=verbose src/components/PlayerStatsPanel.test.tsx && pnpm --filter client exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - PlayerStatsPanel.tsx is a named export, uses `useGameStore((s) => s.selectedPieceId)` and `((s) => s.gameState.pieces)`, returns null when no piece selected or not found
    - PlayerStatsPanel.tsx STAT_LABELS contains all 10 attributes including `['highPass', 'High Pass']` and `['aerialAbility', 'Aerial Ability']`
    - PlayerStatsPanel.module.css exists with `.panel`, `.header`, `.role`, `.statGrid`, `.statRow`, `.statLabel`, `.statValue` selectors
    - PieceOverlay.tsx Props type includes `onInspect: () => void`; the circle onClick calls onInspect unconditionally and onClick only when isClickable
    - PieceOverlay.tsx / HexGrid.tsx contain no socket.emit on the inspect path (inspection is local-only)
    - HexGrid.tsx PieceOverlay call site passes `onInspect={() => selectPiece(piece.id)}`
    - GameBoard.tsx renders `<PlayerStatsPanel />` between `<ActionPanel />` and `<ActionLog />`
    - PlayerStatsPanel.test.tsx asserts: null render; name render for 'home-0'; all 10 labels present; correct value for 'home-9'
    - `pnpm --filter client test -- src/components/PlayerStatsPanel.test.tsx` exits 0 and `tsc --noEmit` clean
  </acceptance_criteria>
  <done>PlayerStatsPanel renders selected-piece stats for both players via the always-on onInspect prop without emitting to the server, sits between ActionPanel and ActionLog, and its tests + typecheck pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                                                          |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| piece click -> client state | A non-active player can now trigger `selectPiece` via `onInspect`. This crosses only into local Zustand state; it must NOT cross into a server emit. |

## STRIDE Threat Register

| Threat ID | Category               | Component                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                           |
| --------- | ---------------------- | ------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-07.1-06 | Elevation of Privilege | onInspect on PieceOverlay | mitigate    | onInspect calls `selectPiece` (local state) only; the move-emitting path stays gated behind `isClickable` (isActivePlayer && MOVEMENT). Acceptance criteria assert no socket.emit on the inspect path, so a non-active player cannot trigger a move by inspecting (D-06). |
| T-07.1-07 | Information Disclosure | PlayerStatsPanel          | accept      | Squad attributes are already part of the broadcast gameState visible to both clients; displaying them reveals no new information. No new server endpoint or data exposure is added.                                                                                       |
| T-07.1-08 | Tampering              | SVG overlay coordinates   | accept      | PitchMarkings/GoalNets are static cosmetic constants with `pointerEvents="none"`; they cannot intercept gameplay clicks or alter state.                                                                                                                                   |
| T-07.1-SC | Tampering              | npm/pnpm installs         | mitigate    | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit confirms zero new dependencies). No install task present; nothing to gate.                                                                                                                     |

</threat_model>

<verification>
Run the full client suite and typecheck: `pnpm --filter client test && pnpm --filter client exec tsc --noEmit`
Confirm PlayerStatsPanel.test.tsx passes and existing HexGrid/PieceOverlay/GameBoard tests do not regress.
Manual smoke (optional): with two clients, the non-active player clicks an opponent piece and the stats
panel appears without any server round-trip.
</verification>

<success_criteria>

- Pitch markings (halfway line, centre circle, penalty/6-yard boxes, corner arcs) and goal nets render correctly (D-07, D-08, D-09, D-12)
- Goal nets fall outside the clip boundary at each pitch end
- Any piece click opens PlayerStatsPanel with name, role, and all 10 attributes for both players (D-05, D-06, TEAM-02)
- Inspection is local state only — no socket.emit on the inspect path
- `pnpm --filter client test` and `tsc --noEmit` both pass
  </success_criteria>

<output>
Create `.planning/phases/07.1-ui-cleanup/07.1-03-SUMMARY.md` when done
</output>
