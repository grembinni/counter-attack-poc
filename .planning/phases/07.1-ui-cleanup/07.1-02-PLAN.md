---
phase: 07.1-ui-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/LobbyScreen.tsx
  - packages/client/src/components/LobbyScreen.test.tsx
  - packages/client/src/App.tsx
  - packages/client/src/components/HexCell.tsx
autonomous: true
requirements: [CONN-02, CONN-03, PITCH-01]
must_haves:
  truths:
    - 'Pressing Enter in the JoinRoomScreen room-code input emits ROOM_JOIN, same as the Join button'
    - "The Join button disables and shows a 'Joining...' label after submit until a roomError arrives"
    - 'When a roomError arrives, the Join button re-enables'
    - "WaitingScreen displays 'Share the code above with your opponent.'"
    - 'App.tsx and LobbyScreen.tsx contain no console.log statements'
    - 'Goal-region hexes render with the same grass-stripe fill as the rest of the pitch (no black fill)'
  artifacts:
    - path: 'packages/client/src/components/LobbyScreen.tsx'
      provides: 'Enter-key submit + loading state + WaitingScreen text (D-02, D-03, D-04)'
    - path: 'packages/client/src/components/LobbyScreen.test.tsx'
      provides: 'CONN-02 coverage: Enter key + loading state'
    - path: 'packages/client/src/components/HexCell.tsx'
      provides: 'Goal hexes use grass fill (D-10)'
    - path: 'packages/client/src/App.tsx'
      provides: 'Debug console.log statements removed'
  key_links:
    - from: 'packages/client/src/components/LobbyScreen.tsx'
      to: 'socket.emit ROOM_JOIN'
      via: 'onKeyDown Enter handler -> handleSubmit'
      pattern: "e.key === 'Enter'"
---

<objective>
Polish the lobby/join flow and clean up Phase-7 debug artifacts in the client. Add Enter-key submit
(D-02) and a loading/disabled state (D-03) to JoinRoomScreen, add the WaitingScreen instructional text
(D-04), remove the goal-hex black fill so goal regions use the grass stripe pattern (D-10), and delete
the diagnostic console.log statements from App.tsx and LobbyScreen.tsx.

Purpose: These are independent client-only micro-improvements identified in Phase 7 UAT. None touch
the server, socket event shapes, or game logic. They have zero shared-data dependency and run in
parallel with Plan 01.

Output: Updated LobbyScreen.tsx, new LobbyScreen.test.tsx, cleaned App.tsx, and a one-line HexCell.tsx fill fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-ui-cleanup/07.1-CONTEXT.md
@.planning/phases/07.1-ui-cleanup/07.1-RESEARCH.md
@.planning/phases/07.1-ui-cleanup/07.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Enter-key submit, loading state, and WaitingScreen text to LobbyScreen (D-02, D-03, D-04)</name>
  <files>packages/client/src/components/LobbyScreen.tsx, packages/client/src/components/LobbyScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/LobbyScreen.tsx (current JoinRoomScreen and WaitingScreen sub-components; confirm the existing `handleSubmit`, the room-code `<input>`, the Join button, the React import on line 1, and the use of `useGameStore`/`setRoomError`/`ClientEvents.ROOM_JOIN`)
    - packages/client/src/components/ActionPanel.test.tsx (analog test boilerplate: vi.mock of '../socket.js', useGameStore.setState in beforeEach, render/fireEvent/screen, afterEach cleanup)
    - packages/client/src/mock/index.js (mockMovementState used to seed gameState in tests)
    - .planning/phases/07.1-ui-cleanup/07.1-PATTERNS.md (exact edits, lines 156-208, and test cases, lines 657-699)
  </read_first>
  <behavior>
    - Enter key: fireEvent.keyDown on the room-code input with key 'Enter' (after typing a valid code) calls socket.emit with 'room:join' and the entered code
    - Loading: after clicking Join (with a valid code), the button is disabled and its text matches /joining/i
    - Re-enable: when useGameStore.setState({ roomError: 'NOT_FOUND' }) fires, the Join button becomes enabled again
    - Guard: an empty input must not emit ROOM_JOIN and must keep the button disabled
  </behavior>
  <action>
    In JoinRoomScreen (LobbyScreen.tsx):
    1. Add `useEffect` to the React import on line 1 (`import { useState, useEffect } from 'react';`).
    2. Add local state `const [isJoining, setIsJoining] = useState(false);` after the existing `useState('')`.
    3. Add `const roomError = useGameStore((s) => s.roomError);` if not already selected in this sub-component.
    4. Add `useEffect(() => { if (roomError) setIsJoining(false); }, [roomError]);` after the state declarations.
    5. Update `handleSubmit` to guard and set loading: early-return when `input.length === 0 || isJoining`;
       call `setRoomError(null)`; call `setIsJoining(true)`; then `socket.emit(ClientEvents.ROOM_JOIN, input)`.
       Remove the two debug console.log lines inside handleSubmit (the '[join] handleSubmit ...' and
       '[join] emitted room:join' lines).
    6. Add `onKeyDown={(e) => { if (e.key === 'Enter') handleSubmit(); }}` to the room-code `<input>`.
    7. Update the Join button: `disabled={input.length === 0 || isJoining}` and label
       `{isJoining ? 'Joining...' : 'Join Game'}`.

    In WaitingScreen (LobbyScreen.tsx): replace the body paragraph text
    `Share this code to invite someone:` with `Share the code above with your opponent.`
    Keep the existing `styles.body` className and surrounding markup unchanged.

    Create packages/client/src/components/LobbyScreen.test.tsx following the ActionPanel.test.tsx analog:
    vi.mock '../socket.js' (emit/on/off/connected mocks), afterEach cleanup, and a beforeEach that
    seeds useGameStore.setState with screen 'JOIN_ROOM', null selection, null roomCode/playerSlot, and
    null errors. Implement the four behavior cases listed in <behavior> above, importing the mocked
    socket via `await import('../socket.js')` to assert emit calls.

  </action>
  <verify>
    <automated>pnpm --filter client test -- --reporter=verbose src/components/LobbyScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LobbyScreen.tsx imports `useEffect` from 'react'
    - LobbyScreen.tsx contains `e.key === 'Enter'` wired to handleSubmit on the room-code input
    - LobbyScreen.tsx Join button has `disabled={input.length === 0 || isJoining}` and renders `'Joining...'` when isJoining
    - handleSubmit early-returns on empty input or while isJoining and calls `setIsJoining(true)` before emit
    - LobbyScreen.tsx contains the exact string `Share the code above with your opponent.`
    - LobbyScreen.tsx contains no `console.log` substring
    - LobbyScreen.test.tsx exists and asserts: Enter emits 'room:join'; button disables + shows /joining/i after click; button re-enables when roomError set
    - `pnpm --filter client test -- src/components/LobbyScreen.test.tsx` exits 0
  </acceptance_criteria>
  <done>Enter-key submit, loading state, and WaitingScreen text implemented; LobbyScreen.test.tsx passes all four cases; no console.log remains in LobbyScreen.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Remove debug console.log from App.tsx and remove black goal-hex fill in HexCell (cleanup, D-10)</name>
  <files>packages/client/src/App.tsx, packages/client/src/components/HexCell.tsx</files>
  <read_first>
    - packages/client/src/App.tsx (locate every console.log and the `onAnyEvent` debug handler with its socket.onAny/offAny registrations; confirm whether removing them empties the `onConnect` handler body)
    - packages/client/src/components/HexCell.tsx (confirm the `isGoal` const built from `isInRegion(hex, 'homeGoal') || isInRegion(hex, 'awayGoal')`, the `baseFill = isGoal ? '#1a1a1a' : ...` ternary, and the `isInRegion` import on the import line)
    - .planning/phases/07.1-ui-cleanup/07.1-RESEARCH.md (Debug Cleanup Inventory, lines 483-503; Pitfall 1, lines 301-308)
  </read_first>
  <action>
    In App.tsx, delete these debug artifacts (8 total):
    - The 6 `console.log('[socket] ...')` statements (connect, game:state, room:joined, room:error,
      game:error, game:disconnect-warning).
    - The `onAnyEvent` function declaration AND its `socket.onAny(onAnyEvent)` registration AND its
      `socket.offAny(onAnyEvent)` cleanup line (remove all three).
    - The `console.log('[socket] connect() called ...')` line.
    If removing the connect-handler console.log leaves `onConnect` with an empty body, delete the
    `onConnect` function together with its `socket.on('connect', onConnect)` and
    `socket.off('connect', onConnect)` lines. Do NOT remove any other socket listener registrations or
    their cleanups — only the debug-only `connect`/`onAny` paths.

    In HexCell.tsx (D-10):
    - Delete the `const isGoal = isInRegion(hex, 'homeGoal') || isInRegion(hex, 'awayGoal');` line.
    - Change `baseFill` from `isGoal ? '#1a1a1a' : isDark ? '#3d6b34' : '#4a7c3f'` to
      `isDark ? '#3d6b34' : '#4a7c3f'`.
    - Remove `isInRegion` from the `@counter-attack/shared` import on the import line, keeping
      `isDifficultAngle` (verify `isInRegion` has no other use in the file before removing).

  </action>
  <verify>
    <automated>pnpm --filter client test && grep -rn "console.log" packages/client/src/App.tsx packages/client/src/components/HexCell.tsx; if [ $? -eq 0 ]; then echo "FAIL: console.log remains"; exit 1; fi; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains no `console.log` substring and no `onAny`/`offAny` references
    - App.tsx still registers and cleans up the non-debug socket listeners (game:state, room:joined, room:error, game:error, game:disconnect-warning handlers remain functional)
    - HexCell.tsx no longer contains the string `#1a1a1a`
    - HexCell.tsx no longer contains `isGoal`
    - HexCell.tsx import line no longer imports `isInRegion` (only `isDifficultAngle` remains from shared, unless other uses exist)
    - `pnpm --filter client test` exits 0 (no regression; existing HexCell/App tests still pass)
  </acceptance_criteria>
  <done>All 10 debug log artifacts removed from App.tsx and LobbyScreen.tsx (LobbyScreen handled in Task 1), goal hexes use grass fill, client test suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                                                                |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client form -> server socket | The room-code input already crosses into a ROOM_JOIN emit. This plan changes UI affordances around that emit but does not alter the payload or validation. |

## STRIDE Threat Register

| Threat ID | Category               | Component             | Disposition | Mitigation Plan                                                                                                                                                                           |
| --------- | ---------------------- | --------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-07.1-03 | Tampering              | JoinRoomScreen input  | accept      | Input retains existing `maxLength={5}` + `.toUpperCase()` normalization; the loading-state change does not weaken it. Server remains authoritative on room validity (CONN-04, unchanged). |
| T-07.1-04 | Denial of Service      | double-submit on Join | mitigate    | D-03 loading state disables the button and guards handleSubmit with `isJoining` early-return, preventing rapid duplicate ROOM_JOIN emits.                                                 |
| T-07.1-05 | Information Disclosure | debug console.log     | mitigate    | Removing the App.tsx/LobbyScreen.tsx console.log statements eliminates leakage of room codes, slots, and tokens to the browser console (room:joined log currently prints `hasToken`).     |
| T-07.1-SC | Tampering              | npm/pnpm installs     | mitigate    | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit confirms zero new dependencies). No install task present; nothing to gate.                                     |

</threat_model>

<verification>
Run the full client suite: `pnpm --filter client test`
Confirm LobbyScreen.test.tsx passes and no existing HexCell/App tests regress.
Confirm no `console.log` remains in App.tsx or LobbyScreen.tsx.
</verification>

<success_criteria>

- Enter key submits the join form and the Join button shows loading/disabled state (D-02, D-03)
- WaitingScreen shows "Share the code above with your opponent." (D-04)
- Goal hexes render with grass fill, not black (D-10)
- Zero console.log statements in App.tsx and LobbyScreen.tsx
- `pnpm --filter client test` exits 0
  </success_criteria>

<output>
Create `.planning/phases/07.1-ui-cleanup/07.1-02-SUMMARY.md` when done
</output>
