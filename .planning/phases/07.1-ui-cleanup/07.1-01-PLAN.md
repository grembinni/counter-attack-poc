---
phase: 07.1-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/teams.ts
  - packages/shared/src/pitch.ts
  - packages/shared/src/pitch.test.ts
autonomous: true
requirements: [TEAM-01, PITCH-03]
must_haves:
  truths:
    - 'HOME_SQUAD and AWAY_SQUAD each hold 11 players positioned on the real 37x26 board (q in [0,36], r in [0,25])'
    - 'Home pieces all sit in the home half (q <= 18); away pieces all sit in the away half (q >= 18)'
    - 'Away positions mirror home positions across q=18 (q_away = 36 - q_home)'
    - 'isDifficultAngle returns true for the 4 corner-kick clusters and false for old penalty-area hexes'
    - 'DIFFICULT_ANGLE_HEXES still contains exactly 16 hexes'
  artifacts:
    - path: 'packages/shared/src/teams.ts'
      provides: '4-5-2 starting positions on the 37x26 board (D-01)'
      contains: 'position: { q: 1, r: 13 }'
    - path: 'packages/shared/src/pitch.ts'
      provides: 'DIFFICULT_ANGLE_HEXES relocated to corner-kick zones (D-11)'
      contains: '{ q: 0, r: 0 }'
    - path: 'packages/shared/src/pitch.test.ts'
      provides: 'Updated assertions covering corner-hex difficult angles (PITCH-03)'
  key_links:
    - from: 'packages/shared/src/pitch.test.ts'
      to: 'packages/shared/src/pitch.ts'
      via: 'isDifficultAngle import'
      pattern: "isDifficultAngle\\(\\{ q: 0, r: 0 \\}\\)"
---

<objective>
Correct the two shared-data sources for Phase 7.1: replace placeholder team starting positions
with a real 4-5-2 formation on the 37x26 board (D-01), and relocate the difficult-angle hexes to
corner-kick zones (D-11). Update pitch.test.ts so the existing difficult-angle assertions reflect
the new corner positions (PITCH-03).

Purpose: Teams currently spawn on a placeholder 25x16 grid, which is wrong for the real board the
client now renders. Difficult-angle hexes are still in their Phase-4 penalty-area approximation.
Both are pure data changes in `packages/shared` with no server or socket impact.

Output: Updated `teams.ts`, `pitch.ts`, and `pitch.test.ts` with green shared-package tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-ui-cleanup/07.1-CONTEXT.md
@.planning/phases/07.1-ui-cleanup/07.1-RESEARCH.md
@.planning/phases/07.1-ui-cleanup/07.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace HOME_SQUAD and AWAY_SQUAD positions with 4-5-2 board coordinates (D-01)</name>
  <files>packages/shared/src/teams.ts</files>
  <read_first>
    - packages/shared/src/teams.ts (current placeholder positions and exact object shape for all 22 pieces; confirm the role distribution is 1 GK + 4 DEF + 3 MID + 3 FWD per team and that every piece has a `highPass` attribute)
    - packages/shared/src/pitch.ts (confirm board bounds: PITCH_HEXES covers q in [0,36], r in [0,25]; kickOffHex = { q: 18, r: 13 })
    - .planning/phases/07.1-ui-cleanup/07.1-PATTERNS.md (the exact position table for all 22 pieces, lines 41-66)
  </read_first>
  <action>
    Replace ONLY the `position:` field on each of the 22 PlayerPiece objects. Do not touch ids, names,
    roles, or any attribute values.

    Set HOME_SQUAD positions exactly:
    home-0 GK = { q: 1, r: 13 }; home-1 DEF = { q: 4, r: 6 }; home-2 DEF = { q: 4, r: 10 };
    home-3 DEF = { q: 4, r: 16 }; home-4 DEF = { q: 4, r: 20 }; home-5 MID = { q: 8, r: 7 };
    home-6 MID = { q: 9, r: 13 }; home-7 MID = { q: 8, r: 19 }; home-8 FWD = { q: 13, r: 9 };
    home-9 FWD = { q: 14, r: 13 }; home-10 FWD = { q: 13, r: 17 }.

    Set AWAY_SQUAD positions as the mirror across q=18 (q_away = 36 - q_home, same r):
    away-0 GK = { q: 35, r: 13 }; away-1 DEF = { q: 32, r: 6 }; away-2 DEF = { q: 32, r: 10 };
    away-3 DEF = { q: 32, r: 16 }; away-4 DEF = { q: 32, r: 20 }; away-5 MID = { q: 28, r: 7 };
    away-6 MID = { q: 27, r: 13 }; away-7 MID = { q: 28, r: 19 }; away-8 FWD = { q: 23, r: 9 };
    away-9 FWD = { q: 22, r: 13 }; away-10 FWD = { q: 23, r: 17 }.

    Match the order in the file to the existing id order. Verify there are no duplicate (q,r)
    coordinates within either squad before saving.

    Replace the file header comment block (currently the placeholder 25x16 note) with a note stating
    that positions use the real 37x26 board (q 0-36, r 0-25), home half q<=18, away mirrors across
    q=18, per D-01 (Phase 7.1). Remove the "PLACEHOLDER" line.

  </action>
  <verify>
    <automated>pnpm --filter shared test -- --reporter=verbose src/teams.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - teams.ts: HOME_SQUAD contains exactly 11 entries; AWAY_SQUAD contains exactly 11 entries
    - teams.ts: home-0 position is `{ q: 1, r: 13 }` and away-0 position is `{ q: 35, r: 13 }`
    - Every home position has q in [1,14]; every away position has q in [22,35]; all r in [6,20]
    - For every i, away-i.q === 36 - home-i.q AND away-i.r === home-i.r
    - No two pieces in the same squad share the same (q,r) pair
    - teams.ts no longer contains the substring "PLACEHOLDER"
    - `pnpm --filter shared test -- src/teams.test.ts` exits 0
  </acceptance_criteria>
  <done>All 22 positions updated to the specified 37x26 coordinates, mirror invariant holds, no duplicates, teams.test.ts passes.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate DIFFICULT_ANGLE_HEXES to corner-kick zones and update pitch.test.ts (D-11, PITCH-03)</name>
  <files>packages/shared/src/pitch.ts, packages/shared/src/pitch.test.ts</files>
  <read_first>
    - packages/shared/src/pitch.ts (current DIFFICULT_ANGLE_HEXES array and its JSDoc; confirm `buildRegion` helper signature and that `isDifficultAngle` reads from this set)
    - packages/shared/src/pitch.test.ts (the existing DIFFICULT_ANGLE_HEXES describe block; confirm the line asserting `isDifficultAngle({ q: 3, r: 7 })` returns true, and the size === 16 assertion)
    - .planning/phases/07.1-ui-cleanup/07.1-PATTERNS.md (exact new hex list, lines 91-100, and test edits, lines 119-147)
  </read_first>
  <action>
    In pitch.ts, replace the contents of the `DIFFICULT_ANGLE_HEXES = buildRegion([...])` array with
    these 16 hexes (4 per corner of the 37x26 board), preserving the `buildRegion` call and export shape:
    Top-left: { q: 0, r: 0 }, { q: 1, r: 0 }, { q: 0, r: 1 }, { q: 1, r: 1 };
    Bottom-left: { q: 0, r: 25 }, { q: 1, r: 25 }, { q: 0, r: 24 }, { q: 1, r: 24 };
    Top-right: { q: 36, r: 0 }, { q: 35, r: 0 }, { q: 36, r: 1 }, { q: 35, r: 1 };
    Bottom-right: { q: 36, r: 25 }, { q: 35, r: 25 }, { q: 36, r: 24 }, { q: 35, r: 24 }.

    Replace the JSDoc above DIFFICULT_ANGLE_HEXES to state the hexes are corner-kick zones (PITCH-03),
    relocated from penalty-area corners per D-11 (Phase 7.1), 4 hexes per corner x 4 corners = 16 total.

    In pitch.test.ts, make these edits:
    1. Change the assertion `expect(isDifficultAngle({ q: 3, r: 7 })).toBe(true)` to
       `expect(isDifficultAngle({ q: 0, r: 0 })).toBe(true)`.
    2. Add an `it(...)` test asserting `isDifficultAngle({ q: 3, r: 7 })` returns `false`
       (proving the old penalty-corner hex is no longer difficult after D-11).
    3. Add an `it(...)` test asserting `isDifficultAngle` returns true for one representative hex from
       each corner cluster: { q: 1, r: 1 }, { q: 1, r: 24 }, { q: 35, r: 1 }, { q: 35, r: 24 }.
    Leave the existing `DIFFICULT_ANGLE_HEXES.size === 16` assertion unchanged.

  </action>
  <verify>
    <automated>pnpm --filter shared test -- --reporter=verbose src/pitch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - pitch.ts: DIFFICULT_ANGLE_HEXES contains the exact 16 corner hexes listed above
    - pitch.ts JSDoc above DIFFICULT_ANGLE_HEXES references "corner" and "D-11"
    - pitch.test.ts contains `isDifficultAngle({ q: 0, r: 0 })` asserted `.toBe(true)`
    - pitch.test.ts contains an assertion that `isDifficultAngle({ q: 3, r: 7 })` is `.toBe(false)`
    - pitch.test.ts asserts true for { q: 1, r: 1 }, { q: 1, r: 24 }, { q: 35, r: 1 }, { q: 35, r: 24 }
    - The size === 16 assertion still passes
    - `pnpm --filter shared test -- src/pitch.test.ts` exits 0
  </acceptance_criteria>
  <done>DIFFICULT_ANGLE_HEXES holds 16 corner hexes, JSDoc updated, pitch.test.ts assertions reflect corner positions, all pitch tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                                       |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | This plan edits pure shared data/test files. No new trust boundary is introduced; data is consumed by already-trusted server initialization and client rendering. |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                                                                                                                                                        |
| --------- | ---------------------- | ------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-07.1-01 | Tampering              | teams.ts / pitch.ts data | accept      | Static data compiled into the server bundle; not user-supplied. Out-of-bounds positions are caught by teams.test.ts / pitch.test.ts (bounds and size assertions) rather than runtime input validation. |
| T-07.1-02 | Information Disclosure | shared package           | accept      | No secrets, PII, or auth material in these files. Squad attributes are gameplay data already broadcast to both clients.                                                                                |
| T-07.1-SC | Tampering              | npm/pnpm installs        | mitigate    | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit confirms zero new dependencies). No install task present; nothing to gate.                                                  |

</threat_model>

<verification>
Run the full shared suite to confirm no regression:
`pnpm --filter shared test`
All teams.ts and pitch.ts consumers (move/shot validators) must still pass with the new data.
</verification>

<success_criteria>

- HOME_SQUAD and AWAY_SQUAD use the 37x26 4-5-2 positions; away mirrors home across q=18
- DIFFICULT_ANGLE_HEXES holds the 16 corner-kick hexes
- pitch.test.ts asserts corner hexes are difficult and old penalty hexes are not
- `pnpm --filter shared test` exits 0
  </success_criteria>

<output>
Create `.planning/phases/07.1-ui-cleanup/07.1-01-SUMMARY.md` when done
</output>
