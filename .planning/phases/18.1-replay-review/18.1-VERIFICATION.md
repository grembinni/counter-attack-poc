---
phase: 18.1-replay-review
verified: 2026-06-21T23:23:49Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
deferred:
  - truth: 'GK_KICK ball deliveries produce a visible replay frame (no teleport)'
    addressed_in: 'Backlog item .planning/todos/pending/2026-06-21-bug-gk-kick-ball-delivery-invisible-during-replay.md'
    evidence: "User explicit decision (2026-06-21, during /gsd-execute-phase 18.1): GK_KICK lacks ballAfter and is excluded from REPLAY_ELIGIBLE_TYPES (gameEngine.ts:3986-3992 comment incorrectly calls it 'dead code' — code review 18.1-REVIEW.md CR-01 confirmed it is live, constructed at gameHandlers.ts:828-836). User accepted this as a tracked gap rather than expanding 18.1's scope; this phase's own must_haves were scoped to HEADED_PASS/GK_PUNT/steal-tackle only, which are correctly fixed and tested."
---

# Phase 18.1: Replay Review Verification Report

**Phase Goal:** Live-session replay correctly tracks ball position on every frame (pickups, intercepted passes, steals) without regression, and post-game replay playback has no unnecessary re-renders or redundant socket emissions.
**Verified:** 2026-06-21T23:23:49Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                          | Status   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A MOVE event ending in a successful steal/tackle produces a replay frame whose ball already shows the new (post-contest) carrier, not the stale pre-contest carrier            | VERIFIED | `gameEngine.ts:631-639` (tackle) and `gameEngine.ts:689-699` (steal) immutably rewrite the already-appended `moveEvent`'s `ballAfter` via `.map((e) => e === moveEvent ? correctedMoveEvent : e)` before returning. Regression test `replay.integration.test.ts:504-613` drives `applyMove` directly with forced dice (`stealDie:6`/`tackleDie:6`) and asserts the returned `MOVE` event's `ballAfter` equals the post-contest carrier, plus a downstream `buildReplayFrames` check. Confirmed passing via `npx vitest run -t "REPLAY-06"` (3/3 passed). FAIL paths (lines 667-682) are untouched, matching the plan's explicit "do not change FAIL paths" instruction — confirmed via diff inspection.                                                                                                     |
| 2   | A HEADED_PASS event produces a visible replay frame whose ball.position/ball.carrierId match the event's ballAfter                                                             | VERIFIED | `'HEADED_PASS'` present in `REPLAY_ELIGIBLE_TYPES` set (`gameEngine.ts:3990`). Test `replay.integration.test.ts:615-669` hand-crafts an eventLog with a `HEADER` (no frame) → `HEADED_PASS` (ballAfter populated) → `MOVE` (frame indexing control) and asserts a frame exists matching the HEADED_PASS ballAfter exactly. Passing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 3   | A GK_PUNT event produces a visible replay frame whose ball.position/ball.carrierId match the event's ballAfter                                                                 | VERIFIED | `'GK_PUNT'` present in `REPLAY_ELIGIBLE_TYPES` set (`gameEngine.ts:3991`). Same test file (lines 671+, mirrors HEADED_PASS structure) — confirmed passing as part of the 3/3 REPLAY-06 test run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 4   | The full server test suite passes with no regression                                                                                                                           | VERIFIED | `pnpm test` equivalent (`npx vitest run`) executed once in `packages/server`: 465 passed, 1 pre-existing skip, 1 pre-existing todo, 0 failures — matches SUMMARY's claimed baseline exactly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 5   | During phase === 'REPLAY' HexGrid wires no clickable hexes — every hex cell renders with cursor 'default' and no onClick effect                                                | VERIFIED | `HexGrid.tsx:441` wraps the entire onClick phase-branch cascade in `if (phase !== 'REPLAY')`; `isClickable` forced false at line 726. Test `HexGrid.test.tsx:654-673` (`no hex is clickable during replay`) seeds the exact geometry that would otherwise produce a clickable hex and asserts no polygon has `cursor: pointer`. A control-case test (line 675) proves the same geometry under `phase: 'MOVE'` DOES produce a clickable hex, validating the assertion is meaningful, not vacuous. Confirmed passing via `npx vitest run -t "DESIGN-02"` (3/3 passed).                                                                                                                                                                                                                                        |
| 6   | During phase === 'REPLAY' HexGrid produces no interactive highlight tints (risk/goal/shot-path/kickoff/move-target) even when piece/ball geometry would otherwise trigger them | VERIFIED | `validMoveHexSet`/`zoiRiskSet`/`tackleRiskSet` (the only previously-unconditional highlight derivations) are guarded with `phase !== 'REPLAY'` at `HexGrid.tsx:161-187`. All other highlight sets (`gkDiveTargetSet`, `gkKickTargetSet`, `quickThrowTargetSet`, `isHeaderNonGoalTarget`, etc.) were independently confirmed (by direct code read) to already be gated behind their own play-phase-specific condition (e.g. `phase === 'GK_KICK_TARGET'`), so REPLAY can never satisfy them — no additional guard was needed there, and none was added (consistent with the plan's narrow-guard approach). Test `HexGrid.test.tsx:691-707` seeds defender-adjacent-to-carrier geometry (which normally triggers a risk tint) under `phase: 'REPLAY'` and asserts zero risk polygons / no goal tint. Passing. |
| 7   | Pieces and the ball still render and move every frame during replay (replay remains visually correct)                                                                          | VERIFIED | The guard is scoped narrowly to highlight-set derivation, onClick derivation, and isClickable — `PitchMarkings`, `GoalNets`, `BallMarker`, `PieceOverlay` rendering is structurally untouched by the diff (confirmed via `git diff fdca370~1 fdca370`). No early-return or duplicated render tree was introduced.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 8   | The full client test suite passes with no regression                                                                                                                           | VERIFIED | `npx vitest run` executed once in `packages/client`: 225/225 passed, 0 failures — matches SUMMARY's claimed baseline exactly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

**Score:** 8/8 truths verified

### Deferred Items

Item not yet met but explicitly accepted by the user as a tracked gap rather than in-scope for this phase.

| #   | Item                                                  | Addressed In                                                                                           | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | GK_KICK ball delivery produces a visible replay frame | Backlog item `.planning/todos/pending/2026-06-21-bug-gk-kick-ball-delivery-invisible-during-replay.md` | Confirmed by direct code read: `gameHandlers.ts:828-836` constructs a live `GK_KICK` `ActionEvent` with no `ballAfter` field (`types.ts:280-288`); `GK_KICK` is absent from `REPLAY_ELIGIBLE_TYPES` (`gameEngine.ts:3971-3992`). The `gameEngine.ts:3988` comment incorrectly calls this "dead code, zero construction sites" — it is reachable via the live `GK_KICK_TARGET` → `GK_KICK_MOVE` flow (client-wired via `gkKickTargetSet`/`emitGKKickTarget` in `HexGrid.tsx:258,558`). Per the task's explicit instructions, this is NOT treated as a phase-blocking failure: the plan's own must_haves were scoped to HEADED_PASS/GK_PUNT/steal-tackle only, those are correctly fixed and tested, and the user made an explicit accept-as-gap decision on 2026-06-21. |

### Required Artifacts

| Artifact                                                   | Expected                                                                                                       | Status   | Details                                                                                                                                                                                                                                         |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/server/src/gameEngine.ts`                        | REPLAY_ELIGIBLE_TYPES includes HEADED_PASS and GK_PUNT; applyMove MOVE-event ballAfter is post-contest-correct | VERIFIED | Both literals present in the set (lines 3990-3991); steal/tackle success paths rewrite `ballAfter` immutably (lines 636-639, 696-699). Diff inspection confirms minimal, surgical change — FAIL paths untouched.                                |
| `packages/server/src/__tests__/replay.integration.test.ts` | Regression tests for contested-steal/tackle MOVE ballAfter and HEADED_PASS/GK_PUNT frame visibility            | VERIFIED | Two `REPLAY-06:`-prefixed `it()` blocks present (lines 504, 615); both pass; case A drives `applyMove` directly (not a hand-crafted eventLog), correctly exercising the actual fix location per the SUMMARY's documented test-design deviation. |
| `packages/client/src/components/HexGrid.tsx`               | phase === 'REPLAY' guard short-circuits highlight-set derivation, onClick derivation, isClickable cascade      | VERIFIED | All three guard sites present and wired (lines 162, 174, 187, 441, 726). `grep -n "REPLAY"` confirms 8 occurrences across comments and conditions.                                                                                              |
| `packages/client/src/components/HexGrid.test.tsx`          | REPLAY-phase assertions: no clickable cursor, no highlight tints                                               | VERIFIED | `describe('HexGrid — DESIGN-02: ...')` block present (line 653) with 3 `it()` cases (no-clickable-cursor, MOVE-phase control case, no-highlight-tint); all 3 pass.                                                                              |

### Key Link Verification

| From                                                                                      | To                               | Via                                                        | Status | Details                                                                                                                                                                                                             |
| ----------------------------------------------------------------------------------------- | -------------------------------- | ---------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| applyMove steal/tackle success returns                                                    | MOVE event in newEventLog        | post-contest ballAfter recomputation (`ballAfter` pattern) | WIRED  | `gameEngine.ts:636,696` — `correctedMoveEvent` built by spreading `moveEvent` with overridden `ballAfter`, then `.map()`'d into the returned `eventLog`.                                                            |
| buildReplayFrames generic flush branch                                                    | HEADED_PASS / GK_PUNT frame push | REPLAY_ELIGIBLE_TYPES membership                           | WIRED  | `gameEngine.ts:4139` — `if (REPLAY_ELIGIBLE_TYPES.has(event.type))` gates the frame push; `HEADED_PASS`/`GK_PUNT` are members (lines 3990-3991).                                                                    |
| HexGrid render body (highlight-set derivation + onClick derivation + isClickable cascade) | phase === 'REPLAY' guard         | early-false / skip-derivation branch                       | WIRED  | `HexGrid.tsx:162,174,187` (highlight sets), `441` (onClick cascade wrapped in `if (phase !== 'REPLAY')`), `726` (isClickable forced false). All 5 sites confirmed present and structurally correct via direct read. |

### Behavioral Spot-Checks

| Behavior                                       | Command                                               | Result                                                                                                 | Status |
| ---------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ------ |
| REPLAY-06 named tests pass                     | `cd packages/server && npx vitest run -t "REPLAY-06"` | 3 passed, 464 skipped (test-file filtering applied by name)                                            | PASS   |
| DESIGN-02 named tests pass                     | `cd packages/client && npx vitest run -t "DESIGN-02"` | 3 passed, 222 skipped                                                                                  | PASS   |
| Full server suite, no regression               | `cd packages/server && npx vitest run` (run once)     | 465 passed, 1 skipped, 1 todo, 0 failed                                                                | PASS   |
| Full client suite, no regression               | `cd packages/client && npx vitest run` (run once)     | 225 passed, 0 failed                                                                                   | PASS   |
| Commits referenced in SUMMARYs exist on branch | `git log --oneline --all \| grep <hashes>`            | All 6 hashes (5dc9dd8, 572d1cb, 5e82044, 3d50107, fdca370, 03056e1) found, merged into current history | PASS   |

### Requirements Coverage

| Requirement | Source Plan     | Description                                                                                                                                                                    | Status    | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| REPLAY-06   | 18.1-01-PLAN.md | Ball position updates correctly on every replay frame in a live session; ball correctly tracks pickups, intercepted passes, and steals mid-replay; UAT Test 6 from v1.1 passes | SATISFIED | Both server-side root causes (stale MOVE.ballAfter on contested pickup; HEADED_PASS/GK_PUNT missing from REPLAY_ELIGIBLE_TYPES) fixed and covered by passing regression tests. UAT Test 6 re-run checkpoint approved by user (recorded in 18.1-01-SUMMARY.md). GK_KICK is a known, explicitly-accepted residual gap (see Deferred Items) — does not block REPLAY-06 satisfaction since the requirement's listed symptom set (pickups, intercepted passes, steals) is the literal scope fixed here, and the user explicitly decided not to expand scope. |
| DESIGN-02   | 18.1-02-PLAN.md | Post-game replay playback is reviewed; unnecessary re-renders, redundant socket emissions, or frame-rate issues are identified and resolved                                    | SATISFIED | RESEARCH.md's audit traced the sole real cost center to HexGrid's unconditional per-frame interactive derivation; server emission (one GAME_STATE per 500ms tick) was independently confirmed already clean, requiring no change. The REPLAY guard is in place, tested, and the human-verify checkpoint (Profiler + Network tab) was approved by the user.                                                                                                                                                                                              |

Both requirement IDs declared in PLAN frontmatter (`REPLAY-06` in 18.1-01, `DESIGN-02` in 18.1-02) match the only two requirement IDs mapped to "Phase 18.1" in REQUIREMENTS.md's Traceability table (lines 104, 107) — no orphaned requirements.

### Anti-Patterns Found

| File                                                       | Line    | Pattern                                                                                                                                                                                                                       | Severity | Impact                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ---------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/server/src/gameEngine.ts`                        | 3988    | Misleading comment ("GK_KICK is intentionally excluded (dead code, zero construction sites)") left uncorrected in shipped code despite code review (CR-01) proving it factually false                                         | INFO     | Does not affect runtime behavior; could mislead a future reader. Tracked in the accepted backlog item, but the comment text itself was not corrected as part of accepting the gap. Non-blocking per user's explicit scope decision.                                                                                                                                                                                                         |
| `packages/server/src/__tests__/replay.integration.test.ts` | 341-350 | Tautological assertion (`intervalCleared` set `true` on both branches of an if/else, so `expect(intervalCleared).toBe(true)` can never fail) — pre-existing, flagged as WR-02 in code review, not addressed by either SUMMARY | WARNING  | This test (`T-08-15: disconnect handler clears replayTimer when set`) provides zero actual regression protection for the disconnect-clears-timer behavior it claims to verify. Pre-existing (not introduced by this phase) and not part of either plan's must_haves, but the file was in-scope for this phase's edits and the gap was not closed. Does not block the phase goal (REPLAY-06/DESIGN-02 are unaffected) but is worth tracking. |

No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER debt markers found in any of the four files modified by this phase (`gameEngine.ts`, `replay.integration.test.ts`, `HexGrid.tsx`, `HexGrid.test.tsx`).

### Human Verification Required

None outstanding. Both `checkpoint:human-verify` gates (UAT Test 6 re-run for plan 01; Profiler/Network inspection for plan 02) were presented to the user during execution and both received "approved" responses, recorded in 18.1-01-SUMMARY.md and 18.1-02-SUMMARY.md respectively. Both SUMMARYs self-disclose that the user's approval did not include itemized instrument readings — this is recorded as approval-of-checkpoint, not independently-confirmed specific values, consistent with the gates.md Escalation Gate pattern (human decision recorded, workflow resumed on the approved path).

### Gaps Summary

No blocking gaps. All 8 must-have truths derived from the two plans' frontmatter are verified against the actual codebase (not just SUMMARY claims) — code was read directly, tests were re-run independently by this verifier (not trusted from SUMMARY), and diffs were inspected to confirm minimal, correctly-scoped changes with no regression to non-REPLAY/non-RED-test behavior.

One deferred item (GK_KICK replay-invisibility) is explicitly out of scope per the user's documented decision and is tracked in a backlog file; it does not block this phase's goal achievement since the goal's literal text ("pickups, intercepted passes, steals") and the plan's must_haves never included GK_KICK.

Two non-blocking residual findings from code review (the misleading GK_KICK comment, and the pre-existing tautological disconnect test) remain uncorrected in the codebase. Neither affects REPLAY-06/DESIGN-02 satisfaction; both are noted above as INFO/WARNING for visibility, not as gaps requiring closure before proceeding.

---

_Verified: 2026-06-21T23:23:49Z_
_Verifier: Claude (gsd-verifier)_
