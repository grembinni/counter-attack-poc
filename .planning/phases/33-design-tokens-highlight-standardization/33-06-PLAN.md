---
phase: 33-design-tokens-highlight-standardization
plan: 06
type: execute
wave: 2
depends_on: [33-04]
files_modified:
  - packages/client/src/components/BallLocationRing.tsx
  - packages/client/src/components/BallLocationRing.test.tsx
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/HexGrid.module.css
  - packages/client/src/components/HexGrid.test.tsx
autonomous: true
requirements: [HILITE-01, HILITE-04]
must_haves:
  truths:
    - "No ad-hoc inline highlight-color polygon literals remain in HexGrid.tsx; every hex tint flows through HexCell's highlightType or ring prop"
    - 'The .hexTackleRisk CSS-module class is removed; tackle-risk renders via the HIGHLIGHT_STYLES table'
    - 'A dedicated white hex-edge ball-location marker renders always-on-top during response phases only, replacing the deleted HEADER-only gold overlay'
    - 'The GK-kick-target, pass-target, tackle-risk tints and the required/confirmed rings render via HexCell props'
  artifacts:
    - path: 'packages/client/src/components/BallLocationRing.tsx'
      provides: 'Standalone always-on-top white hex-edge ball-location marker (HILITE-04)'
      contains: '#ffffff'
      min_lines: 25
    - path: 'packages/client/src/components/BallLocationRing.test.tsx'
      provides: 'Tests: white edge, phase gating, position at ball hex'
      contains: 'BallLocationRing'
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: 'Consolidated highlight routing + BallLocationRing wiring'
      contains: 'BallLocationRing'
  key_links:
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'packages/client/src/components/HexCell.tsx'
      via: 'highlightType + ring props (no inline polygons)'
      pattern: "ring=\\{"
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'packages/client/src/components/BallLocationRing.tsx'
      via: 'topmost SVG sibling after PieceOverlay'
      pattern: '<BallLocationRing'
---

<objective>
Consolidate every ad-hoc inline highlight-color literal in `HexGrid.tsx` into `HexCell`'s `highlightType`/`ring` props (HILITE-01) and add the standalone always-on-top ball-location marker (HILITE-04, D-08/D-09). This depends on 33-04's extended `HexCell` (new tints + `ring` prop). It creates `BallLocationRing.tsx` (a new white hex-edge overlay rendered as the topmost SVG layer), deletes the HEADER-only gold ball-overlay it supersedes, removes the `.hexTackleRisk` CSS class, and migrates the HexGrid tests.

Purpose: `HexGrid.tsx` holds zero highlight-color literals; all hex colors live in the single `HIGHLIGHT_STYLES` table; the ball's hex is always visible during response phases via a dedicated marker.
Output: New `BallLocationRing` component + test; consolidated `HexGrid.tsx`; `.hexTackleRisk` removed; migrated `HexGrid.test.tsx`.
</objective>

<artifacts_this_phase_produces>
Symbols/files created by THIS plan (exclude from source-drift verification):

- New files: `packages/client/src/components/BallLocationRing.tsx`, `packages/client/src/components/BallLocationRing.test.tsx`
- New component/export: `BallLocationRing` (props: `ballPosition: HexCoord`, `phase: GamePhase`)
- New module-level constant in BallLocationRing.tsx: `BALL_MARKER_PHASES` (the gated response-phase set)
  </artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md
@.planning/phases/33-design-tokens-highlight-standardization/33-RESEARCH.md
@.planning/phases/33-design-tokens-highlight-standardization/33-PATTERNS.md
@.planning/phases/33-design-tokens-highlight-standardization/33-04-SUMMARY.md
@packages/client/src/components/HexCell.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the BallLocationRing standalone marker component and test</name>
  <files>packages/client/src/components/BallLocationRing.tsx, packages/client/src/components/BallLocationRing.test.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx (lines 213-223 — the independent-layer <circle> structural template; use the same pointerEvents="none" additive pattern)
    - packages/client/src/utils/hexToPixel.ts (axialToPixel + hexPolygonPoints imports used by HexCell.tsx line 4)
    - packages/shared/src/types.ts (GamePhase union at line 403 — confirm the exact member names for the gate)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (section B4: white hex-edge, strokeWidth 2.5, fill none; exact 10-phase visibility gate)
  </read_first>
  <behavior>
    - Renders a single `<polygon>` outlining the ball's hex edge with fill="none", stroke="#ffffff", strokeWidth 2.5, pointerEvents="none".
    - Renders that polygon ONLY when phase is one of the 10 response phases; returns null for any other phase.
    - The polygon points equal hexPolygonPoints(axialToPixel(ballPosition).cx, .cy) — i.e. it sits exactly on the ball's hex.
  </behavior>
  <action>
    Create `BallLocationRing.tsx` exporting `function BallLocationRing({ ballPosition, phase }: { ballPosition: HexCoord; phase: GamePhase })`. Define a module-level `const BALL_MARKER_PHASES: ReadonlySet<GamePhase> = new Set(['HEADER','SNAPSHOT','SNAPSHOT_TARGET','SNAPSHOT_DEFLECT','GK_DIVE','SHOT','GK_RESTART','GK_QUICK_THROW','GK_KICK_TARGET','GK_KICK_MOVE'])` (D-09, the exact gate resolved in UI-SPEC B4). If `!BALL_MARKER_PHASES.has(phase)` return `null`. Otherwise compute `{ cx, cy } = axialToPixel(ballPosition.q, ballPosition.r)` and render `<polygon points={hexPolygonPoints(cx, cy)} fill="none" stroke="#ffffff" strokeWidth={2.5} pointerEvents="none" />`. It is an SVG fragment (must be a child of the HexGrid `<svg>` root, like HexCell/PieceOverlay). Create `BallLocationRing.test.tsx` (RED-first per Wave 0) that: renders it inside an `<svg>` with phase='HEADER' and asserts exactly one polygon with stroke #ffffff, fill none, stroke-width 2.5; renders with phase='MOVE' and asserts no polygon; renders with a couple of other gated phases (e.g. GK_DIVE, GK_KICK_TARGET) and asserts the marker is present; and asserts the polygon points correspond to the given ballPosition (compare to axialToPixel/hexPolygonPoints of that hex).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- BallLocationRing.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `BallLocationRing.tsx` exports `BallLocationRing` and defines `BALL_MARKER_PHASES` with the 10 phases.
    - Rendering with a gated phase produces one white (stroke #ffffff) fill="none" strokeWidth 2.5 polygon; a non-gated phase produces none.
    - `pnpm --filter @counter-attack/client test -- BallLocationRing.test.tsx` passes.
  </acceptance_criteria>
  <done>A tested standalone white hex-edge marker renders only during the 10 response phases.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate HexGrid inline highlight polygons into HexCell props, delete the HEADER overlay, remove .hexTackleRisk, and wire BallLocationRing</name>
  <files>packages/client/src/components/HexGrid.tsx, packages/client/src/components/HexGrid.module.css</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (highlightType derivation lines ~420-495; the inline overlay polygons lines ~568-651; the SVG layer structure and pieces.map close at ~655-936; imports lines 1-27)
    - packages/client/src/components/HexCell.tsx (post-33-04: the 10-member HexHighlightType union + the ring?: 'required' | 'confirmed' prop)
    - packages/client/src/components/BallLocationRing.tsx (the component created in Task 1)
    - packages/client/src/components/HexGrid.module.css (the .hexTackleRisk rule to delete; keep .hexGrid)
    - .planning/phases/33-design-tokens-highlight-standardization/33-RESEARCH.md (Code Examples: the full inline-literal audit — the exact 5 overlay groups to migrate)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (B1/B2/B4: which literal maps to which type/ring; GK_QUICK_THROW merges into pass-target)
  </read_first>
  <action>
    In the per-hex derivation block, add three tint booleans and a ring value: `isGkKickTargetTint = gkKickTargetSet.has(hexId)`; `isPassTargetTint = (isPassTarget && !isInterceptionRisk && !isConfirmedPassTarget) || quickThrowTargetSet.has(hexId)` (merges GK_QUICK_THROW into pass-target per UI-SPEC B1); `isTackleRiskTint = isInterceptionRisk && !isConfirmedPassTarget`; and `const ring: 'required' | 'confirmed' | undefined = isCentreHex ? 'required' : isConfirmedPassTarget ? 'confirmed' : undefined`. Extend the existing `highlightType` priority ternary to emit `'tackle-risk'` for `isTackleRiskTint` (place adjacent to `isRisk`), `'gk-kick-target'` for `isGkKickTargetTint`, and `'pass-target'` for `isPassTargetTint` (place these before the `isSafeTint` fallback). Pass `ring` to `<HexCell ... {...(ring !== undefined ? { ring } : {})} />`. Then DELETE all six inline `<polygon>` overlay blocks inside `<g key={hexId}>` (lines ~568-651): the two centre-hex gold polygons (now `ring="required"`), the HEADER gold ball-overlay (superseded — remove entirely, D-08), the GK_KICK_TARGET sky-blue polygon (now `'gk-kick-target'`), the QUICK_THROW green polygon (now `'pass-target'`), the safe-pass green polygon (now `'pass-target'`), the interception-risk `.hexTackleRisk` polygon (now `'tackle-risk'`), and the confirmed-pass gold ring polygon (now `ring="confirmed"`). The existing `onClick` variable already covers GK-kick/quick-throw/pass-confirm/deselect interactions and is passed to HexCell — verify each interaction still fires through HexCell after removing the inline handlers. In `HexGrid.module.css`, delete the `.hexTackleRisk` rule (keep `.hexGrid`). Finally, import `BallLocationRing` and render `<BallLocationRing ballPosition={ball.position} phase={phase} />` as the LAST sibling inside the inner `<g>` (immediately after the `pieces.map(...)` block, before the closing `</g>` at line ~932) so it is the topmost always-on-top layer (D-09), outside the highlightType priority resolution.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- HexGrid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(|hexTackleRisk' packages/client/src/components/HexGrid.tsx` returns zero highlight/chrome color-literal hits (only non-color matches, if any, remain).
    - `HexGrid.module.css` no longer contains a `.hexTackleRisk` rule.
    - `HexGrid.tsx` renders `<BallLocationRing ... />` as the last child of the inner `<g>` and no longer contains the HEADER `phase === 'HEADER'` gold ball-overlay polygon.
    - `HexGrid.tsx` passes `ring` and the new highlightType values to HexCell; no inline `<polygon fill="rgba(...)"|"#...">` highlight siblings remain in the per-hex loop.
    - `pnpm --filter @counter-attack/client test -- HexGrid.test.tsx` passes.
  </acceptance_criteria>
  <done>HexGrid routes every highlight through HexCell props, the HEADER gold overlay and .hexTackleRisk are gone, and BallLocationRing is the topmost layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add HexGrid.test.tsx coverage for the new tints, rings, and ball marker</name>
  <files>packages/client/src/components/HexGrid.test.tsx</files>
  <read_first>
    - packages/client/src/components/HexGrid.test.tsx (existing scenario builders: shootingModeStateWithShooterAt, baseStateWithDefender, mock state seeding; and the post-33-04 SAFE_FILL/goalFill constants)
    - packages/client/src/components/HexGrid.tsx (post-Task-2: how gk-kick-target/pass-target/tackle-risk/ring/BallLocationRing render)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (B1/B2/B4 final fills: gk-kick rgba(56,189,248,0.30), pass-target rgba(34,197,94,0.4), tackle-risk rgba(255,140,0,0.55), ring gold #f5c518, ball marker white #ffffff)
  </read_first>
  <behavior>
    - During a GK_KICK_TARGET scenario, a valid kick hex renders a polygon with fill rgba(56,189,248,0.30).
    - During a pass scenario, a safe pass target renders fill rgba(34,197,94,0.4); an interception-risk target renders fill rgba(255,140,0,0.55).
    - A confirmed pass target renders a gold #f5c518 ring polygon (fill none) and NO green pass-target fill on that hex.
    - The KICK_OFF_SETUP centre hex renders a gold #f5c518 required-ring polygon.
    - During HEADER, a white (#ffffff) ball-location marker polygon is present at the ball hex, and the old gold (#f5c518, fillOpacity 0.5) HEADER ball-overlay is absent; during a standard phase (e.g. MOVE) the white marker is absent.
  </behavior>
  <action>
    Add tests covering each behavior above, reusing the existing state-builder helpers and seeding the relevant phase/sets (gkKickTargetSet, valid pass targets, passTargetHex, KICK_OFF_SETUP centre hex, HEADER with a ball position). Assert fills/strokes by the exact values in the behavior block. Include one regression test that during HEADER there is no polygon with fill `#f5c518` at fillOpacity 0.5 over the ball hex (the deleted overlay) but there IS a `stroke="#ffffff"` fill="none" polygon there (the new marker). Keep the SAFE_FILL/goalFill constants from 33-04 intact.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- HexGrid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HexGrid.test.tsx has assertions for gk-kick-target, pass-target, tackle-risk fills, the required and confirmed rings, and the white ball marker's presence (response phase) and absence (standard phase).
    - A test asserts the HEADER gold ball-overlay is gone and the white marker replaces it.
    - `pnpm --filter @counter-attack/client test -- HexGrid.test.tsx` passes.
  </acceptance_criteria>
  <done>HexGrid tests cover the consolidated tints, rings, and the always-on-top ball marker.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                              |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | Ball position and phase come from server-authoritative `GameState`; the marker is pure client SVG paint. No user input reaches any color/geometry value. |

## STRIDE Threat Register

| Threat ID | Category          | Component                                    | Disposition | Mitigation Plan                                                                              |
| --------- | ----------------- | -------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------- |
| T-33-09   | Tampering         | HexGrid highlight routing + BallLocationRing | accept      | Fixed SVG paint from a lookup table / typed GamePhase gate; no injection surface.            |
| T-33-10   | Denial of Service | Extra topmost SVG sibling per render         | accept      | One additional `<polygon>` at most; negligible render cost within the existing ~962-hex SVG. |
| T-33-SC   | Tampering         | npm/pip/cargo installs                       | mitigate    | No package installs this phase — nothing to verify.                                          |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test -- HexGrid.test.tsx BallLocationRing.test.tsx` green.
- `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(|hexTackleRisk' packages/client/src/components/HexGrid.tsx` returns zero color-literal hits.
- `.hexTackleRisk` absent from HexGrid.module.css.
</verification>

<success_criteria>

- Zero inline highlight-color literals remain in HexGrid.tsx; all tints/rings flow through HexCell props.
- BallLocationRing renders always-on-top during the 10 response phases; the HEADER gold overlay is deleted.
- HexGrid tests cover the new tints, rings, and ball marker.
  </success_criteria>

<output>
Create `.planning/phases/33-design-tokens-highlight-standardization/33-06-SUMMARY.md` when done.
</output>
