---
phase: 33-design-tokens-highlight-standardization
plan: 03
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - packages/client/src/components/LobbyScreen.module.css
  - packages/client/src/components/GameSettingsScreen.module.css
  - packages/client/src/components/TeamSelectionScreen.module.css
  - packages/client/src/components/UniformSelectionScreen.module.css
  - packages/client/src/components/LineupAssignmentScreen.module.css
autonomous: true
requirements: [THEME-03]
must_haves:
  truths:
    - 'Every lobby/settings/team/uniform/lineup screen CSS Module sources chrome colors via var(--token) with no raw hex/rgba literals'
    - 'The lineup tier-badge purple and success-green resolve via var(--color-accent-purple)/var(--color-success)'
  artifacts:
    - path: 'packages/client/src/components/UniformSelectionScreen.module.css'
      provides: 'Token-driven chrome for the largest screen module (65 literals migrated)'
      contains: 'var(--color-'
    - path: 'packages/client/src/components/LineupAssignmentScreen.module.css'
      provides: 'Token-driven chrome incl. tier badge colors'
      contains: 'var(--color-accent-purple)'
  key_links:
    - from: 'packages/client/src/components/LineupAssignmentScreen.module.css'
      to: 'packages/client/src/styles/tokens.css'
      via: 'var(--color-accent-purple)/var(--color-success)'
      pattern: "var\\(--color-(accent-purple|success)"
---

<objective>
Migrate all lobby, settings, and selection/draft screen CSS Modules to the token layer from 33-01 (THEME-03, D-06). These five modules hold the largest literal density in the app (UniformSelection 65, LineupAssignment 49, GameSettings 40, TeamSelection 38, Lobby 19). No `--team-accent` runtime injection is needed here — these screens do not thread a per-view team accent (only GameBoard does); this plan is a pure chrome-literal-to-token sweep.

Purpose: Remove hardcoded chrome literals from the pre-game screens so Phase 34 is a pure token-value swap.
Output: All five screen CSS Modules resolve chrome colors via `var(--token)`.
</objective>

<artifacts_this_phase_produces>
Symbols/files created by THIS plan: none new — literal->var() substitution only, referencing tokens defined in 33-01.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md
@.planning/phases/33-design-tokens-highlight-standardization/33-01-SUMMARY.md
@packages/client/src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate LobbyScreen + GameSettingsScreen CSS Modules to tokens</name>
  <files>packages/client/src/components/LobbyScreen.module.css, packages/client/src/components/GameSettingsScreen.module.css</files>
  <read_first>
    - packages/client/src/styles/tokens.css (token names/values)
    - packages/client/src/components/GameSettingsScreen.module.css (incl. the muted-border literal #a0a0a0 at ~59)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (section A token table)
  </read_first>
  <action>
    Replace every chrome color literal in both modules with the matching `var(--token)`: page background `#1a1a2e`->`var(--color-bg-page)`, surfaces `#16213e`->`var(--color-bg-surface)`, secondary fills `#0f3460`->`var(--color-bg-surface-alt)`, default borders `#0f3460`->`var(--color-border)`, muted/lighter borders `#a0a0a0`->`var(--color-border-muted)`, text greys to `var(--color-text-primary)`/`var(--color-text-secondary)`/`var(--color-text-tertiary)`, white text `#ffffff`->`var(--color-text-inverse)`, gold `#f5c518`->`var(--color-accent-gold)`, danger `#ef4444`->`var(--color-danger)`, CTA ready/pending greens/ambers to their `--color-cta-*` tokens. Color values only — no layout/spacing changes.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- LobbyScreen GameSettingsScreen</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' packages/client/src/components/LobbyScreen.module.css packages/client/src/components/GameSettingsScreen.module.css` returns zero non-comment hits.
    - Scoped tests pass (or, if no test file exists for a module, `pnpm --filter @counter-attack/client build` succeeds).
  </acceptance_criteria>
  <done>Lobby and GameSettings CSS Modules resolve chrome via tokens with zero literals.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate TeamSelectionScreen + UniformSelectionScreen CSS Modules to tokens</name>
  <files>packages/client/src/components/TeamSelectionScreen.module.css, packages/client/src/components/UniformSelectionScreen.module.css</files>
  <read_first>
    - packages/client/src/styles/tokens.css (token names/values)
    - packages/client/src/components/TeamSelectionScreen.module.css (muted-border literal #a0a0a0 at ~169)
    - packages/client/src/components/UniformSelectionScreen.module.css (largest literal set — 65 occurrences)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (section A token table + note that jersey-preview .tsx palette literals are out of scope)
  </read_first>
  <action>
    Replace every chrome color literal in both CSS Modules with the matching `var(--token)` using the same mapping as Task 1. UniformSelectionScreen has the highest density — work through it systematically, canonicalizing each literal (hex or rgba) before mapping so duplicates collapse to one token. Do NOT touch `UniformSelectionScreen.tsx` — its `#555`/`#888`/`#fff` jersey-preview palette and `tileRenderPalette.*` values are team-identity/jersey-preview rendering (the same category as the excluded UNIFORM_STYLES system), not chrome. Color values only.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- TeamSelectionScreen UniformSelectionScreen</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' packages/client/src/components/TeamSelectionScreen.module.css packages/client/src/components/UniformSelectionScreen.module.css` returns zero non-comment hits.
    - `UniformSelectionScreen.tsx` is unchanged (jersey-preview literals intentionally retained).
    - Scoped tests pass (or build succeeds if no test file).
  </acceptance_criteria>
  <done>TeamSelection and UniformSelection CSS Modules resolve chrome via tokens; jersey-preview rendering untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate LineupAssignmentScreen CSS Module incl. tier badge colors</name>
  <files>packages/client/src/components/LineupAssignmentScreen.module.css</files>
  <read_first>
    - packages/client/src/styles/tokens.css (token names/values, incl. --color-accent-purple #a855f7 and --color-success #22c55e)
    - packages/client/src/components/LineupAssignmentScreen.module.css (tier-badge purple #a855f7 at ~329, tier-border green at ~341)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (section A: --color-accent-purple and --color-success mappings)
  </read_first>
  <action>
    Replace every chrome color literal in `LineupAssignmentScreen.module.css` with the matching `var(--token)` using the Task 1 mapping. Map the tier-badge purple `#a855f7`->`var(--color-accent-purple)` (line ~329) and the tier-border green `#22c55e`->`var(--color-success)` (line ~341). Color values only — no layout/spacing changes.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- LineupAssignmentScreen</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' packages/client/src/components/LineupAssignmentScreen.module.css` returns zero non-comment hits.
    - The module references `var(--color-accent-purple)` and `var(--color-success)` for the tier badge/border rules.
    - Scoped test passes (or build succeeds if no test file).
  </acceptance_criteria>
  <done>LineupAssignment CSS Module resolves chrome via tokens including tier badge colors.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                              |
| ---------- | ------------------------------------------------------------------------ |
| (none new) | Literal->var() substitution only; no user input reaches any color value. |

## STRIDE Threat Register

| Threat ID | Category               | Component                 | Disposition | Mitigation Plan                                                |
| --------- | ---------------------- | ------------------------- | ----------- | -------------------------------------------------------------- |
| T-33-05   | Tampering              | Screen chrome CSS Modules | accept      | Developer-defined token references only; no injection surface. |
| T-33-06   | Information Disclosure | Screen chrome colors      | accept      | Pure presentation; no secrets/PII.                             |
| T-33-SC   | Tampering              | npm/pip/cargo installs    | mitigate    | No package installs this phase — nothing to verify.            |

</threat_model>

<verification>
- Scoped screen tests green (or build succeeds where no test file exists).
- `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' <all five screen *.module.css>` returns zero non-comment hits.
</verification>

<success_criteria>

- All five lobby/settings/selection screen CSS Modules resolve chrome colors via tokens with zero remaining literals.
  </success_criteria>

<output>
Create `.planning/phases/33-design-tokens-highlight-standardization/33-03-SUMMARY.md` when done.
</output>
