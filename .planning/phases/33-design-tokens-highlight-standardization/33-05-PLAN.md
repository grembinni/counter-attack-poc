---
phase: 33-design-tokens-highlight-standardization
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/PieceOverlay.tsx
  - packages/client/src/components/PieceOverlay.test.tsx
autonomous: true
requirements: [HILITE-03]
must_haves:
  truths:
    - "The 'already-moved-this-stage' piece marker renders as a dark-grey ring plus a light-grey semi-transparent overlay circle (a spent/used-up look), not a green ring"
    - "The 'selected/active' piece ring stays green (#22c55e) and is visually distinct from the moved-this-stage grey marker"
    - 'The moved-this-stage marker still stacks independently with selectable/active/activated/offside states'
  artifacts:
    - path: 'packages/client/src/components/PieceOverlay.tsx'
      provides: 'Grey moved-this-stage ring + overlay treatment (D-05)'
      contains: '#6b7280'
    - path: 'packages/client/src/components/PieceOverlay.test.tsx'
      provides: 'Tests asserting grey moved-this-stage marker distinct from green active ring'
      contains: '#6b7280'
  key_links:
    - from: 'packages/client/src/components/PieceOverlay.tsx'
      to: 'isMovedThisStage overlay + ring'
      via: 'independent boolean-driven layer'
      pattern: 'isMovedThisStage'
---

<objective>
Make the "already-moved-this-stage" piece marker visually distinct from the "selected/active" ring (HILITE-03, D-04/D-05). Today both use identical green `#22c55e`, differing only by radius. This plan changes the `isMovedThisStage` layer in `PieceOverlay.tsx` to a dark-grey ring plus a light-grey semi-transparent overlay circle (a "dimmed/spent" look) while keeping the active ring green, and migrates `PieceOverlay.test.tsx` to assert the distinction.

Purpose: A used-up piece reads as greyed-out, never confusable with the bright-green active selection.
Output: Grey moved-this-stage treatment in PieceOverlay; migrated PieceOverlay tests proving active-green ≠ moved-grey and correct stacking.
</objective>

<artifacts_this_phase_produces>
Symbols/files created by THIS plan: none new — this plan modifies the existing `isMovedThisStage` rendering block and its tests. No new props, exports, or files (the `isMovedThisStage` prop already exists).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md
@.planning/phases/33-design-tokens-highlight-standardization/33-CONTEXT.md
@.planning/phases/33-design-tokens-highlight-standardization/33-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the green moved-this-stage ring with the grey ring + overlay treatment</name>
  <files>packages/client/src/components/PieceOverlay.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx (the isMovedThisStage block at lines 224-240, currently stroke #22c55e; the active-ring block at 174-185; PIECE_RADIUS=12 at line 115; the number-text render at 256-270 which must stay on top)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (section B3, isMovedThisStage row — the authoritative final grey values)
  </read_first>
  <behavior>
    - isMovedThisStage=true renders a dark-grey ring: a `<circle>` with fill="none", stroke="#6b7280", strokeWidth 2.5, r = PIECE_RADIUS+8 (20).
    - isMovedThisStage=true ALSO renders a light-grey overlay circle: a `<circle>` with fill="#9ca3af", fillOpacity 0.35, r = PIECE_RADIUS (12), drawn on top of the piece body but below the player-number text.
    - The active ring (selectionState='active') stays green #22c55e (D-04, unchanged).
    - The grey marker still renders independently of selectionState/isOffside (it can stack).
  </behavior>
  <action>
    In the `isMovedThisStage` conditional block, replace the single green ring with two sibling `<circle>` elements (D-05): first a light-grey overlay circle `fill="#9ca3af" fillOpacity={0.35} r={PIECE_RADIUS}` with `pointerEvents="none"` (the "dimmed/used-up" fill over the piece body), then the dark-grey ring `fill="none" stroke="#6b7280" strokeWidth={2.5} r={PIECE_RADIUS + 8}` with `pointerEvents="none"`. Keep the block in its current position (before the ball-carrier dot and player-number text) so the number stays legible on top of the overlay. Do NOT change the `selectionState==='active'` green ring (D-04). Update the block's doc comment to describe the grey spent/used-up treatment and that it is intentionally distinct from the green active ring (HILITE-03). If the grey overlay proves visually indistinguishable from the `activated` orange state during the human-verify pass, the documented fallback (CONTEXT.md) is to keep the orange-circle-plus-red-X with a minor tone tweak — but implement the grey values above by default and record any deviation.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- PieceOverlay.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The isMovedThisStage block renders a `#6b7280` ring (fill none, strokeWidth 2.5, r 20) AND a `#9ca3af` overlay circle (fillOpacity 0.35, r 12).
    - No `#22c55e` appears in the isMovedThisStage block (green is only in the active-ring block).
    - `grep -c "#6b7280\|#9ca3af" packages/client/src/components/PieceOverlay.tsx` returns at least 2.
    - `pnpm --filter @counter-attack/client build` succeeds.
  </acceptance_criteria>
  <done>The moved-this-stage marker is a grey ring + grey overlay, distinct from the green active ring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate PieceOverlay.test.tsx moved-this-stage assertions to the grey values</name>
  <files>packages/client/src/components/PieceOverlay.test.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.test.tsx (the "D-55: green moved this stage ring" describe block, lines 303-347 — every assertion currently expects #22c55e)
    - packages/client/src/components/PieceOverlay.tsx (post-Task-1 grey ring + overlay values)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (section B3 final values)
  </read_first>
  <behavior>
    - isMovedThisStage=true renders exactly one grey ring (stroke #6b7280, strokeWidth 2.5, r 20, fill none) and one grey overlay circle (fill #9ca3af, fillOpacity 0.35, r 12).
    - isMovedThisStage=false renders neither the grey ring nor the grey overlay.
    - isMovedThisStage + selectionState='active' renders BOTH the green active ring (#22c55e, exactly one) AND the grey ring (#6b7280) — proving the two are now distinct colors (HILITE-03).
    - isMovedThisStage + isOffside renders the grey ring AND the red offside ring; isMovedThisStage + selectionState='activated' renders the orange ring/X AND the grey ring.
  </behavior>
  <action>
    Rewrite the moved-this-stage describe block so all assertions target the grey values instead of `#22c55e`: assert a single `stroke="#6b7280"` ring (fill none, strokeWidth 2.5, r 20) and a single `fill="#9ca3af"` overlay circle (fillOpacity 0.35, r 12). Update the "together with selectionState='active'" test so it now asserts exactly one green ring (`#22c55e`, the active ring) AND exactly one grey ring (`#6b7280`) at distinct radii — this is the direct HILITE-03 proof that the two markers are no longer the same color. Update the "together with isOffside" and "together with selectionState='activated'" stacking tests to assert the grey ring coexists with the red/orange rings. Update the "isMovedThisStage=false renders no ring" test to assert neither `#6b7280` nor `#9ca3af` is present. Leave the unrelated jersey/team-identity absent-literal tests (`#1a56b0`/`#c0392b`/`#9b59b6`/`#db2777`) unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- PieceOverlay.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No test asserts `moved-this-stage === '#22c55e'`.
    - A test asserts the active ring (#22c55e) and the moved-this-stage ring (#6b7280) are both present and are different colors when a piece is both active and moved-this-stage.
    - `pnpm --filter @counter-attack/client test -- PieceOverlay.test.tsx` passes.
  </acceptance_criteria>
  <done>PieceOverlay tests assert the grey moved-this-stage marker, its distinctness from green active, and correct stacking.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                           |
| ---------- | --------------------------------------------------------------------------------------------------------------------- |
| (none new) | Piece-ring colors are developer literals rendered from client-derived boolean state; no user input reaches SVG paint. |

## STRIDE Threat Register

| Threat ID | Category  | Component                           | Disposition | Mitigation Plan                                     |
| --------- | --------- | ----------------------------------- | ----------- | --------------------------------------------------- |
| T-33-08   | Tampering | PieceOverlay ring/overlay rendering | accept      | Fixed SVG paint values; no injection surface.       |
| T-33-SC   | Tampering | npm/pip/cargo installs              | mitigate    | No package installs this phase — nothing to verify. |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test -- PieceOverlay.test.tsx` green.
- `pnpm --filter @counter-attack/client build` succeeds.
</verification>

<success_criteria>

- Moved-this-stage marker is grey (ring + overlay); active ring stays green; tests prove the distinction and stacking.
  </success_criteria>

<output>
Create `.planning/phases/33-design-tokens-highlight-standardization/33-05-SUMMARY.md` when done.
</output>
