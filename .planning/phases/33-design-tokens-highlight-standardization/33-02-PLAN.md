---
phase: 33-design-tokens-highlight-standardization
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - packages/client/src/components/ActionPanel.module.css
  - packages/client/src/components/ActionLog.module.css
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/FreeKickSetupPanel.module.css
  - packages/client/src/components/FreeKickSetupPanel.tsx
  - packages/client/src/components/KickOffSetupPanel.module.css
  - packages/client/src/components/KickOffSetupPanel.tsx
  - packages/client/src/components/PlayerStatsPanel.module.css
  - packages/client/src/components/ReplayPanel.module.css
  - packages/client/src/components/EventBanner.module.css
  - packages/client/src/components/DisconnectBanner.module.css
autonomous: true
requirements: [THEME-03]
must_haves:
  truths:
    - 'Every in-game panel/log/banner CSS Module sources its chrome colors via var(--token) with no raw hex/rgba literals'
    - 'Inline chrome-color literals in the panel .tsx files (#ef4444, #a0a0a0) resolve via var(--token)'
    - "ActionLog's team-prefix fallback color comes from teamAccentColor(undefined), not a hardcoded #888888"
  artifacts:
    - path: 'packages/client/src/components/ActionPanel.module.css'
      provides: 'CTA button token references (ready/pending/danger)'
      contains: 'var(--color-cta'
    - path: 'packages/client/src/components/ActionLog.tsx'
      provides: 'Team-prefix fallback via teamAccentColor(undefined)'
      contains: 'teamAccentColor'
  key_links:
    - from: 'packages/client/src/components/ActionPanel.module.css'
      to: 'packages/client/src/styles/tokens.css'
      via: 'var(--color-cta-ready-bg) etc.'
      pattern: "var\\(--color-cta"
---

<objective>
Migrate all in-game panel, log, and banner chrome colors to the token layer created in 33-01 (THEME-03, D-06). Covers the ActionPanel/ActionLog/FreeKick/KickOff/PlayerStats/Replay/EventBanner/DisconnectBanner CSS Modules plus the inline chrome-color literals embedded in their `.tsx` files (Pitfall 5), and fixes the ActionLog `#888888` fallback anti-pattern.

Purpose: Remove hardcoded chrome literals from the in-game panel area so Phase 34 is a pure token-value swap.
Output: Panel/log/banner CSS Modules and their inline `.tsx` chrome literals all resolve via `var(--token)`; ActionLog fallback color routed through the shared `teamAccentColor` helper.
</objective>

<artifacts_this_phase_produces>
Symbols/files created by THIS plan: none new — this plan only substitutes existing literals for `var(--token)` references defined in 33-01's tokens.css. No new files, exports, props, or components.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md
@.planning/phases/33-design-tokens-highlight-standardization/33-RESEARCH.md
@.planning/phases/33-design-tokens-highlight-standardization/33-01-SUMMARY.md
@packages/client/src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ActionPanel/ActionLog/Replay/EventBanner/DisconnectBanner/PlayerStats CSS Modules to tokens</name>
  <files>packages/client/src/components/ActionPanel.module.css, packages/client/src/components/ActionLog.module.css, packages/client/src/components/ReplayPanel.module.css, packages/client/src/components/EventBanner.module.css, packages/client/src/components/DisconnectBanner.module.css, packages/client/src/components/PlayerStatsPanel.module.css</files>
  <read_first>
    - packages/client/src/styles/tokens.css (the authoritative token names/values from 33-01)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (section A token table: which literal maps to which token, incl. the CTA ready/pending pairs #27ae60/#219a52 and #f39c12/#e67e22)
    - packages/client/src/components/ActionPanel.module.css (CTA ready/pending button colors at ~161/166/171/176)
  </read_first>
  <action>
    In each listed CSS Module, replace every chrome color literal with the matching `var(--token)` from tokens.css: surface/panel backgrounds `#16213e`->`var(--color-bg-surface)`, secondary fills `#0f3460`->`var(--color-bg-surface-alt)`, borders `#0f3460`->`var(--color-border)`, text greys to `var(--color-text-primary)` (#e0e0e0) / `var(--color-text-secondary)` (#a0a0a0) / `var(--color-text-tertiary)` (#808090), white text `#ffffff`->`var(--color-text-inverse)`, gold `#f5c518`->`var(--color-accent-gold)`, danger `#ef4444`->`var(--color-danger)`, CTA ready `#27ae60`->`var(--color-cta-ready-bg)` and hover `#219a52`->`var(--color-cta-ready-bg-hover)`, CTA pending `#f39c12`->`var(--color-cta-pending-bg)` and hover `#e67e22`->`var(--color-cta-pending-bg-hover)`. Change color values only — do not touch layout, sizing, or non-color properties. For `PlayerStatsPanel.module.css`, migrate only chrome (panel/text/border) literals; do NOT migrate any SVG jersey/team-identity rendering (those live in PlayerStatsPanel.tsx and are out of scope, see Task 3 note).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- ActionPanel ActionLog ReplayPanel EventBanner</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' packages/client/src/components/ActionPanel.module.css packages/client/src/components/ActionLog.module.css packages/client/src/components/ReplayPanel.module.css packages/client/src/components/EventBanner.module.css packages/client/src/components/DisconnectBanner.module.css packages/client/src/components/PlayerStatsPanel.module.css` returns zero non-comment hits.
    - ActionPanel.module.css CTA rules reference `var(--color-cta-ready-bg)` and `var(--color-cta-pending-bg)`.
    - Scoped tests pass.
  </acceptance_criteria>
  <done>All six listed CSS Modules resolve chrome colors via tokens with zero remaining literals.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate FreeKick + KickOff panel CSS and inline .tsx chrome literals to tokens</name>
  <files>packages/client/src/components/FreeKickSetupPanel.module.css, packages/client/src/components/FreeKickSetupPanel.tsx, packages/client/src/components/KickOffSetupPanel.module.css, packages/client/src/components/KickOffSetupPanel.tsx</files>
  <read_first>
    - packages/client/src/components/FreeKickSetupPanel.tsx (inline chrome literals at ~187/193: style={{ color: '#ef4444' }})
    - packages/client/src/components/KickOffSetupPanel.tsx (inline chrome literals at ~97/105: '#a0a0a0' and '#ef4444')
    - packages/client/src/styles/tokens.css (token names)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md ("Scope boundary (Pitfall 5)" — inline style var() reads need no cast)
  </read_first>
  <action>
    In `FreeKickSetupPanel.module.css` and `KickOffSetupPanel.module.css`, replace all chrome literals with `var(--token)` using the same mapping as Task 1 (CTA ready/pending pairs, surfaces, borders, text, danger). In `FreeKickSetupPanel.tsx` (lines ~187/193) replace `style={{ color: '#ef4444' }}` with `style={{ color: 'var(--color-danger)' }}`. In `KickOffSetupPanel.tsx` (lines ~97/105) replace the `'#a0a0a0'` branch with `'var(--color-text-secondary)'` and the `'#ef4444'` branch with `'var(--color-danger)'`, preserving the existing conditional logic. Inline styles read `var()` directly as a string — no `React.CSSProperties` cast needed for reads.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- FreeKickSetupPanel KickOffSetupPanel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' packages/client/src/components/FreeKickSetupPanel.module.css packages/client/src/components/KickOffSetupPanel.module.css` returns zero non-comment hits.
    - No `'#ef4444'` or `'#a0a0a0'` string literal remains in FreeKickSetupPanel.tsx or KickOffSetupPanel.tsx; those sites use `'var(--color-danger)'` / `'var(--color-text-secondary)'`.
    - Scoped tests pass.
  </acceptance_criteria>
  <done>FreeKick/KickOff panel CSS and inline chrome literals resolve via tokens with the conditional logic intact.</done>
</task>

<task type="auto">
  <name>Task 3: Route ActionLog team-prefix fallback through teamAccentColor(undefined)</name>
  <files>packages/client/src/components/ActionLog.tsx</files>
  <read_first>
    - packages/client/src/components/ActionLog.tsx (lines ~17/44 early returns and ~758/766/774/782/790 prefixColor fallbacks, all hardcoding '#888888')
    - packages/client/src/hooks/useTeamColors.ts (teamAccentColor(undefined) returns '#888888' — the shared fallback source of truth)
    - .planning/phases/33-design-tokens-highlight-standardization/33-RESEARCH.md (Anti-Patterns: don't perpetuate hardcoded #888888)
  </read_first>
  <action>
    Replace every hardcoded `'#888888'` fallback in ActionLog.tsx (lines ~17, ~44, ~758, ~766, ~774, ~782, ~790) with a call to the shared pure helper `teamAccentColor(undefined)` (import it from `../hooks/useTeamColors.js` if not already imported). This is the correct fallback for the per-event, per-team color loops where a hook cannot be called (Rules of Hooks). Do not change the ActionLog rendering structure or any per-event color logic — only replace the literal fallback with the helper call. Note: PlayerStatsPanel.tsx SVG jersey/team-identity fill/stroke literals (e.g. #f59e0b, #7c3aed, #4c1d95) are OUT OF SCOPE — they are team-identity jersey rendering, the same category as the excluded UNIFORM_STYLES/PieceOverlay system; do not migrate them.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- ActionLog</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'#888888'" packages/client/src/components/ActionLog.tsx` returns 0.
    - ActionLog.tsx imports and calls `teamAccentColor(undefined)` for the fallback color.
    - `pnpm --filter @counter-attack/client test -- ActionLog` passes.
  </acceptance_criteria>
  <done>ActionLog fallback color is sourced from the shared teamAccentColor helper; no hardcoded #888888 remains.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                |
| ---------- | ------------------------------------------------------------------------------------------ |
| (none new) | Only literal->var() substitution and a helper call; no user input reaches any color value. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                | Disposition | Mitigation Plan                                                                                        |
| --------- | ---------------------- | ---------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------ |
| T-33-03   | Tampering              | Inline `var()` reads in panel .tsx files | accept      | `var(--token)` string reads reference developer-defined tokens only; no user-controlled interpolation. |
| T-33-04   | Information Disclosure | Panel chrome colors                      | accept      | Pure presentation; no secrets/PII.                                                                     |
| T-33-SC   | Tampering              | npm/pip/cargo installs                   | mitigate    | No package installs this phase — nothing to verify.                                                    |

</threat_model>

<verification>
- Scoped panel/log/banner tests green.
- `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' <all listed *.module.css>` returns zero non-comment hits.
- No `'#ef4444'`, `'#a0a0a0'`, or `'#888888'` string literals remain in the listed `.tsx` files.
</verification>

<success_criteria>

- All in-game panel/log/banner CSS Modules and their inline `.tsx` chrome literals resolve via tokens.
- ActionLog fallback uses `teamAccentColor(undefined)`.
  </success_criteria>

<output>
Create `.planning/phases/33-design-tokens-highlight-standardization/33-02-SUMMARY.md` when done.
</output>
