---
phase: 33-design-tokens-highlight-standardization
plan: 07
type: execute
wave: 3
depends_on: [33-01, 33-02, 33-03, 33-04, 33-05, 33-06]
files_modified:
  - docs/HIGHLIGHT-REFERENCE.md
autonomous: false
requirements: [HILITE-05]
user_setup: []
must_haves:
  truths:
    - 'A single reference document (docs/HIGHLIGHT-REFERENCE.md) documents every highlight/ring color across all three mechanisms (hex tints, piece rings, standalone overlays)'
    - 'The full client test suite is green after all Phase 33 changes'
    - 'The chrome-literal grep sweep confirms Phase 34 will be a pure token-value swap (no stray chrome literals outside tokens.css and the highlight/ring tables)'
    - 'The traffic-light semantics render correctly in the browser and the BUG-23 KICK_OFF_SETUP repro result is recorded'
  artifacts:
    - path: 'docs/HIGHLIGHT-REFERENCE.md'
      provides: 'Single source-of-truth reference for the highlight/ring color system (HILITE-05)'
      contains: 'HexHighlightType'
      min_lines: 40
  key_links:
    - from: 'docs/HIGHLIGHT-REFERENCE.md'
      to: 'packages/client/src/components/HexCell.tsx'
      via: 'documents HIGHLIGHT_STYLES + ring prop values'
      pattern: 'HIGHLIGHT_STYLES'
---

<objective>
Close out Phase 33 by writing the single highlight/ring reference document (HILITE-05, D-03) and running the phase-gate verification: full client suite green, the chrome-literal grep sweep confirming Phase 34 is a pure value swap, a visual traffic-light pass, and the BUG-23 KICK_OFF_SETUP repro check (Pitfall 3). Depends on all prior Phase 33 plans.

Purpose: Future contributors extend the color system from one authoritative doc, and we confirm the refactor is complete and regression-free before the milestone continues.
Output: `docs/HIGHLIGHT-REFERENCE.md`; a green suite; a human-verified visual + BUG-23 sign-off recorded in the SUMMARY.
</objective>

<artifacts_this_phase_produces>
Symbols/files created by THIS plan (exclude from source-drift verification):

- New file: `docs/HIGHLIGHT-REFERENCE.md`
  </artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md
@.planning/phases/33-design-tokens-highlight-standardization/33-RESEARCH.md
@.planning/phases/33-design-tokens-highlight-standardization/33-VALIDATION.md
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/HIGHLIGHT-REFERENCE.md (three-category reference)</name>
  <files>docs/HIGHLIGHT-REFERENCE.md</files>
  <read_first>
    - docs/ARCHITECTURE.md (heading/tone/structure convention for a permanent reference doc in this repo)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (sections B1/B2/B3/B4 — the authoritative final values to transcribe)
    - packages/client/src/components/HexCell.tsx (final HIGHLIGHT_STYLES + ring prop as shipped)
    - packages/client/src/components/PieceOverlay.tsx (final ring colors as shipped)
    - packages/client/src/components/BallLocationRing.tsx (final ball-marker style + gated phases)
  </read_first>
  <action>
    Create `docs/HIGHLIGHT-REFERENCE.md` (sibling to `docs/ARCHITECTURE.md`, matching its heading/tone). Structure it as THREE sub-sections, NOT one flat table (D-03, RESEARCH Pattern 3): (1) Hex tint types — the `HexHighlightType` union / `HIGHLIGHT_STYLES` table in `HexCell.tsx` (all 10 members: safe, risk, goal, kickoff, shot-path, shot-path-action, header-target, gk-kick-target, pass-target, tackle-risk) with each one's semantic (traffic-light legend), fill, stroke, and opacity; (2) Piece + hex ring colors — `PieceOverlay.tsx` (`selectable` #60a5fa, `active` #22c55e, `activated` #f97316, `isOffside` #dc2626, `isMovedThisStage` grey ring #6b7280 + overlay #9ca3af) and `HexCell`'s `ring` prop (`required`/`confirmed` gold #f5c518), noting which are mutually-exclusive vs. independent/stackable; (3) Standalone always-on-top overlays — the `BallLocationRing` white (#ffffff) hex-edge marker and its exact 10-phase visibility gate. Include the traffic-light legend at the top (green=safe, amber=caution, red=offside-only, blue=neutral info, purple=shot-target, white=trajectory/ball, gold=confirmation/required). State that red renders for exactly one meaning app-wide (offside) and that the goal-target is purple. Cross-reference the source files so the doc stays discoverable.
  </action>
  <verify>
    <automated>test -f docs/HIGHLIGHT-REFERENCE.md && grep -q "gk-kick-target" docs/HIGHLIGHT-REFERENCE.md && grep -q "BallLocationRing" docs/HIGHLIGHT-REFERENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/HIGHLIGHT-REFERENCE.md` exists with three distinct sub-sections (hex tints, rings, standalone overlays).
    - It documents all 10 HexHighlightType members, the piece-ring colors (incl. the grey moved-this-stage values), the required/confirmed rings, and the white ball marker with its gated phase list.
    - It states red = offside only and goal-target = purple.
  </acceptance_criteria>
  <done>A single reference doc covers all three highlight/ring mechanisms with final values and the traffic-light legend.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green + emit the chrome-literal grep sweep for review</name>
  <files>docs/HIGHLIGHT-REFERENCE.md</files>
  <read_first>
    - .planning/phases/33-design-tokens-highlight-standardization/33-VALIDATION.md (Manual-Only Verifications: the grep-sweep command + intentional-exclusion list)
    - .planning/phases/33-design-tokens-highlight-standardization/33-UI-SPEC.md (Implementation Notes 2: the exact grep sweep and what may legitimately remain)
  </read_first>
  <action>
    Run the full client test suite and confirm it is green. Then run the chrome-literal grep sweep `grep -rnE "#[0-9a-fA-F]{3,8}|rgba?\(" packages/client/src/components` and capture the output for the checkpoint reviewer. The ONLY hits that may legitimately remain are: `tokens.css` (chrome token definitions), the highlight/ring tables themselves (`HexCell.tsx` HIGHLIGHT_STYLES + ring, `PieceOverlay.tsx` ring colors, `BallLocationRing.tsx` white marker), and the intentionally-excluded team-identity/decoration renderers (`PitchMarkings.tsx`, `GoalNets.tsx`, `BallMarker.tsx`, `TeamBadge.tsx`, the `PlayerStatsPanel.tsx` mini-jersey SVG, the `UniformSelectionScreen.tsx` jersey preview, `uniformStyles.tsx`) and `*.test.tsx`. Record the grep output (grouped by file) in the plan SUMMARY so the human checkpoint can confirm every remaining hit is on the intentional-exclusion list — i.e. Phase 34 will be a pure token-value swap for all chrome.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test` passes (full client suite green).
    - The grep-sweep output is captured in the SUMMARY, grouped by file, with each remaining hit annotated as an intentional exclusion (token file / highlight-ring table / team-identity-decoration renderer / test file).
    - No chrome CSS Module (`*.module.css`) appears in the grep output.
  </acceptance_criteria>
  <done>Full suite green; the grep sweep is captured and shows only intentional-exclusion literals remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual + BUG-23 verification</name>
  <files>none — verification only, no files modified</files>
  <what-built>
    Phase 33 consolidated the highlight/ring color system and added the chrome token layer (values frozen to today's deep-blue theme). The traffic-light semantics are now: green=safe move, amber=risk, red=offside ONLY, blue=neutral zone/GK-kick, purple=shot/goal-target, white ball-location marker, grey "spent" piece marker. This checkpoint confirms the visuals render correctly and that the deferred BUG-23 (stale KICK_OFF_SETUP shot-path shading) did not regress.
  </what-built>
  <action>
    Pause for human verification (autonomous:false). Present the how-to-verify steps below, run the dev server, and await the resume-signal. Do not mark the phase complete until the reviewer approves the visual pass and the BUG-23 repro result is recorded in the SUMMARY.
  </action>
  <how-to-verify>
    1. Start the app (`pnpm --filter @counter-attack/client dev`) and open a two-tab local match.
    2. Confirm the app's overall appearance is UNCHANGED vs. before Phase 33 (deep-blue theme intact — D-06 froze token values; this phase must not alter the look).
    3. Trigger a shot/goal-line highlight and confirm the goal-target hexes render PURPLE (not red).
    4. Trigger an offside situation and confirm the offside ring is the ONLY red element on the board.
    5. Enter a HEADER/kick response phase and confirm a WHITE hex-edge marker outlines the ball's hex (always visible, on top).
    6. In FREE_KICK_SETUP, move a piece and confirm the "already-moved-this-stage" marker reads as GREY/dimmed (distinct from the bright-green active-selection ring).
    7. Review the grep-sweep output from Task 2 and confirm every remaining color literal is on the intentional-exclusion list (tokens.css, highlight/ring tables, team-identity/decoration renderers, tests) — i.e. Phase 34 is a pure token-value swap.
    8. BUG-23 repro (Pitfall 3): score via a SNAPSHOT_DEFLECT flow, let the match reset to KICK_OFF_SETUP, and confirm NO stray shot-path shading persists on the pitch. Record the result either way (fixed / still reproduces) in the SUMMARY.
  </how-to-verify>
  <verify>
    <human-check>Reviewer confirms steps 1-8 above and records the BUG-23 repro outcome.</human-check>
  </verify>
  <resume-signal>Type "approved" to close the phase, or describe any visual/BUG-23 issues to address.</resume-signal>
  <done>Human reviewer has approved the traffic-light visuals, the no-visual-change check, and recorded the BUG-23 repro result.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                       |
| ---------- | --------------------------------------------------------------------------------- |
| (none new) | Documentation + verification only; no runtime code path or user input introduced. |

## STRIDE Threat Register

| Threat ID | Category               | Component                   | Disposition | Mitigation Plan                                     |
| --------- | ---------------------- | --------------------------- | ----------- | --------------------------------------------------- |
| T-33-11   | Information Disclosure | docs/HIGHLIGHT-REFERENCE.md | accept      | Developer-facing color reference; no secrets/PII.   |
| T-33-SC   | Tampering              | npm/pip/cargo installs      | mitigate    | No package installs this phase — nothing to verify. |

</threat_model>

<verification>
- `docs/HIGHLIGHT-REFERENCE.md` exists and covers all three color mechanisms.
- `pnpm --filter @counter-attack/client test` passes (full client suite).
- Grep sweep shows only intentional-exclusion literals remain.
- Human checkpoint approves the visual pass and records the BUG-23 repro result.
</verification>

<success_criteria>

- Single highlight reference doc written (HILITE-05).
- Full suite green; grep sweep confirms Phase 34 is a pure token-value swap.
- Traffic-light visuals verified; BUG-23 repro result recorded.
  </success_criteria>

<output>
Create `.planning/phases/33-design-tokens-highlight-standardization/33-07-SUMMARY.md` when done.
</output>
