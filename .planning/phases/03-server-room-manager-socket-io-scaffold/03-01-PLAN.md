---
phase: 03-server-room-manager-socket-io-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/package.json
  - packages/server/vitest.config.ts
  - packages/shared/src/events.ts
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - 'pnpm install succeeds at repo root with express, socket.io, cors, nanoid runtime deps and tsx, vitest 2.x, @types/express, @types/cors, socket.io-client dev deps present in packages/server'
    - 'packages/server/package.json scripts.test resolves to `vitest run` (no longer the echo placeholder); scripts.dev resolves to `tsx watch src/main.ts`'
    - 'packages/server/vitest.config.ts mirrors the shared package config: include glob src/**/*.test.ts, environment node'
    - 'SocketData interface in packages/shared/src/events.ts carries a sessionToken field (alongside playerSlot and roomCode) so sessionMiddleware.ts in plan 02 can populate socket.data.sessionToken without TypeScript errors'
    - 'pnpm --filter @counter-attack/shared build succeeds; the SocketData extension does not break any existing shared types'
    - 'Vitest version in packages/server matches packages/shared (2.1.9), not the 4.1.7 mentioned in RESEARCH.md (see RESEARCH Open Question A6)'
  artifacts:
    - path: 'packages/server/package.json'
      provides: 'Runtime + dev dependencies for the room manager scaffold; vitest run wired to scripts.test'
      contains: 'express'
    - path: 'packages/server/vitest.config.ts'
      provides: 'Vitest config so Plan 02 can run roomStore unit tests'
      contains: "include: ['src/**/*.test.ts']"
    - path: 'packages/shared/src/events.ts'
      provides: 'SocketData extended with sessionToken field for reconnection identity'
      contains: 'sessionToken'
  key_links:
    - from: 'packages/server/package.json'
      to: 'packages/shared/package.json'
      via: 'vitest version pin (2.1.9)'
      pattern: '"vitest": "2\\.1\\.9"'
    - from: 'packages/shared/src/events.ts'
      to: 'packages/server/src/sessionMiddleware.ts'
      via: 'SocketData.sessionToken field consumed by middleware in Plan 02'
      pattern: 'sessionToken'
---

<objective>
Install the server-side runtime and dev dependencies (express, socket.io, cors, nanoid, tsx, vitest 2.1.9, @types/express, @types/cors, socket.io-client) into packages/server, wire `vitest run` into the test script, create `packages/server/vitest.config.ts` mirroring the shared package config, and extend the shared `SocketData` interface in `packages/shared/src/events.ts` to carry a `sessionToken` field. This unblocks Plan 02 (roomStore + sessionMiddleware) by giving the middleware a typed field to assign to `socket.data.sessionToken` and unblocks Plan 03 by giving the integration tests a working Vitest runner and socket.io-client.

Purpose: Foundation. Plans 02 and 03 cannot run without these installs and the SocketData extension. Doing this in its own plan keeps file ownership clean — no later plan touches package.json, vitest.config.ts, or events.ts again.

Output:

- packages/server/package.json with new deps and updated scripts
- packages/server/vitest.config.ts (new file)
- packages/shared/src/events.ts with extended SocketData interface
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-server-room-manager-socket-io-scaffold/03-RESEARCH.md
@.planning/phases/03-server-room-manager-socket-io-scaffold/03-PATTERNS.md
@.planning/phases/03-server-room-manager-socket-io-scaffold/03-VALIDATION.md

# Existing artifacts whose patterns/contracts must be mirrored or extended:

@packages/shared/package.json
@packages/shared/vitest.config.ts
@packages/shared/src/events.ts
@packages/server/package.json
@packages/server/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install server deps and wire vitest test script</name>
  <files>packages/server/package.json</files>
  <read_first>
    - packages/server/package.json (existing scripts and deps to preserve workspace:* link)
    - packages/shared/package.json (vitest 2.1.9 pin to match)
    - .planning/phases/03-server-room-manager-socket-io-scaffold/03-RESEARCH.md (Standard Stack section for exact versions)
    - .planning/phases/03-server-room-manager-socket-io-scaffold/03-PATTERNS.md (packages/server/package.json section)
  </read_first>
  <action>
    Use the pnpm CLI from the repo root to install dependencies into the server workspace, then update package.json scripts.

    Runtime dependencies (--filter @counter-attack/server, no -D): express, socket.io, cors, nanoid. Pin exact versions from RESEARCH.md Standard Stack: express 4.22.2, socket.io 4.8.3, cors 2.8.6, nanoid 5.1.11.

    Dev dependencies (--filter @counter-attack/server, -D): tsx 4.22.3, vitest 2.1.9 (MUST match packages/shared — do NOT install 4.1.7), @types/express 5.0.6, @types/cors 2.8.19, socket.io-client 4.8.3.

    Preserve the existing workspace:* link to @counter-attack/shared and the existing @types/node devDependency. Do NOT remove the type: module, main, build, typecheck, or clean fields.

    Update scripts:
    - Replace scripts.test (currently the echo placeholder) with `vitest run`.
    - Add scripts.dev with value `tsx watch src/main.ts`.
    - Leave scripts.build (`tsc`), scripts.typecheck (`tsc --noEmit`), and scripts.clean unchanged.

    Run pnpm install from the repo root to update the lockfile. Per D-03 (pnpm monorepo) all new deps land in packages/server/node_modules via pnpm workspaces — never install at the repo root.

    If pnpm install reports peer-dependency warnings for vitest 2.1.9 vs nodejs 24, document them in the SUMMARY but do not "fix" by upgrading vitest — the pin to 2.1.9 is intentional (see RESEARCH.md Open Question A6).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server exec node -e "const p=require('./package.json');const d=Object.assign({},p.dependencies,p.devDependencies);for(const k of ['express','socket.io','cors','nanoid','tsx','vitest','@types/express','@types/cors','socket.io-client','@counter-attack/shared']){if(!d[k]){console.error('MISSING',k);process.exit(1)}}if(p.scripts.test!=='vitest run'){console.error('test script not vitest run, got:',p.scripts.test);process.exit(1)}if(p.scripts.dev!=='tsx watch src/main.ts'){console.error('dev script wrong, got:',p.scripts.dev);process.exit(1)}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - packages/server/package.json `dependencies` object contains keys: express, socket.io, cors, nanoid, @counter-attack/shared (workspace:*)
    - packages/server/package.json `devDependencies` object contains keys: tsx, vitest, @types/express, @types/cors, socket.io-client, @types/node
    - packages/server/package.json `devDependencies.vitest` value is exactly `"2.1.9"` (string match)
    - packages/server/package.json `scripts.test` value is exactly `"vitest run"`
    - packages/server/package.json `scripts.dev` value is exactly `"tsx watch src/main.ts"`
    - packages/server/package.json `scripts.build`, `scripts.typecheck`, `scripts.clean` are unchanged from the pre-task version
    - pnpm-lock.yaml has been updated (git diff shows changes in pnpm-lock.yaml)
    - `pnpm install` exits 0 from the repo root
  </acceptance_criteria>
  <done>
    Server package has the express/socket.io/cors/nanoid runtime stack and tsx/vitest 2.1.9/@types/express/@types/cors/socket.io-client dev stack installed; scripts.test runs vitest; scripts.dev runs tsx watch; workspace:* link to shared preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create vitest.config.ts for packages/server</name>
  <files>packages/server/vitest.config.ts</files>
  <read_first>
    - packages/shared/vitest.config.ts (exact analog — copy verbatim per PATTERNS.md)
    - .planning/phases/03-server-room-manager-socket-io-scaffold/03-PATTERNS.md (vitest.config.ts section, "exact match, copy verbatim")
  </read_first>
  <action>
    Create packages/server/vitest.config.ts as a verbatim copy of packages/shared/vitest.config.ts: a `defineConfig` call from `vitest/config` exporting a default config with `test.include: ['src/**/*.test.ts']` and `test.environment: 'node'`. No other test fields. Use ESM syntax (matches `type: module` in package.json). Do NOT add coverage, reporters, globals, setupFiles, or any other options — Plans 02 and 03 do not need them.

    The src/**/*.test.ts glob deliberately picks up tests in both packages/server/src/__tests__/ subdirectories AND any colocated *.test.ts files (the shared package uses colocated tests; the server package will use __tests__/ subdirs per PATTERNS.md analog mapping).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server exec node -e "const fs=require('fs');const c=fs.readFileSync('vitest.config.ts','utf8');if(!c.includes(\"include: ['src/**/*.test.ts']\"))throw new Error('include missing');if(!c.includes(\"environment: 'node'\"))throw new Error('environment missing');if(!c.includes(\"from 'vitest/config'\"))throw new Error('import missing');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File packages/server/vitest.config.ts exists
    - File contains the literal string `from 'vitest/config'`
    - File contains the literal string `include: ['src/**/*.test.ts']`
    - File contains the literal string `environment: 'node'`
    - File contains `export default defineConfig(`
    - File does NOT contain `coverage`, `reporters`, `globals`, or `setupFiles` keys
  </acceptance_criteria>
  <done>
    Vitest configured for the server package matching the shared package's config exactly; Plans 02 and 03 can run `pnpm --filter @counter-attack/server test`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend SocketData with sessionToken field</name>
  <files>packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/events.ts (current SocketData interface lines 46-50)
    - .planning/phases/03-server-room-manager-socket-io-scaffold/03-PATTERNS.md (sessionMiddleware.ts section, last paragraph — explicitly notes the missing sessionToken field)
    - .planning/phases/03-server-room-manager-socket-io-scaffold/03-RESEARCH.md (Pattern 3: Session Middleware — shows socket.data.sessionToken assignment)
  </read_first>
  <behavior>
    - The SocketData interface continues to export the existing playerSlot and roomCode fields without breaking changes for any current consumer
    - SocketData additionally exports an optional sessionToken field of type `string | undefined`, allowing socket.data.sessionToken to be assigned in the session middleware without TypeScript errors
    - playerSlot and roomCode change from required to optional (matching the pre-connection / pre-room-join socket state): both become `playerSlot?: 1 | 2` and `roomCode?: string` since a brand-new socket connecting via `io.use()` middleware does NOT yet have these set
    - The JSDoc comment on SocketData updates to describe the three fields together
    - Existing ClientEvents, ServerEvents, ClientToServerEvents, ServerToClientEvents, InterServerEvents declarations are unchanged
    - `pnpm --filter @counter-attack/shared build` succeeds after the edit
  </behavior>
  <action>
    Modify the SocketData interface in packages/shared/src/events.ts so all three fields are optional (sessionMiddleware sets them on first connection; brand-new sockets pre-middleware have none of them). Per RESEARCH.md Pattern 3, the middleware sets sessionToken/playerSlot/roomCode only when a reconnecting token is recognised — so all three must be optional in the type.

    Concrete field shape (all three optional):
    - playerSlot: 1 | 2 (optional)
    - roomCode: string (optional)
    - sessionToken: string (optional, new)

    Update the JSDoc to describe the three fields together (player slot, room code, session token). Do not change any other export or interface in this file.

    Note: changing playerSlot and roomCode from required to optional is technically a widening of the public type. The current consumers (`packages/server/src/index.ts` placeholder) do not destructure SocketData, so no caller breaks. Run `pnpm -r build` to confirm.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared build && pnpm --filter @counter-attack/shared exec node -e "const fs=require('fs');const c=fs.readFileSync('src/events.ts','utf8');if(!/sessionToken\?:\s*string/.test(c))throw new Error('sessionToken field missing or not optional');if(!/playerSlot\?:\s*1\s*\|\s*2/.test(c))throw new Error('playerSlot should be optional');if(!/roomCode\?:\s*string/.test(c))throw new Error('roomCode should be optional');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/events.ts SocketData interface declares `sessionToken?: string` as an optional field
    - packages/shared/src/events.ts SocketData declares `playerSlot?: 1 | 2` (optional, widened from required)
    - packages/shared/src/events.ts SocketData declares `roomCode?: string` (optional, widened from required)
    - All other top-level exports in events.ts (ClientEvents, ServerEvents, ClientToServerEvents, ServerToClientEvents, InterServerEvents) are byte-identical to the pre-task version
    - `pnpm --filter @counter-attack/shared build` exits 0
    - `pnpm -r build` exits 0 (whole monorepo still compiles)
  </acceptance_criteria>
  <done>
    SocketData has the sessionToken field and all three fields are optional; Plan 02 sessionMiddleware can assign socket.data.sessionToken without type errors; the shared package builds clean.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                           | Description                                                                                               |
| ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- |
| npm registry → packages/server                                     | Dependency installation; untrusted package supply chain                                                   |
| @counter-attack/shared (types) → @counter-attack/server (consumer) | Internal workspace types consumed by server runtime — trust boundary low but tsconfig strict mode applies |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                                                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------- | --------- | ---------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-03-01   | Tampering | npm install (express, socket.io, cors, nanoid, tsx, vitest, @types/express, @types/cors, socket.io-client) | mitigate    | slopcheck already run in RESEARCH.md Package Legitimacy Audit on 2026-05-29 — all eight packages returned [OK] verdict from npm registry against expected source repos (expressjs/express, socketio/socket.io, expressjs/cors, ai/nanoid, privatenumber/tsx, vitest-dev/vitest, DefinitelyTyped); no [SUS] or [SLOP] packages; install with exact pinned versions (express 4.22.2, socket.io 4.8.3, cors 2.8.6, nanoid 5.1.11, tsx 4.22.3, vitest 2.1.9, @types/express 5.0.6, @types/cors 2.8.19, socket.io-client 4.8.3); pnpm-lock.yaml commit captures the resolved hashes |
| T-03-02   | Tampering | packages/shared/src/events.ts SocketData widening (required → optional)                                    | accept      | Widening playerSlot/roomCode from required to optional is API-relaxing; existing code does not destructure these fields and `pnpm -r build` verification catches any compile break; no runtime behavior change in this plan                                                                                                                                                                                                                                                                                                                                                    |
| T-03-SC   | Tampering | npm install (all 8 packages) — supply-chain reflexive coverage                                             | mitigate    | All packages disposition Approved in RESEARCH.md audit; no [ASSUMED]/[SUS] requiring human checkpoint; pnpm-lock.yaml diff reviewed by developer at commit time                                                                                                                                                                                                                                                                                                                                                                                                                |

</threat_model>

<verification>
- `pnpm install` exits 0 from repo root
- `pnpm --filter @counter-attack/server exec node -e "..."` automated checks in Task 1 verify pinned versions, scripts.test, scripts.dev
- `pnpm --filter @counter-attack/shared build` succeeds (Task 3 automated verify)
- `pnpm -r build` succeeds (Task 3 acceptance criterion)
- packages/server/vitest.config.ts contents match the include glob and environment from Task 2 automated verify
- git diff packages/shared/src/events.ts shows ONLY changes inside the SocketData interface block; no unrelated exports modified (manual confirmation via Task 3 acceptance criterion comparing other exports to pre-task)
</verification>

<success_criteria>

- packages/server/package.json has express, socket.io, cors, nanoid in dependencies and tsx, vitest 2.1.9, @types/express, @types/cors, socket.io-client in devDependencies
- scripts.test === "vitest run" and scripts.dev === "tsx watch src/main.ts"
- packages/server/vitest.config.ts exists with include glob and node environment
- packages/shared/src/events.ts SocketData declares sessionToken?: string and has all three fields optional
- pnpm -r build succeeds
- pnpm-lock.yaml updated and committed
  </success_criteria>

<output>
Create `.planning/phases/03-server-room-manager-socket-io-scaffold/03-01-SUMMARY.md` when done. The summary MUST list:
- Exact installed versions for all 9 packages (compare to RESEARCH.md Standard Stack pins)
- Any pnpm peer-dependency warnings encountered
- A snippet of the new SocketData interface
- Confirmation that `pnpm -r build` exits 0
</output>
