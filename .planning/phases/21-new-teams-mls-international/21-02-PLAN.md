---
phase: 21-new-teams-mls-international
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - packages/client/src/components/TeamSelectionScreen.tsx
  - packages/client/src/components/TeamSelectionScreen.module.css
  - packages/client/src/components/TeamSelectionScreen.test.tsx
autonomous: false
requirements:
  - LEAGUE-01
  - LEAGUE-02
  - TEAM-08
  - TEAM-09
  - TEAM-10
  - TEAM-11
  - INTL-01
  - INTL-02
  - INTL-03
  - INTL-04
  - INTL-05
  - INTL-06
user_setup: []

must_haves:
  truths:
    - 'Team selection screen shows two tabs (MLS, International); MLS is active on mount'
    - "Clicking a tab renders that league's 6 team cards; each card shows its badge"
    - "When home picks a team, away player's view auto-switches to that team's tab and the card is struck out"
    - 'A struck-out card stays struck out on its tab even when the other tab is active'
  artifacts:
    - path: 'packages/client/src/components/TeamSelectionScreen.tsx'
      provides: 'two-tab layout, MLS_TEAMS/INTL_TEAMS arrays, 10 new badge imports, activeLeague state + auto-switch'
      contains: 'activeLeague'
    - path: 'packages/client/src/components/TeamSelectionScreen.module.css'
      provides: '.tabs / .tab / .tabActive classes; 3-column grid'
      contains: '.tabActive'
    - path: 'packages/client/src/components/TeamSelectionScreen.test.tsx'
      provides: 'per-tab card counts, tab-switch, cross-tab struck-out tests'
      contains: 'aria-selected'
  key_links:
    - from: 'packages/client/src/components/TeamSelectionScreen.tsx activeLeague useEffect'
      to: 'homePickedTeam prop + iAmActive'
      via: 'auto-switch guarded by !iAmActive'
      pattern: '!iAmActive'
    - from: 'packages/client/src/components/TeamSelectionScreen.tsx FULL_BADGE_MAP'
      to: 'packages/client/src/assets/badges/*-full.png'
      via: 'static Vite imports'
      pattern: "FullBadge from '../assets/badges/"
    - from: 'packages/client/src/components/TeamSelectionScreen.tsx isStruckOut'
      to: 'homePickedTeam prop'
      via: 'teamId === homePickedTeam (tab-independent)'
      pattern: 'teamId === homePickedTeam'
---

<objective>
Refactor `TeamSelectionScreen` from a flat 2-team grid into a two-tab (MLS / International) layout showing 6 team cards per tab, with 10 new static Vite badge imports, local `activeLeague` tab state, LEAGUE-02 away-player auto-switch, an updated 3-column CSS grid with tab styles, and updated/new component tests.

Purpose: Delivers the user-facing half of the phase goal — 12 teams selectable across two league tabs with real-time cross-player struck-out feedback (LEAGUE-01, LEAGUE-02). Consumes the 12-member `TeamId` and `TEAM_CONFIGS` from Plan 01. No socket/server changes: `TEAM_HOME_PICKED` broadcast and the `homePickedTeam` prop flow are already wired from Phase 16.
Output: two-tab team selection UI, extended CSS module, green client test suite, plus a human-verify checkpoint for badge rendering and tab interaction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-new-teams-mls-international/21-CONTEXT.md
@.planning/phases/21-new-teams-mls-international/21-RESEARCH.md
@.planning/phases/21-new-teams-mls-international/21-PATTERNS.md
@.planning/phases/21-new-teams-mls-international/21-UI-SPEC.md
@.planning/phases/21-new-teams-mls-international/21-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols this plan newly creates (exclude from source-drift acknowledgement):

- `MLS_TEAMS`, `INTL_TEAMS` per-league TeamId arrays in TeamSelectionScreen.tsx
- `activeLeague` local state + `setActiveLeague` + the auto-switch `useEffect`
- `visibleTeams` derived constant
- 10 new badge import bindings: `laFullBadge`, `miamiFullBadge`, `nashvilleFullBadge`, `seattleFullBadge`, `canadaFullBadge`, `englandFullBadge`, `franceFullBadge`, `mexicoFullBadge`, `spainFullBadge`, `usFullBadge`
- CSS classes `.tabs`, `.tab`, `.tabActive` in TeamSelectionScreen.module.css
- New test describe blocks for LEAGUE-01 two-tab layout and LEAGUE-02 cross-tab struck-out behavior
- `TeamId`/`TEAM_CONFIGS` 12-member extensions are produced by Plan 01 (not new here)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor TeamSelectionScreen to two-tab layout with 10 new badge imports</name>
  <files>packages/client/src/components/TeamSelectionScreen.tsx, packages/client/src/components/TeamSelectionScreen.module.css</files>
  <read_first>
    - packages/client/src/components/TeamSelectionScreen.tsx (current full file: cityFullBadge/crewFullBadge imports lines 17-18; ALL_TEAMS line 21; FULL_BADGE_MAP lines 24-27; component body lines 51-147; the ALL_TEAMS.map grid lines 109-145; badge inline width/height 110 at lines 127-140; isStruckOut/isDisabled logic lines 111-112; iAmActive derivation line 62)
    - packages/client/src/components/TeamSelectionScreen.module.css (current .grid lines ~36-42 = 2-col, max-width 480px; .speedOption / .speedOptionActive as the two-state button convention; .card min-width/min-height)
    - .planning/phases/21-new-teams-mls-international/21-UI-SPEC.md (AUTHORITATIVE CSS values — .tabs/.tab/.tabActive exact declarations, grid 3-col max-width 600px, badge 80x80, aria roles, copy strings; UI-SPEC takes precedence over PATTERNS.md if they differ)
    - .planning/phases/21-new-teams-mls-international/21-PATTERNS.md (Steps 1-8 for the .tsx refactor; the CSS change list)
    - packages/client/src/assets/badges/ (confirm all 10 {team}-full.png files present with seattle spelling)
  </read_first>
  <action>
    In TeamSelectionScreen.tsx:

    1. Add `import { useState, useEffect } from 'react';` at the top if not already present. Add 10 static Vite badge imports after the existing cityFullBadge/crewFullBadge lines: `laFullBadge`, `miamiFullBadge`, `nashvilleFullBadge`, `seattleFullBadge`, `canadaFullBadge`, `englandFullBadge`, `franceFullBadge`, `mexicoFullBadge`, `spainFullBadge`, `usFullBadge` — each from `'../assets/badges/{team}-full.png'`. Use the corrected `seattle-full.png` spelling (not `seatle`).

    2. Remove the `ALL_TEAMS` constant and add two per-league arrays with the D-11/D-12 ordering: `MLS_TEAMS: TeamId[] = ['city', 'crew', 'la', 'miami', 'nashville', 'seattle']` and `INTL_TEAMS: TeamId[] = ['canada', 'england', 'france', 'mexico', 'spain', 'us']`.

    3. Extend `FULL_BADGE_MAP` (`Record<TeamId, string>`) to all 12 teams, mapping each new teamId to its imported badge binding.

    4. Inside the component, after the `iAmActive` derivation, add local tab state: `const [activeLeague, setActiveLeague] = useState<'mls' | 'international'>('mls')` (D-13 default MLS, D-14 local state NOT Zustand). Add a `useEffect` on `[homePickedTeam, iAmActive]` that, only when `homePickedTeam !== null && !iAmActive` (Pitfall 5 / D-15 guard — must NOT fire for home player), sets `activeLeague` to `MLS_TEAMS.includes(homePickedTeam) ? 'mls' : 'international'`. Add `const visibleTeams = activeLeague === 'mls' ? MLS_TEAMS : INTL_TEAMS;`.

    5. Add a tab bar in JSX before the grid: a container with `role="tablist"` and two `<button role="tab">` elements ("MLS" and "International"). Each button sets `aria-selected` to whether its league is active, applies `styles.tabActive` when active else `styles.tab`, and calls `setActiveLeague` on click. Use the exact copy "MLS" and "International" (UI-SPEC copy contract).

    6. Change the grid map from `ALL_TEAMS.map(...)` to `visibleTeams.map(...)`. Keep `isStruckOut = teamId === homePickedTeam` unchanged — it MUST check the prop, not the active tab, so a struck-out team stays struck out across tabs (RESEARCH anti-pattern guard). Add `role="tabpanel"` to the grid container. Change badge inline `width`/`height` and the wrapping `<span>` width/height from 110 to 80 (Pitfall 6, UI-SPEC 80x80 focal point). Keep card `alt` text as `` `${TEAM_CONFIGS[teamId].name} badge` ``.

    7. Remove the stale transitional comments referencing "Phase 21 restores the full 4-team grid" (JSDoc line ~4, line ~16, and the ALL_TEAMS comment line ~20).

    In TeamSelectionScreen.module.css:

    8. Update `.grid` from `grid-template-columns: 1fr 1fr` / `max-width: 480px` to `grid-template-columns: 1fr 1fr 1fr` and the max-width the UI-SPEC specifies (UI-SPEC grid section: max-width 600px). Add `.tabs`, `.tab`, `.tab:hover`, and `.tabActive` classes using the exact declarations from the UI-SPEC "New CSS Classes Required" table (flex row tab container with bottom border; `.tab` idle = 14px/400/#a0a0a0 transparent with transparent 2px bottom border and `transition: color 0.15s ease, border-color 0.15s ease`; `.tabActive` = 14px/700/#e0e0e0 with 2px solid #e0e0e0 bottom border). Follow the `.speedOption`/`.speedOptionActive` structural convention. Reduce `.card` min-width to 100px if needed for the 3-column layout (UI-SPEC spacing note).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - TeamSelectionScreen.tsx imports 10 new `*FullBadge` bindings from `'../assets/badges/{team}-full.png'`, including `seattle-full.png` (no `seatle`)
    - `MLS_TEAMS` = ['city','crew','la','miami','nashville','seattle']; `INTL_TEAMS` = ['canada','england','france','mexico','spain','us']; `ALL_TEAMS` no longer exists
    - FULL_BADGE_MAP has 12 entries typed `Record<TeamId, string>`
    - Component contains `useState<'mls' | 'international'>('mls')` and a `useEffect` whose body is guarded by `!iAmActive` (auto-switch does not run for the home player)
    - `isStruckOut = teamId === homePickedTeam` (checks prop, not activeLeague) is preserved
    - Tab bar has `role="tablist"` with two `role="tab"` buttons carrying `aria-selected`; grid container has `role="tabpanel"`
    - Badge `<img>` and wrapping span use width/height 80 (not 110)
    - `.grid` uses `grid-template-columns: 1fr 1fr 1fr`; `.tabs`, `.tab`, `.tabActive` classes exist in the CSS module
    - No "restores the full 4-team grid" comments remain
    - `pnpm --filter @counter-attack/client exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TeamSelectionScreen renders a two-tab layout with 6 cards per tab, correct badges, guarded auto-switch, and updated CSS; client package type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update and extend TeamSelectionScreen.test.tsx for tabs and cross-tab struck-out</name>
  <files>packages/client/src/components/TeamSelectionScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/TeamSelectionScreen.test.tsx (current: DEFAULT_SPEED_PROPS / render harness; card-count assertions toHaveLength(2) at lines ~43, ~61, ~75; struck-out city test lines ~94-111; useGameStore.setState({ playerSlot }) pattern; existing transitional comments)
    - packages/client/src/components/TeamSelectionScreen.tsx (post-Task-1 — tab roles, aria-selected, MLS_TEAMS/INTL_TEAMS membership drive expected counts)
    - .planning/phases/21-new-teams-mls-international/21-PATTERNS.md (Changes 1-5 for the test file, including the LEAGUE-01 and LEAGUE-02 describe blocks and the tab-vs-card button filtering approach)
    - .planning/phases/21-new-teams-mls-international/21-VALIDATION.md (Wave 0: new test cases for tab switching and cross-tab struck-out are required)
  </read_first>
  <action>
    Update existing card-count assertions from `toHaveLength(2)` to `toHaveLength(6)` (default MLS tab now shows 6 cards) at the three sites (~lines 43, 61, 75) and update their test descriptions from "2 team cards (city, crew)" to "6 team cards on the default MLS tab". When counting team cards, exclude tab buttons (`role="tab"`) and speed buttons (`aria-pressed`) — filter `getAllByRole('button')` to those with neither, matching the PATTERNS.md filtering approach.

    Update the struck-out test: home picking 'city' (an MLS team) means the away MLS-tab view shows 1 disabled (city) and 5 enabled cards — change the expectation from 1-disabled/1-enabled to `disabledCards.toHaveLength(1)` and `enabledCards.toHaveLength(5)`.

    Add a new `describe('LEAGUE-01: two-tab layout')` block with tests: (a) MLS tab has `aria-selected="true"` by default on mount; (b) 6 team cards render on the default MLS tab; (c) clicking the International tab (via `userEvent.click(screen.getByRole('tab', { name: /international/i }))`) sets International `aria-selected="true"` and renders the 6 international cards.

    Add a new `describe('LEAGUE-02: cross-tab struck-out behavior')` block with tests: (a) with `playerSlot: 2` (away) and `homePickedTeam="city"`, the MLS tab is auto-active (`aria-selected="true"`) — verifies the away auto-switch; (b) with the same setup, exactly 1 team card is disabled (city struck out) regardless of tab. Use the existing `useGameStore.setState({ playerSlot })` + `render(<TeamSelectionScreen ... {...DEFAULT_SPEED_PROPS} />)` harness. Remove the transitional "Phase 19 state / restores 4" comments from test descriptions.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test run</automated>
  </verify>
  <acceptance_criteria>
    - No `toHaveLength(2)` team-card assertions remain; default-tab card counts assert 6
    - Struck-out test asserts 1 disabled + 5 enabled cards on the MLS tab when home picked 'city'
    - A LEAGUE-01 describe block asserts MLS `aria-selected="true"` by default and International `aria-selected="true"` after a tab click
    - A LEAGUE-02 describe block asserts the away player's MLS tab is auto-active when `homePickedTeam="city"` and exactly 1 card is disabled
    - Team-card counts exclude `role="tab"` and `aria-pressed` (speed) buttons
    - `pnpm --filter @counter-attack/client test run` exits 0 with all TeamSelectionScreen tests green
  </acceptance_criteria>
  <done>Client test suite passes with per-tab card counts and LEAGUE-01/LEAGUE-02 tab-behavior coverage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Two-tab (MLS / International) team selection screen: 12 teams across two tabs, 6 per tab, each with its full-size badge at 80x80; MLS default; away-player auto-switch to the picked team's tab; cross-tab struck-out cards. Server already accepts all 12 team picks (Plan 01).
  </what-built>
  <how-to-verify>
    1. Start the dev environment (server + client) per the project's usual run command (e.g. `pnpm dev` or the documented start scripts).
    2. Open TWO browser windows/tabs to the app; create a room in window A (home), join with the code in window B (away).
    3. On the team selection screen, confirm the MLS tab is active by default and shows 6 cards: City, Crew, LA, Miami, Nashville, Seattle — each with a correctly-rendered badge image (not broken/placeholder), badges sized ~80x80.
    4. Click the International tab; confirm 6 cards render: Canada, England, France, Mexico, Spain, USA — each with a correct badge. Confirm the tab underline/weight changes to indicate the active tab.
    5. In window A (home), pick a team on the International tab (e.g. France). In window B (away), confirm the view auto-switches to the International tab and the France card shows struck-out (dimmed, line-through, not clickable).
    6. In window B, switch to the MLS tab and back to International — confirm France remains struck out on the International tab.
    7. In window B (away), pick a different available team and confirm the flow advances to the next screen (uniform selection is Phase 22 — advancing past team select is sufficient here).
    8. Confirm each newly-picked team's pieces render on the board with the correct team palette colors.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe any badge-rendering, tab-switch, auto-switch, or struck-out issues observed.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                     |
| -------------------- | ------------------------------------------------------------------------------- |
| client render → user | Presentation-only tab state; no untrusted input processed client-side this plan |

## STRIDE Threat Register

| Threat ID | Category  | Component         | Disposition | Mitigation Plan                                                                                                                                                            |
| --------- | --------- | ----------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-21-03   | Tampering | client team pick  | accept      | Client tab/pick UI is presentation only; the authoritative allow-list check lives server-side in VALID_TEAM_IDS (Plan 01, T-21-01). Client cannot bypass server validation |
| T-21-SC   | Tampering | npm/pnpm installs | accept      | No packages installed this plan (RESEARCH Package Legitimacy Audit: zero new packages)                                                                                     |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test run` — TeamSelectionScreen tests green (per-tab counts, tab switch, cross-tab struck-out)
- `pnpm -w tsc --noEmit` — full monorepo type-check clean
- Human-verify checkpoint: badge rendering, tab switching, away auto-switch, cross-tab struck-out in a two-browser session
</verification>

<success_criteria>

- Two tabs (MLS default, International); 6 cards per tab with correct badges at 80x80
- Away player auto-switches to the picked team's tab; struck-out state persists across tab switches
- Struck-out logic keyed on `homePickedTeam` prop, independent of active tab
- Client package type-checks clean; client test suite passes; human-verify checkpoint approved
  </success_criteria>

<output>
Create `.planning/phases/21-new-teams-mls-international/21-02-SUMMARY.md` when done
</output>
