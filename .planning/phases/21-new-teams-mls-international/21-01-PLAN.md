---
phase: 21-new-teams-mls-international
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/teamConfig.ts
  - packages/shared/src/teamConfig.test.ts
  - packages/server/src/roomHandlers.ts
autonomous: true
requirements:
  - TEAM-08
  - TEAM-09
  - TEAM-10
  - TEAM-11
  - INTL-01
  - INTL-02
  - INTL-03
  - INTL-04
  - INTL-05
  - INTL-06
  - LEAGUE-02
user_setup: []

must_haves:
  truths:
    - 'TEAM_CONFIGS has exactly 12 entries (city, crew + 10 new teams)'
    - 'Each of the 10 new teams resolves to exactly 11 players via getSquadPlayers'
    - 'TypeScript compiles clean across shared + server packages with the 12-member TeamId union'
    - 'Server VALID_TEAM_IDS accepts all 12 team IDs; picking any new team is not rejected'
  artifacts:
    - path: 'packages/shared/src/teamConfig.ts'
      provides: '12-member TeamId union + 10 new TEAM_CONFIGS entries'
      contains: "'la' | 'miami'"
    - path: 'packages/server/src/roomHandlers.ts'
      provides: 'VALID_TEAM_IDS allow-list extended to 12 members'
      contains: 'VALID_TEAM_IDS'
    - path: 'packages/shared/src/teamConfig.test.ts'
      provides: 'count assertions updated 2->12 and 4->14; it.each over 12 teams'
      contains: 'toHaveLength(12)'
  key_links:
    - from: 'packages/shared/src/teamConfig.ts TEAM_CONFIGS[id].playerIds'
      to: 'packages/shared/src/teams.ts PLAYER_POOL'
      via: 'getSquadPlayers ID resolution'
      pattern: 'playerIds'
    - from: 'packages/server/src/roomHandlers.ts VALID_TEAM_IDS'
      to: 'packages/shared/src/teamConfig.ts TeamId'
      via: 'readonly TeamId[] type gate'
      pattern: 'VALID_TEAM_IDS'
---

<objective>
Extend the shared data model from 2 selectable teams to 12: widen the `TeamId` union, add 10 new `TEAM_CONFIGS` entries (4 MLS + 6 international) with correct `playerIds` from `PLAYER_POOL`, `league`, `badgeFile`, and per-team `defaultUniformStyle`, extend the server `VALID_TEAM_IDS` allow-list, and update `teamConfig.test.ts` count/coverage assertions.

Purpose: This is the data foundation for the whole phase. TypeScript's `Record<TeamId, TeamConfig>` and `readonly TeamId[]` types act as compilation gates — the client tab UI (Plan 02) cannot reference new teams until this plan lands. Implements TEAM-08..11 and INTL-01..06; LEAGUE-02 struck-out sync needs no server change (the existing `TEAM_HOME_PICKED` broadcast already covers it).
Output: 12-team data model in `packages/shared`, extended server allow-list, green shared test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-new-teams-mls-international/21-CONTEXT.md
@.planning/phases/21-new-teams-mls-international/21-RESEARCH.md
@.planning/phases/21-new-teams-mls-international/21-PATTERNS.md
@.planning/phases/21-new-teams-mls-international/21-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols this phase newly creates or extends (exclude from source-drift acknowledgement):

- `TeamId` union members (new): `'la'`, `'miami'`, `'nashville'`, `'seattle'`, `'canada'`, `'england'`, `'france'`, `'mexico'`, `'spain'`, `'us'`
- `TEAM_CONFIGS` entries (new keys): `la`, `miami`, `nashville`, `seattle`, `canada`, `england`, `france`, `mexico`, `spain`, `us`
- `VALID_TEAM_IDS` extended array (12 members) in `roomHandlers.ts`
- New MLS/INTL badge import symbols and per-league constants (`MLS_TEAMS`, `INTL_TEAMS`, `activeLeague` state, `.tabs`/`.tab`/`.tabActive` CSS classes) — created in Plan 02
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend TeamId union and add 10 new TEAM_CONFIGS entries</name>
  <files>packages/shared/src/teamConfig.ts</files>
  <read_first>
    - packages/shared/src/teamConfig.ts (current state: TeamId = 'city' | 'crew' at line 13; ColorSchemeId already has all 14 IDs at lines 17-31; TeamConfig interface lines 55-71; existing city/crew TEAM_CONFIGS entries as the canonical shape)
    - packages/shared/src/teams.ts (PLAYER_POOL — read to confirm sourceTeamId slugs and p069-p178 ID ranges before hardcoding playerIds)
    - packages/shared/src/uniformStyles.ts (UniformStyleId union — confirm the 12 style IDs used as defaultUniformStyle values exist)
    - .planning/phases/21-new-teams-mls-international/21-PATTERNS.md (the "All 10 new entries to add" table; the city entry canonical pattern; playerIds array spacing format)
  </read_first>
  <action>
    Widen the `TeamId` union (currently `'city' | 'crew'` at line 13) to include all 12 selectable teams in this exact order: `'city' | 'crew' | 'la' | 'miami' | 'nashville' | 'seattle' | 'canada' | 'england' | 'france' | 'mexico' | 'spain' | 'us'`. Do NOT modify `ColorSchemeId` (it already contains all 14 IDs). Update the line-13 JSDoc comment that says "cosmos and xolos removed (retired)" to note the Phase 21 expansion to 12 selectable teams.

    Add 10 new entries to `TEAM_CONFIGS`, each following the `city` entry shape exactly: `{ id, name, colorSchemeId, palette: COLOR_SCHEME_REGISTRY.<id>.palette, playerIds: [...], league, badgeFile, defaultUniformStyle }`. Insert in order — new MLS teams after `crew`, then international teams. Use these exact field values (per CONTEXT.md D-01..D-17 and PATTERNS.md table):

    - miami (TEAM-08): name 'Miami', colorSchemeId 'miami', league 'mls', badgeFile 'miami.png', defaultUniformStyle 'fade' (D-01), playerIds p069-p079 (sourceTeamId inter-miami)
    - la (TEAM-09): name 'LA', colorSchemeId 'la', league 'mls', badgeFile 'la.png', defaultUniformStyle 'checker' (D-02), playerIds p080-p090 (sourceTeamId lafc)
    - nashville (TEAM-10): name 'Nashville', colorSchemeId 'nashville', league 'mls', badgeFile 'nashville.png', defaultUniformStyle 'corners' (D-03), playerIds p091-p101 (sourceTeamId nashville)
    - seattle (TEAM-11): name 'Seattle', colorSchemeId 'seattle', league 'mls', badgeFile 'seattle.png', defaultUniformStyle 'v-stripe' (D-04), playerIds p102-p112 (sourceTeamId seattle)
    - canada (INTL-01): name 'Canada', colorSchemeId 'canada', league 'international', badgeFile 'canada.png', defaultUniformStyle 'cosmos' (D-05), playerIds p146-p156
    - england (INTL-02): name 'England', colorSchemeId 'england', league 'international', badgeFile 'england.png', defaultUniformStyle 'solid' (D-06), playerIds p124-p134
    - france (INTL-03): name 'France', colorSchemeId 'france', league 'international', badgeFile 'france.png', defaultUniformStyle 'quarters' (D-07), playerIds p168-p178
    - mexico (INTL-04): name 'Mexico', colorSchemeId 'mexico', league 'international', badgeFile 'mexico.png', defaultUniformStyle 'tree-rings' (D-08), playerIds p135-p145
    - spain (INTL-05): name 'Spain', colorSchemeId 'spain', league 'international', badgeFile 'spain.png', defaultUniformStyle 'plus' (D-09), playerIds p157-p167
    - us (INTL-06): name 'USA', colorSchemeId 'us', league 'international', badgeFile 'us.png', defaultUniformStyle 'polka-dots' (D-10), playerIds p113-p123 (sourceTeamId usmnt)

    Each `playerIds` array contains exactly 11 sequential IDs (e.g. miami: 'p069' through 'p079'). CRITICAL per D-17 / Pitfall 2: three teams have a TeamId that differs from their PLAYER_POOL sourceTeamId slug — miami→inter-miami, la→lafc, us→usmnt. Before writing each array, confirm the ID range against PLAYER_POOL in teams.ts by matching on sourceTeamId. Do NOT embed inline player objects (DATA-02 forbids it) — reference IDs only. Do NOT compute palette values; use `COLOR_SCHEME_REGISTRY.<id>.palette` (all 10 registry entries already exist). Follow the city entry's playerIds spacing style (5 IDs on first line, 6 on second).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - teamConfig.ts TeamId union contains all 12 members: `'la' | 'miami' | 'nashville' | 'seattle' | 'canada' | 'england' | 'france' | 'mexico' | 'spain' | 'us'` appended after `'city' | 'crew'`
    - `Object.keys(TEAM_CONFIGS).length === 12` (10 new entries added)
    - Each new entry uses `palette: COLOR_SCHEME_REGISTRY.<id>.palette` (no inline hex literals in the new entries)
    - Each new entry's playerIds array has exactly 11 string IDs; miami=p069-p079, la=p080-p090, nashville=p091-p101, seattle=p102-p112, us=p113-p123, england=p124-p134, mexico=p135-p145, canada=p146-p156, spain=p157-p167, france=p168-p178
    - No inline PoolPlayer objects appear in any TEAM_CONFIGS entry (playerIds are string references only)
    - `pnpm --filter @counter-attack/shared exec tsc --noEmit` exits 0 (Record<TeamId, TeamConfig> completeness gate satisfied)
  </acceptance_criteria>
  <done>TeamId is a 12-member union and TEAM_CONFIGS has 12 entries; shared package type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update teamConfig.test.ts count and coverage assertions</name>
  <files>packages/shared/src/teamConfig.test.ts</files>
  <read_first>
    - packages/shared/src/teamConfig.test.ts (current: COLOR_SCHEME_IDS array line 10 has 4 IDs; toHaveLength(4) line 13; TEAM_IDS array line 62 has 2 IDs; toHaveLength(2) line 65; getSquadPlayers explicit tests lines 118-124; PoolPlayer field test lines 126-141)
    - packages/shared/src/teamConfig.ts (post-Task-1 state — the 12 TeamId members and 14 ColorSchemeId members must match the arrays being written here)
    - .planning/phases/21-new-teams-mls-international/21-PATTERNS.md (the teamConfig.test.ts change list, Changes 1-6)
  </read_first>
  <action>
    Update `COLOR_SCHEME_IDS` (line 10) to the 14 IDs: `['cosmos', 'xolos', 'city', 'crew', 'la', 'miami', 'nashville', 'seattle', 'canada', 'england', 'france', 'mexico', 'spain', 'us']`. Update the registry count assertion (line 13) from `toHaveLength(4)` to `toHaveLength(14)`, and update the describe/it text from "all 4 historical team palettes" / "has exactly 4 entries" to reflect 14. Update the "has entries for cosmos, xolos, city, crew" it-title/body to assert all 14 IDs are present (it uses `arrayContaining(COLOR_SCHEME_IDS)` — the array update alone covers it; update the human-readable title).

    Update `TEAM_IDS` (line 62) to the 12 members: `['city', 'crew', 'la', 'miami', 'nashville', 'seattle', 'canada', 'england', 'france', 'mexico', 'spain', 'us']`. Update the TEAM_CONFIGS count assertion (line 65) from `toHaveLength(2)` to `toHaveLength(12)` and its describe/it text from "only active teams city and crew" / "has exactly 2 entries" to reflect 12 teams.

    The `%s.league === "mls"` per-team assertion (line 81) will now fail for the 6 international teams — change it so league is asserted against the config's own declared value rather than a hardcoded 'mls'. Assert `TEAM_CONFIGS[teamId].league` is one of `'mls' | 'international'` (e.g. `expect(['mls','international']).toContain(TEAM_CONFIGS[teamId].league)`), preserving the LEAGUE-03/D-14 intent. Keep the "does NOT contain cosmos or xolos" test unchanged (still valid). Keep all other per-team `it.each` assertions (id, palette deep-equal, colorSchemeId, badgeFile, 4 palette fields, no primaryColor/secondaryColor) — they now run over all 12 teams automatically.

    Replace the two explicit `getSquadPlayers('city')` / `getSquadPlayers('crew')` length tests (lines 118-124) with a single `it.each(TEAM_IDS)('getSquadPlayers(%s) returns exactly 11 players', (teamId) => expect(getSquadPlayers(teamId)).toHaveLength(11))`. Keep the "each returned player has all required PoolPlayer fields" test (lines 126-141) and extend its coverage to include one international team (e.g. add a `getSquadPlayers('canada')` field check alongside the existing city check) so the international ID-resolution path is exercised.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test run</automated>
  </verify>
  <acceptance_criteria>
    - teamConfig.test.ts COLOR_SCHEME_IDS array has 14 entries; TEAM_IDS array has 12 entries
    - `expect(Object.keys(COLOR_SCHEME_REGISTRY)).toHaveLength(14)` present; `expect(Object.keys(TEAM_CONFIGS)).toHaveLength(12)` present
    - No remaining assertion hardcodes `.league` to `'mls'` for every team; league assertion accepts both 'mls' and 'international'
    - An `it.each` over all 12 TEAM_IDS asserts `getSquadPlayers` returns length 11
    - PoolPlayer-field test covers at least one international team (e.g. canada) in addition to city
    - `pnpm --filter @counter-attack/shared test run` exits 0 with all teamConfig tests green
  </acceptance_criteria>
  <done>Shared test suite passes with 12-team / 14-scheme assertions and all 12 squads resolving to 11 players.</done>
</task>

<task type="auto">
  <name>Task 3: Extend server VALID_TEAM_IDS allow-list to 12 members</name>
  <files>packages/server/src/roomHandlers.ts</files>
  <read_first>
    - packages/server/src/roomHandlers.ts (current: VALID_TEAM_IDS at line 40 = ['city', 'crew'] as const; the teamId === room.homePickedTeam struck-out guard around line 204 — read to confirm no change is needed there)
    - packages/shared/src/teamConfig.ts (post-Task-1 TeamId union — VALID_TEAM_IDS values must all be members of it)
    - .planning/phases/21-new-teams-mls-international/21-RESEARCH.md (Pattern 5, Security Domain V5 note — this allow-list is the only security-relevant change)
  </read_first>
  <action>
    Replace the `VALID_TEAM_IDS` constant (line 40, currently `['city', 'crew'] as const`) with the full 12-member allow-list, keeping the `readonly TeamId[]` type and `as const` assertion: `['city', 'crew', 'la', 'miami', 'nashville', 'seattle', 'canada', 'england', 'france', 'mexico', 'spain', 'us'] as const`. Order matches the TeamId union. Make NO other changes to roomHandlers.ts — the existing input-validation guard (V5) and the `teamId === room.homePickedTeam` struck-out guard (~line 204) already work for all 12 teams and drive LEAGUE-02 real-time sync via the existing `TEAM_HOME_PICKED` broadcast. The `readonly TeamId[]` type is a build-time correctness gate: any non-TeamId string fails compilation.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - roomHandlers.ts VALID_TEAM_IDS array contains all 12 team IDs in TeamId order, typed `readonly TeamId[]` with `as const`
    - No other lines in roomHandlers.ts are modified (struck-out guard and validation guard unchanged)
    - `pnpm --filter @counter-attack/server exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Server accepts all 12 team IDs; server package type-checks clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                    |
| ------------------------------------- | -------------------------------------------------------------- |
| client → server (Socket.io TEAM_PICK) | Client-supplied `teamId` string crosses into server room state |

## STRIDE Threat Register

| Threat ID | Category  | Component                         | Disposition | Mitigation Plan                                                                                                                                                   |
| --------- | --------- | --------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-21-01   | Tampering | roomHandlers.ts TEAM_PICK handler | mitigate    | `VALID_TEAM_IDS` allow-list check rejects any teamId not in the 12-member set; typed `readonly TeamId[]` provides build-time gate (Task 3)                        |
| T-21-02   | Tampering | roomHandlers.ts struck-out guard  | accept      | `teamId === room.homePickedTeam` rejection at ~line 204 already prevents duplicate picks; unchanged this phase (RESEARCH Security Domain — no new attack surface) |
| T-21-SC   | Tampering | npm/pnpm installs                 | accept      | No packages installed this phase (RESEARCH Package Legitimacy Audit: zero new packages); no supply-chain surface                                                  |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test run` — teamConfig tests green (12 teams, 14 schemes, all squads = 11 players)
- `pnpm -w tsc --noEmit` — full monorepo type-check clean (TeamId completeness gate across shared + server)
</verification>

<success_criteria>

- TeamId is a 12-member union; TEAM_CONFIGS has 12 entries; each new team resolves to 11 PLAYER_POOL players via getSquadPlayers
- Three TeamId/sourceTeamId mismatches (miami→inter-miami, la→lafc, us→usmnt) resolved to correct player ranges
- Server VALID_TEAM_IDS accepts all 12 teams
- shared + server packages type-check clean; shared test suite passes
  </success_criteria>

<output>
Create `.planning/phases/21-new-teams-mls-international/21-01-SUMMARY.md` when done
</output>
