---
phase: 10-remaining-action-flows-tech-debt
plan: 03
type: execute
wave: 3
depends_on: ['10-02']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.phase10.test.ts
autonomous: true
requirements: [SHOT-01, SHOT-02, SHOT-03, SHOT-04, SNAP-02, SNAP-03, HEAD-03]
must_haves:
  truths:
    - 'applyDeclareShot transitions ACTION → GK_DIVING and records shotTargetHex (D-01/D-02/D-05)'
    - 'applyGKDive validates parallel-to-goal-line (constant q), ≤3 cumulative hexes, on-pitch (SHOT-04, D-04)'
    - 'GK dive to the 3rd hex applies -1 Saving; shot origin 4+ hexes from new GK position is unsaveable (SHOT-04)'
    - 'applyDeclareHeaderTarget records headerTargetHex; goal-line target on attacker win routes to GK save path (HEAD-03, D-11/D-12)'
    - 'Headed goal attempts are contested only by the GK — no outfield path-deflection (HEAD-03, D-13)'
    - 'Shot resolution rolls path defenders first (deflect→Loose Ball), else shooter-vs-GK duel (D-03)'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applyDeclareShot, applyGKDive, applyDeclareHeaderTarget, defender path-deflection in shot resolution'
      exports: ['applyDeclareShot', 'applyGKDive', 'applyDeclareHeaderTarget']
  key_links:
    - from: 'applyDeclareShot'
      to: 'GamePhase GK_DIVING'
      via: 'phase transition + shotTargetHex set'
      pattern: "phase: 'GK_DIVING'"
    - from: 'shot resolution'
      to: 'validateShotDuel / validateGKDive'
      via: 'shared shotValidator with post-dive distance'
      pattern: 'validateShotDuel|validateGKDive'
---

<objective>
Add the three new pure engine functions that drive the shot-declaration, GK-dive, and header-at-goal flows, plus the defender path-deflection step in shot resolution. All shot duel/dive/handling math already exists in `packages/shared/src/shotValidator.ts`; this plan adds the FSM plumbing and deflection that surround those validators. This plan owns gameEngine.ts in Wave 3 (after plan 02's fixes land in the same file).

Purpose: SHOT-01..04, SNAP-02/03, and HEAD-03 require interactive FSM phases that do not yet exist in the engine. These pure functions are consumed by the handlers in plan 04.
Output: applyDeclareShot, applyGKDive, applyDeclareHeaderTarget, defender-deflection resolution; phase10 engine tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-CONTEXT.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-RESEARCH.md
@packages/shared/src/shotValidator.ts
</context>

<artifacts_produced>

## Artifacts this phase produces (NEW symbols — exclude from drift verification)

New exported engine functions: `applyDeclareShot(state, goalHex)`, `applyGKDive(state, to)`, `applyDeclareHeaderTarget(state, targetHex)`.
New exported result types: `ApplyDeclareShotResult`, `ApplyGKDiveResult`, `ApplyDeclareHeaderTargetResult`.
New helper: defender path-deflection computation (e.g. `computeShotPathDefenders` / inline) consuming `hexLine` from packages/shared.
Uses GamePhase values `SHOT_DECLARED`, `GK_DIVING` and GameState fields `shotTargetHex`, `gkDivePosition`, `headerTargetHex` (all added in plan 01).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: applyDeclareShot + applyGKDive (shot declaration and GK reposition)</name>
  <files>packages/server/src/gameEngine.ts</files>
  <behavior>
    - applyDeclareShot(state, goalHex): rejects when phase !== 'PASS' (WRONG_PHASE); rejects when ELIGIBLE_NEXT_ACTIONS[lastActionType] lacks 'SHOT' (INVALID_SEQUENCE); rejects when goalHex is not a goal-line hex for attackingTeam (INVALID_TARGET); on success returns state with phase 'GK_DIVING', shotTargetHex=goalHex, gkDivePosition=current GK position
    - applyGKDive(state, to): rejects when phase !== 'GK_DIVING' (WRONG_PHASE); rejects when to.q !== gk.position.q (NOT_PARALLEL); rejects when cumulative dive distance from GK start > 3 (TOO_FAR); rejects when to is off-pitch (OFF_PITCH); on success updates gkDivePosition=to
    - GK dive to 3rd hex flags -1 Saving penalty (carried into resolution via gkDivePosition distance); shot origin ≥4 hexes from gkDivePosition is unsaveable
  </behavior>
  <read_first>
    - packages/server/src/gameEngine.ts (applySnapshot result-type + guard pattern ~lines 1625-1723; applyStartMovement ~lines 157-189; the ELIGIBLE_NEXT_ACTIONS import/usage)
    - packages/shared/src/shotValidator.ts (validateGKDive signature — distance is the only consumed input; validateShotDuel)
    - packages/shared/src/pitch.ts (PITCH_REGIONS / goal-line hex definition; PITCH_HEXES)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (new engine functions lines 166-188; Engine Pure Function Result Pattern lines 660-667)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-RESEARCH.md (Pitfall 1 GK dive direction, Pitfall 2 post-dive distance, Open Questions #1/#2 — goal mouth r∈[10..16] at q=0/q=36)
  </read_first>
  <action>
    In packages/server/src/gameEngine.ts add two exported pure functions following the applySnapshot result-discriminated-union pattern (PATTERNS.md lines 142-163, 660-667):
    (1) `applyDeclareShot(state, goalHex: HexCoord): ApplyDeclareShotResult` with reasons 'WRONG_PHASE' | 'INVALID_SEQUENCE' | 'INVALID_TARGET'. Guard phase === 'PASS'. Run the ELIGIBLE_NEXT_ACTIONS sequence check for 'SHOT' (same shape as applySnapshot's SNAPSHOT check). Validate goalHex is a goal-line hex for attackingTeam: q===36 for home attack, q===0 for away attack, r ∈ [10..16] (A1 assumption; reuse the same constants the existing Shoot distance uses). On success return state with phase 'GK_DIVING', shotTargetHex=goalHex, gkDivePosition = the GK piece's current position (GK = piece with role 'GK' on the defending team). Per D-02 you may transit PASS→SHOT_DECLARED→GK_DIVING; a single transition to 'GK_DIVING' is acceptable so long as shotTargetHex is recorded.
    (2) `applyGKDive(state, to: HexCoord): ApplyGKDiveResult` with reasons 'WRONG_PHASE' | 'NOT_PARALLEL' | 'TOO_FAR' | 'OFF_PITCH'. Guard phase === 'GK_DIVING'. Assert to.q === gk.position.q (Pitfall 1 — constant q is parallel to goal line). Assert cumulative distance from the GK's pre-dive starting position (the position recorded when SHOT_DECLARED/GK_DIVING was entered) to `to` is ≤ 3 using hexDistance. Assert PITCH_HEXES membership (Pitfall: off-pitch). On success return state with gkDivePosition=to. The -1-at-3rd-hex penalty and 4+-hex unsaveability are NOT computed here — they are derived at resolution time from hexDistance(gkDivePosition, shooterPos) via validateGKDive (Pitfall 2). Add the matching ApplyDeclareShotResult and ApplyGKDiveResult exported types.
  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/__tests__/gameEngine.phase10.test.ts -t "applyDeclareShot|applyGKDive|SHOT-04|GK" --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function applyDeclareShot\|export function applyGKDive" packages/server/src/gameEngine.ts` returns 2
    - `grep -c "phase: 'GK_DIVING'" packages/server/src/gameEngine.ts` is non-zero
    - phase10 engine tests asserting applyGKDive rejects to.q !== gk.position.q and rejects cumulative >3 hexes pass
    - phase10 engine test asserting applyDeclareShot rejects a non-goal-line goalHex (INVALID_TARGET) passes
    - `cd packages/server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>applyDeclareShot transitions to GK_DIVING with shotTargetHex; applyGKDive enforces parallel-to-goal-line, ≤3 hexes, on-pitch; phase10 tests for these pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: applyDeclareHeaderTarget (HEAD-03) + defender path-deflection in shot resolution (D-03/D-13)</name>
  <files>packages/server/src/gameEngine.ts</files>
  <behavior>
    - applyDeclareHeaderTarget(state, targetHex): rejects when phase !== 'HEADER' (WRONG_PHASE); rejects when both teams have not confirmed contestants (NOT_CONFIRMED); rejects off-pitch targetHex (INVALID_TARGET); on success sets headerTargetHex=targetHex and stays in HEADER for the duel
    - When the header duel resolves with attacker win AND headerTargetHex is a goal-line hex → route to GK save path (GK_DIVING / GK contest); when not goal-line → headed pass: attacker's winning contestant controls ball at headerTargetHex
    - Headed goal attempts apply NO outfield path-deflection — only the GK contests (D-13)
    - Shot resolution (non-header): roll each in-path defender (die 5/6 or combined ≥10 with Tackling → deflect) and within-1-hex defenders (die 6 or combined ≥10 → deflect); first deflection → Loose Ball at deflector position, no shooter-vs-GK duel; otherwise resolve shooter-vs-GK via validateShotDuel using post-dive GK distance
  </behavior>
  <read_first>
    - packages/server/src/gameEngine.ts (HEADER branch ~lines 1110-1234 contestant resolution; applyRoll SHOT branch ~lines 1010-1132; LOOSE_BALL transition pattern)
    - packages/shared/src/shotValidator.ts (validateShotDuel, validateGKDive, getOutsideAreaModifiers, validateHandlingCheck)
    - packages/shared/src/hex.ts (hexLine for shot path; hexDistance)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (applyDeclareHeaderTarget lines 183-188; D-22 GOAL fix already done in plan 02)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-RESEARCH.md (Pattern 4 defender deflection lines 314-326; Open Questions #3 two-band defender sets; Pitfall 5 server-side target validation)
    - packages/server/src/__tests__/gameEngine.phase10.test.ts (HEAD-03 and SNAP_DEFLECT red scaffolds)
  </read_first>
  <action>
    In packages/server/src/gameEngine.ts:
    (1) Add exported `applyDeclareHeaderTarget(state, targetHex: HexCoord): ApplyDeclareHeaderTargetResult` (reasons 'WRONG_PHASE' | 'NOT_CONFIRMED' | 'INVALID_TARGET'). Guard phase === 'HEADER'. Require headerConfirmed?.home && headerConfirmed?.away. Validate PITCH_HEXES membership; if claimed goal-line, verify it matches attackingTeam's goal side (q===36 home / q===0 away, r∈[10..16]) — Pitfall 5. On success return state with headerTargetHex=targetHex (stay in HEADER).
    (2) Extend the HEADER duel resolution: when the attacker wins AND headerTargetHex is a goal-line hex → transition into the GK save path (set up GK_DIVING / GK contest exactly like a declared shot, reusing the same resolution helper); apply D-13 (no outfield path-deflection on headed goal attempts — only GK). When attacker wins and headerTargetHex is NOT goal-line → headed pass: set ball.position=headerTargetHex, carrierId=the winning attacker contestant.
    (3) Add the defender path-deflection step used by regular shot resolution (called from the handler's GK end-turn auto-resolve in plan 04, but implemented as a pure helper here): compute the shot path with `hexLine(shooterPos, shotTargetHex)`. Set A = defenders on the path → deflect on die 5/6 OR combined-with-Tackling ≥10; Set B = defenders within 1 hex of the path but not on it → deflect on die 6 OR combined ≥10 (Open Questions #3). Dice are passed in (pre-generated in the handler) — the helper must NOT call rollDice/Math.random. First deflection returns a result transitioning to LOOSE_BALL with ball at the deflector's position; no shooter-vs-GK duel. If no deflection, resolve shooter-vs-GK via validateShotDuel with distance = hexDistance(gkDivePosition, shooterPos) (Pitfall 2) and validateGKDive for the dive penalty.
  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/__tests__/gameEngine.phase10.test.ts -t "HEAD-03|header.*goal|deflect|SNAP_DEFLECT" --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function applyDeclareHeaderTarget" packages/server/src/gameEngine.ts` returns 1
    - `grep -c "hexLine" packages/server/src/gameEngine.ts` is non-zero within shot-path deflection logic
    - phase10 engine test: header attacker win on a goal-line headerTargetHex routes to GK save path (not headed pass) passes
    - phase10 engine test: header attacker win on a non-goal-line hex delivers ball to that hex with attacker carrierId passes
    - phase10 engine test: in-path defender with die 5 deflects → LOOSE_BALL at defender position passes
    - `cd packages/server && npx vitest run src/__tests__/gameEngine.test.ts src/__tests__/gameEngine.phase8.test.ts` exits 0 (no regressions)
  </acceptance_criteria>
  <done>HEAD-03 target selection + goal-line redirect implemented; headed goals contested only by GK; regular-shot defender path-deflection (two bands) resolves to Loose Ball or shooter-vs-GK duel; phase10 tests green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                |
| --------------------------- | ------------------------------------------------------------------------------------------ |
| client→engine (via handler) | goalHex / GK dive hex / header target hex are client-supplied and re-validated server-side |

## STRIDE Threat Register

| Threat ID | Category  | Component                          | Disposition | Mitigation Plan                                                                                                                                    |
| --------- | --------- | ---------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10-05   | Tampering | header target hex for wrong goal   | mitigate    | applyDeclareHeaderTarget validates targetHex is a goal-line hex for attackingTeam's goal side (q/r bounds) before allowing GK redirect (Pitfall 5) |
| T-10-06   | Tampering | off-pitch GK dive / shot path      | mitigate    | PITCH_HEXES membership check in applyGKDive; constant-q parallel constraint; shot-path via hexLine over valid hexes                                |
| T-10-07   | Tampering | fabricated deflection/duel outcome | mitigate    | Engine helpers consume injected dice only; no rollDice/Math.random in gameEngine.ts                                                                |
| T-10-SC   | Tampering | npm installs                       | accept      | No new packages this phase                                                                                                                         |

</threat_model>

<verification>
- `cd packages/server && npx tsc --noEmit` exits 0
- `grep -c "Math.random" packages/server/src/gameEngine.ts` returns 0
- phase10 engine tests for shot declaration, GK dive, HEAD-03 redirect, and defender deflection all pass
</verification>

<success_criteria>

- applyDeclareShot, applyGKDive, applyDeclareHeaderTarget exported and tested
- Goal-line header redirect routes to GK save; headed goals contested only by GK
- Defender path-deflection (two bands) implemented as pure helper consuming injected dice
  </success_criteria>

<output>
Create `.planning/phases/10-remaining-action-flows-tech-debt/10-03-SUMMARY.md` when done
</output>
