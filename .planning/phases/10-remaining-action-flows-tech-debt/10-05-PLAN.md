---
phase: 10-remaining-action-flows-tech-debt
plan: 05
type: execute
wave: 5
depends_on: ['10-04']
files_modified:
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/ActionLog.tsx
  - packages/server/src/__tests__/game.integration.test.ts
autonomous: false
requirements:
  [
    SHOT-01,
    SHOT-02,
    SHOT-03,
    SHOT-04,
    SNAP-02,
    SNAP-03,
    HEAD-01,
    HEAD-02,
    HEAD-03,
    HEAD-04,
    HEAD-05,
  ]
must_haves:
  truths:
    - 'Shoot button is a two-step flow: click Shoot → click highlighted goal hex → emitDeclareShot (D-01, D-10)'
    - 'Snapshot button in MOVEMENT and ACTION is wired to emitSnapshot (D-10, WR-03)'
    - 'GK_DIVING and SNAP_DEFLECT phases render interactive repositioning UI for the correct team (D-04, D-08)'
    - 'HEADER phase renders a target-hex selection step after both teams confirm; click emits emitHeaderTarget (HEAD-03)'
    - 'Only one HEADER resolution control exists in ActionPanel (D-19 WR-04)'
    - "Pass log entries show the active team's colour indicator (D-27)"
    - 'HIGH_PASS_MOVEMENT highlights clear after movement ends (D-28)'
    - 'The 3 failing integration tests use real squad piece IDs/positions and pass (D-25)'
  artifacts:
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'emitDeclareShot, emitGKDive, emitHeaderTarget, shootingMode state'
      exports: ['emitDeclareShot', 'emitGKDive', 'emitHeaderTarget']
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'Shoot two-step, Snapshot wiring, GK_DIVING/SNAP_DEFLECT/HEADER-target UI, ACTION rename, D-18/D-19'
      contains: 'GK_DIVING'
    - path: 'packages/server/src/__tests__/game.integration.test.ts'
      provides: 'D-25 fixed tests using real squad positions'
  key_links:
    - from: 'ActionPanel Shoot button'
      to: 'emitDeclareShot'
      via: 'shootingMode → HexGrid goal-hex click'
      pattern: 'emitDeclareShot'
    - from: 'HexGrid goal/target click'
      to: 'emitGKDive / emitHeaderTarget / emitDeclareShot'
      via: 'phase-routed click handlers'
      pattern: 'emitHeaderTarget'
---

<objective>
Complete Phase 10 on the client: wire the two-step Shoot flow, the previously-disabled Snapshot button, the new GK_DIVING / SNAP_DEFLECT interactive phases, and the HEADER target-hex selection step; fix the action-log team colour (D-27) and high-pass highlight persistence (D-28); resolve WR-03/WR-04 UI items; and repair the 3 failing integration tests (D-25). Ends with the human UAT checkpoint (UAT-01/02/03).

Purpose: The server flows from plans 02-04 are unreachable without client controls. This plan makes shot declaration, snapshot, header-at-goal, and GK dive playable, then a human verifies the full paths in a live two-player session.
Output: Wired client; fixed ActionLog/HexGrid; green integration suite; human UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-CONTEXT.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-RESEARCH.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-VERIFICATION.md
</context>

<artifacts_produced>

## Artifacts this phase produces (NEW symbols — exclude from drift verification)

New store actions: `emitDeclareShot(goalHex)`, `emitGKDive(to)`, `emitHeaderTarget(targetHex)`.
New store state: `shootingMode: boolean`, `shootTargetHex: HexCoord | null` (+ setter `setShootingMode`).
New ActionPanel UI blocks: GK_DIVING, SNAP_DEFLECT, HEADER target-hex step, two-step Shoot, wired Snapshot.
New HexGrid behavior: goal-line / GK-dive / header-target hex highlight + phase-routed click dispatch.
Uses events GAME_GK_DIVE/GAME_HEADER_TARGET (plan 01) and reuses GAME_SHOT for declaration.
D-25 modifies existing integration tests only (no new production symbols).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Store wiring + ACTION rename (emitDeclareShot/emitGKDive/emitHeaderTarget, shootingMode)</name>
  <files>packages/client/src/store/useGameStore.ts</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts (emitSnapshot ~line 507; emitHeaderContestant ~lines 263-264; headerContestantIds state ~line 62; setGameState selection-clearing block; any phase==='PASS' comparisons)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (useGameStore section lines 491-523 — exact emit/state patterns)
  </read_first>
  <action>
    In packages/client/src/store/useGameStore.ts:
    (1) Add three emit actions following the emitSnapshot pattern (PATTERNS.md lines 502-512): `emitDeclareShot: (goalHex: HexCoord) => socket.emit(ClientEvents.GAME_SHOT, goalHex)` (reuses GAME_SHOT), `emitGKDive: (to: HexCoord) => socket.emit(ClientEvents.GAME_GK_DIVE, to)`, `emitHeaderTarget: (targetHex: HexCoord) => socket.emit(ClientEvents.GAME_HEADER_TARGET, targetHex)`. Add their signatures to the store type.
    (2) Add state fields `shootingMode: boolean` (init false) and `shootTargetHex: HexCoord | null` (init null) after headerContestantIds, plus a `setShootingMode(on: boolean)` action (PATTERNS.md lines 514-521). Clear both in setGameState alongside the existing selectedPassType/passTargetHex clearing so a fresh server broadcast resets shoot mode.
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit && grep -c "emitDeclareShot\|emitGKDive\|emitHeaderTarget" src/store/useGameStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "emitDeclareShot\|emitGKDive\|emitHeaderTarget" packages/client/src/store/useGameStore.ts` returns 3 or more
    - `grep -c "shootingMode\|setShootingMode" packages/client/src/store/useGameStore.ts` returns 3 or more
    - `cd packages/client && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Store exposes emitDeclareShot/emitGKDive/emitHeaderTarget and shootingMode state; cleared on new broadcast.</done>
</task>

<task type="auto">
  <name>Task 2: ActionPanel + HexGrid + ActionLog — Shoot two-step, Snapshot wiring, GK_DIVING/SNAP_DEFLECT/HEADER-target UI, D-18/D-19/D-27/D-28, ACTION rename</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/HexGrid.tsx, packages/client/src/components/ActionLog.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (HIGH_PASS_MOVEMENT block ~lines 82-104; HEADER confirm block ~lines 112-135; passTrigger ~lines 60-61; disabled Shoot/Snapshot buttons ~lines 221-237; phase==='PASS' at lines 69, 150, 152; isGKTeam derivation ~lines 140-142)
    - packages/client/src/components/HexGrid.tsx (pass-target click + highlight overlays; HIGH_PASS_MOVEMENT highlight computation/clearing; piece/hex click routing)
    - packages/client/src/components/ActionLog.tsx (STANDARD_PASS case ~lines 204-209; SHOT_ATTEMPT/GOAL colour derivation ~lines 195-238; pieceColorOf helper)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (ActionPanel section lines 393-487; ActionLog D-27 fix lines 527-557)
    - .planning/phases/08.2-passing-cleanup-inserted/08.2-VERIFICATION.md (HEADER auto-roll behavior note — UAT-03)
  </read_first>
  <action>
    In packages/client/src/components/ActionPanel.tsx:
    (1) Shoot two-step (D-01, PATTERNS.md lines 444-471): replace the permanently-disabled Shoot button with an enabled button (visible when eligible.has('SHOT') and within shooting distance) whose onClick calls setShootingMode(true). Step 2 (HexGrid) highlights goal hexes; clicking one calls emitDeclareShot(hex). No Roll Dice step — server auto-resolves after GK dives.
    (2) Snapshot wiring (D-10): replace the disabled Snapshot button(s) with onClick={emitSnapshot} in both the MOVEMENT block and the ACTION-phase block.
    (3) D-18 (WR-03): change passTrigger from `phase === 'PASS' && lastActionType !== null` to `phase === 'PASS' && isEligible('SNAPSHOT')`.
    (4) GK_DIVING + SNAP_DEFLECT UI blocks (PATTERNS.md lines 397-424): copy the HIGH_PASS_MOVEMENT interactive-repositioning structure. GK_DIVING: label "Reposition GK (up to 3 hexes parallel to goal line)", active team = GK team (derive via carrier/GK lookup), End Turn → emitEndTurn (server auto-resolves). SNAP_DEFLECT: label "Move a player to deflect the snapshot (up to 2 hexes)", active team = defending team, End Turn → emitEndTurn. Non-active team sees a "wait" panel.
    (5) HEADER target-hex step (HEAD-03, PATTERNS.md lines 426-442): after headerConfirmed.home && .away && isActivePlayer, render a "Click a target hex for the header" prompt; HexGrid click routes to emitHeaderTarget. D-19 (WR-04): ensure ActionPanel does NOT render both a Roll Dice and a Header button for HEADER — keep a single resolution route consistent with the auto-confirm design (UAT-03).
    In packages/client/src/components/HexGrid.tsx:
    (7) Add goal-line hex highlighting when shootingMode is true (reuse or distinct colour per Claude's discretion), and route clicks on those hexes to emitDeclareShot. Add GK-dive candidate highlights during GK_DIVING (parallel-to-goal-line hexes ≤3 from GK) routing clicks to emitGKDive. Add header-target highlights during the HEADER target step routing clicks to emitHeaderTarget.
    (8) D-28: ensure HIGH_PASS_MOVEMENT valid-movement highlights are recomputed for the current mover during repositioning and CLEARED when the turn/phase advances (do not leave stale highlight sets after movement ends).
    In packages/client/src/components/ActionLog.tsx:
    (9) D-27 (PATTERNS.md lines 531-557): give STANDARD_PASS and FIRST_TIME_PASS log entries the active team's colour via pieceColorOf(passerId) (add passerId to those ActionEvent types if absent, sourced from the passer piece) — matching the HIGH_PASS/SHOT_ATTEMPT/GOAL colour treatment, replacing prefixColor: null.
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit && npx vitest run src/components/ActionPanel.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GK_DIVING\|SNAP_DEFLECT" packages/client/src/components/ActionPanel.tsx` returns 2 or more
    - `grep -c "emitDeclareShot\|emitSnapshot\|emitHeaderTarget" packages/client/src/components/ActionPanel.tsx` returns 3 or more
    - `grep -c "disabled" packages/client/src/components/ActionPanel.tsx` shows the Shoot/Snapshot buttons are no longer permanently disabled (no `<button ... disabled>` for Shoot/Snapshot)
    - `grep -c "isEligible('SNAPSHOT')" packages/client/src/components/ActionPanel.tsx` returns 1 or more (D-18)
    - `grep -c "prefixColor: null" packages/client/src/components/ActionLog.tsx` for STANDARD_PASS/FIRST_TIME_PASS cases is reduced (team colour applied)
    - `cd packages/client && npx tsc --noEmit` exits 0; ActionPanel.test.tsx passes
  </acceptance_criteria>
  <done>Shoot is a two-step flow; Snapshot wired in MOVEMENT and PASS phases; GK_DIVING/SNAP_DEFLECT/HEADER-target UI render for the correct team; single HEADER resolution route; pass log entries coloured; high-pass highlights clear; client builds and ActionPanel tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the 3 failing integration tests with real squad positions (D-25)</name>
  <files>packages/server/src/__tests__/game.integration.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/game.integration.test.ts (the 3 failing tests: D-10 undo reversal, D-09 UNDO_LOCKED after SLOT_ADVANCE, the former PASS-phase roll test)
    - packages/shared/src/teams.ts (HOME_SQUAD / AWAY_SQUAD piece IDs and their real 37×26 positions)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (game.integration.test.ts D-25 section lines 600-604)
    - .planning/phases/08.2-passing-cleanup-inserted/deferred-items.md (integration test failure root causes)
  </read_first>
  <action>
    In packages/server/src/__tests__/game.integration.test.ts, replace the Phase-2-era hardcoded piece IDs and `{ q: 2, r: 2 }`-style placeholder coordinates in the 3 failing tests with real HOME_SQUAD/AWAY_SQUAD piece IDs imported from packages/shared teams and their actual starting positions on the 37×26 board. Adjust expected coordinates/slots to match the real squad layout so the undo-reversal, UNDO_LOCKED-after-SLOT_ADVANCE, and roll tests assert against real pieces. Do not weaken the assertions — they must still prove the original behavior (undo restores prior hex; UNDO_LOCKED after slot advance; roll resolves in PASS phase).
  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/__tests__/game.integration.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HOME_SQUAD\|AWAY_SQUAD" packages/server/src/__tests__/game.integration.test.ts` is non-zero (real squads imported/used)
    - `cd packages/server && npx vitest run src/__tests__/game.integration.test.ts` exits 0 (all integration tests pass, including the 3 previously failing)
  </acceptance_criteria>
  <done>All 3 previously-failing integration tests use real squad positions, assert against ACTION phase, and pass; full integration suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human UAT — snapshot, header-at-goal, header auto-roll, regular shot + GK save (UAT-01/02/03, D-14)</name>
  <action>Pause for human verification of the live two-player flows described below. Do not auto-advance; wait for the resume signal.</action>
  <what-built>
    Full Phase 10 shot/snapshot/header flows are wired end-to-end: two-step Shoot declaration, interactive GK dive, SNAP_DEFLECT opponent move, header-at-goal via goal-line target hex, and the GK save/handling/restart path. Code-review BLOCKER (CR-01 replay leak) and all warnings are fixed; gameplay bugs (loose-ball boundary, one-steal/one-tackle, pickup-continues-movement, pass log colour, high-pass highlight) are fixed.
  </what-built>
  <how-to-verify>
    Start the dev server and open two browser tabs (create room in one, join in the other). Run a live two-player session and verify:
    1. UAT-01 (Snapshot full path): get the ball carrier into the opponent's penalty area during MOVEMENT; click Snapshot; confirm SNAP_DEFLECT phase fires (opponent moves a player up to 2 hexes, then End Turn); the shot resolves with the -1 snapshot penalty; no GAME_ERROR appears.
    2. UAT-02 (Header at goal): complete a High Pass to reach HEADER phase; both teams select/confirm contestants; the attacker clicks a goal-line hex as the target; the GK dives (GK_DIVING); the shot auto-resolves; both tabs show identical final state.
    3. UAT-03 (HEADER auto-roll): confirm that after both teams confirm contestants the duel fires immediately with NO separate "Roll Header" button, and that no duplicate Roll Dice + Header buttons appear. Acknowledge this is the intended design.
    4. Regular shot + GK save flow (D-14, SHOT-05/SHOT-06): click Shoot → click a highlighted goal hex → GK dives → shot auto-resolves (GOAL/MISS/SAVE) and the action log shows the individual rolls; a scored goal updates the scoreboard and appears in the post-game replay. On a SAVE, verify the handling check fires — a clean catch routes to GK_RESTART (kick/throw/movement) and a spill routes to LOOSE_BALL — and confirm the GK restart options are selectable.
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe any issues (which UAT failed and observed vs expected).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                          |
| ----------------------- | ---------------------------------------------------------------------------------------------------- |
| client UI → socket emit | Client only emits intents (goalHex, dive hex, target hex); server (plan 04) re-validates all of them |

## STRIDE Threat Register

| Threat ID | Category               | Component                       | Disposition                         | Mitigation Plan                                                                                                                                     |
| --------- | ---------------------- | ------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10-13   | Tampering              | client could emit arbitrary hex | accept (client) / mitigate (server) | Client is untrusted by design; all hex payloads re-validated server-side in plan 04 (team/phase/shape/PITCH_HEXES). No new client trust introduced. |
| T-10-14   | Information Disclosure | none                            | accept                              | No secrets or PII in client; game state is already broadcast to both players (ARCH-04)                                                              |
| T-10-SC   | Tampering              | npm installs                    | accept                              | No new packages this phase                                                                                                                          |

</threat_model>

<verification>
- `cd packages/client && npx tsc --noEmit` exits 0
- `cd packages/client && npx vitest run` exits 0 (ActionPanel and store tests pass)
- `cd packages/server && npx vitest run` exits 0 (full server suite green, including the 3 D-25 integration tests)
- Human UAT-01/02/03 approved
</verification>

<success_criteria>

- Two-step Shoot, wired Snapshot, GK_DIVING/SNAP_DEFLECT/HEADER-target UI all functional
- Pass log colour + high-pass highlight fixes applied; WR-03/WR-04 resolved
- 3 integration tests fixed and green
- Human approves UAT-01 (snapshot), UAT-02 (header at goal), UAT-03 (header auto-roll)
  </success_criteria>

<output>
Create `.planning/phases/10-remaining-action-flows-tech-debt/10-05-SUMMARY.md` when done
</output>
