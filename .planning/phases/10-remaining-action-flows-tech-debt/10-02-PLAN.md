---
phase: 10-remaining-action-flows-tech-debt
plan: 02
type: execute
wave: 2
depends_on: ['10-01']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.phase10.test.ts
autonomous: true
requirements: [SNAP-03, HEAD-04]
must_haves:
  truths:
    - 'A scored shot appends a GOAL entry to eventLog so replays show correct scores (D-22)'
    - "Intermediate movement-slot transitions reset lastActionType to 'MOVEMENT_PHASE' (D-17)"
    - 'HEADER tie-break uses an injected die, not Math.random (D-21)'
    - "HEADER LOOSE_BALL case sets lastActionType 'DEFLECTION' not 'HEADER' (D-23)"
    - 'Loose Ball trajectory clamps to PITCH_HEXES; never walks off-board (D-26)'
    - 'A piece may attempt at most one steal and one tackle per movement phase (D-29)'
    - 'Picking up a loose ball during movement does not end the movement action (D-30)'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: '9 code-review/bug fixes'
      contains: 'GOAL'
  key_links:
    - from: 'packages/server/src/gameEngine.ts applyRoll SHOT GOAL branch'
      to: 'state.eventLog'
      via: 'GOAL ActionEvent append'
      pattern: "type: 'GOAL'"
    - from: 'packages/server/src/gameEngine.ts applyStartMovement'
      to: 'stealAttemptedByIds / tackleAttemptedByIds'
      via: 'per-phase tracking reset'
      pattern: "stealAttemptedByIds: \\[\\]"
---

<objective>
Resolve the gameEngine.ts portion of Phase 10 tech debt and player-reported bugs. This plan owns gameEngine.ts exclusively so it runs as a single wave with no file contention; the new engine functions (applyDeclareShot/applyGKDive/applyDeclareHeaderTarget) are added in plan 03 (same file, next wave).

Purpose: Clear the CR-01-adjacent engine debt (D-16/D-17/D-20/D-21/D-22/D-23) and gameplay bugs (D-26/D-29/D-30) that block correct replays, sequence validation, and live play.
Output: Fixed gameEngine.ts; corresponding phase10 engine tests turned green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-CONTEXT.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-REVIEW.md
</context>

<artifacts_produced>

## Artifacts this phase produces (NEW symbols — exclude from drift verification)

Module-level constant hoisted from applySnapshot: the `passTypes` Set (IN-01/D-20).
Uses GameState fields added in plan 01: `stealAttemptedByIds`, `tackleAttemptedByIds`.
Uses GamePhase value `'PASS'` (unchanged).
No new exported functions in this plan (engine functions are added in plan 03).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Apply code-review debt fixes D-16/D-17/D-20/D-21/D-22/D-23 in gameEngine.ts</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (all `'PASS'` GamePhase occurrences: lines 134, 158, 314, 385, 441, 769, 919, 998, 1215, 1250, 1298, 1356, 1438, 1703; applyEndTurn ~lines 585-599; applySnapshot passTypes Set ~line 1143; applyRoll SHOT GOAL branch; HEADER LOOSE_BALL tie branch ~lines 1322-1356)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (gameEngine.ts section lines 134-234 — exact fix excerpts)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-REVIEW.md (WR-01, WR-02, IN-01 descriptions)
  </read_first>
  <action>
    In packages/server/src/gameEngine.ts:
    (1) D-16 (WR-01): remove the dead `'HIGH_PASS'` entry from the `passTypes` Set in applySnapshot; add a one-line comment noting HIGH_PASS transitions to HEADER and is gated by ELIGIBLE_NEXT_ACTIONS.
    (2) D-20 (IN-01): hoist that `passTypes` Set to a module-level `const` (top of file with other consts) so it is not reallocated per call.
    (3) D-17 (WR-02): in applyEndTurn, the non-ATTACKER_2 (intermediate) slot-transition return block (~lines 585-599) must add `lastActionType: 'MOVEMENT_PHASE'` to the returned state spread, matching the ATTACKER_2→null path. See PATTERNS.md lines 190-207.
    (4) D-22: in the applyRoll SHOT branch GOAL return, append `{ type: 'GOAL' as const, scoringTeam: state.attackingTeam, timestamp: Date.now() }` to `eventLog` (PATTERNS.md lines 227-234; RESEARCH Example 2).
    (5) D-21: replace `Math.floor(Math.random() * tied.length)` in the HEADER duel pickWinner tie-break with an injected die. Add a 4th pre-generated die parameter to the engine call path (the handler pre-generates it); use `(d4 - 1) % tied.length` (or equivalent deterministic index) instead of Math.random. Keep the engine pure — no Math.random anywhere in gameEngine.ts after this fix.
    (6) D-23: in the HEADER LOOSE_BALL (equal-scores / tie) return block (~lines 1322-1356), set `lastActionType: 'DEFLECTION'` (not `'HEADER'`).
  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Math.random" packages/server/src/gameEngine.ts` returns 0
    - `grep -c "type: 'GOAL'" packages/server/src/gameEngine.ts` returns 1 or more (GOAL appended in SHOT branch)
    - `grep -c "'STANDARD_PASS'\|'FIRST_TIME_PASS'\|'HIGH_PASS'\|'LONG_BALL'" packages/server/src/gameEngine.ts` is non-zero (pass-type strings preserved)
    - `cd packages/server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Math.random is gone, GOAL is logged, D-16/D-17/D-20/D-23 applied; engine type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gameplay bug fixes D-26 (Loose Ball boundary), D-29 (one steal+tackle per piece), D-30 (loose-ball pickup continues movement)</name>
  <files>packages/server/src/gameEngine.ts</files>
  <behavior>
    - D-26: a Loose Ball trajectory whose computed landing/walk would exit PITCH_HEXES stops at the last in-bounds hex; ball.position is always a member of PITCH_HEXES
    - D-29: applyStartMovement returns state with stealAttemptedByIds=[] and tackleAttemptedByIds=[]; a steal attempt from a piece already in stealAttemptedByIds is rejected; a tackle attempt from a piece already in tackleAttemptedByIds is rejected; a successful/failed attempt appends the piece id to the relevant list
    - D-30: when a moving piece reaches a hex holding the loose ball during a MOVEMENT action, the piece picks up the ball AND retains its remaining pace (movement action is not ended; paceUsedByPieceId reflects only hexes actually moved)
  </behavior>
  <read_first>
    - packages/server/src/gameEngine.ts (applyStartMovement ~lines 157-189; LOOSE_BALL trajectory walk ~lines 1344-1438 using hexLine; steal/tackle attempt logic in applyMove / movement branch; movement pickup logic)
    - packages/shared/src/pitch.ts (PITCH_HEXES set, isPitchHex helper if present)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (applyStartMovement D-29 block lines 209-225; Loose Ball boundary note)
    - packages/server/src/__tests__/gameEngine.phase10.test.ts (the red scaffolds for D-26/D-29/D-30)
  </read_first>
  <action>
    In packages/server/src/gameEngine.ts:
    (1) D-26: in the LOOSE_BALL trajectory walk, validate each stepped hex with PITCH_HEXES membership (use `isPitchHex` or `PITCH_HEXES.some(h => h.q === hex.q && h.r === hex.r)` from packages/shared). When the next trajectory step is off-board (e.g. r=0 on the 37×26 board), stop at the current in-bounds hex and set ball.position there rather than walking off-board.
    (2) D-29: in applyStartMovement add `stealAttemptedByIds: []` and `tackleAttemptedByIds: []` to the returned state (PATTERNS.md lines 209-225). In the steal-attempt path, reject (return the appropriate not-ok reason, e.g. ALREADY_ATTEMPTED) when the attempting piece id is already in `state.stealAttemptedByIds`; on a permitted attempt, append the piece id to stealAttemptedByIds in the returned state. Apply the symmetric logic for tackle attempts using `tackleAttemptedByIds`.
    (3) D-30: in the movement branch where a moving piece lands on the loose-ball hex, transfer ball possession to the piece WITHOUT ending the movement action — keep the piece in the active mover set and preserve its remaining pace (do not zero paceUsedByPieceId beyond hexes actually traversed; do not transition phase). The action continues until pace is exhausted or the player ends the turn.
  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/__tests__/gameEngine.phase10.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stealAttemptedByIds: \[\]\|tackleAttemptedByIds: \[\]" packages/server/src/gameEngine.ts` returns 2 or more (cleared in applyStartMovement)
    - `grep -c "PITCH_HEXES\|isPitchHex" packages/server/src/gameEngine.ts` is non-zero within the LOOSE_BALL trajectory logic
    - phase10 engine tests for D-26 boundary clamp, D-29 one-steal/one-tackle, and D-30 pickup-continues-movement pass (no `.todo`/`.skip` remaining on those cases)
    - `cd packages/server && npx vitest run src/__tests__/gameEngine.test.ts src/__tests__/gameEngine.phase8.test.ts` exits 0 (no regressions in existing engine suites)
  </acceptance_criteria>
  <done>Loose Ball never walks off-board; one steal + one tackle per piece per phase is enforced; loose-ball pickup during movement preserves remaining pace; phase10 engine tests for D-26/D-29/D-30 are green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                    |
| ---------------------- | ------------------------------------------------------------------------------ |
| engine purity boundary | Pure engine must not generate randomness; all dice injected from handler layer |

## STRIDE Threat Register

| Threat ID | Category          | Component                           | Disposition | Mitigation Plan                                                                                           |
| --------- | ----------------- | ----------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------- |
| T-10-02   | Tampering         | HEADER tie-break randomness         | mitigate    | D-21: replace Math.random with injected pre-generated die; engine stays pure and deterministic (testable) |
| T-10-03   | Tampering         | Loose Ball off-board position       | mitigate    | D-26: clamp trajectory to PITCH_HEXES membership so ball.position is always a valid pitch hex             |
| T-10-04   | Denial of Service | repeated steal/tackle by same piece | mitigate    | D-29: per-phase stealAttemptedByIds/tackleAttemptedByIds prevent unbounded re-attempts                    |
| T-10-SC   | Tampering         | npm installs                        | accept      | No new packages this phase                                                                                |

</threat_model>

<verification>
- `cd packages/server && npx tsc --noEmit` exits 0
- `grep -c "Math.random" packages/server/src/gameEngine.ts` returns 0
- `cd packages/server && npx vitest run` — no new failures introduced (the 3 D-25 integration failures remain until plan 05; engine and phase8 suites green)
</verification>

<success_criteria>

- GOAL appended to eventLog; lastActionType reset on intermediate slots; pickWinner deterministic; HEADER LOOSE_BALL = DEFLECTION
- Loose Ball boundary clamp, one-steal/one-tackle, and pickup-continues-movement implemented and tested
  </success_criteria>

<output>
Create `.planning/phases/10-remaining-action-flows-tech-debt/10-02-SUMMARY.md` when done
</output>
