---
phase: 10-remaining-action-flows-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/server/src/__tests__/gameEngine.phase10.test.ts
  - packages/server/src/__tests__/gameHandlers.phase10.test.ts
autonomous: true
requirements: [SHOT-01, SHOT-04, SNAP-02, HEAD-03]
must_haves:
  truths:
    - 'GamePhase union contains SHOT_DECLARED, GK_DIVING, SNAP_DEFLECT and ACTION (not PASS) in shared types'
    - 'GameState carries headerTargetHex, shotTargetHex, gkDivePosition, stealAttemptedByIds, tackleAttemptedByIds'
    - 'ClientEvents and ClientToServerEvents expose GAME_GK_DIVE and GAME_HEADER_TARGET'
    - 'Wave 0 phase10 test files exist and run (red/scaffold) before engine/handler work begins'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'GamePhase union (renamed + 3 new phases), 5 new GameState fields'
      contains: 'SHOT_DECLARED'
    - path: 'packages/shared/src/events.ts'
      provides: 'GAME_GK_DIVE, GAME_HEADER_TARGET events and typed signatures'
      contains: 'GAME_GK_DIVE'
    - path: 'packages/server/src/__tests__/gameEngine.phase10.test.ts'
      provides: 'Wave 0 engine test scaffold'
    - path: 'packages/server/src/__tests__/gameHandlers.phase10.test.ts'
      provides: 'Wave 0 handler test scaffold'
  key_links:
    - from: 'packages/shared/src/types.ts'
      to: 'packages/shared/src/events.ts'
      via: 'HexCoord type used in new event signatures'
      pattern: 'HexCoord'
---

<objective>
Establish the shared-package foundation for Phase 10: rename the GamePhase value `'PASS'` to `'ACTION'` inside `packages/shared`, add three new FSM phases (`SHOT_DECLARED`, `GK_DIVING`, `SNAP_DEFLECT`), add five new GameState fields, add two new client→server events (`GAME_GK_DIVE`, `GAME_HEADER_TARGET`), and create the two Wave 0 server test scaffolds that downstream engine/handler plans turn green.

Purpose: Every downstream plan (engine fixes, new engine functions, handlers, client) consumes these types and events. The shared package must compile and export the new contracts first so the TypeScript compiler guides the rest of the rename.
Output: Updated `types.ts`, `events.ts`, two new phase10 test files (scaffold).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-CONTEXT.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-RESEARCH.md
</context>

<artifacts_produced>

## Artifacts this phase produces (NEW symbols — exclude from drift verification)

New GamePhase union values: `SHOT_DECLARED`, `GK_DIVING`, `SNAP_DEFLECT`. Renamed value: `ACTION` (was `PASS`).
New GameState fields: `headerTargetHex`, `shotTargetHex`, `gkDivePosition`, `stealAttemptedByIds`, `tackleAttemptedByIds`.
New event constants: `GAME_GK_DIVE` (`'game:gk-dive'`), `GAME_HEADER_TARGET` (`'game:header-target'`).
New test files: `packages/server/src/__tests__/gameEngine.phase10.test.ts`, `packages/server/src/__tests__/gameHandlers.phase10.test.ts`.
New engine functions (declared/imported here as red scaffolds, implemented in plans 03/04): `applyDeclareShot`, `applyGKDive`, `applyDeclareHeaderTarget`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Rename GamePhase PASS→ACTION and add 3 new phases + 5 new GameState fields in types.ts</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts (current GamePhase union ~lines 170-184; GameState optional-field block ~lines 239-289)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (types.ts section, lines 28-94 — exact union and field text)
  </read_first>
  <action>
    In packages/shared/src/types.ts: (1) Rename the GamePhase union member `'PASS'` to `'ACTION'` (D-06). Do NOT touch pass-type strings like `'STANDARD_PASS'`/`'FIRST_TIME_PASS'`/`'HIGH_PASS'`/`'LONG_BALL'` — those are LastActionType values, not GamePhase values (Pitfall 3). (2) Add three new GamePhase members positioned after `'ACTION'` and before `'SHOT'`: `'SHOT_DECLARED'`, `'GK_DIVING'`, `'SNAP_DEFLECT'`, each with a JSDoc comment matching PATTERNS.md lines 59-61. (3) Add five new optional GameState fields following the existing optional-field style (the `highPass*` fields are the style template): `headerTargetHex?: HexCoord | null` (HEAD-03 / D-12), `shotTargetHex?: HexCoord | null` (D-05), `gkDivePosition?: HexCoord | null` (D-04 GK_DIVING tracking), `stealAttemptedByIds?: readonly string[]` (D-29), `tackleAttemptedByIds?: readonly string[]` (D-29). Use the exact JSDoc text in PATTERNS.md lines 84-93.
  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SHOT_DECLARED\|GK_DIVING\|SNAP_DEFLECT" packages/shared/src/types.ts` returns 3 or more
    - `grep -c "headerTargetHex\|shotTargetHex\|gkDivePosition\|stealAttemptedByIds\|tackleAttemptedByIds" packages/shared/src/types.ts` returns 5 or more
    - `grep -E "\|[[:space:]]*'PASS'" packages/shared/src/types.ts` returns zero results (GamePhase no longer has PASS member)
    - `grep -c "'STANDARD_PASS'\|'FIRST_TIME_PASS'\|'HIGH_PASS'\|'LONG_BALL'" packages/shared/src/types.ts` is unchanged (pass-type strings preserved)
    - `cd packages/shared && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GamePhase has ACTION (not PASS) plus the three new phases; GameState has the five new fields; shared package type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add GAME_GK_DIVE and GAME_HEADER_TARGET events to events.ts</name>
  <files>packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/events.ts (ClientEvents const ~lines 7-38; ClientToServerEvents interface ~lines 52-91 — existing GAME_HEADER_CONTESTANT and GAME_SHOT entries)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (events.ts section, lines 98-130 — exact entries)
  </read_first>
  <action>
    In packages/shared/src/events.ts: (1) Add to the ClientEvents const, following the `GAME_*: 'game:*'` convention: `GAME_GK_DIVE: 'game:gk-dive'` (Phase 10 GK reposition during GK_DIVING) and `GAME_HEADER_TARGET: 'game:header-target'` (Phase 10 HEAD-03 attacker target-hex selection). (2) Add matching typed entries to the ClientToServerEvents interface following the `[ClientEvents.GAME_SHOT]: (targetHex: HexCoord) => void` style: `[ClientEvents.GAME_GK_DIVE]: (to: HexCoord) => void` and `[ClientEvents.GAME_HEADER_TARGET]: (targetHex: HexCoord) => void`. Reuse the existing `GAME_SHOT` event for shot declaration (D-02, per Claude's discretion in CONTEXT.md) — do NOT add a separate GAME_DECLARE_SHOT event. Keep `GAME_HEADER` in place for now; its removal (D-19) is handled in the handler plan.
  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GAME_GK_DIVE\|GAME_HEADER_TARGET" packages/shared/src/events.ts` returns 4 or more (const entry + interface entry for each)
    - `grep -c "game:gk-dive\|game:header-target" packages/shared/src/events.ts` returns 2
    - `grep -c "GAME_DECLARE_SHOT" packages/shared/src/events.ts` returns 0 (GAME_SHOT reused, no new declare event)
    - `cd packages/shared && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>events.ts exports GAME_GK_DIVE and GAME_HEADER_TARGET with typed HexCoord signatures; shared package type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Wave 0 phase10 test scaffolds (engine + handler)</name>
  <files>packages/server/src/__tests__/gameEngine.phase10.test.ts, packages/server/src/__tests__/gameHandlers.phase10.test.ts</files>
  <behavior>
    - gameEngine.phase10.test.ts imports applyDeclareShot, applyGKDive, applyDeclareHeaderTarget, applyEndTurn, applyRoll, applyStartMovement from ../gameEngine.js and contains describe blocks (skipped or red) for: D-22 GOAL eventLog append, D-17 intermediate-slot lastActionType reset, D-21 pickWinner determinism, D-23 HEADER LOOSE_BALL lastActionType='DEFLECTION', D-29 one-steal-one-tackle per piece, SNAP_DEFLECT transition, HEAD-03 goal-line redirect, applyGKDive parallel-to-goal-line + ≤3-hex guards
    - gameHandlers.phase10.test.ts contains describe blocks (skipped or red) for: D-15 startReplayStream re-fetches live room, D-24 GAME_RESTART_MOVEMENT error snap-back broadcastState, GAME_GK_DIVE phase/team/HexCoord guards, SNAP_DEFLECT GAME_MOVE guard, GAME_HEADER_TARGET both-confirmed + attacker guard
  </behavior>
  <read_first>
    - packages/server/src/__tests__/gameEngine.phase8.test.ts (file structure, imports, fixture pattern — lines 1-80)
    - packages/server/src/__tests__/gameHandlers.test.ts (handler test harness, mock socket/room setup)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (test section lines 561-604; Phase Requirements → Test Map in 10-RESEARCH.md lines 648-672)
  </read_first>
  <action>
    Create packages/server/src/__tests__/gameEngine.phase10.test.ts and packages/server/src/__tests__/gameHandlers.phase10.test.ts. Use the import block from PATTERNS.md lines 572-581 (import applyDeclareShot, applyGKDive, applyDeclareHeaderTarget which do not yet exist — these are red scaffolds turned green in plans 02/03/04). Write one `it(...)` per behavior listed in the behavior block, each with a concrete expect that encodes the target behavior (e.g. expect a GOAL entry in eventLog after a shot-goal; expect lastActionType==='MOVEMENT_PHASE' after ATTACKER_4→DEFENDER_5; expect applyGKDive to reject a hex where to.q !== gk.position.q). Mark each `it` with `.todo` or wrap in `describe.skip` ONLY where the imported symbol does not yet exist and would crash collection; otherwise leave as failing (red) assertions. Per the Nyquist Rule these scaffolds are the automated proof targets for downstream plans. Use real HOME_SQUAD/AWAY_SQUAD piece IDs in fixtures (import from packages/shared teams) per D-25 convention, not Phase-2-era hardcoded coords.
  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/__tests__/gameEngine.phase10.test.ts src/__tests__/gameHandlers.phase10.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File packages/server/src/__tests__/gameEngine.phase10.test.ts exists and is collected by vitest (appears in run output)
    - File packages/server/src/__tests__/gameHandlers.phase10.test.ts exists and is collected by vitest
    - `grep -c "applyDeclareShot\|applyGKDive\|applyDeclareHeaderTarget" packages/server/src/__tests__/gameEngine.phase10.test.ts` returns 3 or more
    - Vitest run does not crash on collection (skipped/todo tests are acceptable; failing red tests are acceptable)
  </acceptance_criteria>
  <done>Both phase10 test files exist, are collected by vitest, and reference the three new engine functions plus the D-15..D-29 behaviors as the proof targets for downstream plans.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                                   |
| ------------------- | ----------------------------------------------------------------------------- |
| client→shared types | New event signatures define the payload contract crossing the socket boundary |

## STRIDE Threat Register

| Threat ID | Category  | Component                                  | Disposition | Mitigation Plan                                                                                                                                         |
| --------- | --------- | ------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10-01   | Tampering | GAME_GK_DIVE / GAME_HEADER_TARGET payloads | mitigate    | Typed `(to: HexCoord)` / `(targetHex: HexCoord)` signatures; runtime shape validation enforced server-side in plan 04 handler (typeof q/r === 'number') |
| T-10-SC   | Tampering | npm/pip/cargo installs                     | mitigate    | No new packages this phase (RESEARCH Package Legitimacy Audit: none); slopcheck N/A                                                                     |

</threat_model>

<verification>
- `cd packages/shared && npx tsc --noEmit` exits 0
- `cd packages/shared && npx vitest run` exits 0 (no regressions in existing shared tests from the field additions)
- Both phase10 server test files are collected by vitest without collection errors
</verification>

<success_criteria>

- GamePhase union: ACTION present, PASS absent, SHOT_DECLARED/GK_DIVING/SNAP_DEFLECT present
- GameState: five new fields present and typed
- events.ts: GAME_GK_DIVE and GAME_HEADER_TARGET present with HexCoord signatures
- Two Wave 0 test scaffolds exist and are collected
  </success_criteria>

<output>
Create `.planning/phases/10-remaining-action-flows-tech-debt/10-01-SUMMARY.md` when done
</output>
