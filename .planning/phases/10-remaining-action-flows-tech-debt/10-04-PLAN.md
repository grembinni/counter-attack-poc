---
phase: 10-remaining-action-flows-tech-debt
plan: 04
type: execute
wave: 4
depends_on: ['10-03']
files_modified:
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/gameHandlers.phase10.test.ts
autonomous: true
requirements: [SHOT-01, SHOT-04, SNAP-02, HEAD-03, HEAD-04]
must_haves:
  truths:
    - 'GAME_SHOT accepts from ACTION phase, validates goalHex, transitions to GK_DIVING (D-02)'
    - 'GAME_GK_DIVE handler validates GK team, phase, HexCoord shape, and snaps back on every error (D-04)'
    - 'GK End Turn pre-generates all dice and auto-resolves the shot (defenders → duel) with full roll logs (D-03)'
    - 'GAME_MOVE handles SNAP_DEFLECT (defending team moves 1 player ≤2 hexes) before snapshot resolves (D-08)'
    - 'GAME_HEADER_TARGET validates both-confirmed + attacker + on-pitch and sets headerTargetHex (HEAD-03)'
    - 'startReplayStream re-fetches the live room inside setTimeout and exits if deleted (D-15 CR-01 BLOCKER)'
    - 'GAME_RESTART_MOVEMENT error paths call broadcastState snap-back (D-24)'
    - 'Duplicate HEADER resolution path removed — single resolution route (D-19 WR-04)'
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'GAME_SHOT rework, GAME_GK_DIVE, GAME_HEADER_TARGET, SNAP_DEFLECT move guard, GK auto-resolve, D-15/D-24/D-19 fixes, rename'
      contains: 'GAME_GK_DIVE'
  key_links:
    - from: 'GAME_GK_DIVE handler'
      to: 'applyGKDive'
      via: 'pure engine call after team/phase/shape guards'
      pattern: 'applyGKDive'
    - from: 'GK End Turn'
      to: 'applyRoll / shot resolution helper'
      via: 'pre-generated dice injection'
      pattern: 'rollDice'
---

<objective>
Wire the new shot/GK-dive/header-target/snap-deflect flows into the Socket.io handler layer and fix the two handler-layer code-review items (D-15 CR-01 BLOCKER stale-reference leak, D-24 missing snap-back) plus D-19 duplicate HEADER button removal. This plan owns gameHandlers.ts exclusively in Wave 4 (after the engine functions exist in plan 03).

Purpose: The engine functions from plan 03 are inert without handlers that guard team/phase/payload, pre-generate dice, and broadcast state. CR-01 is a release BLOCKER (uncleanable replay interval).
Output: Reworked/new handlers, D-15/D-19/D-24 fixes, engine-pure dice injection; phase10 handler tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-CONTEXT.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md
@.planning/phases/10-remaining-action-flows-tech-debt/10-RESEARCH.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-REVIEW.md
</context>

<artifacts_produced>

## Artifacts this phase produces (NEW symbols — exclude from drift verification)

New handlers: `GAME_GK_DIVE`, `GAME_HEADER_TARGET` socket listeners.
New helper: defender-deflection dice pre-generation (e.g. `preGenerateDefenderDice`) and shot auto-resolution path in GAME_END_TURN GK_DIVING branch.
Reworked handler: `GAME_SHOT` (now accepts PASS phase, transitions state, broadcasts).
Removed: dedicated HEADER resolution path (GAME_HEADER handler and/or its ActionPanel button — D-19).
Uses engine functions from plan 03 (`applyDeclareShot`, `applyGKDive`, `applyDeclareHeaderTarget`) and GamePhase/GameState additions from plan 01.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix D-15 (CR-01 BLOCKER), D-24, D-19 in gameHandlers.ts</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <behavior>
    - D-15: after deleteRoom during the 3s replay hold, no setInterval is ever created; startReplayStream re-fetches via getRoom(room.roomCode) and returns early if the room is gone or gameState is null
    - D-24: every error return in GAME_RESTART_MOVEMENT calls broadcastState(io, room) before returning (client stays in sync)
    - D-19: HEADER resolves via exactly one route; no duplicate Roll Dice + Header buttons; GAME_HEADER handler path removed or excluded
  </behavior>
  <read_first>
    - packages/server/src/gameHandlers.ts (startReplayStream ~lines 143-170; GAME_RESTART_MOVEMENT ~lines 496-513; GAME_HEADER handler ~lines 740-784)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (D-15 fix lines 345-371; broadcastState-on-error lines 623-631)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-REVIEW.md (CR-01, WR-04 detail)
    - .planning/phases/08.2-passing-cleanup-inserted/08.2-VERIFICATION.md (D-24 GAME_RESTART_MOVEMENT anti-pattern line 126)
  </read_first>
  <action>
    In packages/server/src/gameHandlers.ts:
    (1) D-15 (CR-01 BLOCKER): rework startReplayStream's setTimeout callback to re-fetch `const liveRoom = getRoom(room.roomCode)` and `if (!liveRoom || liveRoom.gameState === null) return;` before building frames; use liveRoom for buildReplayFrames, replayTimer assignment, and io.to(...).emit. Use the exact structure in PATTERNS.md lines 354-371 / RESEARCH Example 1.
    (2) D-24: add `broadcastState(io, room)` before every error `return` in the GAME_RESTART_MOVEMENT handler (~lines 496-513).
    (3) D-19 (WR-04): remove the duplicate HEADER resolution route. Per CONTEXT D-19, remove the dedicated GAME_HEADER handler path (lines 740-784) so HEADER resolves only via the established auto-confirm route (Phase 8.2 auto-resolves when both teams confirm). Leave GAME_HEADER event constant if still referenced by types, but ensure no second resolution call to applyRoll for HEADER remains. The ActionPanel button removal is done in plan 05.
  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getRoom(room.roomCode)" packages/server/src/gameHandlers.ts` is non-zero (D-15 re-fetch inside setTimeout)
    - phase10 handler test: startReplayStream after deleteRoom during hold creates no interval passes
    - phase10 handler test: GAME_RESTART_MOVEMENT error path calls broadcastState passes
    - `cd packages/server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>CR-01 stale-reference leak fixed; GAME_RESTART_MOVEMENT snaps back on errors; HEADER has a single resolution route; phase10 handler tests for D-15/D-24 pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rework GAME_SHOT; add GAME_GK_DIVE, GAME_HEADER_TARGET, SNAP_DEFLECT move guard, and GK End Turn auto-resolve</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <behavior>
    - GAME_SHOT: phase===PASS guard; attacker-team guard; HexCoord shape validation; calls applyDeclareShot; on ok transitions to GK_DIVING and broadcasts (unlike the old no-broadcast version); on error emits GAME_ERROR + broadcastState snap-back; isProcessing mutex
    - GAME_GK_DIVE: phase===GK_DIVING guard; controlsGKTeam guard (wrong team → WRONG_TEAM); HexCoord shape validation; calls applyGKDive; broadcasts; isProcessing mutex; snap-back on all errors
    - GAME_MOVE SNAP_DEFLECT branch: active team = defending team; locks to one piece; max 2 hexes; adjacency + occupancy + PITCH_HEXES; broadcasts
    - GAME_END_TURN GK_DIVING branch: controlsGKTeam guard; pre-generates defender deflection dice + shooter/GK/handling/tiebreak dice; calls the engine shot-resolution path; broadcasts full outcome with per-roll event-log entries
    - GAME_HEADER_TARGET: phase===HEADER; both-confirmed guard; attacker (active player) guard; HexCoord shape validation; on-pitch via applyDeclareHeaderTarget; broadcasts
  </behavior>
  <read_first>
    - packages/server/src/gameHandlers.ts (GAME_SHOT handler ~lines 750-785; GAME_SNAPSHOT ~lines 1011-1041; HIGH_PASS_MOVEMENT block in GAME_MOVE ~lines 255-316; HIGH_PASS_MOVEMENT end-turn ~lines 365-426; GAME_HEADER_CONTESTANT ~lines 1108-1183; controlsGKTeam ~lines 120-125; socketTeam/isActivePlayer ~lines 74-108; rollDice usage)
    - packages/server/src/gameEngine.ts (applyDeclareShot, applyGKDive, applyDeclareHeaderTarget, shot-resolution helper from plan 03)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-PATTERNS.md (standard handler skeleton lines 242-293; GK end-turn lines 296-313; SNAP_DEFLECT move guard lines 316-331; GAME_SHOT rework lines 334-343; GAME_HEADER_TARGET guard lines 381-389; shared patterns lines 608-685)
    - .planning/phases/10-remaining-action-flows-tech-debt/10-RESEARCH.md (Security Domain threat table; Dice pre-generation pattern)
    - packages/server/src/__tests__/gameHandlers.phase10.test.ts (red scaffolds for GK dive, SNAP_DEFLECT, header target)
  </read_first>
  <action>
    In packages/server/src/gameHandlers.ts, following the mandatory handler skeleton (isProcessing mutex in try/finally, null-state guard, phase guard, HexCoord shape validation, team guard, engine call, broadcastState on success AND all error returns — PATTERNS.md lines 242-293):
    (1) Rework GAME_SHOT (D-02): change the phase guard from `phase !== 'SHOT'` to `phase !== 'PASS'`; add the attacker-team guard (controlsAttackingTeam, lines 104-108) and HexCoord shape validation for goalHex; call `applyDeclareShot(room.gameState, goalHex)`; on ok set room.gameState=result.state and call broadcastState (the old handler intentionally did NOT broadcast — the reworked one MUST). Snap-back broadcastState on every error.
    (2) Add GAME_GK_DIVE handler: use the exact skeleton in PATTERNS.md lines 244-293 — phase===GK_DIVING, controlsGKTeam guard (WRONG_TEAM), HexCoord shape validation (INVALID_TARGET), call applyGKDive, broadcast, snap-back on error.
    (3) Add a SNAP_DEFLECT branch to GAME_MOVE mirroring the HIGH_PASS_MOVEMENT block but: active team = defending team (opponent of attackingTeam), lock to first moved piece, max 2 hexes, adjacency (hexDistance===1 per step), no occupied hex, PITCH_HEXES boundary, broadcastState (PATTERNS.md lines 316-331).
    (4) Add a GK_DIVING branch to GAME_END_TURN (before the MOVEMENT guard): controlsGKTeam guard; pre-generate ALL dice in the handler (defender deflection dice array via the plan-03 helper, plus shooter/GK/handling dice and the header tiebreak die where applicable) using rollDice() — never in the engine (ARCH-01); call the engine shot-resolution path with injected dice; broadcast the resolved state. Ensure every individual roll is recorded in the event log for replay correctness (D-03 "all intermediate rolls logged").
    (5) Add GAME_HEADER_TARGET handler: phase===HEADER, both headerConfirmed.home && .away guard (HEADER_NOT_CONFIRMED), isActivePlayer/attacker guard, HexCoord shape validation, call applyDeclareHeaderTarget, broadcast, snap-back on error.
    Register all new socket.on handlers where the existing handlers are registered.
  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/__tests__/gameHandlers.phase10.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ClientEvents.GAME_GK_DIVE\|ClientEvents.GAME_HEADER_TARGET" packages/server/src/gameHandlers.ts` returns 2 or more
    - `grep -c "controlsGKTeam" packages/server/src/gameHandlers.ts` increased (used in GAME_GK_DIVE and GK end-turn)
    - phase10 handler test: GAME_GK_DIVE from wrong team returns GAME_ERROR 'WRONG_TEAM' with broadcastState snap-back passes
    - phase10 handler test: GAME_SHOT from PASS phase transitions to GK_DIVING and broadcasts passes
    - phase10 handler test: SNAP_DEFLECT GAME_MOVE rejects a 3rd-hex move (max 2) passes
    - phase10 handler test: GAME_HEADER_TARGET before both-confirmed emits HEADER_NOT_CONFIRMED passes
    - every new handler error path calls broadcastState (grep each handler block shows broadcastState before error returns)
    - `cd packages/server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GAME_SHOT reworked to declare from ACTION; GAME_GK_DIVE, GAME_HEADER_TARGET, SNAP_DEFLECT move guard, and GK End Turn auto-resolve all implemented with isProcessing + team + shape guards and snap-back; dice pre-generated in handler; phase10 handler tests green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                     |
| -------------------- | ----------------------------------------------------------------------------------------------- |
| client→server socket | All new GAME\_\* payloads (goalHex, GK dive hex, header target hex) cross from untrusted client |

## STRIDE Threat Register

| Threat ID | Category          | Component                                     | Disposition | Mitigation Plan                                                                                                       |
| --------- | ----------------- | --------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-10-08   | Spoofing          | wrong team submits GK dive hex                | mitigate    | controlsGKTeam(socket, room) guard before applyGKDive; WRONG_TEAM error + snap-back                                   |
| T-10-09   | Tampering         | off-pitch / malformed hex payloads            | mitigate    | typeof q/r === 'number' + null/object checks on every new HexCoord handler (ASVS V5); engine re-validates PITCH_HEXES |
| T-10-10   | Tampering         | client-submitted dice/outcomes                | mitigate    | server pre-generates all dice via rollDice() (crypto.randomInt); engine is pure; client sends no dice values          |
| T-10-11   | Denial of Service | double-click shot/dive race                   | mitigate    | isProcessing mutex in try/finally on every new handler; duplicate dropped silently                                    |
| T-10-12   | Tampering         | attacker selects header target for wrong goal | mitigate    | applyDeclareHeaderTarget validates goal-line side for attackingTeam; handler enforces attacker guard                  |
| T-10-SC   | Tampering         | npm installs                                  | accept      | No new packages this phase                                                                                            |

</threat_model>

<verification>
- `cd packages/server && npx tsc --noEmit` exits 0
- `cd packages/server && npx vitest run src/__tests__/gameHandlers.phase10.test.ts` exits 0
- `cd packages/server && npx vitest run src/__tests__/gameHandlers.test.ts src/__tests__/replay.integration.test.ts` exits 0 (no regressions; CR-01 fix verified against replay tests)
</verification>

<success_criteria>

- GAME_SHOT declares from PASS and broadcasts; GAME_GK_DIVE + GAME_HEADER_TARGET + SNAP_DEFLECT wired with full guards and snap-back
- GK End Turn pre-generates all dice and auto-resolves with logged rolls
- CR-01 BLOCKER fixed; GAME_RESTART_MOVEMENT snap-back added; single HEADER resolution route
  </success_criteria>

<output>
Create `.planning/phases/10-remaining-action-flows-tech-debt/10-04-SUMMARY.md` when done
</output>
