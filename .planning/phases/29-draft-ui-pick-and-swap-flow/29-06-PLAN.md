---
phase: 29-draft-ui-pick-and-swap-flow
plan: 06
type: execute
wave: 4
depends_on: ['29-04', '29-05']
files_modified: []
autonomous: false
requirements: [DRAFT-06, DRAFT-07, DRAFT-08, DRAFT-09, DRAFT-10]

must_haves:
  truths:
    - 'A draft screen appears between formation selection and lineup, showing a 7-card carousel above the lineup grid (DRAFT-06)'
    - 'Two players complete 4 pick-and-swap cycles (1+2+1) delivering 16 cards each in real time (DRAFT-07)'
    - 'Cycle-4 keeper safety auto-selects a keeper for a keeperless player, who then picks 1 (not 2) in the following pick phase (DRAFT-08)'
    - 'Drafted players not in the starting 11 appear on a dynamically sized bench using the draft card style (DRAFT-09)'
    - 'On completion the carousel disappears, starters carry slot numbers, bench cards carry random 15-99 numbers, and team badge/colors are applied (DRAFT-10, D-15/D-23)'
  artifacts:
    - path: 'packages/client/src/components/DraftPackCarousel.tsx'
      provides: 'draft carousel (built in Plan 03, exercised here)'
    - path: 'packages/server/src/draftSession.ts'
      provides: 'draft state machine (built in Plan 02, exercised here)'
  key_links:
    - from: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      to: 'packages/server/src/roomHandlers.ts'
      via: 'DRAFT_PICK emit → server handler → DRAFT_STATE_UPDATED unicast → client re-render'
      pattern: 'DRAFT_PICK|DRAFT_STATE_UPDATED'
---

<objective>
Final full-stack verification of the draft mode built across Plans 01-05. Confirm the automated suite is fully green, then run a real two-browser draft session to visually and functionally verify the interaction contract — the pick-and-swap sequencing (including the A1 open question about the 2-card PICK2 gating granularity), tier-bordered carousel, dynamic bench, keeper safety, reconnect resume, and the hand-off to the existing lineup-confirm flow.

Purpose: This is a heavily visual/interactive phase (UI hint = yes); automated tests cannot fully verify drag feel, tier colors, waiting-state clarity, or the intended cycle cadence. A human confirms the experience before the phase is marked complete.
Output: human sign-off (or a punch list of gap-closure items).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-UI-SPEC.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-RESEARCH.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-04-SUMMARY.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-05-SUMMARY.md
</context>

## Artifacts this phase produces

This plan produces no new source symbols — it verifies the artifacts built by Plans 01-05. It may produce a `29-VERIFICATION.md` gap list if the human reports issues.

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gate — full suite + build green</name>
  <files></files>
  <read_first>
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-04-SUMMARY.md (server-side outcomes to confirm)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-05-SUMMARY.md (client-side outcomes to confirm)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-VALIDATION.md (phase-gate row — full suite must be green before verification)
  </read_first>
  <action>
    Run the whole-monorepo verification gate so the human-verify step starts from a known-green baseline: run the full test suite and typecheck and production build. If anything fails, stop and fix (or route back to the owning plan) before proceeding to the human checkpoint — do not present a red build for manual verification.
  </action>
  <verify>
    <automated>pnpm -r typecheck && pnpm -r test && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -r typecheck` exits 0.
    - `pnpm -r test` exits 0 (all server + client suites green, including draftSession unit/integration, draftReconnect, DraftPackCarousel, and LineupAssignmentScreen tests).
    - `pnpm -r build` exits 0 (client + server + shared build cleanly).
  </acceptance_criteria>
  <done>The full automated suite and build are green; ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Two-browser end-to-end draft verification (human)</name>
  <files></files>
  <action>
    Pause for human verification. Present the two-browser walkthrough below and wait for the resume signal. This is a blocking checkpoint — do not auto-advance. If the human reports issues, capture them as a `29-VERIFICATION.md` gap list for a `/gsd-plan-phase --gaps` cycle instead of marking the phase complete.
  </action>
  <what-built>
    A complete real-time draft mode: a settings-driven draft-pack carousel overlaid on the lineup screen, a server-authoritative 4-cycle pick-and-swap state machine, keeper safety, a dynamic bench, reconnect resume, and post-draft numbering — all built and unit/integration/component tested across Plans 01-05.
  </what-built>
  <how-to-verify>
    Run two browsers against a local dev server and play a full draft:

    1. Start the app (server + client dev). In browser A create a room; on the settings pre-step choose team type = Draft and at least one pool (e.g. Original), confirm. In browser B join with the room code. Proceed through team/uniform/formation selection for both players.
    2. DRAFT-06: Confirm the draft screen appears between formation selection and the normal lineup, showing a left-right navigable carousel of 7 cards ABOVE an empty role-labeled formation grid. Verify each card shows a 3px tier-colored border — Chase gold, Rare silver, Uncommon bronze, Common blue, Keeper green — and that cards sort rarest-first with the view starting at the leftmost card.
    3. DRAFT-07 (cadence): In cycle 1, drag 1 card off the pack onto a lineup slot or bench (pick 1). Confirm the pack row disables with "Waiting for … Player to pick…" until the opponent also picks, then packs swap and you pick 2, then packs swap back and you pick 1. Confirm the "Cycle n of 4 · Pick k of {2 or 1}" counter tracks correctly. Repeat for 4 cycles and confirm each player ends with 16 cards total across lineup + bench. Specifically confirm the intended granularity of the 2-card PICK2 step (research assumption A1: you may make both of your two picks freely without waiting on the opponent between them; the "waiting" indicator appears only once, after your second pick). Flag if you expected per-card waiting instead.
    4. DRAFT-08 (keeper safety): Play a draft where one player deliberately never drafts a keeper until cycle 4. Confirm that on cycle 4 a keeper is auto-selected for them (brief "Keeper auto-selected — cycle 4 safety net." banner), it lands in the empty GK slot, and that player then picks only 1 card (not 2) in the following pick phase. Confirm the pack size reflects the removed keeper (a smaller pack renders correctly).
    5. DRAFT-09 (bench): Draft more than 11 usable cards and confirm overflow cards appear on a dynamically sized bench below the grid using the same card style; confirm you can drag cards between lineup and bench in both directions, and that dropping onto an occupied slot bumps the occupant to the bench.
    6. GK rule (D-09): Try dropping a non-GK card on the GK slot and a GK card on an outfield slot — confirm both are rejected with the correct message and the card snaps back.
    7. D-13 (reconnect): Mid-draft, refresh one browser tab; confirm it resumes exactly where it left off (same pack contents, same drafted lineup/bench) within the grace window.
    8. DRAFT-10 / D-23: Complete the draft; confirm the carousel row disappears, starters carry their formation slot numbers, bench cards carry numbers in the 15-99 range, team badge/colors are applied to all cards, and the normal Confirm button + lineup-confirm flow finalizes the match unchanged.

  </how-to-verify>
  <verify>
    <human-check>Human confirms all 8 walkthrough steps behave as specified, or provides a specific discrepancy list.</human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms all 8 steps behave as described, OR provides a specific punch list of discrepancies (which becomes a `29-VERIFICATION.md` gap list for `/gsd-plan-phase --gaps`).
    - The A1 PICK2-gating cadence is explicitly confirmed acceptable or flagged for change.
  </acceptance_criteria>
  <resume-signal>Type "approved" to complete the phase, or describe the issues to open a gap-closure cycle.</resume-signal>
  <done>Human has signed off on the end-to-end draft experience, or a gap list has been captured.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                                                       |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| n/a      | Verification-only plan; introduces no new code or trust boundary. Server-side controls (T-29-01..T-29-06) were established and tested in Plan 04. |

## STRIDE Threat Register

| Threat ID   | Category                 | Component        | Disposition | Mitigation Plan                                                                                                                                                                                                 |
| ----------- | ------------------------ | ---------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-29-VERIFY | (verification)           | full draft flow  | accept      | This plan asserts, via a real two-player session, that the mitigations from Plans 02/04 hold end-to-end (per-socket privacy, server-authoritative picks, CSPRNG numbering). No new code, no new threat surface. |
| T-29-SC     | Tampering (supply chain) | package installs | accept      | No npm packages installed this plan.                                                                                                                                                                            |

</threat_model>

<verification>
- `pnpm -r typecheck`, `pnpm -r test`, `pnpm -r build` all green (Task 1).
- Human sign-off on the 8-step two-browser walkthrough (Task 2).
</verification>

<success_criteria>

- The full automated suite and build pass.
- A human confirms the end-to-end draft experience meets all five success criteria and the UI-SPEC contract, or a precise gap list is captured for a follow-up gap-closure cycle.
  </success_criteria>

<output>
Create `.planning/phases/29-draft-ui-pick-and-swap-flow/29-06-SUMMARY.md` when done. If the human reports issues, also capture `.planning/phases/29-draft-ui-pick-and-swap-flow/29-VERIFICATION.md` with the gap list.
</output>
