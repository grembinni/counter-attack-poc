---
phase: 29-draft-ui-pick-and-swap-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/server/src/roomStore.ts
autonomous: true
requirements: [DRAFT-06, DRAFT-07, DRAFT-08, DRAFT-09, DRAFT-10]

must_haves:
  truths:
    - 'The shared package exports draft session types (DraftSubStep, DraftSession, DraftClientView, DraftDestination, DraftSlotRef) consumed identically by server and client'
    - 'Three new draft socket events exist in the typed event maps: DRAFT_PICK, DRAFT_REARRANGE (client to server), DRAFT_STATE_UPDATED (server to client)'
    - 'The Room type carries an optional draftSession field so a draft-mode room can hold live session state'
    - "DRAFT_STATE_UPDATED payload (DraftClientView) contains only the receiving player's pack — no field can leak the opponent's pack contents (D-14 privacy at the type level)"
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'DraftSubStep, DraftSession, DraftClientView, DraftDestination, DraftSlotRef, DraftPickPayload, DraftRearrangePayload types'
      contains: 'DraftSession'
    - path: 'packages/shared/src/events.ts'
      provides: 'DRAFT_PICK / DRAFT_REARRANGE client events, DRAFT_STATE_UPDATED server event, and their typed payload signatures'
      contains: 'DRAFT_STATE_UPDATED'
    - path: 'packages/server/src/roomStore.ts'
      provides: 'Room.draftSession optional field'
      contains: 'draftSession'
  key_links:
    - from: 'packages/shared/src/events.ts'
      to: 'packages/shared/src/types.ts'
      via: 'import of DraftClientView / DraftDestination / DraftSlotRef into ClientToServerEvents and ServerToClientEvents'
      pattern: 'DraftClientView'
    - from: 'packages/server/src/roomStore.ts'
      to: 'packages/shared/src/types.ts'
      via: 'import type { DraftSession }'
      pattern: 'DraftSession'
---

<objective>
Establish the shared type + event contract that every other Phase 29 plan depends on: the draft session state shape, the client-facing draft view, the drag destination/source descriptors, and the three new Socket.io events. This is the interface-first layer — it lets the server state machine (Plan 02), server wiring (Plan 04), client carousel (Plan 03), and client screen (Plan 05) all compile against a single source of truth instead of exploring the codebase for contracts.

Purpose: Prevent the "scavenger hunt" anti-pattern. Downstream executors receive exact type names, field names, and event signatures.
Output: New exported types in `packages/shared/src/types.ts`, three new events in `packages/shared/src/events.ts`, and the `Room.draftSession` field in `packages/server/src/roomStore.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-RESEARCH.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-PATTERNS.md
@packages/shared/src/events.ts
@packages/shared/src/types.ts
@packages/shared/src/draftEngine.ts
@packages/server/src/roomStore.ts
</context>

## Artifacts this phase produces

Symbols created by THIS plan (new — exclude from drift/source-grounding checks):

- `packages/shared/src/types.ts`: `DraftSubStep` (union `'PICK1' | 'PICK2' | 'PICK3'`), `DraftSession` (server session state), `DraftClientView` (per-player view payload), `DraftDestination` (`{ type: 'slot'; slotIndex: number } | { type: 'bench' }`), `DraftSlotRef` (`{ type: 'slot'; slotIndex: number } | { type: 'bench'; benchIndex: number }`), `DraftPickPayload`, `DraftRearrangePayload`
- `packages/shared/src/events.ts`: `ClientEvents.DRAFT_PICK` (`'draft:pick'`), `ClientEvents.DRAFT_REARRANGE` (`'draft:rearrange'`), `ServerEvents.DRAFT_STATE_UPDATED` (`'draft:state-updated'`), plus their entries in `ClientToServerEvents` / `ServerToClientEvents`
- `packages/server/src/roomStore.ts`: `Room.draftSession?: DraftSession | null` field

<tasks>

<task type="auto">
  <name>Task 1: Add draft session + client-view types to the shared package</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts (lines 445-501 — the existing DRAFT-01..05 type block: TeamType, DraftPoolId, DraftTier, PACKS_PER_MATCH, PACK_COMPOSITION — add the new types immediately after this block, following its comment/export style)
    - packages/shared/src/draftEngine.ts (lines 33-40, 159-166 — the exact shape of TieredPoolPlayer and DraftPack, which the new types reference)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md (D-01, D-04, D-14, D-15 — cycle/sub-step model, per-player privacy, bench numbering)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-RESEARCH.md (Code Examples — "Room state extension (recommended shape)" and "DRAFT_PICK event contract")
  </read_first>
  <action>
    Add a new exported type block after the existing PACK_COMPOSITION declaration (~line 501). Import/reference `TieredPoolPlayer` and `DraftPack` from `./draftEngine.js` via `import type` at the top of types.ts (verify no circular-import problem — draftEngine.ts already imports from types.ts for DraftPoolId/DraftTier, so types.ts must reference draftEngine's TieredPoolPlayer/DraftPack ONLY as a type import; if tsc reports a cycle, instead define `DraftSession`/`DraftClientView` in draftEngine.ts and re-export from types.ts, keeping the public import site `@counter-attack/shared` unchanged).

    Declare these types with the naming and doc-comment conventions used by the surrounding DRAFT-04/05 block (cite DRAFT-06/07/08/09/10 and the D-numbers in comments):

    - `DraftSubStep = 'PICK1' | 'PICK2' | 'PICK3'` — the three player-facing picking sub-steps within one cycle (D-01). SWAP / SWAP_BACK / NEW_PACK are auto transitions, not sub-steps.
    - `DraftDestination` — discriminated union `{ type: 'slot'; slotIndex: number } | { type: 'bench' }`. The drop target of a pick (D-05): a lineup slot (0..10) or the bench (append).
    - `DraftSlotRef` — `{ type: 'slot'; slotIndex: number } | { type: 'bench'; benchIndex: number }`. Addresses an already-placed drafted card for rearrangement (D-08).
    - `DraftPickPayload = { cardId: string; destination: DraftDestination }` (DRAFT_PICK client payload).
    - `DraftRearrangePayload = { from: DraftSlotRef; to: DraftSlotRef }` (DRAFT_REARRANGE client payload, D-08/D-10 — never advances cycle state).
    - `DraftSession` — the full server-authoritative session object per RESEARCH "Room state extension" shape, but with the picks-remaining refinement: fields `cycle: number` (0 before first pack open, 1..4 during), `subStep: DraftSubStep`, `draftPacks: DraftPack[]` (the 8 generated packs), `homePackOrder: number[]` / `awayPackOrder: number[]` (length-4 index lists into draftPacks, sequential open order, D-04), `homeCurrentPack: TieredPoolPlayer[]` / `awayCurrentPack: TieredPoolPlayer[]`, `homeDraftedIds: string[]` / `awayDraftedIds: string[]`, `homeHasKeeper: boolean` / `awayHasKeeper: boolean` (DRAFT-08 tracking), `homePicksRemaining: number` / `awayPicksRemaining: number` (picks left in current sub-step for that player — PICK1=1, PICK2=2, PICK3=1, reduced to 1 by keeper-safety), `homeLineupSlots: (string | null)[]` / `awayLineupSlots: (string | null)[]` (11 entries, null = empty formation slot, D-11/D-22), `homeBenchIds: string[]` / `awayBenchIds: string[]` (dynamic length, D-09), `homeBenchNumbers: Record<string, number>` / `awayBenchNumbers: Record<string, number>` (filled at draft-complete, D-15), `keeperAutoPickedThisCycle: { home: boolean; away: boolean }` (drives the DRAFT-08 banner), `draftComplete: boolean`.
    - `DraftClientView` — the per-socket private payload for DRAFT_STATE_UPDATED. Fields: `cycle: number`, `subStep: DraftSubStep`, `currentPack: TieredPoolPlayer[]` (THIS player's pack only — comment: never the opponent's, D-14), `picksRemaining: number`, `waitingForOpponent: boolean`, `lineupSlots: (string | null)[]`, `benchIds: string[]`, `benchNumbers: Record<string, number>`, `keeperAutoPickedThisCycle: boolean`, `draftComplete: boolean`. Do NOT include any home/away pair or opponent pack field on this type — the privacy boundary is enforced by the type shape.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/shared typecheck` exits 0 with the new types present.
    - `packages/shared/src/types.ts` exports each of: `DraftSubStep`, `DraftSession`, `DraftClientView`, `DraftDestination`, `DraftSlotRef`, `DraftPickPayload`, `DraftRearrangePayload` (grep confirms each identifier appears in an `export` position).
    - `DraftClientView` has a `currentPack` field of type `TieredPoolPlayer[]` and NO field named `homeCurrentPack`, `awayCurrentPack`, or `opponentPack` (grep -v '^\s*//' then grep confirms absence of opponent-pack identifiers on the DraftClientView declaration).
    - `DraftSession` declares `homePicksRemaining` and `awayPicksRemaining` numeric fields.
  </acceptance_criteria>
  <done>Shared draft types compile and are exported; DraftClientView is opponent-pack-free by construction.</done>
</task>

<task type="auto">
  <name>Task 2: Add the three draft events and the Room.draftSession field</name>
  <files>packages/shared/src/events.ts, packages/server/src/roomStore.ts</files>
  <read_first>
    - packages/shared/src/events.ts (lines 1-3 imports; lines 73-84 LINEUP_SWAP/LINEUP_CONFIRM const entries; lines 204-215 ClientToServerEvents LINEUP_SWAP signature; lines 262-268 ServerToClientEvents LINEUP_ASSIGNMENT_UPDATED signature — mirror these exactly)
    - packages/server/src/roomStore.ts (lines 14-22 shared type imports; lines 51-162 the Room type — add the new field alongside homeAssignment/awayAssignment/homeLineupConfirmed, following the existing JSDoc-per-field convention)
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-PATTERNS.md (events.ts section — the verb:noun naming, past-tense server->client convention)
  </read_first>
  <action>
    In `packages/shared/src/events.ts`:
    - Add to `ClientEvents`: `DRAFT_PICK: 'draft:pick'` and `DRAFT_REARRANGE: 'draft:rearrange'` (place after `LINEUP_CONFIRM`, with a JSDoc citing DRAFT-06/07/D-05/D-14 for DRAFT_PICK and D-08/D-10 for DRAFT_REARRANGE).
    - Add to `ServerEvents`: `DRAFT_STATE_UPDATED: 'draft:state-updated'` (after `LINEUP_ASSIGNMENT_UPDATED`, JSDoc citing D-14 unicast privacy).
    - Import `DraftClientView`, `DraftPickPayload`, `DraftRearrangePayload` from `./types.js` at the top (they are already re-exported from the shared entry — verify the type-import line).
    - Add to `ClientToServerEvents`: `[ClientEvents.DRAFT_PICK]: (payload: DraftPickPayload) => void;` and `[ClientEvents.DRAFT_REARRANGE]: (payload: DraftRearrangePayload) => void;`. Mirror the LINEUP_SWAP object-payload style.
    - Add to `ServerToClientEvents`: `[ServerEvents.DRAFT_STATE_UPDATED]: (view: DraftClientView) => void;` — comment that this is emitted per-socket (never `io.to(room).emit`), mirroring LINEUP_ASSIGNMENT_UPDATED privacy.

    In `packages/server/src/roomStore.ts`:
    - Extend the shared `import type { ... }` (lines 15-21) to also import `DraftSession`.
    - Add `draftSession?: DraftSession | null;` to the `Room` type (after the `awayLineupConfirmed` field, ~line 161), with a JSDoc: "Phase 29 (DRAFT-06..10): live draft session for teamType==='draft' rooms. undefined/null until ROOM_SETTINGS_CONFIRM bootstraps it. Holds cycle/sub-step, per-player packs, drafted ids, lineup/bench state (D-04/D-13)."
    - Do NOT initialize the field in `createRoom` (it stays undefined until the settings handler in Plan 04 populates it) — matches the existing pattern where homeAssignment etc. are set later.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared typecheck && pnpm --filter @counter-attack/server typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Both shared and server typecheck exit 0.
    - `grep` confirms `DRAFT_PICK: 'draft:pick'`, `DRAFT_REARRANGE: 'draft:rearrange'`, and `DRAFT_STATE_UPDATED: 'draft:state-updated'` all present in events.ts.
    - `ClientToServerEvents` contains signatures keyed by `ClientEvents.DRAFT_PICK` and `ClientEvents.DRAFT_REARRANGE`; `ServerToClientEvents` contains a signature keyed by `ServerEvents.DRAFT_STATE_UPDATED`.
    - `packages/server/src/roomStore.ts` `Room` type contains `draftSession?` and roomStore imports `DraftSession` from the shared package.
  </acceptance_criteria>
  <done>The full draft event/type contract compiles across shared and server; Room can hold a draftSession.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                                                                  |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| shared type contract → server & client | Types define the wire shape of draft events; a leaky payload type could institutionalize an information-disclosure bug across every consumer |

## STRIDE Threat Register

| Threat ID | Category                 | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                           |
| --------- | ------------------------ | ------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-29-PRIV | Information Disclosure   | `DraftClientView` payload type | mitigate    | `DraftClientView` carries only `currentPack` (the receiving player's pack) and no home/away or opponent-pack field — the privacy boundary (D-14) is enforced structurally so no server emit can accidentally serialize the opponent's pack via this type. Runtime per-socket emit is enforced in Plan 04. |
| T-29-SC   | Tampering (supply chain) | package installs               | accept      | This plan installs no npm packages (RESEARCH "Installation: None required"); no supply-chain surface introduced.                                                                                                                                                                                          |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared typecheck` and `pnpm --filter @counter-attack/server typecheck` both green.
- `pnpm -r build` still succeeds (no consumer broken by the additive changes).
</verification>

<success_criteria>

- All draft session and view types exist and are exported from `@counter-attack/shared`.
- DRAFT_PICK, DRAFT_REARRANGE, DRAFT_STATE_UPDATED are wired into the typed event maps.
- Room carries an optional draftSession field.
- No existing type/event was removed or renamed (purely additive; Standard-mode flow untouched).
  </success_criteria>

<output>
Create `.planning/phases/29-draft-ui-pick-and-swap-flow/29-01-SUMMARY.md` when done.
</output>
