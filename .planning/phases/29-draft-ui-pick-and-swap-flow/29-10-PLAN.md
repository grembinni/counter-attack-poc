---
phase: 29-draft-ui-pick-and-swap-flow
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/draftSession.ts
  - packages/server/src/draftSession.test.ts
  - packages/server/src/__tests__/draftSession.integration.test.ts
  - .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md
  - .planning/phases/29-draft-ui-pick-and-swap-flow/29-RESEARCH.md
  - .planning/phases/29-draft-ui-pick-and-swap-flow/29-UI-SPEC.md
autonomous: true
requirements: [DRAFT-09, DRAFT-10]
gap_closure: true

must_haves:
  truths:
    - 'Dragging a filled lineup slot onto another filled lineup slot swaps the two cards — both stay in the lineup, neither is sent to the bench (D-24).'
    - 'Dragging a filled lineup slot onto the bench still moves that card to the bench and clears the source slot (unchanged).'
    - 'Dragging a bench card onto a filled lineup slot still displaces the slot occupant to the bench (D-07, unchanged — no source slot to swap into).'
    - 'The GK-slot role rule (D-09) still rejects illegal slot↔slot swaps in BOTH directions (non-GK into GK slot, and GK out into an outfield slot).'
    - "applyPick's occupied-slot displacement to the bench (D-07) is unchanged — a freshly drafted pack card has no vacated source slot."
  artifacts:
    - path: 'packages/server/src/draftSession.ts'
      provides: 'applyRearrange slot↔slot true-swap branch'
      contains: "from.type === 'slot'"
    - path: 'packages/server/src/draftSession.test.ts'
      provides: 'Unit tests for true swap + slot→slot-onto-empty; existing slot→bench / bench→slot-occupied cases kept green'
      contains: 'applyRearrange'
    - path: 'packages/server/src/__tests__/draftSession.integration.test.ts'
      provides: 'Integration test locking D-09 GK-slot enforcement on both ends of a slot↔slot swap'
      contains: 'GK_SLOT_REQUIRES_GK'
    - path: '.planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md'
      provides: 'Narrowed D-07 + new D-24 slot↔slot swap decision'
      contains: 'D-24'
  key_links:
    - from: 'packages/server/src/draftSession.ts applyRearrange'
      to: 'lineupSlots[from.slotIndex]'
      via: "displaced-occupant assignment when from.type === 'slot'"
      pattern: "from\\.type === 'slot'"
    - from: 'packages/server/src/__tests__/draftSession.integration.test.ts'
      to: 'roomHandlers.ts DRAFT_REARRANGE GK-slot guard'
      via: 'illegal slot↔slot DRAFT_REARRANGE emit → GAME_ERROR assertion'
      pattern: 'NON_GK_SLOT_REJECTS_GK'
---

<objective>
Fix the one remaining Phase 29 critical gap (29-VERIFICATION.md Gap 1): post-draft lineup-slot → lineup-slot rearrangement currently sends the displaced occupant to the bench instead of trading places. Make `applyRearrange` perform a true two-way swap when both `from` and `to` are lineup slots, while preserving the existing D-07 bench-displacement behavior everywhere it is still correct (drafting onto an occupied slot via `applyPick`, and bench→slot rearrangement). Narrow the D-07 decision to non-swap moves and record the new slot↔slot swap rule as D-24 so the behavior is not re-introduced.

Purpose: Restore the natural, human-expected semantics for a same-lineup drag (a trade), closing the DRAFT-09/DRAFT-10 partial and clearing the last blocker before Phase 29 can be marked complete.
Output: A corrected pure `applyRearrange`, unit + integration test coverage locking the swap and the D-09 both-ends role rule, and updated decision docs (D-07 narrowed, D-24 added).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-VERIFICATION.md
@.planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md
@packages/server/src/draftSession.ts
@packages/server/src/draftSession.test.ts
@packages/server/src/__tests__/draftSession.integration.test.ts
</context>

<artifacts_produced>

## Artifacts this phase produces

This is a gap-closure plan that NARROWS the behavior of an existing exported function; it introduces no new exported code symbols. The source-grounding / drift pass should treat the following as newly introduced by this plan (not drift against prior source):

- **`D-24`** — new decision ID added to `29-CONTEXT.md` `<decisions>` block: "slot↔slot rearrangement is a true two-way swap."
- **New unit test cases** in `packages/server/src/draftSession.test.ts` inside the existing `describe('applyRearrange (D-08/D-10)')` block: a slot↔slot true-swap case and a slot↔slot-onto-empty-destination case.
- **New integration test case** in `packages/server/src/__tests__/draftSession.integration.test.ts`: D-09 GK-slot enforcement on both ends of a slot↔slot swap, plus a legal outfield swap positive control.

Unchanged (NOT new): `applyRearrange`'s exported signature `(session, side, from, to) => { session, ok, error? }` is identical — only the internal `to.type === 'slot'` displacement branch changes. `applyPick` is untouched. `roomHandlers.ts` DRAFT_REARRANGE handler is read-only reference (no code change).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make applyRearrange perform a true slot↔slot swap</name>
  <files>packages/server/src/draftSession.ts, packages/server/src/draftSession.test.ts</files>
  <read_first>
    - packages/server/src/draftSession.ts — read `applyRearrange` (lines ~258-299) and `applyPick` (lines ~208-250). Note line ~276 already sets `lineupSlots[from.slotIndex] = null` when `from.type === 'slot'`; the fix reuses that just-vacated slot. Note `applyPick`'s occupied-slot branch (~lines 228-238) must NOT change.
    - packages/server/src/draftSession.test.ts — read `describe('applyRearrange (D-08/D-10)')` (lines ~226-284). The slot→bench case (line ~227) and the bench→slot-occupied displacement case (line ~252) ALREADY exist and assert unchanged behavior; do not modify them. The two new cases are added alongside them.
    - packages/shared/src/types.ts lines 517-541 — `DraftDestination`, `DraftSlotRef` (`{type:'slot';slotIndex}` | `{type:'bench';benchIndex}`), `DraftRearrangePayload` shapes.
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-VERIFICATION.md — "Gaps Summary" Gap 1 and "Recommended Fix Plans" 29-10 task 1 for the exact intended semantics.
  </read_first>
  <behavior>
    - New test (true swap): with `homeLineupSlots[3] = 'card-a'` and `homeLineupSlots[7] = 'card-b'`, `applyRearrange(session,'home',{type:'slot',slotIndex:3},{type:'slot',slotIndex:7})` returns ok=true with `lineupSlots[7] === 'card-a'`, `lineupSlots[3] === 'card-b'`, and `homeBenchIds` deep-equal to its pre-call value (no card sent to bench, none lost, none duplicated).
    - New test (slot→slot onto EMPTY destination): source slot occupied, destination slot null → plain move: destination slot receives the card, source slot becomes null, bench unchanged.
    - Existing test kept green (slot→bench, line ~227): card moves to bench, source slot null, cycle/subStep/picksRemaining untouched.
    - Existing test kept green (bench→slot-occupied, line ~252): destination occupant still displaced to bench; the moved bench card takes the slot.
  </behavior>
  <action>
    In `packages/server/src/draftSession.ts`, modify only the `to.type === 'slot'` displacement branch of `applyRearrange` (~lines 286-292). Keep the `cardId` resolution and the `lineupSlots[from.slotIndex] = null` clearing (~line 276) exactly as they are. Inside the `if (displaced)` guard, branch on the ORIGIN: when `from.type === 'slot'`, assign `lineupSlots[from.slotIndex] = displaced` — returning the displaced occupant into the source slot that `cardId` just vacated, i.e. a genuine two-way swap; when `from.type === 'bench'`, keep the current `benchIds = [...benchIds, displaced]` behavior because there is no source lineup slot to return the occupant to. Leave `lineupSlots[to.slotIndex] = cardId` and the `else { benchIds = [...benchIds, cardId] }` bench-destination branch unchanged. Do NOT touch `applyPick` — a freshly drafted pack card has no vacated source slot, so its occupied-slot bench-displacement (~lines 231-234, the `newBenchIds = [...current.benchIds, occupant]` line) must remain exactly as today. Replace the inline `// D-07: displace the destination slot's current occupant to the bench.` comment (~line 289) with a comment that cites D-24 for the `from.type === 'slot'` swap path and D-07 for the `from.type === 'bench'` displacement path. Then in `packages/server/src/draftSession.test.ts`, inside the existing `describe('applyRearrange (D-08/D-10)')` block, ADD the two new `it(...)` cases from the behavior list; reuse the file's `baseSession(overrides)` fixture (constructs a valid `DraftSession` with 11 null slots) and pre-fill `homeLineupSlots` via the overrides. Do not edit the existing slot→bench or bench→slot-occupied cases.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test draftSession.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `applyRearrange` in draftSession.ts contains a branch keyed on `from.type === 'slot'` that assigns the displaced occupant into `lineupSlots[from.slotIndex]`, distinct from the `benchIds` append used when `from.type === 'bench'`.
    - `applyPick` in draftSession.ts is unchanged: its occupied-slot branch still appends the prior occupant to `benchIds` (grep confirms `newBenchIds = [...current.benchIds, occupant]` still present).
    - The new swap test asserts `lineupSlots[B]` equals the card originally at A, `lineupSlots[A]` equals the card originally at B, and `homeBenchIds` is deep-equal (`toEqual`) to its pre-call value.
    - `pnpm --filter @counter-attack/server test draftSession.test.ts` exits 0 with the two new tests passing AND the pre-existing slot→bench (~line 227) and bench→slot-occupied (~line 252) tests still green.
    - `pnpm --filter @counter-attack/server typecheck` exits 0.
  </acceptance_criteria>
  <done>True slot↔slot swaps preserve both cards in the lineup; every other rearrange path keeps its prior behavior; unit suite green and typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Lock D-09 GK-slot enforcement on both ends of a slot↔slot swap (integration)</name>
  <files>packages/server/src/__tests__/draftSession.integration.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/draftSession.integration.test.ts — read the whole file. Reuse `setupThroughDraftUniformConfirm`, `makeDraftDriver`, `driveDraftToCompletionFillingLineups` (~line 592), `SLOT_ROLES` (~line 564, `FORMATIONS['4-4-2']` → slot 0 = GK, slot 1 = non-GK). Use the existing GK-rule DRAFT_PICK test (~line 442) as the GAME_ERROR assertion-shape template and the "post-draft rearrangement" describe (~line 619) as the completed-draft setup template.
    - packages/server/src/roomHandlers.ts lines 902-993 — DRAFT_REARRANGE handler. The GK-slot role check (~lines 954-979) validates the MOVING card against the DESTINATION slot role and is the enforcement point that rejects illegal swaps with `GK_SLOT_REQUIRES_GK` / `NON_GK_SLOT_REJECTS_GK`. Because the GK slot only ever holds a GK and outfield slots only ever hold non-GKs, this destination-only check already rejects illegal swaps on BOTH ends; this test locks that guarantee in. No handler code change is required or permitted in this task.
  </read_first>
  <action>
    In `packages/server/src/__tests__/draftSession.integration.test.ts`, add a new `it(...)` (in a new `describe('Phase 29 Plan 10 — slot↔slot swap GK-slot enforcement', ...)` block, or under the existing Plan 07 Task 1 post-draft-rearrangement describe). Drive a full draft to completion with filled lineups via `driveDraftToCompletionFillingLineups(clientA, clientB, viewA, viewB)`; assert `driver.getViewA().lineupSlots[0]` is non-null (the GK, placed there by `pickIntoLineup`) and `lineupSlots[1]` is non-null (a non-GK). Then emit two ILLEGAL slot↔slot `DRAFT_REARRANGE` payloads from clientA and assert each is rejected with the correct `GAME_ERROR` and fires NO `DRAFT_STATE_UPDATED`: (1) `{ from: { type: 'slot', slotIndex: 1 }, to: { type: 'slot', slotIndex: 0 } }` → `GK_SLOT_REQUIRES_GK` (a non-GK cannot swap into the GK slot); (2) `{ from: { type: 'slot', slotIndex: 0 }, to: { type: 'slot', slotIndex: 1 } }` → `NON_GK_SLOT_REJECTS_GK` (the GK cannot swap out into an outfield slot). Add a positive control: a LEGAL outfield swap `{ from: { type: 'slot', slotIndex: 1 }, to: { type: 'slot', slotIndex: 2 } }` emits `DRAFT_STATE_UPDATED`; capture the two cards at slots 1 and 2 beforehand and assert they trade slots in the returned view and neither id appears in `benchIds`. Use the file's existing `oncePromise` / GAME_ERROR one-shot promise patterns and a short no-event settle window (mirror the ~150ms `setTimeout` guard used at line ~245) to prove no state update fired on the rejected emits. Do not modify `roomHandlers.ts` or `draftSession.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test draftSession.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test asserts the `{ slot 1 → slot 0 }` swap yields `GAME_ERROR === 'GK_SLOT_REQUIRES_GK'` and that no `DRAFT_STATE_UPDATED` is received for it.
    - New test asserts the `{ slot 0 → slot 1 }` swap yields `GAME_ERROR === 'NON_GK_SLOT_REJECTS_GK'` and that no `DRAFT_STATE_UPDATED` is received for it.
    - New test asserts a legal outfield swap `{ slot 1 → slot 2 }` emits `DRAFT_STATE_UPDATED` with the two originally-at-1-and-2 cards traded between `lineupSlots[1]` and `lineupSlots[2]` and neither present in `benchIds`.
    - `pnpm --filter @counter-attack/server test draftSession.integration.test.ts` exits 0.
    - `git diff --name-only` for this task shows only `packages/server/src/__tests__/draftSession.integration.test.ts` changed (no handler/engine edits).
  </acceptance_criteria>
  <done>Both-direction GK-slot enforcement on slot↔slot swaps and a legal outfield swap are locked by an integration test that passes against the Task 1 fix.</done>
</task>

<task type="auto">
  <name>Task 3: Narrow D-07 and record D-24 in the phase decision docs</name>
  <files>.planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md, .planning/phases/29-draft-ui-pick-and-swap-flow/29-RESEARCH.md, .planning/phases/29-draft-ui-pick-and-swap-flow/29-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md — read the `<decisions>` block; D-07 is at line 29, decisions run D-01..D-23 so the next sequential ID is D-24. Cross-check D-08/D-09/D-10 for adjacent wording.
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-RESEARCH.md — the D-07 mirror sentence is at line 24.
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-UI-SPEC.md — the D-07 mirror ("Dropping a card onto an occupied lineup slot bumps the occupant to the bench (D-07)") is at line 101.
    - .planning/phases/29-draft-ui-pick-and-swap-flow/29-VERIFICATION.md — "Recommended Fix Plans" 29-10 task 3 for the exact narrowing intent.
  </read_first>
  <action>
    In `29-CONTEXT.md`, rewrite the D-07 entry (line 29) so its bench-displacement rule is scoped to moves whose SOURCE is NOT a lineup slot — i.e. drafting a fresh pack card onto an occupied slot (`applyPick`) and bench→slot rearrangement. Add a new decision `D-24` in the same `<decisions>` block (next sequential ID after D-23), worded so a future reader cannot re-introduce the bug: state that when both `from` and `to` are lineup slots, `applyRearrange` performs a TRUE two-way swap — the displaced destination occupant returns to the dragged card's just-vacated source slot, never the bench — and that D-07's bench-displacement applies only when the move originates from the bench or the draft pack. Reference `applyRearrange` and note the 29-10 gap-closure origin (29-VERIFICATION.md Gap 1). In `29-RESEARCH.md` (line 24) and `29-UI-SPEC.md` (line 101), append a one-clause cross-reference to the existing D-07 sentence noting the slot↔slot exception per D-24 (do NOT delete the existing D-07 text — it remains correct for the drafting/bench-origin cases). Do not touch `REQUIREMENTS.md` (the separate D-16 "sequential → random 15-99" wording update remains a tracked deferred item).
  </action>
  <verify>
    <automated>grep -n "D-24" .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `29-CONTEXT.md` D-07 entry explicitly limits bench-displacement to moves whose source is the bench or the draft pack (not a lineup slot).
    - `29-CONTEXT.md` `<decisions>` block contains a new `D-24` entry describing the slot↔slot true-swap rule and citing `applyRearrange`.
    - `29-RESEARCH.md` line-24 D-07 sentence and `29-UI-SPEC.md` line-101 D-07 sentence each carry a cross-reference to the D-24 slot↔slot exception, with the original D-07 text retained.
    - `grep -n "D-24" .planning/phases/29-draft-ui-pick-and-swap-flow/29-CONTEXT.md` returns at least one line (exit 0).
  </acceptance_criteria>
  <done>D-07 is narrowed and D-24 documents the slot↔slot swap rule across all three phase docs, preventing re-introduction of the bench-displacement defect.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                              |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| client → server (`DRAFT_REARRANGE`) | Untrusted `{ from, to }` slot refs cross into server-authoritative draft state via `roomHandlers.ts` → `applyRearrange`. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                      |
| ---------- | ---------------------- | -------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-29-10-01 | Tampering              | `applyRearrange` slot↔slot swap branch       | mitigate    | The new swap writes the displaced occupant only into `lineupSlots[from.slotIndex]`, an already-allow-listed (0..10, `INVALID_SLOT_INDEX`) and already-vacated index — no unbounded write, no card duplication/loss. Task 1 asserts card conservation (`benchIds` deep-equal; both ids present after swap).           |
| T-29-10-02 | Elevation of Privilege | `DRAFT_REARRANGE` side resolution            | accept      | Side is resolved from `socket.data.playerSlot` only (never a payload field, T-29-02, roomHandlers.ts ~line 929); the swap fix does not read side from the payload, so it cannot move an opponent's cards. No change to this control.                                                                                 |
| T-29-10-03 | Tampering              | GK-slot role invariant across a swap         | mitigate    | Illegal slot↔slot swaps (non-GK into GK slot / GK into outfield slot) are rejected by the existing destination-only GK check (roomHandlers.ts ~lines 954-979) because the GK slot only ever holds a GK and outfield slots only non-GKs. Task 2 integration test locks BOTH directions plus a legal positive control. |
| T-29-10-04 | Denial of Service      | rearrange after lineup confirm / match start | accept      | Existing lifecycle guard (`LINEUP_ALREADY_CONFIRMED` when `requesterConfirmed                                                                                                                                                                                                                                        |     | room.gameState !== null`, roomHandlers.ts ~lines 931-938) and per-room `isProcessing` mutex remain unchanged; the swap fix touches no lifecycle path. |
| T-29-10-SC | Tampering              | npm/pnpm installs                            | accept      | No package installs in this plan — no new dependencies added; nothing to audit.                                                                                                                                                                                                                                      |

No high-severity threats introduced. All Tampering threats reachable through the changed code are mitigated by allow-listed indices, the state invariant, and the new tests.
</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` — full server suite green (the 1542-test baseline plus the new unit + integration cases).
- `pnpm --filter @counter-attack/server typecheck` — exits 0.
- `pnpm --filter @counter-attack/server build` — exits 0.
- `applyPick` unchanged (grep: occupied-slot branch still appends occupant to `benchIds`).
- `roomHandlers.ts` DRAFT_REARRANGE handler unchanged (git diff shows no server handler edits).
</verification>

<success_criteria>

- Slot↔slot rearrangement is a true two-way swap: both cards remain in the lineup, neither is bumped to the bench (29-VERIFICATION.md Gap 1 closed).
- Slot→bench, bench→slot-with-occupant, and `applyPick` occupied-slot displacement all keep their prior D-07 behavior (regression tests green).
- D-09 GK-slot rule rejects illegal swaps in both directions; a legal outfield swap applies (integration test green).
- D-07 narrowed and D-24 recorded across `29-CONTEXT.md`, `29-RESEARCH.md`, `29-UI-SPEC.md`.
- Follow-up (phase re-verification step, NOT a task in this plan): the orchestrator re-runs the two-browser walkthrough targeting lineup-slot ↔ lineup-slot drags (29-VERIFICATION.md "Human Verification Required" item 1) and confirms both cards trade places before Phase 29 is marked complete.
  </success_criteria>

<output>
Create `.planning/phases/29-draft-ui-pick-and-swap-flow/29-10-SUMMARY.md` when done.
</output>
