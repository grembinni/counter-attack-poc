---
phase: 15-team-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/teamConfig.ts
  - packages/shared/src/teamConfig.test.ts
  - packages/shared/src/index.ts
  - packages/client/src/teamDefaults.ts
  - packages/client/src/components/TeamBadge.tsx
  - packages/client/src/vite-env.d.ts
autonomous: true
requirements: [TEAM-01, TEAM-06]
must_haves:
  truths:
    - 'Four teams (Cosmos, Xolos, City, Crew) exist in shared types with id, name, primaryColor, secondaryColor, badgeFile'
    - 'TEAM_CONFIGS is importable from @counter-attack/shared by both client and server'
    - "TeamBadge component renders a team's PNG badge via an <img> tag with a configurable size"
    - 'TEAM_DEFAULTS maps positional home/away to a TeamId without touching GameState'
  artifacts:
    - path: 'packages/shared/src/teamConfig.ts'
      provides: 'TeamId union, TeamConfig interface, TEAM_CONFIGS record'
      contains: 'export const TEAM_CONFIGS'
    - path: 'packages/client/src/teamDefaults.ts'
      provides: 'TEAM_DEFAULTS client-only positional-to-TeamId map'
      contains: 'TEAM_DEFAULTS'
    - path: 'packages/client/src/components/TeamBadge.tsx'
      provides: 'TeamBadge React component rendering PNG badges'
      exports: ['TeamBadge']
  key_links:
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/teamConfig.ts'
      via: 'barrel re-export'
      pattern: "export \\* from './teamConfig.js'"
    - from: 'packages/client/src/components/TeamBadge.tsx'
      to: 'packages/client/src/assets/badges/*.png'
      via: 'Vite static import'
      pattern: "import .*Badge from '../assets/badges/"
---

<objective>
Establish the Phase 15 data foundation: the shared `TeamConfig` types and `TEAM_CONFIGS` record (TEAM-01), the client-only `TEAM_DEFAULTS` positional map (D-05), and the `TeamBadge` component that renders PNG badge assets (TEAM-06, D-07). This plan creates only NEW files (plus one barrel-export line) so it has zero file-overlap with the rendering/refactor plans that consume it.

Purpose: Every downstream rendering change in Wave 2 depends on `TEAM_CONFIGS`, `TEAM_DEFAULTS`, and `TeamBadge`. Defining these contracts first eliminates the scavenger-hunt anti-pattern — Wave 2 executors import finished symbols.

Output: `teamConfig.ts` (+ test), barrel export, `teamDefaults.ts`, `TeamBadge.tsx`, and `vite-env.d.ts` (PNG import type support).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-team-identity/15-CONTEXT.md
@.planning/phases/15-team-identity/15-RESEARCH.md
@.planning/phases/15-team-identity/15-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols and files created in Phase 15 (across all three plans) — exclude from drift verification:

- `TeamId` (type union) — packages/shared/src/teamConfig.ts
- `TeamConfig` (interface) — packages/shared/src/teamConfig.ts
- `TEAM_CONFIGS` (const record) — packages/shared/src/teamConfig.ts
- `TEAM_DEFAULTS` (const) — packages/client/src/teamDefaults.ts
- `TeamBadge` (component) — packages/client/src/components/TeamBadge.tsx
- `BADGE_MAP` (const) — packages/client/src/components/TeamBadge.tsx
- New SVG pattern ids in PieceOverlay.tsx: `cosmos-jersey-{id}`, `xolos-jersey-{id}`, `city-jersey-{id}`, `crew-jersey-{id}`, `home-gk-checker-{id}`
- New SVG pattern ids in PlayerStatsPanel.tsx: `mini-{teamId}-jersey-{id}`, `mini-home-gk-checker-{id}`
- New file: packages/client/src/vite-env.d.ts
- New badge PNG assets: cosmos.png, xolos.png, city.png, crew.png (already present on disk)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared teamConfig.ts + barrel export + unit test</name>
  <files>packages/shared/src/teamConfig.ts, packages/shared/src/teamConfig.test.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/teamConfig.ts (will not exist yet — confirm absence)
    - packages/shared/src/teams.ts (analog: static typed record + named exports, ESM .js import convention)
    - packages/shared/src/scoreUtils.test.ts (analog: vitest test file structure for shared package)
    - packages/shared/src/index.ts (current barrel — append one line at bottom)
    - .planning/phases/15-team-identity/15-RESEARCH.md (Code Examples section — exact TEAM_CONFIGS values)
  </read_first>
  <behavior>
    - Test: Object.keys(TEAM_CONFIGS) contains exactly 'cosmos', 'xolos', 'city', 'crew'
    - Test: each TEAM_CONFIGS[teamId].id strictly equals its key
    - Test: each config has a truthy name, a primaryColor matching /^#[0-9a-f]{6}$/i, a secondaryColor matching /^#[0-9a-f]{6}$/i, and a badgeFile matching /\.png$/
    - Test: TEAM_CONFIGS.cosmos.name === 'Cosmos' (D-02 confirms spelling — NOT 'Cozmos')
  </behavior>
  <action>
    Create `packages/shared/src/teamConfig.ts` implementing D-03/D-04 exactly. Export a `TeamId` union type `'cosmos' | 'xolos' | 'city' | 'crew'`; a `TeamConfig` interface with fields `id: TeamId`, `name: string`, `primaryColor: string`, `secondaryColor: string`, `badgeFile: string` (badgeFile is a filename key only, e.g. `'cosmos.png'` — NOT an asset path; the actual import happens in TeamBadge); and a `TEAM_CONFIGS: Record<TeamId, TeamConfig>` static record with these exact values per D-04: cosmos name 'Cosmos' primaryColor '#1e3a8a' secondaryColor '#c8a84b' badgeFile 'cosmos.png'; xolos name 'Xolos' primaryColor '#ea580c' secondaryColor '#6b7280' badgeFile 'xolos.png'; city name 'City' primaryColor '#dc143c' secondaryColor '#f5c518' badgeFile 'city.png'; crew name 'Crew' primaryColor '#f5c518' secondaryColor '#111111' badgeFile 'crew.png'. Append `export * from './teamConfig.js';` at the bottom of `packages/shared/src/index.ts` (use the `.js` extension to match the existing ESM barrel convention). Create `packages/shared/src/teamConfig.test.ts` covering the behaviors above using the vitest `describe`/`it.each` structure from scoreUtils.test.ts; import from `./teamConfig.js`.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test --run</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/teamConfig.ts contains `export type TeamId =`
    - packages/shared/src/teamConfig.ts contains `export const TEAM_CONFIGS: Record<TeamId, TeamConfig>`
    - packages/shared/src/teamConfig.ts contains `name: 'Cosmos'` (NOT 'Cozmos')
    - packages/shared/src/index.ts contains `export * from './teamConfig.js'`
    - `pnpm --filter @counter-attack/shared test --run` exits 0 with teamConfig.test.ts passing
    - `pnpm --filter @counter-attack/shared build` exits 0 (TypeScript compiles)
  </acceptance_criteria>
  <done>TEAM_CONFIGS with all four teams is exported from @counter-attack/shared and the unit test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create client TEAM_DEFAULTS, vite-env.d.ts, and TeamBadge component</name>
  <files>packages/client/src/teamDefaults.ts, packages/client/src/vite-env.d.ts, packages/client/src/components/TeamBadge.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (lines 44-56: TeamShieldIcon — the component TeamBadge replaces; note its prop shape `{ color: string }`)
    - packages/client/src/components/ActionLog.tsx (lines 5-8: module-level constant convention to mirror in teamDefaults.ts)
    - packages/client/tsconfig.json (already has `"types": ["vite/client"]` — vite-env.d.ts is the conventional companion shim)
    - packages/client/src/assets/badges/ (confirm cosmos.png, xolos.png, city.png, crew.png present)
    - .planning/phases/15-team-identity/15-PATTERNS.md (TeamBadge.tsx pattern — exact BADGE_MAP + import structure)
  </read_first>
  <action>
    Create three files. (1) `packages/client/src/vite-env.d.ts` containing exactly the triple-slash directive `/// <reference types="vite/client" />` — this guarantees TypeScript resolves `*.png` static imports as string modules (tsconfig already lists `vite/client` in types, but the shim file is the conventional location and prevents "Cannot find module './x.png'" errors). (2) `packages/client/src/teamDefaults.ts` as a module-level (NOT inside any component — Pitfall 6) constant: import `type { TeamId } from '@counter-attack/shared'` and export `const TEAM_DEFAULTS: Record<'home' | 'away', TeamId> = { home: 'cosmos', away: 'xolos' }` per D-05. (3) `packages/client/src/components/TeamBadge.tsx` per D-07: static-import the four PNGs (`import cosmosBadge from '../assets/badges/cosmos.png'` etc.), build a module-level `const BADGE_MAP: Record<TeamId, string> = { cosmos: cosmosBadge, xolos: xolosBadge, city: cityBadge, crew: crewBadge }`, and export `function TeamBadge({ teamId, size = 28 }: { teamId: TeamId; size?: number })` returning `<img src={BADGE_MAP[teamId]} alt={\`${teamId} badge\`} width={size} height={size} style={{ display: 'block' }} />`. Default size 28 (scoreboard size per D-07); the size prop supports Phase 16 reuse. Do NOT use `teamConfig.badgeFile` as the img src (Pitfall 3 — Vite hashes asset URLs at build time; only the static import yields a valid URL).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/src/vite-env.d.ts contains `/// <reference types="vite/client" />`
    - packages/client/src/teamDefaults.ts contains `export const TEAM_DEFAULTS: Record<'home' | 'away', TeamId>` with `home: 'cosmos'` and `away: 'xolos'`
    - packages/client/src/components/TeamBadge.tsx contains `export function TeamBadge`
    - packages/client/src/components/TeamBadge.tsx contains `import cosmosBadge from '../assets/badges/cosmos.png'`
    - packages/client/src/components/TeamBadge.tsx contains `alt={` (non-empty alt for accessibility per UI-SPEC)
    - packages/client/src/components/TeamBadge.tsx does NOT reference `badgeFile`
    - `pnpm --filter @counter-attack/client exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TeamBadge renders a PNG via static import with a default 28px size and a configurable size prop; TEAM_DEFAULTS and the vite-env shim exist and the client type-checks.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                                         |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| (none)   | Pure client/shared data + static asset display. No network input, no server changes, no user input crosses any boundary this phase. |

## STRIDE Threat Register

| Threat ID | Category               | Component         | Disposition | Mitigation Plan                                                                              |
| --------- | ---------------------- | ----------------- | ----------- | -------------------------------------------------------------------------------------------- |
| T-15-01   | Information Disclosure | Badge PNG assets  | accept      | Static public game assets; no PII, no secrets. Bundled client-side intentionally.            |
| T-15-SC   | Tampering              | npm/pnpm installs | accept      | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable). |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test --run` passes (teamConfig.test.ts green)
- `pnpm --filter @counter-attack/shared build` exits 0
- `pnpm --filter @counter-attack/client exec tsc --noEmit` exits 0
- No `badgeFile` string used as an `<img src>` anywhere in TeamBadge.tsx
</verification>

<success_criteria>

- TEAM_CONFIGS exports all four teams (Cosmos, Xolos, City, Crew) with correct colors and is importable from @counter-attack/shared (TEAM-01)
- TeamBadge component exists and renders the correct PNG per teamId with a configurable size, ready for scoreboard wiring (TEAM-06 foundation)
- TEAM_DEFAULTS exists as a client-only constant; no GameState or Socket.io changes (D-05)
  </success_criteria>

<output>
Create `.planning/phases/15-team-identity/15-01-SUMMARY.md` when done
</output>
