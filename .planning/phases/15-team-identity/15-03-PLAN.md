---
phase: 15-team-identity
plan: 03
type: execute
wave: 2
depends_on: ['15-01']
files_modified:
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/GameBoard.test.tsx
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/PlayerStatsPanel.tsx
autonomous: true
requirements: [TEAM-06]
must_haves:
  truths:
    - "The scoreboard top band shows each team's PNG badge (an <img>) for home and away"
    - 'TeamShieldIcon is removed from GameBoard.tsx and replaced by TeamBadge'
    - 'Team-identity colors in GameBoard.tsx, ActionLog.tsx come from TEAM_CONFIGS lookups, not hardcoded literals'
    - 'The PlayerStatsPanel mini token reflects the team jersey colors and the home GK checker pattern'
  artifacts:
    - path: 'packages/client/src/components/GameBoard.tsx'
      provides: 'Scoreboard badge wiring via TeamBadge + D-06 color refactor'
      contains: 'TeamBadge'
    - path: 'packages/client/src/components/PlayerStatsPanel.tsx'
      provides: 'MiniTokenBadge team-color + GK checker refactor'
      contains: 'TEAM_DEFAULTS'
  key_links:
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: 'packages/client/src/components/TeamBadge.tsx'
      via: 'TeamBadge component render in scoreboard'
      pattern: '<TeamBadge'
    - from: 'packages/client/src/components/ActionLog.tsx'
      to: '@counter-attack/shared TEAM_CONFIGS'
      via: 'pieceColorOf lookup'
      pattern: "TEAM_CONFIGS\\["
---

<objective>
Wire team badges into the scoreboard top band and complete the D-06 color-literal refactor across `GameBoard.tsx` (TeamShieldIcon → TeamBadge), `ActionLog.tsx`, and `PlayerStatsPanel.tsx` (including the MiniTokenBadge GK checker update per Pitfall 2). Satisfies TEAM-06 and the remaining D-06/D-07 scope outside PieceOverlay.

Purpose: TEAM-06 requires the badge in the scoreboard; D-06 requires a single color source of truth. This plan owns the three files that PieceOverlay (Plan 02) does not, so it runs in parallel within Wave 2.

Output: GameBoard scoreboard rendering `<TeamBadge>` images; ActionLog and PlayerStatsPanel reading colors via TEAM_CONFIGS; MiniTokenBadge GK checker aligned with the main board.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-team-identity/15-CONTEXT.md
@.planning/phases/15-team-identity/15-RESEARCH.md
@.planning/phases/15-team-identity/15-PATTERNS.md
@.planning/phases/15-team-identity/15-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
See 15-01-PLAN.md "artifacts_this_phase_produces". This plan adds PlayerStatsPanel mini pattern ids: `mini-{teamId}-jersey-{piece.id}`, `mini-home-gk-checker-{piece.id}`. These are NEW symbols — exclude from drift verification. This plan DELETES the `TeamShieldIcon` function and the `HOME_COLOR`/`AWAY_COLOR` constants.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace TeamShieldIcon with TeamBadge + D-06 color refactor in GameBoard.tsx</name>
  <files>packages/client/src/components/GameBoard.tsx, packages/client/src/components/GameBoard.test.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (FULL — TeamShieldIcon def at 44-56; color literals at 139, 156, 162, 178, 215, 216, 248, 251, 288, 294, 332, 338; scoreboard cells at 211-253)
    - packages/client/src/components/TeamBadge.tsx (created in 15-01 — prop shape `{ teamId: TeamId; size?: number }`)
    - packages/client/src/teamDefaults.ts (TEAM_DEFAULTS)
    - packages/shared/src/teamConfig.ts (TEAM_CONFIGS)
    - packages/client/src/components/GameBoard.test.tsx (existing tests — extend; do not break the 6 COMPACT_STATS / clock / scoreboard tests)
    - .planning/phases/15-team-identity/15-PATTERNS.md (GameBoard section — import additions + replacement patterns)
    - .planning/phases/15-team-identity/15-UI-SPEC.md (Color Refactor Targets — note: #1a56b0 also used as a legitimate interactive border/hover color; only replace team-identity occurrences)
  </read_first>
  <behavior>
    - Test: the scoreboard renders at least two `<img>` elements with alt text matching `/badge/` (home + away team badges) — TEAM-06
    - Test: the home scoreboard badge img alt contains 'cosmos' and the away contains 'xolos' (per TEAM_DEFAULTS)
    - Test: GameBoard.tsx source contains no `TeamShieldIcon`
  </behavior>
  <action>
    Add imports after existing imports: `import { TEAM_CONFIGS } from '@counter-attack/shared'`, `import { TEAM_DEFAULTS } from '../teamDefaults.js'`, `import { TeamBadge } from './TeamBadge.js'`. Delete the `TeamShieldIcon` function (lines 44-56). Replace every `<TeamShieldIcon color={...} />` call site with `<TeamBadge teamId={TEAM_DEFAULTS[<team>]} size={28} />`: line 178 player-card icon uses `TEAM_DEFAULTS[displayPiece.teamId]`; line 215 home scoreboard cell uses `TEAM_DEFAULTS['home']`; line 251 away scoreboard cell uses `TEAM_DEFAULTS['away']`. Replace the team-identity color literals with `TEAM_CONFIGS[TEAM_DEFAULTS[<team>]].primaryColor`: line 139 `teamColor` (uses `activeTeam`), line 156 `secondHalfTeamColor` (uses `secondHalfKickOffTeam`), line 216 home score numeral style (`TEAM_DEFAULTS['home']`), line 248 away score numeral style (`TEAM_DEFAULTS['away']`), lines 288/332 home overlay labels (`TEAM_DEFAULTS['home']`), lines 294/338 away overlay labels (`TEAM_DEFAULTS['away']`). For line 162 `resultColor` (win/draw derivation), replace the `#1a56b0`/`#c0392b` branches with `TEAM_CONFIGS[TEAM_DEFAULTS['home']].primaryColor` / `TEAM_CONFIGS[TEAM_DEFAULTS['away']].primaryColor`, keeping the draw `#e0e0e0` literal (it is a neutral text color, not a team identity). Do NOT touch any `#1a56b0` used purely as an interactive border/hover color in GameBoard.module.css (CSS file is out of scope; only the .tsx team-identity literals listed above). Extend GameBoard.test.tsx with the badge-image behaviors above (mock the PNG imports if needed — assert img alt text, not exact src URL, per RESEARCH note on Vitest asset imports).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test --run GameBoard</automated>
  </verify>
  <acceptance_criteria>
    - GameBoard.tsx contains `import { TeamBadge } from './TeamBadge.js'`
    - GameBoard.tsx contains `<TeamBadge teamId={TEAM_DEFAULTS['home']}` and `<TeamBadge teamId={TEAM_DEFAULTS['away']}`
    - `grep -c 'TeamShieldIcon' packages/client/src/components/GameBoard.tsx` returns 0
    - `grep -v '^\s*//' packages/client/src/components/GameBoard.tsx | grep -c '#1a56b0'` returns 0
    - `grep -v '^\s*//' packages/client/src/components/GameBoard.tsx | grep -c '#c0392b'` returns 0
    - `pnpm --filter @counter-attack/client test --run GameBoard` exits 0
  </acceptance_criteria>
  <done>The scoreboard renders home (cosmos) and away (xolos) PNG badges via TeamBadge; TeamShieldIcon is deleted; all team-identity color literals in GameBoard.tsx come from TEAM_CONFIGS; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-06 color refactor in ActionLog.tsx + MiniTokenBadge GK/color update in PlayerStatsPanel.tsx</name>
  <files>packages/client/src/components/ActionLog.tsx, packages/client/src/components/PlayerStatsPanel.tsx</files>
  <read_first>
    - packages/client/src/components/ActionLog.tsx (HOME_COLOR/AWAY_COLOR at 5-8; pieceColorOf at 10-12; usage at line 239)
    - packages/client/src/components/PlayerStatsPanel.tsx (FULL — MiniTokenBadge at 27-60; gkFill at 40; mini pattern ids at 36-37; defs at ~54-72; circle fill at 83)
    - packages/client/src/teamDefaults.ts (TEAM_DEFAULTS)
    - packages/shared/src/teamConfig.ts (TEAM_CONFIGS)
    - packages/client/src/components/PlayerStatsPanel.test.tsx (existing — do not break)
    - .planning/phases/15-team-identity/15-PATTERNS.md (ActionLog + PlayerStatsPanel sections — exact refactor patterns, mini- prefixed ids, home GK checker scaled to 18px tile / 5px checkers)
  </read_first>
  <behavior>
    - Test (ActionLog or via grep): ActionLog.tsx contains no `#1a56b0` and no `#c0392b` literal; pieceColorOf derives color from TEAM_CONFIGS
    - Test: PlayerStatsPanel mini token for a cosmos-mapped home outfield piece references `url(#mini-cosmos-jersey-...)`
    - Test: PlayerStatsPanel home GK mini token references `url(#mini-home-gk-checker-...)` (not solid `#9b59b6`)
  </behavior>
  <action>
    ActionLog.tsx (D-06): add `import { TEAM_CONFIGS } from '@counter-attack/shared'` and `import { TEAM_DEFAULTS } from '../teamDefaults.js'`. Delete the `HOME_COLOR`/`AWAY_COLOR` constants (lines 7-8). Rewrite `pieceColorOf(pieceId)` to map `const positional = pieceId.startsWith('home') ? 'home' : 'away'` then return `TEAM_CONFIGS[TEAM_DEFAULTS[positional]].primaryColor`. At line 239, replace `event.scoringTeam === 'home' ? HOME_COLOR : AWAY_COLOR` with `TEAM_CONFIGS[TEAM_DEFAULTS[event.scoringTeam === 'home' ? 'home' : 'away']].primaryColor` (or reuse pieceColorOf-style lookup). PlayerStatsPanel.tsx (D-06 + D-10, Pitfall 2): add the same two imports. In MiniTokenBadge, compute `const teamId = TEAM_DEFAULTS[piece.teamId]`. Replace the positional `mini-home-stripe`/`mini-away-stripe` pattern ids and their `#1a56b0`/`#c0392b` base fills with a single team-keyed `mini-${teamId}-jersey-${piece.id}` pattern using `TEAM_CONFIGS[teamId].primaryColor` as the base fill (scale the tile to the mini token radius `miniR` per the analog — 18px tile). Update gkFill (line 40): home GK uses `url(#mini-home-gk-checker-${piece.id})`, away GK keeps solid `#f59e0b`. Add a `mini-home-gk-checker-${piece.id}` `<pattern>` def (gated `isGK && piece.teamId === 'home'`) scaled to a 10px tile with 5px `#4c1d95` checkers on a `#7c3aed` base, anchored `x={miniCx - miniR} y={miniCy - miniR}` patternUnits userSpaceOnUse. Update the circle fill (line 83) to reference the new ids. Keep all existing MiniTokenBadge structure and props otherwise.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test --run PlayerStatsPanel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'HOME_COLOR\|AWAY_COLOR' packages/client/src/components/ActionLog.tsx` returns 0
    - ActionLog.tsx contains `TEAM_CONFIGS[TEAM_DEFAULTS[`
    - `grep -v '^\s*//' packages/client/src/components/ActionLog.tsx | grep -c '#1a56b0'` returns 0
    - `grep -v '^\s*//' packages/client/src/components/ActionLog.tsx | grep -c '#c0392b'` returns 0
    - PlayerStatsPanel.tsx contains `mini-${teamId}-jersey-` and `mini-home-gk-checker-` pattern ids
    - PlayerStatsPanel.tsx contains `TEAM_CONFIGS` and `TEAM_DEFAULTS` imports
    - `grep -v '^\s*//' packages/client/src/components/PlayerStatsPanel.tsx | grep -c '#1a56b0'` returns 0
    - `pnpm --filter @counter-attack/client test --run PlayerStatsPanel` exits 0
  </acceptance_criteria>
  <done>ActionLog colors and PlayerStatsPanel mini token colors come from TEAM_CONFIGS; the mini home GK checker matches the main board; no positional hex literals remain in either file; tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                 |
| -------- | --------------------------------------------------------------------------- |
| (none)   | Rendering-only changes. No network input, no server changes, no user input. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                   | Disposition | Mitigation Plan                                                                             |
| --------- | ---------------------- | ------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------- |
| T-15-03   | Information Disclosure | Badge alt text                              | accept      | Alt text is the public team id; no sensitive data.                                          |
| T-15-04   | Tampering              | Color-literal refactor misses an occurrence | mitigate    | grep gates (filtered for comments) on `#1a56b0`/`#c0392b` in each file; Pitfall 5 coverage. |
| T-15-SC   | Tampering              | npm/pnpm installs                           | accept      | No new packages this phase.                                                                 |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test --run GameBoard` passes
- `pnpm --filter @counter-attack/client test --run PlayerStatsPanel` passes
- `pnpm --filter @counter-attack/client exec tsc --noEmit` exits 0
- `grep -c 'TeamShieldIcon' packages/client/src/components/GameBoard.tsx` returns 0
- Comment-filtered grep for `#1a56b0`/`#c0392b` returns 0 in GameBoard.tsx, ActionLog.tsx, PlayerStatsPanel.tsx
</verification>

<success_criteria>

- Team badge displayed in the scoreboard top band for both teams (TEAM-06)
- TeamShieldIcon removed; TeamBadge used everywhere it appeared (D-07)
- Single color source of truth across GameBoard, ActionLog, PlayerStatsPanel (D-06)
- MiniTokenBadge GK checker matches the main board (Pitfall 2 avoided)
  </success_criteria>

<output>
Create `.planning/phases/15-team-identity/15-03-SUMMARY.md` when done
</output>
