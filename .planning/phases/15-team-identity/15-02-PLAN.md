---
phase: 15-team-identity
plan: 02
type: execute
wave: 2
depends_on: ['15-01']
files_modified:
  - packages/client/src/components/PieceOverlay.tsx
  - packages/client/src/components/PieceOverlay.test.tsx
autonomous: true
requirements: [TEAM-02, TEAM-03, TEAM-04, TEAM-05]
must_haves:
  truths:
    - 'A Cosmos outfield piece renders a navy jersey with a wide horizontal white stripe'
    - 'A Xolos outfield piece renders an orange jersey with a grey checker pattern'
    - 'A City outfield piece renders a crimson jersey with fine gold vertical stripes plus a gold arch path'
    - 'A Crew outfield piece renders a gold jersey with diagonal black stripes restricted to the upper shoulder region'
    - 'The home GK renders a purple/dark-purple checker pattern; the away GK renders amber with two orange edge stripes'
    - 'No hardcoded #1a56b0 or #c0392b team-identity literals remain in PieceOverlay.tsx'
  artifacts:
    - path: 'packages/client/src/components/PieceOverlay.tsx'
      provides: 'Four team jersey SVG patterns + GK jersey patterns + TEAM_CONFIGS color lookup'
      contains: 'cosmos-jersey-'
  key_links:
    - from: 'packages/client/src/components/PieceOverlay.tsx'
      to: 'packages/client/src/teamDefaults.ts'
      via: 'TEAM_DEFAULTS[piece.teamId] mapping'
      pattern: "TEAM_DEFAULTS\\[piece.teamId\\]"
    - from: 'packages/client/src/components/PieceOverlay.tsx'
      to: '@counter-attack/shared TEAM_CONFIGS'
      via: 'primaryColor lookup'
      pattern: "TEAM_CONFIGS\\["
---

<objective>
Apply distinct per-team jersey identities to piece tokens in `PieceOverlay.tsx`: four outfield SVG `<pattern>` defs (Cosmos/Xolos/City/Crew per D-08/D-09), the two GK jersey updates (home checker, away edge-stripes per D-10), and the D-06 color-literal refactor — replacing the positional home/away ternary with a `TEAM_CONFIGS[TEAM_DEFAULTS[piece.teamId]]` lookup. Satisfies TEAM-02..05.

Purpose: Outfield pieces must visually display their team's real uniform, not the generic blue/red placeholder. This is the core visual payload of Phase 15.

Output: Extended `PieceOverlay.tsx` with team-keyed jersey patterns + GK patterns; extended `PieceOverlay.test.tsx` asserting each team's fill reference.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-team-identity/15-CONTEXT.md
@.planning/phases/15-team-identity/15-RESEARCH.md
@.planning/phases/15-team-identity/15-PATTERNS.md
@.planning/phases/15-team-identity/15-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
See 15-01-PLAN.md "artifacts_this_phase_produces". This plan adds the PieceOverlay SVG pattern ids: `cosmos-jersey-{piece.id}`, `xolos-jersey-{piece.id}`, `city-jersey-{piece.id}`, `crew-jersey-{piece.id}`, `home-gk-checker-{piece.id}`. These are NEW symbols — exclude from drift verification.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add four outfield jersey patterns + color refactor in PieceOverlay.tsx</name>
  <files>packages/client/src/components/PieceOverlay.tsx, packages/client/src/components/PieceOverlay.test.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx (FULL — current home/away ternary defs at lines 66-98, circle fill ref at 100-116, color logic at 40-53; this is the exact extension point)
    - packages/client/src/components/PieceOverlay.test.tsx (current test fixtures homeOutfield/awayOutfield/homeGK; extend, do not rewrite)
    - packages/client/src/teamDefaults.ts (TEAM_DEFAULTS — created in 15-01)
    - packages/shared/src/teamConfig.ts (TEAM_CONFIGS — created in 15-01)
    - .planning/phases/15-team-identity/15-PATTERNS.md (PieceOverlay section — exact pattern markup for all four teams + circle fill update)
    - .planning/phases/15-team-identity/15-UI-SPEC.md (SVG Jersey Pattern Contract table — tile sizes, fills, opacities)
  </read_first>
  <behavior>
    - Test: a home outfield piece (teamId 'home', maps via TEAM_DEFAULTS to 'cosmos') renders a circle whose fill references `url(#cosmos-jersey-home-5)`
    - Test: an away outfield piece (teamId 'away' → 'xolos') renders a circle whose fill references `url(#xolos-jersey-away-5)`
    - Test (add a City fixture with teamId mapped via a temporary default OR assert pattern def presence): the document contains a `<pattern id="city-jersey-...">` def and a `<pattern id="cosmos-jersey-...">` def when the respective piece renders
    - Test: PieceOverlay.tsx source contains no `#1a56b0` and no `#c0392b` literal (team-identity literals removed)
  </behavior>
  <action>
    Add D-06 imports at the top of PieceOverlay.tsx: `import { TEAM_CONFIGS } from '@counter-attack/shared'` and `import { TEAM_DEFAULTS } from '../teamDefaults.js'`. Replace the outfield `fill`/`stroke` literals (lines 40-53): outfield fill becomes `TEAM_CONFIGS[TEAM_DEFAULTS[piece.teamId]].primaryColor`; keep the GK fill branch as-is for now (home `#9b59b6`/away `#f59e0b` will be superseded by checker/stripe rendering in Task 2 — do not delete the GK stroke logic). Inside the `!isGK` `<defs>` block (lines 66-98), replace the home/away ternary with FOUR `<pattern>` defs keyed by team id, each anchored `x={cx - PIECE_RADIUS} y={cy - PIECE_RADIUS} patternUnits="userSpaceOnUse"` and id-suffixed with `piece.id` (Pitfall 1): cosmos `cosmos-jersey-${piece.id}` 24x24 base `#1e3a8a` + white rect x0 y6 w24 h12 fillOpacity 0.6; xolos `xolos-jersey-${piece.id}` 16x16 base `#ea580c` + two `#6b7280` 8x8 rects at (0,0) and (8,8) fillOpacity 0.7; city `city-jersey-${piece.id}` 4x24 base `#dc143c` + `#f5c518` rect x3 w1 h24 fillOpacity 0.8; crew `crew-jersey-${piece.id}` 8x8 base `#f5c518` + `#111111` diagonal line x1=8 y1=0 x2=0 y2=8 strokeWidth 2 strokeOpacity 0.75. Determine the active team id once via `const teamId = TEAM_DEFAULTS[piece.teamId]`. Update the circle fill ref (lines 100-116): outfield fill becomes `url(#${teamId}-jersey-${piece.id})`. After the base circle, add two sibling overlay elements (both `pointerEvents="none"`): the City arch `<path>` (gated `teamId === 'city' && !isGK`) using `d="M ${cx - PIECE_RADIUS*0.7} ${cy + PIECE_RADIUS*0.3} Q ${cx} ${cy + PIECE_RADIUS*0.9} ${cx + PIECE_RADIUS*0.7} ${cy + PIECE_RADIUS*0.3}"` fill none stroke `#f5c518` strokeWidth 1.5; and the Crew shoulder restriction (gated `teamId === 'crew' && !isGK`) — restrict the diagonal pattern to the top ~30% of the circle (Pitfall 4) using the masking-rect approach from 15-PATTERNS.md (solid `#f5c518` rect covering the lower portion, `y={cy - PIECE_RADIUS * 0.4}` h `PIECE_RADIUS * 1.4`). Extend PieceOverlay.test.tsx with the behaviors above (reuse homeOutfield/awayOutfield fixtures which map to cosmos/xolos; add a city/crew assertion by rendering and querying for the pattern def id).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test --run PieceOverlay</automated>
  </verify>
  <acceptance_criteria>
    - PieceOverlay.tsx contains `id={\`cosmos-jersey-${piece.id}\`}` (or equivalent template literal producing `cosmos-jersey-`)
    - PieceOverlay.tsx contains `xolos-jersey-`, `city-jersey-`, and `crew-jersey-` pattern ids
    - PieceOverlay.tsx contains `TEAM_CONFIGS[TEAM_DEFAULTS[piece.teamId]].primaryColor`
    - PieceOverlay.tsx contains the City arch `<path>` with stroke `#f5c518` and a Crew shoulder-restriction element, both with `pointerEvents="none"`
    - `grep -v '^\s*//' packages/client/src/components/PieceOverlay.tsx | grep -c '#1a56b0'` returns 0 (no team-identity blue literal outside comments)
    - `grep -v '^\s*//' packages/client/src/components/PieceOverlay.tsx | grep -c '#c0392b'` returns 0
    - `pnpm --filter @counter-attack/client test --run PieceOverlay` exits 0
  </acceptance_criteria>
  <done>All four outfield teams render their distinct jersey patterns via TEAM_DEFAULTS-keyed fills; City arch and Crew shoulder constraint applied; no positional color literals remain; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GK jersey patterns (home checker, away edge stripes) in PieceOverlay.tsx</name>
  <files>packages/client/src/components/PieceOverlay.tsx, packages/client/src/components/PieceOverlay.test.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx (post-Task-1 state: GK branch at the `isGK` color logic + circle fill; the `<defs>` block now holds outfield patterns)
    - packages/client/src/components/PieceOverlay.test.tsx (homeGK fixture exists)
    - .planning/phases/15-team-identity/15-PATTERNS.md (Home GK checker pattern def + Away GK edge-stripe sibling rects markup)
    - .planning/phases/15-team-identity/15-UI-SPEC.md (GK Patterns table: home checker `#7c3aed`/`#4c1d95` 12px tile / 6px checkers; away amber `#f59e0b` + `#ea580c` stripe rects)
  </read_first>
  <behavior>
    - Test: a home GK piece renders a circle whose fill references `url(#home-gk-checker-home-0)` (NOT solid `#9b59b6`)
    - Test: a home GK renders a `<pattern id="home-gk-checker-...">` def containing fills `#7c3aed` and `#4c1d95`
    - Test: an away GK renders a solid amber `#f59e0b` circle plus two `#ea580c` sibling `<rect>` edge stripes with `pointerEvents="none"`
  </behavior>
  <action>
    Implement D-10 GK jersey updates. Home GK: add a `<pattern id={\`home-gk-checker-${piece.id}\`}>` def (gated `isGK && piece.teamId === 'home'`) — 12x12 tile anchored `x={cx - PIECE_RADIUS} y={cy - PIECE_RADIUS}` patternUnits userSpaceOnUse: base `<rect>` 12x12 fill `#7c3aed`, two `#4c1d95` 6x6 rects at (0,0) and (6,6). Update the circle fill so home GK uses `url(#home-gk-checker-${piece.id})` instead of the solid `#9b59b6`; keep the existing home GK stroke `#6c3483`. Away GK: keep the solid amber base circle (fill `#f59e0b`, stroke `#d97706`) and add two sibling `<rect>` overlays AFTER the base circle (gated `isGK && piece.teamId === 'away'`, both `pointerEvents="none"`): left stripe `x={cx - PIECE_RADIUS + 4} y={cy - PIECE_RADIUS} width={3} height={PIECE_RADIUS * 2}` fill `#ea580c` fillOpacity 0.85; right stripe `x={cx + PIECE_RADIUS - 7} y={cy - PIECE_RADIUS} width={3} height={PIECE_RADIUS * 2}` fill `#ea580c` fillOpacity 0.85. The away GK is the only piece that retains a solid base-circle fill (the two fixed-position edge stripes are simpler as siblings than a repeating pattern — RESEARCH Pattern 7). Extend PieceOverlay.test.tsx with the GK behaviors above using the existing homeGK fixture and a new away GK fixture.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test --run PieceOverlay</automated>
  </verify>
  <acceptance_criteria>
    - PieceOverlay.tsx contains `home-gk-checker-` pattern id with fills `#7c3aed` and `#4c1d95`
    - PieceOverlay.tsx home GK circle fill references `url(#home-gk-checker-` (no solid `#9b59b6` fill on the home GK circle)
    - PieceOverlay.tsx away GK renders two `<rect>` siblings with fill `#ea580c` and `pointerEvents="none"`
    - `pnpm --filter @counter-attack/client test --run PieceOverlay` exits 0
    - `pnpm --filter @counter-attack/client exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Home GK renders the purple checker pattern, away GK renders amber with two orange edge stripes; GK tests pass and the client type-checks.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                     |
| -------- | ------------------------------------------------------------------------------- |
| (none)   | Rendering-only SVG changes. No network input, no server changes, no user input. |

## STRIDE Threat Register

| Threat ID | Category  | Component                | Disposition | Mitigation Plan                                                                                                       |
| --------- | --------- | ------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-15-02   | Tampering | SVG pattern id collision | mitigate    | All pattern ids suffixed with `piece.id` (Pitfall 1); verified by acceptance criteria asserting `${piece.id}` in ids. |
| T-15-SC   | Tampering | npm/pnpm installs        | accept      | No new packages this phase.                                                                                           |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test --run PieceOverlay` passes
- `pnpm --filter @counter-attack/client exec tsc --noEmit` exits 0
- `grep -v '^\s*//' packages/client/src/components/PieceOverlay.tsx | grep -c '#1a56b0'` returns 0
- `grep -v '^\s*//' packages/client/src/components/PieceOverlay.tsx | grep -c '#c0392b'` returns 0
- All four outfield jersey pattern ids present; both GK patterns present
</verification>

<success_criteria>

- Cosmos horizontal white stripe, Xolos checker, City vertical stripes + arch, Crew shoulder diagonals all render on outfield tokens (TEAM-02..05)
- Home GK checker + away GK edge stripes render (D-10)
- Single source of truth for outfield colors via TEAM_CONFIGS/TEAM_DEFAULTS; no positional hex literals remain (D-06 within PieceOverlay)
  </success_criteria>

<output>
Create `.planning/phases/15-team-identity/15-02-SUMMARY.md` when done
</output>
