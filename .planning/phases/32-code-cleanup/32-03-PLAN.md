---
phase: 32-code-cleanup
plan: 03
type: execute
wave: 2
depends_on: ['32-02']
files_modified:
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/PieceOverlay.tsx
autonomous: true
requirements: [CLEANUP-02]

must_haves:
  truths:
    - 'Every real `TEAM_CONFIGS[...].palette.uiColor` derivation in GameBoard.tsx (10 sites) and ActionLog.tsx (2 sites) is replaced by the canonical `useTeamAccentColor`/`teamAccentColor` from hooks/useTeamColors.ts — no inline `.palette.uiColor` derivation remains in either file (D-04).'
    - "ActionLog.tsx's module-level helpers (pieceColorOf/slotTeamColor) call the PURE `teamAccentColor()`, never the hook, because they run inside consolidateEvents loops (Pitfall 1)."
    - "GameBoard.tsx's inline null-safe `myTeam` derivation (line 197) is replaced by `useMyTeam()`."
    - "PieceOverlay.tsx's `palette.uiColor` occurrence is confirmed a type-shape pass-through (not a TEAM_CONFIGS lookup) and left correct — no forced hook call (Pitfall 5)."
    - 'Color output is behavior-identical: fallback semantics (#888888 miss-fallback, #e0e0e0 tie color) are preserved exactly.'
  artifacts:
    - path: 'packages/client/src/components/GameBoard.tsx'
      provides: 'Accent colors resolved via useTeamAccentColor; myTeam via useMyTeam'
      contains: 'useTeamAccentColor'
    - path: 'packages/client/src/components/ActionLog.tsx'
      provides: 'pieceColorOf/slotTeamColor delegate to pure teamAccentColor'
      contains: 'teamAccentColor'
    - path: 'packages/client/src/components/PieceOverlay.tsx'
      provides: 'Verified pass-through preserved (Pitfall 5)'
  key_links:
    - from: 'GameBoard.tsx'
      to: 'hooks/useTeamColors.ts'
      via: 'import { useTeamAccentColor }'
      pattern: "import.*useTeamAccentColor.*from '\\.\\./hooks/useTeamColors"
    - from: 'ActionLog.tsx'
      to: 'hooks/useTeamColors.ts'
      via: 'import { teamAccentColor } (pure, not the hook)'
      pattern: "import.*teamAccentColor.*from '\\.\\./hooks/useTeamColors"
---

<objective>
CLEANUP-02 (part 2 of 3): Migrate every genuine team-accent-color derivation in `GameBoard.tsx` and `ActionLog.tsx` onto the canonical hooks created in Plan 32-02, and reconcile `GameBoard.tsx`'s inline `myTeam` derivation to `useMyTeam()`. `PieceOverlay.tsx` is a VERIFY-ONLY target: RESEARCH Pitfall 5 established its single `palette.uiColor` is a field pass-through inside an object reconstruction (not a TEAM_CONFIGS lookup), so it must NOT be force-fitted to the hook.

Purpose: These are the color-duplication call sites named in D-04. Consolidating them removes the inline `TEAM_CONFIGS[...].palette.uiColor` cruft while preserving exact color output (including the miss-fallback and tie colors).
Output: GameBoard.tsx and ActionLog.tsx import from `hooks/useTeamColors.ts`; PieceOverlay.tsx confirmed correct as-is.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-code-cleanup/32-CONTEXT.md
@.planning/phases/32-code-cleanup/32-RESEARCH.md
@.planning/phases/32-code-cleanup/32-PATTERNS.md
@packages/client/src/hooks/useTeamColors.ts
@packages/client/src/hooks/useMyTeam.ts
</context>

<artifacts_produced>

## Artifacts this phase produces (new symbols — Plan 03)

No new exported symbols. This plan migrates existing call sites onto Plan 32-02's `useTeamAccentColor` / `teamAccentColor` / `useMyTeam`. It may introduce local `const` color values (e.g. `homeColor`, `awayColor`, `teamColor`, `secondHalfTeamColor`) at the top of the GameBoard component body — these are local variables, not module exports.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate GameBoard.tsx color sites to useTeamAccentColor and myTeam to useMyTeam</name>
  <files>packages/client/src/components/GameBoard.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx — the 10 `palette.uiColor` sites at lines 179, 201, 208, 210, 271, 311, 354, 383, 409, 425 and the null-safe `myTeam` at line 197; read enough surrounding context to see which are component-body consts vs inline-JSX (note: lines 271/311/354/383/409/425 are inline JSX `style={{ color: ... }}`, NOT inside `.map()`)
    - packages/client/src/hooks/useTeamColors.ts and useMyTeam.ts (exact export signatures from Plan 32-02's SUMMARY)
    - .planning/phases/32-code-cleanup/32-RESEARCH.md § Common Pitfalls (rules-of-hooks: never call the hook inside `.map()`/conditionals)
  </read_first>
  <action>
    At the top of the GameBoard component body, resolve the needed accent colors ONCE as `const` values using `useTeamAccentColor(...)`: `const homeColor = useTeamAccentColor(selectedTeams['home']);`, `const awayColor = useTeamAccentColor(selectedTeams['away']);`, `const teamColor = useTeamAccentColor(selectedTeams[activeTeam]);` (replacing line 179), and `const secondHalfTeamColor = useTeamAccentColor(selectedTeams[secondHalfKickOffTeam]);` (replacing line 201). Replace the winner-color ternary at lines 207-211 so its two lookup branches use `homeColor` / `awayColor` while PRESERVING the `'#e0e0e0'` tie branch exactly (`score.home > score.away ? homeColor : score.away > score.home ? awayColor : '#e0e0e0'`). Replace the six inline JSX `style={{ color: TEAM_CONFIGS[selectedTeams['home'|'away']].palette.uiColor }}` at lines 271/311/354/383/409/425 with references to `homeColor` / `awayColor`. Replace the inline `myTeam` derivation at line 197 with `const myTeam = useMyTeam();`. Import `useTeamAccentColor` from `../hooks/useTeamColors.js` and `useMyTeam` from `../hooks/useMyTeam.js`. Behavior note: lines 179/201 currently have NO miss-fallback (they would throw on an undefined team); routing them through `teamAccentColor`'s `?? '#888888'` only hardens the never-hit undefined case and does not change normal-flow output — record this as intentional. After migration, remove the now-unused `TEAM_CONFIGS` import ONLY if no other usage remains in the file (grep first). Do NOT call `useTeamAccentColor` inside any `.map()` or conditional.
  </action>
  <verify>
    <automated>grep -n "palette.uiColor" packages/client/src/components/GameBoard.tsx ; pnpm --filter @counter-attack/client test GameBoard 2>&1 | tail -25 ; pnpm --filter @counter-attack/client typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "palette.uiColor" packages/client/src/components/GameBoard.tsx` returns 0.
    - `grep -n "useTeamAccentColor" packages/client/src/components/GameBoard.tsx` and `grep -n "useMyTeam" ...` both match; the import paths reference `../hooks/`.
    - The `'#e0e0e0'` tie color still appears in the winner ternary (behavior preserved).
    - `pnpm --filter @counter-attack/client test GameBoard` passes (existing GameBoard.test.tsx assertions — including any color-literal assertions — still hold; if a color-literal assertion references a value now sourced through the hook, it must still resolve to the identical color string).
    - `pnpm --filter @counter-attack/client typecheck` reports no errors; `pnpm lint` shows no react-hooks violation for GameBoard (no hook call in a loop/conditional).
  </acceptance_criteria>
  <done>GameBoard.tsx sources all accent colors via useTeamAccentColor and myTeam via useMyTeam, with identical color output and passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ActionLog.tsx color helpers to the PURE teamAccentColor</name>
  <files>packages/client/src/components/ActionLog.tsx</files>
  <read_first>
    - packages/client/src/components/ActionLog.tsx lines 1-16 (pieceColorOf, uses `useGameStore.getState()` — a module-level plain function, NOT a component) and lines 30-45 (slotTeamColor, same shape), and the `consolidateEvents`/`P`/`PNamed` call sites that invoke these helpers inside loops
    - packages/client/src/hooks/useTeamColors.ts (the pure `teamAccentColor` export)
    - .planning/phases/32-code-cleanup/32-RESEARCH.md § Common Pitfalls 1 (these helpers run inside `.map()` loops — they MUST call the pure fn, never the hook)
  </read_first>
  <action>
    In `pieceColorOf` (line 15) and `slotTeamColor` (line 44), replace the trailing `return TEAM_CONFIGS[teamId]?.palette.uiColor ?? '#888888';` with `return teamAccentColor(teamId);` — an exact behavior match (teamAccentColor is defined as `TEAM_CONFIGS[teamId]?.palette.uiColor ?? '#888888'` with the same undefined guard). Keep the existing `if (!selectedTeams) return '#888888';` guards unchanged (that is a distinct selectedTeams-absence check). Import the PURE `teamAccentColor` (NOT `useTeamAccentColor`) from `../hooks/useTeamColors.js`. Do NOT convert these module-level functions into hooks and do NOT call any `use*` hook inside them (Pitfall 1 — they are invoked from consolidateEvents loops, not component render). Remove the now-unused `TEAM_CONFIGS` import from ActionLog.tsx ONLY if no other usage remains (grep first).
  </action>
  <verify>
    <automated>grep -n "palette.uiColor" packages/client/src/components/ActionLog.tsx ; pnpm --filter @counter-attack/client test ActionLog 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "palette.uiColor" packages/client/src/components/ActionLog.tsx` returns 0.
    - `grep -n "teamAccentColor" packages/client/src/components/ActionLog.tsx` matches and the import is the PURE `teamAccentColor` (no `useTeamAccentColor` import in this file).
    - No `use*` hook is called inside `pieceColorOf` or `slotTeamColor` (they remain plain module-level functions).
    - `pnpm --filter @counter-attack/client test ActionLog` passes (existing ActionLog tests, including team-color assertions, still hold).
    - `pnpm lint` shows no `react-hooks/rules-of-hooks` violation for ActionLog.tsx.
  </acceptance_criteria>
  <done>ActionLog.tsx's loop-invoked color helpers delegate to the pure teamAccentColor with identical output and no rules-of-hooks risk.</done>
</task>

<task type="auto">
  <name>Task 3: Verify PieceOverlay.tsx is a pass-through and leave it correct (Pitfall 5)</name>
  <files>packages/client/src/components/PieceOverlay.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx around line 107 (`uiColor: palette.uiColor` inside the `effectivePalette` object reconstruction) and the component's props (it receives a resolved `palette`/`TeamPalette`, plus `piece.teamId` which is positional 'home'/'away', NOT a roster TeamId)
    - packages/client/src/components/HexGrid.tsx line ~677 (where the real `TEAM_CONFIGS[resolvedTeamId]` lookup happens and `palette` is passed down to PieceOverlay)
    - .planning/phases/32-code-cleanup/32-RESEARCH.md § Common Pitfalls 5 (the full reasoning — this occurrence is a field copy to satisfy the TeamPalette type shape, not a duplicated derivation)
  </read_first>
  <action>
    Confirm by direct read that PieceOverlay.tsx's `palette.uiColor` at ~line 107 is a field pass-through inside the away/GK color-swap `effectivePalette` object literal — `palette` is already a fully-resolved prop from HexGrid.tsx, and PieceOverlay has no roster `TeamId` to pass to `useTeamAccentColor`. Make NO functional change: do not add a `useTeamAccentColor`/`teamAccentColor` import, do not thread a new `TeamId` prop. If helpful, add a one-line clarifying comment near line 107 noting this is a type-shape pass-through, not a TEAM_CONFIGS derivation (per Pitfall 5). Record in the SUMMARY that PieceOverlay was verified as out-of-scope for the color-hook migration (the CONTEXT.md D-04 scout's 3rd file was a substring false-match).
  </action>
  <verify>
    <automated>git -C d:/dev/repo/counter-attack-poc diff --stat packages/client/src/components/PieceOverlay.tsx ; pnpm --filter @counter-attack/client test PieceOverlay 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PieceOverlay.tsx has NO new `useTeamAccentColor`/`teamAccentColor` import and no new `TeamId` prop (diff shows at most a comment change).
    - If PieceOverlay.tsx has a test file, `pnpm --filter @counter-attack/client test PieceOverlay` passes; if none exists, confirm the file still typechecks via `pnpm --filter @counter-attack/client typecheck`.
    - The SUMMARY explicitly records PieceOverlay as verified-out-of-scope per Pitfall 5.
  </acceptance_criteria>
  <done>PieceOverlay.tsx confirmed a pass-through and left behavior-unchanged; documented as out-of-scope for the color migration.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                               |
| ---------- | ----------------------------------------------------------------------------------------- |
| (none new) | UI color-derivation refactor only; no new input surface, network path, or trust boundary. |

## STRIDE Threat Register

| Threat ID | Category  | Component                           | Disposition | Mitigation Plan                                                                                                                                                       |
| --------- | --------- | ----------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-32-04   | Tampering | GameBoard/ActionLog color rendering | accept      | Pure client-side derivation of static team config; behavior-identical migration guarded by existing component tests. No untrusted input (RESEARCH § Security Domain). |
| T-32-SC   | Tampering | npm/pip/cargo installs              | mitigate    | No package installs in this plan — no new dependencies introduced.                                                                                                    |

</threat_model>

<verification>
- `grep -c "palette.uiColor" packages/client/src/components/GameBoard.tsx` and `... ActionLog.tsx` both return 0.
- `pnpm --filter @counter-attack/client test GameBoard ActionLog PieceOverlay` passes.
- `pnpm --filter @counter-attack/client typecheck` and `pnpm lint` (react-hooks) clean for these files.
</verification>

<success_criteria>

- All real color-derivation call sites in GameBoard.tsx and ActionLog.tsx are consolidated onto the single canonical helper (D-04, CLEANUP-02), with identical color output.
- ActionLog's loop-invoked helpers use the pure function (Pitfall 1); GameBoard uses the hook in component body only.
- PieceOverlay confirmed a pass-through and left correct (Pitfall 5), preventing a wrong "add the hook here" change.
  </success_criteria>

<output>
Create `.planning/phases/32-code-cleanup/32-03-SUMMARY.md` when done. Record: GameBoard color-const structure, the PieceOverlay verify outcome, and confirmation the `#e0e0e0` tie color and #888888 miss-fallback are preserved.
</output>
