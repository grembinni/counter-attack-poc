---
phase: 32-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - knip.json
  - .github/workflows/ci.yml
  - packages/client/src/store/useGameStore.ts
autonomous: false
requirements: [CLEANUP-01]

must_haves:
  truths:
    - 'Running `pnpm knip` performs whole-workspace dead-code analysis across packages/shared, packages/server, and packages/client and reports zero flagged unused fields/functions/exports/files/dependencies (D-01, D-02).'
    - 'The confirmed-dead `shootTargetHex` field is completely removed from useGameStore.ts (declaration + all three null-write sites) and no reference to it remains anywhere in packages/client.'
    - 'knip is a permanent CI gate: `.github/workflows/ci.yml` runs `pnpm knip` after the shared build and before typecheck, so future dead code fails CI (D-02).'
  artifacts:
    - path: 'knip.json'
      provides: 'Root knip workspaces config for the pnpm monorepo (shared/server/client entry + project globs)'
      contains: 'workspaces'
    - path: 'package.json'
      provides: 'Root `knip` script that builds shared first then runs knip, plus knip devDependency'
      contains: '"knip"'
    - path: '.github/workflows/ci.yml'
      provides: 'CI step running pnpm knip after shared build, before typecheck'
      contains: 'pnpm knip'
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'shootTargetHex field and all its write sites removed'
  key_links:
    - from: 'package.json knip script'
      to: 'packages/shared/dist'
      via: 'pnpm --filter @counter-attack/shared build && knip (Pitfall 6 ordering)'
      pattern: 'shared build.*&&.*knip'
    - from: '.github/workflows/ci.yml knip step'
      to: 'knip.json workspaces config'
      via: 'pnpm knip resolves each package entry/project'
      pattern: 'pnpm knip'
---

<objective>
CLEANUP-01: Install **knip 6.29.0** (or 6.28.0 per the human-verify gate) as a permanent, CI-enforced dead-code detection gate for the pnpm workspace, create its root `knip.json` config, and remove all genuine dead code it flags — starting with the confirmed-dead `shootTargetHex` field in `useGameStore.ts` (the concrete example named in ROADMAP.md's success criteria).

Purpose: Dead code accumulates silently across an 11-phase-deep codebase; a whole-project analyzer wired into CI (D-02) makes future dead code fail the build instead of rotting. knip is pnpm-workspace-aware and detects unused exports/files/dependencies in one pass (D-01).
Output: `knip.json` at repo root, a root `knip` script, a `pnpm knip` CI step (correctly ordered after the shared build), knip added as a devDependency, and `shootTargetHex` plus any other flagged dead code removed — `pnpm knip` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-code-cleanup/32-CONTEXT.md
@.planning/phases/32-code-cleanup/32-RESEARCH.md
@.planning/phases/32-code-cleanup/32-PATTERNS.md
</context>

<artifacts_produced>

## Artifacts this phase produces (new symbols — Plan 01)

- **`knip.json`** — new repo-root config file with a `workspaces` object covering `packages/shared`, `packages/server`, `packages/client` (each with `entry` + `project` globs) and the `$schema` key `https://unpkg.com/knip@6/schema.json`.
- **`"knip"` root package.json script** — value `pnpm --filter @counter-attack/shared build && knip`.
- **`knip` devDependency** — added to root `package.json` via `pnpm add -Dw knip@<approved version>`.
- **`pnpm knip` CI step** — new step in `.github/workflows/ci.yml`, positioned after `pnpm --filter @counter-attack/shared build` and before `pnpm -r typecheck`.
- **Removed (not added):** the `shootTargetHex` field on the store interface and its initial-state / setGameState / setShootingMode write sites.

No new runtime functions, classes, or exported symbols are created by this plan.
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify knip package legitimacy before install ([SUS] gate — T-32-SC)</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/32-code-cleanup/32-RESEARCH.md § Package Legitimacy Audit (documents why knip's [SUS] verdict is a "too-new" heuristic false-positive: 6.29.0 was published 2 days before research; knip has a multi-year history, ~11.6M weekly downloads, and the well-known repo github.com/webpro-nl/knip)
  </read_first>
  <action>Blocking-human checkpoint: pause execution and present the knip legitimacy summary below to the developer BEFORE any install runs. This is a package-legitimacy gate ([SUS] verdict) and is never auto-approvable regardless of workflow.auto_advance. Do not proceed to Task 2 until the developer replies with approval and the pinned version (6.29.0 or 6.28.0).</action>
  <what-built>knip is proposed as a new repo-root devDependency (the dead-code gate). The package-legitimacy seam flagged it [SUS] SOLELY on the too-new publish timestamp of the latest version (6.29.0) — not a real supply-chain concern. This checkpoint exists so a human confirms legitimacy before install; it is a blocking-human gate and is never auto-approvable.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/knip and confirm: repository is github.com/webpro-nl/knip, weekly downloads are in the millions, and the version history is long and steady (not a single freshly-published release).
    2. Confirm the maintainer/repo matches the established knip project (author "webpro").
    3. Choose the version to install: accept `6.29.0`, OR pin `6.28.0` (previous minor, functionally equivalent for this phase) to avoid the just-published signal entirely.
  </how-to-verify>
  <verify><human-check>Developer confirms knip's npmjs.com page shows the established webpro-nl/knip project (multi-million weekly downloads) and states the version to install.</human-check></verify>
  <resume-signal>Reply "approved" (optionally "approved, pin 6.28.0") to authorize the install, or describe concerns to halt.</resume-signal>
  <acceptance_criteria>
    - A human has confirmed knip's npm page shows the established webpro-nl/knip project with multi-million weekly downloads.
    - The exact version to install (6.29.0 or 6.28.0) is explicitly stated in the resume signal and recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Human authorizes the knip install and the pinned version is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Install knip, add knip.json, root script, and the CI gate step</name>
  <files>package.json, pnpm-lock.yaml, knip.json, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/32-code-cleanup/32-RESEARCH.md § Architecture Patterns Pattern 2 (the workspaces config template, grounded in this repo) and § Common Pitfalls 6 (dist/-must-be-built ordering)
    - package.json (root) — scripts block (lines 7-15: build/test/lint/typecheck flat-key style) and devDependencies (lines 25-33)
    - .github/workflows/ci.yml — current steps (install → shared build → typecheck → test → build); the knip step must go AFTER `pnpm --filter @counter-attack/shared build` and BEFORE `pnpm -r typecheck`
    - packages/shared/src/index.ts (barrel export — the real public entry surface) and packages/shared/scripts/seed-rosters.ts (standalone dev script invoked via pnpm seed:rosters, never imported — must be its own entry)
    - eslint.config.js line 15 (`allowDefaultProject: ['packages/*/scripts/*.ts']`) — mirror this scripts exception in knip's shared entry list
    - packages/client/vite.config.ts and packages/{shared,server,client}/vitest.config.ts (knip's Vite/Vitest plugins auto-detect index.html→main.tsx and the test globs — do NOT hand-enumerate test files)
  </read_first>
  <action>
    Install knip at the version approved in Task 1 via `pnpm add -Dw knip@<approved-version>`. Add a root package.json script keyed `"knip"` with value `pnpm --filter @counter-attack/shared build && knip` — the shared-build prefix guarantees packages/shared/dist exists so cross-package `@counter-attack/shared` imports resolve (Pitfall 6; shared's package.json main/exports point at dist/ which is gitignored). Create `knip.json` at repo root with `$schema` `https://unpkg.com/knip@6/schema.json` and a `workspaces` map: `packages/shared` → entry `["src/index.ts", "scripts/seed-rosters.ts"]`, project `["src/**/*.ts", "scripts/**/*.ts"]`; `packages/server` → entry `["src/main.ts", "src/index.ts"]`, project `["src/**/*.ts"]`; `packages/client` → entry `["src/main.tsx", "index.html"]`, project `["src/**/*.{ts,tsx}"]`. Do NOT add broad `ignore` patterns (knip guidance: a surprising result is usually a real finding or a config gap, not something to ignore). In `.github/workflows/ci.yml`, insert `- run: pnpm knip` as a new step immediately after the `- run: pnpm --filter @counter-attack/shared build` line and before `- run: pnpm -r typecheck`. Finally run `pnpm knip --debug` once to capture the finding inventory and confirm `@counter-attack/shared` imports resolve cleanly (no flood of unresolved-import errors — that would be the Pitfall-6 ordering bug, not dead code). Do NOT remove any flagged code in this task; capture the finding list for Task 3.
  </action>
  <verify>
    <automated>pnpm knip --debug 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `knip.json` exists at repo root with a `workspaces` object naming all three packages, each having `entry` and `project` keys.
    - `grep -q '"knip"' package.json` succeeds and the script value contains `pnpm --filter @counter-attack/shared build && knip`.
    - `.github/workflows/ci.yml` contains a `pnpm knip` step located after the shared build step and before `pnpm -r typecheck` (verify by line order).
    - `pnpm knip` runs to completion (exit code MAY be non-zero due to genuine dead-code findings) and its `--debug` output shows `@counter-attack/shared` imports RESOLVED — no unresolved-import flood across the shared package's exports.
    - The non-shootTargetHex findings (if any) are listed in the task output for Task 3 to act on.
  </acceptance_criteria>
  <done>knip is installed at the approved version, configured for the pnpm workspace, wired as a CI step after the shared build, and its finding inventory is captured.</done>
</task>

<task type="auto">
  <name>Task 3: Remove shootTargetHex and all other genuine knip-flagged dead code; pnpm knip reports zero</name>
  <files>packages/client/src/store/useGameStore.ts</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts — lines 60-75 (field decl at 69 `shootTargetHex: HexCoord | null;`), 270-278 (init at 274), 715-728 (setGameState write at 722), 925-932 (setShootingMode write at 930 `set({ shootingMode: on, shootTargetHex: null })`)
    - Task 2's captured knip finding inventory (the authoritative list of what else to remove)
    - .planning/phases/32-code-cleanup/32-CONTEXT.md D-01 (shootTargetHex confirmed dead — only ever assigned null at 274/722/930, never read anywhere in the client)
  </read_first>
  <action>
    Remove the `shootTargetHex` field entirely from useGameStore.ts: delete the interface field declaration at line 69, the initial-state assignment at line 274, the assignment inside setGameState at line 722, and the write inside `setShootingMode` at line 930 (change `set({ shootingMode: on, shootTargetHex: null })` to `set({ shootingMode: on })`). Before deleting, confirm the `HexCoord` import is still used elsewhere in the file; only remove the import if line 69 was its last use. Then confirm zero remaining `shootTargetHex` references across packages/client. Next work through the REST of Task 2's knip inventory: for each flagged unused export/field/file/dependency in packages/shared, packages/server, packages/client, remove it ONLY if it is genuinely dead — no dynamic/string-keyed access, and not a public API intentionally exported for external tooling. If a finding is actually a knip config gap (a legitimately-used entry point knip cannot see), fix the `entry`/`project` config in knip.json rather than deleting live code or adding an `ignore`. Re-run `pnpm knip` iteratively until it reports zero findings. (Note: the two new hooks created by Plan 32-02 in this same wave keep their exports live via their co-located tests, so they must not appear as unused — if they do, it is a knip config issue to fix, not dead code to delete.)
  </action>
  <verify>
    <automated>grep -rn "shootTargetHex" packages/client/src ; pnpm knip 2>&1 | tail -30 ; pnpm --filter @counter-attack/client test useGameStore 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "shootTargetHex" packages/client/src` returns no matches.
    - `pnpm knip` exits 0 with zero reported unused files/exports/dependencies (any residual item must be a documented, justified config-level entry, not live dead code).
    - `pnpm --filter @counter-attack/client test useGameStore` passes — removal did not break the store test suites (useGameStore.test.ts, useGameStore.rule11.test.ts).
    - Every non-shootTargetHex removal is listed in the SUMMARY with its knip category (unused export / file / dependency).
  </acceptance_criteria>
  <done>shootTargetHex and every other genuine knip finding are removed (or resolved as config), pnpm knip is green, and store tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                                                                                                           |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| developer / CI → npm registry | Installing knip pulls a new devDependency from the npm registry — the only boundary this plan crosses. No runtime/request boundary is touched (knip is build-time-only, never shipped in any bundle). |

## STRIDE Threat Register

| Threat ID | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                                |
| --------- | ---------------------- | --------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-32-SC   | Tampering              | knip devDependency install (npm supply chain) | mitigate    | knip flagged [SUS] on a too-new heuristic only; Task 1 is a blocking-human legitimacy checkpoint verifying webpro-nl/knip on npmjs.com before install (never auto-approvable). Version pinned in the lockfile. |
| T-32-01   | Tampering              | dead-code removal in useGameStore.ts          | accept      | Removing an unused field cannot alter runtime behavior (shootTargetHex is never read); store test suites guard against accidental removal of live code.                                                        |
| T-32-02   | Information disclosure | knip / CI output                              | accept      | knip reports file/symbol names only (already in the repo); no secrets are exposed. devDependency never ships to client/server production bundles (RESEARCH § Security Domain).                                 |

</threat_model>

<verification>
- `pnpm knip` exits 0 (zero unused files/exports/dependencies across all three packages).
- `.github/workflows/ci.yml` runs `pnpm knip` after the shared build and before typecheck.
- `grep -rn "shootTargetHex" packages/client/src` returns nothing.
- `pnpm --filter @counter-attack/client test useGameStore` passes.
</verification>

<success_criteria>

- An automated dead-code tool (knip) runs across shared/server/client and reports zero flagged unused fields/functions/exports — the confirmed-dead `shootTargetHex` is gone (ROADMAP success criterion 1, CLEANUP-01).
- knip is a permanent CI-enforced gate (D-02), not a one-time audit.
- The knip install passed a blocking-human legitimacy checkpoint (T-32-SC).
  </success_criteria>

<output>
Create `.planning/phases/32-code-cleanup/32-01-SUMMARY.md` when done. Record: the pinned knip version, the full list of dead-code items removed (with knip category), and any knip.json config adjustments made to resolve config-gap findings.
</output>
