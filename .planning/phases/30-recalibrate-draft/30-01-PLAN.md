---
phase: 30-recalibrate-draft
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/data/player-pool.csv
  - packages/shared/src/teams.ts
  - packages/shared/src/types.ts
  - packages/shared/src/draftEngine.ts
  - packages/shared/src/draftEngine.test.ts
autonomous: true
requirements: [DRAFT-04, DRAFT-11]
must_haves:
  truths:
    - 'teams.ts PLAYER_POOL reflects the finished CSV rebalance (Alex Mățan / Carlo Holse present; João Klauss / Mykhi Joyner / Sang-bin Jeong absent; 188 rows)'
    - "Every player's tier is a pure function of their own total stat via classifyTier — chase>=32, rare===31, uncommon 29-30, common<29 (D-03)"
    - "GK cards are classified by the identical thresholds as outfield cards; classifyTier never returns 'keeper' (D-04/D-05)"
    - "isInPool returns true for a 'legend'-tagged player under poolId 'legends' and an 'icon'-tagged player under 'icons' (D-09)"
    - 'DraftTier is a 4-value union; the shared package typechecks and its test suite is green'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'DraftTier (4 values), TIER_STAT_THRESHOLDS, DRAFT_ROUNDS/RoundConfig/PackSlot/DRAFT_ROUND_COUNT/PACKS_PER_ROUND, round-aware DraftSession/DraftClientView'
      contains: 'TIER_STAT_THRESHOLDS'
    - path: 'packages/shared/src/draftEngine.ts'
      provides: 'classifyTier, simplified assignTiers, isInPool legend/icon bridge, DraftPack.round'
      contains: 'export function classifyTier'
    - path: 'packages/shared/src/teams.ts'
      provides: 'Regenerated PLAYER_POOL from the finished CSV'
      contains: 'Carlo Holse'
  key_links:
    - from: 'packages/shared/src/draftEngine.ts'
      to: 'packages/shared/src/types.ts'
      via: 'classifyTier imports TIER_STAT_THRESHOLDS + DraftTier'
      pattern: 'TIER_STAT_THRESHOLDS'
    - from: 'packages/shared/src/draftEngine.ts'
      to: 'packages/shared/src/teams.ts'
      via: 'PLAYER_POOL import feeds resolvePoolPlayers/assignTiers'
      pattern: 'PLAYER_POOL'
---

<objective>
Lay the shared-package foundation for the recalibrated draft: finish the player-pool data edit and regenerate the derived `teams.ts`, then rewrite the draft type/constant contract and the tier-classification engine to a fixed-absolute-threshold model with Legends/Icons pool support.

Purpose: Every downstream computation (pack generation, session state machine, client tier colors) reads from `teams.ts` and consumes the `types.ts` draft contract. This plan is the interface-first contract all Wave 2 plans build against. It also switches tier classification from session-relative percentile ranking to fixed absolute total-stat thresholds (DRAFT-04) and enables the Legends/Icons pools (DRAFT-11 partial).

Output: Regenerated `teams.ts`; a 4-value `DraftTier`; `TIER_STAT_THRESHOLDS`, `DRAFT_ROUNDS`, and round-aware session types; `classifyTier`; a simplified `assignTiers`; and the `isInPool` PoolTag→DraftPoolId bridge — all green in the shared package.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-recalibrate-draft/30-CONTEXT.md
@.planning/phases/30-recalibrate-draft/30-RESEARCH.md
@.planning/phases/30-recalibrate-draft/30-PATTERNS.md
@.planning/phases/30-recalibrate-draft/30-VALIDATION.md
@packages/shared/src/draftEngine.ts
@packages/shared/scripts/seed-rosters.ts
</context>

## Artifacts this phase produces (Plan 01)

New symbols introduced by this plan (exclude from source-drift verification):

- `TIER_STAT_THRESHOLDS` (const, types.ts) — `{ chase: 32, rare: 31, uncommon: 29 }`
- `DRAFT_ROUNDS` (const, types.ts), `RoundConfig` (type), `PackSlot` (type), `DRAFT_ROUND_COUNT` (const), `PACKS_PER_ROUND` (const)
- `classifyTier` (function, draftEngine.ts)
- `POOL_TAG_TO_DRAFT_POOL` (const, draftEngine.ts)
- `DraftPack.round` (new field on the existing `DraftPack` interface)
- `DraftSession.round` / `DraftClientView.round` (renamed from `cycle`)

Removed symbols (were valid before this phase; their disappearance is intentional, not drift):

- `TIER_PERCENTILE_BOUNDS`, `PACKS_PER_MATCH`, `PACK_COMPOSITION` (types.ts)
- `'keeper'` as a `DraftTier` value
- `DraftSession.homeHasKeeper` / `awayHasKeeper` / `keeperAutoPickedThisCycle`; `DraftClientView.keeperAutoPickedThisCycle`
- `OUTFIELD_TIERS`, `FALLBACK_POOL_ORDER` (draftEngine.ts — `FALLBACK_POOL_ORDER` is re-introduced reordered in Plan 02)

<tasks>

<task type="auto">
  <name>Task 1: Finish CSV edit + regenerate teams.ts</name>
  <files>packages/shared/src/data/player-pool.csv, packages/shared/src/teams.ts</files>
  <read_first>
    - packages/shared/src/data/player-pool.csv (the in-progress working-tree edit — the intended final data state)
    - packages/shared/scripts/seed-rosters.ts (the CSV→teams.ts codegen; note the GK `highPass=0` override at ~line 157 and `EXPECTED_TOTAL = 188` fail-fast at ~line 370)
    - packages/shared/package.json (the `seed:rosters` script)
  </read_first>
  <action>
    Confirm the working-tree CSV reflects D-01: the City roster swap is applied (Alex Mățan and Carlo Holse present as FWD rows; João Klauss, Mykhi Joyner, Sang-bin Jeong absent) and the broad stat rebalance is in place. Per D-02, LEAVE the literal `TBD` City ST row (stats 4,4,3,6,3,3,5) exactly as-is — do NOT rename it or block on it. Do not otherwise edit stat values (the working-tree rebalance is the intended final state). Then regenerate the derived file by running `pnpm --filter @counter-attack/shared run seed:rosters` from the repo root, which rewrites `packages/shared/src/teams.ts`. The seed script's `EXPECTED_TOTAL = 188` assertion is the gate — the current CSV has exactly 188 data rows so it must pass unchanged; if it fails, the CSV row count drifted from D-01 and must be reconciled before proceeding (do not silently bump EXPECTED_TOTAL). Commit both the CSV and the regenerated teams.ts together (single derived-artifact unit).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared run seed:rosters</automated>
  </verify>
  <acceptance_criteria>
    - `seed:rosters` exits 0 (its internal `EXPECTED_TOTAL === 188` assertion passes)
    - `grep -c "Carlo Holse" packages/shared/src/teams.ts` returns >= 1 and `grep -c "João Klauss\|Mykhi Joyner\|Sang-bin Jeong" packages/shared/src/teams.ts` returns 0
    - The `TBD` City ST row remains present in player-pool.csv (D-02 untouched)
  </acceptance_criteria>
  <done>teams.ts is regenerated from the finished CSV, the 188-player assertion passes, and the roster swap is reflected in the generated PLAYER_POOL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the draft type/constant contract in types.ts</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts (lines ~452-618 — the full draft block: DraftPoolId, SELECTABLE_DRAFT_POOLS, DraftTier, TIER_PERCENTILE_BOUNDS, PACKS_PER_MATCH, PACK_COMPOSITION, DraftSubStep, DraftSession, DraftClientView)
    - .planning/phases/30-recalibrate-draft/30-RESEARCH.md (Architecture Patterns → Pattern 2 proposed DRAFT_ROUNDS shape)
    - .planning/phases/30-recalibrate-draft/30-PATTERNS.md (types.ts section — doc-comment convention citing D-numbers)
  </read_first>
  <behavior>
    - DraftTier narrows to exactly `'chase' | 'rare' | 'uncommon' | 'common'` (no 'keeper').
    - SELECTABLE_DRAFT_POOLS widens to all 5 pools: `['original','mls','international','legends','icons']` (D-08).
    - TIER_STAT_THRESHOLDS exports `{ chase: 32, rare: 31, uncommon: 29 }` (D-03).
    - DRAFT_ROUNDS is a 6-entry readonly config array whose picks sum to 17 (D-16): round 1 kind 'gk' picks 2; rounds 2-3 kind 'tiered' picks 3 slots all-common; round 4 slots 2 uncommon + 2 common; rounds 5-6 slots 1 chaseOrRare + 1 uncommon + 2 common (D-12..D-15). Each entry cardsPerPack 4.
    - DRAFT_ROUND_COUNT === 6; PACKS_PER_ROUND === 2.
    - DraftSession/DraftClientView carry `round` (not `cycle`) and no keeper fields.
  </behavior>
  <action>
    Replace the superseded constants. DELETE `TIER_PERCENTILE_BOUNDS`, `PACKS_PER_MATCH`, and `PACK_COMPOSITION`. Narrow `DraftTier` to `'chase' | 'rare' | 'uncommon' | 'common'` (D-05 removes 'keeper'). Widen `SELECTABLE_DRAFT_POOLS` to `['original','mls','international','legends','icons'] as const` (D-08). Add `TIER_STAT_THRESHOLDS = { chase: 32, rare: 31, uncommon: 29 } as const` (D-03). Add the round-structure model per RESEARCH Pattern 2: a `PackSlot` type (`{ tier: 'common'|'uncommon'|'chase'|'rare'; count: number } | { tier: 'chaseOrRare'; count: number }`), a `RoundConfig` discriminated union (`{ round: number; kind: 'gk'; cardsPerPack: 4; picks: 2 } | { round: number; kind: 'tiered'; cardsPerPack: 4; picks: 3; slots: PackSlot[] }`), and `DRAFT_ROUNDS: readonly RoundConfig[]` with the six entries from D-12..D-15 (round 1 gk/picks 2; rounds 2 and 3 tiered/picks 3/slots `[{tier:'common',count:4}]`; round 4 slots `[{tier:'uncommon',count:2},{tier:'common',count:2}]`; rounds 5 and 6 slots `[{tier:'chaseOrRare',count:1},{tier:'uncommon',count:1},{tier:'common',count:2}]`). Add `DRAFT_ROUND_COUNT = DRAFT_ROUNDS.length` and `PACKS_PER_ROUND = 2`. Keep `DraftSubStep = 'PICK1'|'PICK2'|'PICK3'` unchanged. In `DraftSession`: rename `cycle` to `round`; DELETE `homeHasKeeper`, `awayHasKeeper`, and `keeperAutoPickedThisCycle`; keep `homePackOrder`/`awayPackOrder` but redocument them as per-round pack-index maps (length 6, one entry per round). In `DraftClientView`: rename `cycle` to `round`; DELETE `keeperAutoPickedThisCycle`. Preserve the existing `/** D-NN: ... */` doc-comment convention on every new/changed export, citing D-03/D-05/D-08/D-12..D-16/D-19. Do not touch non-draft types in this file.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared exec tsc --noEmit -p tsconfig.json 2>&1 | grep -v "draftEngine\|generateDraftPacks" | grep -c "error TS" | grep -qx 0 && echo TYPES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "keeper" packages/shared/src/types.ts` returns 0 in the draft block (no 'keeper' DraftTier value, no keeper session fields)
    - `grep -c "TIER_PERCENTILE_BOUNDS\|PACKS_PER_MATCH\|PACK_COMPOSITION" packages/shared/src/types.ts` returns 0
    - DRAFT_ROUNDS has 6 entries and `DRAFT_ROUNDS.reduce((s,r)=>s+r.picks,0)` equals 17 (D-16)
    - `DraftSession` and `DraftClientView` expose `round` and no `cycle`/keeper fields
  </acceptance_criteria>
  <done>types.ts defines the full new draft contract: 4-value DraftTier, TIER_STAT_THRESHOLDS, the 6-round DRAFT_ROUNDS table summing to 17 picks, and round-aware/keeper-free session types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: classifyTier + simplified assignTiers + isInPool bridge + DraftPack.round; rewrite classification tests</name>
  <files>packages/shared/src/draftEngine.ts, packages/shared/src/draftEngine.test.ts</files>
  <read_first>
    - packages/shared/src/draftEngine.ts (full — computeTotalStat ~47-59, isInPool ~71-81, assignTiers ~108-146, DraftPack ~163-166, generateDraftPacks ~225-399, FALLBACK_POOL_ORDER ~189, OUTFIELD_TIERS ~192)
    - packages/shared/src/draftEngine.test.ts (existing describe blocks — the TIER_PERCENTILE_BOUNDS assertion is Pitfall 8 and must be inverted)
    - .planning/phases/30-recalibrate-draft/30-RESEARCH.md (Pattern 1 classifyTier, Pattern 5 isInPool bridge, Common Pitfall 1 GK distribution + D-24)
  </read_first>
  <behavior>
    - classifyTier(32)==='chase', classifyTier(33)==='chase', classifyTier(31)==='rare', classifyTier(30)==='uncommon', classifyTier(29)==='uncommon', classifyTier(28)==='common' (D-03 boundaries).
    - assignTiers maps every player (GK and outfield alike) to classifyTier(computeTotalStat(player)); no percentile ranking, no 'keeper'.
    - isInPool(legendTaggedPlayer,'legends')===true; isInPool(iconTaggedPlayer,'icons')===true; isInPool(untaggedFreeAgent,'legends')===false.
    - Some GKs legitimately classify as 'common' — tests MUST NOT assert "no GK is ever common" (D-24).
  </behavior>
  <action>
    Update the module doc comment to describe fixed-absolute-threshold classification (drop the D-05/D-06/D-08 percentile language). Add `classifyTier(totalStat: number): DraftTier` directly below `computeTotalStat`, mirroring its pure-function shape: returns 'chase' when `totalStat >= TIER_STAT_THRESHOLDS.chase`, 'rare' when `totalStat === TIER_STAT_THRESHOLDS.rare`, 'uncommon' when `totalStat >= TIER_STAT_THRESHOLDS.uncommon`, else 'common' (D-03/D-04). Import `TIER_STAT_THRESHOLDS` from types.js and drop the `PACKS_PER_MATCH`/`PACK_COMPOSITION`/`TIER_PERCENTILE_BOUNDS` imports. Rewrite `assignTiers` to `players.map((p) => ({ ...p, tier: classifyTier(computeTotalStat(p)), totalStat: computeTotalStat(p) }))` — delete the outfield filter, stable-sort, percentile-rank loop, and the GK→'keeper' loop entirely (D-04). Add `POOL_TAG_TO_DRAFT_POOL: Readonly<Record<'legend'|'icon', DraftPoolId>> = { legend: 'legends', icon: 'icons' }` and replace `isInPool`'s final `return false;` branch with `return player.poolTag !== undefined && POOL_TAG_TO_DRAFT_POOL[player.poolTag] === poolId;` (D-09), keeping the existing original/mls/international branches and the optional-chaining convention. Add a required `round: number` field to the `DraftPack` interface (1-based round number, consumed by the state machine in Plan 03). DELETE the now-unused `OUTFIELD_TIERS` and `FALLBACK_POOL_ORDER` consts (Plan 02 re-introduces FALLBACK reordered). Replace the `generateDraftPacks` body with a temporary compiling stub that keeps the existing CR-01 input guard (`selectedPools` must be a non-empty subset of `SELECTABLE_DRAFT_POOLS`, throw on violation) and then `throw new Error('generateDraftPacks: round-structured implementation lands in 30-02')`; keep its `{ pool: TieredPoolPlayer[]; packs: DraftPack[] }` return signature so the shared package typechecks. In draftEngine.test.ts: rewrite the classification describe block to assert classifyTier's boundaries and assignTiers' per-player mapping (invert the old `TIER_PERCENTILE_BOUNDS` equality assertion — Pitfall 8), add isInPool legend/icon bridge cases, and DELETE the entire old generateDraftPacks describe block (Plan 02 writes fresh pack tests). Per D-24, do not add any assertion forbidding a GK from being 'common'.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test draftEngine</automated>
  </verify>
  <acceptance_criteria>
    - draftEngine.test.ts passes and contains classifyTier boundary cases plus isInPool legend/icon cases
    - `grep -c "'keeper'\|TIER_PERCENTILE_BOUNDS\|PACK_COMPOSITION\|PACKS_PER_MATCH\|OUTFIELD_TIERS" packages/shared/src/draftEngine.ts` returns 0
    - No test asserts that a GK is never 'common' (D-24)
    - `pnpm --filter @counter-attack/shared exec tsc --noEmit -p tsconfig.json` reports 0 errors (stub keeps generateDraftPacks compiling)
  </acceptance_criteria>
  <done>classifyTier is the single fixed-threshold classifier reused everywhere; assignTiers is a pure per-player map with no percentile logic; isInPool bridges the singular PoolTag to the plural DraftPoolId; DraftPack carries a round field; and the shared package typechecks and tests green with generateDraftPacks stubbed for Plan 02.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                         |
| --------------------------------------- | --------------------------------------------------------------------------------------------------- |
| client → server (ROOM_SETTINGS_CONFIRM) | Untrusted `draftPools` array crosses here; validated against SELECTABLE_DRAFT_POOLS                 |
| build-time CSV → generated teams.ts     | Data pipeline; no runtime trust boundary, but stale output silently corrupts every stat computation |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                    | Disposition | Mitigation Plan                                                                                                                                                                                   |
| --------- | --------- | ------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-30-01   | Tampering | SELECTABLE_DRAFT_POOLS allow-list (widened to legends/icons) | mitigate    | Keep the const the single source of truth; the server ROOM_SETTINGS_CONFIRM handler (Plan 05) already validates against it — widening it here is a controlled allow-list change, not a new bypass |
| T-30-DATA | Tampering | Stale teams.ts vs finished CSV                               | mitigate    | Task 1 regenerates teams.ts and gates on the `EXPECTED_TOTAL === 188` fail-fast assertion; grep confirms roster swap landed                                                                       |
| T-30-SC   | Tampering | npm/pnpm installs                                            | accept      | Zero new packages this phase (RESEARCH Package Legitimacy Audit: Not applicable) — no install surface introduced                                                                                  |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test` is green.
- `pnpm --filter @counter-attack/shared exec tsc --noEmit -p tsconfig.json` reports 0 errors.
- Note (interface-first handoff): the `server` and `client` packages do NOT yet typecheck against the narrowed DraftTier / renamed round fields — that is intentional and resolved by the Wave 2 plans (30-02 shared pack gen, 30-03 server, 30-04 client). Full-workspace `pnpm typecheck` greens after Wave 2.
</verification>

<success_criteria>

- teams.ts regenerated from the finished CSV; 188-player assertion passes; roster swap present.
- DraftTier is 4 values; TIER_STAT_THRESHOLDS and the 6-round DRAFT_ROUNDS (17 picks) exist.
- classifyTier + simplified assignTiers + isInPool legend/icon bridge implemented and unit-tested.
- Shared package typechecks and its test suite is green.
  </success_criteria>

<output>
Create `.planning/phases/30-recalibrate-draft/30-01-SUMMARY.md` when done.
</output>
