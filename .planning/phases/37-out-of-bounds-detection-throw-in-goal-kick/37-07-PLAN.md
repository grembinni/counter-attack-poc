---
phase: 37-out-of-bounds-detection-throw-in-goal-kick
plan: 07
type: execute
wave: 6
depends_on: [37-02, 37-05]
files_modified:
  - packages/client/src/components/ThrowInSetupPanel.tsx
  - packages/client/src/components/ThrowInSetupPanel.module.css
  - packages/client/src/components/ThrowInSetupPanel.test.tsx
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/store/useGameStore.ts
autonomous: true
requirements: [THROWIN-02, THROWIN-03, THROWIN-04]

must_haves:
  truths:
    - 'During THROW_IN_SETUP the throwing manager sees a Throw-In panel and the throw-in hex is highlighted on the pitch'
    - 'The non-active manager sees only a waiting message during THROW_IN_SETUP'
    - "Clicking one of the throwing team's pieces during THROW_IN_SETUP places that thrower at the throw-in hex"
    - "After a throw-in Movement Phase the action buttons read 'Standard Throw-In' / 'High Throw-In' / 'Move'; after the second one only the two throw buttons remain"
    - "Throw target highlights are capped at 6 hexes, matching the server's authoritative cap"
  artifacts:
    - path: 'packages/client/src/components/ThrowInSetupPanel.tsx'
      provides: 'Throw-in setup sidebar panel'
      min_lines: 60
    - path: 'packages/client/src/components/ThrowInSetupPanel.module.css'
      provides: 'Panel styles, 4-multiple spacing, no container border'
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'emitThrowInPlace + throw-in-aware pass target computation'
      contains: 'emitThrowInPlace'
  key_links:
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: 'ThrowInSetupPanel'
      via: 'phase-dispatch ternary'
      pattern: 'THROW_IN_SETUP'
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'emitThrowInPlace'
      via: 'piece click during THROW_IN_SETUP'
      pattern: 'emitThrowInPlace'
    - from: 'packages/client/src/store/useGameStore.ts'
      to: 'packages/shared/src/passValidator.ts'
      via: 'validatePass maxDistance in setSelectedPassType'
      pattern: 'maxDistance'
---

<objective>
Build the client surface for the throw-in: a `ThrowInSetupPanel` sidebar, pitch highlighting and click handling for thrower placement, throw-in-specific action button copy, and a 6-hex-capped target highlight set (THROWIN-02, THROWIN-03, THROWIN-04, D-07, D-08, D-09, D-10).

Purpose: the server-side flow from Plans 37-05/37-06 is unreachable without a UI. Per D-07 the panel follows Phase 35's locked conventions (`"Confirm"` verb, no container border, two-line title+detail helper text, `"{Team} is repositioning…"` waiting phrasing). Per D-08 pitch highlighting maps onto existing `HexHighlightType` members — no new tint types and no edit to `docs/HIGHLIGHT-REFERENCE.md`. Per D-10 the Low/High throw choice reuses the existing `ActionPanel` pass-choice pattern rather than inventing a new selection UI.

Output: `ThrowInSetupPanel` (+ CSS module + tests), `GameBoard` dispatch branch, `HexGrid` placement highlighting/click, `ActionPanel` throw-in labels, and store wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-CONTEXT.md
@.planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-UI-SPEC.md
@.planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-PATTERNS.md
@.planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-05-SUMMARY.md
@docs/HIGHLIGHT-REFERENCE.md
</context>

## Artifacts this phase produces

This plan produces **[37-07]** the `ThrowInSetupPanel` React component (`.tsx`, `.module.css`, `.test.tsx`) and the `emitThrowInPlace` store action.

Full phase inventory: see the "Artifacts this phase produces" section of `37-02-PLAN.md`.

## Decisions locked by this plan (D-07 / D-08 / D-10 discretion resolved)

- **D-07 resolution:** the panel adopts Phase 35's conventions in full — `.panel` with no `border` rule, a 12px/700 `.panelHeading`, 11px/400 `.constraintRow` rows, the `"Confirm"` verb for its one CTA, and the verbatim `"{Attacking/Defending} team is repositioning…"` waiting string. The one adaptation: the panel's CTA is **not** an end-turn button (placement is the only action and it completes the phase), so the panel renders a disabled-until-selected status row plus the shared `ctaColorClass` colour state, and no `withEndTurnConfirm` dialog (there is no "N players left" concept during a single placement).
- **D-08 resolution:** the throw-in hex uses the existing `'safe'` `HexHighlightType` (green — the established "valid placement/destination" tint) and the throwing team's selectable pieces use the existing `selectionState='selectable'` ring already used during `KICK_OFF_SETUP`/`FREE_KICK_SETUP`. No new `HexHighlightType` member, no `RING_STYLES` change, no `docs/HIGHLIGHT-REFERENCE.md` edit.
- **D-10 resolution:** Low/High is chosen through the existing `ActionPanel` `selectedPassType` two-step flow (choose action → choose target), with only the button labels and tooltips changed in a throw-in context. No new component, no new store state.
- **Spacing (UI-SPEC):** `ThrowInSetupPanel.module.css` uses `gap: 4px` and `padding: 4px 8px`, NOT the legacy 3px / `4px 6px` values in `FreeKickSetupPanel.module.css`. The UI-SPEC explicitly forbids propagating those inherited non-4-multiple values into new components.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ThrowInSetupPanel component, styles and tests</name>
  <files>packages/client/src/components/ThrowInSetupPanel.tsx, packages/client/src/components/ThrowInSetupPanel.module.css, packages/client/src/components/ThrowInSetupPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/FreeKickSetupPanel.tsx — the full 226-line file: the early-return-`null` guard block (48-59) that narrows `myTeamOrNull`, the inactive-team waiting branch (73-82), the constraint-row + `disabledTitle` pattern (84-110), the `ctaColorClass` usage (174-178) and the final render (180-225)
    - packages/client/src/components/FreeKickSetupPanel.module.css — the class inventory to mirror (`.panel`, `.panelHeading`, `.constraintRow`, `.errorText`, `.ctaButton`, `.ctaButtonReady`, `.ctaButtonPending`); note `.panel` has NO `border` rule (Phase 35 D-01 lock) and note the legacy 3px/6px values this plan must NOT copy
    - packages/client/src/components/FreeKickSetupPanel.test.tsx — the test scaffolding: how it stubs `useGameStore` and `useMyTeam`, and how it asserts panel copy
    - packages/client/src/hooks/useMyTeam.ts and packages/client/src/utils/ctaColorClass.ts — the two shared helpers the panel uses
    - packages/client/src/store/useGameStore.ts — the selector-per-slice subscription style (`useGameStore((s) => s.gameState.X)`), required to avoid whole-component re-renders
    - .planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-UI-SPEC.md — Typography role mapping (Heading 12px/700, Label 11px/400/1.5), Color roles (`--color-bg-surface` panel, `--color-cta-ready-bg`/`--color-cta-pending-bg` CTA, `--color-danger` errors), the Spacing Scale exception note, and the Copywriting Contract rows for the waiting state and error state
  </read_first>
  <behavior>
    - Renders `null` when `phase !== 'THROW_IN_SETUP'`, when `throwInHex` is null/undefined, when `throwInTeam` is null/undefined, or when `useMyTeam()` returns null
    - When the viewer is NOT the throwing team: renders the heading and a single row reading `"{Attacking|Defending} team is repositioning…"` and no buttons
    - When the viewer IS the throwing team: renders the heading `"Throw-In"`, a two-line helper (title + detail) telling them to select a player to take the throw, a status row naming the selected thrower or stating none is selected, and a `"Confirm"` button
    - The `"Confirm"` button is `disabled` when no thrower is selected, and carries a `title` explaining the unmet constraint
    - Clicking `"Confirm"` with a selected piece calls `emitThrowInPlace(selectedPieceId)`
    - A non-null `gameError` from the store renders in an `.errorText` row
  </behavior>
  <action>
Create `packages/client/src/components/ThrowInSetupPanel.tsx` exporting `export function ThrowInSetupPanel()`.

Subscribe with per-slice selectors: `phase`, `throwInHex`, `throwInTeam`, `pieces`, `selectedPieceId`, `gameError`, and the `emitThrowInPlace` action. Use `useMyTeam()` for the viewer's team.

Structure, copying `FreeKickSetupPanel`'s shape:

1. Early-return-`null` guard block covering all four null conditions in `<behavior>`, narrowing `myTeamOrNull` to `'home' | 'away'` (do NOT coerce with `?? 'away'` — the explicit guard is the established defence-in-depth pattern).
2. `const isMyThrow = myTeam === throwInTeam;` — when false, return the waiting panel: `.panel` > `.panelHeading` reading `Throw-In` + a `.constraintRow` reading `` `${isMyThrow ? 'Attacking' : 'Defending'} team is repositioning…` `` using the `&hellip;` HTML entity exactly as `FreeKickSetupPanel` does. Since the viewer is by definition not the throwing team in this branch, the string resolves to `Defending team is repositioning…`; keep the phrasing verbatim per the Phase 35 lock.
3. Active branch: derive `const selectedPiece = pieces.find((p) => p.id === selectedPieceId && p.teamId === myTeam) ?? null;`. Render:
   - `.panelHeading`: `Throw-In`
   - two-line helper: line 1 `Throw-In!`, line 2 `Select a player to take the throw.` (title + detail, matching the Phase 35 two-line helper pattern; reuse `FreeKickSetupPanel`'s `.constraintRow` for both lines rather than inventing helper classes)
   - status `.constraintRow`: when a thrower is selected, `` `Thrower: #${selectedPiece.number} ${selectedPiece.firstName} ${selectedPiece.lastName}` ``; otherwise `No player selected.`
   - `{gameError && <span className={styles.errorText}>{gameError}</span>}`
   - the `"Confirm"` button: `className={`${styles.ctaButton} ${ctaColorClass(selectedPiece ? 0 : 1, { ready: styles.ctaButtonReady, pending: styles.ctaButtonPending }, true)}`}`, `disabled={selectedPiece === null}`, `title={selectedPiece === null ? 'Select one of your players to take the throw' : undefined}`, `onClick={() => selectedPiece && emitThrowInPlace(selectedPiece.id)}`. The literal button text is `Confirm` — the Phase 35 D-08 verb lock.

Create `packages/client/src/components/ThrowInSetupPanel.module.css` with `.panel`, `.panelHeading`, `.constraintRow`, `.errorText`, `.ctaButton`, `.ctaButton:hover:not(:disabled)`, `.ctaButton:disabled`, `.ctaButtonReady`, `.ctaButtonPending`. Copy every declaration from `FreeKickSetupPanel.module.css` EXCEPT: `.panel` uses `gap: 4px` and `padding: 4px 8px` (not `3px` / `4px 6px`), and `.ctaButton` uses `padding: 4px 8px` (not `3px 8px`). Do not add a `border` rule to `.panel`. Use only the `--color-*` and `--team-accent` custom properties already referenced by `FreeKickSetupPanel.module.css` — no raw hex values.

Create `packages/client/src/components/ThrowInSetupPanel.test.tsx` asserting every case in `<behavior>`, following `FreeKickSetupPanel.test.tsx`'s store-stubbing approach. Include an explicit assertion that the button's text content is exactly `Confirm` and that the waiting-state row's text matches `/team is repositioning/`.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- ThrowInSetupPanel</automated>
</verify>
<acceptance_criteria> - `pnpm --filter @counter-attack/client test -- ThrowInSetupPanel` exits 0 - `grep -c "border" packages/client/src/components/ThrowInSetupPanel.module.css` shows no `border` declaration inside the `.panel` rule (a `border` on `.ctaButton` is expected and allowed) - `grep -n "gap: 4px\|padding: 4px 8px" packages/client/src/components/ThrowInSetupPanel.module.css` returns at least 2 lines - `grep -n "3px" packages/client/src/components/ThrowInSetupPanel.module.css` returns 0 matches - `grep -cE "#[0-9a-fA-F]{3,6}" packages/client/src/components/ThrowInSetupPanel.module.css` returns 0 (tokens only) - `grep -n "Confirm" packages/client/src/components/ThrowInSetupPanel.tsx` returns exactly one button label occurrence - `grep -n "repositioning" packages/client/src/components/ThrowInSetupPanel.tsx` returns one line using the `&hellip;` entity
</acceptance_criteria>
<done>`ThrowInSetupPanel` renders the active and waiting states per the Phase 35 conventions, uses only design tokens and 4-multiple spacing, and its tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the panel, the pitch and the store</name>
  <files>packages/client/src/components/GameBoard.tsx, packages/client/src/components/HexGrid.tsx, packages/client/src/store/useGameStore.ts</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx lines 326-337 — the phase-dispatch ternary (the Pitfall-1 miss site flagged by the comment Plan 37-02 added) and lines 14-16 — the panel import block
    - packages/client/src/components/HexGrid.tsx lines 236-300 — `isKickOffSetup` / `isInMyKickOffZone` and the `isFreeKickSetup` / `activeTeamForStage` / `isInMyFreeKickZone` derivations, the two existing "restart placement zone" precedents evaluated per-hex on every render
    - packages/client/src/components/HexGrid.tsx lines 420-530 — the `highlightType` priority ternary and the per-hex `onClick` dispatch including the `KICK_OFF_SETUP` and `FREE_KICK_SETUP` branches
    - packages/client/src/components/HexGrid.tsx lines 640-800 — the per-piece click/selection eligibility block including the `KICK_OFF_SETUP` and `GK_KICK_MOVE` branches and the ring derivation
    - packages/client/src/components/HexCell.tsx lines 6-110 — the `HexHighlightType` union and `HIGHLIGHT_STYLES` / `RING_STYLES` tables (read-only for this plan; D-08 forbids adding members)
    - packages/client/src/store/useGameStore.ts lines 55-175 (action type declarations) and 985-1070 (the emitter implementations, e.g. `emitFreeKickMove`, `emitGKKickTarget`) and lines 420-495 (`setSelectedPassType`'s `validatePass` loop over `PITCH_HEXES`)
    - .planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-06-SUMMARY.md — the server-side `THROW_IN_MAX_DISTANCE` value and `isThrowInContext` predicate the client must mirror
    - docs/HIGHLIGHT-REFERENCE.md — the tint/ring contract; this plan must not modify it
  </read_first>
  <action>
**`useGameStore.ts`:**
1. Declare and implement `emitThrowInPlace: (pieceId: string) => void` next to `emitFreeKickMove`, implemented as `socket.emit(ClientEvents.GAME_THROW_IN_PLACE, pieceId)`. Add its type to the store's action interface in the same block as the other `emit*` declarations.
2. In `setSelectedPassType`, mirror the server's 6-hex throw cap so highlights match server-side legality. Derive `const isThrowIn = gameState.lastActionType === 'THROW_IN_MOVEMENT_1' || gameState.lastActionType === 'THROW_IN_MOVEMENT_2';` and pass `isThrowIn ? { maxDistance: 6 } : undefined` as `validatePass`'s 6th argument inside the `PITCH_HEXES` loop. Extract the literal `6` into a module-level `const THROW_IN_MAX_DISTANCE = 6;` in this file with a comment noting it mirrors `gameHandlers.ts` and that the server remains authoritative. Leave the interception-risk classification logic untouched — a throw is still interceptable exactly like the pass type it reuses.

**`GameBoard.tsx`:** import `ThrowInSetupPanel` and add `phase === 'THROW_IN_SETUP' ? <ThrowInSetupPanel /> :` to the phase-dispatch ternary, placed after the `FREE_KICK_SETUP` branch. Remove the `// ... added in plans 37-07 / 37-10` portion of the Plan 37-02 comment that refers to the throw-in panel, leaving the goal-kick note for Plan 37-10.

**`HexGrid.tsx`:**

1. Add a `const isThrowInSetup = phase === 'THROW_IN_SETUP';` derivation next to `isKickOffSetup` / `isFreeKickSetup`, plus `const throwInHex = useGameStore((s) => s.gameState.throwInHex);` and `const throwInTeam = useGameStore((s) => s.gameState.throwInTeam);` per-slice selectors, and `const isMyThrowIn = isThrowInSetup && myTeam !== null && myTeam === throwInTeam;`.
2. Hex highlighting: when `isMyThrowIn` and the hex equals `throwInHex`, resolve `highlightType` to the existing `'safe'` member. Insert this into the existing priority ternary at the same precedence as the `kickoff` placement-zone tint (i.e. below risk/goal/shot-path, above the generic safe-move tint) so it cannot be shadowed. Do NOT add a `HexHighlightType` member and do NOT edit `HexCell.tsx` (D-08).
3. Piece selection: in the per-piece block, add a `THROW_IN_SETUP` branch that makes a piece clickable and ring-eligible when `isMyThrowIn && piece.teamId === myTeam`, calling the existing `selectPiece(piece.id)` store action on click — mirroring the `KICK_OFF_SETUP` branch's "both teams may select their own pieces; opponent pieces are no-ops" shape, except only the throwing team may select. Placement itself is confirmed from the panel's `"Confirm"` button (Task 1), not from a hex click, so no `onClick` hex branch is added for `THROW_IN_SETUP`.
4. Confirm that no other phase-conditional block in `HexGrid.tsx` falls through and grants unintended interactions during `THROW_IN_SETUP` — specifically check that `validMoveHexSet`, `shootingMode` and the pass-target sets are all empty or inert in this phase, and add an explicit early guard if any is not.

Do not modify `docs/HIGHLIGHT-REFERENCE.md`.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test</automated>
</verify>
<acceptance_criteria> - `grep -n "THROW_IN_SETUP" packages/client/src/components/GameBoard.tsx` shows the dispatch branch rendering `<ThrowInSetupPanel />` - `grep -n "emitThrowInPlace" packages/client/src/store/useGameStore.ts` returns at least 2 lines (declaration + implementation) - `grep -n "THROW_IN_MAX_DISTANCE" packages/client/src/store/useGameStore.ts` returns at least 2 lines - `grep -n "isThrowInSetup\|isMyThrowIn" packages/client/src/components/HexGrid.tsx` returns at least 3 lines - `git diff packages/client/src/components/HexCell.tsx` is empty and `git diff docs/HIGHLIGHT-REFERENCE.md` is empty (D-08 compliance) - `pnpm --filter @counter-attack/client test` exits 0 with every pre-existing `HexGrid.test.tsx` and `GameBoard.test.tsx` assertion unchanged
</acceptance_criteria>
<done>The panel is dispatched for `THROW_IN_SETUP`, the throw-in hex is tinted with an existing highlight type, the throwing team's pieces are selectable, and the store emits `game:throw-in-place` and caps throw-target highlights at 6 hexes.</done>
</task>

<task type="auto">
  <name>Task 3: Throw-in action button copy in ActionPanel</name>
  <files>packages/client/src/components/ActionPanel.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx lines 14-37 — `PASS_TYPE_LABELS` and `ACTION_SUMMARY`
    - packages/client/src/components/ActionPanel.tsx lines 676-820 — the `PASS`/`KICK_OFF` branch: the `effectiveLastAction` / `eligible` derivation (702-705), the `isKickOff` context flag and its use to suppress buttons (726-737), the helper-block copy (741-750), and the per-action button blocks for `MOVEMENT` / `STANDARD_PASS` / `FIRST_TIME_PASS` / `HIGH_PASS` / `LONG_BALL` / Snapshot / Shoot (751-810)
    - packages/client/src/components/ActionPanel.test.tsx — the existing assertions on button labels for the `PASS` phase, which must keep passing for non-throw-in contexts
    - .planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-UI-SPEC.md — the Copywriting Contract row "Primary CTA (throw-in step choice)": exact labels `"Standard Throw-In"` / `"High Throw-In"` / `"Move"`, with `"Move"` absent at the second decision point
  </read_first>
  <action>
In `ActionPanel.tsx`'s `PASS`/`KICK_OFF` branch, add a throw-in context flag alongside the existing `isKickOff` flag:

`const isThrowIn = lastActionType === 'THROW_IN_MOVEMENT_1' || lastActionType === 'THROW_IN_MOVEMENT_2';`

Then make exactly these changes, all label-level — no change to which actions are eligible (that is `ELIGIBLE_NEXT_ACTIONS`' job, and `THROW_IN_MOVEMENT_2`'s row already omits `MOVEMENT` so the Move button disappears at the second decision point automatically):

1. The `STANDARD_PASS` button's label becomes `Standard Throw-In` when `isThrowIn`, otherwise `Standard Pass`.
2. The `HIGH_PASS` button's label becomes `High Throw-In` when `isThrowIn`, otherwise `High Pass`.
3. The `MOVEMENT` button keeps the label `Move` in all contexts (the UI-SPEC's throw-in choice set uses the same word).
4. Add two `ACTION_SUMMARY` entries — `'Standard Throw-In': 'Throw the ball in low, up to 6 hexes; may be intercepted.'` and `'High Throw-In': 'Throw the ball in high, up to 6 hexes; the receiver must attempt a header.'` — and use them as the `title` for the two relabelled buttons when `isThrowIn`.
5. Change the helper block's two lines when `isThrowIn`: line 1 `Throw-In!`, line 2 `Take the throw now, or take another Movement Phase first.` for `THROW_IN_MOVEMENT_1`, and line 2 `Take the throw — no more Movement Phases available.` for `THROW_IN_MOVEMENT_2`.

Do NOT suppress the Snapshot / Shoot / One-Touch / Long Ball buttons with a new condition — they are already absent because `ELIGIBLE_NEXT_ACTIONS`' throw-in rows contain only `STANDARD_PASS`, `HIGH_PASS` and (for row 1) `MOVEMENT`. Adding a redundant client-side suppression would create a second source of truth. If any of those buttons still render during a throw-in context, that indicates the eligibility row is wrong — fix `actionSequence.ts`, not `ActionPanel.tsx`.

Add `ActionPanel.test.tsx` cases: with `lastActionType: 'THROW_IN_MOVEMENT_1'` the panel shows exactly `Move`, `Standard Throw-In`, `High Throw-In`; with `'THROW_IN_MOVEMENT_2'` it shows exactly `Standard Throw-In` and `High Throw-In` and no `Move`; with `'MOVEMENT_PHASE'` the labels are unchanged from today.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- ActionPanel</automated>
</verify>
<acceptance_criteria> - `grep -n "Standard Throw-In" packages/client/src/components/ActionPanel.tsx` returns at least 2 lines (label + ACTION_SUMMARY key) - `grep -n "High Throw-In" packages/client/src/components/ActionPanel.tsx` returns at least 2 lines - `grep -n "isThrowIn" packages/client/src/components/ActionPanel.tsx` returns at least 4 lines - `grep -n "THROW_IN_MOVEMENT_2" packages/client/src/components/ActionPanel.tsx` shows it used only for helper-line copy, never to suppress a button - `pnpm --filter @counter-attack/client test -- ActionPanel` exits 0 with the three new label cases and every pre-existing label assertion passing
</acceptance_criteria>
<done>The post-movement choice reads `Standard Throw-In` / `High Throw-In` / `Move`, drops `Move` after the second Movement Phase, and non-throw-in contexts are unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                             | Description                                                                                     |
| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| client UI → server socket                            | Every panel/pitch interaction produces a socket emit that the server independently re-validates |
| client `validatePass` mirror → server `validatePass` | The client copy computes highlights only; the server copy is authoritative                      |

## STRIDE Threat Register

| Threat ID | Category                          | Component                                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                |
| --------- | --------------------------------- | ----------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-37-28   | Tampering                         | client-side throw-range cap                           | mitigate    | The client's `{ maxDistance: 6 }` only shrinks the highlight set. The authoritative cap is the identical argument passed by `gameHandlers.ts` (Plan 37-06); a modified client that omits it still gets `INVALID_TARGET` from the server.                                       |
| T-37-29   | Spoofing                          | placing an opponent's piece as thrower                | mitigate    | `HexGrid`'s selection branch only makes `piece.teamId === myTeam` clickable, and the panel only emits the selected id — but the real guard is the server's `NOT_YOUR_PIECE` / `WRONG_TEAM` pair in the `GAME_THROW_IN_PLACE` handler (Plan 37-05). The client gate is UX only. |
| T-37-30   | Tampering (business-logic bypass) | showing a Move button after the second Movement Phase | mitigate    | Button visibility is derived from `ELIGIBLE_NEXT_ACTIONS`, the same table the server enforces with `INVALID_SEQUENCE`. No client-only suppression is added, so the two cannot drift.                                                                                           |
| T-37-31   | Information disclosure            | opponent seeing the throwing team's selection         | accept      | Selection is local component state and the resulting placement is broadcast to both players anyway; the board is fully public to both managers by design.                                                                                                                      |
| T-37-SC   | Tampering                         | npm/pip/cargo installs                                | mitigate    | No packages installed by this plan. Any proposed install stops execution pending a legitimacy table and blocking human checkpoint.                                                                                                                                             |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test` green including `ThrowInSetupPanel.test.tsx` and the new `ActionPanel` label cases
- `git diff` shows `HexCell.tsx` and `docs/HIGHLIGHT-REFERENCE.md` untouched (D-08)
- `ThrowInSetupPanel.module.css` contains no raw hex colours and no 3px values
- Monorepo typecheck clean
</verification>

<success_criteria>

- `THROW_IN_SETUP` renders `ThrowInSetupPanel` for both players, with the waiting message for the non-throwing manager
- The throw-in hex is highlighted with an existing tint and the throwing team's pieces are selectable
- `Confirm` emits `game:throw-in-place`
- The post-movement choice reads `Standard Throw-In` / `High Throw-In` / `Move`, dropping `Move` after Movement Phase 2
- Throw-target highlights stop at 6 hexes
  </success_criteria>

<output>
Create `.planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-07-SUMMARY.md` when done. Record the exact `HexHighlightType` members reused and confirm no new tint types were introduced.
</output>
