---
phase: 37-out-of-bounds-detection-throw-in-goal-kick
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/actionSequence.ts
  - packages/shared/src/actionSequence.test.ts
  - packages/shared/src/events.ts
  - packages/shared/src/events.test.ts
  - packages/shared/src/passValidator.ts
  - packages/shared/src/passValidator.test.ts
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/BallLocationRing.tsx
autonomous: true
requirements: [OOB-05, THROWIN-03, GOALKICK-01, GOALKICK-03, GOALKICK-06]

must_haves:
  truths:
    - 'Six new GamePhase values exist and every compiler-enforced consumer (PHASE_LABEL) handles them'
    - 'Three new LastActionType rows exist with the correct ELIGIBLE_NEXT_ACTIONS sets, enforcing the D-09 two-movement-phase cap'
    - 'Six new ActionEvent variants exist and ActionLog renders each of them'
    - 'The three new client socket events are declared with typed payload signatures'
    - 'validatePass accepts an optional distance-cap override without changing behaviour for any existing caller'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'New GamePhase / LastActionType / ActionEventType / ActionEvent / GameState fields'
      contains: 'THROW_IN_SETUP'
    - path: 'packages/shared/src/actionSequence.ts'
      provides: 'THROW_IN_MOVEMENT_1 / THROW_IN_MOVEMENT_2 / GOAL_KICK_RESTART eligibility rows'
      contains: 'THROW_IN_MOVEMENT_2'
    - path: 'packages/shared/src/events.ts'
      provides: 'GAME_THROW_IN_PLACE / GAME_GOAL_KICK_CHOICE / GAME_GOAL_KICK_TARGET'
      contains: 'game:throw-in-place'
  key_links:
    - from: 'packages/shared/src/types.ts'
      to: 'packages/client/src/components/GameBoard.tsx'
      via: 'PHASE_LABEL Record<GamePhase, string> exhaustiveness'
      pattern: 'GOAL_KICK_MOVE'
    - from: 'packages/shared/src/types.ts'
      to: 'packages/client/src/components/ActionLog.tsx'
      via: 'formatEvent exhaustive switch'
      pattern: "case 'OUT_OF_BOUNDS'"
    - from: 'packages/shared/src/types.ts'
      to: 'packages/server/src/gameHandlers.ts'
      via: 'validUndoPhases registration'
      pattern: 'GOAL_KICK_SETUP_GK'
---

<objective>
Declare every new type contract this phase needs — phases, state fields, last-action rows, event types, socket events — and satisfy every compiler-enforced and non-compiler-enforced consumer list in one pass.

Purpose: interface-first. Plans 37-03 through 37-10 all build against these contracts, so they must exist and compile before any behaviour is implemented. This plan also closes Pitfall 1 (new `GamePhase` values are invisible unless registered in `PHASE_LABEL`, the `GameBoard` dispatch ternary, `validUndoPhases`, and `BALL_MARKER_PHASES`) and Pitfall 2 (new `ActionEventType`s need registration in `ActionLog`, `REPLAY_ELIGIBLE_TYPES`, and `applyUndo`'s boundary scan) up front rather than as a follow-up bug.

Output: all shared contracts + every registration list updated; the monorepo compiles and the existing suite stays green. No new gameplay behaviour ships in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-CONTEXT.md
@.planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-RESEARCH.md
@.planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-PATTERNS.md
@.planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-01-SUMMARY.md
</context>

## Artifacts this phase produces

This plan produces **all** of the following type-level artifacts (marked **[37-02]**); behaviour is added by later plans.

**New `GamePhase` values [37-02]:** `THROW_IN_SETUP`, `GOAL_KICK_SETUP_GK`, `GOAL_KICK_SETUP_OPPONENT`, `GOAL_KICK_CHOICE`, `GOAL_KICK_TARGET`, `GOAL_KICK_MOVE`

**New `LastActionType` values [37-02]:** `THROW_IN_MOVEMENT_1`, `THROW_IN_MOVEMENT_2`, `GOAL_KICK_RESTART`

**New `ActionEventType` + `ActionEvent` variants [37-02]:** `OUT_OF_BOUNDS`, `THROW_IN_PLACE`, `GOAL_KICK_WINDOW_ADVANCE`, `GOAL_KICK_CHOICE`, `GOAL_KICK_MOVE`, `GOAL_KICK`

**New `ClientEvents` [37-02]:** `GAME_THROW_IN_PLACE` = `'game:throw-in-place'`, `GAME_GOAL_KICK_CHOICE` = `'game:goal-kick-choice'`, `GAME_GOAL_KICK_TARGET` = `'game:goal-kick-target'`

**New `GameState` fields [37-02]:** `outOfBoundsEnabled?: boolean`, `throwInHex?: HexCoord | null`, `throwInTeam?: 'home' | 'away' | null`, `throwInPhasesTaken?: 0 | 1 | 2 | null`, `goalKickTeam?: 'home' | 'away' | null`, `goalKickGkId?: string | null`, `goalKickEligibleIds?: { gkTeam: readonly string[]; opponent: readonly string[] } | null`, `goalKickUsedPace?: Readonly<Record<string, number>> | null`, `goalKickTargetHex?: HexCoord | null`, `goalKickMoveSlot?: 'KICKER' | 'OPP' | null`, `goalKickMovedPieceId?: string | null`, `goalKickPaceUsed?: number`

**Extended `ROOM_SETTINGS_CONFIRM` payload [37-02]:** `{ speed, teamType, draftPools, outOfBounds: boolean }`; `ROOM_SETTINGS_CONFIRMED` gains a 4th positional arg `outOfBounds: boolean`

**Extended `validatePass` signature [37-02]:** optional 6th parameter `options?: { maxDistance?: number }`

Produced by other plans: `ball.lastTouchedBy` + `outOfBounds.ts` (37-01); `Room.outOfBoundsEnabled` (37-03); `triggerOutOfBoundsRestart` (37-04); `applyThrowInPlace` (37-05); `ThrowInSetupPanel` (37-07); `applyGoalKickReposition`/`applyGoalKickWindowEnd`/`applyGoalKickChoice` (37-08); `applyGoalKickTarget` (37-09); `GoalKickSetupPanel` (37-10).

<tasks>

<task type="auto">
  <name>Task 1: Declare new phases, state fields, last-action types and action events</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts — the whole file; specifically the `ActionEventType` union (~65-107), the `ActionEvent` discriminated union (~114-401), the `GamePhase` union (~403-430), the `LastActionType` union (~439-452), and the `GameState` optional-field cluster (~758-1012) including `gkKickMovementSlot` / `gkKickMovedPieceId` / `gkKickPaceUsed` and `freeMoveEligibleIds` / `freeMoveUsedPace` which are the shape templates being mirrored
    - .planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-PATTERNS.md — the `packages/shared/src/types.ts` section, which gives the exact field shapes
    - .planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-CONTEXT.md — D-01 (Goal Kick must be structurally independent of the GK_RESTART chain), D-03 (naming discretion, follow `GK_KICK_TARGET`/`GK_KICK_MOVE` convention), D-09 (the two-movement-phase throw-in model)
  </read_first>
  <action>
Extend `packages/shared/src/types.ts` with the following, each carrying a JSDoc comment naming the requirement and decision it implements.

**`GamePhase` union — append six values** (per D-01/D-03, these are genuinely new values, NOT aliases of the `GK_RESTART` chain):
`'THROW_IN_SETUP'`, `'GOAL_KICK_SETUP_GK'`, `'GOAL_KICK_SETUP_OPPONENT'`, `'GOAL_KICK_CHOICE'`, `'GOAL_KICK_TARGET'`, `'GOAL_KICK_MOVE'`.
Comment `GOAL_KICK_SETUP_GK`/`GOAL_KICK_SETUP_OPPONENT` as the two sequential GOALKICK-02 reposition windows, modelled on the existing `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` two-phase-value precedent (RESEARCH.md Open Question 2, resolved in favour of two phase values).

**`LastActionType` union — append three values:** `'THROW_IN_MOVEMENT_1'`, `'THROW_IN_MOVEMENT_2'`, `'GOAL_KICK_RESTART'`.

**`ActionEventType` union + `ActionEvent` discriminated union — append six variants:**

- `OUT_OF_BOUNDS`: `{ type: 'OUT_OF_BOUNDS'; exitHex: HexCoord; kind: 'SIDELINE' | 'BYLINE'; restart: 'THROW_IN' | 'GOAL_KICK'; awardedTo: 'home' | 'away'; lastTouchedByPieceId: string | null; timestamp: number; ballAfter: { position: HexCoord; carrierId: string | null } }`
- `THROW_IN_PLACE`: `{ type: 'THROW_IN_PLACE'; pieceId: string; from: HexCoord; to: HexCoord; timestamp: number; ballAfter: { position: HexCoord; carrierId: string | null } }` — `from`/`to` mirror `KICK_OFF_SETUP`'s shape so replay reconstruction can treat it as a MOVE-like event
- `GOAL_KICK_WINDOW_ADVANCE`: `{ type: 'GOAL_KICK_WINDOW_ADVANCE'; fromWindow: 'GK_TEAM' | 'OPPONENT'; timestamp: number }` — undo boundary, mirrors `FK_STAGE_ADVANCE`
- `GOAL_KICK_CHOICE`: `{ type: 'GOAL_KICK_CHOICE'; gkId: string; choice: 'kick' | 'standard'; timestamp: number }`
- `GOAL_KICK_MOVE`: `{ type: 'GOAL_KICK_MOVE'; slot: 'KICKER' | 'OPP'; pieceId: string; from: HexCoord; to: HexCoord; timestamp: number }` — byte-for-byte the `GK_KICK_MOVE` shape
- `GOAL_KICK`: `{ type: 'GOAL_KICK'; gkId: string; targetHex: HexCoord; accurate: boolean; kickDie: number; kickScore: number; timestamp: number; ballAfter: { position: HexCoord; carrierId: string | null } }` — byte-for-byte the `GK_KICK` shape

Per STATE.md's recorded pitfall, do NOT reuse the generic `DICE_ROLL` type for the goal-kick accuracy roll — `GOAL_KICK` is its own type precisely to avoid reactivating the dormant full-slot Undo lockout.

**`GameState` — append the optional fields listed in "Artifacts this phase produces".** All are `?:` optional (matching `gkKick*` / `freeMove*` / `freeKick*` precedent) so existing test fixtures and construction sites are unaffected. Key JSDoc points:

- `outOfBoundsEnabled?: boolean` — OOB-05/GOALKICK-06. Absent or `false` means the toggle is OFF and the pre-existing clamp-to-boundary behaviour must run byte-for-byte unchanged. Every read site must test `state.outOfBoundsEnabled === true`, never truthiness of a possibly-undefined value.
- `throwInHex` / `throwInTeam` / `throwInPhasesTaken` — THROWIN-02/03. `throwInPhasesTaken` counts COMPLETED Movement Phases: `0` right after placement, `1` after Movement Phase 1, `2` after Movement Phase 2 (D-09 hard cap).
- `goalKickEligibleIds` / `goalKickUsedPace` — GOALKICK-02, per-piece 6-hex budget, copied from `freeMoveEligibleIds`/`freeMoveUsedPace`'s shape (NOT `FREE_KICK_STAGES`' distinct-piece-count budget).
- `goalKickMoveSlot` / `goalKickMovedPieceId` / `goalKickPaceUsed` — GOALKICK-05, copied from `gkKickMovementSlot`/`gkKickMovedPieceId`/`gkKickPaceUsed`, capped at 3.

Make no behavioural change in this task. The file must still be pure type declarations.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared build</automated>
</verify>
<acceptance_criteria> - `grep -c "THROW_IN_SETUP\|GOAL_KICK_SETUP_GK\|GOAL_KICK_SETUP_OPPONENT\|GOAL_KICK_CHOICE\|GOAL_KICK_TARGET\|GOAL_KICK_MOVE" packages/shared/src/types.ts` returns at least 6 - `grep -n "THROW_IN_MOVEMENT_1\|THROW_IN_MOVEMENT_2\|GOAL_KICK_RESTART" packages/shared/src/types.ts` returns 3 or more lines - `grep -n "outOfBoundsEnabled" packages/shared/src/types.ts` shows the field declared with `?:` - `grep -n "goalKickEligibleIds" packages/shared/src/types.ts` shows the `{ gkTeam: readonly string[]; opponent: readonly string[] } | null` shape - `pnpm --filter @counter-attack/shared build` exits 0 - `pnpm --filter @counter-attack/shared exec tsc --noEmit` reports errors ONLY in `actionSequence.ts` (missing Record keys) — that is Task 2's scope
</acceptance_criteria>
<done>All new phases, last-action types, action events and state fields are declared in `types.ts` and the shared package builds.</done>
</task>

<task type="auto">
  <name>Task 2: Add eligibility rows, socket events and the validatePass distance-cap override</name>
  <files>packages/shared/src/actionSequence.ts, packages/shared/src/actionSequence.test.ts, packages/shared/src/events.ts, packages/shared/src/events.test.ts, packages/shared/src/passValidator.ts, packages/shared/src/passValidator.test.ts</files>
  <read_first>
    - packages/shared/src/actionSequence.ts — the whole file; `ELIGIBLE_NEXT_ACTIONS` is `Record<LastActionType, ReadonlySet<NextActionType>>` so the three new keys are compile-enforced
    - packages/shared/src/actionSequence.test.ts — check for any key-count or exhaustiveness assertion that must be updated
    - packages/shared/src/events.ts — `ClientEvents` const object (~19-109) and `ClientToServerEvents` interface (~158-264); note the `GAME_GK_KICK_TARGET` / `GAME_FREE_KICK_MOVE` / `GAME_FREE_KICK_READY` declarations as the shape to copy
    - packages/shared/src/events.test.ts — check for any event-name-count assertion
    - packages/shared/src/passValidator.ts lines 49-105 — `validatePass` signature and the per-type distance caps at lines 79-81
    - packages/shared/src/passValidator.test.ts — existing call sites (they must keep compiling unchanged)
    - .planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-CONTEXT.md — D-09 (throw-in choice model) and D-10 (Low/High throw-type UI is discretionary but must reuse the existing pass-choice pattern)
  </read_first>
  <action>
**`actionSequence.ts`** — add three rows to `ELIGIBLE_NEXT_ACTIONS`:
- `THROW_IN_MOVEMENT_1: new Set<NextActionType>(['STANDARD_PASS', 'HIGH_PASS', 'MOVEMENT'])` — after Movement Phase 1 the thrower may take the throw Low, take it High, or take a second Movement Phase (D-09 step 3).
- `THROW_IN_MOVEMENT_2: new Set<NextActionType>(['STANDARD_PASS', 'HIGH_PASS'])` — D-09's hard cap: `MOVEMENT` is deliberately absent so a third movement phase is server-rejected with `INVALID_SEQUENCE`, not merely hidden in the UI.
- `GOAL_KICK_RESTART: new Set<NextActionType>(['STANDARD_PASS'])` — GOALKICK-03's "Standard Pass" branch; mirrors the existing `FREE_KICK_RESTART` row's purpose. No `MOVEMENT`, no `HIGH_PASS`, no `SNAPSHOT`, no `SHOT`.

Each row gets a comment naming its requirement (THROWIN-03 / D-09, GOALKICK-03). Update `actionSequence.test.ts` if it asserts a fixed number of rows, and add three assertions verifying the exact set contents of the new rows.

**`events.ts`** — add three entries to the `ClientEvents` const object and three matching signatures to `ClientToServerEvents`:

- `GAME_THROW_IN_PLACE: 'game:throw-in-place'` → `(pieceId: string) => void` — THROWIN-02, the attacking manager selects which of their pieces takes the throw. The destination hex is server-owned (`state.throwInHex`) and is deliberately NOT part of the payload so a client can never choose where the throw-in is taken from.
- `GAME_GOAL_KICK_CHOICE: 'game:goal-kick-choice'` → `(choice: 'kick' | 'standard') => void` — GOALKICK-03, mirrors `GAME_GK_RESTART`'s choice-payload shape.
- `GAME_GOAL_KICK_TARGET: 'game:goal-kick-target'` → `(targetHex: HexCoord) => void` — GOALKICK-05, mirrors `GAME_GK_KICK_TARGET`.

Also extend the settings payload in the same file: `ClientEvents.ROOM_SETTINGS_CONFIRM`'s payload type gains `outOfBounds: boolean`, and `ServerEvents.ROOM_SETTINGS_CONFIRMED`'s signature gains a 4th positional parameter `outOfBounds: boolean` (matching the existing positional-args convention used for `speed, teamType, draftPools`). Do NOT add server events for throw-in/goal-kick state — the existing `GAME_STATE` full-snapshot broadcast carries everything.

Goal-kick repositioning during `GOAL_KICK_SETUP_GK`/`GOAL_KICK_SETUP_OPPONENT` and `GOAL_KICK_MOVE` deliberately reuses the existing `GAME_MOVE` + `GAME_END_TURN` events (the `FREE_MOVE_*` and `GK_KICK_MOVE` precedent); throw-in Movement Phases reuse the real `MOVE` phase's existing events; the throw itself reuses `GAME_ROLL`. Add a comment in `events.ts` next to the three new events stating this so a later reader does not add redundant events.

**`passValidator.ts`** — add an optional 6th parameter `options?: { maxDistance?: number }` to `validatePass`. When `options?.maxDistance` is a number, it REPLACES the per-type cap at lines 79-81 (i.e. `if (dist > options.maxDistance) return { ok: false, reason: 'RANGE_EXCEEDED' };` runs instead of the type-specific check); when it is absent, the existing per-type behaviour is byte-for-byte unchanged. All other validation (path blocking, PASS-04 landing constraints, interception collection) is untouched. Document that this exists for THROWIN-04's 6-hex throw cap so range logic stays single-sourced in the shared validator rather than duplicated in the handler layer (RESEARCH.md §4.1 recommendation 1). Do NOT add an `isPitchHex` check to `validatePass` — RESEARCH.md Assumption A4 requires that stay out of this function.

Add two `passValidator.test.ts` cases: a `STANDARD` pass at distance 8 succeeds without options and fails with `{ maxDistance: 6 }`; a `HIGH` pass at distance 6 succeeds with `{ maxDistance: 6 }`.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared test</automated>
</verify>
<acceptance_criteria> - `grep -c "THROW_IN_MOVEMENT_1:\|THROW_IN_MOVEMENT_2:\|GOAL_KICK_RESTART:" packages/shared/src/actionSequence.ts` returns 3 - `grep -n "MOVEMENT" packages/shared/src/actionSequence.ts` shows no `MOVEMENT` inside the `THROW_IN_MOVEMENT_2` set literal - `grep -n "game:throw-in-place\|game:goal-kick-choice\|game:goal-kick-target" packages/shared/src/events.ts` returns 3 lines - `grep -n "outOfBounds" packages/shared/src/events.ts` returns at least 2 lines (payload + broadcast signature) - `grep -n "maxDistance" packages/shared/src/passValidator.ts` returns at least 2 lines - `pnpm --filter @counter-attack/shared test` exits 0 including the new actionSequence and passValidator cases
</acceptance_criteria>
<done>Eligibility rows, socket events, settings payload extension and the `validatePass` cap override all exist and are unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Register the new phases and events in every consumer list (Pitfall 1 + Pitfall 2)</name>
  <files>packages/client/src/components/GameBoard.tsx, packages/client/src/components/ActionLog.tsx, packages/client/src/components/BallLocationRing.tsx, packages/server/src/gameEngine.ts, packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx lines 24-59 — `PHASE_LABEL: Record<GamePhase, string>` (compiler-enforced) and lines 326-337 — the phase-dispatch ternary (NOT compiler-enforced; this is the Pitfall-1 miss site)
    - packages/client/src/components/ActionLog.tsx lines 314-840 — `formatEvent`'s exhaustive switch (its `Formatted` return type makes a missing case a compile error)
    - packages/client/src/components/BallLocationRing.tsx lines 26-38 — `BALL_MARKER_PHASES`
    - packages/server/src/gameEngine.ts lines 4531-4562 — `REPLAY_ELIGIBLE_TYPES`, and lines 4678-4735 — the replay event loop with its `MOVE` / `KICK_OFF_SETUP` accumulation branches
    - packages/server/src/gameEngine.ts lines 1401-1589 — `applyUndo`, specifically the boundary scan (~1410-1421), `moveTypeForPhase` (~1433-1444), the `moveToUndo` `Extract<...>` cast (~1466-1477) and the `lockReset` chain (~1531-1575)
    - packages/server/src/gameHandlers.ts lines 1126-1140 — `validUndoPhases`
    - .planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-RESEARCH.md — Pitfall 1 and Pitfall 2, which enumerate exactly these lists
    - docs/HIGHLIGHT-REFERENCE.md — section 3, which documents `BALL_MARKER_PHASES` as part of the highlight contract
  </read_first>
  <action>
Register the six new phases and six new event types in every consuming list. This task ships no behaviour; it makes the contracts visible everywhere so later plans cannot silently skip a list.

**`GameBoard.tsx` `PHASE_LABEL`** — add all six keys, following the existing uppercase + em-dash naming style (`'GOALIE KICK — SELECT TARGET'`):

- `THROW_IN_SETUP: 'THROW-IN — SETUP'`
- `GOAL_KICK_SETUP_GK: 'GOAL KICK — REPOSITION (KICKING)'`
- `GOAL_KICK_SETUP_OPPONENT: 'GOAL KICK — REPOSITION (DEFENDING)'`
- `GOAL_KICK_CHOICE: 'GOAL KICK — CHOOSE'`
- `GOAL_KICK_TARGET: 'GOAL KICK — SELECT TARGET'`
- `GOAL_KICK_MOVE: 'GOAL KICK — REPOSITION'`

Do NOT touch the phase-dispatch ternary in this plan — Plans 37-07 and 37-10 add the `ThrowInSetupPanel` / `GoalKickSetupPanel` branches when those components exist. Add a `// Phase 37: ThrowInSetupPanel / GoalKickSetupPanel branches added in plans 37-07 / 37-10` comment directly above the ternary so the Pitfall-1 obligation is visible in the code.

**`ActionLog.tsx` `formatEvent`** — add a `case` for each of the six new event types, returning the existing `Formatted` shape (`prefix`, `prefixColor`, `content`, `isGoal: false`). Follow the surrounding prefix conventions (`[FK]`, `[GK KICK]`, bracketed uppercase, `pieceColorOf(...)`/`aaTeamAccentColor(undefined)` for colour):

- `OUT_OF_BOUNDS` → prefix `[OUT]`, content stating the exit kind, the restart awarded, and the awarded team name
- `THROW_IN_PLACE` → prefix `[THROW-IN]`, content naming the placed player (use the file's existing `PNamed`/`pieceName` helper) and the hex
- `GOAL_KICK_WINDOW_ADVANCE` → prefix `[GOAL KICK]`, content naming the window that just ended
- `GOAL_KICK_CHOICE` → prefix `[GOAL KICK]`, content naming the chosen option
- `GOAL_KICK_MOVE` → prefix `[GOAL KICK]`, content matching the existing `GK_KICK_MOVE` case's from→to format
- `GOAL_KICK` → prefix `[GOAL KICK]`, content matching the existing `GK_KICK` case's die/score/accuracy format

**`BallLocationRing.tsx` `BALL_MARKER_PHASES`** — add all six new phases. During every one of them the ball is either fixed at a restart hex or mid-air, which is exactly the condition the existing 11-phase list encodes. Per D-08 this reuses the existing marker; do not add a new tint type or a new entry to `docs/HIGHLIGHT-REFERENCE.md`.

**`gameEngine.ts` `REPLAY_ELIGIBLE_TYPES`** — add `'OUT_OF_BOUNDS'`, `'THROW_IN_PLACE'`, `'GOAL_KICK'` (the three that carry `ballAfter`). Do NOT add `GOAL_KICK_MOVE`, `GOAL_KICK_CHOICE` or `GOAL_KICK_WINDOW_ADVANCE` — they carry no `ballAfter`, matching the existing exclusion of `GK_KICK_MOVE`.

**`gameEngine.ts` `buildReplayFrames` event loop** — add a `THROW_IN_PLACE` accumulation branch immediately after the existing `KICK_OFF_SETUP` branch, pushing `{ to: event.to, ballAfter: event.ballAfter }` into `moveGroup` so the thrower's placement animates like a move. Add `GOAL_KICK_WINDOW_ADVANCE`, `GOAL_KICK_CHOICE` and `GOAL_KICK_MOVE` to the existing `SLOT_ADVANCE || DEFLECT_ATTEMPT` skip guard at the top of the loop so they never prematurely flush an in-progress move group (the WR-03 rationale already documented there).

**`gameEngine.ts` `applyUndo`** — three edits:

1. Boundary scan: add `(state.phase === 'GOAL_KICK_SETUP_GK' || state.phase === 'GOAL_KICK_SETUP_OPPONENT') && evt.type === 'GOAL_KICK_WINDOW_ADVANCE'` to the `isBoundary` disjunction so Undo can never cross from the opponent's window back into the kicking team's window.
2. `moveTypeForPhase`: add a branch mapping `GOAL_KICK_MOVE` → `'GOAL_KICK_MOVE'`. `GOAL_KICK_SETUP_GK`/`GOAL_KICK_SETUP_OPPONENT` fall through to the default `'MOVE'` (they emit `MOVE` events via the `applyFreeMove`-shaped reposition, matching how `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` already work).
3. `moveToUndo` `Extract<...>` cast: add `'GOAL_KICK_MOVE'` to the type list. `lockReset`: add a `GOAL_KICK_MOVE` branch mirroring the existing `GK_KICK_MOVE` branch exactly (decrement `goalKickPaceUsed` by `stepDistance`; when it reaches 0, also null `goalKickMovedPieceId`), and a `GOAL_KICK_SETUP_GK`/`GOAL_KICK_SETUP_OPPONENT` branch that decrements `goalKickUsedPace[moveToUndo.pieceId]` by `stepDistance` and deletes the key when it reaches 0. RESEARCH.md's Don't-Hand-Roll table explicitly requires building these Undo boundaries now rather than repeating the known `FREE_KICK_SETUP` Undo gap.

**`gameHandlers.ts` `validUndoPhases`** — add `'GOAL_KICK_SETUP_GK'`, `'GOAL_KICK_SETUP_OPPONENT'`, `'GOAL_KICK_MOVE'`. Deliberately do NOT add `'THROW_IN_SETUP'`, `'GOAL_KICK_CHOICE'` or `'GOAL_KICK_TARGET'` — those phases contain no reversible piece move, and adding them would make Undo a silent no-op there. Add a comment recording that reasoning.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared build &amp;&amp; pnpm --filter @counter-attack/server test &amp;&amp; pnpm --filter @counter-attack/client test</automated>
</verify>
<acceptance_criteria> - `grep -c "THROW_IN_SETUP:\|GOAL_KICK_SETUP_GK:\|GOAL_KICK_SETUP_OPPONENT:\|GOAL_KICK_CHOICE:\|GOAL_KICK_TARGET:\|GOAL_KICK_MOVE:" packages/client/src/components/GameBoard.tsx` returns 6 - `grep -c "case 'OUT_OF_BOUNDS'\|case 'THROW_IN_PLACE'\|case 'GOAL_KICK_WINDOW_ADVANCE'\|case 'GOAL_KICK_CHOICE'\|case 'GOAL_KICK_MOVE'\|case 'GOAL_KICK'" packages/client/src/components/ActionLog.tsx` returns 6 - `grep -c "THROW_IN_SETUP\|GOAL_KICK_SETUP_GK\|GOAL_KICK_SETUP_OPPONENT\|GOAL_KICK_CHOICE\|GOAL_KICK_TARGET\|GOAL_KICK_MOVE" packages/client/src/components/BallLocationRing.tsx` returns 6 - `grep -n "'OUT_OF_BOUNDS'\|'THROW_IN_PLACE'\|'GOAL_KICK'," packages/server/src/gameEngine.ts` shows all three inside the `REPLAY_ELIGIBLE_TYPES` set - `grep -n "GOAL_KICK_WINDOW_ADVANCE" packages/server/src/gameEngine.ts` shows it in both the `applyUndo` boundary scan and the `buildReplayFrames` skip guard - `grep -n "GOAL_KICK_SETUP_GK" packages/server/src/gameHandlers.ts` shows it inside `validUndoPhases` - `grep -n "THROW_IN_SETUP" packages/server/src/gameHandlers.ts` returns no match inside `validUndoPhases` - Full monorepo typecheck reports zero errors; `pnpm --filter @counter-attack/server test` and `pnpm --filter @counter-attack/client test` both exit 0
</acceptance_criteria>
<done>Every new phase and event type is registered in `PHASE_LABEL`, `ActionLog.formatEvent`, `BALL_MARKER_PHASES`, `REPLAY_ELIGIBLE_TYPES`, `buildReplayFrames`, `applyUndo` and `validUndoPhases`, with the deliberate exclusions documented in code.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                                                    |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| client socket → server handler                | The three new `ClientEvents` declare the wire contract for untrusted client payloads                           |
| shared eligibility table → server enforcement | `ELIGIBLE_NEXT_ACTIONS` is imported client-side for button gating and server-side for authoritative validation |

## STRIDE Threat Register

| Threat ID | Category                          | Component                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                           |
| --------- | --------------------------------- | ------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-37-04   | Tampering (business-logic bypass) | `THROW_IN_MOVEMENT_2` eligibility row | mitigate    | `MOVEMENT` is omitted from the `THROW_IN_MOVEMENT_2` set so a client attempting a third Movement Phase is rejected server-side with `INVALID_SEQUENCE` by the existing `ELIGIBLE_NEXT_ACTIONS[effectiveLastAction].has(passType)` gate in `gameHandlers.ts`, not merely hidden in the UI (D-09 hard cap). |
| T-37-05   | Tampering                         | `GAME_THROW_IN_PLACE` payload         | mitigate    | The payload carries only `pieceId`; the destination hex is server-owned (`state.throwInHex`) and is deliberately not client-supplied, so a client cannot choose where the throw-in is taken from. Ownership/phase enforcement lands in Plan 37-05's handler.                                              |
| T-37-06   | Tampering                         | `validatePass` `maxDistance` override | mitigate    | The override is a server-supplied constant (6) chosen by the handler from server state, never read from a client payload. The client passes the same value only to compute highlight sets.                                                                                                                |
| T-37-07   | Spoofing                          | `ROOM_SETTINGS_CONFIRM.outOfBounds`   | mitigate    | Declared as `boolean`; Plan 37-03's handler adds the `typeof !== 'boolean'` allow-list rejection and the existing host-only (`playerSlot !== 1`) and `settingsConfirmed` locks continue to apply.                                                                                                         |
| T-37-SC   | Tampering                         | npm/pip/cargo installs                | mitigate    | No packages are installed by this plan (RESEARCH.md Package Legitimacy Audit: not applicable). Any proposed install must stop execution pending a legitimacy table plus blocking human checkpoint.                                                                                                        |

</threat_model>

<verification>
- Monorepo typecheck clean with all six new phases, three new last-action rows and six new event variants declared
- `pnpm --filter @counter-attack/shared test` green including new actionSequence + passValidator assertions
- `pnpm --filter @counter-attack/server test` and `pnpm --filter @counter-attack/client test` green with no pre-existing assertion changed
- Every Pitfall-1 / Pitfall-2 list mentioned in RESEARCH.md is updated or has a documented deliberate exclusion
</verification>

<success_criteria>

- All type contracts for Phase 37 exist and compile
- `ELIGIBLE_NEXT_ACTIONS` enforces the D-09 two-movement-phase cap at the type/data level
- No gameplay behaviour has changed: existing suite green
  </success_criteria>

<output>
Create `.planning/phases/37-out-of-bounds-detection-throw-in-goal-kick/37-02-SUMMARY.md` when done. List every registration list touched and every deliberate exclusion with its reason.
</output>
