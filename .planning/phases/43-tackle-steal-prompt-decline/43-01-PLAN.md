---
phase: 43-tackle-steal-prompt-decline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/shared/src/stoppagePhases.ts
  - packages/shared/src/stoppagePhases.test.ts
  - packages/shared/src/moveValidator.ts
  - packages/shared/src/moveValidator.test.ts
  - packages/client/src/components/GameBoard.tsx
autonomous: true
requirements: [TACKLE-02, TACKLE-03, TACKLE-04]

must_haves:
  truths:
    - 'The GamePhase union carries a TACKLE_STEAL_PROMPT member and the whole repo still typechecks.'
    - 'The ActionEventType union carries a TACKLE_STEAL_DECLINED member with a defenderId/carrierId/kind payload.'
    - 'GameState carries the tackleStealDeclineEnabled toggle field and the six ephemeral tackleStealPrompt* fields.'
    - 'validateMove returns STEAL_ATTEMPT defenders ordered by tackling stat, highest first, with equal-tackling ties preserving ZoI-computation order.'
    - 'TACKLE_STEAL_PROMPT is absent from STOPPAGE_PHASES, so a substitution can never be started mid-prompt.'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'TACKLE_STEAL_PROMPT GamePhase, TACKLE_STEAL_DECLINED ActionEventType + payload, tackleStealDeclineEnabled + tackleStealPrompt* GameState fields'
      contains: 'TACKLE_STEAL_PROMPT'
    - path: 'packages/shared/src/events.ts'
      provides: 'GAME_TACKLE_STEAL_CHOICE client event name + typed boolean signature'
      contains: 'GAME_TACKLE_STEAL_CHOICE'
    - path: 'packages/shared/src/moveValidator.ts'
      provides: 'tackling-descending ordering of the STEAL_ATTEMPT defenders array'
      contains: 'tackling'
    - path: 'packages/client/src/components/GameBoard.tsx'
      provides: 'PHASE_LABEL entry keeping Record<GamePhase, string> exhaustive'
      contains: 'TACKLE_STEAL_PROMPT'
  key_links:
    - from: 'packages/shared/src/moveValidator.ts'
      to: 'packages/shared/src/types.ts'
      via: 'PlayerPiece.tackling read by the defender sort comparator'
      pattern: 'tackling'
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: 'packages/shared/src/types.ts'
      via: 'PHASE_LABEL: Record<GamePhase, string> exhaustiveness'
      pattern: 'TACKLE_STEAL_PROMPT'
---

<objective>
Add the complete shared-package type surface for the Tackle/Steal decline prompt — a new
`TACKLE_STEAL_PROMPT` GamePhase, a new `TACKLE_STEAL_DECLINED` ActionEventType, the new
`GameState` toggle and prompt-cluster fields, and the new `GAME_TACKLE_STEAL_CHOICE` client
socket event — and implement D-02's tackling-descending ordering of the `STEAL_ATTEMPT`
defenders array in `moveValidator.ts`.

Purpose: every later plan in this phase consumes these identifiers. Landing them first as a
pure, behaviour-free type/ordering change means the engine, socket and UI plans never have to
invent or re-negotiate a field name mid-implementation.

Output: shared type surface + ordered defender list + the single-line `PHASE_LABEL` entry
that keeps the client's `Record<GamePhase, string>` exhaustive so the repo stays green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-tackle-steal-prompt-decline/43-CONTEXT.md
@.planning/phases/43-tackle-steal-prompt-decline/43-RESEARCH.md
@.planning/phases/43-tackle-steal-prompt-decline/43-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by Phase 43 (all plans). Nothing in this list exists in the codebase
today — do not treat any of them as drift from an existing definition.

| Symbol                                                  | Kind                                                                             | Introduced in |
| ------------------------------------------------------- | -------------------------------------------------------------------------------- | ------------- |
| `'TACKLE_STEAL_PROMPT'`                                 | `GamePhase` union member (`packages/shared/src/types.ts`)                        | 43-01         |
| `'TACKLE_STEAL_DECLINED'`                               | `ActionEventType` union member + `ActionEvent` variant                           | 43-01         |
| `GameState.tackleStealDeclineEnabled`                   | optional boolean field                                                           | 43-01         |
| `GameState.tackleStealPromptTeam`                       | optional `'home' \| 'away' \| null` field                                        | 43-01         |
| `GameState.tackleStealPromptKind`                       | optional `'STEAL' \| 'TACKLE' \| null` field                                     | 43-01         |
| `GameState.tackleStealPromptDefenderId`                 | optional `string \| null` field                                                  | 43-01         |
| `GameState.tackleStealPromptCarrierId`                  | optional `string \| null` field                                                  | 43-01         |
| `GameState.tackleStealPromptQueue`                      | optional `readonly string[]` field                                               | 43-01         |
| `GameState.tackleStealPromptResume`                     | optional resume-snapshot field                                                   | 43-01         |
| `ClientEvents.GAME_TACKLE_STEAL_CHOICE`                 | socket event name `'game:tackle-steal-choice'`                                   | 43-01         |
| `PHASE_LABEL.TACKLE_STEAL_PROMPT`                       | `Record<GamePhase, string>` entry                                                | 43-01         |
| `TACKLE_STEAL_PROMPT_CLEARED`                           | module-level const in `gameEngine.ts`                                            | 43-04         |
| `applyTackleStealChoice`                                | exported engine function                                                         | 43-04         |
| `ApplyTackleStealChoiceResult`                          | exported engine result union                                                     | 43-04         |
| `Room.tackleStealDeclineEnabled`                        | `roomStore.ts` Room field                                                        | 43-03         |
| `buildInitialGameState(..., tackleStealDeclineEnabled)` | new trailing parameter                                                           | 43-03         |
| `'INVALID_TACKLE_STEAL_DECLINE'`                        | GAME_ERROR reason string                                                         | 43-03         |
| `tackleStealDecline`                                    | `ROOM_SETTINGS_CONFIRM` payload field + `ROOM_SETTINGS_CONFIRMED` positional arg | 43-03         |
| `emitTackleStealChoice`                                 | client store action                                                              | 43-05         |
| `TackleStealPromptPanel`                                | new React component + `.module.css`                                              | 43-05         |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the TACKLE_STEAL_PROMPT phase, TACKLE_STEAL_DECLINED event and prompt-cluster GameState fields</name>

  <files>
    packages/shared/src/types.ts,
    packages/shared/src/events.ts,
    packages/shared/src/stoppagePhases.ts,
    packages/shared/src/stoppagePhases.test.ts,
    packages/client/src/components/GameBoard.tsx
  </files>

<read_first> - packages/shared/src/types.ts lines 230-250 (ActionEventType union, `GK_DIVE_AT_FEET_DECLINED` at :242) - packages/shared/src/types.ts lines 760-790 (ActionEvent payload variants, `GK_DIVE_AT_FEET_DECLINED` at :771-777) - packages/shared/src/types.ts lines 900-932 (GamePhase union tail, `GK_DIVE_AT_FEET_PROMPT` at :920) - packages/shared/src/types.ts lines 1526-1540 (`outOfBoundsEnabled` toggle-field comment shape) - packages/shared/src/types.ts lines 1700-1750 (`gkDiveAtFeetTeam`/`gkDiveAtFeetGkId`/`gkDiveAtFeetCarrierId`/`gkDiveAtFeetResume` cluster — the exact shape to mirror) - packages/shared/src/events.ts lines 140-160 (ClientEvents `GAME_GK_DIVE_AT_FEET` name block) and lines 380-395 (its typed `(accept: boolean) => void` signature) - packages/shared/src/stoppagePhases.ts lines 20-60 (STOPPAGE_PHASES array + the "Deliberately EXCLUDED" comment block at :46-56) - packages/client/src/components/GameBoard.tsx lines 33-95 (`PHASE_LABEL: Record<GamePhase, string>`) - .planning/phases/43-tackle-steal-prompt-decline/43-PATTERNS.md section "packages/shared/src/types.ts"
</read_first>

  <behavior>
    - `STOPPAGE_PHASES` still has exactly 15 members after this change and `isStoppagePhase('TACKLE_STEAL_PROMPT')` is false.
    - `pnpm -r typecheck` passes with the new GamePhase member present (proves PHASE_LABEL stayed exhaustive).
  </behavior>

  <action>
    Extend `packages/shared/src/types.ts` with four additions, each commented with a
    `TACKLE-0x (Phase 43)` citation in the same style as the neighbouring Phase 39/40 entries.

    1. GamePhase union: add `'TACKLE_STEAL_PROMPT'` immediately after `'GK_BOX_ENTRY_MOVE'`,
       with a comment citing TACKLE-01/TACKLE-02 and stating it is a mid-duel decision prompt
       inserted before a tackle/steal duel resolves, not a stoppage.

    2. ActionEventType union: add `'TACKLE_STEAL_DECLINED'` immediately after
       `'GK_DIVE_AT_FEET_DECLINED'`, with a comment citing TACKLE-02/D-03.

    3. ActionEvent payload variant, placed immediately after the `'GK_DIVE_AT_FEET_DECLINED'`
       variant: `{ type: 'TACKLE_STEAL_DECLINED'; kind: 'STEAL' | 'TACKLE'; defenderId: string;
       carrierId: string; timestamp: number }`. Do NOT add a `ballAfter` field — a decline
       moves no ball, mirroring `GK_DIVE_AT_FEET_DECLINED`'s deliberate omission, and 43-02
       depends on that omission for the REPLAY_ELIGIBLE_TYPES exclusion.

    4. GameState fields. Add `tackleStealDeclineEnabled?: boolean` next to `outOfBoundsEnabled`
       (~line 1534), documented with the same "every read site must test `=== true`, never
       truthiness of a possibly-undefined value" wording that field already uses. Then add the
       six-field prompt cluster immediately after the `gkBoxEntryResume` block:
       `tackleStealPromptTeam?: 'home' | 'away' | null` (the DEFENDING team — the decider),
       `tackleStealPromptKind?: 'STEAL' | 'TACKLE' | null`,
       `tackleStealPromptDefenderId?: string | null` (the defender currently being asked),
       `tackleStealPromptCarrierId?: string | null` (the ball carrier in the duel),
       `tackleStealPromptQueue?: readonly string[]` (remaining defender ids for the D-01
       multi-defender STEAL sequence, already sorted by tackling descending, with the current
       defender already shifted off), and
       `tackleStealPromptResume?: { phase: GamePhase; activeTeam: 'home' | 'away'; movementSlot: MovementSlot | null } | null`
       — the resume trio must be exactly these three keys, byte-identical to
       `gkDiveAtFeetResume`; do not add a fourth key. Document on the cluster that these are
       EPHEMERAL single-sequence fields cleared wherever the prompt phase exits (43-RESEARCH.md
       "Do We Actually Need stealDeclinedByIds/tackleDeclinedByIds?"), and that they are
       deliberately NOT siblings of `stealAttemptedByIds`/`tackleAttemptedByIds` and therefore
       do NOT participate in that pair's ~26-site reset table. Also record on
       `tackleStealPromptQueue` that no persistent `stealDeclinedByIds`/`tackleDeclinedByIds`
       array is introduced: TACKLE-03's ring persistence is delivered by never appending a
       declined defender to `stealAttemptedByIds`/`tackleAttemptedByIds`.
       `exactOptionalPropertyTypes` is enforced in this repo — declare every optional field as
       `?: T | null` and never assign `undefined` to one.

    In `packages/shared/src/events.ts` add `GAME_TACKLE_STEAL_CHOICE: 'game:tackle-steal-choice'`
    to `ClientEvents` beside `GAME_GK_DIVE_AT_FEET`, and its typed entry
    `[ClientEvents.GAME_TACKLE_STEAL_CHOICE]: (accept: boolean) => void;` in
    `ClientToServerEvents`, matching `GAME_GK_DIVE_AT_FEET`'s boolean-accept convention.
    Do NOT touch `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` in this plan — plan 43-03
    owns those signatures and changing them here would break the server and client build.

    In `packages/shared/src/stoppagePhases.ts` do NOT add an entry to `STOPPAGE_PHASES`. Extend
    the existing "Deliberately EXCLUDED" comment block's mid-duel-decision-prompt bullet to name
    `'TACKLE_STEAL_PROMPT'` alongside `'FOUL_CHOICE'`/`'GK_DIVE_AT_FEET_PROMPT'`, and reaffirm
    the "Exactly 15 values" count in the array's own doc comment.

    In `packages/client/src/components/GameBoard.tsx` add exactly one line to `PHASE_LABEL`:
    `TACKLE_STEAL_PROMPT: 'TACKLE / STEAL',` placed after the `GK_BOX_ENTRY_MOVE` entry. This
    is the only GameBoard change in this plan — panel routing belongs to 43-05. The entry is
    mandatory, not optional: `PHASE_LABEL` is `Record<GamePhase, string>`, so omitting it is a
    compile error (43-RESEARCH.md Pitfall 5).

    In `packages/shared/src/stoppagePhases.test.ts` add a case asserting
    `isStoppagePhase('TACKLE_STEAL_PROMPT')` is false and that `STOPPAGE_PHASES.length === 15`.

  </action>

<acceptance_criteria> - `grep -n "TACKLE_STEAL_PROMPT" packages/shared/src/types.ts` matches the GamePhase union. - `grep -n "TACKLE_STEAL_DECLINED" packages/shared/src/types.ts` matches twice (union member + ActionEvent variant). - `grep -c "ballAfter" ` over the new `TACKLE_STEAL_DECLINED` ActionEvent variant block returns 0 — the variant declares no `ballAfter` key. - All six `tackleStealPrompt*` field names plus `tackleStealDeclineEnabled` are present in `packages/shared/src/types.ts`. - `tackleStealPromptResume`'s object type declares exactly the keys `phase`, `activeTeam`, `movementSlot`. - `grep -n "GAME_TACKLE_STEAL_CHOICE" packages/shared/src/events.ts` matches twice (name constant + typed signature). - `grep -v '^\s*[/*]' packages/shared/src/stoppagePhases.ts | grep -c "'TACKLE_STEAL_PROMPT'"` returns 0 — the phase appears only inside the exclusion comment, never in the array. - `pnpm --filter @counter-attack/shared test -- stoppagePhases` passes, including the new false/length assertions. - `pnpm -r typecheck` exits 0.
</acceptance_criteria>

  <verify>
    <automated>pnpm --filter @counter-attack/shared test -- stoppagePhases &amp;&amp; pnpm -r typecheck</automated>
  </verify>

<done>The new phase, event type, event payload, GameState fields and client event name exist in the shared package; STOPPAGE_PHASES is unchanged at 15 entries; PHASE_LABEL is exhaustive again and the whole repo typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Order STEAL_ATTEMPT defenders by tackling stat, highest first (D-02)</name>

  <files>
    packages/shared/src/moveValidator.ts,
    packages/shared/src/moveValidator.test.ts
  </files>

<read_first> - packages/shared/src/moveValidator.ts lines 28-45 (`MoveResult` union — `STEAL_ATTEMPT` carries `defenders: PlayerPiece[]`) - packages/shared/src/moveValidator.ts lines 96-137 (the ZoI STEAL_ATTEMPT block at :100-112 and the TACKLE_ATTEMPT block at :122-137) - packages/shared/src/moveValidator.test.ts (existing STEAL_ATTEMPT cases — reuse their fixture-construction style) - .planning/phases/43-tackle-steal-prompt-decline/43-RESEARCH.md section "Pattern 2: Sequential Multi-Defender Ordering (D-01/D-02)" and "Pitfall 2"
</read_first>

  <behavior>
    - Given three active ZoI defenders with `tackling` 3, 7 and 5, `validateMove` returns
      `effect.defenders` in tackling order 7, 5, 3.
    - Given two ZoI defenders with equal `tackling`, the returned order matches their relative
      order in `state.pieces` (stable-sort tie-break).
    - A defender already listed in `stealAttemptedByIds` is still excluded after the sort.
    - A red-carded opponent adjacent to the destination is absent from `defenders` even when two
      active opponents are also in range (BUG-38 regression, `isActivePiece` already applied
      upstream at `moveValidator.ts:104`).
  </behavior>

  <action>
    In the `state.ball.carrierId === piece.id` block of `validateMove`, sort the already-filtered
    `defenders` array by `tackling` descending before returning the `STEAL_ATTEMPT` effect. Sort a
    copy, never the array returned by `.filter` in place if that would mutate a caller-visible
    array; a spread-then-sort with comparator `(a, b) => b.tackling - a.tackling` is the intended
    shape. Add a comment citing D-02 that states the tie-break rule explicitly: `Array.prototype.sort`
    is stable in Node 12+/all supported browsers, so defenders with equal `tackling` retain their
    `getZoIDefenders` order (itself derived from `state.pieces` order) — this is the intentional,
    documented tie-break, not an accident (43-RESEARCH.md Pitfall 2).

    Do NOT add a second `isActivePiece` filter here — `opponents` at line 104 is already filtered
    (Phase 42 commit 613a1317) and re-filtering is the explicit anti-pattern named in
    43-RESEARCH.md's "Anti-Patterns to Avoid". Do NOT change the `TACKLE_ATTEMPT` branch: that
    effect carries a single `carrierId` because the mover IS the sole defender, so D-01/D-02's
    sequencing has no meaning there.

    Extend `packages/shared/src/moveValidator.test.ts` with the four behaviours above, pinning
    explicit `tackling` values on the fixture pieces so the assertions do not depend on squad data.

  </action>

<acceptance_criteria> - `grep -n "b.tackling - a.tackling" packages/shared/src/moveValidator.ts` matches inside the STEAL_ATTEMPT block. - `grep -c "isActivePiece" packages/shared/src/moveValidator.ts` is unchanged from its pre-task value (no new filter added). - The TACKLE_ATTEMPT branch (lines ~122-137) is byte-for-byte unchanged. - `pnpm --filter @counter-attack/shared test -- moveValidator` passes, including a case asserting descending tackling order, a case asserting stable tie-break order, and a case asserting a red-carded opponent is absent from a 2-active-defender `defenders` array.
</acceptance_criteria>

  <verify>
    <automated>pnpm --filter @counter-attack/shared test -- moveValidator</automated>
  </verify>

<done>validateMove returns STEAL_ATTEMPT defenders sorted by tackling descending with a documented stable tie-break, the red-card exclusion still holds, and the TACKLE_ATTEMPT branch is untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                                                                                                                |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client socket → server socket handler | Untrusted client payloads enter the game engine here. No handler is added by this plan; the boundary is listed because this plan declares the event name and payload type the 43-05 handler will validate. |
| shared package → server + client      | `validateMove` is imported by both tiers; a change here silently changes what the client previews and what the server authorises.                                                                          |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                                 | Disposition | Mitigation Plan                                                                                                                                                                                                                    |
| --------- | ---------------------- | ----------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-43-01   | Information Disclosure | new `tackleStealPrompt*` GameState fields, broadcast in every full-state snapshot         | accept      | The cluster carries only piece ids and a team label already visible to both managers in the same snapshot (`pieces`, `ball.carrierId`); no hidden information (draft view, lineup privacy) is added.                               |
| T-43-02   | Tampering              | `GAME_TACKLE_STEAL_CHOICE` payload type declared here                                     | mitigate    | Declared as `(accept: boolean) => void` only; runtime `typeof accept !== 'boolean'` validation is a required acceptance criterion of plan 43-05's handler task — a TypeScript signature is never treated as a runtime guard.       |
| T-43-03   | Elevation of Privilege | `validateMove` defender ordering consumed by both client preview and server authorisation | mitigate    | The sort lives in the shared pure validator so client and server derive identical order; the server never trusts a client-supplied defender id — plan 43-04 reads the defender from `tackleStealPromptDefenderId` in server state. |
| T-43-SC   | Tampering              | npm/pip/cargo installs                                                                    | accept      | Zero new external packages in this phase (43-RESEARCH.md "Package Legitimacy Audit": not applicable). No install task exists, so no legitimacy checkpoint is required.                                                             |

</threat_model>

<verification>
- `pnpm -r typecheck` exits 0 (proves PHASE_LABEL exhaustiveness restored).
- `pnpm --filter @counter-attack/shared test` fully green.
- `STOPPAGE_PHASES` still contains exactly 15 entries and excludes `TACKLE_STEAL_PROMPT`.
- No `stealDeclinedByIds` / `tackleDeclinedByIds` identifier exists anywhere in the repo.
</verification>

<success_criteria>

- `TACKLE_STEAL_PROMPT`, `TACKLE_STEAL_DECLINED`, `tackleStealDeclineEnabled`, the six `tackleStealPrompt*` fields and `GAME_TACKLE_STEAL_CHOICE` are all declared.
- `validateMove` returns tackling-descending `STEAL_ATTEMPT` defenders with a documented stable tie-break.
- No behaviour changes for any existing flow: no code enters the new phase yet and no event of the new type is ever emitted yet.
  </success_criteria>

<output>
Create `.planning/phases/43-tackle-steal-prompt-decline/43-01-SUMMARY.md` when done
</output>
