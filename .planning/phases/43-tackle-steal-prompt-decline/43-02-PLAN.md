---
phase: 43-tackle-steal-prompt-decline
plan: 02
type: execute
wave: 2
depends_on: ['43-01']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/roomStore.ts
  - packages/server/src/__tests__/gameEngine.undoReplay43.test.ts
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/ActionLog.test.tsx
  - packages/client/src/components/BallLocationRing.tsx
  - packages/client/src/components/BallLocationRing.test.tsx
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/store/useGameStore.ts
autonomous: true
requirements: [TACKLE-02, TACKLE-04]

must_haves:
  truths:
    - 'The free-move zone check can never hijack the TACKLE_STEAL_PROMPT phase.'
    - 'A TACKLE_STEAL_DECLINED event produces no replay frame and is not an Undo boundary.'
    - 'A decline is visible in the match log to both managers, naming the declining defender.'
    - 'A pending GK box-entry or dive-at-feet offer is still reachable on the broadcast that resumes play after a tackle/steal prompt sequence.'
    - 'No piece can be selected on the board while the tackle/steal prompt is open.'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'ZONE_CHECK_EXEMPT_PHASES entry plus deliberate-exclusion comments for REPLAY_ELIGIBLE_TYPES and applyUndo isBoundary'
      contains: 'TACKLE_STEAL_PROMPT'
    - path: 'packages/server/src/roomStore.ts'
      provides: 'edge-trigger baseline preserved across the prompt interrupt'
      contains: 'TACKLE_STEAL_PROMPT'
    - path: 'packages/client/src/components/ActionLog.tsx'
      provides: 'formatEvent case for TACKLE_STEAL_DECLINED'
      contains: "case 'TACKLE_STEAL_DECLINED'"
    - path: 'packages/server/src/__tests__/gameEngine.undoReplay43.test.ts'
      provides: 'registration-checklist regression suite for the new phase and event'
      min_lines: 60
  key_links:
    - from: 'packages/server/src/roomStore.ts'
      to: 'packages/server/src/gameEngine.ts'
      via: 'broadcastState calls applyFreeMoveZoneCheck, which now exempts TACKLE_STEAL_PROMPT'
      pattern: 'applyFreeMoveZoneCheck'
    - from: 'packages/client/src/components/ActionPanel.tsx'
      to: 'packages/server/src/gameEngine.ts'
      via: 'canUndo boundary mirror kept term-for-term in sync with applyUndo isBoundary'
      pattern: 'isBoundary'
---

<objective>
Register the new `TACKLE_STEAL_PROMPT` phase and `TACKLE_STEAL_DECLINED` event through every
list, set and mirror that this codebase has previously shipped bugs by forgetting
(BUG-30/31, BUG-37) — including the two deliberate NON-registrations (replay eligibility and
Undo boundary) that must be written down as decisions, not left as silent omissions.

Purpose: 43-RESEARCH.md Pitfall 6 documents that `ActionEventType` and `GamePhase` additions
are not exhaustiveness-checked outside `PHASE_LABEL`. Landing the whole checklist before any
code can enter the phase means the engine plan (43-04) cannot ship a phase that the free-move
zone check silently hijacks or that swallows a pending goalkeeper offer.

Output: a fully registered phase/event with a dedicated regression suite proving each
registration and each deliberate exclusion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-tackle-steal-prompt-decline/43-CONTEXT.md
@.planning/phases/43-tackle-steal-prompt-decline/43-RESEARCH.md
@.planning/phases/43-tackle-steal-prompt-decline/43-PATTERNS.md
@.planning/phases/43-tackle-steal-prompt-decline/43-01-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `43-01-PLAN.md` — every symbol listed there is created by Phase 43 and does
not exist in the codebase before this phase. This plan adds no new exported symbols; it only
registers 43-01's symbols into existing lists and adds the test file
`packages/server/src/__tests__/gameEngine.undoReplay43.test.ts`.

<tasks>

<task type="auto">
  <name>Task 1: Register TACKLE_STEAL_PROMPT server-side and preserve the goalkeeper-offer edge trigger</name>

  <files>
    packages/server/src/gameEngine.ts,
    packages/server/src/roomStore.ts
  </files>

<read_first> - packages/server/src/gameEngine.ts lines 2835-2930 (`ZONE_CHECK_EXEMPT_PHASES` doc comment + array, `applyFreeMoveZoneCheck` at :2927) - packages/server/src/gameEngine.ts lines 3460-3535 (`applyUndo` `isBoundary` disjunction; note `GK_DIVE_AT_FEET_DECLINED` is absent by design) - packages/server/src/gameEngine.ts lines 9865-9940 (`REPLAY_ELIGIBLE_TYPES` and its "a future reader must not add them here" exclusion comments) - packages/server/src/roomStore.ts lines 378-490 (`GK_BOX_ENTRY_PHASES` set, `broadcastState`, the `ballPositionChanged` edge-trigger and the `room.lastBroadcastBallPosition` write at :488) - packages/shared/src/types.ts (43-01's new GamePhase/ActionEventType/GameState fields)
</read_first>

  <action>
    Three registrations plus one deliberate non-registration in `gameEngine.ts`:

    1. `ZONE_CHECK_EXEMPT_PHASES` — ADD `'TACKLE_STEAL_PROMPT'` to the set, with a comment citing
       TACKLE-02 stating the ball has not moved while the defending manager decides whether to
       challenge, so the final-third free-move zone check must not overlay
       `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` on top of the prompt (`freeMoveResume` has no way
       to correctly restore an interrupt phase). Same rationale as the adjacent
       `'FOUL_CHOICE'`/`'GK_DIVE_AT_FEET_PROMPT'` entries. This is a REQUIRED addition — omitting
       it makes the prompt phase reachable-but-hijackable.

    2. `REPLAY_ELIGIBLE_TYPES` — DO NOT add `'TACKLE_STEAL_DECLINED'`. Add a comment beside the
       existing `GK_DIVE_AT_FEET_DECLINED` exclusion note stating that
       `TACKLE_STEAL_DECLINED` is deliberately excluded for the identical reason (it carries no
       `ballAfter`), citing TACKLE-02 and reusing this file's own "a future reader must not add
       them here" phrasing.

    3. `applyUndo`'s `isBoundary` disjunction — DO NOT add a term for `'TACKLE_STEAL_DECLINED'`.
       Add a comment beside the existing `evt.type === 'GK_DIVE_AT_FEET'` term stating that a
       decline commits no dice outcome and must remain crossable by Undo, matching
       `GK_DIVE_AT_FEET_DECLINED`'s existing absence, and that the resolved
       `TACKLE_ATTEMPT`/`STEAL_ATTEMPT` terms already present unconditionally at the top of the
       disjunction still cover every duel resolved through the new prompt flow, so no new
       boundary term is needed for the attempt path either.

    In `roomStore.ts`:

    4. `GK_BOX_ENTRY_PHASES` — DO NOT add `'TACKLE_STEAL_PROMPT'`. Extend the set's doc comment
       to name it among the prompt phases that must not be double-interrupted, alongside the
       Phase 39 prompt phases already described there.

    5. `broadcastState` edge-trigger fix (REQUIRED — this is a real regression guard, not
       cosmetic). The final statement `room.lastBroadcastBallPosition = room.gameState.ball.position;`
       currently advances the edge-trigger baseline on every broadcast. A tackle/steal prompt
       broadcast happens with the ball already at its post-move hex but with both goalkeeper
       offers skipped (box entry because `TACKLE_STEAL_PROMPT` is not in `GK_BOX_ENTRY_PHASES`,
       dive-at-feet because its guard requires `phase === 'MOVE'`). Advancing the baseline there
       makes `ballPositionChanged` false on the resume broadcast, permanently swallowing a
       goalkeeper offer that would have fired without the toggle. Guard the assignment so the
       baseline is NOT advanced while `room.gameState.phase === 'TACKLE_STEAL_PROMPT'`, and
       comment it with TACKLE-02 plus the reasoning above. Do not touch the two offer blocks
       themselves.

  </action>

<acceptance_criteria> - `grep -n "'TACKLE_STEAL_PROMPT'" packages/server/src/gameEngine.ts` matches inside the `ZONE_CHECK_EXEMPT_PHASES` array literal. - `grep -v '^\s*[/*]' packages/server/src/gameEngine.ts | grep -c "'TACKLE_STEAL_DECLINED'"` returns 0 — the event string appears only in comments in this file (never inside `REPLAY_ELIGIBLE_TYPES` nor the `isBoundary` disjunction). - `grep -v '^\s*[/*]' packages/server/src/roomStore.ts | grep -c "GK_BOX_ENTRY_PHASES.*TACKLE_STEAL_PROMPT"` returns 0. - `packages/server/src/roomStore.ts` contains a conditional guarding the `room.lastBroadcastBallPosition` assignment on `phase !== 'TACKLE_STEAL_PROMPT'`. - `pnpm --filter @counter-attack/server test` passes with no pre-existing test regressions. - `pnpm -r typecheck` exits 0.
</acceptance_criteria>

  <verify>
    <automated>pnpm --filter @counter-attack/server test &amp;&amp; pnpm -r typecheck</automated>
  </verify>

<done>The new phase is exempt from the free-move zone check and from the box-entry offer set, the decline event is documented as deliberately absent from both replay eligibility and the Undo boundary, and the goalkeeper-offer edge trigger survives the prompt interrupt.</done>
</task>

<task type="auto">
  <name>Task 2: Register the new phase and decline event client-side</name>

  <files>
    packages/client/src/components/ActionLog.tsx,
    packages/client/src/components/BallLocationRing.tsx,
    packages/client/src/components/ActionPanel.tsx,
    packages/client/src/store/useGameStore.ts
  </files>

<read_first> - packages/client/src/components/ActionLog.tsx lines 400-470 (`STEAL_ATTEMPT` and `TACKLE_ATTEMPT` cases — prefix vocabulary `[TACKLE ✓]`/`[TACKLE ✗]`/`[INTERCEPT ✓]`) - packages/client/src/components/ActionLog.tsx lines 1150-1172 (`GK_DIVE_AT_FEET_DECLINED` case — the exact shape to mirror) - packages/client/src/components/BallLocationRing.tsx lines 30-75 (`BALL_MARKER_PHASES` set incl. the Phase 39 prompt-phase block) - packages/client/src/components/ActionPanel.tsx lines 250-332 (the `canUndo` boundary mirror and its "must stay in sync, term for term" comment block) - packages/client/src/store/useGameStore.ts lines 1228-1245 (`selectPiece`'s FOUL_CHOICE / GK_DIVE_AT_FEET_PROMPT / GK_BOX_ENTRY_PROMPT no-op branch) - .planning/phases/43-tackle-steal-prompt-decline/43-PATTERNS.md sections "ActionLog.tsx" and "GameBoard.tsx"
</read_first>

  <action>
    `ActionLog.tsx` — add a `case 'TACKLE_STEAL_DECLINED':` to `formatEvent`, placed immediately
    after the `'GK_DIVE_AT_FEET_DECLINED'` case and structurally identical to it. Return
    `prefix` derived from the event's `kind` so it matches the existing duel vocabulary:
    `'[TACKLE]'` when `event.kind === 'TACKLE'`, `'[STEAL]'` otherwise. Use
    `prefixColor: pieceColorOf(event.defenderId)`, content rendering a leading space then
    `<PNamed pieceId={event.defenderId} />` followed by the literal text `declined to challenge`,
    and `isGoal: false`. Declines ARE logged to both managers — this follows the established
    `GK_DIVE_AT_FEET_DECLINED` precedent and is not a new decision (43-CONTEXT.md discretion
    section).

    `BallLocationRing.tsx` — add `'TACKLE_STEAL_PROMPT'` to `BALL_MARKER_PHASES` in the Phase 39
    prompt-phase block, with a comment citing TACKLE-02: the ball sits with the carrier and does
    not move while the defending manager decides, exactly like `GK_DIVE_AT_FEET_PROMPT` above it.

    `ActionPanel.tsx` — the `canUndo` boundary mirror needs NO new term. Extend the existing
    "must stay in sync, term for term" comment block to record that `TACKLE_STEAL_DECLINED` is
    deliberately omitted here because it is omitted from `applyUndo`'s `isBoundary` reduce
    (43-02 Task 1), and that `TACKLE_STEAL_PROMPT` is rendered by its own dedicated panel rather
    than by `ActionPanel`, so no Undo affordance is ever offered during the prompt.

    `useGameStore.ts` — add `gameState.phase === 'TACKLE_STEAL_PROMPT'` to the existing
    three-way `selectPiece` no-op branch so it becomes a four-way check, and extend that branch's
    comment to name the new phase. This is the defense-in-depth guard that stops a stray
    `selectPiece` call re-selecting a piece while a pure two-button panel is open.

  </action>

<acceptance_criteria> - `grep -n "case 'TACKLE_STEAL_DECLINED'" packages/client/src/components/ActionLog.tsx` matches exactly once. - The new ActionLog case references `event.kind`, `event.defenderId` and the literal string `declined to challenge`. - `grep -n "'TACKLE_STEAL_PROMPT'" packages/client/src/components/BallLocationRing.tsx` matches inside the `BALL_MARKER_PHASES` set literal. - `grep -v '^\s*[/*]' packages/client/src/components/ActionPanel.tsx | grep -c "TACKLE_STEAL_DECLINED"` returns 0 — mentioned in comments only, never added as a boundary term. - `grep -n "TACKLE_STEAL_PROMPT" packages/client/src/store/useGameStore.ts` matches inside `selectPiece`'s two-button-panel no-op branch. - `pnpm --filter @counter-attack/client test` passes with no pre-existing test regressions.
</acceptance_criteria>

  <verify>
    <automated>pnpm --filter @counter-attack/client test</automated>
  </verify>

<done>A decline renders in the match log for both managers, the ball marker renders during the prompt, no piece is selectable during the prompt, and the Undo mirror records the deliberate omission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Registration-checklist regression suite</name>

  <files>
    packages/server/src/__tests__/gameEngine.undoReplay43.test.ts,
    packages/client/src/components/ActionLog.test.tsx,
    packages/client/src/components/BallLocationRing.test.tsx
  </files>

<read_first> - packages/server/src/**tests**/gameEngine.undoReplay39.test.ts (the naming/structure convention this new file mirrors, including its `Partial<Record<ActionEventType, ActionEvent>>` fixture map) - packages/server/src/**tests**/gameEngine.redCardExclusion.test.ts (state-fixture construction style for engine unit tests) - packages/server/src/gameEngine.ts lines 2927-2960 (`applyFreeMoveZoneCheck` signature and early-return shape) - packages/client/src/components/ActionLog.test.tsx (existing decline-event rendering assertions, e.g. for GK_DIVE_AT_FEET_DECLINED) - packages/client/src/components/BallLocationRing.test.tsx (existing phase-membership assertions)
</read_first>

  <behavior>
    - `REPLAY_ELIGIBLE_TYPES.has('TACKLE_STEAL_DECLINED')` is false.
    - Given a MOVE-phase state whose eventLog is `[KICK_OFF, MOVE, TACKLE_STEAL_DECLINED, MOVE]`,
      `applyUndo` reverses the trailing MOVE, and a second `applyUndo` on the result reverses the
      earlier MOVE too — i.e. Undo crosses the decline event.
    - `applyFreeMoveZoneCheck` returns a `TACKLE_STEAL_PROMPT` state with `phase` unchanged even
      when the ball sits in a final-third zone that would otherwise trigger
      `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE`.
    - `isStoppagePhase('TACKLE_STEAL_PROMPT')` is false (asserted here as well as in 43-01, since
      this is the file a future reader greps for the registration checklist).
    - ActionLog renders `declined to challenge` with the declining defender's name for a
      `TACKLE_STEAL_DECLINED` event, and uses the `[STEAL]` prefix for `kind: 'STEAL'` and the
      `[TACKLE]` prefix for `kind: 'TACKLE'`.
    - BallLocationRing renders its marker when `phase` is `TACKLE_STEAL_PROMPT`.
  </behavior>

  <action>
    Create `packages/server/src/__tests__/gameEngine.undoReplay43.test.ts` covering the four
    server behaviours above. Import `REPLAY_ELIGIBLE_TYPES`, `applyUndo` and
    `applyFreeMoveZoneCheck` from `../gameEngine.js` and `isStoppagePhase` from
    `@counter-attack/shared`. Name the describe block after the registration checklist so a
    future reader adding a new `ActionEventType` finds it, and add a file header comment listing
    the six checklist sites (`ActionEventType` union, `formatEvent`, `REPLAY_ELIGIBLE_TYPES`,
    `applyUndo` `isBoundary`, `PHASE_LABEL`, `STOPPAGE_PHASES`) with which of them this phase
    registers versus deliberately excludes.

    Extend the two client test files with the ActionLog and BallLocationRing behaviours above,
    reusing each file's existing fixture helpers rather than introducing a new fixture style.

  </action>

<acceptance_criteria> - `pnpm --filter @counter-attack/server test -- undoReplay43` passes with at least four assertions covering: replay exclusion, Undo crossing the decline (two successive undos), zone-check exemption, and stoppage-phase exclusion. - `pnpm --filter @counter-attack/client test -- ActionLog` passes, including a `[STEAL]`-prefix case and a `[TACKLE]`-prefix case that both assert the rendered text contains `declined to challenge`. - `pnpm --filter @counter-attack/client test -- BallLocationRing` passes with a `TACKLE_STEAL_PROMPT` case.
</acceptance_criteria>

  <verify>
    <automated>pnpm --filter @counter-attack/server test -- undoReplay43 &amp;&amp; pnpm --filter @counter-attack/client test -- ActionLog BallLocationRing</automated>
  </verify>

<done>Every registration and every deliberate non-registration is pinned by an automated assertion, so the BUG-30/31/37 bug class cannot recur silently for this phase.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                                                                                  |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| server broadcast → both clients | `broadcastState` emits the full authoritative snapshot; this plan changes when the edge-trigger baseline advances, which controls whether a goalkeeper interrupt is offered. |
| event log → client rendering    | `formatEvent` renders untrusted-in-principle event payload ids into the match log.                                                                                           |

## STRIDE Threat Register

| Threat ID | Category          | Component                                           | Disposition | Mitigation Plan                                                                                                                                                                                                                                                           |
| --------- | ----------------- | --------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-43-04   | Denial of Service | `broadcastState` edge-trigger baseline preservation | mitigate    | The baseline is held only while `phase === 'TACKLE_STEAL_PROMPT'`; the prompt phase is provably finite (43-04 shifts the queue on every response and exits when empty), so the baseline always advances again. Asserted by 43-06's sequence-termination integration test. |
| T-43-05   | Repudiation       | decline not visible to the opposing manager         | mitigate    | `formatEvent` case renders `TACKLE_STEAL_DECLINED` into the shared match log for both managers, matching the `GK_DIVE_AT_FEET_DECLINED` precedent; asserted in `ActionLog.test.tsx`.                                                                                      |
| T-43-06   | Tampering         | client-rendered piece ids in the new log entry      | accept      | `PNamed`/`pieceColorOf` resolve ids against the authoritative broadcast `pieces` array; an id not present renders as a non-match rather than injecting markup. Same trust model as every existing `formatEvent` case.                                                     |
| T-43-SC   | Tampering         | npm/pip/cargo installs                              | accept      | Zero new external packages (43-RESEARCH.md "Package Legitimacy Audit": not applicable). No install task, so no legitimacy checkpoint applies.                                                                                                                             |

</threat_model>

<verification>
- `pnpm -r test` green.
- `pnpm -r typecheck` exits 0.
- `TACKLE_STEAL_DECLINED` appears in `gameEngine.ts` only inside comments.
- `TACKLE_STEAL_PROMPT` appears in `ZONE_CHECK_EXEMPT_PHASES` and `BALL_MARKER_PHASES`, and in no other phase-membership list.
</verification>

<success_criteria>

- Every checklist site from 43-RESEARCH.md Pitfall 6 is either registered or documented as a deliberate exclusion, and each is covered by an automated assertion.
- No behaviour changes for existing flows: nothing yet enters the new phase, and the only live code change outside comments is the `lastBroadcastBallPosition` guard, which is a no-op until the prompt phase becomes reachable in 43-04.
  </success_criteria>

<output>
Create `.planning/phases/43-tackle-steal-prompt-decline/43-02-SUMMARY.md` when done
</output>
