---
phase: 08.2-passing-cleanup-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/passValidator.ts
  - packages/shared/src/passValidator.test.ts
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/server/src/__tests__/gameEngine.phase8.test.ts
autonomous: true
requirements: [PASS-04]
must_haves:
  truths:
    - "Long Pass accuracy check uses the carrier's highPass attribute, not dribbling (D-04)"
    - 'GameState carries passTargetHex, headerContestants, headerConfirmed, contestedPieceIds, and preGeneratedInterceptionDice fields'
    - 'ClientToServerEvents declares GAME_ROLL with an optional targetHex parameter and a GAME_HEADER_CONTESTANT event'
    - 'Wave 0 failing-test scaffolds exist for the D-04 attribute fix and the HEAD-05 exclusion behavior'
  artifacts:
    - path: 'packages/shared/src/passValidator.ts'
      provides: 'Long Pass accuracy using piece.highPass'
      contains: 'const attribute = piece.highPass'
    - path: 'packages/shared/src/types.ts'
      provides: 'GameState passing/header fields'
      contains: 'passTargetHex'
    - path: 'packages/shared/src/events.ts'
      provides: 'GAME_ROLL targetHex + GAME_HEADER_CONTESTANT'
      contains: 'GAME_HEADER_CONTESTANT'
  key_links:
    - from: 'packages/shared/src/passValidator.ts'
      to: 'piece.highPass'
      via: 'Long Pass attribute selection'
      pattern: "const attribute = piece\\.highPass"
---

<objective>
Lay the shared-package foundation for Phase 8.2 passing cleanup: correct the Long Pass accuracy attribute (D-04), add the GameState fields that the engine and client both consume, and extend the typed Socket.io event surface. Also create the Wave 0 failing-test scaffolds that downstream engine work must turn green.

Purpose: Every other plan in this phase imports these shared types, the corrected validator, and the extended events. Defining the contracts first prevents the executor scavenger-hunt anti-pattern.
Output: One validator one-line fix, four optional GameState fields, two event signature changes, and two failing test scaffolds (D-04 correction, HEAD-05 exclusion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Long Pass accuracy attribute (D-04) and update its test fixture</name>
  <files>packages/shared/src/passValidator.ts, packages/shared/src/passValidator.test.ts</files>
  <read_first>
    - packages/shared/src/passValidator.ts (the JSDoc at lines 120-134 and validatePassAccuracy body at lines 135-146 — current code uses `passType === 'HIGH' ? piece.highPass : piece.dribbling`)
    - packages/shared/src/passValidator.test.ts (locate the existing LONG_CROSS_THIRD / LONG_SAME_THIRD accuracy tests, approx line 207, which assert against a `dribbling`-based combined score)
  </read_first>
  <behavior>
    - Given a piece with highPass=5, dribbling=1, a LONG_SAME_THIRD accuracy check with die=4 yields combined 9 → accurate (uses highPass, not dribbling).
    - Given a piece with highPass=2, dribbling=6, a LONG_SAME_THIRD check with die=4 yields combined 6 → inaccurate with triggerLooseBall:true (proves dribbling is NOT used).
    - HIGH pass accuracy behavior is unchanged (still uses highPass, threshold 8).
  </behavior>
  <action>
    In validatePassAccuracy (packages/shared/src/passValidator.ts), replace the attribute selection line `const attribute = passType === 'HIGH' ? piece.highPass : piece.dribbling;` with `const attribute = piece.highPass;` so HIGH, LONG_SAME_THIRD, and LONG_CROSS_THIRD all read piece.highPass per D-04. Update the JSDoc attribute-mapping note (lines 124-126) to state "Long Pass accuracy uses piece.highPass (D-04, Phase 8.2 correction)" and remove the "Long Pass accuracy uses piece.dribbling" line. Keep the threshold logic unchanged (HIGH=8, LONG_SAME_THIRD=9, LONG_CROSS_THIRD=10) and keep computeCombinedScore usage unchanged. In passValidator.test.ts, update the existing Long Pass accuracy test fixture so its expected combined score is computed from highPass; add the two behavior cases above as explicit assertions (one accurate-via-highPass, one inaccurate-when-dribbling-is-high-but-highPass-is-low). Cite D-04 in a test description string.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test run passValidator</automated>
  </verify>
  <acceptance_criteria>
    - passValidator.ts contains `const attribute = piece.highPass;` and does NOT contain `piece.dribbling` inside validatePassAccuracy
    - passValidator.ts JSDoc no longer contains the string "uses piece.dribbling"
    - passValidator.test.ts contains a test asserting LONG accuracy is computed from highPass (description references D-04)
    - `pnpm --filter @counter-attack/shared test run passValidator` exits 0
  </acceptance_criteria>
  <done>Long Pass accuracy uses highPass for both same-third and cross-third types; tests prove dribbling is no longer consulted; shared suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Add GameState passing/header fields and extend typed events</name>
  <files>packages/shared/src/types.ts, packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/types.ts (the GameState type, especially the existing optional fields region around `snapshotPenalty`, `pendingFreeMove` — copy their optional-field JSDoc style; confirm HexCoord is imported/available)
    - packages/shared/src/events.ts (the ClientEvents const object lines 7-33, and ClientToServerEvents interface lines 47-76; the existing GAME_ROLL signature at lines 53-55 and GAME_SHOT/GAME_HEADER entries as the analog)
  </read_first>
  <action>
    In packages/shared/src/types.ts, add five optional fields to GameState following the existing optional-field JSDoc convention: (1) `passTargetHex?: HexCoord | null` — set by the GAME_ROLL handler before applyRoll, consumed and cleared by the applyRoll PASS branch (D-10); (2) `headerContestants?: { home: string | null; away: string | null } | null` — per-team selected contestant piece IDs during HEADER phase (D-17); (3) `headerConfirmed?: { home: boolean; away: boolean } | null` — true once a team submits its contestant; GAME_ROLL for HEADER requires both true (D-17); (4) `contestedPieceIds?: readonly string[]` — IDs of pieces that contested a header this sequence, excluded from the next Movement Phase, cleared in applyStartMovement (D-21, HEAD-05); (5) `preGeneratedInterceptionDice?: number[]` — dice pre-rolled by the GAME_ROLL handler for each interceptor in validatePass's interceptors list, consumed and cleared by the applyRoll PASS branch interception loop (D-11). Ensure HexCoord is imported if not already. In packages/shared/src/events.ts, add the constant `GAME_HEADER_CONTESTANT: 'game:header-contestant'` to ClientEvents; extend the ClientToServerEvents `[ClientEvents.GAME_ROLL]` signature to add a second optional parameter `targetHex?: HexCoord` after the existing `passType?` parameter (D-10); add `[ClientEvents.GAME_HEADER_CONTESTANT]: (pieceId: string | null) => void` to ClientToServerEvents (D-17). Import HexCoord into events.ts if needed. Do not change any other event.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared build</automated>
  </verify>
  <acceptance_criteria>
    - types.ts GameState contains `passTargetHex`, `headerContestants`, `headerConfirmed`, `contestedPieceIds`, and `preGeneratedInterceptionDice`
    - events.ts ClientEvents contains `GAME_HEADER_CONTESTANT: 'game:header-contestant'`
    - events.ts GAME_ROLL signature contains `targetHex?: HexCoord`
    - events.ts ClientToServerEvents contains a `GAME_HEADER_CONTESTANT` entry typed `(pieceId: string | null) => void`
    - `pnpm --filter @counter-attack/shared build` exits 0 (TypeScript compiles)
  </acceptance_criteria>
  <done>Shared types expose the five passing/header state fields (including preGeneratedInterceptionDice); events expose the targetHex roll parameter and the header-contestant event; shared package builds.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 — scaffold the HEAD-05 exclusion failing test</name>
  <files>packages/server/src/__tests__/gameEngine.phase8.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.phase8.test.ts (existing Phase 8 engine tests — copy the buildInitialGameState / applyRoll setup helpers and assertion style used in this file)
    - packages/server/src/gameEngine.ts (read the current applyStartMovement signature and how movablePieceIds / movement eligibility is currently represented, so the test asserts against the real shape — do not invent a field name)
  </read_first>
  <action>
    Add a failing (initially red) test to gameEngine.phase8.test.ts named with a string referencing HEAD-05 and D-21: "HEAD-05: a piece that contested a header is excluded from the subsequent Movement Phase". Construct a GameState whose `contestedPieceIds` contains one known piece ID, then call applyStartMovement, and assert that the contested piece ID is NOT present in whatever field the engine uses to express movable pieces for that movement (read gameEngine.ts to use the real field — e.g. movablePieceIds, or an exclusion set, or that moving it via applyMove is rejected). Also assert that after the movement starts, `contestedPieceIds` is cleared to an empty array. If applyStartMovement does not yet support exclusion, the assertion will fail — that is the intended Wave 0 RED state; mark it with a comment `// RED until 08.2-02 implements HEAD-05 exclusion`. Do NOT implement the engine change here.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test run gameEngine.phase8 -t HEAD-05 2>&1 | grep -Eiv '^#' | grep -Ei 'HEAD-05|fail|pass|✓|×' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.phase8.test.ts contains a test whose description includes "HEAD-05" and "D-21"
    - The test references `contestedPieceIds` and asserts exclusion + clearing
    - The new test currently FAILS (RED) — exclusion logic is implemented in plan 08.2-02; the comment `// RED until 08.2-02` is present
  </acceptance_criteria>
  <done>A failing HEAD-05 exclusion test exists, pinned to the real applyStartMovement shape, ready for plan 08.2-02 to satisfy.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                                      |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| client→server (Socket.io events) | Pass type, target hex, and header-contestant piece IDs originate on the untrusted client and cross into authoritative game state |

## STRIDE Threat Register

| Threat ID | Category  | Component                                | Disposition | Mitigation Plan                                                                                                                                    |
| --------- | --------- | ---------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| T-08.2-01 | Tampering | events.ts GAME_ROLL targetHex param      | mitigate    | Type the param as `HexCoord` only; runtime shape validation (q,r numbers) is enforced server-side in plan 08.2-04, not trusted from the type alone |
| T-08.2-02 | Tampering | events.ts GAME_HEADER_CONTESTANT pieceId | mitigate    | Type as `string                                                                                                                                    | null`; server-side ownership check (piece belongs to socket's team) enforced in plan 08.2-04 |
| T-08.2-SC | Tampering | npm/pip/cargo installs                   | accept      | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages); no supply-chain surface introduced                       |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test run` — all shared tests pass including the updated Long Pass accuracy test
- `pnpm --filter @counter-attack/shared build` — shared package compiles with the new GameState fields and events
- The HEAD-05 scaffold test is RED (expected until 08.2-02)
</verification>

<success_criteria>

- D-04 implemented: validatePassAccuracy uses piece.highPass for all High/Long types; no `piece.dribbling` remains in that function
- GameState exposes passTargetHex, headerContestants, headerConfirmed, contestedPieceIds
- Events expose GAME_ROLL targetHex and GAME_HEADER_CONTESTANT
- HEAD-05 failing scaffold committed
  </success_criteria>

<output>
Create `.planning/phases/08.2-passing-cleanup-inserted/08.2-01-SUMMARY.md` when done
</output>
