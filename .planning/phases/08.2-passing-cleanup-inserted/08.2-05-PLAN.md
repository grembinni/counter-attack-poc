---
phase: 08.2-passing-cleanup-inserted
plan: 05
type: execute
wave: 5
depends_on: ['08.2-01', '08.2-04']
files_modified:
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/PieceOverlay.tsx
  - packages/client/src/components/HexGrid.module.css
autonomous: true
requirements: [PASS-01, PASS-02, PASS-03, PASS-04, PASS-05]
must_haves:
  truths:
    - 'Selecting a pass type computes valid target hexes client-side and splits them into safe (green) and interception-risk (amber) sets (D-06, D-07, D-09)'
    - 'Clicking a valid target hex confirms passTargetHex and enables the Roll button; clicking the confirmed hex again deselects it (D-06)'
    - 'Roll emits game:roll with the selected passType and confirmed targetHex (D-10)'
    - 'During HEADER phase, eligible own pieces (≤2 hexes from the ball) can be toggled as the contestant with a green ring, and Confirm emits game:header-contestant (D-17)'
    - 'The Roll Header button is enabled only after both teams confirm their contestant (Pitfall 5)'
  artifacts:
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'pass target + header contestant store slices and actions'
      contains: 'setSelectedPassType'
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: 'pass target highlights + header contestant rings'
      contains: 'validPassTargetHexSet'
  key_links:
    - from: 'packages/client/src/components/ActionPanel.tsx'
      to: 'useGameStore.setSelectedPassType / setPassTargetHex / emitRoll'
      via: 'three-step PASS flow'
      pattern: 'setSelectedPassType'
    - from: 'packages/client/src/store/useGameStore.ts setSelectedPassType'
      to: 'validatePass'
      via: 'client-side valid target computation'
      pattern: "validatePass\\("
---

<objective>
Build the client side of Phase 8.2 passing: store slices and actions for pass-target and header-contestant selection, the three-step PASS flow in ActionPanel, pass-target highlighting (green/amber) plus the confirmed-target ring in HexGrid, and the HEADER contestant selection UI (green ring toggle + Confirm + gated Roll Header).

Purpose: This makes passing usable end-to-end in the browser. It computes valid targets via the shared validatePass (D-07), shows interception risk (D-09), sends the target with the roll (D-10), and drives the two-team header contestant selection protocol (D-17).
Output: Extended useGameStore, a three-step ActionPanel PASS flow, HexGrid pass-target and contestant rendering, a PieceOverlay header-contestant ring, and the reused amber risk style.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-PATTERNS.md
@packages/shared/src/events.ts
@packages/shared/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useGameStore — pass target + header contestant slices and actions (D-06, D-07, D-09, D-17)</name>
  <files>packages/client/src/store/useGameStore.ts</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts lines 1-90 (imports; the GameStore type including emitRoll signature ~77; existing slices tackleRiskHexes ~31)
    - packages/client/src/store/useGameStore.ts lines 120-260 (selectPiece action ~120-173 for the hexesInRange+validateMove computation pattern to mirror; setGameState phaseChanged branch ~195-210 for the clear-on-phase-change pattern; emitRoll ~250-252)
    - packages/shared/src/passValidator.ts (validatePass signature + PassType union; the result.ok interceptors array)
    - packages/shared/src/hex.ts (hexLine and getZoIDefenders signatures; PITCH_HEXES export location)
    - packages/shared/src/events.ts (ClientEvents.GAME_ROLL and GAME_HEADER_CONTESTANT signatures from plan 01)
    - packages/shared/src/types.ts (HexCoord; the PassType union used in ActionPanel)
  </read_first>
  <action>
    In useGameStore.ts add state fields following the tackleRiskHexes pattern: `validPassTargetHexes: HexCoord[]`, `interceptionRiskHexes: HexCoord[]`, `passTargetHex: HexCoord | null`, `selectedPassType: PassType | null`, `headerContestantId: string | null`. Add actions: `setSelectedPassType(passType: PassType | null)`, `setPassTargetHex(hex: HexCoord | null)`, `emitHeaderContestant(pieceId: string | null)`. Implement setSelectedPassType mirroring selectPiece: when null, clear selectedPassType/validPassTargetHexes/interceptionRiskHexes and return; otherwise find the carrier (ball.carrierId), map the ActionPanel passType to the validatePass type (STANDARD_PASS->'STANDARD', FIRST_TIME_PASS->'FIRST_TIME', HIGH_PASS->'HIGH', LONG_BALL->'LONG'), iterate PITCH_HEXES calling validatePass(gameState, carrier, carrier.position, hex, vpType); push ok hexes to validTargets; compute the path `hexLine(carrier.position, hex).slice(1,-1)` and if any intermediate hex has `getZoIDefenders(pathHex, opponents).length > 0` push to interceptionRisk (D-09); set selectedPassType, validPassTargetHexes, interceptionRiskHexes, and reset passTargetHex to null. Implement setPassTargetHex as a simple `set({ passTargetHex: hex })`. Implement emitHeaderContestant as `socket.emit(ClientEvents.GAME_HEADER_CONTESTANT, pieceId)`. Extend emitRoll to accept an optional second arg and emit `socket.emit(ClientEvents.GAME_ROLL, passType, targetHex)`; update the GameStore emitRoll type signature to include `targetHex?: HexCoord`. In setGameState's phaseChanged clear branch (~195-210) also reset selectedPassType:null, validPassTargetHexes:[], interceptionRiskHexes:[], passTargetHex:null, headerContestantId:null. Add validatePass, hexLine, getZoIDefenders, PITCH_HEXES to the `@counter-attack/shared` import if not already present.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client build 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - useGameStore.ts declares validPassTargetHexes, interceptionRiskHexes, passTargetHex, selectedPassType, headerContestantId
    - setSelectedPassType iterates PITCH_HEXES with validatePass and splits results into valid + interception-risk via hexLine/getZoIDefenders
    - emitRoll emits both passType and targetHex; its type signature includes `targetHex?: HexCoord`
    - emitHeaderContestant emits ClientEvents.GAME_HEADER_CONTESTANT
    - setGameState clears the new slices on phase change
    - `pnpm --filter @counter-attack/client build` exits 0
  </acceptance_criteria>
  <done>The store computes valid/at-risk pass targets on type selection, holds the confirmed target and header contestant, and emits the extended roll and contestant events.</done>
</task>

<task type="auto">
  <name>Task 2: ActionPanel three-step PASS flow + HEADER contestant confirm + Long Ball Header button (D-06, D-16, D-17)</name>
  <files>packages/client/src/components/ActionPanel.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx lines 40-200 (current two-step PASS flow ~70-146; the local useState selectedPassType to remove; the eligible-actions set; the HEADER block ~188-197 to replace; the emitRoll call site)
    - packages/client/src/store/useGameStore.ts (the new selectedPassType/setSelectedPassType/passTargetHex/emitHeaderContestant/headerContestantId selectors added in Task 1; gameState.headerConfirmed)
    - packages/shared/src/events.ts (ClientEvents.GAME_HEADER for the Long Ball Header button emit)
  </read_first>
  <action>
    In ActionPanel.tsx replace the local `useState` selectedPassType (and any `useEffect` clearing it on phase change) with store selectors: selectedPassType, setSelectedPassType, passTargetHex from useGameStore (clearing is now done by setGameState). Restructure the PASS flow into three steps: Step 1 (selectedPassType === null) keeps the existing pass-type chooser buttons; ALSO, when the eligible actions set contains 'HEADER' (Long Ball case, D-16), render a Header button that emits ClientEvents.GAME_HEADER (server transitions to HEADER phase). Step 2 (selectedPassType !== null && passTargetHex === null): render a "<PassType label> — click a target hex" prompt plus a Back button that calls setSelectedPassType(null). Step 3 (selectedPassType !== null && passTargetHex !== null): render a Roll Dice button whose handler calls emitRoll(selectedPassType, passTargetHex) then setSelectedPassType(null), plus a Back button. Replace the HEADER block (~188-197): read headerContestantId, emitHeaderContestant, and gameState.headerConfirmed from the store; compute myTeam's slot; show "Select contestant on board" when this team has not confirmed, with a Confirm button that calls emitHeaderContestant(headerContestantId) (label "Confirm Selection" if headerContestantId set, else "Decline (no contestant)"); when both headerConfirmed.home && headerConfirmed.away are true, show a "Roll Header" button calling emitRoll() (Pitfall 5 — only enabled when both confirmed). Keep the existing gameError display.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client build 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ActionPanel.tsx no longer declares a local `useState` for selectedPassType (it reads from the store)
    - The PASS flow has three distinct render branches keyed on selectedPassType and passTargetHex
    - A Header button rendering is gated on the eligible set containing 'HEADER' and emits ClientEvents.GAME_HEADER
    - The HEADER block's Roll Header button is gated on both headerConfirmed.home and headerConfirmed.away
    - `pnpm --filter @counter-attack/client build` exits 0
  </acceptance_criteria>
  <done>ActionPanel drives pass type → target hex → Roll, offers a Header button after Long Ball, and gates Roll Header behind both teams confirming.</done>
</task>

<task type="auto">
  <name>Task 3: HexGrid pass-target highlights + confirmed-target ring + header contestant rings (D-06, D-09, D-17)</name>
  <files>packages/client/src/components/HexGrid.tsx, packages/client/src/components/PieceOverlay.tsx, packages/client/src/components/HexGrid.module.css</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx lines 40-260 (store subscriptions ~47-62; the validMoveHexSet/zoiRiskSet/tackleRiskSet O(1) pattern ~72-85; the per-hex map loop classification ~130-160; onClick assignment block; the polygon overlay rendering ~174-221; the piece rendering / PieceOverlay usage and its onClick derivation ~238-251)
    - packages/client/src/components/PieceOverlay.tsx lines 70-100 (the isSelected gold-ring pattern ~81-92 to mirror for the green contestant ring; the props shape)
    - packages/client/src/components/HexGrid.module.css lines 1-20 (.hexZoIRisk and .hexTackleRisk classes — reuse hexTackleRisk amber for interception-risk per D-09/D-20)
    - packages/shared/src/hex.ts (hexDistance for the ≤2 hex header eligibility check)
  </read_first>
  <action>
    In HexGrid.tsx add store subscriptions: validPassTargetHexes, interceptionRiskHexes, passTargetHex, selectedPassType, setPassTargetHex, plus gameState (for headerContestants/ball.position) and the store's headerContestantId + a setter for it (add a `setHeaderContestantId` action in the store if not already present, mirroring a simple set). Build O(1) sets: `validPassTargetHexSet` and `interceptionRiskSet` from the arrays (mirror validMoveHexSet). In the per-hex map loop add: `isPassTarget = phase === 'PASS' && selectedPassType !== null && validPassTargetHexSet.has(hexId)`; `isInterceptionRisk = isPassTarget && interceptionRiskSet.has(hexId)`; `isConfirmedPassTarget = passTargetHex !== null && hex.q === passTargetHex.q && hex.r === passTargetHex.r`. In the onClick block: if isPassTarget && passTargetHex === null assign `onClick = () => setPassTargetHex(hex)`; else if isConfirmedPassTarget assign `onClick = () => setPassTargetHex(null)` (deselect). Render overlays after the existing ones: a green polygon `fill="rgba(34,197,94,0.4)"` when isPassTarget && !isInterceptionRisk; an amber polygon using the `styles.hexTackleRisk` class when isInterceptionRisk; a gold-outline polygon (fill none, stroke "#f5c518", strokeWidth 2) when isConfirmedPassTarget. For header contestants: when phase === 'HEADER', for each own piece compute `hexDistance(piece.position, ball.position) <= 2` as eligibility; make eligible own pieces clickable to toggle headerContestantId in the store (set to the piece id, or null if already selected); pass `isHeaderContestant={piece.id === headerContestantId}` to PieceOverlay. In PieceOverlay.tsx add an optional `isHeaderContestant?: boolean` prop and render a green ring (circle, fill none, stroke "#22c55e", strokeWidth 2.5, r one unit larger than the selected ring, pointerEvents none) when true, mirroring the isSelected ring. In HexGrid.module.css no new class is required if hexTackleRisk is reused; if a dedicated `.hexInterceptionRisk` is added, set `fill: rgba(255,140,0,0.55)` to match the Phase 8.1 amber.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client build 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - HexGrid.tsx builds `validPassTargetHexSet` and `interceptionRiskSet` and uses `.has(hexId)` (no `.some()` in the render loop)
    - Clicking a valid unconfirmed target calls setPassTargetHex(hex); clicking the confirmed target calls setPassTargetHex(null)
    - Green / amber / gold-outline overlays render for safe / interception-risk / confirmed targets respectively
    - PieceOverlay.tsx renders a green ring when isHeaderContestant is true
    - Header-eligible own pieces (≤2 hexes from ball) are clickable to toggle headerContestantId
    - `pnpm --filter @counter-attack/client build` exits 0
  </acceptance_criteria>
  <done>HexGrid highlights safe/at-risk/confirmed pass targets and renders the header contestant green ring with eligibility-gated clicking.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                                                                                                                      |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client UI→client store | User clicks select pass type, target hex, and header contestant; the store computes highlights and emits Socket.io events. The authoritative validation is server-side (plan 04); client computation is UX-only. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                         | Disposition | Mitigation Plan                                                                                                                                                                                                       |
| --------- | ---------------------- | ----------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08.2-11 | Tampering              | client-computed validPassTargetHexes drives the emitted targetHex | mitigate    | Client highlights are advisory only; the server re-runs validatePass on GAME_ROLL (plan 08.2-04) and rejects invalid targets with INVALID_TARGET — the client must handle the error and re-compute (Pitfall 3)        |
| T-08.2-12 | Information Disclosure | header contestant selection visible pre-roll                      | accept      | Both teams' confirmed flags are intentionally broadcast (D-17 simultaneous selection); the selected pieceId is only revealed via the green ring on the selecting client until confirm — acceptable for a 2-player POC |
| T-08.2-SC | Tampering              | npm/pip/cargo installs                                            | accept      | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages)                                                                                                                              |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client build` — client compiles with the new store, ActionPanel, HexGrid, PieceOverlay
- `pnpm -r test run` — full suite green (no client unit tests added; behavior verified manually in plan 08.2-06)
- The three-step PASS flow and header contestant UI are verified by a human in plan 08.2-06
</verification>

<success_criteria>

- D-06/D-07 implemented: pass type selection computes and highlights valid targets; click confirms; Roll enabled
- D-09 implemented: interception-risk targets shown in amber
- D-10 implemented: Roll emits passType + targetHex
- D-16 implemented: Long Ball offers a Header button
- D-17 implemented: header contestant green-ring toggle + Confirm + both-confirmed Roll gate
  </success_criteria>

<output>
Create `.planning/phases/08.2-passing-cleanup-inserted/08.2-05-SUMMARY.md` when done
</output>
