---
phase: 08.2-passing-cleanup-inserted
plan: 03
type: execute
wave: 3
depends_on: ['08.2-01', '08.2-02']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.phase8.test.ts
autonomous: true
requirements: [PASS-03, PASS-05]
must_haves:
  truths:
    - 'The HEADER duel reads the per-team selected contestants from state.headerContestants instead of auto-selecting the nearest defender (D-17)'
    - 'An uncontested header (defender did not select) auto-wins for the attacker with no dice roll (HEAD-02, D-19)'
    - 'When neither team selects a contestant, the header resolves to LOOSE_BALL from ball.position (D-19)'
    - 'Pieces that contested a header are recorded in contestedPieceIds and excluded from the next Movement Phase, then cleared (HEAD-05, D-21)'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'HEADER branch reads headerContestants; applyStartMovement excludes contestedPieceIds'
      contains: 'headerContestants'
    - path: 'packages/server/src/__tests__/gameEngine.phase8.test.ts'
      provides: 'HEAD-05 exclusion satisfied + header contestant duel tests'
      contains: 'HEAD-05'
  key_links:
    - from: 'packages/server/src/gameEngine.ts HEADER branch'
      to: 'state.headerContestants'
      via: 'contestant selection read'
      pattern: "state\\.headerContestants"
    - from: 'packages/server/src/gameEngine.ts applyStartMovement'
      to: 'state.contestedPieceIds'
      via: 'movement exclusion + clear'
      pattern: 'contestedPieceIds'
---

<objective>
Replace the HEADER duel's nearest-defender auto-selection with the per-team contestant selections stored on GameState, handle the uncontested and neither-selected cases per the rules, and implement HEAD-05: pieces that contested a header are excluded from the immediately following Movement Phase and then cleared.

Purpose: Phase 8.2 wires the header contestant selection UI to the existing duel resolution. The engine must read selected contestants (set by the GAME_HEADER_CONTESTANT handler in plan 04) and enforce HEAD-05 movement exclusion, turning the plan-01 RED scaffold green.
Output: A rewritten HEADER branch, an updated applyStartMovement with contestant exclusion + clearing, and engine tests covering contested/uncontested/neither-selected duels plus the HEAD-05 exclusion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-PATTERNS.md
@packages/shared/src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the HEADER branch to read headerContestants and set contestedPieceIds (D-17, D-18, D-19, D-22)</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.phase8.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 900-1068 (current HEADER branch — the nearest-defender auto-select logic ~910-920 that must be replaced; the existing duel resolution, combined-score, and result-state shape to preserve)
    - packages/shared/src/headingValidator.ts (validateHeading signature and the HEAD-01 penaltyModifier it returns for the ≤1 vs ≤2 hex distance rule — reuse it; do NOT hand-roll the -1 penalty)
    - packages/shared/src/hex.ts (hexDistance signature; computeCombinedScore for the duel score)
    - packages/shared/src/types.ts (headerContestants, headerConfirmed, contestedPieceIds field shapes from plan 01; attackingTeam 'home'|'away')
    - packages/server/src/__tests__/gameEngine.phase8.test.ts (the buildInitialGameState helper and the existing HEADER duel tests to extend)
  </read_first>
  <behavior>
    - Both teams selected: duel uses the attacking team's contestant vs the defending team's contestant. Combined score = heading + die (with -1 if the contestant is 2 hexes from ball.position per validateHeading). Attacker wins on tie or higher score → possession transfers to attacker at ball.position, phase 'PASS' (D-22 defers headed-shot to 8.3).
    - Uncontested (attacker selected, defender did not): attacker auto-wins with NO dice roll (HEAD-02).
    - Defender selected but attacker did not: attacker wins by default (they hold possession).
    - Neither team selected: phase 'LOOSE_BALL' from ball.position.
    - After any contested/uncontested resolution, contestedPieceIds contains the ids of pieces that were selected (attacker and/or defender), and headerContestants/headerConfirmed are cleared to null.
  </behavior>
  <action>
    In applyRoll's `case 'HEADER'` (packages/server/src/gameEngine.ts ~900-1068): remove the nearest-defender auto-select block (~910-920) and instead read contestants from state. Compute attackerContestantId = state.attackingTeam === 'home' ? state.headerContestants?.home : state.headerContestants?.away; defenderContestantId = the other team's entry. Resolve attacker = the piece with attackerContestantId, falling back to the ball carrier piece if attackerContestantId is null (attacker holds possession). Resolve defender = the piece with defenderContestantId, or undefined when null. Branch: (a) if both attacker and defender exist → run the existing duel using computeCombinedScore(attacker.heading, d1, penaltiesFromValidateHeading) vs computeCombinedScore(defender.heading, d2, defenderPenalties); attacker wins on tie-or-greater; apply HEAD-01 penalty via validateHeading's penaltyModifier for whichever contestant is 2 hexes from ball.position. (b) defender undefined (uncontested) → attacker auto-wins, no dice. (c) attacker undefined AND defender undefined (neither selected) → return phase 'LOOSE_BALL', ball.carrierId null, ball.position unchanged. For the attacker-wins outcomes, return phase 'PASS', ball {position: state.ball.position, carrierId: attacker.id}, lastActionType 'HEADER', and set contestedPieceIds to the array of selected ids ([attacker?.id, defender?.id] filtered to those actually selected), and clear headerContestants:null, headerConfirmed:null (D-21). Preserve the existing lastDiceRoll/eventLog conventions used by the prior HEADER tests. In gameEngine.phase8.test.ts add tests: both-selected duel (attacker wins on tie), uncontested attacker auto-win (assert no dependency on dice), neither-selected → LOOSE_BALL, and that contestedPieceIds is populated with the selected ids after a contested duel. Cite D-17/D-19/HEAD-02 in test descriptions.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test run gameEngine.phase8 2>&1 | tr -d '\r' | grep -Ei 'HEADER|HEAD-02|contestant|uncontested|fail|✓|×' | head -30</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts HEADER branch references `state.headerContestants` and does NOT contain the nearest-defender auto-select loop
    - The neither-selected path returns `phase: 'LOOSE_BALL'`
    - The uncontested path resolves without reading a defender die (HEAD-02 auto-win)
    - Attacker-wins outcomes set `contestedPieceIds` to the selected ids and clear `headerContestants` to null
    - gameEngine.phase8.test.ts contains tests for both-selected, uncontested, and neither-selected cases
    - `pnpm --filter @counter-attack/server test run gameEngine.phase8` exits 0 for these HEADER tests
  </acceptance_criteria>
  <done>HEADER duel reads selected contestants, handles uncontested auto-win and neither-selected loose ball, and records contestedPieceIds for HEAD-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: applyStartMovement excludes contestedPieceIds then clears them — satisfy the plan-01 HEAD-05 scaffold (D-21)</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.phase8.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 141-172 (applyStartMovement — read how movable/eligible pieces are currently expressed so the exclusion uses the REAL field, not an invented one; the return state shape)
    - packages/server/src/__tests__/gameEngine.phase8.test.ts (the HEAD-05 scaffold test added in plan 01, marked `// RED until 08.2-02` — note: HEAD-05 is satisfied HERE in plan 03; update that comment accordingly)
    - packages/shared/src/types.ts (contestedPieceIds field — readonly string[])
  </read_first>
  <behavior>
    - Given a GameState whose contestedPieceIds contains one known piece id, applyStartMovement excludes that id from the field the engine uses to express movable pieces for the resulting Movement Phase (e.g. movablePieceIds, or movement is rejected for that piece — match the real engine shape).
    - After applyStartMovement returns, state.contestedPieceIds is cleared to an empty array so exclusion applies to exactly one Movement Phase.
  </behavior>
  <action>
    In applyStartMovement (packages/server/src/gameEngine.ts ~141-172): build `const contestedIds = new Set(state.contestedPieceIds ?? [])`. Wherever the function determines which pieces may move this Movement Phase, exclude any piece whose id is in contestedIds (if the engine does not currently materialize a movable list, exclude at the point applyMove eligibility is later checked — read the file to pick the correct mechanism; if movablePieceIds exists, filter it). In the returned state set `contestedPieceIds: []` so the exclusion is consumed by this single movement (D-21 / Pitfall 6 — clear here, never in the HEADER branch). Do not alter unrelated applyStartMovement behavior. In gameEngine.phase8.test.ts, the plan-01 HEAD-05 scaffold should now pass: update its `// RED until 08.2-02` comment to `// GREEN as of 08.2-03` and ensure it asserts both the exclusion and the clearing. If the real movable mechanism differs from the scaffold's assumed field, adjust the assertion to the real field name discovered in applyStartMovement.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test run gameEngine.phase8 -t HEAD-05 2>&1 | tr -d '\r' | grep -Ei 'HEAD-05|fail|pass|✓|×' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts applyStartMovement references `state.contestedPieceIds` and clears it to `[]` in the returned state
    - The HEAD-05 scaffold test from plan 01 now PASSES (green) and its comment no longer says RED
    - The test asserts the contested piece is excluded AND that contestedPieceIds is cleared
    - `pnpm --filter @counter-attack/server test run gameEngine.phase8 -t HEAD-05` exits 0
  </acceptance_criteria>
  <done>Contested pieces are excluded from the immediately following Movement Phase and the tracking list is cleared after one use; the HEAD-05 scaffold is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                                                                                                                                                   |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| handler→engine | headerContestants and headerConfirmed arrive on GameState from the GAME_HEADER_CONTESTANT handler (plan 04), which validates piece ownership. The engine treats stored contestants as already team-validated. |

## STRIDE Threat Register

| Threat ID | Category    | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                                                            |
| --------- | ----------- | ------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08.2-05 | Tampering   | HEADER branch reads state.headerContestants | mitigate    | Ownership of each contestant pieceId to its team is validated server-side in the GAME_HEADER_CONTESTANT handler (plan 08.2-04) before being stored; the engine resolves ids to pieces and ignores ids that no longer exist |
| T-08.2-06 | Repudiation | contestedPieceIds drives movement exclusion | accept      | Exclusion is deterministic from stored state and cleared after one Movement Phase; no audit/repudiation surface for a 2-player POC                                                                                         |
| T-08.2-SC | Tampering   | npm/pip/cargo installs                      | accept      | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages)                                                                                                                                   |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test run gameEngine.phase8` — HEADER duel tests and the HEAD-05 scaffold all green
- `pnpm -r test run` — full suite green (the plan-01 RED scaffold is now satisfied)
- Manual code check: nearest-defender auto-select removed from HEADER branch
</verification>

<success_criteria>

- D-17/D-19 implemented: duel uses headerContestants; uncontested auto-wins; neither selected → LOOSE_BALL
- D-21/HEAD-05 implemented: contestedPieceIds excludes pieces from the next Movement Phase then clears
- HEAD-02 implemented: uncontested attacker auto-win with no dice
- The plan-01 HEAD-05 scaffold turns green
  </success_criteria>

<output>
Create `.planning/phases/08.2-passing-cleanup-inserted/08.2-03-SUMMARY.md` when done
</output>
