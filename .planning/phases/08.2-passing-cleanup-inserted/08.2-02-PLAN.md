---
phase: 08.2-passing-cleanup-inserted
plan: 02
type: execute
wave: 2
depends_on: ['08.2-01']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.test.ts
  - packages/server/src/__tests__/gameEngine.phase8.test.ts
autonomous: true
requirements: [PASS-01, PASS-02, PASS-03, PASS-05]
must_haves:
  truths:
    - 'Standard and First-time passes skip the accuracy check entirely and always deliver the ball to passTargetHex (D-01, D-02)'
    - 'High Pass and Long Ball apply an accuracy check using carrier.highPass; inaccurate passes transition to LOOSE_BALL (D-03, D-04, D-05, PASS-05)'
    - 'An accurate pass moves ball.position to passTargetHex and sets ball.carrierId to the teammate at the target (or null if empty) (D-14)'
    - 'Adjacent defenders auto-roll for interception before delivery; the first success transfers possession with lastActionType SUCCESSFUL_TACKLE (D-11, D-12, PASS-01)'
    - 'An inaccurate pass resolves a LOOSE_BALL by walking the trajectory hex-by-hex and stopping at the first occupied hex (D-23, D-24)'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applyRoll PASS per-type accuracy + ball delivery + interception loop; LOOSE_BALL trajectory walk'
      contains: 'passTargetHex'
    - path: 'packages/server/src/__tests__/gameEngine.test.ts'
      provides: 'Per-type accuracy, ball delivery, and interception unit tests'
      contains: 'STANDARD_PASS'
  key_links:
    - from: 'packages/server/src/gameEngine.ts applyRoll PASS branch'
      to: 'state.passTargetHex'
      via: 'ball delivery destination'
      pattern: "state\\.passTargetHex"
    - from: 'packages/server/src/gameEngine.ts applyRoll LOOSE_BALL branch'
      to: 'hexLine'
      via: 'trajectory walk'
      pattern: "hexLine\\("
---

<objective>
Rewrite the `applyRoll` PASS branch so accuracy is applied per pass type (not always-HIGH), the ball is actually delivered to the chosen target hex, and adjacent defenders auto-roll for interception. Add the LOOSE_BALL trajectory walk so an inaccurate pass stops at the first occupied hex along its path.

Purpose: This is the engine core of Phase 8.2 passing. It fixes two existing bugs (always-HIGH accuracy; ball never moves on accurate pass) and implements PASS-01 (interception), PASS-02 (first-time delivery), PASS-03 (high pass accuracy), and PASS-05 (loose ball on inaccuracy).
Output: A rewritten PASS branch (per-type accuracy + ball delivery + interception loop), an extended LOOSE_BALL branch (trajectory walk), and engine unit tests proving each behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-PATTERNS.md
@packages/shared/src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite applyRoll PASS branch — per-type accuracy, ball delivery, interception loop (D-05, D-11, D-14)</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 702-773 (applyRoll signature at 702; the current PASS branch at 719-773 — the always-HIGH `validatePassAccuracy(carrier, 'HIGH', d1, [])` call and the accurate branch that only sets phase/lastActionType without moving the ball)
    - packages/server/src/gameEngine.ts lines 210-300 (applyMove STEAL_ATTEMPT block ~275-295 — the interception loop mirrors this: `die === 6 || tackling + die >= 10`)
    - packages/shared/src/passValidator.ts (validatePass signature and the PassType union it accepts: 'STANDARD' | 'FIRST_TIME' | 'HIGH' | 'LONG'; validatePassAccuracy signature; confirm validatePass returns an `interceptors` array on ok:true)
    - packages/shared/src/types.ts (the four new fields from plan 01: passTargetHex, headerContestants, headerConfirmed, contestedPieceIds; GamePhase, LastActionType unions; confirm HexCoord shape q,r)
    - packages/server/src/__tests__/gameEngine.test.ts (existing applyRoll test setup helpers and assertion style — copy the GameState builder and dice-injection call convention used for current PASS tests)
  </read_first>
  <behavior>
    - STANDARD_PASS with no adjacent defenders: ball.position equals passTargetHex; ball.carrierId equals the teammate id at target (or null if empty); phase becomes 'PASS'; lastActionType 'STANDARD_PASS'; passTargetHex cleared to null. NO validatePassAccuracy call.
    - FIRST_TIME_PASS: same delivery as STANDARD; lastActionType 'FIRST_TIME_PASS'; passTimeCost 0.
    - HIGH_PASS with highPass=5, die=4 (combined 9 >= 8): accurate → ball delivered to target; phase becomes 'HEADER'; headerContestants and headerConfirmed initialized to the null/false objects.
    - HIGH_PASS with highPass=2, die=3 (combined 5 < 8): inaccurate → phase 'LOOSE_BALL'; ball.carrierId null; ball stays at carrier position.
    - LONG_BALL with highPass=6, die=4 (combined 10 >= 9): accurate → ball delivered; phase 'PASS'; lastActionType 'LONG_BALL'.
    - Interception: a defending piece whose interception die is 6 (or tackling+die>=10) intercepts → ball.carrierId is the interceptor id, ball.position is interceptor.position, attackingTeam flips to interceptor.teamId, lastActionType 'SUCCESSFUL_TACKLE', phase 'PASS'. First successful interceptor in list order wins; later interceptors do not roll.
  </behavior>
  <action>
    In applyRoll's `case 'PASS'` (packages/server/src/gameEngine.ts ~719-773): keep the carrier lookup and the passTimeCost derivation (FIRST_TIME_PASS => 0, else 1). Replace the unconditional `validatePassAccuracy(carrier, 'HIGH', d1, [])` with a per-type gate: compute `requiresAccuracyCheck = state.lastActionType === 'HIGH_PASS' || state.lastActionType === 'LONG_BALL'`; only when true, call validatePassAccuracy with accuracyType 'HIGH' for HIGH_PASS and 'LONG_SAME_THIRD' for LONG_BALL (both now read highPass per plan 01's D-04 fix), and set `accurate` from its result; otherwise `accurate = true` (D-01/D-02 skip the check). On the accurate path: read `targetHex = state.passTargetHex` and return `{ ok: false, reason: 'WRONG_PHASE' }` if it is null/undefined (the handler is responsible for setting it — D-10). Before committing delivery, run the interception loop: call `validatePass(state, carrier, carrier.position, targetHex, validatePassType)` where validatePassType maps STANDARD_PASS->'STANDARD', FIRST_TIME_PASS->'FIRST_TIME', HIGH_PASS->'HIGH', LONG_BALL->'LONG'; take `interceptors` from the ok result; iterate in order using pre-generated dice read from `state.preGeneratedInterceptionDice?.[i] ?? 3`; an interceptor succeeds when `die === 6 || computeCombinedScore(interceptor.tackling, die, []) >= 10`; on first success return state with phase 'PASS', ball at interceptor position/id, attackingTeam and activeTeam set to interceptor.teamId, lastActionType 'SUCCESSFUL_TACKLE', actionCount incremented by passTimeCost, passTargetHex null (D-11/D-12). If no interception: find teammate at targetHex (same teamId, matching q/r), set ball.position = targetHex and ball.carrierId = teammate?.id ?? null; newPhase is 'HEADER' when lastActionType is 'HIGH_PASS' else 'PASS'; when newPhase is 'HEADER' initialize headerContestants to {home:null,away:null} and headerConfirmed to {home:false,away:false}; set lastActionType to the committed pass type, actionCount + passTimeCost, passTargetHex null, lastDiceRoll {rolls:[d1], context:'PASS_ACCURACY'} (D-14). On the inaccurate path: phase 'LOOSE_BALL', ball.carrierId null, ball.position unchanged (stays at carrier position so LOOSE_BALL branch computes from there), actionCount + passTimeCost, passTargetHex null, lastDiceRoll {rolls:[d1], context:'PASS_ACCURACY'} (D-05/PASS-05). Add `computeCombinedScore` and `validatePass` to the existing `@counter-attack/shared` import if not already present. In gameEngine.test.ts add tests for every case in the behavior block above; cite the requirement IDs (PASS-01, PASS-02, PASS-03) in the test descriptions. Construct GameState with passTargetHex pre-set and preGeneratedInterceptionDice as needed. Do NOT add an accuracy check for STANDARD or FIRST_TIME.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test run gameEngine 2>&1 | tr -d '\r' | grep -Ei 'PASS-0|STANDARD_PASS|interception|pass|fail|✓|×' | head -40</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts applyRoll PASS branch contains `requiresAccuracyCheck` and does NOT contain a bare `validatePassAccuracy(carrier, 'HIGH'` call that runs for every pass type
    - The accurate branch assigns `ball: { position: targetHex` (ball is delivered to the target) and clears passTargetHex to null
    - The interception loop checks `die === 6` OR `computeCombinedScore(...) >= 10` and sets lastActionType 'SUCCESSFUL_TACKLE' on success
    - gameEngine.test.ts contains tests whose descriptions reference PASS-01, PASS-02, and PASS-03
    - `pnpm --filter @counter-attack/server test run gameEngine` exits 0
  </acceptance_criteria>
  <done>PASS branch applies accuracy per type, delivers the ball to the target, and auto-rolls interception; engine tests prove STANDARD/FIRST_TIME bypass the check, HIGH/LONG apply it, and interception transfers possession.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LOOSE_BALL trajectory walk (D-23, D-24) and satisfy the PASS-05 scenario test</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.phase8.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 1074-1095 (current LOOSE_BALL branch — calls computeLooseBall and places the ball at the landing hex directly)
    - packages/shared/src/hex.ts (hexLine signature — confirm it returns an ordered array including both endpoints; computeLooseBall signature and its die argument types)
    - packages/server/src/__tests__/gameEngine.phase8.test.ts (existing Phase 8 LOOSE_BALL tests and the buildInitialGameState helper; the HEAD-05 scaffold added in plan 01 must remain present and is NOT addressed here — it is satisfied in plan 08.2-03)
  </read_first>
  <behavior>
    - Given an inaccurate pass result in LOOSE_BALL phase, computeLooseBall yields a landing hex; walking from ball.position toward it, if a piece occupies an intermediate hex, the ball stops there: ball.position = that hex, ball.carrierId = that piece id, lastActionType 'DEFLECTION'.
    - If no piece occupies any trajectory hex, ball lands at the computed landing hex with ball.carrierId null and lastActionType 'DEFLECTION' (D-24).
    - Phase after resolution is 'MOVEMENT' with movementSlot 'ATTACKER_4', attackingTeam unchanged; movedPieceIds reset to [].
  </behavior>
  <action>
    In applyRoll's `case 'LOOSE_BALL'` (packages/server/src/gameEngine.ts ~1074): keep the `computeLooseBall(state.ball.position, d1, d2)` landing computation. After it, compute `trajectory = hexLine(state.ball.position, landing).slice(1)` (drop the start hex; keep through the landing). Walk the trajectory in order; for each hex find the first piece whose position q/r matches; on first match set finalPosition to that hex and finalCarrierId to that piece id and stop. If no match, finalPosition stays the landing hex and finalCarrierId stays null (D-23/D-24). Return state with phase 'MOVEMENT', movementSlot 'ATTACKER_4', movedPieceIds [], paceUsedByPieceId {}, ball {position: finalPosition, carrierId: finalCarrierId}, attackingTeam unchanged, lastActionType 'DEFLECTION', lastDiceRoll {rolls:[d1,d2], context:'LOOSE_BALL'}. Add `hexLine` to the existing `@counter-attack/shared` import in gameEngine.ts if not already imported. In gameEngine.phase8.test.ts add two PASS-05 tests: one where a piece sits on the trajectory (ball stops on it, carrierId is that piece) and one where the path is clear (ball lands unclaimed, carrierId null). Cite PASS-05 and D-23 in the test descriptions. Do NOT alter the HEAD-05 scaffold test added in plan 01.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test run gameEngine.phase8 2>&1 | tr -d '\r' | grep -Ei 'PASS-05|LOOSE|DEFLECTION|trajectory|fail|✓|×' | head -30</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts LOOSE_BALL branch calls `hexLine(state.ball.position, landing)` and walks the result
    - The branch sets `lastActionType: 'DEFLECTION'` and stops at the first occupied trajectory hex
    - gameEngine.phase8.test.ts contains a test referencing PASS-05 where the ball stops on a piece and one where it lands unclaimed (carrierId null)
    - `pnpm --filter @counter-attack/server test run gameEngine.phase8` exits 0 (the HEAD-05 scaffold remains RED and is excluded from this command's pass requirement, OR is filtered — confirm only the new PASS-05 tests are asserted green)
  </acceptance_criteria>
  <done>LOOSE_BALL resolves by walking the trajectory and stopping at the first occupied hex; PASS-05 tests prove both the intercepted and unclaimed landing outcomes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                                                                                                                                                  |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| handler→engine | passTargetHex and preGeneratedInterceptionDice arrive on GameState from the GAME_ROLL handler (plan 04); the engine treats them as already-validated. The engine itself crosses no client boundary directly. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                      | Disposition | Mitigation Plan                                                                                                                                                                   |
| --------- | ---------------------- | -------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08.2-03 | Tampering              | applyRoll consumes state.passTargetHex                         | mitigate    | Engine returns `{ok:false, reason:'WRONG_PHASE'}` when passTargetHex is null; the authoritative validatePass re-check happens server-side in plan 08.2-04 before this branch runs |
| T-08.2-04 | Information Disclosure | interception dice read from state.preGeneratedInterceptionDice | accept      | Dice are pre-generated server-side via crypto.randomInt in the handler; engine never generates dice (purity preserved); no client-supplied randomness enters the loop             |
| T-08.2-SC | Tampering              | npm/pip/cargo installs                                         | accept      | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages)                                                                                          |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test run` — engine suite green for the new PASS-01/02/03/05 tests
- `pnpm -r test run` — full suite green except the intentional HEAD-05 RED scaffold (satisfied in plan 08.2-03)
- Manual code check: no `validatePassAccuracy` call executes for STANDARD_PASS or FIRST_TIME_PASS
</verification>

<success_criteria>

- D-05 implemented: PASS branch branches on lastActionType for accuracy
- D-14 implemented: accurate pass moves ball.position to passTargetHex
- D-11/D-12 implemented: interception auto-roll loop transfers possession on first success
- D-23/D-24 implemented: LOOSE_BALL walks the trajectory and stops at first occupied hex
- PASS-01, PASS-02, PASS-03, PASS-05 covered by engine tests
  </success_criteria>

<output>
Create `.planning/phases/08.2-passing-cleanup-inserted/08.2-02-SUMMARY.md` when done
</output>
