---
phase: 08.2-passing-cleanup-inserted
plan: 06
type: execute
wave: 6
depends_on: ['08.2-05']
files_modified: []
autonomous: false
requirements: [PASS-01, PASS-02, PASS-03, PASS-04, PASS-05]
must_haves:
  truths:
    - 'A human confirms the three-step PASS flow (pass type → target hex → Roll) works end-to-end in the browser (D-06)'
    - 'A human confirms interception-risk pass targets render amber and safe targets render green (D-09)'
    - 'A human confirms the header contestant green ring toggles on click and the Roll Header gate waits for both teams (D-17)'
  artifacts: []
  key_links: []
---

<objective>
Human verification of the Phase 8.2 passing UI behaviors that cannot be asserted by automated tests: the three-step PASS flow, the green/amber pass-target highlighting, and the header contestant selection ring and two-team confirm gate.

Purpose: Plans 02-05 are covered by unit tests for engine, handler, and shared logic, but the visual/interactive UI surface (D-06, D-09, D-17) requires a human to confirm it behaves correctly in a live two-browser session.
Output: A human sign-off (or a list of issues feeding a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the dev servers and confirm a clean build for the verification session</name>
  <files>none (runs build/test/dev scripts only)</files>
  <read_first>
    - package.json (root) and packages/server/package.json, packages/client/package.json (the dev script names — confirm how server and client are started, e.g. a root `pnpm dev` or per-package scripts)
    - .planning/phases/08.2-passing-cleanup-inserted/08.2-VALIDATION.md (the Manual-Only Verifications table — the three behaviors to test)
  </read_first>
  <action>
    Confirm the full suite is green and both packages build: run the shared+server test suite and the client build. Then start the server and client dev processes (use the scripts discovered in the package.json files; run them in the background so the verification can proceed). Report the local client URL (e.g. http://localhost:5173) and confirm the server is listening. Do NOT make code changes; if the build or tests fail, stop and report the failure so it can be addressed before human verification.
  </action>
  <verify>
    <automated>pnpm -r test run 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <done>Both packages build, the full test suite is green, and the dev environment is running and reachable for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the passing UI (three-step flow, green/amber highlights, header contestant ring)</name>
  <action>Human verification only — no code changes. Drive the live two-browser session described below and report PASS or issues for each numbered item.</action>
  <what-built>
    The Phase 8.2 passing UI: a three-step PASS flow (pass type → click target hex → Roll), green/amber pass-target highlighting, and the HEADER contestant selection (green ring toggle + Confirm + two-team Roll gate). The engine, handler, and shared logic are covered by automated tests; this checkpoint verifies the live interactive behavior.
  </what-built>
  <how-to-verify>
    Open the reported client URL in two browser windows (or one window + one private window) and join the same room code so two players are connected.
    1. PASS flow (D-06): get a team into the PASS phase with a ball carrier. In the ActionPanel choose a pass type (e.g. Standard). Confirm the board highlights valid target hexes and the Roll button is NOT yet enabled. Click a highlighted target hex; confirm it shows a gold outline and the Roll button becomes enabled. Click Roll; confirm the ball moves to the target hex.
    2. Interception-risk colors (D-09): choose a pass type where at least one valid target's travel path passes adjacent to an opponent. Confirm those targets render amber/orange while safe targets render green. (If no such position is reachable in the current game, note it and skip.)
    3. Deselect: with a target confirmed (gold outline), click that same hex again; confirm it deselects and the Roll button disables.
    4. Header contestant (D-17): trigger a High Pass that reaches a teammate so the phase becomes HEADER. In each browser, click an eligible own piece (within ~2 hexes of the ball); confirm a green ring appears, and clicking it again removes the ring. Click Confirm in each browser. Confirm the Roll Header button only becomes available after BOTH teams have confirmed.
    Report PASS for each numbered item, or describe what went wrong.
  </how-to-verify>
  <verify><human-check>All four numbered items confirmed in a live two-browser session</human-check></verify>
  <resume-signal>Type "approved" if all items pass, or describe the issues found (which will feed a gap-closure plan via /gsd-plan-phase 08.2 --gaps).</resume-signal>
  <done>Human approval recorded for the three-step PASS flow, green/amber highlighting, and header contestant ring + two-team Roll gate; or issues captured for gap closure.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                                                               |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------- |
| human→running app | A human drives the live two-browser session; no new code or client input boundary is introduced by this verification plan |

## STRIDE Threat Register

| Threat ID | Category                | Component                              | Disposition | Mitigation Plan                                                                                                                  |
| --------- | ----------------------- | -------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-08.2-13 | n/a (verification only) | dev servers started for manual testing | accept      | Dev servers run locally for the verification session only; no production exposure, no new attack surface introduced by this plan |
| T-08.2-SC | Tampering               | npm/pip/cargo installs                 | accept      | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages)                                         |

</threat_model>

<verification>
- `pnpm -r test run` — full suite green before the human session
- Human sign-off on the three manual-only behaviors (D-06, D-09, D-17)
</verification>

<success_criteria>

- The three-step PASS flow works end-to-end in the browser (D-06)
- Interception-risk targets render amber, safe targets green (D-09)
- Header contestant ring toggles and Roll Header waits for both teams (D-17)
- Human approval recorded, or issues captured for gap closure
  </success_criteria>

<output>
Create `.planning/phases/08.2-passing-cleanup-inserted/08.2-06-SUMMARY.md` when done
</output>
