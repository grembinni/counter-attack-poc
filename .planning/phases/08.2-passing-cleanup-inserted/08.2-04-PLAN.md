---
phase: 08.2-passing-cleanup-inserted
plan: 04
type: execute
wave: 4
depends_on: ['08.2-01', '08.2-02', '08.2-03']
files_modified:
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/gameHandlers.test.ts
autonomous: true
requirements: [PASS-01, PASS-02, PASS-03, PASS-04, PASS-05]
must_haves:
  truths:
    - 'GAME_ROLL accepts an optional targetHex, validates its shape (q,r numbers), and rejects malformed targets with GAME_ERROR INVALID_TARGET (D-10, ASVS V5)'
    - 'During PASS phase the server re-runs validatePass authoritatively before applyRoll and commits targetHex to state.passTargetHex (D-10)'
    - 'Interception dice are pre-generated server-side and stored on state.preGeneratedInterceptionDice before applyRoll (D-11, Pitfall 4)'
    - "GAME_HEADER_CONTESTANT validates the pieceId belongs to the socket's team and records the per-team selection and confirmation (D-17, ASVS V4)"
    - 'GAME_ROLL in HEADER phase is rejected unless both teams have confirmed their contestant (Pitfall 5)'
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'GAME_ROLL targetHex validation + passTargetHex commit + interception dice; GAME_HEADER_CONTESTANT handler; HEADER confirm guard'
      contains: 'GAME_HEADER_CONTESTANT'
  key_links:
    - from: 'packages/server/src/gameHandlers.ts GAME_ROLL'
      to: 'validatePass'
      via: 'authoritative pre-roll target validation'
      pattern: "validatePass\\("
    - from: 'packages/server/src/gameHandlers.ts GAME_HEADER_CONTESTANT'
      to: 'room.gameState.headerContestants'
      via: 'per-team contestant store'
      pattern: 'headerContestants'
---

<objective>
Wire the server transport layer for Phase 8.2 passing: extend GAME_ROLL to accept and authoritatively validate a target hex, pre-generate interception dice, commit passTargetHex to state, add the GAME_HEADER_CONTESTANT handler with ownership validation, and guard GAME_ROLL in HEADER phase until both teams confirm.

Purpose: The engine branches from plans 02 and 03 consume state.passTargetHex, state.preGeneratedInterceptionDice, and state.headerContestants. This plan is the only place those fields are set from client input, and it is the authoritative server-side validation boundary (D-10, ASVS V4/V5).
Output: An extended GAME_ROLL handler, a new GAME_HEADER_CONTESTANT handler, a HEADER confirmation guard, and handler unit tests covering valid/invalid targets, interception dice, contestant ownership, and the confirm gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-CONTEXT.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-RESEARCH.md
@.planning/phases/08.2-passing-cleanup-inserted/08.2-PATTERNS.md
@packages/shared/src/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GAME_ROLL — targetHex validation, validatePass gate, passTargetHex commit, interception dice (D-10, D-11)</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts lines 376-489 (the current GAME_ROLL handler — signature at ~388, isActivePlayer gate at ~404, the ELIGIBLE_NEXT_ACTIONS/passType eligibility check, dice pre-generation at ~468-471, and the broadcastState call at ~484)
    - packages/server/src/gameHandlers.ts lines 506-554 (GAME_SHOT handler — the targetHex shape-validation pattern T-07-12 style at ~524-530 and ServerEvents.GAME_ERROR emit convention to mirror)
    - packages/server/src/gameHandlers.ts lines 90-92 (isActivePlayer helper) and ~72 (playerSlot→team mapping)
    - packages/shared/src/passValidator.ts (validatePass signature and PassType union; confirms the ok result carries interceptors)
    - packages/shared/src/events.ts (the GAME_ROLL signature now carrying targetHex from plan 01; ClientEvents/ServerEvents)
    - packages/server/src/diceUtils.ts (rollDice — crypto.randomInt backed; used for interception dice)
  </read_first>
  <action>
    In gameHandlers.ts GAME_ROLL handler (~388): change the listener signature to accept `(passType?, targetHex?: HexCoord)` matching the plan-01 events.ts type. Keep the existing isActivePlayer and passType-eligibility checks. After eligibility passes and BEFORE the d1/d2/d3 generation: when `room.gameState.phase === 'PASS'`, (1) if `!targetHex` emit `ServerEvents.GAME_ERROR, 'MISSING_TARGET'`, broadcastState, return; (2) validate shape — if `typeof targetHex.q !== 'number' || typeof targetHex.r !== 'number'` emit `GAME_ERROR, 'INVALID_TARGET'`, broadcastState, return (ASVS V5, mirror GAME_SHOT); (3) look up the carrier by ball.carrierId — if missing emit `GAME_ERROR, 'WRONG_PHASE'`, broadcastState, return; (4) map passType→validatePass type (STANDARD_PASS->'STANDARD', FIRST_TIME_PASS->'FIRST_TIME', HIGH_PASS->'HIGH', LONG_BALL->'LONG'); (5) call `validatePass(room.gameState, carrier, carrier.position, targetHex, vpType)`; if `!result.ok` emit `GAME_ERROR, 'INVALID_TARGET'`, broadcastState, return (D-10 authoritative gate); (6) commit `room.gameState = { ...room.gameState, passTargetHex: targetHex }`. Then pre-generate interception dice: from the same validatePass ok result take `interceptors`; `const interceptionDice = interceptors.map(() => rollDice())`; if non-empty, set `room.gameState = { ...room.gameState, preGeneratedInterceptionDice: interceptionDice }` (Pitfall 4 — dice generated in handler, never in applyRoll). Keep the existing d1/d2/d3 generation and the applyRoll call and final broadcastState. Add a HEADER-phase guard block (see Task 3) — reference only; implement it there. Import HexCoord, validatePass, and PassType mapping as needed from `@counter-attack/shared`. Do not change non-PASS phases' roll behavior.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server build 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GAME_ROLL listener signature includes `targetHex?: HexCoord`
    - The PASS block emits `'INVALID_TARGET'` for non-number q/r and for a validatePass failure
    - `room.gameState.passTargetHex` is set from the validated targetHex before applyRoll
    - `preGeneratedInterceptionDice` is set from `interceptors.map(() => rollDice())` when interceptors exist
    - `pnpm --filter @counter-attack/server build` exits 0
  </acceptance_criteria>
  <done>GAME_ROLL authoritatively validates the target hex, commits passTargetHex, and pre-generates interception dice before applyRoll.</done>
</task>

<task type="auto">
  <name>Task 2: Add GAME_HEADER_CONTESTANT handler with team-ownership validation (D-17, ASVS V4)</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts lines 810-870 (existing GAME_HEADER handler — copy its phase guard, isProcessing mutex + finally release, and broadcastState pattern)
    - packages/server/src/gameHandlers.ts lines 90-92 and ~72 (isActivePlayer and playerSlot→'home'|'away' mapping — both teams act in HEADER, so use the team mapping, not isActivePlayer, to determine which slot is selecting)
    - packages/shared/src/events.ts (GAME_HEADER_CONTESTANT constant and the `(pieceId: string | null) => void` signature added in plan 01)
    - packages/shared/src/types.ts (headerContestants and headerConfirmed shapes)
  </read_first>
  <action>
    In gameHandlers.ts add `socket.on(ClientEvents.GAME_HEADER_CONTESTANT, (pieceId: string | null) => { ... })`. Body: read roomCode from socket.data, return if undefined; getRoom, return if missing or `room.isProcessing`. Acquire the mutex `room.isProcessing = true` and wrap the body in try/finally that releases it (Pattern B). Inside: if `room.gameState === null || room.gameState.phase !== 'HEADER'` emit `ServerEvents.GAME_ERROR, 'WRONG_PHASE'` and return. Determine `teamSlot = socket.data.playerSlot === 1 ? 'home' : 'away'`. If `pieceId !== null`, look up the piece; if not found or `piece.teamId !== teamSlot` emit `GAME_ERROR, 'INVALID_CONTESTANT'` and return (ASVS V4 ownership check). Store the selection: set `room.gameState.headerContestants` to a copy with `[teamSlot]: pieceId` (initializing from `{home:null,away:null}` if absent) and `room.gameState.headerConfirmed` to a copy with `[teamSlot]: true` (initializing from `{home:false,away:false}`). Call `broadcastState(io, room)` so both clients see the opponent's confirmed flag (Pattern C). Place the handler alongside the other socket.on registrations.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server build 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - gameHandlers.ts registers `socket.on(ClientEvents.GAME_HEADER_CONTESTANT`
    - The handler emits `'INVALID_CONTESTANT'` when the pieceId's teamId does not match the socket's team
    - The handler sets both `headerContestants[teamSlot]` and `headerConfirmed[teamSlot]=true` and calls broadcastState
    - The handler uses the isProcessing mutex with a finally release
    - `pnpm --filter @counter-attack/server build` exits 0
  </acceptance_criteria>
  <done>Each team can select or decline a header contestant; the server validates ownership, records the per-team selection and confirmation, and broadcasts to both clients.</done>
</task>

<task type="auto">
  <name>Task 3: HEADER confirm guard in GAME_ROLL + handler tests (Pitfall 5, PASS-01..05)</name>
  <files>packages/server/src/gameHandlers.ts, packages/server/src/__tests__/gameHandlers.test.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (the GAME_ROLL HEADER-phase sequence-check area ~459 where the confirm guard belongs; ServerEvents.GAME_ERROR convention)
    - packages/server/src/__tests__/gameHandlers.test.ts (existing handler test harness — how a Room, socket.data, and emit spies are constructed; copy the setup for the new tests)
    - packages/shared/src/types.ts (headerConfirmed shape)
  </read_first>
  <action>
    In gameHandlers.ts GAME_ROLL, add a HEADER-phase guard near the existing HEADER sequence check (~459): when `room.gameState.phase === 'HEADER'`, if `!room.gameState.headerConfirmed?.home || !room.gameState.headerConfirmed?.away` emit `ServerEvents.GAME_ERROR, 'HEADER_NOT_CONFIRMED'`, broadcastState, return (Pitfall 5). In gameHandlers.test.ts add tests: (a) GAME_ROLL in PASS phase with a malformed targetHex (q non-number) emits 'INVALID_TARGET' and does not advance state; (b) GAME_ROLL in PASS phase with a valid targetHex commits passTargetHex and reaches applyRoll (assert ball delivered or interception per a controlled setup); (c) GAME_HEADER_CONTESTANT with an opponent's pieceId emits 'INVALID_CONTESTANT'; (d) GAME_HEADER_CONTESTANT with own pieceId sets headerContestants and headerConfirmed for that slot; (e) GAME_ROLL in HEADER phase before both confirmed emits 'HEADER_NOT_CONFIRMED'. Cite PASS-01 and D-10/D-17 in the test descriptions. Use the existing handler test harness; pre-generate any dice the engine path needs via the harness's injection if applicable.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test run gameHandlers 2>&1 | tr -d '\r' | grep -Ei 'INVALID_TARGET|INVALID_CONTESTANT|HEADER_NOT_CONFIRMED|PASS-01|fail|✓|×' | head -30</automated>
  </verify>
  <acceptance_criteria>
    - GAME_ROLL HEADER block emits `'HEADER_NOT_CONFIRMED'` when either team's headerConfirmed is false
    - gameHandlers.test.ts contains tests for INVALID_TARGET, valid-target commit, INVALID_CONTESTANT, contestant store, and HEADER_NOT_CONFIRMED
    - A test description references PASS-01
    - `pnpm --filter @counter-attack/server test run gameHandlers` exits 0
  </acceptance_criteria>
  <done>HEADER rolls are blocked until both teams confirm; handler tests prove the target-validation, contestant-ownership, and confirm-gate behaviors.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                                                                                    |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client→server (Socket.io) | targetHex and header contestant pieceId originate on the untrusted client and cross into authoritative game state through GAME_ROLL and GAME_HEADER_CONTESTANT |

## STRIDE Threat Register

| Threat ID | Category               | Component                                          | Disposition | Mitigation Plan                                                                                                                                                                              |
| --------- | ---------------------- | -------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08.2-07 | Tampering              | GAME_ROLL targetHex (forged/out-of-range)          | mitigate    | Server validates q/r are numbers, then re-runs validatePass authoritatively before committing passTargetHex; invalid → GAME_ERROR INVALID_TARGET + state snap-back via broadcastState (D-10) |
| T-08.2-08 | Tampering              | GAME_HEADER_CONTESTANT pieceId (opponent's piece)  | mitigate    | Server looks up the piece and rejects when piece.teamId !== socket's team slot → GAME_ERROR INVALID_CONTESTANT (ASVS V4)                                                                     |
| T-08.2-09 | Spoofing               | GAME_ROLL fired in HEADER before opponent confirms | mitigate    | headerConfirmed.home && headerConfirmed.away guard rejects with HEADER_NOT_CONFIRMED (Pitfall 5)                                                                                             |
| T-08.2-10 | Elevation of Privilege | non-active player emits GAME_ROLL                  | mitigate    | existing isActivePlayer gate retained on GAME_ROLL; GAME_HEADER_CONTESTANT intentionally allows both teams (HEADER is two-sided) but binds selection to the socket's own team slot           |
| T-08.2-SC | Tampering              | npm/pip/cargo installs                             | accept      | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages)                                                                                                     |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test run gameHandlers` — handler tests green
- `pnpm --filter @counter-attack/server build` — server compiles with the extended handlers
- `pnpm -r test run` — full suite green
</verification>

<success_criteria>

- D-10 implemented: GAME_ROLL validates targetHex shape, re-runs validatePass, commits passTargetHex
- D-11 implemented: interception dice pre-generated server-side before applyRoll
- D-17 implemented: GAME_HEADER_CONTESTANT records per-team selection with ownership validation
- Pitfall 5 implemented: HEADER roll blocked until both teams confirm
- PASS-01..05 wired through the server transport boundary
  </success_criteria>

<output>
Create `.planning/phases/08.2-passing-cleanup-inserted/08.2-04-SUMMARY.md` when done
</output>
