---
phase: 12-visual-token-hex-layer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/PlayerStatsPanel.tsx
  - packages/client/src/components/PlayerStatsPanel.module.css
  - packages/client/src/components/PlayerStatsPanel.test.tsx
autonomous: true
requirements: [VIS-02]
must_haves:
  truths:
    - 'When a piece is selected, the stats panel header shows a mini token circle next to the player name (D-08)'
    - 'The mini token for a home outfield piece carries the vertical-stripe pattern; away carries two horizontal stripes'
    - 'The mini token for a GK is solid-filled with no stripe pattern'
    - 'The header layout keeps the player name and role visible without overflow'
  artifacts:
    - path: 'packages/client/src/components/PlayerStatsPanel.tsx'
      provides: 'MiniTokenBadge inline SVG with self-contained stripe defs in the panel header'
      contains: 'MiniTokenBadge'
    - path: 'packages/client/src/components/PlayerStatsPanel.module.css'
      provides: '.tokenBadge + .headerText rules; .header flex with 6px gap'
      contains: 'tokenBadge'
  key_links:
    - from: 'packages/client/src/components/PlayerStatsPanel.tsx'
      to: 'mini token circle fill'
      via: 'self-contained url(#mini-home-stripe-<id>) / url(#mini-away-stripe-<id>)'
      pattern: "url\\(#mini-(home|away)-stripe"
---

<objective>
Add a self-contained inline SVG mini token badge to the PlayerStatsPanel header so the selected piece's team stripe pattern is shown in the stats context — satisfying VIS-02's requirement that the stripe design is consistent across on-pitch overlays, the player stats panel, and replay.

Purpose: VIS-02 requires the stripe identity to appear in the stats panel. Because PlayerStatsPanel renders in a separate SVG document fragment from HexGrid, it must define its OWN `<defs>` patterns (D-09) — a `url(#...)` reference cannot reach HexGrid's patterns.

Output: Updated `PlayerStatsPanel.tsx` with a `MiniTokenBadge` component, updated `.module.css` header layout, and extended `PlayerStatsPanel.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-visual-token-hex-layer/12-CONTEXT.md
@.planning/phases/12-visual-token-hex-layer/12-UI-SPEC.md
@.planning/phases/12-visual-token-hex-layer/12-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols this phase (Plan 03) creates — exclude from drift verification:

- `MiniTokenBadge` — local component function in `PlayerStatsPanel.tsx` (not exported)
- Self-contained SVG pattern IDs: `mini-home-stripe-<piece.id>` and `mini-away-stripe-<piece.id>`
- CSS classes `.tokenBadge` and `.headerText` in `PlayerStatsPanel.module.css`

This plan does NOT depend on PieceOverlay's `SelectionState` or HexCell's `HexHighlightType` — it is fully self-contained (D-09).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MiniTokenBadge to PlayerStatsPanel header + extend test</name>
  <files>packages/client/src/components/PlayerStatsPanel.tsx, packages/client/src/components/PlayerStatsPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/PlayerStatsPanel.tsx (current header div lines 42-45; STAT_LABELS; render structure)
    - packages/client/src/components/PlayerStatsPanel.test.tsx (current tests + setup — extend in place)
    - packages/client/src/components/PieceOverlay.tsx (fill/stroke color values lines 43-57 to mirror; player-number derivation line 39: `String(Number(piece.id.slice(piece.id.lastIndexOf('-') + 1)) + 1)`)
    - .planning/phases/12-visual-token-hex-layer/12-UI-SPEC.md (§Mini Token Badge Spec — exact 20px svg, r=9, cx/cy=10, fontSize 9, mini stripe band geometry)
    - .planning/phases/12-visual-token-hex-layer/12-PATTERNS.md (PlayerStatsPanel.tsx section — exact MiniTokenBadge implementation reference)
  </read_first>
  <behavior>
    - Test: selecting a home outfield piece renders a mini-token `<circle>` whose fill references url(#mini-home-stripe-<id>) AND a `<pattern>` with that id exists in the panel's own svg
    - Test: selecting an away outfield piece renders fill url(#mini-away-stripe-<id>) with the matching pattern
    - Test: selecting a GK piece renders a solid-fill mini-token circle (#9b59b6 home / #f59e0b away) with NO mini-stripe pattern / no url(#mini-...-stripe) reference
    - Test (existing, must still pass): no piece selected → returns null; selected piece → name + all 10 stat labels render
  </behavior>
  <action>
    Add a local (non-exported) function `MiniTokenBadge({ piece }: { piece: PlayerPiece })` that returns an inline `<svg width={20} height={20} viewBox="0 0 20 20" className={styles.tokenBadge}>`. Inside: compute `isGK = piece.role === 'GK'`, `miniR = 9`, `miniCx = 10`, `miniCy = 10`, `playerNumber` using the exact PieceOverlay derivation. Pattern IDs `mini-home-stripe-${piece.id}` and `mini-away-stripe-${piece.id}`. When `!isGK`, render a `<defs>` containing the team-appropriate `<pattern ... patternUnits="userSpaceOnUse" x={miniCx - miniR} y={miniCy - miniR} width={18} height={18}>`: home → one `<rect x={7} y={0} width={4} height={18} fill="#000000" fillOpacity={0.55} />`; away → two `<rect ... width={18} height={3} fill="#7f0000" fillOpacity={0.65} />` at `y={4}` and `y={11}`. Render the `<circle cx={10} cy={10} r={9}>` with `fill={isGK ? gkFill : url(#<team mini id>)}` where `gkFill` = `#9b59b6` (home) / `#f59e0b` (away); `stroke` = GK `#6c3483`/`#d97706` else outfield `#0d3a82`/`#8e1c12`; `strokeWidth={1.5}`. Render the player-number `<text x={10} y={10} textAnchor="middle" dominantBaseline="central" fontSize={9} fontWeight={700} fill="#ffffff" pointerEvents="none">` (italic for GK to match PieceOverlay convention).

    In the panel render, replace the header div contents: render `<MiniTokenBadge piece={piece} />` first, then wrap the existing `{piece.name}` + role span inside `<div className={styles.headerText}>`.

    Extend `PlayerStatsPanel.test.tsx` in place with the three new mini-token behavior tests above (query `container.querySelector` for the panel's svg, pattern, and circle; assert on `fill`/`getAttribute`). Keep all existing tests intact. Use mock pieces from the existing mock state (e.g. `home-1` outfield, an away outfield id, `home-0` GK) — confirm ids against `mockMovementState`/teams. Do not introduce `any`.

  </action>
  <verify>
    <automated>cd packages/client && pnpm vitest run src/components/PlayerStatsPanel.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `PlayerStatsPanel.tsx` contains a `MiniTokenBadge` function and renders it inside `styles.header` (D-08: mini token in the stats panel header)
    - `PlayerStatsPanel.tsx` contains template strings `mini-home-stripe-` and `mini-away-stripe-` and `patternUnits="userSpaceOnUse"`
    - GK mini token uses solid fill (no `url(#mini-` for GK) — verified by GK test
    - Existing null-state and stat-label tests still pass
    - `pnpm vitest run src/components/PlayerStatsPanel.test.tsx` exits 0
  </acceptance_criteria>
  <done>Stats panel header shows a striped (or solid-GK) mini token for the selected piece; all PlayerStatsPanel tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update PlayerStatsPanel header CSS for the badge</name>
  <files>packages/client/src/components/PlayerStatsPanel.module.css</files>
  <read_first>
    - packages/client/src/components/PlayerStatsPanel.module.css (current `.header` lines 14-21, `.role` lines 23-29)
    - .planning/phases/12-visual-token-hex-layer/12-UI-SPEC.md (§Mini Token Badge Spec — 6px gap, align center; §Spacing Scale)
  </read_first>
  <action>
    Change `.header` from `justify-content: space-between; align-items: baseline;` to `display: flex; align-items: center; gap: 6px;` (keep `font-size: 13px; font-weight: 700; color: #e0e0e0;`). Add `.tokenBadge { flex-shrink: 0; }`. Add `.headerText { display: flex; justify-content: space-between; align-items: baseline; flex: 1; min-width: 0; }`. Leave `.role`, `.panel`, `.statGrid`, `.statRow`, `.statLabel`, `.statValue` unchanged.
  </action>
  <verify>
    <automated>cd packages/client && pnpm vitest run src/components/PlayerStatsPanel.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `PlayerStatsPanel.module.css` `.header` contains `gap: 6px` and `align-items: center`
    - `.header` no longer contains `justify-content: space-between`
    - `.module.css` contains `.tokenBadge` with `flex-shrink: 0` and `.headerText` with `flex: 1` and `min-width: 0`
    - PlayerStatsPanel tests still exit 0
  </acceptance_criteria>
  <done>Header is a 3-item-friendly flex row: [badge][name/role group] with a 6px gap and no overflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A mini team-token badge (~20px SVG) in the PlayerStatsPanel header showing the home/away stripe pattern (or solid GK fill) for the selected piece, with a 6px-gap flex header layout.</what-built>
  <how-to-verify>
    1. Run `pnpm --filter @counter-attack/client dev` and open the client in a browser.
    2. Start/join a game so the board renders.
    3. Click a HOME outfield piece. Confirm the stats panel header shows a small circle with a single vertical black stripe to the left of the player name; the name and role badge sit on one line without wrapping.
    4. Click an AWAY outfield piece. Confirm the mini badge shows two horizontal maroon stripes.
    5. Click a goalkeeper (GK). Confirm the mini badge is solid purple (home) or amber (away) with NO stripe.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (stripe orientation, overflow, missing badge).</resume-signal>
</task>

</tasks>

<verification>
- `cd packages/client && pnpm vitest run src/components/PlayerStatsPanel.test.tsx` exits 0
- Human verification of the mini badge across home/away/GK pieces
</verification>

<success_criteria>

- VIS-02: the stripe identity is now visible in the stats-panel context (third of the three required contexts; on-pitch and replay are covered by the shared PieceOverlay in Plan 01/04)
- PlayerStatsPanel.test.tsx green; header layout intact
  </success_criteria>

<notes_for_executor>
Decision implemented by this plan: D-08 (a mini token rendered in the PlayerStatsPanel header for the selected piece). The badge is rendered with self-contained `<defs>` patterns per D-09.

This plan is fully independent of Plans 01, 02, and 04 — PlayerStatsPanel does not import PieceOverlay, HexCell, or HexGrid, and its SVG patterns are self-contained (D-09). It can run in parallel in Wave 1. Do NOT reference HexGrid's patterns by id from this component. The away mini-stripe bands are 3px tall (not 4px) — proportional to the 18px tile (vs 24px on-pitch).
</notes_for_executor>

<output>
Create `.planning/phases/12-visual-token-hex-layer/12-03-SUMMARY.md` when done
</output>
