---
phase: 12-visual-token-hex-layer
plan: 04
type: execute
wave: 2
depends_on: ['12-visual-token-hex-layer-01', '12-visual-token-hex-layer-02']
files_modified:
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/HexGrid.module.css
autonomous: false
requirements: [VIS-01, UX-05, UX-06]
must_haves:
  truths:
    - "On the pitch, each piece passes a single selectionState ('none'|'selectable'|'active'|'activated') to PieceOverlay"
    - "Header-eligible pieces during HEADER phase show the green 'active' ring (no isHeaderContestant prop)"
    - 'Each hex resolves to exactly one highlightType by priority: risk > goal > shot-path > kickoff > safe'
    - "ZoI/tackle/snap-deflect risk hexes tint orange via highlightType 'risk' (not the old hexZoIRisk class)"
    - "Last-shot-path and HIGH_PASS reposition hexes tint white via highlightType 'shot-path'"
    - 'Pass-target, quick-throw, GK-kick, kickoff-centre, and HEADER ball-position overlays keep their own click handlers and render correctly'
    - 'HIGH_PASS header non-goal target hexes use the white shot-path tint (replacing the old cyan tint) per D-13'
    - "All polygon overlay rendering is consolidated in HexGrid (D-11): tint-only polygons fold into HexCell's highlightType; only action overlays remain as explicit polygons in HexGrid"
  artifacts:
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: 'selectionState derivation per piece; highlightType priority resolution per hex; new PieceOverlay/HexCell call sites'
      contains: 'selectionState'
    - path: 'packages/client/src/components/HexGrid.module.css'
      provides: '.hexZoIRisk removed (folded into HexCell); .hexTackleRisk retained for interception-risk pass overlay'
  key_links:
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'PieceOverlay'
      via: 'selectionState enum prop'
      pattern: "selectionState=\\{"
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'HexCell'
      via: 'highlightType enum prop'
      pattern: "highlightType=\\{"
---

<objective>
Rewire HexGrid to consume the new leaf-component APIs from Plan 01 (PieceOverlay `selectionState`) and Plan 02 (HexCell `highlightType`): compute a single `selectionState` enum per piece, and priority-resolve a single `highlightType` per hex from the existing membership sets — collapsing the tint-only polygon overlays while keeping the action-overlay polygons (pass target, quick throw, GK kick, kickoff centre, HEADER ball position) that carry their own click handlers.

Purpose: This is the integration plan that makes VIS-01 (stripes appear on-pitch and in replay), UX-05 (3 selection rings driven from real game state), and UX-06 (unified hex tints driven from real game state) actually visible in the running game. It also fulfils D-07 (header contestants as 'active'), D-11 (consolidate all polygon overlays in HexGrid), and D-13 (HIGH_PASS targets use white shot-path tint).

Output: Updated `HexGrid.tsx` and `HexGrid.module.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-visual-token-hex-layer/12-CONTEXT.md
@.planning/phases/12-visual-token-hex-layer/12-UI-SPEC.md
@.planning/phases/12-visual-token-hex-layer/12-PATTERNS.md
@.planning/phases/12-visual-token-hex-layer/12-01-SUMMARY.md
@.planning/phases/12-visual-token-hex-layer/12-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan (Plan 04) creates/uses — exclude from drift verification:

- Local `selectionState` constant per piece in `HexGrid.tsx` (typed `SelectionState`, imported from `./PieceOverlay.js`)
- Local `highlightType` constant per hex in `HexGrid.tsx` (typed `HexHighlightType | undefined`, imported from `./HexCell.js`)
- Local priority-resolution booleans: `isRisk`, `isGoalTint`, `isShotPathTint`, `isKickoffTint`, `isSafeTint`, `isSpentNow`

Consumes (created in earlier Plans, already exist): `SelectionState` (Plan 01), `HexHighlightType` (Plan 02), per-piece stripe patterns inside PieceOverlay (Plan 01), `HIGHLIGHT_STYLES` inside HexCell (Plan 02).

CSS REMOVED: `.hexZoIRisk` class in `HexGrid.module.css`. CSS RETAINED: `.hexTackleRisk` (still used by the interception-risk pass overlay — do NOT remove).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Compute selectionState per piece and update the PieceOverlay call site</name>
  <files>packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (full file — especially the piece map lines 535-666: canSelect/canSelectKickOff/canSelectHighPassMove/canSelectSnapDeflect/canSelectGKKickMove, isHeaderEligible line 593, isHeaderContestant line 598, isClickable line 613, handleClick line 622, isSpent expression lines 658-662, the PieceOverlay call lines 649-664)
    - packages/client/src/components/PieceOverlay.tsx (NEW post-Plan-01 props: `selectionState`, `piece`, `onClick`, `onInspect`, `carrierId`, `attackingTeam`; exported `SelectionState`)
    - .planning/phases/12-visual-token-hex-layer/12-RESEARCH.md (Pattern 2 + Pitfall 3 + Pitfall 4: header-eligible AND contestant both map to 'active'; isSpent gating)
    - .planning/phases/12-visual-token-hex-layer/12-PATTERNS.md (HexGrid.tsx selectionState derivation block)
  </read_first>
  <action>
    Import `SelectionState` from `./PieceOverlay.js` (type-only import alongside the existing `PieceOverlay` import, or add `import type { SelectionState } from './PieceOverlay.js';`).

    Keep all existing boolean derivations (`canSelect`, `canSelectKickOff`, `canSelectHighPassMove`, `canSelectSnapDeflect`, `canSelectGKKickMove`, `isHeaderEligible`, `isHeaderContestant`, `isClickable`, `handleClick`) intact — they still drive click behavior.

    After `isClickable` is computed (around line 621), add:
    - `const isSpentNow = phase === 'HIGH_PASS_MOVEMENT' ? piece.id === highPassMovedPieceId && (highPassPaceUsed ?? 0) >= 3 : movedPieceIds.includes(piece.id);` (this is the exact expression currently inlined in the old `isSpent` prop).
    - `const selectionState: SelectionState = isSpentNow ? 'activated' : piece.id === selectedPieceId || isHeaderEligible || isHeaderContestant ? 'active' : isClickable ? 'selectable' : 'none';`

    Per D-07 / RESEARCH Pitfall 3: BOTH `isHeaderEligible` and `isHeaderContestant` map to `'active'` (green ring) — eligible-but-not-yet-toggled pieces also get the green ring; the click handler stays `toggleHeaderContestantId` via the existing `handleClick`.

    Replace the `<PieceOverlay .../>` call (lines 649-664): pass `key={piece.id}`, `piece={displayPiece}`, `selectionState={selectionState}`, `onClick={handleClick}`, `onInspect={() => inspectPiece(piece.id)}`, `carrierId={ball.carrierId}`, `attackingTeam={attackingTeam}`. Remove the old `isSelected`, `isClickable`, `isSpent`, `isHeaderContestant` props entirely.

    Do not change `handleClick` logic or any `canSelect*` conditions. Do not introduce `any`.

  </action>
  <verify>
    <automated>cd packages/client && pnpm vitest run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `HexGrid.tsx` imports `SelectionState` from `./PieceOverlay.js`
    - `HexGrid.tsx` computes `const selectionState: SelectionState` and passes `selectionState={selectionState}` to PieceOverlay
    - `HexGrid.tsx` PieceOverlay call site no longer contains `isSelected=`, `isClickable=`, `isSpent=`, `isHeaderContestant=`
    - Header-eligible AND header-contestant pieces both resolve to `'active'` (assert the `|| isHeaderEligible || isHeaderContestant` condition is present in the 'active' branch)
    - `pnpm vitest run` exits 0 (no client test regressions)
  </acceptance_criteria>
  <done>Each piece passes a single correct selectionState; the four old boolean props are gone from the call site.</done>
</task>

<task type="auto">
  <name>Task 2: Priority-resolve highlightType per hex; update HexCell call site; collapse tint-only overlays</name>
  <files>packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (full file — per-hex render loop lines 236-529: isValidMove, isGoalHex, isShotTarget, isShootingModeGoalHex, isShotPath/snapDeflectPathSet, isGKDiveTarget, isHeaderTargetGoalHex, isHeaderNonGoalTarget, isHighlighted lines 280-289, isHpMoveTarget lines 290-296, the HexCell call lines 363-372, and ALL overlay polygon blocks lines 373-526)
    - packages/client/src/components/HexCell.tsx (NEW post-Plan-02 props: `hex`, `highlightType?`, `onClick`; exported `HexHighlightType`)
    - .planning/phases/12-visual-token-hex-layer/12-RESEARCH.md (Pattern 3 priority resolution + Pitfall 5: keep action-overlay polygons in HexGrid)
    - .planning/phases/12-visual-token-hex-layer/12-PATTERNS.md (HexGrid.tsx — REMOVE vs KEEP overlay lists, highlightType derivation block, HexCell call replacement)
  </read_first>
  <action>
    Import `HexHighlightType` from `./HexCell.js` (`import type { HexHighlightType } from './HexCell.js';`).

    Inside the per-hex map, after `isShotPath` and `isGKDiveTarget` are computed (around line 296), derive priority booleans (D-12 order: risk > goal > shot-path > kickoff > safe):
    - `isRisk` = `(zoiRiskSet.has(hexId) && isValidMove) || (tackleRiskSet.has(hexId) && isValidMove) || isShotPath` (isShotPath = snap-deflect path = risk tint)
    - `isGoalTint` = `isGoalHex || isShotTarget || isShootingModeGoalHex || isHeaderTargetGoalHex`
    - `isShotPathTint` = `lastShotPathSet.has(hexId) || isHpMoveTarget || isGKDiveTarget` (note isHpMoveTarget already includes isGKDiveTarget; keep the explicit OR for clarity)
    - `isKickoffTint` = `inMyZone && !isCentreHex`  (NOTE: `inMyZone` and `isCentreHex` are currently computed AFTER the HexCell call near line 353 — move those two `const` computations ABOVE this derivation so they are in scope)
    - `isSafeTint` = `isHighlighted && !isGoalTint`
    - `const highlightType: HexHighlightType | undefined = isRisk ? 'risk' : isGoalTint ? 'goal' : isShotPathTint ? 'shot-path' : isKickoffTint ? 'kickoff' : isSafeTint ? 'safe' : undefined;`

    Replace the `<HexCell .../>` call (lines 363-372): pass `hex={hex}`, `highlightType={highlightType}`, `onClick={onClick ?? (() => undefined)}`. Remove the `isHighlighted` and `highlightColor` props.

    REMOVE these tint-only overlay polygon blocks (now covered by highlightType):
    - zoiRiskSet `styles.hexZoIRisk` polygon (lines 374-381)
    - tackleRiskSet `styles.hexTackleRisk` polygon (lines 382-390) — this MOVEMENT tackle-risk overlay folds into 'risk'
    - kickoff `inMyZone && !isCentreHex` zone polygon (lines 392-400)
    - snap-deflect `isShotPath` orange polygon (lines 443-452)
    - `lastShotPathSet.has(hexId)` gold polygon (lines 453-462)
    - `isHpMoveTarget` white polygon (lines 485-495) — folds into 'shot-path'

    KEEP these overlay polygons UNCHANGED (they have click handlers or special semantics):
    - centre-hex gold fill + gold ring (lines 401-420)
    - HEADER ball-position gold overlay (lines 421-431)
    - GK_KICK_TARGET sky-blue polygon with onClick (lines 463-473)
    - QUICK_THROW green polygon with onClick (lines 474-484)
    - safe pass-target green polygon with onClick (lines 496-505)
    - interception-risk pass polygon using `styles.hexTackleRisk` with onClick (lines 506-515) — KEEP; this is why `.hexTackleRisk` must stay in CSS
    - confirmed pass-target gold ring with onClick (lines 516-526)

    MODIFY the HEADER non-goal target overlay (`isHeaderNonGoalTarget`, lines 433-442): per D-13 change its tint from cyan `rgba(34,211,238,0.18)` / stroke `rgba(34,211,238,0.4)` to the white shot-path tint `rgba(255,255,255,0.35)` / stroke `#cccccc`. KEEP its `onClick={onClick}` and pointer cursor (it is an action overlay).

    Because `inMyZone`/`isCentreHex` are now referenced in the highlightType derivation, ensure those two `const` lines (currently ~353-357) are relocated above the derivation point; the `cx`/`points` consts they sit near (line 358-359) are used by the kept overlays — leave those where they are needed. Verify no use-before-declaration. Do not introduce `any`.

  </action>
  <verify>
    <automated>cd packages/client && pnpm vitest run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `HexGrid.tsx` imports `HexHighlightType` from `./HexCell.js`
    - `HexGrid.tsx` computes `const highlightType: HexHighlightType | undefined` with the exact priority order `isRisk ? 'risk' : isGoalTint ? 'goal' : isShotPathTint ? 'shot-path' : isKickoffTint ? 'kickoff' : isSafeTint ? 'safe' : undefined`
    - `HexGrid.tsx` HexCell call site passes `highlightType={highlightType}` and no longer contains `isHighlighted=` or `highlightColor=`
    - Per D-11 (consolidate polygon overlays in HexGrid): all tint-only overlay polygons are removed (folded into HexCell `highlightType`) and only the listed action overlays remain as explicit polygons in HexGrid
    - `HexGrid.tsx` no longer references `styles.hexZoIRisk`
    - `HexGrid.tsx` still references `styles.hexTackleRisk` exactly once (the interception-risk pass overlay)
    - The `isHeaderNonGoalTarget` overlay fill is `rgba(255,255,255,0.35)` (white), not `rgba(34,211,238,0.18)` (cyan)
    - `pnpm vitest run` exits 0
  </acceptance_criteria>
  <done>Each hex passes a single priority-resolved highlightType; tint-only overlays are removed; action overlays and HEADER white-tint (D-13) remain correct.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the now-dead .hexZoIRisk CSS class; verify build + types</name>
  <files>packages/client/src/components/HexGrid.module.css</files>
  <read_first>
    - packages/client/src/components/HexGrid.module.css (`.hexZoIRisk` lines 8-10 and `.hexTackleRisk` lines 12-15)
    - packages/client/src/components/HexGrid.tsx (post-Task-2 state — confirm `.hexZoIRisk` is no longer referenced, `.hexTackleRisk` is still referenced once)
  </read_first>
  <action>
    Remove the `.hexZoIRisk` rule (the ZoI steal-risk red tint) from `HexGrid.module.css` — its color is now provided by HexCell's `HIGHLIGHT_STYLES.risk`. KEEP the `.hexTackleRisk` rule: it is still applied to the interception-risk pass-target overlay in HexGrid (a KEEP overlay), so removing it would break that tint. Leave all other CSS unchanged.
  </action>
  <verify>
    <automated>cd packages/client && pnpm vitest run 2>&1 | tail -10 && pnpm tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `HexGrid.module.css` no longer contains `.hexZoIRisk`
    - `HexGrid.module.css` still contains `.hexTackleRisk`
    - `cd packages/client && pnpm tsc --noEmit` exits 0 (whole client now type-checks; PieceOverlay + HexCell + HexGrid APIs all aligned)
    - `pnpm vitest run` exits 0
  </acceptance_criteria>
  <done>Dead .hexZoIRisk class removed; .hexTackleRisk retained; full client type-checks and all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full unified visual system wired into the live board: striped tokens on-pitch and in replay, three selection rings driven by real game state, and the five-color unified hex tint system with priority resolution.</what-built>
  <how-to-verify>
    1. Run `pnpm --filter @counter-attack/client dev` (and the server) and start a 2-player game.
    2. Tokens: confirm home outfield pieces show a vertical black stripe and away show two horizontal maroon stripes on the pitch; GKs stay solid.
    3. Selection rings: during a MOVEMENT phase, confirm selectable own pieces show a BLUE ring; the piece you click shows a GREEN ring; a piece that has already moved this phase shows an ORANGE ring + red X. All three can be visible at once.
    4. Hex tints — trigger and confirm each: a normal move shows YELLOW safe hexes; moving the ball carrier near opponents shows ORANGE risk hexes; SHOT/goal-line selection shows RED goal hexes; KICK_OFF_SETUP shows BLUE zone hexes; a resolved shot path shows WHITE hexes. Where risk and safe overlap, the hex is ORANGE (risk wins).
    5. HIGH_PASS / HEADER: confirm header-eligible pieces show the GREEN active ring, and the header target hexes show WHITE tint (not cyan).
    6. Regression: confirm pass-target (green/amber), quick-throw (green), GK-kick (sky-blue), kickoff centre (gold), and HEADER ball-position (gold) overlays still appear and remain clickable.
    7. Replay: play through to full-time and confirm replay frames show the striped tokens (PieceOverlay is reused).
  </how-to-verify>
  <resume-signal>Type "approved" or list any state whose tint/ring color or clickability is wrong.</resume-signal>
</task>

</tasks>

<verification>
- `cd packages/client && pnpm vitest run` exits 0 (no regressions across the client suite)
- `cd packages/client && pnpm tsc --noEmit` exits 0
- Human verification across tokens, three rings, five tints, priority overlap, D-13 header white tint, kept action overlays, and replay
</verification>

<success_criteria>

- VIS-01 + VIS-02 (on-pitch + replay): striped tokens render on the live board and in replay via the shared PieceOverlay
- UX-05: the three selection rings are driven by real game state with correct priority (activated > active > selectable > none)
- UX-06: every hex resolves to exactly one of the five tints by the D-12 priority order; action overlays unaffected
- D-07: header contestants/eligibles use the green 'active' ring; `isHeaderContestant` prop fully removed
- D-11: polygon overlays consolidated in HexGrid — tint-only overlays folded into HexCell, action overlays retained
- D-13: HIGH_PASS header target hexes use the white shot-path tint
- Full client type-checks; full client test suite green
  </success_criteria>

<notes_for_executor>
Decisions implemented by this plan: D-07 (header contestants/eligibles use the green 'active' ring), D-11 (consolidate all polygon overlay rendering in HexGrid — fold tint-only polygons into HexCell's `highlightType`, keep only action overlays as explicit polygons), D-12 (highlightType priority order), and D-13 (HIGH_PASS header target hexes use the white shot-path tint).

This plan depends on Plans 01 and 02 having landed (PieceOverlay `selectionState` + `SelectionState`, HexCell `highlightType` + `HexHighlightType`). Read 12-01-SUMMARY.md and 12-02-SUMMARY.md first to confirm the exact exported symbol names and any deviations. The single most error-prone step is the use-before-declaration of `inMyZone`/`isCentreHex` — they are currently declared AFTER the HexCell call; move them above the highlightType derivation. Run `pnpm tsc --noEmit` after each task to catch ordering issues early. Do NOT touch PieceOverlay, HexCell, or PlayerStatsPanel in this plan.
</notes_for_executor>

<output>
Create `.planning/phases/12-visual-token-hex-layer/12-04-SUMMARY.md` when done
</output>
