---
phase: 25-bug-uat-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/server/src/gameHandlers.ts
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/replay.integration.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REPLAY-07, REPLAY-08, BUG-22]
must_haves:
  truths:
    - 'D-07: GK_KICK ActionEvent carries `ballAfter: { position: HexCoord; carrierId: string | null }` and GK_KICK ball delivery produces a visible frame in post-game replay'
    - 'D-08: LOOSE_BALL_LAND ActionEvent carries `ballAfter: { position: HexCoord; carrierId: string | null }` and LOOSE_BALL_LAND scatter resolution produces a visible frame in post-game replay'
    - 'BUG-22 requirement is marked closed with a pointer to the Phase 18.2 fix'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'ballAfter field on GK_KICK and LOOSE_BALL_LAND ActionEvent members'
      contains: 'ballAfter'
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'GK_KICK and LOOSE_BALL_LAND in REPLAY_ELIGIBLE_TYPES; LOOSE_BALL_LAND ballAfter populated'
      contains: "'LOOSE_BALL_LAND'"
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'GK_KICK ballAfter populated at construction (receiver resolved before object literal)'
      contains: 'ballAfter'
    - path: 'packages/server/src/__tests__/replay.integration.test.ts'
      provides: 'REPLAY-07 and REPLAY-08 regression tests'
      contains: 'REPLAY-07'
  key_links:
    - from: 'packages/server/src/gameEngine.ts REPLAY_ELIGIBLE_TYPES'
      to: 'buildReplayFrames'
      via: 'REPLAY_ELIGIBLE_TYPES.has(event.type)'
      pattern: 'REPLAY_ELIGIBLE_TYPES.has'
    - from: 'GK_KICK/LOOSE_BALL_LAND ActionEvent'
      to: 'replay frame ball position'
      via: 'ballAfter field read by buildReplayFrames'
      pattern: 'ballAfter'
---

<objective>
Fix the two identical replay defects (REPLAY-07: GK_KICK, REPLAY-08: LOOSE_BALL_LAND) so ball delivery is visible during post-game replay, and close BUG-22 as documentation-only (the code fix already shipped in Phase 18.2). Both replay events currently carry no `ballAfter` field and are excluded from `REPLAY_ELIGIBLE_TYPES`, so the ball appears to teleport in replay. This plan adds the field, populates it at both construction sites, adds the types to the eligibility set with correct per-type comments, adds regression tests, and updates the BUG-22 checkbox.

Purpose: Close all backend/shared replay-visibility and documentation debt for the v1.3 milestone (D-06 through D-12 for replay; D-04/D-05 for BUG-22).
Output: `ballAfter` on two ActionEvent members, populated construction sites, two new eligibility entries with individual comments, two regression tests, BUG-22 marked `[x]`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-bug-uat-closure/25-CONTEXT.md
@.planning/phases/25-bug-uat-closure/25-RESEARCH.md
@.planning/phases/25-bug-uat-closure/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ballAfter to GK_KICK + LOOSE_BALL_LAND types and populate both construction sites</name>
  <files>packages/shared/src/types.ts, packages/server/src/gameHandlers.ts, packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/shared/src/types.ts (lines 272-300 — HP_ACCURACY at 272, LOOSE_BALL_LAND at 289, GK_KICK at 291, and the HEADED_PASS member which already carries `ballAfter` as the shape to mirror)
    - packages/server/src/gameHandlers.ts (lines 800-862 — the GK_KICK `kickEvent` object literal is at line 813; `receiver` is currently resolved at line 823 INSIDE the `if (accurate)` branch)
    - packages/server/src/gameEngine.ts (lines 2740-2780 — LOOSE_BALL_LAND construction at line 2755; the resolved final carrier variable is available just before it) and lines 4348-4373 (REPLAY_ELIGIBLE_TYPES set)
  </read_first>
  <action>
    Three edits, all mechanical applications of the existing HEADED_PASS `ballAfter` pattern.

    (1) In packages/shared/src/types.ts, append the field `ballAfter: { position: HexCoord; carrierId: string | null }` as the last property on BOTH the `GK_KICK` union member (currently ending at `timestamp: number`) and the `LOOSE_BALL_LAND` union member (currently `{ type: 'LOOSE_BALL_LAND'; from: HexCoord; to: HexCoord; timestamp: number }`). Match the HEADED_PASS member's field shape exactly. Because `exactOptionalPropertyTypes` is enforced project-wide, the field is required (not optional) and `carrierId` uses `string | null`.

    (2) In packages/server/src/gameHandlers.ts, resolve the receiver BEFORE the `kickEvent` object literal at line 813 to avoid the "used before assigned" error (Pitfall 1). Move/replicate the receiver lookup so a `const receiver = accurate ? gkEndState.pieces.find(...) : null;` exists prior to construction, reusing the identical `.find(...)` predicate currently at line 823. Then add `ballAfter: { position: targetHex, carrierId: accurate ? (receiver?.id ?? null) : null }` to `kickEvent`. Do NOT leave `ballAfter` as a null placeholder (D-09). The accurate branch must yield the receiver id; the inaccurate branch yields `carrierId: null` (loose ball).

    (3) In packages/server/src/gameEngine.ts, add `ballAfter: { position: finalPosition, carrierId: finalCarrierId }` to the LOOSE_BALL_LAND event object at line 2755, using the already-resolved final landing position and final carrier id present at that point (`carrierId` is `null` for a genuine loose ball). Then in REPLAY_ELIGIBLE_TYPES (line 4349), add two entries — `'GK_KICK'` and `'LOOSE_BALL_LAND'` — each preceded by its OWN inline comment (D-10 forbids a shared comment): one citing REPLAY-07 (GK_KICK ball delivery, carries ballAfter, construction at gameHandlers.ts ~line 813), one citing REPLAY-08 (LOOSE_BALL_LAND scatter, carries ballAfter, construction at gameEngine.ts ~line 2755). Remove the existing incorrect "dead code / zero construction sites" comment about GK_KICK entirely (D-11).

  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit -p tsconfig.json && grep -c "ballAfter" src/gameHandlers.ts && grep -v '^#' ../shared/src/types.ts | grep -c "ballAfter"</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/types.ts: the GK_KICK member and the LOOSE_BALL_LAND member each contain `ballAfter: { position: HexCoord; carrierId: string | null }`
    - packages/server/src/gameHandlers.ts: the `kickEvent` object at ~line 813 contains a `ballAfter` property whose `carrierId` is `accurate ? (receiver?.id ?? null) : null`; `receiver` is declared before the `kickEvent` literal
    - packages/server/src/gameEngine.ts: the LOOSE_BALL_LAND event at ~line 2755 contains `ballAfter: { position: finalPosition, carrierId: finalCarrierId }` (concrete resolved values, not null)
    - packages/server/src/gameEngine.ts: REPLAY_ELIGIBLE_TYPES contains both `'GK_KICK'` and `'LOOSE_BALL_LAND'`, each with its own preceding comment; the old "dead code"/"zero construction sites" GK_KICK comment is gone
    - `npx tsc --noEmit` in packages/server exits 0
  </acceptance_criteria>
  <done>TypeScript compiles clean; both events carry populated `ballAfter`; both are in REPLAY_ELIGIBLE_TYPES with individual comments; the incorrect dead-code comment is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add REPLAY-07 and REPLAY-08 regression tests</name>
  <files>packages/server/src/__tests__/replay.integration.test.ts</files>
  <behavior>
    - REPLAY-07: an eventLog containing a GK_KICK with `ballAfter: { position: kickTarget, carrierId: receiver.id }` followed by a MOVE produces a buildReplayFrames frame whose `ball` equals `{ position: kickTarget, carrierId: receiver.id }`
    - REPLAY-08: an eventLog containing a LOOSE_BALL_LAND with `ballAfter: { position: landHex, carrierId: null }` followed by a MOVE produces a frame whose `ball` equals `{ position: landHex, carrierId: null }`
  </behavior>
  <read_first>
    - packages/server/src/__tests__/replay.integration.test.ts (lines 628-719 — the REPLAY-06 HEADED_PASS/GK_PUNT test is the exact structure to mirror: setupFullTimeRoom → mutate room.gameState.eventLog → buildReplayFrames → .find() by carrierId + position.q → assert frame.ball)
    - packages/shared/src/types.ts (GK_KICK and LOOSE_BALL_LAND member field lists, so the hand-crafted events type-check)
  </read_first>
  <action>
    Add two `it(...)` cases mirroring the REPLAY-06 pattern at lines 628-719. For REPLAY-07 (mirror the HEADED_PASS case): craft a GK_KICK event (`gkId`, `targetHex`, `accurate: true`, `kickDie`, `kickScore`, `timestamp`, `ballAfter: { position: kickTarget, carrierId: receiver.id }`) followed by a MOVE event with a distinct ball position, call `buildReplayFrames`, and assert the frame found by `carrierId === receiver.id && position.q === kickTarget.q` has `ball` deep-equal to `{ position: kickTarget, carrierId: receiver.id }`. For REPLAY-08 (mirror the GK_PUNT case): craft a LOOSE_BALL_LAND event (`from`, `to`, `timestamp`, `ballAfter: { position: to, carrierId: null }`) followed by a MOVE, and assert the frame has `ball` equal to `{ position: to, carrierId: null }` (loose ball, null carrier). Use the same `setupFullTimeRoom()` / `getRoom(roomCode)` helpers already imported in the file.
  </action>
  <verify>
    <automated>cd packages/server && npx vitest run replay.integration -t "REPLAY-07" && npx vitest run replay.integration -t "REPLAY-08"</automated>
  </verify>
  <acceptance_criteria>
    - replay.integration.test.ts contains a test titled with `REPLAY-07` asserting a GK_KICK-derived frame with `carrierId: receiver.id`
    - replay.integration.test.ts contains a test titled with `REPLAY-08` asserting a LOOSE_BALL_LAND-derived frame with `carrierId: null`
    - `npx vitest run replay.integration` exits 0 with both new tests passing
  </acceptance_criteria>
  <done>Both regression tests pass; they fail if either event type is removed from REPLAY_ELIGIBLE_TYPES or if `ballAfter` is dropped.</done>
</task>

<task type="auto">
  <name>Task 3: Close BUG-22 in REQUIREMENTS.md (documentation only)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 73 — the BUG-22 checkbox entry; line 139 — the Traceability row for BUG-22)
  </read_first>
  <action>
    Change the BUG-22 checkbox at line 73 from `[ ]` to `[x]` and append a note that the fix already shipped in Phase 18.2: `carrierExclusionKey: 'highPassCarrierId'` at `gameHandlers.ts:405`, covered by `gameHandlers.phase18-02.test.ts`; checkbox updated in Phase 25 (D-04/D-05). Update the Traceability table row for BUG-22 (line 139) from `Pending` to `Complete` with the same pointer note. No code changes — this is a pure documentation closure.
  </action>
  <verify>
    <automated>grep -n "BUG-22" .planning/REQUIREMENTS.md | grep -q "\[x\]" && grep -n "highPassCarrierId" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md BUG-22 requirement line begins with `- [x]`
    - REQUIREMENTS.md BUG-22 entry references the Phase 18.2 fix location `gameHandlers.ts:405` and `highPassCarrierId`
    - REQUIREMENTS.md Traceability row for BUG-22 reads `Complete`
  </acceptance_criteria>
  <done>BUG-22 is marked complete with a traceable pointer to the shipped Phase 18.2 fix; no source code changed.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 01)

- `ballAfter: { position: HexCoord; carrierId: string | null }` field added to the `GK_KICK` and `LOOSE_BALL_LAND` ActionEvent union members in `packages/shared/src/types.ts`
- `'GK_KICK'` and `'LOOSE_BALL_LAND'` string entries added to `REPLAY_ELIGIBLE_TYPES` in `packages/server/src/gameEngine.ts`
- `const receiver` hoisted before the GK_KICK `kickEvent` literal in `packages/server/src/gameHandlers.ts`
- Test cases `REPLAY-07: GK_KICK produces a visible replay frame` and `REPLAY-08: LOOSE_BALL_LAND produces a visible replay frame` in `packages/server/src/__tests__/replay.integration.test.ts`
- No new files, classes, decorators, or CLI flags.
  </artifacts>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                         |
| -------------------- | ------------------------------------------------------------------- |
| client→server socket | Existing authenticated Socket.io connection; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                 |
| --------- | ---------------------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-25-01   | Tampering              | `ballAfter` field on replay events | accept      | Field is server-constructed from already-validated game state; no user-supplied value enters it. Replay is read-only post-game. |
| T-25-02   | Information Disclosure | replay frames                      | accept      | Replay reveals only ball positions already broadcast during live play; no new data exposure.                                    |

No package installs in this plan — Package Legitimacy Gate not applicable (RESEARCH.md).
</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` passes (full server suite green, including the two new replay tests)
- `npx tsc --noEmit` clean across shared + server
- REQUIREMENTS.md BUG-22 marked `[x]`
</verification>

<success_criteria>

- GK_KICK and LOOSE_BALL_LAND each carry a populated `ballAfter` and appear in `REPLAY_ELIGIBLE_TYPES` with individual explanatory comments
- The incorrect "dead code" comment about GK_KICK is removed
- Two regression tests prove replay-frame visibility for both events
- BUG-22 is documented as closed with a pointer to the Phase 18.2 fix
  </success_criteria>

<output>
Create `.planning/phases/25-bug-uat-closure/25-01-SUMMARY.md` when done
</output>
