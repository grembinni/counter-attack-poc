---
phase: 25-bug-uat-closure
plan: 05
type: execute
wave: 2
depends_on: [25-01, 25-02, 25-03, 25-04]
files_modified: []
autonomous: false
requirements: [OFFSIDE-01, OFFSIDE-02, BUG-23, UX-15]
must_haves:
  truths:
    - 'Offside detection flags the correct pieces, persists across non-moving turns, and clears on valid clear-path plays (OFFSIDE-01 closed)'
    - 'The free-kick restart triggers on possession gain and deflect, placement is correct, and the restricted action set is enforced (OFFSIDE-02 closed)'
    - 'No stale shot-path/goal-target shading remains at KICK_OFF_SETUP after a SNAPSHOT_DEFLECT goal (BUG-23 closed)'
    - 'The pass-result popup, eligible counter, player-number centering, Style 12/13 patterns, and uniform-preservation all behave per the UI-SPEC (UX-15 visual closure)'
  artifacts:
    - path: '.planning/phases/25-bug-uat-closure/25-05-SUMMARY.md'
      provides: 'Recorded UAT outcomes for all four checkpoints'
  key_links:
    - from: 'Wave 1 code changes'
      to: 'human two-tab verification'
      via: 'live browser session'
      pattern: 'UAT'
---

<objective>
Close the human-verification gates for the v1.3 milestone after all Wave 1 code changes are stable: OFFSIDE-01 and OFFSIDE-02 UAT (Phase 17 code, never formally closed), the BUG-23 speculative shading fix, and the UX-15 visual/behavioral changes. This plan is code-free — it runs the two-tab live scenarios defined in D-02, D-15, and the UI-SPEC interaction contract, and records the outcomes. Per D-03, if any OFFSIDE bug surfaces, a gap plan is created within Phase 25 (not deferred to Phase 26); per D-15, if BUG-23 shading persists, it escalates to Phase 26.

Purpose: Formally close OFFSIDE-01/02 (D-01/D-02), verify the BUG-23 fix (D-15), and confirm the UX-15 visual changes in live play.
Output: Recorded UAT results for all four requirement groups; gap-plan trigger if any offside scenario fails; Phase 26 escalation note if BUG-23 persists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-bug-uat-closure/25-CONTEXT.md
@.planning/phases/25-bug-uat-closure/25-UI-SPEC.md
@.planning/phases/17-rule-bugs/17-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: OFFSIDE-01 UAT — offside detection (Scenarios A, B, C)</name>
  <read_first>
    - .planning/phases/25-bug-uat-closure/25-CONTEXT.md (D-02 — the four UAT scenarios)
    - .planning/phases/17-rule-bugs/17-CONTEXT.md (D-22 through D-24 — sticky offsidePieceIds, team-relative detection, clear paths, defender flagging)
  </read_first>
  <what-built>Phase 17's offside detection (`evaluateOffside` + sticky `offsidePieceIds` in gameEngine.ts) is live and unchanged. All Wave 1 code changes are merged and the client/server suites are green.</what-built>
  <how-to-verify>
    Start a two-tab live session (two browser tabs sharing a room code), play to a passing situation, and run these three scenarios:
    1. Scenario A (flag): Forward-pass to a teammate who is behind the second-to-last defender at the moment of ball contact. EXPECT: offside flag raised, the flagged player highlighted via `offsidePieceIds` (deeper-red ring), possession transferred at the pass-origin hex.
    2. Scenario B (no flag — level): Play a pass level with the second-to-last defender. EXPECT: no flag.
    3. Scenario C (no flag — not active): With a player standing in an offside position, play the ball to a DIFFERENT (onside) player. EXPECT: no flag.
    Also confirm stickiness: after a flag, take a non-moving turn and verify the flag persists; then make a valid clear-path play and verify it clears (D-22).
  </how-to-verify>
  <resume-signal>Type "approved" if all of A/B/C plus stickiness/clear behave as expected. If any scenario fails, describe the failure — a gap plan will be created within Phase 25 (D-03).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: OFFSIDE-02 UAT — free-kick restart (Scenario D)</name>
  <read_first>
    - .planning/phases/25-bug-uat-closure/25-CONTEXT.md (D-02 Scenario D)
    - .planning/phases/17-rule-bugs/17-CONTEXT.md (D-30/D-31 placement rules, restricted action set, D-41 deflection addendum)
  </read_first>
  <what-built>Phase 17's free-kick restart flow (foul trigger on possession gain and deflect, placement rules, restricted action set) is live and unchanged.</what-built>
  <how-to-verify>
    Continuing the two-tab session, after an offside flag from Task 1:
    1. Scenario D (restart): The opponent takes a free kick from the pass-origin hex. EXPECT: free-kick placement follows D-30/D-31; the restricted action set is enforced (free kick only — no normal move available); the flow resumes correctly afterward.
    2. Confirm the foul trigger fires both on a possession gain and on a deflect (D-41 addendum) if reachable in the session.
  </how-to-verify>
  <resume-signal>Type "approved" if the free-kick restart, placement, and restricted action set all behave correctly. If any part fails, describe it — a gap plan will be created within Phase 25 (D-03).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BUG-23 + UX-15 visual/behavioral confirmation</name>
  <read_first>
    - .planning/phases/25-bug-uat-closure/25-CONTEXT.md (D-14/D-15 BUG-23; D-16/D-17/D-18/D-19/D-20 UX-15)
    - .planning/phases/25-bug-uat-closure/25-UI-SPEC.md (Interaction Contract + Copywriting Contract + Component Modification Map)
  </read_first>
  <what-built>Wave 1 applied: BUG-23 shading guards (Plan 02), player-number centering (Plan 02), pass-result popup + eligible counter (Plan 03), and the header-contestant-preservation fix (Plan 04). Style 12/13 already satisfy D-18 as built (Style 12 = ✕ via rotate(45), Style 13 = ╬ axis-aligned) — no code change was applied; this UAT confirms it.</what-built>
  <how-to-verify>
    In the two-tab live session, confirm each item:
    1. BUG-23 (D-15): Reproduce SNAPSHOT_DEFLECT → goal → KICK_OFF_SETUP. EXPECT: no shot-path shading and no stale red goal-target tint anywhere on the pitch at kickoff. If shading persists, this escalates to Phase 26 for instrumentation-based root-cause (D-15) — do not treat as approved.
    2. UX-15 pass popup (D-20): Perform a HIGH_PASS/HIGH_PASS_MOVE. EXPECT: an auto-dismissing amber popup reading exactly "Accurate Pass!" or "Loose Ball!" (never a "Continue" button); it auto-advances after ~1.5s with no click.
    3. UX-15 eligible counter (D-19): In MOVEMENT, select an unmoved piece. EXPECT: the "left to move" counter decrements immediately on selection (before committing a destination) and restores on undo/deselect.
    4. UX-15 player number (D-17): EXPECT: jersey numbers appear vertically centered in the piece circle across several pieces and zoom levels.
    5. UX-15 Style 12/13 (D-18): On the uniform selection screen, EXPECT: Style 12 shows diagonal-axis quarters (✕) and Style 13 shows cross-axis quarters (╬). If Style 12 instead shows ╬ (or Style 13 shows ✕), the live mapping is wrong — describe it and a gap plan will be created within Phase 25.
    6. UX-15 header contestant preservation (D-16): Both players are in the HEADER phase contestant selection screen. Player A selects a contestant and confirms. EXPECT: Player B's previously-selected contestant(s) remain highlighted and selected — Player B can still confirm their own selection without re-selecting.
  </how-to-verify>
  <resume-signal>Type "approved" if all six items pass. If BUG-23 shading persists, note it for Phase 26 escalation. If any UX-15 item fails, describe it — a gap plan will be created within Phase 25.</resume-signal>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 05)

- `.planning/phases/25-bug-uat-closure/25-05-SUMMARY.md` recording the outcomes of all four UAT checkpoints
- No source code, files, or symbols produced (UAT-only plan)
  </artifacts>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                |
| -------- | ---------------------------------------------------------- |
| n/a      | UAT-only plan; no code changes and no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan                                                                                    |
| --------- | -------- | --------- | ----------- | -------------------------------------------------------------------------------------------------- |
| T-25-07   | n/a      | none      | accept      | This plan runs human verification only; it introduces no code, endpoints, inputs, or dependencies. |

No package installs — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- All three human-verify checkpoints approved
- Any OFFSIDE or UX-15 failure recorded and routed to a Phase 25 gap plan (D-03)
- BUG-23 persistence, if any, recorded for Phase 26 escalation (D-15)
</verification>

<success_criteria>

- OFFSIDE-01 UAT (Scenarios A/B/C + stickiness/clear) approved
- OFFSIDE-02 UAT (Scenario D restart + restricted action set) approved
- BUG-23 shading confirmed cleared at KICK_OFF_SETUP (or escalated to Phase 26)
- All five UX-15 visual/behavioral items confirmed in live play
  </success_criteria>

<output>
Create `.planning/phases/25-bug-uat-closure/25-05-SUMMARY.md` when done
</output>
