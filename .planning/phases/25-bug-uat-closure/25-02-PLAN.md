---
phase: 25-bug-uat-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/PieceOverlay.tsx
autonomous: true
requirements: [BUG-23, UX-15]
must_haves:
  truths:
    - 'No stale shot-path or goal-target shading remains on the pitch at KICK_OFF_SETUP after a SNAPSHOT_DEFLECT goal'
    - 'Jersey numbers render visually centered inside the piece circle'
    - 'Style 12 renders diagonal-axis quarters (✕); Style 13 renders cross-axis quarters (╬) — already satisfied by the live rotate(45) transform on Style 12 (D-18); verification-only, confirmed at the Plan 05 UAT checkpoint'
  artifacts:
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: "Outer phase!=='KICK_OFF_SETUP' guard on isShotPathTint + useEffect clearing shotTargetHighlight"
      contains: 'KICK_OFF_SETUP'
    - path: 'packages/client/src/components/PieceOverlay.tsx'
      provides: 'Jersey number text with corrected vertical centering'
      contains: 'dominantBaseline'
  key_links:
    - from: 'HexGrid.tsx phase transition to KICK_OFF_SETUP'
      to: 'setShotTargetHighlight(null)'
      via: 'useEffect([phase])'
      pattern: 'setShotTargetHighlight'
---

<objective>
Apply the BUG-23 belt-and-suspenders shading fix and one UX-15 rendering bug fix, all isolated to client rendering files with no overlap with other Wave 1 plans. BUG-23: gate the entire `isShotPathTint` expression on `phase !== 'KICK_OFF_SETUP'` and clear the never-cleared `shotTargetHighlight` state on the KICK_OFF_SETUP transition. UX-15 (player number, D-17): correct the jersey number vertical centering.

UX-15 (Style 12/13, D-18) requires no code change in this plan: the live `uniformStyles.tsx` already satisfies D-18 — Style 12 (`quarterHorizontal`, registry index 12) applies `patternTransform="rotate(45 cx cy)"` and therefore renders diagonal-axis quarters (✕), while Style 13 (`quarterDiagonal`, index 13) is axis-aligned and renders cross-axis quarters (╬). Because the mapping matches D-18 as built, no swap is applied; this decision is confirmed visually at the Plan 05 UAT checkpoint.

Purpose: Eliminate the persistent kick-off shading defect (D-13/D-14) and the player-number centering bug (D-17), and confirm that Style 12/13 already meet D-18 without introducing a regression.
Output: Two HexGrid rendering-guard changes and one PieceOverlay SVG attribute change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-bug-uat-closure/25-CONTEXT.md
@.planning/phases/25-bug-uat-closure/25-RESEARCH.md
@.planning/phases/25-bug-uat-closure/25-PATTERNS.md
@.planning/phases/25-bug-uat-closure/25-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUG-23 — outer KICK_OFF_SETUP guard on isShotPathTint + clear shotTargetHighlight</name>
  <files>packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (line 135 — `const [shotTargetHighlight, setShotTargetHighlight] = useState<HexCoord | null>(null)`; line ~422 — the current `isShotPathTint` expression; line ~497 — the only site that SETS shotTargetHighlight on shot-declaration click)
    - packages/client/src/components/EventBanner.tsx (lines 59-81 — the canonical `useEffect` state-cleanup-with-dependency pattern to mirror; Pitfall 4 forbids updating state in the render body)
  </read_first>
  <action>
    Two edits (D-14). Fix 1: rewrite the `isShotPathTint` expression at ~line 422 so the entire boolean is gated by `phase !== 'KICK_OFF_SETUP'` as an outer AND, rather than gating only the `lastShotPathSet.has(hexId)` sub-clause. Target shape: `phase !== 'KICK_OFF_SETUP' && (lastShotPathSet.has(hexId) || isHpMoveTarget || isGKDiveTarget || isShotPath || highPassContestZoneSet.has(hexId))`. Fix 2: add a `useEffect` (NOT inline in render — Pitfall 4) with dependency array `[phase]` that calls `setShotTargetHighlight(null)` whenever `phase === 'KICK_OFF_SETUP'`, mirroring the EventBanner effect pattern. This clears the stale red goal-target tint that is currently only ever set and never cleared.
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit -p tsconfig.json && grep -c "phase !== 'KICK_OFF_SETUP'" src/components/HexGrid.tsx && grep -q "setShotTargetHighlight(null)" src/components/HexGrid.tsx</automated>
  </verify>
  <acceptance_criteria>
    - HexGrid.tsx `isShotPathTint` is a single expression whose leading term is `phase !== 'KICK_OFF_SETUP' &&` followed by the parenthesized OR of the five sub-conditions
    - HexGrid.tsx contains a `useEffect(() => { if (phase === 'KICK_OFF_SETUP') setShotTargetHighlight(null); }, [phase])` (or equivalent) — a real effect, not a render-body call
    - `npx tsc --noEmit` in packages/client exits 0
  </acceptance_criteria>
  <done>The full shot-path tint expression is phase-gated and `shotTargetHighlight` is cleared on KICK_OFF_SETUP; type-check passes. (Visual closure via the Plan 05 UAT checkpoint.)</done>
</task>

<task type="auto">
  <name>Task 2: UX-15 — correct jersey number vertical centering</name>
  <files>packages/client/src/components/PieceOverlay.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx (lines 254-266 — the jersey number `<text>` element; currently `dominantBaseline="central"`)
  </read_first>
  <action>
    Change `dominantBaseline="central"` to `dominantBaseline="middle"` on the jersey number `<text>` element (~line 254) — `"middle"` is the cross-browser-reliable SVG baseline for vertical centering (D-17). Do NOT also add a `dy` offset in the same change; `"middle"` alone is the first fix per the research. Leave `x={cx}`, `y={cy}`, `textAnchor="middle"`, `fontSize`, `fontWeight`, `fill`, and the GK italic style untouched.
  </action>
  <verify>
    <automated>cd packages/client && grep -q 'dominantBaseline="middle"' src/components/PieceOverlay.tsx && ! grep -q 'dominantBaseline="central"' src/components/PieceOverlay.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - PieceOverlay.tsx jersey number `<text>` element uses `dominantBaseline="middle"`
    - No occurrence of `dominantBaseline="central"` remains in PieceOverlay.tsx
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Jersey number text uses `dominantBaseline="middle"`; type-check passes. (Visual centering confirmed via Plan 05 UAT checkpoint.)</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 02)

- Rewritten `isShotPathTint` expression (outer `phase !== 'KICK_OFF_SETUP'` guard) in `HexGrid.tsx`
- New `useEffect([phase])` clearing `shotTargetHighlight` in `HexGrid.tsx`
- `dominantBaseline="middle"` on the jersey number `<text>` in `PieceOverlay.tsx`
- No change to `uniformStyles.tsx`: Style 12/13 already satisfy D-18 as built (verified at the Plan 05 UAT checkpoint)
- No new files, exported symbols, classes, decorators, or CLI flags.
  </artifacts>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                   |
| -------------------- | --------------------------------------------------------------------------------------------- |
| server→client render | Client renders server-broadcast state; this plan changes only presentation, no input handling |

## STRIDE Threat Register

| Threat ID | Category  | Component                 | Disposition | Mitigation Plan                                                                                                                                    |
| --------- | --------- | ------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-25-03   | Tampering | client-only render guards | accept      | Changes are pure presentation (SVG attributes, render-time boolean guards, local UI state cleanup); no server authority or validation is affected. |

No package installs — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test` passes (no regressions in existing HexGrid/PieceOverlay/uniform tests)
- `npx tsc --noEmit` clean in packages/client
- Visual/behavioral confirmation of BUG-23, player-number centering, and Style 12/13 (already-correct check) handled by the Plan 05 UAT checkpoint
</verification>

<success_criteria>

- `isShotPathTint` is fully phase-gated and `shotTargetHighlight` is cleared at KICK_OFF_SETUP
- Jersey number uses `dominantBaseline="middle"`
- Style 12 renders ✕ (diagonal) and Style 13 renders ╬ (cross) as already built — no swap applied (D-18)
- Client type-check and test suite pass
  </success_criteria>

<output>
Create `.planning/phases/25-bug-uat-closure/25-02-SUMMARY.md` when done
</output>
