---
phase: 47-select-based-roster-interaction
plan: 03
type: execute
wave: 3
depends_on: ["47-01", "47-02"]
files_modified:
  - packages/client/src/components/LineupAssignmentScreen.tsx
autonomous: true
requirements: [ROSTER-01, ROSTER-02, ROSTER-03, ROSTER-04, ROSTER-05, ROSTER-06, ROSTER-07, ROSTER-08]

must_haves:
  truths:
    - "Clicking an eligible mid-match on-pitch card selects it (green); eligible swap targets render blue"
    - "Clicking the selected card again deselects it and clears every blue highlight"
    - "Clicking an eligible blue target calls onReposition (positioning) or stages pendingSub (substitution)"
    - "Substitution mode is bench-first: clicking an on-pitch card with nothing selected does nothing"
    - "Clicking a different eligible bench card switches the selection; clicking a different on-pitch card in positioning mode does not"
    - "The GK card is never selectable in positioning mode"
    - "The SENT OFF placeholder is a blue eligible target in positioning mode only"
    - "Standard pregame slot swap completes by selecting a card then clicking an eligible slot"
    - "Draft pack/slot/bench picks and rearranges complete by click, with GK-slot and swap-vs-move semantics unchanged"
    - "No drag-and-drop state, handler, type, prop, or DOM attribute remains in LineupAssignmentScreen.tsx"
  artifacts:
    - path: "packages/client/src/components/LineupAssignmentScreen.tsx"
      provides: "click-to-select interaction across all four roster surfaces"
      contains: "handleRepositionCardClick"
  key_links:
    - from: "packages/client/src/components/LineupAssignmentScreen.tsx"
      to: "packages/client/src/components/BenchCarousel.tsx"
      via: "onCardClick / onBenchAreaClick / selectedCardId / benchAreaEligible props"
      pattern: "onCardClick=|onBenchAreaClick="
    - from: "packages/client/src/components/LineupAssignmentScreen.tsx"
      to: "packages/client/src/components/DraftPackCarousel.tsx"
      via: "onCardClick / selectedCardId props"
      pattern: "onCardClick="
    - from: "packages/client/src/components/LineupAssignmentScreen.tsx"
      to: "server handlers (GAME_ROSTER_REPOSITION / GAME_SUBSTITUTION / LINEUP_SWAP / DRAFT_PICK / DRAFT_REARRANGE)"
      via: "unchanged onReposition / onSubstitute / onSwap / onDraftPick / onDraftRearrange callbacks"
      pattern: "onReposition\\?\\.|onSubstitute\\?\\.|onSwap\\(|onDraftPick\\?\\.|onDraftRearrange\\?\\."
---

<objective>
Port every interaction surface in `LineupAssignmentScreen.tsx` from native HTML5 drag-and-drop to the app's click-to-select model: mid-match positioning, mid-match substitution, Standard pregame lineup swap, and the draft-mode pack/bench/lineup carousel.

Purpose: This is the phase's core deliverable (ROSTER-01..08). All four surfaces share one component, one `LineupStatCard`, and two carousel collaborators, so the production port must land atomically — a partially ported file does not compile.
Output: A fully click-driven `LineupAssignmentScreen.tsx` with four structurally separate eligibility computations and zero drag code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-select-based-roster-interaction/47-CONTEXT.md
@.planning/phases/47-select-based-roster-interaction/47-RESEARCH.md
@.planning/phases/47-select-based-roster-interaction/47-PATTERNS.md
@.planning/phases/47-select-based-roster-interaction/47-UI-SPEC.md
@.planning/phases/47-select-based-roster-interaction/47-01-SUMMARY.md
@.planning/phases/47-select-based-roster-interaction/47-02-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols/attributes introduced by this plan (do not treat as pre-existing drift):

| Symbol | Kind | File |
|---|---|---|
| `DraftSelection` | type alias | `LineupAssignmentScreen.tsx` |
| `pregameSelectedSlotIndex` | state | `LineupAssignmentScreen.tsx` |
| `draftSelection` | state | `LineupAssignmentScreen.tsx` |
| `repositionSelectedPieceId` | state | `LineupAssignmentScreen.tsx` |
| `substituteSelectedPlayerId` | state | `LineupAssignmentScreen.tsx` |
| `isRepositionEligible` | function | `LineupAssignmentScreen.tsx` |
| `handleRepositionCardClick` | function | `LineupAssignmentScreen.tsx` |
| `isSubstituteEligible` | function | `LineupAssignmentScreen.tsx` |
| `handleSubstituteBenchClick` | function | `LineupAssignmentScreen.tsx` |
| `handleSubstitutePitchClick` | function | `LineupAssignmentScreen.tsx` |
| `isPregameSwapEligible` | function | `LineupAssignmentScreen.tsx` |
| `handlePregameCardClick` | function | `LineupAssignmentScreen.tsx` |
| `violatesGKRule` | function | `LineupAssignmentScreen.tsx` |
| `isDraftSlotEligible` | function | `LineupAssignmentScreen.tsx` |
| `isDraftBenchAreaEligible` | function | `LineupAssignmentScreen.tsx` |
| `handleDraftPackCardClick` | function | `LineupAssignmentScreen.tsx` |
| `handleDraftSlotClick` | function | `LineupAssignmentScreen.tsx` |
| `handleDraftBenchCardClick` | function | `LineupAssignmentScreen.tsx` |
| `handleDraftBenchAreaClick` | function | `LineupAssignmentScreen.tsx` |
| `StatCardProps.isSelected` | prop | `LineupAssignmentScreen.tsx` |
| `StatCardProps.isEligibleTarget` | prop | `LineupAssignmentScreen.tsx` |
| `StatCardProps.isSelectable` | prop | `LineupAssignmentScreen.tsx` |
| `StatCardProps.allowGKSelect` | prop (renamed from `allowGKDrag`) | `LineupAssignmentScreen.tsx` |
| `StatCardProps.onClick` | prop | `LineupAssignmentScreen.tsx` |
| `data-roster-card` | DOM attribute | `LineupAssignmentScreen.tsx` |

Symbols REMOVED by this plan: types `DragState`, `MidmatchDragState`; state `dragSourceIndex`, `dropTargetIndex`, `dragState`, `draftDropTargetIndex`, `midmatchDrag`, `midmatchDropTargetPieceId`; functions `handleMidmatchDragStart`, `handleMidmatchRepositionDrop`, `handleMidmatchSubstituteDrop`, `handleDragStart`, `handleDragOver`, `handleDragLeave`, `handleDrop`, `handleDragEnd`, `handleDraftSlotDragStart`, `handleDraftSlotDragOver`, `handleDraftSlotDragLeave`, `handleDraftSlotDrop`, `handleDraftSlotDragEnd`, `handleDropToBench`; props `StatCardProps.isDragSource`, `isDropTarget`, `onDragStart`, `onDragOver`, `onDragLeave`, `onDrop`, `onDragEnd`, `allowGKDrag`, `midmatchDraggable`, `isSubTarget`.

Symbols KEPT unchanged (explicitly NOT drag types): `MidmatchSubMode`, `PendingSubstitution`, `subMode`, `pendingSub`, `rejectForGKRule`, `showDraftRejection`, `isCardGK`, `ownActiveHexKeys`, `resolveTieredCard`, `benchCards`, `StatCardProps.isSubBlocked`, `StatCardProps.showTierBorder`, `StatCardProps.mode`, `StatCardProps.cardColor`, `StatCardProps.injuryCount`.

<tasks>

<task type="auto">
  <name>Task 1: Port LineupStatCard and both mid-match surfaces to click-select</name>
  <files>packages/client/src/components/LineupAssignmentScreen.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx (full file — types at 125-164, `StatCardProps`/`LineupStatCard` at 178-350, mid-match state at 510-528, handlers at 530-581, `ownActiveHexKeys` at 591-597, `renderMidmatchColumn` at 599-771, the mid-match render branch at 1012-1239 including the mode-toggle buttons at 1146-1194)
    - packages/client/src/components/BenchCarousel.tsx (the contract from 47-02: `onCardClick`, `onBenchAreaClick?`, `selectedCardId?`, `benchAreaEligible?`)
    - packages/client/src/store/useGameStore.ts — the `selectPiece` action (~line 772) is the reference 3-branch toggle shape being ported
    - packages/client/src/components/PieceOverlay.tsx (click-gating idiom; `ACTIVE_RING_STROKE`)
    - .planning/phases/47-select-based-roster-interaction/47-CONTEXT.md (D-03 through D-10)
    - .planning/phases/47-select-based-roster-interaction/47-RESEARCH.md (Pitfalls 1, 2, 3)
  </read_first>
  <action>
**Types.** Delete `MidmatchDragState`. Do NOT introduce a mid-match tagged union to replace it — deliberately use two independent scalars instead (this is the D-10 discretion call, and it structurally enforces the D-07/D-08 asymmetry that a shared union would invite collapsing). Keep `MidmatchSubMode` and `PendingSubstitution` verbatim.

**State.** Delete `midmatchDrag` and `midmatchDropTargetPieceId`. Add `const [repositionSelectedPieceId, setRepositionSelectedPieceId] = useState<string | null>(null)` and `const [substituteSelectedPlayerId, setSubstituteSelectedPlayerId] = useState<string | null>(null)`. Keep `subMode` and `pendingSub` exactly as they are.

**`StatCardProps` / `LineupStatCard`.** Remove `isDragSource`, `isDropTarget`, `onDragStart`, `onDragOver`, `onDragLeave`, `onDrop`, `onDragEnd`, `isSubTarget`. Add `isSelected: boolean`, `isEligibleTarget: boolean`, `onClick: () => void`, and `isSelectable?: boolean` (the rename of `midmatchDraggable` — same parent-computed value, now gating click instead of the `draggable` attribute). Rename `allowGKDrag?: boolean` to `allowGKSelect?: boolean` (ROSTER-06 requires no drag identifier survives; the flag itself is a GK-lock rule, not a drag concept). Keep `isSubBlocked?`, `showTierBorder?`, `mode?`, `cardColor?`, `injuryCount?`, `player`, `slotMeta?`, `slotIndex`, `lineupConfirmed`, `teamId`.

Rename the local `isDraggable` to `isInteractive` and keep its three-way structure verbatim with the renamed inputs: mid-match uses `isSelectable === true`; otherwise `allowGKSelect ? !lineupConfirmed : !isGK && !lineupConfirmed`. Keep the existing `isGK` derivation (`isMidmatch ? player.role === 'GK' : slotIndex === 0`) and the `isGK && !allowGKSelect` LOCK badge condition untouched apart from the rename.

Update the `cardClass` if/else chain preserving its exact branch order: mid-match branch starts from `styles.statCard`, appends `styles.statCardSubBlocked` when `isSubBlocked`, then appends `styles.statCardSelected` when `isSelected` else `styles.statCardEligible` when `isEligibleTarget` (selected wins; never both). The non-mid-match branches keep `statCardLocked` (GK without `allowGKSelect`) and `statCardConfirmed` (lineupConfirmed) ahead of everything, then `styles.statCard` plus `styles.statCardSelected` when `isSelected`, plus `styles.statCardEligible` when `isEligibleTarget`, else bare `styles.statCard`. Keep the `showTierBorder` → `TIER_CARD_CLASS[tier]` composition afterwards, unchanged.

Replace the wrapper `<div>`'s six drag attributes with: `data-roster-card` present unconditionally on every card (the stable locator replacing `[draggable]` for tests and for the eligible-target sweep); `onClick={onClick}` attached only when `isInteractive === true || isEligibleTarget === true`, omitted entirely otherwise (D-04 silent no-op, mirroring `PieceOverlay`'s click-gating); `style={{ cursor: isInteractive || isEligibleTarget ? 'pointer' : 'default' }}`; and, under the same clickable condition, `role="button"`, `tabIndex={0}`, and an `onKeyDown` firing `onClick` on `Enter` or `' '` with `preventDefault()` for Space (UI-SPEC §Accessibility). Attach none of `role`/`tabIndex`/`onKeyDown` when the card is not clickable. Leave the card's inner markup (TeamBadge, cardBody, cardHeader, CardInjuryBadge, LOCK badge, statGrid) completely unchanged.

**Positioning-mode eligibility and handler (ROSTER-05: keep structurally separate from substitution — share no guard body).** Delete `handleMidmatchDragStart` and `handleMidmatchRepositionDrop`.

Add `isRepositionSelectable(piece)` expressing today's `midmatchDraggable` conditions verbatim: `subMode === 'reposition' && readOnly !== true && actionPending !== true && isActivePiece(piece) && slotIndexNum !== 0 && piece.role !== 'GK' && pendingSub === null`, where `slotIndexNum` comes from the existing `/-(\d+)$/` parse of `piece.id`. This is D-09: the GK card is never selectable in positioning mode.

Add `isRepositionEligible(sourcePieceId, targetPieceId)` returning true when `subMode === 'reposition'`, `readOnly !== true`, `actionPending !== true`, `pendingSub === null`, a selection exists, and `targetPieceId !== sourcePieceId`. Port today's behaviour faithfully: EVERY rendered mid-match card other than the source is an eligible target, INCLUDING the GK card — today's `onDragOver` applies no GK check to the target and the server's `GK_SLOT_LOCKED` rejection produces the existing 'Swap rejected — GK cannot be moved.' message. Do not add a new client-side target-side GK exclusion; that would silently delete a live feedback path. D-09 restricts GK selectability only.

Add `handleRepositionCardClick(pieceId)` with the three-branch `selectPiece` shape: if `repositionSelectedPieceId === pieceId`, clear the selection and return (ROSTER-03); else if `repositionSelectedPieceId !== null`, return without changing state when `!isRepositionEligible(repositionSelectedPieceId, pieceId)` (D-04 no-op — the green selection and blue targets stay exactly as they are), otherwise call `onReposition?.(repositionSelectedPieceId, pieceId)` and clear the selection (ROSTER-04, fires synchronously with no confirm popup, preserving the deliberate D-02 asymmetry with substitution); else select `pieceId` only when it is reposition-selectable, otherwise do nothing. D-08 is load-bearing: there is NO "switch selection" branch here — with a card already selected, clicking a different selectable card runs the eligible-target/complete path, never a re-select.

**Substitution-mode eligibility and handlers (structurally separate; two functions, bench-first per D-06).** Delete `handleMidmatchSubstituteDrop` but preserve its staging body verbatim inside the new pitch-click handler.

Add `isSubstituteEligible(piece)` returning true when `subMode === 'substitute'`, `readOnly !== true`, `pendingSub === null`, `substituteSelectedPlayerId !== null`, `isActivePiece(piece)`, and `piece.redCarded !== true`.

Add `handleSubstituteBenchClick(benchIndex)`: return early when `readOnly === true || subMode !== 'substitute' || pendingSub !== null` (today's `onCardDragStart` guard set); resolve `const card = midmatchBenchCards[benchIndex]` and return if absent; if `substituteSelectedPlayerId === card.id` clear the selection and return (explicit deselect); otherwise set the selection to `card.id` unconditionally — this is D-07's "switch selection" behaviour, deliberately asymmetric with positioning mode (D-08), and it must be implemented here and only here. Because `handleSubstituteBenchClick` needs `midmatchBenchCards`, define it inside the `mode === 'midmatch'` render branch (where `midmatchBenchCards` already exists) or hoist the lookup — do not duplicate the bench-card derivation.

Add `handleSubstitutePitchClick(piece)`: return immediately when `substituteSelectedPlayerId === null` (D-06 bench-first only — an on-pitch click with nothing selected does nothing); return when `!isSubstituteEligible(piece)` (D-04); then run today's staging body unchanged — look up `PLAYER_MAP.get(substituteSelectedPlayerId)`, return if absent so the popup never renders a blank name, build `inName` as `${firstName} ${lastName}`, and `setPendingSub({ outPieceId: piece.id, inPlayerId: substituteSelectedPlayerId, outName: \`${piece.firstName} ${piece.lastName}\`, outNumber: piece.number, inName })`. Clear `substituteSelectedPlayerId` after staging. `onSubstitute` is still called ONLY from the existing confirmation popup's Confirm button — do not add a second call site.

**`renderMidmatchColumn`.** Replace the `isDropTargetHere` derivation (`midmatchDropTargetPieceId === piece.id`) with a per-mode eligible-target derivation computed from the active selection: in `reposition` mode, `repositionSelectedPieceId !== null && isRepositionEligible(repositionSelectedPieceId, piece.id)`; in `substitute` mode, `isSubstituteEligible(piece)`. Pass it as `isEligibleTarget`, and pass `isSelected={subMode === 'reposition' && repositionSelectedPieceId === piece.id}`, `isSelectable={isRepositionSelectable(piece)}`, `isSubBlocked={isBlocked}`, and `onClick={() => (subMode === 'reposition' ? handleRepositionCardClick(piece.id) : handleSubstitutePitchClick(piece))}`. Remove the five drag callbacks from the `LineupStatCard` call site. Delete the now-unused `isSubTarget` prop pass-through.

**SENT OFF placeholder (D-05, RESEARCH.md Pitfall 3 — the single easiest state to drop in this port).** The placeholder is a bespoke `<div>` at ~lines 637-686, NOT a `LineupStatCard`. Keep `key`, `className` base `styles.statCardSentOff`, `aria-label="Sent off — slot empty"`, `role="img"`, and the `.sentOffBadge` child exactly as they are; also add `data-roster-card` so the rewritten tests share one locator. Keep the `sentOffSlotHexTaken` derivation from `ownActiveHexKeys` verbatim. Remove `draggable={false}`, `onDragOver`, `onDragLeave`, `onDrop`.

Compute its eligibility as: `subMode === 'reposition' && repositionSelectedPieceId !== null && repositionSelectedPieceId !== piece.id && !sentOffSlotHexTaken && readOnly !== true && actionPending !== true && pendingSub === null`. When true, append `styles.statCardEligible` to its className and attach `onClick={() => handleRepositionCardClick(piece.id)}`; when false, append no class and attach no `onClick`. In `substitute` mode the placeholder is NEVER eligible, never highlighted, and never clickable — this is a deliberate behaviour change from the drag implementation, whose `onDragOver` marked it a target unconditionally in substitute mode (D-05 supersedes that). Deliberate accessibility exception, to be recorded in the SUMMARY: the placeholder keeps `role="img"` and gains no `tabIndex`/`onKeyDown` even when eligible — it is a status indicator, and its existing `role`/`aria-label` are asserted by current tests; keyboard users can still complete repositions between two active cards.

**Mode-toggle clearing (RESEARCH.md Anti-Pattern: selection must not survive a mode toggle).** In the "Substitute" button's `onClick` (~line 1157) and the "Cancel" button's `onClick` (~line 1173), replace `setMidmatchDrag(null)` / `setMidmatchDropTargetPieceId(null)` with `setRepositionSelectedPieceId(null)` AND `setSubstituteSelectedPlayerId(null)`. Both new state variables must be cleared in BOTH buttons — do not assume the old partial-clear behaviour is sufficient. Keep `setPendingSub(null)` in both and keep `setSubMode(...)` unchanged. Also clear both selections in the substitution-confirm popup's Confirm handler (which already calls `setPendingSub(null); setSubMode('reposition');`) and in its Cancel handler.

**Container-level cleanup.** Delete the mid-match root `<div>`'s `onDragEnd={...}` handler entirely — there is no drag gesture whose end needs cleanup under click-select.

**`BenchCarousel` mid-match call site (~line 1117).** Keep `cards`, `teamId`, `benchNumbers`, `unavailablePlayerIds`, `redCardedPlayerIds`, `benchCardStatus`, and the existing `disabled={readOnly === true || subMode === 'reposition' || pendingSub !== null}` expression verbatim. Replace `onCardDragStart` with `onCardClick={handleSubstituteBenchClick}`, pass `selectedCardId={substituteSelectedPlayerId}`, and pass neither `onBenchAreaClick` nor `benchAreaEligible` — mid-match has no bench-as-completion-target semantics (today's `onDropToBench={() => setMidmatchDrag(null)}` was pure drag cleanup with no user-visible effect).

**Helper copy (deliberate, required extension of the UI-SPEC Copywriting Contract).** The two mid-match helper strings at ~lines 1085-1087 describe the retired gesture and would be factually wrong after this port. Change `'Drag a player onto another to swap positions.'` to `'Select a player, then click another to swap positions.'` and `'Drag a bench card onto an on-pitch card to Substitute.'` to `'Select a bench card, then click an on-pitch card to substitute.'`. Leave the `readOnly` string ('Viewing roster — substitutions are only available during a stoppage in play.') unchanged. Record this copy change in the SUMMARY as the one user-facing copy delta in the phase; the UI-SPEC's "no new copy" line covers CTAs and error states, not gesture-describing helper text.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'handleRepositionCardClick\|handleSubstituteBenchClick\|handleSubstitutePitchClick\|isRepositionEligible\|isSubstituteEligible' packages/client/src/components/LineupAssignmentScreen.tsx && grep -c 'midmatchDrag\|MidmatchDragState\|handleMidmatchDragStart\|handleMidmatchRepositionDrop\|handleMidmatchSubstituteDrop' packages/client/src/components/LineupAssignmentScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'midmatchDrag\|MidmatchDragState\|midmatchDropTargetPieceId\|handleMidmatchDragStart\|handleMidmatchRepositionDrop\|handleMidmatchSubstituteDrop\|midmatchDraggable\|allowGKDrag\|isDragSource\|isDropTarget\|isSubTarget' packages/client/src/components/LineupAssignmentScreen.tsx` outputs `0`
    - Each of `isRepositionSelectable`, `isRepositionEligible`, `handleRepositionCardClick`, `isSubstituteEligible`, `handleSubstituteBenchClick`, `handleSubstitutePitchClick` appears in the file
    - `grep -c 'setRepositionSelectedPieceId(null)' packages/client/src/components/LineupAssignmentScreen.tsx` is `>= 4` and `grep -c 'setSubstituteSelectedPlayerId(null)'` is `>= 4` (both cleared in the Substitute toggle, the Cancel toggle, and both popup buttons)
    - `grep -c 'Select a player, then click another to swap positions.' packages/client/src/components/LineupAssignmentScreen.tsx` outputs `1` and `grep -c 'Select a bench card, then click an on-pitch card to substitute.'` outputs `1`
    - `grep -c 'Drag a ' packages/client/src/components/LineupAssignmentScreen.tsx` outputs `0`
    - `grep -c 'statCardEligible' packages/client/src/components/LineupAssignmentScreen.tsx` is `>= 1` (the SENT OFF placeholder branch)
    - `grep -c 'aria-label="Sent off — slot empty"' packages/client/src/components/LineupAssignmentScreen.tsx` outputs `1` and `role="img"` is still present on that div
    - `grep -c 'onCardClick={handleSubstituteBenchClick}' packages/client/src/components/LineupAssignmentScreen.tsx` outputs `1`
    - `onReposition?.(` appears exactly once and `onSubstitute?.(` appears exactly once in the file
  </acceptance_criteria>
  <done>Both mid-match modes select, deselect, highlight, and complete by click with two structurally separate eligibility computations; the SENT OFF placeholder is a positioning-mode-only blue target; selection clears on every mode transition.</done>
</task>

<task type="auto">
  <name>Task 2: Port the Standard pregame and draft-mode surfaces to click-select</name>
  <files>packages/client/src/components/LineupAssignmentScreen.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx (current state after Task 1 — pregame handlers at ~801-831, `renderColumn` at ~835-860, draft state at ~440-441, `rejectForGKRule`/`showDraftRejection` at ~890-907, draft handlers at ~909-967, `renderDraftColumn` at ~972-1010, draft render branch at ~1241-1354, pregame render branch at ~1372-1423)
    - packages/client/src/components/BenchCarousel.tsx (contract from 47-02)
    - packages/client/src/components/DraftPackCarousel.tsx (contract from 47-01)
    - .planning/phases/47-select-based-roster-interaction/47-CONTEXT.md (D-10, D-11, D-12)
    - .planning/phases/47-select-based-roster-interaction/47-RESEARCH.md (§Code Examples — the pregame and pack-card 1:1 mappings)
  </read_first>
  <action>
**Standard pregame surface (ROSTER-07 / D-12).** Delete `dragSourceIndex`, `dropTargetIndex`, `handleDragStart`, `handleDragOver`, `handleDragLeave`, `handleDrop`, `handleDragEnd`. Add `const [pregameSelectedSlotIndex, setPregameSelectedSlotIndex] = useState<number | null>(null)`.

Add `isPregameSwapEligible(sourceIdx, targetIdx)` returning `lineupConfirmed !== true && targetIdx !== 0 && sourceIdx !== targetIdx` — the GK-slot exclusion (`idx === 0`) is today's `handleDragOver`/`handleDrop` rule carried over verbatim.

Add `handlePregameCardClick(idx)` with the same three-branch shape: return immediately when `lineupConfirmed` is true; if `pregameSelectedSlotIndex === idx`, clear and return; else if `pregameSelectedSlotIndex !== null`, return when `!isPregameSwapEligible(pregameSelectedSlotIndex, idx)` (D-04 no-op), otherwise call `onSwap(pregameSelectedSlotIndex, idx)` and clear; else set the selection to `idx` only when `idx !== 0` (the GK slot is never selectable — mirrors the existing `!isGK && !lineupConfirmed` draggability rule).

In `renderColumn`, replace the drag props on `LineupStatCard` with `isSelected={pregameSelectedSlotIndex === slotIndex}`, `isEligibleTarget={pregameSelectedSlotIndex !== null && isPregameSwapEligible(pregameSelectedSlotIndex, slotIndex)}`, and `onClick={() => handlePregameCardClick(slotIndex)}`. Do not pass `allowGKSelect` here (Standard mode keeps the permanent GK lock). Keep `key`, `player`, `slotMeta`, `slotIndex`, `lineupConfirmed`, `teamId` unchanged.

In the pregame render branch's `BenchCarousel` (~line 1394), the bench is inert: keep `disabled`, replace `onCardDragStart={() => {}}` with `onCardClick={() => {}}`, and drop `onDropToBench` entirely (now optional).

**Draft-mode surface (ROSTER-08 / D-11).** Delete `dragState`, `draftDropTargetIndex`, `handleDraftSlotDragStart`, `handleDraftSlotDragOver`, `handleDraftSlotDragLeave`, `handleDraftSlotDrop`, `handleDraftSlotDragEnd`, `handleDropToBench`. Rename the `DragState` type to `DraftSelection` keeping its three members byte-identical (`{ cardId; source: 'pack' }` | `{ cardId; source: 'slot'; slotIndex }` | `{ cardId; source: 'bench'; benchIndex }`) and update its doc comment to describe a single parent-owned SELECTION that resolves every click. Add `const [draftSelection, setDraftSelection] = useState<DraftSelection | null>(null)`.

Extract a pure predicate `violatesGKRule(slotIndex, cardId): boolean` containing only the two conditions currently inside `rejectForGKRule` — `slotIndex === 0 && !isCardGK(cardId)`, or `slotIndex !== 0 && isCardGK(cardId)`. Rewrite `rejectForGKRule` to call `violatesGKRule` and, on a violation, emit the SAME two existing messages via `showDraftRejection` ('Swap rejected — only a goalkeeper card can be placed here.' for a non-GK card on slot 0; 'Swap rejected — goalkeeper slot requires a GK card.' for a GK card on a non-GK slot) and return true. This split is required because `rejectForGKRule` has a message side effect and must never run during render-time eligibility computation.

Add `isDraftSlotEligible(selection, slotIndex)`: true when a selection exists, `lineupConfirmed !== true`, and NOT (`selection.source === 'slot' && selection.slotIndex === slotIndex`) — i.e. every slot other than the selection's own origin slot. Deliberately do NOT consult `violatesGKRule` here: today's `handleDraftSlotDragOver` applies no GK check when highlighting, and the GK rule is enforced at completion time by `rejectForGKRule`'s user-facing message. Excluding GK-violating slots from the blue set would make that message unreachable and would silently delete tested feedback.

Add `isDraftBenchAreaEligible(selection)`: true when a selection exists, `lineupConfirmed !== true`, and `selection.source !== 'bench'` — today's `handleDropToBench` no-ops for a bench-sourced drag, and pack→bench and slot→bench are the two legal bench completions.

Add `handleDraftPackCardClick(cardId)`: return when `draftView.waitingForOpponent` is true or `lineupConfirmed` is true; if `draftSelection?.source === 'pack' && draftSelection.cardId === cardId`, clear and return (deselect); otherwise set `{ cardId, source: 'pack' }`. Per D-11 pack cards mirror the bench-first substitution pattern, so a click on a different pack card always switches the selection — no explicit deselect step.

Add `handleDraftSlotClick(slotIndex, cardIdAtSlot)`: takes the slot index and the card currently occupying it (null for an empty slot). If `draftSelection` is null: select the slot as a source only when `cardIdAtSlot !== null` and `lineupConfirmed !== true`, setting `{ cardId: cardIdAtSlot, source: 'slot', slotIndex }`; an empty slot with nothing selected is a no-op. If `draftSelection?.source === 'slot' && draftSelection.slotIndex === slotIndex`: clear the selection and return (ROSTER-03 deselect). Otherwise, this is a completion attempt: return when `!isDraftSlotEligible(draftSelection, slotIndex)` (D-04); call `rejectForGKRule(slotIndex, draftSelection.cardId)` and, if it returns true, clear the selection and return (message already shown); else dispatch exactly as `handleDraftSlotDrop` does today — `pack` source calls `onDraftPick?.(cardId, { type: 'slot', slotIndex })`; `slot` source calls `onDraftRearrange?.({ type: 'slot', slotIndex: selection.slotIndex }, { type: 'slot', slotIndex })`; `bench` source calls `onDraftRearrange?.({ type: 'bench', benchIndex: selection.benchIndex }, { type: 'slot', slotIndex })` — then clear the selection.

Add `handleDraftBenchCardClick(benchIndex)`: resolve `const cardId = draftView.benchIds[benchIndex]` and return if absent; return when `lineupConfirmed` is true; if the current selection is that same bench entry (`source === 'bench' && benchIndex` matches), clear and return; otherwise set `{ cardId, source: 'bench', benchIndex }`. Per D-11 a bench card is a swap-pattern source like a filled slot; clicking a different bench card while one is selected re-selects it (bench cards are never completion targets in draft mode — the bench AREA is).

Add `handleDraftBenchAreaClick()`: return when `draftSelection` is null or `!isDraftBenchAreaEligible(draftSelection)`; `pack` source calls `onDraftPick?.(draftSelection.cardId, { type: 'bench' })`; `slot` source calls `onDraftRearrange?.({ type: 'slot', slotIndex: draftSelection.slotIndex }, { type: 'bench', benchIndex: draftView.benchIds.length })` — the append-position semantics from today's `handleDropToBench`, unchanged; then clear the selection.

In `renderDraftColumn`, the filled-slot `LineupStatCard` gets `isSelected={draftSelection?.source === 'slot' && draftSelection.slotIndex === slotIndex}`, `isEligibleTarget={draftSelection !== null && isDraftSlotEligible(draftSelection, slotIndex)}`, `onClick={() => handleDraftSlotClick(slotIndex, player.id)}`, and keeps `allowGKSelect` (the renamed `allowGKDrag`) and `showTierBorder`. The empty-slot `<div className={styles.benchSlot} data-slot-index={slotIndex}>` keeps its `data-slot-index` attribute (existing tests locate empty slots by it), loses `onDragOver`/`onDragLeave`/`onDrop`, appends `styles.statCardEligible` to its className when `draftSelection !== null && isDraftSlotEligible(draftSelection, slotIndex)`, and gets `onClick={() => handleDraftSlotClick(slotIndex, null)}` plus `role="button"`, `tabIndex={0}` and an Enter/Space `onKeyDown` only under that same eligible condition.

In the draft render branch: delete the root `<div>`'s `onDragEnd={handleDraftSlotDragEnd}`. `DraftPackCarousel` gets `onCardClick={handleDraftPackCardClick}` and `selectedCardId={draftSelection?.source === 'pack' ? draftSelection.cardId : null}`, keeping `cards`, `teamId`, `disabled={draftView.waitingForOpponent}`. `BenchCarousel` gets `onCardClick={handleDraftBenchCardClick}`, `onBenchAreaClick={handleDraftBenchAreaClick}`, `benchAreaEligible={draftSelection !== null && isDraftBenchAreaEligible(draftSelection)}`, and `selectedCardId={draftSelection?.source === 'bench' ? draftSelection.cardId : null}`, keeping `cards={benchCards}`, `teamId`, `benchNumbers={draftView.benchNumbers}`.

Leave `cardCache`, `resolveTieredCard`, `benchCards`, the draft column-building loop, `roundLabel`/`roundPicks`, the Confirm-section gating, `rejectionMessage` wiring, and the `gameError` effect completely untouched.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'handlePregameCardClick\|isPregameSwapEligible\|violatesGKRule\|isDraftSlotEligible\|isDraftBenchAreaEligible\|handleDraftPackCardClick\|handleDraftSlotClick\|handleDraftBenchCardClick\|handleDraftBenchAreaClick' packages/client/src/components/LineupAssignmentScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dragSourceIndex\|dropTargetIndex\|dragState\|draftDropTargetIndex\|DragState\|handleDragStart\|handleDragOver\|handleDragLeave\|handleDrop\|handleDragEnd\|handleDraftSlot\|handleDropToBench' packages/client/src/components/LineupAssignmentScreen.tsx` outputs `0`
    - Each of `isPregameSwapEligible`, `handlePregameCardClick`, `violatesGKRule`, `isDraftSlotEligible`, `isDraftBenchAreaEligible`, `handleDraftPackCardClick`, `handleDraftSlotClick`, `handleDraftBenchCardClick`, `handleDraftBenchAreaClick`, `DraftSelection`, `draftSelection` appears in the file
    - `rejectForGKRule` and `showDraftRejection` are still present, and both rejection strings ('Swap rejected — only a goalkeeper card can be placed here.' and 'Swap rejected — goalkeeper slot requires a GK card.') still appear exactly once each
    - `grep -c 'data-slot-index' packages/client/src/components/LineupAssignmentScreen.tsx` outputs `1`
    - `onSwap(` appears exactly once, `onDraftPick?.(` appears exactly twice (slot + bench), `onDraftRearrange?.(` appears exactly three times (slot→slot, bench→slot, slot→bench)
    - `grep -c 'onBenchAreaClick={handleDraftBenchAreaClick}\|benchAreaEligible=' packages/client/src/components/LineupAssignmentScreen.tsx` is `>= 2`
  </acceptance_criteria>
  <done>Standard pregame swap and every draft pack/slot/bench interaction complete by click, with GK-slot rules, swap-vs-move semantics, and rejection messages preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Purge residual drag code and prove the client still builds</name>
  <files>packages/client/src/components/LineupAssignmentScreen.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx (current state after Tasks 1 and 2 — full file sweep)
    - .planning/ROADMAP.md (Phase 47 success criterion 5: no drag-and-drop state, handlers, or types remain in this file)
    - .planning/phases/47-select-based-roster-interaction/47-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
Sweep the whole file for any remaining drag artifact and remove it: the identifiers `draggable`, `dataTransfer`, `DragEvent`, `onDragStart`, `onDragOver`, `onDragLeave`, `onDragEnd`, `onDrop`, `effectAllowed`, `DragState`, `MidmatchDragState`, `dragSourceIndex`, `dropTargetIndex`, `midmatchDrag`, `midmatchDropTargetPieceId`, `draftDropTargetIndex`, `isDragSource`, `isDropTarget`, `isSubTarget`, `midmatchDraggable`, `allowGKDrag`, `onCardDragStart`, `onDropToBench`, and the word `drag`/`Drag` in any prop, state, function, or type name.

Also update prose that describes the retired mechanism, since ROSTER-06 is about the file reading as a click-select component: the module header doc comment (line 5's "supports HTML5 native drag-to-swap on outfield cards (GK locked)") becomes a click-to-select description naming Phase 47; the `onSwap` prop doc ("Called when the player drops a card onto another") becomes click-completion wording; the `onDraftPick` doc ("emitted when a pack-row card is dropped onto a lineup slot or the bench") and `onReposition` doc ("Called when a positioning-mode drag lands on another on-pitch card") likewise; the `MidmatchSubMode` doc comment's "the two modes' drop handlers are kept as two structurally separate functions sharing no guard body" becomes "click handlers"; the `PendingSubstitution` doc's "rather than firing synchronously on drop like handleMidmatchRepositionDrop" is repointed at `handleRepositionCardClick`; the `actionPending` prop doc's "disables positioning-mode dragging while true" becomes "disables positioning-mode selection while true"; the `readOnly` prop doc's "bench cards are never draggable and a drop can never call onSubstitute" becomes the click equivalent. Comments explaining WHY a rule exists (Pitfall 5 separation, D-05/D-06 rationale, gap-item-6 occupancy pre-gate, formation-shape grouping) must be preserved — only the gesture nouns change.

Import hygiene: confirm no `React.DragEvent` type import or `import type { DragEvent }` remains, and that every state setter and helper introduced in Tasks 1 and 2 is actually referenced (no orphaned `useState`).

Then prove the client application graph still compiles by running the Vite build. Do NOT run `pnpm typecheck` as a gate in this plan: `packages/client/tsconfig.json` includes `src/**/*`, which pulls in `LineupAssignmentScreen.test.tsx`, `GameBoard.test.tsx`, and `CardInjuryBadge.crossSurface.test.tsx` — all three still reference the removed drag props and are rewritten in plans 47-04 and 47-05. `vite build` compiles only the `index.html` entry graph and is the correct gate here.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -ci 'drag\|dataTransfer\|effectAllowed' packages/client/src/components/LineupAssignmentScreen.tsx; pnpm --filter @counter-attack/client build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci 'drag\|dataTransfer\|effectAllowed' packages/client/src/components/LineupAssignmentScreen.tsx` outputs `0` (case-insensitive, comments included — this is ROADMAP Phase 47 success criterion 5)
    - `pnpm --filter @counter-attack/client build` exits 0
    - `grep -c 'onSwap\|onConfirm\|onDraftPick\|onDraftRearrange\|onSubstitute\|onReposition' packages/client/src/components/LineupAssignmentScreen.tsx` is `>= 12` (every server-emit callback prop is still declared and still called — no emit path was dropped)
    - `grep -c 'MidmatchSubMode\|PendingSubstitution\|rejectForGKRule\|showDraftRejection\|ownActiveHexKeys\|isActivePiece' packages/client/src/components/LineupAssignmentScreen.tsx` is `>= 8` (the explicitly-kept non-drag machinery survived)
  </acceptance_criteria>
  <done>`LineupAssignmentScreen.tsx` contains zero case-insensitive occurrences of "drag" (code or prose), every server-emit callback is intact, and `vite build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (click/keydown) → React component state | Untrusted gesture crosses into client selection state |
| client → Socket.io server (`GAME_ROSTER_REPOSITION`, `GAME_SUBSTITUTION`, `LINEUP_SWAP`, `DRAFT_PICK`, `DRAFT_REARRANGE`) | Existing boundary — crossed by the same callbacks as today, triggered by a click instead of a drop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-08 | Tampering | Client-side eligibility treated as authority | accept (already mitigated upstream) | Explicitly stated per CONTEXT.md Integration Points: this phase swaps the client input mechanism ONLY. `applyRosterReposition`, `applySubstitution`, and the `LINEUP_SWAP`/`DRAFT_PICK`/`DRAFT_REARRANGE` handlers are untouched and remain the sole authority. No new server validation is needed, and client eligibility remains a UX mirror that a crafted socket payload cannot exploit any more than it could before this phase. |
| T-47-09 | Tampering | Stale / stomped selection across rapid clicks | mitigate | All four selections are parent-owned `useState` scalars/unions read at click time; each completion handler clears its own selection on the same code path that fires the emit, and every handler re-checks eligibility against the CURRENT selection before emitting (never against a value captured at highlight time). Additionally, the server's per-room `isProcessing` mutex (STATE.md Decisions Locked) already serialises double-fire attempts. |
| T-47-10 | Tampering | Selection surviving a Reposition↔Substitute mode toggle and being reused as a swap participant | mitigate | Both `repositionSelectedPieceId` and `substituteSelectedPlayerId` are cleared in BOTH mode-toggle buttons and in both substitution-popup buttons (Task 1). This is RESEARCH.md's named anti-pattern and is regression-tested in plan 47-04. |
| T-47-11 | Elevation of Privilege | Selecting the GK in positioning mode, or a locked/confirmed/read-only card, to force an illegal move | mitigate | `isRepositionSelectable` reproduces the server's `GK_SLOT_LOCKED` guard clauses (`slotIndexNum !== 0 && piece.role !== 'GK'`) verbatim, and no `onClick`/`tabIndex`/`role="button"` is attached to non-interactive cards, so locked cards are neither clickable nor focusable. Server-side `GK_SLOT_LOCKED` remains the backstop for the target-side case, whose existing rejection message is deliberately preserved. |
| T-47-12 | Information Disclosure | Widened eligibility accidentally exposing another team's roster surface | accept | Unchanged — `LineupAssignmentScreen` renders only this socket's own assignment/pieces/bench/draftView (D-16: the server never sends the opponent's lineup to this socket). This plan adds no new data source. |
| T-47-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed this phase (RESEARCH.md §Environment Availability). No `[ASSUMED]`/`[SUS]` legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm --filter @counter-attack/client build` exits 0.
- `grep -ci 'drag' packages/client/src/components/LineupAssignmentScreen.tsx` returns 0.
- `pnpm stylelint` still exits 0.
- `pnpm --filter @counter-attack/client typecheck` and the `LineupAssignmentScreen`/`GameBoard`/`CardInjuryBadge.crossSurface` test suites are EXPECTED TO FAIL after this plan — those files are rewritten in 47-04 and 47-05. Do not edit any `.test.tsx` file in this plan.
</verification>

<success_criteria>
- All four surfaces (mid-match positioning, mid-match substitution, Standard pregame, draft) select / deselect / highlight / complete by click.
- Four structurally separate eligibility computations exist (`isRepositionEligible`, `isSubstituteEligible`, `isPregameSwapEligible`, `isDraftSlotEligible` + `isDraftBenchAreaEligible`) — none takes a mode parameter and branches internally.
- The SENT OFF placeholder is a blue eligible target in positioning mode only, and never in substitution mode.
- Zero case-insensitive `drag` occurrences remain in `LineupAssignmentScreen.tsx`.
- `vite build` succeeds.
</success_criteria>

<output>
Create `.planning/phases/47-select-based-roster-interaction/47-03-SUMMARY.md` when done.
</output>
