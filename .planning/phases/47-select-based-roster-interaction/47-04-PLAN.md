---
phase: 47-select-based-roster-interaction
plan: 04
type: execute
wave: 4
depends_on: ["47-03"]
files_modified:
  - packages/client/src/components/LineupAssignmentScreen.test.tsx
autonomous: true
requirements: [ROSTER-01, ROSTER-02, ROSTER-03, ROSTER-04, ROSTER-05, ROSTER-07, ROSTER-08]

must_haves:
  truths:
    - "Every LineupAssignmentScreen behaviour previously proven by drag simulation is proven by click simulation"
    - "Deselect-on-second-click is proven for positioning, pregame, and draft surfaces"
    - "A selection made in Reposition mode does not survive a toggle into Substitute mode"
    - "The D-07 bench selection-switch and the D-08 positioning non-switch are each proven by their own test"
    - "The SENT OFF placeholder is proven eligible in positioning mode and inert in substitution mode"
  artifacts:
    - path: "packages/client/src/components/LineupAssignmentScreen.test.tsx"
      provides: "click-based component coverage for all four roster surfaces plus the new ROSTER-03/05/07 tests"
      contains: "data-roster-card"
  key_links:
    - from: "packages/client/src/components/LineupAssignmentScreen.test.tsx"
      to: "packages/client/src/components/LineupAssignmentScreen.tsx"
      via: "fireEvent.click on [data-roster-card] elements asserting onSwap/onReposition/onSubstitute/onDraftPick/onDraftRearrange"
      pattern: "fireEvent\\.click"
---

<objective>
Rewrite `LineupAssignmentScreen.test.tsx` (1577 lines, ~90 cases) from drag simulation to click simulation, and add the tests the drag model had no equivalent for: deselect-on-second-click (ROSTER-03), mode-crossing selection clearing (ROSTER-05 / RESEARCH.md Pitfall 1), the D-07/D-08 selection-switch asymmetry, and explicit Standard-pregame swap coverage (ROSTER-07).

Purpose: This is the phase's primary behavioural gate. Every ROSTER requirement except ROSTER-06 (static analysis) is proven here.
Output: A fully green, fully click-based `LineupAssignmentScreen.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-select-based-roster-interaction/47-CONTEXT.md
@.planning/phases/47-select-based-roster-interaction/47-VALIDATION.md
@.planning/phases/47-select-based-roster-interaction/47-03-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates no production symbols. It consumes the symbols/attributes created in plans 47-01 through 47-03 (do not treat as drift): `data-roster-card`, `data-interactive`, `.statCardSelected`, `.statCardEligible`, `onCardClick`, `onBenchAreaClick`, `selectedCardId`, `benchAreaEligible`, `isSelected`, `isEligibleTarget`, `isSelectable`, `allowGKSelect`, `DraftSelection`, and the `handle*CardClick` / `is*Eligible` function family in `LineupAssignmentScreen.tsx`.

### Locator and gesture contract (use these exact substitutions)

| Old | New |
|---|---|
| `.closest('[draggable]')` | `.closest('[data-roster-card]')` |
| `.closest('[role="img"]')` (SENT OFF placeholder) | `.closest('[data-roster-card]')`, or keep the `role="img"` locator — the placeholder retains both |
| `fireEvent.dragStart(a, {...}); fireEvent.drop(b, {...})` | `fireEvent.click(a); fireEvent.click(b)` |
| `fireEvent.dragOver(el, {...})` asserting a gold drop-target class | assert a class matching `/statCardEligible/` after the source click |
| `fireEvent.dragEnd(el, {...})` | no equivalent — assert the selection cleared after completion instead |
| `getAttribute('draggable')` | `getAttribute('data-interactive')` |
| bench drop target | `screen.getByTestId('bench-carousel')` clicked directly |
| empty draft slot | `container.querySelector('[data-slot-index="N"]')` clicked directly (attribute preserved) |

<tasks>

<task type="auto">
  <name>Task 1: Convert the draft-mode describe blocks to click simulation</name>
  <files>packages/client/src/components/LineupAssignmentScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.test.tsx (lines 1-373 — imports, `tieredCard`, `makeDraftView`, `NOOP`, and every draft describe block)
    - packages/client/src/components/LineupAssignmentScreen.tsx (the draft-mode handlers written in 47-03: `handleDraftPackCardClick`, `handleDraftSlotClick`, `handleDraftBenchCardClick`, `handleDraftBenchAreaClick`, `isDraftSlotEligible`, `isDraftBenchAreaEligible`, `violatesGKRule`, `rejectForGKRule`)
    - packages/client/src/components/DraftPackCarousel.test.tsx (the click-test idioms established in 47-01)
  </read_first>
  <action>
Update the shared render helpers first: every `render(<LineupAssignmentScreen ... />)` call in this file passes drag callbacks that no longer exist. Replace `onCardDragStart`/`onDropToBench` usages and any prop spread that references them.

Rewrite the describe block currently named 'LineupAssignmentScreen — DRAFT-06/D-05: drag-to-pick' (~line 100) as 'LineupAssignmentScreen — DRAFT-06/D-05/ROSTER-08: click-to-pick'. Its existing test fires `dragStart` on a pack card then `drop` on `[data-slot-index="2"]` and asserts `onDraftPick` was called with `{ type: 'slot', slotIndex: 2 }` — replace with `fireEvent.click(packCardEl)` then `fireEvent.click(emptySlot)` and keep the identical `onDraftPick` assertion.

Rewrite 'LineupAssignmentScreen — DRAFT-09 gap-closure: drag-state never wedges' (~line 296) as a selection-lifecycle block: after a completed pack→slot click sequence the selection must be cleared, so a subsequent lone click on a different empty slot must NOT call `onDraftPick` a second time. This replaces the `dragEnd`-based wedge test with the click-model equivalent invariant.

Add these draft-mode tests (ROSTER-01/02/03/04/08, D-11):
1. Clicking a pack card applies a class matching `/statCardSelected/` to that card's `[data-roster-card]` wrapper, and to no other card.
2. With a pack card selected, every filled lineup-slot card and every empty `[data-slot-index]` slot carries a class matching `/statCardEligible/`, and `screen.getByTestId('bench-carousel')` carries it too.
3. Clicking the selected pack card again clears the selection: no element carries `/statCardSelected/` and no element carries `/statCardEligible/`.
4. Clicking a different pack card while one is selected switches the selection (D-11 pack cards mirror the bench-first pattern) — the second card carries `/statCardSelected/` and the first does not, and `onDraftPick` was never called.
5. Pack card → bench: click a pack card, then click `screen.getByTestId('bench-carousel')`; assert `onDraftPick` is called once with `{ type: 'bench' }`.
6. Filled slot → other slot: click the card in a filled slot (use `makeDraftView`'s `lineupSlots` — `p001` at slot 0 and `p002` at slot 1), then click another slot; assert `onDraftRearrange` is called once with `({ type: 'slot', slotIndex: from }, { type: 'slot', slotIndex: to })` and `onDraftPick` is NOT called.
7. Filled slot → bench: click a filled non-GK slot card, then click the bench container; assert `onDraftRearrange` is called with `({ type: 'slot', slotIndex }, { type: 'bench', benchIndex: draftView.benchIds.length })`.
8. Bench → slot: click a bench card, then click an empty slot; assert `onDraftRearrange` is called with `({ type: 'bench', benchIndex }, { type: 'slot', slotIndex })`.
9. Bench card selected → the bench container is NOT eligible: it carries no `/statCardEligible/` class and clicking it calls neither `onDraftPick` nor `onDraftRearrange` (preserves today's bench→bench no-op).
10. Clicking a slot card while the selection's own slot is that same slot deselects it (ROSTER-03).
11. GK rule preserved: with a non-GK pack card selected, clicking slot 0 shows 'Swap rejected — only a goalkeeper card can be placed here.' and calls neither `onDraftPick` nor `onDraftRearrange`. With a GK pack card selected, clicking a non-GK slot shows 'Swap rejected — goalkeeper slot requires a GK card.' and calls neither. Slot 0 must still carry `/statCardEligible/` while a non-GK card is selected — the rejection message is the feedback path, and hiding the highlight would make it unreachable.
12. `waitingForOpponent: true` — clicking a pack card does not select it (no `/statCardSelected/` appears).

Preserve verbatim every non-interaction draft assertion: carousel layout, round/pick counter text, waiting indicator, `draftComplete` hand-off, Confirm gating on a complete lineup, and the D-23 tier-border-on-lineup-slot-cards checks.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && sed -n '1,420p' packages/client/src/components/LineupAssignmentScreen.test.tsx | grep -c 'dragStart\|dragOver\|dragEnd\|dataTransfer\|onCardDragStart\|onDropToBench'</automated>
  </verify>
  <acceptance_criteria>
    - No `dragStart`, `dragOver`, `dragLeave`, `dragEnd`, `dataTransfer`, `onCardDragStart`, or `onDropToBench` occurrence remains anywhere in the draft describe blocks
    - `grep -c 'statCardEligible' packages/client/src/components/LineupAssignmentScreen.test.tsx` is `>= 3`
    - The file contains assertions for all five draft dispatch shapes: `{ type: 'slot'` with `onDraftPick`, `{ type: 'bench' }` with `onDraftPick`, slot→slot `onDraftRearrange`, bench→slot `onDraftRearrange`, slot→bench `onDraftRearrange`
    - Both draft rejection strings ('Swap rejected — only a goalkeeper card can be placed here.' and 'Swap rejected — goalkeeper slot requires a GK card.') are asserted
    - `pnpm --filter @counter-attack/client test -- LineupAssignmentScreen` reports failures ONLY in non-draft describe blocks at this point
  </acceptance_criteria>
  <done>Every draft-mode behaviour — pick to slot, pick to bench, slot↔slot swap, slot↔bench move, GK rules, selection lifecycle, waiting-for-opponent gating — is proven by clicks.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Standard-mode blocks and add explicit pregame click-swap coverage</name>
  <files>packages/client/src/components/LineupAssignmentScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.test.tsx (the 'LineupAssignmentScreen — Standard-mode non-regression' describe block, ~lines 374-704)
    - packages/client/src/components/LineupAssignmentScreen.tsx (`handlePregameCardClick`, `isPregameSwapEligible`, `renderColumn`, and the pregame render branch)
    - .planning/phases/47-select-based-roster-interaction/47-CONTEXT.md (D-12)
  </read_first>
  <action>
Preserve every existing Standard-mode assertion: formation-column grouping, GK LOCK badge, `statCardLocked`/`statCardConfirmed` classes, Confirm button behaviour and confirmed-state copy, bench rendering, and the `gameError`-driven rejection-message coverage.

Update any render helper in this block that still passes drag callbacks (`onCardDragStart`/`onDropToBench` on the inert pregame `BenchCarousel`) to the new prop names.

Add a new describe block 'LineupAssignmentScreen — ROSTER-07: Standard pregame click-to-swap' covering:
1. Clicking an outfield card applies `/statCardSelected/` to that card's `[data-roster-card]` wrapper.
2. With a card selected, every other outfield card carries `/statCardEligible/`, and the GK card (slot 0) does NOT.
3. Clicking a second outfield card calls `onSwap` exactly once with `(sourceSlotIndex, targetSlotIndex)` and clears the selection (no element carries `/statCardSelected/` or `/statCardEligible/` afterwards).
4. Clicking the selected card again deselects it and clears every blue highlight; `onSwap` is not called (ROSTER-03).
5. Clicking the GK card with nothing selected does not select it, and with an outfield card selected does not call `onSwap` and leaves the existing selection intact (D-04 no-op + GK-slot exclusion).
6. When `lineupConfirmed` is true, clicking any card neither selects it nor calls `onSwap`.

Derive slot indices from `FORMATIONS[formationId].slots` ordering / the `assignment` array the existing helpers already build, rather than hardcoding names — match the conventions already used in this describe block.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'ROSTER-07' packages/client/src/components/LineupAssignmentScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - A describe block whose name contains `ROSTER-07` exists with at least 6 `it(...)` cases
    - `onSwap` is asserted called with a two-argument `(number, number)` pair in at least one test, and asserted NOT called in the GK-click, deselect, and `lineupConfirmed` tests
    - The Standard-mode describe block contains zero `dragStart`/`dragOver`/`dragEnd`/`dataTransfer` occurrences
    - `pnpm --filter @counter-attack/client test -- LineupAssignmentScreen` reports failures ONLY in the mid-match describe blocks at this point
  </acceptance_criteria>
  <done>Standard pregame swap has first-class, gesture-explicit coverage (it previously had none) and the GK slot is proven non-selectable and non-targetable.</done>
</task>

<task type="auto">
  <name>Task 3: Convert the mid-match blocks and add the mode-crossing and asymmetry regression tests</name>
  <files>packages/client/src/components/LineupAssignmentScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.test.tsx (the mid-match describe blocks: 'mid-match substitution mode (SUB-02/03/06/07, D-12/D-13)' ~705-1118, 'Phase 42 — midmatch positioning mode' ~1119-1424 including the 'gap item 6: SENT OFF slot stacking' nested block, and 'Phase 42 — staged substitution with confirmation' ~1425-1577, plus the `renderMidmatch` helper)
    - packages/client/src/components/LineupAssignmentScreen.tsx (`isRepositionSelectable`, `isRepositionEligible`, `handleRepositionCardClick`, `isSubstituteEligible`, `handleSubstituteBenchClick`, `handleSubstitutePitchClick`, the SENT OFF placeholder branch, and the mode-toggle button handlers written in 47-03)
    - .planning/phases/47-select-based-roster-interaction/47-CONTEXT.md (D-04 through D-09)
    - .planning/phases/47-select-based-roster-interaction/47-RESEARCH.md (Pitfalls 1, 2, 3 — each names a test this task must produce)
  </read_first>
  <action>
Convert every mid-match drag sequence to its click equivalent using the locator/gesture table above, keeping each test's existing assertion targets (`onReposition`, `onSubstitute`, `pendingSub` popup text, rejection messages, sub-counter chip, `slotCapNote`, read-only copy, bench OUT/RED CARD badges) unchanged.

Update the helper-copy assertion at ~line 1122 from `'Drag a player onto another to swap positions.'` to `'Select a player, then click another to swap positions.'`, and any substitution-mode copy assertion to `'Select a bench card, then click an on-pitch card to substitute.'`.

Positioning mode (ROSTER-01/02/03/04, D-04, D-08, D-09):
1. Clicking an eligible on-pitch card applies `/statCardSelected/`; every other rendered mid-match card carries `/statCardEligible/`.
2. Clicking a second card calls `onReposition` once with `(sourcePieceId, targetPieceId)` and never `onSubstitute` (replaces the existing drag test at ~line 1128 asserting `('home-1', 'home-2')`).
3. Clicking the selected card again deselects it, clears every `/statCardEligible/`, and does not call `onReposition` (ROSTER-03 — new, no drag equivalent existed).
4. D-08 (new): with card A selected, clicking a DIFFERENT selectable card B completes the swap — it does NOT switch the selection. Assert `onReposition` called once with `(A, B)` and that no card carries `/statCardSelected/` afterwards. This is the negative counterpart to the D-07 test below and must exist.
5. D-09: the GK card never carries `/statCardSelected/` after being clicked with nothing selected, and `onReposition` is not called.
6. GK as a TARGET is preserved: with an outfield card selected, the GK card still carries `/statCardEligible/` and clicking it calls `onReposition` (the server's `GK_SLOT_LOCKED` rejection remains the feedback path — do not assert a client-side block here).
7. `readOnly` true and `actionPending` true each independently prevent selection and prevent `onReposition`.
8. A staged `pendingSub` makes on-pitch cards non-selectable.

SENT OFF placeholder (D-05, RESEARCH.md Pitfall 3):
9. In positioning mode with an on-pitch card selected, the SENT OFF placeholder carries `/statCardEligible/` and clicking it calls `onReposition(sourcePieceId, sentOffPieceId)`.
10. When the placeholder's frozen hex is already occupied by one of this team's own active pieces (`sentOffSlotHexTaken`), it carries no `/statCardEligible/` and clicking it calls neither `onReposition` nor `onSubstitute` (preserves the existing gap-item-6 tests).
11. In substitution mode with a bench card selected, the placeholder carries no `/statCardEligible/` and clicking it stages no `pendingSub` and calls neither callback. This is a deliberate behaviour change from the drag model — assert it explicitly.
12. The placeholder still renders `aria-label="Sent off — slot empty"` and the `SENT OFF` badge.

Substitution mode (ROSTER-01/02/04, D-06, D-07):
13. Bench-first (D-06): clicking an on-pitch card with nothing selected does nothing — no `/statCardSelected/`, no `pendingSub` popup.
14. Clicking a bench card applies `/statCardSelected/` to that bench card and `/statCardEligible/` to every eligible on-pitch card; red-carded/blocked on-pitch cards carry none.
15. Clicking an eligible on-pitch card stages the confirmation popup with the existing 'Substitute {outName} for {inName}?' text and does NOT call `onSubstitute` yet; the popup's Confirm button is still the only `onSubstitute` call site.
16. D-07 (new): with bench card A selected, clicking a different available bench card B switches the selection — B carries `/statCardSelected/`, A does not, and the eligible on-pitch target set is unchanged. `onSubstitute` is not called and no `pendingSub` is staged.
17. OUT / RED CARD bench cards are not selectable; `readOnly` true prevents bench selection.

Mode-crossing regression (ROSTER-05, RESEARCH.md Pitfall 1 — the phase's named highest-risk item, no equivalent exists today):
18. Select an on-pitch card in Reposition mode, click 'Enter substitution mode', and assert no element carries `/statCardSelected/` or `/statCardEligible/`; then click an on-pitch card and assert nothing is staged and `onReposition` is not called (the stale selection cannot be reused as a swap participant).
19. Select a bench card in Substitute mode, click 'Cancel substitution', and assert the selection is cleared and a subsequent single on-pitch click calls neither `onReposition` nor `onSubstitute`.
20. Confirming a staged substitution returns to positioning mode with no selection left over.

Update the file's header doc comment to describe the Phase 47 click-select model and list the ROSTER requirements it covers. Finally run the whole file green.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- LineupAssignmentScreen</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- LineupAssignmentScreen` exits 0 with every test passing
    - `grep -ci 'drag' packages/client/src/components/LineupAssignmentScreen.test.tsx` outputs `0`
    - `grep -c 'fireEvent.click' packages/client/src/components/LineupAssignmentScreen.test.tsx` is `>= 60`
    - `grep -c 'data-roster-card' packages/client/src/components/LineupAssignmentScreen.test.tsx` is `>= 5`
    - `grep -c 'Enter substitution mode' packages/client/src/components/LineupAssignmentScreen.test.tsx` is `>= 2` (used by the mode-crossing regression tests)
    - The file contains a test whose name references D-07 (bench selection switch) AND a test whose name references D-08 (positioning does not switch) — both must be present
    - The file contains a test asserting the SENT OFF placeholder carries no eligible-target class in substitution mode
    - `grep -c 'Select a player, then click another to swap positions.' packages/client/src/components/LineupAssignmentScreen.test.tsx` is `>= 1`
  </acceptance_criteria>
  <done>All ~90 cases pass with click simulation, and the four previously-untestable behaviours (deselect, mode-crossing clear, D-07 switch, D-08 non-switch) each have their own named test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → component | No production trust boundary is crossed; this plan modifies test code only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-13 | Tampering | Regression coverage silently weakened during the drag→click rewrite (a removed assertion is invisible) | mitigate | Task actions enumerate every assertion class that must survive verbatim (dispatch shapes, rejection strings, badge/aria hooks, counter chips), and the acceptance criteria assert on their presence by grep rather than only on a green run. Plan 47-06's `knip` + full-suite gate is the second net. |
| T-47-14 | Tampering | Client-side eligibility asserted as if it were authoritative | accept | These are component tests; server-side eligibility (`applyRosterReposition`, `applySubstitution`, `LINEUP_SWAP`, `DRAFT_PICK`, `DRAFT_REARRANGE`) is untouched by this phase and keeps its own server-package tests. Test 6 (GK-as-target) deliberately encodes that the client does NOT block what the server rejects, so the boundary stays visible in the suite. |
| T-47-15 | Elevation of Privilege | A read-only / action-pending / capped-subs state becoming clickable and reaching a server emit | mitigate | Positioning tests 7 and 8 and substitution test 17 assert each guard independently, mirroring the drag suite's existing guard coverage rather than collapsing them into one case. |
| T-47-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed (Vitest + RTL already configured, RESEARCH.md §Environment Availability). |
</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test -- LineupAssignmentScreen` exits 0.
- `grep -ci 'drag' packages/client/src/components/LineupAssignmentScreen.test.tsx` returns 0.
- `GameBoard.test.tsx` and `CardInjuryBadge.crossSurface.test.tsx` remain red after this plan — they are rewritten in the parallel plan 47-05. Do not edit them here.
- Windows note: if vitest reports a worker crash rather than a test failure, rerun with `--pool=forks` before treating it as red.
</verification>

<success_criteria>
- ROSTER-01, 02, 03, 04, 05, 07, 08 each have at least one named, passing test in this file.
- Zero drag simulation remains.
- The D-07 / D-08 asymmetry and the Pitfall 1 mode-crossing clear are each covered by a dedicated test.
</success_criteria>

<output>
Create `.planning/phases/47-select-based-roster-interaction/47-04-SUMMARY.md` when done.
</output>
