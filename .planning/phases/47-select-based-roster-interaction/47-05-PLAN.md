---
phase: 47-select-based-roster-interaction
plan: 05
type: execute
wave: 4
depends_on: ["47-03"]
files_modified:
  - packages/client/src/components/GameBoard.test.tsx
  - packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx
autonomous: true
requirements: [ROSTER-01, ROSTER-04, ROSTER-06]

must_haves:
  truths:
    - "GameBoard still proves that a mid-match roster swap emits game:roster-reposition with {pieceIdA, pieceIdB}"
    - "GameBoard still proves that a pending game action makes on-field roster cards non-interactive and emits nothing"
    - "The four-surface card/injury glyph contract is still proven with the click-select carousel props"
  artifacts:
    - path: "packages/client/src/components/GameBoard.test.tsx"
      provides: "click-based SUB-08/SUB-09 integration coverage through the GameBoard wrapper"
    - path: "packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx"
      provides: "cross-surface glyph coverage using the new BenchCarousel prop contract"
  key_links:
    - from: "packages/client/src/components/GameBoard.test.tsx"
      to: "packages/client/src/store/useGameStore.ts emit"
      via: "fireEvent.click sequence asserting emitMock('game:roster-reposition', ...)"
      pattern: "game:roster-reposition"
---

<objective>
Update the two collateral test files that render `LineupAssignmentScreen`/`BenchCarousel` and still use the retired drag API, so the full client suite can go green.

Purpose: `GameBoard.test.tsx` is the only place the roster-reposition path is proven end-to-end through the real Zustand store emit (not a mocked callback prop), and `CardInjuryBadge.crossSurface.test.tsx` is the only place the four-surface glyph contract is proven. Both break on the 47-03 prop renames and both must be preserved, not deleted.
Output: Both files click-based and green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-select-based-roster-interaction/47-CONTEXT.md
@.planning/phases/47-select-based-roster-interaction/47-03-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates no production symbols. It consumes symbols created in plans 47-01 through 47-03 (do not treat as drift): `data-roster-card`, `data-interactive`, `.statCardSelected`, `.statCardEligible`, `BenchCarouselProps.onCardClick`, `BenchCarouselProps.onBenchAreaClick`, `BenchCarouselProps.selectedCardId`, `BenchCarouselProps.benchAreaEligible`.

<tasks>

<task type="auto">
  <name>Task 1: Convert GameBoard.test.tsx's SUB-08/SUB-09 roster tests to click simulation</name>
  <files>packages/client/src/components/GameBoard.test.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.test.tsx (the roster/substitution describe block containing tests 3 and 4 at ~lines 784-808, plus the `seedRosterState`, `STOPPAGE_SAMPLE`, and `emitMock` helpers this block relies on)
    - packages/client/src/components/LineupAssignmentScreen.tsx (the `handleRepositionCardClick` / `isRepositionSelectable` behaviour written in 47-03, and the `data-roster-card` / `data-interactive` attributes)
  </read_first>
  <action>
Test 3 (currently 'SUB-08: with the panel open in a stoppage phase, dragging one on-field card onto another emits game:roster-reposition with {pieceIdA, pieceIdB}'): rename the gesture wording to 'clicking one on-field card then another'. Replace `.closest('[draggable]')` with `.closest('[data-roster-card]')` for both `'Fallou Fall'` (source) and `'Mamadou Fall'` (target), and replace the `fireEvent.dragStart(source, {...})` + `fireEvent.drop(target, {...})` pair with `fireEvent.click(source)` then `fireEvent.click(target)`. Keep the `expect(emitMock).toHaveBeenCalledWith('game:roster-reposition', { pieceIdA: 'home-1', pieceIdB: 'home-2' })` assertion byte-identical — this is the only end-to-end proof that the click path still reaches the real store emit.

Test 4 (currently 'SUB-09: with selectedPieceId set (a game action pending), on-field cards are non-draggable and no emit occurs on drop'): rename to reference non-interactive cards and clicks. Keep `useGameStore.setState({ selectedPieceId: 'home-9' })` and the panel-open click. Replace the `expect(source.getAttribute('draggable')).toBe('false')` assertion with an assertion that the card is not interactive under the 47-03 contract — assert the source `[data-roster-card]` element has no `role="button"` attribute AND no `tabIndex` attribute (a non-clickable `LineupStatCard` receives neither). Then `fireEvent.click(source)` followed by `fireEvent.click(target)` and keep `expect(emitMock).not.toHaveBeenCalled()`.

Add one new assertion inside test 4: after clicking the source, no element in the document carries a class matching `/statCardSelected/` — proving `actionPending` blocks selection itself, not merely the emit.

Change nothing else in this file. Do not touch any non-roster GameBoard test.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- GameBoard</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- GameBoard` exits 0
    - `grep -ci 'drag' packages/client/src/components/GameBoard.test.tsx` outputs `0`
    - `grep -c "game:roster-reposition" packages/client/src/components/GameBoard.test.tsx` is `>= 1` and the `{ pieceIdA: 'home-1', pieceIdB: 'home-2' }` payload assertion is unchanged
    - `grep -c 'data-roster-card' packages/client/src/components/GameBoard.test.tsx` is `>= 4`
    - `grep -c 'statCardSelected' packages/client/src/components/GameBoard.test.tsx` is `>= 1`
  </acceptance_criteria>
  <done>The GameBoard-level roster-reposition emit and the action-pending lockout are both proven through clicks against the real store.</done>
</task>

<task type="auto">
  <name>Task 2: Update CardInjuryBadge.crossSurface.test.tsx to the click-select carousel props</name>
  <files>packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx</files>
  <read_first>
    - packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx (all `BenchCarousel` render call sites at ~lines 228-244, 335-340, plus the `renderRegression` helper and the substitution-confirm-popup test at ~lines 450-460)
    - packages/client/src/components/BenchCarousel.tsx (the contract written in 47-02)
    - packages/client/src/components/LineupAssignmentScreen.tsx (the mid-match substitution click flow written in 47-03)
  </read_first>
  <action>
Replace `onCardDragStart={noop}` with `onCardClick={noop}` at every `BenchCarousel` render call site, and delete `onDropToBench={noop}` entirely (the prop is now optional and these call sites have no bench-as-target semantics). Do not add `selectedCardId` or `benchAreaEligible` — these tests assert glyph rendering, not selection state, and the defaults are correct.

In the test 'bench badges are unaffected by an open substitution-confirm popup' (~line 450): keep `fireEvent.click(screen.getByLabelText('Enter substitution mode'))`, then replace `.closest('[draggable]')` with `.closest('[data-roster-card]')` for both `'Fallou Fall'` (bench card) and `'Home DefOne'` (on-pitch target), and replace the `dragStart` + `drop` pair with `fireEvent.click(benchCard)` then `fireEvent.click(target)`. Keep `expect(screen.getByRole('dialog')).toBeDefined()` and `assertBenchBadges()` unchanged — the staged-substitution popup must still appear, now via clicks.

Preserve every glyph-contract assertion verbatim: `glyphContract`, `CLEAN`, `BOOKED_AND_INJURED`, the four-surface ICON-02 parity loop, the `bench-red-card-badge` / `cardUnavailable` gap-item-1 test, and the `actionPending` regression test. Change nothing about `CardInjuryBadge` itself.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- CardInjuryBadge</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- CardInjuryBadge` exits 0 (covers `CardInjuryBadge.crossSurface.test.tsx` and `CardInjuryBadge.audit.test.ts`)
    - `grep -ci 'drag' packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx` outputs `0`
    - `grep -c 'onCardDragStart\|onDropToBench' packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx` outputs `0`
    - `grep -c 'onCardClick' packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx` is `>= 3`
    - `grep -c "getByRole('dialog')" packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx` is `>= 1` (the staged-substitution popup assertion survived)
  </acceptance_criteria>
  <done>The four-surface glyph contract and the bench-badge regressions still pass with the click-select carousel props and a click-staged substitution popup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → component → mocked socket emit | `GameBoard.test.tsx` exercises the real Zustand store's emit path with a mocked socket; no production trust boundary changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-16 | Tampering | Losing the only end-to-end proof that a roster swap reaches `game:roster-reposition` with the correct payload | mitigate | Task 1 requires the `emitMock` payload assertion to remain byte-identical; the acceptance criteria grep for the event name and payload rather than relying only on a green run. |
| T-47-17 | Elevation of Privilege | An action-pending game state no longer blocking a roster swap after the gesture change | mitigate | Task 1 keeps the SUB-09 test and strengthens it: the card must be neither `role="button"` nor focusable, AND no selection may occur, AND no emit may fire. Three independent assertions replace the single `draggable="false"` check. |
| T-47-18 | Tampering | Client-side gating mistaken for server authority | accept | Unchanged by this phase — `GAME_ROSTER_REPOSITION`/`GAME_SUBSTITUTION` server handlers are untouched and keep their own server-package tests; these client tests assert UX gating only. |
| T-47-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed. |
</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test -- GameBoard` exits 0.
- `pnpm --filter @counter-attack/client test -- CardInjuryBadge` exits 0.
- `LineupAssignmentScreen.test.tsx` may still be red while the parallel plan 47-04 is in flight — do not edit it here.
- Windows note: if vitest reports a worker crash rather than a test failure, rerun with `--pool=forks` before treating it as red.
</verification>

<success_criteria>
- Both collateral test files are click-based, contain zero case-insensitive `drag` occurrences, and pass.
- The `game:roster-reposition` emit assertion and the four-surface glyph contract are preserved unchanged.
</success_criteria>

<output>
Create `.planning/phases/47-select-based-roster-interaction/47-05-SUMMARY.md` when done.
</output>
