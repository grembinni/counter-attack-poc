---
phase: 47-select-based-roster-interaction
plan: 06
type: execute
wave: 5
depends_on: ["47-04", "47-05"]
files_modified:
  - docs/HIGHLIGHT-REFERENCE.md
  - packages/client/src/components/LineupAssignmentScreen.module.css
  - packages/client/src/components/LineupAssignmentScreen.tsx
  - packages/client/src/components/BenchCarousel.tsx
  - packages/client/src/components/DraftPackCarousel.tsx
autonomous: true
requirements: [ROSTER-05, ROSTER-06]

must_haves:
  truths:
    - "pnpm knip exits clean with zero output — no dead code survives the drag removal"
    - "The full workspace test suite is green"
    - "pnpm typecheck is green across all three packages"
    - "The card-selection colors are documented in the project's single-source-of-truth highlight reference"
    - "Positioning, substitution, pregame, and draft eligibility are four separate functions, none taking a mode parameter"
  artifacts:
    - path: "docs/HIGHLIGHT-REFERENCE.md"
      provides: "Section 4 documenting the card-selection green/blue tokens and their pitch sources"
      contains: "Card Selection"
  key_links:
    - from: "docs/HIGHLIGHT-REFERENCE.md"
      to: "packages/client/src/components/LineupAssignmentScreen.module.css"
      via: "documented value-match of #22c55e / #60a5fa to .statCardSelected / .statCardEligible"
      pattern: "statCardSelected|statCardEligible"
---

<objective>
Close the phase: prove ROSTER-06's zero-dead-code requirement with a clean `knip` run, prove ROSTER-05's structural separation by inspection, bring the whole workspace green (typecheck + full suite + lint + stylelint), and record the new card-selection colors in the project's canonical highlight reference.

Purpose: Plans 47-01 through 47-05 each gated on a targeted slice. This plan is the only place the phase is verified as a whole, and the only place `knip` — ROADMAP Phase 47 success criterion 5's named tool — actually runs.
Output: A green workspace, a clean `knip`, and an updated `docs/HIGHLIGHT-REFERENCE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-select-based-roster-interaction/47-CONTEXT.md
@.planning/phases/47-select-based-roster-interaction/47-UI-SPEC.md
@.planning/phases/47-select-based-roster-interaction/47-04-SUMMARY.md
@.planning/phases/47-select-based-roster-interaction/47-05-SUMMARY.md
</context>

## Artifacts this phase produces

New artifact introduced by this plan (do not treat as pre-existing drift):

| Symbol | Kind | File |
|---|---|---|
| `## 4. Card Selection (`LineupAssignmentScreen.module.css`)` | doc section | `docs/HIGHLIGHT-REFERENCE.md` |

All other symbols referenced here were created in plans 47-01 through 47-03 and are listed in those plans' "Artifacts this phase produces" tables.

<tasks>

<task type="auto">
  <name>Task 1: Document the card-selection tokens in HIGHLIGHT-REFERENCE.md</name>
  <files>docs/HIGHLIGHT-REFERENCE.md</files>
  <read_first>
    - docs/HIGHLIGHT-REFERENCE.md (full file — §Overview's stated scope, §Traffic-Light Semantic Legend, §2a `PieceOverlay.tsx` piece selection/status rings, §4 Valid-Move Tint Consistency, §Adding a New Highlight)
    - packages/client/src/components/LineupAssignmentScreen.module.css (the `.statCardSelected` / `.statCardEligible` declarations added in 47-01)
    - .planning/phases/47-select-based-roster-interaction/47-UI-SPEC.md (§Color — the resolved swatch decision and its rationale)
  </read_first>
  <action>
This document currently declares itself the single source of truth for highlight/ring colors and scopes itself to `HexCell.tsx`, `PieceOverlay.tsx`, `BallLocationRing.tsx`, and `HeaderTargetRing.tsx`. Phase 47 introduces a fifth consumer (`LineupAssignmentScreen.module.css`), so add a section for it rather than leaving two undocumented color literals outside the reference.

Insert a new section titled `## 4. Card Selection (LineupAssignmentScreen.module.css)` immediately before the existing `## 4. Valid-Move Tint Consistency` section, and renumber that existing section (and any later numbered section) so the sequence stays contiguous. Update the §Overview scope sentence to name `LineupAssignmentScreen.module.css` alongside the four existing files.

The new section contains a table with columns Class / Semantic / Border / Glow / Source token, and exactly two rows:
- `.statCardSelected` — "🟢 this card is the current selection" — `2px solid #22c55e` — `0 0 0 2px rgba(34, 197, 94, 0.4)` — value-matched to `PieceOverlay.tsx`'s exported `ACTIVE_RING_STROKE` (`selectionState="active"`), documented in §2a.
- `.statCardEligible` — "🔵 clicking this card completes the pending action" — `2px solid #60a5fa` — `0 0 0 2px rgba(96, 165, 250, 0.4)` — value-matched to `PieceOverlay.tsx`'s `selectionState="selectable"` ring stroke, documented in §2a.

Add three short notes under the table: (1) these are CSS Module literals, not a TypeScript import of `HIGHLIGHT_STYLES`/`RING_STYLES` — cards are the only CSS-Modules-styled selection surface, and `HexCell.tsx`/`PieceOverlay.tsx` style SVG inline, so object-identity reuse is not available; per UI-SPEC §Color the D-03 requirement is satisfied by value-matching. (2) The classes must remain the last border-declaring rules in `LineupAssignmentScreen.module.css` — they are composed onto `.statCardSentOff`, `.benchSlot`, `.benchCarousel`, and the `.cardTier*` rules, all of which also declare `border`, and CSS Modules resolves by declaration order. (3) Gold (`--color-accent-gold`) is deliberately NOT used for card eligible-target state (D-03) — gold stays reserved for confirm/required-action affordances app-wide.

Add a corresponding row to the §Traffic-Light Semantic Legend if that table enumerates per-color meanings, so green/blue on cards read consistently with green/blue on the pitch. Change no existing color value anywhere in the document.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'Card Selection\|statCardSelected\|statCardEligible' docs/HIGHLIGHT-REFERENCE.md && grep -c 'LineupAssignmentScreen.module.css' docs/HIGHLIGHT-REFERENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Card Selection' docs/HIGHLIGHT-REFERENCE.md` is `>= 1`
    - `grep -c 'statCardSelected' docs/HIGHLIGHT-REFERENCE.md` is `>= 1` and `grep -c 'statCardEligible'` is `>= 1`
    - `grep -c '#22c55e' docs/HIGHLIGHT-REFERENCE.md` is `>= 2` (the pre-existing `ACTIVE_RING_STROKE` row plus the new card row) and `grep -c '#60a5fa'` is `>= 2`
    - `grep -c 'LineupAssignmentScreen.module.css' docs/HIGHLIGHT-REFERENCE.md` is `>= 2` (scope sentence + section heading)
    - Section numbering is contiguous: `grep -n '^## [0-9]' docs/HIGHLIGHT-REFERENCE.md` shows 1, 2, 3, 4, 5 with no repeats or gaps
  </acceptance_criteria>
  <done>The two card-selection colors are traceable from the project's canonical highlight reference back to their `PieceOverlay.tsx` source tokens, and the document's stated scope matches reality.</done>
</task>

<task type="auto">
  <name>Task 2: Prove zero dead code and the four-way eligibility separation</name>
  <files>packages/client/src/components/LineupAssignmentScreen.tsx, packages/client/src/components/BenchCarousel.tsx, packages/client/src/components/DraftPackCarousel.tsx, packages/client/src/components/LineupAssignmentScreen.module.css</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx (full file, post-port — every `is*Eligible` / `handle*Click` function and every prop declaration)
    - packages/client/src/components/BenchCarousel.tsx (full file, post-port)
    - packages/client/src/components/DraftPackCarousel.tsx (full file, post-port)
    - packages/client/src/components/LineupAssignmentScreen.module.css (full file, post-port)
    - knip.json (workspace project globs — `packages/client` uses `index.html` as entry and `src/**/*.{ts,tsx}` as project)
    - .planning/phases/47-select-based-roster-interaction/47-RESEARCH.md (Pitfall 1 — the ROSTER-05 hard constraint)
  </read_first>
  <action>
Run `pnpm knip` (which first builds `@counter-attack/shared`, per the root script). ROADMAP Phase 47 success criterion 5 and ROSTER-06 both name a clean `knip` run as the gate. Fix every finding that this phase introduced: unused exports left behind by the prop renames, unused types, unused files, and unused dependencies. Do not silence findings by editing `knip.json` — the phase's requirement is that no dead code survives, not that the tool stops reporting. If a finding is genuinely pre-existing and unrelated to this phase, verify that claim by checking `git stash`-free `git log`/`git blame` on the reported symbol, and record it in the SUMMARY rather than fixing it.

Also run a stylelint check and remove any CSS rule in `LineupAssignmentScreen.module.css` that no `.tsx` file references any more — `knip` does not analyse CSS Modules, so sweep manually: for each class name declared in that file, confirm at least one `styles.<name>` reference exists across `LineupAssignmentScreen.tsx`, `BenchCarousel.tsx`, and `DraftPackCarousel.tsx`. The three removed drag classes should already be gone from 47-01; this sweep catches anything they left orphaned (for example a rule that only existed to be composed onto a drop-target).

Then verify the ROSTER-05 structural constraint by inspection and record the result in the SUMMARY: confirm that `isRepositionEligible`, `isSubstituteEligible`, `isPregameSwapEligible`, and the draft pair (`isDraftSlotEligible` / `isDraftBenchAreaEligible`) are separate top-level functions; that none of them accepts a `mode`, `subMode`, or `draftMode` parameter; and that none of them contains an `if`/ternary branching on `subMode`. The same must hold for the click handlers — `handleRepositionCardClick`, `handleSubstituteBenchClick`, `handleSubstitutePitchClick`, `handlePregameCardClick`, and the four `handleDraft*Click` functions must be independent function bodies, not one parameterised helper. A single `subMode === 'reposition' ? handleRepositionCardClick(...) : handleSubstitutePitchClick(...)` ternary at the JSX `onClick` call site is the correct and intended dispatch shape and does NOT violate this constraint — the constraint is about the guard bodies, not the call site.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pnpm knip && pnpm stylelint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm knip` exits 0 and produces no findings output
    - `pnpm stylelint` exits 0
    - `knip.json` is unchanged by this plan (`git diff --exit-code knip.json` succeeds)
    - `grep -c 'subMode' packages/client/src/components/LineupAssignmentScreen.tsx` shows `subMode` only inside render/dispatch/guard-entry positions — no `is*Eligible` function signature contains a `mode` parameter (verify by reading each of the five function signatures)
    - Every CSS class declared in `LineupAssignmentScreen.module.css` has at least one `styles.<name>` reference across the three component files
  </acceptance_criteria>
  <done>`knip` and `stylelint` are clean without config edits, no orphaned CSS rule remains, and the four-way eligibility separation is confirmed and recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Full workspace green gate</name>
  <files>packages/client/src/components/LineupAssignmentScreen.tsx</files>
  <read_first>
    - .planning/phases/47-select-based-roster-interaction/47-VALIDATION.md (the phase-gate definition: full suite green plus clean knip before `/gsd-verify-work`)
    - .planning/ROADMAP.md (Phase 47 Success Criteria 1-7 — the checklist this task closes)
    - packages/client/src/components/LineupAssignmentScreen.tsx (for any typecheck fix the gate surfaces)
  </read_first>
  <action>
Run the four workspace-wide gates in order and fix anything that fails, keeping fixes minimal and inside this phase's scope:

1. `pnpm typecheck` — this is the first run of `tsc --noEmit` since the port began, because `packages/client/tsconfig.json` includes `src/**/*` (test files included) and plans 47-03 through 47-05 could not satisfy it in isolation. Expect residual issues here rather than in the earlier plans: `exactOptionalPropertyTypes`-style narrowing on the new optional props (`isSelectable?`, `allowGKSelect?`, `selectedCardId?`, `benchAreaEligible?`, `onBenchAreaClick?`), and `noUncheckedIndexedAccess` on any new array indexing (for example `midmatchBenchCards[benchIndex]` and `draftView.benchIds[benchIndex]` — both must be null-checked, matching the existing convention in this file).
2. `pnpm lint` (eslint) — fix any `react-hooks/exhaustive-deps` warning introduced by new `useState`/`useMemo`/`useCallback` usage, and any unused-variable error left by the drag removal.
3. `pnpm test` (root — runs `pnpm -r test` across shared, server, and client).
4. `pnpm --filter @counter-attack/client build`.

Then walk ROADMAP Phase 47's seven Success Criteria one by one and record in the SUMMARY, for each, the specific test name or command output that proves it. Criterion 5 has two halves — the two-separate-functions half is evidenced by plan 47-06 Task 2's inspection, and the zero-drag-code half by `grep -ci 'drag' packages/client/src/components/LineupAssignmentScreen.tsx` returning 0 plus the clean `knip` run.

Do not add new features, do not refactor beyond what a gate failure requires, and do not weaken any assertion to make a test pass.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && pnpm typecheck && pnpm lint && pnpm test && pnpm --filter @counter-attack/client build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
    - `pnpm test` exits 0 with every workspace package's suite passing
    - `pnpm --filter @counter-attack/client build` exits 0
    - `grep -ci 'drag' packages/client/src/components/LineupAssignmentScreen.tsx packages/client/src/components/BenchCarousel.tsx packages/client/src/components/DraftPackCarousel.tsx packages/client/src/components/LineupAssignmentScreen.module.css` reports `0` for every file
    - The SUMMARY maps each of ROADMAP Phase 47's seven Success Criteria to a named test or command output
  </acceptance_criteria>
  <done>Typecheck, lint, the full workspace test suite, the client build, and knip are all green, and every ROADMAP success criterion has named evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → CI/build tooling | Static analysis and build gates; no runtime trust boundary is modified by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-19 | Tampering | Dead drag code left behind in a hard-to-see place (an unused export, an orphaned CSS rule) quietly reintroducing a second input path | mitigate | `pnpm knip` must exit clean WITHOUT `knip.json` edits (asserted by `git diff --exit-code knip.json`), plus a manual CSS-class reference sweep because knip does not analyse CSS Modules, plus a case-insensitive `drag` grep across all four production files. |
| T-47-20 | Tampering | A gate silenced rather than satisfied (config exclusion, weakened assertion, skipped test) | mitigate | Task 3 forbids weakening assertions and forbids feature work; Task 2 forbids `knip.json` edits. Each ROADMAP success criterion must be mapped to named evidence in the SUMMARY, so a silenced gate has no evidence to cite. |
| T-47-21 | Tampering | Client-side eligibility mistaken for an authorization control at phase close | accept (documented) | Restated for the record: this phase swapped the client input mechanism only. `applyRosterReposition`, `applySubstitution`, and the `LINEUP_SWAP`/`LINEUP_CONFIRM`/`DRAFT_PICK`/`DRAFT_REARRANGE` socket handlers were not modified, so no new server-side validation was required (CONTEXT.md Integration Points, confirmed for the GK lock specifically by D-09). Every client eligibility computation added this phase is a UX mirror of an existing server guard, and the server remains the sole authority. |
| T-47-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this phase installed zero packages (RESEARCH.md §Environment Availability confirms no new dependencies). No `[ASSUMED]`/`[SUS]` legitimacy checkpoint applies. |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm knip`, `pnpm stylelint`, and `pnpm --filter @counter-attack/client build` all exit 0.
- `git diff --exit-code knip.json` succeeds (no config was loosened).
- Windows note: if vitest reports a worker crash rather than a test failure, rerun with `--pool=forks` before treating it as red.
</verification>

<success_criteria>
- ROSTER-06 is proven: zero drag code in any of the four production files and a clean `knip` run with an unmodified `knip.json`.
- ROSTER-05 is proven: four separate eligibility functions, none parameterised by mode.
- The whole workspace is green (typecheck, lint, test, build, stylelint).
- `docs/HIGHLIGHT-REFERENCE.md` documents the card-selection tokens and their pitch sources.
- Every ROADMAP Phase 47 success criterion is mapped to named evidence in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/47-select-based-roster-interaction/47-06-SUMMARY.md` when done.
</output>
