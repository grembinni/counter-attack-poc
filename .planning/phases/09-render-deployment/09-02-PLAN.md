---
phase: 09-render-deployment
plan: 02
type: execute
wave: 2
depends_on: ['09-01']
files_modified:
  - render.yaml
  - .github/workflows/ci.yml
autonomous: true
requirements: [ARCH-05, ARCH-06]

must_haves:
  truths:
    - 'render.yaml at repo root defines one web service that builds with pnpm and starts main.js'
    - 'render.yaml health check path is /healthz (matching the route added in Plan 09-01)'
    - 'render.yaml startCommand targets packages/server/dist/main.js, never index.js'
    - 'render.yaml uses autoDeployTrigger: commit (not the deprecated autoDeploy field)'
    - 'CI workflow runs install --frozen-lockfile → typecheck → test → build on push and pull_request'
  artifacts:
    - path: 'render.yaml'
      provides: 'Render Blueprint IaC for single web service (D-06)'
      contains: 'healthCheckPath: /healthz'
    - path: '.github/workflows/ci.yml'
      provides: 'GitHub Actions CI gate (D-07)'
      contains: 'pnpm -r typecheck'
  key_links:
    - from: 'render.yaml startCommand'
      to: 'packages/server/dist/main.js'
      via: 'node packages/server/dist/main.js'
      pattern: "main\\.js"
    - from: 'render.yaml healthCheckPath'
      to: '/healthz route in createServer.ts (Plan 09-01)'
      via: 'Render health probe'
      pattern: 'healthCheckPath: /healthz'
    - from: '.github/workflows/ci.yml'
      to: 'root package.json scripts'
      via: 'pnpm -r typecheck/test/build'
      pattern: 'pnpm -r (typecheck|test|build)'
---

<objective>
Create the two infrastructure-as-code files that deploy and gate counter-attack-poc:
`render.yaml` (Render Blueprint, D-06) at the repo root and `.github/workflows/ci.yml`
(GitHub Actions CI, D-07). Both reference build artifacts and routes produced by the
code changes in Plan 09-01, so this plan runs in Wave 2 after Wave 1 completes.

Purpose: render.yaml tells Render how to build (pnpm install + recursive build), start
(node packages/server/dist/main.js), and health-check (/healthz) the single web service.
ci.yml blocks broken commits by running typecheck → test → build on every push and PR.

Output:

- `render.yaml` — repo-root Render Blueprint for one Node web service on the free plan
- `.github/workflows/ci.yml` — pnpm monorepo CI workflow (checkout → pnpm → node 22 →
  install → typecheck → test → build)
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-render-deployment/09-CONTEXT.md
@.planning/phases/09-render-deployment/09-RESEARCH.md
@.planning/phases/09-render-deployment/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create render.yaml Render Blueprint (D-06)</name>
  <files>render.yaml</files>
  <read_first>
    - package.json (repo root — confirm `"packageManager": "pnpm@9.15.9"` and `"engines": { "node": ">=22" }`;
      scripts include `build`, `test`, `typecheck`)
    - .planning/phases/09-render-deployment/09-PATTERNS.md (lines 156-179: complete render.yaml content and correctness rules)
    - .planning/phases/09-render-deployment/09-RESEARCH.md (lines 258-285: render.yaml spec, deprecated-field and startCommand warnings)
  </read_first>
  <action>
    Create `render.yaml` at the REPO ROOT (not inside packages/). Define a single service under a `services:`
    list with: `type: web`, `name: counter-attack-poc`, `runtime: node`, `plan: free`,
    `buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm -r build`,
    `startCommand: node packages/server/dist/main.js`, `healthCheckPath: /healthz`,
    `autoDeployTrigger: commit`, and an `envVars` list with two entries: `{ key: NODE_VERSION, value: '22' }`
    and `{ key: NODE_ENV, value: production }`. CRITICAL correctness rules from research:
    use `autoDeployTrigger: commit` NOT the deprecated `autoDeploy: true`; startCommand MUST point at
    `packages/server/dist/main.js` (the runnable entry) NOT `index.js` (a library barrel with no listen call,
    which would silently exit); `corepack enable &&` MUST precede `pnpm install` because Render's Node 22 image
    ships corepack but not pnpm; `NODE_VERSION` is an envVar (Render's Node-version selection mechanism), not a
    top-level schema field; quote the NODE_VERSION value as `'22'`. Do NOT add a `CORS_ORIGIN` envVar
    (Claude's-discretion call: same-origin single-service serving makes it unnecessary per D-09).
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('render.yaml','utf8'); const re=[/type:\s*web/,/runtime:\s*node/,/buildCommand:.*corepack enable.*pnpm install --frozen-lockfile.*pnpm -r build/,/startCommand:\s*node packages\/server\/dist\/main\.js/,/healthCheckPath:\s*\/healthz/,/autoDeployTrigger:\s*commit/]; const bad=[/autoDeploy:\s*true/,/dist\/index\.js/]; const missing=re.filter(r=>!r.test(y)); const present=bad.filter(r=>r.test(y)); if(missing.length||present.length){console.error('missing',missing.map(String),'forbidden-present',present.map(String));process.exit(1)} console.log('render.yaml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `render.yaml` exists at repo root.
    - Contains `type: web`, `runtime: node`, `plan: free`, `name: counter-attack-poc`.
    - Contains `buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm -r build`.
    - Contains `startCommand: node packages/server/dist/main.js`.
    - Contains `healthCheckPath: /healthz`.
    - Contains `autoDeployTrigger: commit` and does NOT contain `autoDeploy: true`.
    - Does NOT contain `dist/index.js`.
    - envVars include `NODE_VERSION` value `'22'` and `NODE_ENV` value `production`.
    - The verify node command prints `render.yaml OK` and exits 0.
  </acceptance_criteria>
  <done>
    render.yaml at repo root encodes a single Node web service with the correct build/start/health-check
    commands, autoDeployTrigger: commit, and NODE_VERSION/NODE_ENV envVars; the verify command passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow (D-07)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - package.json (repo root — confirm `typecheck`, `test`, `build` scripts and `packageManager: pnpm@9.15.9`)
    - .planning/phases/09-render-deployment/09-PATTERNS.md (lines 186-223: complete ci.yml content; note the stale
      node_modules ci.yml is explicitly rejected as a model)
    - .planning/phases/09-render-deployment/09-RESEARCH.md (lines 286-326: CI workflow spec and action-version notes)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml`. Set `name: CI`. Trigger on both `push:` and `pull_request:` (no branch
    filters — run on all). Define one job `ci` with `runs-on: ubuntu-latest` and these steps IN ORDER:
    `uses: actions/checkout@v4`; then `uses: pnpm/action-setup@v4` with `version: 9` (this MUST come before
    setup-node so setup-node can detect the pnpm store for caching); then `uses: actions/setup-node@v4` with
    `node-version: '22'` and `cache: 'pnpm'`; then `run: pnpm install --frozen-lockfile`; then
    `run: pnpm -r typecheck`; then `run: pnpm -r test`; then `run: pnpm -r build`. Run order is
    typecheck → test → build so a type error or failing test aborts before the (slower) build. `version: 9`
    matches the root `packageManager: pnpm@9.15.9`. Do NOT model this on the stale npm-based ci.yml found under
    node_modules (it uses checkout@v2 / setup-node@v1 / npm and is incompatible with this pnpm monorepo).
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); const re=[/name:\s*CI/,/on:[\s\S]*push:/,/on:[\s\S]*pull_request:/,/actions\/checkout@v4/,/pnpm\/action-setup@v4/,/actions\/setup-node@v4/,/node-version:\s*'?22'?/,/cache:\s*'?pnpm'?/,/pnpm install --frozen-lockfile/,/pnpm -r typecheck/,/pnpm -r test/,/pnpm -r build/]; const m=re.filter(r=>!r.test(y)); const setup=y.indexOf('pnpm/action-setup'); const node=y.indexOf('actions/setup-node'); if(m.length){console.error('missing',m.map(String));process.exit(1)} if(setup<0||node<0||setup>node){console.error('pnpm/action-setup must precede actions/setup-node');process.exit(1)} console.log('ci.yml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists.
    - Contains `name: CI` and triggers on both `push:` and `pull_request:`.
    - Contains a `ci` job with `runs-on: ubuntu-latest`.
    - Steps include `actions/checkout@v4`, `pnpm/action-setup@v4` (with `version: 9`), `actions/setup-node@v4`
      (with `node-version: '22'` and `cache: 'pnpm'`).
    - `pnpm/action-setup@v4` appears BEFORE `actions/setup-node@v4` in the file.
    - Contains run steps `pnpm install --frozen-lockfile`, `pnpm -r typecheck`, `pnpm -r test`, `pnpm -r build` in that order.
    - The verify node command prints `ci.yml OK` and exits 0.
  </acceptance_criteria>
  <done>
    .github/workflows/ci.yml defines a CI job that on push and pull_request checks out, sets up pnpm 9 + node 22
    with pnpm caching, installs frozen, then runs typecheck → test → build; the verify command passes.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                               |
| ----------------------------------- | ------------------------------------------------------------------------- |
| GitHub repo → Render build/deploy   | render.yaml drives what Render builds and runs from pushed commits        |
| render.yaml → environment variables | Values declared here are visible in the Render dashboard and repo history |
| GitHub push → CI runner             | ci.yml runs project scripts in an ephemeral CI environment                |

## STRIDE Threat Register

| Threat ID | Category                             | Component                                                  | Disposition | Mitigation Plan                                                                                                                                                                                  |
| --------- | ------------------------------------ | ---------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-09-05   | Information Disclosure               | render.yaml envVars                                        | mitigate    | Declare ONLY non-secret config (NODE_VERSION, NODE_ENV); no API keys, tokens, or secrets committed — secrets belong in the Render dashboard, never in render.yaml                                |
| T-09-06   | Tampering (wrong entrypoint)         | render.yaml startCommand                                   | mitigate    | startCommand pinned to `node packages/server/dist/main.js` (verified entry with listen); verify gate rejects `dist/index.js` so the service cannot be silently mis-pointed at the library barrel |
| T-09-07   | Tampering (deprecated/unsafe config) | render.yaml autoDeploy field                               | mitigate    | Use `autoDeployTrigger: commit`; verify gate fails the build if deprecated `autoDeploy: true` is present                                                                                         |
| T-09-SC   | Tampering                            | npm/pnpm installs in CI (`pnpm install --frozen-lockfile`) | accept      | No new packages introduced this phase (RESEARCH.md Package Legitimacy Audit: all four server packages pre-approved, no [SLOP]/[SUS]); `--frozen-lockfile` blocks lockfile drift in CI            |

</threat_model>

<verification>
- render.yaml verify node command prints `render.yaml OK` (exit 0)
- ci.yml verify node command prints `ci.yml OK` (exit 0)
- `pnpm -r typecheck && pnpm -r test && pnpm -r build` exits 0 locally (mirrors the CI gate; build produces
  packages/client/dist for the render.yaml startCommand to serve)
- Manual (owner, outside code tasks): in Render, New → Blueprint → connect repo → Apply; once deployed,
  `curl https://<service>.onrender.com/healthz` returns `ok`
</verification>

<success_criteria>

- render.yaml at repo root encodes buildCommand, startCommand (main.js), healthCheckPath: /healthz,
  autoDeployTrigger: commit, and NODE_ENV=production as reviewed IaC (ARCH-05, ARCH-06)
- .github/workflows/ci.yml runs install → typecheck → test → build on push and pull_request (ARCH-05, ARCH-06)
- No deprecated autoDeploy field; startCommand never points at index.js
- Local `pnpm -r typecheck && pnpm -r test && pnpm -r build` passes (the same gate CI enforces)
  </success_criteria>

<output>
Create `.planning/phases/09-render-deployment/09-02-SUMMARY.md` when done
</output>
