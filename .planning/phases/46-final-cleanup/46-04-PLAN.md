---
phase: 46-final-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/data/player-pool.csv
  - packages/shared/scripts/seed-rosters.ts
  - packages/shared/src/teams.ts
  - packages/shared/src/teams.test.ts
  - packages/shared/src/teamConfig.ts
  - packages/server/src/roomHandlers.ts
  - packages/server/src/__tests__/lineupAssignment.integration.test.ts
  - packages/server/src/__tests__/substitution.integration.test.ts
autonomous: true
requirements: [CLEANUP-05, CLEANUP-13]
must_haves:
  truths:
    - "A standard (non-Draft) room's first broadcast GameState has 5 available substitutes on each side, one per role"
    - "A generic bench player can be substituted onto the pitch through the existing substitution flow with no new code path"
    - "Bench jersey numbers never collide with the starting XI's 1-11"
    - "packages/shared/src/teams.ts is still genuinely generated from player-pool.csv — running pnpm run seed:rosters reproduces it byte-for-byte"
    - "Generic bench players never appear in any draft pool"
  artifacts:
    - path: "packages/shared/src/data/player-pool.csv"
      provides: "10 generic bench rows (5 home, 5 away), appended last"
      contains: "generic-bench-home"
    - path: "packages/shared/scripts/seed-rosters.ts"
      provides: "Generic-bench jersey-number offset and updated total-count assertion"
      contains: "generic-bench"
    - path: "packages/shared/src/teams.ts"
      provides: "PLAYER_POOL with 198 entries, p189-p198 being the generic bench"
      contains: "generic-bench-away"
    - path: "packages/shared/src/teamConfig.ts"
      provides: "getGenericBenchPlayers lookup mirroring getSquadPlayers"
      exports: ["getGenericBenchPlayers"]
    - path: "packages/server/src/roomHandlers.ts"
      provides: "Standard-room bench falls back to the generic bench instead of an empty array"
  key_links:
    - from: "packages/server/src/roomHandlers.ts"
      to: "getGenericBenchPlayers"
      via: "standard-room else branch fallback when the squad remainder is empty"
      pattern: "getGenericBenchPlayers"
    - from: "packages/shared/src/teams.ts"
      to: "packages/shared/src/data/player-pool.csv"
      via: "pnpm run seed:rosters"
      pattern: "AUTO-GENERATED"
---

<objective>
Give standard (non-Draft) rooms a usable bench by adding two generic 5-player placeholder bench rosters to the player pool and wiring them into the standard-room bench construction, per CONTEXT.md D-05, D-06, D-07, D-08 and D-09.

Purpose: Per D-06, every non-Draft squad in `PLAYER_POOL` holds exactly 11 players today, so `roomHandlers.ts`'s standard-room branch always produces an empty bench — the substitution screen has never had anything to offer in standard mode, while Draft mode works end to end. That branch's own comment declares the empty case deliberate under the old decision D-12; D-06 identifies Phase 46 as the phase that supersedes it. D-05 records the user explicitly acknowledging this is new capability, not a bug fix, and explicitly asking for it in Phase 46 anyway as a **simple patch**. D-09 is equally explicit that per-team roster authoring is a later milestone — this plan must not build a data-driven or per-team bench system.
Output: 10 new `PoolPlayer` records (`p189`-`p198`) generated from CSV through the existing seed pipeline; a `getGenericBenchPlayers` lookup mirroring `getSquadPlayers`; standard rooms broadcast a 5-player bench per side; substitution works through the unchanged existing path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-final-cleanup/46-CONTEXT.md
@.planning/phases/46-final-cleanup/46-RESEARCH.md
@.planning/phases/46-final-cleanup/46-PATTERNS.md
</context>

## Artifacts this phase produces

| Artifact | File | Kind |
|---|---|---|
| 10 CSV rows with `Team` values `generic-bench-home` / `generic-bench-away` | `packages/shared/src/data/player-pool.csv` | data rows (appended last) |
| `GENERIC_BENCH_SLUGS` | `packages/shared/scripts/seed-rosters.ts` | module constant (`Set<string>`) |
| `GENERIC_BENCH_NUMBER_OFFSET` | `packages/shared/scripts/seed-rosters.ts` | module constant (value `11`) |
| `numberOffset` parameter on `buildSquadEntries` | `packages/shared/scripts/seed-rosters.ts` | new optional function parameter (default `0`) |
| `EXPECTED_TOTAL` value change `188` to `198` | `packages/shared/scripts/seed-rosters.ts` | changed constant |
| `PoolPlayer` entries `p189`–`p198` | `packages/shared/src/teams.ts` | generated data (do not hand-write) |
| `GENERIC_BENCH_HOME_TEAM_ID` (`'generic-bench-home'`) | `packages/shared/src/teamConfig.ts` | new exported constant |
| `GENERIC_BENCH_AWAY_TEAM_ID` (`'generic-bench-away'`) | `packages/shared/src/teamConfig.ts` | new exported constant |
| `getGenericBenchPlayers(side: 'home' \| 'away'): PoolPlayer[]` | `packages/shared/src/teamConfig.ts` | new exported function |

No new types, no new socket events, no new GameState fields, no changes to `BenchEntry`.

<tasks>

<task type="auto">
  <name>Task 1: Seed two generic 5-player bench rosters through the existing CSV pipeline</name>
  <files>packages/shared/src/data/player-pool.csv, packages/shared/scripts/seed-rosters.ts, packages/shared/src/teams.ts, packages/shared/src/teams.test.ts, packages/shared/src/teamConfig.ts</files>
  <read_first>
    - `packages/shared/src/data/player-pool.csv` — the header row (`Player,Team,Nationality,Position,Pace,Dribbling,Highpass,Resilience,Shooting,Tackling,Aerial Ability,Saving,Handling,PoolTag`) and a sample of both a GK row and an outfield row, to see which columns are conventionally left blank for each role
    - `packages/shared/scripts/seed-rosters.ts` — the whole file, especially: the header doc comment listing each team's p-ID range (~lines 18–27), `ROLE_MAP` and `ROLE_ORDER`, `FORMATION_POSITIONS` (~lines 60–80), `toSlug` (~line 84), `parseRow`, `buildSquadEntries` (~lines 221–263, note `const number = i + 1`), the `teamOrder` first-appearance ordering loop (~lines 316–326), the per-slug build loop with its `free-agent` special case (~lines 328–364), and the `EXPECTED_TOTAL = 188` fail-fast assertion (~lines 369–375)
    - `packages/shared/src/teams.ts` — the AUTO-GENERATED header comment (lines 1–9) and the shape of one existing entry, `p001` (~lines 48–66), including `highPass: 0` for a GK
    - `packages/shared/src/teams.test.ts` — the length assertion (`toHaveLength(188)`), the last-id assertion (`toBe('p188')`), the "every PoolPlayer has all required stat fields as numbers / integers" test, the "GK players have aerialAbility > 0" test, and the sourceTeamId presence tests
    - `packages/shared/src/teamConfig.ts` — `getSquadPlayers` at ~line 408 (`PLAYER_POOL.filter((p) => p.sourceTeamId === teamId)`), the exact shape the new lookup must mirror, and the `TeamId` union / `TEAM_CONFIGS` record it sits next to
    - `packages/shared/src/draftEngine.ts` — `isInPool` (~lines 96–106) and `computeTotalStat` / `classifyTier` (~lines 50–74); confirm for yourself that a `sourceTeamId` outside `TEAM_CONFIGS` and without a `poolTag` can never match any `DraftPoolId`
    - `packages/client/src/components/NationFlag.tsx` — the `NATION_TO_ISO` map and its silent-null fallback for unmapped nationalities, so the chosen placeholder nationality actually renders a flag
    - `packages/shared/src/types.ts` — `PoolPlayer`, `PlayerPiece.role` (~line 42), `PlayerPiece`'s jersey-number doc comment (~line 37, "GK = 1; others 2-11 in ROLE_ORDER"), `BenchEntry` (~line 109), and `TIER_STAT_THRESHOLDS` (~line 1057: chase 32, rare 31, uncommon 29)
    - `.planning/phases/46-final-cleanup/46-CONTEXT.md` — D-06 (today every non-Draft squad holds exactly 11 players and the empty bench is documented as deliberate under D-12), D-07 (two generic benches, 5 players each, one per role), and D-09 (simple placeholder patch, not a per-team system)
    - `.planning/phases/46-final-cleanup/46-RESEARCH.md` — Pitfalls 2, 3, 4 and 5, and Open Question 3
  </read_first>
  <action>
Use the CSV route, not a hand-edit of `teams.ts`. Open Question 3 in 46-RESEARCH.md left this to the planner; the decision is CSV, because `teams.ts` carries an AUTO-GENERATED header and a hand-edit would be silently destroyed by the next `pnpm run seed:rosters`. The CSV route costs three small script edits, documented below.

**CSV.** Append exactly 10 rows to the END of `packages/shared/src/data/player-pool.csv` — appending last matters: the seed script assigns p-IDs in team-first-appearance order, so appending guarantees the new groups take `p189`–`p198` and no existing player's id shifts. Five rows use `Team` value `generic-bench-home`, five use `generic-bench-away` (D-07: two generic benches, not per-team data for any of the 12 real teams). Note the CSV has no `SourceTeam` column, so the script derives the slug via `toSlug(Team)`; `generic-bench-home` passes through `toSlug` unchanged.

Each group has exactly one row per `Position` value: `GK`, `DEF`, `MID`, `FWD`, `ST` — matching `PlayerPiece.role` exactly (D-07). Player names must contain exactly one space so `parseRow`'s first-space split yields a sensible first/last name; use `Home Keeper`, `Home Defender`, `Home Midfielder`, `Home Forward`, `Home Striker` and the `Away …` equivalents. Set `Nationality` to `United States` for all 10 — it is a key present in `NationFlag.tsx`'s `NATION_TO_ISO` map, so the bench card's flag renders rather than silently returning null. Leave `PoolTag` blank on all 10 (a `poolTag` value would make them draftable as legends or icons).

Stat columns: follow the CSV's existing per-role blank conventions exactly as observed in the file — for the GK rows populate `Saving`/`Handling`/`Aerial Ability` and leave `Highpass` blank; for the outfield rows leave `Saving`/`Handling` blank. Every populated value must be an integer in the 1–6 range already used throughout the file. Choose values so each player's 9-stat total (`computeTotalStat`) lands between 24 and 30 inclusive — that keeps every generic bench player in the `common` or `uncommon` tier and provably out of `rare` (31) and `chase` (32+), so the bench card's tier border reads as an ordinary player rather than a jackpot pull. Do not leave all stats blank: blanks become 0 and would render a zero-stat card.

**Seed script.** Three edits:
1. Add module constants `GENERIC_BENCH_SLUGS` (a `Set<string>` holding `'generic-bench-home'` and `'generic-bench-away'`) and `GENERIC_BENCH_NUMBER_OFFSET = 11`, with a comment citing Phase 46 / CONTEXT.md D-07 and 46-RESEARCH.md Pitfall 3 (bench numbers must not collide with the starting XI's 1-11).
2. Give `buildSquadEntries` an optional third parameter `numberOffset: number = 0` and change its `const number = i + 1` to add the offset. In the per-slug build loop, pass `GENERIC_BENCH_NUMBER_OFFSET` when `GENERIC_BENCH_SLUGS.has(slug)` and nothing otherwise, so the two generic groups get jersey numbers 12–16 and every existing squad keeps 1–11 byte-identically.
3. Change `EXPECTED_TOTAL` from `188` to `198` and update the header doc comment's p-ID range listing to append the two new groups (`generic-bench-home → p189–p193`, `generic-bench-away → p194–p198`) — confirm the actual assigned ranges from the generated output rather than assuming.

**Regenerate.** Run `pnpm --filter @counter-attack/shared run seed:rosters`. Do not hand-edit `packages/shared/src/teams.ts` — inspect the generated diff and confirm the ONLY change is 10 appended entries plus the header's player-count number. If any existing entry changed, stop: the CSV rows were not appended last.

**teamConfig.** Directly beside `getSquadPlayers`, add and export: `GENERIC_BENCH_HOME_TEAM_ID = 'generic-bench-home'`, `GENERIC_BENCH_AWAY_TEAM_ID = 'generic-bench-away'`, and `getGenericBenchPlayers(side: 'home' | 'away'): PoolPlayer[]` implemented as the same one-line `PLAYER_POOL.filter(...)` shape `getSquadPlayers` uses, selecting on the matching constant. Put it in `teamConfig.ts`, NOT `teams.ts` — 46-RESEARCH.md Pitfall 1 records that `teams.ts` holds only generated data while lookups live in `teamConfig.ts`. It reaches consumers automatically through the existing `export * from './teamConfig.js'` barrel line; do not add a new barrel line. Keep this deliberately minimal per D-09 — two constants and one filter, not a bench-composition system.

**teams.test.ts.** Update `toHaveLength(188)` to `198` and the count-arithmetic comment above it to include the 10 generic bench players; update the last-id assertion `toBe('p188')` to `toBe('p198')`. Then ADD new assertions: `getGenericBenchPlayers('home')` and `getGenericBenchPlayers('away')` each return exactly 5 players; each set covers all five roles with no duplicate role; every generic bench player's `number` is in 12–16 inclusive; every generic bench player's `computeTotalStat` classifies to `common` or `uncommon` (never `rare` or `chase`); and — the leakage guard — `resolvePoolPlayers(['original','mls','international','legends','icons'])` contains zero players whose `sourceTeamId` starts with `generic-bench`.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/data/player-pool.csv` ends with 10 rows: 5 whose `Team` column is `generic-bench-home` and 5 whose `Team` column is `generic-bench-away`, each group covering `GK`,`DEF`,`MID`,`FWD`,`ST` exactly once.
    - Re-running `pnpm --filter @counter-attack/shared run seed:rosters` after the edits leaves `git status` clean for `packages/shared/src/teams.ts` (proves the committed file is genuinely generated, not hand-edited).
    - `git diff packages/shared/src/teams.ts` against the pre-change commit shows additions only (plus the header count number) — no existing `PoolPlayer` entry's `id`, `number`, `position`, or stats changed.
    - `PLAYER_POOL` has 198 entries and its last id is `p198`.
    - Every generic bench player has `number` between 12 and 16 inclusive; no generic bench player has `number` at or below 11.
    - Every generic bench player has all 9 stat fields as integers, and `computeTotalStat` for each is at most 30 (below the `rare` threshold of 31).
    - `getGenericBenchPlayers('home')` and `getGenericBenchPlayers('away')` each return exactly 5 players, one per role.
    - `resolvePoolPlayers(['original','mls','international','legends','icons'])` returns zero players with a `generic-bench` `sourceTeamId` — asserted by a test, not by inspection.
    - `pnpm --filter @counter-attack/shared test` exits 0.
    - `pnpm -w typecheck` exits 0 and `pnpm knip` reports zero findings (the three new `teamConfig.ts` exports must be consumed — Task 2 consumes `getGenericBenchPlayers`; if the two ID constants end up unconsumed, inline them rather than leaving dead exports).
  </acceptance_criteria>
  <done>`PLAYER_POOL` holds 198 generated entries including two 5-player generic benches numbered 12-16 that are invisible to every draft pool; `teams.ts` is still reproducible from CSV; `getGenericBenchPlayers` mirrors `getSquadPlayers`; the shared test suite pins all of it.</done>
</task>

<task type="auto">
  <name>Task 2: Hand the generic bench to standard rooms at game-state construction</name>
  <files>packages/server/src/roomHandlers.ts, packages/server/src/__tests__/lineupAssignment.integration.test.ts, packages/server/src/__tests__/substitution.integration.test.ts</files>
  <read_first>
    - `packages/server/src/roomHandlers.ts` lines ~955–995 — the whole bench-construction block: the Phase 40 comment above it stating the bench is a server-authoritative source never taken from the client's `confirmedOrder`, the `if (isDraftRoom)` branch (~lines 967–978) whose `BenchEntry` mapping shape the new code must mirror, and the `else` branch (~lines 979–993) carrying the D-12 "this is expected behavior, not a gap" comment that D-06 identifies Phase 46 as superseding
    - `packages/server/src/roomHandlers.ts` line ~48 — the existing `getSquadPlayers` import from `@counter-attack/shared`, where `getGenericBenchPlayers` joins it
    - `packages/server/src/gameEngine.ts` — `applySubstitution`'s guards (~lines 3620–3700), specifically guard 5 (bench-entry lookup), guard 8 (the incoming id must resolve in `PLAYER_POOL`), guard 9 (GK parity), and the `newPiece` construction that spreads `...outPiece` so the substitute inherits the slot's `id`/`teamId`/`number`/`position`. Read this to confirm for yourself that a generic bench player needs no new code path — 46-RESEARCH.md Pitfall 5 required this check
    - `packages/server/src/__tests__/lineupAssignment.integration.test.ts` lines ~385–405 — the test titled "Phase 40 D-12: a STANDARD room's first broadcast GameState has an EMPTY bench for both teams — nothing generated", which asserts `stateA.bench?.home` and `stateA.bench?.away` both have length 0. This test encodes the behaviour being deliberately replaced and MUST be rewritten, not deleted
    - `packages/server/src/__tests__/substitution.integration.test.ts` — how an end-to-end substitution is driven in a test, so the new coverage reuses the existing harness
    - `packages/shared/src/types.ts` — `BenchEntry` (`playerId` is a `PLAYER_POOL` id, NOT a `PlayerPiece.id`), and `GameState.bench`
    - `.planning/phases/46-final-cleanup/46-CONTEXT.md` — D-05 (deliberate informed scope addition), D-06 (the superseded D-12 empty-bench stance), D-08 (inject for standard/non-Draft mode only, at the same point `confirmedHomeBench`/`confirmedAwayBench` are currently built), and D-09 (placeholder patch only)
  </read_first>
  <action>
Per D-08, the injection point is exactly where `confirmedHomeBench` and `confirmedAwayBench` are already built for standard rooms — the `else` branch at ~line 979 — and standard (non-Draft) mode only. Do not add a second construction site and do not touch the `isDraftRoom` branch.

In that `else` branch, keep the existing `getSquadPlayers(...).filter(...)` squad-remainder computation for both sides — it is harmless and becomes live automatically if a future milestone authors squads deeper than 11, which is the D-09 follow-on. Then, for each side independently, fall back to the generic bench when that side's squad remainder is empty: map `getGenericBenchPlayers('home')` / `getGenericBenchPlayers('away')` into `BenchEntry` objects using the exact same mapping the surrounding code already uses — `{ playerId: p.id, jerseyNumber: p.number, status: 'available' as const }`. Do not invent a different mapping and do not build the `BenchEntry` list from hardcoded literals; the numbers must come from the generated `PoolPlayer.number` so the 12–16 range is enforced in exactly one place.

Import `getGenericBenchPlayers` alongside the existing `getSquadPlayers` import from `@counter-attack/shared`.

Replace the D-12 "no substitute is generated … This is expected behavior, not a gap" comment with a Phase 46 comment recording that CONTEXT.md D-05 through D-09 supersede D-12 for standard rooms, that D-06 identified this phase as the one that does so, that the two benches are deliberately generic placeholders rather than per-team authored data (D-07), that per-team roster depth is a later-milestone concern (D-09), and that the fallback is per-side and only fires when the squad remainder is empty (D-08). Do not delete the note that the bench is server-authoritative and never sourced from the client's `confirmedOrder` payload — that stance is unchanged and load-bearing.

Do NOT touch `applySubstitution` or any other engine code. Guard 8 already resolves the incoming id against `PLAYER_POOL`, guard 9 already enforces GK parity, and the substitute already inherits the slot's own `number`/`position` from `...outPiece` — a generic bench player is indistinguishable in shape from any other `PoolPlayer`, which is exactly the property D-09's "simple patch" framing requires. If reading the engine reveals it does need a change, stop and report rather than adding a generic-player special case.

Do NOT add a per-team bench, a bench-size setting, a UI affordance, or any data-driven bench system (D-09).

Rewrite the `lineupAssignment.integration.test.ts` empty-bench test rather than deleting it: retitle it for Phase 46 / CONTEXT.md D-05 through D-09, and change its assertions to require that a standard room's first broadcast `GameState` has `bench.home` and `bench.away` each of length 5, that every entry's `status` is `'available'`, that every entry's `jerseyNumber` is in 12–16, and that the five entries per side resolve through `PLAYER_POOL` to five distinct roles. Keep the test's existing room-setup harness unchanged so the assertion change is the only delta.

Add one end-to-end substitution test to `substitution.integration.test.ts` proving that in a STANDARD room a generic bench player can actually be substituted onto the pitch: sub a generic bench outfielder in for an outfield starter during a stoppage phase, then assert the pitch piece at that slot now carries the generic player's `playerId`, name and stats while keeping the slot's own `id` and `position`. Also assert the GK-parity guard still rejects substituting the generic GK into an outfield slot.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- lineupAssignment substitution</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getGenericBenchPlayers" packages/server/src/roomHandlers.ts` returns at least 2 (one import, one or more call sites).
    - `grep -c "This is expected behavior, not a gap" packages/server/src/roomHandlers.ts` returns 0.
    - The replacement comment cites `D-06` and `D-08` explicitly.
    - The standard-room branch still calls `getSquadPlayers` for both sides (the squad-remainder path was kept, not deleted).
    - No `BenchEntry` in `roomHandlers.ts` is built from a hardcoded jersey-number literal — every `jerseyNumber` comes from a `PoolPlayer.number`.
    - `git diff --stat` shows no change to `packages/server/src/gameEngine.ts`.
    - The `lineupAssignment.integration.test.ts` bench test asserts `bench.home` and `bench.away` each have length 5, all `status: 'available'`, all `jerseyNumber` in 12–16, and five distinct roles per side.
    - `substitution.integration.test.ts` contains a test proving a generic bench player substitutes onto the pitch in a standard room and that the resulting piece keeps the outgoing slot's `id` and `position` while carrying the incoming player's `playerId` and stats.
    - A test proves the GK-parity guard still rejects the generic GK for an outfield slot.
    - `pnpm --filter @counter-attack/server test` exits 0.
    - `pnpm -w typecheck` exits 0 and `pnpm knip` reports zero findings.
  </acceptance_criteria>
  <done>A standard room broadcasts a 5-player available bench per side sourced from the generated generic bench rosters, substitution works end to end through the unchanged engine path, and the test that previously asserted an empty bench now asserts the new behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (`roomHandlers` game-state construction) | Existing boundary, unchanged in shape. The bench is built server-side from `PLAYER_POOL` and never from a client payload; this plan preserves that. |
| client → server (`game:substitution` action) | Existing boundary, unchanged. `applySubstitution` validates the incoming `playerId` against the server-owned `PLAYER_POOL` and the server-owned `state.bench`; this plan adds pool entries but no new validation path. |

Per 46-RESEARCH.md's Security Domain section, no authentication, session, access-control, or cryptography surface is touched. The one server-side change is a data-seeding branch that runs on server-authoritative room state.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04-T1 | Tampering | standard-room bench construction in `roomHandlers.ts` | mitigate | The bench must remain server-authored. The fallback reads `getGenericBenchPlayers()` from the server's own `PLAYER_POOL` and never from `room.confirmedOrder` or any socket payload; the existing "never sourced from the client's confirmedOrder payload (T-40-11)" comment is preserved as the standing statement of this control. |
| T-46-04-T2 | Tampering | `game:substitution` incoming player id | mitigate | Unchanged existing control: `applySubstitution` guard 5 requires the id to be present in the server-owned `state.bench`, and guard 8 requires it to resolve in `PLAYER_POOL`. This plan is forbidden from modifying `gameEngine.ts`, so neither guard can be weakened; the acceptance criteria assert a zero diff on that file. |
| T-46-04-E | Elevation of Privilege | draft-pool eligibility of the new pool entries | mitigate | New `PoolPlayer` entries could otherwise leak into draft packs and let a player draft an off-roster placeholder. Mitigation: `sourceTeamId` values `generic-bench-home`/`generic-bench-away` are absent from `TEAM_CONFIGS` (so `isInPool` returns false for `mls`/`international`), are not `free-agent` (false for `original`), and carry no `poolTag` (false for `legends`/`icons`). Enforced by an explicit `resolvePoolPlayers` leakage test in Task 1's acceptance criteria. |
| T-46-04-S | Spoofing | jersey-number collision between bench and starting XI | mitigate | Duplicate jersey numbers on one team sheet would make two players visually indistinguishable in any number-keyed display. Mitigation: `GENERIC_BENCH_NUMBER_OFFSET = 11` in the seed script forces 12–16, and a test asserts every generic bench player's `number` is in that range. |
| T-46-04-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages (46-RESEARCH.md Package Legitimacy Audit: this phase adds zero dependencies). The only script run is the repo's own `pnpm run seed:rosters`. If any task appears to require an install, stop and escalate. |
</threat_model>

<verification>
Run in order:

1. `pnpm --filter @counter-attack/shared run seed:rosters` — must complete without the `Expected N players` fail-fast throwing.
2. `git status --short packages/shared/src/teams.ts` — must be clean immediately after step 1 (proof the committed generated file matches the CSV).
3. `pnpm --filter @counter-attack/shared test`
4. `pnpm --filter @counter-attack/server test`
5. `pnpm -w typecheck`
6. `pnpm knip`

All must exit 0; knip must report zero findings.
</verification>

<success_criteria>
- `PLAYER_POOL` has 198 entries; `p189`–`p198` are the two generic benches, one player per role per side, jersey numbers 12–16.
- `packages/shared/src/teams.ts` is reproducible byte-for-byte by re-running the seed script; no existing player's id, number, position, or stats changed.
- No generic bench player is reachable through any `DraftPoolId`, proven by a test.
- A standard room's first broadcast `GameState` carries 5 `available` bench entries per side, injected at the D-08 site only.
- A generic bench player substitutes onto the pitch end to end with no change to `gameEngine.ts`; GK parity still enforced.
- Shared and server suites, `pnpm -w typecheck`, and `pnpm knip` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/46-final-cleanup/46-04-SUMMARY.md` when done. Record the actual p-ID ranges assigned to the two generic bench groups and the chosen stat values per placeholder player.
</output>
