---
phase: 46-final-cleanup
plan: 06
type: execute
wave: 3
depends_on: ["46-01", "46-02", "46-03", "46-04", "46-05"]
files_modified:
  - .planning/phases/46-final-cleanup/46-VALIDATION.md
  - .planning/phases/46-final-cleanup/46-AUDIT.md
autonomous: true
requirements: [CLEANUP-13]
must_haves:
  truths:
    - "pnpm -w typecheck, pnpm -w lint, pnpm -w test and pnpm knip all exit 0 across all three packages after every Phase 46 change has landed"
    - "No code removed or replaced during Phase 46 left behind an orphaned import, an unused local, or a comment describing code that no longer exists"
    - "46-VALIDATION.md names the real plan and task IDs for every requirement and records each one's observed status"
  artifacts:
    - path: ".planning/phases/46-final-cleanup/46-VALIDATION.md"
      provides: "Per-task verification map backfilled with real plan/task IDs and observed statuses; nyquist_compliant set"
      contains: "nyquist_compliant"
    - path: ".planning/phases/46-final-cleanup/46-AUDIT.md"
      provides: "CLEANUP-13 dead-code sweep section appended to the Phase 46 audit record"
      contains: "CLEANUP-13"
  key_links:
    - from: ".planning/phases/46-final-cleanup/46-VALIDATION.md"
      to: "the five preceding plans"
      via: "each row's Plan/Task ID column resolved from TBD to a real id"
      pattern: "46-0[1-5]"
---

<objective>
Run the whole-monorepo green gate that is CLEANUP-13's own success criterion, sweep the files Phase 46 touched for residue left by the removals and relocations, and bring the phase's validation contract up to date with the plans that were actually written.

Purpose: ROADMAP Phase 46 success criterion 5 is "Dead code identified during the milestone is removed, and the full monorepo build/typecheck/test suite stays green." 46-RESEARCH.md's Environment Availability table records that `pnpm -w lint` and `pnpm -w test` were never run during research (only `knip` and `typecheck` were, both clean) — so this gate is the first time the full suite is exercised against this phase's changes, and it is not optional. 46-RESEARCH.md Pitfall 6 also warns that a clean knip/tsc does not mean the audit is done: those tools miss stale comments, orphaned locals, and residue from deleted bindings.
Output: All four gate commands green; a recorded dead-code sweep over exactly the files this phase touched; `46-VALIDATION.md` carrying real task IDs and observed statuses.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-final-cleanup/46-RESEARCH.md
@.planning/phases/46-final-cleanup/46-VALIDATION.md
</context>

## Artifacts this phase produces

| Artifact | File | Kind |
|---|---|---|
| `## CLEANUP-13 — Dead-Code Sweep` section | `.planning/phases/46-final-cleanup/46-AUDIT.md` | appended doc section (per-file residue findings plus disposition) |
| Backfilled Per-Task Verification Map rows | `.planning/phases/46-final-cleanup/46-VALIDATION.md` | replaced table rows (`TBD` becomes a real plan/task id, `pending` becomes an observed status) |
| `nyquist_compliant: true` | `.planning/phases/46-final-cleanup/46-VALIDATION.md` | frontmatter value change |
| `status: complete` | `.planning/phases/46-final-cleanup/46-VALIDATION.md` | frontmatter value change |

No code artifacts are produced by this plan unless the gate surfaces a failure that must be fixed.

<tasks>

<task type="auto">
  <name>Task 1: Run the full monorepo gate and sweep Phase 46's touched files for removal residue</name>
  <files>.planning/phases/46-final-cleanup/46-AUDIT.md</files>
  <read_first>
    - `.planning/phases/46-final-cleanup/46-01-SUMMARY.md` through `46-05-SUMMARY.md` — the authoritative list of every file Phase 46 actually touched, including any extra files plan 46-03 Task 3 modified while closing help-text gaps and any tint change plan 46-05 Task 1 reported
    - `.planning/phases/46-final-cleanup/46-AUDIT.md` — the CLEANUP-07 and CLEANUP-12 sections created by plan 46-03, so the new section is appended in the same document voice and structure
    - `.planning/phases/46-final-cleanup/46-RESEARCH.md` — the "Environment Availability" table (recording that lint and the full test suite were deliberately not run during research, and that `eslint .` on the whole monorepo took 100-170s in the research environment, so this run needs a raised timeout) and Pitfall 6 (clean tooling does not mean the audit is finished)
    - `knip.json` at the repo root — which entry points and workspaces it covers, so a new finding can be interpreted rather than suppressed
    - Root `package.json` — the `typecheck`, `lint`, `test` and `knip` script definitions, so the exact commands are run rather than approximated
  </read_first>
  <action>
Run all four gate commands against the merged result of plans 46-01 through 46-05, in this order, allowing a raised timeout for lint (46-RESEARCH.md measured `eslint .` at 100-170s, which may exceed a default 120s budget — raise the timeout rather than narrowing the command to changed files):

`pnpm -w typecheck`, then `pnpm -w lint`, then `pnpm -w test`, then `pnpm knip`.

All four must exit 0 and `pnpm knip` must report zero findings. If any fails, fix the cause in the owning source file and re-run the full sequence from the top. Do not fix forward by narrowing a command's scope, adding an eslint-disable, suppressing a knip entry in `knip.json`, weakening an assertion, or deleting a test. If a failure genuinely requires a design decision rather than a mechanical fix, stop and report it instead of guessing.

Note the known platform flake recorded in project memory: vitest can crash a worker on this machine; the documented workaround is rerunning with `--pool=forks`. If that occurs, rerun with the workaround, and record both the flake and the workaround in the sweep section rather than silently passing.

Then perform the bounded dead-code sweep. Build the file list from the five preceding summaries — do not sweep the whole repo, since 46-RESEARCH.md Pitfall 6 scopes this explicitly as a bounded planner task rather than an open-ended pass. For each file, check for exactly these five residue classes, which `knip` and `tsc` provably do not catch:

1. Imports that are now unused because their only consumer was removed — especially `useGameStore.ts`, where six `*_CONFIG` bindings were deleted, `GameSettingsScreen.tsx`, where a `.section` wrapper was removed, and `HexGrid.tsx`, where inline phase literals were replaced by a set membership test.
2. Locals, helpers, or CSS classes that are now unreferenced — in particular any `GameSettingsScreen.module.css` rule whose only consumer was the removed standalone Match Speed `.section`.
3. Comments describing code that no longer exists or values that are no longer true — including any remaining reference to `Free Move!`, `size={56}`, a "4-column" or "7-stat" grid, a "17-phase list", the six deleted `*_CONFIG` binding names, or the inline `GK_DIVE`/`SNAPSHOT_DEFLECT` tint exclusions, each used in a context implying it still exists.
4. Documentation counts that drift from the code they describe — `docs/HIGHLIGHT-REFERENCE.md`'s ball-marker phase list versus `BALL_MARKER_PHASES.size`, its valid-move exception list versus `VALID_MOVE_TINT_EXCEPTION_PHASES`, and `packages/shared/src/teams.ts`'s header player count versus `PLAYER_POOL.length`.
5. Planning-doc statements this phase superseded — specifically the `roomHandlers.ts` D-12 "expected behavior, not a gap" claim, which plan 46-04 was required to replace.

Append a `## CLEANUP-13 — Dead-Code Sweep` section to `.planning/phases/46-final-cleanup/46-AUDIT.md` with one row per swept file: file path, residue classes checked, finding (`none` or a description), and disposition (`fixed` or `n/a`). Fix every finding in place as you go and list the fixed files in the plan summary. Close the section by recording the four gate commands with their exit codes and the observed `pnpm -w lint` wall-clock duration, so the next milestone's planner has a measured number instead of research's estimate.
  </action>
  <verify>
    <automated>pnpm -w typecheck &amp;&amp; pnpm -w lint &amp;&amp; pnpm -w test &amp;&amp; pnpm knip</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -w typecheck`, `pnpm -w lint`, `pnpm -w test` and `pnpm knip` each exit 0, run in that order against the merged result of plans 46-01 through 46-05.
    - `pnpm knip` output contains zero findings, and `git diff knip.json` is empty (no finding was suppressed via config).
    - `git diff` shows no `eslint-disable` comment added anywhere in this phase.
    - No test file lost a test or had an assertion weakened during this task — `git diff` on `*.test.ts` and `*.test.tsx` shows additions only, or no change.
    - `.planning/phases/46-final-cleanup/46-AUDIT.md` contains a `## CLEANUP-13 — Dead-Code Sweep` section with one row per file listed across the five preceding summaries, each row naming a finding and a disposition.
    - Every row with a non-`none` finding has disposition `fixed`, and each fixed file appears in the plan summary.
    - The sweep section records the four gate commands, their exit codes, and the measured `pnpm -w lint` duration.
    - `grep -rn "Free Move!" packages/client/src --include=*.tsx` returns no JSX text-node match.
    - `grep -c "size={56}" packages/client/src/components/PlayerStatsPanel.tsx` returns 0.
    - `grep -c "17-phase" docs/HIGHLIGHT-REFERENCE.md` returns 0.
    - The ball-marker phase list and the valid-move exception list in `docs/HIGHLIGHT-REFERENCE.md` each match their code constant's membership.
    - The player count stated in `packages/shared/src/teams.ts`'s header comment equals `PLAYER_POOL.length` (198).
  </acceptance_criteria>
  <done>All four monorepo gate commands are green with no suppressions, the bounded residue sweep over Phase 46's touched files is recorded in `46-AUDIT.md` with every finding fixed, and no doc-versus-code count remains stale.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill the phase validation contract with real task IDs and observed statuses</name>
  <files>.planning/phases/46-final-cleanup/46-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/46-final-cleanup/46-VALIDATION.md` — the whole file: its frontmatter (`status: draft`, `nyquist_compliant: false`), the Per-Task Verification Map whose Plan/Wave/Task ID columns are all `TBD` and whose Status column is all pending, the trailing note stating the planner must fill in actual plan/wave/task numbers, the Manual-Only Verifications table, and the Validation Sign-Off checklist
    - `.planning/phases/46-final-cleanup/46-01-SUMMARY.md` through `46-05-SUMMARY.md` — the real task names and numbers, the automated command each task actually ran, and any test file path that differed from the one research predicted
    - `.planning/phases/46-final-cleanup/46-AUDIT.md` — including the CLEANUP-13 sweep section written in Task 1, which supplies the gate row's evidence, and the CLEANUP-07 and CLEANUP-12 sections, which supply the audit rows' evidence
    - `.planning/phases/46-final-cleanup/46-RESEARCH.md` — the "Validation Architecture" requirement-to-test map the VALIDATION.md table was derived from, so any correction stays traceable
  </read_first>
  <action>
Replace every `TBD` in the Per-Task Verification Map with the real plan id, wave number, and task id read from the five preceding summaries. For example, the CLEANUP-06 ball-marker row resolves to plan `46-01`, wave 1, and that plan's Task 1; the CLEANUP-11 layout row resolves to plan `46-05`, wave 2, Task 2. Where a row's predicted test file turned out to have a different path or name than research guessed — research explicitly flagged the `useGameStore`, `PlayerStatsPanel` and `roomHandlers` rows as unconfirmed — correct the File Exists column to the path actually used and note the correction inline.

Add rows for every requirement the plans covered that the original table omitted, so that all nine of CLEANUP-05 through CLEANUP-13 appear at least once and the table is a complete requirement-to-evidence map rather than a partial one. At minimum this means adding rows for CLEANUP-07, CLEANUP-09 (both the store config-table consolidation in 46-02 and the valid-move tint consolidation in 46-05), CLEANUP-12, and the generic-bench portion of CLEANUP-05. For the audit-shaped requirements CLEANUP-07 and CLEANUP-12, set Test Type to `audit` and cite the corresponding `46-AUDIT.md` section as the evidence rather than inventing a vitest command that does not exist.

Set each row's Status from what was actually observed: green where the row's automated command passed in its own plan's execution or in this plan's Task 1 gate, red where it did not, flaky where a rerun was required. If the known vitest worker-crash flake occurred, mark the row flaky and note the `--pool=forks` workaround. Do not mark a row green that was not actually run.

Leave the Manual-Only Verifications table's content unchanged and leave its items unticked — plan 46-07 executes them.

Update the frontmatter to `status: complete` and `nyquist_compliant: true`. Tick the Validation Sign-Off items only where genuinely satisfied; if any item, such as "no 3 consecutive tasks without automated verify", is not satisfiable from the executed plans, leave it unticked and add a one-line reason beneath the checklist rather than ticking it optimistically.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const t = fs.readFileSync('.planning/phases/46-final-cleanup/46-VALIDATION.md','utf8'); const ids = ['CLEANUP-05','CLEANUP-06','CLEANUP-07','CLEANUP-08','CLEANUP-09','CLEANUP-10','CLEANUP-11','CLEANUP-12','CLEANUP-13']; const missing = ids.filter(i => !t.includes(i)); const tbd = (t.match(/TBD/g) || []).length; const nyq = t.includes('nyquist_compliant: true'); if (missing.length || tbd > 0 || !nyq) { console.error(JSON.stringify({ missing, tbd, nyq })); process.exit(1); } console.log('validation contract ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `46-VALIDATION.md` contains zero occurrences of the string `TBD`.
    - All nine requirement ids CLEANUP-05 through CLEANUP-13 appear in the Per-Task Verification Map.
    - Every Per-Task Verification Map row names a real plan id matching `46-0[1-6]` and a real task number, and no row's Status is still pending.
    - The CLEANUP-07 and CLEANUP-12 rows have Test Type `audit` and cite a `46-AUDIT.md` section as evidence rather than a vitest command.
    - Frontmatter reads `status: complete` and `nyquist_compliant: true`.
    - The Manual-Only Verifications table is byte-identical to its pre-task content and its items remain unticked.
    - Any unticked Validation Sign-Off item has a one-line reason recorded beneath the checklist.
    - The verify command exits 0.
  </acceptance_criteria>
  <done>`46-VALIDATION.md` is a complete, honest requirement-to-evidence map for all nine CLEANUP requirements, with real plan and task ids, observed statuses, no TBDs, and manual verifications left for plan 46-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | This plan runs read-only verification tooling and edits two planning documents. No production code path, socket event, or input-validation surface is added or altered, except where a gate failure forces a mechanical fix in the owning source file. |

Confirmed against 46-RESEARCH.md's Security Domain section: no authentication, session, access-control, cryptography, or external input-validation surface is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-06-T | Tampering | the CLEANUP-13 verification gate itself | mitigate | The most realistic failure mode of a green-gate task is the gate being weakened to pass — suppressing a knip finding in `knip.json`, adding an `eslint-disable`, narrowing a command's scope, or deleting a failing test. Mitigation: the action text forbids all four explicitly, and the acceptance criteria assert an empty `knip.json` diff, zero added `eslint-disable` comments, and additions-only diffs on test files. |
| T-46-06-R | Repudiation | `46-VALIDATION.md` status backfill | mitigate | Marking a row green that was never run would leave a false evidence trail for `/gsd-verify-work` and the milestone audit. Mitigation: statuses must be set from observed runs only, flaky reruns must be recorded with their workaround, and unsatisfiable sign-off items must be left unticked with a written reason rather than ticked. |
| T-46-06-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages (46-RESEARCH.md Package Legitimacy Audit: this phase adds zero dependencies). Every command run is an existing repo script. If a gate failure appears to require a new dependency, stop and escalate rather than installing. |
</threat_model>

<verification>
Run in order:

1. `pnpm -w typecheck`
2. `pnpm -w lint` (raise the timeout — 46-RESEARCH.md measured 100-170s)
3. `pnpm -w test`
4. `pnpm knip`
5. The Task 2 validation-contract check command

All five must exit 0. `pnpm knip` must report zero findings. `git diff knip.json` must be empty.
</verification>

<success_criteria>
- `pnpm -w typecheck`, `pnpm -w lint`, `pnpm -w test` and `pnpm knip` all exit 0 against the merged Phase 46 result, with no suppression, no disabled rule, and no weakened or deleted test.
- `46-AUDIT.md` carries a `## CLEANUP-13 — Dead-Code Sweep` section covering every file Phase 46 touched, with every finding fixed and the gate results and lint duration recorded.
- No documentation count in the repo contradicts the code it describes (ball-marker phase list, valid-move exception list, player-pool count, stat-grid comment).
- `46-VALIDATION.md` has zero `TBD`s, covers all nine CLEANUP requirement ids with real plan and task ids and observed statuses, and is marked `status: complete` / `nyquist_compliant: true` with the manual verifications still open for plan 46-07.
</success_criteria>

<output>
Create `.planning/phases/46-final-cleanup/46-06-SUMMARY.md` when done. Record the four gate exit codes, the measured lint duration, every residue finding fixed, and any row marked red or flaky.
</output>
