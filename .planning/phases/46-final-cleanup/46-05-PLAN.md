---
phase: 46-final-cleanup
plan: 05
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/HexGrid.test.tsx
  - packages/client/src/components/PlayerStatsPanel.tsx
  - packages/client/src/components/PlayerStatsPanel.test.tsx
  - docs/HIGHLIGHT-REFERENCE.md
autonomous: true
requirements: [CLEANUP-06, CLEANUP-09, CLEANUP-11, CLEANUP-13]
must_haves:
  truths:
    - "Every phase that populates validMoveHexes renders those hexes with the same valid-move tint, except where a divergence is deliberate and documented"
    - "The set of deliberate valid-move tint exceptions is enumerated in one place and mirrored in docs/HIGHLIGHT-REFERENCE.md"
    - "The pitch player card's team badge is the same size as the roster card's and bench card's team badge"
    - "The pitch player card's stat-grid doc comment describes the grid the code actually renders"
  artifacts:
    - path: "packages/client/src/components/HexGrid.tsx"
      provides: "Enumerated, commented valid-move tint exception set replacing the ad hoc inline phase exclusions in isHighlighted"
    - path: "packages/client/src/components/PlayerStatsPanel.tsx"
      provides: "TeamBadge at size 48 matching the roster/bench card family, and a corrected stat-grid comment"
      contains: "size={48}"
    - path: "docs/HIGHLIGHT-REFERENCE.md"
      provides: "Valid-move tint consistency section listing every phase that tints valid moves and every deliberate exception"
  key_links:
    - from: "packages/client/src/components/HexGrid.tsx"
      to: "HIGHLIGHT_STYLES in HexCell.tsx"
      via: "the single priority-resolved highlightType ternary"
      pattern: "highlightType"
    - from: "docs/HIGHLIGHT-REFERENCE.md"
      to: "packages/client/src/components/HexGrid.tsx"
      via: "documented exception list mirrors the code constant"
      pattern: "valid-move"
---

<objective>
Close the two remaining visual-consistency requirements that CONTEXT.md raised by name: D-02's valid-move hex tint consistency across movement pattern types (CLEANUP-06/CLEANUP-09), and the pitch/roster/bench card alignment (CLEANUP-11, scoped by D-04).

Purpose: CONTEXT.md D-02 asks that the valid-move hex tint be visually consistent across different movement pattern types, in the same single-source-of-truth spirit as the `HIGHLIGHT_STYLES`/`RING_STYLES` table. `HexGrid.tsx`'s `isHighlighted` derivation (~lines 477–491) already carries a "Bug 2 fix" comment recording that `HIGH_PASS_MOVE` and `GK_KICK_MOVE` were once inconsistent and were brought onto the shared tint, while `GK_DIVE` and `SNAPSHOT_DEFLECT` remain deliberately excluded as inline negated phase literals. That is the exact shape D-02 targets: the rule exists but is expressed as ad hoc exclusions inside one boolean rather than as an enumerated, documented set. Separately, D-04 scopes CLEANUP-11 to whatever the audit finds misaligned beyond Phase 41's already-unified `CardInjuryBadge`; 46-RESEARCH.md found exactly one divergence — `PlayerStatsPanel.tsx`'s 56px team badge against the roster/bench family's 48px — plus a stale stat-grid comment.
Output: One enumerated valid-move tint exception set in `HexGrid.tsx`, mirrored in `docs/HIGHLIGHT-REFERENCE.md`; the pitch player card badge aligned to 48px with its comment corrected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-final-cleanup/46-CONTEXT.md
@.planning/phases/46-final-cleanup/46-RESEARCH.md
@.planning/phases/46-final-cleanup/46-PATTERNS.md
@.planning/phases/46-final-cleanup/46-UI-SPEC.md
@.planning/phases/46-final-cleanup/46-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | File | Kind |
|---|---|---|
| `VALID_MOVE_TINT_EXCEPTION_PHASES` | `packages/client/src/components/HexGrid.tsx` | new module-scope `ReadonlySet<GamePhase>` constant enumerating the phases whose valid-move hexes deliberately do NOT take the shared `safe` tint |
| `TeamBadge size={48}` on the pitch player card | `packages/client/src/components/PlayerStatsPanel.tsx` | changed prop value (was `56`) |
| `## Valid-Move Tint Consistency` section | `docs/HIGHLIGHT-REFERENCE.md` | new doc section mirroring the exception set |

No new colors, no new `HexHighlightType` members, no new CSS classes, no new components.

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and document the valid-move hex tint rule and its deliberate exceptions</name>
  <files>packages/client/src/components/HexGrid.tsx, packages/client/src/components/HexGrid.test.tsx, docs/HIGHLIGHT-REFERENCE.md</files>
  <read_first>
    - `packages/client/src/components/HexGrid.tsx` lines ~420–640 — the whole per-hex derivation block: `isValidMove` (~line 425), the `isHighlighted` derivation (~lines 477–491) including its "Bug 2 fix" comment stating that `HIGH_PASS_MOVE` and `GK_KICK_MOVE` valid-move hexes now use the yellow `safe` tint like normal movement while `SNAPSHOT_DEFLECT` keeps the white shot-path tint and `GK_DIVE` is suppressed, `isHpMoveTarget` (~lines 492–494), `isSafeTint` (~line 570), `isShotPathActionTint` and `isShotPathTint` (~lines 537–545), `isThrowInHexTint` (~lines 516–522), and the single priority-resolved `highlightType` ternary (~lines 613–637) with its "Plan 06 (HILITE-01)" comment recording that every tint now flows through `HexCell`'s props with no ad hoc inline polygon siblings left
    - `packages/client/src/components/HexCell.tsx` — `HIGHLIGHT_STYLES` (~line 18) and the `HexHighlightType` union it is keyed by, so no new member is invented
    - `packages/client/src/store/useGameStore.ts` — every site that assigns `validMoveHexes`, so the phase list in this audit is derived from the code rather than remembered. The `compute*ValidHexes` helpers named in 46-RESEARCH.md's CLEANUP-09 section (`computeMovementValidHexes`, `computeKickOffSetupValidHexes`, `computeFreeKickSetupValidHexes`, `computeResponseMoveValidHexes`, `computeFreeMoveValidHexes`, `computeCornerRepositionValidHexes`, `computePenaltyKickValidHexes`, `computeGkDiveAtFeetTargetHexes`) are the entry points to trace
    - `docs/HIGHLIGHT-REFERENCE.md` — the whole file as updated by plan 46-01, especially the `HIGHLIGHT_STYLES` tint table, the "Adding a New Highlight" checklist, and the structure of the existing per-mechanism sections, so the new section matches their shape
    - `packages/client/src/components/HexGrid.test.tsx` — how the grid is rendered in tests and how an existing tint assertion is written, so the new assertions reuse that harness
    - `.planning/phases/46-final-cleanup/46-CONTEXT.md` — D-02 (the requirement this task satisfies) and D-03 (consolidation depth is planner judgment; no speculative refactor)
    - `.planning/phases/46-final-cleanup/46-01-SUMMARY.md` — the final `BALL_MARKER_PHASES` count, so the doc edits in this task stay consistent with 46-01's
  </read_first>
  <action>
First, build the inventory. Trace every phase in which `validMoveHexes` becomes non-empty, using the `compute*ValidHexes` call sites in `useGameStore.ts` as the entry points. For each such phase, determine which `HexHighlightType` its valid-move hexes actually resolve to through `HexGrid.tsx`'s `highlightType` priority ternary. Expect the large majority to resolve to `safe`; the known exceptions from the code's own comments are `GK_DIVE` (suppressed entirely) and `SNAPSHOT_DEFLECT` (white shot-path tint, on the stated rationale that a defender moving to intercept a snapshot is a different UX context).

Then make the rule explicit rather than inline. Add a module-scope constant `VALID_MOVE_TINT_EXCEPTION_PHASES`, typed `ReadonlySet<GamePhase>`, holding exactly the phases whose valid-move hexes deliberately do not take the shared `safe` tint. Replace the negated inline phase literals inside the `isHighlighted` derivation (`phase !== 'GK_DIVE' && phase !== 'SNAPSHOT_DEFLECT'`) with a single membership test against that constant. Carry the existing "Bug 2 fix" rationale into the constant's comment verbatim in substance, and add for each member a one-line reason it is excepted, citing `Phase 46 / CLEANUP-06 / D-02`. This mirrors the `BALL_MARKER_PHASES` pattern that plan 46-01 just extended: one named, commented, exported-for-testing set instead of literals buried in a boolean.

This is a behaviour-preserving change by default. If, and only if, the inventory turns up a phase whose valid-move hexes resolve to a tint OTHER than `safe` with no documented rationale anywhere in the code, that is a genuine D-02 divergence: bring it onto `safe` and record the change in the plan summary as a behaviour change with the before and after tint named. Do NOT change the two documented exceptions (`GK_DIVE`, `SNAPSHOT_DEFLECT`) — their rationale is recorded in code and predates this phase. Do NOT alter the `highlightType` ternary's priority order, and do NOT add a new `HexHighlightType` member or a new color; per D-03 the default is to make the existing rule explicit, not to redesign it.

Export `VALID_MOVE_TINT_EXCEPTION_PHASES` so the test can assert against it, matching how `BALL_MARKER_PHASES` is exported for the same reason. Re-run `pnpm knip` afterwards to confirm the new export is consumed and produces no finding.

In `HexGrid.test.tsx`: add a test asserting that `VALID_MOVE_TINT_EXCEPTION_PHASES` contains exactly the phases the audit identified (assert the member count and each member by name), and at least two rendering assertions proving the rule holds — one phase from the shared-tint majority renders its valid-move hexes with the `safe` tint, and one exception phase does not.

In `docs/HIGHLIGHT-REFERENCE.md`: add a `## Valid-Move Tint Consistency` section recording (a) the rule — every phase that populates `validMoveHexes` tints those hexes with the shared `safe` style from `HIGHLIGHT_STYLES`; (b) the full list of phases that do so, from the inventory; (c) the exception set with its per-phase rationale, mirroring `VALID_MOVE_TINT_EXCEPTION_PHASES` exactly; and (d) a line in the "Adding a New Highlight" checklist style telling a future contributor that adding a new movement-pattern phase means adding it to the shared tint or to the exception set with a reason, never silently to neither.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- HexGrid</automated>
  </verify>
  <acceptance_criteria>
    - `packages/client/src/components/HexGrid.tsx` declares and exports `VALID_MOVE_TINT_EXCEPTION_PHASES` as a `ReadonlySet<GamePhase>`.
    - `grep -c "phase !== 'GK_DIVE'" packages/client/src/components/HexGrid.tsx` returns 0 inside the `isHighlighted` derivation — the inline negated literals were replaced by the membership test (a mention inside the constant's own comment is acceptable).
    - Every member of `VALID_MOVE_TINT_EXCEPTION_PHASES` has a one-line reason comment citing `D-02`.
    - `HexGrid.test.tsx` asserts the exception set's exact membership and member count, plus at least one `safe`-tint rendering assertion and one exception-phase rendering assertion.
    - `docs/HIGHLIGHT-REFERENCE.md` contains a `## Valid-Move Tint Consistency` section listing the tinting phases, the exception set, and a per-exception rationale; the listed exception names match `VALID_MOVE_TINT_EXCEPTION_PHASES` exactly.
    - `git diff` shows no new `HexHighlightType` union member, no new entry in `HIGHLIGHT_STYLES`, and no new color literal.
    - The `highlightType` priority ternary's branch order is unchanged (verify by reading the diff — only the `isHighlighted` input changed).
    - Any phase brought onto the shared `safe` tint is named in the plan summary with its before and after tint.
    - `pnpm --filter @counter-attack/client test -- HexGrid` exits 0, `pnpm -w typecheck` exits 0, and `pnpm knip` reports zero findings.
  </acceptance_criteria>
  <done>The valid-move tint rule is expressed once as a named, commented, exported exception set instead of inline phase literals; the rule and every exception are documented in the highlight reference; tests pin both the set's membership and the rendered outcome on both sides of the rule.</done>
</task>

<task type="auto">
  <name>Task 2: Align the pitch player card's team-badge size with the roster/bench card family and fix its stale stat-grid comment</name>
  <files>packages/client/src/components/PlayerStatsPanel.tsx, packages/client/src/components/PlayerStatsPanel.test.tsx</files>
  <read_first>
    - `packages/client/src/components/PlayerStatsPanel.tsx` — the whole file, especially the ASCII-diagram doc comment at ~lines 105–120 (which says `[TeamBadge 56px]` and `4-column stat grid → 2 rows of 4+3 (7 role-filtered stats)`), the `<TeamBadge teamId={teamId} size={56} full />` call at ~line 133, and the `styles.statGrid` div at ~line 159
    - `packages/client/src/components/PlayerStatsPanel.module.css` — the `.statGrid` rule (~lines 82–85), which is already `grid-template-columns: repeat(3, 1fr)`; read it to confirm the grid is already 3-column so you do NOT change it
    - `packages/client/src/components/LineupAssignmentScreen.tsx` — the `LineupStatCard` component (~lines 223–343): its `TeamBadge size={48}` call and its `STAT_LABELS` filtering, the target family this card must match
    - `packages/client/src/components/DraftPackCarousel.tsx` — `DraftCardBody` (~lines 87–160): the second member of the already-aligned family, also `size={48}`, reused by `BenchCarousel.tsx`
    - `packages/client/src/components/TeamBadge.tsx` — how the `size` prop lands in the rendered output (a `width`/`height` attribute or a style value), so the test asserts against what is actually rendered
    - `packages/client/src/components/PlayerStatsPanel.test.tsx` — existing assertions, to see whether badge size is already asserted
    - `.planning/phases/46-final-cleanup/46-CONTEXT.md` — D-04, which scopes CLEANUP-11 to whatever the audit finds misaligned beyond Phase 41's already-unified `CardInjuryBadge`
    - `.planning/phases/46-final-cleanup/46-UI-SPEC.md` — the Typography section's binding rule for CLEANUP-11 and the Phase-Specific Component Contracts row for CLEANUP-11
  </read_first>
  <action>
Change the `TeamBadge` `size` prop in `PlayerStatsPanel.tsx` from `56` to `48`, matching `LineupStatCard` and `DraftCardBody`. Keep the `full` prop as-is — only the numeric size diverged (D-04: icon position itself was already unified by Phase 41's `CardInjuryBadge`, so this phase covers the remaining sizing/spacing divergences the audit found).

Correct the stale doc comment block in the same file: the ASCII diagram line reading `[TeamBadge 56px]` becomes `[TeamBadge 48px]`, and the stat-grid line claiming a `4-column stat grid → 2 rows of 4+3 (7 role-filtered stats)` must be rewritten to describe what the code actually renders — a 3-column grid of 6 role-filtered stats (`resilience` always dropped, plus 2 more by role), identical in shape to the roster/bench family. Add a short marker citing `Phase 46 / CLEANUP-11 + CLEANUP-13` on the corrected comment so the correction is traceable.

Do NOT change `.statGrid`'s `grid-template-columns` in `PlayerStatsPanel.module.css` — it is already `repeat(3, 1fr)` and already matches. The divergence was the comment, not the CSS. Do NOT modify `PlayerStatsPanel.module.css` in this task at all.

Do NOT attempt the optional stretch from 46-RESEARCH.md (extracting a shared card-body sub-component consumed by both stylesheets' owners). 46-UI-SPEC.md marks it explicitly optional and it is out of scope for this plan.

In `PlayerStatsPanel.test.tsx`: add or update an assertion pinning the rendered team badge dimension to 48, asserting against whatever attribute `TeamBadge` actually produces for its `size` prop. Also assert the stat grid renders exactly 6 stat chips for a representative outfield role, matching the roster/bench family's count.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- PlayerStatsPanel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "size={48}" packages/client/src/components/PlayerStatsPanel.tsx` returns 1 and `grep -c "size={56}" packages/client/src/components/PlayerStatsPanel.tsx` returns 0.
    - `grep -c "4-column" packages/client/src/components/PlayerStatsPanel.tsx` returns 0.
    - `grep -c "7 role-filtered" packages/client/src/components/PlayerStatsPanel.tsx` returns 0.
    - The corrected comment names `3-column` and `6` role-filtered stats and cites `CLEANUP-11`.
    - `git diff --stat` lists no change to `packages/client/src/components/PlayerStatsPanel.module.css`.
    - `PlayerStatsPanel.test.tsx` contains an assertion pinning the badge dimension to 48 and an assertion that exactly 6 stat chips render for an outfield role.
    - `pnpm --filter @counter-attack/client test -- PlayerStatsPanel` exits 0.
  </acceptance_criteria>
  <done>The pitch player card renders a 48px team badge identical in size to the roster and bench cards, its doc comment describes the real 3-column/6-stat grid, the CSS module is untouched, and tests pin both facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | Both tasks change client-side rendering: which tint a hex resolves to, and a numeric size prop. No server code, no socket event, no validation logic, and no user-supplied input path is touched. |

Confirmed against 46-RESEARCH.md's Security Domain section: no authentication, session, access-control, cryptography, or external input-validation surface is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-05-I | Information Disclosure | valid-move hex tinting in `HexGrid.tsx` | mitigate | Valid-move tints are per-player information — painting them for the non-acting client would leak one player's movement options to the opponent, the class of bug T-37-80 previously fixed. Mitigation: this task changes only which `HexHighlightType` a hex resolves to, never the upstream gating of `validMoveHexes` on the acting player; the acceptance criteria require the `highlightType` priority order and all other tint inputs to be unchanged, and the rendering assertions exercise the rule on both sides. |
| T-46-05-T | Tampering | `HIGHLIGHT_STYLES` / `HexHighlightType` | mitigate | Silently adding a new tint member would fork the single source of truth this phase exists to protect. Mitigation: acceptance criteria assert zero new union members, zero new `HIGHLIGHT_STYLES` entries, and zero new color literals; any deliberate exception must be an enumerated, commented member of the new set and mirrored in the doc. |
| T-46-05-R | Repudiation | undocumented tint exceptions | mitigate | An exception living as an inline phase literal is invisible to the next contributor and to the highlight reference doc — the exact drift that already happened once with `BALL_MARKER_PHASES` after Plan 37-02. Mitigation: the exception set is a named exported constant, mirrored in `docs/HIGHLIGHT-REFERENCE.md`, and pinned by a membership test. |
| T-46-05-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages (46-RESEARCH.md Package Legitimacy Audit: this phase adds zero dependencies). If any task appears to require an install, stop and escalate. |
</threat_model>

<verification>
Run in order:

1. `pnpm --filter @counter-attack/client test -- HexGrid`
2. `pnpm --filter @counter-attack/client test -- PlayerStatsPanel`
3. `pnpm --filter @counter-attack/client test` (full client suite — the `isHighlighted` change touches a shared derivation and could affect other components' rendering assertions)
4. `pnpm -w typecheck`
5. `pnpm knip`

All five must exit 0; knip must report zero findings.
</verification>

<success_criteria>
- Every phase that populates `validMoveHexes` is accounted for: either it tints those hexes with the shared `safe` style, or it is an enumerated member of `VALID_MOVE_TINT_EXCEPTION_PHASES` with a written reason.
- `VALID_MOVE_TINT_EXCEPTION_PHASES` replaces the inline negated phase literals in `isHighlighted` and is pinned by a membership test.
- `docs/HIGHLIGHT-REFERENCE.md` carries a Valid-Move Tint Consistency section whose exception list matches the constant exactly, plus contributor guidance for adding a new movement-pattern phase.
- `PlayerStatsPanel.tsx` uses `size={48}` and its stat-grid comment describes the real 3-column/6-stat grid; the CSS module is untouched.
- Full client suite, `pnpm -w typecheck`, and `pnpm knip` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/46-final-cleanup/46-05-SUMMARY.md` when done. List the full inventory of phases that populate `validMoveHexes` with the tint each resolves to, name every exception and its reason, and flag any phase whose tint this plan changed.
</output>
