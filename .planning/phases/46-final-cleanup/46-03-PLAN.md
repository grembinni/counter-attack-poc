---
phase: 46-final-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/GameSettingsScreen.tsx
  - packages/client/src/components/GameSettingsScreen.test.tsx
  - packages/client/src/components/FreeKickSetupPanel.tsx
  - packages/client/src/components/FreeKickSetupPanel.test.tsx
  - .planning/phases/46-final-cleanup/46-AUDIT.md
autonomous: true
requirements: [CLEANUP-05, CLEANUP-07, CLEANUP-10, CLEANUP-12]
must_haves:
  truths:
    - "The Match Speed control lives inside the Advanced Settings disclosure alongside the other match-rule settings, not as its own always-visible section"
    - "The Free Kick panel describes how the kicker is chosen in the same select-then-lock language the Penalty Kick, Corner Kick and Throw-In panels already use"
    - "Every multi-step phase step's help-text coverage is recorded in a written audit, with any gap found either fixed or explicitly triaged"
    - "Every candidate redundant multi-step flow is recorded in a written audit with a keep-or-collapse decision and a reason"
  artifacts:
    - path: "packages/client/src/components/GameSettingsScreen.tsx"
      provides: "Match Speed button group rendered inside .advancedGrid"
      contains: "SPEED_OPTIONS"
    - path: "packages/client/src/components/FreeKickSetupPanel.tsx"
      provides: "Kicker-selection constraint copy aligned to the select-then-lock family"
    - path: ".planning/phases/46-final-cleanup/46-AUDIT.md"
      provides: "CLEANUP-07 help-text coverage table and CLEANUP-12 redundant-flow disposition table"
      contains: "CLEANUP-12"
  key_links:
    - from: "GameSettingsScreen.tsx Match Speed block"
      to: ".advancedGrid / .advancedColumn"
      via: "moved inside the advancedOpen disclosure"
      pattern: "advancedColumn"
    - from: ".planning/phases/46-final-cleanup/46-AUDIT.md"
      to: ".planning/REQUIREMENTS.md CLEANUP-07 / CLEANUP-12"
      via: "each audited surface cites the requirement it satisfies"
      pattern: "CLEANUP-0?7"
---

<objective>
Land the two remaining interaction-consistency fixes (the misplaced Match Speed control and the Free Kick panel's off-family kicker-selection language) and produce the written audit that closes the two inherently open-ended requirements, CLEANUP-07 (help-text completeness) and CLEANUP-12 (redundant single-action flows).

Purpose: CONTEXT.md D-01 states no user-supplied list of gaps exists for CLEANUP-07/09/12 — the codebase itself is the source. 46-RESEARCH.md already spot-audited the v1.6/v1.7-era surfaces and found no gap; this plan finishes the job for the older surfaces it could not reach and records the result, so "audited and found clean" is an evidenced conclusion rather than an assumption. CLEANUP-10's fix direction is locked by 46-RESEARCH.md and 46-UI-SPEC.md to the minimum-scope language alignment; the Free Kick stage mechanics are explicitly not restructured.
Output: Match Speed sits in the Advanced drawer; the Free Kick panel's kicker copy matches its three sibling panels; `46-AUDIT.md` records the help-text coverage table and the redundant-flow disposition table with reasons.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-final-cleanup/46-CONTEXT.md
@.planning/phases/46-final-cleanup/46-RESEARCH.md
@.planning/phases/46-final-cleanup/46-PATTERNS.md
@.planning/phases/46-final-cleanup/46-UI-SPEC.md
@.planning/todos/pending/2026-08-28-debt-move-speed-setting-to-advanced-settings.md
@.planning/todos/pending/2026-08-28-debt-pk-kicker-selection-pattern-diverges-from-fk.md
</context>

## Artifacts this phase produces

| Artifact | File | Kind |
|---|---|---|
| Match Speed block relocated into `.advancedColumn` | `packages/client/src/components/GameSettingsScreen.tsx` | moved JSX block (no new component, no new CSS class) |
| Kicker-selection constraint copy | `packages/client/src/components/FreeKickSetupPanel.tsx` | changed / added `constraintRow` string |
| `.planning/phases/46-final-cleanup/46-AUDIT.md` | new file | Phase audit record: CLEANUP-07 help-text coverage table + CLEANUP-12 redundant-flow disposition table |

No new exports, no new components, no new CSS classes, no new colors.

<tasks>

<task type="auto">
  <name>Task 1: Relocate the Match Speed control into the Advanced Settings drawer</name>
  <files>packages/client/src/components/GameSettingsScreen.tsx, packages/client/src/components/GameSettingsScreen.test.tsx</files>
  <read_first>
    - `packages/client/src/components/GameSettingsScreen.tsx` — the whole render body: the standalone Match Speed `<div className={styles.section}>` block (~lines 196–216, containing the `Match Speed` `sectionLabel`, the `.speedOptions` wrapper, and the `SPEED_OPTIONS.map` button group with its `aria-pressed` contract), the `advancedOpen` disclosure state (~line 110), the disclosure trigger button with `aria-expanded` (~lines 246–253), and the `.advancedGrid` with its two `.advancedColumn` children (left: Fouls / Booking / Injury, ~lines 256–289; right: Out-of-Bounds / Referee Leniency + stepper / Tackle-Steal Decline, ~lines 290–340)
    - `packages/client/src/components/GameSettingsScreen.module.css` — `.section`, `.sectionLabel`, `.speedOptions`, `.speedOption`, `.speedOptionActive`, `.advancedGrid`, `.advancedColumn`, `.poolRow` — enough to know which wrapper classes travel with the moved block and which do not
    - `packages/client/src/constants/speedOptions.ts` — the `SPEED_OPTIONS` array shape (`value`, `label`, `icon`, `colorClass`)
    - `packages/client/src/components/GameSettingsScreen.test.tsx` — the `openAdvanced()` helper (~line 15), the first render test asserting `screen.getByText('Match Speed')` on initial render (~lines 20–28), and the "speed selection is reflected in the confirmed payload" test (~lines 194–202); both currently assume the speed control is visible without opening Advanced
    - `.planning/todos/pending/2026-08-28-debt-move-speed-setting-to-advanced-settings.md` — the folded todo
    - `.planning/phases/46-final-cleanup/46-UI-SPEC.md` — the Phase-Specific Component Contracts row for the move-speed relocation and the Copywriting Contract row stating no copy change
  </read_first>
  <action>
Move the Match Speed control from its own top-level `.section` into the Advanced disclosure. Delete the standalone `<div className={styles.section}>` wrapper that currently holds it and re-render its contents — the `Match Speed` `sectionLabel` span and the `.speedOptions` button group — as an additional row appended to the bottom of the LEFT `.advancedColumn` (the column currently holding Fouls, Booking, Injury), making that column 4 rows to the right column's 3. The left column is the correct target because the right column's Referee Leniency row already carries a stepper and is the taller of the two.

Preserve the button-group widget verbatim: the `SPEED_OPTIONS.map`, the `styles.speedOption` / `styles.speedOptionActive` class selection, the `styles[colorClass]` append, the `styles.speedIcon` span, the `aria-pressed={value === speed}` attribute, and the `onClick={() => setSpeed(value)}` handler must all be unchanged. Do NOT convert it to a checkbox to match the neighbouring `.poolRow` rows — 46-UI-SPEC.md explicitly requires preserving the existing button-group visual treatment and its `--color-speed-slow/-standard/-fast` family.

Do not change the label text: it stays exactly `Match Speed`. Do not change the `speed` state, its default, or the `speed` field in the `onConfirm` payload.

If the moved block needs a wrapper to sit correctly inside `.advancedColumn` (which is a flex/grid column of `.poolRow`s, not of `.section`s), add the minimum markup needed and reuse an existing class — do not author a new CSS class or a new spacing value. If no existing class fits, add one rule to `GameSettingsScreen.module.css` copying its spacing values from the existing `.advancedColumn` row rhythm rather than inventing new values, and note the addition in the summary.

Update `GameSettingsScreen.test.tsx`: every test that reaches the speed control must call the existing `openAdvanced()` helper first. Specifically — the initial-render test must no longer assert `Match Speed` is visible before the drawer opens; replace that with an assertion that `Match Speed` is ABSENT on first render and PRESENT after `openAdvanced()`, mirroring how the existing Advanced-disclosure describe block asserts the Fouls checkbox is not rendered on first render. The "speed selection is reflected in the confirmed payload" test must call `openAdvanced()` before clicking a speed button; its `speed: 'fast'` payload assertion is unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- GameSettingsScreen</automated>
  </verify>
  <acceptance_criteria>
    - `GameSettingsScreen.test.tsx` contains a test asserting `Match Speed` is not present on first render and is present after `openAdvanced()`.
    - The existing "speed selection is reflected in the confirmed payload" test still asserts `speed: 'fast'` and now calls `openAdvanced()` before the click.
    - `grep -c "SPEED_OPTIONS.map" packages/client/src/components/GameSettingsScreen.tsx` returns 1 (the block was moved, not duplicated).
    - `grep -c "aria-pressed" packages/client/src/components/GameSettingsScreen.tsx` is unchanged from before the edit.
    - The literal string `Match Speed` still appears exactly once in `GameSettingsScreen.tsx`.
    - `git diff packages/client/src/components/GameSettingsScreen.module.css` shows either no change, or at most one added rule whose values are copied from an existing `.advancedColumn`/`.poolRow` rule (no new color, no new font size).
    - `pnpm --filter @counter-attack/client test -- GameSettingsScreen` exits 0.
  </acceptance_criteria>
  <done>Match Speed renders only inside the opened Advanced drawer, as a fourth row of the left column, with its button-group widget, colors, labels, aria contract, and confirmed-payload behaviour unchanged; the test suite reflects the new location.</done>
</task>

<task type="auto">
  <name>Task 2: Align the Free Kick panel's kicker-selection language with the select-then-lock family</name>
  <files>packages/client/src/components/FreeKickSetupPanel.tsx, packages/client/src/components/FreeKickSetupPanel.test.tsx</files>
  <read_first>
    - `packages/client/src/components/FreeKickSetupPanel.tsx` — the whole file: `isKickerSelectionPhase` (~line 83), `kickerLocked` / `checksKickerPlacement` / `kickerConstraintValid` (~lines 115–125), the `disabledTitle` derivation (~line 128), and every `constraintRow` render site (~lines 91, 206–224), especially the red kicker-required row currently reading `Kicker: move a player onto the free-kick hex first — required before any other move`
    - `packages/client/src/components/PenaltyKickSetupPanel.tsx` — the `PENALTY_KICK_TAKER_SELECT` block (~lines 154–204), including the comment explaining that select-then-Confirm exists to close the "misclick commits irreversibly" defect, and its player-facing copy (`Choose your penalty taker.` / `Placing your penalty taker…`)
    - `packages/client/src/components/CornerKickSetupPanel.tsx` — the `CORNER_KICK_TAKER_SELECT` block (~lines 156–196) and its copy
    - `packages/client/src/components/ThrowInSetupPanel.tsx` — the whole panel (~lines 18–103) and its `Thrower: #N Name` copy
    - `packages/client/src/components/FreeKickSetupPanel.test.tsx` — existing assertions on constraint-row text, so the new copy is pinned the same way
    - `.planning/phases/46-final-cleanup/46-UI-SPEC.md` — the Copywriting Contract row for CLEANUP-10 and the Phase-Specific Component Contracts row for CLEANUP-10
    - `.planning/phases/46-final-cleanup/46-RESEARCH.md` — the CLEANUP-10 section's six-panel comparison table and its "minimum vs. stretch" guidance
    - `.planning/todos/pending/2026-08-28-debt-pk-kicker-selection-pattern-diverges-from-fk.md` — the folded todo
  </read_first>
  <action>
Implement the minimum-scope CLEANUP-10 fix only: bring Free Kick's kicker-selection *language* into the same "select → this becomes locked" family the other three panels use, without touching the underlying mechanics.

During the kicker-selection sub-step (`isKickerSelectionPhase === true`, i.e. a kicking stage where `freeKickKickerChosen` is false), render a `constraintRow` that states the selection mechanism explicitly, in the imperative-plus-consequence voice of the sibling panels. Use the string locked by 46-UI-SPEC.md's Copywriting Contract: `Move a player onto the kick spot to become the kicker.` Keep it in the neutral `constraintRow` treatment, not the red `var(--color-danger)` treatment — it is an instruction, not an error.

Once a kicker is locked (`kickerLocked === true`), render a `constraintRow` naming the locked kicker in the sibling panels' `Thrower: #N Name` / `penalty taker` shape — e.g. a `Kicker:` label followed by the locked piece's jersey number and name, derived from the piece already identified by `myPieces.some((p) => movedPieceIds.includes(p.id))` (find it rather than testing for it). Match the exact jersey/name formatting `ThrowInSetupPanel.tsx` uses so the two read identically; read that panel to copy the format, do not invent one.

Keep the existing red `checksKickerPlacement && !kickerLocked` row, but reword it so it no longer duplicates the new instruction row: it should read as the blocking-constraint restatement (kicker required before any other move) rather than repeating the how-to. If, after the rewording, the red row and the new instruction row would render simultaneously with near-identical text, keep only one of them on screen at a time and record which condition governs which — the panel must never show two rows saying the same thing.

Explicitly OUT OF SCOPE for this task, per CONTEXT.md D-03 and 46-RESEARCH.md's CLEANUP-10 note: do not add a separate click-select-then-Confirm affordance for the free-kick kicker; do not modify `FREE_KICK_STAGES`, `freeKickStageTeam`, `freeKickKickerChosen`, `freeKickPlacedPieceIds`, or any server-side free-kick logic; do not invert the alignment by simplifying Penalty Kick / Corner Kick / Throw-In back toward Free Kick's single-step model (their select-then-Confirm pattern exists to fix a real Undo-boundary misclick defect and must not be regressed).

Add a comment on the changed copy citing `Phase 46 / CLEANUP-10` and recording that the alignment is language-only and that the mechanics restructure was deliberately deferred.

In `FreeKickSetupPanel.test.tsx`: add assertions for the new instruction copy during the kicker-selection sub-step and for the locked-kicker row's `Kicker: #N Name` shape after a kicker is locked, matching how the existing tests build panel state.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- FreeKickSetupPanel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Move a player onto the kick spot to become the kicker." packages/client/src/components/FreeKickSetupPanel.tsx` returns 1.
    - The panel renders a locked-kicker row whose format matches `ThrowInSetupPanel.tsx`'s thrower row (same jersey-number prefix character and same name resolution helper).
    - `FreeKickSetupPanel.test.tsx` contains an assertion on the new instruction string and an assertion on the locked-kicker row.
    - `git diff` shows NO change to `FREE_KICK_STAGES`, `freeKickStageTeam`, `freeKickKickerChosen`, or `freeKickPlacedPieceIds` in any file.
    - `git diff --stat` lists no change to `PenaltyKickSetupPanel.tsx`, `CornerKickSetupPanel.tsx`, or `ThrowInSetupPanel.tsx`.
    - No two constraint rows render simultaneously with the same instruction (verify by reading the render conditions and by a test asserting the kicker-selection sub-step shows the instruction row exactly once).
    - `pnpm --filter @counter-attack/client test -- FreeKickSetupPanel` exits 0.
  </acceptance_criteria>
  <done>The Free Kick panel tells the player how the kicker is chosen and who was chosen, in the same voice and format as the Penalty Kick, Corner Kick and Throw-In panels, with no change to free-kick stage mechanics.</done>
</task>

<task type="auto">
  <name>Task 3: Complete and record the help-text and redundant-flow audits</name>
  <files>.planning/phases/46-final-cleanup/46-AUDIT.md</files>
  <read_first>
    - `.planning/phases/46-final-cleanup/46-RESEARCH.md` — the CLEANUP-07 section (its coverage table of already-audited surfaces and its Open Question 2, which scopes exactly what this task must still cover) and the CLEANUP-12 section (its three-candidate table with the caution column)
    - `.planning/phases/46-final-cleanup/46-CONTEXT.md` — D-01 (the audit must scan the codebase, not rely on a user list) and D-03 (no speculative refactor)
    - `packages/client/src/components/ActionPanel.tsx` — the pre-v1.6 branches that 46-RESEARCH.md flagged as the remaining coverage gap: the `MOVE` / `MOVEMENT` branches, the `PASS` chooser (~lines 754–870), `SHOT`, `HEADER` / header-contestant / header-target, `LOOSE_BALL`, `SNAPSHOT` / `SNAPSHOT_TARGET`, `GK_RESTART` / `GK_QUICK_THROW` / `GK_KICK_TARGET`, and `KICK_OFF`
    - `packages/client/src/components/KickOffSetupPanel.tsx` — its `constraintRow` coverage across kick-off steps
    - `packages/client/src/components/GoalKickSetupPanel.tsx` (~lines 150–206) — the `GOAL_KICK_CHOICE` → `GOAL_KICK_TARGET` two-phase flow, CLEANUP-12 candidate 2
    - `packages/client/src/components/CornerKickSetupPanel.tsx` (~lines 122–152) — the `CORNER_KICK_GK_SETUP_ATTACKING`/`_DEFENDING` reposition-then-Confirm flow and its "Assumption A1" comment, CLEANUP-12 candidate 1
    - `packages/shared/src/types.ts` — the full `GamePhase` union, so the help-text audit enumerates every phase rather than a remembered subset
  </read_first>
  <action>
Create `.planning/phases/46-final-cleanup/46-AUDIT.md` recording the completed audits for CLEANUP-07 and CLEANUP-12. This file is the evidence artifact that makes "audited, no gap" a defensible conclusion rather than an assumption; it is what `/gsd-verify-work` will read for those two requirements.

Section 1 — CLEANUP-07 help-text coverage. Enumerate every member of the `GamePhase` union (read it from `packages/shared/src/types.ts`; do not work from memory). For each phase produce a row with: phase name, the component that renders its player-facing panel, whether that panel renders a heading, whether it renders a description/constraint line, and a verdict of `covered` / `gap` / `n/a` (`n/a` only for non-interactive phases such as `REPLAY`, `HALF_TIME`, `FULL_TIME` where no step description applies — state the reason in the row). Copy the already-audited rows from 46-RESEARCH.md's CLEANUP-07 table rather than re-deriving them, and mark them as carried forward; spend the effort on the pre-v1.6 `ActionPanel.tsx` branches that research explicitly could not reach.

For every row marked `gap`, fix it in the same task: add the missing heading or description line to the owning component, following that file's existing `helperBlock` / `helperLine1` + `helperLine2` or `constraintRow` convention exactly, and add the file to this plan's summary as an extra modified file. If a gap is genuinely too large to fix as a copy addition (i.e. it requires new state or a new panel), do not fix it silently — record it in the audit with an explicit `deferred` verdict and a one-line reason, and surface it in the plan summary so the developer sees it.

Section 2 — CLEANUP-12 redundant-flow disposition. Start from 46-RESEARCH.md's three-candidate table and extend it with any further candidate found while enumerating phases in Section 1. For each candidate record: the flow, how many player actions it actually requires at minimum, how many UI steps it presents, a disposition of `collapse` / `keep`, and the reason. Apply these rules when deciding:
  - `keep` when the flow is 0-or-more / variable-count rather than exactly-one (the corner-kick GK reposition case — its own panel comment documents that repositioning is uncapped).
  - `keep` when the split exists to prevent a defect (the Penalty-Kick / Corner-Kick taker select-then-Confirm case — collapsing it reintroduces the misclick-commits-irreversibly bug across an Undo boundary).
  - `keep` when the two UI steps map to two genuinely different server action types with different downstream rules (the `GOAL_KICK_CHOICE` → `GOAL_KICK_TARGET` case — Kick versus Standard Pass alters rules downstream, so collapsing the UI without collapsing the phase machine would be cosmetic and would obscure the rule difference).
  - `collapse` only when the UI presents 2+ steps AND the game logic provably only ever accepts one action AND collapsing removes no reversibility affordance. If any candidate meets all three, implement the collapse in this task and list the touched file in the summary.

Close the file with a short conclusion stating, for each of CLEANUP-07 and CLEANUP-12, whether the requirement is satisfied by fixes, by evidenced no-gap, or partly deferred — with counts.

Do not use this task to open speculative refactors. Per CONTEXT.md D-03 the default is to fix what is concretely wrong and document the rest.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/46-final-cleanup/46-AUDIT.md` exists and contains both a `CLEANUP-07` section and a `CLEANUP-12` section.
    - The CLEANUP-07 table has one row per `GamePhase` union member — the row count equals the union's member count (state both numbers explicitly in the file so they can be compared).
    - Every CLEANUP-07 row carries a verdict of exactly `covered`, `gap`, `n/a`, or `deferred`; every `n/a` and every `deferred` row carries a reason.
    - Zero rows are left with verdict `gap` — every `gap` was either fixed (component listed in the summary) or re-marked `deferred` with a reason.
    - The CLEANUP-12 table carries at least the three candidates from 46-RESEARCH.md, each with a `collapse`/`keep` disposition and a reason drawn from the rules above.
    - The file's conclusion states a satisfied/deferred verdict plus counts for each of CLEANUP-07 and CLEANUP-12.
    - `pnpm --filter @counter-attack/client test` exits 0 (any copy added while closing a `gap` row must not break existing text assertions).
  </acceptance_criteria>
  <done>`46-AUDIT.md` records a complete per-phase help-text coverage table with no unresolved gaps and a reasoned keep-or-collapse disposition for every redundant-flow candidate; any concrete gap found was fixed in-place and the client test suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | All three tasks change client-side presentation: where a settings control is rendered, what a panel's constraint text says, and a planning-directory audit document. No server code, no socket event, no validation logic, and no user-supplied input path is touched. |

Confirmed against 46-RESEARCH.md's Security Domain section: no authentication, session, access-control, cryptography, or external input-validation surface is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03-T | Tampering | `GameSettingsScreen` `onConfirm` payload | mitigate | Relocating the Match Speed control must not change the `speed` value shape or its default. The existing test asserting `speed: 'fast'` in the confirmed payload is retained and re-pointed rather than deleted, so a payload regression fails the build. The server remains the authority on how `speed` is applied. |
| T-46-03-R | Repudiation | Free Kick kicker identification | accept | The copy change only describes the already-existing `movedPieceIds`-based kicker lock; it does not alter who the server records as the kicker, and no audit/event record changes. |
| T-46-03-E | Elevation of Privilege | restart-panel selection affordances | mitigate | This task is explicitly forbidden from adding or removing a Confirm step in any restart panel. The Penalty-Kick / Corner-Kick select-then-Confirm pattern guards an Undo-boundary commit; the acceptance criteria assert zero diff in those three sibling panels, so the guard cannot be regressed by this plan. |
| T-46-03-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs no packages (46-RESEARCH.md Package Legitimacy Audit: this phase adds zero dependencies). If any task appears to require an install, stop and escalate. |
</threat_model>

<verification>
Run in order:

1. `pnpm --filter @counter-attack/client test -- GameSettingsScreen`
2. `pnpm --filter @counter-attack/client test -- FreeKickSetupPanel`
3. `pnpm --filter @counter-attack/client test` (full client suite — Task 3 may have touched additional panels while closing help-text gaps)
4. `pnpm -w typecheck`
5. `pnpm knip`

All five must exit 0; knip must report zero findings.
</verification>

<success_criteria>
- Match Speed is reachable only through the Advanced disclosure, renders as the left column's fourth row, keeps its button-group widget and colors, and still drives the same `speed` value in the confirmed payload.
- The Free Kick panel states how the kicker is chosen and names the locked kicker in the same format the Throw-In panel uses; free-kick stage mechanics and the three sibling panels are untouched.
- `46-AUDIT.md` exists with a complete per-`GamePhase` help-text table (no unresolved `gap` rows) and a reasoned CLEANUP-12 disposition table.
- Full client test suite, `pnpm -w typecheck`, and `pnpm knip` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/46-final-cleanup/46-03-SUMMARY.md` when done. List any component files touched while closing CLEANUP-07 gaps beyond the four declared in `files_modified`.
</output>
