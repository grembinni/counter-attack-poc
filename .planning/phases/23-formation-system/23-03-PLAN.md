---
phase: 23-formation-system
plan: 03
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - packages/client/src/components/UniformSelectionScreen.tsx
  - packages/client/src/components/UniformSelectionScreen.module.css
  - packages/client/src/App.tsx
autonomous: false
requirements: [FORM-01, FORM-02, FORM-03]
must_haves:
  truths:
    - 'A Formation section with four cards (4-4-2, 5-3-2, 4-3-3, 3-4-3) appears between the Team grid and the Style grid'
    - 'Each formation card shows its PNG pitch diagram, label, and one-line tactical description'
    - '4-4-2 is pre-selected on mount; clicking a card changes the selection with white border-glow'
    - 'Confirm emits UNIFORM_CONFIRM with the selected formationId'
    - 'After both confirm (BOTH_FORMATIONS_CONFIRMED), the player sees a formations-locked holding state'
  artifacts:
    - path: 'packages/client/src/components/UniformSelectionScreen.tsx'
      provides: 'formation grid section + extended onConfirm signature'
      contains: 'FORMATION_OPTIONS'
    - path: 'packages/client/src/components/UniformSelectionScreen.module.css'
      provides: 'formationGrid/formationCard/formationCardSelected/formationImage/formationLabel/formationDescription classes'
      contains: 'formationGrid'
    - path: 'packages/client/src/App.tsx'
      provides: 'extended handleUniformConfirm emit + BOTH_FORMATIONS_CONFIRMED handler'
      contains: 'BOTH_FORMATIONS_CONFIRMED'
  key_links:
    - from: 'packages/client/src/components/UniformSelectionScreen.tsx'
      to: 'packages/client/src/App.tsx'
      via: 'onConfirm(teamId, uniformStyle, formationId)'
      pattern: "onConfirm\\(selectedTeam, selectedStyle, selectedFormation\\)"
    - from: 'packages/client/src/App.tsx'
      to: 'packages/shared/src/events.ts'
      via: 'UNIFORM_CONFIRM emit with formationId'
      pattern: 'ClientEvents.UNIFORM_CONFIRM, teamId, uniformStyle, formationId'
---

<objective>
Add the Formation selection section to the existing combined `UniformSelectionScreen` (per D-05,
formation is NOT a separate screen): a four-card grid inserted between the Team grid and the Style
grid, each card showing a PNG pitch diagram, label, and tactical description, with 4-4-2 pre-selected.
Extend the Confirm flow to carry `formationId`, and wire `App.tsx` to emit the extended
UNIFORM_CONFIRM and handle the new BOTH_FORMATIONS_CONFIRMED broadcast with a holding state.

Purpose: FORM-01 (formation choice on the pre-game flow), FORM-02 (mini pitch diagram + description),
and the client half of FORM-03 (confirm + waiting-for-opponent / formations-locked state).

Output: extended UniformSelectionScreen component + CSS, and App.tsx wiring for the new payload and event.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-formation-system/23-CONTEXT.md
@.planning/phases/23-formation-system/23-RESEARCH.md
@.planning/phases/23-formation-system/23-PATTERNS.md
@.planning/phases/23-formation-system/23-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols created by Phase 23 in the client package (exclude from cross-phase drift verification):

- `FORMATION_OPTIONS` (module const: `{ id: FormationId; asset: string; label: string }[]`) — `packages/client/src/components/UniformSelectionScreen.tsx` (NEW)
- Formation PNG imports (`formation442`, `formation532`, `formation433`, `formation343`) — `packages/client/src/components/UniformSelectionScreen.tsx` (NEW)
- `selectedFormation` local state (`useState<FormationId>('4-4-2')`) — `packages/client/src/components/UniformSelectionScreen.tsx` (NEW)
- `homeConfirmedFormation` prop (`FormationId | null`) — `packages/client/src/components/UniformSelectionScreen.tsx` (NEW prop)
- `onConfirm` prop signature gains `formationId: FormationId` — `packages/client/src/components/UniformSelectionScreen.tsx` (SIGNATURE CHANGE)
- CSS classes `.formationGrid`, `.formationCard`, `.formationCardSelected`, `.formationImage`, `.formationLabel`, `.formationDescription` — `packages/client/src/components/UniformSelectionScreen.module.css` (NEW)
- `onBothFormationsConfirmed` handler + `homeConfirmedFormation` / formations-locked state — `packages/client/src/App.tsx` (NEW)
- `ServerEvents.BOTH_FORMATIONS_CONFIRMED` (consumed here; declared in Plan 01) — `packages/shared/src/events.ts`
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add formation grid section + CSS to UniformSelectionScreen</name>
  <files>packages/client/src/components/UniformSelectionScreen.tsx, packages/client/src/components/UniformSelectionScreen.module.css</files>
  <read_first>
    - packages/client/src/components/UniformSelectionScreen.tsx (full file — the Props type at lines 83-94; existing team-card grid loop and sectionLabel; the confirm button click handler; the badge import pattern at file top)
    - packages/client/src/components/UniformSelectionScreen.module.css (.teamGrid, .teamCard, .teamCardSelected, .sectionLabel, .statusLine — the exact spacing/color/typography tokens to mirror)
    - .planning/phases/23-formation-system/23-UI-SPEC.md (Formation Card Content table for exact labels/descriptions/asset names; Spacing/Typography/Color sections; Interaction States; Screen Layout order; Accessibility)
    - .planning/phases/23-formation-system/23-PATTERNS.md (UniformSelectionScreen.tsx and .module.css extension sections — exact JSX, FORMATION_OPTIONS array, state init, CSS class bodies)
  </read_first>
  <action>
    In `packages/client/src/components/UniformSelectionScreen.tsx`:
    - Add four PNG imports at file top following the existing badge import pattern: `import formation442 from '../assets/formations/442.png';` and the same for 532, 433, 343.
    - Import `FormationId` and `FORMATIONS` from `@counter-attack/shared`.
    - Add a module-level `FORMATION_OPTIONS: { id: FormationId; asset: string; label: string }[]` array with the four entries in this order: `{ id: '4-4-2', asset: formation442, label: '4-4-2' }`, then 5-3-2/532, 4-3-3/433, 3-4-3/343 (D-06). The tactical description is read at render time from `FORMATIONS[id].description` (do not re-hardcode strings — they live in the shared table from Plan 01).
    - Add local state `const [selectedFormation, setSelectedFormation] = useState<FormationId>('4-4-2');` (D-07 default; no useEffect — the default is a constant, not team-derived).
    - Add a `homeConfirmedFormation: FormationId | null` field to the `Props` type and destructure it in the component signature (UI-SPEC prop table). It is not required for gating logic in Phase 23 but is wired for Phase 24; render nothing new from it beyond passing through.
    - Extend the `onConfirm` prop type to `(teamId: TeamId, uniformStyle: UniformStyleId, formationId: FormationId) => void`.
    - Insert the Formation section JSX between the team grid (after its closing tag) and the Style section label: a `<p className={styles.sectionLabel}>Formation</p>` followed by `<div className={styles.formationGrid}>` mapping `FORMATION_OPTIONS`. Each card is a `<button>` with: `key={id}`, `disabled={hasConfirmed || awayLocked}`, `aria-pressed={id === selectedFormation}`, `aria-label={`${label} formation`}`, `className={id === selectedFormation ? styles.formationCardSelected : styles.formationCard}`, `onClick={() => { if (!hasConfirmed && !awayLocked) setSelectedFormation(id); }}`. Inside: `<img src={asset} alt={`${label} formation diagram`} className={styles.formationImage} />`, `<p className={styles.formationLabel}>{label}</p>`, `<p className={styles.formationDescription}>{FORMATIONS[id].description}</p>`. Do NOT add any struck-out state — formations have no opponent restriction (UI-SPEC).
    - Update the Confirm click handler to call `onConfirm(selectedTeam, selectedStyle, selectedFormation)` (the existing team+style non-null gate is unchanged; formation is always truthy).

    In `packages/client/src/components/UniformSelectionScreen.module.css`, add the six classes with the exact values from 23-PATTERNS.md / 23-UI-SPEC.md:
    - `.formationGrid`: `display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; max-width: 560px; width: 100%;` plus a `@media (max-width: 640px)` override to `repeat(2, 1fr)`.
    - `.formationCard` / `.formationCardSelected`: mirror `.teamCard` / `.teamCardSelected` but `flex-direction: column; align-items: center; gap: 6px; padding: 8px; background: #1a1a2e;`. Selected adds `box-shadow: 0 0 0 2px rgba(255,255,255,0.45);` and white border `rgba(255,255,255,0.9)`; default border `rgba(255,255,255,0.2)`. Transition `box-shadow 0.15s ease, opacity 0.15s ease`.
    - `.formationImage`: `height: 100px; width: auto; object-fit: contain; display: block;` (UI-SPEC exception — fixed 100px height).
    - `.formationLabel`: `font-size: 14px; font-weight: 700; color: #e0e0e0; margin: 8px 0 0;`.
    - `.formationDescription`: `font-size: 12px; font-weight: 400; color: #a0a0a0; margin: 4px 0 0; text-align: center; line-height: 1.4;`.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - UniformSelectionScreen.tsx imports all four formation PNGs from `../assets/formations/`
    - UniformSelectionScreen.tsx contains `FORMATION_OPTIONS` with 4 entries and `useState<FormationId>('4-4-2')`
    - Props type contains `onConfirm: (teamId: TeamId, uniformStyle: UniformStyleId, formationId: FormationId) => void` and `homeConfirmedFormation: FormationId | null`
    - The Confirm handler calls `onConfirm(selectedTeam, selectedStyle, selectedFormation)`
    - UniformSelectionScreen.module.css contains `.formationGrid`, `.formationCard`, `.formationCardSelected`, `.formationImage`, `.formationLabel`, `.formationDescription`
    - `.formationImage` sets `height: 100px`
    - `pnpm --filter @counter-attack/client exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The formation section renders four cards with pitch diagrams, labels, and descriptions between team and style grids; 4-4-2 pre-selected; Confirm carries formationId; client type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx for extended confirm emit, home-confirmed formation, and BOTH_FORMATIONS_CONFIRMED holding state</name>
  <files>packages/client/src/App.tsx</files>
  <read_first>
    - packages/client/src/App.tsx (full file — the useEffect socket registration/teardown block; onUniformHomeConfirmed handler; handleUniformConfirm; the UniformSelectionScreen render props; the homeConfirmedStyle local state pattern)
    - packages/shared/src/events.ts (BOTH_FORMATIONS_CONFIRMED and extended UNIFORM_HOME_CONFIRMED signatures from Plan 01)
    - .planning/phases/23-formation-system/23-CONTEXT.md (D-12 confirmation flow; the inter-phase holding-state rationale)
    - .planning/phases/23-formation-system/23-RESEARCH.md (Open Question 1 / A3 — Phase 23 broadcasts BOTH_FORMATIONS_CONFIRMED; client shows a "waiting for lineup" holding state; Phase 24 replaces it)
  </read_first>
  <action>
    In `packages/client/src/App.tsx`:
    - Import `FormationId` alongside the existing shared type imports.
    - Add local state `const [homeConfirmedFormation, setHomeConfirmedFormation] = useState<FormationId | null>(null);` co-located with `homeConfirmedStyle`.
    - Add local state `const [formationsLocked, setFormationsLocked] = useState(false);` for the holding state after both confirm.
    - Extend `onUniformHomeConfirmed(teamId, uniformStyle, formationId)` signature (matches the extended UNIFORM_HOME_CONFIRMED payload) and call `setHomeConfirmedFormation(formationId);` alongside the existing sets.
    - Add a new handler `onBothFormationsConfirmed(homeFormation: FormationId, awayFormation: FormationId)` that calls `setFormationsLocked(true);` (Phase 24 will replace this with navigation to the auto-assignment screen; for Phase 23 it flips the holding state). Register it: `socket.on(ServerEvents.BOTH_FORMATIONS_CONFIRMED, onBothFormationsConfirmed);` and add the matching `socket.off(...)` in the cleanup return (Pitfall 9 — every on has a matching off).
    - Update `handleUniformConfirm(teamId, uniformStyle, formationId)` to emit `socket.emit(ClientEvents.UNIFORM_CONFIRM, teamId, uniformStyle, formationId);`.
    - Pass `homeConfirmedFormation={homeConfirmedFormation}` to `<UniformSelectionScreen ... />`.
    - Holding state: while `screen === 'UNIFORM_SELECTION'` and `formationsLocked` is true, render a simple centered status message ("Formations locked — waiting for lineup assignment…") in place of, or overlaid on, the selection screen. Keep it minimal — a single `<div>` with the app container class and the message. This is the Phase 23 terminus; Phase 24 supersedes it. Do NOT call any game-state build or navigate to GAME_BOARD.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx `handleUniformConfirm` emits `ClientEvents.UNIFORM_CONFIRM, teamId, uniformStyle, formationId`
    - App.tsx registers and tears down `ServerEvents.BOTH_FORMATIONS_CONFIRMED` (both a `socket.on` and matching `socket.off`)
    - App.tsx `onUniformHomeConfirmed` accepts a third `formationId` parameter and stores it via `setHomeConfirmedFormation`
    - App.tsx passes `homeConfirmedFormation` prop to UniformSelectionScreen
    - App.tsx renders a formations-locked holding message and never navigates to GAME_BOARD from the formation flow
    - `pnpm --filter @counter-attack/client exec tsc --noEmit` exits 0
    - `pnpm --filter @counter-attack/client build` exits 0 (Vite build succeeds with the new PNG imports)
  </acceptance_criteria>
  <done>App emits the extended UNIFORM_CONFIRM, captures home's confirmed formation, and shows a holding state on BOTH_FORMATIONS_CONFIRMED without advancing to the board; client builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser UAT — formation selection flow (two tabs)</name>
  <files>packages/client/src/components/UniformSelectionScreen.tsx, packages/client/src/App.tsx</files>
  <what-built>
    A Formation section on the combined selection screen with four cards (4-4-2, 5-3-2, 4-3-3, 3-4-3),
    each showing a PNG pitch diagram, label, and tactical description, with 4-4-2 pre-selected. Confirm
    carries the formation to the server; after both players confirm, both see a formations-locked
    holding state. (No client test infrastructure exists — FORM-01 and FORM-02 are visual/interaction
    requirements requiring browser verification per RESEARCH Test Map.)
  </what-built>
  <action>
    This is a human-verification checkpoint — Claude writes no code here. First confirm the client
    builds cleanly (`pnpm --filter @counter-attack/client build` exits 0), then PAUSE and present the
    how-to-verify steps below to the user for two-tab browser verification. Do not proceed past this
    task or mark the plan complete until the user replies with the resume signal.
  </action>
  <how-to-verify>
    1. Start the app locally (server + client dev servers as per project scripts) and open two browser tabs to the client URL.
    2. Tab 1: create a room; Tab 2: join with the room code. Advance both to the combined selection screen.
    3. Confirm the Formation section appears BETWEEN the Team grid and the Style grid, labelled "Formation", with exactly four cards in order 4-4-2, 5-3-2, 4-3-3, 3-4-3.
    4. Confirm each card shows its pitch-diagram PNG (image renders, not broken), its label, and its one-line description (e.g. 4-4-2 → "Balanced. Compact midfield with two mobile strikers.").
    5. Confirm 4-4-2 is pre-selected (white border-glow) on arrival; click 4-3-3 and confirm the glow moves to it.
    6. As Home (Tab 1): pick a team, a formation, a style, click Confirm selection. Confirm Home sees "Waiting for opponent…".
    7. As Away (Tab 2): the screen unlocks after home confirms; pick a different team + a formation + a style, click Confirm.
    8. Confirm BOTH tabs now show the formations-locked holding message ("Formations locked — waiting for lineup assignment…") and neither tab advances to the game board (expected — Phase 24 delivers the next screen).
  </how-to-verify>
  <verify>
    <human-check>All 8 how-to-verify steps pass in a two-tab browser session; user replies "approved".</human-check>
  </verify>
  <done>User has verified the formation selection flow across two browser tabs and replied "approved".</done>
  <resume-signal>Type "approved" if all 8 steps pass, or describe the specific step that failed.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                                                 |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| client UI → server (UNIFORM_CONFIRM emit)             | The client sends a `formationId` string; server re-validates (Plan 02 allow-list) — client is never trusted |
| server broadcast → client (BOTH_FORMATIONS_CONFIRMED) | Server-originated event drives the holding state; no untrusted data rendered as HTML                        |

## STRIDE Threat Register

| Threat ID | Category             | Component                                         | Disposition | Mitigation Plan                                                                                                                              |
| --------- | -------------------- | ------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-23-06   | Tampering            | Client could emit an arbitrary formationId string | mitigate    | Client is not the authority; server allow-list (T-23-03, Plan 02) rejects forged values. Client only offers the 4 typed FORMATION_OPTIONS.   |
| T-23-07   | Cross-site scripting | Formation label/description rendered in DOM       | accept      | Values come from the typed shared FORMATIONS table (static string literals), not user input; React escapes text nodes by default             |
| T-23-SC   | Tampering            | npm/pip/cargo installs                            | mitigate    | No package installs (RESEARCH Package Legitimacy Audit: zero packages); PNG assets already present in repo; no slopcheck checkpoint required |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client exec tsc --noEmit` — client type-checks clean
- `pnpm --filter @counter-attack/client build` — Vite build succeeds (PNG imports resolve)
- Browser UAT (Task 3) — formation section renders and the confirm/holding flow works across two tabs
</verification>

<success_criteria>

- Formation section (four cards with diagram + label + description) renders between team and style grids
- 4-4-2 pre-selected; selection changes with white border-glow; no struck-out state
- Confirm emits UNIFORM_CONFIRM with formationId; App captures home's confirmed formation
- BOTH_FORMATIONS_CONFIRMED drives a formations-locked holding state; no navigation to GAME_BOARD
- Client type-checks and builds clean; browser UAT approved
  </success_criteria>

<output>
Create `.planning/phases/23-formation-system/23-03-SUMMARY.md` when done
</output>
