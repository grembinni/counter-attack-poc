---
phase: 18.2-code-cleanup-behavioral-dup-bugs
plan: 04
type: execute
wave: 3
depends_on: ['18.2-01', '18.2-03']
files_modified:
  - packages/server/src/gameHandlers.ts
  - packages/client/src/store/useGameStore.ts
autonomous: true
requirements: [DESIGN-03]
must_haves:
  truths:
    - 'The 4 server GAME_MOVE response-move branches (HIGH_PASS_MOVE, GK_KICK_MOVE, FIRST_TIME_PASS_MOVE, SNAPSHOT_DEFLECT) share a single parameterized validation helper, each branch becoming a thin call supplying its own field names (Cluster 1)'
    - 'The 4 client selectPiece response-move branches share a single parameterized valid-hex helper (Cluster 3)'
    - 'The byte-identical plain-MOVEMENT valid-hex derivation in selectPiece and setGameState is extracted into one shared helper (Cluster 5)'
    - "BUG-11's carrier-exclusion and BUG-09's staleness fixes are preserved through the consolidation; the divergent Cluster 2 GAME_END_TURN defender-slot logic is NOT merged"
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'Shared validateResponseMoveStep helper consumed by 4 GAME_MOVE branches'
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'Shared computeResponseMoveValidHexes + computeMovementValidHexes helpers'
  key_links:
    - from: 'gameHandlers.ts 4 response-move GAME_MOVE branches'
      to: 'validateResponseMoveStep helper'
      via: 'each branch calls the helper with its per-phase field-name config'
      pattern: 'validateResponseMoveStep'
    - from: 'useGameStore.ts selectPiece + setGameState'
      to: 'computeResponseMoveValidHexes / computeMovementValidHexes helpers'
      via: 'branches and sticky-recompute call the shared helpers'
      pattern: 'computeResponseMoveValidHexes'
---

<artifacts_produced>

## Artifacts this phase produces (symbols created by THIS plan)

New helper functions (module-local or small internal exports — keep them in the same files unless a colocated helper module already exists):

- `validateResponseMoveStep` — server-side shared guard sequence for the 4 GAME_MOVE response-move branches in `packages/server/src/gameHandlers.ts` (Cluster 1). Parameterized by per-phase config (locked-piece-id field, pace-used field, pace cap, movement-slot field, optional carrier-exclusion field, click-distance mode strict-1-vs-range).
- `computeResponseMoveValidHexes` — client-side shared valid-hex computation for the 4 selectPiece response-move branches in `packages/client/src/store/useGameStore.ts` (Cluster 3), same parameterization style.
- `computeMovementValidHexes` — client-side shared plain-MOVEMENT valid-hex (+ tackle-risk) derivation used by both selectPiece (lines ~576-588) and setGameState's generic sticky fallthrough (lines ~717-733) in `useGameStore.ts` (Cluster 5).

These are NEW symbols created by this phase — the plan-review source-grounding pass should treat them as introduced here, not as drift.

Explicitly NOT created: no generic phase-handling framework, no deep-diff utility, no Cluster 2 unification helper (the divergent HP-vs-FTP defender-slot-complete logic stays as two separate code paths per RESEARCH.md Pitfall 3).
</artifacts_produced>

<objective>
Close DESIGN-03: consolidate the confirmed duplicate-logic clusters across the response-move phase families, AFTER the BUG-09/BUG-11 behavioral fixes have landed (Plans 01 and 03), so the shared helpers encode the already-corrected pattern rather than baking a known-buggy one into an abstraction.

Purpose: Eliminate the structural duplication that has already drifted twice (FTP gained carrier-exclusion that HP lacked → BUG-11; SNAPSHOT_DEFLECT gained range-click the others lacked), preventing a future phase from re-discovering the same "fixed in A, forgotten in B" gap.

Output: Three shared helpers (`validateResponseMoveStep`, `computeResponseMoveValidHexes`, `computeMovementValidHexes`) with each duplicated branch reduced to a thin call; behavior preserved (the existing per-phase handler/store tests are the regression safety net).

Scope discipline (RESEARCH.md, PATTERNS.md, D-08):

- Consolidate ONLY Clusters 1, 3, and 5 (the cleanly-identical ones).
- Do NOT merge Cluster 2 (`GAME_END_TURN` defender-slot-complete: HP routes through `applyRoll`, FTP does direct occupant-delivery — Pitfall 3 warns a forced merge recreates the BUG-11 class of bug).
- Do NOT merge Cluster 4's `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` sticky block (its own code comment at useGameStore.ts:686-690 correctly explains why it stays separate — that comment is current and correct).
- Use a data-driven parameterized helper (like the existing PHASE_LABEL-style lookup), NOT a generic framework. A helper that needs an internal conditional to handle one phase differently is a sign the cases aren't identical — stop and keep them separate.

Sequencing: Wave 3 — depends on Plan 01 (BUG-11 touches both consolidation files) and Plan 03 (BUG-09 touches useGameStore.ts). Run after both land.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-CONTEXT.md
@.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-RESEARCH.md
@.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cluster 1 — extract validateResponseMoveStep across the 4 server GAME_MOVE branches</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts lines 281-343 (HIGH_PASS_MOVE branch, incl. BUG-11 guard from Plan 01), 345-421 (SNAPSHOT_DEFLECT branch — multi-hex single-click range mode), 423-486 (GK_KICK_MOVE branch), 488-564 (FIRST_TIME_PASS_MOVE branch, incl. its carrier-exclusion guard) — read all four to confirm the identical guard sequence: active-player → ownership → [optional carrier-exclusion] → single-piece lock → pace-cap → distance → pitch-boundary → occupancy → state update
    - packages/server/src/gameHandlers.ts lines 505-514 (FTP carrier-exclusion guard) and the Plan-01-added HP carrier guard (~line 294 region) — the carrier-exclusion must remain a configured option in the shared helper, not be lost
  </read_first>
  <action>
    Extract a `validateResponseMoveStep(io, socket, room, pieceId, to, config)` helper in `packages/server/src/gameHandlers.ts` that runs the shared guard sequence (active-player, ownership, optional carrier-exclusion, single-piece lock, pace-cap, distance, pitch-boundary, occupancy) and either emits the appropriate GAME_ERROR + broadcastState + signals rejection, or returns the validated piece for the caller to apply its own state-merge. The `config` carries the per-phase field NAMES/values: `lockedPieceIdKey` (e.g. `highPassMovedPieceId`), `paceUsedKey`, `paceCap` (3/3/1/2), `movementSlotKey`, optional `carrierExclusionKey` (e.g. `highPassCarrierId` for HP, `firstTimePassCarrierId` for FTP, undefined for GK_KICK/SNAPSHOT_DEFLECT), and `clickDistanceMode` ('strict-1' for HP/GK_KICK/FTP, 'range' for SNAPSHOT_DEFLECT — range uses `hexDistance <= paceRemaining` instead of `=== 1`).

    Rewrite each of the 4 branches to call the helper with its config, then apply its own state-merge shape (the event-log entry type — HP_MOVE / FTP_MOVE / the SNAPSHOT_DEFLECT and GK_KICK equivalents — and the `...MovedPieceId`/`...PaceUsed` updates remain per-branch since the field names differ). Preserve every existing GAME_ERROR reason string exactly (WRONG_TEAM, WRONG_PIECE, PACE_EXCEEDED, NOT_ADJACENT, OFF_PITCH, OCCUPIED) so existing handler tests still pass unchanged.

    CRITICAL: the BUG-11 carrier-exclusion (HP) added in Plan 01 and FTP's existing carrier-exclusion must both survive as the `carrierExclusionKey` config — verify by grep after the refactor that both `highPassCarrierId` and `firstTimePassCarrierId` are still consulted in the GAME_MOVE path. Do NOT touch the GAME_END_TURN branches (Cluster 2).

  </action>
  <verify>
    <automated>cd packages/server && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - A single `validateResponseMoveStep` helper exists in gameHandlers.ts and is called by all 4 GAME_MOVE response-move branches (HIGH_PASS_MOVE, GK_KICK_MOVE, FIRST_TIME_PASS_MOVE, SNAPSHOT_DEFLECT).
    - Grep confirms `highPassCarrierId` AND `firstTimePassCarrierId` are still consulted in the GAME_MOVE path (BUG-11 + FTP exclusions preserved via `carrierExclusionKey`).
    - SNAPSHOT_DEFLECT's range click-distance mode (multi-hex single click) is preserved (not forced to strict adjacency).
    - The helper contains no per-phase `if (phase === ...)` special-case branch — divergence is expressed only through `config` values.
    - GAME_END_TURN branches (Cluster 2) are unchanged.
    - `cd packages/server && npx vitest run` passes (all existing per-phase handler tests green — the regression safety net) and `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>The 4 server GAME_MOVE response-move branches share one parameterized validation helper; all existing handler tests pass; BUG-11/FTP carrier exclusions and SNAPSHOT_DEFLECT range mode preserved; Cluster 2 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Clusters 3 & 5 — extract client valid-hex helpers in useGameStore.ts</name>
  <files>packages/client/src/store/useGameStore.ts</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts lines 392-424 (HP selectPiece, incl. Plan-01 carrier mirror), 462-506 (FTP, incl. carrier mirror), 509-537 (GK_KICK), 539-574 (SNAPSHOT_DEFLECT range mode) — Cluster 3 branches
    - packages/client/src/store/useGameStore.ts lines 576-588 (plain-MOVEMENT selectPiece valid-hex + tackle-risk derivation) and lines 717-733 (setGameState generic sticky fallthrough — RESEARCH.md confirms these two are byte-for-byte identical) — Cluster 5
    - packages/client/src/store/useGameStore.ts lines 643-684 (the HP/GK_KICK/FTP sticky-selection block) and lines 602-617 (BUG-09 broadened gate from Plan 03) — must be preserved through the refactor
  </read_first>
  <action>
    In `packages/client/src/store/useGameStore.ts`:

    Cluster 3 — extract `computeResponseMoveValidHexes(piece, gameState, config)` returning the valid adjacent/in-range hexes for a response-move phase, parameterized like the server helper: `lockedPieceIdField`, `paceUsedField`, `paceCap`, optional `carrierExclusionField`, `clickDistanceMode` ('strict-1' vs 'range'). Rewrite the HP / FTP / GK_KICK / SNAPSHOT_DEFLECT selectPiece branches to: do their own own-team/ownership and carrier/lock early-return guards (these `set({ selectedPieceId: null, ...})` returns differ per branch and stay inline — including the Plan-01 BUG-11 mirror for HP and FTP's existing mirror), then call the shared helper for the valid-hex list, then `set({ selectedPieceId: id, validMoveHexes: <helper result> })`. Preserve the SNAPSHOT_DEFLECT range semantics via `clickDistanceMode: 'range'`.

    Cluster 5 — extract `computeMovementValidHexes(piece, gameState)` returning `{ validMoveHexes, tackleRiskHexes }` (the `hexesInRange(.,1) → validateMove → filter ok / filter TACKLE_ATTEMPT effect` logic). Replace BOTH the plain-MOVEMENT selectPiece derivation (576-588) and setGameState's generic sticky fallthrough (717-733) with calls to it.

    Do NOT alter the BUG-09 broadened clear-condition gate (602-617) or the FREE_MOVE sticky block (686-715, Cluster 4 — leave separate per its code comment). Optionally, the HP/GK_KICK/FTP sticky-selection block (643-684) MAY reuse `computeResponseMoveValidHexes` if it cleanly fits, but only if it does not require an internal special-case — otherwise leave it as-is (D-08 risk tolerance favors not over-reaching).

  </action>
  <verify>
    <automated>cd packages/client && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `computeResponseMoveValidHexes` exists and is called by all 4 response-move selectPiece branches (HP/FTP/GK_KICK/SNAPSHOT_DEFLECT).
    - `computeMovementValidHexes` exists and is called by BOTH the plain-MOVEMENT selectPiece derivation and setGameState's generic sticky fallthrough (the byte-identical Cluster 5 duplication is gone).
    - Grep confirms `highPassCarrierId` and `firstTimePassCarrierId` are still consulted in the selectPiece path (Plan-01 BUG-11 mirror + FTP mirror preserved).
    - The BUG-09 broadened clear-condition gate (firstTimePassMovementSlot/highPassMovementSlot/gkKickMovementSlot + pace-exhaustion) is unchanged.
    - The FREE_MOVE sticky block (Cluster 4) is unchanged.
    - SNAPSHOT_DEFLECT keeps range-based valid hexes (not forced to adjacency-only).
    - `cd packages/client && npx vitest run` passes (existing HexGrid/useGameStore/component tests are the regression net) and `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>Clusters 3 and 5 are consolidated into two shared client helpers; all existing client tests pass; BUG-09/BUG-11 fixes and FREE_MOVE/SNAPSHOT_DEFLECT semantics preserved.</done>
</task>

</tasks>

<verification>
- `cd packages/server && npx vitest run` and `cd packages/client && npx vitest run` — full suites green (the pre-existing per-phase tests prove behavior is preserved through consolidation).
- `cd packages/shared && npx vitest run` — green (untouched, sanity check).
- `pnpm -r typecheck` (or per-package `npx tsc --noEmit`) clean.
- Grep confirms: one `validateResponseMoveStep`, one `computeResponseMoveValidHexes`, one `computeMovementValidHexes`; `highPassCarrierId` + `firstTimePassCarrierId` still consulted in both GAME_MOVE and selectPiece paths; Cluster 2 GAME_END_TURN and Cluster 4 FREE_MOVE blocks unchanged.
</verification>

<success_criteria>
DESIGN-03 closed: Clusters 1, 3, and 5 are consolidated into shared parameterized helpers with each duplicated branch reduced to a thin call; behavior is preserved (all existing tests pass as the regression net); the BUG-09 and BUG-11 fixes from Plans 01/03 survive the refactor; the divergent Cluster 2 and intentionally-separate Cluster 4 are left untouched per the research's explicit do-not-merge guidance.
</success_criteria>

<output>
Create `.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-04-SUMMARY.md` when done
</output>
