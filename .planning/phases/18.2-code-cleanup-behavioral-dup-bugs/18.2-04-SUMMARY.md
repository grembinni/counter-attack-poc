---
phase: 18.2-code-cleanup-behavioral-dup-bugs
plan: 04
subsystem: api
tags: [socket.io, zustand, game-state, refactor, duplicate-code-elimination]

# Dependency graph
requires:
  - phase: 18.2-code-cleanup-behavioral-dup-bugs (Plan 01)
    provides: HIGH_PASS_MOVE carrier-exclusion guard (highPassCarrierId) on server GAME_MOVE + client selectPiece — consolidated into this plan's shared helpers without losing the fix
  - phase: 18.2-code-cleanup-behavioral-dup-bugs (Plan 03)
    provides: Broadened setGameState clear-condition gate (responseMoveStateChanged/responseMovePaceExhausted) in useGameStore.ts — consolidated alongside without touching that gate
provides:
  - Server validateResponseMoveStep helper consolidating the 4 GAME_MOVE response-move branches (Cluster 1)
  - Client computeResponseMoveValidHexes helper consolidating the 4 selectPiece response-move branches (Cluster 3)
  - Client computeMovementValidHexes helper consolidating the byte-identical plain-MOVEMENT valid-hex derivation (Cluster 5)
affects:
  [
    any future response-move phase additions (a 5th HIGH_PASS_MOVE-style phase now extends a config object instead of duplicating ~70 lines),
    future HIGH_PASS_MOVE/FIRST_TIME_PASS_MOVE/GK_KICK_MOVE/SNAPSHOT_DEFLECT parity audits,
  ]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Parameterized guard-sequence helper: validateResponseMoveStep(io, socket, room, pieceId, to, config) on the server and computeResponseMoveValidHexes(id, piece, gameState, config) on the client both take a per-phase config object (locked-piece-id field, pace-used field, pace cap, optional carrier-exclusion field, click-distance mode) instead of branching internally — divergence is expressed only through config values, never an if(phase===...) special case inside the helper'
    - 'Shared movement-derivation helper: computeMovementValidHexes(piece, gameState) returns { validMoveHexes, tackleRiskHexes } and replaces two call sites that were byte-for-byte identical (selectPiece plain-MOVEMENT branch + setGameState generic sticky fallthrough)'

key-files:
  created: []
  modified:
    - packages/server/src/gameHandlers.ts
    - packages/client/src/store/useGameStore.ts

key-decisions:
  - 'validateResponseMoveStep returns a discriminated union ({ ok: true, piece, distanceMoved } | { ok: false }) rather than throwing or mutating room.gameState directly — keeps the caller in control of the phase-specific state-merge shape (event-log entry type, ...MovedPieceId/...PaceUsed field names), which the plan explicitly required to stay per-branch'
  - "actingTeam is a config field (not derived inside the helper) because SNAPSHOT_DEFLECT's acting-team rule (defendingTeam = opposite of attackingTeam) differs from the other 3 phases' isActivePlayer/activeTeam rule — passing it in as data avoids an internal phase-conditional"
  - "clickDistanceMode: 'strict-1' | 'range' captures the one real behavioral divergence (SNAPSHOT_DEFLECT's multi-hex single-click) as a config value rather than a special case, per the plan's explicit anti-framework guidance"
  - "Did NOT fold the HP/GK_KICK/FTP sticky-selection block (setGameState lines ~673-714 pre-refactor) into computeResponseMoveValidHexes — the plan marked this optional ('only if it does not require an internal special-case') and the block's locked-piece-id/pace-used field selection already uses its own phase-keyed ternary chain; reusing the helper there would require passing the same per-phase config a second time with no LOC reduction benefit, so it was left as-is per D-08 risk tolerance"

patterns-established:
  - 'Server/client config-object parity: ResponseMoveConfig (server) and ResponseMoveValidHexConfig (client) intentionally mirror each other field-for-field (lockedPieceIdKey/lockedPieceIdField, paceUsedKey/paceUsedField, paceCap, clickDistanceMode) so a future phase addition updates both helpers identically rather than inventing new shapes'

requirements-completed: [DESIGN-03]

# Metrics
duration: 35min
completed: 2026-06-22
---

# Phase 18.2 Plan 04: Response-Move Duplicate Code Consolidation (DESIGN-03) Summary

**Extracted three parameterized helpers (`validateResponseMoveStep` server-side, `computeResponseMoveValidHexes` + `computeMovementValidHexes` client-side) that collapse the 4 structurally-identical GAME_MOVE/selectPiece response-move branches and the byte-identical plain-MOVEMENT valid-hex derivation into thin per-phase config calls, while preserving the BUG-09/BUG-11 fixes from Plans 01/03 verbatim.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-06-22T10:50:00Z (approx, includes worktree dependency provisioning)
- **Completed:** 2026-06-22T11:25:00Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments

- Closed DESIGN-03 for Clusters 1, 3, and 5 (the three confirmed cleanly-identical duplicate-logic clusters)
- Cluster 1: `validateResponseMoveStep` consolidates the shared guard sequence (active-player → ownership → optional carrier-exclusion → single-piece lock → pace-cap → distance → pitch-boundary → occupancy) across HIGH_PASS_MOVE, GK_KICK_MOVE, FIRST_TIME_PASS_MOVE, and SNAPSHOT_DEFLECT in `gameHandlers.ts` — net -36 lines while preserving every GAME_ERROR reason string exactly
- Cluster 3: `computeResponseMoveValidHexes` consolidates the same 4 phases' client-side valid-hex computation in `useGameStore.ts`, with each branch keeping its own inline ownership/carrier/lock early-return guards (these differ per branch and stayed inline per the plan)
- Cluster 5: `computeMovementValidHexes` eliminates the byte-identical plain-MOVEMENT `hexesInRange → validateMove → split ok/TACKLE_ATTEMPT` duplication between `selectPiece` and `setGameState`'s generic sticky fallthrough
- BUG-11 (`highPassCarrierId`) and FTP's pre-existing carrier exclusion (`firstTimePassCarrierId`) survive the refactor on both server and client tiers via the `carrierExclusionKey`/equivalent config option
- SNAPSHOT_DEFLECT's range click-distance mode (multi-hex single click) survives via `clickDistanceMode: 'range'`
- BUG-09's broadened `setGameState` clear-condition gate (`responseMoveStateChanged`/`responseMovePaceExhausted`) is untouched — Plan 04's edits are isolated to the valid-hex computation, not the clear/retain decision
- Cluster 2 (`GAME_END_TURN` defender-slot-complete divergence) and Cluster 4 (`FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` sticky block) deliberately left untouched per RESEARCH.md's explicit do-not-merge guidance

## Task Commits

1. **Task 1: Cluster 1 — extract validateResponseMoveStep across the 4 server GAME_MOVE branches** - `05c94fc` (feat)
2. **Task 2: Clusters 3 & 5 — extract client valid-hex helpers in useGameStore.ts** - `fd98056` (feat)

_Note: both task commits were auto-formatted by the repo's lint-staged pre-commit hook (eslint --fix + prettier --write); tests and typecheck were re-verified after each commit to confirm the hook's formatting changes introduced no regressions._

## Files Created/Modified

- `packages/server/src/gameHandlers.ts` - Added `ResponseMoveConfig` type, `ResponseMoveValidation` discriminated-union return type, and the `validateResponseMoveStep` helper (after `controlsGKTeam`); rewrote the HIGH_PASS_MOVE, SNAPSHOT_DEFLECT, GK_KICK_MOVE, and FIRST_TIME_PASS_MOVE branches of the `GAME_MOVE` socket handler to call the helper and apply only their own phase-specific state-merge (event type, slot field, `...MovedPieceId`/`...PaceUsed` keys); added `PlayerPiece` to the shared-types import
- `packages/client/src/store/useGameStore.ts` - Added `computeMovementValidHexes(piece, gameState)` and `computeResponseMoveValidHexes(id, piece, gameState, config)` module-level helpers (before the `create<GameStore>()` call); rewrote the HIGH_PASS_MOVE, FIRST_TIME_PASS_MOVE, GK_KICK_MOVE, and SNAPSHOT_DEFLECT branches of `selectPiece` to call `computeResponseMoveValidHexes` for the valid-hex list after their own inline guards; replaced the plain-MOVEMENT `selectPiece` derivation and `setGameState`'s generic sticky fallthrough with calls to `computeMovementValidHexes`

## Decisions Made

See `key-decisions` in frontmatter — summarized: (1) the server helper returns a discriminated union so callers keep control of their own state-merge shape; (2) `actingTeam` is passed in as config data rather than derived inside the helper, because SNAPSHOT_DEFLECT's acting-team rule differs from the other 3 phases; (3) `clickDistanceMode` captures the one real behavioral divergence as a config value, not a special case; (4) the optional HP/GK_KICK/FTP sticky-selection block reuse was evaluated and skipped — no LOC reduction benefit, left as-is per D-08 risk tolerance.

## Deviations from Plan

None - plan executed exactly as written. Both tasks were pure refactors (no behavior change); the regression safety net (existing per-phase handler/store tests) caught zero regressions, confirming behavior preservation.

## Issues Encountered

None. The worktree required the standard `pnpm install --frozen-lockfile` + `packages/shared` build step (no `node_modules`/`dist` present at start), consistent with Plans 01 and 03's documented environment-provisioning note for fresh worktrees — not logged as a deviation here since it produced no code changes and was already established as expected worktree setup in this wave.

## User Setup Required

None - no external service configuration required.

## Verification Evidence

- `cd packages/server && npx vitest run` — **464 passed**, 1 skipped, 1 todo (identical to pre-refactor baseline — same file/test counts, same skip/todo)
- `cd packages/server && npx tsc --noEmit` — clean
- `cd packages/client && npx vitest run` — **235 passed** across 12 test files (identical to pre-refactor baseline)
- `cd packages/client && npx tsc --noEmit` — clean
- `cd packages/shared && npx vitest run` — 320 passed (untouched by this plan, confirmed green)
- `npx eslint` clean on both modified files
- Grep: exactly 1 `validateResponseMoveStep` definition + 4 call sites (one per GAME_MOVE branch) in `gameHandlers.ts`
- Grep: exactly 1 `computeResponseMoveValidHexes` definition + 4 call sites, and exactly 1 `computeMovementValidHexes` definition + 2 call sites, in `useGameStore.ts`
- Grep: `highPassCarrierId` confirmed consulted in both the new `validateResponseMoveStep` config (server) and the inline `selectPiece` guard (client) — BUG-11 preserved
- Grep: `firstTimePassCarrierId` confirmed consulted in both tiers — FTP carrier exclusion preserved
- Grep: no `if (phase === ...)` special-case branch inside either helper body — divergence expressed only through config values
- Grep: `GAME_END_TURN` branches (Cluster 2) and the `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` sticky block (Cluster 4) confirmed unchanged (line ranges outside this plan's diffs)
- Grep: `responseMoveStateChanged`/`responseMovePaceExhausted` (BUG-09 gate from Plan 03) confirmed unchanged

## Next Phase Readiness

- DESIGN-03 closed for Clusters 1, 3, and 5 — the divergent Cluster 2 (GAME_END_TURN defender-slot-complete) and intentionally-separate Cluster 4 (FREE_MOVE sticky block) remain as-is per research guidance, with their do-not-merge rationale still documented inline in the code
- All 3 packages (server/client/shared) green; typecheck clean across all 3
- No blockers for subsequent phases; this was the final plan (Wave 3, plan 04) in the 18.2-code-cleanup-behavioral-dup-bugs phase's planned scope

## Self-Check: PASSED

- FOUND: packages/server/src/gameHandlers.ts (validateResponseMoveStep helper + 4 call sites)
- FOUND: packages/client/src/store/useGameStore.ts (computeResponseMoveValidHexes + computeMovementValidHexes helpers + 6 call sites)
- FOUND commit: 05c94fc (feat, Task 1)
- FOUND commit: fd98056 (feat, Task 2)

---

_Phase: 18.2-code-cleanup-behavioral-dup-bugs_
_Completed: 2026-06-22_
