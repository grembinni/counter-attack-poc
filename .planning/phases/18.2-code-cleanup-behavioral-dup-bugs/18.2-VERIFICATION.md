---
phase: 18.2-code-cleanup-behavioral-dup-bugs
verified: 2026-06-22T16:30:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "BUG-09 is fixed and covered by regression tests for SNAPSHOT_DEFLECT's non-exhausted sticky-recompute case — Test 7's fixture now sets movementSlot: null and independently re-proven (by this verifier, not just trusting SUMMARY.md) to fail when the production fix is reverted and pass when restored"
  gaps_remaining: []
  regressions: []
---

# Phase 18.2: Code Cleanup & Behavioral Dup-Bugs Verification Report

**Phase Goal:** Duplicate logic across server handlers and client components is consolidated, dead code is removed, and the three behavioral defects that are themselves duplicate-logic gaps (BUG-08, BUG-09, BUG-11) are fixed.
**Verified:** 2026-06-22T16:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plan 18.2-06, targeting the BUG-09 Test 7 fixture-discrimination gap identified in the prior verification round)

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                          | Status   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Duplicate logic across server handlers and client components is consolidated (DESIGN-03)                                                                       | VERIFIED | Regression-checked: `validateResponseMoveStep` (gameHandlers.ts) — 6 occurrences (1 def + 5 call sites); `computeResponseMoveValidHexes` (useGameStore.ts) — 10 occurrences (1 def + 9 references, including the SNAPSHOT_DEFLECT sticky branch). No regression from prior verification's counts.                                                                                                                                                                                                                    |
| 2   | Dead code, unused exports, unreachable branches, stale TODOs, legacy feature flags removed (DESIGN-04)                                                         | VERIFIED | Regression-checked: `applyDeclareHeaderTarget` — 0 matches in `packages/server/src`. `TODO-NAME` placeholder — 0 matches in `ActionLog.tsx`. No regression.                                                                                                                                                                                                                                                                                                                                                          |
| 3   | BUG-08 is fixed and covered by regression tests                                                                                                                | VERIFIED | `describe('HexGrid — BUG-08: tackleRiskSet excludes tackleAttemptedByIds defenders', ...)` present at `HexGrid.test.tsx:249`; passes as part of full 236-test client suite (independently re-run, not just cited).                                                                                                                                                                                                                                                                                                   |
| 4   | BUG-09 is fixed and covered by regression tests for all four named response-move phases (HIGH_PASS_MOVE, FIRST_TIME_PASS_MOVE, GK_KICK_MOVE, SNAPSHOT_DEFLECT) | VERIFIED | **Independently re-proven in this round** (see Behavioral Spot-Checks below) — the prior round's hollow-test gap is closed. `snapshotDeflectState()` (`useGameStore.test.ts:391`) now hardcodes `movementSlot: null`. I personally reverted `useGameStore.ts:758` (`if (false && newState.phase === 'SNAPSHOT_DEFLECT')`), re-ran the suite, and observed Test 7 fail with `expected 0 to be greater than 0` while all 48 other tests stayed green; restored via `git checkout --` and confirmed clean tree + GREEN. |
| 5   | BUG-11 is fixed and covered by regression tests                                                                                                                | VERIFIED | `carrierExclusionKey` guard present at `gameHandlers.ts:224` (`if (config.carrierExclusionKey !== undefined && pieceId === state[config.carrierExclusionKey]) ...`); `gameHandlers.phase18-02.test.ts`'s 2 tests pass as part of full 464-test server suite (independently re-run).                                                                                                                                                                                                                                  |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                                                                                                                  | Expected                                                                                    | Status                        | Details                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/client/src/store/useGameStore.ts`                                                                                               | SNAPSHOT_DEFLECT-aware sticky-recompute path in `setGameState` (BUG-09 gap closure)         | VERIFIED                      | Present unchanged at lines 758-777 (confirmed via `git log` — no commit since `7950a18` from plan 18.2-05; plan 18.2-06 did not touch this file in the committed tree). Calls `computeResponseMoveValidHexes` with `paceCap: 2`, `clickDistanceMode: 'range'`. Directly re-verified by temporary revert/restore (this round).                                                                                                           |
| `packages/client/src/store/useGameStore.test.ts`                                                                                          | Discriminating regression test for the SNAPSHOT_DEFLECT non-exhausted sticky-recompute case | VERIFIED                      | `snapshotDeflectState()` (lines 385-395) hardcodes `movementSlot: null` (line 391) — confirmed by direct file read, not grep alone. Committed at `498adba` ("fix(18.2): set movementSlot: null in snapshotDeflectState() test fixture"). Test 7 (lines 495-511) now empirically discriminates: FAILS when the production branch is reverted, PASSES when restored — proven independently by this verifier (see Behavioral Spot-Checks). |
| All other 18.2 artifacts (validateResponseMoveStep, computeResponseMoveValidHexes, computeMovementValidHexes, BUG-08 test, BUG-11 guards) | Unchanged from prior verification                                                           | VERIFIED (regression-checked) | No changes since prior PASS rounds; re-confirmed present via grep + read, no regression.                                                                                                                                                                                                                                                                                                                                                |

### Key Link Verification

| From                                                    | To                                           | Via                                                                                                                                                           | Status    | Details                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `useGameStore.ts` `setGameState` sticky-selection block | SNAPSHOT_DEFLECT phase-specific recompute    | calls `computeResponseMoveValidHexes` with `snapDeflectMovedPieceId`/`snapDeflectPaceUsed` config                                                             | WIRED     | Confirmed at lines 758-777, unchanged from prior round.                                                                                                                                                                                                                                                                                                                                                   |
| `useGameStore.test.ts` Test 7                           | The SNAPSHOT_DEFLECT sticky-recompute branch | a broadcast with `snapDeflectPaceUsed: 1` and `movementSlot: null` that can ONLY pass if `computeResponseMoveValidHexes` (not the old fallthrough) is reached | **WIRED** | **Upgraded from PARTIAL (prior round) to WIRED.** Independently re-verified this round: with `movementSlot: null` in the fixture, temporarily neutralizing the SNAPSHOT_DEFLECT branch (`if (false && ...)`) causes Test 7 to fail (`expected 0 to be greater than 0`); restoring the branch makes it pass again. The test now discriminates fixed vs. unfixed code, closing the previously-reported gap. |

### Data-Flow Trace (Level 4)

| Artifact                                                                                      | Data Variable    | Source                                                                                                                                                     | Produces Real Data                                                                                                                      | Status                                    |
| --------------------------------------------------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| `useGameStore.ts` `setGameState` (SNAPSHOT_DEFLECT, pace not exhausted, `movementSlot: null`) | `validMoveHexes` | `computeResponseMoveValidHexes(prevSelectedId, piece, newState, {...})` — filtered by pitch/occupancy/distance, NOT gated by `validateMove`'s `WRONG_SLOT` | Yes — 6 valid hexes when the branch is present; 0 valid hexes (independently confirmed by this verifier) when the branch is neutralized | **FLOWING — discrimination re-confirmed** |
| `useGameStore.test.ts` Test 7 (current committed fixture, `movementSlot: null`)               | `validMoveHexes` | Same call as above; fixture now matches the real game's SNAPSHOT_DEFLECT state                                                                             | Yes for the fixed path (6 hexes); No (`[]`, assertion fails) for the unfixed path — the test now distinguishes the two                  | **DISCRIMINATING — gap closed**           |

### Behavioral Spot-Checks

| Behavior                                                                                                       | Command                                                                                                                                       | Result                                                                                                                                                               | Status                                                    |
| -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| Full client test suite passes                                                                                  | `cd packages/client && npx vitest run`                                                                                                        | 236 passed (12 files)                                                                                                                                                | PASS                                                      |
| Full server test suite passes                                                                                  | `cd packages/server && npx vitest run`                                                                                                        | 464 passed, 1 skipped, 1 todo (23 files)                                                                                                                             | PASS — matches prior baseline exactly                     |
| Client typecheck clean                                                                                         | `cd packages/client && npx tsc --noEmit`                                                                                                      | exit 0, no output                                                                                                                                                    | PASS                                                      |
| `snapshotDeflectState()` sets `movementSlot: null`                                                             | Direct file read, `useGameStore.test.ts:385-395`                                                                                              | `movementSlot: null` present in the returned object literal at line 391                                                                                              | PASS — confirmed by reading source, not just grep         |
| `useGameStore.ts:758-777` unchanged from plan 18.2-05 (no production change shipped by plan 18.2-06)           | `git log --oneline -3 -- packages/client/src/store/useGameStore.ts`                                                                           | Most recent commit touching this file is `7950a18` ("fix(18.2-05): add SNAPSHOT_DEFLECT recompute path..."); no plan-06 commit                                       | PASS                                                      |
| **Baseline GREEN** (fixture fix in place, production code untouched)                                           | `cd packages/client && npx vitest run src/store/useGameStore.test.ts`                                                                         | 49/49 tests pass                                                                                                                                                     | PASS                                                      |
| **RED proof (independently reproduced by this verifier)** — temporarily neutralize the SNAPSHOT_DEFLECT branch | Edited `useGameStore.ts:758` to `if (false && newState.phase === 'SNAPSHOT_DEFLECT')`, re-ran `npx vitest run src/store/useGameStore.test.ts` | Exactly 1 failure: Test 7, `AssertionError: expected 0 to be greater than 0` at line 510 (`validMoveHexes.length`); 48/49 other tests stayed green, including Test 6 | **PASS — proves discrimination, matches SUMMARY's claim** |
| **Restore + GREEN proof**                                                                                      | `git checkout -- packages/client/src/store/useGameStore.ts`, then `git status --porcelain` (no output) and re-ran the test file               | Clean tree confirmed; 49/49 tests pass again                                                                                                                         | PASS                                                      |
| Test 6 remains independent of `movementSlot` (verification's reasoning re-derived, not assumed)                | Read `useGameStore.ts:674-681` — the shared clear-condition gate                                                                              | `responseMovePaceExhausted` (not `movementSlot`) gates the clear branch for SNAPSHOT_DEFLECT pace exhaustion; Test 6 unaffected by the fixture change                | PASS — confirms prior verification's caveat was correct   |

### Probe Execution

No probes declared or discovered for this phase (not a migration/tooling phase). Step 7c: SKIPPED (no probes applicable).

### Requirements Coverage

| Requirement | Source Plan               | Description                                                                                | Status        | Evidence                                                                                                                                                                                                                                                                             |
| ----------- | ------------------------- | ------------------------------------------------------------------------------------------ | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| DESIGN-03   | 18.2-04                   | Duplicate logic consolidated into shared helpers                                           | SATISFIED     | Regression-checked, no change since prior PASS.                                                                                                                                                                                                                                      |
| DESIGN-04   | 18.2-02                   | Dead code, stale TODOs removed                                                             | SATISFIED     | Regression-checked, no change since prior PASS.                                                                                                                                                                                                                                      |
| BUG-08      | 18.2-02                   | Tackle/steal ZoI exclusion re-verified with regression test                                | SATISFIED     | Regression-checked, no change since prior PASS.                                                                                                                                                                                                                                      |
| BUG-09      | 18.2-03, 18.2-05, 18.2-06 | Response-move stale highlight clears and recomputes correctly, covered by regression tests | **SATISFIED** | **Gap closed this round.** Both halves of the requirement are now met: the production fix is correct (confirmed in two consecutive rounds), AND the regression test (Test 7) is now empirically proven — by this verifier, independently — to discriminate the fix from its absence. |
| BUG-11      | 18.2-01                   | HIGH_PASS_MOVE carrier exclusion                                                           | SATISFIED     | Regression-checked, no change since prior PASS.                                                                                                                                                                                                                                      |

No orphaned requirements found — REQUIREMENTS.md's Phase 18.2 row maps exactly to DESIGN-03, DESIGN-04, BUG-08, BUG-09, BUG-11, all of which appear in at least one plan's `requirements:` frontmatter field (18.2-01 through 18.2-06).

Note: REQUIREMENTS.md's checkbox/status column (lines 50-51, 62-63, 65, 105-106, 111-112, 114) still shows DESIGN-03/04, BUG-08/09/11 as unchecked/"Pending" — this is documentation bookkeeping that is expected to be updated at milestone-close, not counted against the phase score (consistent with prior verification's note).

### Anti-Patterns Found

No debt markers (`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`) found in the file modified by plan 18.2-06 (`useGameStore.test.ts`).

18.2-REVIEW.md (the latest code review, post-plan-06) reports 0 critical, 4 warnings, 5 info findings — all advisory/non-blocking. It independently confirms the plan 06 fixture fix is correct and adds one new Info-tier finding (IN-05): three sibling fixtures (`highPassMoveState`, `firstTimePassMoveState`, `gkKickMoveState`) still inherit the same stale `movementSlot: 'ATTACKER_4'` value that plan 06 just fixed for `snapshotDeflectState`. The review explicitly confirms this is **currently inert, not a live bug** — `setGameState`'s sticky block for those three phases (lines 707-748) branches purely on `newState.phase` and never reads `movementSlot`, unlike the SNAPSHOT_DEFLECT fallthrough that did depend on it transitively via `validateMove`. I independently re-read lines 707-748 and confirm this analysis is accurate: no `movementSlot` read exists in that block. This is a legitimate latent-risk finding (same fixture-hygiene class that produced the original bug) but it is not a must-have for this phase's goal — the phase's goal was specifically the three named bugs (BUG-08/09/11) plus consolidation/dead-code removal, not a general fixture-hygiene audit. It does not block phase closure; it is reasonable follow-up hygiene work, most naturally captured as a tracked backlog item rather than a new phase-18.2 gap-closure plan, since:

1. It is provably inert today (no live test gap),
2. It would otherwise produce an unbounded gap-closure loop on advisory hygiene findings, which the override guidance explicitly discourages piling up.

No new blocker-tier anti-patterns found. No regressions to the prior round's anti-pattern findings.

| File                                                                                                 | Line    | Pattern                                                            | Severity               | Impact                                                                                                                                                                         |
| ---------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `packages/client/src/store/useGameStore.test.ts` (review IN-05, not this verification's own finding) | 341-383 | Stale `movementSlot: 'ATTACKER_4'` inherited by 3 sibling fixtures | Info (per code review) | Currently inert (confirmed independently in this verification — the 3 affected phases' sticky logic never reads `movementSlot`). Latent risk only; not a phase-18.2 must-have. |

### Human Verification Required

None. This phase is server-authoritative game logic plus presentational React/Zustand state — all claims are verifiable via automated tests, grep, file reads, and direct empirical revert/restore proofs (independently reproduced in this verification, not merely cited from SUMMARY.md). No `<verify><human-check>` blocks were found deferred from any of the 6 plans.

### Gaps Summary

No gaps remain. The single gap from the prior verification round — Test 7's non-discriminating fixture — is closed.

**What was independently verified this round (not just trusted from SUMMARY.md):**

1. Read `useGameStore.test.ts:385-395` directly and confirmed `snapshotDeflectState()` now hardcodes `movementSlot: null` at line 391 (matches the committed fix, commit `498adba`).
2. Confirmed via `git log` that `useGameStore.ts` (the production fix) has no commit from plan 18.2-06 — only the prior `7950a18` (plan 18.2-05) commit touches the SNAPSHOT_DEFLECT branch. The gap closure shipped zero production-code changes, exactly as the plan specified.
3. Ran the baseline test suite myself (`npx vitest run src/store/useGameStore.test.ts`) and confirmed 49/49 GREEN.
4. **Independently reproduced the RED proof**: I personally edited `useGameStore.ts:758` to neutralize the SNAPSHOT_DEFLECT branch (`if (false && newState.phase === 'SNAPSHOT_DEFLECT')`), re-ran the test file, and observed exactly one failure — Test 7, with `AssertionError: expected 0 to be greater than 0` — while Test 6 and all 47 other tests remained green. This is the precise failure mode the BUG-09 fix addresses (WRONG_SLOT rejection under `movementSlot: null`), and it matches the SUMMARY.md's claimed failure output exactly, which gives independent corroboration rather than circular trust.
5. Restored the production file via `git checkout --` and confirmed via `git status --porcelain` (no output) that the tree is clean, then re-ran the suite and confirmed 49/49 GREEN again.
6. Re-derived (not assumed) that Test 6 is unaffected by the fixture change: read `useGameStore.ts:674-681` and confirmed the clear-branch gate uses `responseMovePaceExhausted`, with no read of `movementSlot` anywhere in that condition.
7. Ran the full client suite (236/236 pass) and full server suite (464 pass/1 skip/1 todo, matching the prior baseline exactly) and client typecheck (clean).
8. Cross-referenced REQUIREMENTS.md and confirmed all 5 requirement IDs (DESIGN-03, DESIGN-04, BUG-08, BUG-09, BUG-11) map to Phase 18.2 with no orphans.
9. Read the latest 18.2-REVIEW.md and independently verified its one new finding (IN-05, stale fixtures in 3 sibling phase factories) is correctly classified as inert/Info-tier by checking the cited line ranges myself — confirmed no `movementSlot` read exists in the `HIGH_PASS_MOVE`/`FIRST_TIME_PASS_MOVE`/`GK_KICK_MOVE` sticky block (lines 707-748).

The phase goal — duplicate logic consolidated, dead code removed, BUG-08/09/11 fixed and regression-tested — is now fully achieved and independently verified, not merely asserted by SUMMARY.md.

---

_Verified: 2026-06-22T16:30:00Z_
_Verifier: Claude (gsd-verifier)_
