---
phase: 18.2-code-cleanup-behavioral-dup-bugs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/HexGrid.test.tsx
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.phase10.test.ts
  - packages/client/src/components/ActionLog.tsx
  - packages/shared/src/headingValidator.ts
autonomous: true
requirements: [BUG-08, DESIGN-04]
must_haves:
  truths:
    - 'BUG-08 is re-verified: a fresh render-level regression test proves the tackle-risk tint (tackleRiskSet) clears for a defender already in tackleAttemptedByIds, mirroring the existing steal-tint exclusion tests'
    - 'The deprecated, zero-callsite applyDeclareHeaderTarget function and its ApplyDeclareHeaderTargetResult type are removed from gameEngine.ts, along with their 4 dedicated tests, with the build still passing'
    - 'Two confirmed-stale TODO/placeholder comments are removed/rewritten (ActionLog.tsx:51 TODO-NAME placeholder; headingValidator.ts:42 pre-Phase-4 TODO)'
  artifacts:
    - path: 'packages/client/src/components/HexGrid.test.tsx'
      provides: 'Render-level tackle-risk-tint exclusion regression test(s)'
      contains: 'tackleAttemptedByIds'
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'gameEngine with applyDeclareHeaderTarget removed'
  key_links:
    - from: 'packages/client/src/components/HexGrid.test.tsx tackle-tint test'
      to: 'HexGrid.tsx tackleRiskSet derivation'
      via: 'render with tackleRiskHexes / tackleAttemptedByIds seeded state, assert tint absent'
      pattern: 'tackleRiskHexes'
---

<artifacts_produced>

## Artifacts this phase produces (symbols created by THIS plan)

No new production exported symbols. This plan:

- Adds a new `describe` block (render-level tackle-risk-tint regression tests) to the existing `packages/client/src/components/HexGrid.test.tsx`.
- DELETES the existing exported symbols `applyDeclareHeaderTarget` (function) and `ApplyDeclareHeaderTargetResult` (type) from `packages/server/src/gameEngine.ts` (both `@deprecated`, zero production callsites).
- DELETES the `describe('HEAD-03: header target hex selection (applyDeclareHeaderTarget)', ...)` block and the `applyDeclareHeaderTarget` import from `packages/server/src/__tests__/gameEngine.phase10.test.ts`.
- Removes/rewrites two stale comments (no symbol change).

Symbols deliberately NOT removed (verified live in RESEARCH.md, do not re-flag): `InterServerEvents` (events.ts:156-158 — required Socket.io generic, has eslint-disable), the `gameEngine.ts:2215` "legacy" comment (still load-bearing code path), `makeHeaderState` test helper (used by other tests at gameEngine.phase10.test.ts:234/269). `TACKLE_ALREADY_ATTEMPTED` is already removed (do not re-flag).
</artifacts_produced>

<objective>
Two independent low-risk workstreams that share zero files with the bug-fix plans, so they run in Wave 1:

1. **BUG-08 re-verification (verify-first, per D-07 and RESEARCH.md Pitfall 2):** RESEARCH.md found the ZoI steal/tackle exclusion already correctly implemented end-to-end (moveValidator.ts:94-124, HexGrid.tsx:168-187) by Phase 17.1-09 commits. The only confirmed actionable gap is **missing render-level test coverage for the tackle-tint path** (the steal-tint path has 3 tests at HexGrid.test.tsx:184-264; tackle has zero). Add the symmetric tackle-tint regression test. Only if that test reveals a real defect do you touch production code (it is not expected to).

2. **DESIGN-04 dead-code removal (D-08 scope: genuinely inert only):** Remove the single confirmed-dead `applyDeclareHeaderTarget` function + its type + its 4 paired tests (same commit, per Pitfall 4 — deleting prod without tests breaks the build via a dangling import), and clean up two confirmed-stale comments.

Purpose: Re-verify BUG-08 with a fresh automated repro before assuming correctness; remove confirmed-inert code without scope creep.

Output: New tackle-tint regression test; gameEngine.ts shrunk by one dead function + type; two stale comments cleaned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-CONTEXT.md
@.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-RESEARCH.md
@.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-PATTERNS.md
@.planning/debug/resolved/zoi-tackle-steal-exclusion.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BUG-08 render-level tackle-tint exclusion regression test (verify-first)</name>
  <files>packages/client/src/components/HexGrid.test.tsx, packages/client/src/components/HexGrid.tsx (only if the test reveals a real defect), packages/shared/src/moveValidator.ts (only if the test reveals a real defect)</files>
  <read_first>
    - packages/client/src/components/HexGrid.test.tsx lines 21-264 (the `baseStateWithDefender` helper at line 29 and the existing `describe('HexGrid — D-02 gap closure: zoiRiskSet excludes stealAttemptedByIds defenders', ...)` block at lines 184-264 — the structural template to mirror for tackle)
    - packages/client/src/components/HexGrid.tsx lines 160-200 (the `zoiRiskSet` and `tackleRiskSet` derivation; `tackleRiskSet` sourced from store `tackleRiskHexes`, no separate client filter)
    - packages/shared/src/moveValidator.ts lines 85-129 (the STEAL_ATTEMPT and TACKLE_ATTEMPT exclusion-with-fallthrough blocks; the TACKLE_ATTEMPT block at lines 109-124 omits the effect when `(state.tackleAttemptedByIds ?? []).includes(piece.id)`)
    - .planning/debug/resolved/zoi-tackle-steal-exclusion.md (root-cause diagnosis — read before concluding anything about whether a defect exists)
  </read_first>
  <behavior>
    - Test 1: When a non-carrier piece's candidate move would land adjacent to the ball carrier AND that defender's id is NOT in `tackleAttemptedByIds`, the tackle-risk tint IS present on that hex (regression guard — confirms the test actually exercises the tint path).
    - Test 2: When the same defender's id IS in `tackleAttemptedByIds`, the tackle-risk tint is ABSENT on that hex (the exclusion — mirrors the steal-tint exclusion test). Because `tackleRiskSet` reads from store `tackleRiskHexes` (derived from validateMove's TACKLE_ATTEMPT effect presence), seed the store/props so the validator-omitted case yields an empty `tackleRiskHexes` for the excluded defender.
  </behavior>
  <action>
    Add a new `describe('HexGrid — BUG-08: tackleRiskSet excludes tackleAttemptedByIds defenders', ...)` block to `packages/client/src/components/HexGrid.test.tsx`, mirroring the existing steal-tint `describe` block at lines 184-264 (reuse or adapt the `baseStateWithDefender` helper at line 29; add a `tackleAttemptedByIds` parameter analogous to its existing `stealAttemptedByIds` parameter, and seed `tackleRiskHexes` to represent the validator's TACKLE_ATTEMPT-effect output). Assert the tint class/marker is present when the defender is unexcluded and absent when the defender id is in `tackleAttemptedByIds`. Use the same DOM-query / tint-class assertion style the steal-tint tests use (read them first to match the exact selector — e.g. the orange tackle-risk tint marker).

    Run the new tests. If both pass with NO production change, BUG-08 is confirmed already-correct end-to-end and the test closes the documented coverage gap — do NOT modify production code. ONLY if Test 2 fails (a real defect surfaces) do you then apply the documented exclusion-with-fallthrough fix (`filter((d) => !(state.tackleAttemptedByIds ?? []).includes(d.id))` shape) to the offending tier (moveValidator.ts or HexGrid.tsx) and note the deviation in the SUMMARY. Do NOT reimplement exclusion logic client-side — the shared moveValidator.ts is the single source of truth.

  </action>
  <verify>
    <automated>cd packages/client && npx vitest run src/components/HexGrid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `HexGrid.test.tsx` contains a new `describe` block referencing `tackleAttemptedByIds` with at least 2 tests: tint-present (unexcluded) and tint-absent (excluded).
    - The tint-absent test exercises the `tackleAttemptedByIds` exclusion path symmetrically to the existing steal-tint test at lines 184-264.
    - `cd packages/client && npx vitest run src/components/HexGrid.test.tsx` passes.
    - If no production code was changed, the SUMMARY states "BUG-08 confirmed already-correct; closed test-coverage gap only." If production code WAS changed, the SUMMARY documents the exact defect found and the fix applied.
  </acceptance_criteria>
  <done>The tackle-risk tint exclusion has render-level regression coverage symmetric to the steal-tint coverage; BUG-08 is re-verified with an automated repro rather than a trust-the-prior-claim assumption.</done>
</task>

<task type="auto">
  <name>Task 2: DESIGN-04 — remove dead applyDeclareHeaderTarget (+ type + 4 tests) and 2 stale comments</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.phase10.test.ts, packages/client/src/components/ActionLog.tsx, packages/shared/src/headingValidator.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 2750-2818 (the `ApplyDeclareHeaderTargetResult` type at 2756-2758 and the `applyDeclareHeaderTarget` function at 2785-2817, both `@deprecated`/zero-callsite; the next symbol `computeHeaderDuelWinner` begins ~2819 — do NOT touch it)
    - packages/server/src/__tests__/gameEngine.phase10.test.ts lines 1-25 (the import block — line 21 imports `applyDeclareHeaderTarget`; remove only that import name, keep the other imports) and lines 556-585 (the `describe('HEAD-03: header target hex selection (applyDeclareHeaderTarget)', ...)` block to delete; note `makeHeaderState` at line 149 is used by OTHER tests at 234/269 — do NOT delete the helper)
    - packages/client/src/components/ActionLog.tsx line 51 (the "TODO-NAME" placeholder in a doc-comment — the duel-style log work it labeled already shipped)
    - packages/shared/src/headingValidator.ts line 42 (the "TODO: Verify head-shot composition with FSM before Phase 4 live use" — Phase 4 completed 2026-05-30; head shots are live via applyResolveHeaderTarget/HEAD-03)
  </read_first>
  <action>
    In `packages/server/src/gameEngine.ts`: delete the `export type ApplyDeclareHeaderTargetResult` declaration (lines 2756-2758 plus its preceding doc-comment at 2751-2754) and the entire `export function applyDeclareHeaderTarget(...)` (lines 2785-2817 plus its preceding doc-comment block at 2760-2784). Stop the deletion before `computeHeaderDuelWinner`'s section header (~line 2819) — leave that and everything after intact.

    In `packages/server/src/__tests__/gameEngine.phase10.test.ts`: remove `applyDeclareHeaderTarget` from the import list at line 21 (keep all other imports). Delete the `describe('HEAD-03: header target hex selection (applyDeclareHeaderTarget)', ...)` block (lines 561-585) AND its preceding section-header comment (lines 556-559). Do NOT delete the `makeHeaderState` helper (line 149) — it is used by other tests. Do NOT delete the later `makeHeaderStateWithTarget`/HEAD-03 goal-line tests (598+), which test `applyResolveHeaderTarget`-driven paths, not the deleted function.

    In `packages/client/src/components/ActionLog.tsx` line 51: remove the "(TODO-NAME)" placeholder text from the doc-comment (the labeled duel-format work already shipped — drop only the stale placeholder token, keep any surrounding genuine documentation).

    In `packages/shared/src/headingValidator.ts` line 42: replace the stale "TODO: Verify head-shot composition with FSM before Phase 4 live use" comment with either nothing or a short factual note that head-shot/GK composition is live via HEAD-03 (no TODO). Do NOT leave a TODO referencing a completed milestone.

    Do NOT remove `InterServerEvents` (events.ts) or the gameEngine.ts:2215 "legacy" comment — RESEARCH.md confirmed both are live/load-bearing. Stay within D-08's "genuinely inert only" scope; do not hunt for additional removals.

  </action>
  <verify>
    <automated>cd packages/server && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Grep for `applyDeclareHeaderTarget` across `packages/server/src` (including `__tests__`) returns zero matches.
    - Grep for `ApplyDeclareHeaderTargetResult` across `packages/server/src` returns zero matches.
    - `computeHeaderDuelWinner` and `applyResolveHeaderTarget` still exist in gameEngine.ts (not collaterally deleted).
    - `makeHeaderState` still exists in gameEngine.phase10.test.ts and its other call sites (234/269) still compile.
    - `ActionLog.tsx` no longer contains the literal "TODO-NAME" placeholder.
    - `headingValidator.ts` no longer contains a TODO referencing "Phase 4".
    - `cd packages/server && npx vitest run` passes (full suite, with the 4 deleted tests gone — no orphaned/failing tests) and `npx tsc --noEmit` is clean.
    - `InterServerEvents` (events.ts) and the gameEngine.ts:2215 comment are unchanged.
  </acceptance_criteria>
  <done>The dead applyDeclareHeaderTarget function, its result type, and its 4 dedicated tests are removed in one commit with the build green; two stale comments are cleaned. No live code removed.</done>
</task>

</tasks>

<verification>
- `cd packages/server && npx vitest run` — full server suite green (4 deleted tests removed cleanly, no dangling import).
- `cd packages/client && npx vitest run` — full client suite green (new tackle-tint tests added).
- `cd packages/shared && npx vitest run` — full shared suite green (headingValidator comment change is a no-op behaviorally).
- `pnpm -r typecheck` (or per-package `npx tsc --noEmit`) clean across all three packages.
</verification>

<success_criteria>
BUG-08 re-verified with a fresh automated render-level tackle-tint regression test (production code changed only if a real defect surfaced). DESIGN-04 dead code removed: `applyDeclareHeaderTarget` + `ApplyDeclareHeaderTargetResult` + their 4 tests gone, build green; 2 stale comments cleaned. Scope held to genuinely inert code per D-08 — no live symbols removed, no open-ended bug hunt.
</success_criteria>

<output>
Create `.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-02-SUMMARY.md` when done
</output>
