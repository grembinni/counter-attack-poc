---
phase: 18.2-code-cleanup-behavioral-dup-bugs
plan: 01
subsystem: api
tags: [socket.io, zustand, game-state, elevation-of-privilege, regression-test]

# Dependency graph
requires:
  - phase: 17.1-action-flow-cleanup
    provides: firstTimePassCarrierId server guard + client mirror pattern (Phase 17.1-16) — the verbatim template this plan mirrors
provides:
  - Server GAME_MOVE carrier-exclusion guard for HIGH_PASS_MOVE (highPassCarrierId)
  - Client selectPiece carrier-exclusion mirror for HIGH_PASS_MOVE (UX defense-in-depth)
  - Regression tests on both tiers closing BUG-11
affects:
  [
    18.2-code-cleanup-behavioral-dup-bugs subsequent plans,
    any future HIGH_PASS_MOVE/FIRST_TIME_PASS_MOVE parity audits,
  ]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Carrier-exclusion guard pattern: pieceId === state.<phase>CarrierId check inserted between ownership guard and slot lock-check, mirrored identically server (GAME_MOVE) and client (selectPiece)'

key-files:
  created:
    - packages/server/src/__tests__/gameHandlers.phase18-02.test.ts
  modified:
    - packages/server/src/gameHandlers.ts
    - packages/client/src/store/useGameStore.ts
    - packages/client/src/store/useGameStore.test.ts

key-decisions:
  - "Exactly 2 touch points modified (server GAME_MOVE + client selectPiece) per RESEARCH.md BUG-11 Root Cause Pitfall 1 — the delivery-occupant-lookup touch point does NOT apply to HIGH_PASS_MOVE (no direct delivery-occupant-pickup code path; routes through applyRoll's HEADER/LOOSE_BALL transition which skips occupant pickup for HIGH_PASS)"
  - 'highPassCarrierId: null clear-at-delivery (gameHandlers.ts, pre-existing) left unchanged — confirmed via diff against a85c2fc baseline'

patterns-established:
  - 'Carrier-exclusion guard placement: immediately after the ownership guard (piece.teamId !== activeTeam), before the slot lock-check (movedPieceId ?? null) — applies to both server handler branches and client selectPiece branches'

requirements-completed: [BUG-11]

# Metrics
duration: 25min
completed: 2026-06-22
---

# Phase 18.2 Plan 01: HIGH_PASS_MOVE Carrier-Exclusion Guard (BUG-11) Summary

**Server GAME_MOVE handler and client selectPiece now reject the original high-pass kicker's own piece during HIGH_PASS_MOVE repositioning, closing an Elevation-of-Privilege gap identical in class to the FTP self-pass-reclaim exploit (Phase 17.1-16).**

## Performance

- **Duration:** 25 min
- **Started:** 2026-06-22T10:05:00Z (approx, worktree provisioning + pnpm install included)
- **Completed:** 2026-06-22T10:22:22Z
- **Tasks:** 2 completed
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments

- Closed BUG-11: the high-pass kicker can no longer select or reposition their own piece during the HIGH_PASS_MOVE repositioning slot
- Server-side authoritative rejection (`WRONG_PIECE`) added to the `GAME_MOVE` socket handler's `HIGH_PASS_MOVE` branch, mirroring the already-shipped FTP `firstTimePassCarrierId` guard verbatim
- Client-side `selectPiece` UX mirror added (defense-in-depth; server remains authoritative)
- Two regression tests per tier confirm the guard rejects the carrier and does NOT over-reject non-carrier pieces

## Task Commits

Each task was committed atomically using TDD RED → GREEN:

1. **Task 1: Server GAME_MOVE carrier-exclusion guard for HIGH_PASS_MOVE**
   - `aa4825a` test(18.2-01): add failing test for HIGH_PASS_MOVE carrier-exclusion guard (RED)
   - `50fc6bb` feat(18.2-01): reject high-pass carrier GAME_MOVE during HIGH_PASS_MOVE (GREEN)
2. **Task 2: Client selectPiece carrier-exclusion mirror for HIGH_PASS_MOVE**
   - `4ec6378` test(18.2-01): add failing test for HIGH_PASS_MOVE selectPiece carrier-exclusion (RED)
   - `510d9fd` feat(18.2-01): reject high-pass carrier selection during HIGH_PASS_MOVE (GREEN)

_Note: both tasks followed strict TDD — each RED commit was confirmed failing (test timeout / assertion mismatch) before the GREEN implementation commit was made._

## Files Created/Modified

- `packages/server/src/__tests__/gameHandlers.phase18-02.test.ts` - New test file: socket-driven GAME_MOVE handler tests asserting WRONG_PIECE rejection of the carrier and a regression pass for non-carrier moves (2 tests)
- `packages/server/src/gameHandlers.ts` - Inline guard block added to the `HIGH_PASS_MOVE` branch of the `GAME_MOVE` handler: `if (pieceId === hpState.highPassCarrierId)` → emit `WRONG_PIECE` → broadcastState → return
- `packages/client/src/store/useGameStore.ts` - Inline guard block added to the `HIGH_PASS_MOVE` branch of `selectPiece`: `if (id === gameState.highPassCarrierId)` → clear selection → return
- `packages/client/src/store/useGameStore.test.ts` - New `describe` block with 2 tests mirroring the existing FTP carrier-exclusion test structure

## Decisions Made

- Followed RESEARCH.md's Pitfall 1 correction exactly: this is a 2-touch-point fix (server GAME_MOVE + client selectPiece), NOT 3. The CONTEXT.md "delivery occupant lookup exclusion" touch point does not apply to HIGH_PASS_MOVE since it has no direct delivery-occupant-pickup code path.
- Used real piece ids (`home-9`, `home-5`, `home-7`) and real adjacent positions from the seeded team-selection state in tests, per the STATE.md warning that placeholder coordinates break moves silently.
- Test harness for Task 1 mirrors `gameHandlers.phase17-06.test.ts` exactly (real Socket.io server on port 0, `setupRoom()` helper completing team selection before seeding `HIGH_PASS_MOVE` state directly via `getRoom`).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Worktree had no installed dependencies or shared-package build artifacts**

- **Found during:** Task 1 (first verification run)
- **Issue:** The worktree had no `node_modules` anywhere (root or per-package) and `packages/shared/dist` did not exist, so `npx vitest run` failed with `ERR_MODULE_NOT_FOUND: vitest` and then `Failed to resolve entry for package "@counter-attack/shared"`.
- **Fix:** Ran `pnpm install --frozen-lockfile` at the workspace root (materializes the existing lockfile — not a new/changed dependency) and `npx tsc` inside `packages/shared` to produce `dist/`. Both are standard worktree-provisioning steps, not package additions, so this falls under Rule 3 (blocking issue), not the package-manager-install exclusion (which applies to adding/changing dependencies, not materializing an existing lockfile).
- **Files modified:** None (node_modules and dist/ are gitignored build artifacts, not source)
- **Verification:** `npx vitest run` and `npx tsc --noEmit` both succeeded afterward across server/client/shared packages
- **Committed in:** N/A (gitignored artifacts, not committed)

---

**Total deviations:** 1 auto-fixed (1 blocking — environment provisioning)
**Impact on plan:** No scope creep; this was pure environment setup required to execute and verify the plan's own tasks. No application code beyond the planned 2 touch points was changed.

## Issues Encountered

None beyond the environment provisioning deviation above.

## User Setup Required

None - no external service configuration required.

## Verification Evidence

- `cd packages/server && npx vitest run` — **467 passed**, 1 skipped, 1 todo (baseline 454 + 2 new from `gameHandlers.phase18-02.test.ts`; pre-existing skip/todo unchanged)
- `cd packages/client && npx vitest run` — **227 passed** (baseline 225 + 2 new from `useGameStore.test.ts`)
- `cd packages/shared && npx vitest run` — 320 passed (untouched by this plan, confirmed green)
- `cd packages/server && npx tsc --noEmit` — clean
- `cd packages/client && npx tsc --noEmit` — clean
- `grep -n "highPassCarrierId" packages/server/src/gameHandlers.ts` confirms the new guard at the GAME_MOVE branch plus all pre-existing read/clear/set sites unchanged
- `grep -n "highPassCarrierId" packages/client/src/store/useGameStore.ts` confirms exactly one match — the new selectPiece guard
- `git diff a85c2fc HEAD -- packages/server/src/gameHandlers.ts` shows exactly one 9-line inline guard block added — no other lines touched
- `git diff a85c2fc HEAD -- packages/client/src/store/useGameStore.ts` shows exactly one 8-line inline guard block added — no other lines touched
- `highPassCarrierId: null` clear-at-delivery confirmed unchanged (present, untouched, in the diff against baseline)

## Next Phase Readiness

- BUG-11 fully closed; HIGH_PASS_MOVE and FIRST_TIME_PASS_MOVE now have structurally identical carrier-exclusion protection on both server and client tiers
- No blockers for subsequent 18.2 plans
- Worktree now has installed dependencies and a built `packages/shared/dist` — subsequent plans in this wave running in the same worktree will not need to re-provision (though a fresh worktree per wave/agent will need this step repeated)

## Self-Check: PASSED

- FOUND: packages/server/src/**tests**/gameHandlers.phase18-02.test.ts
- FOUND: .planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-01-SUMMARY.md
- FOUND commit: aa4825a (test, RED)
- FOUND commit: 50fc6bb (feat, GREEN)
- FOUND commit: 4ec6378 (test, RED)
- FOUND commit: 510d9fd (feat, GREEN)
- FOUND commit: 97f7a1b (docs, SUMMARY)

---

_Phase: 18.2-code-cleanup-behavioral-dup-bugs_
_Completed: 2026-06-22_
