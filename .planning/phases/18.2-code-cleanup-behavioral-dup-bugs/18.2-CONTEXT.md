# Phase 18.2: Code Cleanup & Behavioral Dup-Bugs - Context

**Gathered:** 2026-06-20
**Status:** Ready for planning
**Source:** Derived from `.planning/phases/18-design-polish/18-CONTEXT.md` (original Phase 18 discuss-phase session, 2026-06-20) — split out during `/gsd-plan-phase 18`'s `## PHASE SPLIT RECOMMENDED` response. No new discuss-phase session was run; the decisions below are quoted verbatim from the original session.

<domain>
## Phase Boundary

Phase 18.2 covers the code-cleanup workstream split out of the original "Design Polish" phase, including the three behavioral bug-bash defects that are themselves duplicate-logic gaps (per D-07, these are explicitly in scope here, not just inert dead code):

1. **Code cleanup (DESIGN-03, DESIGN-04)** — duplicate logic consolidation and dead code removal.
2. **BUG-08** — ZoI tackle/steal exclusion re-verification.
3. **BUG-09** — response-move stale highlight clearing (supersedes a folded todo).
4. **BUG-11** — `highPassCarrierId` exclusion fix (mirrors the FIRST_TIME_PASS_MOVE carrier-exclusion pattern).

</domain>

<decisions>
## Implementation Decisions

### DESIGN-03 / DESIGN-04: Duplicate/Dead Code Removal — Risk Tolerance

- **D-07 (confirmed in scope, not "too risky"):** Known _behavioral_ defects that are also duplicate-logic gaps are IN SCOPE for this phase, not just inert dead code. Specifically: BUG-11 (HIGH_PASS_MOVE carrier exclusion — same fix pattern as the already-shipped FIRST_TIME_PASS_MOVE fix) and the previously-pending stale-selection todo (superseded by BUG-09 below — see Folded Todos).
- **D-08:** Stay limited to genuinely inert code for everything else flagged by DESIGN-03/04: unused exports, unreachable branches, stale TODO comments, legacy feature flags. Do not go looking for additional behavioral bugs beyond what's explicitly listed below — that scope is closed as of the original discussion.
- **D-09 [informational]:** The 4 pre-existing RED test failures documented in STATE.md (2 MOVE-06 FREE_MOVE scaffolding gaps, 2 abandoned firstTimePassStep design stubs) remain explicitly out of scope — they were deliberately deferred past Phase 17.1, and nothing in this discussion changes that. This is a scope boundary, not a deliverable — no plan touches these stubs, which is the correct (silent) compliance; 18.2-RESEARCH.md's Validation Architecture section confirms the current baseline already shows 0 failing (1 skip + 1 todo, not 4 RED) and reaffirms these remain untouched.

### Bug-Bash Items In Scope Here (BUG-08, BUG-09, BUG-11)

- **BUG-08:** Once a defender's `stealAttemptedByIds` flag is set for a piece (a failed steal attempt), the attacker must be able to move freely adjacent to that defender — no threat tint, no repeat steal challenge. This is the per-action-type ZoI exclusion already specified in Phase 17.1's D-02 (`packages/shared/src/moveValidator.ts`) and the HexGrid risk-tint filtering fix from 17.1-09 (`.planning/debug/zoi-tackle-steal-exclusion.md`). Report indicates this exclusion is not fully working for the steal case — re-verify both the validator-side exclusion and the client-side `zoiRiskSet` tint filter against `stealAttemptedByIds` (not just `tackleAttemptedByIds`).
- **BUG-09 (supersedes/broadens the folded todo — see Folded Todos):** During ANY response-move phase (`HEADER` contestant repositioning, `SNAPSHOT_DEFLECT`, `FIRST_TIME_PASS_MOVE`, `HIGH_PASS_MOVE`, `GK_KICK_MOVE`, `FREE_KICK_SETUP` stages, etc.), the active piece's move-ring highlight must clear once (a) that piece has used its phase-imposed pace allowance, or (b) End Turn hands control to the opponent. Root cause is the same `slotChanged`-style staleness class documented in the original todo (`useGameStore.ts:511`) — that fix only covered `movementSlot`/`firstTimePassMovementSlot`/`highPassMovementSlot`/`gkKickMovementSlot` comparisons; the broader fix here must also cover pace-exhaustion (not just slot hand-off) as a clear condition.
- **BUG-11:** Apply the FIRST_TIME_PASS_MOVE carrier-exclusion fix pattern (Phase 17.1-16: `firstTimePassCarrierId` set at FTP transition, excluded from `GAME_MOVE` repositioning onto the pass target, preserved across undo, cleared at delivery) identically to `HIGH_PASS_MOVE`'s `highPassCarrierId`, which already exists on `GameState` but is never consumed as an exclusion. Mirror the exact 3 touch points from 17.1-16: server `GAME_MOVE` HP handler (reject `pieceId === highPassCarrierId` before the lock check), delivery occupant lookup (exclude the passer), and the client `selectPiece` HP branch (UX-only mirror).

### Folded Todos

- **`.planning/todos/pending/2026-06-20-fix-stale-client-selection-on-ftp-hp-slot-handoff.md`** ("Fix stale client selection on FTP/HP slot hand-off") — folded into this sub-phase, but **superseded/broadened by BUG-09** above rather than implemented as originally scoped. The original todo only covered `firstTimePassMovementSlot`/`highPassMovementSlot`/`gkKickMovementSlot` slot-hand-off staleness in `useGameStore.ts`'s `slotChanged` check; BUG-09 additionally requires clearing the stale highlight on pace-exhaustion (not just slot hand-off) and covers more response-move phases (`HEADER`, `SNAPSHOT_DEFLECT`, `FREE_KICK_SETUP`). Implementing BUG-09 fully resolves this todo — delete the todo file as part of closing BUG-09, don't implement them as two separate fixes.

</decisions>

<canonical_refs>

## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Requirements

- `.planning/REQUIREMENTS.md` — DESIGN-03, DESIGN-04, BUG-08, BUG-09, BUG-11
- `.planning/ROADMAP.md` §Phase 18.2

### Prior Phase Decisions Still in Force

- `.planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md` — D-02 (per-action-type ZoI exclusion) for BUG-08; D-03 (FIRST_TIME_PASS_MOVE two-slot design, used as BUG-11's fix template)
- `.planning/debug/zoi-tackle-steal-exclusion.md` — root-cause diagnosis for the original ZoI exclusion fix; read before re-diagnosing BUG-08
- `.planning/todos/pending/2026-06-20-fix-stale-client-selection-on-ftp-hp-slot-handoff.md` — original (narrower) version of BUG-09; delete once BUG-09 is fixed

### Pattern Map

- `.planning/phases/18-design-polish/18-PATTERNS.md` — covers the `firstTimePassCarrierId` exclusion's 3 server touch points + 1 client mirror (literal copy-paste template for BUG-11), and the `moveValidator.ts`/`HexGrid.tsx` `zoiRiskSet` tint-filter analysis for BUG-08

### Key Source Files to Read Before Planning

- `packages/client/src/store/useGameStore.ts` — `slotChanged` check (~line 511, BUG-09)
- `packages/client/src/components/HexGrid.tsx` — `zoiRiskSet` tint filtering (BUG-08)
- `packages/server/src/gameEngine.ts` — HIGH_PASS_MOVE handler (BUG-11)
- `packages/shared/src/moveValidator.ts` — ZoI exclusion logic (BUG-08)
- `packages/shared/src/types.ts` — `GameState.highPassCarrierId` (already exists, unused — BUG-11)

### Original Discussion (full context, not just this sub-phase's slice)

- `.planning/phases/18-design-polish/18-CONTEXT.md` §decisions (DESIGN-03/DESIGN-04 and Bug-Bash Addendum sections) — the sections quoted above are extracted verbatim from here

</canonical_refs>

<code_context>

## Existing Code Insights

### Reusable Assets

- `firstTimePassCarrierId` exclusion pattern (Phase 17.1-16) — copy-paste template for BUG-11's `highPassCarrierId` fix
- `isOffside` red-ring layer (PieceOverlay.tsx) and the "green moved-this-stage" ring pattern (D-55, Phase 17) — precedent for adding new independent boolean-driven visual layers without disturbing `selectionState`; relevant if BUG-09's highlight-clearing touches `PieceOverlay.tsx`

</code_context>

<specifics>
## Specific Ideas

None beyond the literal fix patterns cited in the decisions above.

</specifics>

<deferred>
## Deferred Ideas

None — DESIGN-03, DESIGN-04, BUG-08, BUG-09, and BUG-11 are all in scope for this sub-phase.

</deferred>

---

_Phase: 18.2-code-cleanup-behavioral-dup-bugs_
_Context derived: 2026-06-20 (split from Phase 18)_
