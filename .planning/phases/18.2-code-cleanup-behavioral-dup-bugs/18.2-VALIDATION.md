---
phase: 18.2
slug: code-cleanup-behavioral-dup-bugs
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-21
---

# Phase 18.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property               | Value                                                                                                      |
| ---------------------- | ---------------------------------------------------------------------------------------------------------- |
| **Framework**          | Vitest (per-package, pinned in each `package.json`)                                                        |
| **Config file**        | `packages/shared/vitest.config.ts`, `packages/server/vitest.config.ts`, `packages/client/vitest.config.ts` |
| **Quick run command**  | `cd packages/<pkg> && npx vitest run <specific-test-file>`                                                 |
| **Full suite command** | `cd packages/<pkg> && npx vitest run` (or `pnpm test` per package script)                                  |
| **Estimated runtime**  | ~20 seconds per package; ~1 minute for all 3                                                               |

**Confirmed current baseline (2026-06-21):** shared 320 passing / server 454 passing (1 skip, 1 todo) / client 225 passing — 0 failing across all three. This supersedes the stale "4 pre-existing RED test failures" figure from the Phase 17.1 era; D-09's instruction to leave the 1 skip + 1 todo untouched still applies.

---

## Sampling Rate

- **After every task commit:** Run the quick command for the specific test file(s) touched by that task (e.g. `npx vitest run src/store/useGameStore.test.ts` for a BUG-09 task)
- **After every plan wave:** Run the full suite for each affected package
- **Before `/gsd-verify-work`:** All 3 packages' full suites green (320/454/225 baseline + new tests)
- **Max feedback latency:** ~20 seconds

---

## Per-Task Verification Map

| Task ID       | Plan | Wave | Requirement | Threat Ref | Secure Behavior                                                                                                                                                                                                    | Test Type        | Automated Command                                                                                                                                            | File Exists        | Status     |
| ------------- | ---- | ---- | ----------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------ | ---------- |
| 18.2-01-01/02 | 01   | 1    | BUG-11      | V4         | Server `GAME_MOVE` rejects `pieceId === highPassCarrierId` during `HIGH_PASS_MOVE` with `WRONG_PIECE`; client `selectPiece` HP branch mirrors the rejection                                                        | integration+unit | `cd packages/server && npx vitest run src/__tests__/gameHandlers.phase18-02.test.ts` + `cd packages/client && npx vitest run src/store/useGameStore.test.ts` | ❌ W0 (new file)   | ⬜ pending |
| 18.2-02-01    | 02   | 1    | BUG-08      | —          | Tackle-risk tint clears after failed tackle attempt (render level) — verify-first; production change only if test reveals a real defect                                                                            | unit/component   | `cd packages/client && npx vitest run src/components/HexGrid.test.tsx`                                                                                       | ❌ W0              | ⬜ pending |
| 18.2-02-02    | 02   | 1    | DESIGN-04   | —          | `applyDeclareHeaderTarget` removal does not break build; 4 dependent tests removed in same commit                                                                                                                  | unit             | `cd packages/server && npx vitest run src/__tests__/gameEngine.phase10.test.ts`                                                                              | ✅ (edit required) | ⬜ pending |
| 18.2-03-01/02 | 03   | 2    | BUG-09      | —          | Trace-then-scope HEADER/SNAPSHOT_DEFLECT/FREE_KICK_SETUP residual symptom; broaden stale-highlight clear gate for HP/FTP/GK_KICK slot hand-off + pace exhaustion                                                   | unit             | `cd packages/client && npx vitest run src/store/useGameStore.test.ts`                                                                                        | ❌ W0              | ⬜ pending |
| 18.2-04-01/02 | 04   | 3    | DESIGN-03   | V4/V5      | Consolidated response-move helpers (`validateResponseMoveStep`, `computeResponseMoveValidHexes`, `computeMovementValidHexes`) preserve existing behavior for HP/FTP/GK_KICK/SNAPSHOT_DEFLECT (Clusters 1/3/5 only) | unit/integration | `cd packages/server && npx vitest run` + `cd packages/client && npx vitest run` (full suites)                                                                | ✅                 | ⬜ pending |

_Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

_Reconciled against final PLAN.md frontmatter (18.2-01 through 18.2-04) after planning and plan-checker verification passed._

---

## Wave 0 Requirements

- [ ] `packages/server/src/__tests__/` — new BUG-11 server regression test (mirror FTP's existing self-pass-reclaim test)
- [ ] `packages/client/src/store/useGameStore.test.ts` — new BUG-11 client test case (carrier-exclusion during HIGH_PASS_MOVE)
- [ ] `packages/client/src/store/useGameStore.test.ts` — new BUG-09 tests (slot hand-off + pace-exhaustion stale-highlight clears)
- [ ] `packages/client/src/components/HexGrid.test.tsx` — new BUG-08 tackle-risk-tint render-level test (mirror existing 3 steal-risk-tint tests, lines 184-264)
- [ ] `packages/server/src/__tests__/gameEngine.phase10.test.ts` — remove 4 `applyDeclareHeaderTarget` tests (lines 558-583) in the same commit as the production-code removal (DESIGN-04)

---

## Manual-Only Verifications

_None — all phase behaviors (BUG-08/09/11 fixes, DESIGN-03 consolidation, DESIGN-04 dead-code removal) have automated verification per the test map above._

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-06-21 (gsd-plan-checker: VERIFICATION PASSED, no blockers)
