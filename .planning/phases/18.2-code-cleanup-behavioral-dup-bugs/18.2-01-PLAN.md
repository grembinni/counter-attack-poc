---
phase: 18.2-code-cleanup-behavioral-dup-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/gameHandlers.phase18-02.test.ts
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/store/useGameStore.test.ts
autonomous: true
requirements: [BUG-11]
must_haves:
  truths:
    - 'The original high-pass kicker cannot reposition their own piece during HIGH_PASS_MOVE — the server rejects GAME_MOVE with WRONG_PIECE'
    - 'The client selectPiece HIGH_PASS_MOVE branch clears selection when the clicked piece is the high-pass carrier (UX mirror, defense-in-depth)'
    - 'highPassCarrierId continues to be cleared at delivery (gameHandlers.ts:695, pre-existing — unchanged)'
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'HIGH_PASS_MOVE GAME_MOVE carrier-exclusion guard'
      contains: 'highPassCarrierId'
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'HIGH_PASS_MOVE selectPiece carrier-exclusion mirror'
      contains: 'highPassCarrierId'
    - path: 'packages/server/src/__tests__/gameHandlers.phase18-02.test.ts'
      provides: 'Server regression test asserting WRONG_PIECE rejection of the carrier'
  key_links:
    - from: 'packages/server/src/gameHandlers.ts HIGH_PASS_MOVE branch'
      to: 'GameState.highPassCarrierId'
      via: 'pieceId === hpState.highPassCarrierId equality check before lock-check'
      pattern: 'pieceId === hpState.highPassCarrierId'
    - from: 'packages/client/src/store/useGameStore.ts HIGH_PASS_MOVE selectPiece branch'
      to: 'GameState.highPassCarrierId'
      via: 'id === gameState.highPassCarrierId equality check before lock-check'
      pattern: 'id === gameState.highPassCarrierId'
---

<artifacts_produced>

## Artifacts this phase produces (symbols created by THIS plan)

No new exported symbols, types, files-as-modules, or fields are created. This plan only:

- Adds an inline guard block (no new identifier) inside the existing `HIGH_PASS_MOVE` branch of the `GAME_MOVE` socket handler in `packages/server/src/gameHandlers.ts`.
- Adds an inline guard block (no new identifier) inside the existing `HIGH_PASS_MOVE` branch of `selectPiece` in `packages/client/src/store/useGameStore.ts`.
- Adds one new test file `packages/server/src/__tests__/gameHandlers.phase18-02.test.ts` and new test cases in the existing `packages/client/src/store/useGameStore.test.ts`.

Existing symbols consulted (NOT created here): `GameState.highPassCarrierId` (already on the type, set at gameHandlers.ts:1366, cleared at gameHandlers.ts:695), `ServerEvents.GAME_ERROR`, `'WRONG_PIECE'`, `broadcastState`.
</artifacts_produced>

<objective>
Close BUG-11: the parity gap where `FIRST_TIME_PASS_MOVE` excludes its passer (`firstTimePassCarrierId`) from repositioning but the structurally-identical `HIGH_PASS_MOVE` never consumes its already-existing `highPassCarrierId` field as an exclusion. The original high-pass kicker can currently select and reposition their own piece during the `HIGH_PASS_MOVE` repositioning slot — an Elevation-of-Privilege defect identical in class to the FTP self-pass-reclaim exploit closed in Phase 17.1-16.

Purpose: Apply the already-shipped, already-correct FTP carrier-exclusion pattern (D-07 confirms this is in scope, not "too risky") identically to HIGH_PASS_MOVE.

Output: Server-side authoritative GAME_MOVE rejection + client-side selectPiece UX mirror + regression tests on both tiers.

Scope correction (RESEARCH.md BUG-11 Root Cause, Pitfall 1): This is a **2-touch-point** fix, NOT 3. Touch point 1 = server GAME_MOVE rejection. Touch point 2 = client selectPiece mirror. The CONTEXT.md "delivery occupant lookup exclusion" touch point does NOT apply — HIGH_PASS_MOVE has no direct delivery-occupant-pickup code path (it routes through `applyRoll`'s HEADER/LOOSE_BALL transition, `gameEngine.ts:1579`/`1614-1651`, which skips occupant pickup for HIGH_PASS). The `highPassCarrierId: null` clear-at-delivery (gameHandlers.ts:695) is already implemented — do NOT add a third touch point.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-CONTEXT.md
@.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-RESEARCH.md
@.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server GAME_MOVE carrier-exclusion guard for HIGH_PASS_MOVE (BUG-11 touch point 1)</name>
  <files>packages/server/src/gameHandlers.ts, packages/server/src/__tests__/gameHandlers.phase18-02.test.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts lines 281-343 (the HIGH_PASS_MOVE GAME_MOVE branch — current full branch; insertion point is between line 293 (the `piece.teamId !== hpState.activeTeam` ownership guard close) and line 295 (the `const lockedId = hpState.highPassMovedPieceId` lock-check))
    - packages/server/src/gameHandlers.ts lines 505-514 (the FIRST_TIME_PASS_MOVE carrier-exclusion guard — the verbatim template to mirror)
    - packages/server/src/gameHandlers.ts lines 716-790 (FTP GAME_END_TURN branch — to find the existing FTP self-pass-reclaim regression test pattern referenced from STATE.md Phase 17.1-16; locate the matching test file to mirror its structure)
    - packages/server/src/__tests__/gameHandlers.phase17-06.test.ts (existing handler integration test in this directory — use as the structural template for socket-driven GAME_MOVE handler tests: room setup, isProcessing, broadcastState assertions)
  </read_first>
  <behavior>
    - Test 1 (RED first): Given a room in phase HIGH_PASS_MOVE with `highPassCarrierId` set to the kicker's piece id, when the active player emits GAME_MOVE for `pieceId === highPassCarrierId`, the server emits GAME_ERROR 'WRONG_PIECE' and does NOT mutate the carrier's position.
    - Test 2: Given the same HIGH_PASS_MOVE state, when the active player emits GAME_MOVE for a DIFFERENT (non-carrier) own-team piece to a legal adjacent hex, the move succeeds (position updates, highPassMovedPieceId set) — regression guard proving the new guard does not over-reject.
  </behavior>
  <action>
    Insert a carrier-exclusion guard into the `HIGH_PASS_MOVE` branch of the `GAME_MOVE` socket handler in `packages/server/src/gameHandlers.ts`, placed immediately after the ownership guard (`if (!piece || piece.teamId !== hpState.activeTeam)` block ending ~line 293) and before the `const lockedId = hpState.highPassMovedPieceId ?? null;` lock-check (~line 295). The guard checks `if (pieceId === hpState.highPassCarrierId)` and, when true, calls `socket.emit(ServerEvents.GAME_ERROR, 'WRONG_PIECE')`, then `broadcastState(io, room)`, then `return`. Use the exact same three-statement body shape as the FTP template at lines 505-514 (emit WRONG_PIECE → broadcastState → return), substituting `hpState.highPassCarrierId` for `ftpState.firstTimePassCarrierId`. Add a short comment referencing BUG-11 / Phase 18.2 and noting this mirrors the FTP guard and that the client selectPiece mirror is defense-in-depth.

    Create the new test file `packages/server/src/__tests__/gameHandlers.phase18-02.test.ts`. Seed a HIGH_PASS_MOVE GameState with `highPassCarrierId` set, `highPassMovedPieceId: null`, `highPassPaceUsed: 0`, `highPassMovementSlot: 'ATTACKER'`, and real adjacent positions (use HOME_SQUAD/AWAY_SQUAD positions per the STATE.md note that placeholder coords break moves silently). Assert WRONG_PIECE on a carrier GAME_MOVE and a successful move on a non-carrier piece. Do NOT widen scope to other phases.

  </action>
  <verify>
    <automated>cd packages/server && npx vitest run src/__tests__/gameHandlers.phase18-02.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/gameHandlers.ts` contains a block matching pattern `pieceId === hpState.highPassCarrierId` inside the `HIGH_PASS_MOVE` GAME_MOVE branch, positioned before the `highPassMovedPieceId` lock-check.
    - That block emits `ServerEvents.GAME_ERROR` with `'WRONG_PIECE'`, calls `broadcastState(io, room)`, and `return`s.
    - `gameHandlers.phase18-02.test.ts` has a test asserting GAME_ERROR 'WRONG_PIECE' when `pieceId === highPassCarrierId` and the carrier piece position is unchanged after the rejected emit.
    - `gameHandlers.phase18-02.test.ts` has a regression test asserting a non-carrier own-team piece moves successfully (position updates) in HIGH_PASS_MOVE.
    - `cd packages/server && npx vitest run src/__tests__/gameHandlers.phase18-02.test.ts` passes.
    - No third (delivery-occupant) touch point was added; gameHandlers.ts:695 `highPassCarrierId: null` clear remains unchanged.
  </acceptance_criteria>
  <done>Server authoritatively rejects the high-pass carrier's own GAME_MOVE during HIGH_PASS_MOVE with WRONG_PIECE; both regression tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client selectPiece carrier-exclusion mirror for HIGH_PASS_MOVE (BUG-11 touch point 2)</name>
  <files>packages/client/src/store/useGameStore.ts, packages/client/src/store/useGameStore.test.ts</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts lines 392-424 (the HIGH_PASS_MOVE selectPiece branch — current full branch; insertion point is after the `if (piece.teamId !== myTeam)` ownership check ending ~line 399 and before `const lockedId = gameState.highPassMovedPieceId ?? null;` ~line 401)
    - packages/client/src/store/useGameStore.ts lines 474-481 (the FIRST_TIME_PASS_MOVE carrier-exclusion mirror — the verbatim template)
    - packages/client/src/store/useGameStore.test.ts (existing store tests — find the FTP carrier-exclusion test if present, or the HIGH_PASS_MOVE selectPiece tests, to mirror structure: store hydration via setPlayerSlot + setGameState, then selectPiece assertions)
  </read_first>
  <behavior>
    - Test 1 (RED first): Given the store hydrated with a HIGH_PASS_MOVE gameState where `highPassCarrierId` is the kicker's id, when `selectPiece(highPassCarrierId)` is called by the owning player, `selectedPieceId` becomes null and `validMoveHexes` is empty (selection rejected).
    - Test 2: Given the same state, when `selectPiece(<a different own-team piece id>)` is called, `selectedPieceId` is set to that piece and `validMoveHexes` is non-empty (regression guard — non-carrier still selectable).
  </behavior>
  <action>
    Insert a carrier-exclusion mirror into the `HIGH_PASS_MOVE` branch of `selectPiece` in `packages/client/src/store/useGameStore.ts`, placed immediately after the `if (piece.teamId !== myTeam)` ownership check (ending ~line 399) and before `const lockedId = gameState.highPassMovedPieceId ?? null;` (~line 401). The guard checks `if (id === gameState.highPassCarrierId)` and, when true, calls `set({ selectedPieceId: null, validMoveHexes: [] })` then `return`. Use the exact same body shape as the FTP mirror at lines 474-481, substituting `gameState.highPassCarrierId` for `gameState.firstTimePassCarrierId`. Add a short comment marking it BUG-11 / Phase 18.2 and noting it is defense-in-depth UX only (server is authoritative).

    IMPORTANT (PATTERNS.md): edit the `HIGH_PASS_MOVE` block at lines 392-424 specifically — do NOT confuse it with the structurally similar `GK_KICK_MOVE` branch at lines 509-537. They are separate `if` blocks.

    Add test cases to `packages/client/src/store/useGameStore.test.ts` per the behavior block. Hydrate the store with `setPlayerSlot` for the carrier's owning team and a HIGH_PASS_MOVE gameState (set `highPassCarrierId`, `highPassMovedPieceId: null`, `highPassPaceUsed: 0`).

  </action>
  <verify>
    <automated>cd packages/client && npx vitest run src/store/useGameStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/client/src/store/useGameStore.ts` contains a block matching pattern `id === gameState.highPassCarrierId` inside the `HIGH_PASS_MOVE` selectPiece branch (lines 392-424 region), positioned before the `highPassMovedPieceId` lock-check.
    - That block calls `set({ selectedPieceId: null, validMoveHexes: [] })` and `return`s.
    - The `GK_KICK_MOVE` branch (lines 509-537 region) is NOT modified.
    - `useGameStore.test.ts` has a test asserting `selectPiece(highPassCarrierId)` yields `selectedPieceId === null` and empty `validMoveHexes`.
    - `useGameStore.test.ts` has a regression test asserting a non-carrier own-team piece is still selectable in HIGH_PASS_MOVE.
    - `cd packages/client && npx vitest run src/store/useGameStore.test.ts` passes.
  </acceptance_criteria>
  <done>Client selectPiece refuses to select the high-pass carrier during HIGH_PASS_MOVE; both store tests pass.</done>
</task>

</tasks>

<verification>
- `cd packages/server && npx vitest run` — full server suite green (baseline 454 passing + new test file).
- `cd packages/client && npx vitest run` — full client suite green (baseline 225 passing + new cases).
- `pnpm -r typecheck` (or per-package `npx tsc --noEmit`) clean.
- Grep confirms `highPassCarrierId` is now read inside both the server `HIGH_PASS_MOVE` GAME_MOVE branch and the client `HIGH_PASS_MOVE` selectPiece branch.
</verification>

<success_criteria>
BUG-11 closed: the high-pass kicker cannot reposition their own piece during HIGH_PASS_MOVE (server rejects with WRONG_PIECE; client mirror prevents selection), covered by regression tests on both tiers. Exactly 2 touch points modified; no delivery-occupant touch point added; clear-at-delivery unchanged.
</success_criteria>

<output>
Create `.planning/phases/18.2-code-cleanup-behavioral-dup-bugs/18.2-01-SUMMARY.md` when done
</output>
