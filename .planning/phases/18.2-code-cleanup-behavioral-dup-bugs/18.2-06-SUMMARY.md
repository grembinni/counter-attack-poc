---
phase: 18.2-code-cleanup-behavioral-dup-bugs
plan: 06
subsystem: testing
tags: [vitest, zustand, regression-test, test-fixture]

# Dependency graph
requires:
  - phase: 18.2-code-cleanup-behavioral-dup-bugs (plan 05)
    provides: 'BUG-09 production fix (SNAPSHOT_DEFLECT sticky branch, useGameStore.ts:758-777) and the original (hollow) Test 7 regression guard'
provides:
  - "Discriminating Test 7 regression guard for BUG-09's SNAPSHOT_DEFLECT sticky-recompute fix"
  - 'Empirical RED/GREEN proof that Test 7 now catches a revert of useGameStore.ts:758-777'
affects: [18.2-VERIFICATION.md, BUG-09 requirement closure]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - packages/client/src/store/useGameStore.test.ts

key-decisions:
  - 'Hardcoded movementSlot: null directly in the snapshotDeflectState() object literal (not added as an overrides parameter) since SNAPSHOT_DEFLECT always has movementSlot null in the real game — no caller should ever need to override it.'

patterns-established: []

requirements-completed: [BUG-09]

# Metrics
duration: ~15min
completed: 2026-06-22
---

# Phase 18.2 Plan 06: SNAPSHOT_DEFLECT Test 7 fixture discrimination fix Summary

**Fixed the hollow `snapshotDeflectState()` test fixture by adding `movementSlot: null`, making Test 7 a true regression guard for the BUG-09 SNAPSHOT_DEFLECT sticky-recompute fix, and empirically proved its discriminating power by reverting and restoring the production branch.**

## Performance

- **Duration:** ~15 min
- **Tasks:** 2 completed
- **Files modified:** 1 (`packages/client/src/store/useGameStore.test.ts`)

## Accomplishments

- `snapshotDeflectState()` (lines 385-394 of `useGameStore.test.ts`) now sets `movementSlot: null`, matching the real game's SNAPSHOT_DEFLECT state (gameEngine.ts nulls `movementSlot` before that phase is ever reached).
- Test 7 ("same-phase SNAPSHOT_DEFLECT broadcast with pace NOT exhausted... retains selection and recomputes a non-empty validMoveHexes") is now empirically proven to discriminate the BUG-09 fix from its absence.
- Test 6 (pace-exhaustion clear test, same fixture) confirmed to remain green — its assertion is gated by `snapDeflectPaceUsed >= 2` via the earlier `responseMovePaceExhausted` clear-condition, not by `movementSlot`.
- No production code ships changed: the temporary revert of `useGameStore.ts:758-777` used to prove discrimination was fully undone via `git checkout --`, confirmed by a clean `git status --porcelain`.

## Task Commits

1. **Task 1: Make snapshotDeflectState() set movementSlot: null and confirm Test 6 + Test 7 still pass** - `498adba` (fix)
2. **Task 2: Prove Test 7 discriminates — temporarily revert the SNAPSHOT_DEFLECT branch, confirm RED, restore, confirm GREEN and clean tree** - no commit (empirical proof step only; produced zero file changes since the temporary revert was undone before any commit)

_Note: Task 2 is a verification/proof step per the plan's design — it intentionally produces no diff. The proof is recorded below._

## Files Created/Modified

- `packages/client/src/store/useGameStore.test.ts` - Added `movementSlot: null` to the `snapshotDeflectState()` factory's returned object literal (one field, no signature change to the `overrides` parameter).

## Empirical Discrimination Proof (Task 2)

1. **Baseline GREEN** (with Task 1's fixture in place): `npx vitest run src/store/useGameStore.test.ts` — all 49 tests pass, including Test 6 and Test 7.
2. **Simulated absence of the fix:** in `packages/client/src/store/useGameStore.ts`, the `if (newState.phase === 'SNAPSHOT_DEFLECT')` condition at line 758 was temporarily changed to `if (false && newState.phase === 'SNAPSHOT_DEFLECT')`, neutralizing the branch so SNAPSHOT_DEFLECT falls through to the generic `computeMovementValidHexes` path (lines 810-811).
3. **RED confirmed:** re-running the suite produced exactly one failure:
   ```
   FAIL  Test 7 (regression guard): same-phase SNAPSHOT_DEFLECT broadcast with pace NOT
         exhausted (snapDeflectPaceUsed 1 of 2) retains selection and recomputes a
         non-empty validMoveHexes
   AssertionError: expected 0 to be greater than 0
     at src/store/useGameStore.test.ts:510:41
     expect(state.validMoveHexes.length).toBeGreaterThan(0);
   ```
   `validMoveHexes` was `[]` (length 0) under the reverted branch, exactly the WRONG_SLOT failure mode the BUG-09 fix addresses. Test 6 and all 47 other tests remained green — confirming the revert's blast radius was isolated to Test 7 as designed.
4. **Restore:** `git checkout -- packages/client/src/store/useGameStore.ts` reverted the temporary edit exactly.
5. **GREEN confirmed:** re-running the suite showed all 49 tests passing again, including Test 7.
6. **Clean tree confirmed:** `git status --porcelain` reported no output — `useGameStore.ts` is byte-for-byte unmodified; only the Task-1 fixture change to `useGameStore.test.ts` is committed.

## Decisions Made

- Hardcoded `movementSlot: null` directly in the `snapshotDeflectState()` literal rather than adding it as a parameterized `overrides` field, per the plan's explicit instruction — SNAPSHOT_DEFLECT always has `movementSlot` null in the real game, so no caller needs to vary it.

## Deviations from Plan

None - plan executed exactly as written. The worktree required `pnpm install` (root) and `pnpm --filter @counter-attack/shared build` to bootstrap `node_modules` and the shared package's `dist/` output before vitest could resolve `@counter-attack/shared` — consistent with the bootstrap note in plan 18.2-05's SUMMARY. This was tooling setup, not a plan deviation.

## Issues Encountered

None beyond the expected worktree bootstrap (pnpm install + shared package build), which is standard for a fresh worktree and was anticipated by the prior plan's SUMMARY.

## Verification Results

- `cd packages/client && npx vitest run` — **236/236 tests passing** (matches the expected unchanged baseline from 18.2-05; no new test added).
- `cd packages/client && npx tsc --noEmit` — exits 0, no output.
- `grep -n "movementSlot:\s*null" packages/client/src/store/useGameStore.test.ts` — matches at line 391, inside `snapshotDeflectState()`.
- `git status --porcelain` — clean; only the Task-1 commit (`498adba`) touches `useGameStore.test.ts`. `useGameStore.ts` is unmodified.
- Discrimination proof recorded above: Test 7 FAILS (`validMoveHexes` is `[]`) when `useGameStore.ts:758-777` is reverted, PASSES when restored.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

BUG-09's "covered by regression tests" half is now satisfied: Test 7 is a true, empirically-proven regression guard for the SNAPSHOT_DEFLECT sticky-recompute fix. This closes the single remaining verification gap recorded in `18.2-VERIFICATION.md` (status: gaps_found, score 4/5). No further gap-closure plans are anticipated for BUG-09. Orchestrator should re-run phase verification to confirm Phase 18.2 can close.

---

_Plan: 18.2-06_
_Completed: 2026-06-22_
