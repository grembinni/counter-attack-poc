---
phase: 18-design-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/ActionPanel.test.tsx
autonomous: true
requirements: [DESIGN-01]
must_haves:
  truths:
    - 'D-13: Every non-active-player wait state (the generic waitingPanel plus the two HEADER-phase special cases) renders the same two-line text: "Opponent'"'"'s Turn" / "Waiting for opponent..."'
    - 'D-13: HIGH_PASS_MOVE helper text reads "High Pass Aerial Challenge!" / "Move 1 player to challenge (max 3 hexes)." (fixes the misleading "Header!" label and the Arial/Aerial typo found during review)'
    - 'D-13: The GK_RESTART "Kick (High Pass)" button is renamed to "Punt (High Pass)" so the term matches GK_KICK_TARGET'"'"'s "Punt!" header; the underlying emitGKRestart(''kick'') call and restart-type value are unchanged — this is a display-text-only rename'
    - 'D-13: The MOVE phase shows a static "Move!" as line 1 and "Move up to {N} players." (with "(2 hex max)" appended only for the ATTACKER_2 slot) as line 2 — ATTACKER_4/DEFENDER_5 show no hex-cap text since they use each piece'"'"'s own pace stat'
    - 'D-13: State 12d (pass target hex clicked, auto-rolling) is explicitly UNCHANGED — still renders null, no new "Auto-rolling" text added'
  artifacts:
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'Convention-corrected phase-prompt text, unified wait-state text, and the Kick->Punt button rename'
      contains: "Opponent's Turn"
  key_links:
    - from: 'packages/client/src/components/ActionPanel.tsx'
      to: 'waitingPanel'
      via: 'shared non-active-player wait state'
      pattern: "Opponent's Turn"
---

<objective>
Apply the D-13 ActionPanel.tsx text corrections (DESIGN-01 scope extension, locked via direct user review of the full trigger/helper-text/actions inventory):
1. Unify every non-active-player wait state to one shared two-line text ("Opponent's Turn" / "Waiting for opponent..."), replacing the generic single-line waitingPanel and the two HEADER-phase special cases (5b, 5f) that currently diverge from it.
2. Rewrite every active-player phase prompt's helper text per the D-13 locked table (14 states), including the HIGH_PASS_MOVE "Header!"->"High Pass Aerial Challenge!" fix and the GK_RESTART "Kick (High Pass)"->"Punt (High Pass)" button rename.
3. Fix the MOVE phase's two-line structure (static "Move!" headline + player-count/hex-cap detail line).

Explicitly OUT of scope: state 12d (auto-roll transition) stays exactly as today (renders null) — no new text or behavior. No change to action-availability logic (which buttons appear when) — only text changes and the one button-label rename.

Purpose: DESIGN-01 messaging consistency — ActionPanel.tsx prompts are the second most visible status text after the scoreboard (which 18-01 already covers) and were left out of the original 18-01/18-02 scope by mistake.
Output: ActionPanel.tsx with every locked D-13 string applied; new ActionPanel.test.tsx covering the unified wait state, the HIGH_PASS_MOVE fix, and the Punt rename.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-design-polish/18-CONTEXT.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates or modifies (exclude from drift verification):

- `waitingPanel` JSX const (ActionPanel.tsx) — rewritten to a two-line helperBlock ("Opponent's Turn" / "Waiting for opponent...")
- Rewritten helper text strings throughout `ActionPanel`'s phase-gated render branches (HIGH_PASS_MOVE, FIRST_TIME_PASS_MOVE, GK_DIVE, SNAPSHOT_DEFLECT, HEADER's 5a/5c/5e branches, SNAPSHOT_TARGET, GK_RESTART, GK_QUICK_THROW, GK_KICK_TARGET, GK_KICK_MOVE, FREE_MOVE_ATTACK/DEFENSE, PASS's loose-ball branch, MOVE) — no new props, no new components, same JSX structure
- GK_RESTART's "Kick (High Pass)" button label text changed to "Punt (High Pass)" (the `onClick={() => emitGKRestart('kick')}` handler and the `'kick'` string value are UNCHANGED)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Unify all non-active-player wait states to one shared two-line text</name>
  <files>packages/client/src/components/ActionPanel.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (lines 70-76 = waitingPanel const, single helperLine2-only text; lines 236-263 = HEADER phase's headerAccuracyRollPending branch — the 5b sub-branch at lines 255-262 has its own custom "Accurate High Pass!" / "Waiting for Opponent." JSX, diverging from waitingPanel; lines 284-309 = HEADER's bothConfirmed-false branch where myConfirmed-true renders a bare helperLine2-only "Waiting for Opponent." at lines 302-306, also diverging from waitingPanel)
    - .planning/phases/18-design-polish/18-CONTEXT.md (D-13's locked table — the row "ALL non-active-player wait states (1w, 2w, 3w, 4w, 5b, 5d, 5f, 6w, 7w, 8w, 9w, 10w, 11w)" and the confirmed decision "(a) wait-state unification is intentional — 5b's 'Accurate High Pass!' context is deliberately dropped in favor of full consistency")
  </read_first>
  <action>
    Rewrite the `waitingPanel` const (currently lines 70-76: a single `<span className={styles.helperLine2}>Waiting for Opponent.</span>` inside `helperBlock`) to a two-line `helperBlock`: `<span className={styles.helperLine1}>Opponent's Turn</span>` followed by `<span className={styles.helperLine2}>Waiting for opponent...</span>`. Every existing `if (!isActivePlayer) return waitingPanel;` call site (used by 1w/2w/3w/4w/6w/7w/8w/9w/10w/11w — these are early-return guards, no changes needed at the call sites themselves) now automatically renders the new two-line text.

    Replace the HEADER phase's 5b branch (lines 255-262, the custom block returning "Accurate High Pass!" / "Waiting for Opponent." JSX) with a direct `return waitingPanel;` — delete the custom inline JSX entirely; it is now identical in content to the shared wait state, so reuse it instead of duplicating the markup.

    In the HEADER phase's bothConfirmed-false branch (around line 284-309), change the `myConfirmed && (...)` block (lines 302-306, currently a bare `helperLine2`-only "Waiting for Opponent.") to render the same two-line shape as `waitingPanel`: `helperLine1` = `Opponent's Turn`, `helperLine2` = `Waiting for opponent...`. (This branch cannot simply `return waitingPanel` because it shares a parent `<div className={styles.panel}>` with the `!myConfirmed` sibling branch and the `gameError` display — keep it as inline JSX matching `waitingPanel`'s two-line content exactly.)

  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `waitingPanel` renders both `Opponent's Turn` (helperLine1) and `Waiting for opponent...` (helperLine2) — source assertion: the const's JSX contains both literal strings.
    - The HEADER phase's headerAccuracyRollPending non-attacker branch (5b) no longer contains the literal string `Accurate High Pass!` anywhere near a "waiting" context — source assertion: `grep -n "Accurate High Pass" packages/client/src/components/ActionPanel.tsx` shows it ONLY in the 5a (active attacker, "Click to continue.") branch, not in any waiting branch.
    - The HEADER phase's myConfirmed-true branch (formerly bare "Waiting for Opponent.") now renders `Opponent's Turn` and `Waiting for opponent...` (source assertion).
    - Typecheck passes.
  </acceptance_criteria>
  <done>Every non-active-player wait state in ActionPanel.tsx — the shared waitingPanel plus the two HEADER-phase special cases — renders identical "Opponent's Turn" / "Waiting for opponent..." text; no duplicate/diverging wait-state markup remains.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite all active-player phase-prompt text per the D-13 locked table, including the Kick->Punt rename</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/ActionPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (lines 134-630 = every phase-gated render branch; exact target text is in the action block below, current text was inventoried directly from this file)
    - .planning/phases/18-design-polish/18-CONTEXT.md (D-13's full locked table — this is the authoritative source for every string below; the table also documents the moveValidator.ts:87-89 ATTACKER_2-only hex-cap finding referenced in the MOVE phase instruction)
    - packages/shared/src/moveValidator.ts (lines 77-89 = slot player-counts and the ATTACKER_2-only `Math.min(piece.pace, 2)` cap — confirms ATTACKER_4/DEFENDER_5 have no fixed hex number, only ATTACKER_2 does)
  </read_first>
  <action>
    Apply these exact text changes (current value -> target value), one render branch at a time. Do NOT change any action-availability logic, button `onClick` handlers, or conditional gating — only the literal text content and the one button label below.

    - HIGH_PASS_MOVE (~line 142): helperLine1 `'Header!'` -> `'High Pass Aerial Challenge!'`; helperLine2 `'Move 1 player to challenge.'` -> `'Move 1 player to challenge (max 3 hexes).'`
    - FIRST_TIME_PASS_MOVE (~line 170): helperLine1 `'First-time pass!'` -> `'First-Time Pass!'`; helperLine2 `'Move 1 player to receive the ball.'` -> `'Move 1 player to receive the ball (max 1 hex).'`
    - GK_DIVE (~line 198): helperLine1 `'Attempt save!'` -> `'Attempt Save!'`; helperLine2 unchanged in substance (`'Dive to a highlighted hex. (Max 3 hexes away)'` -> `'Dive to a highlighted hex (max 3 hexes).'`, punctuation/casing normalized only).
    - SNAPSHOT_DEFLECT (~line 219-221, currently a single `phaseLabel` span, NOT a two-line helperBlock): convert to a two-line `helperBlock` matching the other phases' shape: helperLine1 `'Snapshot - Deflection Attempt!'`, helperLine2 `'Move 1 player to deflect the shot (up to 2 hexes).'`.
    - HEADER 5a branch (~line 245): helperLine1 `'Accurate High Pass!'` stays unchanged (already correct); helperLine2 `'Click to continue.'` stays unchanged.
    - HEADER 5c branch (~line 274): helperLine1 `'Header won!'` -> `'Header Won!'`; helperLine2 `'Choose target.'` -> `'Select a target hex.'`
    - HEADER 5e branch (~line 289): helperLine1 `'Contesting Header!'` -> `'Contest Header!'`; helperLine2 `'({n}) selected within range.'` -> `'{n} players selected within range.'` (drop the parentheses, add "players").
    - SNAPSHOT_TARGET (~line 321, currently a single `phaseLabel` span `'Snapshot! Click a goal hex to target'`): split into two-line `helperBlock`: helperLine1 `'Snapshot!'`, helperLine2 `'Select a goal hex to target.'`
    - GK_RESTART (~line 341): helperLine1 `'Choose Keeper Action'` -> `'Goalie Restart!'`. Add a new helperLine2 `'Choose an action.'` (currently no helperLine2 exists here). Rename the first button's text from `'Kick (High Pass)'` to `'Punt (High Pass)'` (line ~344) — the `onClick={() => emitGKRestart('kick')}` handler is UNCHANGED, only the button's child text changes.
    - GK_QUICK_THROW (~line 367, currently `gkLabel`/`phaseLabel` spans, not a helperBlock): convert to two-line `helperBlock`: helperLine1 `'Quick Throw!'`, helperLine2 `'Select a target hex (up to 11 hexes).'`
    - GK_KICK_TARGET (~line 385): helperLine1 `'Keeper Kick'` -> `'Punt!'`; helperLine2 `"Target anywhere but the opponent's final 3rd"` -> `"Target anywhere except the opponent's final third."`
    - GK_KICK_MOVE (~line 402-404, currently a single `phaseLabel` span): convert to two-line `helperBlock`: helperLine1 `'Ball in Air!'`, helperLine2 `'Move 1 player to receive the ball (max 3 hexes).'`
    - FREE_MOVE_ATTACK / FREE_MOVE_DEFENSE (~line 427): helperLine1 `'Free Move!'` stays unchanged; helperLine2 stays the same shape (`'{sideLabel} — move up to 6 hexes per player in the opponent's third.'`) — no text change needed here, already matches the locked target.
    - PASS loose-ball branch (~line 453): helperLine1 `'Loose Ball!'` stays unchanged; helperLine2 `'Move to collect.'` stays unchanged — already matches the locked target.
    - PASS/KICK_OFF action chooser (~line 504, currently a single `phaseLabel` span `'Choose action.'`): change text to `'Choose Action'` (capitalization change, drop trailing period). Keep it a single-line `phaseLabel` span — the D-13 table specifies no line 2 for this state.
    - PASS/KICK_OFF target-hex prompt (~line 564): unchanged — `{PASS_TYPE_LABELS[selectedPassType]} — click a target hex` already matches the locked target's `'{Pass Type Label}'` / `'Select a target hex.'` shape closely enough; leave as-is (single phaseLabel line is acceptable here, D-13 does not call for a structural split for this state).
    - PASS/KICK_OFF target-hex-clicked (~line 576, `return null`): DO NOT CHANGE. D-13 explicitly confirms no new "Auto-rolling" text — this stays `return null`.
    - MOVE phase (~line 599-600): currently `slotHelperLine1 = slotTotal != null ? \`Move up to ${slotTotal} players\` : null` rendered AS helperLine1, and a separate `slotHelperLine2` for the ATTACKER_2 hex cap. Restructure so helperLine1 is now the STATIC string `'Move!'`, and helperLine2 becomes the dynamic text: `` `Move up to ${slotTotal} players.` `` for ATTACKER_4/DEFENDER_5, or `` `Move up to ${slotTotal} players. (2 hex max)` `` when `movementSlot === 'ATTACKER_2'`. Do not add a third line — concatenate the hex-cap note onto helperLine2 for the ATTACKER_2 case only.

    Extend ActionPanel.test.tsx (create if it does not exist — check first; if it exists, add to it without removing existing tests). Add test cases asserting: (1) a non-active player in MOVE phase renders `/Opponent's Turn/` and `/Waiting for opponent/`; (2) HIGH_PASS_MOVE active player renders `/High Pass Aerial Challenge!/` and does NOT render `/^Header!$/` as a standalone heading; (3) GK_RESTART renders a button with text `/Punt \(High Pass\)/` and does NOT render `/Kick \(High Pass\)/`; (4) MOVE phase with movementSlot `'ATTACKER_2'` renders both `/Move!/` and `/2 hex max/`; (5) MOVE phase with movementSlot `'ATTACKER_4'` renders `/Move!/` and does NOT render `/hex max/`. Model the mock/setState pattern on GameBoard.test.tsx or ActionLog.test.tsx (whichever exists at execution time) — set `playerSlot`, `gameState.phase`, `gameState.activeTeam`, and any phase-specific fields (`movementSlot`, `headerConfirmed`, etc.) needed to reach each branch.

  </action>
  <verify>
    <automated>cd packages/client && npx vitest run src/components/ActionPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - GK_RESTART renders a button with the exact text `Punt (High Pass)` and no button anywhere in the file reads `Kick (High Pass)` — source assertion: `grep -c "Kick (High Pass)" packages/client/src/components/ActionPanel.tsx` returns 0; `grep -c "Punt (High Pass)" packages/client/src/components/ActionPanel.tsx` returns 1.
    - HIGH_PASS_MOVE's helper text contains `High Pass Aerial Challenge!` (not `Header!`, not `Arial`) — source assertion: `grep -c "Arial" packages/client/src/components/ActionPanel.tsx` returns 0.
    - MOVE phase in the ATTACKER_2 slot renders text containing `2 hex max`; MOVE phase in ATTACKER_4/DEFENDER_5 slots does NOT render any hex-cap text (test assertions 4/5).
    - State 12d (`passTargetHex !== null`) still returns `null` — source assertion: the relevant branch is unchanged, still `return null;` with no new JSX.
    - All new ActionPanel.test.tsx assertions pass; `npx vitest run src/components/ActionPanel.test.tsx` exits 0; typecheck (`cd packages/client && npx tsc --noEmit`) is clean.
  </acceptance_criteria>
  <done>Every D-13-locked helper-text string and the Kick->Punt button rename are applied exactly; the MOVE phase's two-line restructure correctly scopes the hex-cap note to ATTACKER_2 only; state 12d is untouched; new tests cover the key changes and pass.</done>
</task>

</tasks>

<verification>
- `cd packages/client && npx tsc --noEmit` is clean.
- `cd packages/client && npx vitest run src/components/ActionPanel.test.tsx` passes.
- `grep -c "Kick (High Pass)" packages/client/src/components/ActionPanel.tsx` returns 0; `grep -c "Punt (High Pass)" packages/client/src/components/ActionPanel.tsx` returns 1.
- `grep -c "Arial" packages/client/src/components/ActionPanel.tsx` returns 0.
- Manual check: every non-active-player branch renders "Opponent's Turn" / "Waiting for opponent..." with no leftover divergent wait-state text.
</verification>

<success_criteria>
DESIGN-01 (ActionPanel.tsx portion, D-13) is satisfied: all 14 active-player phase prompts read the locked target text, all non-active-player wait states are unified to one shared two-line text, the GK_RESTART button reads "Punt (High Pass)" consistently with GK_KICK_TARGET's "Punt!", the MOVE phase correctly scopes its hex-cap note to the ATTACKER_2 slot only, and state 12d's auto-roll transition is unchanged — all covered by new ActionPanel tests.
</success_criteria>

<output>
Create `.planning/phases/18-design-polish/18-03-SUMMARY.md` when done.
</output>
