---
phase: 18-design-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/ActionLog.test.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DESIGN-01, MATCH-06]
must_haves:
  truths:
    - 'Every dice roll that resolves an action logs in the format {Stat Name} {statValue} + {roll} - {penalty} = {combined}, always showing the - {penalty} term even when penalty is 0 (D-12)'
    - 'D-12: SHOT_ATTEMPT, TACKLE_ATTEMPT, STEAL_ATTEMPT, and HEADER all render the SAME spelled-out structure via one shared formatter — no compact (die+stat=score) parenthetical shorthand survives for these 4 types'
    - 'D-12 stat mapping: SHOT_ATTEMPT uses Shooting (shooter) / Saving (GK); TACKLE_ATTEMPT uses Tackling (defender) / Dribbling (carrier); STEAL_ATTEMPT uses Tackling (defender only); HEADER uses Aerial Ability (both sides)'
    - 'DICE_ROLL remains exempt (no stat+roll+penalty triple) — unchanged'
    - "Per-player move log lines show the player's first and last name plus the hex path with arrows (e.g. 'Jane Doe | 19,14 -> 18,15 -> 17,15')"
    - "MATCH-06 requirement text in REQUIREMENTS.md reads the perspective-neutral wording from PROJECT.md (each team's DEF/MID within symmetric columns of kick-off hex)"
  artifacts:
    - path: 'packages/client/src/components/ActionLog.tsx'
      provides: 'Shared fmtStatRoll dice-roll formatter (spelled-out Stat Name format, always shows penalty term) + name-resolved move-group rendering'
      contains: 'fmtStatRoll'
    - path: '.planning/REQUIREMENTS.md'
      provides: 'Corrected MATCH-06 requirement text'
      contains: 'symmetric columns of kick-off'
  key_links:
    - from: 'packages/client/src/components/ActionLog.tsx'
      to: 'useGameStore.getState().gameState.pieces'
      via: 'name resolution for move-group log lines'
      pattern: 'firstName'
---

<objective>
Two DESIGN-01 ActionLog deliverables plus the doc-only MATCH-06 fix:
1. (D-12, supersedes D-03's original "keep the compact parenthesized layout" framing) Replace the compact `(die+stat-penalty=score)` shorthand with a spelled-out `{Stat Name} {statValue} + {roll} - {penalty} = {combined}` format, ALWAYS showing the `- {penalty}` term (even `- 0`). Apply this SAME structure uniformly to SHOT_ATTEMPT, TACKLE_ATTEMPT, STEAL_ATTEMPT, and HEADER (DICE_ROLL stays exempt — no stat triple).
2. (D-01 per-player move log) Render the per-player move-group log line with the player's `{firstName} {lastName}` (resolved from `pieces` by pieceId) plus the hex-path-with-arrows, replacing the current terse `A3`-style label.
3. (D-10 / MATCH-06) Rewrite the MATCH-06 requirement text in REQUIREMENTS.md to the perspective-neutral wording. No code change for this item.

Purpose: DESIGN-01 logging consistency (one spelled-out stat+roll+penalty format everywhere a duel/roll resolves; readable per-player move history) and MATCH-06 documentation correction.
Output: A shared `fmtStatRoll` formatter, name-resolved move-group rendering, and a corrected REQUIREMENTS.md line.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-design-polish/18-CONTEXT.md
@.planning/phases/18-design-polish/18-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates or modifies (exclude from drift verification):

- `fmtStatRoll(statName: string, statValue: number, roll: number, penalty: number, combined: number): string` — NEW module-level helper in `ActionLog.tsx` replacing both the old `fmtScore` closure and `fmtHeading`. Produces `"{statName} {statValue} + {roll} - {Math.abs(penalty)} = {combined}"`, ALWAYS including the `- {penalty}` term (including `- 0`) — never omits it, never uses a compact parenthesized form.
- `pieceName(pieceId: string): string` — NEW module-level helper resolving `{firstName} {lastName}` from `useGameStore.getState().gameState.pieces` (falls back to the piece id when not found)
- Updated `MoveGroup` rendering in the `ActionLog` component (uses `pieceName` for the displayed label)
- Corrected MATCH-06 line in `.planning/REQUIREMENTS.md`
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Shared fmtStatRoll formatter — spelled-out stat name, always showing the penalty term (D-12)</name>
  <files>packages/client/src/components/ActionLog.tsx, packages/client/src/components/ActionLog.test.tsx</files>
  <read_first>
    - packages/client/src/components/ActionLog.tsx (lines 58-65 = current fmtHeading helper; lines 180-186 = DICE_ROLL case; lines 205-219 = STEAL_ATTEMPT case; lines 220-236 = TACKLE_ATTEMPT case; lines 275-304 = SHOT_ATTEMPT case with the local fmtScore at 276-279 that omits the -0 term; lines 326-388 = HEADER case using fmtHeading)
    - .planning/phases/18-design-polish/18-CONTEXT.md (D-03's literal example: "Tackling 4 + 3 - 0 = 7"; **D-12 is the authoritative spec for this task** — supersedes D-03's "keep the existing parenthesized layout" framing entirely. D-12 gives the exact per-event stat-name mapping: SHOT_ATTEMPT = Shooting/Saving, TACKLE_ATTEMPT = Tackling/Dribbling, STEAL_ATTEMPT = Tackling, HEADER = Aerial Ability)
    - packages/server/src/gameEngine.ts (lines ~601-602 = TACKLE_ATTEMPT computes defCombined from piece.tackling, carCombined from carrier.dribbling; line ~565 = STEAL_ATTEMPT computes combined from defender.tackling — confirms the D-12 stat mapping against the actual server-side computation, not just the display layer)
  </read_first>
  <action>
    Delete the local `fmtScore` closure (ActionLog.tsx:276-279) and the module-level `fmtHeading` helper (lines 58-65). Replace both with ONE new module-level helper: `fmtStatRoll(statName: string, statValue: number, roll: number, penalty: number, combined: number): string`, returning the literal string `` `${statName} ${statValue} + ${roll} - ${Math.abs(penalty)} = ${combined}` `` — no parentheses, no compact shorthand, ALWAYS includes the `- {penalty}` term (a zero penalty renders literally as `- 0`, never omitted). This is a hard replacement of the old compact `(die+stat=score)` parenthesized layout, not an extension of it.

    Rewire all four named cases to call `fmtStatRoll` with the D-12 stat-name mapping:
    - SHOT_ATTEMPT (lines 275-304): shooter side calls `fmtStatRoll('Shooting', shooterRawStat, event.shooterDie, event.shooterPenaltyTotal, event.shooterScore)`; GK side calls `fmtStatRoll('Saving', gkRawStat, event.gkDie, event.gkPenaltyTotal, event.gkScore)`. Keep the surrounding outcome/handling-result text (e.g. `${outcome} — ${shooterStr} vs ${gkStr}`) — only the per-side duel string changes shape. The handling sub-check line (`handling: {die} vs {gkHandling} ({result})`) is unchanged — it is not part of the stat+roll+penalty triple.
    - TACKLE_ATTEMPT (lines 220-236): defender side calls `fmtStatRoll('Tackling', defStat, event.defenderDie, 0, event.defenderCombined)`; carrier side calls `fmtStatRoll('Dribbling', carrStat, event.carrierDie, 0, event.carrierCombined)` (penalty is always 0 — TACKLE_ATTEMPT carries no penalty field). Keep the `vs` comparison structure: `{result} -> {defenderStr} vs {carrierStr}`.
    - STEAL_ATTEMPT (lines 205-219): single side, `fmtStatRoll('Tackling', defenderCombined - defenderDie, event.defenderDie, 0, event.defenderCombined)` (penalty is always 0 — STEAL_ATTEMPT carries no penalty field). Replace the current `(die: X, score: Y)` text with the `fmtStatRoll` output.
    - HEADER (lines 326-388): replace the `fmtHeading`-produced `aScore`/`dScore` strings with `fmtStatRoll('Aerial Ability', attackerRawStat, event.attackerDie, attackerPenalty, event.attackerCombined)` and the defender equivalent, where `rawStat`/`penalty` are derived the same way `fmtHeading` derived them (`penalty = die + stat - combined`, `rawStat = aerialAbility`). Keep the uncontested-header branch (no dice) untouched — D-12 only applies to the contested-duel branch.
    - DICE_ROLL (lines 180-186): unchanged — no stat+roll+penalty triple exists here. Keep/add the existing exemption comment.

    Create packages/client/src/components/ActionLog.test.tsx (no test file exists today). Model the mock/setState setup on GameBoard.test.tsx (vi.mock('../socket.js', ...), useGameStore.setState with a gameState containing an eventLog and selectedTeams). Render `<ActionLog />` and assert: (1) a TACKLE_ATTEMPT event renders `'Tackling'` and `'Dribbling'` plus a literal `- 0` for both sides; (2) a SHOT_ATTEMPT event with a non-zero penalty renders `'Shooting'`/`'Saving'` plus `- {abs(penalty)}`; (3) a SHOT_ATTEMPT event with zero penalty renders `- 0` (regression guard against the old omit-the-term behavior); (4) a STEAL_ATTEMPT event renders `'Tackling'` and `- 0`; (5) a contested HEADER event renders `'Aerial Ability'` for both sides. Seed selectedTeams so pieceColorOf resolves (e.g. both teams set to a valid TeamId).

  </action>
  <verify>
    <automated>cd packages/client && npx vitest run src/components/ActionLog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `fmtStatRoll` is a module-level function (not a closure) — source assertion: `grep -n "function fmtStatRoll\|const fmtStatRoll" packages/client/src/components/ActionLog.tsx` shows it at module scope, outside `formatEvent`.
    - `fmtStatRoll` never omits the penalty term: behavior assertion via test (3) — a zero-penalty SHOT_ATTEMPT renders `- 0` in the log output, not an omitted term.
    - TACKLE_ATTEMPT log output contains both `'Tackling'` and `'Dribbling'` plus `- 0` for each side (test 1).
    - SHOT_ATTEMPT with a non-zero penalty renders `'Shooting'`/`'Saving'` plus `- {abs(penalty)}` (test 2).
    - STEAL_ATTEMPT log output contains `'Tackling'` and `- 0` (test 4).
    - Contested HEADER log output contains `'Aerial Ability'` for both sides (test 5).
    - No compact `(die+stat...)`-shaped parenthetical survives for these 4 event types: `grep -nE "\(\\\$\{.*die.*\+.*stat" packages/client/src/components/ActionLog.tsx` (or equivalent manual check) returns no match in the SHOT_ATTEMPT/TACKLE_ATTEMPT/STEAL_ATTEMPT/HEADER branches.
    - The unused `fmtHeading` and old `fmtScore` helpers are removed (source assertion: `grep -c "fmtHeading\|fmtScore" packages/client/src/components/ActionLog.tsx` returns 0).
    - `npx vitest run src/components/ActionLog.test.tsx` exits 0; typecheck (`cd packages/client && npx tsc --noEmit`) is clean.
  </acceptance_criteria>
  <done>One shared fmtStatRoll drives all 4 stat+roll+penalty log cases with a spelled-out stat name and always-shown penalty term (including - 0); fmtHeading and the old compact fmtScore are gone; new ActionLog tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Per-player move-log lines show first + last name (D-01)</name>
  <files>packages/client/src/components/ActionLog.tsx, packages/client/src/components/ActionLog.test.tsx</files>
  <read_first>
    - packages/client/src/components/ActionLog.tsx (lines 8-13 = pieceColorOf using useGameStore.getState() — the module-level store-access pattern to reuse; lines 27-35 = MoveGroup type; lines 52-56 = pieceNum helper; lines 74-153 = consolidateEvents building MoveGroup.pieceLabel; lines 504-523 = the move_group rendering in the ActionLog component where pieceLabel + path are displayed)
    - packages/shared/src/types.ts (lines 25-27 = PlayerPiece firstName/lastName; line 89+ = MOVE ActionEvent carries pieceId only, no name — names must be resolved from gameState.pieces by pieceId)
    - .planning/phases/18-design-polish/18-CONTEXT.md (D-01 per-player move logging example: "[team-color] [MOVE] {firstName} {lastName} | 19,14 -> 18,15 -> 17,15 -> 16,16" — one log line per activated piece, path grows in place)
  </read_first>
  <action>
    Add a module-level `pieceName(pieceId: string): string` helper next to `pieceColorOf` (ActionLog.tsx:8-13), using the same `useGameStore.getState().gameState.pieces` access pattern. It finds the piece by id and returns `${firstName} ${lastName}`; if no piece is found (e.g. id not in current pieces), fall back to returning the existing `pieceNum`-style label so nothing breaks. Resolve the name at render time in the ActionLog component's `move_group` branch (lines 504-523), not inside consolidateEvents — consolidateEvents only has the pieceId; keep storing pieceId-derived data on MoveGroup, and look up the display name when rendering.

    Update the `move_group` render branch (lines 504-523) so the displayed label is `{firstName} {lastName}` (via `pieceName`) followed by ` | ` then the existing arrow-joined path (`item.path.map(...).join(' → ')`). Match the D-01 example shape: `[MOVE] Jane Doe | 19,14 → 18,15 → 17,15`. Keep the team-colored prefix span (`item.prefix` with `item.prefixColor`) and apply the team color to the name span (reuse `item.pieceColor`). Add the resolved piece id to the MoveGroup type (it already has `groupKey` of the form `${slot}:${pieceId}` and `prefixColor`/`pieceColor`; add a `pieceId: string` field to MoveGroup and populate it in all four move_group-producing branches of consolidateEvents — MOVE, GK_KICK_MOVE, HP_MOVE, and the FTP path if present — so the render branch can call `pieceName(item.pieceId)`). Keep the existing `pieceLabel` field as a fallback for the `pieceName` helper's not-found case, or pass it through — choose whichever keeps the not-found fallback working; do not remove the path-arrow rendering.

    Extend ActionLog.test.tsx (created in Task 1): seed gameState.pieces with at least one piece that has firstName/lastName and an eventLog containing two consecutive MOVE events for that piece (so consolidateEvents merges them into one move_group). Assert the rendered log line contains the full `${firstName} ${lastName}` string AND the arrow-joined path (e.g. text matching both the name and `→`).

  </action>
  <verify>
    <automated>cd packages/client && npx vitest run src/components/ActionLog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `pieceName` is a module-level helper using `useGameStore.getState().gameState.pieces` (source assertion: `grep -n "function pieceName\|const pieceName" packages/client/src/components/ActionLog.tsx`).
    - A merged two-step MOVE move_group renders the piece's `${firstName} ${lastName}` and the arrow-joined path in the same log entry (test assertion).
    - When a pieceId is not present in gameState.pieces, the move_group still renders without throwing (fallback path — behavior assertion: a MOVE event for an unknown id does not crash the render; covered by a test case or by the existing label fallback).
    - `MoveGroup` carries a `pieceId: string` field populated in every move_group branch of consolidateEvents (source assertion: `pieceId` appears in the MoveGroup type and in each `items.push({ kind: 'move_group', ... })`).
    - `npx vitest run src/components/ActionLog.test.tsx` exits 0; typecheck clean.
  </acceptance_criteria>
  <done>Per-player move-log lines show first + last name plus the growing hex path with arrows, resolved from pieces by id, with a safe fallback; tests cover the name+path rendering.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite MATCH-06 requirement text to perspective-neutral wording (D-10, doc-only)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 56 = current MATCH-06 entry under "Carry-forward from v1.1")
    - .planning/PROJECT.md (line 81 = the drafted perspective-neutral wording: "each team's DEF/MID within symmetric columns of kick-off hex")
    - .planning/phases/18-design-polish/18-CONTEXT.md (D-10: "Rewrite the MATCH-06 requirement text in REQUIREMENTS.md to the perspective-neutral wording already drafted in PROJECT.md's Active section ... No code change — purely correcting requirement documentation to match already-shipped behavior")
  </read_first>
  <action>
    Edit ONLY the MATCH-06 line in .planning/REQUIREMENTS.md (currently line 56). Replace the existing requirement text with the perspective-neutral wording from PROJECT.md line 81: state that each team's DEF/MID pieces are placed within symmetric hex-columns of the kick-off hex from their own end (the symmetric-mirror intent). Preserve the existing `- [ ] **MATCH-06** _(req text only)_:` checkbox/ID/tag prefix and the entry's position in the "Carry-forward from v1.1" section. Do NOT touch any other requirement, the Traceability table (MATCH-06 stays Phase 18 / Pending there), or any code file. This is a documentation correction only.
  </action>
  <verify>
    <automated>grep -n "symmetric columns of kick-off\|symmetric hex-columns of the kick-off\|DEF/MID" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The MATCH-06 line in REQUIREMENTS.md contains the perspective-neutral wording (mentions "DEF/MID", "symmetric", and "kick-off hex") — source assertion via the grep above returning the MATCH-06 line.
    - The `- [ ] **MATCH-06** _(req text only)_:` prefix is preserved (still an unchecked, doc-only-tagged entry).
    - No other line in REQUIREMENTS.md changed except MATCH-06 (behavior assertion: `git diff --stat .planning/REQUIREMENTS.md` shows only REQUIREMENTS.md, and the diff hunk touches only the MATCH-06 line).
    - No code file modified by this task.
  </acceptance_criteria>
  <done>MATCH-06 requirement text reads the perspective-neutral symmetric-columns wording; no other requirement or code changed.</done>
</task>

</tasks>

<verification>
- `cd packages/client && npx vitest run src/components/ActionLog.test.tsx` passes.
- `cd packages/client && npx tsc --noEmit` is clean (fmtHeading/fmtScore removal, fmtStatRoll addition, and MoveGroup.pieceId addition compile).
- `grep -c "fmtHeading\|fmtScore" packages/client/src/components/ActionLog.tsx` returns 0.
- MATCH-06 line in REQUIREMENTS.md shows the perspective-neutral wording.
</verification>

<success_criteria>
DESIGN-01 (ActionLog portion) and MATCH-06 are satisfied: one shared fmtStatRoll formatter, spelled out with the stat name, always shows the `- {penalty}` term (including `- 0`) across SHOT_ATTEMPT/TACKLE_ATTEMPT/STEAL_ATTEMPT/HEADER per D-12; per-player move-log lines show first+last name plus the arrow path; and the MATCH-06 requirement text is corrected to the perspective-neutral wording.
</success_criteria>

<output>
Create `.planning/phases/18-design-polish/18-02-SUMMARY.md` when done.
</output>
