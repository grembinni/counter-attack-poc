---
phase: 18-design-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/GameBoard.test.tsx
autonomous: true
requirements: [DESIGN-01]
must_haves:
  truths:
    - 'Every GamePhase value renders a scoreboard label following the TEAM + plain VERB/NOUN convention (no -ING gerunds, no redundant PHASE suffix), except the explicit D-11 overrides listed below'
    - "D-11: The PASS phase scoreboard reads 'CHOOSING ACTION' (reverted from the original 'CHOOSE ACTION' plan — gerund kept intentionally per user correction)"
    - "D-11: The GK_DIVE phase scoreboard reads 'GOALIE DIVE' (not 'GK DIVE' or the stale 'GK DIVING')"
    - "D-11: SNAPSHOT_TARGET reads 'SNAPSHOT - SELECT TARGET', SNAPSHOT_DEFLECT reads 'SNAPSHOT - RESPONSE MOVE', GK_RESTART reads 'GOALIE RESTART', GK_KICK_TARGET reads 'GOALIE KICK — SELECT TARGET', GK_KICK_MOVE reads 'GOALIE KICK — REPOSITION', FIRST_TIME_PASS_MOVE reads 'FIRST-TIME PASS — RESPONSE MOVE', FREE_KICK_SETUP reads 'OFFSIDES - FREE KICK SETUP'"
    - 'While in MOVE phase, the scoreboard appends the numbered slot suffix (MOVE 4 / MOVE 5 / MOVE 2) matching the active 4-5-2 movement slot'
    - "The active team name is prepended to the phase label per the convention (e.g. 'HOME TEAM MOVE')"
    - 'D-04: any other stale/contradictory phase-label wording found during the sweep is corrected inline as part of this task, not deferred to a separate tone-audit pass'
  artifacts:
    - path: 'packages/client/src/components/GameBoard.tsx'
      provides: 'Convention-corrected PHASE_LABEL map + slot-suffix helper + team-prefixed render'
      contains: 'PHASE_LABEL'
  key_links:
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: 'PHASE_LABEL'
      via: 'phaseLabel render at scoreboard phaseSummary'
      pattern: "PHASE_LABEL\\[phase\\]"
---

<objective>
Rewrite the `PHASE_LABEL` map in `GameBoard.tsx` so every `GamePhase` value follows the LOCKED DESIGN-01 naming convention (D-01/D-02), with the D-11 corrections applied (GOALIE expansion, SELECT TARGET / RESPONSE MOVE phrasing, PASS kept as a gerund). Add the MOVE-slot numbered suffix (MOVE 4 / MOVE 5 / MOVE 2) computed at the render call site from `gameState.movementSlot`.

Purpose: DESIGN-01 messaging consistency — the scoreboard/phase prompt is the most visible status text in the match.
Output: A corrected flat `Record<GamePhase, string>` map (D-02: data, not a templating engine) plus a small slot-suffix helper, wired into the existing team-prefixed phase summary render.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-design-polish/18-CONTEXT.md
@.planning/phases/18-design-polish/18-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates or renames (exclude from drift verification):

- `PHASE_LABEL` map values (rewritten in place — same `Record<GamePhase, string>` shape, new string values)
- `moveSlotSuffix(slot: MovementSlot | null): string` — NEW module-level helper in `GameBoard.tsx` returning `' 4'` / `' 5'` / `' 2'` / `''`
- Updated `phaseLabel` render expression in the scoreboard `phaseSummary` block
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite PHASE_LABEL map to the locked naming convention</name>
  <files>packages/client/src/components/GameBoard.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (lines 16-43 = current PHASE_LABEL map; lines 134-137 = current teamName/teamColor/phaseLabel derivation; lines 239-246 = phaseSummary render call site)
    - .planning/phases/18-design-polish/18-CONTEXT.md (DESIGN-01 section: D-01 list of every GamePhase to sweep, D-02 "implement as data, no templating engine", worked convention example with KICKOFF/MOVE; **D-11 addendum has the authoritative corrected target table — D-11 OVERRIDES the target values listed in D-01/this task's own action text wherever they conflict**)
    - .planning/phases/18-design-polish/18-PATTERNS.md (GameBoard PHASE_LABEL section — confirms keep flat Record shape, recommends prepending team name at JSX call site, not function-valued map)
    - packages/shared/src/types.ts (line 45 MovementSlot union: 'ATTACKER_4' | 'DEFENDER_5' | 'ATTACKER_2'; line 371 movementSlot field on GameState)
  </read_first>
  <action>
    Rewrite each value in the `PHASE_LABEL: Record<GamePhase, string>` map (currently GameBoard.tsx:17-43) to the plain VERB/NOUN convention. Keep it a flat `Record<GamePhase, string>` — do NOT convert to function values or introduce any templating (D-02). The map values are the PHASE portion only; the active team name is prepended separately at the render call site (Task 2), so do not embed `{team}` placeholders in the map strings.

    Required exact target values — this is the FULL, FINAL list (D-11 in 18-CONTEXT.md is the authoritative source; it supersedes the original D-01 worked example for the rows marked "D-11 correction" below):
    - `LOBBY: ''`
    - `KICK_OFF: 'KICKOFF'` (one word per the convention example)
    - `KICK_OFF_SETUP: 'KICKOFF SETUP'`
    - `MOVE: 'MOVE'` (was 'MOVEMENT PHASE' — drop redundant PHASE suffix; the numbered slot suffix is appended at render time in Task 2)
    - `PASS: 'CHOOSING ACTION'` — **D-11 correction**: kept as the original gerund form; do NOT change to 'CHOOSE ACTION'
    - `SNAPSHOT_TARGET: 'SNAPSHOT - SELECT TARGET'` — D-11 correction
    - `GK_DIVE: 'GOALIE DIVE'` — D-11 correction (GK → GOALIE; also fixes the stale 'GK DIVING')
    - `SNAPSHOT_DEFLECT: 'SNAPSHOT - RESPONSE MOVE'` — D-11 correction
    - `SHOT: 'SHOT'`
    - `HEADER: 'HEADER'`
    - `SNAPSHOT: 'SNAPSHOT'`
    - `LOOSE_BALL: 'LOOSE BALL'`
    - `HIGH_PASS_MOVE: 'HIGH PASS — REPOSITION'` (em-dash preserved — keeps REPOSITION, unlike the D-11 RESPONSE MOVE phases below)
    - `GK_RESTART: 'GOALIE RESTART'` — D-11 correction
    - `GK_QUICK_THROW: 'QUICK THROW'`
    - `GK_KICK_TARGET: 'GOALIE KICK — SELECT TARGET'` — D-11 correction
    - `GK_KICK_MOVE: 'GOALIE KICK — REPOSITION'` — D-11 correction
    - `FREE_MOVE_ATTACK: 'FREE MOVE — ATTACK'`
    - `FREE_MOVE_DEFENSE: 'FREE MOVE — DEFENSE'`
    - `FIRST_TIME_PASS_MOVE: 'FIRST-TIME PASS — RESPONSE MOVE'` — D-11 correction (hyphenated FIRST-TIME, em-dash, RESPONSE MOVE not REPOSITION)
    - `FREE_KICK_SETUP: 'OFFSIDES - FREE KICK SETUP'` — D-11 correction
    - `HALF_TIME: 'HALF TIME'`
    - `FULL_TIME: 'FULL TIME'`
    - `REPLAY: 'REPLAY'`

    Note the asymmetry is intentional (confirmed D-11): `HIGH_PASS_MOVE` and `GK_KICK_MOVE` keep the em-dash + `REPOSITION` phrasing; `SNAPSHOT_DEFLECT` and `FIRST_TIME_PASS_MOVE` use `RESPONSE MOVE` instead. Keep em-dashes (`—`) exactly where shown above — do NOT flatten them to plain spaces or hyphens (this reverts an earlier draft of this plan that had incorrectly flattened them).

    Update the map's leading doc-comment to reference DESIGN-01 (Phase 18) as the source of the naming convention instead of the old "UI-SPEC Turn Indicator Spec table" note.

  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `PHASE_LABEL` remains typed `Record<GamePhase, string>` (no function values, no new type) — source assertion: the literal `Record<GamePhase, string>` annotation is unchanged.
    - `PHASE_LABEL.PASS === 'CHOOSING ACTION'` (D-11: intentionally NOT 'CHOOSE ACTION') and `PHASE_LABEL.GK_DIVE === 'GOALIE DIVE'` and `PHASE_LABEL.MOVE === 'MOVE'`.
    - `PHASE_LABEL.SNAPSHOT_TARGET === 'SNAPSHOT - SELECT TARGET'`, `PHASE_LABEL.SNAPSHOT_DEFLECT === 'SNAPSHOT - RESPONSE MOVE'`, `PHASE_LABEL.GK_RESTART === 'GOALIE RESTART'`, `PHASE_LABEL.GK_KICK_TARGET === 'GOALIE KICK — SELECT TARGET'`, `PHASE_LABEL.GK_KICK_MOVE === 'GOALIE KICK — REPOSITION'`, `PHASE_LABEL.FIRST_TIME_PASS_MOVE === 'FIRST-TIME PASS — RESPONSE MOVE'`, `PHASE_LABEL.FREE_KICK_SETUP === 'OFFSIDES - FREE KICK SETUP'` (all D-11 corrections).
    - No PHASE_LABEL value other than `PASS` contains the substring 'ING ' (PASS's gerund is an intentional, locked exception per D-11), and no value ends in ' PHASE' (no redundant PHASE suffix).
    - Typecheck passes (the `Record<GamePhase, string>` exhaustiveness check still compiles — every GamePhase key present).
  </acceptance_criteria>
  <done>All 24 GamePhase entries (including LOBBY) follow the plain VERB/NOUN convention except the explicit D-11 overrides (PASS gerund, GOALIE expansions, SELECT TARGET / RESPONSE MOVE phrasing); typecheck is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add MOVE-slot numbered suffix + team prefix at the render call site, with tests</name>
  <files>packages/client/src/components/GameBoard.tsx, packages/client/src/components/GameBoard.test.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (lines 109-137 = component body where teamName/phaseLabel are derived; lines 239-246 = the phaseSummary JSX where teamName and phaseLabel render today; line 113 shows actionCount selector pattern for adding a movementSlot selector)
    - packages/client/src/components/GameBoard.test.tsx (lines 1-50 = mock/beforeEach setup using mockMovementState and useGameStore.setState; the existing CLOCK-01 describe block is the template for a new describe block)
    - .planning/phases/18-design-polish/18-CONTEXT.md (DESIGN-01 worked example: "ATTACKER_4 slot → MOVE 4, DEFENDER_5 → MOVE 5, ATTACKER_2 → MOVE 2"; convention shows scoreboard text as "{UPPER team name} {LABEL}")
    - .planning/phases/18-design-polish/18-PATTERNS.md (GameBoard PHASE_LABEL section — "numbered suffix must be computed at the render call site by reading gameState.movementSlot ... append the trailing digit"; "continue string-concatenating team name at the JSX call site")
    - packages/shared/src/types.ts (line 45 MovementSlot, line 371 movementSlot field)
  </read_first>
  <action>
    Add a module-level helper `moveSlotSuffix(slot: MovementSlot | null): string` to GameBoard.tsx (import the `MovementSlot` type from `@counter-attack/shared` alongside the existing `GamePhase` import). It returns the numbered suffix derived from the active 4-5-2 slot: `ATTACKER_4` → `' 4'`, `DEFENDER_5` → `' 5'`, `ATTACKER_2` → `' 2'`, and `''` for `null` or any non-MOVE context. Implement it as a flat object lookup keyed by `MovementSlot` (the same lookup-table-as-data shape as PHASE_LABEL per D-02), returning `''` when the key is absent.

    In the component body, add a `movementSlot` selector (`useGameStore((s) => s.gameState.movementSlot)`) mirroring the existing `actionCount` selector style at line 113. Compute the displayed phase label as the base `PHASE_LABEL[phase]` plus, when `phase === 'MOVE'`, the `moveSlotSuffix(movementSlot)` (so MOVE renders 'MOVE 4' / 'MOVE 5' / 'MOVE 2', and shows plain 'MOVE' if movementSlot is null). Keep the existing `teamName` (line 135) prepended at the JSX call site exactly as it already is — the convention's `{UPPER team name} {LABEL}` is satisfied by the existing `<span className={styles.teamName}>{teamName}</span>` followed by the phase label span (GameBoard.tsx:240-245). Do NOT change the REPLAY suppression guard (`phase !== 'REPLAY'`) at line 243 or the `teamColor` styling — only the phase-label string changes.

    Add a new describe block to GameBoard.test.tsx, modeled on the existing CLOCK-01 block (mock/setState pattern at lines 19-50), asserting: (1) PASS phase renders text matching /CHOOSING ACTION/ (D-11: gerund is intentional here, do NOT assert against it); (2) GK_DIVE phase renders /GOALIE DIVE/ and NOT /GK DIVING/ or /GK DIVE/; (3) MOVE phase with movementSlot 'DEFENDER_5' renders /MOVE 5/; (4) MOVE phase with movementSlot 'ATTACKER_4' renders /MOVE 4/; (5) SNAPSHOT_TARGET phase renders /SNAPSHOT - SELECT TARGET/; (6) FIRST_TIME_PASS_MOVE phase renders /FIRST-TIME PASS — RESPONSE MOVE/. Use `useGameStore.setState({ gameState: { ...mockMovementState, phase, movementSlot } })` to drive each case (mockMovementState is already MOVE phase per the existing tests).

  </action>
  <verify>
    <automated>cd packages/client && npx vitest run src/components/GameBoard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `moveSlotSuffix('ATTACKER_4') === ' 4'`, `moveSlotSuffix('DEFENDER_5') === ' 5'`, `moveSlotSuffix('ATTACKER_2') === ' 2'`, `moveSlotSuffix(null) === ''` (behavior assertion via the new test cases).
    - GameBoard renders /CHOOSING ACTION/ when phase is PASS (D-11: this is the locked, intentional target — do not regress to asserting 'CHOOSE ACTION').
    - GameBoard renders /GOALIE DIVE/ when phase is GK_DIVE and does NOT render /GK DIVING/ or /GK DIVE/ (test assertion).
    - GameBoard renders /MOVE 5/ when phase is MOVE and movementSlot is 'DEFENDER_5' (test assertion).
    - GameBoard renders /SNAPSHOT - SELECT TARGET/ when phase is SNAPSHOT_TARGET, and /FIRST-TIME PASS — RESPONSE MOVE/ when phase is FIRST_TIME_PASS_MOVE (test assertions, D-11).
    - All existing GameBoard.test.tsx tests still pass (no regression in CLOCK / ActionPanel / overlay tests).
    - `npx vitest run src/components/GameBoard.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The MOVE phase shows the numbered slot suffix, the team name still prefixes the label, all D-11 corrected labels are asserted by new tests, and the full GameBoard test file is green.</done>
</task>

</tasks>

<verification>
- Typecheck clean across the client package (`cd packages/client && npx tsc --noEmit`).
- `cd packages/client && npx vitest run src/components/GameBoard.test.tsx` passes.
- Manual grep confirms no gerund (`ING `) or redundant ` PHASE'` survives in the PHASE_LABEL literal.
</verification>

<success_criteria>
DESIGN-01 (GameBoard portion) is satisfied: every GamePhase scoreboard label follows the locked plain VERB/NOUN convention, the PASS and GK_DIVE stale labels are corrected, the MOVE slot suffix (4/5/2) renders from movementSlot, and the active team name still prefixes the label — all covered by new GameBoard tests.
</success_criteria>

<output>
Create `.planning/phases/18-design-polish/18-01-SUMMARY.md` when done.
</output>
