---
phase: 22-uniform-selection-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/events.ts
  - packages/shared/src/types.ts
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.teamselect.test.ts
  - packages/server/src/__tests__/gameEngine.test.ts
  - packages/server/src/__tests__/gameEngine.phase8.test.ts
  - packages/server/src/__tests__/kickoffDebug.test.ts
autonomous: true
requirements: [UNIFORM-04]
must_haves:
  truths:
    - 'GameState carries selectedUniformStyles: { home, away } in every snapshot'
    - 'buildInitialGameState accepts selectedUniformStyles and embeds it in the returned state'
    - 'UNIFORM_CONFIRM, UNIFORM_SELECTION_START, UNIFORM_HOME_CONFIRMED event names + typed signatures exist'
    - 'buildReplayFrames carries selectedUniformStyles into replay frames'
    - 'All existing gameEngine tests compile and pass with the updated signature'
  artifacts:
    - path: 'packages/shared/src/events.ts'
      provides: 'UNIFORM_CONFIRM (client), UNIFORM_SELECTION_START + UNIFORM_HOME_CONFIRMED (server) names and typed maps'
      contains: 'UNIFORM_CONFIRM'
    - path: 'packages/shared/src/types.ts'
      provides: 'GameState.selectedUniformStyles field'
      contains: 'selectedUniformStyles'
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'buildInitialGameState 4-param signature + selectedUniformStyles in state and replay frames'
      contains: 'selectedUniformStyles'
  key_links:
    - from: 'packages/server/src/gameEngine.ts'
      to: 'packages/shared/src/types.ts'
      via: 'GameState.selectedUniformStyles return field'
      pattern: 'selectedUniformStyles'
---

<objective>
Establish the shared type + event contracts and the engine signature that Phase 22's server and client waves build against. Add `GameState.selectedUniformStyles`, the three new socket events (`UNIFORM_CONFIRM`, `UNIFORM_SELECTION_START`, `UNIFORM_HOME_CONFIRMED`), extend `buildInitialGameState` to accept and embed uniform styles, and carry the field through `buildReplayFrames`. Update all existing `buildInitialGameState` call sites in tests so the package compiles.

Purpose: Interface-first ordering — the server (22-02) and client (22-03) waves both consume these contracts; defining them here prevents a scavenger hunt (UNIFORM-04 data-model foundation).
Output: Compiling shared + server packages with the new event names, `selectedUniformStyles` field, and updated engine signature.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-uniform-selection-screen/22-CONTEXT.md
@.planning/phases/22-uniform-selection-screen/22-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created by Phase 22 (exclude from source-drift verification):

- `ClientEvents.UNIFORM_CONFIRM` — const string `'uniform:confirm'` (events.ts)
- `ServerEvents.UNIFORM_SELECTION_START` — const string `'uniform:selection-start'` (events.ts)
- `ServerEvents.UNIFORM_HOME_CONFIRMED` — const string `'uniform:home-confirmed'` (events.ts)
- `ClientToServerEvents[ClientEvents.UNIFORM_CONFIRM]` — `(teamId: TeamId, uniformStyle: UniformStyleId) => void`
- `ServerToClientEvents[ServerEvents.UNIFORM_SELECTION_START]` — `() => void`
- `ServerToClientEvents[ServerEvents.UNIFORM_HOME_CONFIRMED]` — `(teamId: TeamId, uniformStyle: UniformStyleId) => void`
- `GameState.selectedUniformStyles` — `{ home: UniformStyleId; away: UniformStyleId }` field (types.ts)
- `buildInitialGameState(roomCode, selectedTeams, gameSpeed, selectedUniformStyles)` — 4-param signature (gameEngine.ts)
- `Room.homePickedUniformStyle` / `Room.awayPickedTeam` — added in 22-02
- `Screen 'UNIFORM_SELECTION'` — union member added in 22-03
- `UniformSelectionScreen` React component + `UniformSelectionScreen.module.css` — new files in 22-03
- `NEUTRAL_PALETTE`, `ALL_STYLE_IDS` — module constants in 22-03
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add three uniform socket events to events.ts</name>
  <files>packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/events.ts (see current ClientEvents/ServerEvents const objects, ClientToServerEvents/ServerToClientEvents interfaces, and the TEAM_PICK / TEAM_HOME_PICKED / TEAM_SELECTION_START analog entries)
    - packages/shared/src/uniformStyles.ts (confirm `UniformStyleId` is exported for the typed signatures)
    - packages/shared/src/teamConfig.ts (confirm `TeamId` export path — events.ts imports TeamId from './teamConfig.js')
  </read_first>
  <action>
    Add to the `ClientEvents` const object: `UNIFORM_CONFIRM: 'uniform:confirm'` with a JSDoc comment citing Phase 22 D-14.
    Add to the `ServerEvents` const object: `UNIFORM_SELECTION_START: 'uniform:selection-start'` (JSDoc cite D-13) and `UNIFORM_HOME_CONFIRMED: 'uniform:home-confirmed'` (JSDoc cite D-15).
    Add to `ClientToServerEvents`: `[ClientEvents.UNIFORM_CONFIRM]: (teamId: TeamId, uniformStyle: UniformStyleId) => void;` — payload is two positional args (teamId, uniformStyle) matching the emit convention used by TEAM_PICK.
    Add to `ServerToClientEvents`: `[ServerEvents.UNIFORM_SELECTION_START]: () => void;` (no payload — per D-13 each player already knows their own team) and `[ServerEvents.UNIFORM_HOME_CONFIRMED]: (teamId: TeamId, uniformStyle: UniformStyleId) => void;`.
    Add the `UniformStyleId` type import from './uniformStyles.js' at the top of the file alongside the existing `TeamId` import. Do NOT add an enum — this file uses typed const objects only.
  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - events.ts contains `UNIFORM_CONFIRM: 'uniform:confirm'`
    - events.ts contains `UNIFORM_SELECTION_START: 'uniform:selection-start'`
    - events.ts contains `UNIFORM_HOME_CONFIRMED: 'uniform:home-confirmed'`
    - events.ts contains `[ClientEvents.UNIFORM_CONFIRM]: (teamId: TeamId, uniformStyle: UniformStyleId) => void`
    - events.ts contains `[ServerEvents.UNIFORM_SELECTION_START]: () => void`
    - events.ts imports `UniformStyleId` from './uniformStyles.js'
    - `cd packages/shared && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Three new event names exist in the const objects and their typed signatures exist in the two event-map interfaces; shared package type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add selectedUniformStyles field to GameState</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts (read the `selectedTeams` field around line 477–478 and its JSDoc + surrounding fields to match ordering and comment style; confirm `UniformStyleId` import is present or add it)
    - packages/shared/src/uniformStyles.ts (confirm `UniformStyleId` export)
  </read_first>
  <action>
    Add a new required field to the `GameState` type immediately after `selectedTeams`:
    `selectedUniformStyles: { home: UniformStyleId; away: UniformStyleId };` with a JSDoc comment citing Phase 22 D-16, parallel to the existing `selectedTeams` JSDoc. Ensure `UniformStyleId` is imported into types.ts (import from './uniformStyles.js' if not already present). Do not reorder or alter any other GameState field.
  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `selectedUniformStyles: { home: UniformStyleId; away: UniformStyleId }`
    - The `selectedUniformStyles` field appears immediately after `selectedTeams` in the GameState declaration
    - types.ts has an import bringing `UniformStyleId` into scope
    - `cd packages/shared && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GameState declares selectedUniformStyles as a required field directly after selectedTeams; shared package type-checks.</done>
</task>

<task type="auto">
  <name>Task 3: Extend buildInitialGameState + buildReplayFrames and repair test call sites</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.teamselect.test.ts, packages/server/src/__tests__/gameEngine.test.ts, packages/server/src/__tests__/gameEngine.phase8.test.ts, packages/server/src/__tests__/kickoffDebug.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 149–184 (current `buildInitialGameState` signature and returned object literal) and lines 4197–4230 (`buildReplayFrames` manual initial-state literal)
    - packages/server/src/__tests__/gameEngine.teamselect.test.ts (all `buildInitialGameState('ROOM1', { home: 'city', away: 'crew' })` call sites and the file-header comment describing the 2-arg signature)
    - packages/server/src/__tests__/gameEngine.test.ts (DEFAULT_TEAMS constant and its `buildInitialGameState('ROOMn', DEFAULT_TEAMS)` calls)
    - packages/server/src/__tests__/gameEngine.phase8.test.ts (DEFAULT_TEAMS_P8 constant and its calls)
    - packages/server/src/__tests__/kickoffDebug.test.ts (its single call site)
  </read_first>
  <action>
    In gameEngine.ts, change the `buildInitialGameState` signature to add a required 4th parameter:
    `buildInitialGameState(roomCode: string, selectedTeams: { home: TeamId; away: TeamId }, gameSpeed: GameSpeed = 'standard', selectedUniformStyles: { home: UniformStyleId; away: UniformStyleId }): GameState` per D-17. Note: because `gameSpeed` keeps its default but a required param follows it, every caller must now pass all four positional arguments (TypeScript requires the 4th, which forces the 3rd to be supplied). Import `UniformStyleId` into gameEngine.ts from '@counter-attack/shared'.
    Add `selectedUniformStyles,` to the returned object literal (around line 181, immediately after `selectedTeams,`) with a comment citing Phase 22 D-17.
    In `buildReplayFrames` (line ~4206 initial-state literal), add `selectedUniformStyles: finalState.selectedUniformStyles,` immediately after the `selectedTeams: finalState.selectedTeams,` line so replay frames carry uniform styles.
    Update every test call site to pass a 4th argument. Define a shared literal for reuse in each test file, e.g. a `DEFAULT_STYLES = { home: 'pinstripes-vertical', away: 'bar-diagonal' } as UniformStyleId map` local constant (use style IDs that exist in `UNIFORM_STYLE_META`), and pass it as the 4th arg to each `buildInitialGameState(...)` call. For calls that currently omit gameSpeed (2-arg form), supply `'standard'` as the 3rd arg then the styles as the 4th. Update the stale file-header comment in gameEngine.teamselect.test.ts that claims a 2-arg signature.
  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit && npx vitest run src/__tests__/gameEngine.teamselect.test.ts src/__tests__/gameEngine.test.ts src/__tests__/gameEngine.phase8.test.ts src/__tests__/kickoffDebug.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts `buildInitialGameState` signature contains `selectedUniformStyles: { home: UniformStyleId; away: UniformStyleId }` as the 4th parameter
    - gameEngine.ts returned object literal contains `selectedUniformStyles,`
    - gameEngine.ts buildReplayFrames initial-state literal contains `selectedUniformStyles: finalState.selectedUniformStyles`
    - Zero `buildInitialGameState(` call sites remain with fewer than 4 arguments across the four test files
    - `cd packages/server && npx tsc --noEmit` exits 0
    - The four named vitest test files pass
  </acceptance_criteria>
  <done>Engine builds and replays with selectedUniformStyles; all existing gameEngine tests compile against the new 4-arg signature and pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                               |
| -------------------- | ----------------------------------------------------------------------------------------- |
| client→server socket | UNIFORM_CONFIRM payload (teamId, uniformStyle) is untrusted; validated in 22-02, not here |

## STRIDE Threat Register

| Threat ID | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                         |
| --------- | ---------------------- | ------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| T-22-01   | Tampering              | UNIFORM_CONFIRM payload typing              | mitigate    | Typed signature `(teamId: TeamId, uniformStyle: UniformStyleId)` narrows accepted shape; server allow-list validation deferred to 22-02 |
| T-22-02   | Information Disclosure | selectedUniformStyles in GameState snapshot | accept      | Uniform style is non-sensitive cosmetic data broadcast to both room members by design                                                   |
| T-22-SC   | Tampering              | npm/pip/cargo installs                      | mitigate    | No package installs in this plan — no new dependencies added                                                                            |

</threat_model>

<verification>
- `cd packages/shared && npx tsc --noEmit` exits 0
- `cd packages/server && npx tsc --noEmit` exits 0
- `cd packages/server && npx vitest run src/__tests__/gameEngine.teamselect.test.ts src/__tests__/gameEngine.test.ts src/__tests__/gameEngine.phase8.test.ts src/__tests__/kickoffDebug.test.ts` passes
</verification>

<success_criteria>

- GameState has selectedUniformStyles; buildInitialGameState and buildReplayFrames carry it
- Three new events with typed signatures exist in events.ts
- Shared and server packages type-check; all four touched test files pass
  </success_criteria>

<output>
Create `.planning/phases/22-uniform-selection-screen/22-01-SUMMARY.md` when done
</output>
