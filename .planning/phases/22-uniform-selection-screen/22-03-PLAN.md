---
phase: 22-uniform-selection-screen
plan: 03
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - packages/client/src/components/UniformSelectionScreen.tsx
  - packages/client/src/components/UniformSelectionScreen.module.css
  - packages/client/src/components/UniformSelectionScreen.test.tsx
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/App.tsx
  - packages/client/src/components/HexGrid.tsx
autonomous: false
requirements: [UNIFORM-02, UNIFORM-03, UNIFORM-04]
must_haves:
  truths:
    - "After team selection the player sees all 18 uniform styles rendered against their team's palette (UNIFORM-02)"
    - "The team's defaultUniformStyle is pre-selected when the screen loads (UNIFORM-03)"
    - 'Confirming emits UNIFORM_CONFIRM(teamId, uniformStyle); the confirmed style renders on pieces via HexGrid resolving from selectedUniformStyles (UNIFORM-04)'
    - 'Before a team is selected, style swatches render in a neutral black/white scheme (D-05)'
    - "Away sees home's team struck out and an 'Opponent confirmed' banner after home confirms (D-04/D-11)"
    - "Both players must confirm before advancing; each sees 'Waiting for opponent…' after confirming (D-06)"
  artifacts:
    - path: 'packages/client/src/components/UniformSelectionScreen.tsx'
      provides: 'Combined team + style selection screen'
      min_lines: 120
    - path: 'packages/client/src/components/UniformSelectionScreen.module.css'
      provides: 'Dark-theme CSS module for the screen'
      contains: '.styleGrid'
    - path: 'packages/client/src/components/UniformSelectionScreen.test.tsx'
      provides: 'Component test for pre-selection, palette re-render, confirm emit'
      contains: 'UniformSelectionScreen'
  key_links:
    - from: 'packages/client/src/App.tsx'
      to: 'UniformSelectionScreen'
      via: 'UNIFORM_SELECTION render branch + UNIFORM_CONFIRM emit + UNIFORM_HOME_CONFIRMED/UNIFORM_SELECTION_START handlers'
      pattern: 'UNIFORM_(SELECTION|HOME_CONFIRMED|CONFIRM)'
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'gameState.selectedUniformStyles'
      via: 'uniformStyle prop resolution (replaces teamConfig.defaultUniformStyle)'
      pattern: 'selectedUniformStyles'
---

<objective>
Deliver the combined team + uniform selection screen and wire it into the client flow. Build the new `UniformSelectionScreen` component (all 12 teams flat + all 18 style tiles rendered via `UNIFORM_STYLES`, neutral palette before team pick, live re-render in team palette after pick, defaultUniformStyle pre-selected, single Confirm), add its CSS module, add a component test, extend the `Screen` union, register the two new socket handlers + emit in App.tsx with a render branch, and change HexGrid to resolve each piece's `uniformStyle` from `gameState.selectedUniformStyles` (D-18).

Purpose: Client half of Phase 22 — the visible screen (UNIFORM-02/03) and match-long application of the confirmed style (UNIFORM-04).
Output: A working uniform selection screen; pieces render the confirmed style in-match.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-uniform-selection-screen/22-CONTEXT.md
@.planning/phases/22-uniform-selection-screen/22-PATTERNS.md
@.planning/phases/22-uniform-selection-screen/22-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
See 22-01-PLAN.md `<artifacts_this_phase_produces>` for the full Phase 22 symbol list. This plan adds:

- `UniformSelectionScreen` — React component (new file)
- `UniformSelectionScreen.module.css` — CSS module (new file); classes `.screen`, `.heading`, `.statusLine`, `.sectionLabel`, `.teamGrid`, `.teamCard`, `.teamCardSelected`, `.teamCardStruckOut`, `.styleGrid`, `.styleTile`, `.styleTileSelected`, `.opponentBanner`, `.bannerPiece`, `.bannerText`, `.confirmButton`, plus copied speed-selector classes
- `NEUTRAL_PALETTE` — module const (TeamPalette shape, grey scheme)
- `ALL_TEAMS` — module const ordered TeamId[] (MLS first, then International)
- `ALL_STYLE_IDS` — module const `Object.keys(UNIFORM_STYLES) as UniformStyleId[]`
- `Screen 'UNIFORM_SELECTION'` — union member (useGameStore.ts)
- `homeConfirmedStyle` local state + `handleUniformConfirm` handler (App.tsx)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build UniformSelectionScreen component + CSS module</name>
  <files>packages/client/src/components/UniformSelectionScreen.tsx, packages/client/src/components/UniformSelectionScreen.module.css</files>
  <behavior>
    - On mount with a selectedTeam already set (or when a team is clicked), selectedStyle initializes to TEAM_CONFIGS[selectedTeam].defaultUniformStyle (UNIFORM-03)
    - All 18 style tiles always render; before any team is selected they use NEUTRAL_PALETTE; after a team is selected they re-render with TEAM_CONFIGS[selectedTeam].palette (D-05)
    - Clicking a non-struck-out team card sets selectedTeam and re-renders style tiles in the new palette
    - Clicking a style tile moves the selected highlight to that tile
    - Confirm button is disabled while selectedTeam === null OR selectedStyle === null; clicking it calls onConfirm(selectedTeam, selectedStyle) and then shows "Waiting for opponent…"
    - Away player: the card whose teamId === homePickedTeam renders struck-out and is not clickable
    - Away player: after homeConfirmedStyle is non-null, an "Opponent confirmed" banner (role="status") renders home's piece in home's palette + confirmed style
  </behavior>
  <read_first>
    - packages/client/src/components/TeamSelectionScreen.tsx (full — the closest analog: badge imports, Props shape, playerSlot/home-first turn logic, speed selector block, team card grid render, aria attributes)
    - packages/client/src/components/TeamSelectionScreen.module.css (full — copy .screen/.heading/.statusLine and the entire speed-selector class set verbatim; adapt .card states)
    - packages/client/src/styles/uniformStyles.tsx (UNIFORM_STYLES registry + UniformStyleRenderer signature `({ cx, cy, R, palette, isGK, pieceId }) => { patternDef, fill, overlay }`)
    - packages/shared/src/uniformStyles.ts (UNIFORM_STYLE_META keys/order for ALL_STYLE_IDS and aria labels)
    - packages/shared/src/teamConfig.ts (TeamPalette shape: homePrime/homeAlt/homeFont/awayPrime/awayAlt/awayFont/uiColor; TEAM_CONFIGS[].palette and TEAM_CONFIGS[].defaultUniformStyle)
    - .planning/phases/22-uniform-selection-screen/22-PATTERNS.md (SVG tile render pattern, NEUTRAL_PALETTE constant, opponent-banner pattern, confirm-button pattern — copy the identifiers exactly)
    - .planning/phases/22-uniform-selection-screen/22-UI-SPEC.md (Layout Contract, SVG Tile Rendering Contract, Color/Typography/Spacing tokens, Copywriting Contract exact strings)
  </read_first>
  <action>
    Create `UniformSelectionScreen.tsx`. Import UNIFORM_STYLES from '../styles/uniformStyles.js', UNIFORM_STYLE_META + TEAM_CONFIGS from '@counter-attack/shared', types TeamId/UniformStyleId/GameSpeed/TeamPalette from '@counter-attack/shared', useGameStore, and the 12 full-badge PNG imports (verbatim from TeamSelectionScreen). Import from './UniformSelectionScreen.module.css'.
    Define module consts: `NEUTRAL_PALETTE: TeamPalette = { homePrime:'#555', homeAlt:'#ccc', homeFont:'#fff', awayPrime:'#555', awayAlt:'#ccc', awayFont:'#fff', uiColor:'#555' }` (D-05, matches PATTERNS/UI-SPEC); `ALL_TEAMS: TeamId[] = ['city','crew','la','miami','nashville','seattle','canada','england','france','mexico','spain','us']` (D-02 MLS-first ordering); `ALL_STYLE_IDS = Object.keys(UNIFORM_STYLES) as UniformStyleId[]`; a FULL_BADGE_MAP keyed by TeamId to the imported PNGs.
    Props: `{ homePickedTeam: TeamId | null; homeConfirmedStyle: UniformStyleId | null; onConfirm: (teamId: TeamId, uniformStyle: UniformStyleId) => void; selectedSpeed: GameSpeed; onSpeedChange: (speed: GameSpeed) => void }`.
    Local state: `selectedTeam: TeamId | null` (null initially for away until they pick; home also starts null and picks their own team here), `selectedStyle: UniformStyleId | null`, `hasConfirmed: boolean`.
    Home-first logic from playerSlot: `iAmHome = playerSlot === 1`. Heading copy exactly: home "Home: choose your team + style", away "Away: choose your team + style" (UI-SPEC Copywriting).
    useEffect on [selectedTeam]: when selectedTeam !== null, setSelectedStyle(TEAM_CONFIGS[selectedTeam].defaultUniformStyle) — pre-selection (UNIFORM-03 / D-09).
    Render, in order (UI-SPEC Layout Contract): heading; optional opponent banner (away only, when homeConfirmedStyle !== null — role="status", left border home palette.homePrime, 48×48 SVG of home's piece via UNIFORM_STYLES[homeConfirmedStyle] with TEAM_CONFIGS[homePickedTeam].palette, text "Opponent confirmed"); speed selector block (home only, copied from TeamSelectionScreen, locked once hasConfirmed); sectionLabel "Team"; teamGrid mapping ALL_TEAMS — each a button, struck-out when teamId === homePickedTeam (away), aria-pressed={teamId===selectedTeam}, aria-label from TEAM_CONFIGS[teamId].name, selected border via palette.homePrime inline; sectionLabel "Style"; styleGrid mapping ALL_STYLE_IDS — for each style at index i, n=i+1, palette = selectedTeam ? TEAM_CONFIGS[selectedTeam].palette : NEUTRAL_PALETTE, call UNIFORM_STYLES[styleId]({cx:40,cy:40,R:30,palette,isGK:false,pieceId:`style-${n}`}), render an 80×80 svg with <defs>{patternDef}</defs>, <circle cx=40 cy=40 r=30 fill={result.fill}/>, {result.overlay}, and centered jersey <text> showing n (fill selectedTeam?palette.homeFont:'#fff'); button aria-pressed={styleId===selectedStyle}, aria-label from UNIFORM_STYLE_META[styleId].name, selected class + inline border/box-shadow in accent (selectedTeam palette.homePrime else '#e0e0e0'); confirm block — when !hasConfirmed render Confirm button labeled "Confirm selection" disabled when selectedTeam===null||selectedStyle===null, onClick calls onConfirm(selectedTeam, selectedStyle) then setHasConfirmed(true); else render statusLine "Waiting for opponent…".
    Create `UniformSelectionScreen.module.css` per 22-PATTERNS.md CSS block: .screen, .heading, .statusLine, .sectionLabel, .teamGrid (repeat(6,1fr) gap 16px max-width 800px + @media max-width 760px → repeat(3,1fr)), .teamCard/.teamCardSelected/.teamCardStruckOut, .styleGrid (repeat(6,1fr) gap 8px max-width 560px), .styleTile/.styleTileSelected (80×80), .opponentBanner/.bannerPiece/.bannerText, .confirmButton (+ :disabled), and the copied speed-selector classes. Use only the dark-theme color literals #1a1a2e/#16213e/#0f3460/#e0e0e0/#a0a0a0 and multiples-of-4 spacing per UI-SPEC (exceptions: 80px tiles, R=30, 8px 20px confirm padding).
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - UniformSelectionScreen.tsx contains `ALL_STYLE_IDS = Object.keys(UNIFORM_STYLES)`
    - UniformSelectionScreen.tsx contains `NEUTRAL_PALETTE` with `homePrime: '#555'`
    - UniformSelectionScreen.tsx contains `ALL_TEAMS` beginning `['city', 'crew', 'la', 'miami', 'nashville', 'seattle'`
    - UniformSelectionScreen.tsx contains a useEffect that sets `TEAM_CONFIGS[selectedTeam].defaultUniformStyle`
    - UniformSelectionScreen.tsx contains `pieceId: ` `style-` template and calls `UNIFORM_STYLES[styleId]`
    - UniformSelectionScreen.tsx contains the exact strings "Confirm selection", "Waiting for opponent…", "Opponent confirmed"
    - UniformSelectionScreen.tsx renders the opponent banner with `role="status"` gated on `homeConfirmedStyle`
    - UniformSelectionScreen.module.css contains `.styleGrid`, `.teamGrid`, `.styleTileSelected`, `.opponentBanner`, `.confirmButton`
    - UniformSelectionScreen.module.css uses only color literals #1a1a2e/#16213e/#0f3460/#e0e0e0/#a0a0a0 for theme surfaces/text (accent colors set inline in TSX, not in the module)
    - `cd packages/client && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The screen renders 12 teams + 18 style tiles with neutral-then-palette swatches, pre-selects defaultUniformStyle, supports team/style selection and single Confirm, and shows the away opponent banner; client type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Component test for pre-selection, palette re-render, and confirm emit</name>
  <files>packages/client/src/components/UniformSelectionScreen.test.tsx</files>
  <behavior>
    - Rendering as home (playerSlot 1) with no team selected shows all 18 style tiles and a disabled Confirm button
    - Selecting a team pre-selects that team's defaultUniformStyle (its tile has aria-pressed=true)
    - After selecting a team AND a style, clicking "Confirm selection" calls the onConfirm prop with (teamId, styleId) and then the screen shows "Waiting for opponent…"
    - Rendering as away (playerSlot 2) with homePickedTeam set shows that team's card as disabled/struck-out
    - When homeConfirmedStyle is provided (away view), the "Opponent confirmed" banner renders
  </behavior>
  <read_first>
    - packages/client/src/components/TeamSelectionScreen.test.tsx (full — mirror the render harness: how useGameStore playerSlot is mocked/set, testing-library queries, how props are passed, how button clicks are asserted)
    - packages/client/src/components/UniformSelectionScreen.tsx (the component under test — Task 1 output; use its aria-labels/roles/copy for queries)
    - packages/shared/src/teamConfig.ts (a known team's defaultUniformStyle to assert pre-selection, e.g. city → 'pinstripes-vertical')
  </read_first>
  <action>
    Create `UniformSelectionScreen.test.tsx` mirroring TeamSelectionScreen.test.tsx's harness (same testing-library imports, same useGameStore playerSlot setup approach — set playerSlot via the store the way the existing test does). Write tests covering the five behaviors above. Use `vi.fn()` for onConfirm and onSpeedChange. Query style tiles by their aria-label (UNIFORM_STYLE_META name) or by role button + aria-pressed; query the Confirm button by name "Confirm selection". Assert onConfirm was called with the picked teamId and the pre-selected/overridden styleId. For the away banner test, render with playerSlot 2, a homePickedTeam, and a non-null homeConfirmedStyle and assert the role="status" element with text "Opponent confirmed" is present. Do not test SVG pattern internals — assert selection state and callback wiring only.
  </action>
  <verify>
    <automated>cd packages/client && npx vitest run src/components/UniformSelectionScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - UniformSelectionScreen.test.tsx imports and renders `UniformSelectionScreen`
    - A test asserts the Confirm button is disabled before both selections and that clicking it after both selections calls onConfirm with (teamId, styleId)
    - A test asserts selecting a team pre-selects its defaultUniformStyle
    - A test asserts the away struck-out card for homePickedTeam
    - A test asserts the "Opponent confirmed" banner renders when homeConfirmedStyle is set (away view)
    - `cd packages/client && npx vitest run src/components/UniformSelectionScreen.test.tsx` passes
  </acceptance_criteria>
  <done>Component test covers pre-selection, disabled/enabled Confirm, confirm-emit payload, away struck-out card, and opponent banner; the test file passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Screen union, App.tsx handlers/branch, and HexGrid style resolution</name>
  <files>packages/client/src/store/useGameStore.ts, packages/client/src/App.tsx, packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts (the `Screen` union type — add the new member; confirm setScreen needs no change)
    - packages/client/src/App.tsx (full — socket useEffect handler-registration pattern with paired socket.on/socket.off, local state homePickedTeam/selectedSpeed, handleTeamPick emit, screen routing JSX, TeamSelectionScreen import/usage)
    - packages/client/src/components/HexGrid.tsx lines 66–67 (selectedTeams selector), lines 630–640 (resolvedTeamId/teamConfig derivation in the piece loop), lines 815–820 (PieceOverlay uniformStyle + palette props)
    - packages/shared/src/events.ts (UNIFORM_SELECTION_START, UNIFORM_HOME_CONFIRMED, UNIFORM_CONFIRM signatures from 22-01)
  </read_first>
  <action>
    In useGameStore.ts: add `'UNIFORM_SELECTION'` to the `Screen` union (between 'TEAM_SELECTION' and 'GAME_BOARD'), with a Phase 22 comment. No new store actions.
    In App.tsx: import UniformSelectionScreen and the UniformStyleId type. Add local state `const [homeConfirmedStyle, setHomeConfirmedStyle] = useState<UniformStyleId | null>(null);` alongside homePickedTeam. In the socket useEffect, add two named handlers registered with matching cleanup socket.off: `onUniformSelectionStart()` → `setScreen('UNIFORM_SELECTION')`; `onUniformHomeConfirmed(teamId, uniformStyle)` → `setHomeConfirmedStyle(uniformStyle)` (homePickedTeam is already tracked from TEAM_HOME_PICKED). Register `socket.on(ServerEvents.UNIFORM_SELECTION_START, onUniformSelectionStart)` and `socket.on(ServerEvents.UNIFORM_HOME_CONFIRMED, onUniformHomeConfirmed)` with paired offs in the return cleanup. Add `function handleUniformConfirm(teamId: TeamId, uniformStyle: UniformStyleId) { socket.emit(ClientEvents.UNIFORM_CONFIRM, teamId, uniformStyle); }`. Add a screen render branch: when `screen === 'UNIFORM_SELECTION'` render `<UniformSelectionScreen homePickedTeam={homePickedTeam} homeConfirmedStyle={homeConfirmedStyle} onConfirm={handleUniformConfirm} selectedSpeed={selectedSpeed} onSpeedChange={handleSpeedChange} />` (place it in the existing ternary chain, before the TEAM_SELECTION branch).
    In HexGrid.tsx: add a selector `const selectedUniformStyles = useGameStore((s) => s.gameState.selectedUniformStyles);` next to the existing selectedTeams selector (line ~67). In the piece loop where resolvedTeamId/teamConfig are derived (~line 635), add `const resolvedUniformStyle = selectedUniformStyles[displayPiece.teamId];` (D-18). Change the PieceOverlay prop at line ~818 from `uniformStyle={teamConfig.defaultUniformStyle}` to `uniformStyle={resolvedUniformStyle}`. Leave `palette={teamConfig.palette}` unchanged and make no changes to PieceOverlay itself.
  </action>
  <verify>
    <automated>cd packages/client && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - useGameStore.ts Screen union contains `'UNIFORM_SELECTION'`
    - App.tsx imports UniformSelectionScreen and declares `homeConfirmedStyle` state
    - App.tsx registers `ServerEvents.UNIFORM_SELECTION_START` and `ServerEvents.UNIFORM_HOME_CONFIRMED` handlers with matching `socket.off` in cleanup
    - App.tsx contains `socket.emit(ClientEvents.UNIFORM_CONFIRM, teamId, uniformStyle)`
    - App.tsx has a render branch for `screen === 'UNIFORM_SELECTION'` rendering `<UniformSelectionScreen`
    - HexGrid.tsx contains `selectedUniformStyles` selector and `uniformStyle={resolvedUniformStyle}` (no remaining `uniformStyle={teamConfig.defaultUniformStyle}`)
    - `cd packages/client && npx tsc --noEmit` exits 0 and `npx vitest run` passes (existing HexGrid/App tests included)
  </acceptance_criteria>
  <done>Screen union extended; App routes to and emits from the new screen; HexGrid resolves each piece's uniformStyle from gameState.selectedUniformStyles; client type-checks and full client suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The combined uniform selection screen (12 teams flat + 18 style tiles), the deferred build flow (game starts only after both confirm), the away opponent banner, and match-long application of the confirmed style to pieces.</what-built>
  <how-to-verify>
    Run the app with two browser tabs (home = tab that created the room, away = tab that joined):
    1. Both players pick teams as before; after away picks, BOTH tabs land on the uniform selection screen (heading "Home/Away: choose your team + style").
    2. Before selecting a team, confirm the 18 style tiles render in neutral grey (black/white scheme).
    3. Home: pick a team → the 18 tiles re-render in that team's colors and the team's default style tile is highlighted. Pick a different style, then click "Confirm selection" → button becomes "Waiting for opponent…".
    4. Away tab: confirm home's team card is struck-out, and after home confirms an "Opponent confirmed" banner shows home's piece in home's colors. Away picks a team + style and clicks Confirm.
    5. Both tabs advance to the game board. Confirm each team's pieces render in the uniform style that player confirmed (not necessarily the team default), and the opponent's pieces are unaffected.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/flow issues (e.g. wrong pre-selection, banner missing, styles not applied in-match).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                                                         |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| client→server socket | UNIFORM_CONFIRM emitted from this screen carries user-chosen teamId/uniformStyle — validated server-side in 22-02, not trusted here |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                    | Disposition | Mitigation Plan                                                                                                         |
| --------- | --------- | ------------------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-22-07   | Spoofing  | client emitting a team it did not pick / out-of-turn confirm | mitigate    | Server (22-02) re-validates teamId against allow-list and enforces home-first order; client screen is presentation only |
| T-22-08   | Tampering | forged uniformStyle id via crafted emit                      | mitigate    | Server (22-02) VALID_UNIFORM_STYLE_IDS allow-list rejects unknown ids; client only offers ids from UNIFORM_STYLES       |
| T-22-SC   | Tampering | npm/pip/cargo installs                                       | mitigate    | No package installs in this plan — reuses existing UNIFORM_STYLES renderer and badge assets                             |

</threat_model>

<verification>
- `cd packages/client && npx tsc --noEmit` exits 0
- `cd packages/client && npx vitest run` passes (full client suite — UniformSelectionScreen, App, HexGrid tests)
- Two-tab human checkpoint confirms the full flow and in-match style application
</verification>

<success_criteria>

- Uniform selection screen renders all styles in the team palette with defaultUniformStyle pre-selected (UNIFORM-02/03)
- Confirm emits UNIFORM_CONFIRM; HexGrid applies the confirmed style to pieces for the whole match (UNIFORM-04)
- Away sees struck-out home team + opponent banner; both-confirm gating advances to the game
- Client type-checks, full client vitest suite passes, human checkpoint approved
  </success_criteria>

<output>
Create `.planning/phases/22-uniform-selection-screen/22-03-SUMMARY.md` when done
</output>
