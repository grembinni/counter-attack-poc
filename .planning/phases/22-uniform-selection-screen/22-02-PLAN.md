---
phase: 22-uniform-selection-screen
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - packages/server/src/roomHandlers.ts
  - packages/server/src/roomStore.ts
autonomous: true
requirements: [UNIFORM-04]
must_haves:
  truths:
    - "Away's TEAM_PICK no longer builds game state — it stores awayPickedTeam and broadcasts UNIFORM_SELECTION_START"
    - 'A UNIFORM_CONFIRM handler enforces home-first order, allow-list validation, and isProcessing mutex'
    - "Home's UNIFORM_CONFIRM broadcasts UNIFORM_HOME_CONFIRMED; away's UNIFORM_CONFIRM builds game state with both teams + both styles"
    - 'buildInitialGameState is called with the 4-arg signature including selectedUniformStyles'
  artifacts:
    - path: 'packages/server/src/roomHandlers.ts'
      provides: 'UNIFORM_CONFIRM handler + deferred-build TEAM_PICK away branch'
      contains: 'UNIFORM_CONFIRM'
    - path: 'packages/server/src/roomStore.ts'
      provides: 'Room.homePickedUniformStyle + Room.awayPickedTeam fields'
      contains: 'homePickedUniformStyle'
  key_links:
    - from: 'packages/server/src/roomHandlers.ts'
      to: 'packages/server/src/gameEngine.ts'
      via: 'buildInitialGameState 4-arg call in UNIFORM_CONFIRM away branch'
      pattern: "buildInitialGameState\\("
    - from: 'packages/server/src/roomHandlers.ts'
      to: 'packages/shared/src/events.ts'
      via: 'UNIFORM_SELECTION_START + UNIFORM_HOME_CONFIRMED emits'
      pattern: 'UNIFORM_(SELECTION_START|HOME_CONFIRMED)'
---

<objective>
Rewire the server so game state is built only after both players confirm team + uniform style (deferred build, D-13). Add `awayPickedTeam` + `homePickedUniformStyle` to the Room type, change the away `TEAM_PICK` branch to broadcast `UNIFORM_SELECTION_START` instead of building state, and add a `UNIFORM_CONFIRM` handler that enforces home-first order, validates against allow-lists, uses the isProcessing mutex, broadcasts `UNIFORM_HOME_CONFIRMED` on home's confirm, and builds the initial game state with both teams and both uniform styles on away's confirm.

Purpose: Server half of the confirm flow (UNIFORM-04 — confirmed choice stored in game state).
Output: roomHandlers.ts + roomStore.ts compiling; UNIFORM_CONFIRM flow functional and guarded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-uniform-selection-screen/22-CONTEXT.md
@.planning/phases/22-uniform-selection-screen/22-PATTERNS.md
</context>

<artifacts_this_phase_produces>
See 22-01-PLAN.md `<artifacts_this_phase_produces>` for the full Phase 22 symbol list. This plan adds:

- `Room.homePickedUniformStyle?: UniformStyleId` (roomStore.ts)
- `Room.awayPickedTeam?: TeamId` (roomStore.ts)
- `VALID_UNIFORM_STYLE_IDS` — module-level allow-list const (roomHandlers.ts)
- `UNIFORM_CONFIRM` socket handler (roomHandlers.ts)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add awayPickedTeam + homePickedUniformStyle to Room type</name>
  <files>packages/server/src/roomStore.ts</files>
  <read_first>
    - packages/server/src/roomStore.ts (read the `Room` type around lines 45–81, especially the existing optional `homePickedTeam?: TeamId` and `gameSpeed?: GameSpeed` fields, and the room-construction default literal around line 129 where isProcessing:false is set)
    - packages/shared/src/uniformStyles.ts (confirm UniformStyleId export for the import)
  </read_first>
  <action>
    Add two optional fields to the `Room` type, following the JSDoc + optional-field style of the existing `homePickedTeam?: TeamId`:
    `awayPickedTeam?: TeamId;` (comment: Phase 22 D-13 — away's team stored on second TEAM_PICK; game state deferred) and
    `homePickedUniformStyle?: UniformStyleId;` (comment: Phase 22 D-15 — set on home's UNIFORM_CONFIRM; presence gates away's confirm branch).
    Ensure `UniformStyleId` and `TeamId` are imported into roomStore.ts from '@counter-attack/shared'. Do not change the room-construction default literal — both fields stay undefined until set by handlers.
  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - roomStore.ts Room type contains `awayPickedTeam?: TeamId`
    - roomStore.ts Room type contains `homePickedUniformStyle?: UniformStyleId`
    - roomStore.ts imports `UniformStyleId` from '@counter-attack/shared'
    - `cd packages/server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Room type carries both new optional fields; server package type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Defer game-state build in TEAM_PICK away branch</name>
  <files>packages/server/src/roomHandlers.ts</files>
  <read_first>
    - packages/server/src/roomHandlers.ts lines 177–244 (the whole TEAM_PICK handler — the home branch broadcasting TEAM_HOME_PICKED and the away branch currently building game state via buildInitialGameState and broadcastState)
    - packages/shared/src/events.ts (confirm ServerEvents.UNIFORM_SELECTION_START name added in 22-01)
  </read_first>
  <action>
    In the `TEAM_PICK` handler's `else` branch (away pick, currently lines ~210–240), keep the slot-2 guard (`playerSlot !== 2` → GAME_ERROR 'WRONG_TURN') and the duplicate-team guard (`teamId === room.homePickedTeam` → GAME_ERROR 'TEAM_ALREADY_PICKED'). Replace the game-state build (the `selectedTeams` literal, the buildInitialGameState try/catch, `room.gameState = gameState`, and `broadcastState(io, room)`) with: set `room.awayPickedTeam = teamId;` then `io.to(roomCode).emit(ServerEvents.UNIFORM_SELECTION_START);` (no payload — D-13). Leave the home branch (homePickedTeam undefined → TEAM_HOME_PICKED) unchanged. The isProcessing mutex try/finally wrapping stays intact.
    The `buildInitialGameState` import may become unused in this branch — it is re-used by the UNIFORM_CONFIRM handler in Task 3, so keep the import.
  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - roomHandlers.ts TEAM_PICK away branch contains `room.awayPickedTeam = teamId`
    - roomHandlers.ts TEAM_PICK away branch contains `io.to(roomCode).emit(ServerEvents.UNIFORM_SELECTION_START)`
    - roomHandlers.ts TEAM_PICK away branch no longer calls `broadcastState` and no longer calls `buildInitialGameState`
    - roomHandlers.ts still retains the `playerSlot !== 2` and `TEAM_ALREADY_PICKED` guards in the away branch
    - `cd packages/server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Away's TEAM_PICK stores the away team and broadcasts UNIFORM_SELECTION_START without building game state.</done>
</task>

<task type="auto">
  <name>Task 3: Add UNIFORM_CONFIRM handler with guards + deferred build</name>
  <files>packages/server/src/roomHandlers.ts</files>
  <read_first>
    - packages/server/src/roomHandlers.ts lines 39–56 (VALID_TEAM_IDS + VALID_GAME_SPEEDS allow-list pattern), lines 177–244 (TEAM_PICK handler — isProcessing mutex try/finally, home-first slot enforcement, buildInitialGameState try/catch error guard, broadcastState call), lines 247–284 (TEAM_SPEED_SET as a second allow-list handler example)
    - packages/shared/src/events.ts (ClientEvents.UNIFORM_CONFIRM, ServerEvents.UNIFORM_HOME_CONFIRMED signatures from 22-01)
    - packages/server/src/gameEngine.ts lines 149–153 (new 4-arg buildInitialGameState signature from 22-01)
    - packages/shared/src/uniformStyles.ts (UNIFORM_STYLE_META keys are the valid UniformStyleId values for the allow-list)
  </read_first>
  <action>
    Add a module-level allow-list constant near VALID_TEAM_IDS: `VALID_UNIFORM_STYLE_IDS: readonly UniformStyleId[] = Object.keys(UNIFORM_STYLE_META) as UniformStyleId[];` (import UNIFORM_STYLE_META and UniformStyleId from '@counter-attack/shared').
    Register a new `socket.on(ClientEvents.UNIFORM_CONFIRM, (teamId, uniformStyle) => { ... })` handler inside the `if (!reconnectOnly)` block, after the TEAM_SPEED_SET handler. Structure it exactly like TEAM_PICK:
    - Read roomCode from socket.data; return if undefined. getRoom; return if falsy.
    - isProcessing mutex: `if (room.isProcessing) return; room.isProcessing = true; try { ... } finally { room.isProcessing = false; }`.
    - Inside try: allow-list validate BOTH `teamId` against VALID_TEAM_IDS (GAME_ERROR 'INVALID_TEAM') and `uniformStyle` against VALID_UNIFORM_STYLE_IDS (GAME_ERROR 'INVALID_STYLE').
    - Home-first enforcement keyed on `room.homePickedUniformStyle === undefined`:
      - Home branch: require `playerSlot === 1` (else GAME_ERROR 'WRONG_TURN'). Set `room.homePickedUniformStyle = uniformStyle;` and `io.to(roomCode).emit(ServerEvents.UNIFORM_HOME_CONFIRMED, teamId, uniformStyle);`.
      - Away branch (else): require `playerSlot === 2` (else GAME_ERROR 'WRONG_TURN'). Build the game state: `const selectedTeams = { home: room.homePickedTeam!, away: teamId };` and `const selectedUniformStyles = { home: room.homePickedUniformStyle, away: uniformStyle };`. Call `buildInitialGameState(roomCode, selectedTeams, room.gameSpeed ?? 'standard', selectedUniformStyles)` inside a try/catch that emits GAME_ERROR 'SERVER_ERROR' and returns on throw (mirror the TEAM_PICK CR-03 guard). On success set `room.gameState = gameState;` and call `broadcastState(io, room);`.
    Do not add any new GamePhase; this handler only builds the existing KICK_OFF_SETUP initial state via the engine.
  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - roomHandlers.ts contains `VALID_UNIFORM_STYLE_IDS`
    - roomHandlers.ts registers `socket.on(ClientEvents.UNIFORM_CONFIRM,`
    - The UNIFORM_CONFIRM handler validates teamId against VALID_TEAM_IDS and uniformStyle against VALID_UNIFORM_STYLE_IDS
    - The handler contains `room.homePickedUniformStyle = uniformStyle` and `io.to(roomCode).emit(ServerEvents.UNIFORM_HOME_CONFIRMED, teamId, uniformStyle)` in the home branch
    - The away branch calls `buildInitialGameState(roomCode, selectedTeams, room.gameSpeed ?? 'standard', selectedUniformStyles)` and `broadcastState(io, room)`
    - The handler uses the `if (room.isProcessing) return; room.isProcessing = true; try { ... } finally { room.isProcessing = false; }` mutex
    - `cd packages/server && npx tsc --noEmit` exits 0 and `npx vitest run` passes
  </acceptance_criteria>
  <done>UNIFORM_CONFIRM handler enforces order + allow-lists under the mutex; home confirm broadcasts, away confirm builds and broadcasts the game state with both uniform styles.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                                           |
| -------------------- | --------------------------------------------------------------------------------------------------------------------- |
| client→server socket | UNIFORM_CONFIRM (teamId, uniformStyle) is untrusted client input crossing into room state and game-state construction |

## STRIDE Threat Register

| Threat ID | Category               | Component                                         | Disposition | Mitigation Plan                                                                                                         |
| --------- | ---------------------- | ------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-22-03   | Tampering              | UNIFORM_CONFIRM teamId/uniformStyle payload       | mitigate    | Allow-list validation against VALID_TEAM_IDS and VALID_UNIFORM_STYLE_IDS; reject with GAME_ERROR before any state write |
| T-22-04   | Elevation of Privilege | out-of-turn confirm (away confirming before home) | mitigate    | Home-first enforcement keyed on room.homePickedUniformStyle + playerSlot guard emitting WRONG_TURN                      |
| T-22-05   | Denial of Service      | concurrent UNIFORM_CONFIRM double-fire race       | mitigate    | room.isProcessing mutex try/finally wraps the whole handler (mirrors TEAM_PICK)                                         |
| T-22-06   | Denial of Service      | buildInitialGameState throw crashing Node process | mitigate    | try/catch around buildInitialGameState emits GAME_ERROR 'SERVER_ERROR' and returns                                      |
| T-22-SC   | Tampering              | npm/pip/cargo installs                            | mitigate    | No package installs in this plan                                                                                        |

</threat_model>

<verification>
- `cd packages/server && npx tsc --noEmit` exits 0
- `cd packages/server && npx vitest run` passes (full server suite, confirming no regression in existing room/team-pick tests)
</verification>

<success_criteria>

- Room type carries awayPickedTeam + homePickedUniformStyle
- TEAM_PICK away branch defers build and broadcasts UNIFORM_SELECTION_START
- UNIFORM_CONFIRM handler validated, guarded, mutex-protected; away confirm builds game state with both styles
- Server package type-checks and full vitest suite passes
  </success_criteria>

<output>
Create `.planning/phases/22-uniform-selection-screen/22-02-SUMMARY.md` when done
</output>
