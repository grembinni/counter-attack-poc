---
phase: 35-actionpanel-log-standardization
verified: 2026-07-27T18:30:00Z
status: human_needed
score: 23/23 must-haves verified (programmatic evidence)
overrides_applied: 0
human_verification:
  - test: 'Play through a full match (or use existing dev tooling to step through phases) and visually confirm every ActionPanel/ActionLog-area container reads as visually distinct from the pitch background now that all frame borders are removed (ActionLog, ActionPanel confirmCard, FreeKickSetupPanel confirmCard, ReplayPanel, SideLog collapsed/expanded strip).'
    expected: "Each panel is still legible and visually separated from the pitch by its background color alone (var(--color-bg-surface)) — no panel looks like it 'floats' or bleeds into the board with no border."
    why_human: 'Border removal (D-01/PANEL-02) is a purely visual affordance change; grep/stylelint can confirm the declaration is gone but cannot confirm the resulting rendered contrast is still legible on an actual screen.'
  - test: "Step through at least 8-10 of ActionPanel's ~18 phase states (MOVE, HIGH_PASS_MOVE, GK_RESTART, HEADER contest, PASS chooser, PASS target prompt, FREE_MOVE, SNAPSHOT_DEFLECT, a waiting/non-active-player state, KickOffSetupPanel, FreeKickSetupPanel) and confirm the 'Actions' heading, two-line helper block, and Confirm/Confirmed button read naturally and consistently."
    expected: 'Every state shows one heading, one short title line, one detail line, and a single Confirm-labeled CTA that changes color (orange while an eligible player remains, green when ready) exactly as the phase-specific waiting text and terminology (Keeper, not GK/Goalie) describe.'
    why_human: "Tests assert exact strings and class names in isolation per phase; only a human reading the panel in sequence across a real game flow can judge whether the unified format/tone genuinely reads as 'one consistent system' rather than merely passing per-phase assertions."
  - test: "Confirm the CTA color transition (orange->green) is visually distinguishable against the new charcoal/graphite background for all five newly-converted phases (HIGH_PASS_MOVE, FIRST_TIME_PASS_MOVE, SNAPSHOT_DEFLECT, GK_KICK_MOVE, FREE_MOVE) plus FreeKickSetupPanel's Confirm button, matching the pre-existing MOVE/HEADER look."
    expected: 'The pending (orange) and ready (green) states are as visually obvious in these five phases as they already are in MOVE/HEADER.'
    why_human: 'Color rendering and perceptual distinctness cannot be verified by source/class-name assertions alone.'
---

# Phase 35: ActionPanel & Log Standardization Verification Report

**Phase Goal:** ActionPanel and ActionLog present one consistent visual and language system — format, borders, buttons, and terminology — across every game phase.
**Verified:** 2026-07-27T18:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Roadmap Success Criteria (PANEL-01..04) plus the plan-level must-haves from all 5 sub-plans (35-01..35-05) were merged into one truth set. All 23 are programmatically VERIFIED against the current codebase (not SUMMARY claims) via direct grep/read of the source files and a full client test/typecheck/stylelint run. No FAILED truths were found. Three items require a human to actually look at the rendered UI before the phase can be marked fully `passed` (see Human Verification Required).

| #   | Truth                                                                                                                                    | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | (SC1/PANEL-01) ActionPanel help text follows one consistent format and tone across every game phase, no per-phase drift                  | ✓ VERIFIED | All 18 `<PanelShell>` render sites in `ActionPanel.tsx` emit exactly one heading (`grep -c 'styles.panelHeading'` = 1, `grep -c '<PanelShell'` = 18). `grep -c styles.helperLine1` == `grep -c styles.helperLine2` (17 = 17) — every title line has a paired detail line, no bare `.phaseLabel` outliers remain (`.phaseLabel`/`.gkLabel` removed from `ActionPanel.module.css`).                                                                                                                                                                                                                             |
| 2   | (SC2/PANEL-02) No component in the ActionPanel/ActionLog area is framed with a border                                                    | ✓ VERIFIED | `ActionLog.module.css` `.panel` — no border. `ActionPanel.module.css` `.confirmCard` — no border (only `.backButton`'s exempt button border at line 57 remains). `FreeKickSetupPanel.module.css` `.confirmCard` — no border. `ReplayPanel.module.css` `.panel` — no border. `GameBoard.module.css` `.sideLogCollapsed`/`.sideLogExpanded` — no `border-right` (verified lines 312-331); `.sideLogHeader`'s internal `border-bottom` divider deliberately kept (documented, not a frame). `KickOffSetupPanel.module.css`/`FreeKickSetupPanel.module.css` `.panel` were already border-free.                    |
| 3   | (SC3/PANEL-03) Every ActionPanel button shares consistent sizing, color-state logic, and interaction behavior                            | ✓ VERIFIED | All 28 `<button>` elements in `ActionPanel.tsx` compose `styles.ctaButton` (or `styles.backButton`) as their base class — confirmed by direct read of every button's className. All 7 confirm-and-advance CTAs (`HIGH_PASS_MOVE`, `FIRST_TIME_PASS_MOVE`, `SNAPSHOT_DEFLECT`, `GK_KICK_MOVE`, `FREE_MOVE_*`, `HEADER`, `MOVE`) call `ctaClass(...)` → shared `ctaColorClass()` (`grep -c 'ctaClass('` = 7). `FreeKickSetupPanel` calls the same `ctaColorClass` directly (second real consumer, `grep -c 'ctaColorClass('` = 1). `ctaColorClass.ts` is a zero-import pure function with 7 passing unit tests. |
| 4   | (SC4/PANEL-04) Terminology and phrasing are consistent across every ActionPanel/ActionLog state                                          | ✓ VERIFIED | `grep -ci "goalie\|goalkeeper"` = 0 in both `ActionPanel.tsx` and `ActionLog.tsx`. `grep -c "End Turn"` = 0 and `grep -c "Confirm Selection"` = 0 in `ActionPanel.tsx`; `Confirm`/`Confirmed`/`Yes, end turn` verbs consistently applied across `ActionPanel.tsx`, `FreeKickSetupPanel.tsx`, `KickOffSetupPanel.tsx`. `ActionLog.tsx`: 0 ALL-CAPS narration leaks (`SCORED!/ACCURATE/ATTACKER WINS/...` = 0), 0 ASCII `-> ` arrows, `RESULT_LABEL`/`SHOT_OUTCOME_LABEL`/`HEADER_RESULT_LABEL` typed lookup maps in place, `prefix="K"` (not `"GK"`) on keeper labels, "keeper out of range" wording.          |
| 5   | (35-01) Single shared function decides CTA ready/pending color for every ActionPanel-slot panel                                          | ✓ VERIFIED | `ctaColorClass.ts` exists, zero imports, exported once; consumed by `ActionPanel.tsx` (7 sites via `ctaClass` adapter) and `FreeKickSetupPanel.tsx` (1 site).                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 6   | (35-01) ActionPanel's End Turn/Confirm turns orange whenever an unmoved eligible player remains, every repositioning phase               | ✓ VERIFIED | 5 previously-hardcoded-green phases (`HIGH_PASS_MOVE`, `FIRST_TIME_PASS_MOVE`, `SNAPSHOT_DEFLECT`, `GK_KICK_MOVE`, `FREE_MOVE_*`) now route through `ctaClass(<phase's own remaining>)`; full test suite includes 10 two-way pending/ready assertions for these phases (all pass).                                                                                                                                                                                                                                                                                                                            |
| 7   | (35-01) ActionPanel no longer contains an unreachable FREE_KICK_SETUP block                                                              | ✓ VERIFIED | `grep -c "FREE_KICK_SETUP"` on `ActionPanel.tsx` = 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 8   | (35-02) Match log panel shows exactly one heading ("MATCH LOG")                                                                          | ✓ VERIFIED | `grep -c "ACTION LOG"` / `panelHeader` on `ActionLog.tsx`/`.module.css` = 0; `GameBoard.tsx`'s `SideLog` `<span>MATCH LOG</span>` (line 127) is the sole remaining heading.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 9   | (35-02) Match log panel not framed by a border                                                                                           | ✓ VERIFIED | `ActionLog.module.css` `.panel` has no `border` declaration (only `border-radius`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 10  | (35-02) No log entry renders ALL-CAPS narration or a raw enum value                                                                      | ✓ VERIFIED | `grep -cE "SCORED!                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | ACCURATE          | ATTACKER WINS | DEFENDER WINS                                                                                                           | LOOSE BALL \(tie\) | CONTESTING HEADER"`= 0 on`ActionLog.tsx`; `RESULT_LABEL[`/`SHOT_OUTCOME_LABEL[`/`HEADER_RESULT_LABEL[` lookups present (4/2/1 occurrences respectively). |
| 11  | (35-02) Every directional/result arrow is unicode →                                                                                      | ✓ VERIFIED | `grep -c -- "'-> '"` = 0 on `ActionLog.tsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 12  | (35-02) Every binary-outcome log event carries ✓/✗; informational events carry none                                                      | ✓ VERIFIED | `[HANDLING ✓]`/`[HANDLING ✗]` glyph now present (line 535); `[DEFLECT MOVE]` (glyph-free rename, line 810) no longer collides with `DEFLECT_ATTEMPT`'s `[DEFLECT ✓/✗]`. Full 34-row audit table recorded in 35-02-SUMMARY.md.                                                                                                                                                                                                                                                                                                                                                                                 |
| 13  | (35-02) Goalkeeper never called "GK"/"Goalie"/"Goalkeeper" in ActionLog                                                                  | ✓ VERIFIED | `prefix="K"` (line 754), "Goal — keeper out of range" (line 505); `grep -ci "goalie\|goalkeeper"` = 0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 14  | (35-02) Raw axial hex coordinates unchanged (D-13)                                                                                       | ✓ VERIFIED | No coordinate-rendering lines were touched by plans 35-01..05; confirmed by reading `formatEvent`'s MOVE/pass/kick cases — coordinate interpolations are untouched from pre-phase form.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 15  | (35-03) No panel sharing the ActionPanel slot is framed by a border, nor is the side-log wrapper                                         | ✓ VERIFIED | See truth #2 above — `ReplayPanel`, `FreeKickSetupPanel` confirmCard, and both `SideLog` states confirmed border-free.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 16  | (35-03) Confirm-and-advance button reads "Confirm" in KickOffSetupPanel and FreeKickSetupPanel                                           | ✓ VERIFIED | `grep -c "Ready$\|>Ready<"` on `KickOffSetupPanel.tsx` = 0; `grep -c ">End Turn<\|End Turn$"` on `FreeKickSetupPanel.tsx` = 0; both files' CTA reads `Confirm`.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 17  | (35-03) FreeKickSetupPanel derives CTA color from the shared helper, not a second implementation                                         | ✓ VERIFIED | `endTurnColorClass` is now assigned from a single `ctaColorClass(...)` call (line 174); `grep -c "ctaColorClass("` = 1; local ready/pending ternary deleted.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 18  | (35-03) KickOffSetupPanel's waiting state names what it is waiting for                                                                   | ✓ VERIFIED | `grep -c "Waiting for opponent"` on `KickOffSetupPanel.tsx` = 0; `grep -c "Waiting for the opponent to confirm their placement"` = 1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 19  | (35-03) All four slot panels label their heading with the same CSS class name                                                            | ✓ VERIFIED | `ActionPanel`, `KickOffSetupPanel`, `FreeKickSetupPanel`, `ReplayPanel` all use `styles.panelHeading` (ReplayPanel's `.heading` renamed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 20  | (35-04) ActionPanel shows a heading in every phase state                                                                                 | ✓ VERIFIED | 18/18 render sites wrapped in `<PanelShell>`; `grep -c '<div className={styles.panel}>'` = 0 (only `PanelShell`'s own implementation references `styles.panel`).                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 21  | (35-04) ActionPanel's confirm-and-advance button reads "Confirm" in every phase; confirmCard border-free; no Goalie/Goalkeeper wording   | ✓ VERIFIED | `grep -cE "End Turn                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Confirm Selection | Goalie        | Goalkeeper"`on`ActionPanel.tsx`= 0;`.confirmCard`border removed (only`.backButton`'s button border remains at line 57). |
| 22  | (35-05) Every ActionPanel waiting state names who is acting and what they are doing                                                      | ✓ VERIFIED | `grep -c "Waiting for opponent"` on `ActionPanel.tsx` = 0; 13 phase-specific detail strings present via `waitingPanel(detail)` (14 occurrences: 1 declaration + 13 call sites).                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 23  | (35-05) Every ActionPanel phase state presents help text in the same two-line title+detail shape; waiting markup written once and reused | ✓ VERIFIED | `helperLine1` count (17) == `helperLine2` count (17); `waitingHelperBlock` appears 3 times (declaration, `waitingPanel` body, HEADER `myConfirmed` branch reuse); `.phaseLabel`/`.gkLabel` removed from CSS with `GameBoard.module.css`'s own identically-named `.phaseLabel` confirmed untouched.                                                                                                                                                                                                                                                                                                            |

**Score:** 23/23 truths verified against the codebase (programmatic evidence). Full client test suite (472/472), typecheck, stylelint, and the targeted grep/read audits above corroborate every claim in the five plan SUMMARYs. Visual/UX rendering itself (border removal legibility, color-transition perceptibility, cross-phase "reads as one system" judgment) has not been confirmed by a human against the running app — see Human Verification Required.

### Required Artifacts

| Artifact                                                              | Expected                                                                                                           | Status     | Details                                                                                    |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------ |
| `packages/client/src/utils/ctaColorClass.ts`                          | Zero-import pure CTA color-state selector                                                                          | ✓ VERIFIED | Exists, 1 exported function, 0 imports, docblock present.                                  |
| `packages/client/src/utils/ctaColorClass.test.ts`                     | Unit coverage for ready/pending/disabled branches                                                                  | ✓ VERIFIED | 7 tests, all pass.                                                                         |
| `packages/client/src/components/ActionPanel.tsx`                      | PanelShell heading wrapper, Confirm verb, Keeper terminology, ctaColorClass consumer, dead FREE_KICK_SETUP removed | ✓ VERIFIED | All confirmed via direct grep/read (see truths above).                                     |
| `packages/client/src/components/ActionPanel.module.css`               | `.panelHeading` class; border-free `.confirmCard`                                                                  | ✓ VERIFIED | Present; `.confirmCard` border removed; only `.backButton`'s exempt button border remains. |
| `packages/client/src/components/ActionLog.tsx`                        | formatEvent with sentence-case narration, unicode arrows, glyph rule, Keeper terminology, no internal header       | ✓ VERIFIED | Confirmed via grep audit; label maps present.                                              |
| `packages/client/src/components/ActionLog.module.css`                 | Border-free `.panel`; `.panelHeader` removed                                                                       | ✓ VERIFIED | Confirmed — whole file is 24 lines, no border/panelHeader rule.                            |
| `packages/client/src/components/FreeKickSetupPanel.tsx`/`.module.css` | Confirm verb, ctaColorClass consumer, border-free confirmCard                                                      | ✓ VERIFIED | Confirmed.                                                                                 |
| `packages/client/src/components/KickOffSetupPanel.tsx`                | Confirm verb, context-specific waiting state                                                                       | ✓ VERIFIED | Confirmed; new `KickOffSetupPanel.test.tsx` (6 tests) exists and passes.                   |
| `packages/client/src/components/ReplayPanel.tsx`/`.module.css`        | Border-free `.panel`; `panelHeading` class                                                                         | ✓ VERIFIED | Confirmed.                                                                                 |
| `packages/client/src/components/GameBoard.module.css`                 | Border-free side-log wrapper (both states)                                                                         | ✓ VERIFIED | Confirmed; `.sideLogHeader`'s internal divider deliberately kept.                          |

### Key Link Verification

| From                     | To                                                     | Via                                                                | Status  | Details                                                                                                                                                                               |
| ------------------------ | ------------------------------------------------------ | ------------------------------------------------------------------ | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ActionPanel.tsx`        | `utils/ctaColorClass.ts`                               | named import + `ctaClass` adapter                                  | ✓ WIRED | `import { ctaColorClass } from '../utils/ctaColorClass.js'`; 7 `ctaClass(` call sites.                                                                                                |
| `FreeKickSetupPanel.tsx` | `utils/ctaColorClass.ts`                               | named import replacing local ternary                               | ✓ WIRED | 1 `ctaColorClass(` call site; `endTurnColorClass` local ternary deleted.                                                                                                              |
| `GameBoard.tsx`          | `ActionLog.tsx`                                        | SideLog wrapper renders ActionLog beneath its own MATCH LOG header | ✓ WIRED | `<ActionLog />` rendered inside `SideLog()`; MATCH LOG is the sole heading.                                                                                                           |
| `GameBoard.tsx`          | `ActionPanel`/`KickOffSetupPanel`/`FreeKickSetupPanel` | phase-conditional `topBandRight`                                   | ✓ WIRED | Confirmed `KICK_OFF_SETUP`→`KickOffSetupPanel`, `FREE_KICK_SETUP`→`FreeKickSetupPanel`, else→`ActionPanel`; `ActionPanel`'s own dead FK branch removed, consistent with this routing. |
| `ReplayPanel.tsx`        | `ReplayPanel.module.css`                               | `styles.panelHeading` replaces `styles.heading`                    | ✓ WIRED | Confirmed rename on both sides.                                                                                                                                                       |

### Behavioral Spot-Checks

| Behavior                                                                        | Command                                          | Result                         | Status |
| ------------------------------------------------------------------------------- | ------------------------------------------------ | ------------------------------ | ------ |
| Full client test suite passes (includes all D-01..D-13/PANEL-01..04 assertions) | `pnpm --filter @counter-attack/client test`      | 25 files, 472/472 tests passed | ✓ PASS |
| Client typecheck is clean                                                       | `pnpm --filter @counter-attack/client typecheck` | exit 0, no output              | ✓ PASS |
| CSS modules pass stylelint (no hardcoded literals, declaration-strict-value)    | `pnpm stylelint`                                 | exit 0, no output              | ✓ PASS |
| `ctaColorClass` unit tests                                                      | (included in full suite)                         | 7/7 passed                     | ✓ PASS |
| `KickOffSetupPanel.test.tsx` (new file this phase)                              | (included in full suite)                         | 6/6 passed                     | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan                | Description                                                                              | Status      | Evidence                                                                                                                                                         |
| ----------- | -------------------------- | ---------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PANEL-01    | 35-02, 35-05               | ActionPanel/ActionLog help text follows one consistent format and tone                   | ✓ SATISFIED | Two-line helperBlock uniform across all render sites; ActionLog language unified (sentence case, single arrow glyph).                                            |
| PANEL-02    | 35-01, 35-02, 35-03, 35-04 | No component in the ActionPanel/ActionLog area uses a border for framing                 | ✓ SATISFIED | All 6 container borders removed across ActionLog/ActionPanel/FreeKickSetupPanel/ReplayPanel/GameBoard SideLog; button borders explicitly and correctly retained. |
| PANEL-03    | 35-01, 35-03               | All ActionPanel buttons share consistent sizing, color-state logic, interaction behavior | ✓ SATISFIED | Single `ctaColorClass` helper with two real consumers (`ActionPanel`, `FreeKickSetupPanel`); all buttons share `styles.ctaButton` base class.                    |
| PANEL-04    | 35-02, 35-03, 35-04, 35-05 | Terminology and phrasing are consistent across all ActionPanel/ActionLog states          | ✓ SATISFIED | Single "Confirm"/"Confirmed"/"Yes, end turn" verb set; single "Keeper" term; unified glyph/casing/arrow rules in ActionLog.                                      |

No orphaned requirements: REQUIREMENTS.md's traceability table maps all four PANEL-0x IDs to Phase 35 and no other phase claims them. **Documentation gap (not a code gap):** REQUIREMENTS.md still shows PANEL-01..04 as unchecked `[ ]` / status "Pending" (lines 25-28, 79-82), and STATE.md's phase table still lists Phase 34/35 as "Not started" — both are stale relative to the actual completed work (5 plans executed, merged, and code-reviewed per git log through `7bbe955`). This should be corrected as part of milestone close-out; it does not indicate any missing implementation.

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX` debt markers found in any file modified by this phase. Code review (`35-REVIEW.md`, 2026-07-27) found 0 critical issues, 5 warnings, 3 info items — all non-blocking and independently re-confirmed present in the current source during this verification pass:

| File                                         | Line                      | Pattern                                                                                                                     | Severity   | Impact                                                                                                                                                                                    |
| -------------------------------------------- | ------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ActionLog.tsx`                              | 309-314, 448, 888         | `isGoal` field computed on every branch, never consumed at the render call site                                             | ℹ️ Info    | Dead field, no visible/functional effect — does not affect PANEL-01..04 goal.                                                                                                             |
| `ActionPanel.tsx`                            | 703-732                   | `actionCount` (wide-layout trigger) doesn't gate `MOVEMENT`/`STANDARD_PASS` by `!isKickOff` the way the rendered buttons do | ⚠️ Warning | Currently does not flip the `wide` threshold (still below 5) but is a latent miscalculation risk if `ELIGIBLE_NEXT_ACTIONS` changes. Does not affect the phase's observable truths today. |
| `ActionPanel.tsx`                            | 276-286, 395-422, 605-629 | `canUndo`'s `moveTypeForPhase` maps two phases (`GK_KICK_MOVE`, `SNAPSHOT_DEFLECT`) that never render an Undo button        | ⚠️ Warning | Pre-existing dead-code-or-missing-feature ambiguity, not introduced by this phase and not part of PANEL-01..04's scope.                                                                   |
| `KickOffSetupPanel.tsx`                      | 76-84, 131-138            | Disabled-button tooltip doesn't reflect which of two constraints actually failed                                            | ⚠️ Warning | Cosmetic tooltip-only defect; the on-panel status rows show correct per-constraint state.                                                                                                 |
| `KickOffSetupPanel.tsx`                      | 25, 86-89, 119-140        | `localReady`/"Confirmed" state is client-only and can desync from server truth on remount/reconnect                         | ⚠️ Warning | Pre-existing architecture gap (server doesn't broadcast per-team readiness); not introduced by this phase's wording changes.                                                              |
| `ActionPanel.tsx` / `FreeKickSetupPanel.tsx` | various                   | `withEndTurnConfirm`/confirm-dialog JSX duplicated near-verbatim across two files                                           | ℹ️ Info    | The same phase's `ctaColorClass` extraction pattern wasn't applied here; a future DRY opportunity, not a functional defect.                                                               |
| `ActionLog.tsx`                              | 871, 890                  | Array index used as React `key` in the render list                                                                          | ℹ️ Info    | Currently harmless (append-only, whole-list re-render); fragile if reordering is ever introduced.                                                                                         |

None of these anti-patterns contradict or undermine any of the 23 verified truths above — they are pre-existing or newly-surfaced code-quality observations orthogonal to the format/border/button/terminology consistency goal.

### Human Verification Required

See frontmatter `human_verification` for the structured form. Summary: this phase is entirely a visual/UX consistency refactor (borders, headings, button color states, wording) across ~4 components and ~18+ phase states. Every claim has strong programmatic evidence (grep-level source assertions plus a green 472-test suite, clean typecheck, clean stylelint), but — consistent with how Phase 34 (also a visual-only phase) was verified — actually looking at the rendered game across a representative set of phases has not yet happened for this phase (no `35-UAT.md` exists). Three specific checks are requested:

1. Border removal doesn't leave any panel looking visually "unframed"/illegible against the pitch.
2. The unified heading/two-line-helper/Confirm-verb/Keeper-terminology system reads naturally when stepping through a real sequence of phases, not just individually-asserted strings.
3. The orange→green CTA color transition is visually obvious in the five newly-converted phases, matching the pre-existing MOVE/HEADER look.

### Gaps Summary

No FAILED truths. No missing artifacts. No broken key links. All 472 client tests pass; typecheck and stylelint are clean. The only outstanding items are (a) three human visual-confirmation checks appropriate for a UI-only phase with no prior UAT run, and (b) a documentation-only staleness gap in REQUIREMENTS.md/STATE.md's tracking tables (not a code gap — the requirement IDs are correctly mapped to this phase, just not yet marked complete/checked).

---

_Verified: 2026-07-27T18:30:00Z_
_Verifier: Claude (gsd-verifier)_
