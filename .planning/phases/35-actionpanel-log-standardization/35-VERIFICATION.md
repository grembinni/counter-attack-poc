---
phase: 35-actionpanel-log-standardization
verified: 2026-07-27T21:54:51Z
status: human_needed
score: 27/27 must-haves verified (programmatic evidence)
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 23/23 (initial phase verification, pre-UAT)
  gaps_closed:
    - 'KickOffSetupPanel text is centered, matching ActionPanel/FreeKickSetupPanel siblings'
    - "KickOffSetupPanel's placement status uses human-friendly setup language, not raw validation state"
    - "KickOffSetupPanel's centre-hex constraint status uses human-friendly language, not raw state labels"
    - "ActionPanel's action-chooser helper text tells the player what actions are available (move/pass/shoot), not a vague generic line"
    - "ActionPanel's FREE_MOVE phase title reads 'Position for Kick!'"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Step through at least 8-10 of ActionPanel's ~18 phase states (MOVE, HIGH_PASS_MOVE, GK_RESTART, HEADER contest, PASS chooser, PASS target prompt, FREE_MOVE, SNAPSHOT_DEFLECT, a waiting/non-active-player state, KickOffSetupPanel, FreeKickSetupPanel) and confirm the heading/helper/Confirm/Keeper system reads naturally, now with the 5 reworded strings (centered KickOffSetupPanel text, human-friendly centre-hex/placement guidance, the move/pass/shoot action-chooser line, and the 'Position for Kick!' FREE_MOVE title) in place."
    expected: 'Every state shows one heading, one short title line, one detail line, and a single Confirm-labeled CTA; the specific wording just changed by plan 35-06 reads naturally in the context of a real phase sequence, not just in isolated unit-test assertions.'
    why_human: '35-UAT.md test 2 originally flagged 5 concrete wording/alignment complaints from a real walkthrough; those 5 are now fixed at the source level (verified below), but only a human re-walking the same sequence can confirm the fixes read naturally together and that no new complaint surfaces from the reworded copy.'
  - test: 'Confirm each ActionPanel/ActionLog-area container (ActionLog, ActionPanel confirmCard, FreeKickSetupPanel confirmCard, ReplayPanel, SideLog collapsed/expanded strip) still reads as visually distinct from the pitch now that all frame borders are removed.'
    expected: "Each panel is still legible and visually separated from the pitch by its background color alone (var(--color-bg-surface)) — no panel looks like it 'floats' or bleeds into the board with no border."
    why_human: '35-UAT.md test 1 (border removal legibility) was never executed — testing paused after test 2 surfaced issues. This is a purely visual affordance check that grep/stylelint cannot answer.'
  - test: "Confirm the CTA color transition (orange->green) is visually distinguishable against the charcoal/graphite background for all five newly-converted phases (HIGH_PASS_MOVE, FIRST_TIME_PASS_MOVE, SNAPSHOT_DEFLECT, GK_KICK_MOVE, FREE_MOVE) plus FreeKickSetupPanel's Confirm button, matching the pre-existing MOVE/HEADER look."
    expected: 'The pending (orange) and ready (green) states are as visually obvious in these five phases as they already are in MOVE/HEADER.'
    why_human: '35-UAT.md test 3 was never executed — testing paused after test 2. Color rendering and perceptual distinctness cannot be verified by source/class-name assertions alone.'
---

# Phase 35: ActionPanel & Log Standardization Verification Report (Gap-Closure Pass)

**Phase Goal:** Standardize ActionPanel/ActionLog and the same-slot sibling panels (KickOffSetupPanel, FreeKickSetupPanel, ReplayPanel) onto one consistent heading/CTA-color/waiting-copy/log-language system, and close the 5 diagnosed UAT gaps from 35-UAT.md test 2.
**Verified:** 2026-07-27T21:54:51Z
**Status:** human_needed
**Re-verification:** Yes — gap-closure pass after plan 35-06 (5 gaps from 35-UAT.md test 2)

## Goal Achievement

### Observable Truths

The 5 gaps recorded in `35-UAT.md`'s `## Gaps` section (all `status: failed`, tied to test 2) map to 4 `must_haves.truths` in `35-06-PLAN.md`. Each is re-verified directly against current source (not against SUMMARY claims), plus a full regression check of the phase's other 23 previously-verified truths (from the initial `35-VERIFICATION.md`, unaffected files).

| #    | Truth (UAT gap → plan truth)                                                                                                              | Status                  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | UAT gap: KickOffSetupPanel text is centered, matching ActionPanel/FreeKickSetupPanel siblings                                             | ✓ VERIFIED              | `KickOffSetupPanel.module.css` line 18: `.panelHeading { ...; text-align: center; }`; line 28: `.constraintRow { ...; text-align: center; }`. Both rules now match the already-centered declarations in `ActionPanel.module.css`/`FreeKickSetupPanel.module.css`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2    | UAT gap: KickOffSetupPanel's centre-hex constraint status uses human-friendly language, not raw state labels                              | ✓ VERIFIED              | `KickOffSetupPanel.tsx` lines 108-110: `centreHexOccupied ? 'A player is on the centre hex — ready to go.' : 'Place a player on the centre hex to start the kick-off.'` — the raw `'Centre hex: occupied'` / `'Centre hex: EMPTY — required'` strings are gone (`grep -c "Centre hex:"` = 0).                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 3    | UAT gap: KickOffSetupPanel's placement status uses human-friendly setup language, not raw validation state                                | ✓ VERIFIED              | `KickOffSetupPanel.tsx` lines 86-89 add `zoneGuidance`/`piecesLabel` consts; lines 118-120: `placementValid ? 'All your players are in a legal position.' : \`${piecesLabel} out of position — move into ${zoneGuidance}.\``— the raw`'Placement: valid'`/`'Placement: N pieces out of zone'` strings are gone (`grep -c "Placement:"` = 0).                                                                                                                                                                                                                                                                                                                                                                                                            |
| 4    | UAT gap: ActionPanel's action-chooser helper text tells the player what actions are available (move/pass/shoot), not a vague generic line | ✓ VERIFIED              | `ActionPanel.tsx` line 743: `: 'Move a player, pass to a teammate, or take a shot.'` — the old `'Select how to move or use the ball.'` string is gone (`grep -c "Select how to move or use the ball"` = 0); assertion updated at `ActionPanel.test.tsx:556`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 5    | UAT gap: ActionPanel's FREE_MOVE phase title reads "Position for Kick!"                                                                   | ✓ VERIFIED              | `ActionPanel.tsx` line 650: `<span className={styles.helperLine1}>Position for Kick!</span>` — `grep -c "Free Move!"` on `ActionPanel.tsx` = 0; both matching assertions updated at `ActionPanel.test.tsx:225,239` (`getByText('Position for Kick!')`); the accompanying `helperLine2` detail line (`{remaining} players still eligible to move — up to 6 hexes each…`, added by plan 35-05) is untouched, so the two-line shape from PANEL-01 is preserved.                                                                                                                                                                                                                                                                                            |
| 6-28 | (SC1-4/PANEL-01..04 + the 23 plan-level truths from plans 35-01..35-05)                                                                   | ✓ VERIFIED (regression) | Re-confirmed via direct grep/read, unchanged from the initial `35-VERIFICATION.md` (2026-07-27T18:30:00Z): zero `border: 1px solid var(--color-border)` outside `.backButton`/`.ctaButton` button exemptions in `ActionLog.module.css`/`ActionPanel.module.css`/`FreeKickSetupPanel.module.css`/`ReplayPanel.module.css`/`GameBoard.module.css`; `ctaColorClass(...)` is the single CTA color-state implementation (`ctaClass(` = 7 in `ActionPanel.tsx`, `ctaColorClass(` = 1 in `FreeKickSetupPanel.tsx`); zero `goalie`/`goalkeeper` (case-insensitive) in `ActionPanel.tsx`/`ActionLog.tsx`; zero `End Turn`/`Confirm Selection` in `ActionPanel.tsx`. No regression introduced by plan 35-06's edits to `ActionPanel.tsx`/`KickOffSetupPanel.tsx`. |

**Score:** 27/27 truths verified against the codebase (programmatic evidence): the 5 gap-closure truths (collapsed to 4 distinct must_haves, listed as items 1-5 above with item 2/3 split back out to map 1:1 to the 5 UAT gap entries) plus the 23 previously-verified phase truths, none regressed. Full client test suite (472/472), typecheck, and stylelint all green. Visual/perceptual judgment (does the reworded copy read naturally in a live walkthrough; is border removal still legible; is the orange→green transition visually obvious) has not been re-confirmed by a human — see Human Verification Required.

### Required Artifacts

| Artifact                                                      | Expected                                                                | Status     | Details                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------- | ----------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/client/src/components/KickOffSetupPanel.module.css` | `text-align: center` on `.panelHeading` and `.constraintRow`            | ✓ VERIFIED | Both rules present (lines 18, 28); no other declaration in the file changed.                                                                                                                                                                                                                             |
| `packages/client/src/components/KickOffSetupPanel.tsx`        | Human-friendly centre-hex and placement guidance strings                | ✓ VERIFIED | `zoneGuidance`/`piecesLabel` consts added (lines 86-89); both status rows rewritten (lines 108-110, 118-120); `disabledTitle`, boolean constraint logic (`constraintsMet`/`placementValid`/`piecesOutOfZone`/`centreHexOccupied`), and all other JSX untouched, matching the plan's copy/CSS-only scope. |
| `packages/client/src/components/ActionPanel.tsx`              | "Position for Kick!" FREE_MOVE title and reworded action-chooser detail | ✓ VERIFIED | Both strings present exactly as specified (lines 650, 743); no other JSX/logic in the touched blocks changed.                                                                                                                                                                                            |
| `packages/client/src/components/ActionPanel.test.tsx`         | 3 updated assertions + descriptions matching the new copy               | ✓ VERIFIED | `getByText('Position for Kick!')` at lines 225, 239; `getByText('Move a player, pass to a teammate, or take a shot.')` at line 556; test descriptions ("Position for Kick helper text") updated to match.                                                                                                |

### Key Link Verification

None declared in `35-06-PLAN.md` (`key_links: []`) — this plan changed only static JSX copy strings and CSS `text-align` declarations, introducing no new wiring. Confirmed: no import, export, or component-composition change occurred in either touched file beyond the two new local `const` declarations in `KickOffSetupPanel.tsx`.

### Behavioral Spot-Checks

| Behavior                                                                                | Command                                                                      | Result                                                                                                           | Status |
| --------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------ |
| KickOffSetupPanel + ActionPanel targeted suites pass                                    | `pnpm --filter @counter-attack/client test -- KickOffSetupPanel ActionPanel` | 2 files, 74/74 tests passed                                                                                      | ✓ PASS |
| Full client test suite passes (all 5 gap-closure fixes plus all prior phase assertions) | `pnpm --filter @counter-attack/client test`                                  | 25 files, 472/472 tests passed                                                                                   | ✓ PASS |
| Client typecheck is clean                                                               | `pnpm --filter @counter-attack/client typecheck`                             | exit 0, no output                                                                                                | ✓ PASS |
| CSS modules pass stylelint                                                              | `pnpm stylelint`                                                             | exit 0, no output                                                                                                | ✓ PASS |
| Git commits for plan 35-06 exist and match claimed diffs                                | `git show --stat f63bf99`, `git show --stat eb28ae5`                         | Both commits present; diffs match KickOffSetupPanel.{tsx,module.css} and ActionPanel.{tsx,test.tsx} as described | ✓ PASS |

### Probe Execution

Not applicable — this is a UI copy/CSS gap-closure plan with no `scripts/*/tests/probe-*.sh` declared or discovered.

### Requirements Coverage

| Requirement | Source Plan                       | Description                                                                              | Status                               | Evidence                                                                                                                                                                                                                                                             |
| ----------- | --------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PANEL-01    | 35-02, 35-05, 35-06               | ActionPanel/ActionLog help text follows one consistent format and tone                   | ✓ SATISFIED                          | Gap-closure copy changes preserve the two-line `helperLine1`/`helperLine2` shape (`grep -c styles.helperLine1` == `grep -c styles.helperLine2` unchanged); KickOffSetupPanel's centering brings its heading/status text in line with ActionPanel/FreeKickSetupPanel. |
| PANEL-02    | 35-01, 35-02, 35-03, 35-04        | No component in the ActionPanel/ActionLog area uses a border for framing                 | ✓ SATISFIED (unchanged by this plan) | Re-confirmed no regression: all container borders remain removed; 35-06 touched no CSS border declarations.                                                                                                                                                          |
| PANEL-03    | 35-01, 35-03                      | All ActionPanel buttons share consistent sizing, color-state logic, interaction behavior | ✓ SATISFIED (unchanged by this plan) | Re-confirmed no regression: `ctaColorClass` remains the single implementation; 35-06 touched no button color logic.                                                                                                                                                  |
| PANEL-04    | 35-02, 35-03, 35-04, 35-05, 35-06 | Terminology and phrasing are consistent across all ActionPanel/ActionLog states          | ✓ SATISFIED                          | The 5 gap-closure wording fixes (human-friendly KickOffSetupPanel status text, named actions in the chooser, renamed FREE_MOVE title) remove the last raw-state-label and vague-phrasing outliers flagged by UAT.                                                    |

**Documentation staleness (not a code gap, carried forward from the initial verification):** `REQUIREMENTS.md` still shows `PANEL-02`/`PANEL-03` as unchecked `[ ]` / status "Pending" (lines 26-27, 80-81) despite the code-level evidence above showing both satisfied since plans 35-01/35-03/35-04. `PANEL-01`/`PANEL-04` were updated to `[x]`/"Complete" by commit `ee59837` after this gap-closure plan, but `PANEL-02`/`PANEL-03` were not touched in that same update. This is a tracking-table gap, not a missing implementation — no orphaned requirement IDs exist (all four PANEL-0x map only to Phase 35).

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX` debt markers in any file touched by plan 35-06. The current `35-REVIEW.md` (re-reviewed post-wave-4, commit `77483d6`) documents 0 critical, 10 warnings, 3 info items across the phase's full file set — none of the warnings concern the 5 gap-closure edits themselves; two (WR-05, WR-06) are pre-existing `KickOffSetupPanel.tsx` defects (wrong disabled-tooltip reason when only the zone constraint fails; `localReady` is client-only state with no server-truth backing) that were present before plan 35-06 and are out of that plan's copy/CSS-only scope. None of these findings contradict or undermine any of the 27 verified truths above.

### Human Verification Required

See frontmatter `human_verification` for the structured form. `35-UAT.md` shows testing paused after test 2 surfaced the 5 now-closed issues — tests 1 (border-removal legibility) and 3 (CTA color-transition visibility) were never executed and remain outstanding regardless of this gap-closure pass. Additionally, since test 2's specific complaints are now fixed at the source, a human should re-walk the same phase sequence to confirm the reworded copy (centered KickOffSetupPanel status rows, human-friendly guidance text, the move/pass/shoot chooser line, "Position for Kick!") reads naturally together and that no new complaint surfaces.

### Gaps Summary

All 5 gaps diagnosed in `35-UAT.md` from test 2 are closed and verified against current source: KickOffSetupPanel's heading/constraint-row text is centered; its centre-hex and placement status rows use human-friendly guidance instead of raw validation labels; ActionPanel's action-chooser detail line names move/pass/shoot; and the FREE_MOVE title reads "Position for Kick!". Full client test suite (472/472), typecheck, and stylelint are green, and the 3 relevant test files (`KickOffSetupPanel.test.tsx`, `ActionPanel.test.tsx`) pass with updated assertions matching the new copy. No regressions were found in the 23 previously-verified phase truths.

The phase is not yet fully `passed` because `35-UAT.md`'s tests 1 and 3 were never executed (testing paused before reaching them) and test 2's fix should be re-walked in context — these are the same category of visual/perceptual checks the initial phase verification already flagged as requiring a human, now narrowed to exactly what `35-UAT.md` left outstanding.

---

_Verified: 2026-07-27T21:54:51Z_
_Verifier: Claude (gsd-verifier)_
