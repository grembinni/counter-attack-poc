---
phase: 35-actionpanel-log-standardization
plan: 06
type: execute
wave: 4
depends_on: ['35-03', '35-05']
files_modified:
  - packages/client/src/components/KickOffSetupPanel.module.css
  - packages/client/src/components/KickOffSetupPanel.tsx
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/ActionPanel.test.tsx
autonomous: true
requirements: [PANEL-01, PANEL-04]
gap_closure: true

must_haves:
  truths:
    - "KickOffSetupPanel's heading and constraint rows render centered text, matching the already-centered ActionPanel/FreeKickSetupPanel siblings"
    - "KickOffSetupPanel's centre-hex and placement status rows show human-friendly setup guidance, not raw validation-state labels"
    - "ActionPanel's non-kick-off action chooser detail line names the concrete available actions (move a player, pass, shoot) instead of the vague 'use the ball' phrasing"
    - "ActionPanel's FREE_MOVE_ATTACK/FREE_MOVE_DEFENSE title reads 'Position for Kick!' (not 'Free Move!')"
  artifacts:
    - path: 'packages/client/src/components/KickOffSetupPanel.module.css'
      provides: 'text-align: center on .panelHeading and .constraintRow'
      contains: 'text-align: center'
    - path: 'packages/client/src/components/KickOffSetupPanel.tsx'
      provides: 'Human-friendly centre-hex and placement guidance strings'
      contains: 'Place a player on the centre hex to start the kick-off.'
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: "'Position for Kick!' FREE_MOVE title and reworded action-chooser detail"
      contains: 'Position for Kick!'
  key_links: []
---

<objective>
Close all 5 diagnosed gaps from 35-UAT.md test 2 (unified heading/helper/Confirm/Keeper system):
1. KickOffSetupPanel text isn't centered like its ActionPanel/FreeKickSetupPanel siblings.
2. KickOffSetupPanel's placement-status row shows raw validation text ("Placement: valid").
3. KickOffSetupPanel's centre-hex row shows raw constraint text ("Centre hex: occupied").
4. ActionPanel's non-kick-off action chooser detail line is vague ("Select how to move or use the ball.").
5. ActionPanel's FREE_MOVE_ATTACK/FREE_MOVE_DEFENSE title ("Free Move!") should read "Position for Kick!".

Purpose: These are the last copy/CSS-consistency gaps blocking PANEL-01 (consistent format/tone) and PANEL-04 (consistent terminology/phrasing) closure for Phase 35's human-visual UAT.
Output: KickOffSetupPanel.module.css, KickOffSetupPanel.tsx, and ActionPanel.tsx updated with centered text and human-friendly/renamed copy; ActionPanel.test.tsx assertions updated to match the new strings (no test regressions).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-actionpanel-log-standardization/35-UAT.md

# Sibling panel already centered — pattern reference for gap 1 (do not modify this file)

@packages/client/src/components/FreeKickSetupPanel.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: KickOffSetupPanel centering + human-friendly setup copy (gaps 1, 2, 3)</name>
  <files>packages/client/src/components/KickOffSetupPanel.module.css, packages/client/src/components/KickOffSetupPanel.tsx</files>
  <action>
Gap 1 (CSS centering): In KickOffSetupPanel.module.css, add `text-align: center;` to the `.panelHeading` rule (~line 14-18) and the `.constraintRow` rule (~line 20-27). This matches the already-centered `.panelHeading`/`.constraintRow` declarations in ActionPanel.module.css and FreeKickSetupPanel.module.css — do not touch any other rule in this file (the `.panel` container stays `flex-direction: column`, unlike FreeKickSetupPanel's row-wrap layout, so no `flex: 0 0 100%` change is needed here).

Gap 3 (centre-hex row, KickOffSetupPanel.tsx line ~103): Replace the raw constraint-state text with player-facing instructions. Change:
`{centreHexOccupied ? 'Centre hex: occupied' : 'Centre hex: EMPTY — required'}`
to:
`{centreHexOccupied ? 'A player is on the centre hex — ready to go.' : 'Place a player on the centre hex to start the kick-off.'}`

Gap 2 (placement row, KickOffSetupPanel.tsx line ~111): First add two new `const` declarations directly after the existing `disabledTitle` const (~line 84, before the `handleReadyClick` function and the `return` statement): `zoneGuidance` set to `isAttacking ? 'your half or the centre circle' : 'your own half, clear of the centre circle'`, and `piecesLabel` set to a template string: `` `${piecesOutOfZone} player${piecesOutOfZone === 1 ? '' : 's'}` ``. Then replace the placement row body:
``{placementValid ? 'Placement: valid' : `Placement: ${piecesOutOfZone} pieces out of zone`}``
with:
``{placementValid ? 'All your players are in a legal position.' : `${piecesLabel} out of position — move into ${zoneGuidance}.`}``

Do not change the `disabledTitle` button-tooltip text, the `constraintsMet`/`placementValid`/`piecesOutOfZone`/`centreHexOccupied` boolean logic, or any other JSX in this file — this task is copy/CSS-only.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- KickOffSetupPanel</automated>
</verify>
<done>KickOffSetupPanel's heading and both constraint rows render centered; the centre-hex row reads a plain instruction instead of "Centre hex: ..."; the placement row reads a plain instruction instead of "Placement: ..."; existing KickOffSetupPanel.test.tsx assertions (none of which assert the old exact strings) still pass unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: ActionPanel action-chooser wording + FREE_MOVE rename (gaps 4, 5)</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/ActionPanel.test.tsx</files>
  <action>
Gap 4 (ActionPanel.tsx line ~743, non-kick-off branch of the "Choose an Action!" helperLine2): Replace
`: 'Select how to move or use the ball.'`
with
`: 'Move a player, pass to a teammate, or take a shot.'`
This is the generic action-chooser detail line shown whenever `selectedPassType === null` and `phase !== 'KICK_OFF'` — leave the `isKickOff` branch's "Play starts with a Standard Pass..." string untouched.

Gap 5 (ActionPanel.tsx line ~650, FREE_MOVE_ATTACK/FREE_MOVE_DEFENSE helperLine1 — shared by both phases in the same return block): Replace
`<span className={styles.helperLine1}>Free Move!</span>`
with
`<span className={styles.helperLine1}>Position for Kick!</span>`

Then update ActionPanel.test.tsx so all now-stale assertions match the new copy exactly (do not add new tests — only fix the 3 assertions this rewording breaks):

- Line ~225: `expect(screen.getByText('Free Move!')).toBeDefined();` → `expect(screen.getByText('Position for Kick!')).toBeDefined();`
- Line ~239: `expect(screen.getByText('Free Move!')).toBeDefined();` → `expect(screen.getByText('Position for Kick!')).toBeDefined();`
- Line ~556: `expect(screen.getByText('Select how to move or use the ball.')).toBeDefined();` → `expect(screen.getByText('Move a player, pass to a teammate, or take a shot.')).toBeDefined();`

The `it(...)` test-description strings at lines ~218, ~232, and ~548 reference the old copy in prose ("sees the Free Move helper text", "select-how-to-move detail") — update these descriptions too so they stay accurate, but this is a comment/label-only change with no behavioral effect.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- ActionPanel</automated>
</verify>
<done>FREE_MOVE_ATTACK and FREE_MOVE_DEFENSE both render "Position for Kick!" as the helper title; the non-kick-off action chooser detail line names move/pass/shoot; all ActionPanel.test.tsx assertions (including the 3 updated ones) pass with zero failures.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                                                                                                          |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| None new | This plan changes only static JSX copy strings and CSS `text-align` declarations in two existing, already-reviewed client components. No new trust boundary, input path, or data flow is introduced. |

## STRIDE Threat Register

| Threat ID | Category  | Component         | Disposition | Mitigation Plan                                                                                                                                               |
| --------- | --------- | ----------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-35-SC   | Tampering | npm/pnpm installs | accept      | No package is added, removed, or upgraded in this plan — `package.json` and `pnpm-lock.yaml` are not in `files_modified`. No Package Legitimacy Gate applies. |

</threat_model>

<verification>
Run the full client suite plus typecheck and stylelint to confirm the copy/CSS changes introduce no regressions beyond the two touched components:

```
pnpm --filter @counter-attack/client test
pnpm --filter @counter-attack/client typecheck
pnpm stylelint
```

Note: the whole-workspace `pnpm lint` command has a pre-existing, unrelated `packages/shared` typescript-eslint file-count-cap failure (documented in 35-05-SUMMARY.md and PROJECT.md tech debt) — this is out of scope for this plan. If checking client-only lint cleanliness, scope eslint to the two touched files directly instead of running the workspace-wide `pnpm lint`.
</verification>

<success_criteria>

- KickOffSetupPanel's `.panelHeading` and `.constraintRow` both render centered text (CSS `text-align: center` present in KickOffSetupPanel.module.css).
- KickOffSetupPanel's centre-hex row and placement row both show plain player-facing instructions with no "Centre hex:" or "Placement:" label prefix remaining in the source.
- ActionPanel's non-kick-off action-chooser detail line names move/pass/shoot instead of "use the ball".
- ActionPanel's FREE_MOVE_ATTACK/FREE_MOVE_DEFENSE title reads "Position for Kick!" with zero remaining "Free Move!" occurrences in ActionPanel.tsx or ActionPanel.test.tsx.
- `pnpm --filter @counter-attack/client test` passes with zero failures.
- `pnpm --filter @counter-attack/client typecheck` and `pnpm stylelint` both pass clean.
  </success_criteria>

<output>
Create `.planning/phases/35-actionpanel-log-standardization/35-06-SUMMARY.md` when done
</output>
