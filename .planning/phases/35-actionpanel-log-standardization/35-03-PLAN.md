---
phase: 35-actionpanel-log-standardization
plan: 03
type: execute
wave: 2
depends_on: ['35-01']
files_modified:
  - packages/client/src/components/ReplayPanel.tsx
  - packages/client/src/components/ReplayPanel.module.css
  - packages/client/src/components/FreeKickSetupPanel.tsx
  - packages/client/src/components/FreeKickSetupPanel.module.css
  - packages/client/src/components/FreeKickSetupPanel.test.tsx
  - packages/client/src/components/KickOffSetupPanel.tsx
  - packages/client/src/components/GameBoard.module.css
autonomous: true
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04]
must_haves:
  truths:
    - 'No panel sharing the ActionPanel render slot is framed by a border, and neither is the side-log wrapper'
    - 'The confirm-and-advance button reads "Confirm" in KickOffSetupPanel and FreeKickSetupPanel'
    - 'FreeKickSetupPanel derives its CTA color from the same shared helper ActionPanel uses — not a second implementation'
    - "KickOffSetupPanel's waiting state names what it is waiting for instead of the bare generic phrase"
    - 'All four slot panels label their heading with the same CSS class name'
  artifacts:
    - path: 'packages/client/src/components/FreeKickSetupPanel.tsx'
      provides: 'Confirm-verb CTA whose color comes from ctaColorClass'
      contains: 'ctaColorClass'
    - path: 'packages/client/src/components/KickOffSetupPanel.tsx'
      provides: 'Confirm-verb CTA and a context-specific waiting state'
    - path: 'packages/client/src/components/ReplayPanel.module.css'
      provides: 'Border-free .panel and a panelHeading class matching its siblings'
      contains: '.panelHeading'
    - path: 'packages/client/src/components/GameBoard.module.css'
      provides: 'Border-free side-log wrapper'
  key_links:
    - from: 'packages/client/src/components/FreeKickSetupPanel.tsx'
      to: 'packages/client/src/utils/ctaColorClass.ts'
      via: 'named import replacing the local endTurnColorClass ternary'
      pattern: "from '\\.\\./utils/ctaColorClass\\.js'"
    - from: 'packages/client/src/components/ReplayPanel.tsx'
      to: 'packages/client/src/components/ReplayPanel.module.css'
      via: 'styles.panelHeading replaces styles.heading'
      pattern: 'styles\\.panelHeading'
---

<objective>
Bring the three same-slot sibling panels (`KickOffSetupPanel`, `FreeKickSetupPanel`,
`ReplayPanel`) and the `SideLog` wrapper into the phase's single visual and language system:
strip every container border (D-01), make `FreeKickSetupPanel` the second consumer of the
shared `ctaColorClass` helper instead of a second implementation (D-06), unify the
confirm-and-advance verb to "Confirm" (D-08), give `KickOffSetupPanel` a context-specific
waiting state (D-09), and normalize the heading CSS class name across all siblings (D-07).

Purpose: D-05 established that PANEL-01..04 apply to the full render-slot component set, not
just the two headline-named files. This plan closes the sibling half. It also completes D-06:
after this plan there is exactly one CTA color-state implementation in the codebase, called
from two components.

Output: four border-free container selectors, one deleted duplicate color-state ternary, and
one canonical confirm verb across the sibling panels.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-actionpanel-log-standardization/35-CONTEXT.md
@.planning/phases/35-actionpanel-log-standardization/35-PATTERNS.md
@.planning/phases/35-actionpanel-log-standardization/35-01-SUMMARY.md
</context>

## Artifacts this phase produces

Consumed by **this plan** (created in plan 35-01):
`ctaColorClass(eligibleRemaining, classes, enabled?)` — `packages/client/src/utils/ctaColorClass.ts`.

Created by **this plan**:

| Symbol          | Location                                                | Kind                                |
| --------------- | ------------------------------------------------------- | ----------------------------------- |
| `.panelHeading` | `packages/client/src/components/ReplayPanel.module.css` | CSS class (renamed from `.heading`) |

Removed by **this plan**:

| Symbol                                                                                      | Location                                                                 |
| ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `endTurnColorClass` local ternary                                                           | `packages/client/src/components/FreeKickSetupPanel.tsx` (lines 169-174)  |
| `.heading` rule                                                                             | `packages/client/src/components/ReplayPanel.module.css` (line 15)        |
| `border: 1px solid var(--color-border)` on `.panel`                                         | `packages/client/src/components/ReplayPanel.module.css` (line 7)         |
| `border: 1px solid var(--color-border)` on `.confirmCard`                                   | `packages/client/src/components/FreeKickSetupPanel.module.css` (line 82) |
| `border-right: 1px solid var(--color-border)` on `.sideLogCollapsed` and `.sideLogExpanded` | `packages/client/src/components/GameBoard.module.css` (lines 319, 330)   |

Sibling plans in this phase produce: `RESULT_LABEL` / `SHOT_OUTCOME_LABEL` /
`HEADER_RESULT_LABEL` (ActionLog.tsx, plan 35-02); `ACTION_PANEL_HEADING` / `PanelShell` /
`.panelHeading` in ActionPanel (plan 35-04); `waitingHelperBlock` / `waitingPanel(detail)` /
`actingSideLabel` (plan 35-05).

<tasks>

<task type="auto">
  <name>Task 1: Strip every container border in the slot set and normalize the heading class name (D-01, D-07)</name>
  <files>packages/client/src/components/ReplayPanel.module.css, packages/client/src/components/ReplayPanel.tsx, packages/client/src/components/FreeKickSetupPanel.module.css, packages/client/src/components/GameBoard.module.css</files>
  <read_first>
    - `packages/client/src/components/KickOffSetupPanel.module.css` (lines 1-20) and
      `packages/client/src/components/FreeKickSetupPanel.module.css` (lines 1-30) — the
      already-compliant target end-state: `.panel` has `border-radius: 4px` and NO `border`,
      and `.panelHeading` is `font-size: 12px; font-weight: 700; color: var(--color-text-primary); text-align: center;`
      inside a `flex: 0 0 100%` full-width group. These two files define the pattern the others
      must match.
    - `packages/client/src/components/ReplayPanel.module.css` (whole file, 75 lines) — the
      `.panel` border to remove at line 7, the `.heading` rule at line 15 to rename, and the
      `.ctaButton` border at line 59 which uses `var(--color-border-muted)` and is KEPT.
    - `packages/client/src/components/FreeKickSetupPanel.module.css` (lines 45-95) — the
      `.confirmCard` border at line 82 to remove, and the `.ctaButton` border at line 52
      (`var(--color-border-muted)`) which is KEPT.
    - `packages/client/src/components/GameBoard.module.css` (lines 309-346) — the
      `.sideLogCollapsed` / `.sideLogExpanded` `border-right` declarations to remove, and
      `.sideLogHeader`'s `border-bottom` which is KEPT (see the action for why).
    - `packages/client/src/components/ReplayPanel.tsx` (lines 29-56) — the single
      `styles.heading` usage to rename.
    - `stylelint.config.js` — `declaration-strict-value` with `expandShorthand: true` governs
      `border-color`; removing a declaration outright is compliant.
  </read_first>
  <action>
    Discriminator to apply throughout (derived during planning, verified against all six CSS
    modules in the slot set): container/frame borders in this codebase use
    `var(--color-border)`, button borders use `var(--color-border-muted)`. Remove the former,
    keep the latter. The one documented exception is `ActionPanel.module.css`'s `.backButton`,
    which uses `var(--color-border)` but is a button and is explicitly exempt under D-01
    ("buttons keep their border everywhere") — that file is owned by plan 35-04, not this one.

    D-01 removals — delete only the `border` / `border-right` declaration line, keeping every
    other declaration (`background`, `border-radius`, `padding`, `width`, `flex-*`,
    `overflow`, `transition`) byte-for-byte:
    1. `ReplayPanel.module.css` `.panel` — remove `border: 1px solid var(--color-border);` (line 7).
    2. `FreeKickSetupPanel.module.css` `.confirmCard` — remove `border: 1px solid var(--color-border);` (line 82).
    3. `GameBoard.module.css` `.sideLogExpanded` — remove `border-right: 1px solid var(--color-border);` (line 330).
    4. `GameBoard.module.css` `.sideLogCollapsed` — remove `border-right: 1px solid var(--color-border);` (line 319).
       CONTEXT.md's D-01 enumerates only `.sideLogExpanded`, but both selectors are the same
       `SideLog` wrapper in its two states and both draw the identical vertical frame line
       against the pitch; removing only one would leave the collapsed strip framed and the
       expanded panel not, which is exactly the per-state drift PANEL-02 exists to eliminate.
       Record this widening in the SUMMARY.

    Do NOT remove `GameBoard.module.css` `.sideLogHeader`'s `border-bottom: 1px solid var(--color-border)`
    (line 344). D-01 scopes border removal to "container/frame elements"; this is an internal
    horizontal divider separating the MATCH LOG label from the entry list, not a frame around a
    container, and removing it would run the header text into the first log entry. This is a
    deliberate in-scope decision, not an oversight — state it explicitly in the SUMMARY so
    verification does not read it as a missed site.

    Do NOT add a `box-shadow` or change any `background` to compensate. Every affected
    container already sets `background: var(--color-bg-surface)`, which is the differentiation
    mechanism the already-compliant `KickOffSetupPanel`/`FreeKickSetupPanel` `.panel` selectors
    rely on — CONTEXT.md's discretion clause is satisfied by the existing background, so no
    substitute affordance is needed.

    D-07 heading-class normalization — `ReplayPanel` is the only sibling using `styles.heading`
    for the structural role its siblings call `panelHeading`. In `ReplayPanel.module.css`
    rename the `.heading` selector to `.panelHeading` (rename only; do not change its
    declarations). In `ReplayPanel.tsx` change `<span className={styles.heading}>Replay</span>`
    to `<span className={styles.panelHeading}>Replay</span>`. Verify with
    `grep -rn "styles.heading" packages/client/src` that no other call site exists before
    renaming. Do not change the heading text "Replay", and do not add a `!` to it — sibling
    panel headings ("Kick-Off Setup", "Offside — Free Kick", "Replay") are consistently
    punctuation-free; the `!` convention in this phase applies to ActionPanel's `helperLine1`
    action-prompt titles, not to panel headings.

  </action>
  <verify>
    <automated>pnpm stylelint && pnpm --filter @counter-attack/client test && pnpm --filter @counter-attack/client typecheck && pnpm -r build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "border: 1px solid var(--color-border);" packages/client/src/components/ReplayPanel.module.css` returns `0`; `grep -c "border: 1px solid var(--color-border-muted);" packages/client/src/components/ReplayPanel.module.css` returns `1` (button border kept).
    - Source assertion: `grep -c "border: 1px solid var(--color-border);" packages/client/src/components/FreeKickSetupPanel.module.css` returns `0`; `grep -c "border: 1px solid var(--color-border-muted);" packages/client/src/components/FreeKickSetupPanel.module.css` returns `1`.
    - Source assertion: `grep -c "border-right" packages/client/src/components/GameBoard.module.css` returns `0`.
    - Source assertion: `grep -c "border-bottom: 1px solid var(--color-border);" packages/client/src/components/GameBoard.module.css` is unchanged from its pre-task value — the `.sideLogHeader` divider survived.
    - Source assertion: `grep -c "styles.heading" packages/client/src/components/ReplayPanel.tsx` returns `0`; `grep -c "styles.panelHeading" packages/client/src/components/ReplayPanel.tsx` returns `1`.
    - Source assertion: `grep -c "^\.panelHeading" packages/client/src/components/ReplayPanel.module.css` returns `1`; `grep -c "^\.heading" packages/client/src/components/ReplayPanel.module.css` returns `0`.
    - Source assertion: `grep -rlc "border: 1px solid var(--color-border);" packages/client/src/components/ActionLog.module.css packages/client/src/components/KickOffSetupPanel.module.css` finds no matches (cross-check that the other slot-set panels are border-free — ActionLog was cleared by plan 35-02, KickOffSetupPanel was already compliant).
    - `pnpm stylelint` exits 0, `pnpm --filter @counter-attack/client test` passes, `pnpm -r build` exits 0.
  </acceptance_criteria>
  <done>Every container in the ActionPanel/ActionLog area except `ActionPanel.module.css`'s `.confirmCard` (owned by plan 35-04) is border-free; all four slot panels name their heading class `panelHeading`.</done>
</task>

<task type="auto">
  <name>Task 2: Make FreeKickSetupPanel the second consumer of ctaColorClass and adopt the Confirm verb (D-06, D-08)</name>
  <files>packages/client/src/components/FreeKickSetupPanel.tsx, packages/client/src/components/FreeKickSetupPanel.test.tsx</files>
  <read_first>
    - `packages/client/src/utils/ctaColorClass.ts` — the plan-35-01 signature
      `ctaColorClass(eligibleRemaining, { ready, pending }, enabled?)`. The third `enabled`
      parameter exists specifically to absorb this component's `constraintsMet` gate.
    - `.planning/phases/35-actionpanel-log-standardization/35-01-SUMMARY.md` — confirms the
      helper's final shape and the `ctaClass` adapter convention ActionPanel adopted.
    - `packages/client/src/components/FreeKickSetupPanel.tsx` (lines 100-135 `constraintsMet` /
      `withEndTurnConfirm`, 120 `remaining`, 132-156 `confirmDialog`, 158-174 `canUndo` and
      `endTurnColorClass`, 176-222 the render return) — every site touched by this task.
    - `packages/client/src/components/ActionPanel.tsx` — post-plan-35-01 state. Mirror its
      `ctaClass` adapter shape exactly so both components read identically at the call site.
    - `packages/client/src/components/FreeKickSetupPanel.test.tsx` (lines 67-135 and 140-310) —
      the 13 `screen.getByRole('button', { name: /end turn/i })` and `/end turn/i` queries that
      must be requeried after the rename, plus line 231's `it` title asserting the button
      "reads \"End Turn\"".
    - `packages/client/src/components/FreeKickSetupPanel.module.css` (lines 45-60) — confirms
      `.ctaButtonReady` / `.ctaButtonPending` exist in this module under the same names as in
      `ActionPanel.module.css`, which is why the shared helper can take them as parameters.
  </read_first>
  <action>
    D-06 — replace the duplicate color-state implementation. Delete the local
    `endTurnColorClass` const (lines 169-174), which currently nests a `constraintsMet` gate
    around a `placedCount >= stage.max` ready/pending ternary. Import the shared helper with
    `import { ctaColorClass } from '../utils/ctaColorClass.js';` and compute the class with a
    single call that passes this component's own values:

    `const endTurnColorClass = ctaColorClass(remaining, { ready: styles.ctaButtonReady, pending: styles.ctaButtonPending }, constraintsMet);`

    This is behavior-preserving and must be verified as such: `remaining` is already computed
    as `Math.max(stage.max - placedCount, 0)` at line 120, so `remaining <= 0` is exactly
    equivalent to the old `placedCount >= stage.max` condition, and passing `constraintsMet` as
    `enabled` reproduces the old `: ''` branch. Declare `endTurnColorClass` after `remaining`
    and after `constraintsMet` so both are in scope. Leave the `className={`${styles.ctaButton} ${endTurnColorClass}`}`
    composition on the button unchanged.

    Leave the `confirmDialog` affirm button's hardcoded `styles.ctaButtonReady` alone — a modal
    affirm has no partial-progress state, matching the identical carve-out ActionPanel keeps.

    D-08 — unify the confirm-and-advance verb:
    1. The main CTA button's label changes from `End Turn` to `Confirm`.
    2. The `confirmDialog`'s own affirm button currently also reads `Confirm`. Renaming the CTA
       to `Confirm` would put two identically-named buttons in the DOM whenever the dialog is
       open, making both the UI and every accessible-name query ambiguous. Change the dialog's
       affirm label to `Yes, end turn`, which answers the dialog's own prompt ("…are you sure
       you want to end your turn?") directly and keeps `Confirm` unambiguous as the CTA verb.
       Leave the dialog's `Cancel` button unchanged.
    3. The `Undo` button is unchanged — it is not a confirm-and-advance CTA.
    4. Do not change `disabledTitle`, the `constraintRow` texts, `nextActionText`, or the
       `panelHeading` text `Offside — Free Kick`.

    Update `FreeKickSetupPanel.test.tsx`: replace every
    `screen.getByRole('button', { name: /end turn/i })` with
    `screen.getByRole('button', { name: /^confirm$/i })` (the anchored form is required so it
    cannot also match the dialog's `Yes, end turn`), and update the `it` titles that say
    "End Turn" to say "Confirm". At line 231 the `it` title asserts the button `reads "End Turn"` —
    change both the title and, if the body asserts the label text, the assertion, to `Confirm`.
    Then add a `describe('FreeKickSetupPanel — D-06/D-08: shared CTA color helper and Confirm verb', …)`
    asserting:
    - on a kicking stage with `freeKickPlacedPieceIds: []` and the kicker locked in
      `movedPieceIds` (so `constraintsMet` is true and `remaining > 0`), the Confirm button
      className contains `ctaButtonPending` and not `ctaButtonReady`;
    - with `freeKickPlacedPieceIds` filled to `stage.max` (so `remaining === 0`), the className
      contains `ctaButtonReady` and not `ctaButtonPending`;
    - on a kicking stage with no kicking-team piece in `movedPieceIds` (so `constraintsMet` is
      false), the className contains neither `ctaButtonReady` nor `ctaButtonPending`, and the
      button is `disabled`;
    - clicking Confirm while `remaining > 0` opens the dialog, and the dialog exposes buttons
      named `Cancel` and `Yes, end turn` (and clicking `Yes, end turn` calls
      `emitFreeKickReady`).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- FreeKickSetupPanel && pnpm --filter @counter-attack/client typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "constraintsMet$" packages/client/src/components/FreeKickSetupPanel.tsx` shows no remaining nested ternary; specifically `grep -c "placedCount >= stage.max" packages/client/src/components/FreeKickSetupPanel.tsx` returns `0`.
    - Source assertion: `grep -c "utils/ctaColorClass.js" packages/client/src/components/FreeKickSetupPanel.tsx` returns `1`, and `grep -c "ctaColorClass(" packages/client/src/components/FreeKickSetupPanel.tsx` returns `1`.
    - Source assertion: `grep -c ">End Turn<\|End Turn$" packages/client/src/components/FreeKickSetupPanel.tsx` returns `0`; `grep -c "Yes, end turn" packages/client/src/components/FreeKickSetupPanel.tsx` returns `1`; `grep -c ">$" packages/client/src/components/FreeKickSetupPanel.tsx` is not used as a gate — instead assert `grep -c "Confirm" packages/client/src/components/FreeKickSetupPanel.tsx` returns `1` (the single CTA label).
    - Source assertion: `grep -c "styles.ctaButtonReady" packages/client/src/components/FreeKickSetupPanel.tsx` returns exactly `2` — one inside the `ctaColorClass` argument object, one on the dialog affirm button.
    - Behavior assertion (test): all four new D-06/D-08 assertions pass, including the `constraintsMet: false` case producing neither color class.
    - Behavior assertion (test): every pre-existing FreeKickSetupPanel test still passes after requerying by `/^confirm$/i` — the D-54 kicker-first gate, the D-50 defender-zone gate, the placements counter, the next-action preview, and the `NOT_YOUR_STAGE` error surface are all unchanged.
    - `pnpm lint` and `pnpm --filter @counter-attack/client typecheck` exit 0.
  </acceptance_criteria>
  <done>`FreeKickSetupPanel` has zero local color-state logic and calls `ctaColorClass` once; its CTA reads `Confirm`, its dialog affirm reads `Yes, end turn`, and all prior gating behavior is proven unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Adopt the Confirm verb and a context-specific waiting state in KickOffSetupPanel (D-08, D-09)</name>
  <files>packages/client/src/components/KickOffSetupPanel.tsx</files>
  <read_first>
    - `packages/client/src/components/KickOffSetupPanel.tsx` (whole file, 132 lines) — in
      particular the `localReady` ternary at lines 116-129 that swaps the `Ready` button for a
      disabled `Waiting for opponent…` button, and the `constraintRow` spans at lines 94-110
      whose styling/shape a new status row must match.
    - `packages/client/src/components/FreeKickSetupPanel.tsx` (lines 72-81) — the D-09 target
      pattern: a `panelHeading` plus a `constraintRow` reading
      `{isKicking ? 'Attacking' : 'Defending'} team is repositioning…`. This is the shape to
      imitate, adapted to kick-off's simultaneous-placement model.
    - `packages/client/src/components/KickOffSetupPanel.module.css` (lines 1-40) — confirms
      `.constraintRow` and `.ctaButton` exist and that `.panelHeading` / `.constraintRow` /
      `.errorText` share the `flex: 0 0 100%` full-width group a new row must join.
    - `packages/client/src/components/GameBoard.tsx` (lines 326-337) — confirms
      `KickOffSetupPanel` fully replaces `ActionPanel` during `KICK_OFF_SETUP`, so this panel is
      solely responsible for that phase's language.
    - `grep -rn "KickOffSetupPanel" packages/client/src --include=*.tsx` — establish whether a
      `KickOffSetupPanel.test.tsx` exists. It does not today; if the executor adds assertions
      they must go in a new `KickOffSetupPanel.test.tsx` following
      `FreeKickSetupPanel.test.tsx`'s `useGameStore.setState` + `render` fixture style.
  </read_first>
  <action>
    D-08 — change the enabled button's label from `Ready` to `Confirm`. Leave its `disabled`
    gate (`!constraintsMet`), its `title={!constraintsMet ? disabledTitle : undefined}`, and
    its `onClick={handleReadyClick}` handler unchanged. Do not rename `emitReady`,
    `handleReadyClick`, or `localReady` — the socket event name and internal identifiers are
    protocol/implementation detail, and D-08 governs user-facing button text only.

    D-09 — make the post-confirm waiting state specific. In the `localReady` branch:
    1. Change the disabled button's label from `Waiting for opponent…` to `Confirmed`, so the
       button reflects this player's own completed action rather than doubling as a status
       message, and so the verb pair reads Confirm → Confirmed.
    2. Immediately above the button (inside the same `.panel`, after the existing
       `constraintRow` spans and the `gameError` span), render a
       `<span className={styles.constraintRow}>` reading
       `Waiting for the opponent to confirm their placement…` — use the `&hellip;` entity for
       the ellipsis, matching this file's and FreeKickSetupPanel's existing convention. Render
       this row only when `localReady` is true.

    Wording rationale to record in the SUMMARY: `FreeKickSetupPanel`'s
    `{Attacking|Defending} team is repositioning…` framing does not transfer literally here
    because both teams place simultaneously during `KICK_OFF_SETUP` — there is no single
    "acting team" to name. CONTEXT.md's Claude's-Discretion clause anticipates exactly this
    ("it currently has no waiting-team distinction to draw on beyond 'opponent'"). Naming the
    awaited action ("to confirm their placement") is the available specificity gain, and it is
    strictly more informative than the generic phrase D-09 replaces.

    Do not change the `panelHeading` text `Kick-Off Setup`, the two `constraintRow`
    centre-hex/placement status texts, `disabledTitle`, or any placement-validation logic.

    Create `packages/client/src/components/KickOffSetupPanel.test.tsx` covering:
    - phase gating: renders `null` when `phase !== 'KICK_OFF_SETUP'`, and renders the
      `Kick-Off Setup` heading when it is;
    - the CTA reads `Confirm` (assert `screen.getByRole('button', { name: /^confirm$/i })`) and
      is `disabled` when the attacking team has no piece on the centre hex;
    - the CTA is enabled once the centre hex is occupied and all pieces are in-zone;
    - after clicking Confirm, `emitReady` was called, the button reads `Confirmed` and is
      `disabled`, and the text `Waiting for the opponent to confirm their placement` is present;
    - before clicking Confirm, that waiting text is absent (`queryByText` returns null).
    Mirror `FreeKickSetupPanel.test.tsx`'s store-mock and `render` setup, including its
    `playerSlot` handling for home/away.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- KickOffSetupPanel && pnpm --filter @counter-attack/client test && pnpm --filter @counter-attack/client typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "Ready$\|>Ready<" packages/client/src/components/KickOffSetupPanel.tsx` returns `0` (the `Ready` button label is gone; `localReady` / `handleReadyClick` / `emitReady` identifiers may remain).
    - Source assertion: `grep -c "Confirm" packages/client/src/components/KickOffSetupPanel.tsx` returns `2` — the enabled CTA label `Confirm` and the disabled `Confirmed`.
    - Source assertion: `grep -c "Waiting for opponent" packages/client/src/components/KickOffSetupPanel.tsx` returns `0`.
    - Source assertion: `grep -c "Waiting for the opponent to confirm their placement" packages/client/src/components/KickOffSetupPanel.tsx` returns `1`.
    - Source assertion: `grep -c "emitReady" packages/client/src/components/KickOffSetupPanel.tsx` returns `2` — the store selector and the handler call; the socket event is untouched.
    - Behavior assertion (test): `packages/client/src/components/KickOffSetupPanel.test.tsx` exists and all 5 listed cases pass.
    - Behavior assertion (test): before Confirm is clicked the waiting text is absent; after it is clicked the button reads `Confirmed`, is disabled, and the waiting text is present.
    - `pnpm --filter @counter-attack/client test` (full suite) passes; `pnpm lint` and typecheck exit 0.
  </acceptance_criteria>
  <done>`KickOffSetupPanel` uses the canonical `Confirm` verb, its post-confirm state names what it is waiting for, and a new test file locks both behaviors.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                                                                                                                                                                                                      |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client→server socket emit | `emitReady` (KickOffSetupPanel) and `emitFreeKickReady` (FreeKickSetupPanel) cross into the server. Both handlers, their event names, and their payloads are unchanged by this plan — only button labels and CSS classes change. |
| server→client game state  | `freeKickPlacedPieceIds`, `movedPieceIds`, `freeKickStageIndex` drive the constraint gates. No new field is read and no gate condition is altered.                                                                               |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------- | ---------------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-35-07   | Elevation of Privilege | `FreeKickSetupPanel` Confirm button | mitigate    | Task 2's refactor must not weaken the `disabled={!constraintsMet}` gate that enforces the D-54 kicker-first and D-50 defender-zone rules client-side. `constraintsMet` is passed to `ctaColorClass` as `enabled` but the `disabled` prop keeps reading `!constraintsMet` directly and independently. Acceptance criteria include a `constraintsMet: false` test asserting the button is `disabled` and colorless, and the whole pre-existing D-50/D-54 suite must re-pass. Server-side `applyFreeKickReady` remains the authoritative gate regardless. |
| T-35-08   | Elevation of Privilege | `KickOffSetupPanel` Confirm button  | mitigate    | Task 3 changes label text only; `disabled={!constraintsMet}` and `handleReadyClick` are explicitly preserved, and the new test file asserts the disabled state when the centre hex is unoccupied.                                                                                                                                                                                                                                                                                                                                                      |
| T-35-09   | Tampering              | three `.module.css` files           | accept      | Removing `border` / `border-right` declarations and renaming `.heading` to `.panelHeading` cannot affect behavior or authorization. `pnpm stylelint` gates remaining declarations; `pnpm -r build` proves the CSS-module class rename resolves.                                                                                                                                                                                                                                                                                                        |
| T-35-SC   | Tampering              | npm/pnpm installs                   | accept      | No package is added, removed, or upgraded in this plan — `package.json` and `pnpm-lock.yaml` are not in `files_modified`. No Package Legitimacy Gate applies.                                                                                                                                                                                                                                                                                                                                                                                          |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test` (full client suite) passes, including the new `KickOffSetupPanel.test.tsx`.
- `pnpm --filter @counter-attack/client typecheck` exits 0.
- `pnpm lint` exits 0.
- `pnpm stylelint` exits 0.
- `pnpm -r build` exits 0 (proves the `.heading` → `.panelHeading` CSS-module rename resolves at build time).
- `grep -rc "border: 1px solid var(--color-border);" packages/client/src/components/ReplayPanel.module.css packages/client/src/components/FreeKickSetupPanel.module.css` returns `0` for both.
- `grep -c "border-right" packages/client/src/components/GameBoard.module.css` returns `0`.
- `grep -rn "End Turn\|>Ready<" packages/client/src/components/FreeKickSetupPanel.tsx packages/client/src/components/KickOffSetupPanel.tsx` returns no match other than the dialog's `Yes, end turn`.
</verification>

<success_criteria>

- `ReplayPanel.module.css` `.panel`, `FreeKickSetupPanel.module.css` `.confirmCard`, and both `GameBoard.module.css` `.sideLog*` selectors have no border; every `--color-border-muted` button border survives.
- `.sideLogHeader`'s divider is deliberately retained and the decision is documented in the SUMMARY.
- All four slot panels use the CSS class name `panelHeading` for their heading span.
- `FreeKickSetupPanel` contains no local ready/pending ternary and calls `ctaColorClass` exactly once, with `constraintsMet` passed as the `enabled` argument.
- `KickOffSetupPanel` and `FreeKickSetupPanel` both label their confirm-and-advance CTA `Confirm`; the FreeKick dialog affirm reads `Yes, end turn`.
- `KickOffSetupPanel`'s waiting state names the awaited action; `Waiting for opponent…` no longer appears in the file.
- A new `KickOffSetupPanel.test.tsx` exists; the full client suite, typecheck, lint, stylelint, and build are green.
  </success_criteria>

<output>
Create `.planning/phases/35-actionpanel-log-standardization/35-03-SUMMARY.md` when done.
</output>
