---
phase: 02-move-validator-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/hex.ts
  - packages/shared/src/hex.test.ts
  - packages/shared/src/scoreUtils.ts
  - packages/shared/src/scoreUtils.test.ts
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - DICE-03
  - DICE-04
  - DICE-05
must_haves:
  truths:
    - 'GameState type carries movement-phase tracking (movedPieceIds, paceUsedByPieceId, movementSlot) so validators can be pure functions over a single state object'
    - 'hexLine(from, to) returns every hex traversed by a straight pass path, used by passValidator for blocking and interception detection'
    - 'getZoIDefenders(position, opponents) returns the typed PlayerPiece[] of adjacent opponents, used by validators that need consequence data'
    - 'computeCombinedScore enforces the DICE-04 max -2 penalty cap as a single source of truth'
    - 'computeLooseBall maps a 1-6 direction die and 1-6 distance die to a raw HexCoord destination per the rulebook deflection ruler'
    - 'Every Phase 2 validator can import from a stable foundation without circular dependencies'
    - 'Each validator domain (move, pass, shot, heading, snapshot) is implemented in a dedicated file with a co-located *.test.ts, following the hex.ts/hex.test.ts pattern (D-09)'
    - 'packages/shared/src/index.ts re-exports ALL Phase 2 validator modules (moveValidator, passValidator, shotValidator, headingValidator, snapshotValidator) plus scoreUtils — pre-registered in Wave 1 so Wave 2 plans can run in parallel without concurrent edits to index.ts'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'GameState extended with movedPieceIds, paceUsedByPieceId, movementSlot (D-08)'
      contains: 'movementSlot'
    - path: 'packages/shared/src/hex.ts'
      provides: 'hexLine and getZoIDefenders utilities'
      exports: ['hexLine', 'getZoIDefenders']
    - path: 'packages/shared/src/scoreUtils.ts'
      provides: 'computeCombinedScore (DICE-03/DICE-04) and computeLooseBall (DICE-05)'
      exports: ['computeCombinedScore', 'computeLooseBall']
    - path: 'packages/shared/src/index.ts'
      provides: 'Barrel export including scoreUtils AND all five Phase 2 validator modules (pre-registered to avoid Wave 2 concurrent edits)'
      contains: 'scoreUtils'
  key_links:
    - from: 'packages/shared/src/hex.ts'
      to: 'packages/shared/src/types.ts'
      via: 'import type { HexCoord, PlayerPiece }'
      pattern: "import type \\{[^}]*PlayerPiece"
    - from: 'packages/shared/src/scoreUtils.ts'
      to: 'packages/shared/src/types.ts'
      via: 'import type { HexCoord }'
      pattern: "import type \\{[^}]*HexCoord"
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/scoreUtils.ts'
      via: 'export *'
      pattern: "export \\* from './scoreUtils"
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/moveValidator.ts'
      via: 'export *'
      pattern: "export \\* from './moveValidator"
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/passValidator.ts'
      via: 'export *'
      pattern: "export \\* from './passValidator"
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/shotValidator.ts'
      via: 'export *'
      pattern: "export \\* from './shotValidator"
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/headingValidator.ts'
      via: 'export *'
      pattern: "export \\* from './headingValidator"
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/snapshotValidator.ts'
      via: 'export *'
      pattern: "export \\* from './snapshotValidator"
---

<objective>
Establish the shared-package foundation that Phase 2 validators build on: extend `GameState` with movement-phase tracking fields (D-08), add `hexLine` (D-02) and `getZoIDefenders` (D-03) utilities to `hex.ts`, introduce `scoreUtils.ts` with `computeCombinedScore` (DICE-03/DICE-04) and `computeLooseBall` (DICE-05/D-06/D-07), and **pre-register all six new barrel exports in `index.ts` (scoreUtils + the five Phase 2 validator modules) so Wave 2 plans can execute in parallel without concurrent edits to `index.ts`**. All Wave 2 validator plans depend on these contracts.

Purpose: Define the interface contracts (types + utility signatures) before validators consume them, eliminating scavenger-hunt exploration in Wave 2, AND lock in the barrel export ordering during Wave 1 so the three Wave 2 plans never touch `index.ts`.
Output: Extended types/hex modules, new scoreUtils module, fully tested utilities, barrel index updated with all six Phase 2 export lines, `pnpm --filter=@counter-attack/shared test` green for the modules created in this plan (Phase 1 + scoreUtils + hex extensions). Validator modules are NOT created in this plan — only their export lines are added to index.ts; whole-package TypeScript build is NOT expected to succeed at the end of Task 2 because the exported modules do not yet exist. The build gate moves to Wave 2 plans which create those modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-move-validator-unit-tests/02-CONTEXT.md
@.planning/phases/02-move-validator-unit-tests/02-RESEARCH.md
@.planning/phases/02-move-validator-unit-tests/02-PATTERNS.md
@packages/shared/src/types.ts
@packages/shared/src/hex.ts
@packages/shared/src/hex.test.ts
@packages/shared/src/index.ts
@tsconfig.base.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend GameState with movement-phase tracking and add hex utilities (hexLine, getZoIDefenders)</name>
  <files>packages/shared/src/types.ts, packages/shared/src/hex.ts, packages/shared/src/hex.test.ts</files>
  <read_first>
    - packages/shared/src/types.ts (current GameState shape — extend in place, do not reorder existing fields)
    - packages/shared/src/hex.ts (existing AXIAL_DIRECTIONS, hexDistance, hexNeighbors, hexesInRange, isUnderZoI; follow the same JSDoc + `Source:` comment style)
    - packages/shared/src/hex.test.ts (Vitest import block and describe/it/expect structure — match exactly when appending new describes)
    - .planning/phases/02-move-validator-unit-tests/02-CONTEXT.md sections D-02, D-03, D-08
    - .planning/phases/02-move-validator-unit-tests/02-RESEARCH.md (Pattern 2 hexLine, Pattern 6 getZoIDefenders, Pitfalls 1, 2, 4)
    - tsconfig.base.json (strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, verbatimModuleSyntax all enabled)
  </read_first>
  <behavior>
    - GameState gains three REQUIRED fields (no `?:`): `movedPieceIds: readonly string[]`, `paceUsedByPieceId: Readonly<Record<string, number>>`, `movementSlot: 'ATTACKER_4' | 'DEFENDER_5' | 'ATTACKER_2' | null`
    - `hexLine({q:0,r:0}, {q:0,r:0})` returns `[{q:0,r:0}]` (single hex when from === to)
    - `hexLine({q:0,r:0}, {q:3,r:0})` returns 4 hexes including both endpoints `[{q:0,r:0},{q:1,r:0},{q:2,r:0},{q:3,r:0}]`
    - `hexLine` length === `hexDistance(from, to) + 1` for any non-zero distance
    - `hexLine` first element equals `from`; last element equals `to`; every adjacent pair has `hexDistance === 1`
    - `hexLine` on a diagonal axis (e.g., `{q:0,r:0}` → `{q:2,r:-2}`) returns a connected path of length 3 with each step adjacent
    - `getZoIDefenders(position, opponents)` returns only opponent pieces whose `position` has `hexDistance === 1` from `position`
    - `getZoIDefenders` returns `[]` when no opponent is adjacent
    - `getZoIDefenders` excludes opponents at distance 0 (same hex) and distance ≥ 2
    - `getZoIDefenders` preserves the `PlayerPiece` shape (returned objects are the same references from the input array)
  </behavior>
  <action>
    Extend `packages/shared/src/types.ts` by appending three fields to the `GameState` type literal in this exact order after `eventLog`: `movedPieceIds: readonly string[]`, `paceUsedByPieceId: Readonly<Record<string, number>>`, `movementSlot: 'ATTACKER_4' | 'DEFENDER_5' | 'ATTACKER_2' | null`. Use REQUIRED fields (no `?:`) because `exactOptionalPropertyTypes` is enabled (Pitfall 2). Add a JSDoc comment block above the three new fields documenting defaults (`[]`, `{}`, `null`) when outside MOVEMENT phase, and citing D-08.

    Extend `packages/shared/src/hex.ts`:
    1. Update the existing `import type { HexCoord } from './types.js'` line to also import `PlayerPiece` (single import statement with both type names).
    2. Append a new exported function `hexLine(from: HexCoord, to: HexCoord): HexCoord[]` after `hexesInRange`. Implement using the redblobgames axial-lerp + cube-round algorithm per D-02 and RESEARCH.md Pattern 2 (simplified variant). When `n = hexDistance(from, to)` is 0, return `[from]`. Otherwise iterate `i` from 0 to `n` inclusive, lerp `fq, fr`, derive `fs = -fq - fr`, round each, apply the standard cube-round tiebreak (`if (dq > dr && dq > ds) rq = -rr - rs; else if (dr > ds) rr = -rq - rs;`), and push `{q: rq, r: rr}`. Include a JSDoc block matching the style of `hexDistance` with `Source: redblobgames.com/grids/hexagons/#line-drawing`.
    3. Append a new exported function `getZoIDefenders(position: HexCoord, opponentPieces: readonly PlayerPiece[]): PlayerPiece[]` after `isUnderZoI`. Body: `return opponentPieces.filter((p) => hexDistance(position, p.position) === 1);`. JSDoc references D-03/D-04 and explains the typed-defenders variant exists alongside `isUnderZoI` (boolean form) for validator consequence data.

    Extend `packages/shared/src/hex.test.ts`:
    1. Update the existing `import { ... } from './hex.js'` line to also import `hexLine, getZoIDefenders`.
    2. Add `import type { PlayerPiece } from './types.js'` if needed for `getZoIDefenders` fixtures.
    3. Append two new `describe` blocks at the bottom: `describe('hexLine', ...)` with at least 4 `it` cases covering (a) `from === to` returns `[from]`, (b) distance-3 line length === 4 with correct endpoints, (c) every consecutive pair has `hexDistance === 1`, (d) diagonal axis path correctness; and `describe('getZoIDefenders', ...)` with at least 3 `it` cases covering (a) returns adjacent opponent, (b) excludes distance-2 opponent, (c) returns `[]` when no opponents adjacent. Construct minimal `PlayerPiece` fixtures with all 9 attribute fields (set non-relevant attributes to `1` or `5`).

    Do not modify any existing exported function. Do not change `isUnderZoI`. Do not introduce circular imports (hex.ts depends only on types.ts). Keep all imports using `.js` extensions per `verbatimModuleSyntax`.

  </action>
  <verify>
    <automated>pnpm --filter=@counter-attack/shared build &amp;&amp; pnpm --filter=@counter-attack/shared test -- hex</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/types.ts` contains the literal string `movementSlot: 'ATTACKER_4' | 'DEFENDER_5' | 'ATTACKER_2' | null`
    - `packages/shared/src/types.ts` contains the literal string `movedPieceIds: readonly string[]`
    - `packages/shared/src/types.ts` contains the literal string `paceUsedByPieceId: Readonly<Record<string, number>>`
    - `packages/shared/src/hex.ts` contains `export function hexLine(from: HexCoord, to: HexCoord): HexCoord[]`
    - `packages/shared/src/hex.ts` contains `export function getZoIDefenders(position: HexCoord, opponentPieces: readonly PlayerPiece[]): PlayerPiece[]`
    - `packages/shared/src/hex.ts` imports `PlayerPiece` from `./types.js`
    - `packages/shared/src/hex.test.ts` contains `describe('hexLine'` and `describe('getZoIDefenders'`
    - `pnpm --filter=@counter-attack/shared build` exits 0 (TypeScript strict-mode compilation succeeds — this is BEFORE Task 2 adds the not-yet-existing barrel exports)
    - `pnpm --filter=@counter-attack/shared test -- hex` exits 0 with all hex tests passing including new hexLine and getZoIDefenders describes
    - No `import` statement in `hex.ts` references `socket.io`, `express`, or any path outside `./types.js`
  </acceptance_criteria>
  <done>
    GameState carries the three D-08 movement-tracking fields as required (non-optional) properties; `hexLine` returns a correct connected path of length `hexDistance+1` per redblobgames algorithm; `getZoIDefenders` returns typed `PlayerPiece[]` filtered to distance-1 opponents; all hex tests pass; the shared package still compiles in isolation with zero server imports.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create scoreUtils module (computeCombinedScore, computeLooseBall) with full tests, pre-register ALL Phase 2 barrel exports in index.ts</name>
  <files>packages/shared/src/scoreUtils.ts, packages/shared/src/scoreUtils.test.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/hex.ts (JSDoc + Source comment style; AXIAL_DIRECTIONS ordering [E, NE, NW, W, SW, SE] — `LOOSE_BALL_DIRECTIONS` MUST match this order)
    - packages/shared/src/hex.test.ts (Vitest test layout)
    - packages/shared/src/index.ts (current barrel exports — append SIX new `export *` lines in the same style)
    - .planning/phases/02-move-validator-unit-tests/02-CONTEXT.md sections D-06, D-07; Specific Ideas section (Loose Ball direction mapping)
    - .planning/phases/02-move-validator-unit-tests/02-RESEARCH.md (Pattern 4 computeCombinedScore, Pattern 5 computeLooseBall, Pitfall 6 off-by-one)
  </read_first>
  <behavior>
    - `computeCombinedScore(5, 4, [])` returns `9`
    - `computeCombinedScore(5, 4, [-1])` returns `8` (single -1 penalty applied)
    - `computeCombinedScore(5, 4, [-1, -1])` returns `7` (-2 cap reached exactly)
    - `computeCombinedScore(5, 4, [-1, -1, -1])` returns `7` (DICE-04 caps cumulative penalty at -2 even when sum is -3)
    - `computeCombinedScore(5, 4, [-2, -2])` returns `7` (cap at -2 regardless of magnitude)
    - `computeCombinedScore(0, 0, [])` returns `0`
    - `computeLooseBall({q:0,r:0}, 1, 1)` returns `{q:1, r:0}` (direction 1 = E, AXIAL_DIRECTIONS[0])
    - `computeLooseBall({q:0,r:0}, 2, 1)` returns `{q:1, r:-1}` (direction 2 = NE)
    - `computeLooseBall({q:0,r:0}, 3, 1)` returns `{q:0, r:-1}` (direction 3 = NW)
    - `computeLooseBall({q:0,r:0}, 4, 1)` returns `{q:-1, r:0}` (direction 4 = W)
    - `computeLooseBall({q:0,r:0}, 5, 1)` returns `{q:-1, r:1}` (direction 5 = SW)
    - `computeLooseBall({q:0,r:0}, 6, 1)` returns `{q:0, r:1}` (direction 6 = SE)
    - `computeLooseBall({q:0,r:0}, 1, 6)` returns `{q:6, r:0}` (distance scales linearly)
    - `computeLooseBall({q:5,r:-3}, 2, 3)` returns `{q:8, r:-6}` (non-origin start, distance 3 NE)
    - After this task, `packages/shared/src/index.ts` contains the literal export lines for: `scoreUtils.js`, `moveValidator.js`, `passValidator.js`, `shotValidator.js`, `headingValidator.js`, `snapshotValidator.js`
  </behavior>
  <action>
    Create `packages/shared/src/scoreUtils.ts`:
    - File-level JSDoc citing DICE-03, DICE-04, DICE-05, D-06, D-07
    - `import type { HexCoord } from './types.js'`
    - Export `function computeCombinedScore(attribute: number, diceValue: number, penalties: number[]): number` — sum penalties, clamp `Math.max(totalPenalty, -2)` (DICE-04 cap), return `attribute + diceValue + clampedPenalty`. JSDoc explicitly cites DICE-04 max -2 cap as the single source of truth (RESEARCH.md "Don't Hand-Roll" pattern).
    - Module-private constant `const LOOSE_BALL_DIRECTIONS: readonly HexCoord[]` ordered exactly: `[{q:1,r:0}, {q:1,r:-1}, {q:0,r:-1}, {q:-1,r:0}, {q:-1,r:1}, {q:0,r:1}]` with inline comments labeling each (1=E, 2=NE, 3=NW, 4=W, 5=SW, 6=SE). Add a comment block citing `Source: Counter Attack rulebook v1.4.1 deflection ruler` and noting "Order matches AXIAL_DIRECTIONS in hex.ts; physical rulebook verification before Phase 4 live use (per assumption A2 in 02-RESEARCH.md)".
    - Export `function computeLooseBall(from: HexCoord, direction: 1 | 2 | 3 | 4 | 5 | 6, distance: 1 | 2 | 3 | 4 | 5 | 6): HexCoord`. Body: `const dir = LOOSE_BALL_DIRECTIONS[direction - 1]!; return { q: from.q + dir.q * distance, r: from.r + dir.r * distance };`. The non-null assertion is required by `noUncheckedIndexedAccess` — add an inline comment justifying it (the literal union 1|2|3|4|5|6 maps to indices 0-5 by construction).

    Create `packages/shared/src/scoreUtils.test.ts`:
    - Vitest import block matching `hex.test.ts`
    - `import { computeCombinedScore, computeLooseBall } from './scoreUtils.js'`
    - `describe('computeCombinedScore', ...)` with at least 5 `it` cases covering: no penalties, single penalty, exact -2 cap, sum-below-cap clamps to -2, multi-penalty sum (e.g., [-2, -2]) clamps to -2
    - `describe('computeLooseBall', ...)` with at least 7 `it` cases covering all six direction values at distance 1 (asserting each matches the expected hex), distance scaling (direction 1 at distance 6), and a non-origin starting hex

    Modify `packages/shared/src/index.ts` — append six new lines (in this exact order) after the existing `export * from './pitch.js';` line, preserving file ordering. This pre-registers ALL Phase 2 validator barrel exports during Wave 1 so the three Wave 2 plans (02-02, 02-03, 02-04) never touch `index.ts` and can execute concurrently without write conflicts. The literal lines to append, verbatim:

    Line A: `export * from './scoreUtils.js';`
    Line B: `export * from './moveValidator.js';`
    Line C: `export * from './passValidator.js';`
    Line D: `export * from './shotValidator.js';`
    Line E: `export * from './headingValidator.js';`
    Line F: `export * from './snapshotValidator.js';`

    Add a comment line above these six exports stating: `// Phase 2 barrel exports — pre-registered in Wave 1 (Plan 02-01) to permit parallel Wave 2 execution. Validator modules themselves are created by plans 02-02, 02-03, 02-04. TypeScript build will fail until those plans complete; this is expected and documented in 02-01-PLAN.md.`

    Do NOT add `computeLooseBall` boundary checks (deferred to Phase 4 per CONTEXT.md Deferred Ideas). Do NOT import anything from validator files (none exist yet; this is the foundation). Do NOT introduce any random number generation — dice values are injected parameters only.

  </action>
  <verify>
    <automated>pnpm --filter=@counter-attack/shared test -- scoreUtils</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/shared/src/scoreUtils.ts` exists and exports `computeCombinedScore` and `computeLooseBall`
    - File `packages/shared/src/scoreUtils.test.ts` exists with `describe('computeCombinedScore'` and `describe('computeLooseBall'`
    - `packages/shared/src/scoreUtils.ts` contains the literal `LOOSE_BALL_DIRECTIONS` constant with six entries in order E, NE, NW, W, SW, SE
    - `packages/shared/src/scoreUtils.ts` contains the literal text `DICE-04` in a comment or JSDoc near the penalty clamp
    - `packages/shared/src/index.ts` contains the literal string `export * from './scoreUtils.js';`
    - `packages/shared/src/index.ts` contains the literal string `export * from './moveValidator.js';`
    - `packages/shared/src/index.ts` contains the literal string `export * from './passValidator.js';`
    - `packages/shared/src/index.ts` contains the literal string `export * from './shotValidator.js';`
    - `packages/shared/src/index.ts` contains the literal string `export * from './headingValidator.js';`
    - `packages/shared/src/index.ts` contains the literal string `export * from './snapshotValidator.js';`
    - `pnpm --filter=@counter-attack/shared test -- scoreUtils` exits 0 with all scoreUtils tests passing (Vitest can collect a single file's tests without compiling the entire package)
    - `computeCombinedScore(5, 4, [-1, -1, -1])` evaluates to exactly `7` (DICE-04 cap verified by test)
    - `computeLooseBall({q:0,r:0}, 1, 1)` returns `{q:1, r:0}` (direction mapping verified)
    - No `import` statement in `scoreUtils.ts` references `socket.io`, `express`, `crypto`, or any random-number source
    - NOTE: `pnpm --filter=@counter-attack/shared build` is **expected to FAIL** at the end of this task because the five validator module files referenced by the new barrel exports do not yet exist. The build gate is intentionally deferred to Wave 2; this is recorded in the plan's `<objective>` and is not a verification failure.
  </acceptance_criteria>
  <done>
    `scoreUtils.ts` exports the two utilities; combined score respects the DICE-04 -2 penalty cap; Loose Ball direction mapping covers all six dice values and scales by distance; barrel index is pre-loaded with all six Phase 2 export lines (scoreUtils + the five validator modules) so Wave 2 plans can run concurrently; scoreUtils tests pass.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                                                                                                                      |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none in this plan) | Phase 2 is pure in-process TypeScript with no I/O, no networking, no auth, no user input, no persistence. No trust boundary is crossed inside `packages/shared`. |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                                 |
| --------- | ---------------------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | Tampering              | computeCombinedScore penalty cap   | mitigate    | DICE-04 max -2 cap is enforced inside `computeCombinedScore` via `Math.max(totalPenalty, -2)`; centralized so callers cannot accidentally bypass. Verified by unit tests including `[-1,-1,-1]` and `[-2,-2]` cases.            |
| T-02-02   | Tampering              | computeLooseBall direction mapping | accept      | Direction mapping is a hard-coded constant referenced against the physical rulebook (A2 assumption). No external input affects the mapping. Verification before Phase 4 live use is gated by Phase 4 acceptance per CONTEXT.md. |
| T-02-03   | Information Disclosure | scoreUtils / hex utilities         | N/A         | No PII, secrets, or sensitive data are present in this phase.                                                                                                                                                                   |
| T-02-04   | Denial of Service      | hexLine on huge distances          | accept      | `hexLine` is O(n) where n = `hexDistance`. Inputs are constrained by pitch geometry (~600 hexes max). No public network attacker can supply unbounded inputs in this phase — `packages/shared` has no I/O surface.              |
| T-02-05   | Elevation of Privilege | All utilities                      | N/A         | No auth or authorization boundary exists in this phase.                                                                                                                                                                         |
| T-02-06   | Spoofing               | All utilities                      | N/A         | No identity or session concept in this phase.                                                                                                                                                                                   |
| T-02-07   | Repudiation            | All utilities                      | N/A         | No logging or audit trail concern in this phase.                                                                                                                                                                                |
| T-02-08   | Tampering              | Barrel export ordering             | mitigate    | All six Phase 2 export lines are pre-written in Wave 1 in a deterministic order; Wave 2 plans never edit `index.ts`. Eliminates the concurrent-write race that would corrupt the barrel during parallel execution.              |
| T-02-SC   | Tampering              | npm/pnpm installs                  | accept      | No new packages installed in this phase. Phase 1 audit (Vitest, TypeScript, honeycomb-grid) remains in effect; no new supply-chain surface area.                                                                                |

Note: ASVS L1 categories V2 (auth), V3 (sessions), V4 (access control), V5 (validation/sanitization of untrusted input), V6 (crypto) do not apply to this plan. All "input" to functions is constructed in-process by other shared-package code or tests and is fully typed by TypeScript.
</threat_model>

<verification>
- Task 1: `pnpm --filter=@counter-attack/shared build` exits 0 AND `pnpm --filter=@counter-attack/shared test -- hex` exits 0 with Phase 1 hex tests + new hexLine + getZoIDefenders describes green (the index.ts edits happen in Task 2)
- Task 2: `pnpm --filter=@counter-attack/shared test -- scoreUtils` exits 0 with scoreUtils tests green. **Whole-package `build` is expected to FAIL** because index.ts now references five validator modules that do not yet exist; the build gate is owned by Wave 2 plans.
- Phase 1 tests in `hex.test.ts` still pass (no regressions on `hexDistance`, `hexNeighbors`, `hexesInRange`, `isUnderZoI`)
- New tests added in this plan: ≥ 4 for `hexLine`, ≥ 3 for `getZoIDefenders`, ≥ 5 for `computeCombinedScore`, ≥ 7 for `computeLooseBall` (≥ 19 new tests total)
- `packages/shared` source contains zero imports of `socket.io`, `express`, `crypto`, or any random source (ARCH-07 isolation)
- After Task 2, `index.ts` contains all six Phase 2 barrel exports (scoreUtils + moveValidator + passValidator + shotValidator + headingValidator + snapshotValidator) in that order.
</verification>

<success_criteria>

- `GameState` carries `movedPieceIds`, `paceUsedByPieceId`, `movementSlot` as required fields (no `?:`) typed per D-08
- `hex.ts` exports `hexLine` (redblobgames algorithm) and `getZoIDefenders` (typed PlayerPiece[] filter)
- `scoreUtils.ts` exports `computeCombinedScore` (DICE-04 -2 cap) and `computeLooseBall` (DICE-05 with deflection-ruler direction mapping)
- `index.ts` re-exports the new scoreUtils module AND pre-registers all five Phase 2 validator modules so Wave 2 plans never touch `index.ts`
- All Wave 2 validator plans can `import { ... } from '@counter-attack/shared'` (via relative `.js` paths in source) without additional foundation work
- `pnpm --filter=@counter-attack/shared test -- hex` and `pnpm --filter=@counter-attack/shared test -- scoreUtils` both green; no regressions in Phase 1 tests
- Whole-package `build` is intentionally red at end of Wave 1; first Wave 2 plan that creates a validator module turns the build green for that subset; all three Wave 2 plans completing turns the build fully green.
  </success_criteria>

<output>
Create `.planning/phases/02-move-validator-unit-tests/02-01-SUMMARY.md` when done, documenting:
- Final shape of the new GameState fields (with their JSDoc default behavior)
- Final hexLine and getZoIDefenders signatures
- Final scoreUtils signatures including the LOOSE_BALL_DIRECTIONS order
- Test counts per module
- The final contents of `packages/shared/src/index.ts` showing all six Phase 2 barrel export lines in order
- Confirmation that the build is expected to be red at end-of-plan and which Wave 2 plans turn it green
- Any deviations from the plan (especially around the cube-round tiebreak or attribute selection)
</output>
