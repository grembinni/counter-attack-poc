---
phase: 02-move-validator-unit-tests
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/shared/src/shotValidator.ts
  - packages/shared/src/shotValidator.test.ts
  - packages/shared/src/headingValidator.ts
  - packages/shared/src/headingValidator.test.ts
  - packages/shared/src/snapshotValidator.ts
  - packages/shared/src/snapshotValidator.test.ts
autonomous: true
requirements:
  - SHOT-01
  - SHOT-02
  - SHOT-03
  - SHOT-04
  - SHOT-06
  - HEAD-01
  - HEAD-02
  - HEAD-03
  - HEAD-04
  - HEAD-05
  - SNAP-01
  - SNAP-02
  - SNAP-03
must_haves:
  truths:
    - 'validateShotDuel returns AUTO_MISS when shooterDice === 1 before any attribute calculation (SHOT-03)'
    - 'validateShotDuel applies computeCombinedScore to both shooter (shooting) and goalkeeper (saving), returning GOAL if shooter > GK, otherwise SAVE with needsHandlingCheck'
    - 'validateGKDive returns saveable=false when distance > 3 (SHOT-04: 4+ hexes unsavable)'
    - 'validateGKDive applies -1 savingPenalty at distance === 3 (SHOT-04: 3rd-hex penalty); 0 at distance 1 or 2'
    - 'validateHandlingCheck returns triggerLooseBall: true when dice >= handling (SHOT-06: roll ≥ Handling = spill)'
    - 'validateHeading returns ok:true contested:false for an uncontested header (no challenger within 2 hexes) (HEAD-02)'
    - 'validateHeading returns penaltyModifier 0 for challenger at distance 1, -1 at distance 2 (HEAD-01)'
    - 'validateHeading returns OUT_OF_RANGE for challenger at distance > 2'
    - 'validateHeading returns CONSECUTIVE_HEADER when the previous action was a headed pass (HEAD-04)'
    - 'validateHeading exposes excludedPieceIds for challengers so the Phase 4 FSM can enforce HEAD-05'
    - 'validateSnapshot rejects with WRONG_PHASE outside MOVEMENT/PASS/SNAPSHOT phases (SNAP-01 mechanical)'
    - "validateSnapshot success carries shootingPenalty: -1 and a deflectionEffect signal for the opponent's 2-hex deflection move (SNAP-02)"
    - 'Snapshot results carry the same SHOT-derived information so callers reuse validateShotDuel for the actual shot resolution (SNAP-03)'
    - "All three validator modules are importable from '@counter-attack/shared' immediately upon creation because the barrel export lines were pre-registered in Plan 02-01 — this plan does NOT modify index.ts"
  artifacts:
    - path: 'packages/shared/src/shotValidator.ts'
      provides: 'validateShotDuel, validateGKDive, validateHandlingCheck'
      exports:
        [
          'validateShotDuel',
          'validateGKDive',
          'validateHandlingCheck',
          'ShotDuelResult',
          'DiveResult',
          'HandlingResult',
        ]
    - path: 'packages/shared/src/headingValidator.ts'
      provides: 'validateHeading'
      exports: ['validateHeading', 'HeadingResult']
    - path: 'packages/shared/src/snapshotValidator.ts'
      provides: 'validateSnapshot'
      exports: ['validateSnapshot', 'SnapshotResult']
    - path: 'packages/shared/src/shotValidator.test.ts'
      provides: '≥7 tests covering SHOT-01..04, SHOT-06'
      contains: "describe('validateShotDuel'"
    - path: 'packages/shared/src/headingValidator.test.ts'
      provides: '≥6 tests covering HEAD-01..05'
      contains: "describe('validateHeading'"
    - path: 'packages/shared/src/snapshotValidator.test.ts'
      provides: '≥4 tests covering SNAP-01..03'
      contains: "describe('validateSnapshot'"
  key_links:
    - from: 'packages/shared/src/shotValidator.ts'
      to: 'packages/shared/src/scoreUtils.ts'
      via: 'import { computeCombinedScore }'
      pattern: "import \\{[^}]*computeCombinedScore"
    - from: 'packages/shared/src/headingValidator.ts'
      to: 'packages/shared/src/hex.ts'
      via: 'import { hexDistance }'
      pattern: "import \\{[^}]*hexDistance"
---

<objective>
Implement the three smaller domain validators bundled together: shot/save/handling (`shotValidator.ts`), heading duels (`headingValidator.ts`), and snapshot trigger (`snapshotValidator.ts`). Each delivers pure functions over typed inputs with discriminated-union results and dedicated unit tests. Together they complete the Phase 2 validator suite alongside Plans 02 and 03.

**Critical execution note (file ownership):** This plan does NOT modify `packages/shared/src/index.ts`. The barrel export lines `export * from './shotValidator.js';`, `export * from './headingValidator.js';`, and `export * from './snapshotValidator.js';` were pre-registered by Plan 02-01 Task 2 in Wave 1 so this plan (and 02-02, 02-03) can execute in parallel without concurrent writes to `index.ts`.

Purpose: Cover the remaining 13 phase requirements (SHOT-01..04, SHOT-06; HEAD-01..05; SNAP-01..03) in one cohesive plan; each validator is small (~one function with ~5 tests) so they fit cleanly into a single ~40% context budget.
Output: Three validator modules with co-located tests; `pnpm --filter=@counter-attack/shared test` green with all phase validators covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-move-validator-unit-tests/02-CONTEXT.md
@.planning/phases/02-move-validator-unit-tests/02-RESEARCH.md
@.planning/phases/02-move-validator-unit-tests/02-PATTERNS.md
@.planning/phases/02-move-validator-unit-tests/02-01-SUMMARY.md
@packages/shared/src/types.ts
@packages/shared/src/hex.ts
@packages/shared/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement shotValidator (validateShotDuel, validateGKDive, validateHandlingCheck) with tests</name>
  <files>packages/shared/src/shotValidator.ts, packages/shared/src/shotValidator.test.ts</files>
  <read_first>
    - packages/shared/src/types.ts (PlayerPiece attribute set including shooting, saving, handling)
    - packages/shared/src/scoreUtils.ts (computeCombinedScore signature from Plan 01)
    - packages/shared/src/index.ts (read-only — confirm `export * from './shotValidator.js';` is present from Plan 02-01; halt if missing rather than re-add)
    - .planning/phases/02-move-validator-unit-tests/02-CONTEXT.md (D-04 result shape; dice injection pattern; deferred GK dive boundary)
    - .planning/phases/02-move-validator-unit-tests/02-RESEARCH.md (Pattern 3 dice injection; the SHOT-01..04, SHOT-06 entries in the Phase Requirements table)
    - .planning/phases/02-move-validator-unit-tests/02-PATTERNS.md (shotValidator.ts pattern including auto-miss guard ordering)
    - .planning/phases/02-move-validator-unit-tests/02-01-SUMMARY.md (Plan 01 confirmed deliverables)
  </read_first>
  <behavior>
    `validateShotDuel(shooter, goalkeeper, shooterDice, gkDice, shooterPenalties, gkPenalties): ShotDuelResult`:
    - If `shooterDice === 1` return `{ outcome: 'MISS', reason: 'AUTO_MISS' }` BEFORE computing scores (SHOT-03 precedence)
    - Otherwise `shooterScore = computeCombinedScore(shooter.shooting, shooterDice, shooterPenalties)`, `gkScore = computeCombinedScore(goalkeeper.saving, gkDice, gkPenalties)`
    - If `shooterScore > gkScore` return `{ outcome: 'GOAL' }`
    - Otherwise return `{ outcome: 'SAVE', needsHandlingCheck: true }`
    - Ties go to the GK (save) — this is implicit in `>` not `>=`; document in JSDoc

    `validateGKDive(gk, distance): DiveResult`:
    - `distance > 3` → `{ saveable: false, reason: 'OUT_OF_RANGE' }` (SHOT-04: 4+ hexes unsavable)
    - `distance === 3` → `{ saveable: true, savingPenalty: -1 }` (SHOT-04: -1 at the 3rd hex)
    - `distance === 1 or 2` → `{ saveable: true, savingPenalty: 0 }`
    - `distance === 0` (GK stays put) → `{ saveable: true, savingPenalty: 0 }` (GK does not need to dive)
    - Negative distance → defensive: treat as `distance: 0`. Document this.
    - Document goal-line boundary (which axis "parallel to the goal line" means) as deferred to Phase 4 in JSDoc.

    `validateHandlingCheck(gk, diceValue): HandlingResult` (SHOT-06):
    - If `diceValue >= gk.handling` → `{ caught: false, triggerLooseBall: true }` (spill)
    - Else → `{ caught: true }`

    SHOT-02 mechanical signal (outside-area penalty + GK 1-hex move): expose a small helper `getOutsideAreaModifiers(): { shootingPenalty: -1; gkMayMoveOneHex: true }` or document SHOT-02 mechanics inline in `validateShotDuel` JSDoc with the assertion that pitch-area boundary detection is deferred to Phase 4. Whichever choice the executor makes, the SHOT-02 deferral must be explicit in code.

  </behavior>
  <action>
    Create `packages/shared/src/shotValidator.ts`:
    - `import type { PlayerPiece } from './types.js';`
    - `import { computeCombinedScore } from './scoreUtils.js';`
    - Export `type ShotDuelResult = { outcome: 'GOAL' } | { outcome: 'MISS'; reason: 'AUTO_MISS' } | { outcome: 'SAVE'; needsHandlingCheck: true };`
    - Export `type DiveResult = { saveable: true; savingPenalty: number } | { saveable: false; reason: 'OUT_OF_RANGE' };`
    - Export `type HandlingResult = { caught: true } | { caught: false; triggerLooseBall: true };`
    - Implement and export `validateShotDuel`, `validateGKDive`, `validateHandlingCheck` per the behavior section.
    - Add a top-of-file JSDoc citing SHOT-01, SHOT-02 (deferred boundary), SHOT-03, SHOT-04, SHOT-06.
    - In `validateGKDive` add an inline comment citing CONTEXT.md Deferred Ideas for the goal-line boundary.

    Create `packages/shared/src/shotValidator.test.ts` with at least 7 tests:
    1. `validateShotDuel returns AUTO_MISS on shooterDice === 1 regardless of attributes`
    2. `validateShotDuel returns GOAL when shooterScore > gkScore (no auto-miss)`
    3. `validateShotDuel returns SAVE with needsHandlingCheck when GK >= shooter`
    4. `validateShotDuel applies DICE-04 -2 cap to shooter penalties` (e.g., penalties [-1,-1,-1])
    5. `validateGKDive returns saveable:false at distance 4 with OUT_OF_RANGE`
    6. `validateGKDive returns savingPenalty -1 at distance 3`
    7. `validateGKDive returns savingPenalty 0 at distance 1 and distance 2`
    8. `validateHandlingCheck returns caught:false triggerLooseBall when dice equals handling exactly` (SHOT-06: ≥ Handling = spill)
    9. `validateHandlingCheck returns caught:true when dice < handling`

    **DO NOT modify `packages/shared/src/index.ts`.** The barrel export line `export * from './shotValidator.js';` was added to `index.ts` by Plan 02-01 Task 2 in Wave 1. If the line is missing, halt and report a Plan 02-01 regression — do not re-add it.

  </action>
  <verify>
    <automated>pnpm --filter=@counter-attack/shared test -- shotValidator</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/shotValidator.ts` exists and exports `validateShotDuel`, `validateGKDive`, `validateHandlingCheck` plus the three result types
    - `validateShotDuel` returns `{ outcome: 'MISS', reason: 'AUTO_MISS' }` when called with `shooterDice = 1` and shooter.shooting = 10, gk.saving = 1 (proves auto-miss precedence over attribute calculation)
    - `validateGKDive(piece, 4)` returns `{ saveable: false, reason: 'OUT_OF_RANGE' }`
    - `validateGKDive(piece, 3)` returns `{ saveable: true, savingPenalty: -1 }`
    - `validateHandlingCheck(piece, piece.handling)` returns `{ caught: false, triggerLooseBall: true }` (≥ Handling triggers spill)
    - `packages/shared/src/shotValidator.test.ts` contains ≥7 `it(` cases
    - `packages/shared/src/index.ts` was NOT modified by this task
    - `pnpm --filter=@counter-attack/shared test -- shotValidator` exits 0
    - No `import` statement in `shotValidator.ts` references `socket.io`, `express`, or `crypto`
  </acceptance_criteria>
  <done>
    Shot duel resolves AUTO_MISS before attribute calculation, applies DICE-04 cap via `computeCombinedScore`, and returns GOAL/SAVE based on score comparison. GK dive enforces the 3-hex distance rule with -1 at the 3rd hex. Handling check spills on `dice >= handling`. Tests cover all branches; barrel index already re-exports the module via Plan 02-01's pre-registration.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement headingValidator and snapshotValidator with tests</name>
  <files>packages/shared/src/headingValidator.ts, packages/shared/src/headingValidator.test.ts, packages/shared/src/snapshotValidator.ts, packages/shared/src/snapshotValidator.test.ts</files>
  <read_first>
    - packages/shared/src/types.ts (GameState shape including phase union and the D-08 fields)
    - packages/shared/src/hex.ts (hexDistance signature)
    - packages/shared/src/index.ts (read-only — confirm `export * from './headingValidator.js';` and `export * from './snapshotValidator.js';` are present from Plan 02-01; halt if missing rather than re-add)
    - .planning/phases/02-move-validator-unit-tests/02-CONTEXT.md sections D-04; deferred ideas about penalty-area boundary; Specific Ideas about heading consecutiveness
    - .planning/phases/02-move-validator-unit-tests/02-RESEARCH.md (HEAD-01..05 and SNAP-01..03 rows of the Phase Requirements table)
    - .planning/phases/02-move-validator-unit-tests/02-PATTERNS.md (headingValidator and snapshotValidator sections)
    - .planning/phases/02-move-validator-unit-tests/02-01-SUMMARY.md and 02-02-SUMMARY.md if available (for consistency with prior precedence patterns)
  </read_first>
  <behavior>
    `validateHeading(state, challenger, ballPosition, options): HeadingResult` where `options: { previousActionWasHeadedPass: boolean; otherChallengerIds?: readonly string[] }` (or equivalent; executor picks ergonomic shape and documents it). The PlayerPiece set of challengers is supplied via `otherChallengerIds` so HEAD-05 exclusion can include all participants.

    - Distance check (HEAD-01): `dist = hexDistance(challenger.position, ballPosition)`. If `dist > 2` → `{ ok: false, reason: 'OUT_OF_RANGE' }`. If `dist === 1` penaltyModifier = 0; if `dist === 2` penaltyModifier = -1.
    - Consecutive header check (HEAD-04): if `options.previousActionWasHeadedPass === true` → `{ ok: false, reason: 'CONSECUTIVE_HEADER' }`
    - Uncontested (HEAD-02): if no other challenger ids supplied OR `otherChallengerIds.length === 0`, return `{ ok: true, contested: false }` (auto-win — no dice roll)
    - Contested: return `{ ok: true, contested: true, penaltyModifier, excludedPieceIds: [...challenger.id, ...otherChallengerIds] }` so the Phase 4 FSM has the full HEAD-05 exclusion set
    - Precedence: consecutive header check BEFORE distance check (FSM context dominates over geometry)
    - HEAD-03 (headed-shot declaration): document in JSDoc that this is the same call shape — caller indicates intent via an `aimedAtGoal: boolean` field on options if executor includes it; the discriminated result MAY add `needsGKSave: true` to the contested success branch when `aimedAtGoal === true`. If executor omits the aimedAtGoal handling, document it as a TODO citing CONTEXT.md (heading at goal is a small Phase 4 wiring concern, not a validator-correctness concern).

    `validateSnapshot(state, piece): SnapshotResult`:
    - Wrong-phase rejection (SNAP-01 mechanical): if `state.phase !== 'MOVEMENT' && state.phase !== 'PASS' && state.phase !== 'SNAPSHOT'` → `{ ok: false, reason: 'WRONG_PHASE' }`
    - Otherwise return `{ ok: true, shootingPenalty: -1, deflectionEffect: { type: 'OPPONENT_MOVES', maxHexes: 2 } }` (SNAP-02)
    - Penalty-area boundary check is DEFERRED to Phase 4 (Deferred Ideas in CONTEXT.md) — document in JSDoc that the validator does not currently check ball-in-penalty-area; Phase 4 FSM will gate `validateSnapshot` calls behind the area test
    - SNAP-03 (standard shooting rules apply) is satisfied by the caller invoking `validateShotDuel` after a successful snapshot — document this composition relationship in JSDoc (per RESEARCH.md Open Question 2, now RESOLVED: validateSnapshot is called by the Phase 4 FSM after any pass; Phase 2 only validates legality given current state)

  </behavior>
  <action>
    Create `packages/shared/src/headingValidator.ts`:
    - `import type { GameState, PlayerPiece, HexCoord } from './types.js';`
    - `import { hexDistance } from './hex.js';`
    - Export `type HeadingResult = { ok: false; reason: 'OUT_OF_RANGE' | 'CONSECUTIVE_HEADER' } | { ok: true; contested: false } | { ok: true; contested: true; penaltyModifier: number; excludedPieceIds: string[] };`
    - Define and export `function validateHeading(state: GameState, challenger: PlayerPiece, ballPosition: HexCoord, options: { previousActionWasHeadedPass: boolean; otherChallengerIds?: readonly string[] }): HeadingResult` implementing the behavior section. Choose a signature variant if needed (e.g., extracting `previousActionWasHeadedPass` from `state.eventLog` is NOT required in Phase 2 — accept it as an option parameter so the validator stays pure).
    - JSDoc cites HEAD-01..05 and explicitly notes HEAD-03 handling (in-line comment or `aimedAtGoal` extension).
    - The `state` argument is accepted for future use (eventLog consultation in later phases) but Phase 2 only requires it for type discipline — document this.

    Create `packages/shared/src/headingValidator.test.ts` with at least 6 tests:
    1. `rejects when previousActionWasHeadedPass is true (CONSECUTIVE_HEADER) even at distance 1`
    2. `rejects when challenger distance > 2 (OUT_OF_RANGE)`
    3. `returns contested:false (uncontested) when otherChallengerIds is empty`
    4. `returns penaltyModifier 0 at distance 1 with at least one other challenger`
    5. `returns penaltyModifier -1 at distance 2 with at least one other challenger`
    6. `excludedPieceIds includes both the challenger and all otherChallengerIds (HEAD-05)`

    Create `packages/shared/src/snapshotValidator.ts`:
    - `import type { GameState, PlayerPiece } from './types.js';`
    - Export `type SnapshotResult = { ok: false; reason: 'WRONG_PHASE' } | { ok: true; shootingPenalty: -1; deflectionEffect: { type: 'OPPONENT_MOVES'; maxHexes: 2 } };`
    - Export `function validateSnapshot(state: GameState, piece: PlayerPiece): SnapshotResult` per behavior. The `piece` argument is accepted for type discipline / future use; the Phase 2 validator does not gate on piece identity beyond what the FSM provides.
    - JSDoc cites SNAP-01 (deferred penalty-area boundary), SNAP-02 (-1 shoot penalty + opponent 2-hex deflection), SNAP-03 (composition with validateShotDuel; Open Question 2 RESOLVED — FSM-driven composition).

    Create `packages/shared/src/snapshotValidator.test.ts` with at least 4 tests:
    1. `returns WRONG_PHASE outside MOVEMENT/PASS/SNAPSHOT`
    2. `accepts in MOVEMENT phase with shootingPenalty -1 and deflectionEffect maxHexes 2`
    3. `accepts in PASS phase` (SNAP-01 post-pass trigger)
    4. `accepts in SNAPSHOT phase` (already snapshotting — composability)

    **DO NOT modify `packages/shared/src/index.ts`.** Both `export * from './headingValidator.js';` and `export * from './snapshotValidator.js';` were added to `index.ts` by Plan 02-01 Task 2 in Wave 1. If either line is missing, halt and report a Plan 02-01 regression — do not re-add them.

    Do NOT add boundary checks for penalty area or final third (deferred). Do NOT throw — all validators return discriminated results. Do NOT use optional chaining where strict null checks are required.

  </action>
  <verify>
    <automated>pnpm --filter=@counter-attack/shared test -- "Validator"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/headingValidator.ts` exists and exports `validateHeading` and `HeadingResult`
    - `packages/shared/src/headingValidator.ts` contains the literal substring `CONSECUTIVE_HEADER` and `OUT_OF_RANGE` (the two failure reasons)
    - `packages/shared/src/headingValidator.test.ts` contains ≥6 `it(` cases including one asserting CONSECUTIVE_HEADER precedence over distance
    - `packages/shared/src/snapshotValidator.ts` exists and exports `validateSnapshot` and `SnapshotResult`
    - `packages/shared/src/snapshotValidator.ts` references `state.phase` and the three accepting phase literals `'MOVEMENT'`, `'PASS'`, `'SNAPSHOT'`
    - `packages/shared/src/snapshotValidator.ts` returns success with `shootingPenalty: -1` and a `deflectionEffect` containing `maxHexes: 2`
    - `packages/shared/src/snapshotValidator.test.ts` contains ≥4 `it(` cases
    - `packages/shared/src/index.ts` was NOT modified by this task
    - `pnpm --filter=@counter-attack/shared test -- "Validator"` exits 0 with all validator tests in this plan green (heading + snapshot + shot from Task 1)
    - With all three Wave 2 plans (02-02, 02-03, 02-04) complete, `pnpm --filter=@counter-attack/shared build` AND `pnpm --filter=@counter-attack/shared test` both exit 0 (full suite green covering Phase 1 + Plan 01 + Plan 02 + Plan 03 + this plan)
    - No `import` statement in either validator references `socket.io`, `express`, or any random source
  </acceptance_criteria>
  <done>
    `validateHeading` enforces the consecutive-header restriction, OUT_OF_RANGE beyond 2 hexes, uncontested auto-win, and distance-based -1 penalty at distance 2; `excludedPieceIds` carries all participants for HEAD-05. `validateSnapshot` rejects wrong-phase and returns the -1 shooting penalty + 2-hex opponent deflection signal. All ≥10 tests pass; barrel exports were pre-registered in Plan 02-01; full shared suite green once all three Wave 2 plans complete, covering Phase 2's 28 requirement IDs.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                           |
| ------------------- | --------------------------------------------------------------------- |
| (none in this plan) | Pure TypeScript functions; no I/O, no networking, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                         | Disposition | Mitigation Plan                                                                                                                                                                                          |
| --------- | ---------------------------------- | --------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-30   | Tampering                          | SHOT-03 auto-miss precedence      | mitigate    | Auto-miss check on `shooterDice === 1` is the FIRST branch of `validateShotDuel` — attribute calculation cannot bypass it. Test asserts AUTO_MISS even when shooter has shooting=10 and gk has saving=1. |
| T-02-31   | Tampering                          | DICE-04 -2 cap in shot duel       | mitigate    | Shot scores are computed via `computeCombinedScore`, which centrally enforces the cap. Test verifies multi-penalty input clamps at -2.                                                                   |
| T-02-32   | Tampering                          | GK dive distance rules            | mitigate    | Distance thresholds are hard-coded literals (3 = -1 penalty, 4 = OUT_OF_RANGE). Both boundary cases tested.                                                                                              |
| T-02-33   | Tampering                          | Handling spill threshold          | mitigate    | `dice >= handling` is the literal threshold; test verifies the boundary `dice === handling`.                                                                                                             |
| T-02-34   | Tampering                          | Consecutive header restriction    | mitigate    | HEAD-04 enforced via `previousActionWasHeadedPass` parameter checked before geometry; tested directly.                                                                                                   |
| T-02-35   | Information Disclosure             | excludedPieceIds in HeadingResult | N/A         | Excluded piece IDs are caller-supplied + the challenger's own id — no privileged data leakage.                                                                                                           |
| T-02-36   | Denial of Service                  | All validators                    | accept      | Each validator is O(1) or O(otherChallengerIds.length). Inputs bounded by game geometry.                                                                                                                 |
| T-02-37   | Elevation / Spoofing / Repudiation | All validators                    | N/A         | No auth, session, or audit concern in this pure-function phase.                                                                                                                                          |
| T-02-38   | Tampering                          | concurrent index.ts writes        | mitigate    | This plan explicitly does not modify `index.ts`; all three export lines were pre-registered in Wave 1. Eliminates the parallel-execution write race identified by the checker.                           |
| T-02-SC   | Tampering                          | npm/pnpm installs                 | accept      | No new packages installed.                                                                                                                                                                               |

Note: ASVS L1 V2-V6 do not apply.
</threat_model>

<verification>
- `pnpm --filter=@counter-attack/shared test -- "Validator"` exits 0 with shot, heading, and snapshot validator tests passing
- Once all three Wave 2 plans (02-02, 02-03, 02-04) have completed, `pnpm --filter=@counter-attack/shared build` and `pnpm --filter=@counter-attack/shared test` both exit 0 with the FULL suite green
- Phase 2 success criterion "20+ unit tests" is met cumulatively across all four plans (Plan 01 adds ≥19; Plan 02 adds ≥10; Plan 03 adds ≥12; Plan 04 adds ≥17 here — total ≥58 new Phase 2 tests, exceeding the 20+ requirement)
- All three validator files have zero imports from `socket.io`, `express`, or any package outside `packages/shared/src/`
- `git diff packages/shared/src/index.ts` produced by this plan is empty (no modifications)
- Barrel index `packages/shared/src/index.ts` re-exports all five validator modules (move, pass, shot, heading, snapshot) plus scoreUtils — all six lines were authored in Wave 1 by Plan 02-01
</verification>

<success_criteria>

- `validateShotDuel` handles SHOT-01 (combined-score duel), SHOT-03 (auto-miss precedence), and SHOT-06 (composes with handling check via `needsHandlingCheck` flag)
- `validateGKDive` covers SHOT-04 distance rules (3-hex max, -1 at 3rd hex, 4+ unsavable)
- `validateHandlingCheck` enforces SHOT-06 (`dice >= handling` = spill → triggerLooseBall)
- `validateHeading` enforces HEAD-01 (distance penalty), HEAD-02 (uncontested auto-win), HEAD-04 (consecutive restriction), HEAD-05 (excludedPieceIds exposure); HEAD-03 wiring documented
- `validateSnapshot` enforces SNAP-01 wrong-phase rejection, SNAP-02 (-1 shoot penalty + 2-hex deflection effect), SNAP-03 composition note
- All ≥17 new tests pass; `index.ts` is untouched by this plan; barrel exports pre-registered in Plan 02-01
- Phase 2 cumulative test count comfortably exceeds the success criterion "20+ unit tests"
  </success_criteria>

<output>
Create `.planning/phases/02-move-validator-unit-tests/02-04-SUMMARY.md` when done, documenting:
- Final shapes of `ShotDuelResult`, `DiveResult`, `HandlingResult`, `HeadingResult`, `SnapshotResult`
- Whether `aimedAtGoal` HEAD-03 wiring was implemented or deferred
- Cumulative Phase 2 test count and breakdown per module
- Confirmation that `packages/shared/src/index.ts` was not modified by this plan and that the full shared package now builds green
- Any deviations from the planned signatures (especially around HeadingValidator options shape)
</output>
