---
phase: 04-game-engine-phase-fsm
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.test.ts
  - packages/server/src/__tests__/game.integration.test.ts
  - .planning/phases/04-game-engine-phase-fsm/04-RESEARCH.md
autonomous: true
requirements: [TEAM-01, TEAM-03, PITCH-02]

must_haves:
  truths:
    - 'buildInitialGameState produces a KICK_OFF state with 22 placed pieces, a randomly-assigned referee card, a coin-flipped attackingTeam, and an empty eventLog (D-12, D-13, D-14, TEAM-01, TEAM-03)'
    - 'applyStartMovement transitions KICK_OFF→MOVEMENT with movementSlot ATTACKER_4, guarded to the attacking team (D-14, FSM)'
    - 'applyMove re-validates via validateMove, repositions the piece, appends a MOVE ActionEvent, and never auto-advances the slot (D-01, D-06)'
    - 'applyEndTurn advances the slot via advanceMovementSlot (ATTACKER_4→DEFENDER_5→ATTACKER_2→PASS) and appends SLOT_ADVANCE (D-03, D-04)'
    - 'applyUndo pops the last MOVE delta only when no SLOT_ADVANCE/DICE_ROLL exists in the current slot (D-09, D-10)'
    - 'MOVE-06 free-move state is set when the ball carrier crosses between final thirds (D-15)'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'buildInitialGameState, advanceMovementSlot, applyStartMovement, applyMove, applyEndTurn, applyUndo (socket.io-free pure functions)'
      contains: 'export function buildInitialGameState'
    - path: 'packages/server/src/__tests__/gameEngine.test.ts'
      provides: 'unit tests for engine functions + random referee assignment + applyStartMovement transition'
      contains: 'buildInitialGameState'
  key_links:
    - from: 'packages/server/src/gameEngine.ts'
      to: '@counter-attack/shared validateMove'
      via: 'applyMove delegates re-validation'
      pattern: 'validateMove'
    - from: 'packages/server/src/gameEngine.ts'
      to: '@counter-attack/shared HOME_SQUAD/AWAY_SQUAD'
      via: 'buildInitialGameState places squad pieces'
      pattern: 'HOME_SQUAD'
---

<objective>
Create `packages/server/src/gameEngine.ts` — the socket.io-free pure-function core of Phase 4. It builds the initial GameState when both players are present (with a randomly-assigned referee card per TEAM-03), transitions KICK_OFF→MOVEMENT via `applyStartMovement`, applies validated moves, advances the 4-5-2 FSM slot sequence, applies undo within the locked decision boundaries, and sets the MOVE-06 free-move state. All functions return discriminated-union results and never mutate the input state.

This is Wave 2 — depends on Wave 1's shared types/data (ActionEvent, RefereeCard, MovementSlot, HOME_SQUAD/AWAY_SQUAD, PITCH_REGIONS). Wave 3 wires these functions to socket handlers.

Purpose: ARCH-01 server-authority requires all FSM transitions and state mutations to live server-side as testable pure logic. Keeping the engine socket-free (the established Phase 2/3 pattern) means it is unit-tested without a server harness. `applyStartMovement` provides the FSM edge that makes the Movement Phase reachable over the wire (Phase goal: 4-5-2 sequence end-to-end).
Output: gameEngine.ts (pure functions), gameEngine.test.ts (unit, Wave 0 stub turned green), and the game.integration.test.ts skeleton (lifecycle harness + setupRoom helper, scenarios filled in Wave 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-game-engine-phase-fsm/04-CONTEXT.md
@.planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md
@.planning/phases/04-game-engine-phase-fsm/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildInitialGameState + advanceMovementSlot + applyStartMovement</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.test.ts</files>
  <read_first>
    - packages/shared/src/types.ts (GameState, GamePhase, MovementSlot, RefereeCard, ActionEvent — AFTER Wave 1)
    - packages/shared/src/teams.ts (HOME_SQUAD, AWAY_SQUAD)
    - packages/shared/src/pitch.ts (PITCH_REGIONS.kickOffHex)
    - packages/server/src/roomStore.ts (module-header + pure-function JSDoc style; the stub GameState literal in joinRoom lines ~141-156 shows the full GameState shape to fill)
    - packages/server/src/__tests__/roomStore.test.ts (Vitest unit style + clearAllRooms afterEach)
    - .planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md ("gameEngine.ts NEW" — header, buildInitialGameState, advanceMovementSlot, SLOT_SEQUENCE)
  </read_first>
  <behavior>
    - buildInitialGameState(roomCode) returns phase 'KICK_OFF', movementSlot null, eventLog [], score {home:0,away:0}, actionCount 0, half 1
    - pieces array has 22 entries — all of HOME_SQUAD and AWAY_SQUAD
    - refereeCard.leniency is randomly assigned (TEAM-03) using `crypto.randomInt(1, 7)` → integer in 1..6; running buildInitialGameState repeatedly yields more than one distinct leniency value
    - attackingTeam is 'home' or 'away' (coin flip); ball.position equals PITCH_REGIONS.kickOffHex
    - advanceMovementSlot: ATTACKER_4 → {nextSlot:'DEFENDER_5', nextPhase:'MOVEMENT'}; DEFENDER_5 → {nextSlot:'ATTACKER_2', nextPhase:'MOVEMENT'}; ATTACKER_2 → {nextSlot:null, nextPhase:'PASS'}
    - applyStartMovement on non-KICK_OFF phase → {ok:false, reason:'WRONG_PHASE'}; on success returns new state with phase 'MOVEMENT', movementSlot 'ATTACKER_4', and a KICK_OFF (or MOVEMENT_START) ActionEvent appended
  </behavior>
  <action>
    Create packages/server/src/gameEngine.ts with a module header JSDoc (responsibilities + ARCH-01 + D-12/D-13/D-14 references). Import `randomInt` from 'crypto', type-import GameState/GamePhase/MovementSlot/RefereeCard/ActionEvent/PlayerPiece from '@counter-attack/shared', and value-import HOME_SQUAD/AWAY_SQUAD/PITCH_REGIONS. No socket.io imports in this file (pure functions only — established Phase 2/3 pattern).

    Export `buildInitialGameState(roomCode: string): GameState`. Use `randomInt(0, 2) === 0` for the coin flip (D-13 — server-side randomness only; never client). Set `attackingTeam` from the flip. Build `pieces` as `[...HOME_SQUAD, ...AWAY_SQUAD]` (TEAM-01 — all 22 loaded at match start). Set `refereeCard: { leniency: randomInt(1, 7) }` (TEAM-03 — referee card randomly assigned at match start; `crypto.randomInt(1, 7)` yields an integer in 1..6 matching the dice-face range). This reuses the `randomInt` import already present for the coin flip — one additional call, no static constant and no Phase 8 deferral for the value. Set `phase: 'KICK_OFF'` (D-14), `movementSlot: null`, `activeTeam: attackingTeam`, `ball: { position: PITCH_REGIONS.kickOffHex, carrierId: null }`, `score: { home: 0, away: 0 }`, `actionCount: 0`, `half: 1`, `eventLog: []`, `movedPieceIds: []`, `paceUsedByPieceId: {}`. Add JSDoc citing D-12/D-13/D-14, TEAM-01, TEAM-03.

    Add module-private `const SLOT_SEQUENCE: readonly MovementSlot[] = ['ATTACKER_4', 'DEFENDER_5', 'ATTACKER_2']`. Export `advanceMovementSlot(state: GameState): { nextSlot: MovementSlot | null; nextPhase: GamePhase }` — find the index of state.movementSlot in SLOT_SEQUENCE; if it is the last (ATTACKER_2), return `{ nextSlot: null, nextPhase: 'PASS' }` (D-04 auto-transition); otherwise return the next slot with `nextPhase: 'MOVEMENT'` (D-03). This is the explicit-FSM-object pattern (STATE.md locked decision) — no if/else slot chains anywhere else.

    Export the result union `ApplyStartMovementResult` ({ok:false; reason:'WRONG_PHASE'} | {ok:true; state:GameState}) and `applyStartMovement(state: GameState): ApplyStartMovementResult` — guard-first: if `state.phase !== 'KICK_OFF'` return {ok:false, reason:'WRONG_PHASE'}. On success build a NEW state via spread (never mutate readonly): set `phase: 'MOVEMENT'`, `movementSlot: 'ATTACKER_4'` (the Movement Phase always opens on the attacker's 4-move slot), `activeTeam: state.attackingTeam`, and append a KICK_OFF ActionEvent (`{ type: 'KICK_OFF', timestamp: Date.now() }`) to eventLog marking the kick-off→movement edge (event-type choice is Claude's discretion per D-07 — a dedicated MOVEMENT_START variant is acceptable if added to ActionEventType in Wave 1, otherwise reuse KICK_OFF). This is the FSM edge that makes MOVEMENT reachable over the wire; the player-ownership guard (only the attacking team may start movement) is enforced in the Wave 3 handler (04-03/T1), since the engine is socket-free and has no socket identity.

    Create packages/server/src/__tests__/gameEngine.test.ts mirroring roomStore.test.ts header (`import { describe, it, expect } from 'vitest'`, no clearAllRooms needed since engine is pure but include it if importing roomStore helpers — engine functions need none). Test buildInitialGameState: phase 'KICK_OFF', 22 pieces, attackingTeam in {'home','away'}, ball.position === kickOffHex, eventLog empty. For TEAM-03, assert `refereeCard.leniency` is an integer satisfying `>= 1 && <= 6`, AND assert randomness: call buildInitialGameState 10 times and assert the set of observed leniency values has size >= 2 (proves it is not a static constant — TEAM-03 random assignment). Test advanceMovementSlot for all three transitions. Test applyStartMovement: from a KICK_OFF state returns ok:true with phase 'MOVEMENT' and movementSlot 'ATTACKER_4'; from a MOVEMENT state returns {ok:false, reason:'WRONG_PHASE'}. Reference TEAM-01/TEAM-03 in test names.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test src/__tests__/gameEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts contains `export function buildInitialGameState`, `export function advanceMovementSlot`, and `export function applyStartMovement`
    - gameEngine.ts imports `randomInt` from 'crypto' and has zero `socket.io` imports
    - buildInitialGameState sets `refereeCard: { leniency: randomInt(1, 7) }` (no static `{ leniency: 3 }`, no Phase 8 TODO on the value) — TEAM-03
    - gameEngine.test.ts asserts random referee leniency (>=2 distinct values across 10 builds) and leniency in 1..6 (TEAM-03 cited)
    - buildInitialGameState test asserts 22 pieces and phase 'KICK_OFF' (TEAM-01 cited)
    - advanceMovementSlot test asserts ATTACKER_2 → {nextSlot:null, nextPhase:'PASS'} (D-04)
    - applyStartMovement test asserts KICK_OFF→MOVEMENT with movementSlot 'ATTACKER_4' and WRONG_PHASE outside KICK_OFF
    - `pnpm --filter @counter-attack/server test src/__tests__/gameEngine.test.ts` exits 0
  </acceptance_criteria>
  <done>Initial-state construction (with random referee card), slot FSM, and the KICK_OFF→MOVEMENT transition are implemented as pure functions and unit-tested green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: applyMove, applyEndTurn, applyUndo + MOVE-06 free-move state</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (Task 1 output — buildInitialGameState, advanceMovementSlot, applyStartMovement, SLOT_SEQUENCE)
    - packages/shared/src/moveValidator.ts (validateMove signature + MoveResult union: WRONG_SLOT/OUT_OF_RANGE/OCCUPIED/PACE_EXCEEDED/ALREADY_MOVED_IN_ATTACKER4 + STEAL_ATTEMPT effect)
    - packages/shared/src/pitch.ts (isInRegion for homeThird/awayThird — MOVE-06 final-third crossing detection)
    - .planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md ("gameEngine.ts NEW" — ApplyMoveResult/ApplyEndTurnResult/ApplyUndoResult, guard-first applyMove, immutable spread, stubDice) and RESEARCH Pitfalls 1/2/4/7/8
  </read_first>
  <behavior>
    - applyMove on non-MOVEMENT phase → {ok:false, reason:'WRONG_SLOT'}; unknown pieceId → {ok:false, reason:'PIECE_NOT_FOUND'}; piece not on the slot's acting team → {ok:false, reason:'WRONG_TEAM'}
    - applyMove on validateMove failure → {ok:false, reason:'MOVE_INVALID', detail:<validator reason>}
    - applyMove success → new state with piece repositioned, paceUsedByPieceId incremented, MOVE ActionEvent appended; movementSlot unchanged (no auto-advance, D-01)
    - applyMove with STEAL_ATTEMPT effect → resolves with stubDice() (fixed 3) and appends STEAL_ATTEMPT event (Phase 5 replaces dice)
    - applyMove where ball carrier crosses homeThird↔awayThird → sets pendingFreeMove free-move state (D-15, MOVE-06)
    - applyEndTurn → advanceMovementSlot result applied; SLOT_ADVANCE appended; movedPieceIds/paceUsedByPieceId reset for the new slot
    - applyUndo blocked ({ok:false, reason:'UNDO_LOCKED'}) if a SLOT_ADVANCE or DICE_ROLL exists since the last SLOT_ADVANCE; otherwise pops last MOVE and reverses position+pace (D-09, D-10); {ok:false,reason:'NOTHING_TO_UNDO'} if no MOVE in current slot
  </behavior>
  <action>
    In gameEngine.ts export the three result unions per 04-PATTERNS.md: `ApplyMoveResult` ({ok:false; reason:'WRONG_SLOT'|'WRONG_TEAM'|'PIECE_NOT_FOUND'|'MOVE_INVALID'; detail?:string} | {ok:true; state:GameState}), `ApplyEndTurnResult` ({ok:false;reason:'WRONG_SLOT'|'WRONG_TEAM'} | {ok:true;state:GameState}), `ApplyUndoResult` ({ok:false;reason:'UNDO_LOCKED'|'NOTHING_TO_UNDO'} | {ok:true;state:GameState}).

    Add module-private `activeTeamForSlot(state): 'home'|'away'` mapping ATTACKER_4/ATTACKER_2 → state.attackingTeam and DEFENDER_5 → the other team (RESEARCH Pitfall 2 — attacker vs moving team). Add `// TODO Phase 5: replace with crypto.randomInt(1, 7)` stubDice returning 3 (RESEARCH Pitfall 7).

    Export `applyMove(state, pieceId, to): ApplyMoveResult` guard-first: (1) phase !== 'MOVEMENT' || movementSlot===null → WRONG_SLOT; (2) piece = pieces.find by id, missing → PIECE_NOT_FOUND; (3) piece.teamId !== activeTeamForSlot(state) → WRONG_TEAM (T-4-01 spoofing guard at engine layer; handler also guards by socket slot in Wave 3); (4) result = validateMove(state, piece, to); if !result.ok → {ok:false, reason:'MOVE_INVALID', detail: result.reason}. On success build NEW state via spread (RESEARCH Pitfall 1 — never mutate readonly): map pieces to reposition the moved piece; increment paceUsedByPieceId[pieceId]; append a MOVE ActionEvent using `from: piece.position` (server-derived, never client from-coord — T-4-03) with the current slot and Date.now(). If validateMove returned a STEAL_ATTEMPT effect, call stubDice() and append a STEAL_ATTEMPT event with result 'SUCCESS'/'FAIL' (compare stub 3 + tackling vs threshold per scoreUtils convention) — keep deterministic for tests. MOVE-06: if the moved piece is the ball carrier (state.ball.carrierId === pieceId), compute previous final-third membership (isInRegion(piece.position,...)) vs destination membership; if it crossed homeThird↔awayThird (Pitfall 8 — only cross between the two final thirds, not middle-third entries), set a `pendingFreeMove` free-move marker on the returned state (shape Claude's discretion per A5, e.g. `{ team, hexesAllowed: 6 }`) and do NOT auto-advance the slot.

    Export `applyEndTurn(state): ApplyEndTurnResult` — guard phase/slot; call advanceMovementSlot; return new state with updated movementSlot + phase, a SLOT_ADVANCE ActionEvent appended (from: old slot, to: new slot), and movedPieceIds/paceUsedByPieceId reset to []/{} for the new slot. D-04: when nextPhase is 'PASS', set phase 'PASS' and movementSlot null.

    Export `applyUndo(state): ApplyUndoResult` — find the slice of eventLog since the last SLOT_ADVANCE (RESEARCH Pitfall 4 — current slot only). If that slice contains any SLOT_ADVANCE or DICE_ROLL → UNDO_LOCKED (D-09). Find the last MOVE in that slice; if none → NOTHING_TO_UNDO. Otherwise return new state with that piece moved back to the MOVE event's `from`, paceUsedByPieceId decremented for that piece, and the MOVE event removed from eventLog (D-10). Immutable spread throughout.

    Extend gameEngine.test.ts: build a `baseState` MOVEMENT fixture (movementSlot 'ATTACKER_4', attackingTeam 'home', refereeCard {leniency:3}, pieces with name/role) per 04-PATTERNS.md fixture pattern — note the fixture may hardcode leniency for determinism; this does not contradict TEAM-03 (which governs buildInitialGameState's random assignment, tested in Task 1). Add unit tests: applyMove WRONG_SLOT outside MOVEMENT (MOVE-01); PIECE_NOT_FOUND; WRONG_TEAM for opponent piece (T-4-01); MOVE_INVALID surfaced for occupied/pace (MOVE-02, MOVE-03); success appends MOVE event and does not change movementSlot (D-01); MOVE-06 sets free-move state on final-third crossing; applyEndTurn advances slot and appends SLOT_ADVANCE; applyUndo reverses last MOVE (D-10) and is UNDO_LOCKED after a SLOT_ADVANCE (D-09). Reference req/decision ids in test names.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test src/__tests__/gameEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts exports `applyMove`, `applyEndTurn`, `applyUndo` and the three Apply*Result types
    - applyMove builds MOVE event with `from: piece.position` (server-derived, not a parameter)
    - gameEngine.ts contains `// TODO Phase 5` on the stubDice function
    - test asserts applyMove returns WRONG_TEAM for an opponent-owned piece (T-4-01)
    - test asserts applyUndo returns UNDO_LOCKED after a SLOT_ADVANCE in the current slot (D-09)
    - test asserts MOVE-06 free-move state set when ball carrier crosses final thirds (D-15)
    - `pnpm --filter @counter-attack/server test src/__tests__/gameEngine.test.ts` exits 0
  </acceptance_criteria>
  <done>All engine mutation functions implemented immutably with guard-first results; unit suite green.</done>
</task>

<task type="auto">
  <name>Task 3: game.integration.test.ts skeleton (lifecycle harness + setupRoom) + mark RESEARCH open questions resolved</name>
  <files>packages/server/src/__tests__/game.integration.test.ts, .planning/phases/04-game-engine-phase-fsm/04-RESEARCH.md</files>
  <read_first>
    - packages/server/src/__tests__/room.integration.test.ts (server lifecycle: beforeEach listen(0)/afterEach close, createClient, oncePromise, waitForConnect helpers — copy exactly)
    - packages/server/src/createServer.ts (buildServer factory signature)
    - packages/shared/src/events.ts (ClientEvents/ServerEvents — AFTER Wave 1, includes GAME_MOVE pieceId signature, GAME_END_TURN, GAME_UNDO, GAME_START_MOVEMENT, GAME_ERROR)
    - .planning/phases/04-game-engine-phase-fsm/04-RESEARCH.md (the `## Open Questions` section near line 636 — three questions to mark resolved)
    - .planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md ("game.integration.test.ts NEW" — lifecycle, createClient, oncePromise, setupRoom helper, scenario shape)
  </read_first>
  <behavior>
    - The test file imports buildServer, sets up/tears down an http server on port 0, and exposes a setupRoom() helper that creates a room, joins a second client, and resolves the KICK_OFF GameState
    - One smoke scenario passes: after setupRoom the broadcast state has phase 'KICK_OFF' with 22 pieces (proves end-to-end init wiring will be testable in Wave 3)
    - All other MOVE-01..MOVE-06 / FSM / undo / isProcessing scenarios are present as `it.todo(...)` placeholders so Wave 3 fills them
    - RESEARCH.md `## Open Questions` heading is renamed to `## Open Questions (RESOLVED)` and each of the three questions is prefixed with `RESOLVED:`
  </behavior>
  <action>
    Create packages/server/src/__tests__/game.integration.test.ts. Copy the server lifecycle harness from room.integration.test.ts verbatim: the `httpServer`/`address`/`connectedClients` module vars, `beforeEach` that calls buildServer() and listens on port 0, `afterEach` that disconnects clients + closes the server + clearAllRooms(). Copy the `createClient`, `oncePromise`, and `waitForConnect` helpers exactly (no changes). Add a `setupRoom()` async helper per 04-PATTERNS.md that: creates two clients, waits for both to connect, emits ROOM_CREATE from client A and awaits ROOM_JOINED for the roomCode, then emits ROOM_JOIN from client B and awaits the GAME_STATE broadcast, returning { clientA, clientB, roomCode, state }. Add ONE real `it` smoke test asserting the setupRoom state has `phase === 'KICK_OFF'` and `pieces.length === 22` (this exercises the Wave 3 roomStore→buildInitialGameState wiring once present; until Wave 3 wires it the state may be 'LOBBY' — so gate this assertion behind the same it that Wave 3 will complete; if the wiring is not yet present, keep it as `it.todo`). Add `it.todo` placeholders named for: MOVE-01 WRONG_PHASE, MOVE-02 PACE, MOVE-03 OCCUPIED, MOVE-04/05 STEAL, MOVE-06 free-move, FSM ATTACKER_4→DEFENDER_5→ATTACKER_2→PASS sequencing, D-09/D-10 undo, isProcessing duplicate drop. Do not implement scenario bodies — Wave 3 fills them (now reachable via game:start-movement). The file must compile and the suite must run green (todos do not fail).

    Also resolve the RESEARCH open-questions warning (low-touch documentation fix): in .planning/phases/04-game-engine-phase-fsm/04-RESEARCH.md, rename the `## Open Questions` heading (near line 636) to `## Open Questions (RESOLVED)`, and prefix each of the three numbered question answers with `RESOLVED:` (e.g. the Recommendation line for OQ-1 becomes `RESOLVED: Add pieceId...`). This records that all three open questions were settled during planning — OQ-1 (pieceId payload) is implemented in 04-01, OQ-2 (attackingTeam vs activeTeam) is implemented in 04-02, OQ-3 (MOVE-06 free-move handling) is implemented in 04-02 Task 2. Do not change the question text itself, only the heading suffix and the RESOLVED prefixes.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test src/__tests__/game.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - game.integration.test.ts imports `buildServer` and contains a `setupRoom` helper returning { clientA, clientB, roomCode, state }
    - file contains `it.todo(` placeholders for MOVE-01, MOVE-06, FSM sequencing, undo, and isProcessing
    - 04-RESEARCH.md contains the heading `## Open Questions (RESOLVED)` and each of the three questions carries a `RESOLVED:` prefix
    - `pnpm --filter @counter-attack/server test src/__tests__/game.integration.test.ts` exits 0 (todos counted, no failures)
  </acceptance_criteria>
  <done>Integration harness and setupRoom helper exist; scenario todos enumerate Wave 3 work; RESEARCH open questions marked resolved; suite runs green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                                                         |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client socket → gameEngine | game action intents reach engine functions in Wave 3; the engine re-validates everything (ARCH-01). This plan establishes the engine guards the handlers depend on. |

## STRIDE Threat Register

| Threat ID | Category               | Component                    | Disposition | Mitigation Plan                                                                                                                |
| --------- | ---------------------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-4-01    | Spoofing               | `applyMove` WRONG_TEAM guard | mitigate    | applyMove rejects pieces whose teamId != activeTeamForSlot(state); Wave 3 handler adds the socket-slot guard on top            |
| T-4-03    | Tampering              | `applyMove` from-coord       | mitigate    | MOVE event records `from: piece.position` looked up from state.pieces by id; client never supplies an authoritative from-coord |
| T-4-04    | Elevation of Privilege | `applyEndTurn` slot guard    | mitigate    | applyEndTurn guards phase/slot; Wave 3 handler checks socket slot ownership before calling it                                  |
| T-4-05    | Elevation of Privilege | `applyStartMovement` guard   | mitigate    | applyStartMovement rejects non-KICK_OFF phase; Wave 3 handler restricts the event to the attacking team's socket               |
| T-4-02    | Tampering              | double-action race           | transfer    | isProcessing mutex is a handler concern (Wave 3); engine functions are pure and side-effect-free                               |
| T-4-SC    | Tampering              | npm installs                 | accept      | No new packages in Phase 4 (RESEARCH §Package Legitimacy Audit)                                                                |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` exits 0 (gameEngine unit suite + integration skeleton green)
- `pnpm -r build` exits 0 (server consumes Wave 1 shared types cleanly)
- grep confirms gameEngine.ts has no `from 'socket.io'` import
- grep confirms gameEngine.ts uses `randomInt(1, 7)` for the referee card (no static `leniency: 3` in buildInitialGameState)
</verification>

<success_criteria>

- buildInitialGameState loads both squads (TEAM-01), randomly assigns a referee card (TEAM-03), coin-flips attackingTeam (D-13), and sets KICK_OFF (D-14)
- applyStartMovement transitions KICK_OFF→MOVEMENT (movementSlot ATTACKER_4), making the Movement Phase reachable over the wire
- applyMove re-validates and never auto-advances the slot (D-01, D-06)
- FSM enforces ATTACKER_4→DEFENDER_5→ATTACKER_2→PASS (D-03, D-04)
- undo respects the D-09 boundary and D-10 reversal
- MOVE-06 free-move state set on final-third crossing (D-15)
- integration harness ready for Wave 3 scenarios; RESEARCH open questions marked resolved
  </success_criteria>

<output>
Create `.planning/phases/04-game-engine-phase-fsm/04-02-SUMMARY.md` when done.
</output>
</tasks>
