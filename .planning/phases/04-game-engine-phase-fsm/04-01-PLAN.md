---
phase: 04-game-engine-phase-fsm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/shared/src/teams.ts
  - packages/shared/src/pitch.ts
  - packages/shared/src/index.ts
  - packages/shared/src/teams.test.ts
  - packages/shared/src/pitch.test.ts
autonomous: true
requirements: [TEAM-01, TEAM-02, TEAM-03, PITCH-01, PITCH-02, PITCH-03]

must_haves:
  truths:
    - 'Both hardcoded squads expose 11 players each with all 9 attributes plus name and role (TEAM-01, TEAM-02)'
    - 'GameState carries a RefereeCard type and a typed ActionEvent[] eventLog (TEAM-03, D-08)'
    - 'Pitch regions (final thirds, penalty areas, 6-yard boxes, centre circle, kickoff hex) are queryable via O(1) Set membership (PITCH-02)'
    - 'Difficult-angle hexes are encoded and queryable (PITCH-03)'
    - 'game:end-turn, game:undo, game:error, and game:start-movement events exist as typed Socket.io event entries'
  artifacts:
    - path: 'packages/shared/src/teams.ts'
      provides: 'HOME_SQUAD and AWAY_SQUAD readonly PlayerPiece[] (11 each)'
      contains: 'export const HOME_SQUAD'
    - path: 'packages/shared/src/pitch.ts'
      provides: 'PITCH_REGIONS, DIFFICULT_ANGLE_HEXES, isInRegion, isDifficultAngle, isPitchHex'
      contains: 'export const PITCH_REGIONS'
    - path: 'packages/shared/src/types.ts'
      provides: 'ActionEvent union, RefereeCard, MovementSlot, extended PlayerPiece/GameState'
      contains: 'export type ActionEvent'
    - path: 'packages/shared/src/events.ts'
      provides: 'GAME_END_TURN, GAME_UNDO, GAME_ERROR, GAME_START_MOVEMENT + typed maps'
      contains: 'GAME_END_TURN'
  key_links:
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/teams.js'
      via: 'barrel re-export'
      pattern: "export \\* from './teams.js'"
    - from: 'packages/shared/src/pitch.ts'
      to: 'packages/shared/src/hex.js'
      via: 'hexesInRange import for centreCircle'
      pattern: 'hexesInRange'
---

<objective>
Extend the `@counter-attack/shared` package with the data and type foundations Phase 4 needs: the `ActionEvent` discriminated union and `RefereeCard` type (replacing the `readonly unknown[]` eventLog placeholder), `name`/`role` fields on `PlayerPiece`, the `attackingTeam` field on `GameState`, two hardcoded 11-player squads, encoded pitch regions with O(1) membership helpers, difficult-angle hexes, and the new Socket.io event constants (`game:end-turn`, `game:undo`, `game:error`, `game:start-movement`).

This is Wave 1 — pure data and types, no server dependency. All four downstream waves consume these symbols.

Purpose: Phase 4 adds wiring and data, not new algorithms. This plan delivers the data + type contracts that gameEngine.ts (Wave 2) and gameHandlers.ts (Wave 3) build against, so executors receive concrete contracts rather than exploring.
Output: Extended types.ts, events.ts, pitch.ts; new teams.ts; updated index.ts barrel; teams.test.ts and pitch.test.ts (Wave 0 stubs turned green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-game-engine-phase-fsm/04-CONTEXT.md
@.planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend types.ts and events.ts with Phase 4 contracts</name>
  <files>packages/shared/src/types.ts, packages/shared/src/events.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/types.ts (current PlayerPiece, GameState, GamePhase, movementSlot inline union — see exact line state in 04-PATTERNS.md "types.ts EXTEND")
    - packages/shared/src/events.ts (current ClientEvents/ServerEvents const objects and ClientToServerEvents/ServerToClientEvents interfaces)
    - packages/shared/src/index.ts (existing barrel export ordering)
    - .planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md (exact extend snippets for types.ts and events.ts)
  </read_first>
  <behavior>
    - types.ts exports a named `MovementSlot = 'ATTACKER_4' | 'DEFENDER_5' | 'ATTACKER_2'` and GameState.movementSlot becomes `MovementSlot | null`
    - PlayerPiece gains `name: string` and `role: 'GK' | 'DEF' | 'MID' | 'FWD'`
    - types.ts exports `RefereeCard = { leniency: number }` and `ActionEventType` string union
    - types.ts exports `ActionEvent` discriminated union with variants MOVE | SLOT_ADVANCE | DICE_ROLL | STEAL_ATTEMPT | GOAL | KICK_OFF (shapes per D-07)
    - GameState.eventLog becomes `readonly ActionEvent[]`; GameState gains `refereeCard: RefereeCard` and `attackingTeam: 'home' | 'away'`
    - events.ts: ClientEvents gains GAME_END_TURN:'game:end-turn', GAME_UNDO:'game:undo', and GAME_START_MOVEMENT:'game:start-movement'; ServerEvents gains GAME_ERROR:'game:error'
    - events.ts: ClientToServerEvents.GAME_MOVE signature changes to `(pieceId: string, to: HexCoord) => void`; gains GAME_END_TURN, GAME_UNDO, and GAME_START_MOVEMENT as `() => void`; ServerToClientEvents gains GAME_ERROR as `(reason: string) => void`
  </behavior>
  <action>
    In packages/shared/src/types.ts, add `export type MovementSlot = 'ATTACKER_4' | 'DEFENDER_5' | 'ATTACKER_2'` (export it as a named type per D-07/A1 so ActionEvent and gameEngine can reference it). Add `name: string` and `role: 'GK' | 'DEF' | 'MID' | 'FWD'` to PlayerPiece after `aerialAbility`. Add `export type RefereeCard = { leniency: number }` (TEAM-03). Add `export type ActionEventType = 'MOVE' | 'SLOT_ADVANCE' | 'DICE_ROLL' | 'STEAL_ATTEMPT' | 'GOAL' | 'KICK_OFF'`. Add the `export type ActionEvent` discriminated union with the six variants exactly as specified in 04-PATTERNS.md (MOVE carries pieceId/from/to/slot/timestamp; SLOT_ADVANCE carries from/to(MovementSlot|null)/timestamp; DICE_ROLL carries result/timestamp; STEAL_ATTEMPT carries defenderId/result('SUCCESS'|'FAIL')/timestamp; GOAL carries scoringTeam/timestamp; KICK_OFF carries timestamp). In GameState: replace `eventLog: readonly unknown[]` with `eventLog: readonly ActionEvent[]` (D-08); replace the inline `movementSlot` union with `movementSlot: MovementSlot | null`; add `refereeCard: RefereeCard` (TEAM-03) and `attackingTeam: 'home' | 'away'` (A1, set at KICK_OFF, constant through Movement Phase). Preserve all existing JSDoc on the D-08 movement-tracking fields.

    In packages/shared/src/events.ts, append `GAME_END_TURN: 'game:end-turn'`, `GAME_UNDO: 'game:undo'`, and `GAME_START_MOVEMENT: 'game:start-movement'` inside the ClientEvents `as const` object, and `GAME_ERROR: 'game:error'` inside ServerEvents. The `game:start-movement` event is the wire path that transitions the FSM out of KICK_OFF into MOVEMENT (without it the Movement Phase is unreachable over the wire — see 04-02/T1 applyStartMovement and 04-03/T1 handler). Change `[ClientEvents.GAME_MOVE]` in ClientToServerEvents from `(from: HexCoord, to: HexCoord) => void` to `(pieceId: string, to: HexCoord) => void` (RESEARCH OQ-1 — pieceId removes adjacency ambiguity). Add `[ClientEvents.GAME_END_TURN]: () => void`, `[ClientEvents.GAME_UNDO]: () => void`, and `[ClientEvents.GAME_START_MOVEMENT]: () => void` to ClientToServerEvents. Add `[ServerEvents.GAME_ERROR]: (reason: string) => void` to ServerToClientEvents. Keep the existing `import type { HexCoord, GameState }` line.

    In packages/shared/src/index.ts, add `export * from './teams.js'` after the existing `./pitch.js` export line. Do not reorder existing exports. No code blocks needed — types.ts re-exports flow through the existing `export * from './types.js'`.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared build</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `export type MovementSlot = 'ATTACKER_4' | 'DEFENDER_5' | 'ATTACKER_2'`
    - types.ts contains `export type ActionEvent` and `export type RefereeCard`
    - types.ts contains `eventLog: readonly ActionEvent[]` (no remaining `readonly unknown[]`)
    - types.ts GameState contains `refereeCard: RefereeCard` and `attackingTeam: 'home' | 'away'`
    - PlayerPiece contains `name: string` and `role: 'GK' | 'DEF' | 'MID' | 'FWD'`
    - events.ts contains `GAME_END_TURN: 'game:end-turn'`, `GAME_UNDO: 'game:undo'`, `GAME_ERROR: 'game:error'`, `GAME_START_MOVEMENT: 'game:start-movement'`
    - events.ts ClientToServerEvents declares GAME_MOVE as `(pieceId: string, to: HexCoord) => void` and declares GAME_START_MOVEMENT as `() => void`
    - index.ts contains `export * from './teams.js'`
    - `pnpm --filter @counter-attack/shared build` exits 0
  </acceptance_criteria>
  <done>Shared types and events compile with all Phase 4 contracts present (including game:start-movement); barrel re-exports teams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create teams.ts with two hardcoded squads + teams.test.ts</name>
  <files>packages/shared/src/teams.ts, packages/shared/src/teams.test.ts</files>
  <read_first>
    - packages/shared/src/types.ts (PlayerPiece shape AFTER Task 1 — must include name, role, all 9 attributes, id, teamId, position)
    - packages/shared/src/pitch.ts (static-data export + block-comment header pattern to mirror)
    - packages/shared/src/moveValidator.test.ts (Vitest describe/it/expect unit test style for shared package)
    - .planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md ("teams.ts NEW" — attribute ranges, id scheme, starting positions)
  </read_first>
  <behavior>
    - HOME_SQUAD has exactly 11 players with ids 'home-0'..'home-10'; AWAY_SQUAD has 11 with ids 'away-0'..'away-10'
    - Every player carries all 9 attributes as integers in 1..10, plus name (non-empty), role, teamId, position
    - Each squad has exactly 1 player with role 'GK'
    - GK has high saving/handling, low pace/shooting; FWD has high pace/shooting, low tackling/saving (role-appropriate ranges per 04-PATTERNS.md)
    - teams.test.ts asserts the above for both squads (TEAM-01, TEAM-02)
  </behavior>
  <action>
    Create packages/shared/src/teams.ts mirroring pitch.ts's `import type { PlayerPiece } from './types.js'`, block-comment header, and `readonly` export pattern. Export `HOME_SQUAD: readonly PlayerPiece[]` and `AWAY_SQUAD: readonly PlayerPiece[]`, each with 11 fully-populated PlayerPiece literals. Use deterministic ids `'home-0'`..`'home-10'` and `'away-0'`..`'away-10'` (RESEARCH §Don't Hand-Roll — no UUID). Set `teamId` accordingly. Assign roles: 1 GK, 4 DEF, 3 MID, 3 FWD per squad. Attribute values are integers 1–10 following 04-PATTERNS.md role conventions (GK: saving 8–10, handling 7–9, pace 2–4, shooting 1–2; DEF: tackling 7–9, pace 4–6, shooting 2–4; MID: balanced 5–7; FWD: pace 7–9, shooting 7–9, tackling 1–3, saving 1). Set readable `name` per player (e.g. 'Home GK', 'Home DEF 1'). Set placeholder starting `position`: home GK `{ q: 0, r: 7 }`, home DEF in q 2–4, MID in q 5–8, FWD in q 9–11 (spread r values across 0–15); away squad symmetric (q 24 down to q 12). Mark positions as placeholder-grid in the header comment (Phase 6 replaces with real coords). Define a single static `RefereeCard` is NOT in this file — referee card construction lives in gameEngine.ts (Wave 2); teams.ts is squad data only.

    Create packages/shared/src/teams.test.ts mirroring moveValidator.test.ts header (`import { describe, it, expect } from 'vitest'`). Add describe blocks for HOME_SQUAD and AWAY_SQUAD asserting: length 11 (TEAM-01); every attribute is an integer 1..10 (TEAM-01); every player has non-empty name and a valid role (TEAM-02); exactly 1 GK per squad; all ids unique and following the prefix scheme. Reference TEAM-01/TEAM-02 in test names.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test src/teams.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - teams.ts contains `export const HOME_SQUAD` and `export const AWAY_SQUAD`
    - HOME_SQUAD and AWAY_SQUAD each have length 11 (asserted in teams.test.ts)
    - teams.test.ts asserts every attribute integer in 1..10 and exactly 1 GK per squad
    - teams.test.ts asserts name and role present on every player (TEAM-02 cited in a test name)
    - `pnpm --filter @counter-attack/shared test src/teams.test.ts` exits 0
  </acceptance_criteria>
  <done>Both squads exist with 11 fully-attributed players each and the unit suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend pitch.ts with region encoding + pitch.test.ts</name>
  <files>packages/shared/src/pitch.ts, packages/shared/src/pitch.test.ts</files>
  <read_first>
    - packages/shared/src/pitch.ts (existing PITCH_HEXES export — preserve unchanged, append below it)
    - packages/shared/src/hex.ts (hexesInRange signature for centreCircle derivation)
    - packages/shared/src/moveValidator.test.ts (Vitest style)
    - .planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md ("pitch.ts EXTEND" — hexKey helper, PitchRegions type, Set-based regions, DIFFICULT_ANGLE_HEXES, predicates) and RESEARCH Pitfall 6 (Set over Array.includes)
  </read_first>
  <behavior>
    - isInRegion(hex, 'homeThird') is true for q<=7 hexes, false for q>=17 hexes; isInRegion(hex, 'awayThird') symmetric
    - isInRegion(hex, 'homePenaltyArea') true for a known inside hex (e.g. {q:1,r:7}), false for a midfield hex
    - isDifficultAngle returns true for an encoded dot-marked hex and false for centre
    - isPitchHex returns true for an in-grid hex and false for an out-of-grid hex (e.g. {q:99,r:99})
    - PITCH_REGIONS.kickOffHex equals {q:12,r:7}; PITCH_HEXES still exports 400 hexes (PITCH-01 unchanged)
  </behavior>
  <action>
    In packages/shared/src/pitch.ts, keep the existing `PITCH_HEXES` export unchanged. Add `import { hexesInRange } from './hex.js'`. Add a module-private `hexKey = (h: HexCoord): string => \`${h.q},${h.r}\`` and a `buildRegion = (hexes: HexCoord[]): ReadonlySet<string> => new Set(hexes.map(hexKey))` (RESEARCH Pitfall 6 — Set over Array.includes for structural equality). Export `type PitchRegions` with ReadonlySet<string> fields: homeThird, awayThird, middleThird, homePenaltyArea, awayPenaltyArea, homeSixYardBox, awaySixYardBox, centreCircle, plus `kickOffHex: HexCoord`. Export `const PITCH_REGIONS: PitchRegions` deriving each region by filtering PITCH_HEXES per the 25×16 placeholder boundaries in 04-PATTERNS.md (homeThird q<=7, awayThird q>=17, middleThird 8..16, homePenaltyArea q<=3 & r 4..11, awayPenaltyArea q>=21 & r 4..11, homeSixYardBox q<=1 & r 6..9, awaySixYardBox q>=23 & r 6..9, centreCircle hexesInRange({q:12,r:7},3)) and `kickOffHex: { q: 12, r: 7 }`. Export `const DIFFICULT_ANGLE_HEXES: ReadonlySet<string>` built from the 16 placeholder dot-marked coords in 04-PATTERNS.md (PITCH-03). Add a PLACEHOLDER comment stating these boundaries are approximations pending physical board measurements (Phase 6 replaces). Export predicates `isInRegion(hex, region: keyof Omit<PitchRegions, 'kickOffHex'>): boolean`, `isDifficultAngle(hex): boolean`, and `isPitchHex(hex): boolean` (the last used for Loose Ball boundary enforcement). All use hexKey + Set.has (O(1)).

    Create packages/shared/src/pitch.test.ts mirroring moveValidator.test.ts header. Assert: PITCH_HEXES length is 400 (PITCH-01); isInRegion true/false for known home/away third + penalty-area hexes (PITCH-02); kickOffHex equals {q:12,r:7}; isDifficultAngle true for one encoded hex and false for {q:12,r:7} (PITCH-03); isPitchHex true for {q:0,r:0} and false for {q:99,r:99}. Reference PITCH-01/02/03 in test names.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test src/pitch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - pitch.ts contains `export const PITCH_REGIONS` and `export const DIFFICULT_ANGLE_HEXES`
    - pitch.ts exports `isInRegion`, `isDifficultAngle`, `isPitchHex` and imports `hexesInRange`
    - pitch.ts region lookups use `Set` / `.has(` (no `Array.prototype.includes` on HexCoord)
    - pitch.test.ts asserts PITCH_HEXES length 400 (PITCH-01) and region membership (PITCH-02) and difficult-angle membership (PITCH-03)
    - `pnpm --filter @counter-attack/shared test src/pitch.test.ts` exits 0
    - `pnpm --filter @counter-attack/shared test` exits 0 (full shared suite still green)
  </acceptance_criteria>
  <done>Pitch regions and difficult-angle hexes are encoded with O(1) helpers and the unit suite is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                                                                                                                         |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none in this plan) | Wave 1 is pure shared data/types — no socket input, no untrusted boundary crossed. Threats are realised in Wave 3 (gameHandlers.ts) where these types are consumed. |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                         | Disposition | Mitigation Plan                                                                                                                                                |
| --------- | --------- | ----------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-4-03    | Tampering | `ActionEvent.MOVE` carries `pieceId` not authoritative from-coord | mitigate    | MOVE event shape uses server-derived `from` (piece.position before move), never client-supplied; enforced when gameEngine.applyMove builds the event in Wave 2 |
| T-4-SC    | Tampering | npm/pip/cargo installs                                            | accept      | No new packages introduced in Phase 4 (RESEARCH §Package Legitimacy Audit — none new); nothing to verify                                                       |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared build` exits 0
- `pnpm --filter @counter-attack/shared test` exits 0 (existing 103 tests + new teams/pitch suites all green)
- Grep confirms no remaining `readonly unknown[]` in types.ts
</verification>

<success_criteria>

- TEAM-01: both squads, 11 players, 9 attributes each — present and tested
- TEAM-02: name + role fields on every PlayerPiece — present and tested
- TEAM-03: RefereeCard type added to shared and on GameState (random assignment happens Wave 2)
- PITCH-01: PITCH_HEXES placeholder grid unchanged (400 hexes)
- PITCH-02: pitch regions encoded with O(1) Set membership and tested
- PITCH-03: DIFFICULT_ANGLE_HEXES encoded and tested
- New Socket.io events (game:end-turn, game:undo, game:error, game:start-movement) typed and exported
  </success_criteria>

<output>
Create `.planning/phases/04-game-engine-phase-fsm/04-01-SUMMARY.md` when done.
</output>
</tasks>
