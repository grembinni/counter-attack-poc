---
phase: 04-game-engine-phase-fsm
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - packages/server/src/gameHandlers.ts
  - packages/server/src/roomStore.ts
  - packages/server/src/createServer.ts
  - packages/server/src/__tests__/game.integration.test.ts
autonomous: true
requirements: [TEAM-01, PITCH-02, PITCH-03]

must_haves:
  truths:
    - 'game:move, game:end-turn, game:undo, game:start-movement handlers each acquire the per-room isProcessing mutex and release it in finally (SC-5, T-4-02)'
    - "A game action from the wrong player's socket is rejected with game:error and the state is re-broadcast (T-4-01, T-4-04, D-06)"
    - 'game:start-movement transitions the broadcast state from KICK_OFF to MOVEMENT/ATTACKER_4 and is restricted to the attacking team (FSM, T-4-05)'
    - 'joinRoom builds the real GameState via buildInitialGameState when the second player joins (D-12, D-14)'
    - 'registerGameHandlers is called for both fresh and reconnected sockets in createServer'
    - 'The FSM sequence ATTACKER_4→DEFENDER_5→ATTACKER_2→PASS is enforced end-to-end over the wire (MOVE-01, FSM)'
    - 'A duplicate action arriving while isProcessing is true is silently dropped (SC-5)'
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'registerGameHandlers(io, socket) with game:move/end-turn/undo/start-movement handlers'
      contains: 'export function registerGameHandlers'
    - path: 'packages/server/src/__tests__/game.integration.test.ts'
      provides: 'filled MOVE/FSM/undo/isProcessing scenarios driven through the game:start-movement wire path'
      contains: 'isProcessing'
  key_links:
    - from: 'packages/server/src/gameHandlers.ts'
      to: 'packages/server/src/gameEngine.js'
      via: 'handlers call applyStartMovement/applyMove/applyEndTurn/applyUndo'
      pattern: 'applyStartMovement|applyMove|applyEndTurn|applyUndo'
    - from: 'packages/server/src/roomStore.ts'
      to: 'packages/server/src/gameEngine.js'
      via: 'joinRoom calls buildInitialGameState'
      pattern: 'buildInitialGameState'
    - from: 'packages/server/src/createServer.ts'
      to: 'packages/server/src/gameHandlers.js'
      via: 'registerGameHandlers wired in connection handler'
      pattern: 'registerGameHandlers'
---

<objective>
Wire the Wave 2 game engine to Socket.io. Create `packages/server/src/gameHandlers.ts` with `game:move`, `game:end-turn`, `game:undo`, and `game:start-movement` handlers — each guarded by the per-room `isProcessing` mutex, an active-player slot check, and broadcasting full state after every change. Replace the stub LOBBY GameState in `roomStore.joinRoom()` with `buildInitialGameState()` (D-12), and register the game handlers in `createServer.ts` for both fresh and reconnected sockets. Fill the integration test scenarios that the Wave 2 skeleton enumerated — now reachable end-to-end because `game:start-movement` provides the wire path into the MOVEMENT phase.

This is Wave 3 — depends on Wave 2's engine functions. It is the final wave; after it, the full Movement Phase runs end-to-end over the wire.

Purpose: ARCH-01/ARCH-04 require the server to be the sole authority that validates actions and broadcasts full snapshots. This plan delivers the socket boundary where the security guards (T-4-01..T-4-05) and the isProcessing mutex (SC-5) are enforced, and the kick-off→movement transition that satisfies the phase goal (4-5-2 sequence over the wire).
Output: gameHandlers.ts, extended roomStore.ts + createServer.ts, and the completed game.integration.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-game-engine-phase-fsm/04-CONTEXT.md
@.planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md
@.planning/phases/04-game-engine-phase-fsm/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gameHandlers.ts with mutex + active-player guards (incl. game:start-movement)</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/roomHandlers.ts (registerXHandlers pattern, AppSocket/AppServer aliases, socket.data read, JSDoc header style — copy structure)
    - packages/server/src/roomStore.ts (getRoom, broadcastState, Room.isProcessing field)
    - packages/server/src/gameEngine.ts (applyStartMovement/applyMove/applyEndTurn/applyUndo signatures + Apply*Result unions + activeTeamForSlot semantics from Wave 2)
    - packages/shared/src/events.ts (ClientEvents.GAME_MOVE pieceId signature, GAME_END_TURN, GAME_UNDO, GAME_START_MOVEMENT; ServerEvents.GAME_ERROR)
    - .planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md ("gameHandlers.ts NEW" — full mutex/guard/broadcast structure) and RESEARCH Pitfalls 2/3/5
  </read_first>
  <behavior>
    - Each handler returns silently if roomCode is undefined, room is missing, or room.isProcessing is already true (SC-5 duplicate drop)
    - Each handler sets isProcessing true, runs in try, releases in finally (RESEARCH Pitfall 5 — never permanently locked)
    - game:start-movement when phase != 'KICK_OFF' emits GAME_ERROR 'WRONG_PHASE' and re-broadcasts; from a non-attacking socket emits GAME_ERROR 'WRONG_TEAM' and re-broadcasts (T-4-05); on success advances to MOVEMENT/ATTACKER_4 and broadcasts
    - game:move when phase != 'MOVEMENT' emits GAME_ERROR 'WRONG_PHASE' and re-broadcasts (D-06 snap-back)
    - game:move/end-turn from a socket whose playerSlot maps to the non-acting team emits GAME_ERROR 'WRONG_TEAM' and re-broadcasts (T-4-01, T-4-04)
    - On applyStartMovement/applyMove/applyEndTurn/applyUndo ok:false → emit GAME_ERROR with the reason and re-broadcast; on ok:true → assign room.gameState and broadcastState
  </behavior>
  <action>
    Create packages/server/src/gameHandlers.ts mirroring roomHandlers.ts exactly: JSDoc module header (cross-reference ARCH-01, ARCH-04, SC-5, T-4-01..T-4-05, the RESEARCH Pitfall-2 socket.data rationale), the same import block (type-import ClientToServerEvents/InterServerEvents/ServerToClientEvents/SocketData, value-import ClientEvents/ServerEvents from '@counter-attack/shared', type-import Server/Socket from 'socket.io'), and the identical `AppSocket`/`AppServer` type aliases. Import `broadcastState`, `getRoom` from './roomStore.js' and `applyStartMovement`, `applyMove`, `applyEndTurn`, `applyUndo` from './gameEngine.js'.

    Export `registerGameHandlers(io: AppServer, socket: AppSocket): void`. Add a module-private helper `actingTeam(state): 'home'|'away'` (ATTACKER_4/ATTACKER_2 → attackingTeam, DEFENDER_5 → other) and `slotToTeam` mapping such that a handler can compare `socket.data.playerSlot`'s team to the acting team. Map playerSlot→team via state: the team a slot owns is determined by which team that slot's player controls; derive the socket's team by checking room.players slot ownership against state.attackingTeam (the home/away assignment is on state — a socket controls 'home' or 'away' based on the coin flip recorded at build time). Add a private `isActivePlayer(socket, room): boolean` that returns true only when the socket's controlled team equals actingTeam(room.gameState) (RESEARCH Pitfall 2/3, T-4-01). Add a private `controlsAttackingTeam(socket, room): boolean` that returns true when the socket's controlled team equals room.gameState.attackingTeam (used by the start-movement guard, T-4-05).

    Implement four `socket.on` handlers, each following the exact mutex structure from 04-PATTERNS.md:
    - GAME_START_MOVEMENT `()`: drop if no room or isProcessing; set isProcessing true; in try — if gameState null or phase !== 'KICK_OFF' emit GAME_ERROR 'WRONG_PHASE' + broadcastState + return; if !controlsAttackingTeam emit GAME_ERROR 'WRONG_TEAM' + broadcastState + return (T-4-05 — only the attacking team may start the Movement Phase); result = applyStartMovement(gameState); if !result.ok emit GAME_ERROR result.reason + broadcastState + return; assign room.gameState = result.state; broadcastState; finally release isProcessing. This handler is the wire edge that moves the FSM from KICK_OFF into MOVEMENT/ATTACKER_4.
    - GAME_MOVE `(pieceId: string, to: HexCoord)`: drop if no room or isProcessing; set isProcessing true; in try — if gameState null or phase !== 'MOVEMENT' emit GAME_ERROR 'WRONG_PHASE' + broadcastState + return; if !isActivePlayer emit GAME_ERROR 'WRONG_TEAM' + broadcastState + return (T-4-01); result = applyMove(gameState, pieceId, to); if !result.ok emit GAME_ERROR result.reason + broadcastState (D-06 snap-back) + return; assign room.gameState = result.state; broadcastState; finally release isProcessing.
    - GAME_END_TURN `()`: same mutex/guard skeleton; if !isActivePlayer emit GAME_ERROR 'WRONG_TEAM' + broadcastState + return (T-4-04); result = applyEndTurn(gameState); on ok assign + broadcast.
    - GAME_UNDO `()`: same mutex skeleton; active-player guard; result = applyUndo(gameState); on ok:false (UNDO_LOCKED/NOTHING_TO_UNDO) emit GAME_ERROR reason + broadcast; on ok assign + broadcast.
    Always call broadcastState (ARCH-04 single entry point) — never `io.to().emit` directly. Release isProcessing in `finally` on every handler.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server build</automated>
  </verify>
  <acceptance_criteria>
    - gameHandlers.ts contains `export function registerGameHandlers(io: AppServer, socket: AppSocket): void`
    - gameHandlers.ts registers a `GAME_START_MOVEMENT` handler that calls `applyStartMovement` and guards on KICK_OFF phase + attacking-team ownership (T-4-05)
    - each of the four handlers sets `isProcessing = true` and releases it in a `finally` block
    - each handler returns early on `room.isProcessing` (SC-5 duplicate drop)
    - GAME_MOVE handler emits `GAME_ERROR` 'WRONG_TEAM' for a non-acting socket before calling applyMove (T-4-01)
    - handlers call `broadcastState` and never call `io.to(...).emit` directly
    - `pnpm --filter @counter-attack/server build` exits 0
  </acceptance_criteria>
  <done>All four game handlers exist with mutex, active-player/attacking-team guards, and ARCH-04 broadcast; the KICK_OFF→MOVEMENT wire edge is in place; server compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire buildInitialGameState into joinRoom + registerGameHandlers into createServer</name>
  <files>packages/server/src/roomStore.ts, packages/server/src/createServer.ts</files>
  <read_first>
    - packages/server/src/roomStore.ts (joinRoom success path lines ~136-158 — the stub LOBBY GameState literal to replace; import block)
    - packages/server/src/createServer.ts (connection handler: reconnect path ~line 120 registerRoomHandlers(io,socket,true); fresh path ~line 126 registerRoomHandlers(io,socket,false); import block ~line 25)
    - packages/server/src/gameEngine.ts (buildInitialGameState signature)
    - packages/server/src/gameHandlers.ts (registerGameHandlers signature — Task 1)
    - .planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md ("roomStore.ts EXTEND" and "createServer.ts EXTEND")
  </read_first>
  <behavior>
    - joinRoom success path sets room.gameState = buildInitialGameState(roomCode) instead of the LOBBY literal (D-12, D-14)
    - createServer registers game handlers for fresh connections (after registerRoomHandlers(io,socket,false)) and reconnected sockets (after registerRoomHandlers(io,socket,true))
    - existing room lifecycle and reconnect behaviour is unchanged (Phase 3 tests stay green)
  </behavior>
  <action>
    In packages/server/src/roomStore.ts, add `import { buildInitialGameState } from './gameEngine.js'` to the import block. In the joinRoom success path (step 4), replace the entire stub `room.gameState = { roomCode, phase:'LOBBY', ... }` object literal (lines ~143-156) with `room.gameState = buildInitialGameState(roomCode)` (D-12 — real state built immediately on second-player join; D-14 — buildInitialGameState already sets phase 'KICK_OFF'). Update the step-4 JSDoc comment to say it builds the real KICK_OFF state. Leave broadcastState, the Room type, and every other export unchanged.

    In packages/server/src/createServer.ts, add `import { registerGameHandlers } from './gameHandlers.js'` alongside the existing roomHandlers import (~line 25). In the reconnect block, immediately after `registerRoomHandlers(io, socket, true)` (~line 120), add `registerGameHandlers(io, socket)` so reconnected sockets can continue mid-game. In the fresh-connection path, immediately after `registerRoomHandlers(io, socket, false)` (~line 126), add `registerGameHandlers(io, socket)`. Do not change any other connection-handler logic.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test src/__tests__/room.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - roomStore.ts contains `room.gameState = buildInitialGameState(roomCode)` and no remaining `phase: 'LOBBY'` literal in joinRoom
    - roomStore.ts imports `buildInitialGameState` from './gameEngine.js'
    - createServer.ts calls `registerGameHandlers(io, socket)` in both the reconnect and fresh-connection paths
    - `pnpm --filter @counter-attack/server test src/__tests__/room.integration.test.ts` exits 0 (Phase 3 lifecycle unaffected; broadcast now carries KICK_OFF state)
  </acceptance_criteria>
  <done>joinRoom builds real state and game handlers are registered on every socket; Phase 3 integration suite stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Fill game.integration.test.ts scenarios (full 4-5-2 sequence over the wire)</name>
  <files>packages/server/src/__tests__/game.integration.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/game.integration.test.ts (Wave 2 skeleton — setupRoom helper + it.todo placeholders)
    - packages/server/src/__tests__/room.integration.test.ts (oncePromise/createClient usage patterns for emitting events and awaiting responses)
    - packages/server/src/gameEngine.ts (applyStartMovement + slot transitions + applyMove/undo semantics to predict expected wire outcomes)
    - packages/shared/src/teams.ts (piece ids 'home-0'..'home-10' / 'away-0'..'away-10' and starting positions for move targets)
    - .planning/phases/04-game-engine-phase-fsm/04-PATTERNS.md ("game.integration.test.ts NEW" — scenario shape) and 04-VALIDATION.md (Per-Task Verification Map rows 4-03-01..03)
  </read_first>
  <behavior>
    - game:move before MOVEMENT (KICK_OFF) returns game:error 'WRONG_PHASE' (MOVE-01 at wire)
    - game:start-movement from the attacking client advances the broadcast state to phase 'MOVEMENT', movementSlot 'ATTACKER_4'; from the non-attacking client returns game:error 'WRONG_TEAM' (T-4-05)
    - game:move/end-turn from the non-acting client returns game:error 'WRONG_TEAM' (T-4-01, T-4-04)
    - FSM: after start-movement, emitting game:end-turn three times (ATTACKER_4→DEFENDER_5→ATTACKER_2→PASS) advances the broadcast state's movementSlot/phase correctly (FSM sequencing)
    - game:undo after a SLOT_ADVANCE returns game:error 'UNDO_LOCKED' (D-09); undo within a slot reverses the last move (D-10)
    - two rapid identical game actions: the second is silently dropped while the first is processing (SC-5)
  </behavior>
  <action>
    In packages/server/src/__tests__/game.integration.test.ts, replace the it.todo placeholders with real `it` scenarios using setupRoom() and the oncePromise/createClient helpers. The MOVEMENT phase is now reachable over the wire via `game:start-movement` (04-03/T1), so drive scenarios end-to-end rather than deferring to unit coverage. Determine which client controls the attacking team by reading `state.attackingTeam` from the setupRoom result and the room's slot ownership (the attacking client is the one whose controlled team equals attackingTeam). Implement these active tests:
    - MOVE-01 WRONG_PHASE: emit `game:move` at KICK_OFF (before start-movement); await GAME_ERROR 'WRONG_PHASE'.
    - T-4-05 start-movement guard: emit `game:start-movement` from the NON-attacking client; await GAME_ERROR 'WRONG_TEAM'. Then emit from the attacking client; await the GAME_STATE broadcast and assert `phase === 'MOVEMENT'` and `movementSlot === 'ATTACKER_4'`.
    - FSM sequencing: from a MOVEMENT/ATTACKER_4 state (reached via start-movement), emit `game:end-turn` from the acting client and assert the broadcast advances movementSlot ATTACKER_4→DEFENDER_5; emit again (from the now-acting client) and assert DEFENDER_5→ATTACKER_2; emit a third time and assert the broadcast reaches phase 'PASS' with movementSlot null (D-03/D-04 over the wire).
    - T-4-01 WRONG_TEAM move: after start-movement (slot ATTACKER_4, attacker acting), emit `game:move` from the NON-acting client; await GAME_ERROR 'WRONG_TEAM'.
    - D-10 undo + D-09 UNDO_LOCKED: after start-movement, emit a valid `game:move` from the acting client (pick a piece id + legal destination from teams.ts starting positions), assert the broadcast reflects the move; emit `game:undo` and assert the broadcast reverses it (D-10). Then emit a valid move, emit `game:end-turn` (producing a SLOT_ADVANCE), then emit `game:undo` and await GAME_ERROR 'UNDO_LOCKED' (D-09).
    - SC-5 duplicate drop: emit two `game:end-turn` actions back-to-back synchronously from the acting client; assert only one state transition results for the pair within the timeout (the second is dropped while isProcessing is true).
    Use the standard oncePromise timeout on every awaited response and ensure connectedClients teardown covers all created clients. If a specific move target in teams.ts placeholder positions is not a legal validateMove destination, choose the nearest legal adjacent hex (read validateMove + teams.ts positions to pick a guaranteed-valid move). No remaining it.todo for FSM sequencing, undo, or MOVE-01 — these are now wire-reachable. MOVE-06 free-move over the wire may remain a single documented `it.todo('MOVE-06 free-move — engine-covered in gameEngine.test.ts; full wire exercise lands with the free-move handler in Phase 5')` if constructing a ball-carrier final-third crossing over the wire is impractical with placeholder positions; the engine path is unit-tested in 04-02.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test src/__tests__/game.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - game.integration.test.ts has active `it` tests (not todo) for MOVE-01 WRONG_PHASE, start-movement guard + transition (T-4-05), FSM sequencing ATTACKER_4→DEFENDER_5→ATTACKER_2→PASS, WRONG_TEAM move rejection (T-4-01), D-10 undo + D-09 UNDO_LOCKED, and isProcessing duplicate drop (SC-5)
    - a test emits `game:start-movement` from the attacking client and asserts the broadcast reaches phase 'MOVEMENT' / movementSlot 'ATTACKER_4'
    - the FSM-sequencing test asserts the broadcast advances through DEFENDER_5, ATTACKER_2, then phase 'PASS'
    - the duplicate-drop test asserts the second of two back-to-back actions yields no second state transition
    - any remaining it.todo is limited to MOVE-06 and references the gameEngine.test.ts unit coverage + Phase 5 wire path
    - `pnpm --filter @counter-attack/server test src/__tests__/game.integration.test.ts` exits 0
  </acceptance_criteria>
  <done>The full 4-5-2 Movement Phase sequence — start-movement, slot advances, move, undo, mutex — is asserted end-to-end over the wire; suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                                                                                                               |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client socket → server handlers | Untrusted game action intents (game:move, game:end-turn, game:undo, game:start-movement) cross here. Every payload is re-validated by the engine; every action is guarded by mutex + active-player check. |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                                                             |
| --------- | ---------------------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-4-01    | Spoofing               | GAME_MOVE / GAME_END_TURN handlers | mitigate    | isActivePlayer(socket,room) compares the socket's coin-flip team to actingTeam(state); non-acting socket gets GAME_ERROR 'WRONG_TEAM' and state re-broadcast — verified in integration test |
| T-4-02    | Tampering              | per-handler isProcessing mutex     | mitigate    | room.isProcessing checked at entry (drop if true) and released in finally; duplicate-action drop verified in integration test (SC-5)                                                        |
| T-4-03    | Tampering              | GAME_MOVE pieceId lookup           | mitigate    | handler passes pieceId to applyMove which looks the piece up by id from state.pieces; client from-coord is never trusted (engine records server-derived from)                               |
| T-4-04    | Elevation of Privilege | GAME_END_TURN handler              | mitigate    | active-player guard runs before applyEndTurn; wrong-slot socket cannot advance the FSM                                                                                                      |
| T-4-05    | Elevation of Privilege | GAME_START_MOVEMENT handler        | mitigate    | controlsAttackingTeam guard + applyStartMovement KICK_OFF guard run before transition; only the attacking team's socket can open the Movement Phase — verified in integration test          |
| T-4-SC    | Tampering              | npm installs                       | accept      | No new packages in Phase 4 (RESEARCH §Package Legitimacy Audit)                                                                                                                             |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` exits 0 (all unit + both integration suites green)
- `pnpm -r test` exits 0 (full repo: shared + server)
- `pnpm -r build` exits 0
- grep confirms gameHandlers.ts releases isProcessing in `finally` in every handler and never calls `io.to().emit` directly
- grep confirms gameHandlers.ts registers a `GAME_START_MOVEMENT` handler calling `applyStartMovement`
</verification>

<success_criteria>

- SC-5: isProcessing mutex drops duplicate actions (verified at wire)
- MOVE-01: out-of-phase / out-of-sequence move rejected with game:error
- T-4-01/T-4-04: wrong-player actions rejected with WRONG_TEAM
- T-4-05: only the attacking team can start the Movement Phase
- FSM: ATTACKER_4→DEFENDER_5→ATTACKER_2→PASS enforced end-to-end over the wire (phase goal met)
- D-09/D-10: undo boundary + reversal verified over the wire
- D-12/D-14: real KICK_OFF GameState built on second-player join
- registerGameHandlers wired on fresh + reconnect paths
- full repo test suite and build green
  </success_criteria>

<output>
Create `.planning/phases/04-game-engine-phase-fsm/04-03-SUMMARY.md` when done.
</output>
</tasks>
