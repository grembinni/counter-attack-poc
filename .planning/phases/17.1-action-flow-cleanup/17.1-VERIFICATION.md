---
phase: 17.1-action-flow-cleanup
verified: 2026-06-19T20:00:00Z
status: gaps_found
score: 4/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 3/5
  gaps_closed:
    - 'CR-01-new (client cannot select/move a piece during FIRST_TIME_PASS_MOVE): fixed by plan 17.1-12 — independently re-confirmed by direct code read of useGameStore.ts selectPiece (FIRST_TIME_PASS_MOVE branch, lines 375-406, returns early without falling through to validateMove WRONG_SLOT) and HexGrid.tsx (canSelectFirstTimePassMove wired into isClickable line 640 and handleClick line 673-674), plus a targeted re-run of HexGrid.test.tsx -t "CR-01-new" (4/4 passing).'
    - 'CR-02-new (interception loop shadowing the FTP transition for a defender-nearby pass): fixed by plan 17.1-13 — independently re-confirmed by direct code read of gameEngine.ts applyRoll (isFirstTimePass bypass guard at lines 1153-1159, FIRST_TIME_PASS early-return at line 1243 unconditionally reached once the loop is bypassed), plus a targeted re-run of gameEngine.phase17.test.ts -t "CR-02-new" (2/2 passing).'
  gaps_remaining:
    - 'Review-CR-01 (new, server, applyUndo): undoing an FTP_MOVE or HP_MOVE reverts piece position and the event log but never resets firstTimePassMovedPieceId/firstTimePassPaceUsed (or highPassMovedPieceId/highPassPaceUsed), permanently locking the repositioning slot after a single Undo. Independently confirmed real by direct code read — not previously caught because plan 17.1-11 was scoped only to the move-type lookup, and both of its new tests assert only piece?.position, never the lock-field reset.'
    - 'Review-CR-02 (new, server, GAME_END_TURN FTP delivery): the DEFENDER-slot-complete ball delivery in gameHandlers.ts looks up the receiver with a team-restricted filter (p.teamId === ftpEndState.attackingTeam) instead of the team-agnostic occupant-pickup pattern used everywhere else (gameEngine.ts BUG-04, lines 1272-1297). A defender can legally reposition exactly onto passTargetHex during the DEFENDER slot (no exclusion in gameHandlers.ts:524-531), producing an inconsistent board state: phase becomes PASS (not LOOSE_BALL) with a null-carrier ball sitting on a hex a defender physically occupies, and possession is not transferred. Independently confirmed real and directly reachable using the exact fixture introduced by plan 17.1-13 itself (firstTimePassNearDefenderState places a defender 1 hex from the target — i.e., exactly adjacent and able to step onto it during the DEFENDER slot).'
  regressions: []
gaps:
  - truth: 'First-time pass flight lets both teams reposition one player ≤1 hex, then delivers the ball without interception; undo is available at the FTP_REPOSITION boundary'
    status: failed
    reason: "Repositioning (client selection) and no-interception-on-delivery (server bypass) are now both genuinely fixed and independently re-confirmed against the live codebase (CR-01-new, CR-02-new). However, a fresh code review surfaced — and this verification independently confirmed by direct code reading, not by trusting review prose — two further defects in the surrounding code that make the same success criterion still false in realistic play: (1) Undo, which the criterion explicitly names ('undo is available at the FTP_REPOSITION boundary'), permanently locks the repositioning slot after a single use because applyUndo never resets the phase-specific firstTimePassMovedPieceId/firstTimePassPaceUsed lock fields — confirmed by reading every site that sets (gameHandlers.ts:544-545) or clears (gameEngine.ts:1257; gameHandlers.ts:626-627,649-650,693-694,721-722) these fields: applyUndo (gameEngine.ts:906-917) is not among the clearing sites. After one Undo, the client's own selectPiece gate (useGameStore.ts:383-392) and the server's GAME_MOVE gate (gameHandlers.ts:502-512) both still treat the slot as filled/pace-exhausted, so no further selection or move is possible; the only way out is End Turn, which then logs an FTP_REPOSITION event citing the undone piece's ID even though that piece never completed a move in the slot. (2) The FTP ball-delivery receiver lookup in gameHandlers.ts (DEFENDER-slot-complete branch) only matches attacking-team occupants, unlike the team-agnostic BUG-04 pattern used for STANDARD_PASS/LONG_BALL elsewhere in gameEngine.ts; a defender who repositions onto passTargetHex during the DEFENDER slot (legal — no exclusion check exists) causes the ball to be delivered with carrierId: null on a hex a defender is standing on, with phase set to PASS (not LOOSE_BALL) and possession left unchanged — an invalid/inconsistent board state with no analog elsewhere in the codebase. This is directly reachable using the very fixture plan 17.1-13 introduced (defender 1 hex from target)."
    artifacts:
      - path: 'packages/server/src/gameEngine.ts'
        issue: 'applyUndo (lines 818-918) reverses piece position, paceUsedByPieceId, movedPieceIds, eventLog, pendingFreeMove, and ball position (lines 906-917), but never resets firstTimePassMovedPieceId/firstTimePassPaceUsed/highPassMovedPieceId/highPassPaceUsed. These fields are otherwise only ever cleared at slot-boundary/end-turn time (gameEngine.ts:1257 for the initial FTP transition; gameHandlers.ts:626-627, 649-650, 693-694, 721-722 for end-turn slot advances) — applyUndo is conspicuously absent from that list.'
      - path: 'packages/server/src/gameHandlers.ts'
        issue: "GAME_END_TURN's FIRST_TIME_PASS_MOVE DEFENDER-slot-complete branch (lines 699-727) looks up the delivery receiver via `p.teamId === ftpEndState.attackingTeam` (line 710) instead of a team-agnostic occupant search; also, the GAME_MOVE FIRST_TIME_PASS_MOVE branch (lines 488-549) has no check excluding passTargetHex from valid defender repositioning destinations during the DEFENDER slot (lines 514-531), making the defender-on-target scenario legally reachable."
    missing:
      - "Fix applyUndo to reset firstTimePassMovedPieceId/firstTimePassPaceUsed (when state.phase === 'FIRST_TIME_PASS_MOVE') and highPassMovedPieceId/highPassPaceUsed (when state.phase === 'HIGH_PASS_MOVE') as part of the returned state, mirroring how the generic paceUsedByPieceId/movedPieceIds fields are already reset."
      - 'Add a regression test asserting that after applyUndo on an FTP_MOVE/HP_MOVE, the phase-specific lock/pace fields are null/0 and a piece can be re-selected and re-moved within the same slot (the existing tests at gameEngine.phase17.test.ts:668-702 and game.integration.test.ts:378-413 only assert piece?.position and do not catch this).'
      - 'Fix the GAME_END_TURN FTP delivery receiver lookup in gameHandlers.ts to use a team-agnostic occupant search (mirroring gameEngine.ts BUG-04 pattern at lines 1272-1297), transferring possession to the occupant when it is on the opposing (defending) team.'
      - 'Add an integration test seeding a defender exactly on passTargetHex after the DEFENDER repositioning slot completes, asserting the ball/possession transfers correctly to the defender rather than leaving a null-carrier ball under an occupying piece.'
    severity_note: 'Two distinct defects of different severity: the Undo lock (item 1) is a near-guaranteed dead-end for any player who uses the criterion-named Undo feature during FTP/HP repositioning — judged severe enough on its own to keep this criterion FAILED. The delivery-receiver mismatch (item 2) is a real, independently-confirmed, directly-reachable correctness bug, but it is conditional on a defender choosing to land exactly on the 1-hex target — narrower in practical reach than item 1, though not dismissed as cosmetic since it produces an invalid board state with no possession-transfer recovery path.'
human_verification:
  - test: 'In a live two-browser session, during FIRST_TIME_PASS_MOVE, move a piece in the ATTACKER (or DEFENDER) slot, then click Undo, then attempt to select and move any piece again in the same slot.'
    expected: 'After Undo, the player should be able to select a piece (the same one or a different own-team piece) and complete a valid ≤1-hex move within the same repositioning slot, exactly as if no move had happened yet.'
    why_human: 'Confirms the player-facing dead-end (no selectable pieces, Undo button greyed out with no further recourse except End Turn) matches the code-level analysis of applyUndo never resetting firstTimePassMovedPieceId/firstTimePassPaceUsed.'
  - test: 'In a live two-browser session, play a first-time pass aimed at a hex within 1 hex of an opposing defender, then during the DEFENDER repositioning slot, move that defender exactly onto the pass target hex, then End Turn.'
    expected: 'Per success criterion 3, the ball should be delivered to the target hex with correct possession — if the defender occupies that hex, the defender should receive the ball (possession transfers to the defending team), not a null-carrier "loose" ball sitting under the defender with possession unchanged.'
    why_human: 'Confirms the board-state inconsistency (PASS phase with carrierId: null on an occupied hex, attackingTeam unchanged) is visible/reachable in live play and surfaces whatever UI the player sees in this broken state (e.g., can either team act on the "loose" ball, does the client render two pieces on one hex correctly, etc.).'
---

# Phase 17.1: Action Flow Cleanup Verification Report

**Phase Goal:** The codebase is aligned with the corrected Counter Attack v1.4.1 phase/action model — GamePhase enum renamed, aerial stat consolidated, ZoI exclusion fixed, first-time-pass repositioning phase added, and GK restart / loose-ball / shot-range mechanics corrected.
**Verified:** 2026-06-19T20:00:00Z
**Status:** gaps_found
**Re-verification:** Yes — third cycle. Cycle 1 found the undo clause broken (old CR-01), closed by plan 17.1-11. Cycle 2 found two new defects (CR-01-new: client could not select/move pieces during FIRST_TIME_PASS_MOVE; CR-02-new: server interception loop could shadow the FTP transition for a defender-nearby pass), closed by plans 17.1-12/17.1-13. This cycle (3) independently re-confirms CR-01-new/CR-02-new are genuinely fixed, and independently investigates two further defects (Review-CR-01, Review-CR-02) surfaced by a fresh code review run after plans 12-13 landed.

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                         | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | All 8 GamePhase literals are renamed to the v1.4.1 names and a new FIRST_TIME_PASS_MOVE phase exists; typecheck and full test suite pass                                                      | ✓ VERIFIED | Unchanged since prior cycle. `pnpm -r typecheck` clean across all 3 packages (re-run, confirmed this cycle). `pnpm --filter shared test`: 317/317. `pnpm --filter client test`: 106/106. `pnpm --filter server test`: 301/307 passed, 4 failed (2 MOVE-06 FREE_MOVE pre-existing, 2 abandoned `firstTimePassStep` stale tests), 1 skipped, 1 todo — identical to the documented baseline across all three prior cycles, re-confirmed by direct re-run this cycle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2   | PlayerPiece has a single aerial stat (aerialAbility); a defender who has attempted a tackle/steal no longer re-triggers that same challenge but still projects the other ZoI                  | ✓ VERIFIED | Unchanged since prior cycle — `moveValidator.ts`/`passValidator.ts` untouched by plans 11/12/13 (confirmed via `git log` on these files). `passValidator.test.ts` 25/25 pass on re-run. No regression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 3   | First-time pass flight lets both teams reposition one player ≤1 hex, then delivers the ball without interception; undo is available at the FTP_REPOSITION boundary                            | ✗ FAILED   | **Repositioning (client selection) and no-interception clauses: now genuinely fixed.** CR-01-new (client cannot select pieces) and CR-02-new (interception loop shadows the transition) independently re-confirmed fixed by direct code read (see Key Link table) and targeted test re-runs (4/4 and 2/2 passing respectively). **Undo clause and delivery-correctness: FALSE.** Two further defects, independently confirmed against the live codebase (not merely cited from the fresh code review): Undo during FTP_MOVE/HP_MOVE permanently locks the repositioning slot (Review-CR-01 — `applyUndo` never resets `firstTimePassMovedPieceId`/`firstTimePassPaceUsed`/`highPass*` equivalents), and FTP ball delivery can produce an invalid board state when a defender legally repositions onto the target hex (Review-CR-02 — team-restricted receiver lookup instead of the team-agnostic BUG-04 pattern used elsewhere). |
| 4   | GK carrying in its own penalty area at end of MOVE → GK_RESTART; GK save spill → GK_RESTART; loose-ball scatter clamps to the pitch; regular shot beyond 11 hexes is rejected                 | ✓ VERIFIED | Unchanged since prior cycle — D-06/D-07/D-08/D-09 logic untouched by plans 11/12/13 (confirmed via `git log` on `gameEngine.ts` showing only the CR-01/CR-02-new-related commits since plan 10). No regression possible; re-confirmed present by direct read at the same line ranges as the prior cycle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 5   | A STANDARD pass to a defender-occupied hex is allowed and auto-intercepted (case 1); intermediate on-path blocking still returns PATH_BLOCKED (case 2); ZoI defenders roll-intercept (case 3) | ✓ VERIFIED | Unchanged since prior cycle — `passValidator.ts` logic for STANDARD pass cases 1-3 untouched by plans 11/12/13. `passValidator.test.ts` 25/25 pass on re-run. The new `isFirstTimePass` bypass added by plan 17.1-13 (gameEngine.ts:1153-1159) is additive and gated on `FIRST_TIME_PASS` only — confirmed by a passing no-regression test (`gameEngine.phase17.test.ts` "regression: STANDARD_PASS near the same defender still auto-intercepts") proving STANDARD_PASS still intercepts identically.                                                                                                                                                                                                                                                                                                                                                                                                                            |

**Score:** 4/5 truths fully verified; truth 3 still fails — its repositioning/no-interception clauses are now fixed, but its undo clause and the delivery-correctness implied by "delivers the ball" are newly found to be false for two further, independently-confirmed reasons.

### Required Artifacts

| Artifact                                                            | Expected                                                                          | Status     | Details                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/server/src/gameEngine.ts` (`applyUndo`)                   | Phase-aware move-type lookup AND phase-specific lock-field reset                  | ⚠️ PARTIAL | Move-type lookup fix (CR-01-old) confirmed present and correct. Lock-field reset (Review-CR-01) confirmed ABSENT — `firstTimePassMovedPieceId`/`firstTimePassPaceUsed`/`highPassMovedPieceId`/`highPassPaceUsed` are never touched in the function's return value (lines 906-917). |
| `packages/server/src/gameEngine.ts` (`applyRoll` PASS case)         | FIRST_TIME_PASS_MOVE transition reachable, not shadowed by interception loop      | ✓ VERIFIED | `isFirstTimePass` bypass confirmed present and correct at lines 1153-1159; transition at line 1243 unconditionally reached for an accurate FTP. CR-02-new closed.                                                                                                                  |
| `packages/client/src/components/HexGrid.tsx`                        | Piece-selection/click support for FIRST_TIME_PASS_MOVE                            | ✓ VERIFIED | `canSelectFirstTimePassMove` confirmed wired into `isClickable` (line 640) and `handleClick` (line 673-674), mirroring `canSelectHighPassMove`. CR-01-new closed.                                                                                                                  |
| `packages/client/src/store/useGameStore.ts`                         | `selectPiece`/`setGameState` branches for FIRST_TIME_PASS_MOVE                    | ✓ VERIFIED | `selectPiece` (lines 375-406) has a dedicated FIRST_TIME_PASS_MOVE branch that returns early, never reaching the generic MOVE branch's `validateMove` WRONG_SLOT guard. `setGameState` sticky-selection (lines 543-559) extended to cover this phase. CR-01-new closed.            |
| `packages/server/src/gameHandlers.ts` (GAME_END_TURN FTP delivery)  | Team-agnostic occupant-aware ball delivery, mirroring BUG-04                      | ✗ STUB     | `receiver` lookup at line 708-713 filters on `p.teamId === ftpEndState.attackingTeam` only — confirmed by direct read and comparison against the team-agnostic pattern at `gameEngine.ts:1272-1297`. Review-CR-02.                                                                 |
| `packages/server/src/gameHandlers.ts` (GAME_MOVE FTP DEFENDER slot) | Exclusion or special handling of `passTargetHex` as a defender destination        | ✗ MISSING  | Occupancy check at lines 524-531 only checks other pieces' current positions; no exclusion of `passTargetHex` itself, confirming a defender can legally land exactly on the delivery hex. Review-CR-02.                                                                            |
| `packages/server/src/__tests__/gameEngine.phase17.test.ts`          | Regression coverage for FTP-near-defender bypass (CR-02-new)                      | ✓ VERIFIED | `Phase 17.1-13 CR-02-new` describe block (2 tests) confirmed present and passing.                                                                                                                                                                                                  |
| `packages/client/src/components/HexGrid.test.tsx`                   | Coverage of piece selection during FIRST_TIME_PASS_MOVE (CR-01-new)               | ✓ VERIFIED | `HexGrid — CR-01-new: FIRST_TIME_PASS_MOVE piece selection` describe block (4 tests) confirmed present and passing.                                                                                                                                                                |
| (no test file)                                                      | Regression coverage for applyUndo resetting FTP/HP lock fields                    | ✗ MISSING  | Confirmed absent — `gameEngine.phase17.test.ts:668-702` and `game.integration.test.ts:378-413` (the only two undo-during-FTP/HP tests in the codebase) assert only `piece?.position`, never the lock/pace fields. Review-CR-01.                                                    |
| (no test file)                                                      | Integration coverage for defender landing on `passTargetHex` during DEFENDER slot | ✗ MISSING  | Confirmed absent — no test in `gameHandlers.phase17.test.ts` or elsewhere exercises the GAME_END_TURN FTP delivery path with a defender occupying the target hex. Review-CR-02.                                                                                                    |

### Key Link Verification

| From                                                       | To                                                       | Via                                                                                         | Status      | Details                                                                                                                                                                                                                                                                                |
| ---------------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `useGameStore.ts` selectPiece                              | `FIRST_TIME_PASS_MOVE` valid-move computation            | Dedicated phase branch (mirroring HIGH_PASS_MOVE)                                           | ✓ WIRED     | Confirmed at `useGameStore.ts:375-406`; returns early, never falls through to generic MOVE branch.                                                                                                                                                                                     |
| `HexGrid.tsx` isClickable                                  | `FIRST_TIME_PASS_MOVE` piece selectability               | `canSelectFirstTimePassMove` boolean                                                        | ✓ WIRED     | Confirmed at `HexGrid.tsx:598-603, 640, 673-674`.                                                                                                                                                                                                                                      |
| `gameEngine.ts` applyRoll PASS case                        | `FIRST_TIME_PASS_MOVE` transition (line 1243)            | `isFirstTimePass` bypass of the interception loop                                           | ✓ WIRED     | Confirmed at `gameEngine.ts:1153-1159`; transition unconditionally reached for an accurate FTP regardless of nearby defenders.                                                                                                                                                         |
| `gameEngine.ts` applyUndo                                  | `firstTimePassMovedPieceId`/`highPassMovedPieceId` reset | Expected: phase-specific reset alongside the generic field resets                           | ✗ NOT_WIRED | No such reset exists in `applyUndo`'s return (lines 906-917). The only sites that ever null these fields are the initial FTP transition (`gameEngine.ts:1257`) and end-turn slot advances (`gameHandlers.ts:626-627,649-650,693-694,721-722`) — `applyUndo` is absent from both lists. |
| `gameHandlers.ts` GAME_END_TURN FTP DEFENDER-slot-complete | Ball delivery to `passTargetHex`                         | Expected: team-agnostic occupant search (mirroring `gameEngine.ts` BUG-04, lines 1272-1297) | ✗ NOT_WIRED | `receiver` lookup at `gameHandlers.ts:708-713` filters on `attackingTeam` only; a defender occupying the target hex evaluates to `receiver === undefined`, producing `carrierId: null` on an occupied hex with phase `PASS` (not `LOOSE_BALL`) and no possession transfer.             |

### Behavioral Spot-Checks

| Behavior                                                                                   | Command                                                                          | Result                                                                                             | Status                                                       |
| ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| Typecheck clean across all packages                                                        | `pnpm -r typecheck`                                                              | 0 errors, all 3 packages                                                                           | ✓ PASS                                                       |
| Shared test suite green                                                                    | `pnpm --filter @counter-attack/shared test`                                      | 317/317 passed                                                                                     | ✓ PASS                                                       |
| Client test suite green                                                                    | `pnpm --filter @counter-attack/client test`                                      | 106/106 passed                                                                                     | ✓ PASS                                                       |
| Server test suite — 4 documented pre-existing failures only, no new regressions            | `pnpm --filter @counter-attack/server test`                                      | 301 passed, 4 failed (2 MOVE-06 FREE_MOVE, 2 abandoned `firstTimePassStep`), 1 skipped, 1 todo     | ✓ PASS (matches documented baseline exactly across 3 cycles) |
| CR-01-new HexGrid piece-selection tests pass                                               | `pnpm --filter @counter-attack/client test -- HexGrid -t "CR-01-new"`            | 4/4 passed                                                                                         | ✓ PASS                                                       |
| CR-02-new interception-bypass tests pass                                                   | `pnpm --filter @counter-attack/server test -- gameEngine.phase17 -t "CR-02-new"` | 2/2 passed                                                                                         | ✓ PASS                                                       |
| `applyUndo` resets `firstTimePassMovedPieceId`/`firstTimePassPaceUsed` after FTP_MOVE undo | Direct read of `gameEngine.ts:906-917`                                           | No such reset present in the returned state object                                                 | ✗ FAIL (confirms Review-CR-01)                               |
| FTP delivery receiver lookup is team-agnostic, mirroring BUG-04                            | Direct read of `gameHandlers.ts:708-713` vs. `gameEngine.ts:1272-1297`           | `gameHandlers.ts` filters on `attackingTeam` only; `gameEngine.ts` BUG-04 pattern is team-agnostic | ✗ FAIL (confirms Review-CR-02)                               |

### Requirements Coverage

Phase 17.1 declares no formal requirement IDs (`requirements: none`/`[]` in ROADMAP.md and all 13 PLAN.md frontmatter blocks — "ad hoc cleanup phase; all decisions captured as D-01–D-11 in 17.1-CONTEXT.md"). Cross-referenced `.planning/REQUIREMENTS.md`: zero direct references to "Phase 17.1" — confirming no orphaned requirements were expected for this phase, consistent with all prior verification cycles.

Carried forward from cycle 2: `REQUIREMENTS.md` line 39 marks **PASS-02** `[x]` Complete under "Phase 17" ("each team may move one player up to 1 hex"). With CR-01-new/CR-02-new now closed, the click-to-move repositioning capability described by PASS-02 is exercisable in the browser for the common case. However, the newly-confirmed Review-CR-01 (undo lock) means PASS-02's repositioning guarantee is not durable across an Undo, and Review-CR-02 means the post-repositioning delivery is not always correct. PASS-02's `[x]` mark remains optimistic relative to current behavior; flagged for awareness, REQUIREMENTS.md itself is not modified by this report.

### Anti-Patterns Found

| File                                                         | Line    | Pattern                                                                                                                                                                                                                    | Severity   | Impact                                                                                                                                                                                                                                          |
| ------------------------------------------------------------ | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/server/src/gameEngine.ts`                          | 906-917 | `applyUndo`'s returned state resets generic `paceUsedByPieceId`/`movedPieceIds` but omits the phase-specific `firstTimePassMovedPieceId`/`firstTimePassPaceUsed`/`highPassMovedPieceId`/`highPassPaceUsed` fields entirely | 🛑 Blocker | Using Undo during FIRST_TIME_PASS_MOVE or HIGH_PASS_MOVE permanently locks the repositioning slot — no further piece selection or move is possible; only End Turn recovers, and it logs a stale piece ID                                        |
| `packages/server/src/gameHandlers.ts`                        | 708-713 | FTP ball-delivery receiver lookup filters on `attackingTeam` only, unlike the team-agnostic BUG-04 pattern used for STANDARD_PASS/LONG_BALL elsewhere                                                                      | 🛑 Blocker | A defender legally repositioning onto `passTargetHex` during the DEFENDER slot causes the ball to be delivered as `carrierId: null` on an occupied hex with phase `PASS` (not `LOOSE_BALL`) and no possession transfer — an invalid board state |
| `packages/server/src/gameHandlers.ts`                        | 514-531 | GAME_MOVE FTP DEFENDER-slot branch has no exclusion/special handling of `passTargetHex` as a destination                                                                                                                   | ⚠️ Warning | Root enabler of the above — the defender-on-target scenario is legally reachable with no warning to the player; review's WR-02 also flags the missing visual cue for this hex                                                                   |
| `packages/client/src/components/ActionLog.tsx`               | 74-153  | `consolidateEvents` has no `FTP_MOVE` consolidation branch (unlike `MOVE`/`GK_KICK_MOVE`/`HP_MOVE`)                                                                                                                        | ℹ️ Info    | Currently harmless (FTP pace cap is 1, so at most one `FTP_MOVE` per slot today) but a latent inconsistency if the FTP pace cap is ever raised; not a blocker for this verification                                                             |
| `packages/server/src/__tests__/gameEngine.phase17.test.ts`   | 538-554 | Stale test asserts the abandoned single-step `firstTimePassStep`/`firstTimePassPath` design instead of the current two-slot model                                                                                          | ⚠️ Warning | One of the 4 documented pre-existing failures; unchanged across all 3 verification cycles, confirmed not worsened                                                                                                                               |
| `packages/server/src/__tests__/gameHandlers.phase17.test.ts` | 289-307 | Same abandoned-design staleness, different entry point                                                                                                                                                                     | ⚠️ Warning | Same as above                                                                                                                                                                                                                                   |
| `packages/server/src/__tests__/gameEngine.phase17.test.ts`   | 509-530 | 2 pre-existing MOVE-06 FREE_MOVE failures, out of 17.1's declared scope                                                                                                                                                    | ℹ️ Info    | Confirmed unchanged across all 3 verification cycles; not introduced or worsened by this phase                                                                                                                                                  |

No new `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any file modified by plans 17.1-11/12/13.

### Human Verification Required

#### 1. Undo permanently locks the FTP/HP repositioning slot

**Test:** In a live two-browser session, during FIRST_TIME_PASS_MOVE (or HIGH_PASS_MOVE), move a piece in the active slot, then click Undo, then attempt to select and move any piece again in the same slot.
**Expected:** After Undo, the player should be able to select and move a piece within the same slot, exactly as if the original move had never happened.
**Why human:** Confirms the player-visible dead-end (no selectable pieces, Undo button greys out with no further recourse) matches the static code analysis of `applyUndo` never resetting the lock/pace fields, and documents exactly what feedback (if any) the player sees.

#### 2. Defender landing on the FTP target hex produces an invalid delivery

**Test:** In a live two-browser session, play a first-time pass aimed within 1 hex of an opposing defender, then during the DEFENDER repositioning slot move that defender exactly onto the pass target hex, then End Turn.
**Expected:** The ball should be delivered with correct possession (to the defender, since a defending piece now occupies the target hex) rather than left as a carrierless "loose" ball on an occupied hex with possession unchanged.
**Why human:** Confirms the board-state inconsistency is reachable and visible in live play, and surfaces what the client renders/allows in this broken state.

## Gaps Summary

This is the third verification cycle for Phase 17.1. The history so far:

- **Cycle 1:** found the undo clause of success criterion 3 broken (old CR-01 — wrong move-type lookup in `applyUndo`/`canUndo`). Closed by plan 17.1-11.
- **Cycle 2:** re-verified the undo fix as genuinely correct, but a fresh code review then surfaced two further Critical defects: CR-01-new (client could not select/move any piece during FIRST_TIME_PASS_MOVE — `HexGrid.tsx`/`useGameStore.ts` were never extended) and CR-02-new (the server's interception loop could shadow the FIRST_TIME_PASS_MOVE transition for a defender-nearby pass). Both were independently confirmed real and assigned to gap-closure plans 17.1-12 and 17.1-13.
- **Cycle 3 (this report):** independently re-confirmed CR-01-new and CR-02-new are now genuinely fixed — not by trusting the fresh review's prose, but by direct reads of `useGameStore.ts`/`HexGrid.tsx` (selection wiring) and `gameEngine.ts` (interception bypass), plus targeted test re-runs (4/4 and 2/2 passing respectively) and a full-suite regression check (server 301/4-pre-existing/1/1, client 106/106, shared 317/317 — identical to the documented baseline across all three cycles, no new regressions, typecheck clean).

However, while verifying the fix for CR-01-new/CR-02-new, the same fresh code review surfaced two further Critical findings in the surrounding code, and this verification independently confirmed both are real, not review false positives:

1. **Review-CR-01 (applyUndo never resets FTP/HP lock fields):** confirmed by direct read of every site in the codebase that sets or clears `firstTimePassMovedPieceId`/`firstTimePassPaceUsed`/`highPassMovedPieceId`/`highPassPaceUsed` — `applyUndo` is the one mutator of game state during this phase that never touches them, and as a result a single Undo during FTP_MOVE or HP_MOVE makes the repositioning slot permanently unusable (both the client's own selection gate and the server's GAME_MOVE gate independently continue to treat the slot as locked/exhausted). This is judged severe enough to keep success criterion 3 FAILED: Undo is explicitly named in the criterion's text, it is a normal action a player would reasonably take, and the result is a genuine dead-end requiring an End Turn workaround that fabricates a misleading log entry (an `FTP_REPOSITION` event citing a piece ID that never completed its move).

2. **Review-CR-02 (FTP delivery receiver lookup is team-restricted, unlike the established team-agnostic BUG-04 pattern):** confirmed by direct comparison of `gameHandlers.ts:708-713` against `gameEngine.ts:1272-1297`, and confirmed directly reachable using the exact fixture plan 17.1-13 itself introduced (a defender 1 hex — i.e., adjacent — to the FTP target, free to step onto it during the DEFENDER slot with no exclusion check in `gameHandlers.ts:514-531`). This produces an invalid board state (phase `PASS`, `carrierId: null`, on a hex a defender occupies, possession unchanged) with no equivalent inconsistency anywhere else in the codebase. This is real and not cosmetic, but narrower in reach than Review-CR-01 since it requires the defender to land exactly on the specific target hex rather than triggering on every use of a named feature.

Neither of these two new findings looks like an intentional deviation — there is no alternative implementation achieving the same intent for either, and both are confirmed-reachable, untested gaps in scope that slipped through the narrowly-scoped plan 17.1-11 (which fixed only the move-type lookup, not the lock-field reset) and the original D-03 FTP delivery handler (which was modeled on HIGH_PASS_MOVE's team-restricted receiver pattern before the team-agnostic BUG-04 fix existed, and was never reconciled with it). No override is suggested for either gap.

**Net result:** the repositioning-selection and no-interception-delivery clauses of success criterion 3 are now genuinely fixed (closing cycle 2's gaps), but the criterion's undo clause and implicit delivery-correctness are newly found false for two further, independently-confirmed reasons. Phase 17.1 is not yet complete. A new gap-closure plan addressing Review-CR-01 (applyUndo lock-field reset) and Review-CR-02 (team-agnostic FTP delivery receiver lookup) is required before Phase 17.1 can be considered to have achieved its goal.

---

_Verified: 2026-06-19T20:00:00Z_
_Verifier: Claude (gsd-verifier)_
