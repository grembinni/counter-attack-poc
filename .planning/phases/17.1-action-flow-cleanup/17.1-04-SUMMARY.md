---
phase: 17.1-action-flow-cleanup
plan: 04
subsystem: game-engine
tags: [gameEngine, loose-ball, shot, boundary-clamping, range-gate, tests]

# Dependency graph
requires:
  - phase: 17.1-action-flow-cleanup
    provides: D-11 rename sweep + D-01/D-02 type changes + D-03/D-06/D-07 handlers
provides:
  - D-08: loose-ball board-edge clamping using direction-delta walk + isPitchHex
  - D-09: regular shot 11-hex range gate in applyDeclareShot
  - Engine tests proving D-08 clamp and D-09 gate behavior
affects:
  - 17.1-05 (DEFLECT_ACTION intercept)
  - 17.1-06 (summary / final wave)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'Direction-delta clamp walk: step by LOOSE_BALL_DIRECTIONS[dir-1] up to distance, keep last isPitchHex coord'
    - 'Fail-fast guard chain: phase → sequence → shooter-exists → range → GK → transition'

key-files:
  created: []
  modified:
    - packages/shared/src/scoreUtils.ts
    - packages/server/src/gameEngine.ts
    - packages/server/src/__tests__/gameEngine.phase17.test.ts
    - packages/server/src/__tests__/game.integration.test.ts
    - packages/client/src/components/ActionLog.tsx

key-decisions:
  - 'D-08: Export LOOSE_BALL_DIRECTIONS from scoreUtils.ts; clamp walk replaces hexLine in LOOSE_BALL branch'
  - 'D-09: Range guard uses hexDistance(shooter.position, goalHex) > 11 before GK_DIVE transition; snapshot 6-hex gate unchanged'
  - 'computeLooseBall removed from gameEngine imports — direction-delta walk computes path directly'
  - 'ActionLog.tsx: FTP_REPOSITION suppressed (like HP_REPOSITION); FTP_MOVE formatted like HP_MOVE'

patterns-established:
  - 'D-08 clamp pattern: for(step<distance){ next=from+delta*(step+1); if(isPitchHex)clampedPos=next; else break }'
  - 'applyDeclareShot guard order: WRONG_PHASE → goalQ bounds → SNAPSHOT_TARGET branch → INVALID_SEQUENCE → GK_exists → shooter_exists+range → GK_DIVE'

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-06-15
---

# Phase 17.1 Plan 04: D-08 Loose-Ball Board-Edge Clamp + D-09 Shot Range Gate Summary

**Board-edge clamping for loose-ball scatter using direction-delta walk; 11-hex range gate on regular shot target; 4 engine tests proving both behaviors**

## Performance

- **Duration:** 15 min
- **Started:** 2026-06-15T20:44:14Z
- **Completed:** 2026-06-15T20:59:32Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- D-08: Loose-ball scatter no longer goes off-pitch — direction-delta walk clamps to last valid isPitchHex coord
- D-09: Regular shot target hex rejected with INVALID_TARGET when shooter is >11 hexes from the goal
- 4 new passing engine tests (2 clamp, 2 range gate) bringing server total to 286 passing
- Rule 2 auto-fix: ActionLog.tsx handles FTP_REPOSITION and FTP_MOVE types (typecheck restored)
- Rule 1 auto-fix: integration test shooter repositioned within D-09 range gate (hexDist=11)

## Task Commits

1. **Task 1: D-08 clamp + D-09 range gate** — `645fcd3` (feat)
2. **Task 2: Engine tests + auto-fixes** — `c327468` (test)

## Files Created/Modified

- `packages/shared/src/scoreUtils.ts` — Export `LOOSE_BALL_DIRECTIONS` (was private `const`)
- `packages/server/src/gameEngine.ts` — D-08 clamp walk in LOOSE_BALL branch; D-09 guard in applyDeclareShot; remove unused `computeLooseBall` import
- `packages/server/src/__tests__/gameEngine.phase17.test.ts` — Add `applyDeclareShot`/`isPitchHex` imports; `looseBallNearEdgeState` fixture; D-08 and D-09 describe blocks (4 tests total)
- `packages/server/src/__tests__/game.integration.test.ts` — Reposition `seedPassPhaseForShot` shooter to hexDist=11 from goal (Rule 1 fix)
- `packages/client/src/components/ActionLog.tsx` — Add `FTP_REPOSITION` suppression in consolidate filter; add `FTP_REPOSITION` and `FTP_MOVE` cases in `formatEvent` switch (Rule 2 fix)

## Decisions Made

- Exported `LOOSE_BALL_DIRECTIONS` from `scoreUtils.ts` instead of duplicating the direction table in gameEngine.ts — single source of truth.
- Removed `computeLooseBall` import from gameEngine since direction-delta walk computes the same path inline. ESLint flagged unused import, which prompted the removal.
- D-09 guard placed after shooter lookup so the same `shooter` variable is reused for `hexLine(shooter.position, goalHex)` — no redundant lookup.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Integration test shooter was >11 hexes from goal after D-09 range gate added**

- **Found during:** Task 2 (running full test suite after D-09 implementation)
- **Issue:** `seedPassPhaseForShot` used carrier at `{q:15, r:17}` (distance=21 from goal) — rejected by new D-09 range gate, causing `game:shot from shooter in PASS phase` integration test to fail
- **Fix:** Reposition carrier to `{q:25, r:13}` (home) / `{q:11, r:13}` (away) — hexDist=11 from respective goals
- **Files modified:** `packages/server/src/__tests__/game.integration.test.ts`
- **Verification:** Integration test passes (17 passing, 1 todo)
- **Committed in:** `c327468` (Task 2 commit)

**2. [Rule 2 - Missing Critical] ActionLog.tsx lacked cases for FTP_REPOSITION and FTP_MOVE ActionEventTypes**

- **Found during:** Task 2 (running `pnpm -r typecheck` to verify overall typecheck state)
- **Issue:** `FTP_REPOSITION` and `FTP_MOVE` were added to `ActionEventType` in Wave 1 (Plan 01 / feat f0b8c98), but `ActionLog.tsx`'s `formatEvent` exhaustive switch was not updated. The pre-commit `pnpm build` of `@counter-attack/shared` in Task 1 surfaced this latent error. TypeScript error TS2366: Function lacks ending return statement.
- **Fix:** Added `FTP_REPOSITION` to consolidate-filter suppression list (mirrors `HP_REPOSITION`); added `FTP_REPOSITION` and `FTP_MOVE` cases to `formatEvent` switch (mirrors `HP_REPOSITION` / `HP_MOVE` patterns)
- **Files modified:** `packages/client/src/components/ActionLog.tsx`
- **Verification:** `pnpm -r typecheck` clean; `pnpm test` client 91 passing
- **Committed in:** `c327468` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 Rule 1 - bug, 1 Rule 2 - missing critical)
**Impact on plan:** Both auto-fixes necessary for correctness. No scope creep.

## Issues Encountered

None — plan executed cleanly after auto-fixes.

## Known Stubs

None introduced in this plan. Pre-existing stubs from earlier waves (MOVE-06 FREE_MOVE, PASS-02 firstTimePassStep) remain as documented RED stubs; count is unchanged at 4 failing tests.

## Threat Flags

None — changes are internal engine validation logic only; no new network endpoints or trust boundaries.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- D-08 and D-09 complete; full engine test suite green (286 server + 91 client + 241 shared)
- Typecheck clean across all packages
- 4 pre-existing RED stubs unchanged (MOVE-06 ×2, PASS-02 ×2)
- Ready for Plan 05 (D-10: DEFLECT_ACTION pass intercept changes)

---

_Phase: 17.1-action-flow-cleanup_
_Completed: 2026-06-15_
