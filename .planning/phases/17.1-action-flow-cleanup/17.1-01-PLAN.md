---
phase: 17.1-action-flow-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/actionSequence.ts
  - packages/shared/src/snapshotValidator.ts
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/mock/mockMovementState.ts
  - packages/client/src/mock/mockPassState.ts
  - packages/shared/src/actionSequence.test.ts
  - packages/shared/src/moveValidator.test.ts
  - packages/shared/src/passValidator.test.ts
  - packages/shared/src/snapshotValidator.test.ts
  - packages/client/src/components/ActionPanel.test.tsx
  - packages/client/src/store/useGameStore.rule11.test.ts
  - packages/server/src/__tests__/gameEngine.test.ts
  - packages/server/src/__tests__/gameEngine.phase8.test.ts
  - packages/server/src/__tests__/gameEngine.phase10.test.ts
  - packages/server/src/__tests__/gameEngine.phase17.test.ts
  - packages/server/src/__tests__/gameEngine.rule11.test.ts
  - packages/server/src/__tests__/gameHandlers.test.ts
  - packages/server/src/__tests__/gameHandlers.phase10.test.ts
  - packages/server/src/__tests__/gameHandlers.phase17.test.ts
  - packages/server/src/__tests__/gameHandlers.rule11.test.ts
  - packages/server/src/__tests__/game.integration.test.ts
  - packages/server/src/__tests__/kickoffSetup.integration.test.ts
  - packages/server/src/__tests__/replay.integration.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - 'TypeScript compiles with zero errors after all 8 GamePhase literals are renamed'
    - 'The full test suite passes with the renamed phase literals'
    - "No old phase literal ('MOVEMENT', 'GK_DIVING', etc.) remains as a GamePhase value anywhere"
    - "LastActionType values and lastDiceRoll.context strings containing 'PASS' are unchanged"
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'GamePhase union with renamed values + FIRST_TIME_PASS_MOVE added'
      contains: 'FIRST_TIME_PASS_MOVE'
    - path: 'packages/client/src/components/GameBoard.tsx'
      provides: 'PHASE_LABEL map keyed by renamed GamePhase values'
      contains: 'FIRST_TIME_PASS_MOVE'
  key_links:
    - from: 'packages/shared/src/types.ts'
      to: 'packages/server/src/gameEngine.ts'
      via: 'GamePhase union consumed by exhaustive phase switches'
      pattern: 'FIRST_TIME_PASS_MOVE'
---

<objective>
Rename 8 GamePhase string literal values across the entire monorepo as a single atomic,
TypeScript-compile-breaking change (D-11), and add the new 'FIRST_TIME_PASS_MOVE' phase value
to the GamePhase union (D-03 prerequisite). This MUST be the first plan executed and land as a
standalone commit so all subsequent plans reference the new names.

Purpose: Align phase naming with the v1.4.1 plan document so later logic-change plans (Waves 2-5)
operate on the corrected vocabulary. No behaviour changes — pure string substitution plus one
new union member.

Output: All .ts/.tsx source and test files updated to the new phase literals; clean typecheck;
green test suite; FIRST_TIME_PASS_MOVE present in the GamePhase union and the GameBoard PHASE_LABEL map.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md
@.planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md
</context>

<rename_table>
The exact, complete mapping (D-11). Every occurrence that is a GamePhase value must change:

| Old literal            | New literal          |
| ---------------------- | -------------------- |
| `'MOVEMENT'`           | `'MOVE'`             |
| `'SHOT_DECLARED'`      | `'SNAPSHOT_TARGET'`  |
| `'GK_DIVING'`          | `'GK_DIVE'`          |
| `'SNAP_DEFLECT'`       | `'SNAPSHOT_DEFLECT'` |
| `'HIGH_PASS_MOVEMENT'` | `'HIGH_PASS_MOVE'`   |
| `'QUICK_THROW'`        | `'GK_QUICK_THROW'`   |
| `'GK_KICK_MOVEMENT'`   | `'GK_KICK_MOVE'`     |

**MUST NOT change:**

- `'PASS'` — GamePhase value stays as `'PASS'` (rename deferred; too many non-phase PASS strings make a safe bulk rename risky)
- `LastActionType` values: `'STANDARD_PASS'`, `'FIRST_TIME_PASS'`, `'HIGH_PASS'`
- `lastDiceRoll.context` strings: `'PASS_ACCURACY'`, `'HEADING_DUEL'`
- `passType` parameter literals: `'STANDARD_PASS'`, `'FIRST_TIME_PASS'`, `'HIGH_PASS'`, `'STANDARD'`, `'FIRST_TIME'`, `'HIGH'`, `'LONG'`
- Comments mentioning "pass" / "movement"
- `'GK_KICK_TARGET'` — explicitly NOT renamed (user confirmed)
- CSS class names / helper text strings (display labels are values in PHASE_LABEL only)
  </rename_table>

<tasks>

<task type="auto">
  <name>Task 1: Rename GamePhase literals in shared + server source</name>
  <files>packages/shared/src/types.ts, packages/shared/src/actionSequence.ts, packages/shared/src/snapshotValidator.ts, packages/server/src/gameEngine.ts, packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/shared/src/types.ts (GamePhase union lines 277-299; ActionEventType lines 57-81)
    - packages/shared/src/actionSequence.ts (ELIGIBLE_NEXT_ACTIONS table — 'PASS' key references)
    - packages/server/src/gameEngine.ts (all `phase: '...'` and `state.phase === '...'` / `state.phase !== '...'` sites)
    - packages/server/src/gameHandlers.ts (DICE_PHASES set line ~75; phase guards lines ~129, ~219, ~885)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (D-11 scoping rule + gameEngine hotspot line list)
  </read_first>
  <action>
    Apply the rename_table to every GamePhase-value occurrence in these 5 files. In types.ts also
    ADD the new union member `'FIRST_TIME_PASS_MOVE'` to the GamePhase union (group it next to the
    post-rename `'HIGH_PASS_MOVE'`). For each old literal, change ONLY occurrences that are GamePhase
    values: `state.phase === 'X'`, `state.phase !== 'X'`, `phase: 'X'`, GamePhase union members,
    Set membership in DICE_PHASES, validUndoPhases array entries. Do NOT touch `'PASS'` — it is
    NOT in the rename table for this phase; `state.phase === 'PASS'` stays as-is. Do NOT touch
    `'STANDARD_PASS'`, `'FIRST_TIME_PASS'`, `'HIGH_PASS'`, `'PASS_ACCURACY'`, passType params, or
    `'GK_KICK_TARGET'`. Do NOT add FIRST_TIME_PASS_MOVE logic here (that is Plan 03) — only
    the type union member is added in this plan.
  </action>
  <verify>
    <automated>cd packages/shared && pnpm typecheck && cd ../server && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "'MOVEMENT'\|'GK_DIVING'\|'SNAP_DEFLECT'\|'HIGH_PASS_MOVEMENT'\|'QUICK_THROW'\|'GK_KICK_MOVEMENT'\|'SHOT_DECLARED'" packages/shared/src packages/server/src --include=*.ts | grep -v "STANDARD_PASS\|FIRST_TIME_PASS\|HIGH_PASS'\|PASS_ACCURACY" | grep -v '//'` returns no GamePhase-value lines
    - GamePhase union in types.ts contains the literal `'FIRST_TIME_PASS_MOVE'`
    - GamePhase union in types.ts contains `'MOVE'`, `'GK_DIVE'`, `'SNAPSHOT_DEFLECT'`, `'HIGH_PASS_MOVE'`, `'GK_QUICK_THROW'`, `'GK_KICK_MOVE'`, `'SNAPSHOT_TARGET'`
    - GamePhase union in types.ts still contains `'PASS'` (unchanged — NOT renamed)
    - `'STANDARD_PASS'` and `'FIRST_TIME_PASS'` LastActionType values still present unchanged in types.ts
    - `'GK_KICK_TARGET'` still present unchanged
    - `pnpm typecheck` passes for both `packages/shared` and `packages/server`
  </acceptance_criteria>
  <done>shared + server typecheck clean; 7 phase literals renamed (PASS excluded); FIRST_TIME_PASS_MOVE added to union; no PASS phase string altered</done>
</task>

<task type="auto">
  <name>Task 2: Rename GamePhase literals in client source + PHASE_LABEL map</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/GameBoard.tsx, packages/client/src/components/HexGrid.tsx, packages/client/src/store/useGameStore.ts, packages/client/src/mock/mockMovementState.ts, packages/client/src/mock/mockPassState.ts</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (PHASE_LABEL Record<GamePhase,string> lines 16-38)
    - packages/client/src/components/ActionPanel.tsx (phase === '...' branches; canUndo boundary)
    - packages/client/src/components/HexGrid.tsx (SHOT_DECLARED + phase references)
    - packages/client/src/store/useGameStore.ts (phase-gated store logic)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (GameBoard PHASE_LABEL full target map)
  </read_first>
  <action>
    Apply the rename_table to every GamePhase-value occurrence in these client files. In GameBoard.tsx
    update the 7 renamed keys of the `PHASE_LABEL: Record<GamePhase, string>` map AND add a new
    `FIRST_TIME_PASS_MOVE: 'FIRST-TIME PASS — REPOSITION'` entry (the Record is exhaustive over GamePhase
    so the new union member from Task 1 forces this entry to exist for typecheck to pass). Keep the
    existing display-label STRINGS as-is (e.g. key `MOVE` keeps label `'MOVEMENT PHASE'`; key `PASS`
    stays as key `PASS` since 'PASS' is NOT renamed). In ActionPanel.tsx rename
    `phase === 'MOVEMENT'/'GK_DIVING'/'SNAP_DEFLECT'/'HIGH_PASS_MOVEMENT'` branches to the new values;
    do NOT rename `phase === 'PASS'` — it stays as `'PASS'`. Do NOT yet add a FIRST_TIME_PASS_MOVE panel
    (that is Plan 06). In HexGrid.tsx rename SHOT_DECLARED references to SNAPSHOT_TARGET and any other
    phase literals except `'PASS'`. Do NOT change passType strings or LastActionType values.
  </action>
  <verify>
    <automated>cd packages/client && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - GameBoard.tsx PHASE_LABEL contains keys `MOVE`, `PASS`, `SNAPSHOT_TARGET`, `GK_DIVE`, `SNAPSHOT_DEFLECT`, `HIGH_PASS_MOVE`, `GK_QUICK_THROW`, `GK_KICK_MOVE`, and `FIRST_TIME_PASS_MOVE`
    - GameBoard.tsx PHASE_LABEL has NO keys `MOVEMENT`, `SHOT_DECLARED`, `GK_DIVING`, `SNAP_DEFLECT`, `HIGH_PASS_MOVEMENT`, `QUICK_THROW`, `GK_KICK_MOVEMENT` (but still has `PASS`)
    - `grep -n "'GK_DIVING'\|'SHOT_DECLARED'\|'SNAP_DEFLECT'\|'HIGH_PASS_MOVEMENT'" packages/client/src/components/ActionPanel.tsx packages/client/src/components/HexGrid.tsx` returns nothing
    - `pnpm typecheck` passes for `packages/client`
  </acceptance_criteria>
  <done>client typecheck clean; PHASE_LABEL exhaustive over renamed union including FIRST_TIME_PASS_MOVE; 'PASS' key unchanged; other 7 renames complete</done>
</task>

<task type="auto">
  <name>Task 3: Rename GamePhase literals in all test files + full suite green</name>
  <files>packages/shared/src/actionSequence.test.ts, packages/shared/src/moveValidator.test.ts, packages/shared/src/passValidator.test.ts, packages/shared/src/snapshotValidator.test.ts, packages/client/src/components/ActionPanel.test.tsx, packages/client/src/store/useGameStore.rule11.test.ts, packages/server/src/__tests__/gameEngine.test.ts, packages/server/src/__tests__/gameEngine.phase8.test.ts, packages/server/src/__tests__/gameEngine.phase10.test.ts, packages/server/src/__tests__/gameEngine.phase17.test.ts, packages/server/src/__tests__/gameEngine.rule11.test.ts, packages/server/src/__tests__/gameHandlers.test.ts, packages/server/src/__tests__/gameHandlers.phase10.test.ts, packages/server/src/__tests__/gameHandlers.phase17.test.ts, packages/server/src/__tests__/gameHandlers.rule11.test.ts, packages/server/src/__tests__/game.integration.test.ts, packages/server/src/__tests__/kickoffSetup.integration.test.ts, packages/server/src/__tests__/replay.integration.test.ts</files>
  <read_first>
    - packages/shared/src/passValidator.test.ts (expect('PASS')/expect('MOVEMENT') assertions)
    - packages/server/src/__tests__/gameEngine.phase17.test.ts (phase assertions for renamed values)
    - .planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md (Test Files Requiring Updates section)
  </read_first>
  <action>
    Apply the rename_table to every GamePhase-value occurrence in all listed test files. Targets are
    `expect(...).toBe('MOVEMENT')`, `phase: 'PASS'` in test fixtures/seeds, `toBe('GK_DIVING')`, etc.
    Use the same scoping discipline: do NOT change `'STANDARD_PASS'`, `'FIRST_TIME_PASS'`, `'HIGH_PASS'`,
    or `'PASS_ACCURACY'` in test assertions about LastActionType / lastDiceRoll.context. After renaming,
    run the entire suite; it must be fully green with no behaviour having changed (this plan is a pure rename).
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "toBe('MOVEMENT')\|toBe('GK_DIVING')\|toBe('SNAP_DEFLECT')\|toBe('HIGH_PASS_MOVEMENT')\|toBe('SHOT_DECLARED')\|toBe('QUICK_THROW')\|toBe('GK_KICK_MOVEMENT')" packages --include=*.test.ts --include=*.test.tsx` returns nothing (note: `toBe('PASS')` is NOT in this list — PASS is still a valid GamePhase value)
    - `pnpm test` exits 0 (entire monorepo suite passes)
    - `pnpm typecheck` passes across all three packages
  </acceptance_criteria>
  <done>Entire test suite green after rename; no old phase literal appears in any test assertion; typecheck clean monorepo-wide</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 01)

**GamePhase union (types.ts) — renamed values (7 renames; PASS stays as PASS):**

- `'MOVEMENT'` → `'MOVE'`
- `'SHOT_DECLARED'` → `'SNAPSHOT_TARGET'`
- `'GK_DIVING'` → `'GK_DIVE'`
- `'SNAP_DEFLECT'` → `'SNAPSHOT_DEFLECT'`
- `'HIGH_PASS_MOVEMENT'` → `'HIGH_PASS_MOVE'`
- `'QUICK_THROW'` → `'GK_QUICK_THROW'`
- `'GK_KICK_MOVEMENT'` → `'GK_KICK_MOVE'`
- `'PASS'` — **unchanged** (PASS → CHOOSE_ACTION deferred)

**New GamePhase value added:** `'FIRST_TIME_PASS_MOVE'` (type-level only; handler logic added in Plan 03)

**GameBoard PHASE_LABEL map:** new key `FIRST_TIME_PASS_MOVE` → label `'FIRST-TIME PASS — REPOSITION'`; 7 renamed keys; `PASS` key unchanged.

No function signatures change in this plan. No LastActionType / ActionEventType values change.
</artifacts_produced>

<verification>
- `pnpm typecheck` clean across packages/shared, packages/server, packages/client
- `pnpm test` green for the entire monorepo
- No GamePhase-value occurrence of any old literal remains in source or tests
- LastActionType values and lastDiceRoll.context strings unchanged
</verification>

<success_criteria>

- 7 GamePhase literals renamed per the exact mapping table (PASS excluded — deferred)
- `'FIRST_TIME_PASS_MOVE'` added to the GamePhase union and PHASE_LABEL map
- Zero typecheck errors; full test suite passes
- This plan committed as a standalone commit before any logic-change plan runs
  </success_criteria>

<output>
Create `.planning/phases/17.1-action-flow-cleanup/17.1-01-SUMMARY.md` when done
</output>
