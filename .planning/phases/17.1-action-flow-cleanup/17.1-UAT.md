---
status: diagnosed
phase: 17.1-action-flow-cleanup
source:
  [
    17.1-01-SUMMARY.md,
    17.1-02-SUMMARY.md,
    17.1-03-SUMMARY.md,
    17.1-04-SUMMARY.md,
    17.1-05-SUMMARY.md,
    17.1-06-SUMMARY.md,
  ]
started: 2026-06-19T14:03:42Z
updated: 2026-06-19T14:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Aerial Ability stat label

expected: Open the Player Stats panel for any player (goalkeeper or outfielder). The aerial stat is labeled "Aerial Ability" — there is no separate "Heading" stat anywhere.
result: pass

### 2. Tackle and steal ZoI are independent per action type

expected: When a defender attempts a tackle against the ball carrier and fails, that same defender no longer shows a tackle-risk (orange) zone for the rest of the sequence — the carrier can move past them again without re-triggering a tackle from that defender. If the carrier instead tries to dribble directly into that defender's hex (steal), the steal risk is still shown and still active.
result: issue
reported: "After a failed tackle the threat no longer show but neither does valid move squares next to the ball carrier. After a failed tackle the defender should still be able to move next to the ball carrier it just wont trigger a new tackle this move phase. After a failed steal the defender threat hexes still shows. After a failed steal the attacker should still be able to move next to the defender it just wont trigger a new steal from that defender this move phase. Threat hexes should show save movement if there are no defenders in range who can still make a steal or tackle action (depending on the use case)"
severity: major

### 3. First-time pass repositioning phase

expected: After completing an accurate first-time pass, both teams get a turn to reposition one player up to 1 hex (you'll see "First-time pass! Move 1 player to receive the ball." with Undo/End Turn controls). After both teams end their turn, the ball is delivered to the declared target hex and play resumes normally.
result: issue
reported: "there was no prompt for either team to move after first-time pass"
severity: major

### 4. GK Restart when keeper ends a move holding the ball in their own box

expected: If your goalkeeper is the ball carrier and ends the MOVE phase positioned inside their own penalty area, the game transitions to a GK Restart phase (kick/throw/move options) instead of the normal choose-action state.
result: pass

### 5. GK save spill keeps the ball with the keeper

expected: On a shot save where the goalkeeper doesn't cleanly hold the ball (a "spill"), the keeper still ends up holding the ball and the game transitions to GK Restart — the ball does not become a loose ball out on the pitch.
result: pass

### 6. Loose ball never scatters off the pitch

expected: When the ball becomes loose near the edge of the pitch and the scatter roll would send it past the boundary, the ball stops at the last hex still on the pitch instead of leaving the playing area.
result: issue
reported: "yes but a bug with loose ball - [LOOSE BALL] 21,4 -> 24,1 - that movement is not possible with 1 roll for direction and 1 roll for range will need to review all loose ball behavior and make sure it aligns with 1 roll for direction and 1 roll for range"
severity: major

### 7. Regular shot beyond 11 hexes is rejected

expected: Trying to declare a regular shot at a goal-line hex more than 11 hexes from the shooter is rejected — that hex isn't a valid shot target.
result: issue
reported: "not working as expected, i.e. when attacker is at 11,5 - goal hexes 0,10->0,12 are the only hexes in range and should be the only shot targets highlighted. Currently all goal line hexes are highlighted"
severity: major

### 8. Passing directly to a hex occupied by a defender is now allowed

expected: A standard pass aimed at a hex where an opposing defender is standing is now a legal pass target (no longer blocked outright). The ball goes to that hex and the defender standing there immediately intercepts it — no dice roll needed.
result: pass

## Summary

total: 8
passed: 4
issues: 4
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "When a defender attempts a tackle against the ball carrier and fails, that same defender no longer shows a tackle-risk (orange) zone for the rest of the sequence — the carrier can move past them again without re-triggering a tackle from that defender. If the carrier instead tries to dribble directly into that defender's hex (steal), the steal risk is still shown and still active."
  status: failed
  reason: "User reported: After a failed tackle the threat no longer show but neither does valid move squares next to the ball carrier. After a failed tackle the defender should still be able to move next to the ball carrier it just wont trigger a new tackle this move phase. After a failed steal the defender threat hexes still shows. After a failed steal the attacker should still be able to move next to the defender it just wont trigger a new steal from that defender this move phase. Threat hexes should show safe movement if there are no defenders in range who can still make a steal or tackle action (depending on the use case)"
  severity: major
  test: 2
  root_cause: "Two independent bugs. (1) moveValidator.ts tackle branch (lines 106-124) returns {ok:false, reason:'TACKLE_ALREADY_ATTEMPTED'} for a spent tackler instead of falling through to {ok:true} with no effect (the steal branch at lines 95-104 does this correctly already) — useGameStore.ts derives both validMoveHexes and tackleRiskHexes from this single validateMove() result, so the ok:false rejection strips the hex from valid moves entirely instead of just clearing the risk tint. (2) HexGrid.tsx's zoiRiskSet (steal-risk tint, lines 141-150) calls getZoIDefenders directly with no exclusion filter at all — stealAttemptedByIds is never referenced anywhere in packages/client/src, so the steal-risk tint never clears."
  artifacts:
  - path: "packages/shared/src/moveValidator.ts"
    issue: "Tackle branch (lines 106-124) rejects the move (ok:false) for an excluded tackler instead of allowing it with no TACKLE_ATTEMPT effect, asymmetric with the correct steal branch"
  - path: "packages/client/src/components/HexGrid.tsx"
    issue: "zoiRiskSet (lines 141-150) computed via raw getZoIDefenders with no stealAttemptedByIds filter — steal-risk tint never excludes spent defenders"
  - path: "packages/client/src/store/useGameStore.ts"
    issue: "validMoveHexes and tackleRiskHexes both derived from the same validateMove() call (lines 438-450, 540-556) — inherits the moveValidator ok:false bug"
    missing:
  - "moveValidator.ts: change tackle branch to mirror steal branch — skip TACKLE_ATTEMPT effect but still return {ok:true} when defender is in tackleAttemptedByIds"
  - "HexGrid.tsx: filter getZoIDefenders(hex, opponents) results by state.stealAttemptedByIds before adding to zoiRiskSet (need to subscribe to that gameState field)"
    debug_session: ".planning/debug/zoi-tackle-steal-exclusion.md"

- truth: "After completing an accurate first-time pass, both teams get a turn to reposition one player up to 1 hex (you'll see \"First-time pass! Move 1 player to receive the ball.\" with Undo/End Turn controls). After both teams end their turn, the ball is delivered to the declared target hex and play resumes normally."
  status: failed
  reason: "User reported: there was no prompt for either team to move after first-time pass"
  severity: major
  test: 3
  root_cause: "gameEngine.ts applyRoll()'s 'PASS' case has an occupant-check early-return (lines 1223-1248) that fires before the D-03 FIRST_TIME_PASS_MOVE transition block (line 1312). Whenever the pass target hex is occupied — which is virtually always true for a first-time pass, since it's aimed at a visible teammate — the function returns phase:'PASS' immediately, never reaching the FIRST_TIME_PASS_MOVE check. A pre-17.1 test (gameEngine.test.ts:849) locks in this buggy behavior by asserting phase==='PASS' for an occupied-target first-time pass; it was never updated when 17.1-03 added D-03. The 17.1-03 test suite never exercised this path end-to-end (its fixtures construct state already inside FIRST_TIME_PASS_MOVE), so the gap went undetected."
  artifacts:
  - path: "packages/server/src/gameEngine.ts"
    issue: "applyRoll() 'PASS' case occupant-check (lines 1223-1248) shadows the FIRST_TIME_PASS_MOVE transition (lines 1308-1332) for any occupied target hex"
  - path: "packages/server/src/**tests**/gameEngine.test.ts"
    issue: "Line 849 'FIRST_TIME_PASS (PASS-02)' test asserts phase==='PASS' for an occupied-target delivery, locking in the regression"
    missing:
  - "Reorder applyRoll() so the FIRST_TIME_PASS_MOVE transition check (newLastActionType === 'FIRST_TIME_PASS') runs before the generic occupant-check, so it always enters repositioning regardless of target occupancy"
  - "Update gameEngine.test.ts:849 to expect phase==='FIRST_TIME_PASS_MOVE' instead of 'PASS'"
  - "Add a new end-to-end test driving an occupied-target first-time pass through applyRoll() into FIRST_TIME_PASS_MOVE"
    debug_session: ".planning/debug/ftp-no-reposition-prompt.md"

- truth: "When the ball becomes loose near the edge of the pitch and the scatter roll would send it past the boundary, the ball stops at the last hex still on the pitch instead of leaving the playing area."
  status: failed
  reason: "User reported: yes but a bug with loose ball - [LOOSE BALL] 21,4 -> 24,1 - that movement is not possible with 1 roll for direction and 1 roll for range will need to review all loose ball behavior and make sure it aligns with 1 roll for direction and 1 roll for range"
  severity: major
  test: 6
  root_cause: "LOOSE_BALL_DIRECTIONS (scoreUtils.ts:48-55) is a stale fixed-delta table referencing a no-longer-existent AXIAL_DIRECTIONS coordinate system, but the live pitch uses ODD-Q offset coordinates where diagonal neighbor deltas must alternate by column parity. computeLooseBall and the D-08 clamp walk in gameEngine.ts both apply the same fixed (q,r) delta repeated `distance` times with no parity correction. For NE and SW specifically, this causes every multi-step scatter (distance >= 2) to diverge from a true straight line. Verified the reported case mathematically: (21,4) + NE-delta\*3 = (24,1) is exactly what the buggy formula produces, but hexDistance((21,4),(24,1)) via the project's own toCube/hexDistance is 5, not 3 — confirming a real arithmetic bug, not user error. Pre-existing (predates phase 17.1); the D-08 clamp walk faithfully reproduced the already-broken formula. Affects roughly 1/3 of direction rolls (NE, SW) for nearly all distance values >= 2."
  artifacts:
  - path: "packages/shared/src/scoreUtils.ts"
    issue: "LOOSE_BALL_DIRECTIONS (lines 48-55) uses stale axial deltas with no column-parity adjustment; computeLooseBall (lines 69-78) applies fixed delta x distance"
  - path: "packages/server/src/gameEngine.ts"
    issue: "LOOSE_BALL case clamp walk (lines 1972-1988) reimplements the same flawed delta-times-step formula"
  - path: "packages/shared/src/hex.ts"
    issue: "Contains the correct ODD-Q offset geometry (ODD_Q_NEIGHBORS, toCube/fromCube, hexDistance, hexLine) that should be the source of truth for the scatter walk but isn't consulted"
  - path: "packages/shared/src/scoreUtils.test.ts"
    issue: "Existing tests (lines 30-61) validate against the buggy formula itself rather than true grid geometry; one test case asserts a result that is itself geometrically wrong"
    missing:
  - "Redefine the loose-ball scatter as a true single-direction walk using cube-coordinate unit vectors (parity-independent), converting to/from ODD-Q offset only at the boundary, mirroring hexLine/hexesInRange in hex.ts"
  - "Update computeLooseBall and the gameEngine.ts D-08 clamp walk to share a single correct source of truth for the scatter trajectory"
  - "Add a regression test asserting hexDistance(from, computeLooseBall(from, direction, distance)) === distance for all 6 directions x both column parities x distances 1-6"
    debug_session: ".planning/debug/loose-ball-scatter-rolls.md"

- truth: "Trying to declare a regular shot at a goal-line hex more than 11 hexes from the shooter is rejected — that hex isn't a valid shot target."
  status: failed
  reason: "User reported: not working as expected, i.e. when attacker is at 11,5 - goal hexes 0,10->0,12 are the only hexes in range and should be the only shot targets highlighted. Currently all goal line hexes are highlighted"
  severity: major
  test: 7
  root_cause: "HexGrid.tsx's isShootingModeGoalHex computation (lines 262-269) has two branches joined by ||: the SNAPSHOT_TARGET branch correctly gates on hexDistance(snapCarrier.position, hex) <= 6, but the regular-shot 'shootingMode ||' branch has no distance filter at all — it only checks isActivePlayer and goal-line membership, so it unconditionally highlights all 7 goal-line hexes whenever shooting mode is active. This was never updated when D-09 added the server-side 11-hex gate (applyDeclareShot, gameEngine.ts:2836) in phase 17.1 plan 04. No security/validation gap exists — the server still correctly rejects an out-of-range click with INVALID_TARGET — this is a pure client-side highlight accuracy bug."
  artifacts:
  - path: "packages/client/src/components/HexGrid.tsx"
    issue: "isShootingModeGoalHex (lines 256-269) missing hexDistance(shooter.position, hex) <= 11 check in the shootingMode branch"
    missing:
  - "Resolve the regular shooter the same way snapCarrier is resolved (ball.carrierId -> pieces.find), and add hexDistance(shooter.position, hex) <= 11 to the shootingMode branch condition, mirroring the existing <= 6 pattern for SNAPSHOT_TARGET"
    debug_session: ".planning/debug/shot-range-gate-no-filter.md"
