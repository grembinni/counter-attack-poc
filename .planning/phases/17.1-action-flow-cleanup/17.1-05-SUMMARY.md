---
phase: 17.1-action-flow-cleanup
plan: 05
subsystem: passValidator, gameEngine
tags: [pass-intercept, passValidator, gameEngine, D-10, intercept-split]

# Dependency graph
requires:
  - phase: 17.1-action-flow-cleanup
    provides: 'prior waves 1-4 changes (D-11, D-01, D-02, D-03, D-06, D-07, D-08, D-09)'
provides:
  - 'PassResult union split: autoIntercepts[] (case 1 no-roll) and rollIntercepts[] (case 3 threshold-roll)'
  - 'Case-1 auto-intercept: STANDARD pass to defender-occupied destination is now allowed and immediately intercepted'
  - 'applyRoll PASS branch consumes autoIntercepts before rollIntercepts'
  - 'gameHandlers dice pre-generation updated to use rollIntercepts.length'
affects:
  - 'Any future pass-related feature that reads PassResult ok:true variants'
  - 'Client-side highlight logic — destination hex occupied by defender is now a valid pass target'

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'D-10 three-case split: case 1 autoIntercepts (no dice), case 2 PATH_BLOCKED, case 3 rollIntercepts (die===6||combined>=10)'

key-files:
  created: []
  modified:
    - packages/shared/src/passValidator.ts
    - packages/shared/src/passValidator.test.ts
    - packages/server/src/gameEngine.ts
    - packages/server/src/gameHandlers.ts

key-decisions:
  - 'PassResult ok:true carries autoIntercepts+rollIntercepts instead of single interceptors list'
  - 'Case-1 destination defender detection ONLY for STANDARD pass (FIRST_TIME has no path blocking; HIGH/LONG skip interception)'
  - 'gameHandlers pre-generates dice for rollIntercepts only (autoIntercepts need no dice)'
  - 'gameHandlers.ts fix treated as Rule 3 blocking deviation (same task scope as Task 2)'

patterns-established:
  - 'auto-intercept: defender at destination hex → autoIntercepts; no dice; immediate possession transfer'
  - 'roll-intercept: ZoI-adjacent defender → rollIntercepts; die===6 || combined>=10 threshold unchanged'

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-06-15
---

# Phase 17.1 Plan 05: Pass Interception Return-Shape Change (D-10) Summary

**PassResult split into autoIntercepts/rollIntercepts with case-1 destination-defender auto-intercept (no roll) and case-2 PATH_BLOCKED preserved for intermediate blockers**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-15T21:03:19Z
- **Completed:** 2026-06-15T21:09:02Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Changed `PassResult` union to expose `autoIntercepts: PlayerPiece[]` and `rollIntercepts: PlayerPiece[]` instead of single `interceptors` list
- Added case-1 detection: STANDARD pass to a defender-occupied destination hex is now allowed (not PATH_BLOCKED) and produces an auto-intercept (no dice roll required)
- Updated `applyRoll` PASS branch to process `autoIntercepts` first (immediate possession transfer) then `rollIntercepts` (existing `die === 6 || combined >= 10` threshold unchanged)
- Migrated all 25 passValidator tests to new field names; added explicit case-1 test; retained all case-2 PATH_BLOCKED coverage

## Task Commits

Each task was committed atomically:

1. **Task 1: passValidator return-shape change + case-1 auto-intercept** - `25f6015` (feat)
2. **Task 2: applyRoll PASS loop consumes autoIntercepts then rollIntercepts** - `e742024` (feat)
3. **Task 3: passValidator tests — add case-1, keep case-2 PATH_BLOCKED** - `cdb90de` (test)

## Files Created/Modified

- `packages/shared/src/passValidator.ts` — PassResult union changed; interception logic refactored into autoIntercepts/rollIntercepts; case-1 destination-defender detection added
- `packages/shared/src/passValidator.test.ts` — 25 tests passing; all `result.interceptors` migrated; new case-1 test added; ZoI tests renamed to "rollInterceptor"
- `packages/server/src/gameEngine.ts` — applyRoll PASS branch: autoIntercepts loop (no dice) before rollIntercepts loop (threshold unchanged)
- `packages/server/src/gameHandlers.ts` — Pre-generated interception dice updated to use `rollIntercepts.length` (not `interceptors`)

## Decisions Made

- Case-1 detection is scoped to STANDARD pass only: FIRST_TIME has no path blocking, HIGH/LONG skip interception entirely — consistent with existing guard structure
- The case-2 guard (intermediate hex blocking via `hexLine.slice(1,-1)`) remains completely unchanged — its behavior is correct and unrelated to the destination check
- `gameHandlers.ts` update to use `rollIntercepts` was handled as a Rule 3 blocking deviation (same commit as Task 2 since it was the direct consumer)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] gameHandlers.ts referenced passResult.interceptors after Task 1 removed the field**

- **Found during:** Task 2 (server typecheck)
- **Issue:** `gameHandlers.ts` line 1238 read `passResult.interceptors.length` and `passResult.interceptors.map(...)` to pre-generate interception dice. After Task 1 renamed the field, this caused a TypeScript error.
- **Fix:** Updated `gameHandlers.ts` to read `passResult.rollIntercepts.length` and `passResult.rollIntercepts.map(...)`. Auto-intercepts (case 1) need no dice, so only `rollIntercepts` drives dice pre-generation.
- **Files modified:** `packages/server/src/gameHandlers.ts`
- **Verification:** `pnpm typecheck` clean across all packages
- **Committed in:** `e742024` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Required fix — the gameHandlers.ts update is semantically correct (only roll-intercepts need pre-generated dice). No scope creep.

## Issues Encountered

None — pre-existing RED stubs (4 server tests in phase17 test files: MOVE-06 x2, PASS-02 x2) remain unchanged as documented in plan context.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- D-10 complete: case 1 auto-intercept, case 2 PATH_BLOCKED preserved, case 3 roll-intercept
- All 286 server + 91 client + 242 shared tests passing (242 shared = 241 baseline + 1 new case-1 test)
- 4 pre-existing RED stubs remain (Phase 17.1 Wave 5+ work, not caused by this plan)
- Typecheck clean across all packages
- Phase 17.1 complete — all 5 waves executed

---

_Phase: 17.1-action-flow-cleanup_
_Completed: 2026-06-15_

## Self-Check: PASSED

- [x] `packages/shared/src/passValidator.ts` — exists, contains `autoIntercepts`, no `interceptors` field
- [x] `packages/server/src/gameEngine.ts` — exists, contains `autoIntercepts` and `rollIntercepts`
- [x] `packages/shared/src/passValidator.test.ts` — exists, 25 tests passing
- [x] Commits 25f6015, e742024, cdb90de — all present in git log
- [x] `pnpm typecheck` — clean
- [x] `pnpm test -- passValidator` — 25 tests passed
