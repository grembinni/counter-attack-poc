---
phase: 17.1-action-flow-cleanup
plan: 06
type: execute
wave: 3
depends_on:
  - 17.1-02
files_modified:
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/ActionPanel.test.tsx
  - packages/client/src/components/BallMarker.tsx
  - packages/client/src/components/HexCell.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - 'During FIRST_TIME_PASS_MOVE the active player sees a repositioning panel with Undo and End Turn, mirroring the HIGH_PASS_MOVE panel'
    - 'Undo is enabled in FIRST_TIME_PASS_MOVE only when the last event is an FTP_REPOSITION boundary'
    - 'The uncommitted BallMarker and HexCell visual changes are included in the phase commit'
  artifacts:
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'FIRST_TIME_PASS_MOVE panel branch + FTP_REPOSITION canUndo boundary'
      contains: 'FIRST_TIME_PASS_MOVE'
  key_links:
    - from: 'packages/client/src/components/ActionPanel.tsx'
      to: 'packages/shared/src/types.ts'
      via: "canUndo checks evt.type === 'FTP_REPOSITION'"
      pattern: 'FTP_REPOSITION'
---

<objective>
Complete the client-side surface for the first-time pass repositioning phase (D-03): add a
FIRST_TIME_PASS_MOVE panel branch to ActionPanel mirroring the HIGH_PASS_MOVE panel, extend the
canUndo boundary to recognise the FTP_REPOSITION event, and finalise the already-complete uncommitted
visual changes in BallMarker.tsx and HexCell.tsx by including them in this phase's commit.

Purpose: Without the panel branch, the active player has no UI to reposition or End Turn during
FIRST_TIME_PASS_MOVE. The visual files are done work that must land with 17.1. The GameBoard PHASE_LABEL
entry for FIRST_TIME_PASS_MOVE was already added in Plan 01, so no GameBoard change is needed here.

Output: ActionPanel FIRST_TIME_PASS_MOVE branch + FTP_REPOSITION canUndo; an ActionPanel test for the
new panel; committed BallMarker/HexCell visual changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md
@.planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActionPanel FIRST_TIME_PASS_MOVE panel + FTP_REPOSITION canUndo boundary</name>
  <files>packages/client/src/components/ActionPanel.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (HIGH_PASS_MOVE panel block ~lines 125-146 post-rename; canUndo boundary ~line 115; waitingPanel / isActivePlayer guards)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (ActionPanel D-03 panel sketch + canUndo extension)
  </read_first>
  <action>
    Add a `if (phase === 'FIRST_TIME_PASS_MOVE')` branch to ActionPanel, placed adjacent to the
    HIGH_PASS_MOVE block and before the generic isActivePlayer guard. Copy the HIGH_PASS_MOVE panel
    structure: return null if myTeam === null, return waitingPanel if not the active player, otherwise
    render the helper block (line 1 e.g. "First-time pass!", line 2 e.g. "Move 1 player to receive the
    ball."), an Undo button disabled when `!canUndo`, an End Turn button, and the gameError text. Extend
    the canUndo boundary expression to also return true when
    `phase === 'FIRST_TIME_PASS_MOVE' && evt.type === 'FTP_REPOSITION'` (mirrors the existing HIGH_PASS_MOVE
    + HP_REPOSITION clause). Reuse the existing emitUndo / emitEndTurn handlers.
  </action>
  <verify>
    <automated>cd packages/client && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - ActionPanel.tsx contains a `phase === 'FIRST_TIME_PASS_MOVE'` branch rendering Undo + End Turn buttons
    - The canUndo expression includes a clause `phase === 'FIRST_TIME_PASS_MOVE' && evt.type === 'FTP_REPOSITION'`
    - The new branch returns waitingPanel when the viewer is not the active player
    - `pnpm typecheck` for packages/client passes
  </acceptance_criteria>
  <done>Active player gets an FTP repositioning panel; Undo gated on the FTP_REPOSITION boundary</done>
</task>

<task type="auto">
  <name>Task 2: ActionPanel test for FIRST_TIME_PASS_MOVE panel</name>
  <files>packages/client/src/components/ActionPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.test.tsx (existing HIGH_PASS_MOVE panel test as the analog; render/store-mock helpers)
    - packages/client/src/components/ActionPanel.tsx (the branch added in Task 1)
  </read_first>
  <action>
    Add a test rendering ActionPanel with `phase: 'FIRST_TIME_PASS_MOVE'` for the active player; assert
    the helper text and both the Undo and End Turn buttons render. Add an assertion that Undo is disabled
    when canUndo is false (no FTP_REPOSITION as last event) and enabled when the last event is an
    FTP_REPOSITION. Add a non-active-player render asserting the waiting panel appears. Reuse the existing
    HIGH_PASS_MOVE test's store-mock setup as the template.
  </action>
  <verify>
    <automated>pnpm test -- ActionPanel</automated>
  </verify>
  <acceptance_criteria>
    - Test renders FIRST_TIME_PASS_MOVE panel and asserts Undo + End Turn buttons present for the active player
    - Test asserts Undo disabled without FTP_REPOSITION and enabled with it
    - Test asserts non-active player sees the waiting panel
    - `pnpm test -- ActionPanel` passes
  </acceptance_criteria>
  <done>FIRST_TIME_PASS_MOVE panel behaviour covered by passing ActionPanel tests</done>
</task>

<task type="auto">
  <name>Task 3: Finalise uncommitted BallMarker + HexCell visual changes</name>
  <files>packages/client/src/components/BallMarker.tsx, packages/client/src/components/HexCell.tsx</files>
  <read_first>
    - packages/client/src/components/BallMarker.tsx (current uncommitted state — soccer-ball dot pattern, BALL_R=12, patches in #111 on cream)
    - packages/client/src/components/HexCell.tsx (current uncommitted state — 'reachable' highlight stroke removed)
    - .planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md (Visual Changes section — both confirmed complete as-is)
  </read_first>
  <action>
    These two files already contain complete, intended visual changes (per CONTEXT.md they are done as-is).
    Do NOT add further visual work. Verify they still typecheck and that the client test suite passes with
    them in place so they are committed cleanly as part of the 17.1 phase. Make no functional edits unless a
    typecheck/test failure requires a minimal fix.
  </action>
  <verify>
    <automated>cd packages/client && pnpm typecheck && cd ../.. && pnpm test -- HexCell</automated>
  </verify>
  <acceptance_criteria>
    - BallMarker.tsx and HexCell.tsx typecheck without errors
    - HexCell test suite passes with the current visual changes in place
    - No new visual behaviour added beyond the existing uncommitted changes
    - Full `pnpm test` for packages/client passes
  </acceptance_criteria>
  <done>Visual changes verified clean and included in the phase; no additional visual work introduced</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 06)

**ActionPanel.tsx — new React branch:** `phase === 'FIRST_TIME_PASS_MOVE'` panel (Undo + End Turn,
helper text), mirroring HIGH_PASS_MOVE. canUndo boundary extended with the
`FIRST_TIME_PASS_MOVE && FTP_REPOSITION` clause.

**BallMarker.tsx / HexCell.tsx:** pre-existing uncommitted visual changes finalised and committed (no
new behaviour). GameBoard PHASE_LABEL FIRST_TIME_PASS_MOVE entry was added in Plan 01 — not modified here.

No type changes, no new function signatures.
</artifacts_produced>

<verification>
- `pnpm typecheck` clean for packages/client
- `pnpm test` green for client (ActionPanel + HexCell)
</verification>

<success_criteria>

- FIRST_TIME_PASS_MOVE panel present with correct Undo gating
- Visual files committed as part of 17.1
- Client suite green
  </success_criteria>

<output>
Create `.planning/phases/17.1-action-flow-cleanup/17.1-06-SUMMARY.md` when done
</output>
