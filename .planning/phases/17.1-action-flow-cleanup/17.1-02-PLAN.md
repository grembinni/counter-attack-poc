---
phase: 17.1-action-flow-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 17.1-01
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/teams.ts
  - packages/shared/src/moveValidator.ts
  - packages/server/src/gameEngine.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - 'PlayerPiece has no `heading` field; aerialAbility is the sole aerial stat for all roles'
    - "Every outfielder's aerialAbility equals the value its heading field previously held"
    - 'A defender who has attempted a tackle no longer projects tackle ZoI but still projects steal ZoI (and vice versa)'
    - 'stealAttemptedByIds and tackleAttemptedByIds reset to [] at every CHOOSE_ACTION transition'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'PlayerPiece without heading; GameState with stealAttemptedByIds/tackleAttemptedByIds; HEADER ActionEvent with aerialAbility fields'
      contains: 'stealAttemptedByIds'
    - path: 'packages/shared/src/moveValidator.ts'
      provides: 'ZoI steal exclusion filter mirroring the tackle exclusion'
      contains: 'stealAttemptedByIds'
  key_links:
    - from: 'packages/shared/src/moveValidator.ts'
      to: 'packages/shared/src/types.ts'
      via: 'reads state.stealAttemptedByIds / state.tackleAttemptedByIds'
      pattern: 'stealAttemptedByIds'
    - from: 'packages/server/src/gameEngine.ts'
      to: 'packages/shared/src/types.ts'
      via: 'every CHOOSE_ACTION spread resets both flag arrays'
      pattern: "stealAttemptedByIds: \\[\\]"
---

<objective>
Consolidate the aerial stat model (D-01): remove the duplicate `heading` field from PlayerPiece,
migrate values into `aerialAbility`, and rename the HEADER ActionEvent fields. Fix the Zone of
Influence exclusion and reset timing (D-02): exclude flagged defenders from their respective
action type only, and reset both flag arrays at every CHOOSE_ACTION transition.

Purpose: D-01 fixes a stat duplication that makes header duel scoring read the wrong field.
D-02 fixes currently-broken gameplay where a defender re-triggers a tackle/steal challenge after
already attempting one this sequence.

Output: PlayerPiece with a single aerial stat; teams.ts migrated; moveValidator steal exclusion;
gameEngine heading→aerialAbility call-site updates and flag-array resets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md
@.planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stat model surgery — remove heading, add flag-array fields + ActionEvent rename (types.ts)</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts (PlayerPiece lines ~5-35; HEADER ActionEvent ~236-239; GameState ~417 near contestedPieceIds; ActionEventType lines 57-81)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (types.ts Edits 1,2,4,5)
  </read_first>
  <action>
    Apply four type-level edits to types.ts: (1) delete `heading: number;` from PlayerPiece — keep
    the existing `aerialAbility: number;`. (2) In the HEADER ActionEvent type rename field
    `attackerHeading` → `attackerAerialAbility` and `defenderHeading` → `defenderAerialAbility`.
    (3) Add two optional readonly fields to GameState near `contestedPieceIds`:
    `stealAttemptedByIds?: readonly string[]` and `tackleAttemptedByIds?: readonly string[]`, each with
    a doc comment noting they reset at every CHOOSE_ACTION transition (D-02). (4) Add `'FTP_REPOSITION'`
    to the ActionEventType union, mirroring the existing `'HP_REPOSITION'` member (needed by Plan 03 undo).
    Note `tackleAttemptedByIds` is already read at moveValidator.ts:116 via `?? []` — this declares it formally.
  </action>
  <verify>
    <automated>cd packages/shared && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "heading" packages/shared/src/types.ts` returns no `heading: number` PlayerPiece field
    - types.ts contains `attackerAerialAbility` and `defenderAerialAbility`; contains neither `attackerHeading` nor `defenderHeading`
    - types.ts GameState contains `stealAttemptedByIds` and `tackleAttemptedByIds`
    - ActionEventType union contains `'FTP_REPOSITION'`
    - `pnpm typecheck` for packages/shared reports errors ONLY in teams.ts/gameEngine.ts (call sites fixed in Tasks 2-3), not in types.ts itself
  </acceptance_criteria>
  <done>PlayerPiece has no heading; flag arrays + FTP_REPOSITION + renamed ActionEvent fields declared on types</done>
</task>

<task type="auto">
  <name>Task 2: Migrate heading→aerialAbility values in teams.ts (all squads + free agents)</name>
  <files>packages/shared/src/teams.ts</files>
  <read_first>
    - packages/shared/src/teams.ts (all player objects in TEAM_SQUADS — 4 squads — and FREE_AGENTS)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (teams.ts migration pattern: outfielder copy, GK delete)
  </read_first>
  <action>
    For every player object in all four TEAM_SQUADS and in FREE_AGENTS: for outfielders (role !== 'GK'),
    copy the current `heading` numeric value into `aerialAbility`, then delete the `heading:` line. For
    GKs (role === 'GK'), `aerialAbility` already holds the correct value — just delete the `heading:` line.
    Do not change any other attribute. After this task no `heading:` field remains in teams.ts.
  </action>
  <verify>
    <automated>cd packages/shared && pnpm typecheck && pnpm test -- teams</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "heading" packages/shared/src/teams.ts` returns 0
    - Every player object in teams.ts has an `aerialAbility:` field
    - `pnpm typecheck` for packages/shared passes (teams.ts no longer errors)
    - teams.test.ts passes
  </acceptance_criteria>
  <done>No heading field in teams.ts; outfielder aerialAbility values migrated from former heading; GK aerialAbility untouched</done>
</task>

<task type="auto">
  <name>Task 3: gameEngine heading→aerialAbility call sites + moveValidator steal exclusion + flag resets</name>
  <files>packages/shared/src/moveValidator.ts, packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/shared/src/moveValidator.ts (steal path ~line 95; tackle exclusion ~lines 107-120; line 116 tackleAttemptedByIds usage)
    - packages/server/src/gameEngine.ts (piece.heading sites: 1548, 2367, 1710, 1711, 1731, 1734, 1800, 1803, 1831, 1834, 2407, 2410, 2474; HEADER ActionEvent log field sites; applyStartMovement reset lines 249-250; every `phase: 'CHOOSE_ACTION'` spread)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (moveValidator steal-exclusion sketch; gameEngine D-02 reset pattern)
  </read_first>
  <action>
    Three changes. (A) In gameEngine.ts replace every `piece.heading` / `player.heading` read with
    `.aerialAbility` at all listed line sites, and rename the HEADER ActionEvent log field keys
    `attackerHeading`/`defenderHeading` → `attackerAerialAbility`/`defenderAerialAbility` at each HEADER
    ActionEvent construction site. Leave the `'HEADING_DUEL'` lastDiceRoll.context string unchanged.
    (B) In moveValidator.ts, in the steal path (ball carrier moving into ZoI), filter the
    `getZoIDefenders` result to exclude defenders whose id is in `state.stealAttemptedByIds ?? []` BEFORE
    the `defenders.length > 0` check — mirroring the existing tackle exclusion at lines ~107-120. A
    defender flagged for tackle must still project steal ZoI and vice versa. (C) In gameEngine.ts add
    `stealAttemptedByIds: []` and `tackleAttemptedByIds: []` to EVERY state spread that sets
    `phase: 'CHOOSE_ACTION'` (the applyEndTurn ATTACKER_2 branch, the applyRoll LOOSE_BALL→CHOOSE_ACTION
    branch, and any other CHOOSE_ACTION spread). Keep the existing reset in applyStartMovement (249-250)
    as a safety net.
  </action>
  <verify>
    <automated>cd packages/shared && pnpm typecheck && cd ../server && pnpm typecheck && cd ../.. && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.heading\b" packages/server/src/gameEngine.ts` returns no PlayerPiece stat reads (HEADING_DUEL context string may remain)
    - `grep -n "attackerHeading\|defenderHeading" packages/server/src/gameEngine.ts` returns nothing
    - moveValidator.ts steal path filters by `stealAttemptedByIds`; `grep -c "stealAttemptedByIds" packages/shared/src/moveValidator.ts` is >= 1
    - Every `phase: 'CHOOSE_ACTION'` spread in gameEngine.ts is accompanied by `stealAttemptedByIds: []` and `tackleAttemptedByIds: []` (verify by inspecting each match of `phase: 'CHOOSE_ACTION'`)
    - `pnpm test` passes across the monorepo; `pnpm typecheck` clean
  </acceptance_criteria>
  <done>No heading stat reads remain; HEADER ActionEvent uses aerialAbility fields; steal exclusion mirrors tackle exclusion; both flag arrays reset at every CHOOSE_ACTION transition</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 02)

**PlayerPiece (types.ts):** field `heading: number` REMOVED. `aerialAbility: number` is sole aerial stat.

**GameState (types.ts):** new optional fields `stealAttemptedByIds?: readonly string[]`,
`tackleAttemptedByIds?: readonly string[]`.

**HEADER ActionEvent (types.ts):** fields renamed `attackerHeading`→`attackerAerialAbility`,
`defenderHeading`→`defenderAerialAbility`.

**ActionEventType (types.ts):** new member `'FTP_REPOSITION'` (consumed by Plan 03/06 undo).

**moveValidator.ts:** steal path now filters ZoI defenders by `stealAttemptedByIds` (new behaviour).

**gameEngine.ts:** all `piece.heading` reads → `.aerialAbility`; flag-array reset added at every
CHOOSE_ACTION spread. No new function signatures.
</artifacts_produced>

<verification>
- `pnpm typecheck` clean monorepo-wide
- `pnpm test` green
- No `heading` field anywhere in PlayerPiece type, teams data, or engine stat reads
- ZoI steal exclusion verified to mirror tackle exclusion
- Both flag arrays reset at every CHOOSE_ACTION transition
</verification>

<success_criteria>

- D-01 complete: single aerial stat, values migrated, ActionEvent fields renamed
- D-02 complete: per-action-type ZoI exclusion + reset at every CHOOSE_ACTION
- Full suite green
  </success_criteria>

<output>
Create `.planning/phases/17.1-action-flow-cleanup/17.1-02-SUMMARY.md` when done
</output>
