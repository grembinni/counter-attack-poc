---
phase: 17.1-action-flow-cleanup
plan: 04
type: execute
wave: 4
depends_on:
  - 17.1-03
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.phase17.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - 'A loose-ball scatter that would leave the pitch stops at the last valid pitch hex and routes to 'PASS' (Move only)'
    - 'A regular shot whose goal hex is more than 11 hexes from the shooter is rejected with INVALID_TARGET'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'board-edge clamp walk in LOOSE_BALL branch; range gate in applyDeclareShot'
      contains: 'isPitchHex'
  key_links:
    - from: 'packages/server/src/gameEngine.ts'
      to: 'packages/shared/src/pitch.ts'
      via: 'isPitchHex used to clamp scatter path; hexDistance used for range gate'
      pattern: 'isPitchHex'
---

<objective>
Implement two mechanics fixes (D-08, D-09) in gameEngine.ts: board-edge clamping for loose-ball
scatter so the ball never leaves the pitch, and a range gate on regular shot target selection so a
goal hex more than 11 hexes from the shooter is rejected.

Purpose: D-08 closes a gap where computeLooseBall deliberately ignores boundaries and could place the
ball off-pitch. D-09 enforces the regular-shot 11-hex range (snapshot's 6-hex range is already enforced).

Output: A clamp walk in the applyRoll LOOSE_BALL branch and a distance guard in applyDeclareShot, with
engine tests covering both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md
@.planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Board-edge clamping for loose-ball scatter (D-08) + regular shot range gate (D-09)</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (applyRoll LOOSE_BALL branch after computeLooseBall; applyDeclareShot lines ~2681-2710; snapshot 6-hex gate ~line 2663 for reference)
    - packages/shared/src/scoreUtils.ts (computeLooseBall + LOOSE_BALL_DIRECTIONS direction-delta mapping)
    - packages/shared/src/pitch.ts (isPitchHex)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (D-08 clamp walk sketch; D-09 range gate sketch)
  </read_first>
  <action>
    (A) D-08: In the applyRoll LOOSE_BALL branch, after computing the raw scatter landing via
    computeLooseBall, add a clamp walk: read the direction delta from LOOSE_BALL_DIRECTIONS for the
    rolled direction, step one hex at a time up to `distance` steps, keep the last coord for which
    `isPitchHex` returns true, and break at the first off-pitch step. Use the clamped coord as the
    landing hex; treat it as an empty landing → 'PASS' (Move only). Add the comment
    `// pending out-of-bounds rules — ball stopped at board edge for now`. Ensure the 'PASS'
    spread here also carries the D-02 flag-array resets (consistency with Plan 02).
    (B) D-09: In applyDeclareShot for the regular shot, immediately before returning the GK_DIVE
    transition, add `if (!shooter || hexDistance(shooter.position, goalHex) > 11) return { ok: false, reason: 'INVALID_TARGET' };`.
    Do NOT alter the snapshot 6-hex gate (already enforced ~line 2663).
  </action>
  <verify>
    <automated>cd packages/server && pnpm typecheck && cd ../.. && pnpm test -- gameEngine</automated>
  </verify>
  <acceptance_criteria>
    - applyRoll LOOSE_BALL branch references `isPitchHex` and `LOOSE_BALL_DIRECTIONS` in a step-and-clamp loop
    - The clamped landing routes to `phase: ''PASS''` and includes `stealAttemptedByIds: []`/`tackleAttemptedByIds: []`
    - The clamp branch contains the `pending out-of-bounds rules — ball stopped at board edge` comment
    - applyDeclareShot contains `hexDistance(shooter.position, goalHex) > 11` returning `INVALID_TARGET`
    - `pnpm typecheck` clean; `pnpm test` for gameEngine passes
  </acceptance_criteria>
  <done>Loose-ball scatter clamps to last pitch hex; regular shot beyond 11 hexes rejected with INVALID_TARGET</done>
</task>

<task type="auto">
  <name>Task 2: Engine tests for board-edge clamp and shot range gate</name>
  <files>packages/server/src/__tests__/gameEngine.phase17.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.phase17.test.ts (existing seed helpers; LOOSE_BALL and shot fixtures)
    - packages/server/src/gameEngine.ts (the LOOSE_BALL clamp + applyDeclareShot gate from Task 1)
    - packages/shared/src/pitch.ts (board bounds q∈[0,36], r∈[0,25] for choosing off-pitch scatter seeds)
  </read_first>
  <action>
    Add two test cases. (1) D-08: seed a LOOSE_BALL state where the scatter direction+distance would
    carry the ball past a board edge; inject deterministic dice so the direction points off-pitch;
    assert the resulting ball position satisfies isPitchHex and equals the last valid hex along the path,
    and that phase === ''PASS''. (2) D-09: seed a regular shot where the goal hex is >11 hexes
    from the shooter; assert applyDeclareShot returns `{ ok: false, reason: 'INVALID_TARGET' }`; add a
    positive case at distance ≤11 asserting the GK_DIVE transition still occurs. Use real squad positions.
  </action>
  <verify>
    <automated>pnpm test -- gameEngine.phase17</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains a D-08 case asserting clamped ball is on-pitch (isPitchHex true) and phase 'PASS'
    - Test file contains a D-09 negative case (distance >11 → INVALID_TARGET) and a positive case (≤11 → GK_DIVE)
    - `pnpm test -- gameEngine.phase17` passes including the new cases
  </acceptance_criteria>
  <done>Clamp and range-gate behaviour proven by passing tests including boundary and positive cases</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 04)

**gameEngine.ts:**

- applyRoll LOOSE_BALL branch: clamp walk using `isPitchHex` + `LOOSE_BALL_DIRECTIONS`; off-pitch
  scatter now stops at last valid hex → 'PASS'.
- applyDeclareShot: new `hexDistance(...) > 11 → INVALID_TARGET` guard for regular shots.

No type changes, no new function signatures. Snapshot 6-hex gate unchanged.
</artifacts_produced>

<verification>
- `pnpm typecheck` clean
- `pnpm test` green including new D-08/D-09 cases
</verification>

<success_criteria>

- D-08 clamp: ball never lands off-pitch
- D-09 gate: regular shot beyond 11 hexes rejected; ≤11 still transitions to GK_DIVE
- Full suite green
  </success_criteria>

<output>
Create `.planning/phases/17.1-action-flow-cleanup/17.1-04-SUMMARY.md` when done
</output>
