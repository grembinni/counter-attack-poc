---
phase: 17.1-action-flow-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 17.1-02
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/gameEngine.phase17.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - 'After a first-time pass target is selected, both teams reposition one player up to 1 hex, then the ball is delivered to passTargetHex and the phase becomes CHOOSE_ACTION with lastActionType FIRST_TIME_PASS'
    - 'When ATTACKER_2 End Turn fires with the GK carrying the ball in its own penalty area, the phase becomes GK_RESTART instead of CHOOSE_ACTION'
    - 'A GK save spill (saveOutcome LOOSE_BALL) routes to GK_RESTART with the GK holding the ball, not to LOOSE_BALL'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'FIRST_TIME_PASS_MOVE handler, GK_RESTART end-turn trigger, spill→GK_RESTART'
      contains: 'FIRST_TIME_PASS_MOVE'
  key_links:
    - from: 'packages/server/src/gameEngine.ts'
      to: 'packages/server/src/gameHandlers.ts'
      via: 'FIRST_TIME_PASS_MOVE added to validUndoPhases + DICE_PHASES'
      pattern: 'FIRST_TIME_PASS_MOVE'
---

<objective>
Implement three engine phase-flow changes that depend on the Wave-1 rename being in place:
D-03 (the FIRST_TIME_PASS_MOVE repositioning phase end-to-end), D-06 (GK_RESTART trigger when the
GK carries the ball in its own penalty area at end of MOVE phase), and D-07 (GK save spill routes
to GK_RESTART instead of LOOSE_BALL).

Purpose: D-03 delivers the first-time pass mid-flight repositioning rule. D-06 and D-07 are new
triggers into the existing GK_RESTART phase, correcting restart routing.

Output: A working FIRST_TIME_PASS_MOVE two-slot alternating handler with FTP_REPOSITION undo support;
applyEndTurn GK_RESTART branch; applyRoll spill→GK_RESTART arm; engine phase-17 tests covering all three.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md
@.planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIRST_TIME_PASS_MOVE handler (D-03) — copy HIGH_PASS_MOVE two-slot pattern</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (HIGH_PASS_MOVE handler block ~lines 530-639; TODO stub ~line 1230; firstTimePassStep / firstTimePassPath / passTargetHex GameState fields)
    - packages/server/src/gameHandlers.ts (DICE_PHASES set ~line 75; validUndoPhases ~line 885)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (D-03 FIRST_TIME_PASS_MOVE sketch)
  </read_first>
  <action>
    Expand the TODO stub at gameEngine.ts:~1230 into a full FIRST_TIME_PASS_MOVE handler by copying the
    HIGH_PASS_MOVE handler's two-slot alternating structure. Active team alternates ATTACKER slot
    (attackingTeam) then DEFENDER slot (defending team); each may move one player ≤1 hex. Track repositioning
    with fields paralleling the high-pass equivalents (firstTimePassMovementSlot, firstTimePassMovedPieceId,
    firstTimePassPaceUsed — add to GameState/types if not already present, reusing the HIGH_PASS field shape).
    On the ATTACKER→DEFENDER slot boundary, append an `FTP_REPOSITION` ActionEvent (mirrors HP_REPOSITION).
    After BOTH End Turns complete: deliver the ball to `passTargetHex` (set ball.position and ball.carrierId
    to the receiving piece per HIGH_PASS pattern), set `phase: 'CHOOSE_ACTION'`, `lastActionType: 'FIRST_TIME_PASS'`,
    and include the D-02 resets `stealAttemptedByIds: []` / `tackleAttemptedByIds: []`. Ball does NOT move during
    repositioning. No interception check on delivery (per D-03). In gameHandlers.ts add `'FIRST_TIME_PASS_MOVE'`
    to validUndoPhases (line ~885) and to DICE_PHASES if the post-repositioning step triggers a roll
    (it does not move the ball with a roll here — add only if the existing flow requires it; otherwise leave DICE_PHASES).
  </action>
  <verify>
    <automated>cd packages/server && pnpm typecheck && cd ../.. && pnpm test -- gameEngine.phase17</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts contains a FIRST_TIME_PASS_MOVE handler block (no remaining `TODO: FIRST_TIME_PLAYER_MOVES` stub)
    - The handler appends an `FTP_REPOSITION` ActionEvent at the slot boundary
    - On completion the handler sets `phase: 'CHOOSE_ACTION'`, `lastActionType: 'FIRST_TIME_PASS'`, and delivers ball to passTargetHex
    - gameHandlers.ts validUndoPhases array includes `'FIRST_TIME_PASS_MOVE'`
    - `pnpm typecheck` clean; gameEngine.phase17 tests for the FTP flow pass
  </acceptance_criteria>
  <done>FIRST_TIME_PASS_MOVE alternates both team slots, supports FTP_REPOSITION undo, and delivers the ball to CHOOSE_ACTION without interception</done>
</task>

<task type="auto">
  <name>Task 2: GK_RESTART end-turn trigger (D-06) + GK save spill route (D-07)</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (applyEndTurn: nextSlot===null check ~line 666, CHOOSE_ACTION/HALF_TIME/FULL_TIME branch ~line 680; applyRoll SHOT branch saveOutcome==='SAVE' arm lines 1476-1490 and saveOutcome==='LOOSE_BALL' arm lines 1493-1501)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (D-06 GK_RESTART trigger sketch; D-07 spill→GK_RESTART sketch)
    - .planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md (D-06/D-07 decisions; isInRegion + PITCH_REGIONS usage)
  </read_first>
  <action>
    (A) D-06: In applyEndTurn, after the `nextSlot === null` computation and BEFORE the
    CHOOSE_ACTION/HALF_TIME/FULL_TIME branch, insert a guard: resolve the ball carrier; if
    `carrier?.role === 'GK'` and the carrier is in its own penalty area (home → PITCH_REGIONS.homePenaltyArea,
    away → PITCH_REGIONS.awayPenaltyArea via `isInRegion`), return state with `phase: 'GK_RESTART'`,
    `activeTeam: carrier.teamId`, `movementSlot: null`, cleared `movedPieceIds`/`paceUsedByPieceId`, and the
    D-02 resets `stealAttemptedByIds: []`/`tackleAttemptedByIds: []`, appending the slot-advance event.
    (B) D-07: In the applyRoll SHOT branch, change the `saveOutcome === 'LOOSE_BALL'` arm (lines ~1493-1501)
    to mirror the `saveOutcome === 'SAVE'` arm (1476-1490): set `phase: 'GK_RESTART'`, `ball.carrierId = gk.id`,
    `activeTeam = gk.teamId`. Add the comment `// pending out-of-bounds rules — spill treated as clean catch for now`.
  </action>
  <verify>
    <automated>cd packages/server && pnpm typecheck && cd ../.. && pnpm test -- gameEngine</automated>
  </verify>
  <acceptance_criteria>
    - applyEndTurn contains a GK-carrier own-penalty-area guard returning `phase: 'GK_RESTART'` before the CHOOSE_ACTION branch
    - The D-06 guard uses `isInRegion` with `PITCH_REGIONS.homePenaltyArea`/`awayPenaltyArea`
    - applyRoll SHOT `saveOutcome === 'LOOSE_BALL'` arm sets `phase: 'GK_RESTART'` and `ball.carrierId` to the GK id (no longer LOOSE_BALL)
    - The spill arm contains the `pending out-of-bounds rules` comment
    - `pnpm test` for gameEngine passes; typecheck clean
  </acceptance_criteria>
  <done>GK carrying in own area at end of MOVE → GK_RESTART; GK save spill → GK_RESTART with GK holding ball</done>
</task>

<task type="auto">
  <name>Task 3: Engine tests for FIRST_TIME_PASS_MOVE, GK_RESTART trigger, and spill route</name>
  <files>packages/server/src/__tests__/gameEngine.phase17.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.phase17.test.ts (existing phase-17 test fixtures/seed helpers; HIGH_PASS_MOVE test pattern if present)
    - packages/server/src/gameEngine.ts (the three handlers modified in Tasks 1-2)
    - .planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md (D-03/D-06/D-07 expected outcomes)
  </read_first>
  <action>
    Add three test cases. (1) D-03: seed a FIRST_TIME_PASS_MOVE state with a passTargetHex; run both
    team End Turns through the handler; assert final phase === 'CHOOSE_ACTION', lastActionType === 'FIRST_TIME_PASS',
    ball delivered to passTargetHex, and that an FTP_REPOSITION event was logged at the slot boundary.
    (2) D-06: seed an ATTACKER_2 end-of-MOVE state with the GK as ball carrier positioned in its own
    penalty area; assert applyEndTurn returns phase === 'GK_RESTART' with activeTeam === GK's team; add a
    negative case where the GK is OUTSIDE its penalty area asserting phase === 'CHOOSE_ACTION'. (3) D-07:
    seed a SHOT state whose roll yields saveOutcome 'LOOSE_BALL'; assert applyRoll returns phase === 'GK_RESTART'
    and ball.carrierId === gk.id. Reuse existing seed helpers and real HOME_SQUAD/AWAY_SQUAD positions
    (per STATE.md note: placeholder coords break adjacency silently).
  </action>
  <verify>
    <automated>pnpm test -- gameEngine.phase17</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains a FIRST_TIME_PASS_MOVE case asserting CHOOSE_ACTION + FIRST_TIME_PASS + ball at passTargetHex
    - Test file contains a D-06 positive case (GK in own area → GK_RESTART) AND a negative case (GK outside area → CHOOSE_ACTION)
    - Test file contains a D-07 case asserting spill → GK_RESTART with GK holding the ball
    - `pnpm test -- gameEngine.phase17` passes including the three new cases
  </acceptance_criteria>
  <done>All three phase-flow changes covered by passing engine tests including the D-06 negative case</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 03)

**gameEngine.ts — new/changed handler logic:**

- FIRST_TIME_PASS_MOVE handler (replaces TODO stub ~line 1230): two-slot alternating reposition →
  delivers ball to passTargetHex → CHOOSE_ACTION, lastActionType FIRST_TIME_PASS.
- applyEndTurn: new GK-carrier own-penalty-area branch → GK_RESTART (D-06).
- applyRoll SHOT: saveOutcome 'LOOSE_BALL' arm now routes to GK_RESTART (D-07).

**New ActionEvent emitted:** `FTP_REPOSITION` at the FTP slot boundary (type member added in Plan 02).

**GameState fields (if newly added):** firstTimePassMovementSlot / firstTimePassMovedPieceId /
firstTimePassPaceUsed (mirroring HIGH_PASS reposition fields).

**gameHandlers.ts:** `'FIRST_TIME_PASS_MOVE'` added to validUndoPhases.

No function signatures change (handlers keep the apply\* shape).
</artifacts_produced>

<verification>
- `pnpm typecheck` clean
- `pnpm test` green including new phase-17 cases
- FIRST_TIME_PASS_MOVE delivers without interception; D-06 and D-07 route to GK_RESTART
</verification>

<success_criteria>

- D-03, D-06, D-07 implemented and covered by passing tests
- FTP_REPOSITION undo boundary wired in handler
- Full suite green
  </success_criteria>

<output>
Create `.planning/phases/17.1-action-flow-cleanup/17.1-03-SUMMARY.md` when done
</output>
