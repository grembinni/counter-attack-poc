---
phase: 17.1-action-flow-cleanup
plan: 05
type: execute
wave: 5
depends_on:
  - 17.1-04
files_modified:
  - packages/shared/src/passValidator.ts
  - packages/shared/src/passValidator.test.ts
  - packages/server/src/gameEngine.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - 'A STANDARD pass whose destination hex is occupied by a defender is allowed and auto-intercepted by that defender (no roll)'
    - 'A STANDARD pass whose path passes THROUGH an intermediate defender still returns PATH_BLOCKED'
    - 'ZoI defenders adjacent to the path are returned as rollIntercepts and resolved with the die===6 || combined>=10 threshold'
    - 'applyRoll PASS processes autoIntercepts first (immediate interception), then rollIntercepts'
  artifacts:
    - path: 'packages/shared/src/passValidator.ts'
      provides: 'PassResult with autoIntercepts/rollIntercepts; case-1 destination-defender auto-intercept'
      contains: 'autoIntercepts'
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applyRoll PASS loop consuming autoIntercepts then rollIntercepts'
      contains: 'autoIntercepts'
  key_links:
    - from: 'packages/server/src/gameEngine.ts'
      to: 'packages/shared/src/passValidator.ts'
      via: 'applyRoll PASS interception loop reads passResult.autoIntercepts / rollIntercepts'
      pattern: 'autoIntercepts'
---

<objective>
Implement the pass-interception return-shape change (D-10). Split the single `interceptors` list into
`autoIntercepts` (case 1: destination hex IS a defender's hex → immediate interception, no roll) and
`rollIntercepts` (case 3: ZoI defenders adjacent to the path → die===6 || combined>=10). Preserve
PATH_BLOCKED for case 2 (intermediate on-path defender). Update the applyRoll PASS loop to consume the
new shape and add a passValidator test for case 1 without breaking existing PATH_BLOCKED coverage.

Purpose: Previously a pass to an occupied hex was rejected via PATH_BLOCKED; the corrected rule allows
it as a direct pass that the occupying defender auto-intercepts.

Output: New PassResult union; case-1 auto-intercept logic; applyRoll PASS loop ordering auto-then-roll;
a new case-1 test plus retained case-2 PATH_BLOCKED tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md
@.planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: passValidator return-shape change + case-1 auto-intercept (D-10)</name>
  <files>packages/shared/src/passValidator.ts</files>
  <read_first>
    - packages/shared/src/passValidator.ts (PassResult type lines ~27-30; STANDARD path-block guard lines 76-82; ZoI interception loop ~line 113+; success returns ~lines 126-130)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (passValidator full D-10 sketch: new PassResult union, destDefender, autoIntercepts/rollIntercepts construction, return statements)
  </read_first>
  <action>
    Change the PassResult union so the `ok: true` variants carry `autoIntercepts: PlayerPiece[]` and
    `rollIntercepts: PlayerPiece[]` instead of a single `interceptors` list (keep the FIRST_TIME variant's
    `effect: { type: 'FIRST_TIME_PLAYER_MOVES' }`). Keep the `ok: false` reasons including PATH_BLOCKED.
    Keep the existing case-2 intermediate-blocking guard (lines 76-82) unchanged — intermediate on-path
    defenders still return PATH_BLOCKED. Add case-1 detection: for STANDARD passes, find a defender whose
    `position` exactly equals the destination `to` (`p.position.q === to.q && p.position.r === to.r`); if
    found, push it to `autoIntercepts` (this is NEW — destination-occupied passes are now allowed). Build
    `rollIntercepts` from getZoIDefenders along the travel path and at the destination, excluding any id
    already in autoIntercepts (per PATTERNS.md sketch; skip for LONG/HIGH pass types). Return
    `{ ok: true, autoIntercepts, rollIntercepts }` (and the FIRST_TIME variant adds the effect).
  </action>
  <verify>
    <automated>cd packages/shared && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PassResult `ok: true` variants expose `autoIntercepts` and `rollIntercepts`; no `interceptors` field remains
    - The case-2 intermediate-block guard still returns `{ ok: false, reason: 'PATH_BLOCKED' }`
    - For a STANDARD pass to a defender-occupied destination, that defender appears in `autoIntercepts`
    - `pnpm typecheck` for packages/shared shows errors only at the gameEngine consumer (fixed in Task 2), not within passValidator.ts
  </acceptance_criteria>
  <done>PassResult split into autoIntercepts/rollIntercepts; case-1 allowed + auto-intercepted; case-2 PATH_BLOCKED preserved</done>
</task>

<task type="auto">
  <name>Task 2: applyRoll PASS loop consumes autoIntercepts then rollIntercepts (D-10)</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (applyRoll PASS branch interception loop; existing die===6 || combined>=10 threshold ~line 1106)
    - packages/shared/src/passValidator.ts (new PassResult shape from Task 1)
    - .planning/phases/17.1-action-flow-cleanup/17.1-PATTERNS.md (applyRoll PASS loop sketch)
  </read_first>
  <action>
    Update the applyRoll PASS branch to read the new PassResult shape. Process `passResult.autoIntercepts`
    FIRST: if non-empty, the first auto-intercepting defender takes the ball immediately with no dice —
    set ball.position/carrierId to that defender, transition to CHOOSE_ACTION (with D-02 flag resets), and
    return. If there are no auto-intercepts, iterate `passResult.rollIntercepts` with the existing
    `die === 6 || combined >= 10` threshold (unchanged at ~line 1106). Preserve all current downstream
    behaviour for the no-interception delivery path.
  </action>
  <verify>
    <automated>cd packages/server && pnpm typecheck && cd ../.. && pnpm test -- gameEngine</automated>
  </verify>
  <acceptance_criteria>
    - applyRoll PASS branch references `passResult.autoIntercepts` and `passResult.rollIntercepts`
    - autoIntercepts are processed before rollIntercepts; an auto-intercept transfers possession without a die roll
    - The rollIntercepts threshold remains `die === 6 || combined >= 10`
    - `pnpm typecheck` clean; `pnpm test` for gameEngine passes
  </acceptance_criteria>
  <done>applyRoll PASS handles auto-then-roll interception ordering against the new PassResult shape</done>
</task>

<task type="auto">
  <name>Task 3: passValidator tests — add case-1, keep case-2 PATH_BLOCKED</name>
  <files>packages/shared/src/passValidator.test.ts</files>
  <read_first>
    - packages/shared/src/passValidator.test.ts (existing PATH_BLOCKED tests; existing interceptor assertions to migrate to autoIntercepts/rollIntercepts)
    - packages/shared/src/passValidator.ts (final shape from Task 1)
    - .planning/phases/17.1-action-flow-cleanup/17.1-CONTEXT.md (D-10 three-case definitions)
  </read_first>
  <action>
    Migrate existing assertions that read `result.interceptors` to the new `autoIntercepts`/`rollIntercepts`
    fields. Add a NEW case-1 test: a STANDARD pass whose destination hex is occupied by a defender →
    `ok: true` with that defender in `autoIntercepts` and not in `rollIntercepts`. KEEP the existing case-2
    tests (intermediate on-path defender → `ok: false, reason: 'PATH_BLOCKED'`) unchanged in intent — only
    adjust field names where they previously inspected `interceptors`. Add/confirm a case-3 test where a
    ZoI-adjacent (not on-path) defender appears in `rollIntercepts`.
  </action>
  <verify>
    <automated>pnpm test -- passValidator</automated>
  </verify>
  <acceptance_criteria>
    - A case-1 test asserts the destination defender is in `autoIntercepts` for a STANDARD pass
    - Case-2 PATH_BLOCKED tests still present and passing
    - A case-3 test asserts a ZoI-adjacent defender appears in `rollIntercepts`
    - No remaining reference to `result.interceptors` in the test file
    - `pnpm test -- passValidator` passes
  </acceptance_criteria>
  <done>Case 1 covered, case 2 PATH_BLOCKED retained, case 3 rollIntercepts covered; suite green</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (Plan 05)

**passValidator.ts — PassResult union change:**

- `ok: true` variants now expose `autoIntercepts: PlayerPiece[]` and `rollIntercepts: PlayerPiece[]`
  (replacing single `interceptors`). FIRST_TIME variant retains `effect: { type: 'FIRST_TIME_PLAYER_MOVES' }`.
- New behaviour: STANDARD pass to a defender-occupied destination is allowed and yields that defender in
  autoIntercepts (case 1). PATH_BLOCKED retained for intermediate on-path defenders (case 2).

**gameEngine.ts:** applyRoll PASS branch now consumes autoIntercepts (no-roll) before rollIntercepts
(threshold roll). No new function signatures.
</artifacts_produced>

<verification>
- `pnpm typecheck` clean
- `pnpm test` green including new case-1 and retained case-2 tests
</verification>

<success_criteria>

- D-10 complete: case 1 auto-intercept, case 2 PATH_BLOCKED preserved, case 3 roll-intercept
- applyRoll consumes the new shape with auto-then-roll ordering
- Full suite green
  </success_criteria>

<output>
Create `.planning/phases/17.1-action-flow-cleanup/17.1-05-SUMMARY.md` when done
</output>
