---
phase: 17.1-action-flow-cleanup
plan: '06'
subsystem: ui
tags: [react, actionpanel, first-time-pass, ftp-reposition, canundo]

requires:
  - phase: 17.1-action-flow-cleanup
    provides: 'FTP_REPOSITION ActionEventType + FIRST_TIME_PASS_MOVE in GamePhase union (plans 02 + 03)'

provides:
  - 'ActionPanel FIRST_TIME_PASS_MOVE panel branch (Undo + End Turn, helper text)'
  - 'canUndo FTP_REPOSITION boundary clause'
  - '4 new ActionPanel tests for FIRST_TIME_PASS_MOVE behaviour'

affects: [17.1-action-flow-cleanup]

tech-stack:
  added: []
  patterns:
    - 'ActionPanel phase block: copy HIGH_PASS_MOVE structure for new phase panels'
    - 'canUndo boundary: one OR clause per phase/event pair'

key-files:
  created: []
  modified:
    - packages/client/src/components/ActionPanel.tsx
    - packages/client/src/components/ActionPanel.test.tsx

key-decisions:
  - 'FIRST_TIME_PASS_MOVE panel placed immediately after HIGH_PASS_MOVE block (before isActivePlayer guard)'
  - 'BallMarker.tsx + HexCell.tsx already committed in 17.1-01; no separate commit needed for Task 3'

patterns-established:
  - 'Each new repositioning phase gets its own panel block before the isActivePlayer guard'

requirements-completed: []

duration: 4min
completed: 2026-06-15
---

# Phase 17.1 Plan 06: ActionPanel FIRST_TIME_PASS_MOVE Panel Summary

**FIRST_TIME_PASS_MOVE repositioning panel added to ActionPanel with FTP_REPOSITION canUndo boundary; 4 new tests; BallMarker + HexCell visual changes already committed in plan 01**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-15T21:12:05Z
- **Completed:** 2026-06-15T21:16:03Z
- **Tasks:** 3 (2 with new commits; Task 3 was a no-op — files already committed)
- **Files modified:** 2 (ActionPanel.tsx, ActionPanel.test.tsx)

## Accomplishments

- Added `FIRST_TIME_PASS_MOVE` panel branch to ActionPanel mirroring the `HIGH_PASS_MOVE` block: shows "First-time pass! Move 1 player to receive the ball." helper text with Undo (gated on canUndo) and End Turn buttons for the active player; returns `waitingPanel` for the non-active player
- Extended the shared `canUndo` boundary expression to include `phase === 'FIRST_TIME_PASS_MOVE' && evt.type === 'FTP_REPOSITION'` — exact mirror of the existing `HIGH_PASS_MOVE` + `HP_REPOSITION` clause
- Added 4 new ActionPanel test cases covering: active player panel renders, Undo disabled without FTP_REPOSITION boundary, Undo enabled with FTP_REPOSITION in log, non-active player waiting panel

## Task Commits

Each task was committed atomically:

1. **Task 1: ActionPanel FIRST_TIME_PASS_MOVE panel + FTP_REPOSITION canUndo boundary** - `f468039` (feat)
2. **Task 2: ActionPanel test for FIRST_TIME_PASS_MOVE panel** - `349608d` (test)
3. **Task 3: Finalise uncommitted BallMarker + HexCell visual changes** - already in `5a3a288` (17.1-01 commit)

**Plan metadata:** (see docs commit below)

## Files Created/Modified

- `packages/client/src/components/ActionPanel.tsx` — FIRST_TIME_PASS_MOVE panel block + FTP_REPOSITION canUndo clause
- `packages/client/src/components/ActionPanel.test.tsx` — 4 new FIRST_TIME_PASS_MOVE panel tests

## Decisions Made

- Placed FIRST_TIME_PASS_MOVE panel block immediately after HIGH_PASS_MOVE block (before the isActivePlayer guard) so the non-active player correctly receives the `waitingPanel` return — consistent with the established pattern for two-team repositioning phases
- Task 3 required no commit: BallMarker.tsx and HexCell.tsx were captured by the pre-commit hook's lint-staged stash mechanism during the 17.1-01 commit and are already in the repository with the correct visual changes

## Deviations from Plan

### Auto-fixed Issues

None.

### Other Notes

**Task 3 — files already committed:** The plan described BallMarker.tsx and HexCell.tsx as "uncommitted local changes." These were captured in commit `5a3a288` (17.1-01, wave 1) via the lint-staged stash restore mechanism. Running `git diff HEAD` on both files showed no diff. Typecheck and HexCell tests both pass, confirming the changes are present and correct. No additional commit was needed; all Task 3 acceptance criteria are satisfied.

---

**Total deviations:** 0 auto-fixed.
**Impact on plan:** No deviations from intended logic. Task 3 non-commit is expected (work already landed).

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 17.1 wave 3 (plan 06) complete — all 6 plans of Phase 17.1 now have SUMMARY files
- Client suite: 95/95 tests passing
- `pnpm typecheck` clean for packages/client
- FIRST_TIME_PASS_MOVE has full server-side handler (plan 03), shared types (plan 02), and now client UI (plan 06)

## Self-Check

- `packages/client/src/components/ActionPanel.tsx` exists: FOUND
- `packages/client/src/components/ActionPanel.test.tsx` exists: FOUND
- Commit `f468039` exists: FOUND (feat(17.1-06): ActionPanel FIRST_TIME_PASS_MOVE panel)
- Commit `349608d` exists: FOUND (test(17.1-06): ActionPanel tests for FIRST_TIME_PASS_MOVE panel)
- All 95 client tests pass: PASS
- `pnpm typecheck` for packages/client: PASS

## Self-Check: PASSED

---

_Phase: 17.1-action-flow-cleanup_
_Completed: 2026-06-15_
