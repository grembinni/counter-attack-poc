---
phase: 13-layout-clock
plan: 02
type: execute
wave: 1
depends_on: ['13-01']
files_modified:
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/GameBoard.module.css
  - packages/client/src/components/ConnectionStatus.module.css
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, CLOCK-01, CLOCK-02]
must_haves:
  truths:
    - 'A persistent top band renders in every phase with home score left, away score right, and a centre clock'
    - 'The clock displays MM:00 derived from actionCount with no PLAY_PHASES gating'
    - 'The action section swaps ActionPanel / KickOffSetupPanel / ReplayPanel by phase'
    - 'The log section is collapsed by default and expands on chevron click'
    - 'A compact 2x3 player card shows the last-selected piece and never blanks after first selection'
    - 'HALF_TIME and FULL_TIME render as overlays over the pitch with the top band still visible'
  artifacts:
    - path: 'packages/client/src/components/GameBoard.tsx'
      provides: 'Top-band layout host with clock, scoreboard, compact card, action section, log toggle, phase overlays'
      contains: 'topBand'
    - path: 'packages/client/src/components/GameBoard.module.css'
      provides: 'Top band CSS grid + overlay + compact card styles'
      contains: '.topBand'
    - path: 'packages/client/src/components/ConnectionStatus.module.css'
      provides: '8px connection dot per UI-SPEC'
      contains: '8px'
  key_links:
    - from: 'GameBoard.tsx'
      to: 'useGameStore.gameState.actionCount'
      via: 'clock derivation'
      pattern: 'actionCount'
    - from: 'GameBoard.tsx'
      to: 'emitHalfTimeStart'
      via: 'Start 2nd Half button in HALF_TIME overlay'
      pattern: 'emitHalfTimeStart'
    - from: 'GameBoard.tsx pitchContainer'
      to: 'phase overlay'
      via: 'position: relative ancestor + absolute overlay'
      pattern: 'position: relative'
---

<objective>
Rewrite GameBoard into a top-band layout: a single 80px CSS-grid strip spanning full width with [home score | centre section | compact player card | action section | log toggle | away score], and the hex grid filling the area below. Reformat the match clock to MM:00 (event-driven, always visible). Convert HALF_TIME and FULL_TIME into overlay cards rendered over the pitch. Absorb TurnIndicator logic (PHASE_LABEL, SLOT_TOTAL, moves-remaining) into the centre section. Fix the ConnectionStatus dot to 8px.

This delivers D-01 through D-11, D-13, and satisfies LAYOUT-01, LAYOUT-02, CLOCK-01, CLOCK-02. The Wave 0 GameBoard.test.tsx assertions go from RED to GREEN.

Purpose: The pitch gains full width; all game info (score, clock, phase, action buttons, log) stays visible in every phase including HALF_TIME/FULL_TIME/REPLAY.
Output: Rewritten GameBoard.tsx + GameBoard.module.css; ConnectionStatus.module.css dot resized.

Note: This plan does NOT delete TurnIndicator/HalfTimeScreen/FullTimeScreen or change App.tsx routing — those happen in Plan 03 (Wave 2), after the overlays are proven working, to avoid orphaning a phase. GameBoard.tsx here STOPS importing TurnIndicator/HalfTimeScreen/FullTimeScreen/PlayerStatsPanel (their content is inlined or dropped), but the files remain on disk until Plan 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-layout-clock/13-CONTEXT.md
@.planning/phases/13-layout-clock/13-PATTERNS.md
@.planning/phases/13-layout-clock/13-UI-SPEC.md
@.planning/phases/13-layout-clock/13-RESEARCH.md
</context>

<artifacts_this_phase_produces>
NEW symbols created by this plan (exclude from drift verification):

- GameBoard.tsx: `COMPACT_STATS` array, `PHASE_LABEL` const (relocated from TurnIndicator), `SLOT_TOTAL` const (relocated from TurnIndicator), `clockDisplay` value, `logExpanded`/`setLogExpanded` state, `lastPieceRef` ref, `displayPiece` value, inline HALF_TIME/FULL_TIME overlay JSX, inline centre-section JSX, inline compact-player-card JSX
- GameBoard.module.css new classes: `.topBand`, `.topBandSection`, `.scoreColumn`, `.scoreTeamName`, `.scoreNumeral`, `.centerSection`, `.clockDisplay`, `.connectionLine`, `.phaseSummary`, `.teamName`, `.phaseLabel`, `.movesRemaining`, `.playerCard`, `.playerCardHeader`, `.compactStatsGrid`, `.compactStat`, `.compactStatLabel`, `.compactStatValue`, `.playerCardPlaceholder`, `.actionSection`, `.logCollapsed`, `.logExpanded`, `.logChevron`, `.logHeader`, `.overlay`, `.overlayCard`, `.overlayHeading`, `.overlayBody`, `.overlayScoreRow`, `.overlayScore`, `.overlayTeamLabel`, `.overlayCtaButton`, `.overlayResultLine`
- ConnectionStatus.module.css: `.dot` width/height changed to 8px (modification, not new)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite GameBoard.tsx with top band, MM:00 clock, compact card, centre section, action swap, log toggle, and phase overlays</name>
  <files>packages/client/src/components/GameBoard.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (current 85-line implementation — the file being rewritten; note imports, selectors, phase-swap block lines 70-80, DisconnectBanner placement line 62)
    - packages/client/src/components/TurnIndicator.tsx (source of PHASE_LABEL lines 6-27, SLOT_TOTAL lines 30-34, teamName/teamColor lines 47-48, remaining computation lines 53-56 — copy these verbatim into GameBoard)
    - packages/client/src/components/HalfTimeScreen.tsx (source of HALF_TIME overlay content: canStart logic lines 16-19, secondHalfKickOffTeam/color lines 22-24, card JSX lines 26-68, emitHalfTimeStart usage — inline this into the overlay)
    - packages/client/src/components/FullTimeScreen.tsx (source of FULL_TIME overlay content: resultText/resultColor lines 13-16, card JSX lines 18-46 — inline this into the overlay)
    - packages/client/src/components/PlayerStatsPanel.tsx (compact-card stat rendering pattern: selectedPieceId + pieces selectors, piece lookup, stat label/value rows)
    - packages/shared/src/types.ts (lines 3-27 — confirm PlayerPiece fields: pace, shooting, tackling, dribbling, heading, saving, handling, resilience, aerialAbility, highPass, name, role. There is NO passing or stamina field.)
    - .planning/phases/13-layout-clock/13-UI-SPEC.md (Layout Contract, Typography, Color, Copywriting Contract, States table — exact dimensions, colors, and copy strings)
  </read_first>
  <action>
    Rewrite GameBoard.tsx as a single layout host. Imports: keep useGameStore, HexGrid, ActionLog, ConnectionStatus, DisconnectBanner, ActionPanel, KickOffSetupPanel, ReplayPanel, styles. Add `import { useRef, useState } from 'react'` and `import type { PlayerPiece } from '@counter-attack/shared'`. REMOVE imports of TurnIndicator, HalfTimeScreen, FullTimeScreen, and PlayerStatsPanel (their content is inlined or dropped; the files stay on disk until Plan 03).

    DELETE the PLAY_PHASES Set entirely (current lines 13-27). The clock is now always rendered (CLOCK-02) — no phase gating.

    Copy verbatim from TurnIndicator.tsx into GameBoard.tsx module scope: the `PHASE_LABEL: Record<GamePhase, string>` table and the `SLOT_TOTAL: Record<MovementSlot, number>` table. Add the corresponding `import type { GamePhase, MovementSlot } from '@counter-attack/shared'`.

    Add a module-scope `COMPACT_STATS` array of exactly six [field, label] pairs using ONLY confirmed PlayerPiece fields (passing/stamina DO NOT EXIST): ['pace','PAC'], ['shooting','SHT'], ['tackling','TAC'], ['heading','HED'], ['dribbling','DRB'], ['highPass','HPS']. Type it as Array<[keyof PlayerPiece, string]>.

    Selectors (per-slice, Zustand): score, phase, actionCount from gameState; plus activeTeam, movementSlot, paceUsedByPieceId (centre section); selectedPieceId, pieces (player card); playerSlot, and gameState.kickOffTeam, gameState.addedTime, and emitHalfTimeStart (HALF_TIME overlay). Drop the addedTime usage in the header clock (no longer needed there).

    Clock: `const clockDisplay = String(actionCount).padStart(2, '0') + ':00'` (D-08/D-09). Render it always — no PLAY_PHASES check.

    Centre section derived values (from TurnIndicator): teamName = activeTeam === 'home' ? 'HOME TEAM' : 'AWAY TEAM'; teamColor = activeTeam === 'home' ? '#1a56b0' : '#c0392b'; phaseLabel = PHASE_LABEL[phase]; remaining = phase === 'MOVEMENT' && movementSlot != null ? SLOT_TOTAL[movementSlot] - Object.keys(paceUsedByPieceId).length : null.

    Persistent player card (D-03, never blank after first selection): const lastPieceRef = useRef<PlayerPiece | null>(null); const currentPiece = selectedPieceId ? (pieces.find((p) => p.id === selectedPieceId) ?? null) : null; if (currentPiece) lastPieceRef.current = currentPiece; const displayPiece = lastPieceRef.current. Render the 2x3 grid mapping over COMPACT_STATS (label from the pair, value from displayPiece[field]) plus a header row with displayPiece.name and displayPiece.role. When displayPiece is null, render the placeholder text "Select a piece".

    Log toggle (D-05, local state): const [logExpanded, setLogExpanded] = useState(false). Collapsed: a 32px section with a single chevron button showing '›' (expand). Expanded: a 240px section showing a "MATCH LOG" header with a '‹' collapse button, then <ActionLog />. Use the className styles.logCollapsed vs styles.logExpanded. Per 13-RESEARCH.md Pitfall 4, render the "MATCH LOG" heading in the LogToggle wrapper; ActionLog keeps its own internal header (acceptable) — do not modify ActionLog.

    Action section phase swap (preserve existing logic): phase === 'KICK_OFF_SETUP' ? <KickOffSetupPanel /> : phase === 'REPLAY' ? <ReplayPanel /> : <ActionPanel />.

    Top band JSX: a div className={styles.topBand} containing in order — home ScoreColumn (team name "Home" + score.home numeral, color #1a56b0), centre section (clock line / ConnectionStatus line / phase-summary line with teamName colored + phaseLabel + moves-remaining when present), compact player card, action section (the phase swap), log toggle, away ScoreColumn (team name "Away" + score.away numeral, color #c0392b).

    Below the top band: render <DisconnectBanner /> (preserve placement — now between top band and pitch), then a main pitch container div className={styles.pitchContainer} containing <HexGrid /> and, conditionally, the phase overlay.

    Phase overlay (D-13): when phase === 'HALF_TIME' || phase === 'FULL_TIME', render a div className={styles.overlay} wrapping div className={styles.overlayCard}. For HALF_TIME, inline the HalfTimeScreen card content: heading "Half Time", body "End of 1st Half", score row, added-time note (only when addedTime !== null && addedTime > 0), 2nd-half kick-off assignment line, and the "Start 2nd Half" button. The button calls emitHalfTimeStart() and is disabled unless canStart, where myTeam = playerSlot === 1 ? 'home' : playerSlot === 2 ? 'away' : null and canStart = myTeam !== null && myTeam !== kickOffTeam (D-28 logic preserved verbatim). For FULL_TIME, inline the FullTimeScreen content: heading "Full Time", score row, result line (resultText = score.home > score.away ? 'Home wins' : score.away > score.home ? 'Away wins' : 'Draw'; resultColor #1a56b0 / #c0392b / #e0e0e0), and body "Replay starting…". No FULL_TIME button.

    Apply UI-SPEC colors/typography via the CSS classes created in Task 2 (clock #f5c518, dim text #a0a0a0, etc.). Inline team colors where the value is dynamic (teamColor, score numeral color, result color). Do NOT place fenced code blocks in this file's comments; ordinary TSX is expected. Remove the old "Counter Attack" title text and the old timeLabel logic entirely.

  </action>
  <verify>
    <automated>cd packages/client && pnpm vitest run src/components/GameBoard.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - GameBoard.tsx does NOT contain "PLAY_PHASES"
    - GameBoard.tsx does NOT import TurnIndicator, HalfTimeScreen, FullTimeScreen, or PlayerStatsPanel
    - GameBoard.tsx contains "padStart(2, '0')" and the literal ":00" for the clock
    - GameBoard.tsx contains "COMPACT_STATS" and the field tokens 'dribbling' and 'highPass' (NOT 'passing' or 'stamina')
    - GameBoard.tsx does NOT contain the strings "passing" or "stamina"
    - GameBoard.tsx contains "emitHalfTimeStart" and "Start 2nd Half"
    - GameBoard.tsx contains "Select a piece" (player card placeholder) and "MATCH LOG" (log header)
    - GameBoard.tsx contains the phase-swap ternary referencing KickOffSetupPanel, ReplayPanel, and ActionPanel
    - GameBoard.tsx contains "useRef" and "lastPieceRef" (persistent card)
    - `cd packages/client && pnpm vitest run src/components/GameBoard.test.tsx` exits 0 (all Wave 0 assertions pass)
  </acceptance_criteria>
  <done>
    GameBoard.tsx renders the full top band, MM:00 clock, compact card, phase-aware action section, collapsible log, and HALF_TIME/FULL_TIME overlays. GameBoard.test.tsx passes. TurnIndicator/HalfTimeScreen/FullTimeScreen/PlayerStatsPanel are no longer imported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite GameBoard.module.css for the top band grid, overlays, and compact card; fix ConnectionStatus dot to 8px</name>
  <files>packages/client/src/components/GameBoard.module.css, packages/client/src/components/ConnectionStatus.module.css</files>
  <read_first>
    - packages/client/src/components/GameBoard.module.css (current 67-line file — note .gameBoard flex column, .header, .gameLayout, .pitchContainer, .sidebar to be replaced)
    - packages/client/src/components/HalfTimeScreen.module.css (source for overlay card styling: .card max-width/background/border/padding/gap; .heading, .body, .scoreRow, .score, .teamLabel, .ctaButton — merge equivalents into GameBoard.module.css overlay classes)
    - packages/client/src/components/ConnectionStatus.module.css (current .dot is 10px — change to 8px)
    - .planning/phases/13-layout-clock/13-PATTERNS.md (GameBoard.module.css section lines 171-321 — concrete class definitions for .topBand, .topBandSection, .scoreColumn, .clockDisplay, .phaseLabel, .teamName, .compactStatsGrid, .compactStat, .logCollapsed, .logExpanded, .pitchContainer with position:relative, .overlay, .overlayCard)
    - .planning/phases/13-layout-clock/13-UI-SPEC.md (Spacing Scale, Typography, Color, Layout Contract — exact px values: top band 80px, score columns 56px, log collapsed 32px / expanded 240px, overlay card max-width 440px)
  </read_first>
  <action>
    Rewrite GameBoard.module.css. Keep `.gameBoard { height: 100vh; display: flex; flex-direction: column; }`. DELETE the classes .header, .headerScore, .homeTeam, .awayTeam, .headerTime, .sidebar, and .gameLayout (the old sidebar layout is gone).

    Add `.topBand { display: grid; grid-template-columns: 56px 1fr 1fr 1fr auto 56px; height: 80px; flex-shrink: 0; background: #1a1a2e; border-bottom: 1px solid #0f3460; }`. The six grid tracks map to: home score (56px), centre section (1fr), player card (1fr), action section (1fr), log toggle (auto), away score (56px).

    Add `.topBandSection` (background #16213e, border-right 1px solid #0f3460, flex column, align/justify center, padding 0 8px) for centre/card/action sections. Add `.scoreColumn` (flex column center, font-family 'Courier New', score numeral 20px/700, team name 11px/400 #a0a0a0, background #16213e, border-right #0f3460). The away score column should NOT have border-right (it is the last track) — give it a variant class or override.

    Add centre-section classes: `.clockDisplay` (font-size 20px, font-weight 700, color #f5c518), `.connectionLine` (compact, holds ConnectionStatus), `.phaseSummary` row, `.teamName` (13px/700, color applied inline), `.phaseLabel` (11px/700 #a0a0a0 uppercase letter-spacing 0.05em), `.movesRemaining` (13px/400 #a0a0a0).

    Add player-card classes: `.playerCard` (background #16213e, border 1px solid #0f3460, border-radius 4px, padding 8px), `.playerCardHeader` (name 13px/700 #e0e0e0 + role 11px/400 #a0a0a0 uppercase), `.compactStatsGrid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 4px 8px; }`, `.compactStat` (flex, gap 4px, 11px), `.compactStatLabel` (#a0a0a0), `.compactStatValue` (font-weight 700, color #f5c518), `.playerCardPlaceholder` (11px/400 #a0a0a0).

    Add action-section class `.actionSection` (background #16213e, border 1px solid #0f3460, border-radius 4px, padding 8px).

    Add log-toggle classes: `.logCollapsed { width: 32px; display: flex; align-items: center; justify-content: center; background: #16213e; border-left: 1px solid #0f3460; }`, `.logExpanded { width: 240px; display: flex; flex-direction: column; background: #16213e; border-left: 1px solid #0f3460; overflow: hidden; }`, `.logChevron` (13px/700 #a0a0a0, button reset: background none, border none, cursor pointer), `.logHeader` (11px/700 #a0a0a0 uppercase, flex row space-between).

    Replace `.pitchContainer` with: `flex: 1; background: #0a0a0a; padding: 16px; overflow: hidden; display: flex; align-items: center; justify-content: center; position: relative;` — the `position: relative` is MANDATORY (Pitfall 2) so the overlay anchors to the pitch and not the viewport.

    Add overlay classes: `.overlay { position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; background: rgba(0,0,0,0.6); z-index: 10; }`. Add `.overlayCard` matching HalfTimeScreen.module.css .card: width 100%, max-width 440px, background #16213e, border 1px solid #0f3460, border-radius 4px, padding 24px, flex column, gap 16px. Add `.overlayHeading` (20px/700 #e0e0e0), `.overlayBody` (13px/400 #a0a0a0, line-height 1.5), `.overlayScoreRow` (flex row, gap, align center), `.overlayScore` (28px/700 #e0e0e0 'Courier New'), `.overlayTeamLabel` (13px/700, color inline), `.overlayResultLine` (13px/700, color inline), and `.overlayCtaButton` matching the existing .ctaButton (background #0f3460, color #fff, border none, border-radius 4px, padding 10px 20px, 13px/700, cursor pointer; :hover:not(:disabled) background #1a56b0; :disabled opacity 0.5 cursor default).

    Then edit ConnectionStatus.module.css: change `.dot` width and height from 10px to 8px (Pitfall 6 / UI-SPEC). Leave border-radius and flex-shrink unchanged.

  </action>
  <verify>
    <automated>cd packages/client && pnpm vitest run src/components/GameBoard.test.tsx 2>&1 | tail -10 && pnpm --filter @counter-attack/client build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - GameBoard.module.css contains ".topBand" with "grid-template-columns: 56px 1fr 1fr 1fr auto 56px"
    - GameBoard.module.css contains ".pitchContainer" and that rule contains "position: relative"
    - GameBoard.module.css contains ".overlay" with "position: absolute" and "inset: 0" and "z-index: 10"
    - GameBoard.module.css contains ".overlayCard" with "max-width: 440px"
    - GameBoard.module.css contains ".compactStatsGrid" with "repeat(2, 1fr)"
    - GameBoard.module.css contains ".clockDisplay" with "#f5c518"
    - GameBoard.module.css does NOT contain ".sidebar" or ".header {"
    - ConnectionStatus.module.css .dot contains "width: 8px" and "height: 8px" and does NOT contain "10px"
    - `pnpm --filter @counter-attack/client build` exits 0 (no TypeScript or CSS-module reference errors)
    - `cd packages/client && pnpm vitest run src/components/GameBoard.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    GameBoard.module.css implements the 80px top-band grid, 8px-anchored phase overlay, compact stats grid, and dark-theme tokens; old header/sidebar classes are removed; pitchContainer is position:relative. ConnectionStatus dot is 8px. Build and GameBoard tests are green.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                        |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | Client-only render layer reading server-authoritative Zustand state. No new input field, no new network call, no new server event. |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                                                                                                         |
| --------- | ---------------------- | -------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-13-02   | Tampering              | GameBoard layout / clock display | accept      | Clock and all top-band values are derived read-only from server-broadcast GameState (server-authoritative). No client writes back. Per 13-RESEARCH.md Security Domain (V5 Input Validation: no user input to validate). |
| T-13-03   | Information Disclosure | HALF_TIME/FULL_TIME overlay      | accept      | Overlay shows only score/phase already visible to both players; no PII, no secrets.                                                                                                                                     |

No high-severity threats. No package installs (no Package Legitimacy Gate required).
</threat_model>

<verification>
- `cd packages/client && pnpm vitest run src/components/GameBoard.test.tsx` exits 0 (LAYOUT-01/02, CLOCK-01/02 green).
- `pnpm --filter @counter-attack/client build` exits 0.
- `pnpm --filter @counter-attack/client test --run` — full client suite still green (no regression in existing component tests).
- Manual (per VALIDATION.md): launch dev, join a room in two tabs, confirm top band visible with scoreboard + clock + action section in every phase; reach HALF_TIME and confirm the overlay sits over the pitch with the clock still visible above it; click the log chevron to expand/collapse.
</verification>

<success_criteria>

- The top band renders in every phase with scores at the edges and a centre MM:00 clock (LAYOUT-01, CLOCK-01, CLOCK-02).
- The action section swaps panels by phase and the log toggles (LAYOUT-02).
- HALF_TIME/FULL_TIME render as overlays over the pitch with the top band visible (D-13, CLOCK-02).
- Compact card persists last selection; ConnectionStatus dot is 8px.
  </success_criteria>

<output>
Create `.planning/phases/13-layout-clock/13-02-SUMMARY.md` when done.
</output>
