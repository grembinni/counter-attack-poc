---
phase: 13-layout-clock
plan: 03
type: execute
wave: 2
depends_on: ['13-02']
files_modified:
  - packages/client/src/App.tsx
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/components/TurnIndicator.tsx
  - packages/client/src/components/TurnIndicator.module.css
  - packages/client/src/components/HalfTimeScreen.tsx
  - packages/client/src/components/HalfTimeScreen.module.css
  - packages/client/src/components/FullTimeScreen.tsx
  - packages/client/src/components/FullTimeScreen.module.css
autonomous: true
requirements: [LAYOUT-01, CLOCK-02]
must_haves:
  truths:
    - 'HALF_TIME and FULL_TIME phases route to GameBoard (no separate screen) so the top band and clock stay visible'
    - 'App.tsx no longer imports or renders HalfTimeScreen or FullTimeScreen'
    - "The Screen type no longer contains 'HALF_TIME' or 'FULL_TIME'"
    - 'TurnIndicator, HalfTimeScreen, and FullTimeScreen component files are deleted'
    - 'The full client test suite passes'
  artifacts:
    - path: 'packages/client/src/App.tsx'
      provides: 'Simplified routing: all game phases render GameBoard'
      contains: 'GAME_BOARD'
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'Screen type without HALF_TIME/FULL_TIME'
      contains: 'REPLAY'
  key_links:
    - from: 'App.tsx onGameState'
      to: 'setScreen'
      via: 'HALF_TIME/FULL_TIME fall through to GAME_BOARD'
      pattern: 'setScreen'
---

<objective>
Finalise the Phase 13 routing simplification (D-12): remove the HALF_TIME and FULL_TIME branches from App.tsx so those phases render GameBoard (with their overlays from Plan 02), remove 'HALF_TIME'/'FULL_TIME' from the store Screen type, and delete the now-unused TurnIndicator, HalfTimeScreen, and FullTimeScreen component + CSS files.

This depends on Plan 02 (Wave 1): the GameBoard overlays must exist first, otherwise HALF_TIME/FULL_TIME would fall through to a GameBoard that renders no half/full-time UI. Running this after Plan 02 guarantees no orphaned phase.

Purpose: Eliminate the screen-level fork so the persistent top band + clock are present in every phase (LAYOUT-01, CLOCK-02), and remove dead code.
Output: App.tsx routing simplified; Screen type trimmed; three component pairs deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-layout-clock/13-CONTEXT.md
@.planning/phases/13-layout-clock/13-PATTERNS.md
@.planning/phases/13-layout-clock/13-RESEARCH.md
</context>

<artifacts_this_phase_produces>
NEW/CHANGED symbols (exclude from drift verification):

- App.tsx: onGameState routing simplified (HALF_TIME/FULL_TIME branches removed); render tree collapsed to `screen === 'GAME_BOARD' || screen === 'REPLAY' ? <GameBoard /> : <LobbyScreen />`; HalfTimeScreen/FullTimeScreen imports removed
- useGameStore.ts: `Screen` union type loses 'HALF_TIME' and 'FULL_TIME' members
- Deleted files: TurnIndicator.tsx, TurnIndicator.module.css, HalfTimeScreen.tsx, HalfTimeScreen.module.css, FullTimeScreen.tsx, FullTimeScreen.module.css
- Preserved: `emitHalfTimeStart` action in useGameStore.ts (still called by GameBoard overlay — do NOT delete)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Simplify App.tsx routing and trim the Screen type</name>
  <files>packages/client/src/App.tsx, packages/client/src/store/useGameStore.ts</files>
  <read_first>
    - packages/client/src/App.tsx (current — onGameState routing lines 22-38, render tree lines 86-101, HalfTimeScreen/FullTimeScreen imports lines 5-6)
    - packages/client/src/store/useGameStore.ts (Screen type lines 22-30; confirm emitHalfTimeStart action exists and is NOT removed)
    - .planning/phases/13-layout-clock/13-PATTERNS.md (App.tsx and useGameStore.ts edit sections lines 325-409 — exact before/after for routing and Screen type)
    - .planning/phases/13-layout-clock/13-RESEARCH.md (Pitfall 1 — Screen enum out-of-sync; Open Question 2 — REPLAY value retained)
  </read_first>
  <action>
    In App.tsx onGameState (currently lines 27-37): remove the `if (state.phase === 'HALF_TIME') setScreen('HALF_TIME')` and `else if (state.phase === 'FULL_TIME') setScreen('FULL_TIME')` branches. Keep the REPLAY branch (`if (state.phase === 'REPLAY') setScreen('REPLAY')`) and the else branch that sets GAME_BOARD when not already there. Net result: HALF_TIME and FULL_TIME fall through to the else branch and set screen to GAME_BOARD (so GameBoard renders with its Plan 02 overlays).

    In App.tsx render tree (currently lines 86-101): replace the multi-branch ternary with `{screen === 'GAME_BOARD' || screen === 'REPLAY' ? <GameBoard /> : <LobbyScreen />}`. Remove the `import { HalfTimeScreen } from './components/HalfTimeScreen.js'` and `import { FullTimeScreen } from './components/FullTimeScreen.js'` lines (current lines 5-6).

    In useGameStore.ts Screen type (lines 22-30): remove the `| 'HALF_TIME'` and `| 'FULL_TIME'` members. Keep 'REPLAY' (Open Question 2 / Key Fact 2 — App.tsx still branches on it; retaining it avoids a breaking grep). Leave the `emitHalfTimeStart` action fully intact — it is still invoked by the GameBoard HALF_TIME overlay (Key Fact 3). Before removing the two Screen members, grep the client package for `'HALF_TIME'` and `'FULL_TIME'` as Screen values to confirm App.tsx and useGameStore.ts are the only consumers (RESEARCH.md Assumption A2 / Pitfall 1); GameBoard.tsx references `phase === 'HALF_TIME'` (a GamePhase, not a Screen) which is unaffected and must remain.

    Do not place fenced code blocks in source comments. Ordinary TS/TSX edits only.

  </action>
  <verify>
    <automated>cd packages/client && pnpm --filter @counter-attack/client build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx does NOT import HalfTimeScreen or FullTimeScreen
    - App.tsx does NOT contain `setScreen('HALF_TIME')` or `setScreen('FULL_TIME')`
    - App.tsx render tree contains "screen === 'GAME_BOARD' || screen === 'REPLAY'"
    - useGameStore.ts Screen type does NOT contain a Screen member "'HALF_TIME'" or "'FULL_TIME'" (the GamePhase usage elsewhere is unaffected)
    - useGameStore.ts still contains "emitHalfTimeStart"
    - `pnpm --filter @counter-attack/client build` exits 0
  </acceptance_criteria>
  <done>
    App.tsx routes all game phases (including HALF_TIME/FULL_TIME) to GameBoard, no longer imports the retired screens, and the Screen type is trimmed. emitHalfTimeStart preserved. Build is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete retired TurnIndicator, HalfTimeScreen, and FullTimeScreen component and CSS files</name>
  <files>packages/client/src/components/TurnIndicator.tsx, packages/client/src/components/TurnIndicator.module.css, packages/client/src/components/HalfTimeScreen.tsx, packages/client/src/components/HalfTimeScreen.module.css, packages/client/src/components/FullTimeScreen.tsx, packages/client/src/components/FullTimeScreen.module.css</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (CONFIRM Plan 02 already inlined PHASE_LABEL, SLOT_TOTAL, the HALF_TIME overlay with emitHalfTimeStart + Start 2nd Half, and the FULL_TIME overlay — these must exist before deletion)
    - packages/client/src/App.tsx (CONFIRM Task 1 removed the HalfTimeScreen/FullTimeScreen imports)
  </read_first>
  <action>
    First grep the entire client package for any remaining importers of TurnIndicator, HalfTimeScreen, or FullTimeScreen (search for the import path tokens './TurnIndicator', './HalfTimeScreen', './FullTimeScreen', and the bare component names in import statements). The only expected former importers are GameBoard.tsx (TurnIndicator/HalfTimeScreen/FullTimeScreen — removed in Plan 02) and App.tsx (HalfTimeScreen/FullTimeScreen — removed in Task 1). If any OTHER importer is found, STOP and report it; do not delete.

    If no remaining importers exist, delete all six files: TurnIndicator.tsx, TurnIndicator.module.css, HalfTimeScreen.tsx, HalfTimeScreen.module.css, FullTimeScreen.tsx, FullTimeScreen.module.css. Use the platform delete (PowerShell `Remove-Item` or `git rm`). Do NOT delete TurnIndicator.test or any test file unless one exists specifically for these components — grep first; there are no known test files for these three components.

    After deletion, run the full client test suite and the build to confirm nothing references the deleted modules.

  </action>
  <verify>
    <automated>cd packages/client && pnpm --filter @counter-attack/client build 2>&1 | tail -10 && pnpm --filter @counter-attack/client test --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Files TurnIndicator.tsx, TurnIndicator.module.css, HalfTimeScreen.tsx, HalfTimeScreen.module.css, FullTimeScreen.tsx, FullTimeScreen.module.css no longer exist in packages/client/src/components/
    - No remaining file in packages/client/src imports './TurnIndicator', './HalfTimeScreen', or './FullTimeScreen'
    - `pnpm --filter @counter-attack/client build` exits 0
    - `pnpm --filter @counter-attack/client test --run` exits 0 (full client suite green, including GameBoard.test.tsx)
  </acceptance_criteria>
  <done>
    The three retired component/CSS pairs are deleted with no dangling imports. Build and full client test suite are green.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                 |
| ---------- | ------------------------------------------------------------------------------------------- |
| (none new) | Routing simplification and dead-code deletion only. No new input, network, or server event. |

## STRIDE Threat Register

| Threat ID | Category          | Component       | Disposition | Mitigation Plan                                                                                                                                                                                                                  |
| --------- | ----------------- | --------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-13-04   | Denial of Service | App.tsx routing | accept      | Removing screen branches cannot introduce a render loop — the else branch is guarded by `if (s !== 'GAME_BOARD')`. Per 13-RESEARCH.md Pitfall 1, the Screen type and routing are changed together to avoid a blank-screen state. |

No high-severity threats. No package installs (no Package Legitimacy Gate required).
</threat_model>

<verification>
- `pnpm --filter @counter-attack/client build` exits 0 (no references to deleted modules, Screen type consistent with App.tsx).
- `pnpm --filter @counter-attack/client test --run` exits 0 (full suite green).
- Manual (per VALIDATION.md): reach HALF_TIME in a 2-player session and confirm the half-time overlay shows over the pitch with the top band + clock visible (proves routing fall-through works); reach FULL_TIME and confirm the full-time overlay + clock.
</verification>

<success_criteria>

- HALF_TIME and FULL_TIME render GameBoard with overlays and a visible clock (LAYOUT-01, CLOCK-02 finalised).
- Screen type trimmed; App.tsx routing simplified; retired files deleted with no dangling imports.
- emitHalfTimeStart preserved and functional via the GameBoard overlay.
  </success_criteria>

<output>
Create `.planning/phases/13-layout-clock/13-03-SUMMARY.md` when done.
</output>
