---
phase: 13-layout-clock
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/client/src/components/GameBoard.test.tsx
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, CLOCK-01, CLOCK-02]
must_haves:
  truths:
    - 'A GameBoard.test.tsx file exists and runs under Vitest'
    - 'Tests assert the clock renders MM:00 derived from actionCount'
    - 'Tests assert the clock renders in non-play phases (HALF_TIME, KICK_OFF_SETUP)'
    - 'Tests assert scoreboard shows home and away score'
    - 'Tests assert the action section swaps panel by phase and the log toggle exists'
  artifacts:
    - path: 'packages/client/src/components/GameBoard.test.tsx'
      provides: 'Wave 0 test scaffold for LAYOUT-01/02 and CLOCK-01/02'
      contains: 'describe('
  key_links:
    - from: 'GameBoard.test.tsx'
      to: 'useGameStore'
      via: 'useGameStore.setState in beforeEach'
      pattern: "useGameStore\\.setState"
---

<objective>
Create the Wave 0 test scaffold `GameBoard.test.tsx` that encodes the Phase 13 requirements (LAYOUT-01, LAYOUT-02, CLOCK-01, CLOCK-02) as executable assertions BEFORE the GameBoard rewrite happens. The VALIDATION.md contract mandates this file exist before implementation (`wave_0_complete: false` gate).

Purpose: Establish the Nyquist feedback signal so the Wave 1 rewrite has a failing-then-passing target. The clock format (`${actionCount}:00`), always-visible clock, scoreboard scores, phase-aware action section, and log toggle are all asserted here.
Output: `packages/client/src/components/GameBoard.test.tsx` covering all four phase requirements.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-layout-clock/13-CONTEXT.md
@.planning/phases/13-layout-clock/13-VALIDATION.md
@.planning/phases/13-layout-clock/13-UI-SPEC.md
@packages/client/src/components/PlayerStatsPanel.test.tsx
@packages/client/src/components/GameBoard.tsx
</context>

<artifacts_this_phase_produces>
Symbols created in Phase 13 (exclude from drift verification — these are NEW, not pre-existing):

- `packages/client/src/components/GameBoard.test.tsx` (new file)
- Inside GameBoard.tsx (created in Plan 02): `COMPACT_STATS` constant, `PHASE_LABEL` constant (moved from TurnIndicator), `SLOT_TOTAL` constant (moved from TurnIndicator), `clockDisplay` value, `logExpanded` state, `lastPieceRef` ref, inline PhaseOverlay markup
- New CSS classes in GameBoard.module.css (created in Plan 02): `.topBand`, `.topBandSection`, `.scoreColumn`, `.clockDisplay`, `.phaseLabel`, `.teamName`, `.compactStatsGrid`, `.compactStat`, `.compactStatLabel`, `.compactStatValue`, `.logCollapsed`, `.logExpanded`, `.overlay`, `.overlayCard`
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GameBoard.test.tsx Wave 0 scaffold covering LAYOUT-01/02 and CLOCK-01/02</name>
  <files>packages/client/src/components/GameBoard.test.tsx</files>
  <read_first>
    - packages/client/src/components/PlayerStatsPanel.test.tsx (MUST read — exact test harness pattern: vi.mock('../socket.js'), afterEach cleanup, beforeEach useGameStore.setState with gameState/screen/selectedPieceId/validMoveHexes/playerSlot/roomCode/disconnectWarning/roomError/gameError)
    - packages/client/src/components/GameBoard.tsx (current implementation — note the clock formula being replaced and the phase-swap pattern that must survive)
    - packages/client/src/mock/mockMovementState.ts (the GameState shape used as base; confirm it has score, phase, actionCount, activeTeam, movementSlot, paceUsedByPieceId fields)
    - .planning/phases/13-layout-clock/13-VALIDATION.md (Per-Task Verification Map — the four requirement assertions this file must cover)
    - .planning/phases/13-layout-clock/13-UI-SPEC.md (Copywriting Contract — exact strings: "MATCH LOG", "Select a piece", clock format)
  </read_first>
  <behavior>
    - Test (CLOCK-01): with gameState.actionCount=7, GameBoard renders text matching "7:00". With actionCount=45, renders "45:00". With actionCount=46, renders "46:00". With actionCount=0, renders "0:00" (or "00:00" — assert via regex /0?0:00/ to allow padStart variance; the implementation uses String(actionCount).padStart(2,'0') so 0 → "00:00").
    - Test (CLOCK-02): with gameState.phase='HALF_TIME', the clock text (regex /\d+:00/) is still present in the document. Repeat for phase='KICK_OFF_SETUP'. This proves no PLAY_PHASES gating.
    - Test (LAYOUT-01): with gameState.score={home:2,away:1}, both "2" and "1" appear in the rendered output (scoreboard shows home and away scores).
    - Test (LAYOUT-02): with phase='KICK_OFF_SETUP' the KickOffSetupPanel is reachable (assert a KickOffSetupPanel-specific text or testid is present); with phase='REPLAY' the ReplayPanel is reachable; with phase='MOVEMENT' the ActionPanel is reachable. Plus: a log toggle control exists (the '›' chevron button) by default.
  </behavior>
  <action>
    Create packages/client/src/components/GameBoard.test.tsx following the EXACT harness from PlayerStatsPanel.test.tsx: import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest'; import { render, screen, cleanup } from '@testing-library/react'; import { useGameStore } from '../store/useGameStore.js'; import { mockMovementState } from '../mock/index.js'; import { GameBoard } from './GameBoard.js'. Add vi.mock('../socket.js', () => ({ socket: { emit: vi.fn(), on: vi.fn(), off: vi.fn() } })). Add afterEach(() => cleanup()). In beforeEach, call useGameStore.setState with the SAME keys as PlayerStatsPanel.test.tsx (gameState: { ...mockMovementState }, screen: 'GAME_BOARD', selectedPieceId: null, validMoveHexes: [], playerSlot: 1, roomCode: 'ABC12', disconnectWarning: false, roomError: null, gameError: null).

    Write describe blocks: "GameBoard — CLOCK-01: MM:00 format", "GameBoard — CLOCK-02: clock visible in all phases", "GameBoard — LAYOUT-01: scoreboard scores", "GameBoard — LAYOUT-02: phase-aware action section and log toggle".

    Set per-test state by spreading mockMovementState and overriding the relevant field, e.g. useGameStore.setState({ gameState: { ...mockMovementState, actionCount: 45 } }) then render(<GameBoard />). Use screen.getByText with a regex (e.g. /45:00/) for clock assertions and screen.getAllByText for score digits.

    For the LAYOUT-02 panel-swap assertions, identify a stable text/label unique to each panel by reading KickOffSetupPanel.tsx, ReplayPanel.tsx, and ActionPanel.tsx headings; if a unique heading is unavailable, assert the panel's container via a known button label. For the log toggle, assert the '›' chevron button is in the document by default (collapsed state).

    Because GameBoard.tsx is rewritten in Plan 02, these tests are EXPECTED to fail or partially fail until Plan 02 lands — that is the intended RED state. Do NOT modify GameBoard.tsx in this plan. If the current GameBoard.tsx makes a test throw at import/render time, wrap the not-yet-implemented assertions so the file still parses and runs (the suite must execute, not error at collection). Prefer assertions that will naturally fail (not throw) — e.g. expect(()=>screen.getByText(/45:00/)).not.toThrow() is acceptable, but a plain getByText that throws is also acceptable as a RED signal as long as the test FILE loads.

    Do NOT use fenced code in production; this is a test file so normal test code is expected. No fenced blocks in this action prose.

  </action>
  <verify>
    <automated>cd packages/client && pnpm vitest run src/components/GameBoard.test.tsx 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - File packages/client/src/components/GameBoard.test.tsx exists
    - GameBoard.test.tsx contains the string "describe" at least 4 times (one per requirement group)
    - GameBoard.test.tsx contains "useGameStore.setState" (state seeding present)
    - GameBoard.test.tsx contains "mockMovementState"
    - GameBoard.test.tsx references the clock format via a regex or literal containing ":00"
    - GameBoard.test.tsx contains both "HALF_TIME" and "KICK_OFF_SETUP" (CLOCK-02 cross-phase coverage)
    - `cd packages/client && pnpm vitest run src/components/GameBoard.test.tsx` executes the suite (collection succeeds; tests may be RED) — the command exits without a collection/parse error
  </acceptance_criteria>
  <done>
    GameBoard.test.tsx exists, loads under Vitest, and contains executable assertions for clock format, cross-phase clock visibility, scoreboard scores, and phase-aware action section + log toggle. Tests may be red (GameBoard not yet rewritten) but the suite collects and runs.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                               |
| ---------- | ------------------------------------------------------------------------- |
| (none new) | This plan creates a test file only. No runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category  | Component          | Disposition | Mitigation Plan                                                                                                                                                                       |
| --------- | --------- | ------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-13-01   | Tampering | GameBoard.test.tsx | accept      | Test-only file; no production code path, no input handling, no network. Per 13-RESEARCH.md Security Domain, Phase 13 introduces no new attack surface (no input, no API, no secrets). |

No high-severity threats. No package installs in this plan (no Package Legitimacy Gate required).
</threat_model>

<verification>
- `cd packages/client && pnpm vitest run src/components/GameBoard.test.tsx` collects and runs the suite.
- The rest of the client suite is unaffected: `pnpm --filter @counter-attack/client test --run` still collects all existing test files.
</verification>

<success_criteria>

- GameBoard.test.tsx exists and encodes LAYOUT-01, LAYOUT-02, CLOCK-01, CLOCK-02 as Vitest assertions.
- The Wave 0 gate from VALIDATION.md is satisfied (test file present before implementation).
  </success_criteria>

<output>
Create `.planning/phases/13-layout-clock/13-01-SUMMARY.md` when done.
</output>
