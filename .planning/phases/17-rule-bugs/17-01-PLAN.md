---
phase: 17-rule-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/shared/src/actionSequence.ts
  - packages/server/src/__tests__/gameEngine.phase17.test.ts
  - packages/server/src/__tests__/gameHandlers.phase17.test.ts
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03, BUG-04, BUG-05, MOVE-06, PASS-02]
must_haves:
  truths:
    - 'The shared type system knows about the FREE_MOVE phase and the new GameState fields PASS-02 and MOVE-06 require'
    - 'The GAME_CANCEL_MOVEMENT socket event is typed in the shared events contract'
    - 'Failing Wave-0 test files exist for every Phase 17 engine and handler behavior not already covered'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'FREE_MOVE phase literal + firstTimePassPath/firstTimePassStep/freeMoveEligibleIds/freeMoveUsedPace fields'
      contains: "'FREE_MOVE'"
    - path: 'packages/shared/src/events.ts'
      provides: 'GAME_CANCEL_MOVEMENT client event'
      contains: 'GAME_CANCEL_MOVEMENT'
    - path: 'packages/server/src/__tests__/gameEngine.phase17.test.ts'
      provides: 'Unit test scaffolds for BUG-02/03/04/05, MOVE-06, PASS-02 engine layer'
    - path: 'packages/server/src/__tests__/gameHandlers.phase17.test.ts'
      provides: 'Integration test scaffolds for game:cancel_movement and SNAP_DEFLECT PASS-02 resolution'
  key_links:
    - from: 'packages/shared/src/events.ts ClientToServerEvents'
      to: 'ClientEvents.GAME_CANCEL_MOVEMENT'
      via: 'interface entry'
      pattern: 'GAME_CANCEL_MOVEMENT.*=> void'
---

<objective>
Establish the shared-type and test foundation for all seven Phase 17 fixes. This plan defines the contracts (new `GamePhase` literal, new `GameState` fields, new socket event) that downstream plans implement against, and creates the Wave-0 failing test files that downstream plans turn green.

Purpose: Interface-first ordering — every later plan consumes the `FREE_MOVE` phase, the four new `GameState` fields, and the `GAME_CANCEL_MOVEMENT` event. Defining them once here removes the scavenger-hunt for contracts and guarantees type-checks compile across packages.
Output: Extended `types.ts`, `events.ts`, `actionSequence.ts`; two new test files seeded with failing (RED) test cases for the behaviors not yet implemented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-rule-bugs/17-CONTEXT.md
@.planning/phases/17-rule-bugs/17-PATTERNS.md
@.planning/phases/17-rule-bugs/17-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols created across the whole of Phase 17 (defined here in Plan 01 unless noted):

- `GamePhase` member `'FREE_MOVE'` (types.ts)
- `GameState.firstTimePassPath?: readonly HexCoord[] | null` (types.ts)
- `GameState.firstTimePassStep?: 'ATTACKER' | null` (types.ts)
- `GameState.freeMoveEligibleIds?: readonly string[] | null` (types.ts)
- `GameState.freeMoveUsedPace?: Readonly<Record<string, number>> | null` (types.ts)
- `ClientEvents.GAME_CANCEL_MOVEMENT = 'game:cancel_movement'` (events.ts)
- `ClientToServerEvents[ClientEvents.GAME_CANCEL_MOVEMENT]: () => void` (events.ts)
- `applyCancelMovement` + `ApplyCancelMovementResult` (gameEngine.ts — Plan 03)
- `applyFreeMoveEnd` (gameEngine.ts — Plan 04)
- `emitCancelMovement` store action (client store — Plan 03)
- New test files: `gameEngine.phase17.test.ts`, `gameHandlers.phase17.test.ts` (this plan)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared GamePhase union and GameState fields</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts (read the full GamePhase union ~lines 277-297, the GameState movement fields ~lines 344-352, and the optional GameState fields ~lines 384-450 to match the exact JSDoc + `?` + `| null` convention)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (section "packages/shared/src/types.ts" — exact field definitions to copy)
  </read_first>
  <action>
    Add the string literal `'FREE_MOVE'` to the `GamePhase` union (place it after `'GK_KICK_MOVEMENT'` and before `'HALF_TIME'`), with a comment citing MOVE-06 (Phase 17).

    Add four new optional fields to the `GameState` interface, each `?`-optional with `| null`, each with a JSDoc comment citing the decision that introduced it. Use these exact names and types:
    - `firstTimePassPath?: readonly HexCoord[] | null` — PASS-02 (D-17): hex path from passer to target computed by hexLine; null outside the FIRST_TIME_PASS attacker sub-state.
    - `firstTimePassStep?: 'ATTACKER' | null` — PASS-02 (D-17): sub-step within First-time Pass flow; 'ATTACKER' = attacker may reposition 1 non-passer player ≤1 hex; null otherwise.
    - `freeMoveEligibleIds?: readonly string[] | null` — MOVE-06 (D-13): piece IDs eligible for the free 6-hex move; set on FREE_MOVE entry; null outside FREE_MOVE.
    - `freeMoveUsedPace?: Readonly<Record<string, number>> | null` — MOVE-06 (D-13): cumulative hexes used per piece during FREE_MOVE (max 6 each); null outside FREE_MOVE.

    Confirm `HexCoord` is already imported/declared in this file (it is used throughout); do not add a duplicate import.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains the literal `'FREE_MOVE'` inside the GamePhase union
    - types.ts contains `firstTimePassPath`, `firstTimePassStep`, `freeMoveEligibleIds`, `freeMoveUsedPace` field declarations, each with `| null` in its type
    - `pnpm --filter @counter-attack/shared exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The shared package compiles with FREE_MOVE in the phase union and all four new optional GameState fields declared with JSDoc.</done>
</task>

<task type="auto">
  <name>Task 2: Add GAME_CANCEL_MOVEMENT event to shared events contract</name>
  <files>packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/events.ts (read the `ClientEvents` const object ~lines 8-52 and the `ClientToServerEvents` interface ~lines 70-122 to match exact key/value style, including how GAME_UNDO is declared)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (section "packages/shared/src/events.ts" — exact entries to copy)
  </read_first>
  <action>
    In the `ClientEvents` const object, add a new entry directly after `GAME_UNDO`:
    `GAME_CANCEL_MOVEMENT: 'game:cancel_movement'` with a JSDoc comment "Phase 17 BUG-02: cancels MOVEMENT phase before any piece has moved."

    In the `ClientToServerEvents` interface, add a zero-argument entry directly after the `[ClientEvents.GAME_UNDO]` entry:
    `[ClientEvents.GAME_CANCEL_MOVEMENT]: () => void;` with a JSDoc comment "Phase 17 BUG-02: revert MOVEMENT phase → PASS. Guard: paceUsedByPieceId must be empty."

    Do NOT add a new ServerToClient event — BUG-02 reuses the existing GAME_ERROR + broadcastState flow.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - events.ts `ClientEvents` contains `GAME_CANCEL_MOVEMENT: 'game:cancel_movement'`
    - events.ts `ClientToServerEvents` contains `[ClientEvents.GAME_CANCEL_MOVEMENT]: () => void`
    - `pnpm --filter @counter-attack/shared exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The shared events contract types the new cancel-movement client event with a zero-arg signature.</done>
</task>

<task type="auto">
  <name>Task 3: Seed Wave-0 failing test files for engine and handler fixes</name>
  <files>packages/server/src/__tests__/gameEngine.phase17.test.ts, packages/server/src/__tests__/gameHandlers.phase17.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.test.ts (read the full file to reuse its GameState fixture builders, import style, and the existing PASS-branch interception fixtures around lines 760-930 — reuse `interceptionPassState` and the helper patterns)
    - packages/server/src/__tests__/gameHandlers.test.ts (read the socket-mock + room setup harness to mirror it for the new integration cases)
    - packages/server/src/__tests__/gameHandlers.phase10.test.ts (read for the SNAP_DEFLECT end-turn integration test pattern)
    - .planning/phases/17-rule-bugs/17-VALIDATION.md (the Per-Task Verification Map — one test case per row that is marked ❌ W0)
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Code Examples section for the expected post-fix behavior of each function)
  </read_first>
  <action>
    Create `gameEngine.phase17.test.ts` with `describe` blocks and `it` cases (initially RED) for every engine behavior in the VALIDATION map not already covered by the existing suite. Reuse the fixture builders and `interceptionPassState`-style state objects from `gameEngine.test.ts` — import the same helpers; do not duplicate squad/position literals. Required cases:
    - BUG-02: `applyCancelMovement` returns `{ok:true}` with phase 'PASS' when `paceUsedByPieceId` is empty; returns `{ok:false, reason:'PIECES_ALREADY_MOVED'}` when a piece has partial pace; returns `{ok:false, reason:'WRONG_PHASE'}` when phase !== 'MOVEMENT'.
    - BUG-03: `applyUndo` succeeds (reverses last MOVE) when `state.phase === 'HIGH_PASS_MOVEMENT'` with an HP_REPOSITION boundary in the eventLog; under-undo guard: returns locked/no-op when no MOVE after the last boundary.
    - BUG-04: pass landing on a hex occupied by a defender → `ball.carrierId` is the defender's id AND `attackingTeam`/`activeTeam` flip to the defender's team; pass landing on a hex occupied by a teammate → `ball.carrierId` is the teammate's id, possession unchanged.
    - BUG-05: SHOT save with `handling.caught === false` → resulting LOOSE_BALL `ball.position` equals the GK's effective position, not the shot origin.
    - MOVE-06: `applyEndTurn` from ATTACKER_2→null with `pendingFreeMove` set and ≥1 outfield piece in the opponent's third → result phase 'FREE_MOVE' with `freeMoveEligibleIds` non-empty and `pendingFreeMove` null; with zero eligible pieces → result phase 'PASS' and `pendingFreeMove` null.
    - PASS-02: `applyRoll` PASS branch with `lastActionType === 'FIRST_TIME_PASS'` and an accurate roll → result stays phase 'PASS' with `firstTimePassStep === 'ATTACKER'` and `firstTimePassPath` populated.

    Create `gameHandlers.phase17.test.ts` with integration `it` cases:
    - BUG-02: emitting `game:cancel_movement` in MOVEMENT with no moves reverts the room's gameState to phase 'PASS'; emitting it with a moved piece emits GAME_ERROR and leaves phase 'MOVEMENT'.
    - PASS-02: SNAP_DEFLECT end-turn with `lastActionType === 'FIRST_TIME_PASS'` resolves as a pass (ball delivered to target / LOOSE_BALL at deflector) rather than a shot duel.

    For functions/fields not yet implemented (`applyCancelMovement`, FREE_MOVE branch, PASS-02 branch), write the test against the intended final signature so it fails RED now and goes GREEN when the downstream plan lands. Do NOT stub the production functions in this plan. If a referenced export does not yet exist, import it with a `// Wave 0 RED — implemented in plan NN` comment so the file still parses (use a type-only import or an `@ts-expect-error` on the missing import as needed so `tsc` does not hard-fail the other packages' builds — the test command, not tsc, surfaces the RED state).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run gameEngine.phase17 gameHandlers.phase17 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/server/src/__tests__/gameEngine.phase17.test.ts` exists and is discovered by vitest
    - File `packages/server/src/__tests__/gameHandlers.phase17.test.ts` exists and is discovered by vitest
    - Running the two files reports failing (RED) cases for every not-yet-implemented behavior — i.e. the run reports test failures, not "no tests found" and not all-green
    - gameEngine.phase17.test.ts contains `it` cases referencing applyCancelMovement, applyUndo (HIGH_PASS_MOVEMENT), BUG-04 possession transfer, BUG-05 GK position, FREE_MOVE transition, and PASS-02 attacker step
  </acceptance_criteria>
  <done>Two Wave-0 test files exist and fail RED for all unimplemented Phase 17 behaviors, reusing existing fixtures.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @counter-attack/shared exec tsc --noEmit` exits 0 (shared types compile)
- `pnpm --filter @counter-attack/server test --run gameEngine.phase17 gameHandlers.phase17` discovers both files and reports RED cases (expected at this stage)
- No production engine/handler/client code is modified in this plan
</verification>

<success_criteria>

- FREE_MOVE phase literal and four new GameState fields exist in shared types and compile
- GAME_CANCEL_MOVEMENT typed in shared events
- Two Wave-0 test files seeded and failing RED for all unimplemented behaviors
  </success_criteria>

<output>
Create `.planning/phases/17-rule-bugs/17-01-SUMMARY.md` when done
</output>
