---
phase: 17-rule-bugs
plan: 02
type: execute
wave: 2
depends_on: ['17-01']
files_modified:
  - packages/server/src/gameEngine.ts
autonomous: true
requirements: [BUG-01, BUG-04, BUG-05]
must_haves:
  truths:
    - 'A header pass (the FIRST_TIME_PASS following a won high-pass header) is never intercepted server-side'
    - 'A grounded pass landing on an occupied hex is picked up by that piece; a defender pickup transfers possession'
    - "A goalkeeper save that drops the ball spawns the loose ball at the goalkeeper's hex"
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'BUG-01 interception skip, BUG-04 occupant pickup, BUG-05 GK loose-ball position — all in applyRoll'
      contains: 'isHeaderPass'
  key_links:
    - from: 'applyRoll PASS case'
      to: 'interception loop'
      via: 'isHeaderPass guard'
      pattern: "if \\(!isHeaderPass\\)"
    - from: 'applyRoll PASS case (after interception loop)'
      to: 'state.pieces occupant at targetHex'
      via: 'occupant lookup + possession flip'
      pattern: "occupant\\.teamId !== carrier\\.teamId"
---

<objective>
Fix the three engine-only pass/save defects, all located inside `applyRoll` in `gameEngine.ts`: BUG-01 (header pass unblockable), BUG-04 (pass to occupied hex → pickup + possession transfer), BUG-05 (loose ball after save spawns at GK hex).

Purpose: These are pure FSM corrections with no client or socket changes. They share one file (`gameEngine.ts`) so they are grouped to avoid file-ownership conflicts, but each is an independent, surgical edit.
Output: Three targeted edits in `applyRoll`, turning the matching Wave-0 RED tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-rule-bugs/17-CONTEXT.md
@.planning/phases/17-rule-bugs/17-RESEARCH.md
@.planning/phases/17-rule-bugs/17-PATTERNS.md
</context>

<artifacts_this_phase_produces>

- Local `isHeaderPass` boolean + `occupant` lookup inside applyRoll PASS case (gameEngine.ts). No new exported symbols. New GameState fields consumed (from Plan 01) are not set here.
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BUG-01 — skip interception loop for header pass</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (read the `applyRoll` PASS case from ~line 893 through the interception loop; the loop is around the section between the accuracy roll and the delivery return — confirm the variable that holds the resolved last-action-type, referenced in research as `newLastActionType`)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (section "BUG-01: Skip interception loop when lastActionType === 'HEADER'")
    - packages/server/src/__tests__/gameEngine.test.ts (existing BUG-01 / interception coverage ~lines 760-930)
  </read_first>
  <behavior>
    - When the resolved last-action-type for the pass is 'HEADER', the interception `for` loop over interceptors is not entered, and the ball delivers to the target regardless of any defender in the ZoI.
    - When the last-action-type is any non-HEADER pass type, the interception loop runs exactly as before (no behavior change).
  </behavior>
  <action>
    Per D-01/D-02: wrap the existing interception `for` loop in `applyRoll`'s PASS case with a guard `const isHeaderPass = <resolvedLastActionType> === 'HEADER';` and `if (!isHeaderPass) { ... existing loop unchanged ... }`. Do not restructure the surrounding code or alter the loop body. The HIGH_PASS contest and HEADER duel are out of scope — only the FIRST_TIME_PASS that carries `lastActionType === 'HEADER'` is made unblockable. Use the actual resolved-last-action-type variable present at that point in the function (research names it `newLastActionType`; confirm against the source before editing).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run gameEngine</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts contains `const isHeaderPass =` and `if (!isHeaderPass)` wrapping the interception loop
    - A header-pass test asserts `ball.carrierId` is the target-hex teammate (delivered), not an interceptor
    - `pnpm --filter @counter-attack/server test --run gameEngine` exits 0 with the BUG-01 case green
    - Existing non-header interception tests (PASS-01) still pass
  </acceptance_criteria>
  <done>Header passes skip interception; all other pass interception behavior is unchanged; BUG-01 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BUG-04 — pass to occupied hex becomes a pickup with possession transfer</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (read the PASS delivery return path after the interception loop — the existing teammate-only lookup `state.pieces.find(p => p.teamId === carrier.teamId && ...)` and the final delivery return object, ~lines 1089-1164)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (section "BUG-04: Occupant check after interception loop" — exact return shape)
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Pitfall 1: teammate vs any-piece lookup)
  </read_first>
  <behavior>
    - A grounded pass (STANDARD_PASS, FIRST_TIME_PASS, LONG_BALL) whose targetHex is occupied by ANY piece sets `ball.carrierId` to that piece's id and `ball.position` to that piece's position.
    - If the occupant's team differs from the passing carrier's team, `attackingTeam` and `activeTeam` both flip to the occupant's team; phase stays 'PASS'.
    - If the occupant is a teammate, possession is unchanged.
    - If no piece occupies targetHex, the existing delivery behavior (teammate/empty hex) is preserved.
    - HIGH_PASS is out of scope (it routes to HEADER, not this delivery path).
  </behavior>
  <action>
    Per D-08/D-09/D-10: after the interception loop and BEFORE the existing teammate-only delivery return, find any piece at the target hex: `const occupant = state.pieces.find(p => p.position.q === targetHex.q && p.position.r === targetHex.r)`. If `occupant` exists, return an `{ok:true, state}` object with `phase: 'PASS'`, `ball: { position: occupant.position, carrierId: occupant.id }`, `attackingTeam`/`activeTeam` set to `occupant.teamId` when `occupant.teamId !== carrier.teamId` (else unchanged), and preserve the existing `lastActionType`, `actionCount` increment, `passTargetHex: null`, `preGeneratedInterceptionDice: []`, and updated `eventLog` exactly as the existing return does. Leave the existing no-occupant return (teammate / empty hex) untouched as the fall-through. Use the actual local variable names present in the function (`carrier`, `targetHex`, `newLastActionType`, `passTimeCost`, `newEventLog`) — confirm them in the source before editing; do not invent names.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run gameEngine</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts contains an `occupant` lookup over `state.pieces` matched on `targetHex.q`/`targetHex.r`
    - gameEngine.ts contains a possession-flip condition `occupant.teamId !== carrier.teamId`
    - Test: pass to a defender-occupied hex → `ball.carrierId === defender.id` and `attackingTeam`/`activeTeam === defender.teamId`
    - Test: pass to a teammate-occupied hex → `ball.carrierId === teammate.id`, possession unchanged
    - `pnpm --filter @counter-attack/server test --run gameEngine` exits 0
  </acceptance_criteria>
  <done>Grounded passes onto occupied hexes are pickups; defender pickups transfer possession; BUG-04 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BUG-05 — loose ball after save spawns at goalkeeper hex</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (read the SHOT case SAVE branch ~lines 1187-1422: `gkEffectivePos` is computed at ~line 1187; the catch path at ~line 1398 already uses it; the dropped-ball LOOSE_BALL return at ~line 1414 is the fix site)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (section "BUG-05: Loose ball spawns at GK hex")
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Pitfall 2)
  </read_first>
  <behavior>
    - On a save where `handling.caught === false` (the GK fumbles), the resulting LOOSE_BALL state's `ball.position` equals `gkEffectivePos` (the GK's effective position), and `ball.carrierId` is null.
    - The caught path (`handling.caught === true`) is unchanged — ball stays with the GK at `gkEffectivePos`.
  </behavior>
  <action>
    Per D-11: in the SHOT SAVE branch where `handling.caught === false`, ensure the LOOSE_BALL return uses `ball: { position: gkEffectivePos, carrierId: null }`. `gkEffectivePos` is already in scope (computed ~line 1187 as `state.gkDivePosition ?? gk.position`). If the current code references `state.ball.position` or the shooter origin instead, replace it with `gkEffectivePos`. This is a single-reference correction — do not touch the catch path or any other LOOSE_BALL return in the file.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run gameEngine</automated>
  </verify>
  <acceptance_criteria>
    - The `handling.caught === false` LOOSE_BALL return in the SHOT SAVE branch uses `position: gkEffectivePos`
    - Test: a save fumble produces a LOOSE_BALL with `ball.position` equal to the GK effective position (not the shot origin)
    - `pnpm --filter @counter-attack/server test --run gameEngine` exits 0
    - Existing SHOT/save tests (Phase 10/11) still pass
  </acceptance_criteria>
  <done>Save fumbles drop the loose ball at the GK hex; BUG-05 test green; no other LOOSE_BALL returns altered.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @counter-attack/server test --run gameEngine` exits 0 with BUG-01, BUG-04, BUG-05 cases green
- No regressions in existing gameEngine, phase8, phase10, rule11 suites
- No files other than gameEngine.ts modified
</verification>

<success_criteria>

- Header passes are unblockable server-side (interception loop skipped)
- Grounded passes onto occupied hexes are pickups with correct possession transfer
- Save fumbles spawn loose ball at GK position
  </success_criteria>

<output>
Create `.planning/phases/17-rule-bugs/17-02-SUMMARY.md` when done
</output>
