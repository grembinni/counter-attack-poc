---
phase: 17-rule-bugs
plan: 04
type: execute
wave: 4
depends_on: ['17-01', '17-03']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
  - packages/client/src/components/ActionPanel.tsx
autonomous: false
requirements: [MOVE-06]
user_setup: []
must_haves:
  truths:
    - "After a movement turn in which the ball carrier crossed into the opponent's third, the active team enters a FREE_MOVE phase"
    - "Each eligible outfield player in the opponent's final third can move up to 6 hexes during FREE_MOVE"
    - 'FREE_MOVE ends on End Turn (→ PASS); with zero eligible players the phase is skipped entirely'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applyEndTurn FREE_MOVE branch + applyFreeMoveEnd + FREE_MOVE handling in applyMove'
      contains: "phase: 'FREE_MOVE'"
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'FREE_MOVE branches in GAME_MOVE and GAME_END_TURN handlers'
      contains: 'FREE_MOVE'
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'FREE_MOVE phase render branch'
      contains: 'FREE_MOVE'
  key_links:
    - from: 'applyEndTurn (ATTACKER_2→null)'
      to: 'FREE_MOVE phase'
      via: 'pendingFreeMove check + eligible-player filter'
      pattern: 'pendingFreeMove !== null'
    - from: 'gameHandlers GAME_END_TURN FREE_MOVE branch'
      to: 'applyFreeMoveEnd'
      via: 'phase guard'
      pattern: 'applyFreeMoveEnd'
---

<objective>
Complete MOVE-06: the free 6-hex move for outfield players in the opponent's final third. The `pendingFreeMove` field is already set by `applyMove` when the carrier crosses thirds; this plan consumes it by transitioning to a new `FREE_MOVE` phase after the MOVEMENT turn ends, letting each eligible player move up to 6 hexes, and ending the phase on End Turn.

Purpose: Finish the scaffolded free-move rule end-to-end (engine transition + per-piece move handling + phase-end + client panel). Each eligible player gets an independent 6-hex allowance — not a shared pool.
Output: `applyEndTurn` FREE_MOVE branch, `applyFreeMoveEnd` function, FREE_MOVE move handling, handler wiring (reuse GAME_MOVE + GAME_END_TURN), FREE_MOVE ActionPanel branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-rule-bugs/17-CONTEXT.md
@.planning/phases/17-rule-bugs/17-RESEARCH.md
@.planning/phases/17-rule-bugs/17-PATTERNS.md
</context>

<artifacts_this_phase_produces>

- `applyFreeMoveEnd(state): {ok:true; state:GameState}` (gameEngine.ts)
- FREE_MOVE branch inside `applyEndTurn` (gameEngine.ts) consuming `pendingFreeMove`, setting `freeMoveEligibleIds`/`freeMoveUsedPace` (fields from Plan 01)
- FREE_MOVE handling inside `applyMove` (per-piece 6-hex allowance tracking via `freeMoveUsedPace`)
- FREE_MOVE branches in GAME_MOVE and GAME_END_TURN handlers (gameHandlers.ts)
- FREE_MOVE render branch (ActionPanel.tsx)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: applyEndTurn FREE_MOVE transition + applyFreeMoveEnd</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (read `applyEndTurn` from ~line 639: the `if (nextSlot === null)` block ~line 666, the half-end early return ~lines 681-698, and the normal ATTACKER_2→PASS return ~lines 700-715; read `applyMove` ~lines 557-565 to confirm `pendingFreeMove = { team, hexesAllowed: 6 }` shape; read `isInRegion` usage for homeThird/awayThird)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (section "MOVE-06: FREE_MOVE phase transition in applyEndTurn" — exact baseState shape and eligible filter)
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Code Examples MOVE-06; Pitfall 3: consuming pendingFreeMove too early; Pitfall 7: opponentThird region by attackingTeam)
  </read_first>
  <behavior>
    - `applyEndTurn` at ATTACKER_2→null (nextSlot === null), when `state.pendingFreeMove !== null` and ≥1 outfield piece of the free team is in the opponent's third: returns phase 'FREE_MOVE', `freeMoveEligibleIds` = those piece ids, `freeMoveUsedPace` = {}, `pendingFreeMove` = null, with all normal slot-reset/clock fields applied.
    - Same transition with zero eligible pieces: returns phase 'PASS' (skip FREE_MOVE), `pendingFreeMove` = null.
    - If the half/full time ends on this end-turn, `pendingFreeMove` is discarded (no free move after a half ends) — the HALF_TIME/FULL_TIME early return path clears it.
    - Intermediate slot transitions (ATTACKER_4→DEFENDER_5, DEFENDER_5→ATTACKER_2) preserve `pendingFreeMove` unchanged (no FREE_MOVE fired mid-turn).
    - `applyFreeMoveEnd(state)`: returns phase 'PASS' with `freeMoveEligibleIds: null`, `freeMoveUsedPace: null`.
  </behavior>
  <action>
    Per D-12/D-13/D-15 and Pitfalls 3+7: inside the `if (nextSlot === null)` block of `applyEndTurn`, AFTER the existing clock/half-end checks and BEFORE the normal PASS return, insert a `if (state.pendingFreeMove !== null) { ... }` branch. Compute `freeTeam = state.pendingFreeMove.team` and `opponentThird = freeTeam === 'home' ? 'awayThird' : 'homeThird'`. Filter `state.pieces` for `p.teamId === freeTeam && p.role !== 'GK' && isInRegion(p.position, opponentThird)` → `eligibleIds`. Build the baseState exactly as the normal PASS return does (same `movementSlot: null`, `activeTeam`, `eventLog` with slotAdvanceEvent, `movedPieceIds: []`, `paceUsedByPieceId: {}`, clock fields, `lastActionType`), plus `pendingFreeMove: null`. If `eligibleIds.length === 0` return baseState (phase 'PASS'). Otherwise return baseState spread with `phase: 'FREE_MOVE'`, `freeMoveEligibleIds: eligibleIds`, `freeMoveUsedPace: {}`. Use the actual local variable names present (`nextActiveTeam`, `slotAdvanceEvent`, `newActionCount`, `newAddedTime`) — confirm in source.

    Ensure the HALF_TIME/FULL_TIME early return path sets `pendingFreeMove: null` so a half-end discards the pending free move (Pitfall 3). Do NOT modify intermediate-slot returns — they already propagate `pendingFreeMove` via spread.

    Add `export function applyFreeMoveEnd(state: GameState): { ok: true; state: GameState }` returning `{...state, phase: 'PASS', freeMoveEligibleIds: null, freeMoveUsedPace: null}`.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run gameEngine</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts contains a `state.pendingFreeMove !== null` branch inside applyEndTurn's nextSlot===null block setting `phase: 'FREE_MOVE'`
    - The branch computes `opponentThird` from `freeTeam` (not a fixed region) and filters `role !== 'GK'`
    - Zero-eligible path returns `phase: 'PASS'` with `pendingFreeMove: null`
    - HALF_TIME/FULL_TIME return clears `pendingFreeMove`
    - gameEngine.ts contains `export function applyFreeMoveEnd`
    - Wave-0 MOVE-06 engine tests (both FREE_MOVE-enter and skip cases) pass; `pnpm --filter @counter-attack/server test --run gameEngine` exits 0
  </acceptance_criteria>
  <done>applyEndTurn transitions to FREE_MOVE with correct eligibility, skips when empty, discards on half-end; applyFreeMoveEnd returns to PASS; engine tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FREE_MOVE per-piece move handling in applyMove</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (read `applyMove` ~lines 291-610 fully: the phase guard, the pace/distance validation using hexDistance, and how MOVEMENT tracks `paceUsedByPieceId`; FREE_MOVE will mirror this with `freeMoveUsedPace` and a 6-hex cap)
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Don't Hand-Roll: hexDistance for distance; D-13 each player independent up-to-6)
    - .planning/phases/17-rule-bugs/17-CONTEXT.md (D-13: each eligible player independently gets up to 6 hexes — not a shared pool)
  </read_first>
  <behavior>
    - In FREE_MOVE phase, a move is valid only for a piece whose id is in `freeMoveEligibleIds`, the active team, and only while that piece's cumulative `freeMoveUsedPace[pieceId]` plus the step distance does not exceed 6.
    - Each accepted step updates `freeMoveUsedPace[pieceId]` by the step's hex distance and moves the piece.
    - A move for an ineligible piece, the wrong team, or one that would exceed 6 cumulative hexes returns a MOVE_INVALID-style failure (reuse the existing failure reason the function already returns for over-pace/invalid moves).
    - Standard adjacency/occupancy validation that MOVEMENT already enforces applies unchanged.
  </behavior>
  <action>
    Per D-13: extend `applyMove`'s phase handling to accept `state.phase === 'FREE_MOVE'`. Add a FREE_MOVE branch that: rejects if `pieceId` is not in `state.freeMoveEligibleIds`; computes the step distance via `hexDistance(piece.position, to)`; rejects if `(state.freeMoveUsedPace?.[pieceId] ?? 0) + stepDistance > 6`; otherwise returns `{ok:true, state}` with the piece moved and `freeMoveUsedPace` updated to add the step distance for that piece id (preserving other pieces' entries). Keep the existing adjacency/occupancy validation that the function already performs. Reuse the existing failure-reason literals (do not invent new ones unless the union lacks a suitable one; if so, add a `FREE_MOVE_EXHAUSTED`-style reason to the result type). Confirm `hexDistance` is already imported.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run gameEngine</automated>
  </verify>
  <acceptance_criteria>
    - applyMove accepts `state.phase === 'FREE_MOVE'` and gates on `freeMoveEligibleIds` membership
    - A move accumulating >6 hexes for one piece is rejected; a 6th hex within budget is accepted
    - `freeMoveUsedPace[pieceId]` increases by the step distance on each accepted move; other pieces' entries are preserved
    - Test: two different eligible pieces can each move up to 6 hexes independently
    - `pnpm --filter @counter-attack/server test --run gameEngine` exits 0
  </acceptance_criteria>
  <done>applyMove handles FREE_MOVE with a per-piece 6-hex cap tracked in freeMoveUsedPace; engine tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire FREE_MOVE handlers + ActionPanel branch</name>
  <files>packages/server/src/gameHandlers.ts, packages/client/src/components/ActionPanel.tsx</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (read GAME_MOVE handler ~lines 257-340 including the SNAP_DEFLECT phase branch ~lines 339-391 as the template for adding a FREE_MOVE phase branch; read GAME_END_TURN ~lines 508+ to add a FREE_MOVE branch calling applyFreeMoveEnd; read imports to add applyFreeMoveEnd)
    - packages/client/src/components/ActionPanel.tsx (read the HIGH_PASS_MOVEMENT branch ~lines 108-125 and GK_KICK_MOVEMENT branch ~lines 339-353 as the render template; read the isActivePlayer / waitingPanel pattern)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (sections "MOVE-06: FREE_MOVE phase — reuse GAME_MOVE and GAME_END_TURN" and "MOVE-06: FREE_MOVE phase branch" for ActionPanel)
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Open Question 3 recommendation: reuse game:move + game:end-turn, no new events)
  </read_first>
  <action>
    Per D-14 and Open Question 3 (reuse existing events):

    gameHandlers.ts GAME_MOVE: add a phase branch so that when `room.gameState.phase === 'FREE_MOVE'`, the handler validates `isActivePlayer`, calls `applyMove(room.gameState, pieceId, to)`, emits the reason on `!result.ok` and broadcastState, sets state and broadcastState on success — mirroring the existing MOVEMENT/SNAP_DEFLECT branches with the isProcessing mutex already wrapping the handler. Ensure broadcastState fires after every accepted move so both clients see repositioning (Anti-pattern note in RESEARCH).

    gameHandlers.ts GAME_END_TURN: add a branch `if (room.gameState.phase === 'FREE_MOVE') { ... }` near the other phase branches (after the HIGH_PASS_MOVEMENT branch) that checks `isActivePlayer`, calls the new `applyFreeMoveEnd(room.gameState)`, sets `room.gameState = result.state`, broadcastState. Import `applyFreeMoveEnd`.

    ActionPanel.tsx: add an `if (phase === 'FREE_MOVE') { ... }` branch (place it alongside the other phase branches, before the generic isActivePlayer guard). When `myTeam === null` return null; when `!isActivePlayer` return the waiting panel; otherwise render a panel with a helper block "Free Move!" / "Move up to 6 hexes per player in the opponent's third." and an End Turn button calling `emitEndTurn`, plus the gameError display — copy the structure of the GK_KICK_MOVEMENT branch.

  </action>
  <verify>
    <human-check>
      1. As home, carry the ball from your own third into the opponent's third during MOVEMENT, then End Turn.
      2. Confirm the ActionPanel shows "Free Move!" with the helper text and an End Turn button.
      3. Move two different players in the opponent's third up to 6 hexes each; confirm each is independently capped at 6.
      4. Press End Turn; confirm the phase advances to PASS.
      5. Repeat but with no outfield players in the opponent's third when crossing — confirm FREE_MOVE is skipped and you go straight to PASS (no empty panel shown).
    </human-check>
    <automated>pnpm --filter @counter-attack/server test --run gameHandlers && pnpm --filter @counter-attack/client test --run ActionPanel 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - gameHandlers.ts GAME_MOVE has a `phase === 'FREE_MOVE'` branch calling applyMove + broadcastState
    - gameHandlers.ts GAME_END_TURN has a `phase === 'FREE_MOVE'` branch calling applyFreeMoveEnd
    - ActionPanel.tsx has an `if (phase === 'FREE_MOVE')` render branch showing the free-move helper text + End Turn
    - `pnpm --filter @counter-attack/server test --run gameHandlers` exits 0; client tests pass
    - Human checks confirm independent 6-hex moves, End Turn → PASS, and empty-third skip
  </acceptance_criteria>
  <done>FREE_MOVE moves and end-turn are wired through existing events; ActionPanel renders the free-move prompt; tests green and human verification confirms behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>MOVE-06 FREE_MOVE phase end-to-end: triggered after a carrier crosses into the opponent's third, each eligible outfield player moves up to 6 hexes independently, End Turn → PASS, empty-third skip.</what-built>
  <how-to-verify>
    1. Two tabs, same room, start match. As the active team, carry the ball into the opponent's final third during MOVEMENT, then End Turn.
    2. Confirm the FREE_MOVE panel ("Free Move! Move up to 6 hexes per player in the opponent's third.") appears for the active team and a waiting panel for the opponent.
    3. Move two eligible players; confirm each can move up to 6 hexes and no more.
    4. End Turn → confirm transition to PASS for the opponent.
    5. Do a crossing turn with no other outfield players in the opponent's third — confirm FREE_MOVE is skipped (straight to PASS, no empty panel).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @counter-attack/server test --run` exits 0 (MOVE-06 engine + handler cases green)
- `pnpm --filter @counter-attack/client test --run` exits 0
- Human checkpoint confirms FREE_MOVE behavior including the empty-third skip
</verification>

<success_criteria>

- FREE_MOVE fires only after a carrier crosses thirds, inheriting the crossing team's possession
- Each eligible outfield player moves up to 6 hexes independently
- FREE_MOVE ends on End Turn (→ PASS); zero-eligible case is skipped silently
  </success_criteria>

<output>
Create `.planning/phases/17-rule-bugs/17-04-SUMMARY.md` when done
</output>
