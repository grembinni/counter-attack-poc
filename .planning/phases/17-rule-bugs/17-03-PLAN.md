---
phase: 17-rule-bugs
plan: 03
type: execute
wave: 3
depends_on: ['17-01', '17-02']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/store/gameStore.ts
autonomous: false
requirements: [BUG-02, BUG-03]
user_setup: []
must_haves:
  truths:
    - 'A Cancel control is shown in MOVEMENT phase only before any piece has moved, and reverts to PASS without consuming a slot'
    - 'Undo works in HIGH_PASS_MOVEMENT phase with the same slot-boundary rules as MOVEMENT'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applyCancelMovement function + applyUndo HIGH_PASS_MOVEMENT boundary extension'
      contains: 'export function applyCancelMovement'
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'GAME_CANCEL_MOVEMENT handler + GAME_UNDO multi-phase guard'
      contains: 'GAME_CANCEL_MOVEMENT'
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'Cancel button in MOVEMENT; Undo button in HIGH_PASS_MOVEMENT'
  key_links:
    - from: 'ActionPanel MOVEMENT branch'
      to: 'emitCancelMovement store action'
      via: 'onClick when paceUsedByPieceId empty'
      pattern: 'emitCancelMovement'
    - from: 'gameHandlers GAME_CANCEL_MOVEMENT'
      to: 'applyCancelMovement'
      via: 'handler call'
      pattern: "applyCancelMovement\\(room\\.gameState\\)"
---

<objective>
Implement BUG-02 (Cancel control in MOVEMENT phase) and BUG-03 (Undo in HIGH_PASS_MOVEMENT phase). Both are about reversing player intent before an irreversible action, so they are grouped: server pure functions + handler wiring + ActionPanel UI + store action.

Purpose: Restore escape hatches the rulebook expects — cancel a movement turn before committing any move, and undo pre-header repositioning moves with the same boundary logic that already governs MOVEMENT undo.
Output: `applyCancelMovement` engine function, extended `applyUndo` boundary scan, `GAME_CANCEL_MOVEMENT` handler, relaxed `GAME_UNDO` phase guard, Cancel + Undo buttons in ActionPanel, `emitCancelMovement` store action.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-rule-bugs/17-CONTEXT.md
@.planning/phases/17-rule-bugs/17-RESEARCH.md
@.planning/phases/17-rule-bugs/17-PATTERNS.md
</context>

<artifacts_this_phase_produces>

- `applyCancelMovement(state): ApplyCancelMovementResult` (gameEngine.ts)
- `ApplyCancelMovementResult` discriminated union type (gameEngine.ts)
- `GAME_CANCEL_MOVEMENT` socket handler (gameHandlers.ts)
- `emitCancelMovement` Zustand store action (gameStore.ts)
- Cancel button + HIGH_PASS_MOVEMENT Undo button (ActionPanel.tsx)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BUG-02 + BUG-03 engine functions</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (read `applyUndo` from ~line 784 — especially the slot-boundary `reduce` at ~line 786 that scans for SLOT_ADVANCE/KICK_OFF; and read the discriminated-union return style of any nearby `apply*` function such as applyStartMovement for the new function shape)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (sections "BUG-02: New applyCancelMovement function" and "BUG-03: Extend applyUndo to accept HIGH_PASS_MOVEMENT")
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Pitfall 5: paceUsedByPieceId emptiness check; Pitfall 6: HP_REPOSITION boundary)
  </read_first>
  <behavior>
    - `applyCancelMovement`: returns `{ok:false, reason:'WRONG_PHASE'}` when phase !== 'MOVEMENT'; returns `{ok:false, reason:'PIECES_ALREADY_MOVED'}` when `Object.keys(state.paceUsedByPieceId).length > 0`; otherwise returns `{ok:true, state}` with phase 'PASS', `movementSlot: null`, `movedPieceIds: []`, `paceUsedByPieceId: {}`. No movement slot is consumed.
    - `applyUndo` in HIGH_PASS_MOVEMENT: treats `HP_REPOSITION` as a slot boundary (in addition to SLOT_ADVANCE/KICK_OFF) so undo only reverses MOVE events after the most recent boundary and never crosses into pre-HEADER moves.
  </behavior>
  <action>
    Add an exported `ApplyCancelMovementResult` union: `{ok:false; reason:'WRONG_PHASE'|'PIECES_ALREADY_MOVED'} | {ok:true; state: GameState}`. Add `export function applyCancelMovement(state: GameState): ApplyCancelMovementResult` implementing the behavior above, per D-03/D-04 and Pitfall 5 (guard on `paceUsedByPieceId` emptiness, NOT `movedPieceIds`).

    Extend the boundary scan in `applyUndo` (the `reduce` at ~line 786) so the boundary predicate is `evt.type === 'SLOT_ADVANCE' || evt.type === 'KICK_OFF' || (state.phase === 'HIGH_PASS_MOVEMENT' && evt.type === 'HP_REPOSITION')`. Leave the rest of `applyUndo` unchanged — it stays phase-agnostic per D-06 (the handler guard, Task 2, controls which phases may call it).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run gameEngine</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts contains `export function applyCancelMovement` and `export type ApplyCancelMovementResult`
    - applyCancelMovement guards on `Object.keys(state.paceUsedByPieceId).length` (not movedPieceIds)
    - applyUndo boundary predicate includes `state.phase === 'HIGH_PASS_MOVEMENT' && evt.type === 'HP_REPOSITION'`
    - Wave-0 BUG-02 and BUG-03 engine tests in gameEngine.phase17.test.ts pass
    - `pnpm --filter @counter-attack/server test --run gameEngine` exits 0
  </acceptance_criteria>
  <done>applyCancelMovement exists with correct guards; applyUndo recognises HP_REPOSITION as a boundary in HIGH_PASS_MOVEMENT; engine tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GAME_CANCEL_MOVEMENT handler and relax GAME_UNDO phase guard</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (read the GAME_RESTART_MOVEMENT handler ~lines 907-938 as the copy template; read the GAME_UNDO handler ~lines 875-905, specifically the phase check at ~line 884; read the imports block ~lines 41-60 to add applyCancelMovement)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (sections "New handler pattern (BUG-02)" and "Handler extension pattern (BUG-03)")
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Security Domain: roomCode + isProcessing mutex + isActivePlayer guards)
  </read_first>
  <action>
    Import `applyCancelMovement` from `./gameEngine.js`.

    Add a `socket.on(ClientEvents.GAME_CANCEL_MOVEMENT, () => {...})` handler by copying the GAME_RESTART_MOVEMENT structure exactly: `roomCode` defined check, `getRoom`, `room.isProcessing` mutex, phase guard requiring `phase === 'MOVEMENT'` (emit GAME_ERROR 'WRONG_PHASE' otherwise), `isActivePlayer` guard (emit 'WRONG_TEAM' otherwise), call `applyCancelMovement(room.gameState)`, on `!result.ok` emit `result.reason` and broadcastState, on success set `room.gameState = result.state` and broadcastState, with the `finally { room.isProcessing = false }`.

    Relax the GAME_UNDO phase guard at ~line 884: replace `room.gameState.phase !== 'MOVEMENT'` with a check against an allowed list — `const validUndoPhases: GamePhase[] = ['MOVEMENT', 'HIGH_PASS_MOVEMENT']; if (room.gameState === null || !validUndoPhases.includes(room.gameState.phase)) { ... emit WRONG_PHASE ... }`. Import `GamePhase` from `@counter-attack/shared` if not already imported.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run gameHandlers</automated>
  </verify>
  <acceptance_criteria>
    - gameHandlers.ts contains `socket.on(ClientEvents.GAME_CANCEL_MOVEMENT` with isProcessing mutex, MOVEMENT phase guard, isActivePlayer guard, and applyCancelMovement call
    - GAME_UNDO guard uses a `validUndoPhases` list containing both 'MOVEMENT' and 'HIGH_PASS_MOVEMENT'
    - Wave-0 BUG-02 integration test (cancel reverts to PASS; rejects when moved) passes
    - `pnpm --filter @counter-attack/server test --run gameHandlers` exits 0
  </acceptance_criteria>
  <done>The cancel-movement event reverts MOVEMENT→PASS server-side with full guards; undo is accepted in HIGH_PASS_MOVEMENT; handler tests green.</done>
</task>

<task type="auto">
  <name>Task 3: ActionPanel Cancel + HIGH_PASS_MOVEMENT Undo buttons + store action</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/store/gameStore.ts</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (read the MOVEMENT branch ~lines 497-548 including the `canUndo` computation ~512-518 and the End Turn / Undo buttons; read the HIGH_PASS_MOVEMENT branch ~lines 108-125; read the `← Back` button style usage ~lines 482-484)
    - packages/client/src/store/gameStore.ts (read how emitUndo / emitEndTurn are defined — the socket.emit wrapper pattern — to add emitCancelMovement identically)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (sections "BUG-02: Cancel button in MOVEMENT phase", "BUG-03: Undo button in HIGH_PASS_MOVEMENT phase", "Back/Cancel Button Style")
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Pitfall 5)
  </read_first>
  <action>
    In gameStore.ts: add an `emitCancelMovement` action that calls `socket.emit(ClientEvents.GAME_CANCEL_MOVEMENT)` — mirror the existing `emitUndo` action exactly (same socket reference, same null-guard if present). Expose it on the store interface.

    In ActionPanel.tsx MOVEMENT branch: subscribe to `emitCancelMovement` via `useGameStore`. After the End Turn button in the MOVEMENT return block, render a Cancel button visible ONLY when `Object.keys(paceUsedByPieceId).length === 0` (Pitfall 5 — use paceUsedByPieceId, not movedPieceIds). Use the existing `← Back` button style (the `styles.backButton` class) with label `← Cancel` and `onClick={emitCancelMovement}`. `paceUsedByPieceId` is already subscribed in this component.

    In ActionPanel.tsx HIGH_PASS_MOVEMENT branch (~lines 108-125): add an Undo button after the End Turn button. Reuse the same `canUndo` logic the MOVEMENT branch uses (eventLog scan for a MOVE after the last boundary, disabled when `lastDiceRoll` is set). To avoid duplication, lift the `canUndo` computation to a shared `const` above both phase branches (or replicate the same expression) and use `disabled={!canUndo} onClick={emitUndo}` on the new button with the `styles.ctaButton` class and label `Undo`.

  </action>
  <verify>
    <human-check>
      1. Start a match, enter MOVEMENT phase. Confirm a `← Cancel` button is visible while no piece has moved. Move one hex with any piece; confirm the Cancel button disappears.
      2. Click `← Cancel` before moving — confirm the panel returns to the PASS action chooser (no slot consumed).
      3. Trigger a high pass to reach HIGH_PASS_MOVEMENT; reposition a piece; confirm an `Undo` button appears and reverses the move; confirm Undo is disabled before any move.
    </human-check>
    <automated>pnpm --filter @counter-attack/client test --run ActionPanel 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - gameStore.ts exposes `emitCancelMovement` calling `socket.emit(ClientEvents.GAME_CANCEL_MOVEMENT)`
    - ActionPanel.tsx MOVEMENT branch renders `← Cancel` gated on `Object.keys(paceUsedByPieceId).length === 0`
    - ActionPanel.tsx HIGH_PASS_MOVEMENT branch renders an `Undo` button using the shared `canUndo` value and `emitUndo`
    - `pnpm --filter @counter-attack/client test --run` exits 0 (no broken client tests)
    - Human checks for Cancel visibility and HIGH_PASS_MOVEMENT undo pass
  </acceptance_criteria>
  <done>Cancel appears only before the first move and reverts to PASS; Undo is available in HIGH_PASS_MOVEMENT; client tests green and human verification confirms UI behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>BUG-02 Cancel control (MOVEMENT phase, pre-move only) and BUG-03 Undo control (HIGH_PASS_MOVEMENT phase), fully wired client→server→broadcast.</what-built>
  <how-to-verify>
    1. Open two browser tabs, join the same room, start a match.
    2. As the active team, choose Move to enter MOVEMENT. Verify `← Cancel` is shown. Click it — verify you return to the PASS action chooser with no slot consumed (slot helper text resets).
    3. Re-enter MOVEMENT, move one piece one hex. Verify `← Cancel` is now hidden and `Undo` works.
    4. Execute a High Pass to reach HIGH_PASS_MOVEMENT. Reposition a piece, then click `Undo` — verify the piece returns and pace is restored. Verify `Undo` is disabled before any move.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @counter-attack/server test --run` exits 0 (engine + handler phase17 cases green)
- `pnpm --filter @counter-attack/client test --run` exits 0
- Human checkpoint confirms Cancel and HIGH_PASS_MOVEMENT Undo behavior in a live two-tab session
</verification>

<success_criteria>

- Cancel reverts MOVEMENT→PASS only before the first move, consuming no slot
- Undo functions in HIGH_PASS_MOVEMENT with correct boundary scoping
- Server guards (mutex, phase, active-player) enforced on the new event
  </success_criteria>

<output>
Create `.planning/phases/17-rule-bugs/17-03-SUMMARY.md` when done
</output>
