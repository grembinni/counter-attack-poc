---
phase: 17-rule-bugs
plan: 05
type: execute
wave: 5
depends_on: ['17-04']
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/offside.ts
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/offside.test.ts
  - packages/client/src/components/PieceOverlay.tsx
  - packages/client/src/components/PieceOverlay.test.tsx
  - packages/client/src/components/HexGrid.tsx
autonomous: false
requirements: [OFFSIDE-01]
user_setup: []
must_haves:
  truths:
    - "A piece past the half-field line in its team's attacking direction, ahead of the ball, with ≤1 opposing piece equal-to-or-ahead of it, is flagged offside at end-of-phase (D-21)"
    - 'A flagged piece stays flagged across subsequent end-of-phase checks until it ends a turn equal-to-or-behind the ball OR with ≥2 opposing pieces equal-to-or-ahead (sticky, D-22/D-23)'
    - 'The offside check applies to every piece on the pitch, each evaluated relative to its own team attacking direction (D-24)'
    - 'A flagged piece renders a double-width red ring, layered independently of selectionState (D-25)'
  artifacts:
    - path: 'packages/shared/src/offside.ts'
      provides: 'Pure isPiecePastHalfway / isAheadOf / opposingPiecesAheadOrEqual / evaluateOffside helpers'
      contains: 'export function evaluateOffside'
    - path: 'packages/shared/src/types.ts'
      provides: 'GameState.offsidePieceIds field'
      contains: 'offsidePieceIds'
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'evaluateOffside invoked at end-of-phase transitions in applyEndTurn (and applyFreeMoveEnd)'
      contains: 'evaluateOffside'
    - path: 'packages/client/src/components/PieceOverlay.tsx'
      provides: 'isOffside prop + double-width red ring layer'
      contains: 'isOffside'
  key_links:
    - from: 'applyEndTurn end-of-phase returns'
      to: 'state.offsidePieceIds'
      via: 'evaluateOffside(state) recompute with sticky carry-forward'
      pattern: 'evaluateOffside'
    - from: 'HexGrid PieceOverlay render'
      to: 'PieceOverlay isOffside prop'
      via: 'offsidePieceIds.includes(piece.id)'
      pattern: 'isOffside='
---

<objective>
Implement OFFSIDE-01: server-side offside detection with a sticky per-piece flag, plus the client double-width red ring marker. This plan delivers the shared offside geometry helpers, the new `GameState.offsidePieceIds` field, the engine wiring that re-evaluates the flag at every end-of-phase where pieces can move, and the PieceOverlay/HexGrid rendering of the flag. It does NOT implement the foul/free-kick consequence — that is OFFSIDE-02 (plan 17-06).

Purpose: Interface-first — the `offsidePieceIds` field and `evaluateOffside` helper defined here are consumed by 17-06's foul-trigger logic. The geometry helpers are pure and unit-testable, isolating the trickiest rule logic (team-relative direction, sticky carry-forward) from the FSM wiring.
Output: `packages/shared/src/offside.ts`, `GameState.offsidePieceIds`, `evaluateOffside` calls at end-of-phase transitions, `isOffside` PieceOverlay prop + red ring, HexGrid wiring, and a dedicated offside unit test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-rule-bugs/17-CONTEXT.md
@packages/shared/src/pitch.ts
@packages/shared/src/types.ts
</context>

<artifacts_this_phase_produces>
Symbols created/changed by this plan (OFFSIDE-01):

- `GameState.offsidePieceIds?: readonly string[]` (types.ts) — sticky set of currently-flagged piece IDs; default `[]` at match start and after every reset/kick-off.
- `packages/shared/src/offside.ts` (new module) exporting:
  - `OFFSIDE_HALFWAY_Q` constant = `PITCH_REGIONS.kickOffHex.q` (18) — the half-field boundary column.
  - `attackingDirection(team: 'home' | 'away'): 1 | -1` — +1 for home (attacks toward higher q), -1 for away.
  - `isPastHalfway(piece, team): boolean` — D-21 condition 1.
  - `isAheadOf(aheadQ, refQ, team): boolean` — generic "a is ahead of ref in team's direction" (strictly ahead).
  - `opposingPiecesEqualOrAhead(state, piece): number` — count of OTHER-team pieces (any role, GK included) at equal-or-ahead q (D-21 condition 3 / D-24).
  - `isOffsideNow(state, piece): boolean` — all three D-21 conditions hold for this piece right now.
  - `isClearedNow(state, piece): boolean` — D-22 clear condition (equal-or-behind ball OR ≥2 opposing equal-or-ahead).
  - `evaluateOffside(state: GameState): readonly string[]` — returns the next sticky `offsidePieceIds`: keeps already-flagged ids unless `isClearedNow`, adds newly-`isOffsideNow` ids (D-23 sticky semantics).
- `isOffside?: boolean` prop on `PieceOverlay` (PieceOverlay.tsx) + double-width red ring SVG layer.
- New test file `packages/server/src/__tests__/offside.test.ts`.
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared offside geometry + GameState.offsidePieceIds + RED unit tests</name>
  <files>packages/shared/src/types.ts, packages/shared/src/offside.ts, packages/server/src/__tests__/offside.test.ts</files>
  <read_first>
    - packages/shared/src/pitch.ts (read lines 1-72 — confirm `PITCH_REGIONS.kickOffHex` is `{q:18,r:13}`, `isInRegion`, and the `q<=10`/`q>=26` third boundaries; offside direction uses the same q-axis convention)
    - packages/shared/src/types.ts (read GameState fields ~lines 336-574 just edited in prior plans; match the exact `?`-optional + JSDoc convention used by freeMoveEligibleIds ~line 561; confirm `PlayerPiece` shape — id, teamId, position, role — at lines ~5-31)
    - packages/server/src/gameEngine.ts (read the half-boundary comment at lines 2999-3001 in applyKickOffReady: home own half = q<=18, away own half = q>=18, where 18 = kickOffHex.q; home attacks toward higher q, away toward lower q — this is the direction convention offside mirrors, fixed across halves)
    - packages/server/src/__tests__/gameEngine.test.ts (read the GameState fixture builders / squad-position helpers near the top of the file to reuse them in offside.test.ts — do NOT duplicate squad literals)
  </read_first>
  <behavior>
    - `attackingDirection('home')` → 1; `attackingDirection('away')` → -1.
    - `isPastHalfway(piece, 'home')` true when `piece.position.q > 18`; for 'away' true when `piece.position.q < 18` (strictly past the kickOffHex.q line; exactly on q=18 is NOT past).
    - `isAheadOf(aheadQ, refQ, 'home')` true when `aheadQ > refQ`; for 'away' true when `aheadQ < refQ` (strictly ahead).
    - `opposingPiecesEqualOrAhead(state, piece)` counts pieces whose `teamId !== piece.teamId` (any role incl. GK) whose q is equal-to-or-ahead of `piece.position.q` in `piece`'s team direction (home: opp.q >= piece.q; away: opp.q <= piece.q).
    - `isOffsideNow(state, piece)` is true iff ALL of: (1) `isPastHalfway`, (2) `piece` is strictly ahead of the ball (`isAheadOf(piece.q, ball.position.q, team)`), (3) `opposingPiecesEqualOrAhead(state, piece) <= 1` (D-21).
    - `isClearedNow(state, piece)` is true iff NOT strictly-ahead-of-ball (i.e. equal-or-behind ball) OR `opposingPiecesEqualOrAhead(state, piece) >= 2` (D-22 — logical complement of D-21's conditions 2 and 3).
    - `evaluateOffside(state)` returns a new id array = (existing `offsidePieceIds` minus any now-`isClearedNow`) ∪ (every piece where `isOffsideNow`). A piece already flagged but NOT yet cleared and NOT currently `isOffsideNow` stays flagged (sticky, D-23). Order: stable, deduplicated.
    - Edge: a defending-team piece that pushes past the ball with ≤1 opponent equal-or-ahead is flagged (D-24 — applies to all pieces, team-relative).
  </behavior>
  <action>
    Per D-21/D-22/D-23/D-24 and the half-boundary convention at gameEngine.ts:2999-3001:

    In `packages/shared/src/types.ts`, add ONE new optional field to the `GameState` interface, placed immediately after `freeMoveUsedPace` (~line 567), matching the surrounding `?`-optional + `| null`-or-default convention:
    `offsidePieceIds?: readonly string[];` with JSDoc: "OFFSIDE-01 (D-23): sticky set of piece IDs currently flagged offside. Re-evaluated at every end-of-phase where pieces can move; persists until each piece's D-22 clear condition holds. Default [] at match start and after every reset/kick-off." (Do NOT use `| null`; default is `[]` consistent with movedPieceIds.)

    Create `packages/shared/src/offside.ts` as a pure module (no I/O, no side effects) importing `PITCH_REGIONS` from `./pitch.js` and the `GameState`, `PlayerPiece`, `HexCoord` types from `./types.js`. Export `OFFSIDE_HALFWAY_Q = PITCH_REGIONS.kickOffHex.q`. Implement every helper listed in <artifacts_this_phase_produces> with the exact signatures and the semantics in <behavior>. Use `attackingDirection` internally so home/away logic is written once (compare `q * dir` so `> ` means "ahead"). `evaluateOffside` reads `state.offsidePieceIds ?? []` for the prior sticky set. Confirm whether the shared package has a barrel `index.ts` that must re-export the new module — if so, add the export there (read packages/shared/src/index.ts before editing).

    Create `packages/server/src/__tests__/offside.test.ts` (RED first) with `describe`/`it` cases covering: each helper's home and away direction; `isOffsideNow` true/false at the three-condition boundaries (exactly-on-halfway not past; exactly-level-with-ball not ahead; exactly 1 vs exactly 2 opposing equal-or-ahead); `evaluateOffside` sticky carry-forward (a flagged id that is neither cleared nor currently-offside stays); `evaluateOffside` clear (flagged id that becomes equal-or-behind ball is dropped; flagged id with ≥2 opponents equal-or-ahead is dropped); D-24 defender-flagged case. Reuse the fixture builders from `gameEngine.test.ts` for piece/state construction.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared exec tsc --noEmit && pnpm --filter @counter-attack/server test --run offside 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/offside.ts` exists and exports `evaluateOffside`, `isOffsideNow`, `isClearedNow`, `opposingPiecesEqualOrAhead`, `isPastHalfway`, `isAheadOf`, `attackingDirection`, `OFFSIDE_HALFWAY_Q`
    - `types.ts` GameState contains `offsidePieceIds?: readonly string[]`
    - `pnpm --filter @counter-attack/shared exec tsc --noEmit` exits 0
    - `offside.test.ts` is discovered by vitest and its cases pass GREEN (helpers are pure and fully implemented this task — RED only transiently before implementation lands)
    - Tests assert: halfway boundary is exclusive; ball-level is not "ahead"; the ≤1/≥2 opposing-count boundary; sticky carry-forward; both clear conditions; a defender flagged team-relative (D-24)
  </acceptance_criteria>
  <done>Pure offside helpers and the sticky `offsidePieceIds` field exist, compile, and pass a dedicated unit suite covering both directions, all three trigger conditions at their boundaries, sticky persistence, and both clear conditions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire evaluateOffside into end-of-phase engine transitions</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (read `applyEndTurn` fully, lines 643-775: the HALF_TIME/FULL_TIME return ~687-701, the GK_RESTART return ~713-728, the normal ATTACKER_2→PASS return ~733-749, and the intermediate-slot return ~762-774 — every `ok:true` return that ends a movement turn must carry an updated `offsidePieceIds`; read `applyFreeMoveEnd` added in plan 17-04 — its PASS return must also re-evaluate; check existing imports at the top of the file for how shared helpers are imported)
    - packages/shared/src/offside.ts (the `evaluateOffside` signature created in Task 1)
    - packages/server/src/__tests__/gameEngine.test.ts (reuse fixtures; find where existing applyEndTurn tests live to colocate the new offside-at-end-turn cases or add them to offside.test.ts)
  </read_first>
  <behavior>
    - After `applyEndTurn` resolves a turn that ends the MOVE phase (every `nextSlot === null` return: HALF_TIME/FULL_TIME, GK_RESTART, normal PASS), the returned state's `offsidePieceIds` equals `evaluateOffside(stateWithFinalPiecePositions)` — i.e. recomputed from the post-turn piece/ball positions with sticky carry-forward.
    - Intermediate-slot returns (ATTACKER_4→DEFENDER_5, DEFENDER_5→ATTACKER_2) ALSO re-evaluate offside, because pieces moved during that slot (D-23: re-evaluated at every phase-end where pieces can move).
    - `applyFreeMoveEnd`'s PASS return re-evaluates offside (FREE_MOVE moved pieces).
    - Re-evaluation NEVER clears a flag that D-22 has not satisfied; it carries the sticky set forward (uses `evaluateOffside`, never recomputes from scratch).
    - Dice-only / no-movement transitions are NOT given offside recomputation here (Claude's Discretion in CONTEXT: avoid needless checks where no piece can move). Document which returns are intentionally skipped in a code comment.
  </behavior>
  <action>
    Per D-23 and the CONTEXT "Claude's Discretion (Offside)" note (only re-check where pieces can actually move):

    Import `evaluateOffside` from the shared package (match the existing shared-import style at the top of gameEngine.ts — confirm whether the file imports from `@counter-attack/shared` or a relative path; mirror what `isInRegion`/`hexDistance` use).

    In `applyEndTurn`, for EACH `ok:true` return (the HALF_TIME/FULL_TIME return, the GK_RESTART return, the normal ATTACKER_2→PASS return, and the intermediate-slot return), set `offsidePieceIds` on the returned state to `evaluateOffside({ ...state, /* with the same piece positions that return uses */ })`. Since none of these returns mutate piece positions (pieces already moved via applyMove), compute `const nextOffside = evaluateOffside(state)` once near the top of `applyEndTurn` AFTER the WRONG_SLOT guard, and spread `offsidePieceIds: nextOffside` into each `ok:true` return object. Do NOT recompute per-return unless a return changes piece positions (none do).

    In `applyFreeMoveEnd` (from plan 17-04), add `offsidePieceIds: evaluateOffside(state)` to its returned PASS state.

    Add a one-line comment at each skipped dice-only transition site you encounter naming why offside is not recomputed there (no piece movement). Do NOT touch applyRoll's possession-delivery branches — the foul trigger on possession gain is OFFSIDE-02 (plan 17-06); this task only updates the sticky flag at movement-phase ends.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run offside gameEngine 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts imports `evaluateOffside`
    - Every `ok:true` return in `applyEndTurn` carries `offsidePieceIds: <evaluateOffside result>`
    - `applyFreeMoveEnd` return carries `offsidePieceIds`
    - A test drives `applyEndTurn` with a piece positioned past halfway / ahead of ball / ≤1 opponent ahead and asserts the returned `offsidePieceIds` contains that piece id; a follow-up `applyEndTurn` with the piece dropped behind the ball asserts the id is removed (sticky → cleared)
    - `pnpm --filter @counter-attack/server test --run offside gameEngine` exits 0
  </acceptance_criteria>
  <done>Offside is re-evaluated (sticky) at every movement-phase-ending transition in applyEndTurn and at applyFreeMoveEnd; engine tests confirm flag set, sticky persistence, and clear across successive end-turns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PieceOverlay double-width red ring + HexGrid wiring</name>
  <files>packages/client/src/components/PieceOverlay.tsx, packages/client/src/components/PieceOverlay.test.tsx, packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx (read the `Props` type ~lines 28-37, the component signature ~46-53, and the three existing selection rings ~228-271: 'selectable' #3b82f6 strokeWidth 2 r+2, 'active' #22c55e strokeWidth 2.5 r+4, 'activated' #f97316. The offside ring is a SEPARATE boolean-driven layer, NOT a selectionState member)
    - packages/client/src/components/PieceOverlay.test.tsx (read the render harness ~lines 90-110 and the existing ring assertions ~207-240 to mirror the test style for the new red ring)
    - packages/client/src/components/HexGrid.tsx (read the PieceOverlay render block ~lines 685-695; read how it accesses gameState fields like `attackingTeam`/`ball` to add `offsidePieceIds`)
  </read_first>
  <behavior>
    - `PieceOverlay` accepts a new `isOffside?: boolean` prop, default false.
    - When `isOffside` is true, the component renders an additional `<circle>` ring: a distinguishable deeper-red stroke (`#dc2626` — NOT `#ef4444`, which is already used by the unrelated away-team/role rect at PieceOverlay.tsx:~133; different shade keeps the offside ring distinct even though the shapes already differ), `strokeWidth` roughly double the existing selection rings (use 5), radius slightly outside the active ring (e.g. `PIECE_RADIUS + 6`), `fill="none"`, `pointerEvents="none"`.
    - The red ring renders regardless of `selectionState` — a piece can be simultaneously offside and selectable/active/activated (both rings visible).
    - HexGrid passes `isOffside={offsidePieceIds.includes(piece.id)}` reading `offsidePieceIds` from gameState (default `[]` when absent).
  </behavior>
  <action>
    Per D-25 (double-width red ring, independent layer):

    In `PieceOverlay.tsx`: add `isOffside?: boolean;` to the `Props` type (with a JSDoc citing OFFSIDE-01/D-25), destructure it in the component signature with a `= false` default. Add a new conditional ring block AFTER the three existing selection rings and BEFORE the ball-carrier indicator: `{isOffside && (<circle cx={cx} cy={cy} r={PIECE_RADIUS + 6} fill="none" stroke="#dc2626" strokeWidth={5} pointerEvents="none" />)}`. Do not fold it into the `selectionState` switch — it is an independent layer (a piece can be offside AND active).

    In `PieceOverlay.test.tsx`: add the new prop to the render harness (default false), and two `it` cases mirroring the existing ring tests: (a) `isOffside={true}` renders exactly one circle with stroke `#dc2626` and strokeWidth 5; (b) `isOffside={true}` together with `selectionState='active'` renders BOTH the green active ring AND the red offside ring (assert both strokes present).

    In `HexGrid.tsx`: read `offsidePieceIds` from gameState (alongside how `ball`/`attackingTeam` are read), defaulting to `[]`, and pass `isOffside={offsidePieceIds.includes(piece.id)}` to `<PieceOverlay>` in the render block.

  </action>
  <verify>
    <human-check>
      1. Start a match in two tabs. Maneuver an attacker past the half line, ahead of the ball, with ≤1 defender goal-side of them, and end the turn.
      2. Confirm that piece shows a thick red ring (clearly thicker than the blue/green selection rings) around its token, on both tabs.
      3. Select that same piece (if selectable) and confirm BOTH the selection ring and the red offside ring are visible at once.
      4. Move a second defender goal-side of the flagged attacker (so ≥2 opponents are level-or-ahead) and end the turn; confirm the red ring clears.
    </human-check>
    <automated>pnpm --filter @counter-attack/client test --run PieceOverlay 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `PieceOverlay.tsx` Props includes `isOffside?: boolean` and renders a deeper-red (#dc2626) strokeWidth-5 ring when true, as a layer independent of selectionState
    - `PieceOverlay.test.tsx` asserts the red ring renders for `isOffside` and coexists with the green active ring
    - `HexGrid.tsx` passes `isOffside={offsidePieceIds.includes(piece.id)}` from gameState
    - `pnpm --filter @counter-attack/client test --run PieceOverlay` exits 0
    - Human check confirms the thick red ring appears, coexists with selection rings, and clears per D-22
  </acceptance_criteria>
  <done>Flagged pieces render a distinct double-width red ring driven by `offsidePieceIds`, layered over any selection ring; component tests green and human verification confirms appear/coexist/clear behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify OFFSIDE-01 detection + red ring</name>
  <what-built>OFFSIDE-01 end-to-end detection: sticky server flag (evaluateOffside at every movement-phase end) + double-width red ring marker, team-relative across all pieces.</what-built>
  <how-to-verify>
    1. Two tabs, same room, start a match.
    2. As the attacking team, move a forward past the halfway line and ahead of the ball with only 0–1 opponents goal-side of them; End Turn. Confirm a thick red ring appears on that forward on BOTH tabs.
    3. Without clearing the condition, play another phase/turn — confirm the ring persists (sticky) even if the forward did not move.
    4. Drop the forward equal-to-or-behind the ball (or move a second defender goal-side so ≥2 opponents are level/ahead); End Turn. Confirm the ring clears.
    5. Push a DEFENDER past the ball with ≤1 of their opponents goal-side; End Turn. Confirm the defender is also flagged (rule is team-relative, all pieces).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                                                                                                                                                                     |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client→server (GAME_END_TURN / GAME_MOVE) | The client triggers phase transitions; the server recomputes `offsidePieceIds` authoritatively in `applyEndTurn`/`applyFreeMoveEnd`. The client receives `offsidePieceIds` only for rendering — it never sets or trusts a client-supplied flag. |
| server→client (GAME_STATE)                | `offsidePieceIds` is broadcast as part of GameState; the client renders the red ring from it but cannot influence flagging.                                                                                                                     |

## STRIDE Threat Register

| Threat ID  | Category          | Component                                               | Disposition | Mitigation Plan                                                                                                                                                  |
| ---------- | ----------------- | ------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-17-05-01 | Tampering         | offsidePieceIds in client-supplied state                | mitigate    | Flag is computed only inside server engine functions from server-held piece/ball positions; no client event writes offsidePieceIds. Client value is render-only. |
| T-17-05-02 | Spoofing          | Client renders a false offside ring                     | accept      | Cosmetic only — the ring has no game-state effect in this plan; the foul consequence (OFFSIDE-02) is server-authoritative and independent of the client ring.    |
| T-17-05-03 | Denial of Service | evaluateOffside O(n²) over pieces at every end-of-phase | accept      | n ≤ 22 pieces; the nested opposing-count scan is ≤ 484 comparisons per end-turn — negligible. No new unbounded loop or client-forced repetition introduced.      |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared exec tsc --noEmit` exits 0 (shared offside module + GameState field compile)
- `pnpm --filter @counter-attack/server test --run offside gameEngine` exits 0 (helpers + sticky end-of-phase wiring green)
- `pnpm --filter @counter-attack/client test --run PieceOverlay` exits 0 (red ring + coexistence)
- Human checkpoint confirms detection, stickiness, clear, and team-relative (defender) flagging
</verification>

<success_criteria>

- Pure team-relative offside helpers exist in shared and pass a boundary-covering unit suite (both directions, all three conditions, sticky, both clears)
- `GameState.offsidePieceIds` is recomputed (sticky) at every movement-phase-ending transition
- Flagged pieces render a distinct double-width red ring, independent of selection state, on both clients
  </success_criteria>

<output>
Create `.planning/phases/17-rule-bugs/17-05-SUMMARY.md` when done
</output>
