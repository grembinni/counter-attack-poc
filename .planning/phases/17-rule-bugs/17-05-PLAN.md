---
phase: 17-rule-bugs
plan: 05
type: execute
wave: 5
depends_on: ['17-01', '17-02', '17-04']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/gameHandlers.ts
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/HexGrid.tsx
autonomous: false
requirements: [PASS-02]
user_setup: []
must_haves:
  truths:
    - 'After a First-time Pass target is chosen, the attacker may move 1 non-passer player up to 1 hex before the defender responds'
    - 'The defender then moves 1 player up to 1 hex onto the pass path (reusing SNAP_DEFLECT)'
    - 'SNAP_DEFLECT resolution follows the pass path (not a shot path) when lastActionType is FIRST_TIME_PASS'
    - 'The pass path is highlighted throughout the attacker step and SNAP_DEFLECT phase'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'FIRST_TIME_PASS attacker-step intermediate state in applyRoll PASS case'
      contains: 'firstTimePassStep'
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'Attacker-step end-turn → SNAP_DEFLECT; SNAP_DEFLECT FIRST_TIME_PASS resolution discriminant'
      contains: 'FIRST_TIME_PASS'
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'Attacker repositioning prompt in PASS phase'
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: 'Pass-path highlight during attacker step + SNAP_DEFLECT'
  key_links:
    - from: 'applyRoll PASS case (FIRST_TIME_PASS)'
      to: 'firstTimePassPath / firstTimePassStep'
      via: 'intermediate PASS state via hexLine'
      pattern: "firstTimePassStep: 'ATTACKER'"
    - from: 'SNAP_DEFLECT end-turn handler'
      to: 'firstTimePassPath resolution'
      via: 'lastActionType discriminant'
      pattern: "lastActionType === 'FIRST_TIME_PASS'"
---

<objective>
Implement PASS-02: mid-pass player repositioning during a First-time Pass flight. After the pass target is chosen, the attacker moves 1 non-passer player up to 1 hex, then the defender moves 1 player up to 1 hex onto the pass path (reusing the existing SNAP_DEFLECT phase). Resolution follows the pass path: a defender on the path → LOOSE_BALL; otherwise the ball delivers (with BUG-04 occupied-hex pickup logic).

Purpose: Replace the long-standing TODO at gameEngine.ts:1148, completing the First-time Pass flow with the rulebook's mid-flight movement, while reusing SNAP_DEFLECT infrastructure rather than building a new phase.
Output: FIRST_TIME_PASS intermediate attacker-step state in `applyRoll`; attacker-step end-turn → SNAP_DEFLECT wiring; SNAP_DEFLECT resolution discriminant on `lastActionType`; ActionPanel attacker-step prompt; HexGrid pass-path highlight.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-rule-bugs/17-CONTEXT.md
@.planning/phases/17-rule-bugs/17-RESEARCH.md
@.planning/phases/17-rule-bugs/17-PATTERNS.md
</context>

<artifacts_this_phase_produces>

- FIRST_TIME_PASS intermediate-state branch in `applyRoll` PASS case (gameEngine.ts) setting `firstTimePassPath` + `firstTimePassStep` (fields from Plan 01)
- Attacker-step end-turn → SNAP_DEFLECT transition (gameHandlers.ts GAME_END_TURN, PASS phase + firstTimePassStep)
- SNAP_DEFLECT FIRST_TIME_PASS resolution discriminant (gameHandlers.ts GAME_END_TURN, SNAP_DEFLECT phase)
- Attacker-step render branch in PASS phase (ActionPanel.tsx)
- Pass-path highlight overlay for attacker step + SNAP_DEFLECT (HexGrid.tsx)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FIRST_TIME_PASS attacker-step intermediate state in applyRoll</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (read the PASS case of `applyRoll`: the TODO at ~line 1148 "FIRST_TIME_PLAYER_MOVES (PASS-02) deferred"; the surrounding delivery return; confirm the resolved-last-action-type variable and `hexLine` import; note the BUG-04 occupant logic added in Plan 02 lives just above)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (section "PASS-02: First-time Pass attacker repositioning sub-state" — exact intermediate-state shape)
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Code Examples PASS-02; Pattern 4; Open Question 1 recommendation to keep it in PASS phase via firstTimePassStep flag)
    - .planning/phases/17-rule-bugs/17-CONTEXT.md (D-17 through D-20)
  </read_first>
  <behavior>
    - When the resolved last-action-type is 'FIRST_TIME_PASS' and the accuracy roll passes (no interception, given BUG-01/interception already handled above), `applyRoll` returns an intermediate state: phase stays 'PASS', `firstTimePassStep: 'ATTACKER'`, `firstTimePassPath: hexLine(carrier.position, targetHex)`, `passTargetHex: targetHex` preserved, ball in flight (`carrierId: null`), `lastActionType: 'FIRST_TIME_PASS'`, action count incremented, interception dice cleared, eventLog updated.
    - STANDARD_PASS / LONG_BALL delivery is unchanged (falls through to the existing return / BUG-04 occupant return).
  </behavior>
  <action>
    Per D-17/D-20: replace the TODO at ~line 1148 with a branch `if (<resolvedLastActionType> === 'FIRST_TIME_PASS') { return {ok:true, state: {...}} }` that produces the intermediate state described in <behavior>, using `firstTimePassPath: hexLine(carrier.position, targetHex)` and `firstTimePassStep: 'ATTACKER'`. Keep ball in flight (`ball: { position: targetHex, carrierId: null }`) so no pickup happens yet. Leave the STANDARD_PASS / LONG_BALL return path (including the BUG-04 occupant pickup from Plan 02) as the fall-through for non-FIRST_TIME passes. Use the real local variable names (`carrier`, `targetHex`, `newLastActionType`, `passTimeCost`, `newEventLog`, `d1`) — confirm in source; do not invent.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run gameEngine</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts no longer contains the "FIRST_TIME_PLAYER_MOVES (PASS-02) deferred" TODO
    - applyRoll returns `firstTimePassStep: 'ATTACKER'` and a populated `firstTimePassPath` for a FIRST_TIME_PASS accurate delivery, phase staying 'PASS'
    - STANDARD_PASS delivery behavior is unchanged (regression tests pass)
    - Wave-0 PASS-02 engine test passes; `pnpm --filter @counter-attack/server test --run gameEngine` exits 0
  </acceptance_criteria>
  <done>First-time Pass enters the attacker repositioning sub-state with the pass path recorded; other pass types unaffected; engine tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Attacker-step end-turn → SNAP_DEFLECT + SNAP_DEFLECT FIRST_TIME_PASS resolution</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (read GAME_END_TURN ~lines 508+; the SNAP_DEFLECT end-turn resolution block ~lines 700-780 that uses `computeShotPathDeflection` with `hexLine(snapShooter.position, snapTarget)`; the GAME_MOVE SNAP_DEFLECT branch ~lines 339-391 which already limits the defender to 1 player move; the attacker repositioning during PASS step needs a 1-hex limit — read how movedPieceId/paceUsed is tracked for SNAP_DEFLECT and mirror it for the attacker step)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (sections "PASS-02: SNAP_DEFLECT end-turn discriminant" and the SNAP_DEFLECT analog)
    - .planning/phases/17-rule-bugs/17-RESEARCH.md (Pitfall 4: pass path vs shot path resolution; D-18 reuse SNAP_DEFLECT; D-20 attacker 1-hex limit, passer cannot move)
    - .planning/phases/17-rule-bugs/17-CONTEXT.md (D-17 step sequence, D-18, D-19, D-20)
  </read_first>
  <action>
    Per D-17/D-18/D-20:

    Attacker 1-hex repositioning step: in GAME_MOVE, when `phase === 'PASS' && firstTimePassStep === 'ATTACKER'`, allow the active (attacking) team to move exactly 1 non-passer player up to 1 hex. Reject moving the passer (the piece that made the pass — identify via the pass eventLog / carrier-at-pass-time) and reject a second piece or a >1-hex move. Track the moved piece similarly to the SNAP_DEFLECT defender-move tracking. Use `hexDistance(...) <= 1` for the limit. Broadcast after the move.

    In GAME_END_TURN, add/extend a branch so that when `phase === 'PASS' && firstTimePassStep === 'ATTACKER'`, the active team's End Turn transitions to `phase: 'SNAP_DEFLECT'` (defender's turn), clearing `firstTimePassStep` to null while PRESERVING `firstTimePassPath`, `passTargetHex`, and `lastActionType === 'FIRST_TIME_PASS'`, and switching `activeTeam` to the defending team. Reset the SNAP_DEFLECT per-move tracking fields (snapDeflectMovedPieceId/snapDeflectPaceUsed) to allow the defender's 1 move.

    SNAP_DEFLECT resolution discriminant: at the top of the SNAP_DEFLECT end-turn resolution block, branch on `room.gameState.lastActionType === 'FIRST_TIME_PASS'`. When true, resolve against `firstTimePassPath` (the pass path), NOT a shot path: if any defending non-GK piece sits on a `firstTimePassPath` hex → transition to LOOSE_BALL with the ball at that deflector's position; otherwise deliver the ball to `passTargetHex` using the same occupant/pickup logic as BUG-04 (occupant at target → pickup + possession transfer if defender; else loose/teammate), and clear `firstTimePassPath`/`passTargetHex`/`firstTimePassStep`. When `lastActionType !== 'FIRST_TIME_PASS'`, run the existing snapshot shot-path resolution unchanged.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test --run gameHandlers</automated>
  </verify>
  <acceptance_criteria>
    - GAME_MOVE PASS+ATTACKER step limits the move to 1 non-passer piece, ≤1 hex (rejects passer, rejects >1 hex, rejects 2nd piece)
    - GAME_END_TURN PASS+ATTACKER step transitions to SNAP_DEFLECT, preserves firstTimePassPath/passTargetHex/lastActionType, switches activeTeam to defender, resets SNAP_DEFLECT move tracking
    - SNAP_DEFLECT end-turn checks `lastActionType === 'FIRST_TIME_PASS'`: defender on path → LOOSE_BALL at deflector; clear path → delivery with BUG-04 pickup logic
    - Snapshot (non-FIRST_TIME_PASS) SNAP_DEFLECT resolution is unchanged (existing tests pass)
    - Wave-0 PASS-02 handler test passes; `pnpm --filter @counter-attack/server test --run gameHandlers` exits 0
  </acceptance_criteria>
  <done>Attacker repositions 1 hex, defender responds via SNAP_DEFLECT, and resolution follows the pass path for First-time Pass; snapshot path untouched; handler tests green.</done>
</task>

<task type="auto">
  <name>Task 3: ActionPanel attacker-step prompt + HexGrid pass-path highlight</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (read the PASS phase branch ~line 363+ and the SNAP_DEFLECT branch ~lines 154-170 for the prompt structure; read how `useGameStore` selectors subscribe to gameState fields)
    - packages/client/src/components/HexGrid.tsx (read the SNAP_DEFLECT pass/shot-path highlight logic — the existing highlight overlay for the snapshot path — and the isHeaderPass guard ~lines 322-325; identify the highlightType mechanism / HIGHLIGHT_STYLES used for path tints)
    - .planning/phases/17-rule-bugs/17-PATTERNS.md (sections "PASS-02: Attacker repositioning prompt in PASS phase" and the SNAP_DEFLECT highlight analog)
    - .planning/phases/17-rule-bugs/17-CONTEXT.md (D-19: pass path highlighted throughout attacker step + SNAP_DEFLECT; specifics: PASS-02 should feel like SNAP_DEFLECT)
  </read_first>
  <action>
    Per D-19:

    ActionPanel.tsx: subscribe to `gameState.firstTimePassStep`. Add a branch at the top of the PASS-phase block: when `phase === 'PASS' && firstTimePassStep === 'ATTACKER'`, render a prompt panel "Move 1 player up to 1 hex (not the passer), then End Turn." with an End Turn button (`emitEndTurn`) and the gameError display; show the waiting panel for the non-active team. Place this branch before the normal 3-step PASS chooser so it pre-empts it.

    HexGrid.tsx: subscribe to `gameState.firstTimePassPath`. Highlight every hex in `firstTimePassPath` using the same path-highlight overlay/highlightType the SNAP_DEFLECT snapshot path uses, active while `firstTimePassStep === 'ATTACKER'` OR `phase === 'SNAP_DEFLECT' && lastActionType === 'FIRST_TIME_PASS'`. Reuse the existing path-tint highlightType (do not introduce a new color); the goal is visual parity with the snapshot deflect path so the defender sees which hexes to target.

  </action>
  <verify>
    <human-check>
      1. As the attacker, execute a First-time Pass and choose a target. Confirm the pass path is highlighted on the board.
      2. Confirm ActionPanel shows "Move 1 player up to 1 hex (not the passer), then End Turn." and that you can move one non-passer player 1 hex (not the passer, not 2 hexes).
      3. End Turn → confirm it becomes the defender's SNAP_DEFLECT turn with the pass path still highlighted.
      4. Defender moves a player onto the path → confirm LOOSE_BALL at that hex; or moves off the path → confirm the ball delivers to the target.
    </human-check>
    <automated>pnpm --filter @counter-attack/client test --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - ActionPanel.tsx renders the attacker-step prompt when `phase === 'PASS' && firstTimePassStep === 'ATTACKER'`
    - HexGrid.tsx highlights `firstTimePassPath` hexes during the attacker step and during SNAP_DEFLECT when lastActionType is FIRST_TIME_PASS, reusing the existing path highlightType
    - `pnpm --filter @counter-attack/client test --run` exits 0
    - Human checks confirm path highlight persists across both steps and the 1-hex/non-passer attacker limit holds
  </acceptance_criteria>
  <done>The attacker sees the repositioning prompt and the pass path stays highlighted through the defender's SNAP_DEFLECT step; client tests green and human verification confirms the flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PASS-02 First-time Pass mid-flight repositioning end-to-end: attacker moves 1 non-passer ≤1 hex, defender responds via SNAP_DEFLECT, resolution follows the pass path, with path highlight throughout.</what-built>
  <how-to-verify>
    1. Two tabs, same room, start match. Get the ball to a player and execute a First-time Pass to a target hex.
    2. Confirm the pass path highlights and the attacker sees "Move 1 player up to 1 hex (not the passer)". Move a non-passer 1 hex; confirm you cannot move the passer or move 2 hexes. End Turn.
    3. Confirm the defender now has a SNAP_DEFLECT turn with the path still highlighted. Move a defender ONTO the path → confirm LOOSE_BALL spawns at that defender. Replay and move the defender OFF the path → confirm the ball delivers to the target (and is picked up if a player stands there, transferring possession if a defender).
    4. Confirm a snapshot (non-First-time-Pass) SNAP_DEFLECT still resolves as a shot duel (regression check).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @counter-attack/server test --run` exits 0 (PASS-02 engine + handler cases green; snapshot regression intact)
- `pnpm --filter @counter-attack/client test --run` exits 0
- Full suite green before phase verification: `pnpm --filter @counter-attack/server test --run && pnpm --filter @counter-attack/client test --run && pnpm --filter @counter-attack/shared test --run`
- Human checkpoint confirms the full First-time Pass flow and snapshot regression
</verification>

<success_criteria>

- First-time Pass enters attacker 1-hex repositioning, then defender SNAP_DEFLECT
- Resolution follows the pass path (LOOSE_BALL on path / delivery off path) when lastActionType is FIRST_TIME_PASS
- Pass path highlighted across both steps; snapshot deflect behavior unchanged
  </success_criteria>

<output>
Create `.planning/phases/17-rule-bugs/17-05-SUMMARY.md` when done
</output>
