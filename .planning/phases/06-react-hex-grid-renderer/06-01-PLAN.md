---
phase: 06-react-hex-grid-renderer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/pitch.ts
  - packages/shared/src/pitch.test.ts
  - packages/client/package.json
  - packages/client/tsconfig.json
  - packages/client/vite.config.ts
  - packages/client/vitest.config.ts
  - packages/client/index.html
  - packages/client/src/main.tsx
  - packages/client/src/utils/hexToPixel.ts
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/store/useGameStore.test.ts
  - packages/client/src/mock/index.ts
  - packages/client/src/mock/mockMovementState.ts
  - packages/client/src/mock/mockPassState.ts
  - packages/client/src/mock/mockShotState.ts
  - packages/client/src/mock/mockGKRestartState.ts
autonomous: true
requirements:
  - PITCH-04
  - PITCH-05
  - UX-01
  - UX-02
  - UX-03
  - UX-04

must_haves:
  truths:
    - 'pnpm --filter @counter-attack/shared test passes after pitch.ts replacement (no 25×16 coordinates remain)'
    - 'PITCH_HEXES.length === 962 (37×26 grid, per D-04)'
    - 'PITCH_REGIONS.kickOffHex === { q: 18, r: 13 } (per D-05)'
    - 'homeGoal and awayGoal regions exist in PitchRegions type and PITCH_REGIONS'
    - 'pnpm --filter @counter-attack/client build exits 0 (Vite scaffold compiles)'
    - 'pnpm --filter @counter-attack/client test passes (Zustand store selectPiece/movePiece unit tests green)'
    - 'hexToPixel.ts exports axialToPixel, hexPolygonPoints, computeViewBox, HEX_SIZE'
    - 'useGameStore exports a Zustand 4.x store with gameState, screen, selectedPieceId, validMoveHexes, setScreen, selectPiece, movePiece'
  artifacts:
    - path: packages/shared/src/pitch.ts
      provides: 37x26 pitch grid with D-05 region boundaries
      contains: 'PITCH_HEXES.length === 962'
    - path: packages/shared/src/pitch.test.ts
      provides: Rewritten tests for 37x26 grid
      contains: 'toHaveLength(962)'
    - path: packages/client/package.json
      provides: React 18, Vite 5, Zustand 4 dependencies
      contains: 'react'
    - path: packages/client/vite.config.ts
      provides: Vite config with react plugin
      contains: 'plugin-react'
    - path: packages/client/src/utils/hexToPixel.ts
      provides: axialToPixel formula per D-03
      contains: 'axialToPixel'
    - path: packages/client/src/store/useGameStore.ts
      provides: Zustand 4.x curried store
      contains: 'create<GameStore>()'
    - path: packages/client/src/store/useGameStore.test.ts
      provides: Unit tests for selectPiece/movePiece
      contains: 'selectPiece'
    - path: packages/client/src/mock/mockMovementState.ts
      provides: MOVEMENT phase mock GameState
      contains: 'MOVEMENT'
  key_links:
    - from: packages/client/src/store/useGameStore.ts
      to: packages/shared/src/moveValidator.ts
      via: validateMove import from @counter-attack/shared
      pattern: 'validateMove'
    - from: packages/client/src/store/useGameStore.ts
      to: packages/client/src/mock/mockMovementState.ts
      via: mockMovementState as initial gameState
      pattern: 'mockMovementState'
---

<objective>
Replace the placeholder 25×16 pitch with the real 37×26 grid (D-04, D-05), rewrite pitch.test.ts for the new boundaries, and bootstrap the React client scaffold with Vite 5 + Zustand 4 store + hexToPixel utility + all 4 mock states.

Purpose: This wave produces the data foundation (pitch constants) and client infrastructure (package deps, store, utility) on which Wave 2 rendering depends. Nothing renders until this plan's artifacts exist.

Output: Updated pitch.ts (962 hexes, real regions), rewritten pitch.test.ts (all green), client package.json with pinned deps, vite.config.ts, tsconfig.json, vitest.config.ts, index.html, main.tsx, hexToPixel.ts, useGameStore.ts + tests, and 4 mock state files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-react-hex-grid-renderer/06-CONTEXT.md
@.planning/phases/06-react-hex-grid-renderer/06-RESEARCH.md
@.planning/phases/06-react-hex-grid-renderer/06-PATTERNS.md
@.planning/phases/06-react-hex-grid-renderer/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace pitch.ts (37×26) and rewrite pitch.test.ts</name>
  <files>packages/shared/src/pitch.ts, packages/shared/src/pitch.test.ts</files>
  <read_first>
    - packages/shared/src/pitch.ts — full current content; understand the structure to preserve (hexKey, buildRegion, PitchRegions type, isInRegion, isDifficultAngle, isPitchHex functions)
    - packages/shared/src/pitch.test.ts — all 11 existing test cases with hardcoded 25×16 coordinates; every assertion must be rewritten
    - packages/shared/src/hex.ts — hexesInRange signature (used for centreCircle region)
    - packages/shared/src/types.ts — HexCoord type shape
    - .planning/phases/06-react-hex-grid-renderer/06-CONTEXT.md — D-04, D-05 (exact region boundaries)
    - .planning/phases/06-react-hex-grid-renderer/06-PATTERNS.md — pitch.ts replacement section with key-changes table
  </read_first>
  <behavior>
    - PITCH_HEXES contains exactly 962 hexes (37 columns q∈[0,36] × 26 rows r∈[0,25])
    - isInRegion({q:5, r:12}, 'homeThird') === true (q≤10 per D-05)
    - isInRegion({q:11, r:12}, 'homeThird') === false (q=11 is middleThird boundary)
    - isInRegion({q:30, r:12}, 'awayThird') === true (q≥26 per D-05)
    - isInRegion({q:25, r:12}, 'awayThird') === false
    - isInRegion({q:3, r:10}, 'homePenaltyArea') === true (q∈[0,5] r∈[5,19])
    - isInRegion({q:33, r:10}, 'awayPenaltyArea') === true (q∈[31,36] r∈[5,19])
    - isInRegion({q:0, r:12}, 'homeGoal') === true (q=0 r∈[9,15])
    - isInRegion({q:1, r:12}, 'homeGoal') === false
    - isInRegion({q:36, r:12}, 'awayGoal') === true (q=36 r∈[9,15])
    - isInRegion({q:18, r:13}, 'centreCircle') === true (kickoff hex in circle)
    - PITCH_REGIONS.kickOffHex equals {q:18, r:13}
    - isDifficultAngle({q:3, r:7}) === true (near penalty corner, home end)
    - isDifficultAngle({q:18, r:13}) === false (kickoff hex is not difficult-angle)
    - DIFFICULT_ANGLE_HEXES.size === 16
    - isPitchHex({q:36, r:25}) === true
    - isPitchHex({q:37, r:0}) === false
    - isPitchHex({q:0, r:26}) === false
  </behavior>
  <action>
    Replace packages/shared/src/pitch.ts with the 37×26 grid per D-04 and D-05. Preserve all function signatures and module structure exactly — only the constant values change.

    PITCH_HEXES: Generate via nested loop q∈[0,36], r∈[0,25] (962 hexes total). Remove the placeholder comment block.

    PitchRegions type: Add homeGoal and awayGoal as ReadonlySet<string> fields. Keep kickOffHex as HexCoord. Update the isInRegion return type's keyof Omit to include the new fields.

    PITCH_REGIONS constants per D-05:
    - homeThird: q∈[0,10] (filter PITCH_HEXES where h.q <= 10)
    - middleThird: q∈[11,25] (h.q >= 11 && h.q <= 25)
    - awayThird: q∈[26,36] (h.q >= 26)
    - homePenaltyArea: q∈[0,5] r∈[5,19] (h.q <= 5 && h.r >= 5 && h.r <= 19)
    - awayPenaltyArea: q∈[31,36] r∈[5,19] (h.q >= 31 && h.r >= 5 && h.r <= 19)
    - homeSixYardBox: q∈[0,1] r∈[8,17] (h.q <= 1 && h.r >= 8 && h.r <= 17)
    - awaySixYardBox: q∈[35,36] r∈[8,17] (h.q >= 35 && h.r >= 8 && h.r <= 17)
    - homeGoal: q=0 r∈[9,15] (h.q === 0 && h.r >= 9 && h.r <= 15)
    - awayGoal: q=36 r∈[9,15] (h.q === 36 && h.r >= 9 && h.r <= 15)
    - centreCircle: buildRegion(hexesInRange({q:18, r:13}, 3))
    - kickOffHex: {q:18, r:13}

    DIFFICULT_ANGLE_HEXES: Use 16 hexes approximated from Counter Attack rules (penalty-area corner positions on the 37×26 grid). Docs/board-photo.jpg does not exist — use these reasonable approximations, noted as TODO for board-photo verification:
    Home end (near penalty area corners, inside but near the edge of the penalty box):
    - {q:3, r:6}, {q:4, r:6}, {q:3, r:7}, {q:4, r:7}  (upper/near-post side)
    - {q:3, r:17}, {q:4, r:17}, {q:3, r:18}, {q:4, r:18}  (lower/far-post side)
    Away end (mirror):
    - {q:32, r:6}, {q:33, r:6}, {q:32, r:7}, {q:33, r:7}
    - {q:32, r:17}, {q:33, r:17}, {q:32, r:18}, {q:33, r:18}
    Add a comment: "TODO: Verify against docs/board-photo.jpg when available (D-06)"

    isInRegion function: Update the keyof Omit<PitchRegions, 'kickOffHex'> — homeGoal and awayGoal are now valid region names. No logic change; function still calls PITCH_REGIONS[region].has(hexKey(hex)).

    Rewrite packages/shared/src/pitch.test.ts completely. All imports use './pitch.js'. Tests must cover:
    - PITCH_HEXES.length === 962
    - kickOffHex equals {q:18, r:13}
    - isInRegion truths for homeThird (q≤10), awayThird (q≥26), homePenaltyArea, awayPenaltyArea, homeGoal (q=0 r=12), awayGoal (q=36 r=12)
    - isInRegion false cases for boundary conditions (q=11 not homeThird, q=25 not awayThird, q=1 r=12 not homeGoal)
    - centreCircle contains kickOffHex
    - isDifficultAngle true for one known difficult-angle hex ({q:3, r:7}), false for kickoff hex
    - DIFFICULT_ANGLE_HEXES.size === 16
    - isPitchHex true for {q:36, r:25}, false for {q:37, r:0}, false for {q:0, r:26}

    Run pnpm --filter @counter-attack/shared test after writing. All tests must pass before proceeding.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test</automated>
  </verify>
  <done>pnpm --filter @counter-attack/shared test exits 0 with all pitch.test.ts assertions passing. PITCH_HEXES.length is 962. PITCH_REGIONS.kickOffHex is {q:18, r:13}. homeGoal and awayGoal are valid isInRegion keys. DIFFICULT_ANGLE_HEXES.size is 16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Client scaffold — package.json, tsconfig, vite.config, vitest.config, index.html, main.tsx</name>
  <files>
    packages/client/package.json,
    packages/client/tsconfig.json,
    packages/client/vite.config.ts,
    packages/client/vitest.config.ts,
    packages/client/index.html,
    packages/client/src/main.tsx
  </files>
  <read_first>
    - packages/client/package.json — current placeholder; understand existing fields to preserve name/version/private/type
    - packages/client/tsconfig.json — current content; only "types": ["vite/client"] needs adding
    - packages/server/package.json — monorepo package conventions (private:true, type:module, script names)
    - packages/shared/vitest.config.ts — vitest config pattern to adapt
    - .planning/phases/06-react-hex-grid-renderer/06-RESEARCH.md — Pattern 1 (Vite config), exact version numbers for all packages, Pitfalls 1 (Zustand 5) and 2 (React 19)
    - .planning/phases/06-react-hex-grid-renderer/06-PATTERNS.md — package.json, tsconfig.json, vitest.config.ts, main.tsx sections
    - tsconfig.base.json — strict flags inherited (verbatimModuleSyntax, exactOptionalPropertyTypes)
  </read_first>
  <behavior>
    This task creates/updates config files only — no logic to test. Verification is via successful build.
  </behavior>
  <action>
    Update packages/client/package.json. Keep name "@counter-attack/client", version "0.0.1", private true, type "module". Replace scripts: dev → "vite", build → "vite build", preview → "vite preview", typecheck → "tsc --noEmit", test → "vitest run". Add to dependencies: "@counter-attack/shared": "workspace:*", "react": "^18.3.1", "react-dom": "^18.3.1", "zustand": "^4.5.7", "honeycomb-grid": "^4.1.5", "socket.io-client": "^4.8.3". Add to devDependencies: "vite": "^5.4.21", "@vitejs/plugin-react": "^4.7.0", "@types/react": "^18.3.1", "@types/react-dom": "^18.3.1", "vitest": "^2.1.9", "jsdom": "^25.0.0", "@testing-library/react": "^14.3.1". CRITICAL: Pin react and react-dom to ^18.3.1 (NOT latest which is v19). Pin zustand to ^4.5.7 (NOT latest which is v5).

    Update packages/client/tsconfig.json. Add "types": ["vite/client"] inside compilerOptions. All other fields remain identical to the current file. This one addition enables CSS Module import typing without TS errors.

    Create packages/client/vite.config.ts:
    - import { defineConfig } from 'vite'
    - import react from '@vitejs/plugin-react'
    - export default defineConfig({ plugins: [react()], build: { outDir: 'dist', sourcemap: true } })
    - No resolve.alias needed — @counter-attack/shared resolves via workspace:* + exports field

    Create packages/client/vitest.config.ts:
    - import { defineConfig } from 'vitest/config'
    - export default defineConfig({ test: { include: ['src/**/*.test.{ts,tsx}'], environment: 'jsdom' } })
    - Uses jsdom (not node) for React/DOM test environment

    Create packages/client/index.html at the package root (sibling to package.json, not inside src/):
    - Standard Vite HTML entry: DOCTYPE html, lang="en", charset UTF-8, viewport meta, title "Counter Attack"
    - Body contains: div id="root", script type="module" src="/src/main.tsx"

    Create packages/client/src/main.tsx (replace existing main.ts — delete or overwrite):
    - Import StrictMode from 'react'
    - Import { createRoot } from 'react-dom/client'
    - Import { App } from './App.js'
    - Get container via document.getElementById('root'); throw if null
    - Call createRoot(container).render(StrictMode wrapped App)
    - Named export pattern: no default exports

    Create a minimal packages/client/src/App.tsx stub (needed for main.tsx to compile):
    - Single named export: export function App() returning JSX div with text "Counter Attack — Loading..."
    - Import styles from './App.module.css'
    - Create packages/client/src/App.module.css with empty/minimal content (just a .app class)
    - This stub is replaced in Plan 06-03

    After creating all files, run pnpm install from the repo root to resolve new deps, then pnpm --filter @counter-attack/shared build (required before client can resolve the workspace dep), then pnpm --filter @counter-attack/client build to verify Vite compiles without errors.

  </action>
  <verify>
    <automated>pnpm install && pnpm --filter @counter-attack/shared build && pnpm --filter @counter-attack/client build</automated>
  </verify>
  <done>pnpm --filter @counter-attack/client build exits 0. packages/client/tsconfig.json contains "types": ["vite/client"]. package.json contains react@^18.3.1 and zustand@^4.5.7 (not v19 or v5). vite.config.ts contains plugins: [react()]. index.html contains div id="root" and script src="/src/main.tsx".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: hexToPixel utility + Zustand store + mock states + store unit tests</name>
  <files>
    packages/client/src/utils/hexToPixel.ts,
    packages/client/src/store/useGameStore.ts,
    packages/client/src/store/useGameStore.test.ts,
    packages/client/src/mock/index.ts,
    packages/client/src/mock/mockMovementState.ts,
    packages/client/src/mock/mockPassState.ts,
    packages/client/src/mock/mockShotState.ts,
    packages/client/src/mock/mockGKRestartState.ts
  </files>
  <read_first>
    - packages/shared/src/types.ts — full GameState shape (every field is required; exactOptionalPropertyTypes is enabled so optional fields must be explicitly undefined or omitted)
    - packages/shared/src/teams.ts — HOME_SQUAD and AWAY_SQUAD; understand all position values (currently set to 25×16 placeholder positions; mock states must update positions to fit inside 37×26 grid q∈[0,36] r∈[0,25])
    - packages/shared/src/moveValidator.ts — validateMove(state: GameState, piece: PlayerPiece, to: HexCoord) exact signature; second argument is PlayerPiece not pieceId
    - packages/shared/src/hex.ts — hexesInRange signature
    - .planning/phases/06-react-hex-grid-renderer/06-RESEARCH.md — Pattern 2 (Zustand store), Pattern 3 (hexToPixel), Pitfall 7 (validateMove second arg), Pitfall 5 (SVG origin clipping), Pitfall 6 (re-renders)
    - .planning/phases/06-react-hex-grid-renderer/06-PATTERNS.md — hexToPixel.ts, useGameStore.ts, mockMovementState.ts sections
    - .planning/phases/06-react-hex-grid-renderer/06-UI-SPEC.md — Store shape, interaction contract
    - packages/shared/src/moveValidator.test.ts — baseState fixture shape (the exact GameState structure with all required fields) — READ lines 5-40
  </read_first>
  <behavior>
    hexToPixel:
    - axialToPixel(0, 0, 20) returns {cx:0, cy:0}
    - axialToPixel(1, 0, 20) returns {cx:30, cy:~17.32} (within 0.01 tolerance)
    - axialToPixel(36, 25, 20): cx = 20 * 1.5 * 36 = 1080; cy = 20 * (√3/2 * 36 + √3 * 25) ≈ 1489.6
    - hexPolygonPoints(0, 0, 20) returns string with 6 "x,y" pairs joined by spaces
    - computeViewBox(20) returns string starting "0 0 "

    useGameStore (selectPiece/movePiece logic — key business rules):
    - selectPiece with an id whose piece exists sets selectedPieceId to that id and populates validMoveHexes
    - selectPiece with the same id again (toggle) clears selectedPieceId and validMoveHexes to []
    - selectPiece never iterates all 962 PITCH_HEXES — uses hexesInRange(piece.position, piece.pace) to limit candidates
    - validateMove is called with (gameState, piece, hex) where piece is looked up by id first
    - movePiece updates the piece's position in gameState.pieces, clears selectedPieceId, clears validMoveHexes
    - movePiece does nothing if selectedPieceId is null
    - setScreen updates the screen field

  </behavior>
  <action>
    Create packages/client/src/utils/hexToPixel.ts as a pure-math utility with no imports:
    - Export const HEX_SIZE = 20
    - Export function axialToPixel(q: number, r: number, hexSize: number = HEX_SIZE): { cx: number; cy: number } implementing the D-03 formula: cx = hexSize * (3/2) * q; cy = hexSize * ((Math.sqrt(3)/2) * q + Math.sqrt(3) * r)
    - Export function hexPolygonPoints(cx: number, cy: number, hexSize: number = HEX_SIZE): string iterating 6 flat-top angles (0°, 60°, 120°, 180°, 240°, 300°) and joining as "x,y" pairs with spaces
    - Export function computeViewBox(hexSize: number = HEX_SIZE): string computing max pixel coords at q=36,r=25 plus 2×hexSize padding; returns "0 0 {width} {height}"
    - Apply SVG origin pitfall fix: computeViewBox returns viewBox starting at 0 0 with the HexGrid component expected to use a <g transform="translate({HEX_SIZE}, {HEX_SIZE * √3/2})"> wrapper to offset the origin so q=0,r=0 hex is not clipped
    - All functions have JSDoc citing CONTEXT.md D-03 and redblobgames.com/grids/hexagons/

    Create packages/client/src/mock/mockMovementState.ts:
    - Import type { GameState } from '@counter-attack/shared'
    - Import { HOME_SQUAD, AWAY_SQUAD } from '@counter-attack/shared'
    - HOME_SQUAD and AWAY_SQUAD have placeholder positions (25×16 grid). Override positions for each piece so they fit inside the 37×26 grid and represent a realistic formation. Home team: GK at q=1,r=13; DEFs at q=4,r=7 q=5,r=10 q=5,r=16 q=4,r=19; MIDs at q=9,r=8 q=10,r=13 q=9,r=18; FWDs at q=14,r=9 q=15,r=13 q=14,r=17. Away team mirror (subtract from q=36): GK at q=35,r=13; DEFs at q=32,r=7 q=31,r=10 q=31,r=16 q=32,r=19; MIDs at q=27,r=8 q=26,r=13 q=27,r=18; FWDs at q=22,r=9 q=21,r=13 q=22,r=17.
    - Spread HOME_SQUAD and AWAY_SQUAD but override positions with the above values using pieces.map(). The mock state must use real piece attribute values from HOME_SQUAD/AWAY_SQUAD — do not re-hardcode attributes.
    - Export const mockMovementState: GameState = { roomCode: 'MOCK1', phase: 'MOVEMENT', activeTeam: 'home', attackingTeam: 'home', pieces: [...mapped home pieces, ...mapped away pieces], ball: { position: { q: 18, r: 13 }, carrierId: 'home-9' }, score: { home: 0, away: 0 }, actionCount: 3, half: 1, eventLog: [], refereeCard: { leniency: 4 }, movedPieceIds: [], paceUsedByPieceId: {}, movementSlot: 'ATTACKER_4', lastDiceRoll: null, pendingFreeMove: null }
    - exactOptionalPropertyTypes is enabled: pendingFreeMove and lastDiceRoll must be null (not omitted) to satisfy the type

    Create packages/client/src/mock/mockPassState.ts:
    - Same structure as mockMovementState but phase: 'PASS', movementSlot: null, actionCount: 4
    - Include at least one ActionEvent in eventLog: { type: 'MOVE', pieceId: 'home-9', from: {q:14,r:13}, to: {q:15,r:13}, slot: 'ATTACKER_4', timestamp: Date.now() }
    - Ball position at {q:15, r:13}, carrierId: 'home-9'

    Create packages/client/src/mock/mockShotState.ts:
    - phase: 'SHOT', movementSlot: null, actionCount: 5
    - Ball inside away penalty area: position {q:33, r:13}, carrierId: 'home-9'
    - Include a MOVE event in eventLog

    Create packages/client/src/mock/mockGKRestartState.ts:
    - phase: 'GK_RESTART', movementSlot: null, actionCount: 6
    - Ball at away goal: position {q:36, r:13}, carrierId: 'away-0'
    - lastDiceRoll: { rolls: [4, 3], context: 'SHOT_DUEL' }
    - Include DICE_ROLL and GOAL events in eventLog

    Create packages/client/src/mock/index.ts as a barrel export:
    - export { mockMovementState } from './mockMovementState.js'
    - export { mockPassState } from './mockPassState.js'
    - export { mockShotState } from './mockShotState.js'
    - export { mockGKRestartState } from './mockGKRestartState.js'

    Create packages/client/src/store/useGameStore.ts:
    - Import { create } from 'zustand'
    - import type { GameState, HexCoord } from '@counter-attack/shared'
    - import { validateMove, hexesInRange } from '@counter-attack/shared' (value import, not type-only)
    - import { mockMovementState } from '../mock/index.js'
    - Declare Screen type as 'CREATE_ROOM' | 'JOIN_ROOM' | 'WAITING' | 'GAME_BOARD'
    - Declare GameStore type with fields: gameState: GameState; screen: Screen; selectedPieceId: string | null; validMoveHexes: HexCoord[]; and actions: setScreen(s: Screen): void; selectPiece(id: string): void; movePiece(targetHex: HexCoord): void
    - Use curried TypeScript form: export const useGameStore = create<GameStore>()((set, get) => ({ ... }))
    - Initial state: gameState: mockMovementState, screen: 'CREATE_ROOM', selectedPieceId: null, validMoveHexes: []
    - selectPiece(id): if selectedPieceId === id, toggle off (set null/[]); otherwise look up piece via gameState.pieces.find(p => p.id === id); if no piece return; compute candidates via hexesInRange(piece.position, piece.pace); filter via validateMove(gameState, piece, hex).ok; set selectedPieceId and validMoveHexes
    - movePiece(targetHex): if no selectedPieceId return; map pieces replacing the selected piece's position; set new gameState with updated pieces; clear selectedPieceId and validMoveHexes
    - setScreen(s): set({ screen: s })

    Create packages/client/src/store/useGameStore.test.ts:
    - import { describe, it, expect, beforeEach } from 'vitest'
    - import { useGameStore } from './useGameStore.js'
    - Reset store state between tests using beforeEach(() => useGameStore.setState({ gameState: mockMovementState, selectedPieceId: null, validMoveHexes: [] }))
    - Test selectPiece: calling selectPiece('home-8') sets selectedPieceId to 'home-8' and populates validMoveHexes with at least 1 hex (home-8 has pace:8 and is not adjacent to opponent at start)
    - Test selectPiece toggle: calling selectPiece('home-8') twice clears selection
    - Test movePiece: after selectPiece, validMoveHexes[0] exists; calling movePiece(validMoveHexes[0]) updates the piece position in gameState.pieces, clears selectedPieceId to null, clears validMoveHexes to []
    - Test movePiece with no selection: movePiece({q:18,r:13}) does nothing when selectedPieceId is null
    - Test setScreen: setScreen('GAME_BOARD') sets screen to 'GAME_BOARD'
    - Run pnpm --filter @counter-attack/client test to verify all tests pass

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test</automated>
  </verify>
  <done>pnpm --filter @counter-attack/client test exits 0. useGameStore.test.ts passes all 5 tests. hexToPixel.ts exports axialToPixel, hexPolygonPoints, computeViewBox, HEX_SIZE. All 4 mock state files export named constants typed as GameState. mock/index.ts barrel-exports all 4. useGameStore uses create<GameStore>()() curried form.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                               |
| ---------------------------- | ------------------------------------------------------------------------- |
| User input → LobbyScreen     | Room code input field in Join Room screen — untrusted string, max 6 chars |
| ActionEvent data → ActionLog | ActionEvent union from mock GameState rendered in the action log panel    |

## STRIDE Threat Register

| Threat ID | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                                     |
| --------- | ---------------------- | -------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-06-01   | Tampering              | ActionLog.tsx ActionEvent rendering          | mitigate    | Use JSX string interpolation only — never dangerouslySetInnerHTML on any ActionEvent field (enforced by React auto-escaping)        |
| T-06-02   | Information Disclosure | navigator.clipboard.writeText in LobbyScreen | accept      | Requires HTTPS in production (fine for localhost dev); flagged for Phase 9 deployment (ARCH-06)                                     |
| T-06-03   | Spoofing               | Room code input in JoinRoom screen           | mitigate    | Apply .toUpperCase().slice(0,6) transform on every keystroke; no server call in Phase 6 so no server-side validation risk           |
| T-06-SC   | Tampering              | npm package installs                         | mitigate    | All packages verified via slopcheck audit in 06-RESEARCH.md — all [OK] or false-positive [SUS] (vitest); no [SLOP] packages present |

</threat_model>

<verification>
After all three tasks complete:

1. `pnpm --filter @counter-attack/shared test` exits 0 — pitch.test.ts all green with 37×26 values
2. `pnpm --filter @counter-attack/client test` exits 0 — useGameStore.test.ts all green
3. `pnpm --filter @counter-attack/client build` exits 0 — Vite compiles TypeScript cleanly
4. `pnpm -r test` exits 0 — full monorepo test suite green
5. PITCH_HEXES.length === 962 (verify in REPL or test output)
6. packages/client/package.json contains react@^18.3.1 and zustand@^4.5.7 (grep to confirm not v19/v5)
   </verification>

<success_criteria>

1. packages/shared/src/pitch.ts encodes the 37×26 grid with all D-05 region boundaries including homeGoal and awayGoal
2. packages/shared/src/pitch.test.ts passes with 962-hex assertions, {q:18,r:13} kickoff, and new region boundary tests
3. packages/client builds with React 18 + Vite 5 + Zustand 4 (pinned versions, not npm latest)
4. useGameStore.ts implements selectPiece (toggle-deselect, validateMove-based candidate filtering via hexesInRange) and movePiece (position update + clear selection)
5. All 4 mock states export a complete GameState with every required field (no undefined on required fields)
6. pnpm -r test exits 0
   </success_criteria>

<output>
Create `.planning/phases/06-react-hex-grid-renderer/06-01-SUMMARY.md` when done.
</output>
