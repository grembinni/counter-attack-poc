---
phase: 06-react-hex-grid-renderer
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/HexGrid.module.css
  - packages/client/src/components/HexCell.tsx
  - packages/client/src/components/HexCell.module.css
  - packages/client/src/components/PieceOverlay.tsx
  - packages/client/src/components/BallMarker.tsx
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/GameBoard.module.css
autonomous: true
requirements:
  - PITCH-04
  - PITCH-05
  - UX-02

must_haves:
  truths:
    - 'HexGrid renders an <svg> element with exactly 962 <polygon> children (one per PITCH_HEXES entry)'
    - 'PieceOverlay renders at the correct hex pixel coordinate for each PlayerPiece'
    - 'BallMarker renders as a visually distinct off-white circle at ball.position hex'
    - 'Clicking an own-team piece in MOVEMENT phase triggers selectPiece and renders validMoveHexes as gold highlight overlays'
    - 'Clicking a highlighted destination hex triggers movePiece and the piece moves'
    - 'Clicking a non-highlighted hex produces no effect'
    - 'All SVG elements (HexCell polygons, PieceOverlay circles, BallMarker) are children of the same <svg> root'
    - 'pnpm --filter @counter-attack/client build exits 0'
  artifacts:
    - path: packages/client/src/components/HexGrid.tsx
      provides: SVG root with 962 HexCell children + PieceOverlay + BallMarker
      contains: 'PITCH_HEXES'
    - path: packages/client/src/components/HexCell.tsx
      provides: Single hex polygon + highlight overlay + difficult-angle dot
      contains: 'hexPolygonPoints'
    - path: packages/client/src/components/PieceOverlay.tsx
      provides: SVG circle + label for one PlayerPiece
      contains: 'axialToPixel'
    - path: packages/client/src/components/BallMarker.tsx
      provides: SVG circle for ball position
      contains: 'axialToPixel'
    - path: packages/client/src/components/GameBoard.tsx
      provides: Pitch container + sidebar layout shell
      contains: 'HexGrid'
  key_links:
    - from: packages/client/src/components/HexGrid.tsx
      to: packages/client/src/store/useGameStore.ts
      via: Zustand selector subscriptions for pieces, ball, validMoveHexes, selectedPieceId
      pattern: 'useGameStore'
    - from: packages/client/src/components/HexCell.tsx
      to: packages/client/src/store/useGameStore.ts
      via: onClick calls selectPiece or movePiece from store
      pattern: 'selectPiece|movePiece'
    - from: packages/client/src/components/HexGrid.tsx
      to: packages/shared/src/pitch.ts
      via: PITCH_HEXES iteration
      pattern: 'PITCH_HEXES'
---

<objective>
Build the SVG hex pitch rendering layer: HexGrid (SVG container), HexCell (individual hex polygon with fill states and highlight), PieceOverlay (piece circle + label), BallMarker (ball circle), and the GameBoard layout shell connecting pitch + sidebar.

Purpose: This plan produces the visible, interactive game board. A user opening the dev server after this plan will see the full 37×26 pitch with pieces and ball, and can click to select pieces and see valid-move highlights.

Output: 8 files — HexGrid.tsx, HexGrid.module.css, HexCell.tsx, HexCell.module.css, PieceOverlay.tsx, BallMarker.tsx, GameBoard.tsx, GameBoard.module.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-react-hex-grid-renderer/06-CONTEXT.md
@.planning/phases/06-react-hex-grid-renderer/06-RESEARCH.md
@.planning/phases/06-react-hex-grid-renderer/06-PATTERNS.md
@.planning/phases/06-react-hex-grid-renderer/06-UI-SPEC.md
@.planning/phases/06-react-hex-grid-renderer/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HexCell and HexGrid SVG pitch rendering</name>
  <files>
    packages/client/src/components/HexCell.tsx,
    packages/client/src/components/HexCell.module.css,
    packages/client/src/components/HexGrid.tsx,
    packages/client/src/components/HexGrid.module.css
  </files>
  <read_first>
    - packages/client/src/utils/hexToPixel.ts — axialToPixel, hexPolygonPoints, computeViewBox, HEX_SIZE (outputs of Plan 01 Task 3)
    - packages/shared/src/pitch.ts — PITCH_HEXES (962 hexes), isInRegion, isDifficultAngle, PITCH_REGIONS (for centreCircle + kickOffHex)
    - packages/shared/src/types.ts — HexCoord, PlayerPiece, BallState, GameState
    - packages/client/src/store/useGameStore.ts — selectPiece, movePiece, validMoveHexes, selectedPieceId, gameState selectors
    - .planning/phases/06-react-hex-grid-renderer/06-UI-SPEC.md — Hex Grid Visual Spec (fill colors, highlight colors, overlay elements, half-hex clipPath), Piece Overlay Spec, BallMarker Spec, Color tables, Layout spec
    - .planning/phases/06-react-hex-grid-renderer/06-PATTERNS.md — HexCell component pattern, HexGrid component pattern (z-order anti-pattern note: all elements in same SVG)
    - .planning/phases/06-react-hex-grid-renderer/06-RESEARCH.md — Pitfall 5 (SVG origin clipping), Pitfall 6 (re-renders), anti-patterns (separate SVG for overlays is forbidden)
  </read_first>
  <action>
    Create packages/client/src/components/HexCell.tsx:
    - Named export: export function HexCell({ hex, isHighlighted, onClick }: Props)
    - Props type: hex: HexCoord; isHighlighted: boolean; onClick: () => void
    - Compute pixel coords via axialToPixel(hex.q, hex.r) and polygon points via hexPolygonPoints(cx, cy) (both from hexToPixel.js)
    - Determine isGoal: check hex.q === 0 && hex.r >= 9 && hex.r <= 15 (homeGoal) OR hex.q === 36 && hex.r >= 9 && hex.r <= 15 (awayGoal) — use isInRegion from @counter-attack/shared for homeGoal and awayGoal
    - Base fill: isGoal → '#1a1a1a'; otherwise (hex.q + hex.r) % 2 === 0 → '#4a7c3f'; else '#3d6b34'
    - Render base polygon: fill=baseFill, stroke='#2d5227', strokeWidth={0.5}; if isHighlighted attach onClick and cursor:pointer; if not isHighlighted attach no onClick handler (clicks silently ignored)
    - Render highlight overlay polygon (only when isHighlighted): separate polygon with same points, fill='#f5c518', fillOpacity={0.55}, stroke='#d4a017', strokeWidth={1.5}, pointerEvents='none'; on hover (React useState): fillOpacity={0.75}, strokeWidth={2}
    - Render difficult-angle dot (only when isDifficultAngle(hex)): circle cx={cx} cy={cy} r={3} fill='#ffffff' fillOpacity={0.3} pointerEvents='none'
    - Return a React Fragment (not a div — SVG children must be SVG elements)
    - import type { HexCoord } from '@counter-attack/shared'; import { isInRegion, isDifficultAngle } from '@counter-attack/shared'; import { axialToPixel, hexPolygonPoints } from '../utils/hexToPixel.js'; import { useState } from 'react'
    - Note: hover state via useState only on highlighted hexes (non-highlighted hexes have no visual hover)

    Create packages/client/src/components/HexCell.module.css:
    - Minimal file. HexCell uses inline SVG fill attributes for colors (not CSS classes). CSS module is a placeholder for future additions. Content: empty or just /* HexCell — fill colors via inline SVG attributes */

    Create packages/client/src/components/HexGrid.tsx:
    - Named export: export function HexGrid()
    - Zustand selectors — subscribe to individual slices to avoid Pitfall 6 re-renders:
      const pieces = useGameStore((s) => s.gameState.pieces)
      const ball = useGameStore((s) => s.gameState.ball)
      const phase = useGameStore((s) => s.gameState.phase)
      const activeTeam = useGameStore((s) => s.gameState.activeTeam)
      const validMoveHexes = useGameStore((s) => s.validMoveHexes)
      const selectedPieceId = useGameStore((s) => s.selectedPieceId)
      const selectPiece = useGameStore((s) => s.selectPiece)
      const movePiece = useGameStore((s) => s.movePiece)
    - Build a Set<string> from validMoveHexes using the hexKey pattern (h.q + ',' + h.r) for O(1) membership checks when rendering highlights
    - Build a Set<string> for piece positions keyed by hexKey for overlap detection
    - SVG structure:
      <svg class={styles.hexGrid} viewBox={computeViewBox()} width="100%" preserveAspectRatio="xMidYMid meet">
        <defs><clipPath id="pitch-clip"><rect x="0" y="0" width={full-width} height={full-height} /></clipPath></defs>
        <g transform={`translate(${HEX_SIZE}, ${HEX_SIZE * Math.sqrt(3) / 2})`} clipPath="url(#pitch-clip)">
          {PITCH_HEXES.map((hex) => (
            <HexCell
              key={`${hex.q},${hex.r}`}
              hex={hex}
              isHighlighted={validMoveHexSet.has(`${hex.q},${hex.r}`)}
              onClick={validMoveHexSet.has(`${hex.q},${hex.r}`) ? () => movePiece(hex) : () => undefined}
            />
          ))}
          {/* BallMarker — above hexes, below pieces */}
          <BallMarker ball={ball} />
          {/* PieceOverlay — topmost layer, all 22 pieces */}
          {pieces.map((piece) => (
            <PieceOverlay
              key={piece.id}
              piece={piece}
              isSelected={piece.id === selectedPieceId}
              isClickable={phase === 'MOVEMENT' && piece.teamId === activeTeam}
              onClick={() => selectPiece(piece.id)}
            />
          ))}
        </g>
      </svg>
    - The <g transform="translate(...)"> offsets the entire pitch so q=0,r=0 hex is not clipped (Pitfall 5 fix). HEX_SIZE = 20, translate(20, 20 * √3/2) ≈ translate(20, 17.32)
    - All SVG children (HexCell, BallMarker, PieceOverlay) are inside the SAME <svg> root and the same <g> — z-order is DOM order: hexes → BallMarker → PieceOverlay (pieces on top)
    - Do NOT use a second <svg> for overlays
    - import type { HexCoord } from '@counter-attack/shared'; import { PITCH_HEXES } from '@counter-attack/shared'; import { useGameStore } from '../store/useGameStore.js'; import { computeViewBox, HEX_SIZE } from '../utils/hexToPixel.js'; import { HexCell } from './HexCell.js'; import { PieceOverlay } from './PieceOverlay.js'; import { BallMarker } from './BallMarker.js'; import styles from './HexGrid.module.css'

    Create packages/client/src/components/HexGrid.module.css:
    - .hexGrid: display block; max-width: 100%
    - No fixed width or height — viewBox + width:100% controls aspect ratio

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client build</automated>
  </verify>
  <done>Build exits 0. HexGrid.tsx imports PITCH_HEXES and renders it in a map. HexCell.tsx uses hexPolygonPoints and isInRegion. Both files use named exports. No default exports. No separate SVG root for overlays.</done>
</task>

<task type="auto">
  <name>Task 2: PieceOverlay, BallMarker, and GameBoard layout</name>
  <files>
    packages/client/src/components/PieceOverlay.tsx,
    packages/client/src/components/BallMarker.tsx,
    packages/client/src/components/GameBoard.tsx,
    packages/client/src/components/GameBoard.module.css
  </files>
  <read_first>
    - packages/shared/src/types.ts — PlayerPiece type (id, teamId, position, role, name), BallState type
    - packages/client/src/utils/hexToPixel.ts — axialToPixel function for pixel coordinate computation
    - .planning/phases/06-react-hex-grid-renderer/06-UI-SPEC.md — Piece Overlay Spec (radius 10px, fill colors per team, selected ring r=12 gold, player number label font-size 9 SVG, GK italic, cursor rules), BallMarker Spec (r=6, fill #f5f0dc, stroke #222222, ball carrier inner ring r=5), Layout spec (header 48px, pitch container calc(100vw-280px), sidebar 280px)
    - .planning/phases/06-react-hex-grid-renderer/06-PATTERNS.md — PieceOverlay pattern, BallMarker pattern, GameBoard layout pattern
    - packages/client/src/store/useGameStore.ts — for understanding what TurnIndicator and ActionLog stubs should receive (GameBoard imports them as stubs in this plan)
  </read_first>
  <action>
    Create packages/client/src/components/PieceOverlay.tsx:
    - Named export: export function PieceOverlay({ piece, isSelected, isClickable, onClick }: Props)
    - Props type: piece: PlayerPiece; isSelected: boolean; isClickable: boolean; onClick: () => void
    - Compute pixel coords via axialToPixel(piece.position.q, piece.position.r) from '../utils/hexToPixel.js'
    - Player number: extract from piece.id by taking everything after the last '-'; 'home-10' → '10', 'away-0' → '0'
    - Home fill '#1a56b0', home stroke '#0d3a82'; Away fill '#c0392b', away stroke '#8e1c12'; stroke-width 1.5
    - Render: base circle r=10, fill, stroke, strokeWidth=1.5, cursor=isClickable?'pointer':'default', onClick=isClickable?onClick:undefined
    - If isSelected: render additional ring circle r=12, fill='none', stroke='#f5c518', strokeWidth={2}
    - Ball carrier indicator: check if piece.id equals the carrierId from ball (pass ball.carrierId as a prop or pass via context); simpler: accept carrierId: string | null as a prop — if piece.id === carrierId, render inner circle r=5, fill='#f5f0dc', fillOpacity={0.6}; pass this from HexGrid via pieces.map with ball.carrierId
    - SVG text element: textAnchor='middle', dominantBaseline='central', fontSize={9}, fontWeight={700}, fill='#ffffff', fontStyle=piece.role==='GK'?'italic':'normal'
    - All SVG elements (not a div wrapper) — return a React Fragment
    - import type { PlayerPiece } from '@counter-attack/shared'; import { axialToPixel } from '../utils/hexToPixel.js'

    Update HexGrid.tsx to pass ball.carrierId to each PieceOverlay:
    <PieceOverlay key={piece.id} piece={piece} isSelected={...} isClickable={...} onClick={...} carrierId={ball.carrierId} />
    (This requires updating Props type in PieceOverlay.tsx to include carrierId: string | null)

    Create packages/client/src/components/BallMarker.tsx:
    - Named export: export function BallMarker({ ball }: { ball: BallState })
    - import type { BallState } from '@counter-attack/shared'; import { axialToPixel } from '../utils/hexToPixel.js'
    - Compute pixel coords via axialToPixel(ball.position.q, ball.position.r)
    - Render: circle cx={cx} cy={cy} r={6} fill='#f5f0dc' stroke='#222222' strokeWidth={1.5}
    - No pointer events — ball is not clickable directly

    Create packages/client/src/components/GameBoard.tsx:
    - Named export: export function GameBoard()
    - Renders the full game board layout per UI-SPEC: header bar, pitch container, sidebar
    - import styles from './GameBoard.module.css'
    - import { HexGrid } from './HexGrid.js'
    - For TurnIndicator and ActionLog: create minimal inline stub components or import from stub files that return placeholder <div> elements — these stubs are replaced in Plan 06-03. Use: <div className={styles.turnIndicator}>Turn Indicator (coming in Plan 03)</div> and <div className={styles.actionLog}>Action Log (coming in Plan 03)</div> as placeholders.
    - Structure (per UI-SPEC layout):
      <div className={styles.gameBoard}>
        <header className={styles.header}><span>Counter Attack</span></header>
        <main className={styles.gameLayout}>
          <div className={styles.pitchContainer}><HexGrid /></div>
          <aside className={styles.sidebar}>
            <div className={styles.turnIndicatorPlaceholder}>Turn Indicator</div>
            <div className={styles.actionLogPlaceholder}>Action Log</div>
          </aside>
        </main>
      </div>

    Create packages/client/src/components/GameBoard.module.css:
    - .gameBoard: height 100vh; display flex; flexDirection column
    - .header: height 48px; background #1a1a2e; display flex; alignItems center; padding 0 16px; color #e0e0e0; fontSize 13px; fontWeight 700; flexShrink 0
    - .gameLayout: flex 1; display flex; flexDirection row; overflow hidden
    - .pitchContainer: flex 1; background #0a0a0a; padding 16px; overflow hidden; display flex; alignItems flex-start
    - .sidebar: width 280px; flexShrink 0; background #1a1a2e; padding 24px; display flex; flexDirection column; gap 16px; overflowY auto
    - .turnIndicatorPlaceholder, .actionLogPlaceholder: color #a0a0a0; fontSize 13px; padding 8px; border 1px solid #0f3460; borderRadius 4px

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client build</automated>
  </verify>
  <done>Build exits 0. PieceOverlay renders home pieces with #1a56b0 fill and away pieces with #c0392b fill. BallMarker renders at ball.position. GameBoard has header + pitch container + sidebar layout. All exports are named (no defaults). No second SVG root for overlays.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                              |
| ---------------------------------- | ------------------------------------------------------------------------ |
| Zustand store → SVG elements       | Piece positions and ball position from store rendered as SVG coordinates |
| User click → selectPiece/movePiece | Click events on SVG elements trigger store mutations                     |

## STRIDE Threat Register

| Threat ID | Category  | Component              | Disposition | Mitigation Plan                                                                                                                              |
| --------- | --------- | ---------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-04   | Tampering | HexCell onClick        | mitigate    | Non-highlighted hexes have no onClick handler assigned; highlighted hexes call movePiece only — no arbitrary action execution                |
| T-06-05   | Tampering | PieceOverlay onClick   | mitigate    | isClickable guard: only own-team pieces in MOVEMENT phase have onClick; opponent pieces and own pieces in non-MOVEMENT phase have no handler |
| T-06-SC   | Tampering | npm/pip/cargo installs | mitigate    | All packages verified in 06-RESEARCH.md Package Legitimacy Audit; no new packages in Wave 2                                                  |

</threat_model>

<verification>
After both tasks complete:

1. `pnpm --filter @counter-attack/client build` exits 0
2. `pnpm -r test` exits 0 (shared + client test suites both green)
3. HexGrid.tsx contains PITCH_HEXES and maps it (grep confirms)
4. HexCell.tsx uses isInRegion and isDifficultAngle from @counter-attack/shared
5. PieceOverlay.tsx uses axialToPixel from hexToPixel.js (not hexToPixel.ts)
6. All SVG overlay elements are inside the same <svg> root as hex polygons (no separate <svg> for overlays)
7. GameBoard.tsx imports HexGrid and renders a pitch container + sidebar layout

Manual verification (checkpoint in Plan 06-03):

- Run `pnpm --filter @counter-attack/client dev` (after ensuring @counter-attack/shared is built)
- Open browser at localhost:5173
- The 37×26 SVG pitch renders with diagonal green stripe pattern
- Pieces appear as colored circles on their starting positions
- Ball appears as off-white circle at {q:18, r:13}
- Clicking a home team piece (blue) in MOVEMENT phase highlights valid destination hexes in gold
- Clicking a highlighted hex moves the piece
  </verification>

<success_criteria>

1. HexGrid renders 962 hex polygons (one per PITCH_HEXES entry) in a flat-top orientation with diagonal stripe fill
2. PieceOverlay renders all 22 pieces as correctly colored circles (blue home, red away) at their starting positions
3. BallMarker renders at {q:18, r:13} as an off-white circle visually distinct from piece circles
4. Clicking home team piece in MOVEMENT phase shows gold highlight overlays on valid destination hexes
5. Clicking a highlighted hex triggers movePiece and the piece moves
6. Non-highlighted hexes produce no visual or functional response to clicks
7. pnpm --filter @counter-attack/client build exits 0
   </success_criteria>

<output>
Create `.planning/phases/06-react-hex-grid-renderer/06-02-SUMMARY.md` when done.
</output>
