---
phase: 16-player-roster-team-selection
plan: 04
type: execute
wave: 3
depends_on: ['16-03']
files_modified:
  - packages/client/src/mock/mockMovementState.ts
  - packages/client/src/teamDefaults.ts
  - packages/client/src/components/PieceOverlay.tsx
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/PlayerStatsPanel.tsx
  - packages/client/src/components/PlayerStatsPanel.module.css
  - packages/client/src/components/TeamSelectionScreen.tsx
  - packages/client/src/components/TeamSelectionScreen.module.css
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/App.tsx
autonomous: false
requirements: [PLAY-01, PLAY-02, PLAY-03, SELECT-01]
user_setup: []

must_haves:
  truths:
    - 'mockMovementState builds pieces from TEAM_SQUADS and includes selectedTeams'
    - 'teamDefaults.ts is deleted; all 4 components read gameState.selectedTeams from Zustand'
    - 'Player card shows firstName (line1), lastName (line2), badge|role|#number (line3)'
    - 'MiniTokenBadge reads piece.number directly'
    - 'TeamSelectionScreen renders 4 team cards (no Free Agent), home-first turn order, struck-out home pick'
    - 'Per D-13 the selection cards use the {teamid}-full.png assets (already in repo); regular {teamid}.png stays in scoreboard/player card'
    - 'App routes TEAM_SELECTION to TeamSelectionScreen; team:selection-start and team:home-picked wired with cleanup'
    - 'Selected team badge/colors appear in scoreboard, player card, and tokens during the match'
  artifacts:
    - path: 'packages/client/src/components/TeamSelectionScreen.tsx'
      provides: 'Team selection UI'
      contains: 'FULL_BADGE_MAP'
    - path: 'packages/client/src/components/TeamSelectionScreen.module.css'
      provides: '2x2 grid card styles'
      contains: '.grid'
    - path: 'packages/client/src/App.tsx'
      provides: 'TEAM_SELECTION routing + socket handlers'
      contains: 'TEAM_SELECTION'
  key_links:
    - from: 'packages/client/src/App.tsx'
      to: 'TeamSelectionScreen'
      via: "render branch on screen === 'TEAM_SELECTION'"
      pattern: 'TEAM_SELECTION.*TeamSelectionScreen'
    - from: 'packages/client/src/components/PlayerStatsPanel.tsx'
      to: 'gameState.selectedTeams'
      via: 'useGameStore selector'
      pattern: "useGameStore\\(\\(?s\\)? => s.gameState.selectedTeams"
    - from: 'packages/client/src/components/TeamSelectionScreen.tsx'
      to: '../assets/badges/{teamid}-full.png'
      via: 'static Vite import'
      pattern: "from '\\.\\./assets/badges/.*-full.png'"
---

<objective>
Complete the client side of Phase 16. Repair `mockMovementState` to use `TEAM_SQUADS` + `selectedTeams`, delete `teamDefaults.ts` and migrate all four consuming components to read `gameState.selectedTeams` from Zustand, redesign the player card header (PLAY-02), build the `TeamSelectionScreen` (PLAY-03 / SELECT-01) with its CSS module, extend the `Screen` union, and wire the `team:selection-start` / `team:home-picked` socket handlers and the `TEAM_SELECTION` render branch in `App.tsx`. End with a human-verify checkpoint for the two-browser flow.

Purpose: implements PLAY-02 (player card), PLAY-03 (FA not selectable), and the client half of SELECT-01 (selection screen, turn order, team propagation to scoreboard/tokens/cards).
Output: new TeamSelectionScreen + CSS, redesigned PlayerStatsPanel, migrated components, updated store/App, repaired mock.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-player-roster-team-selection/16-CONTEXT.md
@.planning/phases/16-player-roster-team-selection/16-RESEARCH.md
@.planning/phases/16-player-roster-team-selection/16-PATTERNS.md
@.planning/phases/16-player-roster-team-selection/16-UI-SPEC.md
@packages/shared/src/teams.ts
@packages/shared/src/teamConfig.ts
</context>

<artifacts_this_phase_produces>

- `TeamSelectionScreen` component + `TeamSelectionScreen.module.css` (packages/client/src/components/)
- `Screen` union member `'TEAM_SELECTION'` (packages/client/src/store/useGameStore.ts)
- App.tsx `team:selection-start` / `team:home-picked` handlers + `homePickedTeam` local state + `handleTeamPick`
- PlayerStatsPanel `.firstName` / `.lastName` / `.playerMeta` CSS classes
- Deletion of `packages/client/src/teamDefaults.ts` (and its `TEAM_DEFAULTS` export)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Repair mock + delete teamDefaults + migrate 4 components + player card redesign</name>
  <files>packages/client/src/mock/mockMovementState.ts, packages/client/src/teamDefaults.ts, packages/client/src/components/PieceOverlay.tsx, packages/client/src/components/GameBoard.tsx, packages/client/src/components/ActionLog.tsx, packages/client/src/components/PlayerStatsPanel.tsx, packages/client/src/components/PlayerStatsPanel.module.css</files>
  <read_first>
    - packages/client/src/mock/mockMovementState.ts (lines 1-2 import HOME_SQUAD/AWAY_SQUAD; lines 49-67 pieces map + GameState fields incl. kickOffActive)
    - packages/client/src/teamDefaults.ts (TEAM_DEFAULTS export to delete)
    - packages/client/src/components/PlayerStatsPanel.tsx (full file: TEAM_DEFAULTS import line 4, usage line 39, MiniTokenBadge number derivation line ~36, header render lines ~128-136)
    - packages/client/src/components/PlayerStatsPanel.module.css (.headerText and .role classes)
    - packages/client/src/components/ActionLog.tsx (pieceColorOf helper, TEAM_DEFAULTS usage lines 4 & 11)
    - packages/client/src/components/PieceOverlay.tsx and GameBoard.tsx (TEAM_DEFAULTS import + usage; note GameBoard also reads piece.name somewhere)
    - packages/client/src/components/TeamBadge.tsx (size prop; static badge import pattern)
    - .planning/phases/16-player-roster-team-selection/16-PATTERNS.md (PlayerStatsPanel/ActionLog/PieceOverlay/GameBoard modify sections; shared selector pattern)
    - .planning/phases/16-player-roster-team-selection/16-UI-SPEC.md (Modified PlayerStatsPanel header structure + new CSS classes + typography/color rules)
  </read_first>
  <action>
    `mockMovementState.ts`: replace the `HOME_SQUAD, AWAY_SQUAD` import with `TEAM_SQUADS` from `@counter-attack/shared`. Build home pieces from `TEAM_SQUADS.cosmos` (`{ ...p, teamId: 'home' }`) and away pieces from `TEAM_SQUADS.xolos` (`{ ...p, teamId: 'away', id: p.id.replace('home-','away-'), position: { q: 36 - p.position.q, r: p.position.r } }`) — then apply the existing `HOME_POSITIONS`/`AWAY_POSITIONS` overrides by id as before. Add `selectedTeams: { home: 'cosmos', away: 'xolos' }` to the GameState object (after `kickOffActive: false`). (mockPassState/mockShotState/mockGKRestartState spread mockMovementState transitively, so they inherit selectedTeams — no edits needed there.)
    DELETE `packages/client/src/teamDefaults.ts` entirely (D-17).
    In `PieceOverlay.tsx`, `GameBoard.tsx`, `ActionLog.tsx`, `PlayerStatsPanel.tsx`: remove `import { TEAM_DEFAULTS } from '../teamDefaults.js';`. In each component body add `const selectedTeams = useGameStore((s) => s.gameState.selectedTeams);` and replace every `TEAM_DEFAULTS[piece.teamId]` (and `TEAM_DEFAULTS[positional]`) with `selectedTeams[...]`. For `ActionLog.tsx`'s module-level `pieceColorOf` helper, add a `selectedTeams` parameter (`pieceColorOf(pieceId, selectedTeams)`) and read `selectedTeams` via the Zustand selector inside the ActionLog component, passing it down (do NOT convert the helper into a hook). For any `piece.name` read in GameBoard.tsx, replace with `piece.firstName + ' ' + piece.lastName` (D-08).
    `PlayerStatsPanel.tsx` (D-08, D-09): change `MiniTokenBadge` number source from the id-slice derivation to `const playerNumber = String(piece.number);`. Import `{ TeamBadge } from './TeamBadge.js'`. Redesign the header per UI-SPEC: `.headerText` becomes a flex column (`flex-direction: column; gap: 2px;`) containing `<span class={styles.firstName}>{piece.firstName}</span>`, `<span class={styles.lastName}>{piece.lastName}</span>`, and a `<span class={styles.playerMeta}><TeamBadge teamId={selectedTeams[piece.teamId]} size={20} />{piece.role}&nbsp;#{piece.number}</span>`. The line-3 TeamBadge uses the regular `{teamid}.png` (size 20, NOT the full-size variant) per D-13. Keep the existing `MiniTokenBadge` and 10-attribute grid unchanged.
    `PlayerStatsPanel.module.css`: add `.firstName { font-size: 13px; font-weight: 700; color: #e0e0e0; }`, `.lastName { font-size: 13px; font-weight: 700; color: #e0e0e0; }`, `.playerMeta { display: flex; align-items: center; gap: 4px; font-size: 11px; color: #a0a0a0; text-transform: uppercase; letter-spacing: 0.05em; }`. Change `.headerText` from `justify-content: space-between; align-items: baseline;` to `flex-direction: column; gap: 2px;`. Cite PLAY-02, D-08, D-09.
  </action>
  <verify>
    <automated>cd packages/client; npx tsc --noEmit && pnpm run test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - teamDefaults.ts no longer exists (file deleted)
    - No client file imports `../teamDefaults` (grep across packages/client/src returns 0)
    - PieceOverlay/GameBoard/ActionLog/PlayerStatsPanel each contain `s.gameState.selectedTeams`
    - PlayerStatsPanel.tsx contains `String(piece.number)` and renders `piece.firstName`, `piece.lastName`, and a `TeamBadge` with `size={20}`
    - PlayerStatsPanel.module.css contains `.firstName`, `.lastName`, `.playerMeta`
    - mockMovementState.ts contains `TEAM_SQUADS` and `selectedTeams: { home: 'cosmos', away: 'xolos' }`
    - PlayerStatsPanel.test.tsx (from plan 01) PASSES (firstName/lastName/role/#number assertions GREEN)
  </acceptance_criteria>
  <done>teamDefaults deleted; 4 components read selectedTeams; player card shows the 3-line layout reading piece.number; mock repaired; PlayerStatsPanel test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: TeamSelectionScreen + Screen union + App wiring</name>
  <files>packages/client/src/components/TeamSelectionScreen.tsx, packages/client/src/components/TeamSelectionScreen.module.css, packages/client/src/store/useGameStore.ts, packages/client/src/App.tsx</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts (Screen union line 22; playerSlot field; emit action pattern e.g. emitReady line 581)
    - packages/client/src/App.tsx (full file: socket.on/off handler pattern lines 21-77; render ternary line 82; setScreen usage; useGameStore selectors)
    - packages/client/src/components/TeamBadge.tsx (static Vite badge import pattern, lines 7-18)
    - packages/client/src/components/LobbyScreen.module.css (.page, .heading, .ctaButton classes to mirror)
    - packages/client/src/assets/badges/ (confirm cosmos-full.png, xolos-full.png, city-full.png, crew-full.png exist — D-13)
    - packages/shared/src/teamConfig.ts (TEAM_CONFIGS: name, primaryColor per TeamId)
    - packages/shared/src/events.ts (ServerEvents.TEAM_SELECTION_START / TEAM_HOME_PICKED; ClientEvents.TEAM_PICK)
    - .planning/phases/16-player-roster-team-selection/16-UI-SPEC.md (TeamSelectionScreen layout tree, color rules, copywriting contract, interaction contract)
    - .planning/phases/16-player-roster-team-selection/16-PATTERNS.md (TeamSelectionScreen.tsx, App.tsx, useGameStore.ts modify sections)
  </read_first>
  <action>
    `useGameStore.ts`: extend the `Screen` union to add `'TEAM_SELECTION'` (between `'WAITING'` and `'GAME_BOARD'`). No store-level homePickedTeam (D-14 keeps it local to App).
    Create `TeamSelectionScreen.tsx` (D-10–D-14, PLAY-03, SELECT-01). Props: `{ homePickedTeam: TeamId | null; onPick: (teamId: TeamId) => void }`. Per D-13 static-import the four FULL-SIZE badges (`cosmos-full.png`, `xolos-full.png`, `city-full.png`, `crew-full.png` from `../assets/badges/`) into `FULL_BADGE_MAP: Record<TeamId, string>` (same static-import pattern as TeamBadge.tsx — NOT dynamic import; these `-full.png` variants are used ONLY on this screen). `const ALL_TEAMS: TeamId[] = ['cosmos','xolos','city','crew']` (exactly 4; never includes Free Agents — PLAY-03). Read `playerSlot` via `useGameStore(s => s.playerSlot)`. Derive `isHomeTurn = homePickedTeam === null`, `iAmHome = playerSlot === 1`, `iAmActive = isHomeTurn ? iAmHome : !iAmHome`. Render heading "Home: choose your team" or "Away: choose your team" per turn (UI-SPEC copywriting); a status line "Waiting for home player to choose..." when away and `isHomeTurn`. Render a 2×2 grid of `<button class={styles.card or styles.cardStruckOut}>` — each card: `style={{ borderColor: TEAM_CONFIGS[teamId].primaryColor }}`, full badge `<img>` at 110×110 (object-fit contain), team name `<span>` from `TEAM_CONFIGS[teamId].name`. `isStruckOut = teamId === homePickedTeam`; `disabled = !iAmActive || isStruckOut`. On click of an enabled card call `onPick(teamId)`. Cite PLAY-03, SELECT-01, D-11, D-12, D-13.
    Create `TeamSelectionScreen.module.css` per UI-SPEC: `.screen` (full-viewport flex column, center, bg #1a1a2e), `.heading` (20px/700/#e0e0e0), `.statusLine` (13px/400/#a0a0a0), `.grid` (display grid, `grid-template-columns: 1fr 1fr`, gap 16px, max-width 480px), `.card` (border 2px solid, radius 4px, bg #16213e, padding 16px, flex column align center gap 8px, cursor pointer; min 120×180), `.card:disabled` (opacity 0.4, cursor not-allowed), `.cardStruckOut` (opacity 0.4, cursor not-allowed; team name span uses text-decoration line-through), card hover when active `box-shadow: 0 0 0 2px` accent. Use only the established spacing/typography/color tokens from UI-SPEC.
    `App.tsx`: add `import { TeamSelectionScreen } from './components/TeamSelectionScreen.js';` and `import type { TeamId } from '@counter-attack/shared';`. Add local state `const [homePickedTeam, setHomePickedTeam] = useState<TeamId | null>(null);`. Add handlers `onTeamSelectionStart` (calls `setHomePickedTeam(null); setScreen('TEAM_SELECTION')`) and `onTeamHomePicked` (calls `setHomePickedTeam(teamId)`). Register `socket.on(ServerEvents.TEAM_SELECTION_START, onTeamSelectionStart)` and `socket.on(ServerEvents.TEAM_HOME_PICKED, onTeamHomePicked)` in the useEffect and add matching `socket.off(...)` in the cleanup return (Pitfall 9 — every on has an off). Add `function handleTeamPick(teamId: TeamId) { socket.emit(ClientEvents.TEAM_PICK, teamId); }` (import ClientEvents). Extend the render ternary: add `screen === 'TEAM_SELECTION' ? <TeamSelectionScreen homePickedTeam={homePickedTeam} onPick={handleTeamPick} /> :` BEFORE the existing GAME_BOARD/REPLAY branch. The transition to GAME_BOARD continues to be driven by the existing `onGameState` handler (server broadcasts GameState after away picks). Cite D-10, D-11, D-14, Pitfall 9.
  </action>
  <verify>
    <automated>cd packages/client; npx tsc --noEmit && pnpm run test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - useGameStore.ts Screen union contains `'TEAM_SELECTION'`
    - TeamSelectionScreen.tsx contains `FULL_BADGE_MAP`, static imports from `../assets/badges/*-full.png`, `ALL_TEAMS` of exactly 4 ids (no FA), and `onPick`
    - TeamSelectionScreen.module.css contains `.grid`, `.card`, `.cardStruckOut`
    - App.tsx contains `TEAM_SELECTION` render branch, `onTeamSelectionStart`, `onTeamHomePicked`, both `socket.on` and matching `socket.off`, and `handleTeamPick` emitting `ClientEvents.TEAM_PICK`
    - TeamSelectionScreen.test.tsx (from plan 01) PASSES (4 cards, slot-based enable/disable, struck-out, onPick)
    - `pnpm --filter @counter-attack/client run test` GREEN (modulo pre-existing ActionPanel.test.tsx failure)
  </acceptance_criteria>
  <done>TeamSelectionScreen renders 4 team cards with home-first turn order and struck-out home pick; App routes and wires the socket events with cleanup; client suite GREEN except the known ActionPanel failure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full team-selection flow end to end: slot-2 join shows TEAM_SELECTION on both clients; home picks first; away sees the struck-out home card and 3 active cards; after away picks both transition to GAME_BOARD with the selected teams' badges/colors on scoreboard, player cards, and tokens.</what-built>
  <how-to-verify>
    1. Run `pnpm run dev` (or the project's dev command for client + server).
    2. Open two browser tabs. In tab A create a room; copy the room code.
    3. In tab B join with that code.
    4. CONFIRM: both tabs show the TEAM_SELECTION screen with a 2×2 grid of 4 team cards (Cosmos, Xolos, City, Crew). No Free Agent card is present (PLAY-03).
    5. In tab A (home): all 4 cards are clickable. In tab B (away): all 4 cards are greyed/disabled.
    6. In tab A pick a team (e.g. City). CONFIRM: tab B now shows City struck-out and the other 3 cards active; tab A is waiting.
    7. In tab B pick a remaining team. CONFIRM: both tabs transition to the game board.
    8. CONFIRM on the board: scoreboard badges, player-card badge (line 3), and token colors match the two chosen teams. Select a piece and CONFIRM the player card shows firstName (line 1), lastName (line 2), badge|role|#number (line 3) with the real player's number (PLAY-02).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any mismatch (wrong colors, FA card visible, turn order broken, card layout off).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                                         |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- |
| user click → client onPick                 | Client emits team:pick; server (plan 03) is authoritative — client-side disable is UX only, not a security control. |
| server GameState broadcast → client render | Trusted: client renders the authoritative selectedTeams from the snapshot.                                          |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                 |
| --------- | ---------------------- | ------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-16-01   | Tampering              | client turn-order UI (TeamSelectionScreen) | accept      | Client disable/struck-out is UX only; the authoritative turn-order check lives server-side (plan 03 T-16-01). A tampered client sending out-of-turn team:pick is rejected by the server (WRONG_TURN). No new server trust placed in the client. |
| T-16-06   | Information Disclosure | client reads gameState.selectedTeams       | accept      | selectedTeams contains only non-sensitive team ids already chosen by both players; no PII or secret.                                                                                                                                            |
| T-16-SC   | Tampering              | npm installs                               | accept      | No package installs in this plan (uses existing React/Zustand/Vite + committed badge PNGs). RESEARCH Package Legitimacy Audit: no new runtime packages this phase.                                                                              |

</threat_model>

<verification>
- `cd packages/client && npx tsc --noEmit` clean
- `pnpm --filter @counter-attack/client run test` GREEN (modulo pre-existing ActionPanel failure)
- `pnpm run test` full suite GREEN (modulo pre-existing ActionPanel failure)
- Human checkpoint: two-browser team-selection flow approved
</verification>

<success_criteria>

- TeamSelectionScreen (4 cards, no FA, home-first, struck-out) works in a live two-browser session
- Player card shows firstName/lastName/badge/role/#number with piece.number
- Selected teams' badges and colors propagate to scoreboard, player cards, and tokens
- teamDefaults.ts deleted; all consumers read gameState.selectedTeams
- Full test suite green except the known unrelated ActionPanel failure
  </success_criteria>

<output>
Create `.planning/phases/16-player-roster-team-selection/16-04-SUMMARY.md` when done
</output>
