---
phase: 16-player-roster-team-selection
plan: 02
type: execute
wave: 1
depends_on: ['16-01']
files_modified:
  - packages/shared/scripts/seed-rosters.ts
  - packages/shared/package.json
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/shared/src/teams.ts
autonomous: true
requirements: [PLAY-01, PLAY-03, SELECT-01]

must_haves:
  truths:
    - 'Per D-01 the five committed CSVs at packages/shared/src/data/ are the source of truth and stay in the repo'
    - 'Per D-02 `pnpm run seed:rosters` runs a dev-tool seed script and its output teams.ts is committed (not a build step)'
    - 'Running `pnpm run seed:rosters` reads the 5 CSVs and regenerates teams.ts'
    - 'teams.ts exports TEAM_SQUADS (4 squads x 11 players) and FREE_AGENTS (24 players)'
    - "Each GK has number 1 and highPass 0; STR positions are mapped to ST; 'Arial Ability' maps to aerialAbility"
    - 'PlayerPiece has firstName/lastName/number/nationality and no name field'
    - 'GameState has selectedTeams: { home: TeamId; away: TeamId }'
    - 'events.ts declares TEAM_SELECTION_START, TEAM_HOME_PICKED, TEAM_PICK'
  artifacts:
    - path: 'packages/shared/scripts/seed-rosters.ts'
      provides: 'One-time CSV → teams.ts generator'
      contains: 'Arial Ability'
    - path: 'packages/shared/src/teams.ts'
      provides: 'TEAM_SQUADS + FREE_AGENTS'
      contains: 'export const TEAM_SQUADS'
    - path: 'packages/shared/src/types.ts'
      provides: 'PlayerPiece firstName/lastName/number/nationality + GameState.selectedTeams'
      contains: 'selectedTeams'
    - path: 'packages/shared/src/events.ts'
      provides: 'team selection socket events'
      contains: 'TEAM_SELECTION_START'
  key_links:
    - from: 'packages/shared/src/teams.ts'
      to: 'packages/shared/src/types.ts (PlayerPiece)'
      via: 'import type'
      pattern: "import type \\{ PlayerPiece"
    - from: 'packages/shared/scripts/seed-rosters.ts'
      to: 'packages/shared/src/data/*.csv'
      via: 'fs.createReadStream'
      pattern: 'createReadStream'
---

<objective>
Build the Phase 16 data layer. Author the one-time seed script (D-01, D-02) that converts the five committed CSV files into `teams.ts` (exporting `TEAM_SQUADS` and `FREE_AGENTS`), perform the `PlayerPiece` type surgery (remove `name`; add `firstName`/`lastName`/`number`/`nationality`), add `GameState.selectedTeams`, and add the three team-selection socket events. After this plan, the seeded `teams.ts` is committed source and the shared package type-checks.

Purpose: implements PLAY-01 (roster from CSV) and PLAY-03 (Free Agents stored, not in squads), and lays the shared-type foundation that the server (plan 03) and client (plan 04) consume for SELECT-01.
Output: seed script + `seed:rosters` npm script + regenerated teams.ts + updated types.ts + updated events.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-player-roster-team-selection/16-CONTEXT.md
@.planning/phases/16-player-roster-team-selection/16-RESEARCH.md
@.planning/phases/16-player-roster-team-selection/16-PATTERNS.md
</context>

<artifacts_this_phase_produces>

- `TEAM_SQUADS`, `FREE_AGENTS` (packages/shared/src/teams.ts) — replace HOME_SQUAD/AWAY_SQUAD
- `PlayerPiece.firstName/lastName/number/nationality` (packages/shared/src/types.ts); `name` removed
- `GameState.selectedTeams` (packages/shared/src/types.ts)
- `ServerEvents.TEAM_SELECTION_START`, `ServerEvents.TEAM_HOME_PICKED`, `ClientEvents.TEAM_PICK` (packages/shared/src/events.ts)
- `seed-rosters.ts` + `seed:rosters` script (packages/shared)
  </artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlayerPiece + GameState type surgery and new events</name>
  <files>packages/shared/src/types.ts, packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/types.ts (PlayerPiece lines ~3-27; GameState; locate `kickOffActive` field)
    - packages/shared/src/events.ts (ServerEvents/ClientEvents const objects; ServerToClientEvents/ClientToServerEvents interfaces; current import line)
    - packages/shared/src/teamConfig.ts (TeamId is exported HERE, line 7: `export type TeamId = 'cosmos' | 'xolos' | 'city' | 'crew'`)
    - .planning/phases/16-player-roster-team-selection/16-PATTERNS.md (types.ts and events.ts modify sections)
  </read_first>
  <action>
    In `packages/shared/src/types.ts`: add `import type { TeamId } from './teamConfig.js';` at the top. In `PlayerPiece`, REMOVE the `name: string` field and its JSDoc; ADD four required fields `firstName: string;`, `lastName: string;`, `number: number;`, `nationality: string;` (place them before `role`). In `GameState`, add `selectedTeams: { home: TeamId; away: TeamId };` immediately after the existing `kickOffActive: boolean;` field. Do NOT keep a `name` alias or computed getter (D-06). Cite D-06, D-15.
    In `packages/shared/src/events.ts`: import `TeamId` from `./teamConfig.js` (add to existing import block). Add to `ServerEvents` const object: `TEAM_SELECTION_START: 'team:selection-start',` and `TEAM_HOME_PICKED: 'team:home-picked',`. Add to `ClientEvents`: `TEAM_PICK: 'team:pick',`. Add to the `ServerToClientEvents` interface: `[ServerEvents.TEAM_SELECTION_START]: () => void;` and `[ServerEvents.TEAM_HOME_PICKED]: (teamId: TeamId) => void;`. Add to the `ClientToServerEvents` interface: `[ClientEvents.TEAM_PICK]: (teamId: TeamId) => void;`. Cite D-10, D-11.
    Note: this change will break callsites that read `piece.name` and `buildInitialGameState` callers and the mock states — those are repaired in plans 03 and 04. Within this plan, only types.ts and events.ts are touched.
  </action>
  <verify>
    <automated>cd packages/shared; npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `firstName: string`, `lastName: string`, `number: number`, `nationality: string`, and `selectedTeams: { home: TeamId; away: TeamId }`
    - types.ts contains no `name: string` field on PlayerPiece (grep for `name: string` in the PlayerPiece block returns 0)
    - events.ts contains `TEAM_SELECTION_START`, `TEAM_HOME_PICKED`, `TEAM_PICK`
    - types.ts imports TeamId from './teamConfig.js'
  </acceptance_criteria>
  <done>PlayerPiece carries the four identity fields with no `name`; GameState carries selectedTeams; events.ts declares all three team events with TeamId-typed payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Seed script + regenerate teams.ts</name>
  <files>packages/shared/scripts/seed-rosters.ts, packages/shared/package.json, packages/shared/src/teams.ts</files>
  <read_first>
    - packages/shared/src/teams.ts (current HOME_SQUAD/AWAY_SQUAD shape, formation position comment block lines ~26-34, GK D-04 rule comments)
    - packages/shared/src/data/cosmos_players.csv (header row — exact column names incl. 'Arial Ability' typo; 11 rows)
    - packages/shared/src/data/city_players.csv (contains 'STR' position; GK Roman Bürki all-values-present)
    - packages/shared/src/data/fa_players.csv (24 rows; FA → null TeamId)
    - packages/shared/package.json (scripts block; tsx is NOT yet a devDep here)
    - .planning/phases/16-player-roster-team-selection/16-RESEARCH.md (Pattern 1, Pattern 2, all 7 CSV pitfalls, teams.ts output shape)
    - .planning/phases/16-player-roster-team-selection/16-PATTERNS.md (seed-rosters.ts patterns: column-index map, ROLE_MAP, GK overrides, output template)
  </read_first>
  <action>
    Add `"tsx": "4.22.3"` to `packages/shared/package.json` devDependencies (same version already pinned in packages/server) and add script `"seed:rosters": "tsx scripts/seed-rosters.ts"` to the scripts block (per D-02 the seed is a dev tool invoked via `pnpm run seed:rosters`, NOT a build step; the output teams.ts is committed).
    Create `packages/shared/scripts/seed-rosters.ts` using Node built-ins only (`fs`, `readline`, `path`, `url`). Per D-01 the five CSVs at `packages/shared/src/data/` are the source of truth and remain in the repo. Behavior:
    - Parse each CSV via createReadStream + readline + `line.split(',').map(s => s.trim())`. Build a header→index map from row 0; access columns by the EXACT header string `'Arial Ability'` (typo preserved per D-05; map to `aerialAbility`).
    - Column mapping (D-05): `Player` → split on first space (token[0] = firstName, remainder = lastName per D-07; "Van De Kerkhof" → firstName 'Van', lastName 'De Kerkhof'); `Nationality` → nationality; `Position` → role via ROLE_MAP `{GK:'GK',DEF:'DEF',MID:'MID',FWD:'FWD',ST:'ST',STR:'ST'}` (STR→ST per Pitfall 2); numeric attrs `Pace/Dribbling/Heading/Highpass/Resilience/Shooting/Tackling/Saving/Handling` plus `Arial Ability`→aerialAbility using `toInt(s)= s===''?0:parseInt(s,10)` (blanks→0 per D-05).
    - GK overrides (Pitfall 3, D-04): for any row with role 'GK' force `highPass: 0` and `heading: 0` regardless of CSV value.
    - Jersey numbers (D-04, Pattern 2): per squad sort by `ROLE_ORDER = ['GK','DEF','MID','FWD','ST']` then assign `number = i+1` (GK→1). FA players numbered 1..24 in CSV order.
    - Positions are NOT from CSV: copy the formation constants from the existing teams.ts comment block by role (GK home {q:1,r:13}; DEF home q=6 r∈{6,13,19}; MID home q=10 r∈{9,17}; FWD home q=15 r∈{4,9,17,22}; ST per coin flip at runtime — store a home-side ST position). Store home-side positions only for all squads; away mirroring happens at runtime in buildInitialGameState (plan 03). Assign positions by squad-internal role order so each role slot maps to its formation coordinate. id = `home-{index}` (0..10) per squad.
    - CSV team name → TeamId map (D-03): 'Cozmos'→cosmos, 'CITY'→city, 'Crew'→crew, 'Xolos'→xolos, 'FA'→null. FA rows go to a separate FREE_AGENTS list (teamId placeholder 'home').
    - Write `packages/shared/src/teams.ts` via a string template: `import type { PlayerPiece } from './types.js';` then `export const TEAM_SQUADS: Record<TeamId, readonly PlayerPiece[]> = { cosmos: [...], xolos: [...], city: [...], crew: [...] };` and `export const FREE_AGENTS: readonly PlayerPiece[] = [...];`. Add `import type { TeamId } from './teamConfig.js';`. Do NOT emit HOME_SQUAD/AWAY_SQUAD. teamId for all squad players = 'home' placeholder (overridden at runtime).
    Then RUN `pnpm --filter @counter-attack/shared run seed:rosters` to generate the committed teams.ts.
  </action>
  <verify>
    <automated>cd packages/shared; pnpm run seed:rosters && npx tsc --noEmit && pnpm run test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/package.json scripts contains `seed:rosters` and devDependencies contains `tsx`
    - seed-rosters.ts contains the literal `'Arial Ability'` and `STR` and forces `highPass` 0 for GK
    - teams.ts contains `export const TEAM_SQUADS` and `export const FREE_AGENTS` and `import type { PlayerPiece }`
    - teams.ts contains no `HOME_SQUAD` and no `AWAY_SQUAD` and no `name:` field on any player
    - `pnpm --filter @counter-attack/shared run test` PASSES the teams.test.ts assertions from plan 01 (4×11 squads, FREE_AGENTS=24, GK number 1 / highPass 0, no name field) — shared suite GREEN
  </acceptance_criteria>
  <done>`pnpm run seed:rosters` regenerates teams.ts; shared package type-checks; the plan-01 teams.test.ts assertions pass (GREEN). gameEngine.teamselect.test.ts and client tests remain RED (consumed in plans 03/04).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                  |
| ---------------------------- | ---------------------------------------------------------------------------- |
| CSV file → seed script       | Dev-time only; CSVs are committed, trusted repo content. No runtime parsing. |
| shared types → server/client | Type contract; enforced at compile time.                                     |

## STRIDE Threat Register

| Threat ID | Category               | Component                         | Disposition | Mitigation Plan                                                                                                                                                                                                                            |
| --------- | ---------------------- | --------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-16-02   | Tampering              | teamId payload type (events.ts)   | mitigate    | `team:pick` typed as `(teamId: TeamId) => void` constrains the union at the type layer; runtime allow-list validation is added in plan 03 (server handler).                                                                                |
| T-16-03   | Information Disclosure | seed script reads local CSVs only | accept      | No secrets in CSVs (player stats only); seed is a dev tool, output committed. ASVS V5: input is trusted committed data.                                                                                                                    |
| T-16-SC   | Tampering              | npm installs                      | mitigate    | Only dependency added is `tsx@4.22.3`, already pinned and in use in packages/server (RESEARCH Package Legitimacy Audit: no new runtime packages). Pinned exact version; no `[ASSUMED]`/`[SUS]` packages → no blocking checkpoint required. |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared run seed:rosters` regenerates teams.ts deterministically
- `pnpm --filter @counter-attack/shared run test` GREEN (plan-01 roster assertions satisfied)
- `cd packages/shared && npx tsc --noEmit` clean
</verification>

<success_criteria>

- TEAM_SQUADS (4×11) + FREE_AGENTS (24) exported, all players carry firstName/lastName/number/nationality, GK number 1 / highPass 0, STR→ST applied
- PlayerPiece has no `name`; GameState has selectedTeams; events.ts has all three team events
- Shared test suite passes (modulo unrelated pre-existing failures elsewhere)
  </success_criteria>

<output>
Create `.planning/phases/16-player-roster-team-selection/16-02-SUMMARY.md` when done
</output>
