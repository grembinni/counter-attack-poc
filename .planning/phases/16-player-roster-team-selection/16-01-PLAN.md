---
phase: 16-player-roster-team-selection
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared/src/teams.test.ts
  - packages/client/src/components/PlayerStatsPanel.test.tsx
  - packages/client/src/components/TeamSelectionScreen.test.tsx
  - packages/server/src/__tests__/gameEngine.teamselect.test.ts
autonomous: true
requirements: [PLAY-01, PLAY-02, PLAY-03, SELECT-01]

must_haves:
  truths:
    - 'Updated teams.test.ts asserts firstName/lastName instead of name, 4 squads x 11 players, FREE_AGENTS length 24'
    - 'Updated PlayerStatsPanel.test.tsx asserts firstName/lastName/badge/role/number lines instead of /Home GK/'
    - 'New TeamSelectionScreen.test.tsx covers PLAY-03 (FA absent) and SELECT-01 (home active / away disabled / struck-out)'
    - 'New gameEngine.teamselect.test.ts asserts buildInitialGameState(roomCode, selectedTeams) sets gameState.selectedTeams'
    - 'All four test files are RED (fail / fail to compile) because production code does not yet exist'
  artifacts:
    - path: 'packages/shared/src/teams.test.ts'
      provides: 'TEAM_SQUADS + FREE_AGENTS roster assertions'
      contains: 'TEAM_SQUADS'
    - path: 'packages/client/src/components/TeamSelectionScreen.test.tsx'
      provides: 'Selection screen behavior assertions'
      contains: 'TeamSelectionScreen'
    - path: 'packages/server/src/__tests__/gameEngine.teamselect.test.ts'
      provides: 'selectedTeams embedding assertion'
      contains: 'selectedTeams'
  key_links:
    - from: 'packages/shared/src/teams.test.ts'
      to: 'packages/shared/src/teams.js (TEAM_SQUADS export)'
      via: 'import'
      pattern: "import.*TEAM_SQUADS.*from './teams"
---

<objective>
Establish the Nyquist Wave 0 RED test state for Phase 16. Replace `name`-based assertions with `firstName`/`lastName`, add roster-shape assertions for the new `TEAM_SQUADS` record and `FREE_AGENTS` export, add the player-card redesign assertions, create the new TeamSelectionScreen behavior tests, and add the `buildInitialGameState(roomCode, selectedTeams)` assertion. These tests MUST fail (or fail to compile) until the production plans (02–04) land.

Purpose: lock the executable specification for PLAY-01/02/03 and SELECT-01 before any production code is written, so subsequent waves have a green-target to satisfy.
Output: 1 new shared test, 1 updated shared test, 1 updated + 1 new client test, 1 new server test — all RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-player-roster-team-selection/16-CONTEXT.md
@.planning/phases/16-player-roster-team-selection/16-VALIDATION.md
@.planning/phases/16-player-roster-team-selection/16-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols/files this PHASE creates (so drift verification excludes them):

- `TEAM_SQUADS: Record<TeamId, readonly PlayerPiece[]>` (packages/shared/src/teams.ts) — replaces HOME_SQUAD/AWAY_SQUAD
- `FREE_AGENTS: readonly PlayerPiece[]` (packages/shared/src/teams.ts)
- `PlayerPiece.firstName`, `PlayerPiece.lastName`, `PlayerPiece.number`, `PlayerPiece.nationality` (packages/shared/src/types.ts) — `name` removed
- `GameState.selectedTeams: { home: TeamId; away: TeamId }` (packages/shared/src/types.ts)
- `ServerEvents.TEAM_SELECTION_START`, `ServerEvents.TEAM_HOME_PICKED`, `ClientEvents.TEAM_PICK` (packages/shared/src/events.ts)
- `TeamSelectionScreen` component (packages/client/src/components/TeamSelectionScreen.tsx)
- `Room.homePickedTeam?: TeamId` (packages/server/src/roomStore.ts)
- `seed-rosters.ts` script + `seed:rosters` npm script (packages/shared)
- This plan's new test files: TeamSelectionScreen.test.tsx, gameEngine.teamselect.test.ts
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update shared + server roster/engine RED tests</name>
  <files>packages/shared/src/teams.test.ts, packages/server/src/__tests__/gameEngine.teamselect.test.ts</files>
  <read_first>
    - packages/shared/src/teams.test.ts (current HOME_SQUAD/AWAY_SQUAD tests asserting player.name)
    - packages/shared/src/data/cosmos_players.csv, fa_players.csv (row counts: 11 per team, 24 FA)
    - packages/server/src/__tests__/gameEngine.test.ts (existing buildInitialGameState describe block, lines 79-120)
    - .planning/phases/16-player-roster-team-selection/16-RESEARCH.md (Pitfall 5, Test Map)
  </read_first>
  <action>
    Rewrite `packages/shared/src/teams.test.ts` to import `{ TEAM_SQUADS, FREE_AGENTS }` from `./teams.js` (NOT HOME_SQUAD/AWAY_SQUAD). Assertions: (a) `Object.keys(TEAM_SQUADS)` equals `['cosmos','xolos','city','crew']`; (b) each of the 4 squads `toHaveLength(11)`; (c) each squad has exactly 1 player with `role === 'GK'`; (d) GK in each squad has `number === 1` and `highPass === 0`; (e) within each squad `number` values are the set 1..11 with no duplicates; (f) every player has `firstName.length > 0`, `lastName.length > 0`, `nationality.length > 0` (replaces the removed `name` checks per PLAY-01); (g) NO player object has a `name` key — assert `('name' in player) === false`; (h) `FREE_AGENTS` `toHaveLength(24)`; (i) every player `role` is one of `'GK'|'DEF'|'MID'|'FWD'|'ST'` (proving STR→ST mapping). Cite PLAY-01.
    Create `packages/server/src/__tests__/gameEngine.teamselect.test.ts` importing `buildInitialGameState` from `../gameEngine.js`. Assert: `buildInitialGameState('ROOM1', { home: 'cosmos', away: 'xolos' }).selectedTeams` deep-equals `{ home: 'cosmos', away: 'xolos' }`; assert the returned `pieces` array has 22 members; assert home pieces have ids starting `home-` and away pieces `away-`; assert at least one away piece position has `q > 18` (mirrored to the away half via `q_away = 36 - q_home`). Cite SELECT-01, PLAY-01.
    Do NOT implement any production code. These tests are expected to fail to compile (TEAM_SQUADS/FREE_AGENTS not yet exported; two-arg signature not yet present).
  </action>
  <verify>
    <automated>cd packages/shared; npx tsc --noEmit 2>&1 | grep -q "TEAM_SQUADS\|FREE_AGENTS" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - teams.test.ts contains `TEAM_SQUADS` and `FREE_AGENTS` and contains `'name' in`
    - teams.test.ts contains no occurrence of `player.name` (assert via grep returns 0 after `grep -v '^//'`)
    - gameEngine.teamselect.test.ts contains `selectedTeams` and `buildInitialGameState('ROOM1', { home: 'cosmos', away: 'xolos' }`
    - `pnpm --filter @counter-attack/shared run test` exits non-zero OR shared `tsc --noEmit` reports unresolved TEAM_SQUADS/FREE_AGENTS (RED state confirmed)
  </acceptance_criteria>
  <done>Both files exist with the assertions above; suite is RED because production exports do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Update PlayerStatsPanel test + create TeamSelectionScreen RED test</name>
  <files>packages/client/src/components/PlayerStatsPanel.test.tsx, packages/client/src/components/TeamSelectionScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/PlayerStatsPanel.test.tsx (current /Home GK/i assertion at line 39, mock setup lines 1-26)
    - packages/client/src/mock/mockMovementState.ts (how gameState mock is shaped; selectedPieceId usage)
    - .planning/phases/16-player-roster-team-selection/16-UI-SPEC.md (Interaction Contract turn-sequence table, player card lines 1-3)
    - .planning/phases/16-player-roster-team-selection/16-CONTEXT.md (D-09, D-11, D-12, PLAY-03)
  </read_first>
  <action>
    Update `PlayerStatsPanel.test.tsx`: the mock `gameState` must include `selectedTeams: { home: 'cosmos', away: 'xolos' }` so the component's `useGameStore(s => s.gameState.selectedTeams)` read resolves. Replace `screen.getByText(/Home GK/i)` assertion with assertions that the selected GK's `firstName` and `lastName` (the real values from the seeded cosmos squad GK — read them from teams once seeded, or assert presence of a `firstName`/`lastName` two-line render by querying the rendered text nodes) appear, and that line 3 shows the role text (e.g. `getByText('GK')`) and the number (e.g. `getByText('#1')`). Keep the existing 10-attribute-label test unchanged. Cite PLAY-02, D-09.
    Create `packages/client/src/components/TeamSelectionScreen.test.tsx`. Mock `useGameStore` to control `playerSlot`. Render `<TeamSelectionScreen homePickedTeam={null} onPick={vi.fn()} />`. Assertions: (1) PLAY-03 — exactly 4 team cards render (cosmos, xolos, city, crew); NO Free Agent card and no card labelled FA. (2) SELECT-01 home-first — with `playerSlot=1` and `homePickedTeam=null`, all 4 card buttons are enabled (not `disabled`); with `playerSlot=2` and `homePickedTeam=null`, all 4 are `disabled`. (3) SELECT-01 struck-out — render with `playerSlot=2` and `homePickedTeam='cosmos'`: the cosmos card is `disabled` (struck-out) and the other 3 are enabled. (4) clicking an enabled card calls `onPick` with that teamId. Cite PLAY-03, SELECT-01, D-11, D-12.
    Do NOT implement TeamSelectionScreen.tsx — the import will fail to resolve (RED).
  </action>
  <verify>
    <automated>cd packages/client; npx tsc --noEmit 2>&1 | grep -q "TeamSelectionScreen" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - PlayerStatsPanel.test.tsx mock gameState contains `selectedTeams`
    - PlayerStatsPanel.test.tsx contains no `/Home GK/` assertion (grep returns 0)
    - TeamSelectionScreen.test.tsx exists, imports `./TeamSelectionScreen`, contains assertions for 4 cards, disabled-by-slot, struck-out, and onPick
    - `pnpm --filter @counter-attack/client run test` exits non-zero (TeamSelectionScreen module unresolved → RED)
  </acceptance_criteria>
  <done>Both client test files exist with the assertions above; client suite is RED because TeamSelectionScreen.tsx and the new PlayerPiece fields do not exist yet.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                                              |
| ------------- | ------------------------------------------------------------------------ |
| test → source | Tests import production symbols; no untrusted runtime input crosses here |

## STRIDE Threat Register

| Threat ID | Category  | Component                                   | Disposition | Mitigation Plan                                                                                                            |
| --------- | --------- | ------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------- |
| T-16-01   | Tampering | team:pick turn-order (tested in later plan) | accept      | This plan only authors assertions; turn-order enforcement is mitigated in plan 03. No runtime surface introduced here.     |
| T-16-SC   | Tampering | npm/pip/cargo installs                      | accept      | No package installs in this plan (test-only edits). RESEARCH Package Legitimacy Audit: no new runtime packages this phase. |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared run test` is RED (TEAM_SQUADS/FREE_AGENTS unresolved)
- `pnpm --filter @counter-attack/client run test` is RED (TeamSelectionScreen unresolved)
- No production source files modified in this plan
</verification>

<success_criteria>

- All four test files exist with the specified assertions
- Both shared and client suites fail (RED) for the expected missing-symbol reasons
- Pre-existing ActionPanel.test.tsx failure is the only unrelated failure tolerated
  </success_criteria>

<output>
Create `.planning/phases/16-player-roster-team-selection/16-01-SUMMARY.md` when done
</output>
