---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 16
type: execute
wave: 8
depends_on: ['39-06', '39-08', '39-09', '39-15']
files_modified:
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/GameBoard.test.tsx
  - packages/client/src/components/BallLocationRing.tsx
  - packages/client/src/components/BallLocationRing.test.tsx
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/HexGrid.test.tsx
autonomous: true
requirements: [FOUL-03, GKDIVE-02, PEN-02]

must_haves:
  truths:
    - 'Each new Phase 39 phase renders its own panel in the sidebar instead of the generic ActionPanel'
    - 'The ball location marker is visible during every new Phase 39 phase'
    - 'Reposition destinations during the penalty windows and the goalkeeper box-entry move are highlighted with the existing safe tint'
    - 'Both managers see an actionable Start 2nd Half button, and after confirming see a waiting message (D-16)'
  artifacts:
    - path: 'packages/client/src/components/GameBoard.tsx'
      provides: 'Phase dispatch for the four new panels and the mutual-confirm half-time button'
      contains: 'FoulChoicePanel'
    - path: 'packages/client/src/components/BallLocationRing.tsx'
      provides: 'Ball marker registration for the new phases'
      contains: 'PENALTY_KICK_SETUP_ATTACKING'
  key_links:
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: 'secondHalfConfirmed'
      via: 'both-confirm button state replacing the kickOffTeam gate'
      pattern: 'secondHalfConfirmed'
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'validHexes for the new reposition phases'
      via: 'safe highlight type'
      pattern: 'GK_BOX_ENTRY_MOVE'
---

<objective>
Wire the four new panels into `GameBoard.tsx`'s phase dispatch, register the new phases with the ball-location marker and the hex highlight layer, and replace the single-team `canStart` half-time gate with D-16's mutual-confirm UI.

Purpose: the panels, store wiring and server flows all exist by this wave but are unreachable — `GameBoard.tsx` still renders the generic `ActionPanel` for every new phase, `BallLocationRing` hides the ball marker, and `HexGrid` produces no highlight for the new reposition windows.

Output: the dispatch branches, the marker registration, the highlight wiring, the half-time button rework, and their tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-08-SUMMARY.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-09-SUMMARY.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-15-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`, `Room.lastBroadcastBallPosition`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto">
  <name>Task 1: Phase dispatch for the four new panels in GameBoard</name>
  <files>packages/client/src/components/GameBoard.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx lines 340-378 (the `topBandRight` phase-dispatch ternary chain, including the goal-kick and corner-kick multi-phase groupings and the `cornerKickTeam != null` persistent-signal branch)
    - packages/client/src/components/GameBoard.tsx lines 1-80 (the import block and `PHASE_LABEL`, already extended in Plan 39-01)
    - The four panel components from Plans 39-08 and 39-09 and their self-guarding `return null` behaviour
  </read_first>
  <action>
Extend the `topBandRight` dispatch chain with four new branches, inserted after the corner-kick branch and before the `REPLAY` branch so the existing ordering is untouched:

- `phase === 'FOUL_CHOICE'` → `<FoulChoicePanel />`
- `phase === 'GK_DIVE_AT_FEET_PROMPT'` → `<GkDiveAtFeetPromptPanel />`
- `phase === 'GK_BOX_ENTRY_PROMPT' || phase === 'GK_BOX_ENTRY_MOVE'` → `<GkBoxEntryPromptPanel />`
- `phase === 'PENALTY_KICK_SETUP_ATTACKING' || phase === 'PENALTY_KICK_SETUP_DEFENDING' || phase === 'PENALTY_KICK_TAKER_SELECT' || phase === 'PENALTY_KICK'` → `<PenaltyKickSetupPanel />`

Do NOT add a persistent-signal fallback branch analogous to the corner kick's `phase === 'PASS' && cornerKickTeam != null`. The penalty duel has its own dedicated `PENALTY_KICK` phase rather than resolving inside the ordinary `PASS` phase, so no such fallback is needed — record that in a comment so a reader comparing against the corner-kick branch does not assume an omission.

Add the four imports at the top of the file.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client typecheck && pnpm --filter @counter-attack/client test -- GameBoard.test.tsx</automated>
</verify>
<acceptance_criteria> - `grep -c "FoulChoicePanel\|GkDiveAtFeetPromptPanel\|GkBoxEntryPromptPanel\|PenaltyKickSetupPanel" packages/client/src/components/GameBoard.tsx` returns at least 8 (import plus usage for each) - All eight new `GamePhase` values route away from the generic `ActionPanel` - The existing kick-off, free-kick, throw-in, goal-kick, corner-kick and replay branches are unchanged - `pnpm --filter @counter-attack/client typecheck` exits 0
</acceptance_criteria>
<done>Every new Phase 39 phase renders its own sidebar panel.</done>
</task>

<task type="auto">
  <name>Task 2: Ball marker and hex highlight registration for the new phases</name>
  <files>packages/client/src/components/BallLocationRing.tsx, packages/client/src/components/BallLocationRing.test.tsx, packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/components/BallLocationRing.tsx lines 25-80 (`BALL_MARKER_PHASES` and its Phase 37/38 additions with their per-block comments)
    - packages/client/src/components/BallLocationRing.test.tsx (the membership-assertion style)
    - packages/client/src/components/HexGrid.tsx lines 820-870 (the `GOAL_KICK_SETUP_GK`/`_OPPONENT` highlight branch showing how a reposition phase maps its valid hexes to a `highlightType`)
    - packages/client/src/store/useGameStore.ts (the valid-hex derivation for `PENALTY_KICK_SETUP_*` and `GK_BOX_ENTRY_MOVE` added in Plan 39-05 — HexGrid must consume it, not re-derive it)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Highlight/tint mapping table: `safe` for penalty reposition destinations and for the box-entry one-hex destinations; `ring="required"` gold for the penalty spot; NO new tint type; no red forbidden-zone tint for the penalty area)
    - docs/HIGHLIGHT-REFERENCE.md (the existing `HexHighlightType` members — this phase adds none)
  </read_first>
  <action>
`BallLocationRing.tsx`: add all eight new Phase 39 phases to `BALL_MARKER_PHASES`, in one commented block matching the Phase 37/38 precedent blocks. The ball is at a fixed restart hex or mid-decision throughout every one of them. Extend `BallLocationRing.test.tsx` with membership assertions for each.

`HexGrid.tsx`: map the store-provided valid hexes to highlights for the new interactive phases, copying the goal-kick reposition branch's shape:

- `PENALTY_KICK_SETUP_ATTACKING` / `PENALTY_KICK_SETUP_DEFENDING` → `safe` (the same green tint every other reposition window uses).
- `GK_BOX_ENTRY_MOVE` → `safe`.
- `PENALTY_KICK_TAKER_SELECT` → no hex highlight; the taker is chosen by clicking a PIECE, so rely on the existing piece-selectable outline the store's branch already drives.
- Add the penalty spot marker: when the phase is any of the four penalty phases and `penaltyKickSpot` is non-null, render that hex with the existing `ring="required"` gold ring, mirroring kick-off's "must place kicker here" marker. Reuse the existing ring mechanism — add no new `HexHighlightType` member.
- Add NO red forbidden-zone tint for the penalty area. UI-SPEC is explicit: an ineligible placement is rejected server-side and surfaced through the panel's `.errorText` row, which is the established convention for zone restrictions.

Do not modify `docs/HIGHLIGHT-REFERENCE.md` — this phase introduces no new highlight type.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- BallLocationRing.test.tsx HexGrid.test.tsx</automated>
</verify>
<acceptance_criteria> - `BALL_MARKER_PHASES` contains all eight new phase values - `pnpm --filter @counter-attack/client test -- BallLocationRing.test.tsx` passes with 8 new membership assertions - `HexGrid.tsx` maps the penalty reposition phases and `GK_BOX_ENTRY_MOVE` to the existing `safe` highlight type and adds no new `HexHighlightType` member - `grep -c "HexHighlightType" docs/HIGHLIGHT-REFERENCE.md` shows the member list is unchanged from before this plan - No red/danger tint is applied to penalty-area hexes anywhere in `HexGrid.tsx` - `pnpm --filter @counter-attack/client typecheck` exits 0
</acceptance_criteria>
<done>The ball marker and reposition highlights work across every new phase, reusing only existing tints.</done>
</task>

<task type="auto">
  <name>Task 3: Mutual-confirm Start 2nd Half button and GameBoard tests</name>
  <files>packages/client/src/components/GameBoard.tsx, packages/client/src/components/GameBoard.test.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx lines 188-230 (`const canStart = myTeam !== null && myTeam !== kickOffTeam;`, `secondHalfKickOffTeam`, `secondHalfTeamName`) and lines 393-440 (the HALF_TIME overlay markup, including the button at ~428-435 with its `disabled={!canStart}` and `title="Only the 2nd half kick-off team can start"`)
    - packages/client/src/components/GameBoard.test.tsx (the store-mock harness and the existing half-time overlay assertions)
    - packages/client/src/store/useGameStore.ts (`emitHalfTimeStart`, and add a `secondHalfConfirmed` selector read here in GameBoard)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Copywriting Contract rows for the second-half mutual-confirm button and its waiting state)
  </read_first>
  <action>
Replace the D-28 single-team gate with D-16's mutual-confirm UI.

Delete `const canStart = myTeam !== null && myTeam !== kickOffTeam;` and add a `secondHalfConfirmed` store selector. Derive:

- `const myConfirmed = myTeam !== null && (secondHalfConfirmed?.[myTeam] ?? false);`
- `const canStart = myTeam !== null && !myConfirmed;`

Button behaviour:

- Before this manager has confirmed: enabled, labelled `Start 2nd Half`, `onClick={() => emitHalfTimeStart()}`. Remove the `title="Only the 2nd half kick-off team can start"` tooltip entirely — it is now false for both managers.
- After this manager has confirmed but before the opponent has: replace the button with the waiting text `Waiting for {opponent} to start the 2nd half…`, where `{opponent}` is `Home` or `Away` derived from `myTeam`. Use the same text element class the overlay already uses for its secondary lines rather than introducing a new class.

Keep `secondHalfKickOffTeam`/`secondHalfTeamName` and the `2ND HALF KICK OFF {Team}` informational line — that still correctly tells both managers who will kick off; only the ACTION gate changes.

Extend `GameBoard.test.tsx` with:

1. In `HALF_TIME` with `secondHalfConfirmed: null`, BOTH the home-viewing and away-viewing renders show an enabled `Start 2nd Half` button (this is the D-16 regression test — previously one side saw a disabled button).
2. Clicking it calls `emitHalfTimeStart` once.
3. With `secondHalfConfirmed: { home: true, away: false }` and `myTeam: 'home'`, the button is replaced by `Waiting for Away to start the 2nd half…`.
4. With the same state and `myTeam: 'away'`, the enabled `Start 2nd Half` button is shown.
5. No rendered element carries the removed `Only the 2nd half kick-off team can start` tooltip text.
6. Phase-dispatch assertions: each of the eight new phases renders its expected panel and NOT the generic `ActionPanel` (mock the four panels to identifiable stubs so the assertion is about dispatch, not panel internals).
   </action>
   <verify>
   <automated>pnpm --filter @counter-attack/client test -- GameBoard.test.tsx && pnpm --filter @counter-attack/client test</automated>
   </verify>
   <acceptance_criteria> - `grep -c "Only the 2nd half kick-off team can start" packages/client/src/components/GameBoard.tsx` returns 0 - `grep -c "myTeam !== kickOffTeam" packages/client/src/components/GameBoard.tsx` returns 0 - `GameBoard.tsx` reads `secondHalfConfirmed` from the store and derives `canStart` from it - The exact string `Waiting for` appears in the half-time overlay branch - `pnpm --filter @counter-attack/client test -- GameBoard.test.tsx` passes with at least 12 new assertions covering the numbered list - `pnpm --filter @counter-attack/client test` (full client suite) is green - `pnpm build` exits 0
   </acceptance_criteria>
   <done>Both managers can start the second half, each sees a waiting state after confirming, and every new phase dispatches to its own panel.</done>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                 |
| ------------------------------------ | --------------------------------------------------------------------------- |
| broadcast `GameState` → dispatch     | Phase-driven rendering only; no client decision affects authoritative state |
| overlay button → `emitHalfTimeStart` | An untrusted confirm re-validated by `socketTeam` server-side               |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                    | Disposition | Mitigation Plan                                                                                                                                                        |
| ---------- | ---------------------- | -------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-16-01 | Elevation of Privilege | Either manager clicking Start 2nd Half       | mitigate    | The button is intentionally enabled for both (D-16); the server sets only the confirming socket's own flag via `socketTeam(socket)` and requires both before starting. |
| T-39-16-02 | Elevation of Privilege | A panel rendering for the wrong manager      | mitigate    | Each panel self-guards on `useMyTeam()` internally (Plans 39-08/39-09); the dispatch chain only decides WHICH panel, never who may act.                                |
| T-39-16-03 | Information Disclosure | Highlighting hexes the viewer may not act on | mitigate    | Valid-hex derivation in the store is already gated on `myTeam === activeTeam` / `gkBoxEntryTeam` (Plan 39-05), so the non-acting manager sees no destination tints.    |
| T-39-16-04 | Denial of Service      | Double-click on Start 2nd Half               | mitigate    | Server per-room `isProcessing` mutex plus the engine's idempotent same-team confirm; the button also disappears once this manager has confirmed.                       |
| T-39-16-SC | Tampering              | npm/pip/cargo installs                       | accept      | No packages installed this phase.                                                                                                                                      |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test` green
- `pnpm build` green
- Zero occurrences of the removed half-time tooltip string
</verification>

<success_criteria>
All eight new phases dispatch to their own panels; the ball marker shows during each; penalty and box-entry reposition destinations use the existing `safe` tint and the penalty spot uses the existing gold required ring with no new highlight type; both managers see an actionable Start 2nd Half button and a waiting state after confirming.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-16-SUMMARY.md` when done
</output>
