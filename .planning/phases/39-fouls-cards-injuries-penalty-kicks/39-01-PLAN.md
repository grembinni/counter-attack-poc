---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/pitch.ts
  - packages/shared/src/pitch.test.ts
  - packages/shared/src/events.ts
  - packages/server/src/gameEngine.ts
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/ActionLog.test.tsx
autonomous: true
requirements:
  [
    FOUL-01,
    FOUL-02,
    FOUL-03,
    FOUL-04,
    FOUL-05,
    CARD-01,
    CARD-02,
    CARD-03,
    CARD-04,
    INJURY-01,
    INJURY-02,
    INJURY-03,
    INJURY-04,
    GKDIVE-01,
    GKDIVE-02,
    GKDIVE-03,
    GKDIVE-04,
    GKDIVE-05,
    PEN-01,
    PEN-02,
    PEN-03,
    FK-01,
    SETTINGS-01,
    SETTINGS-02,
    SETTINGS-03,
  ]

must_haves:
  truths:
    - 'Every Phase 39 GamePhase, ActionEventType, GameState field and PlayerPiece field exists in packages/shared/src/types.ts and the whole monorepo typechecks'
    - 'A LOOSE_BALL_LAND log entry shows the scatter direction and the distance-die value alongside the from/to coordinates (D-15)'
    - 'buildInitialGameState accepts and stores the three new Fouls/Booking/Injury toggles on GameState'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'Phase 39 data model — phases, events, GameState/PlayerPiece fields'
      contains: "'FOUL_CHOICE'"
    - path: 'packages/shared/src/pitch.ts'
      provides: 'PENALTY_SPOT constant'
      contains: 'PENALTY_SPOT'
    - path: 'packages/shared/src/events.ts'
      provides: 'Phase 39 client socket events + extended settings payloads'
      contains: 'GAME_FOUL_CHOICE'
    - path: 'packages/client/src/components/ActionLog.tsx'
      provides: 'formatEvent cases for every new Phase 39 ActionEvent'
      contains: "case 'FOUL_CALLED'"
  key_links:
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: 'packages/shared/src/types.ts GamePhase'
      via: 'exhaustive PHASE_LABEL Record'
      pattern: 'FOUL_CHOICE:'
    - from: 'packages/server/src/gameEngine.ts LOOSE_BALL branch'
      to: "ActionEvent 'LOOSE_BALL_LAND'"
      via: 'direction/distance fields on the constructed event'
      pattern: 'direction,'
---

<objective>
Land the entire Phase 39 contract surface in one plan, then immediately repair every compile-time consumer that the new union members break, so waves 2+ start from a green `pnpm build` / `pnpm test`.

This plan implements the data model behind all 25 phase requirements (FOUL-01..05, CARD-01..04, INJURY-01..04, GKDIVE-01..05, PEN-01..03, FK-01, SETTINGS-01..03) and delivers D-15 (loose-ball log fix) end-to-end.

Purpose: `PHASE_LABEL` in `GameBoard.tsx` is an exhaustive `Record<GamePhase, string>` and `formatEvent` in `ActionLog.tsx` is an exhaustive switch with a declared non-optional return type. Adding a `GamePhase` or `ActionEvent` member anywhere breaks the client typecheck until both are updated. Splitting the contract from its registrations would leave the repo red across a wave boundary.

Output: shared types/events/constants, the two exhaustive-consumer registrations, `buildInitialGameState` toggle plumbing, and the D-15 loose-ball event extension.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-PATTERNS.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

**Shared types (`packages/shared/src/types.ts`)**

- `PlayerPiece.injuryCount`, `PlayerPiece.yellowCards`, `PlayerPiece.redCarded`
- `GamePhase`: `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`
- `ActionEventType`: `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`
- `LOOSE_BALL_LAND.direction`, `LOOSE_BALL_LAND.distance`
- `GameState`: `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`

**Shared constants/helpers**

- `PENALTY_SPOT` (`packages/shared/src/pitch.ts`)
- `packages/shared/src/fouls.ts`: `FOUL_TRIGGER_DIE`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`
- `validateDiveAtFeetDistance` (`packages/shared/src/shotValidator.ts`)

**Shared events (`packages/shared/src/events.ts`)**

- `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`
- `ROOM_SETTINGS_CONFIRM` payload keys `fouls`/`booking`/`injury`; three extra `ROOM_SETTINGS_CONFIRMED` boolean args

**Server**

- `packages/server/src/gameEngine.ts`: `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `buildInitialGameState` params `foulsEnabled`/`bookingEnabled`/`injuryEnabled`
- `packages/server/src/roomStore.ts`: `Room.foulsEnabled`, `Room.bookingEnabled`, `Room.injuryEnabled`

**Client**

- Components `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel` (+ `.module.css` + tests)
- `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`
- CSS custom properties `--color-card-yellow`, `--color-card-red` (`packages/client/src/styles/tokens.css`)

<tasks>

<task type="auto">
  <name>Task 1: Declare the Phase 39 data model in shared types and add PENALTY_SPOT</name>
  <files>packages/shared/src/types.ts, packages/shared/src/pitch.ts, packages/shared/src/pitch.test.ts</files>
  <read_first>
    - packages/shared/src/types.ts (lines 1-160 for PlayerPiece/RefereeCard/ActionEventType, 150-580 for the ActionEvent union, 577-662 for GamePhase/LastActionType, 898-1353 for GameState)
    - packages/shared/src/pitch.ts (PITCH_REGIONS, homePenaltyArea/awayPenaltyArea definitions and the GOAL_R_VALUES precedent)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md (Pitfall 6 — no `PlayerAttributes` type exists; these fields are 100% new on `PlayerPiece`)
  </read_first>
  <action>
Add every Phase 39 type member listed under "Artifacts this phase produces" above. Concretely:

`PlayerPiece` (currently `types.ts:14-43`) gains three OPTIONAL fields so no existing construction site breaks:
`injuryCount?: number` (0 = uninjured, 1 = injured once, 2+ = INJURY-03 further injuries), `yellowCards?: 0 | 1 | 2`, `redCarded?: boolean`.
Document on `injuryCount` that INJURY-02's "-1 to all attributes, floored at 1" is applied by MUTATING the stored numeric attributes at injury time (see Task-note below), so `injuryCount` is a counter for INJURY-03/CARD-02 bookkeeping and badge rendering, not a live modifier.

DECISION (planner, resolves RESEARCH.md Assumption A2): INJURY-02 is implemented as a stored decrement of `pace`, `shooting`, `tackling`, `dribbling`, `saving`, `handling`, `resilience`, `aerialAbility`, `highPass` (each floored at 1), NOT as a `computeCombinedScore` penalty-array entry. Rationale to record in the comment: `pace` is a movement budget consumed by `moveValidator`/`applyMove`, never an input to `computeCombinedScore` — the penalty-array approach cannot satisfy INJURY-02's literal "all attributes". Stored mutation is also safe against Undo because `applyUndo` already treats a resolved `TACKLE_ATTEMPT`/`STEAL_ATTEMPT` as an unconditional boundary (`gameEngine.ts` ~1646), so Undo can never cross back over the roll that caused the injury.

`GamePhase` (currently `types.ts:577-624`) gains, appended after `CORNER_KICK_FINAL_SETUP` and before `HALF_TIME`, with per-member comments naming the requirement:
`'FOUL_CHOICE'` (FOUL-03), `'GK_DIVE_AT_FEET_PROMPT'` (GKDIVE-02/D-07), `'GK_BOX_ENTRY_PROMPT'` (D-10), `'GK_BOX_ENTRY_MOVE'` (D-10), `'PENALTY_KICK_SETUP_ATTACKING'` (PEN-02/D-08), `'PENALTY_KICK_SETUP_DEFENDING'` (PEN-02/D-08), `'PENALTY_KICK_TAKER_SELECT'` (PEN-02), `'PENALTY_KICK'` (PEN-01).

`ActionEventType` (currently `types.ts:82-143`) gains `'FOUL_CALLED'`, `'INJURY_CHECK'`, `'BOOKING_CHECK'`, `'FOUL_CHOICE_MADE'`, `'GK_DIVE_AT_FEET'`, `'GK_DIVE_AT_FEET_DECLINED'`, `'GK_BOX_ENTRY_MOVE'`, `'PENALTY_KICK_WINDOW_ADVANCE'`, `'PENALTY_KICK_TAKER_PLACED'`, `'PENALTY_KICK'`, `'SECOND_HALF_CONFIRM'`. Add the standing comment that none of these may be collapsed into the generic `DICE_ROLL` type (STATE.md v1.6 pitfall — reusing `DICE_ROLL` reactivates a dormant full-slot Undo lockout).

`ActionEvent` union gains matching variants:

- `FOUL_CALLED`: `{ type; defenderId: string; victimId: string; hex: HexCoord; source: 'TACKLE' | 'STEAL' | 'GK_DIVE_AT_FEET'; defenderDie: number; professional: boolean; timestamp: number }`
- `INJURY_CHECK`: `{ type; victimId: string; die: number; resilience: number; injured: boolean; injuryCount: number; timestamp: number }`
- `BOOKING_CHECK`: `{ type; defenderId: string; die: number; leniency: number; card: 'none' | 'yellow' | 'red'; secondYellow: boolean; professional: boolean; timestamp: number }`
- `FOUL_CHOICE_MADE`: `{ type; team: 'home' | 'away'; choice: 'continue' | 'restart'; restart: 'FREE_KICK' | 'PENALTY' | null; timestamp: number }`
- `GK_DIVE_AT_FEET`: `{ type; gkId: string; carrierId: string; gkDie: number; carrierDie: number; gkCombined: number; carrierCombined: number; distance: number; savingPenalty: number; result: 'SUCCESS' | 'FAIL'; timestamp: number; ballAfter: { position: HexCoord; carrierId: string | null } }`
- `GK_DIVE_AT_FEET_DECLINED`: `{ type; gkId: string; carrierId: string; timestamp: number }`
- `GK_BOX_ENTRY_MOVE`: `{ type; gkId: string; from: HexCoord; to: HexCoord; timestamp: number }`
- `PENALTY_KICK_WINDOW_ADVANCE`: `{ type; from: 'ATTACKING' | 'DEFENDING'; timestamp: number }`
- `PENALTY_KICK_TAKER_PLACED`: `{ type; pieceId: string; hex: HexCoord; timestamp: number }`
- `PENALTY_KICK`: `{ type; takerId: string; gkId: string; takerDie: number; gkDie: number; takerCombined: number; gkCombined: number; result: 'GOAL' | 'SAVED' | 'TIE'; timestamp: number; ballAfter: { position: HexCoord; carrierId: string | null } }`
- `SECOND_HALF_CONFIRM`: `{ type; team: 'home' | 'away'; bothConfirmed: boolean; timestamp: number }`

Extend the existing `LOOSE_BALL_LAND` variant (`types.ts:356-362`) with two REQUIRED fields (D-15): `direction: 1 | 2 | 3 | 4 | 5 | 6` and `distance: 1 | 2 | 3 | 4 | 5 | 6`. There is exactly one construction site (`gameEngine.ts` ~3439) — Task 3 patches it. Required (not optional) is deliberate: it makes the compiler point at any future construction site.

`GameState` gains all fields listed in the Artifacts block, all OPTIONAL, following the `goalKick*`/`cornerKick*` per-restart dedicated-field-cluster convention actually shipped in Phases 37/38 (NOT the unbuilt generic `RestartSetupState` refactor):

- Toggles: `foulsEnabled?: boolean`, `bookingEnabled?: boolean`, `injuryEnabled?: boolean`. Document that every read site must test `=== true`, mirroring the existing `outOfBoundsEnabled` comment at `types.ts:1225`.
- Foul cluster: `foulDefenderId?: string | null`, `foulVictimId?: string | null`, `foulHex?: HexCoord | null`, `foulSource?: 'TACKLE' | 'STEAL' | 'GK_DIVE_AT_FEET' | null`, `foulResume?: { phase: GamePhase; activeTeam: 'home' | 'away'; attackingTeam: 'home' | 'away'; movementSlot: MovementSlot | null; lastActionType: LastActionType | null } | null` (shape modelled on the existing `freeMoveResume` field).
- GK dive-at-feet cluster: `gkDiveAtFeetUsedByTeam?: { home: boolean; away: boolean } | null` (D-09 SHARED cap with the shot-block `GK_DIVE`), `gkDiveAtFeetTeam?: 'home' | 'away' | null`, `gkDiveAtFeetGkId?: string | null`, `gkDiveAtFeetCarrierId?: string | null`, `gkDiveAtFeetDistance?: number | null`, `gkDiveAtFeetResume?: { phase: GamePhase; activeTeam: 'home' | 'away'; movementSlot: MovementSlot | null } | null`.
- Box-entry cluster (D-10/D-11): `gkBoxEntryUsedByTeam?: { home: boolean; away: boolean } | null` (INDEPENDENT of the D-09 cap — document this explicitly), `gkBoxEntryTeam?: 'home' | 'away' | null`, `gkBoxEntryResume?: { phase: GamePhase; activeTeam: 'home' | 'away'; movementSlot: MovementSlot | null } | null`.
- Penalty-kick cluster: `penaltyKickTeam?: 'home' | 'away' | null` (the KICKING team), `penaltyKickSpot?: HexCoord | null`, `penaltyKickEligibleIds?: { attacking: readonly string[]; defending: readonly string[] } | null`, `penaltyKickUsedPace?: Readonly<Record<string, number>> | null`, `penaltyKickTakerId?: string | null`.
- Half-time cluster (D-16): `secondHalfConfirmed?: { home: boolean; away: boolean } | null`. Document that this deliberately copies `headerConfirmed`'s GameState-scoped `{home, away}` shape, NOT `LINEUP_CONFIRM`'s Room-scoped flags (RESEARCH.md Pitfall 4 — half-time is mid-match, `Room` fields have no path into `broadcastState`).

In `packages/shared/src/pitch.ts`, add after `GOAL_R_VALUES`:
`export const PENALTY_SPOT: Readonly<Record<'home' | 'away', HexCoord>> = { home: { q: 4, r: 13 }, away: { q: 32, r: 13 } };`
Key it by the DEFENDING team (the team whose penalty area contains the spot). Comment the derivation: `homePenaltyArea` is `q<=5, r 5..19` and `homeGoal` is `q=0, r 10..16`, so the spot sits on the goal centre-line (`r=13`) two thirds of the way out (`q=4`); the away spot is the `36 - q` mirror, matching the existing `q_away = 36 - q_home` mirroring convention.

Add a `pitch.test.ts` case asserting both `PENALTY_SPOT` hexes satisfy `isPitchHex` and `isInRegion(spot, 'homePenaltyArea' | 'awayPenaltyArea')` respectively, and that neither is inside the corresponding six-yard box.

Do NOT change `RefereeCard` or the `randomInt(1, 7)` leniency roll — CONTEXT.md "Explicitly reconsidered, no change".
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared typecheck && pnpm --filter @counter-attack/shared test -- pitch.test.ts</automated>
</verify>
<acceptance_criteria> - `grep -c "FOUL_CHOICE\|GK_DIVE_AT_FEET_PROMPT\|GK_BOX_ENTRY_PROMPT\|GK_BOX_ENTRY_MOVE\|PENALTY_KICK_SETUP_ATTACKING\|PENALTY_KICK_SETUP_DEFENDING\|PENALTY_KICK_TAKER_SELECT" packages/shared/src/types.ts` returns at least 7 - `packages/shared/src/types.ts` contains `injuryCount?: number`, `yellowCards?: 0 | 1 | 2`, `redCarded?: boolean` inside the `PlayerPiece` type block - The `LOOSE_BALL_LAND` ActionEvent variant declares `direction` and `distance` as required (no `?`) - `packages/shared/src/pitch.ts` exports `PENALTY_SPOT` with `home` = `{ q: 4, r: 13 }` and `away` = `{ q: 32, r: 13 }` - `pnpm --filter @counter-attack/shared typecheck` exits 0 - `pnpm --filter @counter-attack/shared test -- pitch.test.ts` passes, including the new PENALTY_SPOT assertions - `packages/shared/src/types.ts` `RefereeCard` is byte-identical to its pre-plan definition
</acceptance_criteria>
<done>All Phase 39 phases, event types, event variants, GameState fields and PlayerPiece fields compile in `packages/shared`; PENALTY_SPOT exists and is covered by a passing test.</done>
</task>

<task type="auto">
  <name>Task 2: Register the Phase 39 socket events and extend the settings payloads</name>
  <files>packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/events.ts (full file — `ClientEvents`/`ServerEvents` maps, the `ClientToServerEvents`/`ServerToClientEvents` typed-payload interfaces, and the Phase 37/38 comment convention explaining which flows deliberately reuse `GAME_MOVE`/`GAME_END_TURN`)
    - packages/shared/src/types.ts (as modified by Task 1)
  </read_first>
  <action>
Add five new `ClientEvents` entries plus their typed payload signatures, following the existing naming style (`game:` kebab-case values) and the Phase 37/38 doc-comment convention:

- `GAME_FOUL_CHOICE: 'game:foul-choice'` — payload `(choice: 'continue' | 'restart') => void` (FOUL-03/D-01). Comment: mirrors `GAME_GK_RESTART`'s choice-payload shape.
- `GAME_GK_DIVE_AT_FEET: 'game:gk-dive-at-feet'` — payload `(accept: boolean) => void` (GKDIVE-02/D-07).
- `GAME_GK_BOX_ENTRY_RESPONSE: 'game:gk-box-entry-response'` — payload `(accept: boolean) => void` (D-10).
- `GAME_GK_BOX_ENTRY_MOVE: 'game:gk-box-entry-move'` — payload `(to: HexCoord) => void` (D-10). Comment: mirrors `GAME_GK_DIVE`'s single-hex payload.
- `GAME_PENALTY_KICK_TAKER: 'game:penalty-kick-taker'` — payload `(pieceId: string) => void` (PEN-02). Comment: mirrors `GAME_CORNER_KICK_TAKER`; the destination hex is NOT part of the payload (the server places the taker on `penaltyKickSpot`).

Add an explicit comment stating that the PEN-02 penalty-kick reposition windows deliberately reuse the existing `GAME_MOVE` + `GAME_END_TURN` events (handled inside `applyMove`/`applyEndTurn` dispatch), exactly as the goal-kick and corner-kick reposition windows already do — do not add redundant reposition events.

Extend `ROOM_SETTINGS_CONFIRM`'s payload object type with `fouls: boolean; booking: boolean; injury: boolean` alongside the existing `speed`/`teamType`/`draftPools`/`outOfBounds` keys (SETTINGS-01/D-12).

Extend `ROOM_SETTINGS_CONFIRMED`'s server-to-client signature with three additional trailing boolean args in the order `fouls`, `booking`, `injury`, after the existing `outOfBounds` arg. Keep the positional-arg style the existing signature already uses; do not convert it to an object.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared typecheck && pnpm --filter @counter-attack/shared build</automated>
</verify>
<acceptance_criteria> - `grep -c "game:foul-choice\|game:gk-dive-at-feet\|game:gk-box-entry-response\|game:gk-box-entry-move\|game:penalty-kick-taker" packages/shared/src/events.ts` returns 5 - The `ClientEvents.GAME_FOUL_CHOICE` payload type is exactly `(choice: 'continue' | 'restart') => void` - `ROOM_SETTINGS_CONFIRM`'s payload type declares `fouls: boolean`, `booking: boolean`, `injury: boolean` - `ROOM_SETTINGS_CONFIRMED`'s signature has three boolean parameters after the existing `outOfBounds` parameter - `pnpm --filter @counter-attack/shared build` exits 0
</acceptance_criteria>
<done>All five new client events and the extended settings payloads compile and are exported from the shared barrel.</done>
</task>

<task type="auto">
  <name>Task 3: Repair every exhaustive consumer — gameEngine LOOSE_BALL_LAND + toggles, GameBoard PHASE_LABEL, ActionLog formatEvent</name>
  <files>packages/server/src/gameEngine.ts, packages/client/src/components/GameBoard.tsx, packages/client/src/components/ActionLog.tsx, packages/client/src/components/ActionLog.test.tsx</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 3326-3480 (the `LOOSE_BALL` case in `applyRoll`, where `direction` and `distance` are already in scope, and the single `LOOSE_BALL_LAND` construction site at ~3439)
    - packages/server/src/gameEngine.ts lines 329-391 (`buildInitialGameState` signature and returned `GameState` literal, including the `outOfBoundsEnabled: boolean = false` trailing parameter added in Phase 37)
    - packages/client/src/components/GameBoard.tsx lines 27-78 (the exhaustive `PHASE_LABEL: Record<GamePhase, string>`)
    - packages/client/src/components/ActionLog.tsx lines 320-1043 (`formatEvent`'s switch, the `P`/`PNamed`/`pieceColorOf`/`pieceNum` helpers, and the `CORNER_KICK_ACCURACY`/`GOAL_KICK`/`TACKLE_ATTEMPT` vs-comparison formatting precedents)
    - packages/client/src/components/ActionLog.test.tsx (existing test structure and store-mocking helper)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Copywriting Contract — `LOOSE_BALL_LAND` extended log entry wording, Body role 13px for all new log entries)
  </read_first>
  <action>
Three registrations, all mechanical, all required to restore `pnpm build`:

1. `gameEngine.ts` — LOOSE_BALL_LAND (D-15). In the `LOOSE_BALL` case of `applyRoll`, the local consts `direction` and `distance` are already derived from `d1`/`d2` at the top of the case. Add `direction,` and `distance,` to the `looseBallLandEvent` object literal. No other logic changes in that branch — the OOB-05 "disabled path stays identical" guarantee block must remain byte-for-byte unchanged apart from these two added properties.

2. `gameEngine.ts` — `buildInitialGameState` toggles (SETTINGS-01/FOUL-05/CARD-04/INJURY-04). Append three trailing parameters after the existing `outOfBoundsEnabled: boolean = false`: `foulsEnabled: boolean = false`, `bookingEnabled: boolean = false`, `injuryEnabled: boolean = false`. Add all three to the returned `GameState` literal next to `outOfBoundsEnabled`. Also initialise `secondHalfConfirmed: null`, `gkDiveAtFeetUsedByTeam: null`, and `gkBoxEntryUsedByTeam: null` in that literal. Defaults stay `false` so the server-side safe default is "feature off" even if a caller forgets to pass them — Plan 39-03 wires the real values from `Room`.

3. `GameBoard.tsx` — `PHASE_LABEL`. Add one entry per new `GamePhase`, using the UI-SPEC's scoreboard vocabulary (uppercase, matching the existing `GOAL KICK SETUP` / `CORNER KICK` style):
   `FOUL_CHOICE: 'FOUL'`, `GK_DIVE_AT_FEET_PROMPT: 'DIVE AT FEET'`, `GK_BOX_ENTRY_PROMPT: 'KEEPER RESPONSE'`, `GK_BOX_ENTRY_MOVE: 'KEEPER RESPONSE'`, `PENALTY_KICK_SETUP_ATTACKING: 'PENALTY SETUP'`, `PENALTY_KICK_SETUP_DEFENDING: 'PENALTY SETUP'`, `PENALTY_KICK_TAKER_SELECT: 'PENALTY TAKER'`, `PENALTY_KICK: 'PENALTY KICK'`.
   Change nothing else in `GameBoard.tsx` in this plan — panel dispatch and the half-time button are Plan 39-16's scope.

4. `ActionLog.tsx` — `formatEvent` cases for all eleven new `ActionEvent` variants plus the extended `LOOSE_BALL_LAND`. This is D-02's "logs always" half: EVERY roll gets a log line regardless of outcome. Follow the established prefix + team-coloured player-name style, reusing `PNamed`, `P`, `pieceColorOf`, `pieceNum`. Concrete formats:
   - `LOOSE_BALL_LAND` (D-15): keep the existing `[LOOSE BALL]` prefix and extend the content to name the direction and the distance-die value alongside the coordinates, e.g. `scatters NE (3 hexes): 12,7 → 15,7`. Map the direction die to the labels already documented in `scoreUtils.ts`'s `LOOSE_BALL_CUBE_DIRECTIONS`: 1=E, 2=NE, 3=NW, 4=W, 5=SW, 6=SE — declare that mapping as a module-scope const in `ActionLog.tsx` rather than an inline ternary chain. Use `hex` (singular) when `distance === 1`.
   - `FOUL_CALLED`: prefix `[FOUL]`, colour from `defenderId`; content names the fouling player, the victim, the defender's die, and appends ` — Professional Foul (DOGSO)` when `professional` is true.
   - `INJURY_CHECK`: prefix `[INJURY ✓]` when `injured`, `[INJURY ✗]` otherwise; content uses the existing vs-comparison shape: victim name, `die: {die} vs Resilience {resilience}`, then `Injured` / `No injury`.
   - `BOOKING_CHECK`: prefix `[BOOKING]`; content: defender name, `die: {die} vs Leniency {leniency}`, then `Yellow Card` / `Red Card (2nd Yellow)` / `Red Card` / `No card`, appending ` (DOGSO)` when `professional` is true.
   - `FOUL_CHOICE_MADE`: prefix `[FOUL]`; content `Play continues` or `Free Kick awarded` / `Penalty awarded` keyed off `restart`.
   - `GK_DIVE_AT_FEET`: prefix `[DIVE AT FEET]`; content mirrors `TACKLE_ATTEMPT`'s vs-comparison line exactly (both names, both dice, both combined scores) and appends ` (−1 at 3 hexes)` when `savingPenalty === -1`.
   - `GK_DIVE_AT_FEET_DECLINED`: prefix `[DIVE AT FEET]`; content `{GK name} declined to dive`.
   - `GK_BOX_ENTRY_MOVE`: prefix `[KEEPER RESPONSE]`; content `{GK name} {from.q},{from.r} → {to.q},{to.r}`.
   - `PENALTY_KICK_WINDOW_ADVANCE`: prefix `[PENALTY KICK]`; content `Attacking team finished repositioning` / `Defending team finished repositioning`.
   - `PENALTY_KICK_TAKER_PLACED`: prefix `[PENALTY KICK]`; content `{name} will take the penalty ({hex.q},{hex.r})`.
   - `PENALTY_KICK`: prefix `[PENALTY ✓]` on `GOAL`, `[PENALTY ✗]` on `SAVED`, `[PENALTY]` on `TIE`; content mirrors `SHOT_ATTEMPT`'s vs-comparison shape, explicitly showing the GK's `−2` as a separate term (matching the existing "never a compact parenthetical" comment at `ActionLog.tsx:203`), and ends with `Goal!` / `Saved` / `Tie — loose ball`.
   - `SECOND_HALF_CONFIRM`: prefix `[HALF TIME]`; content `{Home|Away} confirmed` plus ` — starting 2nd half` when `bothConfirmed`.

Extend `ActionLog.test.tsx` with one assertion per new event type verifying the rendered text contains the distinguishing substring above, plus a dedicated D-15 test asserting a `LOOSE_BALL_LAND` with `direction: 2, distance: 3` renders both `NE` and `3 hex`.
</action>
<verify>
<automated>pnpm build && pnpm --filter @counter-attack/client test -- ActionLog.test.tsx</automated>
</verify>
<acceptance_criteria> - `pnpm build` exits 0 across all three packages (this is the gate proving both exhaustive consumers were repaired) - `pnpm typecheck` exits 0 - `pnpm --filter @counter-attack/client test -- ActionLog.test.tsx` passes with at least 11 new assertions, one per new event type - A `LOOSE_BALL_LAND` event with `direction: 2, distance: 3` renders text containing both `NE` and `3 hex` - `grep -c "FOUL_CHOICE:\|GK_DIVE_AT_FEET_PROMPT:\|GK_BOX_ENTRY_PROMPT:\|GK_BOX_ENTRY_MOVE:\|PENALTY_KICK_SETUP_ATTACKING:\|PENALTY_KICK_SETUP_DEFENDING:\|PENALTY_KICK_TAKER_SELECT:\|PENALTY_KICK:" packages/client/src/components/GameBoard.tsx` returns 8 - `buildInitialGameState` has exactly three new trailing boolean parameters named `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, each defaulting to `false` - `pnpm --filter @counter-attack/server test` is green (no regression in the existing loose-ball tests)
</acceptance_criteria>
<done>`pnpm build` and `pnpm test` are green with the full Phase 39 contract in place; the loose-ball log shows direction and distance.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                         |
| ------------------------------- | ----------------------------------------------------------------------------------- |
| client socket → server handlers | Untrusted client payloads for every newly declared `ClientEvents` member cross here |
| server engine → broadcast state | New `GameState` fields are broadcast verbatim to both players                       |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                    |
| ---------- | ---------------------- | ----------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-01-01 | Tampering              | New `ClientEvents` declarations                 | mitigate    | This plan declares payload TYPES only and registers zero `socket.on` listeners. Every listener added in Plans 39-11/39-13/39-15 must shape-validate before use (their own T-rows). |
| T-39-01-02 | Information Disclosure | New `GameState` fields in `broadcastState`      | accept      | Both players already receive the full authoritative snapshot every action; the new foul/card/injury/penalty fields carry no per-player secret (unlike the draft-privacy fields).   |
| T-39-01-03 | Tampering              | `LOOSE_BALL_LAND.direction/.distance`           | mitigate    | Both values are the server's own `crypto.randomInt` dice (`d1`/`d2`) already in scope; they are recorded, never read back from the client.                                         |
| T-39-01-04 | Elevation of Privilege | `foulsEnabled`/`bookingEnabled`/`injuryEnabled` | mitigate    | Default `false` on `buildInitialGameState`; only Plan 39-03's host-only `ROOM_SETTINGS_CONFIRM` handler may set them, after a `typeof === 'boolean'` allow-list check.             |
| T-39-01-SC | Tampering              | npm/pip/cargo installs                          | accept      | No packages are installed in this phase (RESEARCH.md: "Not applicable — this phase installs no external packages"); no `[ASSUMED]`/`[SUS]` entries exist.                          |

</threat_model>

<verification>
- `pnpm build` green
- `pnpm typecheck` green
- `pnpm test` green (full monorepo suite, ~60s)
- No new `socket.on` listener registered anywhere in this plan
</verification>

<success_criteria>
Every Phase 39 GamePhase, ActionEventType, ActionEvent variant, GameState field and PlayerPiece field exists and compiles; `PENALTY_SPOT` is defined and tested; all five new client events are declared with typed payloads; the settings payloads carry three new booleans; `ActionLog` renders a distinct line for all eleven new events; a loose-ball landing logs its direction and distance (D-15); `pnpm build` and `pnpm test` are green.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-01-SUMMARY.md` when done
</output>
