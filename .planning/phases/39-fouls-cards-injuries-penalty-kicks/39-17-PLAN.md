---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 17
type: execute
wave: 9
depends_on: ['39-15', '39-16']
files_modified:
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/ActionPanel.test.tsx
  - packages/server/src/__tests__/gameEngine.undoReplay39.test.ts
autonomous: false
requirements:
  [
    FOUL-01,
    FOUL-02,
    FOUL-03,
    FOUL-04,
    CARD-01,
    CARD-02,
    CARD-03,
    INJURY-01,
    INJURY-02,
    INJURY-03,
    GKDIVE-02,
    GKDIVE-03,
    GKDIVE-04,
    GKDIVE-05,
    PEN-01,
    PEN-02,
    PEN-03,
    FK-01,
    SETTINGS-01,
    SETTINGS-02,
    SETTINGS-03,
  ]

must_haves:
  truths:
    - 'The client Undo affordance agrees with the server about whether Undo is available in every new Phase 39 phase'
    - 'Every new ActionEventType is either registered as an Undo boundary or deliberately excluded, with the decision recorded'
    - 'Replay reconstructs a match containing fouls, cards, injuries and a penalty kick without crashing or dropping frames'
    - 'A human confirms the full foul, card, injury, GK-dive and penalty flows in a live two-browser session'
  artifacts:
    - path: 'packages/server/src/__tests__/gameEngine.undoReplay39.test.ts'
      provides: 'Cross-cutting Undo/Replay registration coverage for every Phase 39 event type'
      min_lines: 150
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'Client canUndo mirror kept in sync with applyUndo boundaries'
      contains: 'FOUL_CHOICE_MADE'
  key_links:
    - from: 'packages/client/src/components/ActionPanel.tsx canUndo'
      to: 'packages/server/src/gameEngine.ts applyUndo isBoundary'
      via: 'mirrored boundary event-type list'
      pattern: 'GK_DIVE_AT_FEET'
---

<objective>
Close the phase with the two things that historically break in this codebase and cannot be caught by any single feature plan: the client/server Undo-boundary mirror, and Replay reconstruction of the new event types. Then get a human to confirm the whole thing works in a live two-browser session.

Purpose: STATE.md records this exact defect class shipping twice already (BUG-30/31 and BUG-37) — "new dice-roll event types are invisible to Undo/Replay unless registered in every relevant list (isBoundary server + client mirror, REPLAY_ELIGIBLE_TYPES)". Phase 39 adds eleven new event types and eight new phases, the largest single batch of the milestone.

Output: the client mirror update, a cross-cutting registration test suite, and a blocking human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-VALIDATION.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-15-SUMMARY.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-16-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`, `Room.lastBroadcastBallPosition`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto">
  <name>Task 1: Mirror the new Undo boundaries in the client canUndo derivation</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/ActionPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx lines 260-300 (the `isBoundary` reduce that mirrors `applyUndo`, and the `canUndo` derivation built on it)
    - packages/server/src/gameEngine.ts lines 1644-1700 (`applyUndo`'s `isBoundary` reduce AS MODIFIED by Plans 39-07, 39-10, 39-12 and 39-14 — read the current committed version, not the pre-phase one)
    - packages/client/src/components/CornerKickSetupPanel.tsx lines 230-320 (the precedent for a panel-local boundary mirror and its "must stay in sync with applyUndo's isBoundary" comment)
    - packages/client/src/components/ActionPanel.test.tsx (existing canUndo assertions)
  </read_first>
  <action>
Read the server's current `isBoundary` disjunction and reproduce every Phase 39 term in `ActionPanel.tsx`'s mirror, term for term. As of the preceding plans the new terms are:

- `evt.type === 'GK_DIVE_AT_FEET'` (unconditional, added in Plan 39-12)
- `state.phase === 'FOUL_CHOICE' && evt.type === 'FOUL_CHOICE_MADE'` (Plan 39-10)
- `(state.phase === 'PENALTY_KICK_SETUP_ATTACKING' || state.phase === 'PENALTY_KICK_SETUP_DEFENDING') && evt.type === 'PENALTY_KICK_WINDOW_ADVANCE'` and `state.phase === 'PENALTY_KICK_TAKER_SELECT' && evt.type === 'PENALTY_KICK_TAKER_PLACED'` (Plan 39-07)
- `state.phase === 'GK_BOX_ENTRY_MOVE' && evt.type === 'GK_BOX_ENTRY_MOVE'` (Plan 39-14)
- `state.phase === 'HALF_TIME' && evt.type === 'SECOND_HALF_CONFIRM'` (Plan 39-14)

Verify each against the actual committed server code before writing it — if a preceding plan named a term differently, the SERVER is the source of truth and the mirror must match it, not this list.

Add or extend the existing "must stay in sync with applyUndo's isBoundary" comment to name Phase 39 and list the new terms.

Also confirm the client's `moveTypeForPhase` equivalent (if `ActionPanel` carries one) handles the new reposition phases. The penalty reposition windows and the box-entry move emit plain `MOVE` and `GK_BOX_ENTRY_MOVE` events respectively; if the client maps phase → expected move event type anywhere, add the `GK_BOX_ENTRY_MOVE` mapping and leave the penalty phases on the default `MOVE` branch.

Extend `ActionPanel.test.tsx` with assertions that `canUndo` is false immediately after each new boundary event in its guarding phase, and that the pre-existing canUndo behaviour in `MOVE`/`HIGH_PASS_MOVE`/`FREE_KICK_SETUP` is unchanged.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- ActionPanel.test.tsx</automated>
</verify>
<acceptance_criteria> - Every Phase 39 term present in `gameEngine.ts`'s `applyUndo` `isBoundary` disjunction also appears in `ActionPanel.tsx`'s mirror (verify by reading both and listing them in the task notes) - `grep -c "GK_DIVE_AT_FEET\|FOUL_CHOICE_MADE\|PENALTY_KICK_WINDOW_ADVANCE\|PENALTY_KICK_TAKER_PLACED\|GK_BOX_ENTRY_MOVE\|SECOND_HALF_CONFIRM" packages/client/src/components/ActionPanel.tsx` returns at least 6 - `pnpm --filter @counter-attack/client test -- ActionPanel.test.tsx` passes with at least 6 new assertions - The existing canUndo assertions for `MOVE`, `HIGH_PASS_MOVE` and `FREE_KICK_SETUP` still pass unchanged
</acceptance_criteria>
<done>The client Undo affordance and the server Undo authority agree in every new Phase 39 phase.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-cutting Undo/Replay registration suite for all eleven new event types</name>
  <files>packages/server/src/__tests__/gameEngine.undoReplay39.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts — `applyUndo` in full, `REPLAY_ELIGIBLE_TYPES`, and `buildReplayFrames` (particularly the `flushMoveGroup` handling and the comments explaining which event types are deliberately excluded and why)
    - packages/server/src/__tests__/replay.integration.test.ts (the existing replay-frame reconstruction assertions)
    - packages/server/src/__tests__/gameEngine.phase26-undo.test.ts (the existing Undo regression suite this one complements)
    - packages/shared/src/types.ts (the eleven new `ActionEventType` members)
  </read_first>
  <action>
Create `gameEngine.undoReplay39.test.ts` as a registration audit — a single suite whose job is to make the BUG-30/31/37 defect class impossible to reintroduce.

1. Enumerate the eleven new `ActionEventType` members as a literal array in the test. For each, assert exactly one of two things holds and record which: it appears in `REPLAY_ELIGIBLE_TYPES`, or it does not AND the event variant has no `ballAfter` field. This turns the "carries `ballAfter` → must be replay-eligible" rule into an executable invariant rather than a comment. Expected classification per the preceding plans: `PENALTY_KICK` and `GK_DIVE_AT_FEET` are eligible; the other nine are not.
2. Assert `buildReplayFrames` completes without throwing on a synthetic `eventLog` containing all eleven new event types interleaved with `MOVE` and `GOAL`, and that the frame count is greater than zero. This is the direct guard against the `CORNER_KICK_CLEAR_OUT_MOVE` crash class (a missing switch case crashing the render loop).
3. Assert `applyUndo` returns `{ ok: false }` (locked) when the current slot's events include any of `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `GK_DIVE_AT_FEET` or `PENALTY_KICK` — a committed dice outcome must never be reachable by Undo.
4. Assert no Phase 39 code path ever emits the generic `DICE_ROLL` type: drive a foul, a dive-at-feet and a penalty duel through the engine and assert the resulting `eventLog` contains zero `DICE_ROLL` events. STATE.md names reusing `DICE_ROLL` as the specific anti-pattern that reactivates a dormant full-slot Undo lockout.
5. Assert `applyUndo`'s boundary reduce recognises each new phase-guarded boundary term by constructing a state in the guarding phase with that event as the last entry and checking the resulting slice is empty.
6. Assert an injury's stored attribute degradation survives an Undo of the following action — the degradation must not be reversible, which is the property that makes stored mutation (rather than a penalty array) safe.
   </action>
   <verify>
   <automated>pnpm --filter @counter-attack/server test -- gameEngine.undoReplay39.test.ts</automated>
   </verify>
   <acceptance_criteria> - `pnpm --filter @counter-attack/server test -- gameEngine.undoReplay39.test.ts` passes with at least 20 assertions - The suite enumerates all eleven new `ActionEventType` members explicitly in a literal array - The `buildReplayFrames` test feeds all eleven types and asserts no throw - The `DICE_ROLL` test drives three distinct Phase 39 flows and asserts zero `DICE_ROLL` events - `pnpm --filter @counter-attack/server test` (full server suite) is green - `pnpm test` (full monorepo suite) is green
   </acceptance_criteria>
   <done>Every new event type's Undo and Replay registration is asserted, not assumed.</done>
   </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live two-browser verification of the full Phase 39 feature set</name>
  <what-built>
Phase 39 in full: foul detection on a defender die of 1, the injury and booking chain, Professional Fouls, persistent card and injury indicators, the continue-or-restart choice, foul-triggered free kicks, the goalkeeper dive-at-feet interrupt with its once-per-cycle cap shared with the shot-block dive, the box-entry goalkeeper response move, penalty kicks with full-squad repositioning and the -2 goalkeeper penalty, the four game-creation toggles, the loose-ball log fix, and the second-half mutual-confirm gate.

All automated suites are green: `pnpm test` across all three packages.
</what-built>
<how-to-verify>
Start the app (`pnpm --filter @counter-attack/server dev` and `pnpm --filter @counter-attack/client dev`) and open two browser windows as two managers.

**A. Settings (SETTINGS-01/02/03, D-12/D-13/D-14)**

1. On the Game Settings screen, confirm all four checkboxes — Fouls, Booking, Injury, Out-of-Bounds / Restarts — are CHECKED by default.
2. Uncheck Fouls. Confirm Booking and Injury both grey out, show `(requires Fouls)`, and cannot be clicked.
3. Re-check Fouls, confirm all four are on, and start a match.

**B. Foul, injury, booking (FOUL-01/02, CARD-01, INJURY-01, D-02/D-03)**

4. Play until a tackle or steal rolls a defender die of 1. In the Action Log, confirm you see the duel line, then a `[FOUL]` line, then an `[INJURY ...]` line showing the die vs. Resilience, then a `[BOOKING]` line showing the die vs. Leniency — in that order, every time, whatever the outcomes.
5. Confirm a `Foul!` banner appears. If the injury check actually injured, confirm a `... is Injured!` banner follows it. If a card was actually issued, confirm a card banner follows, showing a small yellow or red card rectangle. Confirm banners appear in SEQUENCE, not just the last one.
6. Confirm NO banner appears for a check that produced no injury / no card, but the log line is still there.

**C. Indicators (D-04/D-05, CARD-02, INJURY-02/03)**

7. Confirm the booked player now carries a small coloured card badge on the board, in the corner OPPOSITE the ball-possession dot.
8. Confirm an injured player carries a white plus-sign badge in that same corner, and that a player who is both shows the plus sign layered ON TOP of the card.
9. Select the injured player and confirm the player card panel shows an `INJ` chip and that the displayed attribute numbers are each 1 lower than before the injury (never below 1).
10. Get a player to a second yellow. Confirm the badge turns red, the banner reads `Red Card (2nd Yellow)`, and that player can no longer be selected to move.

**D. Continue or restart (FOUL-03, FK-01, D-01)**

11. On the next foul, confirm the FOULED manager (and only that manager) sees a two-button panel: `Continue Play` and `Take the Free Kick`. Confirm the other manager sees a waiting message.
12. Click `Continue Play` once and confirm the movement phase resumes exactly where it was — same slot, same team.
13. On a later foul, click `Take the Free Kick` and confirm the normal free-kick setup flow starts with the ball at the foul spot.

**E. Goalkeeper dive at feet (GKDIVE-02/03/04/05, D-07, D-09)**

14. Dribble a carrier to within 3 hexes of the opposing goalkeeper, across the goal. Confirm the DEFENDING manager gets a `Dive at Feet?` prompt with `Dive` / `Decline`.
15. From exactly 3 hexes, confirm the prompt mentions the -1 penalty.
16. Click `Decline` and confirm play resumes and the prompt reappears on the next qualifying step.
17. Click `Dive`. Confirm the log shows the duel with both dice. If it succeeded and the landing hex was occupied, confirm the occupying piece (and the ball if it was there) moved one hex further along the dive direction.
18. Immediately after using the dive, declare a shot with the same goalkeeper's team defending. Confirm the goalkeeper does NOT get the shot-block reposition window this movement cycle.
19. Confirm the dive prompt does not reappear again in the same movement cycle, but does become available again in the next one.

**F. Box-entry goalkeeper response (D-10/D-11)**

20. Play the ball into the penalty area by a PASS (not a shot). Confirm the defending manager gets a `Goalkeeper Reposition?` prompt.
21. Click `Reposition`, then click an adjacent hex. Confirm the goalkeeper moves exactly one hex and play resumes.
22. Confirm this prompt does not reappear on a second entry in the same movement cycle — and confirm that having used it did NOT prevent a `Dive at Feet?` prompt later in that same cycle.

**G. Penalty kick (GKDIVE-03, PEN-01/02/03, D-08)**

23. Trigger a goalkeeper die of 1 during a dive at feet. Confirm the foul panel's second button reads `Take the Penalty` (not `Take the Free Kick`), and that choosing it starts the penalty setup.
24. Confirm a `Penalty Kick!` banner appears and the ball sits on the penalty spot with a gold ring.
25. In the attacking window, confirm you can move ANY of your on-pitch players, as many hexes as you like, one click at a time — no 6-hex cap. Confirm the panel says only the penalty taker and goalkeeper may stand in the penalty area, and that trying to move an ordinary player into the area is rejected with an inline error.
26. Confirm, then confirm the defending window as the other manager.
27. Choose a penalty taker by clicking one of your outfielders. Confirm they are placed on the spot with the ball.
28. Take the penalty. Confirm the log shows the taker's score versus the goalkeeper's score, with the goalkeeper's `-2` shown as its own term. Confirm a goal, a save, or (on a tie) a Loose Ball starting from the penalty spot.

**H. Bug fixes (D-15, D-16)**

29. Cause any Loose Ball. Confirm the `[LOOSE BALL]` log entry now names the scatter DIRECTION (E / NE / NW / W / SW / SE) and the DISTANCE die value, alongside the coordinates.
30. Play to half time. Confirm BOTH managers see an enabled `Start 2nd Half` button. Click it in one window and confirm that window switches to `Waiting for ... to start the 2nd half…` while the other still shows its button. Click the second one and confirm the second half begins. Repeat once in the opposite order and confirm it works the same way.

**I. Toggles off**

31. Start a fresh match with Fouls UNCHECKED. Confirm no foul, injury or booking ever occurs, no dive-at-feet prompt ever appears, and play behaves exactly as it did before this phase.
32. Start a match with Fouls on but Booking off. Confirm fouls and injuries occur but no card is ever issued. Then Fouls on, Injury off: cards but no injuries.
    </how-to-verify>
    <resume-signal>Type "approved" if every step behaves as described, or describe each step number that failed and what you saw instead.</resume-signal>
    <verify>
    <automated>pnpm test</automated>
    </verify>
    <acceptance_criteria> - `pnpm test` (full monorepo suite) is green before the checkpoint is presented - The human has walked all 32 numbered steps in a live two-browser session - Every failure is recorded with its step number and observed behaviour in the plan summary - Approval is explicit ("approved"), not inferred from silence
    </acceptance_criteria>
    <done>A human has confirmed the full Phase 39 feature set behaves as specified in a live two-browser match, or has recorded the specific step failures for gap closure.</done>
    </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                     |
| ------------------------------- | ------------------------------------------------------------------------------- |
| client Undo affordance → server | A client offering Undo where the server forbids it is a UX desync, not a breach |
| replay stream → both clients    | Reconstructed frames are derived from the authoritative event log only          |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                                           |
| ---------- | ---------------------- | ------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-17-01 | Tampering              | Undo rewinding a committed card/injury/goal | mitigate    | `applyUndo` treats every Phase 39 dice-outcome event as a boundary or lockout; asserted by the registration suite, including the injury-survives-Undo test.               |
| T-39-17-02 | Elevation of Privilege | Client-side Undo bypassing the server       | mitigate    | `canUndo` is a display affordance only; `applyUndo` re-derives the boundary server-side on every `GAME_UNDO`. The mirror exists for UX honesty, not enforcement.          |
| T-39-17-03 | Denial of Service      | Replay crashing on an unhandled event type  | mitigate    | The registration suite drives `buildReplayFrames` over all eleven new types and asserts no throw — the direct guard against the `CORNER_KICK_CLEAR_OUT_MOVE` crash class. |
| T-39-17-04 | Repudiation            | A match outcome not reconstructible         | mitigate    | Every ball-affecting Phase 39 event carries `ballAfter` and is registered in `REPLAY_ELIGIBLE_TYPES`; the classification is asserted as an executable invariant.          |
| T-39-17-SC | Tampering              | npm/pip/cargo installs                      | accept      | No packages installed this phase.                                                                                                                                         |

</threat_model>

<verification>
- `pnpm test` (full monorepo suite) green
- `pnpm build` green
- `pnpm lint` and `pnpm stylelint` green
- Human checkpoint approved, or failures recorded by step number
</verification>

<success_criteria>
The client Undo affordance mirrors every server boundary added this phase; all eleven new event types have an asserted Undo and Replay classification; replay reconstruction survives all of them; no Phase 39 path emits the generic `DICE_ROLL` type; and a human has confirmed the full feature set in a live two-browser match.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-17-SUMMARY.md` when done
</output>
