---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 08
type: execute
wave: 3
depends_on: ['39-01', '39-05']
files_modified:
  - packages/client/src/components/PenaltyKickSetupPanel.tsx
  - packages/client/src/components/PenaltyKickSetupPanel.module.css
  - packages/client/src/components/PenaltyKickSetupPanel.test.tsx
autonomous: true
requirements: [PEN-01, PEN-02]

must_haves:
  truths:
    - 'During a penalty reposition window the acting manager sees how many of their players are still eligible to move and a Confirm button; the other manager sees a waiting message'
    - 'Confirming with players still unmoved raises the existing soft are-you-sure dialog, not a hard block'
    - 'The penalty-area restriction is stated on screen throughout the reposition windows'
    - 'The taker-select step tells the kicking manager to choose a penalty taker and tells the other manager to wait'
  artifacts:
    - path: 'packages/client/src/components/PenaltyKickSetupPanel.tsx'
      provides: 'Panel covering all four penalty-kick phases'
      contains: 'Penalty Kick'
      min_lines: 120
    - path: 'packages/client/src/components/PenaltyKickSetupPanel.module.css'
      provides: 'Panel styling with no container border (Phase 35 convention)'
      contains: '.panel'
  key_links:
    - from: 'packages/client/src/components/PenaltyKickSetupPanel.tsx'
      to: 'useGameStore.emitEndTurn'
      via: 'withEndTurnGuard soft-confirm wrapper'
      pattern: 'emitEndTurn'
---

<objective>
Build `PenaltyKickSetupPanel.tsx` as a direct structural copy of `GoalKickSetupPanel.tsx`'s reposition-window branch, covering all four penalty-kick phases with the exact copy locked in the UI-SPEC Copywriting Contract.

Purpose: D-08 explicitly directs reuse of the turn-based sequential reposition window Goal Kick and Corner Kick already established, generalised to the full remaining squad. Copying the proven panel avoids re-deriving the eligible-count, waiting-state and soft-end-turn semantics.

Output: the panel, its CSS module and its component test. Phase dispatch into `GameBoard.tsx` is Plan 39-16's scope.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-PATTERNS.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-05-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto">
  <name>Task 1: Build PenaltyKickSetupPanel and its CSS module</name>
  <files>packages/client/src/components/PenaltyKickSetupPanel.tsx, packages/client/src/components/PenaltyKickSetupPanel.module.css</files>
  <read_first>
    - packages/client/src/components/GoalKickSetupPanel.tsx (full file — the guard block, `withEndTurnGuard`/`pendingEndTurn` soft-confirm dialog, the `remaining` derivation, `ctaColorClass` usage, `restartErrorMessage(gameError)` handling, and the waiting-state branches)
    - packages/client/src/components/GoalKickSetupPanel.module.css (full file — `.panel` has background/border-radius/padding and deliberately NO `border` property; `.panelHeading`, `.constraintRow`, `.errorText`, `.ctaButton`, `.ctaButtonReady`, `.ctaButtonPending`, `.confirmOverlay`, `.confirmCard`, `.confirmText`, `.confirmActions`)
    - packages/client/src/hooks/useMyTeam.ts
    - packages/client/src/utils/ctaColorClass.ts and packages/client/src/utils/restartErrorMessage.ts
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Copywriting Contract rows for every `PenaltyKickSetupPanel` string; Spacing Scale xs=4px/sm=8px; Typography Heading 12px/700 and Label 11px/400; Color accent rules)
  </read_first>
  <action>
Create `PenaltyKickSetupPanel.tsx` as a structural copy of `GoalKickSetupPanel.tsx`, covering `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT` and `PENALTY_KICK`.

Guard block (mirroring the goal-kick panel's D-04/Pitfall-4 shape): return `null` unless the phase is one of those four, `penaltyKickTeam` is non-null/non-undefined, and `useMyTeam()` returns non-null. Never coerce a null team to a default.

Store selectors: `phase`, `activeTeam`, `penaltyKickTeam`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `movedPieceIds`, `gameError`, `emitEndTurn`. Use one `useGameStore((s) => ...)` call per field (the Pitfall 6 per-slice-selector rule), never a whole-state selector.

Heading is the literal string `Penalty Kick` in every branch, unchanging across sub-steps — mirroring `GoalKickSetupPanel`'s single unchanging `Goal Kick` heading.

Reposition-window branches (`PENALTY_KICK_SETUP_ATTACKING` / `PENALTY_KICK_SETUP_DEFENDING`):

- `actingTeam` is `penaltyKickTeam` for the attacking window and the opposite team for the defending window.
- When `myTeam !== actingTeam`, render only the heading plus `{Team} is repositioning…` where `{Team}` is `Attacking` or `Defending` — the locked Phase 35/37/38 waiting-state phrasing.
- When acting: `eligibleIds` is `penaltyKickEligibleIds?.attacking` or `?.defending`; `remaining` counts ids with `(penaltyKickUsedPace?.[id] ?? 0) === 0 && !movedPieceIds.includes(id)`.
- Constraint row 1: `` `${remaining} players still eligible to move — reposition freely, then Confirm.` `` — no hex-cap qualifier, because PEN-02 is unbudgeted.
- Constraint row 2, always visible during both windows: `Only the penalty taker and goalkeeper may stand in the penalty area.`
- `gameError` renders through `restartErrorMessage(...)` into `.errorText`, exactly as the goal-kick panel does.
- CTA is a single `Confirm` button coloured by `ctaColorClass(remaining, { ready, pending }, true)` and wrapped in `withEndTurnGuard(remaining, emitEndTurn)`, reusing the goal-kick panel's soft dialog verbatim including its `{N} players left to reposition, are you sure you want to end your turn?` text with `Cancel` / `Yes, end turn`.

`PENALTY_KICK_TAKER_SELECT` branch: when `myTeam !== penaltyKickTeam`, show `{Team} is choosing a penalty taker…`. When acting, show the constraint row `Choose your penalty taker.` and no CTA — the choice is made by clicking a piece on the board, which Plan 39-05's store branch already routes to `emitPenaltyKickTaker`. Surface `gameError` in `.errorText`; the locked error copy for an unresolved selection is `Select a player to take the penalty first.`

`PENALTY_KICK` branch: the duel resolves server-side on the roll, so render heading plus a single constraint row telling the kicking manager to take the kick and the defending manager to wait. Do not add a second CTA verb — the single-CTA verb lock applies.

Create `PenaltyKickSetupPanel.module.css` by copying `GoalKickSetupPanel.module.css`'s class set verbatim. `.panel` must define `background`, `border-radius` and `padding` only, with NO `border` property (the Phase 35 panel-family convention cited by D-01). Spacing values come only from the declared scale: `gap: 4px`, `padding: 4px 8px` for internal rows, 8px between sections. Typography: `.panelHeading` 12px/700, `.constraintRow` and `.errorText` 11px/400/1.5. Colours: `.ctaButtonReady` uses `var(--color-cta-ready-bg)`, `.ctaButtonPending` uses `var(--color-cta-pending-bg)`, `.errorText` uses `var(--color-danger)`; introduce no new colour value and no new font size.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client typecheck && pnpm stylelint</automated>
</verify>
<acceptance_criteria> - `packages/client/src/components/PenaltyKickSetupPanel.tsx` exports `PenaltyKickSetupPanel` and returns `null` for any phase outside the four penalty phases - `grep -c "border:" packages/client/src/components/PenaltyKickSetupPanel.module.css` shows no `border:` declaration inside the `.panel` rule - The file contains the exact strings `Only the penalty taker and goalkeeper may stand in the penalty area.`, `Choose your penalty taker.` and `players still eligible to move — reposition freely, then Confirm.` - The constraint row text contains no hex-count cap phrase (`grep -c "hexes each" PenaltyKickSetupPanel.tsx` returns 0) - Exactly one `Confirm` CTA button exists in the reposition branches - Every numeric spacing value in `PenaltyKickSetupPanel.module.css` is a multiple of 4, and every font-size is one of 11, 12, 13, 20 - `pnpm stylelint` exits 0
</acceptance_criteria>
<done>The panel renders every penalty-kick sub-step with the locked copy and the established restart-panel conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Component tests for PenaltyKickSetupPanel</name>
  <files>packages/client/src/components/PenaltyKickSetupPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/GoalKickSetupPanel.test.tsx (the store-mock harness, the `useMyTeam` mock, and the assertion style for waiting states and the soft dialog)
    - packages/client/src/components/PenaltyKickSetupPanel.tsx (as written in Task 1)
  </read_first>
  <action>
Create `PenaltyKickSetupPanel.test.tsx` asserting:

1. Returns `null` when the phase is `MOVE`, and when the phase is a penalty phase but `penaltyKickTeam` is `null`.
2. In `PENALTY_KICK_SETUP_ATTACKING` as the acting manager: the heading `Penalty Kick`, the eligible-count row with the correct N derived from `penaltyKickEligibleIds.attacking` minus `movedPieceIds`, and the penalty-area constraint row are all present, and a `Confirm` button is rendered.
3. In the same phase as the non-acting manager: `Attacking is repositioning…` (or the exact locked phrasing) is shown and no `Confirm` button exists.
4. In `PENALTY_KICK_SETUP_DEFENDING` the acting side is the team opposite `penaltyKickTeam`, and the waiting text reads `Defending`.
5. Clicking `Confirm` with `remaining > 0` opens the soft dialog containing `players left to reposition, are you sure you want to end your turn?` with `Cancel` and `Yes, end turn`; `Cancel` closes it without calling `emitEndTurn`; `Yes, end turn` calls `emitEndTurn` exactly once.
6. Clicking `Confirm` with `remaining === 0` calls `emitEndTurn` immediately with no dialog.
7. In `PENALTY_KICK_TAKER_SELECT` the kicking manager sees `Choose your penalty taker.` and no CTA button; the other manager sees the waiting text.
8. A non-null `gameError` renders through the `.errorText` row in every branch that declares one.
   </action>
   <verify>
   <automated>pnpm --filter @counter-attack/client test -- PenaltyKickSetupPanel.test.tsx</automated>
   </verify>
   <acceptance_criteria> - `pnpm --filter @counter-attack/client test -- PenaltyKickSetupPanel.test.tsx` passes with at least 10 assertions matching the numbered list - The soft-dialog test asserts both the Cancel (no emit) and confirm (one emit) paths - The `remaining === 0` test asserts `emitEndTurn` was called without any dialog appearing - `pnpm --filter @counter-attack/client test` (full client suite) is green
   </acceptance_criteria>
   <done>Panel behaviour across all four penalty phases is regression-locked.</done>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                   |
| ---------------------- | ----------------------------------------------------------------------------- |
| panel button → emitter | Panel clicks become socket emits; the server re-validates phase and ownership |

## STRIDE Threat Register

| Threat ID  | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                        |
| ---------- | ---------------------- | -------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-08-01 | Elevation of Privilege | Non-acting manager sees a CTA          | mitigate    | `myTeam !== actingTeam` renders the waiting state with no button; the server's `activeTeam` guard in `applyPenaltyKickReposition`/`applyPenaltyKickWindowEnd` is the real enforcement. |
| T-39-08-02 | Denial of Service      | Double-click on Confirm                | mitigate    | Server per-room `isProcessing` mutex drops duplicates; the panel adds no local retry.                                                                                                  |
| T-39-08-03 | Tampering              | Panel-side penalty-area hint diverging | accept      | The hint is copy only; placement legality is decided by `PENALTY_AREA_RESTRICTED` in the engine (Plan 39-07).                                                                          |
| T-39-08-SC | Tampering              | npm/pip/cargo installs                 | accept      | No packages installed this phase.                                                                                                                                                      |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test` green
- `pnpm stylelint` green
- `pnpm build` green
</verification>

<success_criteria>
`PenaltyKickSetupPanel` covers all four penalty phases with the locked copy, the correct acting/waiting split, the unbudgeted eligible-count row, the always-visible penalty-area constraint, the soft end-turn dialog, and no container border; component tests cover every branch.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-08-SUMMARY.md` when done
</output>
