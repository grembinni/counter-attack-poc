---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 07
type: execute
wave: 3
depends_on: ['39-01', '39-02']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.penaltyKick.test.ts
autonomous: true
requirements: [PEN-01, PEN-02, PEN-03]

must_haves:
  truths:
    - 'Awarding a penalty places the ball on the defending penalty spot and opens the attacking team reposition window first, then the defending team (PEN-02, D-08)'
    - 'Every on-pitch piece of each team is eligible to reposition, with no third-of-pitch filter and no per-piece hex budget (PEN-02)'
    - 'Only the chosen penalty taker and the defending goalkeeper may end up inside the penalty area (PEN-02)'
    - 'The penalty duel applies a flat -2 to the goalkeeper combined score (PEN-01)'
    - 'A tied penalty duel routes to the existing LOOSE_BALL phase with the ball at the penalty spot (PEN-03)'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'Penalty-kick phase chain'
      contains: 'export function triggerPenaltyKick'
    - path: 'packages/server/src/__tests__/gameEngine.penaltyKick.test.ts'
      provides: 'Engine-level coverage for PEN-01/02/03'
      min_lines: 200
  key_links:
    - from: 'applyPenaltyKickDuel'
      to: 'computeCombinedScore'
      via: "penalties array [-2] on the goalkeeper's score"
      pattern: 'computeCombinedScore\\('
    - from: 'applyPenaltyKickDuel tie branch'
      to: "GamePhase 'LOOSE_BALL'"
      via: 'ball repositioned to penaltyKickSpot before the scatter roll'
      pattern: "phase: 'LOOSE_BALL'"
---

<objective>
Build the complete penalty-kick phase chain in `gameEngine.ts` as pure, testable reducer functions: the award trigger, full-squad eligibility, the two sequential reposition windows, taker selection, and the -2 goalkeeper duel with its tie-to-loose-ball branch.

Purpose: this chain must exist before the foul chain (Plan 39-10), because `applyFoulChoice`'s "take the restart" branch routes a GK-dive-at-feet foul straight into it (GKDIVE-03). Building it first avoids a partial "free-kick only" implementation.

Output: `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel` and their unit suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-PATTERNS.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-VALIDATION.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-02-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write gameEngine.penaltyKick.test.ts specifying the whole chain</name>
  <files>packages/server/src/__tests__/gameEngine.penaltyKick.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.cornerKick.test.ts (the sibling engine-level suite — copy its `GameState` fixture builder, its direct pure-function invocation style, and its assertion conventions)
    - packages/server/src/__tests__/testHelpers.ts (shared fixture helpers already available to server tests)
    - packages/server/src/gameEngine.ts lines 4634-4645 (`computeGoalKickEligibleIds`), 4646-4773 (`applyGoalKickReposition`), 4787-4880 (`applyGoalKickWindowEnd`) — the exact functions the new ones are modelled on
    - packages/shared/src/pitch.ts (`PENALTY_SPOT`, `PITCH_REGIONS.homePenaltyArea`/`awayPenaltyArea`, `isInRegion`, `isPitchHex`)
    - packages/shared/src/scoreUtils.ts (`computeCombinedScore`'s `Math.max(totalPenalty, -2)` clamp)
  </read_first>
  <behavior>
    - `triggerPenaltyKick(state, kickingTeam)` returns a state with `phase: 'PENALTY_KICK_SETUP_ATTACKING'`, `penaltyKickTeam: kickingTeam`, `penaltyKickSpot` equal to `PENALTY_SPOT[defendingTeam]`, `ball.position` at that spot with `carrierId: null` and `lastTouchedBy` carried through unchanged, `attackingTeam` and `activeTeam` both set to `kickingTeam`, `penaltyKickEligibleIds` populated, `penaltyKickUsedPace: {}`, `penaltyKickTakerId: null`, `movedPieceIds: []`, `lastDiceRoll: null`.
    - `computePenaltyKickEligibleIds(pieces, kickingTeam)` returns EVERY on-pitch piece of each team split into `attacking`/`defending` — no third-of-pitch filter (contrast with `computeGoalKickEligibleIds`) — and excludes any `redCarded` piece.
    - `applyPenaltyKickReposition(state, pieceId, to)` rejects: wrong phase (`WRONG_PHASE`); a piece whose `teamId !== state.activeTeam` (`WRONG_TEAM`); a piece not in the active window's eligible list (`MOVE_INVALID`/`NOT_ELIGIBLE`); a non-adjacent destination (`MOVE_INVALID`/`OUT_OF_RANGE`); an off-pitch destination (`MOVE_INVALID`/`OFF_PITCH`); an occupied destination (`MOVE_INVALID`/`OCCUPIED`); and a destination inside the defending penalty area for any piece other than the defending goalkeeper or the chosen taker (`MOVE_INVALID`/`PENALTY_AREA_RESTRICTED`).
    - `applyPenaltyKickReposition` accepts an unlimited number of single-step moves for the same piece — there is no budget rejection at any step count (PEN-02 is unbudgeted). Moving a NEW piece activates (locks) any other piece with an in-progress unfinished activation, mirroring `applyGoalKickReposition`'s abandonment sweep.
    - `applyPenaltyKickWindowEnd(state, team)` from `PENALTY_KICK_SETUP_ATTACKING` hands off to `PENALTY_KICK_SETUP_DEFENDING`, flips `activeTeam` to the defending team, resets `movedPieceIds` to `[]` and `penaltyKickUsedPace` to `{}`, and appends a `PENALTY_KICK_WINDOW_ADVANCE` event with `from: 'ATTACKING'`. From `PENALTY_KICK_SETUP_DEFENDING` it advances to `PENALTY_KICK_TAKER_SELECT` with `activeTeam` back to `penaltyKickTeam` and appends `from: 'DEFENDING'`. Calling it with the wrong team returns `WRONG_TEAM`.
    - `applyPenaltyKickTaker(state, pieceId)` rejects a goalkeeper, a piece of the defending team, and a `redCarded` piece; on success it moves that piece onto `penaltyKickSpot`, sets `penaltyKickTakerId`, sets `ball.carrierId` to the taker, appends `PENALTY_KICK_TAKER_PLACED`, and transitions to `PENALTY_KICK`. If the spot is occupied by another piece, that occupant is first displaced to its nearest unoccupied on-pitch neighbour outside the penalty area.
    - `applyPenaltyKickDuel(state, takerDie, gkDie)` computes `computeCombinedScore(taker.shooting, takerDie, [])` versus `computeCombinedScore(gk.saving, gkDie, [-2])` (PEN-01). Taker strictly greater → `GOAL`: score incremented for the kicking team, a `GOAL` event appended alongside the `PENALTY_KICK` event, and the existing post-goal kick-off reset path taken. Goalkeeper strictly greater → `SAVED`: ball to the goalkeeper, phase and `lastActionType` matching the existing save-catch handling. Exactly equal → `TIE`: `phase: 'LOOSE_BALL'` with `ball.position` at `penaltyKickSpot` and `carrierId: null` (PEN-03), leaving the scatter dice to the existing `LOOSE_BALL` branch of `applyRoll`.
    - Every terminal branch of `applyPenaltyKickDuel` clears the whole `penaltyKick*` cluster back to `null`/`{}`.
  </behavior>
  <action>
Create `packages/server/src/__tests__/gameEngine.penaltyKick.test.ts` asserting every case in the `<behavior>` block, importing the not-yet-existing functions from `../gameEngine.js`. The suite MUST fail at the end of this task — that is the intended RED state.

Build fixtures on the real 37x26 board using real region coordinates: draw penalty-area hexes from `PITCH_REGIONS.awayPenaltyArea`/`homePenaltyArea` and the spot from `PENALTY_SPOT`, never invented literals. STATE.md records that placeholder coordinates like `{q:11,r:7}` break move assertions silently.

Inject dice explicitly as function arguments (`takerDie`, `gkDie`); do not stub `rollDice`. The engine layer must stay deterministic and dice-free, matching every existing `applyRoll`-style signature.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- gameEngine.penaltyKick.test.ts</automated>
</verify>
<acceptance_criteria> - The file exists with at least 25 `it(` blocks covering every bullet in `<behavior>` - Running the suite at the end of this task FAILS on unresolved imports from `../gameEngine.js` (RED state recorded in task notes) - Every hex coordinate in the fixtures is derived from `PENALTY_SPOT` or a `PITCH_REGIONS` set, or is a computed neighbour of one — no bare invented literals - No `vi.mock` of `diceUtils` appears in the file
</acceptance_criteria>
<done>A failing engine suite fully specifies PEN-01, PEN-02 and PEN-03.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the penalty award, eligibility, reposition windows and taker selection</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.penaltyKick.test.ts (Task 1's spec)
    - packages/server/src/gameEngine.ts lines 4634-4645 (`computeGoalKickEligibleIds`), 4646-4773 (`applyGoalKickReposition` — adjacency, `isPitchHex`, occupancy, abandonment sweep, MOVE-event emission, `ApplyMoveResult` shape), 4787-4880 (`applyGoalKickWindowEnd` — the two-window handoff and its `GOAL_KICK_WINDOW_ADVANCE` boundary event)
    - packages/shared/src/offside.ts lines 234-281 (`triggerOffsideFoul` — the exact restart-trigger return shape to mirror)
    - packages/server/src/gameEngine.ts lines 1486-1502 (`ZONE_CHECK_EXEMPT_PHASES`) and 1644-1662 (`applyUndo`'s `isBoundary` reduce)
    - packages/shared/src/pitch.ts (`PENALTY_SPOT`, `isInRegion`, `isPitchHex`), packages/shared/src/hex.ts (`hexDistance`, `hexNeighbors`)
  </read_first>
  <action>
Add the following to `gameEngine.ts`, placed next to the existing goal-kick chain so the restart families stay grouped:

`export function triggerPenaltyKick(state: GameState, kickingTeam: 'home' | 'away'): GameState` — modelled byte-for-byte on `triggerOffsideFoul`'s return shape (spread `...state`, then override), substituting the penalty fields listed in the Task 1 behaviour block. The defending team is the opposite of `kickingTeam`; the spot is `PENALTY_SPOT[defendingTeam]`. Carry `ball.lastTouchedBy` through unchanged, exactly as `triggerOffsideFoul` does. Clear `movedPieceIds`, `paceUsedByPieceId`, `lastDiceRoll`, `stealAttemptedByIds` and `tackleAttemptedByIds`, matching what other break-in-play transitions clear.

`export function computePenaltyKickEligibleIds(pieces: readonly PlayerPiece[], kickingTeam: 'home' | 'away'): { attacking: readonly string[]; defending: readonly string[] }` — the `computeGoalKickEligibleIds` shape with the region filter REMOVED (PEN-02 is full-squad) and a `redCarded !== true` filter added. Add a comment stating the region filter's deliberate absence so a future reader does not "restore" it by pattern-matching the goal-kick sibling.

`export function applyPenaltyKickReposition(state, pieceId, to): ApplyMoveResult` — structurally copy `applyGoalKickReposition`'s body (phase guard, piece lookup, `teamId !== state.activeTeam` guard, eligible-list guard, `movedPieceIds` activation lock, `hexDistance !== 1` → `OUT_OF_RANGE`, `!isPitchHex` → `OFF_PITCH`, occupancy → `OCCUPIED`, abandonment sweep, `MOVE` event with `slot: 'ATTACKER_2'`), with two deliberate deviations:

1. Delete the `usedSoFar + stepDistance > 6` budget rejection entirely — PEN-02 repositioning is unbudgeted. `penaltyKickUsedPace` is still accumulated because the abandonment sweep and the client's in-progress-activation detection both read it.
2. Insert a new guard after the `OCCUPIED` check: reject with `{ ok: false, reason: 'MOVE_INVALID', detail: 'PENALTY_AREA_RESTRICTED' }` when `isInRegion(to, defendingAreaKey)` is true AND the moving piece is neither the defending goalkeeper (`role === 'GK' && teamId === defendingTeam`) nor `state.penaltyKickTakerId`. `defendingAreaKey` is `state.penaltyKickTeam === 'home' ? 'awayPenaltyArea' : 'homePenaltyArea'`.

`export function applyPenaltyKickWindowEnd(state, team): { ok: true; state: GameState } | { ok: false; reason: string }` — copy `applyGoalKickWindowEnd`'s two-window handoff, emitting `PENALTY_KICK_WINDOW_ADVANCE` with `from: 'ATTACKING' | 'DEFENDING'`, and targeting `PENALTY_KICK_TAKER_SELECT` (not a choice phase) after the defending window.

`export function applyPenaltyKickTaker(state, pieceId): { ok: true; state: GameState } | { ok: false; reason: string }` — phase guard `PENALTY_KICK_TAKER_SELECT`; reject a goalkeeper (`TAKER_INVALID`), a defending-team piece (`WRONG_TEAM`), a `redCarded` piece (`TAKER_INVALID`), and an unknown id (`PIECE_NOT_FOUND`). On success: if another piece occupies `penaltyKickSpot`, relocate it to the nearest `hexNeighbors` hex that is on-pitch, unoccupied and outside the defending penalty area (walk outward one ring if all six are blocked, and if still blocked leave it in place — never throw). Then move the taker to the spot, set `penaltyKickTakerId`, set `ball` to `{ position: spot, carrierId: pieceId, lastTouchedBy: { pieceId, teamId } }`, append `PENALTY_KICK_TAKER_PLACED`, and set `phase: 'PENALTY_KICK'`.

Register the new phases in the two existing lists in this file:

- `ZONE_CHECK_EXEMPT_PHASES` — add all four penalty phases (the ball sits on a fixed restart hex throughout, exactly like every goal-kick/corner-kick phase already in the set).
- `applyUndo`'s `isBoundary` reduce — add `((state.phase === 'PENALTY_KICK_SETUP_ATTACKING' || state.phase === 'PENALTY_KICK_SETUP_DEFENDING') && evt.type === 'PENALTY_KICK_WINDOW_ADVANCE')` and `(state.phase === 'PENALTY_KICK_TAKER_SELECT' && evt.type === 'PENALTY_KICK_TAKER_PLACED')`, mirroring the `GOAL_KICK_WINDOW_ADVANCE` and `CORNER_KICK_TAKER_PLACED` rows. This is the STATE.md per-event-type registration checklist — skipping it is the BUG-30/BUG-31/BUG-37 defect class.
- `applyUndo`'s `moveTypeForPhase` mapping — the penalty reposition windows emit plain `MOVE` events, so they take the default `'MOVE'` branch exactly like `GOAL_KICK_SETUP_GK`/`_OPPONENT`; confirm no new mapping entry is needed and note that in a comment.
  </action>
  <verify>
  <automated>pnpm --filter @counter-attack/server test -- gameEngine.penaltyKick.test.ts</automated>
  </verify>
  <acceptance_criteria> - `gameEngine.ts` exports `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker` - `computePenaltyKickEligibleIds` contains no `isInRegion` call - `applyPenaltyKickReposition` contains no numeric pace-budget comparison (no `> 6` or equivalent) - `grep -c "PENALTY_AREA_RESTRICTED" packages/server/src/gameEngine.ts` returns at least 1 - `ZONE_CHECK_EXEMPT_PHASES` contains all four `PENALTY_KICK*` phase values - `applyUndo`'s `isBoundary` expression references `PENALTY_KICK_WINDOW_ADVANCE` and `PENALTY_KICK_TAKER_PLACED` - All non-duel assertions in `gameEngine.penaltyKick.test.ts` pass (duel assertions may still fail until Task 3)
  </acceptance_criteria>
  <done>A penalty can be awarded, both teams can freely reposition in turn under the penalty-area restriction, and a taker can be placed on the spot.</done>
  </task>

<task type="auto">
  <name>Task 3: Implement the penalty duel with the -2 goalkeeper penalty and the tie-to-loose-ball branch</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.penaltyKick.test.ts (Task 1's duel spec)
    - packages/server/src/gameEngine.ts — the `SHOT` branch of `applyRoll` (the GOAL scoring path, the score increment, the post-goal kick-off reset via `buildKickOffPieces`, and the save-catch `GK_RESTART` transition) and the `LOOSE_BALL` branch entry conditions at ~3336
    - packages/shared/src/scoreUtils.ts lines 28-37 (`computeCombinedScore` and its `Math.max(totalPenalty, -2)` clamp)
    - packages/server/src/gameEngine.ts lines 6968-7010 (`REPLAY_ELIGIBLE_TYPES`)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md (Architecture Pattern 5 — the -2 clamp interaction note)
  </read_first>
  <action>
Add `export function applyPenaltyKickDuel(state: GameState, takerDie: number, gkDie: number): { ok: true; state: GameState } | { ok: false; reason: string }` to `gameEngine.ts`.

Guards: phase must be `PENALTY_KICK` (`WRONG_PHASE`); `penaltyKickTakerId` and the defending goalkeeper must both resolve to pieces (`PIECE_NOT_FOUND`).

Scoring (PEN-01): `const takerCombined = computeCombinedScore(taker.shooting, takerDie, []);` and `const gkCombined = computeCombinedScore(gk.saving, gkDie, [-2]);`. Do not modify `computeCombinedScore` itself. Add a comment recording RESEARCH.md's clamp interaction: because the clamp is on the SUMMED penalty (`Math.max(total, -2)`), an already-penalised goalkeeper does not stack beyond -2 — this matches existing shot-duel behaviour and is not a new edge case.

Note explicitly in a comment that INJURY-02 is NOT threaded here as a penalty-array entry: injury degradation is a stored attribute mutation (Plan 39-02's `applyInjuryDegradation`), so `taker.shooting` / `gk.saving` already carry it. Adding an injury penalty here would double-count.

Append a `PENALTY_KICK` `ActionEvent` in every branch with all six numeric fields, the `result` discriminant and `ballAfter`.

Branches:

- `takerCombined > gkCombined` → `GOAL`. Increment `score[penaltyKickTeam]`, append a `GOAL` event after the `PENALTY_KICK` event, and take the same post-goal path the existing `SHOT`-branch goal already takes (kick-off reset pieces, `phase: 'KICK_OFF_SETUP'`, `kickOffTeam` handling, `lastActionType` reset). Reuse that existing code path rather than re-deriving it.
- `gkCombined > takerCombined` → `SAVED`. Ball to the goalkeeper (`carrierId: gk.id`, `position: gk.position`, `lastTouchedBy` = the goalkeeper), and route to the existing post-save restart the shot-save catch already uses (`GK_RESTART`), so the goalkeeper's restart options are unchanged.
- Exactly equal → `TIE` (PEN-03). Set `phase: 'LOOSE_BALL'`, `ball: { position: penaltyKickSpot, carrierId: null, lastTouchedBy: { pieceId: gk.id, teamId: gk.teamId } }`, `lastDiceRoll: null`. Do NOT roll or compute the scatter here — the existing `LOOSE_BALL` branch of `applyRoll` owns the direction/distance dice and the `computeLooseBall` walk. PEN-03 says "following the existing Loose Ball rules"; this is a new trigger context, not new mechanics.

Every branch clears the penalty cluster: `penaltyKickTeam: null, penaltyKickSpot: null, penaltyKickEligibleIds: null, penaltyKickUsedPace: {}, penaltyKickTakerId: null` — except the TIE branch, which must clear `penaltyKickTeam`/`penaltyKickEligibleIds`/`penaltyKickUsedPace`/`penaltyKickTakerId` but may keep `penaltyKickSpot` until the scatter resolves if the loose-ball walk needs it; if it does not need it, clear that too and say so in a comment.

Register `'PENALTY_KICK'` in `REPLAY_ELIGIBLE_TYPES` (it carries `ballAfter`, matching the inclusion rule stated in that set's comments). Deliberately do NOT add `PENALTY_KICK_WINDOW_ADVANCE` or `PENALTY_KICK_TAKER_PLACED` there — they carry no `ballAfter`, matching the existing `GOAL_KICK_WINDOW_ADVANCE` exclusion; state that reasoning in a comment beside the addition.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- gameEngine.penaltyKick.test.ts && pnpm --filter @counter-attack/server test</automated>
</verify>
<acceptance_criteria> - `pnpm --filter @counter-attack/server test -- gameEngine.penaltyKick.test.ts` passes with zero failures - `applyPenaltyKickDuel` calls `computeCombinedScore(gk.saving, gkDie, [-2])` with a literal `-2` penalty array - `computeCombinedScore`'s own body in `packages/shared/src/scoreUtils.ts` is unchanged - The TIE branch sets `phase: 'LOOSE_BALL'` and does not call `computeLooseBall` - `REPLAY_ELIGIBLE_TYPES` contains `'PENALTY_KICK'` and does not contain `'PENALTY_KICK_WINDOW_ADVANCE'` or `'PENALTY_KICK_TAKER_PLACED'` - `pnpm --filter @counter-attack/server test` (full server suite) is green — no regression in the existing shot/goal/loose-ball suites
</acceptance_criteria>
<done>The penalty duel resolves to goal, save or loose ball with the -2 goalkeeper penalty applied, and the event is replay-visible.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                   |
| ------------------------------- | ----------------------------------------------------------------------------- |
| socket handler → engine reducer | Handlers (Plan 39-11) pass client-derived ids/hexes into these pure functions |
| engine → broadcast state        | Every returned state is broadcast verbatim to both players                    |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                | Disposition | Mitigation Plan                                                                                                                                                                               |
| ---------- | ---------------------- | ---------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-07-01 | Tampering              | `applyPenaltyKickDuel` dice              | mitigate    | `takerDie`/`gkDie` are parameters supplied by the handler from `rollDice()` (`crypto.randomInt`); the engine never accepts a client-declared die and never calls `Math.random`.               |
| T-39-07-02 | Elevation of Privilege | `applyPenaltyKickReposition` team bypass | mitigate    | Engine-level `piece.teamId !== state.activeTeam` → `WRONG_TEAM` guard, layered under the handler's own `socketTeam` check (defence in depth, mirroring `applyGoalKickReposition`).            |
| T-39-07-03 | Tampering              | Malformed / off-pitch destination hex    | mitigate    | `hexDistance !== 1` → `OUT_OF_RANGE`, `!isPitchHex(to)` → `OFF_PITCH`, occupancy → `OCCUPIED`, checked in that order before any state mutation (the 37-13 off-grid-poisoning fix's ordering). |
| T-39-07-04 | Tampering              | Illegal penalty-area placement           | mitigate    | Server-side `PENALTY_AREA_RESTRICTED` rejection; the client highlight filter added in Plan 39-05 is explicitly documented as convenience only.                                                |
| T-39-07-05 | Tampering              | Ineligible penalty taker                 | mitigate    | `applyPenaltyKickTaker` rejects goalkeepers, defending-team pieces, red-carded pieces and unknown ids before any mutation.                                                                    |
| T-39-07-06 | Repudiation            | Penalty outcome not auditable            | mitigate    | Every branch appends a `PENALTY_KICK` event carrying both dice, both combined scores and the result; the event is registered in `REPLAY_ELIGIBLE_TYPES`.                                      |
| T-39-07-SC | Tampering              | npm/pip/cargo installs                   | accept      | No packages installed this phase.                                                                                                                                                             |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` green
- `pnpm build` green
- No `Math.random` added to `gameEngine.ts`
</verification>

<success_criteria>
`triggerPenaltyKick` opens a correctly-seeded attacking reposition window; full-squad eligibility excludes red-carded pieces and applies no region filter; unbudgeted single-step repositioning works with the penalty-area restriction enforced server-side; the window handoff and taker placement emit their undo-boundary events; the duel applies exactly -2 to the goalkeeper and routes goal / save / tie-to-loose-ball; all new event types are registered in the undo and replay lists.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-07-SUMMARY.md` when done
</output>
