---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 10
type: execute
wave: 4
depends_on: ['39-07']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.fouls.test.ts
  - packages/server/src/__tests__/gameEngine.booking.test.ts
  - packages/server/src/__tests__/gameEngine.injury.test.ts
autonomous: true
requirements:
  [
    FOUL-01,
    FOUL-02,
    FOUL-03,
    FOUL-04,
    FOUL-05,
    CARD-01,
    CARD-02,
    CARD-03,
    INJURY-01,
    INJURY-02,
    INJURY-03,
    FK-01,
  ]

must_haves:
  truths:
    - 'A tackle or steal whose defending die is exactly 1 calls a foul on that defender (FOUL-01)'
    - 'A foul immediately rolls the injury check then the booking check, both committed to state before the attacker chooses anything (FOUL-02)'
    - 'An injured player has every attribute reduced by 1, floored at 1, for the rest of the match (INJURY-02)'
    - 'A second yellow card becomes a red card for that player (CARD-02)'
    - 'A Professional Foul rolls straight red-versus-yellow instead of the normal booking roll (FOUL-04, CARD-03)'
    - 'Choosing Continue Play resumes exactly the state the duel would have produced without the foul; choosing the restart awards a free kick via the existing FREE_KICK_SETUP flow (FOUL-03, FK-01)'
    - 'With the Fouls toggle off, no foul, injury or booking is ever detected or rolled (FOUL-05)'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'Inline foul sub-resolution, FOUL_CHOICE handling and the foul free-kick trigger'
      contains: 'export function resolveFoulChain'
    - path: 'packages/server/src/__tests__/gameEngine.fouls.test.ts'
      provides: 'FOUL-01..05 and FK-01 coverage'
      min_lines: 200
    - path: 'packages/server/src/__tests__/gameEngine.booking.test.ts'
      provides: 'CARD-01..03 coverage'
      min_lines: 120
    - path: 'packages/server/src/__tests__/gameEngine.injury.test.ts'
      provides: 'INJURY-01..03 coverage'
      min_lines: 120
  key_links:
    - from: 'applyMove STEAL_ATTEMPT / TACKLE_ATTEMPT branches'
      to: 'resolveFoulChain'
      via: 'inline call using the defender die already in scope'
      pattern: 'resolveFoulChain\\('
    - from: 'applyFoulChoice restart branch'
      to: 'triggerFoulFreeKick / triggerPenaltyKick'
      via: 'foulSource discriminant'
      pattern: "foulSource === 'GK_DIVE_AT_FEET'"
---

<objective>
Implement the heart of Phase 39: FOUL-01's die-of-1 detection wired inline into the existing duel-resolution branches, FOUL-02's unconditional injury-then-booking chain, FOUL-04/CARD-03's Professional Foul, INJURY-01/02/03's degradation, CARD-01/02's booking, FOUL-03's continue-or-restart choice, FK-01's free-kick trigger, and FOUL-05's toggle gate.

Purpose: RESEARCH.md's primary recommendation is that injury and booking must be inline sub-resolutions appended to `eventLog` inside `applyMove`'s existing `STEAL_ATTEMPT`/`TACKLE_ATTEMPT` branches — never a phase transition of their own — so they always fire regardless of the attacker's later continue-or-restart choice.

Output: `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, the two `applyMove` hook points, and three unit suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-PATTERNS.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-VALIDATION.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-07-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the three failing engine suites for fouls, booking and injury</name>
  <files>packages/server/src/__tests__/gameEngine.fouls.test.ts, packages/server/src/__tests__/gameEngine.booking.test.ts, packages/server/src/__tests__/gameEngine.injury.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.phase17.test.ts and gameEngine.test.ts (the `applyMove` fixture style — how a `GameState` is seeded into `MOVE` with real squad positions and how a `dice` object is injected)
    - packages/server/src/__tests__/testHelpers.ts
    - packages/server/src/gameEngine.ts lines 890-1050 (the exact `STEAL_ATTEMPT` and `TACKLE_ATTEMPT` branches, their `die`/`defDie`/`carDie` variables, their event shapes, the FAIL-continuation return at 1034-1048 and the SUCCESS return at 1003-1029)
    - packages/shared/src/fouls.ts (Plan 39-02's helpers — the tests must exercise the engine's use of them, not re-test the helpers)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md (Architecture Pattern 1; Pitfall 2; Pitfall 5)
  </read_first>
  <behavior>
    **gameEngine.fouls.test.ts (FOUL-01..05, FK-01)**
    - `applyMove` with a `STEAL_ATTEMPT` effect and `dice.stealDie === 1` on a state with `foulsEnabled: true` appends a `FOUL_CALLED` event naming the defender as `defenderId`, the carrier as `victimId`, `source: 'STEAL'`, `defenderDie: 1`, and transitions `phase` to `'FOUL_CHOICE'`.
    - The same with `dice.tackleDie === 1` on a `TACKLE_ATTEMPT` yields `source: 'TACKLE'`.
    - `dice.stealDie === 6` (or any value other than 1) appends no `FOUL_CALLED` and leaves the pre-existing steal/tackle behaviour byte-for-byte identical, including the phase.
    - The foul fires on a die of 1 regardless of whether the duel itself SUCCEEDED or FAILED — assert both outcomes.
    - With `foulsEnabled: false` (and with the field absent entirely), `dice.stealDie === 1` appends no `FOUL_CALLED`, no `INJURY_CHECK` and no `BOOKING_CHECK`, and the phase is whatever the duel would have produced (FOUL-05).
    - Event ORDER within the single returned state: the duel event (`STEAL_ATTEMPT`/`TACKLE_ATTEMPT`), then `FOUL_CALLED`, then `INJURY_CHECK` (when injury enabled), then `BOOKING_CHECK` (when booking enabled) — FOUL-02's "in that order".
    - `foulResume` on the `FOUL_CHOICE` state captures the phase/activeTeam/attackingTeam/movementSlot/lastActionType the duel would otherwise have produced.
    - `applyFoulChoice(state, 'continue')` restores exactly those `foulResume` fields, clears the whole `foul*` cluster, and appends `FOUL_CHOICE_MADE` with `choice: 'continue', restart: null`.
    - `applyFoulChoice(state, 'restart')` with `foulSource: 'TACKLE'` yields `phase: 'FREE_KICK_SETUP'`, `freeKickHex` equal to the FOULER's position, `freeKickAttackingTeam` and `attackingTeam`/`activeTeam` set to the fouled team, `freeKickStageIndex: 0`, `freeKickPlacedPieceIds: []`, `freeKickKickerChosen: false`, ball at the foul hex with `carrierId: null`, and appends `FOUL_CHOICE_MADE` with `restart: 'FREE_KICK'` (FK-01).
    - `applyFoulChoice(state, 'restart')` with `foulSource: 'GK_DIVE_AT_FEET'` yields `phase: 'PENALTY_KICK_SETUP_ATTACKING'` via `triggerPenaltyKick` and `restart: 'PENALTY'` (GKDIVE-03), never `FREE_KICK_SETUP`.
    - `applyFoulChoice` from a non-`FOUL_CHOICE` phase returns `WRONG_PHASE`; with an invalid choice string returns `INVALID_CHOICE`.
    - FOUL-04: a foul where a teammate of the fouler can reach the foul hex within remaining pace produces `FOUL_CALLED` with `professional: false`; a foul where none can produces `professional: true`.

    **gameEngine.booking.test.ts (CARD-01..03)**
    - With `bookingEnabled: true` and `refereeCard.leniency: 4`, a booking die of 4 appends `BOOKING_CHECK` with `card: 'yellow'` and sets the fouler's `yellowCards` to 1; a die of 3 appends `card: 'none'` and leaves `yellowCards` at 0 (CARD-01).
    - A fouler who already has `yellowCards: 1` and rolls a yellow gets `card: 'red', secondYellow: true`, `redCarded: true` and `yellowCards: 2` (CARD-02).
    - A red-carded piece is removed from play: assert the engine's chosen dismissal representation is applied consistently (see the action block) and that the piece can no longer be selected for a subsequent move.
    - A `professional: true` foul with a die >= leniency yields `card: 'red', secondYellow: false, professional: true` on the very first booking (CARD-03); with a die < leniency it yields `card: 'yellow'`.
    - With `bookingEnabled: false` but `foulsEnabled: true`, a die-of-1 foul appends `FOUL_CALLED` and (if injury enabled) `INJURY_CHECK`, but NO `BOOKING_CHECK`, and `yellowCards` is unchanged.
    - The booking die is a separate argument from the foul-trigger die — assert a foul triggered by `stealDie: 1` still produces a booking outcome driven by the injected booking die, not by `1`.

    **gameEngine.injury.test.ts (INJURY-01..03)**
    - With `injuryEnabled: true` and a victim with `resilience: 4`, an injury die of 4 appends `INJURY_CHECK` with `injured: true` and every one of the victim's nine numeric attributes reduced by 1 (floored at 1, and a `highPass: 0` goalkeeper value left at 0) with `injuryCount: 1` (INJURY-01/INJURY-02).
    - An injury die of 3 against `resilience: 4` appends `INJURY_CHECK` with `injured: false` and leaves every attribute unchanged.
    - A second injury on the same player yields `injuryCount: 2` and a second -1 on every attribute, and the player REMAINS on the pitch (D-06: Phase 39 always takes the no-substitute branch of INJURY-03).
    - With `injuryEnabled: false` but `foulsEnabled: true`, no `INJURY_CHECK` is appended and attributes are unchanged.
    - The injury die is a separate argument from the foul-trigger die (Assumption A1 resolved: fresh rolls).

  </behavior>
  <action>
Create all three test files asserting every bullet in `<behavior>`. Import from `../gameEngine.js`. The suites MUST fail at the end of this task.

DECISION (planner, resolves RESEARCH.md Assumption A1): the injury check and the booking check each use a FRESH die, separate from the foul-trigger die. REQUIREMENTS.md FOUL-02 says a foul "rolls an injury check ... and a booking check", and INJURY-01/CARD-01 each say "a die" — three distinct dice per foul, exactly as `SHOT_ATTEMPT` already rolls shooter/GK/handling. Reusing the trigger die (fixed at 1) would make both checks almost always fail their thresholds, inverting the intended balance. Record this decision as a comment in `gameEngine.fouls.test.ts`.

Inject all dice as explicit arguments so the suites are deterministic: extend the `dice` object `applyMove` already accepts with `injuryDie` and `bookingDie` (Task 2 implements the reads). Do NOT `vi.mock('../diceUtils.js')` — the engine layer must stay dice-free and the handler owns `rollDice()`.

Seed every fixture from real `HOME_SQUAD`/`AWAY_SQUAD` positions so adjacency actually holds; STATE.md records that placeholder coordinates break `applyMove` silently.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- gameEngine.fouls.test.ts gameEngine.booking.test.ts gameEngine.injury.test.ts</automated>
</verify>
<acceptance_criteria> - All three files exist with a combined total of at least 40 `it(` blocks - Running the three suites at the end of this task FAILS (RED state recorded in task notes) - No `vi.mock('../diceUtils.js')` appears in any of the three files - `gameEngine.fouls.test.ts` contains the recorded A1 decision comment about fresh dice - Fixtures use real squad positions, not invented coordinates
</acceptance_criteria>
<done>Three failing suites fully specify FOUL-01..05, CARD-01..03, INJURY-01..03 and FK-01.</done>
</task>

<task type="auto">
  <name>Task 2: Implement resolveFoulChain and hook it into both duel branches of applyMove</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 890-1050 (the full `STEAL_ATTEMPT` and `TACKLE_ATTEMPT` branches — the `die`/`defDie` extraction, the event append at 925 and 967, the `newStealAttemptedByIds`/`newTackleAttemptedByIds` tracking, the SUCCESS early return at 1003-1029, the FAIL return at 1034-1048, and the `stealSuccess` fall-through at 1052+)
    - packages/shared/src/fouls.ts (`FOUL_TRIGGER_DIE`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `applyInjuryDegradation`, `isProfessionalFoul`)
    - packages/server/src/gameEngine.ts lines 4646-4773 (the `newPieces` immutable-rewrite style used elsewhere in this file)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md (Architecture Pattern 1; Pitfall 2)
  </read_first>
  <action>
Add `export function resolveFoulChain(input: { state: GameState; pieces: readonly PlayerPiece[]; eventLog: readonly ActionEvent[]; defenderId: string; victimId: string; foulHex: HexCoord; source: 'TACKLE' | 'STEAL' | 'GK_DIVE_AT_FEET'; defenderDie: number; injuryDie: number; bookingDie: number; }): { fouled: boolean; pieces: readonly PlayerPiece[]; eventLog: readonly ActionEvent[]; foulFields: Partial<GameState> }` to `gameEngine.ts`, placed immediately above `applyMove` so both hook points can see it.

Body:

1. Return `{ fouled: false, pieces, eventLog, foulFields: {} }` immediately when `state.foulsEnabled !== true` or `defenderDie !== FOUL_TRIGGER_DIE` (FOUL-01/FOUL-05). Test `=== true` explicitly, never truthiness.
2. Compute `professional = isProfessionalFoul(state, defenderId, foulHex)` (FOUL-04).
3. Append `FOUL_CALLED` with `defenderId`, `victimId`, `hex: foulHex`, `source`, `defenderDie`, `professional`.
4. When `state.injuryEnabled === true`: compute `injured = rollsInjury(injuryDie, victim.resilience)`; append `INJURY_CHECK` with `victimId`, `die: injuryDie`, `resilience: victim.resilience`, `injured`, and the post-application `injuryCount`. When injured, rewrite the victim in `pieces` via `applyInjuryDegradation(victim)` (INJURY-01/02). A second injury simply increments again and the player stays on the pitch — D-06 fixes Phase 39 to INJURY-03's no-substitute branch; add a comment saying Phase 40 will later read this same state and that no stub is needed here.
   Note in a comment that `victim.resilience` must be read from the CURRENT (possibly already-degraded) piece, because degradation is a stored mutation, so a previously-injured player is genuinely easier to injure again.
5. When `state.bookingEnabled === true`: compute `resolveBooking({ die: bookingDie, leniency: state.refereeCard.leniency, priorYellows: fouler.yellowCards ?? 0, professional })`; append `BOOKING_CHECK` with `defenderId`, `die: bookingDie`, `leniency`, `card`, `secondYellow`, `professional`. Apply the outcome to the fouler in `pieces`: a yellow increments `yellowCards`; a red sets `redCarded: true` and sets `yellowCards` to `2` when `secondYellow` (CARD-01/02/03).
6. Return `foulFields` = `{ foulDefenderId: defenderId, foulVictimId: victimId, foulHex, foulSource: source }` and `fouled: true`.

Dismissal representation for a red card: keep the piece in `state.pieces` with `redCarded: true` rather than splicing it out of the array. Rationale to record in a comment: `pieces` is indexed by id across the replay builder, the offside evaluator and every client selector, and removing an entry mid-match would break `pieces.find(...)` call sites throughout. Instead, make the piece unselectable and non-participating by adding a `redCarded !== true` filter to `moveValidator`'s selectable-piece derivation is OUT OF SCOPE here — instead add the guard directly in `applyMove`: reject with `{ ok: false, reason: 'MOVE_INVALID', detail: 'RED_CARDED' }` when the moving piece has `redCarded === true`, placed with the other early piece guards. Also exclude `redCarded` pieces from `isProfessionalFoul` (already done in Plan 39-02) and from `computePenaltyKickEligibleIds` (already done in Plan 39-07).

Hook points in `applyMove`:

- Extend the `dice` parameter's inline type with `injuryDie?: number` and `bookingDie?: number`, defaulting each to `3` at the read site exactly as `stealDie`/`tackleDie` already default.
- `STEAL_ATTEMPT` branch: immediately after `newEventLog = [...newEventLog, stealEvent];` (currently line 925), call `resolveFoulChain` with `defenderId: stealDefenderId`, `victimId: pieceId` (the carrier being stolen from), `foulHex: to`, `source: 'STEAL'`, `defenderDie: die`. Capture the returned pieces/eventLog/foulFields into local variables that the branch's downstream returns already thread.
- `TACKLE_ATTEMPT` branch: immediately after `newEventLog = [...newEventLog, tackleEvent];` (currently line 967), call it with `defenderId: pieceId`, `victimId: carrierId`, `foulHex: to`, `source: 'TACKLE'`, `defenderDie: defDie`.
- In BOTH branches, when `fouled` is true, override the branch's return with `phase: 'FOUL_CHOICE'`, `attackingTeam` and `activeTeam` set to the FOULED team (the victim's team), the merged `foulFields`, and `foulResume` capturing the phase/activeTeam/attackingTeam/movementSlot/lastActionType the branch WOULD have returned. Compute the would-be return object first, then derive `foulResume` from it — do not duplicate the branch's transition logic.

The `TACKLE_ATTEMPT` SUCCESS branch already early-returns with a full state object; restructure minimally so the foul override wraps that object rather than bypassing it, preserving the corrected `moveEvent` `ballAfter` rewrite, the `checkHalfEndOnTackle` result, the offside re-evaluation and the `THROW_IN_TEARDOWN` spread. Do not delete or reorder any of those.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- gameEngine.fouls.test.ts gameEngine.injury.test.ts gameEngine.booking.test.ts</automated>
</verify>
<acceptance_criteria> - `gameEngine.ts` exports `resolveFoulChain` - `resolveFoulChain` returns early when `state.foulsEnabled !== true` — verified by the FOUL-05 tests passing - `grep -c "computeCombinedScore" ` inside `resolveFoulChain`'s body returns 0 (injury/booking are bare `>=` comparisons per Pitfall 2) - `applyMove` rejects a move by a piece with `redCarded === true` with detail `RED_CARDED` - All `gameEngine.injury.test.ts` and `gameEngine.booking.test.ts` assertions pass; the `applyFoulChoice` assertions in `gameEngine.fouls.test.ts` may still fail until Task 3 - `pnpm --filter @counter-attack/server test -- gameEngine.test.ts gameEngine.phase17.test.ts` is green — the non-foul duel paths are unchanged
</acceptance_criteria>
<done>A defender die of 1 calls a foul and immediately resolves injury and booking inline, before any client input.</done>
</task>

<task type="auto">
  <name>Task 3: applyFoulChoice, triggerFoulFreeKick, and undo/replay registration</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.fouls.test.ts (the `applyFoulChoice` spec from Task 1)
    - packages/shared/src/offside.ts lines 234-281 (`triggerOffsideFoul` — the exact return shape `triggerFoulFreeKick` mirrors)
    - packages/server/src/gameEngine.ts — `triggerPenaltyKick` (added in Plan 39-07), `ZONE_CHECK_EXEMPT_PHASES` (~1486), `applyUndo`'s `isBoundary` reduce (~1644) and its `moveTypeForPhase` mapping (~1834), `REPLAY_ELIGIBLE_TYPES` (~6968)
    - packages/shared/src/types.ts (`foulResume`'s declared shape)
  </read_first>
  <action>
Add two functions to `gameEngine.ts`:

`export function triggerFoulFreeKick(state: GameState, foulerId: string, foulHex: HexCoord): GameState` — modelled byte-for-byte on `triggerOffsideFoul`'s return object, with these substitutions: `freeKickHex: foulHex` (the FOULER's tackle hex, not an offside offender's position); `freeKickAttackingTeam` and `attackingTeam`/`activeTeam` set to the fouled team (the team opposite the fouler's); `ball: { position: foulHex, carrierId: null, lastTouchedBy: state.ball.lastTouchedBy }`; `freeKickStageIndex: 0`, `freeKickPlacedPieceIds: []`, `freeKickKickerChosen: false`, `movedPieceIds: []`, `lastDiceRoll: null`. Omit the `offsidePieceIds` field entirely — that one is offside-specific. FK-01 requires the EXISTING `FREE_KICK_SETUP` flow, so change nothing downstream of this transition.

`export function applyFoulChoice(state: GameState, choice: 'continue' | 'restart'): { ok: true; state: GameState } | { ok: false; reason: string }`:

- Phase guard: `state.phase !== 'FOUL_CHOICE'` → `WRONG_PHASE`.
- Choice guard: anything other than `'continue'`/`'restart'` → `INVALID_CHOICE`.
- Append `FOUL_CHOICE_MADE` with `team: state.attackingTeam`, `choice`, and `restart` = `null` for continue, `'PENALTY'` when `state.foulSource === 'GK_DIVE_AT_FEET'`, otherwise `'FREE_KICK'`.
- `'continue'`: restore `phase`, `activeTeam`, `attackingTeam`, `movementSlot` and `lastActionType` from `state.foulResume` (falling back to `'PASS'`/current values if `foulResume` is null, and logging nothing extra), then clear the foul cluster.
- `'restart'`: when `state.foulSource === 'GK_DIVE_AT_FEET'`, return `triggerPenaltyKick(clearedState, fouledTeam)` (GKDIVE-03/PEN-01); otherwise return `triggerFoulFreeKick(clearedState, state.foulDefenderId!, state.foulHex!)` (FK-01).
- Both branches clear `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume` to `null`.

Registration checklist (the STATE.md per-event-type pitfall — this is the BUG-30/31/37 defect class and must not be skipped):

- `ZONE_CHECK_EXEMPT_PHASES`: add `'FOUL_CHOICE'`. The ball has not moved and no zone crossing should fire while a manager is deciding.
- `applyUndo`'s `isBoundary` reduce: `TACKLE_ATTEMPT` and `STEAL_ATTEMPT` are ALREADY unconditional boundaries there, so an Undo can never cross back over the roll that caused the foul — verify this by reading the reduce and record in a comment that no new boundary term is needed for `FOUL_CALLED`/`INJURY_CHECK`/`BOOKING_CHECK`, and that this is precisely why the stored injury/card mutations are safe.
- Add `'FOUL_CHOICE_MADE'` to the boundary disjunction guarded on `state.phase === 'FOUL_CHOICE'` so a manager cannot Undo their own committed choice.
- `REPLAY_ELIGIBLE_TYPES`: add none of `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK` or `FOUL_CHOICE_MADE` — none carries `ballAfter`, matching the existing `GOAL_KICK_CHOICE`/`GOAL_KICK_WINDOW_ADVANCE` exclusion rule. Record that decision as a comment next to the exclusion so a later reader does not assume it was an oversight.
- Confirm the generic `DICE_ROLL` type is NOT used for any of the new rolls (STATE.md v1.6 pitfall) and assert this with a test in `gameEngine.fouls.test.ts` that scans the produced `eventLog` for `type === 'DICE_ROLL'` after a foul and expects zero.
  </action>
  <verify>
  <automated>pnpm --filter @counter-attack/server test -- gameEngine.fouls.test.ts && pnpm --filter @counter-attack/server test</automated>
  </verify>
  <acceptance_criteria> - `gameEngine.ts` exports `applyFoulChoice` and `triggerFoulFreeKick` - `applyFoulChoice('restart')` with `foulSource: 'GK_DIVE_AT_FEET'` returns a state whose phase is `PENALTY_KICK_SETUP_ATTACKING`, and with `foulSource: 'TACKLE'` returns `FREE_KICK_SETUP` - `triggerFoulFreeKick` sets no `offsidePieceIds` field - `ZONE_CHECK_EXEMPT_PHASES` contains `'FOUL_CHOICE'` - `applyUndo`'s `isBoundary` references `FOUL_CHOICE_MADE` - `REPLAY_ELIGIBLE_TYPES` contains none of `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE` - A post-foul `eventLog` contains zero events of type `DICE_ROLL` - `pnpm --filter @counter-attack/server test -- gameEngine.fouls.test.ts gameEngine.booking.test.ts gameEngine.injury.test.ts` passes with zero failures - `pnpm --filter @counter-attack/server test` (full server suite) is green
  </acceptance_criteria>
  <done>The attacker's continue-or-restart choice resolves correctly to resumed play, a free kick, or a penalty, and every new event is correctly registered for Undo and Replay.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                     |
| ------------------------------- | ------------------------------------------------------------------------------- |
| socket handler → engine reducer | `applyFoulChoice` receives a client-chosen enum; `applyMove` receives ids/hexes |
| engine → broadcast state        | Card, injury and foul state are broadcast to both players                       |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                                    |
| ---------- | ---------------------- | ------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-10-01 | Tampering              | Injury / booking dice                      | mitigate    | `injuryDie`/`bookingDie` are parameters supplied by the handler from `rollDice()` (`crypto.randomInt`); `resolveFoulChain` never generates a die and contains no `Math.random`.                    |
| T-39-10-02 | Tampering              | Client claiming a foul outcome directly    | mitigate    | Foul detection reads the SERVER's own `dice.stealDie`/`dice.tackleDie`; no client field participates. The client cannot make `defenderDie` equal 1.                                                |
| T-39-10-03 | Elevation of Privilege | Wrong manager submitting the foul choice   | mitigate    | Engine sets `attackingTeam` to the fouled team; Plan 39-13's handler enforces `socketTeam(socket) === state.attackingTeam` before calling `applyFoulChoice`, which additionally phase-guards.      |
| T-39-10-04 | Tampering              | Red-carded piece continuing to play        | mitigate    | `applyMove` rejects any move by a `redCarded` piece with `RED_CARDED`; the piece is also excluded from professional-foul cover and penalty-kick eligibility.                                       |
| T-39-10-05 | Tampering              | Undo rewinding a committed card or injury  | mitigate    | `applyUndo` already treats resolved `TACKLE_ATTEMPT`/`STEAL_ATTEMPT` as unconditional boundaries, and `FOUL_CHOICE_MADE` is added as a boundary — no Undo can reach back across a foul resolution. |
| T-39-10-06 | Repudiation            | Foul/injury/booking outcomes not auditable | mitigate    | Each check appends its own dedicated `ActionEvent` carrying the die, the threshold attribute and the outcome; the generic `DICE_ROLL` type is deliberately unused (STATE.md pitfall).              |
| T-39-10-SC | Tampering              | npm/pip/cargo installs                     | accept      | No packages installed this phase.                                                                                                                                                                  |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` green (all three new suites plus every pre-existing suite)
- `pnpm build` green
- No `Math.random` in `gameEngine.ts`
- Post-foul event logs contain zero `DICE_ROLL` events
</verification>

<success_criteria>
A defender die of exactly 1 on a tackle or steal calls a foul, immediately rolls injury then booking with fresh dice, applies stored attribute degradation and card state, detects Professional Fouls by remaining-pace reachability, and hands the fouled manager a continue-or-restart choice that resumes play exactly or routes to a free kick (tackle/steal source) or penalty (GK-dive source); with Fouls disabled none of it fires; all new events are correctly registered for Undo and excluded from Replay.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-10-SUMMARY.md` when done
</output>
