---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 02
type: execute
wave: 2
depends_on: ['39-01']
files_modified:
  - packages/shared/src/fouls.ts
  - packages/shared/src/fouls.test.ts
  - packages/shared/src/shotValidator.ts
  - packages/shared/src/shotValidator.test.ts
  - packages/shared/src/index.ts
autonomous: true
requirements: [FOUL-04, CARD-01, CARD-02, CARD-03, INJURY-01, INJURY-02, GKDIVE-02, PEN-01]

must_haves:
  truths:
    - 'An injury die greater than or equal to the fouled player every attribute Resilience injures that player, and a lower die does not (INJURY-01)'
    - 'An injured player has every numeric attribute reduced by exactly 1, never below 1 (INJURY-02)'
    - 'A booking die greater than or equal to the referee Leniency issues a yellow, and a second yellow on the same player becomes a red (CARD-01/CARD-02)'
    - 'A Professional Foul is detected only when no other defending piece can reach the foul hex within its remaining pace (FOUL-04)'
    - 'A dive at feet from exactly 3 hexes carries a -1 saving penalty, 0-2 hexes carries none, and beyond 3 hexes is out of range (GKDIVE-02)'
  artifacts:
    - path: 'packages/shared/src/fouls.ts'
      provides: 'Pure foul/injury/booking/professional-foul helpers'
      exports:
        [
          'FOUL_TRIGGER_DIE',
          'isInjured',
          'applyInjuryDegradation',
          'rollsInjury',
          'resolveBooking',
          'isProfessionalFoul',
        ]
      min_lines: 120
    - path: 'packages/shared/src/fouls.test.ts'
      provides: 'Unit coverage for every helper above'
      min_lines: 150
    - path: 'packages/shared/src/shotValidator.ts'
      provides: 'validateDiveAtFeetDistance sibling of validateGKDive'
      contains: 'validateDiveAtFeetDistance'
  key_links:
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/fouls.js'
      via: 'barrel re-export'
      pattern: "export \\* from './fouls.js'"
---

<objective>
Build the pure, side-effect-free rule kernel for Phase 39 in `packages/shared`, so the engine plans in waves 3-6 assemble already-tested primitives instead of inventing rule math inline.

Purpose: RESEARCH.md Pitfall 2 identifies injury/booking as an INVERTED convention versus every other duel in the codebase (bare `die >= attribute`, higher attribute = better for its owner, never routed through `computeCombinedScore`). RESEARCH.md Pitfall 5 identifies the Professional Foul reachability check as the single highest-implementation-risk item in the phase with no existing precedent. Isolating both as pure functions with dedicated unit tests is the only way to verify them without driving a full socket integration.

Output: `packages/shared/src/fouls.ts`, its unit test, the `validateDiveAtFeetDistance` sibling in `shotValidator.ts`, and the barrel export.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-PATTERNS.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT` (`pitch.ts`); `FOUL_TRIGGER_DIE`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul` (`fouls.ts`); `validateDiveAtFeetDistance` (`shotValidator.ts`)
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; `ROOM_SETTINGS_CONFIRM` keys `fouls`/`booking`/`injury`
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write fouls.test.ts covering every rule primitive before implementing them</name>
  <files>packages/shared/src/fouls.test.ts</files>
  <read_first>
    - packages/shared/src/offside.test.ts (the sibling pure-module test file — copy its describe/it structure, its `PlayerPiece`/`GameState` fixture-builder style, and its import-from-relative-`.js` convention)
    - packages/shared/src/types.ts (as modified by 39-01 — `PlayerPiece` incl. `injuryCount`/`yellowCards`/`redCarded`, `GameState.paceUsedByPieceId`, `RefereeCard.leniency`)
    - packages/shared/src/hex.ts (`hexDistance` signature)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md (Pitfall 2 inverted comparison; Pitfall 5 reachability formula)
  </read_first>
  <behavior>
    - `rollsInjury(die, resilience)` is `true` for die === resilience, `true` for die > resilience, `false` for die < resilience. Explicit cases: (6, 6) true; (5, 6) false; (1, 1) true; (3, 5) false.
    - `rollsBooking(die, leniency)` uses the identical `>=` shape. Explicit cases: (4, 4) true; (3, 4) false; (6, 1) true.
    - `applyInjuryDegradation(piece)` returns a NEW piece with `pace`, `shooting`, `tackling`, `dribbling`, `saving`, `handling`, `resilience`, `aerialAbility`, `highPass` each decremented by 1 but never below 1, `injuryCount` incremented by 1, and `id`/`teamId`/`position`/`firstName`/`lastName`/`number`/`nationality`/`role` untouched. Explicit cases: an attribute at 1 stays 1; an attribute at 5 becomes 4; a piece with `highPass: 0` (goalkeepers legitimately carry 0) stays at 0 rather than being raised to 1 — floor is `Math.max(1, v - 1)` applied only when `v > 0`, documented at the call site.
    - `applyInjuryDegradation` applied twice yields `injuryCount === 2` and a second -1 on every attribute (INJURY-03's "continues at degraded attributes" branch, D-06).
    - `isInjured(piece)` is `true` when `(piece.injuryCount ?? 0) > 0`, `false` for `undefined`/`0`.
    - `resolveBooking({ die, leniency, priorYellows, professional })` returns `{ card: 'none' | 'yellow' | 'red', secondYellow: boolean }`. Cases: normal foul, die < leniency, 0 priors → `{ card: 'none', secondYellow: false }`. Normal foul, die >= leniency, 0 priors → `{ card: 'yellow', secondYellow: false }`. Normal foul, die >= leniency, 1 prior yellow → `{ card: 'red', secondYellow: true }` (CARD-02). Professional foul, die >= leniency → `{ card: 'red', secondYellow: false }` (CARD-03 straight red). Professional foul, die < leniency → `{ card: 'yellow', secondYellow: false }` (CARD-03 otherwise-yellow). Professional foul, die < leniency, 1 prior yellow → `{ card: 'red', secondYellow: true }` (the second-yellow rule still upgrades).
    - `isProfessionalFoul(state, foulerId, foulHex)` is `true` only when NO other piece on the fouler's team (excluding the fouler itself and excluding any `redCarded` piece) satisfies `hexDistance(other.position, foulHex) <= other.pace - (state.paceUsedByPieceId[other.id] ?? 0)`. Cases: a teammate 2 hexes away with `pace: 5` and 0 pace used → `false` (a defender could have covered). The same teammate with 4 of 5 pace already used → `true` (only 1 hex of budget left). Only the fouler on the team → `true`. A teammate in range but `redCarded: true` → `true` (dismissed pieces cannot cover).
    - `FOUL_TRIGGER_DIE === 1` (FOUL-01's fixed trigger value, exported so the engine never hardcodes a magic `1`).
  </behavior>
  <action>
Create `packages/shared/src/fouls.test.ts` with a Vitest suite asserting every case in the `<behavior>` block above. Import the (not-yet-existing) helpers from `./fouls.js`. The suite MUST fail at this point with a module-resolution error — that is the intended RED state, and Task 2 makes it green.

Build fixtures with a local `makePiece(overrides: Partial<PlayerPiece>): PlayerPiece` factory supplying all required `PlayerPiece` fields (`id`, `teamId`, `position`, the nine numeric attributes, `firstName`, `lastName`, `number`, `nationality`, `role`), mirroring `offside.test.ts`'s fixture style. Build `GameState` fixtures with a `makeState(overrides: Partial<GameState>): GameState` factory — only `pieces`, `paceUsedByPieceId`, and `refereeCard` matter for these helpers, but every required field must be supplied so the fixture typechecks under `exactOptionalPropertyTypes`.

Do NOT route any injury or booking assertion through `computeCombinedScore`; assert the bare `>=` behaviour directly, and add a comment in the file naming RESEARCH.md Pitfall 2 so a future refactor does not "correct" the inversion.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared test -- fouls.test.ts</automated>
</verify>
<acceptance_criteria> - `packages/shared/src/fouls.test.ts` exists and contains at least 20 `it(` blocks - Running `pnpm --filter @counter-attack/shared test -- fouls.test.ts` at the end of this task FAILS with an unresolved-import error for `./fouls.js` (RED state confirmed and recorded in the task notes) - The file contains an explicit comment naming "Pitfall 2" and the inverted `die >= attribute` convention - The file asserts the professional-foul redCarded exclusion case and the pace-already-spent case
</acceptance_criteria>
<done>A failing unit suite exists that fully specifies every foul/injury/booking/professional-foul primitive.</done>
</task>

<task type="auto">
  <name>Task 2: Implement packages/shared/src/fouls.ts and export it from the barrel</name>
  <files>packages/shared/src/fouls.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/fouls.test.ts (as written in Task 1 — it is the specification)
    - packages/shared/src/offside.ts (the analog module: pure functions, early `return state` guard clauses instead of throws, no imports from `packages/server`)
    - packages/shared/src/hex.ts (`hexDistance`)
    - packages/shared/src/index.ts (barrel-export ordering and the per-line phase comments)
  </read_first>
  <action>
Create `packages/shared/src/fouls.ts` implementing exactly the contract the Task 1 suite asserts. Required exports and signatures:

- `export const FOUL_TRIGGER_DIE = 1;` — FOUL-01's fixed trigger; comment that the trigger is the DEFENDER's own die (`dice.stealDie` for `STEAL_ATTEMPT`, `dice.tackleDie` for `TACKLE_ATTEMPT`), and that nutmeg is explicitly NOT a v1.6 trigger (REQUIREMENTS.md FOUL-01).
- `export const INJURY_DEGRADED_ATTRIBUTES = ['pace','shooting','tackling','dribbling','saving','handling','resilience','aerialAbility','highPass'] as const;`
- `export function isInjured(piece: PlayerPiece): boolean`
- `export function injuryPenalty(piece: PlayerPiece): number` — returns `-(piece.injuryCount ?? 0)`, provided for any future duel-site use; document that INJURY-02 is delivered by stored degradation, so this helper must NOT be added to `computeCombinedScore` penalty arrays (double-counting).
- `export function applyInjuryDegradation(piece: PlayerPiece): PlayerPiece` — immutable; decrements each `INJURY_DEGRADED_ATTRIBUTES` key by 1 with `v > 0 ? Math.max(1, v - 1) : v`, increments `injuryCount`.
- `export function rollsInjury(die: number, resilience: number): boolean` — `die >= resilience`.
- `export function rollsBooking(die: number, leniency: number): boolean` — `die >= leniency`.
- `export function resolveBooking(input: { die: number; leniency: number; priorYellows: 0 | 1 | 2; professional: boolean }): { card: 'none' | 'yellow' | 'red'; secondYellow: boolean }` — CARD-01/02/03. Order of evaluation: compute the base outcome first (professional ? (rollsBooking ? 'red' : 'yellow') : (rollsBooking ? 'yellow' : 'none')); then, if the base outcome is `'yellow'` and `priorYellows >= 1`, upgrade to `{ card: 'red', secondYellow: true }`.
- `export function isProfessionalFoul(state: GameState, foulerId: string, foulHex: HexCoord): boolean` — FOUL-04, using RESEARCH.md Pitfall 5's straight-line formula `hexDistance(other.position, foulHex) <= other.pace - (state.paceUsedByPieceId[other.id] ?? 0)`. No path-walk, no occupancy simulation — comment that "as the crow flies" matches the move-range highlighting standard already used elsewhere. Exclude the fouler itself and any `redCarded` piece. Return `true` only when the filtered list is empty.

Every guard clause returns a value (never throws), mirroring `offside.ts`. The module imports only from `./types.js`, `./hex.js` — never from `packages/server`.

Add `export * from './fouls.js'; // Phase 39 FOUL-01..04/CARD-01..03/INJURY-01..02: pure foul, injury, booking and professional-foul helpers` to `packages/shared/src/index.ts`, placed after the `outOfBounds.js` line to keep the barrel in phase order.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared test -- fouls.test.ts && pnpm --filter @counter-attack/shared build</automated>
</verify>
<acceptance_criteria> - `pnpm --filter @counter-attack/shared test -- fouls.test.ts` passes with zero failures - `packages/shared/src/fouls.ts` exports all of `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul` - `grep -c "computeCombinedScore" packages/shared/src/fouls.ts` returns 0 outside comments (verify with `grep -v '^\s*[*/]' packages/shared/src/fouls.ts | grep -c computeCombinedScore` → 0) - `grep -c "from './fouls.js'" packages/shared/src/index.ts` returns 1 - `pnpm --filter @counter-attack/shared build` exits 0
</acceptance_criteria>
<done>The foul rule kernel is implemented, fully unit-tested, and exported from `@counter-attack/shared`.</done>
</task>

<task type="auto">
  <name>Task 3: Add validateDiveAtFeetDistance alongside validateGKDive</name>
  <files>packages/shared/src/shotValidator.ts, packages/shared/src/shotValidator.test.ts</files>
  <read_first>
    - packages/shared/src/shotValidator.ts lines 80-120 (`validateGKDive`'s `DiveResult` type, the `d > 3` range guard and the `d === 3 ? -1 : 0` penalty band)
    - packages/shared/src/shotValidator.test.ts (existing `validateGKDive` test cases — the new sibling's tests mirror them)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md (Architecture Pattern 4)
  </read_first>
  <action>
Add `export function validateDiveAtFeetDistance(distance: number): DiveResult` immediately after `validateGKDive` in `shotValidator.ts`. It has the same body shape as `validateGKDive` — `const d = Math.max(distance, 0); if (d > 3) return { saveable: false, reason: 'OUT_OF_RANGE' }; const savingPenalty = d === 3 ? -1 : 0; return { saveable: true, savingPenalty };` — but takes no `PlayerPiece` parameter, because GKDIVE-02's dive-at-feet band depends only on distance.

Do NOT refactor `validateGKDive` to call the new function or vice versa. They are the same rule applied in two different contexts (shot-block dive vs. dive-at-feet); GKDIVE-05/D-09 already couples them behaviourally via a shared cap, and collapsing them into one function would make a future divergence of either band silently affect the other. Record that reasoning as a comment on the new function.

Extend `shotValidator.test.ts` with a `describe('validateDiveAtFeetDistance')` block asserting distance 0 → `{ saveable: true, savingPenalty: 0 }`, distance 2 → `{ saveable: true, savingPenalty: 0 }`, distance 3 → `{ saveable: true, savingPenalty: -1 }`, distance 4 → `{ saveable: false, reason: 'OUT_OF_RANGE' }`, and a negative distance clamped to 0 → `{ saveable: true, savingPenalty: 0 }`.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared test -- shotValidator.test.ts</automated>
</verify>
<acceptance_criteria> - `packages/shared/src/shotValidator.ts` exports `validateDiveAtFeetDistance` - `pnpm --filter @counter-attack/shared test -- shotValidator.test.ts` passes, including at least 5 new `validateDiveAtFeetDistance` assertions - `validateGKDive`'s body is unchanged (a `git diff` of `shotValidator.ts` shows only additions) - `pnpm --filter @counter-attack/shared test` (full shared suite) is green
</acceptance_criteria>
<done>GKDIVE-02's distance-banded penalty exists as a tested pure function ready for the engine plan.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                     |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------- |
| shared kernel → server/client | These pure helpers are imported by BOTH packages; a client-importable helper must never be the authority source |

## STRIDE Threat Register

| Threat ID  | Category               | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                                   |
| ---------- | ---------------------- | ------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-02-01 | Tampering              | `rollsInjury` / `rollsBooking` | mitigate    | Both take an already-rolled `die: number` and never generate one. All dice remain `crypto.randomInt` server-side (`rollDice()` in `packages/server/src/diceUtils.ts`); no `Math.random` may appear in `fouls.ts`. |
| T-39-02-02 | Tampering              | `applyInjuryDegradation`       | mitigate    | Pure and immutable — returns a new `PlayerPiece`, never mutates the input. Only the server engine may write the result into `GameState.pieces`; the client importing it cannot alter authoritative state.         |
| T-39-02-03 | Elevation of Privilege | `isProfessionalFoul`           | mitigate    | Reads only `state.pieces` and `state.paceUsedByPieceId` from the server-authoritative snapshot; takes no client-supplied argument other than ids the caller already validated.                                    |
| T-39-02-SC | Tampering              | npm/pip/cargo installs         | accept      | No packages installed (RESEARCH.md Package Legitimacy Audit: not applicable).                                                                                                                                     |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test` fully green
- `pnpm build` green
- No `Math.random` in `packages/shared/src/fouls.ts`
</verification>

<success_criteria>
`fouls.ts` exists with all nine exports, every one covered by a passing unit test including the inverted `die >= attribute` convention, the two-injury INJURY-03 branch, all six `resolveBooking` card outcomes, and the four `isProfessionalFoul` reachability cases; `validateDiveAtFeetDistance` exists and is tested; the barrel re-exports `fouls.js`.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-02-SUMMARY.md` when done
</output>
