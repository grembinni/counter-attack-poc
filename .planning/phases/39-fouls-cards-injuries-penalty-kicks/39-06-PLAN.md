---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 06
type: execute
wave: 3
depends_on: ['39-01', '39-04']
files_modified:
  - packages/client/src/components/PieceOverlay.tsx
  - packages/client/src/components/PieceOverlay.test.tsx
  - packages/client/src/components/PlayerStatsPanel.tsx
  - packages/client/src/components/PlayerStatsPanel.module.css
  - packages/client/src/components/PlayerStatsPanel.test.tsx
autonomous: true
requirements: [CARD-02, INJURY-02, INJURY-03]

must_haves:
  truths:
    - 'A booked player shows a persistent card-coloured badge on the board for the rest of the match (D-04)'
    - 'An injured player shows a persistent plus-sign badge on the board for the rest of the match (D-04)'
    - 'A player who is both booked and injured shows both badges in the same corner with the injury badge drawn on top (D-05)'
    - 'The player card panel shows a card chip and/or an injury chip beside the role chip for the selected player (D-04)'
  artifacts:
    - path: 'packages/client/src/components/PieceOverlay.tsx'
      provides: 'On-board card and injury badges opposite the ball-possession dot'
      contains: 'cardBadge'
    - path: 'packages/client/src/components/PlayerStatsPanel.tsx'
      provides: 'Roster-panel card/injury chips'
      contains: 'injuryChip'
  key_links:
    - from: 'packages/client/src/components/PieceOverlay.tsx'
      to: 'PlayerPiece.yellowCards / .redCarded / .injuryCount'
      via: 'conditional SVG badge rendering at the negated ball-dot offset'
      pattern: 'dotOffsetX'
---

<objective>
Render D-04/D-05's persistent card and injury indicators in both places the decisions require: on the board (`PieceOverlay.tsx`) and in the player card panel (`PlayerStatsPanel.tsx`).

Purpose: cards and injuries are permanent match state (CARD-02's second-yellow dismissal and INJURY-02/03's degraded attributes both persist to full time). Without a persistent visual the players cannot tell which pieces are booked, which are injured, and which are effectively out of the match.

Output: two SVG badges positioned at the negated ball-dot offset with correct layering, and two roster chips.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-PATTERNS.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-04-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto">
  <name>Task 1: Card and injury badges on PieceOverlay</name>
  <files>packages/client/src/components/PieceOverlay.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx (full file — `PIECE_RADIUS = 12` at ~103, `dotOffsetX`/`dotOffsetY` at ~124-125, the `isBallCarrier` conditional block at ~212-226 with its `r={PIECE_RADIUS * 0.59}`, `fill="#f5f0dc"`, `stroke="rgba(0,0,0,0.5)"`, `strokeWidth={1}`, `pointerEvents="none"` treatment, the `isOffside` ring block at ~200-211 that shows how an independent overlay layer is added, and the trailing player-number `<text>` element)
    - packages/client/src/components/PieceOverlay.test.tsx (existing render harness and how it asserts on SVG output)
    - packages/client/src/styles/tokens.css (the `--color-card-yellow` / `--color-card-red` tokens added in Plan 39-04)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Color section: card/injury badge geometry, D-05)
  </read_first>
  <action>
Per D-04/D-05, add two independent badge layers to `PieceOverlay.tsx`, rendered AFTER the existing `isBallCarrier` block and BEFORE the player-number `<text>` element so they sit above the jersey but below the number.

Derive the badge anchor from the existing dot geometry, negated: `const badgeCx = cx - dotOffsetX;` and `const badgeCy = cy - dotOffsetY;`. Because `dotOffsetY` is always `+PIECE_RADIUS * 0.715` and `dotOffsetX` flips sign by `teamId`, this lands the badge in the top-left corner for home pieces and the top-right for away pieces — the corner opposite the ball dot in every case, exactly as D-05 requires. Add a comment stating this derivation so a future change to the dot geometry moves both together.

Card badge (rendered first):

- Condition: `const cardColor = piece.redCarded === true ? 'red' : (piece.yellowCards ?? 0) > 0 ? 'yellow' : null;` — red always wins, matching CARD-02's second-yellow-becomes-red rule.
- Shape: an SVG `<rect>` (deliberately NOT a circle, so it reads distinctly from the round ball dot) sized to the same visual scale as the dot: width `PIECE_RADIUS * 0.59 * 1.5`, height `PIECE_RADIUS * 0.59 * 2`, `rx={1.5}`, centred on `badgeCx`/`badgeCy` (i.e. `x = badgeCx - width / 2`, `y = badgeCy - height / 2`).
- Fill `var(--color-card-yellow)` or `var(--color-card-red)` keyed off `cardColor`; `stroke="rgba(0,0,0,0.5)"`, `strokeWidth={1}`, `pointerEvents="none"` — the same stroke treatment the ball dot uses.
- Add `data-testid="piece-card-badge"` and `data-card={cardColor}` for test assertions.

Injury badge (rendered second, so it layers on top per D-05):

- Condition: `(piece.injuryCount ?? 0) > 0`.
- Shape: a white plus sign built from two crossed `<rect>` bars inside a `<g>`, sized to the same `PIECE_RADIUS * 0.59` scale (bar length `PIECE_RADIUS * 0.59 * 1.8`, bar thickness `PIECE_RADIUS * 0.59 * 0.6`, `rx={1}`), centred on `badgeCx`/`badgeCy`. Fill `var(--color-text-inverse)` with `stroke="rgba(0,0,0,0.5)"`, `strokeWidth={0.75}` for legibility against any jersey colour. `pointerEvents="none"`.
- Add `data-testid="piece-injury-badge"`.
- D-05 explicitly requires shape-based distinction (cross vs. rectangle), not colour-based, because both may occupy the same corner simultaneously — record that in a comment.

Do not add a new colour literal; both fills come from the tokens added in Plan 39-04. Do not modify the `isBallCarrier`, `isOffside`, uniform-pattern or number-rendering code.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- PieceOverlay.test.tsx</automated>
</verify>
<acceptance_criteria> - `PieceOverlay.tsx` derives the badge anchor as the negation of `dotOffsetX`/`dotOffsetY` (no independently hardcoded offset constants) - `grep -c "var(--color-card-yellow)\|var(--color-card-red)" packages/client/src/components/PieceOverlay.tsx` returns at least 1 and no raw `#eab308`/`#ef4444` literal appears in the file - The injury badge JSX appears strictly after the card badge JSX in source order - `data-testid="piece-card-badge"` and `data-testid="piece-injury-badge"` both exist - `pnpm --filter @counter-attack/client typecheck` exits 0
</acceptance_criteria>
<done>Booked and injured pieces render persistent, correctly-layered badges opposite the ball dot.</done>
</task>

<task type="auto">
  <name>Task 2: PieceOverlay badge tests</name>
  <files>packages/client/src/components/PieceOverlay.test.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.test.tsx (full file — the props fixture and SVG-container render helper)
    - packages/client/src/components/PieceOverlay.tsx (as modified in Task 1)
  </read_first>
  <action>
Add a `describe('card and injury badges (D-04/D-05)')` block asserting:

1. A piece with no cards and no injury renders neither `piece-card-badge` nor `piece-injury-badge`.
2. `yellowCards: 1` renders `piece-card-badge` with `data-card="yellow"`.
3. `redCarded: true` renders `data-card="red"`, and `yellowCards: 1, redCarded: true` also renders `data-card="red"` (red wins).
4. `injuryCount: 1` renders `piece-injury-badge`; `injuryCount: 2` still renders exactly one injury badge (INJURY-03's second injury does not stack a second icon).
5. A piece with both `yellowCards: 1` and `injuryCount: 1` renders both badges, and the injury badge's DOM position is after the card badge's (assert via `compareDocumentPosition` or by index within the rendered element list) — this is D-05's layering requirement.
6. For a `teamId: 'home'` piece the badge centre x is LESS than `cx` and its centre y is LESS than `cy`; for `teamId: 'away'` the badge centre x is GREATER than `cx` and y is LESS than `cy` — proving the "opposite corner from the ball dot" placement for both teams.
   </action>
   <verify>
   <automated>pnpm --filter @counter-attack/client test -- PieceOverlay.test.tsx</automated>
   </verify>
   <acceptance_criteria> - `pnpm --filter @counter-attack/client test -- PieceOverlay.test.tsx` passes with at least 8 new assertions matching the numbered list - The layering test asserts document order, not merely that both badges exist - The placement test covers both `home` and `away` pieces
   </acceptance_criteria>
   <done>Badge presence, colour precedence, layering and per-team placement are regression-locked.</done>
   </task>

<task type="auto">
  <name>Task 3: Card and injury chips in the player card panel</name>
  <files>packages/client/src/components/PlayerStatsPanel.tsx, packages/client/src/components/PlayerStatsPanel.module.css, packages/client/src/components/PlayerStatsPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/PlayerStatsPanel.tsx lines 120-170 (`.cardHeader`, `.playerMeta`, the existing `.roleChip` and `.jerseyNum` spans — the exact row the new chips join)
    - packages/client/src/components/PlayerStatsPanel.module.css (`.playerMeta`, `.roleChip`, `.jerseyNum` — copy `.roleChip`'s padding/radius/font conventions for the new chips)
    - packages/client/src/components/PlayerStatsPanel.test.tsx (render harness and store fixture)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Design System row "Roster/lineup panel indicator (D-04)"; Typography Label role 11px/400; Spacing `sm` 8px chip gap)
  </read_first>
  <action>
Add up to two chips to the existing `.playerMeta` row, rendered after `.jerseyNum`, so the row reads role, number, then status:

- Card chip: rendered when `piece.redCarded === true` or `(piece.yellowCards ?? 0) > 0`. Text is `RED` or `YELLOW`. Class `styles.cardChip`, with `data-card={cardColor}` driving the background via two attribute selectors, exactly mirroring the `EventBanner` `.cardBadge[data-card=...]` pattern established in Plan 39-04. Red wins over yellow, same precedence as the board badge.
- Injury chip: rendered when `(piece.injuryCount ?? 0) > 0`. Text is `INJ` for `injuryCount === 1` and `INJ ×2` for `injuryCount >= 2`, so INJURY-03's second injury is visible (INJURY-02's degraded attribute numbers are already visible in the stat rows because the degradation is a stored mutation — add a comment saying so, so nobody adds a redundant "-1" annotation).
- Both chips carry `data-testid="stats-card-chip"` / `data-testid="stats-injury-chip"`.

In `PlayerStatsPanel.module.css`, add `.cardChip` and `.injuryChip` copying `.roleChip`'s padding, border-radius and font-size (Label role 11px/400 per UI-SPEC — do not introduce a new size or weight), with an 8px left gap. `.cardChip[data-card='yellow']` uses `background: var(--color-card-yellow)` and `.cardChip[data-card='red']` uses `background: var(--color-card-red)`; set the chip text colour to `var(--color-text-inverse)` on red and to the existing dark text token on yellow so both clear contrast. `.injuryChip` uses the existing surface-alt background with `var(--color-text-inverse)` text — introduce no new colour value.

Extend `PlayerStatsPanel.test.tsx` with assertions that: no chips render for a clean player; a yellow-carded player renders `stats-card-chip` with `data-card="yellow"`; a red-carded player renders `data-card="red"`; an injured player renders `stats-injury-chip` reading `INJ`; a twice-injured player reads `INJ ×2`; and a booked-and-injured player renders both chips.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- PlayerStatsPanel.test.tsx && pnpm stylelint</automated>
</verify>
<acceptance_criteria> - `pnpm --filter @counter-attack/client test -- PlayerStatsPanel.test.tsx` passes with at least 6 new assertions - `PlayerStatsPanel.module.css` defines `.cardChip`, `.cardChip[data-card='yellow']`, `.cardChip[data-card='red']` and `.injuryChip`, with font-size 11px and no new font weight - No new colour literal appears in `PlayerStatsPanel.module.css`; all colours are `var(--...)` references - `pnpm stylelint` exits 0 - `pnpm --filter @counter-attack/client test` (full client suite) is green
</acceptance_criteria>
<done>The player card shows card and injury status chips alongside the role chip, completing D-04's "badges AND panel indicator" requirement.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                           |
| ------------------------------------- | --------------------------------------------------------------------- |
| broadcast `GameState.pieces` → SVG/UI | Pure read-only projection; no user input crosses back into game state |

## STRIDE Threat Register

| Threat ID  | Category               | Component                       | Disposition | Mitigation Plan                                                                                                                                            |
| ---------- | ---------------------- | ------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-06-01 | Spoofing               | Badge state                     | accept      | Rendered directly from the server-authoritative `PlayerPiece` fields; the client can display but never assign a card or injury.                            |
| T-39-06-02 | Information Disclosure | Opponent card/injury visibility | accept      | Cards and injuries are public match facts in the physical game; both managers are meant to see them. No per-socket filtering is warranted (unlike drafts). |
| T-39-06-03 | Denial of Service      | Badge rendering cost            | accept      | At most two extra SVG elements per piece across 22 pieces; far inside SVG's comfortable range for this grid (project stack rationale).                     |
| T-39-06-SC | Tampering              | npm/pip/cargo installs          | accept      | No packages installed this phase.                                                                                                                          |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test` green
- `pnpm stylelint` green
- `pnpm build` green
</verification>

<success_criteria>
Booked and injured pieces carry persistent board badges in the corner opposite the ball dot, with the injury cross layered over the card rectangle when both apply and red taking precedence over yellow; the player card panel shows matching status chips; every colour comes from the two Phase 39 semantic tokens or existing tokens.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-06-SUMMARY.md` when done
</output>
