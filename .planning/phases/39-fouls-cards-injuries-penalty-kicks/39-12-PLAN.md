---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 12
type: execute
wave: 5
depends_on: ['39-10']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.gkDiveAtFeet.test.ts
autonomous: true
requirements: [GKDIVE-01, GKDIVE-02, GKDIVE-03, GKDIVE-04, GKDIVE-05]

must_haves:
  truths:
    - 'A goalkeeper adjacent to the ball carrier resolves through the existing tackle duel with the goalkeeper as tackler, with no new duel type (GKDIVE-01)'
    - 'Whenever the carrier ends a move within 3 hexes of the goalkeeper parallel to the goal line, the defending manager is offered a dive, with a -1 dice penalty from the 3rd hex (GKDIVE-02)'
    - 'A goalkeeper die of 1 in a dive at feet awards a penalty kick regardless of who won the duel (GKDIVE-03)'
    - 'A successful dive landing on an occupied hex displaces every piece there, and the ball if present, one hex further in the dive direction (GKDIVE-04)'
    - 'A goalkeeper may dive at feet at most once per movement cycle, and using it also disables the shot-blocking GK_DIVE reposition for that cycle (GKDIVE-05, D-09)'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'Dive-at-feet offer, duel, displacement and the shared GK_DIVE cap helper'
      contains: 'export function enterGkDiveOrSkip'
    - path: 'packages/server/src/__tests__/gameEngine.gkDiveAtFeet.test.ts'
      provides: 'GKDIVE-01..05 engine coverage'
      min_lines: 220
  key_links:
    - from: 'the four phase GK_DIVE transition sites'
      to: 'enterGkDiveOrSkip'
      via: 'shared cap-checking helper replacing each inline transition'
      pattern: 'enterGkDiveOrSkip\\('
    - from: 'applyGkDiveAtFeetResponse'
      to: 'resolveFoulChain'
      via: 'GKDIVE-03 foul on a goalkeeper die of 1, source GK_DIVE_AT_FEET'
      pattern: "source: 'GK_DIVE_AT_FEET'"
---

<objective>
Implement the goalkeeper dive-at-feet interrupt end to end in the engine: the repeating offer, the reused tackle duel with the distance-banded penalty, the foul-on-1 that awards a penalty kick, the displacement of pieces on a successful landing, and the once-per-movement-cycle cap that D-09 extends to cover the existing shot-block `GK_DIVE`.

Purpose: RESEARCH.md Pitfall 3 confirms `gameEngine.ts` has FOUR distinct `phase: 'GK_DIVE'` transition sites (lines 3098, 3198, 5892, 6180). Missing even one silently breaks D-09, and the header-derived paths are the ones least likely to be manually tested.

Output: `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `enterGkDiveOrSkip`, the four call-site replacements, and a unit suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-PATTERNS.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-10-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write gameEngine.gkDiveAtFeet.test.ts covering GKDIVE-01..05 and D-09</name>
  <files>packages/server/src/__tests__/gameEngine.gkDiveAtFeet.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.phase10.test.ts and shotGkRange.test.ts (existing GK/shot fixture style, including how a goalkeeper piece is positioned relative to its own goal)
    - packages/server/src/gameEngine.ts lines 890-1050 (the `TACKLE_ATTEMPT` duel GKDIVE-01 reuses) and the four `phase: 'GK_DIVE'` transition sites at 3098, 3198, 5892, 6180
    - packages/shared/src/shotValidator.ts (`validateGKDive` and Plan 39-02's `validateDiveAtFeetDistance`)
    - packages/shared/src/scoreUtils.ts (`computeCombinedScore`, `computeLooseBall`, and the cube-vector direction helpers GKDIVE-04's displacement walk reuses)
    - packages/shared/src/pitch.ts (`GOAL_R_VALUES`, `PITCH_REGIONS.homeGoal`/`awayGoal`, `isPitchHex`)
  </read_first>
  <behavior>
    - `computeGkDiveAtFeetOffer(state)` returns `null` when: Fouls-independent preconditions fail (no carrier, no defending goalkeeper), the carrier is farther than 3 hexes from the goalkeeper, the carrier is not parallel to the goal line relative to the goalkeeper, or the defending team's `gkDiveAtFeetUsedByTeam` flag is already true (GKDIVE-05).
    - "Parallel to the goal line" is evaluated as the carrier and the goalkeeper sharing the same offset column band toward the defended goal — define it as `Math.abs(carrier.position.q - gk.position.q) <= 3` combined with `hexDistance(carrier.position, gk.position) <= 3` and the carrier being on the goalkeeper's side of the pitch. Assert the boundary cases: distance 3 offers, distance 4 does not.
    - When an offer exists, `computeGkDiveAtFeetOffer` returns `{ gkId, carrierId, distance, team }` with `distance` equal to `hexDistance(gk.position, carrier.position)`.
    - `applyGkDiveAtFeetResponse(state, false, ...)` appends `GK_DIVE_AT_FEET_DECLINED`, restores the phase/activeTeam/movementSlot from `gkDiveAtFeetResume`, clears the `gkDiveAtFeet*` context fields, and does NOT set the `gkDiveAtFeetUsedByTeam` flag — declining must not consume the once-per-cycle allowance.
    - `applyGkDiveAtFeetResponse(state, true, { gkDie, carrierDie, injuryDie, bookingDie })` computes `computeCombinedScore(gk.saving, gkDie, [savingPenalty])` versus `computeCombinedScore(carrier.dribbling, carrierDie, [])`, where `savingPenalty` comes from `validateDiveAtFeetDistance(distance)` and is `-1` only at distance exactly 3 (GKDIVE-02). Defender wins ties, matching the existing `TACKLE_ATTEMPT` convention (`defCombined >= carCombined` → SUCCESS).
    - A successful dive appends `GK_DIVE_AT_FEET` with `result: 'SUCCESS'`, moves the goalkeeper onto the carrier's hex, transfers the ball to the goalkeeper, and sets `gkDiveAtFeetUsedByTeam[team] = true` (GKDIVE-05).
    - A failed dive appends `result: 'FAIL'`, leaves the ball with the carrier, still sets `gkDiveAtFeetUsedByTeam[team] = true`, and resumes the interrupted phase from `gkDiveAtFeetResume`.
    - GKDIVE-04: when the goalkeeper's landing hex is occupied by pieces other than the carrier, `computeGkDiveDisplacement` pushes each occupant, and the ball when it sits on that hex, exactly one hex further along the dive direction (goalkeeper origin → landing hex). Assert: a single occupant is pushed one hex; an occupant pushed into another occupied hex cascades (the second occupant is pushed too); an occupant that would be pushed off-pitch stays put and the dive still resolves without throwing.
    - GKDIVE-03: any dive-at-feet with `gkDie === 1` produces a `FOUL_CALLED` event with `source: 'GK_DIVE_AT_FEET'`, regardless of whether the duel was SUCCESS or FAIL — assert both. The foul chain still runs its injury and booking checks (subject to their toggles), and the phase becomes `FOUL_CHOICE` with the fouled (attacking) team deciding. Choosing `restart` from that state produces a penalty kick, not a free kick (already covered by Plan 39-10's `applyFoulChoice` tests; assert here only that `foulSource` is `'GK_DIVE_AT_FEET'`).
    - D-09 shared cap: `enterGkDiveOrSkip(state, ...)` transitions to `'GK_DIVE'` normally when the goalkeeper's team has not used its dive-at-feet this cycle, and SKIPS the interactive reposition (returning a state that proceeds with the goalkeeper at its current position, `gkDivePosition` set to the goalkeeper's own position) when `gkDiveAtFeetUsedByTeam[gkTeam]` is true.
    - `gkDiveAtFeetUsedByTeam` resets to `{ home: false, away: false }` at every point the 4-5-2 movement cycle restarts — assert it is cleared by the same transition that resets `movedPieceIds`/`paceUsedByPieceId` for a new movement cycle.
  </behavior>
  <action>
Create the suite asserting every bullet above, importing from `../gameEngine.js`. It MUST fail at the end of this task.

Position goalkeepers and carriers using real `PITCH_REGIONS`/`GOAL_R_VALUES`-derived coordinates so the parallel-to-goal-line and distance predicates are exercised against the real 37x26 geometry.

Inject every die as an explicit argument. Do NOT `vi.mock` `diceUtils`.

Add a dedicated `describe('D-09 shared cap — all four GK_DIVE entry points')` block that drives each of the four transition sites and asserts the skip behaviour at each: (1) a declared shot via `applyDeclareShot`, (2) the uncontested-attacker-win header goal-line route, (3) the contested-duel-win header goal-line route, and (4) `applyResolveHeaderTarget`'s goal-line route. RESEARCH.md Pitfall 3 names these four explicitly; missing one is the documented failure mode.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- gameEngine.gkDiveAtFeet.test.ts</automated>
</verify>
<acceptance_criteria> - The file exists with at least 25 `it(` blocks - It contains a `describe` block that exercises all four `GK_DIVE` entry points by name - Running the suite at the end of this task FAILS (RED state recorded in task notes) - No `vi.mock('../diceUtils.js')` appears in the file - Goalkeeper/carrier coordinates derive from `PITCH_REGIONS`/`GOAL_R_VALUES`, not invented literals
</acceptance_criteria>
<done>A failing suite fully specifies GKDIVE-01 through GKDIVE-05 including the four-site shared cap.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the dive-at-feet offer, duel and GKDIVE-04 displacement</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.gkDiveAtFeet.test.ts (Task 1's spec)
    - packages/server/src/gameEngine.ts lines 930-1050 (the `TACKLE_ATTEMPT` duel — the exact `computeCombinedScore` pairing, tie-goes-to-defender rule, event shape and ball-transfer handling GKDIVE-01 reuses)
    - packages/server/src/gameEngine.ts — `resolveFoulChain` (added in Plan 39-10) and the `applyMove` hook points it is called from
    - packages/shared/src/shotValidator.ts (`validateDiveAtFeetDistance`)
    - packages/shared/src/scoreUtils.ts lines 50-100 (`LOOSE_BALL_CUBE_DIRECTIONS`, `computeLooseBall`, and `toCube`/`fromCube` in hex.ts — the parity-safe directional walk GKDIVE-04's displacement must reuse rather than hand-rolling offset deltas)
    - packages/shared/src/hex.ts (`hexDistance`, `hexNeighbors`, `toCube`, `fromCube`)
  </read_first>
  <action>
Add three functions to `gameEngine.ts`, grouped beside the existing GK code.

`export function computeGkDiveAtFeetOffer(state: GameState): { gkId: string; carrierId: string; distance: number; team: 'home' | 'away' } | null` — GKDIVE-02's repeating interrupt eligibility. Returns `null` unless: `state.ball.carrierId` resolves to a piece; the defending team (the team not equal to `state.attackingTeam`) has a `role === 'GK'` piece that is not `redCarded`; `hexDistance(gk.position, carrier.position) <= 3`; the carrier lies on the goalkeeper's side of the pitch, parallel to the goal line, expressed as `Math.abs(carrier.position.q - gk.position.q) <= 3` (a carrier beyond the goalkeeper's column band is running away from goal, not across it); and `state.gkDiveAtFeetUsedByTeam?.[gkTeam] !== true` (GKDIVE-05). Add a comment naming GKDIVE-02's verbatim "within 3 hexes of the goalkeeper, parallel to the goal line" wording next to the predicate so a later reader can re-check it against the rulebook.

`export function computeGkDiveDisplacement(pieces: readonly PlayerPiece[], ball: BallState, from: HexCoord, to: HexCoord): { pieces: readonly PlayerPiece[]; ball: BallState }` — GKDIVE-04. Derive the dive direction as the cube-vector difference `toCube(to) - toCube(from)` normalised to a unit step, then reuse the same `toCube`/`fromCube` round-trip `computeLooseBall` uses. Do NOT hand-roll fixed ODD-Q offset deltas — Phase 17.1-08 already fixed exactly that bug once and the reasoning is recorded in `scoreUtils.ts`. Push every occupant of `to` (and the ball when `ball.position` equals `to` with no carrier) one step along that direction; if the pushed hex is itself occupied, cascade the push recursively; if a pushed hex is off-pitch (`!isPitchHex`), leave that piece where it is and stop that chain. Never throw.

`export function applyGkDiveAtFeetResponse(state: GameState, accept: boolean, dice: { gkDie: number; carrierDie: number; injuryDie: number; bookingDie: number }): { ok: true; state: GameState } | { ok: false; reason: string }`:

- Phase guard `!== 'GK_DIVE_AT_FEET_PROMPT'` → `WRONG_PHASE`.
- `accept === false`: append `GK_DIVE_AT_FEET_DECLINED` with `gkId`/`carrierId`, restore phase/activeTeam/movementSlot from `gkDiveAtFeetResume`, clear `gkDiveAtFeetTeam`/`gkDiveAtFeetGkId`/`gkDiveAtFeetCarrierId`/`gkDiveAtFeetDistance`/`gkDiveAtFeetResume`, and leave `gkDiveAtFeetUsedByTeam` untouched.
- `accept === true`: compute `savingPenalty` from `validateDiveAtFeetDistance(state.gkDiveAtFeetDistance ?? 0)` — if it returns `saveable: false` treat the offer as stale and return `WRONG_PHASE`. Compute `gkCombined = computeCombinedScore(gk.saving, gkDie, [savingPenalty])` and `carrierCombined = computeCombinedScore(carrier.dribbling, carrierDie, [])`; SUCCESS when `gkCombined >= carrierCombined` (defender wins ties, matching `TACKLE_ATTEMPT`). Append `GK_DIVE_AT_FEET` with both dice, both combined scores, `distance`, `savingPenalty`, `result` and `ballAfter`.
  - SUCCESS: run `computeGkDiveDisplacement` for the goalkeeper's landing hex (the carrier's position), then move the goalkeeper there and transfer the ball to the goalkeeper (`carrierId: gk.id`, `lastTouchedBy` = the goalkeeper). Set `attackingTeam`/`activeTeam` to the goalkeeper's team and `lastActionType: 'SUCCESSFUL_TACKLE'`, mirroring the successful-tackle turnover path.
  - FAIL: leave positions and the ball unchanged.
  - Either way set `gkDiveAtFeetUsedByTeam[team] = true`.
- GKDIVE-03: after the duel event is appended, call `resolveFoulChain` with `defenderId: gkId`, `victimId: carrierId`, `foulHex: carrier.position`, `source: 'GK_DIVE_AT_FEET'`, `defenderDie: gkDie`, and the injury/booking dice. When it reports `fouled`, override the return with `phase: 'FOUL_CHOICE'`, the fouled (carrier's) team as `attackingTeam`/`activeTeam`, the returned `foulFields`, and `foulResume` capturing the state the duel would otherwise have produced — the same override shape Plan 39-10 established in `applyMove`. This must happen regardless of SUCCESS or FAIL, which is exactly what GKDIVE-03's "regardless of the duel's outcome" requires.
- When not fouled, restore the interrupted phase from `gkDiveAtFeetResume` (FAIL) or move to the turnover phase (SUCCESS).
  </action>
  <verify>
  <automated>pnpm --filter @counter-attack/server test -- gameEngine.gkDiveAtFeet.test.ts</automated>
  </verify>
  <acceptance_criteria> - `gameEngine.ts` exports `computeGkDiveAtFeetOffer`, `computeGkDiveDisplacement` and `applyGkDiveAtFeetResponse` - `computeGkDiveDisplacement` uses `toCube`/`fromCube` and contains no hardcoded ODD-Q offset delta table - `applyGkDiveAtFeetResponse` calls `validateDiveAtFeetDistance` rather than inlining a `d === 3 ? -1 : 0` expression - Declining does not set `gkDiveAtFeetUsedByTeam`; accepting always does, on both SUCCESS and FAIL - `resolveFoulChain` is invoked with `source: 'GK_DIVE_AT_FEET'` on both SUCCESS and FAIL paths - All GKDIVE-01..04 assertions in `gameEngine.gkDiveAtFeet.test.ts` pass (the D-09 four-site block may still fail until Task 3)
  </acceptance_criteria>
  <done>The dive-at-feet interrupt resolves through the existing tackle duel with the correct distance penalty, displacement and foul-on-1 routing.</done>
  </task>

<task type="auto">
  <name>Task 3: enterGkDiveOrSkip and the four GK_DIVE call-site replacements (D-09)</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts line 3098 (header goal-line route, uncontested-attacker-win branch), line 3198 (header goal-line route, contested-duel-win branch), line 5892 (`applyResolveHeaderTarget`'s goal-line route), line 6180 (`applyDeclareShot`) — read the full surrounding return object at each site
    - packages/server/src/gameHandlers.ts lines 3009-3087 (the `GAME_GK_DIVE` handler's auto-resolve wiring — the reason "disabled" must mean "skip the interactive phase", since the phase resolves on a single click)
    - packages/server/src/__tests__/gameEngine.gkDiveAtFeet.test.ts (the D-09 four-site block from Task 1)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md (Pitfall 3; Assumption A3 and the recommendation that "disabled" means skip-the-interactive-phase-entirely)
    - packages/server/src/gameEngine.ts — the movement-cycle reset points that clear `movedPieceIds`/`paceUsedByPieceId`
  </read_first>
  <action>
Add `export function enterGkDiveOrSkip(state: GameState, gkTeam: 'home' | 'away', gkDivePosition: HexCoord): { phase: GamePhase; gkDivePosition: HexCoord | null }` to `gameEngine.ts`.

Behaviour (D-09, resolving RESEARCH.md Assumption A3 with its recommended reading): when `state.gkDiveAtFeetUsedByTeam?.[gkTeam] === true`, return `{ phase: 'SHOT', gkDivePosition }` — the goalkeeper stays exactly where it is and the shot proceeds to its normal resolution with no reposition window. Otherwise return `{ phase: 'GK_DIVE', gkDivePosition }`, the current behaviour. Document at the function that `GK_DIVE` auto-resolves on the goalkeeper's single click (`gameHandlers.ts` `GAME_GK_DIVE` rolls the shot immediately after the dive), which is why "disabled from diving" is implemented as skipping the reposition rather than as a decline affordance.

Replace ALL FOUR inline `phase: 'GK_DIVE'` transitions with a call to this helper, feeding each site's existing `gkDivePosition` seed value. Read each surrounding return object fully first — each site currently sets other fields (shot target, header winner, deflection state) that must be preserved untouched. The four sites are:

1. `applyDeclareShot`, currently line 6180.
2. Header goal-line route, uncontested-attacker-win branch, currently line 3098.
3. Header goal-line route, contested-duel-win branch, currently line 3198.
4. `applyResolveHeaderTarget`'s goal-line route, currently line 5892.

After the replacement, `grep -n "phase: 'GK_DIVE'" packages/server/src/gameEngine.ts` must return only the occurrence inside `enterGkDiveOrSkip` itself.

Movement-cycle reset: locate every point that resets `movedPieceIds: []` and `paceUsedByPieceId: {}` to begin a fresh 4-5-2 movement cycle (the kick-off/slot-cycle restart paths, not the mid-cycle slot advances) and add `gkDiveAtFeetUsedByTeam: { home: false, away: false }` there. GKDIVE-05 caps the dive at once per MOVEMENT CYCLE, not once per slot — a slot advance inside the same cycle must NOT clear it. State this distinction in a comment at each reset site you touch, and add a test asserting a slot advance leaves the flag set.

Also register the two new dive events:

- `applyUndo`'s `isBoundary` reduce: add `evt.type === 'GK_DIVE_AT_FEET'` unconditionally (a resolved duel is a committed dice outcome, exactly like `TACKLE_ATTEMPT`/`STEAL_ATTEMPT` already are).
- `ZONE_CHECK_EXEMPT_PHASES`: add `'GK_DIVE_AT_FEET_PROMPT'`.
- `REPLAY_ELIGIBLE_TYPES`: add `'GK_DIVE_AT_FEET'` (it carries `ballAfter`); do NOT add `'GK_DIVE_AT_FEET_DECLINED'` (no `ballAfter`), and note the reason in a comment.
  </action>
  <verify>
  <automated>pnpm --filter @counter-attack/server test -- gameEngine.gkDiveAtFeet.test.ts && pnpm --filter @counter-attack/server test</automated>
  </verify>
  <acceptance_criteria> - `gameEngine.ts` exports `enterGkDiveOrSkip` - `grep -c "phase: 'GK_DIVE'" packages/server/src/gameEngine.ts` returns exactly 1, and that occurrence is inside `enterGkDiveOrSkip` - All four former transition sites call `enterGkDiveOrSkip` and preserve every other field of their original return object - `gkDiveAtFeetUsedByTeam` is reset at each movement-cycle restart and is NOT reset by a slot advance (asserted by a test) - `applyUndo`'s `isBoundary` references `GK_DIVE_AT_FEET`; `ZONE_CHECK_EXEMPT_PHASES` contains `GK_DIVE_AT_FEET_PROMPT`; `REPLAY_ELIGIBLE_TYPES` contains `GK_DIVE_AT_FEET` but not `GK_DIVE_AT_FEET_DECLINED` - `pnpm --filter @counter-attack/server test -- gameEngine.gkDiveAtFeet.test.ts` passes with zero failures, including all four D-09 entry-point cases - `pnpm --filter @counter-attack/server test` (full server suite) is green — no regression in the existing shot and header suites
  </acceptance_criteria>
  <done>All four shot-blocking dive entry points share one cap helper, so using the dive-at-feet interrupt genuinely disables the shot-block dive for the rest of the movement cycle.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                     |
| ------------------------------- | --------------------------------------------------------------- |
| socket handler → engine reducer | `applyGkDiveAtFeetResponse` receives a client boolean plus dice |
| engine → broadcast state        | Displacement moves opponent pieces; both players see the result |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                                             |
| ---------- | ---------------------- | ------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-12-01 | Tampering              | Dive duel dice                              | mitigate    | All four dice are parameters supplied by Plan 39-15's handler from `rollDice()`; the engine generates none and contains no `Math.random`.                                   |
| T-39-12-02 | Elevation of Privilege | Attacking manager accepting the dive        | mitigate    | Engine phase-guards `GK_DIVE_AT_FEET_PROMPT` and reads the goalkeeper from `gkDiveAtFeetTeam`; Plan 39-15's handler additionally enforces `controlsGKTeam`.                 |
| T-39-12-03 | Tampering              | Bypassing the once-per-cycle cap            | mitigate    | `gkDiveAtFeetUsedByTeam` is set inside the engine on every accepted dive and is only cleared at a movement-cycle restart; `computeGkDiveAtFeetOffer` refuses to re-offer.   |
| T-39-12-04 | Tampering              | Displacement pushing a piece off the pitch  | mitigate    | `computeGkDiveDisplacement` checks `isPitchHex` on every pushed hex and leaves the piece in place rather than writing an off-grid coordinate (the 37-13 off-grid lesson).   |
| T-39-12-05 | Denial of Service      | Infinite cascade in displacement recursion  | mitigate    | The cascade walks strictly outward one hex per step and terminates at the first off-pitch hex; bound the recursion by the pitch dimension and assert termination in a test. |
| T-39-12-06 | Elevation of Privilege | Shot-block dive after a dive-at-feet (D-09) | mitigate    | A single `enterGkDiveOrSkip` helper replaces all four transition sites, so no path can reach the reposition window while the cap is set.                                    |
| T-39-12-SC | Tampering              | npm/pip/cargo installs                      | accept      | No packages installed this phase.                                                                                                                                           |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` green
- `pnpm build` green
- Exactly one `phase: 'GK_DIVE'` literal remains in `gameEngine.ts`
- No `Math.random` in `gameEngine.ts`
</verification>

<success_criteria>
The goalkeeper interrupt is offered whenever the carrier ends a move within 3 hexes parallel to the goal line and the cap is unused; accepting resolves through the existing tackle duel with a -1 penalty at distance 3; a goalkeeper die of 1 fouls into a penalty regardless of the duel result; a successful landing displaces occupants and the ball one hex along the dive direction without ever writing an off-grid coordinate; and using the interrupt disables the shot-block dive at all four of its entry points for the rest of the movement cycle.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-12-SUMMARY.md` when done
</output>
