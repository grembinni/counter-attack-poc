---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 14
type: execute
wave: 6
depends_on: ['39-12']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.boxEntry.test.ts
  - packages/server/src/__tests__/gameEngine.secondHalf.test.ts
autonomous: true
requirements: [GKDIVE-02, GKDIVE-05]

must_haves:
  truths:
    - 'The first time the ball enters a penalty area during a movement cycle, the defending manager is offered a one-hex goalkeeper reposition (D-10)'
    - 'The box-entry response fires on any means of entry — pass, shot, move or loose ball — not only on a declared shot (D-10)'
    - 'The box-entry response has its own once-per-movement-cycle cap, independent of the dive-at-feet / shot-block cap (D-11)'
    - 'The second half starts only after BOTH managers confirm; either may confirm first (D-16)'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'Box-entry offer/response/move and the second-half mutual-confirm reducer'
      contains: 'export function computeBoxEntryOffer'
    - path: 'packages/server/src/__tests__/gameEngine.boxEntry.test.ts'
      provides: 'D-10/D-11 coverage'
      min_lines: 160
    - path: 'packages/server/src/__tests__/gameEngine.secondHalf.test.ts'
      provides: 'D-16 mutual-confirm coverage'
      min_lines: 80
  key_links:
    - from: 'broadcast-time ball position'
      to: 'computeBoxEntryOffer'
      via: 'penalty-area membership test against the pre-action ball position'
      pattern: 'homePenaltyArea'
    - from: 'applySecondHalfConfirm'
      to: 'GameState.secondHalfConfirmed'
      via: 'headerConfirmed-shaped both-confirm gate'
      pattern: 'secondHalfConfirmed'
---

<objective>
Implement the two remaining engine-side scope items the user explicitly pulled into this phase: D-10/D-11's box-entry goalkeeper response move, and D-16's second-half mutual-confirm gate.

Purpose: D-10 is a new capability confirmed directly by the user — it triggers on mere ball entry into the penalty area by ANY means, and is explicitly NOT the existing shot-declared `GK_DIVE` phase. D-16 replaces today's single-team `canStart` gate with a both-confirm gate. RESEARCH.md Pitfall 4 warns that `LINEUP_CONFIRM`'s flags live on the pre-match `Room` object and cannot be copied verbatim for a mid-match gate; `GameState.headerConfirmed`'s shape is the correct analog.

Output: `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`, and two unit suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-PATTERNS.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-12-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing box-entry and second-half suites</name>
  <files>packages/server/src/__tests__/gameEngine.boxEntry.test.ts, packages/server/src/__tests__/gameEngine.secondHalf.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.phase8.test.ts (existing `applyHalfTimeStart` coverage — the fixture that seeds a `HALF_TIME` state)
    - packages/server/src/gameEngine.ts — `applyHalfTimeStart` and the `HALF_TIME` → `KICK_OFF_SETUP` transition it performs
    - packages/shared/src/types.ts (`headerConfirmed`'s `{ home: boolean; away: boolean } | null` shape — the declared template for `secondHalfConfirmed`)
    - packages/shared/src/pitch.ts (`PITCH_REGIONS.homePenaltyArea`/`awayPenaltyArea`, `isInRegion`, `isPitchHex`), packages/shared/src/hex.ts (`hexNeighbors`)
    - packages/server/src/gameEngine.ts — `computeGkDiveAtFeetOffer` and `applyGkDiveAtFeetResponse` from Plan 39-12 (the prompt-pattern these mirror)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md (Pitfall 4)
  </read_first>
  <behavior>
    **gameEngine.boxEntry.test.ts (D-10/D-11)**
    - `computeBoxEntryOffer(prevBallPosition, nextState)` returns `{ team, gkId }` when the ball was OUTSIDE both penalty areas before the action and is INSIDE one of them after, and the defending team of that area has a non-red-carded goalkeeper, and `gkBoxEntryUsedByTeam[defendingTeam] !== true`.
    - It returns `null` when the ball was already inside that area before the action (only the FIRST entry per cycle offers), when the ball never entered, when the cap flag is already set, and when the defending team has no goalkeeper.
    - It fires for every means of entry: assert with a state produced by a pass landing inside the area, a shot travelling into it, a carrier's move into it, and a loose-ball landing inside it. D-10 says "by any means".
    - The offered team is the team that DEFENDS the entered area: entering `homePenaltyArea` offers the response to the home team, and `awayPenaltyArea` to the away team.
    - `applyBoxEntryResponse(state, false)` restores phase/activeTeam/movementSlot from `gkBoxEntryResume`, clears `gkBoxEntryTeam`/`gkBoxEntryResume`, and sets `gkBoxEntryUsedByTeam[team] = true` (declining still consumes the once-per-cycle opportunity, because the offer is a one-shot per entry and re-offering on the next sub-action would spam the manager).
    - `applyBoxEntryResponse(state, true)` transitions to `GK_BOX_ENTRY_MOVE` with `activeTeam` set to the responding team, leaving `gkBoxEntryResume` intact for the post-move restore.
    - `applyBoxEntryMove(state, to)` rejects a non-adjacent hex (`OUT_OF_RANGE`), an off-pitch hex (`OFF_PITCH`) and an occupied hex (`OCCUPIED`). On success it moves the goalkeeper exactly one hex, appends `GK_BOX_ENTRY_MOVE` with `gkId`/`from`/`to`, sets `gkBoxEntryUsedByTeam[team] = true`, restores the interrupted phase from `gkBoxEntryResume`, and clears the box-entry context fields.
    - D-11 independence: a state where `gkBoxEntryUsedByTeam.home === true` still yields a dive-at-feet offer from `computeGkDiveAtFeetOffer` when its own preconditions hold, and vice versa — using one cap must not consume the other. Assert both directions.
    - `gkBoxEntryUsedByTeam` resets at each movement-cycle restart and NOT at a slot advance, mirroring the dive-at-feet cap.

    **gameEngine.secondHalf.test.ts (D-16)**
    - `applySecondHalfConfirm(state, 'home')` from a `HALF_TIME` state with `secondHalfConfirmed: null` returns a state still in `HALF_TIME` with `secondHalfConfirmed: { home: true, away: false }` and a `SECOND_HALF_CONFIRM` event with `team: 'home', bothConfirmed: false`.
    - A second call with `'away'` sets both flags, appends `SECOND_HALF_CONFIRM` with `bothConfirmed: true`, and only then performs the existing `applyHalfTimeStart` transition to `KICK_OFF_SETUP`.
    - Either team may confirm first — assert the away-then-home order produces the identical end state.
    - A repeated confirm from the SAME team is idempotent: it does not append a duplicate event and does not start the half.
    - Calling it outside `HALF_TIME` returns `WRONG_PHASE`.
    - After the half starts, `secondHalfConfirmed` is cleared back to `null`.

  </behavior>
  <action>
Create both suites asserting every bullet above, importing from `../gameEngine.js`. They MUST fail at the end of this task.

For the box-entry suite, build the "before" and "after" ball positions from real `PITCH_REGIONS.homePenaltyArea`/`awayPenaltyArea` members and real non-member hexes — never invented coordinates.

Record in a comment in `gameEngine.secondHalf.test.ts` that RESEARCH.md Pitfall 4 rules out copying `LINEUP_CONFIRM`'s `Room`-scoped flags: half-time is mid-match, `GameState` already exists, and `headerConfirmed`'s `{ home, away }` shape is the correct GameState-scoped analog.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- gameEngine.boxEntry.test.ts gameEngine.secondHalf.test.ts</automated>
</verify>
<acceptance_criteria> - Both files exist with a combined total of at least 25 `it(` blocks - The box-entry suite covers all four means of entry (pass, shot, move, loose ball) as separate cases - The D-11 independence test asserts BOTH directions (box-entry used does not block dive-at-feet, and vice versa) - Running both suites at the end of this task FAILS (RED state recorded in task notes) - `gameEngine.secondHalf.test.ts` contains the recorded Pitfall 4 comment
</acceptance_criteria>
<done>Two failing suites fully specify the box-entry response and the second-half mutual-confirm gate.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the box-entry goalkeeper response (D-10/D-11)</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.boxEntry.test.ts (Task 1's spec)
    - packages/server/src/gameEngine.ts — `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse` and the `gkDiveAtFeetResume` restore pattern from Plan 39-12 (this mechanic mirrors them structurally)
    - packages/server/src/gameEngine.ts lines 1486-1502 (`ZONE_CHECK_EXEMPT_PHASES`), 1644-1662 (`applyUndo` `isBoundary`), 6968-7010 (`REPLAY_ELIGIBLE_TYPES`)
    - packages/server/src/gameEngine.ts — `applyFreeMoveZoneCheck` and its `ballZone` before/after comparison (the closest existing "did the ball cross a region boundary as a result of this action" mechanism, and the reason the box-entry check must compare a PRE-action ball position rather than re-deriving it)
    - packages/shared/src/pitch.ts (`isInRegion`, `isPitchHex`), packages/shared/src/hex.ts (`hexDistance`, `hexNeighbors`)
  </read_first>
  <action>
Add three functions to `gameEngine.ts`.

`export function computeBoxEntryOffer(prevBallPosition: HexCoord, state: GameState): { team: 'home' | 'away'; gkId: string } | null` — D-10. Determine which penalty area (if any) now contains `state.ball.position` via `isInRegion(pos, 'homePenaltyArea')` / `'awayPenaltyArea'`. Return `null` when neither contains it, or when `prevBallPosition` was already inside that SAME area (only the first entry offers). The responding team is the area's owner: `homePenaltyArea` → `'home'`, `awayPenaltyArea` → `'away'`. Return `null` when that team's `role === 'GK'` piece is missing or `redCarded`, or when `state.gkBoxEntryUsedByTeam?.[team] === true` (D-11's own cap). Add a comment stating explicitly that this is NOT the shot-declared `GK_DIVE` mechanic and fires on any means of entry (pass, shot, move, loose ball), per D-10.

Comparing against a caller-supplied `prevBallPosition` rather than re-deriving from `eventLog` is deliberate: the same reasoning `ball.lastTouchedBy` was introduced with in Phase 37 (never derive retroactively from an event-log scan). Record that in a comment.

`export function applyBoxEntryResponse(state: GameState, accept: boolean): { ok: true; state: GameState } | { ok: false; reason: string }` — phase guard `!== 'GK_BOX_ENTRY_PROMPT'` → `WRONG_PHASE`. On `false`: restore `phase`/`activeTeam`/`movementSlot` from `gkBoxEntryResume`, clear `gkBoxEntryTeam`/`gkBoxEntryResume`, and set `gkBoxEntryUsedByTeam[team] = true`. On `true`: set `phase: 'GK_BOX_ENTRY_MOVE'` and `activeTeam` to the responding team, keeping `gkBoxEntryResume` for the post-move restore.

`export function applyBoxEntryMove(state: GameState, to: HexCoord): { ok: true; state: GameState } | { ok: false; reason: string }` — phase guard `!== 'GK_BOX_ENTRY_MOVE'`; resolve the responding team's goalkeeper; then, in this order (matching `applyGoalKickReposition`'s 37-13 ordering rationale): `hexDistance(gk.position, to) !== 1` → `MOVE_INVALID`/`OUT_OF_RANGE`; `!isPitchHex(to)` → `MOVE_INVALID`/`OFF_PITCH`; occupied → `MOVE_INVALID`/`OCCUPIED`. On success move the goalkeeper, append `GK_BOX_ENTRY_MOVE` with `gkId`/`from`/`to`, set `gkBoxEntryUsedByTeam[team] = true`, restore the interrupted phase/activeTeam/movementSlot from `gkBoxEntryResume`, and clear `gkBoxEntryTeam`/`gkBoxEntryResume`.

D-11 independence: `gkBoxEntryUsedByTeam` and `gkDiveAtFeetUsedByTeam` are two separate fields and neither function may read or write the other. Add a comment on each saying so explicitly — D-11 states a goalkeeper may use the box-entry response AND still be eligible for dive-at-feet in the same cycle.

Movement-cycle reset: add `gkBoxEntryUsedByTeam: { home: false, away: false }` beside the `gkDiveAtFeetUsedByTeam` reset Plan 39-12 added at each movement-cycle restart (not at slot advances).

Registration checklist:

- `ZONE_CHECK_EXEMPT_PHASES`: add `'GK_BOX_ENTRY_PROMPT'` and `'GK_BOX_ENTRY_MOVE'`.
- `applyUndo`'s `isBoundary`: add `(state.phase === 'GK_BOX_ENTRY_MOVE' && evt.type === 'GK_BOX_ENTRY_MOVE')` so the reposition cannot be undone back across the offer.
- `REPLAY_ELIGIBLE_TYPES`: do NOT add `GK_BOX_ENTRY_MOVE` — it carries no `ballAfter`, matching the existing `GK_KICK_MOVE` and `GOAL_KICK_MOVE` exclusions. Note the reason in a comment.
  </action>
  <verify>
  <automated>pnpm --filter @counter-attack/server test -- gameEngine.boxEntry.test.ts</automated>
  </verify>
  <acceptance_criteria> - `gameEngine.ts` exports `computeBoxEntryOffer`, `applyBoxEntryResponse` and `applyBoxEntryMove` - Neither `applyBoxEntryResponse` nor `applyBoxEntryMove` reads or writes `gkDiveAtFeetUsedByTeam` (D-11 independence — verify by grep within those function bodies) - `computeBoxEntryOffer` takes a `prevBallPosition` parameter and performs no `eventLog` scan - `ZONE_CHECK_EXEMPT_PHASES` contains both box-entry phases; `applyUndo`'s `isBoundary` references `GK_BOX_ENTRY_MOVE`; `REPLAY_ELIGIBLE_TYPES` does not contain it - `pnpm --filter @counter-attack/server test -- gameEngine.boxEntry.test.ts` passes with zero failures
  </acceptance_criteria>
  <done>The first ball entry into a penalty area each movement cycle offers the defending manager a one-hex goalkeeper reposition, capped independently of the dive-at-feet allowance.</done>
  </task>

<task type="auto">
  <name>Task 3: Implement the second-half mutual-confirm gate (D-16)</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.secondHalf.test.ts (Task 1's spec)
    - packages/server/src/gameEngine.ts — `applyHalfTimeStart` in full (its `HALF_TIME` guard and its `KICK_OFF_SETUP` transition, including the `HALF_TIME_KICKOFF_RESET` event if present)
    - packages/shared/src/types.ts (`headerConfirmed`'s shape and `secondHalfConfirmed`'s declaration from Plan 39-01)
    - packages/server/src/gameEngine.ts lines 6968-7010 (`REPLAY_ELIGIBLE_TYPES`)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md (Pitfall 4)
  </read_first>
  <action>
Add `export function applySecondHalfConfirm(state: GameState, team: 'home' | 'away'): { ok: true; state: GameState } | { ok: false; reason: string }` to `gameEngine.ts`, placed next to `applyHalfTimeStart`.

Behaviour (D-16):

- Phase guard `state.phase !== 'HALF_TIME'` → `WRONG_PHASE`.
- Read `const prior = state.secondHalfConfirmed ?? { home: false, away: false }`.
- Idempotence: when `prior[team] === true`, return `{ ok: true, state }` unchanged — no duplicate event, no transition. A manager clicking twice must be a no-op, not an error.
- Set `next = { ...prior, [team]: true }`. Append `SECOND_HALF_CONFIRM` with `team` and `bothConfirmed: next.home && next.away`.
- When only one side has confirmed, return the state still in `HALF_TIME` with `secondHalfConfirmed: next` — the waiting manager's UI reads this to show its waiting text.
- When both have confirmed, call the EXISTING `applyHalfTimeStart` on the updated state and return its result with `secondHalfConfirmed: null` cleared. Do not duplicate `applyHalfTimeStart`'s transition logic — the kick-off reset, half increment and `kickOffTeam` handling all live there and must stay in one place.

Do NOT delete or change `applyHalfTimeStart`; it remains the single owner of the actual transition. `applySecondHalfConfirm` is a gate in front of it.

Record in a comment that RESEARCH.md Pitfall 4 rules out the `LINEUP_CONFIRM` storage pattern: those flags live on the pre-match `Room` object because `GameState` does not exist yet at that point, whereas half-time is mid-match and `GameState.headerConfirmed`'s `{ home, away }` shape is the correct GameState-scoped analog that already reaches both clients through `broadcastState` with no new plumbing.

Register `'SECOND_HALF_CONFIRM'` nowhere in `REPLAY_ELIGIBLE_TYPES` (it carries no `ballAfter` and represents no board change), and add it to `applyUndo`'s `isBoundary` guarded on `state.phase === 'HALF_TIME'` so a confirm cannot be undone. Note both decisions in comments.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- gameEngine.secondHalf.test.ts && pnpm --filter @counter-attack/server test</automated>
</verify>
<acceptance_criteria> - `gameEngine.ts` exports `applySecondHalfConfirm` - `applySecondHalfConfirm` calls `applyHalfTimeStart` rather than reimplementing the transition - A repeated confirm from the same team returns an unchanged state with no additional `SECOND_HALF_CONFIRM` event - `secondHalfConfirmed` is `null` on the returned state once the half has started - `applyHalfTimeStart`'s own body is unchanged (git diff shows no modification inside it) - `applyUndo`'s `isBoundary` references `SECOND_HALF_CONFIRM`; `REPLAY_ELIGIBLE_TYPES` does not - `pnpm --filter @counter-attack/server test -- gameEngine.secondHalf.test.ts` passes with zero failures - `pnpm --filter @counter-attack/server test` (full server suite) is green
</acceptance_criteria>
<done>The second half starts only when both managers have confirmed, in either order, reusing the existing transition.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                           |
| ------------------------------- | --------------------------------------------------------------------- |
| socket handler → engine reducer | Box-entry accept/decline, a destination hex, and a half-start confirm |
| engine → broadcast state        | Confirm flags and goalkeeper position reach both players              |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                                                     |
| ---------- | ---------------------- | --------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-14-01 | Elevation of Privilege | Attacking manager repositioning the keeper    | mitigate    | Engine resolves the goalkeeper from `gkBoxEntryTeam`, never from the caller; Plan 39-15's handler adds the `controlsGKTeam`-style ownership check.                  |
| T-39-14-02 | Tampering              | Off-grid goalkeeper destination               | mitigate    | `hexDistance !== 1` → `OUT_OF_RANGE`, then `!isPitchHex` → `OFF_PITCH`, then occupancy — the same ordered guard set that fixed the 37-13 off-grid poisoning bug.    |
| T-39-14-03 | Tampering              | Repeated box-entry offers spamming a manager  | mitigate    | `gkBoxEntryUsedByTeam` is set on BOTH accept and decline, and `computeBoxEntryOffer` refuses to re-offer while it is set; cleared only at a movement-cycle restart. |
| T-39-14-04 | Elevation of Privilege | One manager starting the half alone           | mitigate    | `applySecondHalfConfirm` transitions only when both flags are true; Plan 39-15's handler enforces that a socket may only set its OWN team's flag.                   |
| T-39-14-05 | Denial of Service      | Repeated half-start confirms                  | mitigate    | Same-team re-confirm is an explicit idempotent no-op, so a click-spamming client cannot grow `eventLog` unboundedly.                                                |
| T-39-14-06 | Tampering              | Undo rewinding a confirm or a keeper response | mitigate    | Both `SECOND_HALF_CONFIRM` and `GK_BOX_ENTRY_MOVE` are registered as phase-guarded Undo boundaries.                                                                 |
| T-39-14-SC | Tampering              | npm/pip/cargo installs                        | accept      | No packages installed this phase.                                                                                                                                   |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` green
- `pnpm build` green
- `applyHalfTimeStart` body unchanged
</verification>

<success_criteria>
The first penalty-area entry of each movement cycle, by any means, offers the defending manager a one-hex goalkeeper reposition under its own independent cap; accepting, declining and moving all behave correctly and never write an off-grid coordinate; the second half starts only after both managers confirm, in either order, with same-team re-confirms treated as no-ops.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-14-SUMMARY.md` when done
</output>
