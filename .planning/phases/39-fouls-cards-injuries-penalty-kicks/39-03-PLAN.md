---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 03
type: execute
wave: 2
depends_on: ['39-01']
files_modified:
  - packages/client/src/components/GameSettingsScreen.tsx
  - packages/client/src/components/GameSettingsScreen.module.css
  - packages/client/src/components/GameSettingsScreen.test.tsx
  - packages/client/src/App.tsx
  - packages/server/src/roomStore.ts
  - packages/server/src/roomHandlers.ts
  - packages/server/src/__tests__/room.integration.test.ts
autonomous: true
requirements: [SETTINGS-01, SETTINGS-02, SETTINGS-03, FOUL-05, CARD-04, INJURY-04]

must_haves:
  truths:
    - 'Game creation offers four independent toggles — Fouls, Booking, Injury, Out-of-Bounds/Restarts — and all four default to checked (SETTINGS-01, D-14)'
    - 'Booking and Injury checkboxes are visually greyed out and non-interactive whenever Fouls is unchecked (D-13)'
    - 'The three new booleans reach GameState via ROOM_SETTINGS_CONFIRM → Room → buildInitialGameState and are visible in every broadcast snapshot'
    - 'A forged non-boolean fouls/booking/injury payload is rejected before any room mutation'
  artifacts:
    - path: 'packages/client/src/components/GameSettingsScreen.tsx'
      provides: 'Three new checkbox rows with dependency-disabled state'
      contains: 'disabled={!fouls}'
    - path: 'packages/server/src/roomStore.ts'
      provides: 'Room-level storage for the three new toggles'
      contains: 'foulsEnabled'
    - path: 'packages/server/src/roomHandlers.ts'
      provides: 'Validation, persistence and propagation of the three toggles'
      contains: 'INVALID_FOULS'
  key_links:
    - from: 'packages/server/src/roomHandlers.ts LINEUP_CONFIRM'
      to: 'buildInitialGameState'
      via: 'three trailing boolean arguments after outOfBoundsEnabled'
      pattern: 'room\\.foulsEnabled \\?\\? false'
    - from: 'packages/client/src/App.tsx'
      to: 'ClientEvents.ROOM_SETTINGS_CONFIRM'
      via: 'handleSettingsConfirm bundled payload'
      pattern: 'fouls'
---

<objective>
Deliver SETTINGS-01/02/03 (and the toggle halves of FOUL-05, CARD-04, INJURY-04) end-to-end: three new game-creation checkboxes, the Fouls-dependency grey-out, the default-ON flip for all four toggles, and the full client → Room → GameState plumbing.

Purpose: every rule branch built in waves 3-7 reads `state.foulsEnabled === true` / `state.bookingEnabled === true` / `state.injuryEnabled === true`. Landing the plumbing early and in one place means no later plan has to touch `roomHandlers.ts` or `GameSettingsScreen.tsx`.

Output: a working four-toggle settings screen and the server-side transport for its three new fields.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-PATTERNS.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; `ROOM_SETTINGS_CONFIRM` keys `fouls`/`booking`/`injury`; three extra `ROOM_SETTINGS_CONFIRMED` boolean args
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `buildInitialGameState` params `foulsEnabled`/`bookingEnabled`/`injuryEnabled`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto">
  <name>Task 1: Add the three toggles to GameSettingsScreen with the Fouls dependency grey-out and default-ON flip</name>
  <files>packages/client/src/components/GameSettingsScreen.tsx, packages/client/src/components/GameSettingsScreen.module.css</files>
  <read_first>
    - packages/client/src/components/GameSettingsScreen.tsx (full file — the `Props.onConfirm` settings-bundle type at lines 20-34, the `outOfBounds` `useState<boolean>(false)` at line 42, the "Restarts" section markup at lines 137-147, the `ALL_DRAFT_POOLS.map` disabled-row rendering at lines 153-171, and `handleConfirm` at lines 76-84)
    - packages/client/src/components/GameSettingsScreen.module.css (`.section`, `.sectionLabel`, `.poolRow`, `.poolRowDisabled`, `.comingSoon`)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Copywriting Contract rows for the new settings rows and the `(requires Fouls)` helper text; Spacing Scale `md` 16px note that the new checkboxes slot into the existing `.section` gap rhythm)
  </read_first>
  <action>
Per D-12/D-13/D-14, change `GameSettingsScreen.tsx` as follows.

Rename the existing section label from `Restarts` to `Match Rules` — UI-SPEC leaves this to the planner and explicitly flags that "Restarts" reads awkwardly once Fouls/Booking/Injury sit under it. All four checkboxes live in that one `.section`, in the order: `Fouls`, `Booking`, `Injury`, `Out-of-Bounds / Restarts`.

Add three `useState<boolean>(true)` hooks: `fouls`, `booking`, `injury` (D-14 all default ON). Flip the existing `const [outOfBounds, setOutOfBounds] = useState<boolean>(false)` at line 42 to `useState<boolean>(true)` and replace its "default off, matching the server-side safe default" comment with a note that D-14 explicitly changed this to default-ON while the SERVER default in `buildInitialGameState` deliberately stays `false`.

Each row copies the existing `outOfBounds` row markup verbatim (a `<label className={styles.poolRow}>` wrapping an `<input type="checkbox" checked={...} onChange={() => setX((v) => !v)} />` plus the label text) — D-12 requires exact reuse of that pattern.

Booking and Injury additionally take `disabled={!fouls}`, apply `className={!fouls ? styles.poolRowDisabled : styles.poolRow}`, and render `<span className={styles.comingSoon}> (requires Fouls)</span>` when `!fouls`. This is the same computed-`disabled` + conditional-class + trailing-note shape the `ALL_DRAFT_POOLS.map` block already uses for Legends/Icons; do not invent a new CSS class. Reuse `.comingSoon` for the helper text as UI-SPEC's Copywriting Contract specifies ("styled identically to `.comingSoon`").

Guard the toggle handlers so a disabled row can never flip state even if clicked programmatically: `function toggleBooking() { if (!fouls) return; setBooking((v) => !v); }` and the same for injury — mirroring `toggleDraftPool`'s `if (!SELECTABLE_DRAFT_POOLS.includes(poolId)) return;` early return.

Extend the `Props.onConfirm` settings-bundle type with `fouls: boolean; booking: boolean; injury: boolean` and pass all three from `handleConfirm`. When `fouls` is false, pass `booking: false` and `injury: false` rather than the raw local state — SETTINGS-02/SETTINGS-03 say Booking and Injury have no effect unless Fouls is enabled, and normalising at the source keeps the server from having to re-derive it.

`GameSettingsScreen.module.css` needs no new class; if the four-row section needs any spacing change, use only the declared 4px/8px/16px scale from UI-SPEC (no new values).
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- GameSettingsScreen.test.tsx && pnpm --filter @counter-attack/client typecheck</automated>
</verify>
<acceptance_criteria> - `packages/client/src/components/GameSettingsScreen.tsx` contains `useState<boolean>(true)` four times (fouls, booking, injury, outOfBounds) - The Booking and Injury `<input>` elements both carry `disabled={!fouls}` - `grep -c "requires Fouls" packages/client/src/components/GameSettingsScreen.tsx` returns 2 - The section label string is `Match Rules` and the four checkbox labels are exactly `Fouls`, `Booking`, `Injury`, `Out-of-Bounds / Restarts` - `handleConfirm` passes `booking: fouls && booking` and `injury: fouls && injury` (or equivalent normalising expression) - `pnpm --filter @counter-attack/client typecheck` exits 0 - `packages/client/src/components/GameSettingsScreen.module.css` gains no new numeric spacing value outside {4, 8, 16, 24}
</acceptance_criteria>
<done>The settings screen shows four default-checked toggles with Booking/Injury greyed out when Fouls is off.</done>
</task>

<task type="auto">
  <name>Task 2: Component tests for the four-toggle screen</name>
  <files>packages/client/src/components/GameSettingsScreen.test.tsx, packages/client/src/App.tsx</files>
  <read_first>
    - packages/client/src/components/GameSettingsScreen.test.tsx (existing render helper, store mock and `onConfirm` spy assertions)
    - packages/client/src/App.tsx lines 40-60, 120-140, 190-260, 315-330 (`handleSettingsConfirm`, the `onRoomSettingsConfirmed` listener registration/cleanup, and the `<GameSettingsScreen onConfirm={handleSettingsConfirm} .../>` render site)
    - packages/client/src/components/GameSettingsScreen.tsx (as modified in Task 1)
  </read_first>
  <action>
Extend `GameSettingsScreen.test.tsx` with assertions covering D-12/D-13/D-14 and SETTINGS-01/02/03:

1. On first render all four checkboxes (`Fouls`, `Booking`, `Injury`, `Out-of-Bounds / Restarts`) are checked.
2. Unchecking `Fouls` makes the `Booking` and `Injury` inputs `disabled` and renders `(requires Fouls)` twice.
3. Clicking a disabled `Booking` input does not change its checked state.
4. Re-checking `Fouls` re-enables both and removes the helper text.
5. Confirming with all four checked calls `onConfirm` with `fouls: true, booking: true, injury: true, outOfBounds: true`.
6. Confirming with `Fouls` unchecked calls `onConfirm` with `fouls: false, booking: false, injury: false` regardless of the Booking/Injury local state before Fouls was turned off.

In `App.tsx`, extend `handleSettingsConfirm`'s parameter type and the object it forwards to `socket.emit(ClientEvents.ROOM_SETTINGS_CONFIRM, settings)` with the three new booleans (the object is already forwarded wholesale, so this is a type-level change plus any local settings-echo state), and extend the `onRoomSettingsConfirmed` handler's parameter list with the three trailing booleans added to `ROOM_SETTINGS_CONFIRMED` in Plan 39-01. Store them alongside the existing `outOfBounds` echo state if that state exists; if `App.tsx` currently discards `outOfBounds` on the echo, discard the three new ones the same way rather than introducing new state.

Do not change the socket listener registration/cleanup structure — every `socket.on` in `App.tsx` already has a matching `socket.off` in the cleanup return, and that pairing must be preserved.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- GameSettingsScreen.test.tsx</automated>
</verify>
<acceptance_criteria> - `pnpm --filter @counter-attack/client test -- GameSettingsScreen.test.tsx` passes with at least 6 new assertions matching the numbered list above - The test asserting a Fouls-off confirm payload checks all three of `fouls: false`, `booking: false`, `injury: false` - `packages/client/src/App.tsx` typechecks against the extended `ROOM_SETTINGS_CONFIRMED` signature - Every `socket.on(...)` added or changed in `App.tsx` still has a matching `socket.off(...)` in the same effect's cleanup - `pnpm --filter @counter-attack/client test` (full client suite) is green
</acceptance_criteria>
<done>The four-toggle behaviour is regression-locked by component tests and the client emits the extended payload.</done>
</task>

<task type="auto">
  <name>Task 3: Server-side validation, Room storage and GameState propagation for the three toggles</name>
  <files>packages/server/src/roomStore.ts, packages/server/src/roomHandlers.ts, packages/server/src/__tests__/room.integration.test.ts</files>
  <read_first>
    - packages/server/src/roomStore.ts (the `Room` type, specifically the `outOfBoundsEnabled` field added in Phase 37 and the surrounding optional-field convention)
    - packages/server/src/roomHandlers.ts lines 228-245 (the late-joiner `ROOM_SETTINGS_CONFIRMED` re-emit), lines 404-537 (the whole `ROOM_SETTINGS_CONFIRM` handler incl. the `typeof outOfBounds !== 'boolean'` guard at 451-456, the `room.outOfBoundsEnabled = outOfBounds` store at 516, and the `io.to(roomCode).emit(ServerEvents.ROOM_SETTINGS_CONFIRMED, ...)` at 522-528), and lines 855-880 (the `buildInitialGameState` call inside `LINEUP_CONFIRM`)
    - packages/server/src/gameEngine.ts lines 329-391 (`buildInitialGameState` as extended by Plan 39-01)
    - packages/server/src/__tests__/room.integration.test.ts (existing `ROOM_SETTINGS_CONFIRM` coverage and its socket-client harness)
  </read_first>
  <action>
Mirror the existing `outOfBoundsEnabled` two-step plumbing exactly, three more times.

`roomStore.ts`: add `foulsEnabled?: boolean`, `bookingEnabled?: boolean`, `injuryEnabled?: boolean` to the `Room` type, directly beside `outOfBoundsEnabled`, with a comment naming SETTINGS-01/FOUL-05/CARD-04/INJURY-04.

`roomHandlers.ts` `ROOM_SETTINGS_CONFIRM`:

- Destructure `fouls`, `booking`, `injury` from the payload alongside `outOfBounds`, and widen the inline payload type accordingly.
- Immediately after the existing `typeof outOfBounds !== 'boolean'` guard, add three ASVS V5 allow-list guards emitting `INVALID_FOULS`, `INVALID_BOOKING`, `INVALID_INJURY` respectively and returning before any room mutation. Keep them as three separate guards with distinct error codes (not one combined check) so the client error surface names the offending field, matching how `INVALID_SPEED`/`INVALID_TEAM_TYPE`/`INVALID_OUT_OF_BOUNDS` are already separated.
- Store `room.foulsEnabled = fouls; room.bookingEnabled = fouls && booking; room.injuryEnabled = fouls && injury;` in the "Store settings and lock" block. The `fouls &&` normalisation is SETTINGS-02/SETTINGS-03 enforced server-side — the client already normalises, but the server must not trust it.
- Append `room.foulsEnabled`, `room.bookingEnabled`, `room.injuryEnabled` as the three trailing args of the `ROOM_SETTINGS_CONFIRMED` emit.

`roomHandlers.ts` late-joiner re-emit (~line 234): append `joinedRoom.foulsEnabled ?? false`, `joinedRoom.bookingEnabled ?? false`, `joinedRoom.injuryEnabled ?? false` as the three trailing args so a late joiner receives the same settings as the host.

`roomHandlers.ts` `LINEUP_CONFIRM` (~line 871): append `room.foulsEnabled ?? false`, `room.bookingEnabled ?? false`, `room.injuryEnabled ?? false` after the existing `room.outOfBoundsEnabled ?? false` argument in the `buildInitialGameState(...)` call, matching the parameter order Plan 39-01 declared.

Extend `packages/server/src/__tests__/room.integration.test.ts` with:

- A test that a `ROOM_SETTINGS_CONFIRM` carrying `fouls: 'yes'` (a non-boolean) emits `GAME_ERROR` with `INVALID_FOULS` and leaves `room.settingsConfirmed` false.
- The same for `booking` → `INVALID_BOOKING` and `injury` → `INVALID_INJURY`.
- A test that confirming with `fouls: false, booking: true, injury: true` results in `room.bookingEnabled === false` and `room.injuryEnabled === false` (server-side SETTINGS-02/03 normalisation).
- A test that a happy-path confirm with all four true reaches `ROOM_SETTINGS_CONFIRMED` with the three trailing booleans set to `true`.
  </action>
  <verify>
  <automated>pnpm --filter @counter-attack/server test -- room.integration.test.ts</automated>
  </verify>
  <acceptance_criteria> - `grep -c "INVALID_FOULS\|INVALID_BOOKING\|INVALID_INJURY" packages/server/src/roomHandlers.ts` returns at least 3 - `packages/server/src/roomStore.ts` declares `foulsEnabled`, `bookingEnabled`, `injuryEnabled` on the `Room` type - `roomHandlers.ts` assigns `room.bookingEnabled = fouls && booking` and `room.injuryEnabled = fouls && injury` (server-side normalisation, not raw pass-through) - The `buildInitialGameState(...)` call inside `LINEUP_CONFIRM` passes exactly three new trailing arguments after `room.outOfBoundsEnabled ?? false` - Both `ROOM_SETTINGS_CONFIRMED` emit sites (the confirm handler and the late-joiner re-emit) pass the same three trailing booleans - `pnpm --filter @counter-attack/server test -- room.integration.test.ts` passes with at least 5 new assertions - `pnpm test` (full monorepo suite) is green
  </acceptance_criteria>
  <done>The three toggles are validated, normalised, stored on `Room`, echoed to both players, and baked into `GameState` at match start.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                          |
| ------------------------------------------- | -------------------------------------------------------------------- |
| host client → `ROOM_SETTINGS_CONFIRM`       | Untrusted settings payload; host-only, once-only, pre-match          |
| `Room` (pre-match) → `GameState` (in-match) | Toggles cross the lifecycle boundary exactly once, at LINEUP_CONFIRM |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                      | Disposition | Mitigation Plan                                                                                                                                                             |
| ---------- | ---------------------- | ---------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-03-01 | Tampering              | `ROOM_SETTINGS_CONFIRM` fouls/booking/injury   | mitigate    | Three separate `typeof x !== 'boolean'` guards emitting `INVALID_FOULS`/`INVALID_BOOKING`/`INVALID_INJURY` before any `room.*` mutation, mirroring `INVALID_OUT_OF_BOUNDS`. |
| T-39-03-02 | Elevation of Privilege | Non-host player confirming settings            | mitigate    | Existing `socket.data.playerSlot !== 1` host guard and `room.settingsConfirmed` lock are upstream of the new fields; do not reorder the new guards above them.              |
| T-39-03-03 | Tampering              | Client bypassing the SETTINGS-02/03 dependency | mitigate    | Server recomputes `bookingEnabled = fouls && booking` and `injuryEnabled = fouls && injury`; a modified client sending `fouls:false, booking:true` still gets Booking off.  |
| T-39-03-04 | Denial of Service      | Double-submitted `ROOM_SETTINGS_CONFIRM`       | mitigate    | Existing `room.settingsConfirmed` early-reject (`SETTINGS_ALREADY_CONFIRMED`) plus the client's `hasConfirmed` guard; unchanged by this plan.                               |
| T-39-03-SC | Tampering              | npm/pip/cargo installs                         | accept      | No packages installed this phase.                                                                                                                                           |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test` green
- `pnpm --filter @counter-attack/server test` green
- `pnpm build` green
- A live snapshot after LINEUP_CONFIRM contains `foulsEnabled`, `bookingEnabled`, `injuryEnabled`
</verification>

<success_criteria>
Four independent toggles exist at game creation, all default checked; Booking and Injury are visibly disabled and functionally inert while Fouls is off; forged non-boolean payloads are rejected with field-specific error codes; the three booleans are stored on `Room`, echoed to both players (including late joiners), and present on every broadcast `GameState`.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-03-SUMMARY.md` when done
</output>
