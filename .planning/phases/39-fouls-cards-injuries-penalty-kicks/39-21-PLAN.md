---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 21
type: execute
wave: 12
depends_on: ['39-20']
files_modified:
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/store/useGameStore.test.ts
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/HexGrid.test.tsx
  - packages/client/src/components/GkDiveAtFeetPromptPanel.tsx
  - packages/client/src/components/GkDiveAtFeetPromptPanel.test.tsx
  - packages/client/src/components/BallLocationRing.tsx
autonomous: true
gap_closure: true
requirements: [GKDIVE-02, GKDIVE-04]

must_haves:
  truths:
    - 'After the goalkeeper manager clicks Dive, their goalkeeper becomes selectable and the legal dive-destination hexes are highlighted on the board'
    - 'Clicking a highlighted hex emits GAME_GK_DIVE_AT_FEET_TARGET with that hex and nothing else'
    - 'The attacking manager sees a waiting message during the destination step and can select nothing'
    - 'The highlighted set is produced by the same shared computeGkDiveAtFeetTargetHexes the server validates against — no client-local re-derivation'
  artifacts:
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'emitGkDiveAtFeetTarget action and the GK_DIVE_AT_FEET_TARGET selectPiece branch'
      contains: 'emitGkDiveAtFeetTarget'
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: 'Goalkeeper selectability and the destination-hex click branch for the dive target step'
      contains: 'canSelectGkDiveAtFeetTarget'
    - path: 'packages/client/src/components/GkDiveAtFeetPromptPanel.tsx'
      provides: 'Destination-step copy for both the deciding and the waiting manager'
      contains: 'GK_DIVE_AT_FEET_TARGET'
  key_links:
    - from: 'HexGrid hex onClick'
      to: 'useGameStore.emitGkDiveAtFeetTarget'
      via: 'dedicated GAME_GK_DIVE_AT_FEET_TARGET emission ahead of the generic isValidMove branch'
      pattern: 'emitGkDiveAtFeetTarget\\('
    - from: 'useGameStore.selectPiece'
      to: 'computeGkDiveAtFeetTargetHexes'
      via: 'shared helper imported from @counter-attack/shared to build validMoveHexes'
      pattern: 'computeGkDiveAtFeetTargetHexes'
---

<objective>
Close the client half of UAT gap 3: give the goalkeeper's manager an actual board interaction for choosing the dive destination that Plan 39-20 made the server require.

Purpose: Plan 39-20 introduced `GK_DIVE_AT_FEET_TARGET` and the `GAME_GK_DIVE_AT_FEET_TARGET` socket event, but nothing in the client can select the goalkeeper, highlight the legal hexes, or emit that event — so after 39-20 alone the dive prompt would accept and then dead-end. This plan supplies the selection, the highlight and the emission.

Output: a store action and selection branch, a HexGrid click branch, panel copy for the new step, and the ball marker for the new phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UAT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-20-SUMMARY.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-16-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols introduced by THIS plan:

- **Client store** (`packages/client/src/store/useGameStore.ts`): `emitGkDiveAtFeetTarget(to: HexCoord) => void` on the store interface and its implementation
- **Client** (`packages/client/src/components/HexGrid.tsx`): local `canSelectGkDiveAtFeetTarget` predicate and the `GK_DIVE_AT_FEET_TARGET` hex-click branch

<tasks>

<task type="auto">
  <name>Task 1: Add the emitGkDiveAtFeetTarget action and the GK_DIVE_AT_FEET_TARGET selection branch</name>
  <files>packages/client/src/store/useGameStore.ts, packages/client/src/store/useGameStore.test.ts</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts lines 190-215 — the store interface declarations for `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove` and `emitPenaltyKickTaker`, including their doc-comment style
    - packages/client/src/store/useGameStore.ts lines 1770-1830 — the implementations of `emitCornerKickTaker`, `emitPenaltyKickTaker` and `emitGkBoxEntryMove` (the `socket.emit(ClientEvents.X, payload)` + optimistic-clear shape to copy)
    - packages/client/src/store/useGameStore.ts lines 1289-1307 — the `GK_BOX_ENTRY_MOVE` `selectPiece` branch (the closest analogue: goalkeeper-only selectability keyed on an explicit team field, then a valid-hex computation)
    - packages/client/src/store/useGameStore.ts lines 1213-1226 — the FOUL_CHOICE / GK_DIVE_AT_FEET_PROMPT / GK_BOX_ENTRY_PROMPT no-op branch. `GK_DIVE_AT_FEET_TARGET` must NOT be added to it — the target step is the one dive phase that DOES have board interaction
    - packages/client/src/store/useGameStore.ts lines 1321-1470 — `setGameState`'s sticky-selection block, to determine whether the new phase needs a sticky arm (it does not: the target step is a single click that ends the phase, exactly like `GK_BOX_ENTRY_MOVE`, which has no sticky arm either)
    - packages/shared/src/shotValidator.ts — `computeGkDiveAtFeetTargetHexes` as added by Plan 39-20
    - packages/shared/src/events.ts — `ClientEvents.GAME_GK_DIVE_AT_FEET_TARGET` as added by Plan 39-20
  </read_first>
  <action>
In `packages/client/src/store/useGameStore.ts`:

1. Declare `emitGkDiveAtFeetTarget: (to: HexCoord) => void;` on the store interface immediately after `emitGkDiveAtFeet`, documenting it as GKDIVE-02/39-UAT gap 3's destination submission.

2. Implement it beside `emitGkBoxEntryMove`, emitting `ClientEvents.GAME_GK_DIVE_AT_FEET_TARGET` with the hex as the sole argument and clearing `selectedPieceId`/`validMoveHexes` optimistically the same way `emitGkBoxEntryMove` does. Do not clear `gameError` differently from the sibling actions.

3. Add a `GK_DIVE_AT_FEET_TARGET` branch to `selectPiece`, placed immediately after the `GK_BOX_ENTRY_MOVE` branch. It derives `myTeam` via `deriveMyTeam(playerSlot)` with the D-04/Pitfall 4 explicit-null no-op, then rejects (clearing selection) unless `myTeam === gameState.gkDiveAtFeetTeam` AND `piece.id === gameState.gkDiveAtFeetGkId`. Note the guard is on the goalkeeper's ID, not merely `role === 'GK'` — the state already names the exact diving keeper, so use it. On acceptance it sets `selectedPieceId` to that id and `validMoveHexes` to `computeGkDiveAtFeetTargetHexes(gameState)` imported from `@counter-attack/shared`. Do NOT re-derive the neighbour/range filter locally — importing the shared helper is what guarantees the highlighted set and the server's authority check can never drift (Plan 39-20 put it in `shared` for exactly this reason). Set `tackleRiskHexes: []` alongside, matching the sibling branches.

Add store tests in `packages/client/src/store/useGameStore.test.ts` covering the acceptance criteria below, reusing the file's existing socket-mock and state-seeding helpers.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test useGameStore</automated>
</verify>
<acceptance_criteria> - `grep -c "emitGkDiveAtFeetTarget" packages/client/src/store/useGameStore.ts` returns at least 3 (interface declaration, implementation, and the ClientEvents emission line) - `grep -n "computeGkDiveAtFeetTargetHexes" packages/client/src/store/useGameStore.ts` returns at least 1 import-based usage - `grep -n "GK_DIVE_AT_FEET_TARGET" packages/client/src/store/useGameStore.ts` shows the new `selectPiece` branch and shows the phase is ABSENT from the FOUL_CHOICE/prompt no-op branch - Store test: with `phase: 'GK_DIVE_AT_FEET_TARGET'`, `gkDiveAtFeetTeam: 'away'`, `gkDiveAtFeetGkId` set and the reader's slot resolving to `away`, `selectPiece(gkId)` sets `selectedPieceId === gkId` and a non-empty `validMoveHexes` - Store test: every hex in the resulting `validMoveHexes` is at `hexDistance === 1` from the carrier's position - Store test: `selectPiece(someOutfieldPieceId)` in that phase leaves `selectedPieceId === null` and `validMoveHexes` empty - Store test: when the reader's team is the ATTACKING team, `selectPiece(gkId)` leaves `selectedPieceId === null` - Store test: `emitGkDiveAtFeetTarget({ q: 5, r: 5 })` calls `socket.emit` exactly once with `ClientEvents.GAME_GK_DIVE_AT_FEET_TARGET` and that hex, and clears `selectedPieceId` - `pnpm --filter @counter-attack/client test useGameStore` passes
</acceptance_criteria>
<done>The goalkeeper is selectable during the target step and the legal hexes come from the same function the server enforces.</done>
</task>

<task type="auto">
  <name>Task 2: Make the goalkeeper clickable and route hex clicks to the dive-target event in HexGrid</name>
  <files>packages/client/src/components/HexGrid.tsx, packages/client/src/components/HexGrid.test.tsx</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx lines 630-720 — the `onClick` phase-branch cascade, especially the `GK_BOX_ENTRY_MOVE` branch (`emitGkBoxEntryMove(hex)`) which must be matched, and note that these dedicated branches deliberately precede the generic `isValidMove && selectedPieceId` branch that would otherwise emit `GAME_MOVE`
    - packages/client/src/components/HexGrid.tsx lines 885-955 — the `canSelectPenaltyKickSetup` / `canSelectPenaltyKickTaker` / `canSelectGkBoxEntryMove` / `canSelectCornerKickGk` predicate block and its comment conventions
    - packages/client/src/components/HexGrid.tsx lines 1080-1100 — the `canSelect` disjunction that gates piece clickability
    - packages/client/src/components/HexGrid.tsx lines 1170-1200 — the nested selection-state ternary chain that assigns a piece's ring/highlight treatment
    - packages/client/src/components/HexGrid.tsx — the store-selector block at the top of the component, to add `gkDiveAtFeetTeam` / `gkDiveAtFeetGkId` selectors and the `emitGkDiveAtFeetTarget` action selector in the same style as `gkBoxEntryTeam` / `emitGkBoxEntryMove`
    - packages/client/src/components/HexGrid.test.tsx — the existing rendering harness and the pattern used by the `GK_BOX_ENTRY_MOVE` cases
  </read_first>
  <action>
In `packages/client/src/components/HexGrid.tsx`:

1. Add store selectors for `gameState.gkDiveAtFeetTeam`, `gameState.gkDiveAtFeetGkId` and the `emitGkDiveAtFeetTarget` action, mirroring the existing `gkBoxEntryTeam` / `emitGkBoxEntryMove` selector lines.

2. Add a `canSelectGkDiveAtFeetTarget` predicate next to `canSelectGkBoxEntryMove`: true when `phase === 'GK_DIVE_AT_FEET_TARGET'`, `myTeam !== null`, `myTeam === gkDiveAtFeetTeam`, and `piece.id === gkDiveAtFeetGkId`. Mirror `useGameStore.selectPiece`'s guard exactly (ID equality, not `role === 'GK'`), so a piece can never look clickable but be rejected by the store. There is no `isActivePlayer` term — the responding team is named explicitly by `gkDiveAtFeetTeam`, the same rationale recorded on `canSelectGkBoxEntryMove`.

3. Include `canSelectGkDiveAtFeetTarget` in the `canSelect` disjunction and in the selection-state ternary chain, in the same position relative to `canSelectGkBoxEntryMove` in both.

4. Add an `onClick` branch for `phase === 'GK_DIVE_AT_FEET_TARGET' && isValidMove && selectedPieceId`, emitting `emitGkDiveAtFeetTarget(hex)`. Place it directly beside the `GK_BOX_ENTRY_MOVE` branch and therefore BEFORE the generic `isValidMove && selectedPieceId` branch — otherwise the click would emit `GAME_MOVE` and be rejected with a wrong-phase error. Carry a comment saying so, matching the box-entry branch's own comment.

Add `HexGrid.test.tsx` cases for the acceptance criteria, reusing the existing render harness.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test HexGrid</automated>
</verify>
<acceptance_criteria> - `grep -c "canSelectGkDiveAtFeetTarget" packages/client/src/components/HexGrid.tsx` returns at least 3 (definition, `canSelect` disjunction, selection-state ternary) - `grep -n "emitGkDiveAtFeetTarget(hex)" packages/client/src/components/HexGrid.tsx` returns exactly 1 line, and its `onClick` branch appears at a lower line number than the generic `isValidMove && selectedPieceId` branch - HexGrid test: in `GK_DIVE_AT_FEET_TARGET` with the reader on the goalkeeper's team, the goalkeeper piece renders with a click handler and an outfield teammate does not - HexGrid test: clicking a hex from `validMoveHexes` while the goalkeeper is selected calls `emitGkDiveAtFeetTarget` once with that hex and does NOT call `emitMove` - HexGrid test: in the same phase with the reader on the ATTACKING team, the goalkeeper piece has no click handler - `pnpm --filter @counter-attack/client test HexGrid` passes
</acceptance_criteria>
<done>The board interaction for choosing a dive destination exists and cannot be confused with a normal move.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the dive panel copy to the destination step and mark the ball during it</name>
  <files>packages/client/src/components/GkDiveAtFeetPromptPanel.tsx, packages/client/src/components/GkDiveAtFeetPromptPanel.test.tsx, packages/client/src/components/BallLocationRing.tsx</files>
  <read_first>
    - packages/client/src/components/GkDiveAtFeetPromptPanel.tsx (full file, 80 lines) — the `phase !== 'GK_DIVE_AT_FEET_PROMPT'` guard, the waiting-state branch keyed on `decidingTeam`, the `distanceQualifier` derivation, and the two `ctaButton` elements
    - packages/client/src/components/GkDiveAtFeetPromptPanel.test.tsx (full file) — the existing store mock and assertions on the Dive/Decline buttons
    - packages/client/src/components/GkBoxEntryPromptPanel.tsx — the sibling panel that already spans BOTH a prompt phase and a follow-on move phase (`GK_BOX_ENTRY_PROMPT` + `GK_BOX_ENTRY_MOVE`); copy its two-phase structure and its "choose a hex" copy conventions rather than inventing a new shape
    - packages/client/src/components/BallLocationRing.tsx lines 30-72 — the `BALL_MARKER_PHASES` `Set&lt;GamePhase&gt;` and its Phase 39 block comment
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md — the GK dive-at-feet panel section, for the two-line title+detail convention (Phase 35 panel family: no container border, helper line 1 + helper line 2)
  </read_first>
  <action>
1. `GkDiveAtFeetPromptPanel.tsx` — widen the top guard to `phase !== 'GK_DIVE_AT_FEET_PROMPT' && phase !== 'GK_DIVE_AT_FEET_TARGET'` (still returning null when `myTeamOrNull` is null). Keep the existing prompt-phase rendering byte-for-byte. Add a `GK_DIVE_AT_FEET_TARGET` arm that:
   - For the manager whose team is NOT `gkDiveAtFeetTeam`: renders the existing waiting-panel shape with `helperLine1` reading "Dive at Feet" and `helperLine2` saying the other side is choosing where their keeper dives.
   - For the deciding manager: renders `helperLine1` "Dive at Feet" and a `helperLine2` instructing them to click a highlighted hex next to the carrier to send their keeper there. Preserve the existing `distanceQualifier` (-1 dice penalty at distance 3) in this arm too, since the penalty band is still keyed on the recorded GK-to-carrier distance (Plan 39-20 kept that basis deliberately). Render NO buttons in this arm — the destination is chosen on the board, matching `GkBoxEntryPromptPanel`'s move-phase arm.
   - Keep the `restartErrorMessage(gameError)` error line in both arms.

2. `BallLocationRing.tsx` — add `'GK_DIVE_AT_FEET_TARGET'` to `BALL_MARKER_PHASES`, immediately after `'GK_DIVE_AT_FEET_PROMPT'`, extending the existing Phase 39 block comment to cover it (the ball is stationary with the carrier while the keeper's destination is chosen).

3. Add panel tests for both arms.
   </action>
   <verify>
   <automated>pnpm --filter @counter-attack/client test GkDiveAtFeetPromptPanel &amp;&amp; pnpm --filter @counter-attack/client test BallLocationRing</automated>
   </verify>
   <acceptance_criteria> - `grep -c "GK_DIVE_AT_FEET_TARGET" packages/client/src/components/GkDiveAtFeetPromptPanel.tsx` returns at least 2 (guard + arm) - `grep -n "GK_DIVE_AT_FEET_TARGET" packages/client/src/components/BallLocationRing.tsx` returns 1 line inside `BALL_MARKER_PHASES` - Panel test: rendering with `phase: 'GK_DIVE_AT_FEET_TARGET'` as the goalkeeper's manager shows the hex-selection instruction and renders NO `Dive` and NO `Decline` button - Panel test: rendering the same phase as the attacking manager shows a waiting message and no buttons - Panel test: the existing `GK_DIVE_AT_FEET_PROMPT` cases (Dive/Decline buttons present, distance qualifier at distance 3) still pass unchanged - `pnpm --filter @counter-attack/client test` passes with zero failures - `pnpm lint` and `pnpm stylelint` pass
   </acceptance_criteria>
   <done>Both managers see correct copy during the destination step and the ball stays marked while it is chosen.</done>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                                                  |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| client UI → `GAME_GK_DIVE_AT_FEET_TARGET` emission | The client decides which hex to send; the server re-validates it. The client is a convenience layer here, never an authority |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                           |
| ---------- | ---------------------- | --------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-21-01 | Elevation of Privilege | Client-side selectability guards                                | accept      | `canSelectGkDiveAtFeetTarget` and the `selectPiece` guard are UX affordances only; Plan 39-20's handler team guard and `applyGkDiveAtFeetTarget`'s membership check are the enforcement points and are unaffected by any client tampering |
| T-39-21-02 | Tampering              | Divergence between the highlighted set and the server-legal set | mitigate    | Both sides call the same `computeGkDiveAtFeetTargetHexes` exported from `@counter-attack/shared`; no client-local re-derivation is permitted, and an acceptance criterion greps for the import                                            |
| T-39-21-03 | Denial of Service      | Hex click routed to the generic `GAME_MOVE` branch              | mitigate    | The dedicated `onClick` branch is ordered before the generic `isValidMove` branch, with a grep-checkable line-order acceptance criterion                                                                                                  |
| T-39-21-SC | Tampering              | npm/pnpm installs                                               | accept      | This plan adds no dependency and runs no package-manager install; no new supply-chain surface                                                                                                                                             |

</threat_model>

<verification>
- `pnpm typecheck` passes across all three packages
- `pnpm lint` passes
- `pnpm stylelint` passes
- `pnpm --filter @counter-attack/client test` passes with zero failures
- Manual trace (documented in the SUMMARY, not a test): prompt → Dive → goalkeeper selectable → highlighted neighbours of the carrier → click → `GAME_GK_DIVE_AT_FEET_TARGET` emitted with that hex
</verification>

<success_criteria>

- 39-UAT gap 3 (client half) closed: the goalkeeper's manager picks the dive destination on the board and the emission reaches the server event added by Plan 39-20
- The highlighted set is provably the shared helper's output, not a client re-derivation
- No existing dive-prompt behaviour regresses
  </success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-21-SUMMARY.md` when done. Record whether `setGameState`'s sticky-selection block needed an arm for the new phase (expected: no, matching `GK_BOX_ENTRY_MOVE`) and the exact copy used in both panel arms.
</output>
