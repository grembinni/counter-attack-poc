---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 09
type: execute
wave: 3
depends_on: ['39-01', '39-05']
files_modified:
  - packages/client/src/components/FoulChoicePanel.tsx
  - packages/client/src/components/FoulChoicePanel.module.css
  - packages/client/src/components/FoulChoicePanel.test.tsx
  - packages/client/src/components/GkDiveAtFeetPromptPanel.tsx
  - packages/client/src/components/GkDiveAtFeetPromptPanel.module.css
  - packages/client/src/components/GkDiveAtFeetPromptPanel.test.tsx
  - packages/client/src/components/GkBoxEntryPromptPanel.tsx
  - packages/client/src/components/GkBoxEntryPromptPanel.module.css
  - packages/client/src/components/GkBoxEntryPromptPanel.test.tsx
autonomous: true
requirements: [FOUL-03, GKDIVE-02, GKDIVE-03]

must_haves:
  truths:
    - 'After a foul resolves, the fouled team manager sees a two-button panel offering Continue Play or the restart, and the other manager sees a waiting message (FOUL-03, D-01)'
    - 'When the foul came from a GK dive at feet, the restart button reads Take the Penalty instead of Take the Free Kick (GKDIVE-03)'
    - 'When the carrier ends a move within dive range, the defending manager sees a Dive / Decline prompt (GKDIVE-02, D-07)'
    - 'When the ball first enters the penalty box in a movement cycle, the defending manager sees a Reposition / Decline prompt (D-10)'
  artifacts:
    - path: 'packages/client/src/components/FoulChoicePanel.tsx'
      provides: 'FOUL-03 two-button continue/restart panel'
      contains: 'Continue Play'
    - path: 'packages/client/src/components/GkDiveAtFeetPromptPanel.tsx'
      provides: 'GKDIVE-02 dive interrupt prompt'
      contains: 'Dive at Feet?'
    - path: 'packages/client/src/components/GkBoxEntryPromptPanel.tsx'
      provides: 'D-10 box-entry goalkeeper reposition prompt'
      contains: 'Goalkeeper Reposition?'
  key_links:
    - from: 'packages/client/src/components/FoulChoicePanel.tsx'
      to: 'useGameStore.emitFoulChoice'
      via: 'the two co-equal CTA buttons'
      pattern: 'emitFoulChoice'
---

<objective>
Build the three near-identical two-button decision panels D-01, D-07 and D-10 require, all following the Phase 35 panel-family convention (no container border, two-line title + detail, co-equal un-coloured buttons).

Purpose: FOUL-03's continue-or-restart choice is a genuinely new UI pattern — offside currently auto-triggers `FREE_KICK_SETUP` with no manager choice at all, so there is nothing to reuse. The dive-at-feet and box-entry prompts are structurally the same panel with different copy, so building all three together keeps them literally consistent.

Output: three panels, three CSS modules, three component tests. Phase dispatch into `GameBoard.tsx` is Plan 39-16's scope.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-PATTERNS.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-05-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto">
  <name>Task 1: FoulChoicePanel — the FOUL-03 continue-or-restart decision</name>
  <files>packages/client/src/components/FoulChoicePanel.tsx, packages/client/src/components/FoulChoicePanel.module.css</files>
  <read_first>
    - packages/client/src/components/FreeKickSetupPanel.tsx (guard-block shape, `useMyTeam` usage, `restartErrorMessage(gameError)` handling)
    - packages/client/src/components/FreeKickSetupPanel.module.css (`.panel` with no `border` property — the Phase 35 convention D-01 cites)
    - packages/client/src/components/ActionPanel.module.css (`.helperBlock`, `.helperLine1`, `.helperLine2` — the two-line title+detail convention UI-SPEC names as this panel's structural template, and `.ctaButton`'s default un-coloured styling)
    - packages/client/src/store/useGameStore.ts (the `emitFoulChoice` action added in Plan 39-05, and the `foulSource`/`foulVictimId`/`foulDefenderId`/`attackingTeam` selectors)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Copywriting Contract rows for `FoulChoicePanel`; the co-equal-buttons rule in Visual hierarchy)
  </read_first>
  <action>
Create `FoulChoicePanel.tsx`. Return `null` unless `phase === 'FOUL_CHOICE'` and `useMyTeam()` is non-null.

The deciding manager is the FOULED side. After a foul the engine sets `attackingTeam` to the fouled team (Plan 39-10), so the deciding team is `attackingTeam`; read it via a dedicated selector and compare against `useMyTeam()`.

Non-deciding manager: render heading plus `{Team} is deciding whether to continue play…` where `{Team}` is `Attacking` or `Defending` relative to the reader, matching the locked Phase 35/37/38 waiting-state convention.

Deciding manager:

- Heading (`.helperLine1`, Heading role 12px/700): the literal `Foul!`
- Detail (`.helperLine2`, Label role 11px/400): `` `${victimName}'s side may continue play or take the restart.` `` where `victimName` resolves `foulVictimId` through the `pieces` array (fall back to the raw id only if the piece is missing, never render `undefined`).
- Two co-equal `.ctaButton` buttons, neither colour-promoted (no `ctaButtonReady`/`ctaButtonPending` class — D-01 and UI-SPEC's Visual hierarchy both require co-equal styling, mirroring the existing Kick / Standard-Pass precedent):
  - `Continue Play` → `emitFoulChoice('continue')`
  - `Take the Free Kick` → `emitFoulChoice('restart')`, EXCEPT when `foulSource === 'GK_DIVE_AT_FEET'`, in which case the same button reads `Take the Penalty` (GKDIVE-03 — same panel, conditional label, no separate component).
- `gameError` renders through `restartErrorMessage(...)` into `.errorText`.
- No confirmation modal. The choice is irreversible but is a single explicit click with clear labelling — UI-SPEC's Destructive-confirmation row rules out a second dialog here.

Create `FoulChoicePanel.module.css` copying `FreeKickSetupPanel.module.css`'s `.panel` (background, border-radius, padding, NO `border`), plus `.helperLine1` (12px/700), `.helperLine2` (11px/400/1.5), `.ctaButton` (default un-coloured, `--team-accent` hover only), `.errorText` (`var(--color-danger)`, 11px). Spacing from the declared scale only: `gap: 4px`, `padding: 4px 8px`, 8px between the detail line and the button row. Do NOT copy `FreeKickSetupPanel`'s legacy 3px/6px micro-scale — UI-SPEC explicitly forbids propagating it.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client typecheck && pnpm stylelint</automated>
</verify>
<acceptance_criteria> - `FoulChoicePanel.tsx` exports `FoulChoicePanel` and returns `null` for any phase other than `FOUL_CHOICE` - The file contains the exact strings `Foul!`, `Continue Play`, `Take the Free Kick` and `Take the Penalty` - The restart button label is selected by `foulSource === 'GK_DIVE_AT_FEET'` - Neither CTA button applies a ready/pending colour class - `FoulChoicePanel.module.css`'s `.panel` rule contains no `border` declaration - Every spacing value in `FoulChoicePanel.module.css` is one of 4, 8, 16; every font-size is 11 or 12 - `pnpm stylelint` exits 0
</acceptance_criteria>
<done>The fouled manager can choose to continue play or take the correct restart for the foul's source.</done>
</task>

<task type="auto">
  <name>Task 2: GkDiveAtFeetPromptPanel and GkBoxEntryPromptPanel</name>
  <files>packages/client/src/components/GkDiveAtFeetPromptPanel.tsx, packages/client/src/components/GkDiveAtFeetPromptPanel.module.css, packages/client/src/components/GkBoxEntryPromptPanel.tsx, packages/client/src/components/GkBoxEntryPromptPanel.module.css</files>
  <read_first>
    - packages/client/src/components/FoulChoicePanel.tsx and FoulChoicePanel.module.css (as written in Task 1 — these two panels are structurally identical to it)
    - packages/client/src/store/useGameStore.ts (`emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove` and the `gkDiveAtFeetTeam`/`gkDiveAtFeetCarrierId`/`gkDiveAtFeetDistance`/`gkBoxEntryTeam` selectors)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Copywriting Contract rows for both prompts, including their waiting-state strings)
  </read_first>
  <action>
Create both panels as structural copies of `FoulChoicePanel`, differing only in phase guard, copy and emitter. Each gets its own `.module.css` copying `FoulChoicePanel.module.css` verbatim — do not share one CSS module across components, matching the one-module-per-component convention every existing panel follows.

`GkDiveAtFeetPromptPanel.tsx` (GKDIVE-02 / D-07):

- Returns `null` unless `phase === 'GK_DIVE_AT_FEET_PROMPT'` and `useMyTeam()` is non-null.
- Deciding manager is `gkDiveAtFeetTeam`. Non-deciding manager sees `{Team} is deciding whether to dive…`.
- Heading `Dive at Feet?`; detail `` `${carrierName} is within range — dive to win the ball back?` `` resolving `gkDiveAtFeetCarrierId` through `pieces`.
- Append the distance qualifier to the detail line when `gkDiveAtFeetDistance === 3`: ` (−1 dice penalty at this range)`. This surfaces GKDIVE-02's -1 band before the manager commits, which the Copywriting Contract's detail line leaves open and is the only honest way to present the choice.
- Buttons: `Dive` → `emitGkDiveAtFeet(true)`; `Decline` → `emitGkDiveAtFeet(false)`. Co-equal, un-coloured.

`GkBoxEntryPromptPanel.tsx` (D-10 / D-11) covers TWO phases:

- `GK_BOX_ENTRY_PROMPT`: returns `null` unless `useMyTeam()` is non-null. Deciding manager is `gkBoxEntryTeam`; the other sees `{Team} is deciding whether to reposition…`. Heading `Goalkeeper Reposition?`; detail `The ball has entered the box — reposition your goalkeeper 1 hex?`; buttons `Reposition` → `emitGkBoxEntryResponse(true)` and `Decline` → `emitGkBoxEntryResponse(false)`.
- `GK_BOX_ENTRY_MOVE`: after accepting, the manager picks the destination on the board (Plan 39-05's store branch highlights the six neighbours and calls `emitGkBoxEntryMove`). In this phase render the heading plus a single constraint row `Select an adjacent hex for your goalkeeper.` for the acting manager and `{Team} is repositioning…` for the other, with NO buttons — the board click is the input.

Both panels surface `gameError` through `restartErrorMessage(...)` into `.errorText`.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client typecheck && pnpm stylelint</automated>
</verify>
<acceptance_criteria> - Both components exist, are exported by name, and return `null` outside their guarded phases - `GkDiveAtFeetPromptPanel.tsx` contains the exact strings `Dive at Feet?`, `Dive`, `Decline` - The `−1 dice penalty` qualifier renders only when `gkDiveAtFeetDistance === 3` - `GkBoxEntryPromptPanel.tsx` contains the exact strings `Goalkeeper Reposition?`, `The ball has entered the box — reposition your goalkeeper 1 hex?`, `Reposition`, `Decline`, `Select an adjacent hex for your goalkeeper.` - `GkBoxEntryPromptPanel` renders zero buttons while `phase === 'GK_BOX_ENTRY_MOVE'` - Neither `.module.css` `.panel` rule contains a `border` declaration; all spacing values are multiples of 4 - `pnpm --filter @counter-attack/client typecheck` and `pnpm stylelint` both exit 0
</acceptance_criteria>
<done>Both goalkeeper interrupt prompts render with the locked copy and route to their emitters.</done>
</task>

<task type="auto">
  <name>Task 3: Component tests for all three prompt panels</name>
  <files>packages/client/src/components/FoulChoicePanel.test.tsx, packages/client/src/components/GkDiveAtFeetPromptPanel.test.tsx, packages/client/src/components/GkBoxEntryPromptPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/CornerKickSetupPanel.test.tsx (the store-mock + `useMyTeam`-mock harness this project uses for panel tests)
    - The three panel components written in Tasks 1 and 2
  </read_first>
  <action>
Create one test file per panel.

`FoulChoicePanel.test.tsx`: returns `null` outside `FOUL_CHOICE`; the deciding manager sees `Foul!`, the victim's name in the detail line, and both buttons; the non-deciding manager sees the waiting text and no buttons; `Continue Play` calls `emitFoulChoice('continue')` once; `Take the Free Kick` calls `emitFoulChoice('restart')` once; with `foulSource: 'GK_DIVE_AT_FEET'` the second button reads `Take the Penalty` and still emits `'restart'`; a `gameError` renders in the error row.

`GkDiveAtFeetPromptPanel.test.tsx`: returns `null` outside `GK_DIVE_AT_FEET_PROMPT`; the goalkeeper's manager sees `Dive at Feet?` and both buttons; the attacking manager sees `is deciding whether to dive`; `Dive` emits `true` and `Decline` emits `false`; with `gkDiveAtFeetDistance: 3` the `−1` qualifier appears and with `gkDiveAtFeetDistance: 2` it does not.

`GkBoxEntryPromptPanel.test.tsx`: returns `null` outside the two box-entry phases; in `GK_BOX_ENTRY_PROMPT` the responding manager sees both buttons and `Reposition`/`Decline` emit `true`/`false`; the other manager sees the waiting text; in `GK_BOX_ENTRY_MOVE` the acting manager sees `Select an adjacent hex for your goalkeeper.` and zero buttons.

Resolve all player names from a real store-mocked `pieces` fixture so no assertion depends on a placeholder string.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- FoulChoicePanel.test.tsx GkDiveAtFeetPromptPanel.test.tsx GkBoxEntryPromptPanel.test.tsx</automated>
</verify>
<acceptance_criteria> - All three test files exist and the combined run passes with at least 18 assertions - The `Take the Penalty` test asserts both the label change AND that the emitted payload is still `'restart'` - The `GK_BOX_ENTRY_MOVE` test asserts `queryAllByRole('button')` has length 0 - `pnpm --filter @counter-attack/client test` (full client suite) is green - `pnpm build` exits 0
</acceptance_criteria>
<done>All three decision panels are regression-locked, including the GKDIVE-03 conditional restart label.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                              |
| ---------------------- | ------------------------------------------------------------------------ |
| panel button → emitter | Every click becomes an untrusted socket payload re-validated server-side |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                   | Disposition | Mitigation Plan                                                                                                                                                        |
| ---------- | ---------------------- | ------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| T-39-09-01 | Elevation of Privilege | Wrong manager sees a decision button        | mitigate    | Each panel gates its buttons on `useMyTeam() === decidingTeam`; the authoritative check is the handler's `socketTeam`/`controlsGKTeam` guard in Plans 39-13 and 39-15. |
| T-39-09-02 | Tampering              | Forged `choice` / `accept` payload          | mitigate    | Handlers validate the enum (`'continue'                                                                                                                                | 'restart'`) and `typeof accept === 'boolean'` before dispatch; the panel's typed emitter is not the defence. |
| T-39-09-03 | Denial of Service      | Rapid double-click on Dive/Decline          | mitigate    | Per-room `isProcessing` mutex drops duplicates; panels add no local retry or optimistic transition.                                                                    |
| T-39-09-04 | Information Disclosure | Waiting-state text leaking the other choice | accept      | Waiting text names only which team is deciding, never what they picked — matching the existing Phase 35/37/38 waiting-state convention.                                |
| T-39-09-SC | Tampering              | npm/pip/cargo installs                      | accept      | No packages installed this phase.                                                                                                                                      |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test` green
- `pnpm stylelint` green
- `pnpm build` green
</verification>

<success_criteria>
Three two-button panels exist with the locked copy, co-equal un-coloured buttons, no container border, correct acting/waiting splits, the GKDIVE-03 conditional `Take the Penalty` label, the GKDIVE-02 distance-3 penalty qualifier, and the button-free `GK_BOX_ENTRY_MOVE` state; all three are covered by component tests.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-09-SUMMARY.md` when done
</output>
