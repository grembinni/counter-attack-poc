---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 15
type: execute
wave: 7
depends_on: ['39-13', '39-14']
files_modified:
  - packages/server/src/roomStore.ts
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/gkDiveAtFeet.integration.test.ts
  - packages/server/src/__tests__/gameHandlers.boxEntry.test.ts
  - packages/server/src/__tests__/gameHandlers.halfTime.test.ts
autonomous: true
requirements: [GKDIVE-01, GKDIVE-02, GKDIVE-03, GKDIVE-04, GKDIVE-05]

must_haves:
  truths:
    - 'The dive-at-feet prompt is offered automatically whenever the post-action state qualifies, without any client polling'
    - 'The box-entry prompt is offered automatically the first time the ball enters a penalty area in a movement cycle'
    - 'Only the goalkeeper team manager can accept or decline either prompt, or submit the box-entry destination hex'
    - 'Both managers must click Start 2nd Half before the second half begins, in either order'
  artifacts:
    - path: 'packages/server/src/roomStore.ts'
      provides: 'Post-broadcast offer hooks and the previous-ball-position tracker'
      contains: 'lastBroadcastBallPosition'
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'GK dive-at-feet, box-entry and reworked half-time handlers'
      contains: 'GAME_GK_BOX_ENTRY_MOVE'
    - path: 'packages/server/src/__tests__/gkDiveAtFeet.integration.test.ts'
      provides: 'GKDIVE-01..05 socket-level coverage'
      min_lines: 200
  key_links:
    - from: 'broadcastState'
      to: 'computeGkDiveAtFeetOffer / computeBoxEntryOffer'
      via: 'single post-action offer hook, mirroring applyFreeMoveZoneCheck'
      pattern: 'computeBoxEntryOffer\\('
    - from: 'GAME_HALF_TIME_START'
      to: 'applySecondHalfConfirm'
      via: 'replacing the single-team NOT_KICK_OFF_TEAM gate'
      pattern: 'applySecondHalfConfirm'
---

<objective>
Expose the goalkeeper interrupt mechanics over sockets and replace the single-team second-half gate with the mutual-confirm gate.

Purpose: both goalkeeper offers are POST-ACTION checks that must fire after literally any action, so they belong at the single `broadcastState` entry point that `applyFreeMoveZoneCheck` already uses — not bolted onto individual handlers, which would guarantee a missed path. D-16's half-time rework replaces `NOT_KICK_OFF_TEAM` with an either-order both-confirm gate.

Output: the offer hooks in `roomStore.ts`, three socket handlers, the reworked `GAME_HALF_TIME_START`, and three test suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-VALIDATION.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-13-SUMMARY.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-14-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`, `Room.lastBroadcastBallPosition`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto">
  <name>Task 1: Post-action offer hooks in broadcastState</name>
  <files>packages/server/src/roomStore.ts</files>
  <read_first>
    - packages/server/src/roomStore.ts (full file — the `Room` type, `broadcastState`'s body, and the existing `applyFreeMoveZoneCheck` call that Phase 17 describes as "the single ARCH-04 entry point so the trigger fires after literally any action with zero per-handler changes elsewhere")
    - packages/server/src/gameEngine.ts — `applyFreeMoveZoneCheck` (the precedent for a post-action check that may itself change the phase before the snapshot is emitted), plus `computeGkDiveAtFeetOffer` and `computeBoxEntryOffer` from Plans 39-12 and 39-14
    - packages/shared/src/types.ts (`gkDiveAtFeet*` and `gkBoxEntry*` GameState clusters)
  </read_first>
  <action>
Add `lastBroadcastBallPosition?: HexCoord | null` to the `Room` type in `roomStore.ts`, with a comment that it exists solely so `computeBoxEntryOffer` can compare the pre-action ball position without a retroactive `eventLog` scan (the same reasoning `ball.lastTouchedBy` was introduced with in Phase 37).

In `broadcastState`, immediately after the existing `applyFreeMoveZoneCheck` step and before the snapshot is emitted, add a single offer-hook block that runs at most one offer per broadcast:

1. Capture `const prevBallPosition = room.lastBroadcastBallPosition ?? room.gameState.ball.position;` at the top of the block.
2. Box entry (D-10), checked FIRST because a ball entering the box is the more urgent response: when the current phase is an ordinary play phase (not already one of the Phase 39 prompt phases, not a restart-setup phase, not `HALF_TIME`/`FULL_TIME`/`REPLAY`), call `computeBoxEntryOffer(prevBallPosition, room.gameState)`. On a non-null result, set `room.gameState` to `{ ...state, phase: 'GK_BOX_ENTRY_PROMPT', gkBoxEntryTeam: team, activeTeam: team, gkBoxEntryResume: { phase: state.phase, activeTeam: state.activeTeam, movementSlot: state.movementSlot } }`.
3. Dive at feet (GKDIVE-02), checked only when no box-entry offer fired: when the current phase is `MOVE` and `state.foulsEnabled === true` is NOT required (GKDIVE-02 is a duel offer, gated by the Fouls toggle only for its foul-on-1 consequence — gate the OFFER on `state.foulsEnabled === true` because FOUL-05 explicitly lists "GK-dive-at-feet" among what the Fouls toggle enables), call `computeGkDiveAtFeetOffer(room.gameState)`. On a non-null result, set `phase: 'GK_DIVE_AT_FEET_PROMPT'`, `gkDiveAtFeetTeam`/`gkDiveAtFeetGkId`/`gkDiveAtFeetCarrierId`/`gkDiveAtFeetDistance` from the offer, `activeTeam` to the goalkeeper's team, and `gkDiveAtFeetResume: { phase: state.phase, activeTeam: state.activeTeam, movementSlot: state.movementSlot }`.
4. After the block, set `room.lastBroadcastBallPosition = room.gameState.ball.position;` so the next broadcast compares against this one.

Guard the whole block behind `room.gameState !== null`. It must be a pure phase substitution — never emit two snapshots, never call `broadcastState` recursively.

Add a comment naming FOUL-05's scope ("Fouls (detection, injury, GK-dive-at-feet, professional-foul check, and the resulting restart)") as the reason the dive offer is gated on `foulsEnabled` while the box-entry offer (a D-10 scope expansion, not part of any toggle's locked scope) is not.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server typecheck && pnpm --filter @counter-attack/server test</automated>
</verify>
<acceptance_criteria> - `roomStore.ts` declares `lastBroadcastBallPosition` on the `Room` type and assigns it at the end of every `broadcastState` call where `gameState` is non-null - `broadcastState` calls `computeBoxEntryOffer` and `computeGkDiveAtFeetOffer` at most once each per invocation, and never both in the same invocation - The dive-at-feet offer is gated on `room.gameState.foulsEnabled === true` - `broadcastState` still emits exactly one snapshot per invocation (no recursion, no duplicate emit) - `pnpm --filter @counter-attack/server test` (existing suites) is green — the existing `applyFreeMoveZoneCheck` behaviour is unchanged
</acceptance_criteria>
<done>Both goalkeeper offers fire automatically after any action, from one place, with no per-handler wiring.</done>
</task>

<task type="auto">
  <name>Task 2: The three goalkeeper handlers and the reworked half-time handler</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts lines 3009-3087 (`GAME_GK_DIVE` — the five-step handler shape and `controlsGKTeam` usage)
    - packages/server/src/gameHandlers.ts lines 2704-2745 (the current `GAME_HALF_TIME_START` handler with its `NOT_KICK_OFF_TEAM` single-team gate — this is what D-16 replaces)
    - packages/server/src/gameHandlers.ts (the `socketTeam(socket)` helper and `startReplayStream`)
    - packages/server/src/diceUtils.ts (`rollDice()`)
    - packages/server/src/gameEngine.ts (`applyGkDiveAtFeetResponse`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`)
  </read_first>
  <action>
Add three handlers, each a copy of `GAME_GK_DIVE`'s five-step shape (mutex → null-state guard → phase guard → payload validation → team guard → engine delegate → `broadcastState` → `finally` release), plus one rework.

1. `socket.on(ClientEvents.GAME_GK_DIVE_AT_FEET, (accept: boolean) => ...)`:
   - Phase guard `!== 'GK_DIVE_AT_FEET_PROMPT'` → `WRONG_PHASE`.
   - Payload validation `typeof accept !== 'boolean'` → `INVALID_TARGET`.
   - Team guard: `socketTeam(socket) !== room.gameState.gkDiveAtFeetTeam` → `WRONG_TEAM`. Do not reuse `controlsGKTeam` here — that helper derives the goalkeeper's team from `attackingTeam`/`ball.carrierId` per phase, and this phase already names the team explicitly in `gkDiveAtFeetTeam`. Add a comment saying so.
   - Roll all four dice server-side before the call: `const gkDie = rollDice(); const carrierDie = rollDice(); const injuryDie = rollDice(); const bookingDie = rollDice();` then `applyGkDiveAtFeetResponse(room.gameState, accept, { gkDie, carrierDie, injuryDie, bookingDie })`. Roll them unconditionally even when `accept === false`, so the handler stays free of rule logic.
   - On success assign and `broadcastState`; if the resulting phase is `FULL_TIME`, call `startReplayStream(io, room)`.

2. `socket.on(ClientEvents.GAME_GK_BOX_ENTRY_RESPONSE, (accept: boolean) => ...)`: same shape; phase guard `!== 'GK_BOX_ENTRY_PROMPT'`; team guard against `room.gameState.gkBoxEntryTeam`; delegate to `applyBoxEntryResponse(room.gameState, accept)`. No dice.

3. `socket.on(ClientEvents.GAME_GK_BOX_ENTRY_MOVE, (to: HexCoord) => ...)`: same shape; phase guard `!== 'GK_BOX_ENTRY_MOVE'`; payload validation `typeof to !== 'object' || to === null || typeof to.q !== 'number' || typeof to.r !== 'number'` → `INVALID_TARGET` (verbatim the `GAME_GK_DIVE` guard); team guard against `room.gameState.gkBoxEntryTeam`; delegate to `applyBoxEntryMove(room.gameState, to)`.

4. Rework `GAME_HALF_TIME_START` (D-16). Keep the mutex, the `HALF_TIME` phase guard and the snap-back `broadcastState` on every error path. REMOVE the `secondHalfKickOffTeam` derivation and the `NOT_KICK_OFF_TEAM` rejection entirely — both managers may now confirm. Replace the body with: resolve `const team = socketTeam(socket);` and reject with `WRONG_TEAM` when it is `null` (a spectator socket); then call `applySecondHalfConfirm(room.gameState, team)`, emit `result.reason` + `broadcastState` on failure, assign + `broadcastState` on success. A socket may only ever set its OWN team's flag — the team comes from `socketTeam(socket)`, never from the payload (the event carries no payload).

Import the four engine functions alongside the existing `gameEngine` imports.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server typecheck && pnpm --filter @counter-attack/server test</automated>
</verify>
<acceptance_criteria> - All three new `socket.on` registrations exist, each with `room.isProcessing = true`, a `try` block and a `finally { room.isProcessing = false; }` - `grep -c "NOT_KICK_OFF_TEAM" packages/server/src/gameHandlers.ts` returns 0 - `GAME_HALF_TIME_START` calls `applySecondHalfConfirm` and derives the team from `socketTeam(socket)`, never from a payload - The `GAME_GK_DIVE_AT_FEET` handler calls `rollDice()` exactly four times and reads no die from the payload - `GAME_GK_BOX_ENTRY_MOVE` validates `to.q`/`to.r` as numbers before use - Every new error path calls `broadcastState(io, room)` before returning - `pnpm --filter @counter-attack/server typecheck` exits 0
</acceptance_criteria>
<done>Both goalkeeper prompts and the mutual half-time confirm are reachable over sockets under the standard guards.</done>
</task>

<task type="auto">
  <name>Task 3: Integration suites for the dive-at-feet, box-entry and half-time flows</name>
  <files>packages/server/src/__tests__/gkDiveAtFeet.integration.test.ts, packages/server/src/__tests__/gameHandlers.boxEntry.test.ts, packages/server/src/__tests__/gameHandlers.halfTime.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/cornerKick.integration.test.ts (the template VALIDATION.md names for the dive suite — the two-socket harness and phase-driving helpers)
    - packages/server/src/__tests__/gameHandlers.phase17.test.ts and gameHandlers.test.ts (existing `GAME_HALF_TIME_START` coverage that must be updated for D-16, and the handler-level mocking style)
    - packages/server/src/gameHandlers.ts and packages/server/src/roomStore.ts (as modified in Tasks 1 and 2)
  </read_first>
  <action>
Create/extend three suites.

`gkDiveAtFeet.integration.test.ts` (GKDIVE-01..05 over sockets):

1. Moving a carrier to a hex within 3 of the defending goalkeeper, parallel to the goal line, produces a broadcast with `phase: 'GK_DIVE_AT_FEET_PROMPT'` and `gkDiveAtFeetTeam` set to the defending team — with no client request for it.
2. The attacking manager emitting `GAME_GK_DIVE_AT_FEET` receives `WRONG_TEAM` and the phase is unchanged.
3. A non-boolean payload receives `INVALID_TARGET`.
4. `accept: false` from the goalkeeper's manager appends `GK_DIVE_AT_FEET_DECLINED`, restores the interrupted phase, and leaves the cap unset so a further qualifying move offers again.
5. `accept: true` appends `GK_DIVE_AT_FEET` with both dice and both combined scores, and sets the cap so a further qualifying move does NOT re-offer (GKDIVE-05).
6. After a dive-at-feet has been used, declaring a shot proceeds without a `GK_DIVE` phase — the goalkeeper does not get the shot-block reposition (D-09 over the wire).
7. A double-emitted `GAME_GK_DIVE_AT_FEET` produces exactly one dive event (mutex).
8. With `foulsEnabled: false` on the room's `GameState`, a qualifying carrier move produces NO `GK_DIVE_AT_FEET_PROMPT` (FOUL-05 covers the dive-at-feet).

`gameHandlers.boxEntry.test.ts` (D-10/D-11 over sockets):

1. A pass that lands the ball inside the defending penalty area produces a broadcast with `phase: 'GK_BOX_ENTRY_PROMPT'` and `gkBoxEntryTeam` set to the area's owner.
2. The attacking manager emitting `GAME_GK_BOX_ENTRY_RESPONSE` receives `WRONG_TEAM`.
3. `accept: false` restores the interrupted phase and sets the cap, so a second entry in the same cycle does not re-offer.
4. `accept: true` moves to `GK_BOX_ENTRY_MOVE`; a `GAME_GK_BOX_ENTRY_MOVE` to a non-adjacent hex is rejected; to an adjacent unoccupied on-pitch hex it succeeds, appends `GK_BOX_ENTRY_MOVE`, and restores the interrupted phase.
5. A malformed hex payload (`{ q: 'x', r: 2 }`) receives `INVALID_TARGET` and mutates nothing.
6. D-11 independence over the wire: after using the box-entry response, a qualifying carrier move still produces a `GK_DIVE_AT_FEET_PROMPT`.

`gameHandlers.halfTime.test.ts` (D-16) — extend the existing half-time coverage rather than duplicating it:

1. From `HALF_TIME`, `GAME_HALF_TIME_START` from EITHER manager is accepted (no `NOT_KICK_OFF_TEAM`) and leaves the phase at `HALF_TIME` with `secondHalfConfirmed` showing one side true.
2. The second manager's confirm transitions to `KICK_OFF_SETUP` and clears `secondHalfConfirmed`.
3. The away-then-home order produces the identical end state as home-then-away.
4. A repeated confirm from the same manager does not start the half and appends no duplicate `SECOND_HALF_CONFIRM`.
5. `GAME_HALF_TIME_START` outside `HALF_TIME` still returns `WRONG_PHASE`.
6. Update or delete any pre-existing assertion that expects `NOT_KICK_OFF_TEAM`, recording in the summary that the behaviour was intentionally replaced by D-16.
   </action>
   <verify>
   <automated>pnpm --filter @counter-attack/server test -- gkDiveAtFeet.integration.test.ts gameHandlers.boxEntry.test.ts gameHandlers.halfTime.test.ts && pnpm --filter @counter-attack/server test</automated>
   </verify>
   <acceptance_criteria> - All three suites pass with a combined total of at least 20 assertions covering the numbered lists - The D-09-over-the-wire test drives a real shot declaration after a used dive-at-feet and asserts no `GK_DIVE` phase is reached - The D-11-over-the-wire test asserts a dive offer still appears after a used box-entry response - No test in the repository still asserts `NOT_KICK_OFF_TEAM` - `pnpm --filter @counter-attack/server test` (full server suite) is green - `pnpm build` exits 0
   </acceptance_criteria>
   <done>Both goalkeeper interrupts and the mutual half-time gate are proven end-to-end, including their access-control failures and cap independence.</done>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                 |
| ------------------------------- | --------------------------------------------------------------------------- |
| client socket → `gameHandlers`  | Three new untrusted payloads plus a payload-free half-start confirm         |
| `broadcastState` → both clients | The offer hook mutates phase before the snapshot; both players see the same |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                                                             |
| ---------- | ---------------------- | --------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-39-15-01 | Elevation of Privilege | Attacking manager answering a keeper prompt   | mitigate    | Each handler compares `socketTeam(socket)` against the explicit `gkDiveAtFeetTeam` / `gkBoxEntryTeam` field, not against a derived helper, and rejects with `WRONG_TEAM`.   |
| T-39-15-02 | Elevation of Privilege | One manager starting the second half alone    | mitigate    | `GAME_HALF_TIME_START` derives the team from `socketTeam(socket)` and can only set that socket's own flag; the transition requires both flags true inside the engine.       |
| T-39-15-03 | Tampering              | Client-declared dive dice                     | mitigate    | The handler calls `rollDice()` four times (`crypto.randomInt`) and the event payload carries no die field; regression-tested.                                               |
| T-39-15-04 | Tampering              | Malformed hex for the box-entry move          | mitigate    | Verbatim reuse of `GAME_GK_DIVE`'s `typeof to.q !== 'number'` guard, emitting `INVALID_TARGET` before any mutation.                                                         |
| T-39-15-05 | Denial of Service      | Double-click on a prompt or on the half start | mitigate    | Per-room `isProcessing` mutex on all four handlers with `finally` release, plus the engine's idempotent same-team half-start confirm.                                       |
| T-39-15-06 | Denial of Service      | Offer hook re-firing every broadcast          | mitigate    | Both offers are capped per movement cycle and the hook runs at most one offer per broadcast; `lastBroadcastBallPosition` makes the box-entry check edge-triggered.          |
| T-39-15-07 | Information Disclosure | Reconnect snapshot re-triggering a prompt     | mitigate    | The hook only fires on a genuine transition (`prevBallPosition` differs / phase is an ordinary play phase); a reconnect re-emits the already-prompted phase, not a new one. |
| T-39-15-SC | Tampering              | npm/pip/cargo installs                        | accept      | No packages installed this phase.                                                                                                                                           |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` green
- `pnpm build` green
- Zero occurrences of `NOT_KICK_OFF_TEAM` in the repository
- Every new handler has a `finally { room.isProcessing = false; }`
</verification>

<success_criteria>
Qualifying carrier moves and first-entry ball movements into the box automatically produce their prompts from a single `broadcastState` hook; only the goalkeeper's manager can answer them; dive dice are server-generated; the box-entry destination is shape-validated and adjacency-checked; using one cap does not consume the other; and the second half starts only after both managers confirm, in either order.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-15-SUMMARY.md` when done
</output>
