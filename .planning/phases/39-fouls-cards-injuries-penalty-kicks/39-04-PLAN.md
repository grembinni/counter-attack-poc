---
phase: 39-fouls-cards-injuries-penalty-kicks
plan: 04
type: execute
wave: 2
depends_on: ['39-01']
files_modified:
  - packages/client/src/components/EventBanner.tsx
  - packages/client/src/components/EventBanner.module.css
  - packages/client/src/components/EventBanner.test.tsx
  - packages/client/src/styles/tokens.css
autonomous: true
requirements: [FOUL-02, CARD-01, CARD-02, CARD-03, INJURY-01]

must_haves:
  truths:
    - 'A single broadcast that appends FOUL_CALLED, INJURY_CHECK and BOOKING_CHECK displays all three banners in sequence, not just the last one'
    - 'A foul always shows a banner; an injury banner appears only when the check actually injures; a booking banner appears only when a card is actually issued (D-02)'
    - 'Every booking banner renders a card-coloured badge matching the card issued, and a Professional Foul booking additionally renders a DOGSO label (D-03)'
    - 'Entering the penalty-kick setup phase shows a Penalty Kick! restart banner'
  artifacts:
    - path: 'packages/client/src/components/EventBanner.tsx'
      provides: 'Queue-based multi-event banner processing plus foul/injury/booking variants'
      contains: 'lastProcessedLengthRef.current'
      min_lines: 200
    - path: 'packages/client/src/styles/tokens.css'
      provides: 'Card colour semantic tokens'
      contains: '--color-card-yellow'
  key_links:
    - from: 'packages/client/src/components/EventBanner.tsx'
      to: 'gameState.eventLog'
      via: 'slice of every newly-appended event, queued in order'
      pattern: 'eventLog\\.slice\\('
    - from: 'RESTART_BANNERS'
      to: "GamePhase 'PENALTY_KICK_SETUP_ATTACKING'"
      via: 'one-entry-per-restart-family registry row'
      pattern: 'PENALTY_KICK_SETUP_ATTACKING'
---

<objective>
Fix the confirmed live `EventBanner.tsx` tail-only diffing defect and build D-02's foul → injury → booking banner sequence on top of it, including D-03's card-colour badge and DOGSO label.

Purpose: RESEARCH.md Pitfall 1 confirms `EventBanner.tsx:106-116` inspects only `eventLog[eventLog.length - 1]` while advancing `lastProcessedLengthRef` past ALL new events. Phase 39's foul chain appends up to five events in one `broadcastState` (`MOVE` + `TACKLE_ATTEMPT` + `FOUL_CALLED` + `INJURY_CHECK` + `BOOKING_CHECK`), so without this fix the foul and injury banners silently never fire. STATE.md has flagged this as a v1.6 pitfall since Phase 37 and it has never been fixed.

Output: a queue-based banner processor, the three new banner variants, the card badge + DOGSO rendering, the Penalty Kick restart-banner row, and the two new colour tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-CONTEXT.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-RESEARCH.md
@.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by Phase 39 (they do not exist before this phase — exclude from drift verification):

- **Shared types** (`packages/shared/src/types.ts`): `PlayerPiece.injuryCount`/`.yellowCards`/`.redCarded`; `GamePhase` members `FOUL_CHOICE`, `GK_DIVE_AT_FEET_PROMPT`, `GK_BOX_ENTRY_PROMPT`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT`, `PENALTY_KICK`; `ActionEventType` members `FOUL_CALLED`, `INJURY_CHECK`, `BOOKING_CHECK`, `FOUL_CHOICE_MADE`, `GK_DIVE_AT_FEET`, `GK_DIVE_AT_FEET_DECLINED`, `GK_BOX_ENTRY_MOVE`, `PENALTY_KICK_WINDOW_ADVANCE`, `PENALTY_KICK_TAKER_PLACED`, `PENALTY_KICK`, `SECOND_HALF_CONFIRM`; `LOOSE_BALL_LAND.direction`/`.distance`; `GameState` fields `foulsEnabled`, `bookingEnabled`, `injuryEnabled`, `foulDefenderId`, `foulVictimId`, `foulHex`, `foulSource`, `foulResume`, `gkDiveAtFeetUsedByTeam`, `gkDiveAtFeetTeam`, `gkDiveAtFeetGkId`, `gkDiveAtFeetCarrierId`, `gkDiveAtFeetDistance`, `gkDiveAtFeetResume`, `gkBoxEntryUsedByTeam`, `gkBoxEntryTeam`, `gkBoxEntryResume`, `penaltyKickTeam`, `penaltyKickSpot`, `penaltyKickEligibleIds`, `penaltyKickUsedPace`, `penaltyKickTakerId`, `secondHalfConfirmed`
- **Shared constants/helpers**: `PENALTY_SPOT`; `FOUL_TRIGGER_DIE`, `INJURY_DEGRADED_ATTRIBUTES`, `isInjured`, `injuryPenalty`, `applyInjuryDegradation`, `rollsInjury`, `rollsBooking`, `resolveBooking`, `isProfessionalFoul`; `validateDiveAtFeetDistance`
- **Shared events**: `ClientEvents.GAME_FOUL_CHOICE`, `GAME_GK_DIVE_AT_FEET`, `GAME_GK_BOX_ENTRY_RESPONSE`, `GAME_GK_BOX_ENTRY_MOVE`, `GAME_PENALTY_KICK_TAKER`; extended `ROOM_SETTINGS_CONFIRM`/`ROOM_SETTINGS_CONFIRMED` payloads
- **Server**: `gameEngine.ts` `resolveFoulChain`, `applyFoulChoice`, `triggerFoulFreeKick`, `triggerPenaltyKick`, `computePenaltyKickEligibleIds`, `applyPenaltyKickReposition`, `applyPenaltyKickWindowEnd`, `applyPenaltyKickTaker`, `applyPenaltyKickDuel`, `enterGkDiveOrSkip`, `computeGkDiveAtFeetOffer`, `applyGkDiveAtFeetResponse`, `computeGkDiveDisplacement`, `computeBoxEntryOffer`, `applyBoxEntryResponse`, `applyBoxEntryMove`, `applySecondHalfConfirm`; `roomStore.ts` `Room.foulsEnabled`/`.bookingEnabled`/`.injuryEnabled`
- **Client**: `FoulChoicePanel`, `GkDiveAtFeetPromptPanel`, `GkBoxEntryPromptPanel`, `PenaltyKickSetupPanel`; `useGameStore` actions `emitFoulChoice`, `emitGkDiveAtFeet`, `emitGkBoxEntryResponse`, `emitGkBoxEntryMove`, `emitPenaltyKickTaker`; CSS tokens `--color-card-yellow`, `--color-card-red`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the failing multi-event and banner-variant tests to EventBanner.test.tsx</name>
  <files>packages/client/src/components/EventBanner.test.tsx</files>
  <read_first>
    - packages/client/src/components/EventBanner.test.tsx (full file — the store-mock helper, how it re-renders with a mutated `eventLog`, and the existing GOAL/TACKLE/HP_ACCURACY assertions)
    - packages/client/src/components/EventBanner.tsx (full file — `RESTART_BANNERS` at 24-29, `getBannerMessage` at 43-70, the tail-only diff effect at 106-116, the phase-entry effect at 122-129, the auto-dismiss effect at 135-143, and the render body at 147-156)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Copywriting Contract rows for the foul/injury/booking banners; Color section's booking-badge geometry)
  </read_first>
  <behavior>
    - Appending three qualifying events (`FOUL_CALLED`, an injuring `INJURY_CHECK`, a yellow `BOOKING_CHECK`) in ONE store update displays `Foul!` first; after its 1000ms duration elapses the injury banner appears; after that duration the booking banner appears. Assert the ordered sequence with fake timers — this is the Pitfall 1 regression test and it fails against the current tail-only implementation.
    - Appending a non-injuring `INJURY_CHECK` (`injured: false`) produces NO injury banner, while the `FOUL_CALLED` in the same update still produces `Foul!` (D-02: banners only when there is an impact on play).
    - Appending a `BOOKING_CHECK` with `card: 'none'` produces no booking banner.
    - A yellow `BOOKING_CHECK` renders text `{Player Name} — Yellow Card` and an element carrying the yellow card badge (assert via `data-testid="card-badge"` and its `data-card="yellow"` attribute).
    - A `BOOKING_CHECK` with `card: 'red', secondYellow: true` renders `{Player Name} — Red Card (2nd Yellow)` and `data-card="red"`.
    - A `BOOKING_CHECK` with `professional: true` additionally renders the literal text `DOGSO`; a `professional: false` booking does not.
    - An injuring `INJURY_CHECK` renders `{Player Name} is Injured!`.
    - Transitioning `phase` to `PENALTY_KICK_SETUP_ATTACKING` shows `Penalty Kick!` exactly once, and a subsequent broadcast still in that phase does not re-fire it.
    - Mounting directly into `PENALTY_KICK_SETUP_ATTACKING` (reconnect-snapshot case) shows no banner — the existing `prevPhaseRef` mount-safety must be preserved.
  </behavior>
  <action>
Extend `EventBanner.test.tsx` with a `describe('multi-event broadcast (Pitfall 1)')` block and a `describe('foul/injury/booking banners')` block asserting every case in the `<behavior>` list. Use `vi.useFakeTimers()` + `vi.advanceTimersByTime(...)` wrapped in `act()` to walk the queue between banners; the existing auto-dismiss effect already uses `setTimeout`, so fake timers are the correct instrument.

Resolve player names through the same store-mocked `pieces` array the existing tests use, so `{Player Name}` in the assertions is a real fixture name, not a placeholder.

At the end of this task the multi-event sequence test and every new banner-variant test MUST fail — that is the RED state Task 2 turns green. Record which assertions fail in the task notes.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- EventBanner.test.tsx</automated>
</verify>
<acceptance_criteria> - `EventBanner.test.tsx` contains a test that appends three qualifying events in one update and asserts all three banner texts appear in order - `EventBanner.test.tsx` asserts `data-card="yellow"` and `data-card="red"` badge attributes - `EventBanner.test.tsx` asserts the literal string `DOGSO` appears only for `professional: true` - Running `pnpm --filter @counter-attack/client test -- EventBanner.test.tsx` at the end of this task reports failures for the new multi-event and variant tests (RED state confirmed in task notes) - The pre-existing GOAL/TACKLE/HP_ACCURACY tests are untouched and still present
</acceptance_criteria>
<done>A failing suite pins both the Pitfall 1 fix and the full D-02/D-03 banner contract.</done>
</task>

<task type="auto">
  <name>Task 2: Replace tail-only diffing with an ordered banner queue and add the new variants</name>
  <files>packages/client/src/components/EventBanner.tsx</files>
  <read_first>
    - packages/client/src/components/EventBanner.tsx (full file)
    - packages/client/src/components/EventBanner.test.tsx (as written in Task 1 — the specification)
    - packages/shared/src/types.ts (the `FOUL_CALLED`/`INJURY_CHECK`/`BOOKING_CHECK` variants added in 39-01)
    - packages/client/src/components/ActionLog.tsx lines 1-60 (the `pieceName`/`PNamed` store-lookup pattern for resolving a player id to a display name outside a hook)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Copywriting Contract; Color section)
  </read_first>
  <action>
Rework the eventLog effect (currently lines 106-116) to process EVERY newly-appended event:

- Compute `const newEvents = eventLog.slice(lastProcessedLengthRef.current)`, map each through `getBannerMessage`, filter out nulls, and enqueue the survivors in order.
- Keep `lastProcessedLengthRef.current = eventLog.length` advancing unconditionally, exactly as today.
- Hold the queue in a `useRef<Banner[]>([])` plus the existing `active` state. When `active` is `null` and the queue is non-empty, shift the head into `active`. The existing auto-dismiss effect sets `active` to `null` after `active.duration`; extend it so that on dismissal it pulls the next queued banner instead of simply going idle. Use a ref for the queue (not state) so enqueueing never triggers an extra render pass, and drive the drain from the existing dismiss effect.
- Preserve the phase-entry effect (`prevPhaseRef`) unchanged in structure; a phase banner enqueues through the SAME queue so a restart banner never overwrites a mid-sequence foul banner.
- All state updates stay inside `useEffect` — never in the render body (Phase 18.4 gap-closure G1 rule).

Widen the banner payload type from `{ message; variant; duration }` to `{ message: string; variant: 'goal' | 'notable'; duration: number; cardColor?: 'yellow' | 'red'; dogso?: boolean }`.

Extend `getBannerMessage` with three new cases, each returning `duration: 1000` and `variant: 'notable'` (matching the existing `Tackle! Turnover!` / `INTERCEPTION!!` precedent):

- `FOUL_CALLED` → always `{ message: 'Foul!' }`. D-02: fires on every foul regardless of outcome.
- `INJURY_CHECK` → `null` unless `event.injured === true`; when injured, `{ message: '<name> is Injured!' }`.
- `BOOKING_CHECK` → `null` when `event.card === 'none'`; otherwise `{ message: '<name> — Yellow Card' }` for yellow, `'<name> — Red Card (2nd Yellow)'` when `card === 'red' && secondYellow`, `'<name> — Red Card'` for any other red, with `cardColor: event.card` and `dogso: event.professional`.

`getBannerMessage` is a module-scope function today and cannot call hooks. Change its signature to `getBannerMessage(event: ActionEvent, pieceName: (id: string) => string)` and pass a name resolver built in the component from a `useGameStore((s) => s.gameState.pieces)` selector, mirroring how `ActionLog.tsx` resolves ids to names. Do not read the store inside the module-scope function.

Add one row to `RESTART_BANNERS`: `PENALTY_KICK_SETUP_ATTACKING: 'Penalty Kick!'`. Exactly one row for the whole penalty-kick family — the table's existing one-entry-per-family invariant (T-38-66) forbids also registering `PENALTY_KICK_SETUP_DEFENDING`/`PENALTY_KICK_TAKER_SELECT`/`PENALTY_KICK`. Replace the stale "Penalty Kick ... is Phase 39 scope" comment with a note that the row now exists.

In the render body, when `active.cardColor` is set render a card badge element before the message: `<span data-testid="card-badge" data-card={active.cardColor} className={styles.cardBadge} />`. When `active.dogso` is true, render `<span className={styles.dogsoLabel}>DOGSO</span>` beside it. Keep the existing `role="status" aria-live="assertive"` wrapper and the `animationDuration` inline style untouched.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- EventBanner.test.tsx</automated>
</verify>
<acceptance_criteria> - `pnpm --filter @counter-attack/client test -- EventBanner.test.tsx` passes with zero failures, including the three-events-in-one-update sequence test - `grep -c "eventLog\[eventLog.length - 1\]" packages/client/src/components/EventBanner.tsx` returns 0 - `grep -c "eventLog.slice(" packages/client/src/components/EventBanner.tsx` returns at least 1 - `RESTART_BANNERS` contains exactly one new key, `PENALTY_KICK_SETUP_ATTACKING`, and does NOT contain `PENALTY_KICK_SETUP_DEFENDING`, `PENALTY_KICK_TAKER_SELECT` or `PENALTY_KICK` - No `setActive`/`setState` call appears outside a `useEffect` callback in `EventBanner.tsx` - `pnpm --filter @counter-attack/client typecheck` exits 0
</acceptance_criteria>
<done>Every newly-appended event gets its banner, in order; the foul/injury/booking variants and the Penalty Kick restart banner all fire per D-02.</done>
</task>

<task type="auto">
  <name>Task 3: Card colour tokens and the badge / DOGSO styling</name>
  <files>packages/client/src/styles/tokens.css, packages/client/src/components/EventBanner.module.css</files>
  <read_first>
    - packages/client/src/styles/tokens.css (the existing custom-property block; specifically `--color-text-inverse` at ~line 36, `--color-cta-pending-bg` at ~59, `--color-danger` at ~63, `--color-confirm-pending-bg` at ~91, and the existing precedent comments where one token reuses another token's swatch under a new semantic name)
    - packages/client/src/components/EventBanner.module.css (the `.banner`, `.goal`, `.notable` classes and the `bannerFade` keyframes; note `.banner`'s 28px font-size is pre-existing and out of Phase 39's typography cap)
    - .planning/phases/39-fouls-cards-injuries-penalty-kicks/39-UI-SPEC.md (Color: new semantic tokens table and the booking-banner badge geometry; Typography: DOGSO label is Label role 11px/700)
  </read_first>
  <action>
In `packages/client/src/styles/tokens.css`, add two custom properties in the same block and comment style as the existing swatch-reuse precedents:

- `--color-card-yellow: #eab308;` — comment that it reuses `--color-confirm-pending-bg`'s swatch under a dedicated card semantic.
- `--color-card-red: #ef4444;` — comment that it reuses `--color-danger`'s swatch under a dedicated card semantic.

Introduce no other new colour value anywhere in this plan.

In `EventBanner.module.css`, add:

- `.cardBadge` — an inline-block roughly 18px × 24px (taller than wide, a miniature card silhouette per UI-SPEC), `border-radius: 2px`, `border: 1px solid rgba(0,0,0,0.5)` (matching the ball-dot stroke treatment), `vertical-align: middle`, and an 8px right margin (the `sm` token). Its background is set from the `data-card` attribute with two attribute selectors: `.cardBadge[data-card='yellow'] { background: var(--color-card-yellow); }` and `.cardBadge[data-card='red'] { background: var(--color-card-red); }`.
- `.dogsoLabel` — `font-size: 11px; font-weight: 700; letter-spacing: 0.08em; color: var(--color-text-inverse);` plus an 8px left margin, matching UI-SPEC's Label role and the `.helperLine1` letter-spacing convention. Do not introduce a new font size or weight — 11px/700 already exist app-wide.

Use only 4px-multiple spacing values (4, 8, 16). Do not touch `.banner`'s 28px font-size — UI-SPEC's Typography scope note explicitly preserves it for visual parity with the existing goal/interception/tackle banners.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- EventBanner.test.tsx && pnpm stylelint</automated>
</verify>
<acceptance_criteria> - `grep -c "\-\-color-card-yellow\|\-\-color-card-red" packages/client/src/styles/tokens.css` returns 2 - `--color-card-yellow` is exactly `#eab308` and `--color-card-red` is exactly `#ef4444` - `EventBanner.module.css` defines `.cardBadge`, `.cardBadge[data-card='yellow']`, `.cardBadge[data-card='red']` and `.dogsoLabel` - `.banner`'s `font-size` value in `EventBanner.module.css` is unchanged from before this plan - Every spacing value added to `EventBanner.module.css` is a multiple of 4 - `pnpm stylelint` exits 0 - `pnpm --filter @counter-attack/client test` (full client suite) is green
</acceptance_criteria>
<done>Booking banners render a correctly-coloured card badge and, for Professional Fouls, a DOGSO label, using two new semantic tokens and no new raw colour values.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                             |
| --------------------------------- | --------------------------------------------------------------------------------------- |
| broadcast `GameState` → banner UI | The client renders whatever the server-authoritative event log says; it decides nothing |

## STRIDE Threat Register

| Threat ID  | Category               | Component                   | Disposition | Mitigation Plan                                                                                                                                                                |
| ---------- | ---------------------- | --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-39-04-01 | Spoofing               | Banner content              | accept      | `EventBanner` is read-only projection of `eventLog`; it emits no socket events and mutates no state. A wrong banner is a display bug, not a state-integrity issue.             |
| T-39-04-02 | Denial of Service      | Unbounded banner queue      | mitigate    | Cap the queue at 5 entries (the maximum a single foul broadcast can produce: MOVE + duel + FOUL_CALLED + INJURY_CHECK + BOOKING_CHECK) and drop the overflow; assert in tests. |
| T-39-04-03 | Information Disclosure | Reconnect snapshot → banner | mitigate    | Preserve the existing `prevPhaseRef`/`lastProcessedLengthRef` mount-initialisation (T-38-64) so a mid-match reconnect replays no historical banners.                           |
| T-39-04-SC | Tampering              | npm/pip/cargo installs      | accept      | No packages installed this phase.                                                                                                                                              |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test` green
- `pnpm stylelint` green
- `pnpm build` green
- No occurrence of `eventLog[eventLog.length - 1]` remains in `EventBanner.tsx`
</verification>

<success_criteria>
A single broadcast carrying foul + injury + booking displays all three banners in order; non-impacting injury/booking checks show no banner but still log; booking banners carry the correct card colour badge and, for a Professional Foul, a DOGSO label; the Penalty Kick restart banner fires once on phase entry; two new semantic colour tokens exist and no raw colour literal was added.
</success_criteria>

<output>
Create `.planning/phases/39-fouls-cards-injuries-penalty-kicks/39-04-SUMMARY.md` when done
</output>
