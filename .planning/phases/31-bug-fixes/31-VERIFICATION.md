---
phase: 31-bug-fixes
verified: 2026-07-24T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/4
  gaps_closed:
    - 'After a goal is scored and the match resets to kickoff, replaying that goal shows every player reconstructed at their correct kickoff position (not just some). (BUG-30)'
    - "A header-duel winner's piece id, once added to movedPieceIds by applyResolveHeaderTarget on a non-goal route, remains in movedPieceIds through the following Movement Phase (folded header-winner eligibility todo, BUG-31 family)."
    - 'REQUIREMENTS.md BUG-30 checkbox/status table entry updated to Complete (orchestrator applied the 2-line doc fix directly after re-verification identified it as the only remaining gap).'
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
---

# Phase 31: Bug Fixes Verification Report (Re-Verification After Gap Closure)

**Phase Goal:** Fix BUG-30 (replay piece reconstruction), BUG-31 (eligible-players/End-Turn timing), BUG-32 (GK deflection selection), and the folded header-winner eligibility todo.
**Verified:** 2026-07-24T00:00:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plans 31-05, 31-06)

**Post-verification note:** the sole remaining gap below (REQUIREMENTS.md BUG-30 marker stale) was a 2-line documentation fix applied directly by the orchestrator immediately after this report was generated — see commit `f9fa946`. All 5 truths are now satisfied.

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                  | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | After a goal is scored and the match resets to kickoff, replaying that goal shows every player reconstructed at their correct kickoff position (not just some). (BUG-30 / ROADMAP SC1) | ✓ VERIFIED | All 5 real GOAL-construction sites now populate `piecesAfter`: `gameEngine.ts:2184`, `gameEngine.ts:2279` (from 31-01), and `gameHandlers.ts:1045`, `gameHandlers.ts:1694`, `gameHandlers.ts:2479` (from 31-05, confirmed by direct read of each site — each hoists `const resetPieces = buildKickOffPieces(...)` once, feeds it to both `pieces:` and `piecesAfter:`). `buildReplayFrames` (gameEngine.ts:4715-4717) applies `event.piecesAfter` when present. `shotGkRange.test.ts` (6/6 passing) drives all 3 GK-out-of-range branches through the real socket handlers and asserts full-piece reconstruction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2   | The eligible-players-remaining message and End Turn button color update the moment a player starts a move, and correctly reflect state after an Undo. (BUG-31 / ROADMAP SC2)           | ✓ VERIFIED | `ActionPanel.tsx:924-926` unchanged from prior verification — `startedCount = Object.keys(paceUsedByPieceId).length`; derivation is live and unaffected by the gap-closure edits (ActionPanel.tsx was not touched by plans 31-05/31-06).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 3   | The goalkeeper can never be selected as an eligible deflection responder. (BUG-32 / ROADMAP SC3)                                                                                       | ✓ VERIFIED | `HexGrid.tsx:740` — `piece.role !== 'GK'` clause still present and unchanged. `gameHandlers.ts:453` — `if (sdPiece.role === 'GK')` server-side rejection still present and unchanged (file was modified by 31-05 only at the 3 unrelated GOAL sites; this line was not touched).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 4   | A header-duel winner's piece is marked spent (in `movedPieceIds`) and stays unselectable through the following Movement Phase (folded header-winner eligibility todo, BUG-31 family).  | ✓ VERIFIED | Traced end-to-end: `applyResolveHeaderTarget` (gameEngine.ts:3533-3535, :3569-3571) appends the winner's id to `movedPieceIds` AND now also sets `carriedMovedPieceIds: resolvedWinner ? [resolvedWinner.id] : (state.carriedMovedPieceIds ?? [])` on both non-goal branches (occupant-PASS and empty-hex/loose-ball). `applyStartMovement` (gameEngine.ts:465) now reads `movedPieceIds: [...(state.carriedMovedPieceIds ?? [])]` instead of the prior unconditional `[]`, and clears the carry immediately after with `carriedMovedPieceIds: []` (line 469) so it is consumed for exactly one Movement Phase. `gameEngine.rule11.test.ts` (30/30 passing) contains a chained regression suite (`describe('RULE-02 gap: header winner stays spent through applyStartMovement...')`) that pipes `applyResolveHeaderTarget`'s PASS output directly into `applyStartMovement` and asserts the winner id survives into the MOVE-phase `movedPieceIds` on both non-goal branches, a no-carry control confirming the common path still resets to `[]`, and a carry-cleared assertion confirming one-phase-only semantics. |
| 5   | REQUIREMENTS.md's BUG-30/BUG-31/BUG-32 completion marks are accurate as of this re-verification.                                                                                       | ✗ FAILED   | BUG-31 (line 40, `[x]`) and BUG-32 (line 41, `[x]`) are accurate. BUG-30 (line 39) is still marked `[ ]` with "(partial — 2 of 5 GOAL-construction paths fixed...)" text, and the traceability table (line 87) still says "Partial" — both stale relative to the now-verified-closed codebase state. Documentation-only gap; does not indicate any code defect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

**Score:** 4/5 truths verified (the 1 failure is a documentation-sync gap, not a code defect)

### Data-Flow / Wiring Trace: applyResolveHeaderTarget → applyStartMovement

Traced directly (not copied from SUMMARY claims):

1. `applyResolveHeaderTarget`, occupant-PASS branch (gameEngine.ts:3510-3543): returns `phase: 'PASS'`, `movedPieceIds: resolvedWinner ? [...state.movedPieceIds, resolvedWinner.id] : state.movedPieceIds`, **and** `carriedMovedPieceIds: resolvedWinner ? [resolvedWinner.id] : (state.carriedMovedPieceIds ?? [])`.
2. `applyResolveHeaderTarget`, empty-hex/loose-ball branch (gameEngine.ts:3551-3579): identical pattern — both `movedPieceIds` append and `carriedMovedPieceIds` set.
3. `applyStartMovement` (gameEngine.ts:435-490), the sole PASS/KICK_OFF/LOOSE_BALL → MOVE transition: line 465 `movedPieceIds: [...(state.carriedMovedPieceIds ?? [])]` — reads the carry (not the stale `movedPieceIds` from the PASS state, which is correct since MOVE is a fresh phase and only the carried id is meaningful); line 469 `carriedMovedPieceIds: []` clears the carry immediately after use.
4. Net effect: winner id present in PASS-phase `movedPieceIds` (append) → present in `carriedMovedPieceIds` (carry) → survives into MOVE-phase `movedPieceIds` via the carry-read at line 465 → carry itself cleared so it cannot leak into a second Movement Phase.
5. `contestedPieceIds: []` (line 473) — confirmed **unchanged**, exactly as both plans declared out of scope. The identical unconditional-reset defect flagged in `31-REVIEW.md` CR-01 for `contestedPieceIds` remains present and untouched, which is correct per this phase's explicit scope boundary (not part of BUG-30/31/32/the folded todo).

This closes the previously-confirmed gap: the prior verification's empirical chain (`PASS movedPieceIds = ['home-fwd']` → `MOVE movedPieceIds = []`) is now `PASS movedPieceIds = ['home-fwd']`, `carriedMovedPieceIds = ['home-fwd']` → `MOVE movedPieceIds = ['home-fwd']`, `carriedMovedPieceIds = []`.

### Required Artifacts

| Artifact                                                                                               | Expected                                                                                | Status     | Details                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/server/src/gameHandlers.ts` (3 GK-out-of-range GOAL sites)                                   | Each populates `piecesAfter: resetPieces` fed by a single hoisted const                 | ✓ VERIFIED | `grep -c "piecesAfter: resetPieces"` = 3 (lines 1045, 1694, 2479); `grep -c "const resetPieces = buildKickOffPieces"` = 3; `grep -c "pieces: resetPieces"` = 3. Direct read of full context at all 3 sites confirms single hoist, no divergence, no double `buildKickOffPieces` call. |
| `packages/server/src/gameEngine.ts` (`buildReplayFrames` GOAL branch)                                  | Applies `event.piecesAfter` conditionally                                               | ✓ VERIFIED | Lines 4715-4717, unchanged from prior verification.                                                                                                                                                                                                                                   |
| `packages/shared/src/types.ts` (`carriedMovedPieceIds`)                                                | New optional field mirroring `contestedPieceIds`                                        | ✓ VERIFIED | Line 809: `carriedMovedPieceIds?: readonly string[];` present.                                                                                                                                                                                                                        |
| `packages/server/src/gameEngine.ts` (`applyResolveHeaderTarget`)                                       | Both non-goal branches populate `carriedMovedPieceIds`                                  | ✓ VERIFIED | Lines 3540-3542 and 3576-3578 — both branches set the field alongside the pre-existing `movedPieceIds` append. Goal-line GK_DIVE branch (lines ~3459-3487) confirmed unchanged — no `carriedMovedPieceIds` reference there.                                                           |
| `packages/server/src/gameEngine.ts` (`applyStartMovement`)                                             | Merges carry into `movedPieceIds`, then clears carry                                    | ✓ VERIFIED | Line 465 (merge/read) and line 469 (clear) present exactly as specified. `contestedPieceIds: []` at line 473 confirmed untouched.                                                                                                                                                     |
| `packages/server/src/__tests__/shotGkRange.test.ts`                                                    | Regression coverage for all 3 GK-out-of-range replay paths                              | ✓ VERIFIED | 6/6 tests passing, includes the 3 new replay-reconstruction assertions driving real socket handlers.                                                                                                                                                                                  |
| `packages/server/src/__tests__/gameEngine.rule11.test.ts`                                              | Chained `applyResolveHeaderTarget` → `applyStartMovement` regression + no-carry control | ✓ VERIFIED | 30/30 tests passing, includes the new `describe('RULE-02 gap: header winner stays spent through applyStartMovement...')` block with 4 tests (both non-goal branches chained, no-carry control, one-phase-only carry-cleared assertion).                                               |
| `packages/client/src/components/ActionPanel.tsx`                                                       | BUG-31 `startedCount` derivation unchanged                                              | ✓ VERIFIED | Not in either gap-closure plan's `files_modified`; confirmed unchanged at lines 924-926.                                                                                                                                                                                              |
| `packages/client/src/components/HexGrid.tsx` / `packages/server/src/gameHandlers.ts` (BUG-32 GK gates) | Client + server GK exclusion unchanged                                                  | ✓ VERIFIED | `HexGrid.tsx:740` and `gameHandlers.ts:453` confirmed unchanged (the 3 gap-closure edits in gameHandlers.ts are at unrelated line ranges: 1013-1045, 1657-1694, 2438-2479).                                                                                                           |
| `.planning/REQUIREMENTS.md`                                                                            | BUG-30 marked Complete post gap-closure                                                 | ✗ STALE    | Line 39/87 still show partial/Partial — not updated after 31-05 closed the gap.                                                                                                                                                                                                       |

### Key Link Verification

| From                                         | To                                 | Via                                                 | Status  | Details                                                                                                       |
| -------------------------------------------- | ---------------------------------- | --------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------- |
| `gameHandlers.ts` 3 GOAL sites               | GOAL variant (`types.ts`)          | `piecesAfter: resetPieces`                          | ✓ WIRED | Confirmed at all 3 sites.                                                                                     |
| `buildReplayFrames` GOAL branch              | `current.pieces`                   | `current = {...current, pieces: event.piecesAfter}` | ✓ WIRED | Now a real no-op-free link for all 5 GOAL-producing paths (previously a no-op for 3 of 5).                    |
| `applyResolveHeaderTarget` non-goal branches | `applyStartMovement` (PASS → MOVE) | `carriedMovedPieceIds` merge-then-clear             | ✓ WIRED | Confirmed by direct trace (see Data-Flow section above) — this was the previously NOT_WIRED link; now closed. |
| `canSelectSnapDeflect` (client)              | server SNAPSHOT_DEFLECT handler    | GK rejection at both layers                         | ✓ WIRED | Unchanged, re-confirmed.                                                                                      |
| ActionPanel `remaining`                      | `paceUsedByPieceId` (server state) | `startedCount` derivation                           | ✓ WIRED | Unchanged, re-confirmed.                                                                                      |

### Behavioral Spot-Checks

| Behavior                                                           | Command                                                                                                                                              | Result                                  | Status |
| ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | ------ |
| gameEngine.rule11 suite (includes new chained header-winner tests) | `pnpm --filter @counter-attack/server test gameEngine.rule11 -- --run`                                                                               | 30/30 passed                            | ✓ PASS |
| shotGkRange suite (includes new replay-reconstruction assertions)  | `pnpm --filter @counter-attack/server test shotGkRange -- --run`                                                                                     | 6/6 passed                              | ✓ PASS |
| Full server suite                                                  | `pnpm --filter @counter-attack/server test -- --run`                                                                                                 | 33 files, 627 passed, 1 skipped, 1 todo | ✓ PASS |
| Full client suite                                                  | `pnpm --filter @counter-attack/client test -- --run`                                                                                                 | 20 files, 376 passed                    | ✓ PASS |
| Typecheck (all 3 packages)                                         | `pnpm --filter @counter-attack/server typecheck && pnpm --filter @counter-attack/client typecheck && pnpm --filter @counter-attack/shared typecheck` | zero errors, all 3                      | ✓ PASS |
| Debt-marker scan on all files modified across 31-05/31-06          | `grep -n -E "TBD\|FIXME\|XXX\|HACK\|PLACEHOLDER"` across gameHandlers.ts, gameEngine.ts, types.ts, shotGkRange.test.ts, gameEngine.rule11.test.ts    | No matches                              | ✓ PASS |

### Requirements Coverage

| Requirement            | Source Plan(s) | Description                                                                                | Status                                             | Evidence                                                                                                                                                     |
| ---------------------- | -------------- | ------------------------------------------------------------------------------------------ | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| BUG-30                 | 31-01, 31-05   | Replay reconstructs all player positions at kickoff after a goal                           | ✓ SATISFIED (codebase) / ✗ STALE (REQUIREMENTS.md) | All 5 real GOAL-construction sites populate `piecesAfter`; codebase fix fully verified. REQUIREMENTS.md text/table still says "partial" — not yet corrected. |
| BUG-31                 | 31-02          | Eligible-players-remaining message / End Turn color update immediately, correct after Undo | ✓ SATISFIED                                        | Verified unchanged from prior verification; REQUIREMENTS.md `[x]` accurate.                                                                                  |
| BUG-31 (folded family) | 31-04, 31-06   | Folded header-winner-eligibility todo                                                      | ✓ SATISFIED                                        | `carriedMovedPieceIds` carry mechanism verified end-to-end via direct trace and passing chained regression tests.                                            |
| BUG-32                 | 31-03          | GK cannot be selected as an eligible deflection responder                                  | ✓ SATISFIED                                        | Verified unchanged from prior verification; REQUIREMENTS.md `[x]` accurate.                                                                                  |

No orphaned requirement IDs. All 6 plans (31-01 through 31-06) declare only BUG-30, BUG-31, or BUG-32 in their `requirements:` frontmatter — every ID is accounted for above.

### Anti-Patterns Found

None. No TBD/FIXME/XXX/HACK/PLACEHOLDER markers in any file touched by the gap-closure plans (`gameHandlers.ts`, `gameEngine.ts`, `types.ts`, `shotGkRange.test.ts`, `gameEngine.rule11.test.ts`). No stub returns, no hardcoded empty-data anti-patterns.

### Human Verification Required

None. All 5 truths (4 bug-fix truths + 1 documentation-accuracy truth) are programmatically verifiable via direct code inspection, empirical chained-function tracing, and full-suite test execution.

### Gaps Summary

**Both previously-confirmed code gaps are now closed, independently re-verified by direct code reading (not SUMMARY.md trust):**

1. **BUG-30 replay reconstruction is now 5/5 fixed.** All 3 remaining `gameHandlers.ts` GOAL sites (SNAPSHOT_DEFLECT, declared-shot, header-at-goal) now hoist a single `resetPieces` const and feed it to both `state.pieces` and the GOAL event's `piecesAfter` field, exactly mirroring the pattern the 2 `gameEngine.ts` sites already used. `buildReplayFrames` applies this universally. Confirmed via direct read of all 3 sites' full surrounding code (not just grep counts) and via the `shotGkRange.test.ts` regression suite (6/6 passing) which drives each branch through the real socket handler.

2. **The folded header-winner eligibility todo (BUG-31 family) now survives into the Movement Phase.** A new `carriedMovedPieceIds` field on `GameState` is populated by both non-goal branches of `applyResolveHeaderTarget` and merged into `movedPieceIds` by `applyStartMovement` (replacing the prior unconditional `[]` reset), then cleared immediately after so the winner stays spent for exactly one Movement Phase. Traced end-to-end by this verifier (not copied from the SUMMARY's narrative): the winner id is present in `carriedMovedPieceIds` after `applyResolveHeaderTarget` and present in `movedPieceIds` after the subsequent `applyStartMovement` call — the defect from the prior verification's empirical chain no longer reproduces. The pre-existing, explicitly out-of-scope `contestedPieceIds` unconditional-reset defect (CR-01) was confirmed left untouched, as both plans declared.

**Documentation-sync gap (found and closed):** REQUIREMENTS.md line 39/87 still marked BUG-30 as "partial" / "Partial" — stale text left over from before this gap-closure round, since neither 31-05-PLAN.md nor its SUMMARY declared REQUIREMENTS.md as a file to update. This never reflected a code defect. The orchestrator applied the 2-line fix directly (commit `f9fa946`) immediately after this report was generated. All 5 truths are now satisfied; status is `passed`.

---

_Verified: 2026-07-24T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
