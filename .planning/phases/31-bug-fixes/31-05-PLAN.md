---
phase: 31-bug-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/shotGkRange.test.ts
autonomous: true
gap_closure: true
requirements: [BUG-30]

must_haves:
  truths:
    - 'Replaying a goal scored via the SNAPSHOT_DEFLECT GK-out-of-range auto-goal path reconstructs EVERY player at the new kickoff formation, not just the ball (BUG-30 / D-01).'
    - 'Replaying a goal scored via the declared-shot GK-out-of-range auto-goal path reconstructs EVERY player at the new kickoff formation.'
    - 'Replaying a goal scored via the header-at-goal GK-out-of-range auto-goal path reconstructs EVERY player at the new kickoff formation.'
    - "The single resetPieces value used for state.pieces at each of the 3 gameHandlers.ts sites is the exact same array attached as piecesAfter on that site's GOAL event (no divergence, no double buildKickOffPieces call)."
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: "All 3 GK-out-of-range auto-GOAL construction sites hoist a resetPieces const feeding both state.pieces and the GOAL event's piecesAfter field (D-01)"
      contains: 'piecesAfter: resetPieces'
    - path: 'packages/server/src/__tests__/shotGkRange.test.ts'
      provides: 'Regression coverage driving each GK-out-of-range branch through the real socket handler (GAME_SHOT / SNAPSHOT_DEFLECT end-turn / GAME_HEADER_TARGET) and asserting buildReplayFrames reconstructs all pieces at the kickoff formation'
      contains: 'buildReplayFrames'
  key_links:
    - from: 'gameHandlers.ts SNAPSHOT_DEFLECT auto-goal site (~1013-1043)'
      to: 'GOAL event piecesAfter'
      via: 'const resetPieces = buildKickOffPieces(...); pieces: resetPieces; piecesAfter: resetPieces'
      pattern: 'piecesAfter: resetPieces'
    - from: 'gameHandlers.ts declared-shot auto-goal site (~1657-1687)'
      to: 'GOAL event piecesAfter'
      via: 'same resetPieces hoist-and-attach'
      pattern: 'piecesAfter: resetPieces'
    - from: 'gameHandlers.ts header-at-goal auto-goal site (~2438-2467)'
      to: 'GOAL event piecesAfter'
      via: 'same resetPieces hoist-and-attach'
      pattern: 'piecesAfter: resetPieces'
---

<objective>
Close the BUG-30 gap (VERIFICATION.md truth 1, status: partial): 3 of the 5 real GOAL-construction code paths still omit `piecesAfter`, so goals scored via the GK-out-of-range auto-goal branches replay with every player frozen at stale pre-goal positions. Plan 31-01 fixed the 2 `gameEngine.ts` sites and made `piecesAfter` optional; the 3 `gameHandlers.ts` sites (SNAPSHOT_DEFLECT ~line 1013-1043, declared-shot ~line 1657-1687, header-at-goal ~line 2438-2467) were left as a documented "Known Follow-up Gap". This plan applies the identical `resetPieces`-hoist-then-attach pattern to all 3 sites so `buildReplayFrames` reconstructs every piece, and adds regression coverage that drives each branch through the real socket handler.

Purpose: A goal is the single most-important replay moment. Three of five reachable goal paths (all normal "GK badly out of position" outcomes, not edge cases) currently teleport half the team to stale positions in replay — the exact symptom BUG-30 was filed to fix.
Output: `piecesAfter: resetPieces` on all 3 `gameHandlers.ts` GOAL events, each fed by a single hoisted `resetPieces` const; 3 new regression assertions in `shotGkRange.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-bug-fixes/31-CONTEXT.md
@.planning/phases/31-bug-fixes/31-PATTERNS.md
@.planning/phases/31-bug-fixes/31-VERIFICATION.md
@.planning/phases/31-bug-fixes/31-01-SUMMARY.md
</context>

<artifacts_produced>

## Artifacts this phase produces (new symbols — Plan 05)

- **`resetPieces`** — a new local `const` hoisted above each of the 3 GK-out-of-range auto-GOAL `room.gameState = {...}` assignments in `packages/server/src/gameHandlers.ts` (SNAPSHOT_DEFLECT ~line 1013, declared-shot ~line 1657, header-at-goal ~line 2438), computed once via `buildKickOffPieces(newKickOffTeam, <baseState>.selectedTeams, <baseState>.selectedFormation)` and referenced by both `pieces:` and the GOAL event's `piecesAfter:` field.
- New test assertions (and any needed imports: `buildReplayFrames` from `../gameEngine.js`, `buildKickOffPieces` from `@counter-attack/shared`) inside the existing `shotGkRange.test.ts` suite — no new test file.

No new functions, classes, fields, CLI flags, or files are created. `piecesAfter` already exists (optional) on the `GOAL` variant from Plan 31-01; this plan only populates it at the 3 remaining sites.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing replay-reconstruction assertions for all 3 GK-out-of-range auto-goal paths (RED)</name>
  <files>packages/server/src/__tests__/shotGkRange.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/shotGkRange.test.ts — the 3 existing "GK far from every shot-path hex ... auto-resolves to GOAL" tests: regular-shot (`describe('Regular shot (GAME_SHOT): GK-range auto-GOAL gate')`, ~line 159), snapshot (`describe('Snapshot shot: GK-range gate and deterministic SAVE')` first `it`, ~line 253), header (`describe('Header shot: GK-range gate and deterministic SAVE')` first `it`, ~line 356). Reuse its `setupRoom()` helper, `getRoom(roomCode)`, the `oncePromise`/`GAME_STATE` wait pattern, and its `vi.mock('../diceUtils.js')` deterministic dice. Do NOT invent a new harness.
    - packages/server/src/__tests__/replay.integration.test.ts lines 25-35 (the `buildReplayFrames` import from `../gameEngine.js`) and ~504-526 (frame lookup: `buildReplayFrames(room.gameState)` then `frame.pieces.find((p) => p.id === id)!.position`) — mirror this assertion style.
    - packages/server/src/gameHandlers.ts lines 1013-1043, 1657-1687, 2438-2467 (the 3 GOAL literals — confirm none currently carry `piecesAfter`, and note each computes `newKickOffTeam` above the assignment and uses `<baseState>.selectedTeams`/`<baseState>.selectedFormation` for `pieces:`).
    - packages/server/src/gameEngine.ts lines 4686-4698 (buildReplayFrames GOAL branch: applies `event.piecesAfter` only when present) and 4509-4516 (`REPLAY_ELIGIBLE_TYPES` includes `'GOAL'`).
    - .planning/phases/31-bug-fixes/31-01-SUMMARY.md ("Known Follow-up Gap" section) and 31-VERIFICATION.md (gap 1).
  </read_first>
  <behavior>
    - For each of the 3 existing GK-out-of-range auto-goal tests, after the current `state.phase === 'KICK_OFF_SETUP'` + `score.home === 1` assertions pass, re-fetch the authoritative room state via `getRoom(roomCode)!.gameState`, run `buildReplayFrames(...)` on it, and assert the LAST produced frame (the GOAL frame — GOAL is the terminal event in each scenario) has EVERY piece at its new-kickoff-formation position: for each piece id, `frame.pieces.find(...).position` equals the corresponding piece position from `buildKickOffPieces(newKickOffTeam, selectedTeams, selectedFormation)`, where `newKickOffTeam` is the conceding team (`'away'` when home scores).
    - These assertions FAIL against current code: the 3 `gameHandlers.ts` GOAL events omit `piecesAfter`, so `buildReplayFrames` leaves `current.pieces` at the manipulated pre-goal positions (attacker at `{q:30,r:13}`/`{q:33,r:13}`, GK at `{q:5,r:0}`), which do NOT match the kickoff formation.
  </behavior>
  <action>
    Add the `buildReplayFrames` import (from `../gameEngine.js`) and `buildKickOffPieces` import (from `@counter-attack/shared`, alongside the existing shared imports) at the top of `shotGkRange.test.ts`. In each of the 3 GK-out-of-range auto-goal `it` blocks, after the existing terminal assertions, add a replay-reconstruction assertion block: read `const finalRoomState = getRoom(roomCode)!.gameState!`, compute `const expectedPieces = buildKickOffPieces('away', finalRoomState.selectedTeams, finalRoomState.selectedFormation)` (home scores in all 3 scenarios, so the conceding/kickoff team is `'away'`), call `const frames = buildReplayFrames(finalRoomState)`, take the last frame (`frames[frames.length - 1]`), and assert every piece in `expectedPieces` has a matching-position piece in the frame — e.g. iterate `expectedPieces` and assert `frame.pieces.find((p) => p.id === expected.id)!.position` deep-equals `expected.position`. Also add a focused assertion that the previously-manipulated attacker piece is NOT at its stale pre-goal hex in the frame, to make the RED failure unambiguous. Do NOT modify any source file in this task (tests only). Expect all 3 assertions to FAIL (RED). If the snapshot/header scenarios `setupRoom` fixture makes the attacker id awkward to recover after the reset, capture the attacker/GK ids into locals before mutating `room.gameState` (the tests already capture `homeCarrier`/`carrier`/`homeAttacker` and `awayGK`) and reuse them.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test shotGkRange 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - All 3 GK-out-of-range auto-goal tests now contain a `buildReplayFrames`-driven assertion that every piece reconstructs to the `buildKickOffPieces('away', ...)` formation, and each FAILS against current unmodified source (assertion shows at least one piece at a stale non-kickoff position).
    - `git diff --name-only` shows ONLY `packages/server/src/__tests__/shotGkRange.test.ts` changed.
    - Tests remain deterministic (dice already mocked to 3 in this file) — same result on repeat runs.
  </acceptance_criteria>
  <done>shotGkRange.test.ts asserts full-piece replay reconstruction for all 3 GK-out-of-range auto-goal paths; RED confirmed for BUG-30 gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate piecesAfter at all 3 gameHandlers.ts GK-out-of-range GOAL sites via a hoisted resetPieces (GREEN, D-01)</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts lines 1009-1046 (SNAPSHOT_DEFLECT site: base `baseSnapState`, `newKickOffTeam` in scope), 1653-1690 (declared-shot site: base `declaredState`), 2434-2470 (header-at-goal site: base `headerTargetState`). Note `buildKickOffPieces` is already imported at line 63.
    - packages/server/src/gameEngine.ts lines 2128-2181 and 2224-2276 (the 2 reference sites from Plan 31-01: `const resetPieces = buildKickOffPieces(newKickOffTeam, ...); ... pieces: resetPieces ... piecesAfter: resetPieces, // D-01 (BUG-30)`) — mirror this exact pattern.
    - .planning/phases/31-bug-fixes/31-PATTERNS.md (resetPieces-hoist recommendation) and 31-REVIEW.md CR-02 (fix snippet — adapt, do not copy verbatim).
  </read_first>
  <behavior>
    - After this change, each of the 3 sites hoists a single `const resetPieces = buildKickOffPieces(newKickOffTeam, <baseState>.selectedTeams, <baseState>.selectedFormation)` above its `room.gameState = {...}` assignment, uses `pieces: resetPieces` in place of the inline `buildKickOffPieces(...)` call, and adds `piecesAfter: resetPieces` to the GOAL event literal — making Task 1's 3 assertions pass (GREEN) while every other `shotGkRange.test.ts` test (the in-range GK_DIVE/SAVE cases) stays green.
  </behavior>
  <action>
    In `packages/server/src/gameHandlers.ts`, at EACH of the 3 GK-out-of-range auto-GOAL construction sites — SNAPSHOT_DEFLECT (base `baseSnapState`, ~line 1013), declared-shot (base `declaredState`, ~line 1657), and header-at-goal (base `headerTargetState`, ~line 2438): (1) immediately above the `room.gameState = {...}` assignment, add `const resetPieces = buildKickOffPieces(newKickOffTeam, <baseState>.selectedTeams, <baseState>.selectedFormation);` using that site's base-state variable and its already-in-scope `newKickOffTeam`; (2) replace the inline `pieces: buildKickOffPieces(newKickOffTeam, <baseState>.selectedTeams, <baseState>.selectedFormation)` with `pieces: resetPieces,`; (3) add `piecesAfter: resetPieces, // D-01 (BUG-30): reconstruct all pieces at kickoff in replay` as a new line inside that site's `{ type: 'GOAL' as const, ... }` event literal, immediately after its `ballAfter:` line. Compute `resetPieces` exactly once per site (do NOT call `buildKickOffPieces` twice). Do NOT alter any other field, the surrounding `newScore`/`outOfRangeEvent` logic, or the `broadcastState(io, room); return;` tail. Do NOT touch `gameEngine.ts` or `shared/types.ts` — `piecesAfter` is already an optional field on the GOAL variant.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared build && pnpm --filter @counter-attack/server typecheck && pnpm --filter @counter-attack/server test shotGkRange 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "piecesAfter: resetPieces" packages/server/src/gameHandlers.ts` returns 3.
    - `grep -c "pieces: resetPieces" packages/server/src/gameHandlers.ts` returns 3 (the inline `buildKickOffPieces(...)` at each `pieces:` key is replaced by the hoisted const).
    - `grep -c "const resetPieces = buildKickOffPieces" packages/server/src/gameHandlers.ts` returns 3.
    - `pnpm --filter @counter-attack/server typecheck` reports zero errors.
    - All 3 Task-1 replay-reconstruction assertions now PASS; the in-range GK_DIVE/SAVE tests in `shotGkRange.test.ts` still pass.
  </acceptance_criteria>
  <done>All 3 gameHandlers.ts GK-out-of-range GOAL events carry piecesAfter fed from a single hoisted resetPieces; BUG-30 replay reconstruction is closed for all 5 goal paths with regression coverage.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                                                                     |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| server → replay stream | Server-computed replay frames are broadcast to both clients; no untrusted input crosses here (replay is derived purely from the server-authoritative eventLog). |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                        | Disposition | Mitigation Plan                                                                                                                                                                                 |
| --------- | ---------------------- | ---------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-31-07   | Tampering              | GOAL `piecesAfter` reconstruction at the 3 gameHandlers.ts sites | accept      | `piecesAfter` is computed server-side from `buildKickOffPieces` on the server-authoritative state; no client input influences it. Correctness is enforced by the regression tests in this plan. |
| T-31-08   | Information disclosure | replay frames                                                    | accept      | Replay contains only already-broadcast game state (both players saw every action live); no new secret is exposed.                                                                               |
| T-31-SC   | Tampering              | npm/pip/cargo installs                                           | mitigate    | No package installs in this plan — no new dependencies introduced.                                                                                                                              |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared build` succeeds.
- `pnpm --filter @counter-attack/server typecheck` reports zero errors.
- `pnpm --filter @counter-attack/server test shotGkRange` passes, including the 3 new replay-reconstruction assertions and the pre-existing in-range GK_DIVE/SAVE cases.
- Full server suite remains green: `pnpm --filter @counter-attack/server test 2>&1 | tail -20`.
</verification>

<success_criteria>

- Replaying a goal scored via any of the 3 GK-out-of-range auto-goal paths (SNAPSHOT_DEFLECT, declared-shot, header-at-goal) reconstructs every player at the new kickoff formation, proven by regression tests that drive each branch through the real socket handler (BUG-30 gap closed — all 5 goal paths now correct).
- Each site computes `resetPieces` once and uses it for both `state.pieces` and the GOAL event's `piecesAfter` (no divergence, no double computation).
  </success_criteria>

<output>
Create `.planning/phases/31-bug-fixes/31-05-SUMMARY.md` when done. Record `requirements-completed: [BUG-30]` and note this closes the "Known Follow-up Gap" documented in 31-01-SUMMARY.md (the final 3 of 5 GOAL paths).
</output>
