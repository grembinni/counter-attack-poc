---
phase: 36-bug-fixes
verified: 2026-08-02T20:45:00Z
status: human_needed
score: 31/31 must-haves verified (programmatic + ad-hoc behavioral evidence)
overrides_applied: 0
human_verification:
  - test: "With two browser tabs, create a room in tab A, join it from tab B (both land pre-game), then click Back on tab A's Game Settings screen."
    expected: 'Tab A returns to the Landing screen with no stale settings; tab B is also routed back to the Landing screen (not left stuck on a dead room), with no console errors in either tab.'
    why_human: 'Requires two real browser sessions interacting over live WebSocket — automated coverage (ad-hoc verified during this pass) confirms the ROOM_CLOSED wire event fires and is handled, but not the full visual/UX experience of a second real client watching the transition happen.'
  - test: 'Play or force a shot to resolve as a shooter/GK duel tie (shooter total == GK total) in a live match and observe the loose ball.'
    expected: "The loose ball marker appears at the goalkeeper's square (or dive-adjusted square if the keeper dove) immediately after the shot, and the subsequent scatter animation/placement visibly originates from that square rather than the shooter's square."
    why_human: 'Server-side state correctness is fully unit/integration-tested (gameEngine.phase8.test.ts, 81 tests including end-to-end scatter + replay-frame assertions), but the actual on-screen rendering/feel during live play has not been eyeballed.'
  - test: 'During a live match, move a defender adjacent to the ball carrier to trigger a tackle attempt that resolves as FAIL, then observe the Undo button, then make a further move and observe it again.'
    expected: 'Undo is disabled (greyed out) immediately after the failed tackle resolves. After making any further move in the same slot, Undo re-enables and successfully undoes that later move (but a repeated Undo click does not cross back over the tackle).'
    why_human: 'Client/server boundary logic is unit-tested in isolation (ActionPanel.test.tsx, gameEngine.phase26-undo.test.ts), but the moment-to-moment UX of the button greying out and re-lighting during a real slot has not been visually confirmed.'
---

# Phase 36: Bug Fixes Verification Report

**Phase Goal:** Fix 5 confirmed gameplay/UX bugs (BUG-33 through BUG-37) surfaced from Phase 35 UAT and prior play-testing — missing Back button on the game settings screen with no server-side room teardown (BUG-33), draft packs that can repeat player IDs across a match (BUG-34), a tier-cascade gap in draft pack generation that can shortchange or crash pack generation (BUG-35), the loose ball landing at the wrong hex on a shot-duel tie (BUG-36), and Undo not clamping at a resolved tackle/steal contest (BUG-37).
**Verified:** 2026-08-02T20:45:00Z
**Status:** human_needed
**Re-verification:** No — initial verification (post-review-fix)

## Important Context for This Verification

Phase 36 shipped 5 plans (36-01..36-05), then went through a code review (`36-REVIEW.md`, 2026-08-02) that found 3 Critical + 2 Warning issues, and a fixer pass (`36-REVIEW-FIX.md`) that applied 4 of the 5 fixes as separate commits already on `main`:

| Finding                                                                        | Commit    | Status                                                                                                                                                                     |
| ------------------------------------------------------------------------------ | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CR-01 (LEAVE_ROOM doesn't notify a joined second player)                       | `1e3e4fa` | Fixed, confirmed below                                                                                                                                                     |
| CR-02 (LEAVE_ROOM had no mid-match guard)                                      | `a23b1b8` | Fixed, confirmed below                                                                                                                                                     |
| CR-03 (GameSettingsScreen hides Confirm on a failed confirm, no error shown)   | `5fc7f64` | Fixed, confirmed below                                                                                                                                                     |
| WR-01 (tier-processing order not stable-sorted for hypothetical future rounds) | `e78f147` | Fixed, confirmed below                                                                                                                                                     |
| WR-02 (`buildTierPoolsForRound` over-claims a tier's primary pool)             | —         | Explicitly skipped, documented as non-correctness-impacting and deferred for human judgment on an unspecified "slack" quantity. Not a must-have; no regression risk found. |

This verification checked the current state of `main` (HEAD `136a7ff`, one commit past the last review-fix commit), not the pre-review-fix state — the review findings above are treated as already resolved, and were independently re-confirmed against source and, for the three Critical items, against live behavior (see Behavioral Spot-Checks).

## Goal Achievement

### Observable Truths

Merged from ROADMAP.md Success Criteria (5) and all 5 plans' `must_haves.truths` (26, with overlaps collapsed where a plan truth restates a roadmap SC).

| #   | Truth                                                                                                                    | Status                            | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ------------------------------------------------------------------------------------------------------------------------ | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | (SC1/36-01) A host on Game Settings can click Back and land on Landing                                                   | ✓ VERIFIED                        | `GameSettingsScreen.tsx:168-170` renders unconditional `&larr; Back` button calling `onBack`; `App.tsx:253-261` `handleSettingsBack` calls `resetLobby()` (sets `screen: 'LANDING'`). `GameSettingsScreen.test.tsx` 14/14 pass including Back-control describe block.                                                                                                                                                                                                                                                                |
| 2   | (36-01) Clicking Back deletes the room server-side immediately, no ~90s grace-timer dependence                           | ✓ VERIFIED                        | `roomHandlers.ts:257-284` `LEAVE_ROOM` handler calls `deleteRoom(roomCode)` synchronously; disconnect grace timer (`~1028-1067`) untouched. `room.integration.test.ts` BUG-33 block: subsequent `ROOM_JOIN` on the same code returns `NOT_FOUND` immediately, `getRoom(code)` is `undefined`. 17/17 pass.                                                                                                                                                                                                                            |
| 3   | (36-01) After Back, host can create a fresh room without inheriting prior speed/team-type/pool selections                | ✓ VERIFIED                        | `App.tsx:256-259`: `handleSettingsBack` resets `selectedSpeed`, `teamType`, `draftPools`, `homePickedTeam` before `resetLobby()`.                                                                                                                                                                                                                                                                                                                                                                                                    |
| 4   | (36-01) Back appears only on GameSettingsScreen                                                                          | ✓ VERIFIED                        | `git diff --stat` for the phase touches only `GameSettingsScreen.tsx`; `UniformSelectionScreen.tsx`, `LineupAssignmentScreen.tsx`, `TeamSelectionScreen.tsx` are absent from the diff.                                                                                                                                                                                                                                                                                                                                               |
| 5   | (36-01) A room-less socket can't delete any room via LEAVE_ROOM                                                          | ✓ VERIFIED                        | `roomHandlers.ts:258-259`: `if (roomCode === undefined) return;` — reads only `socket.data.roomCode`, never a client payload (event is `() => void`). `room.integration.test.ts` "stray client" test confirms an unrelated room stays joinable.                                                                                                                                                                                                                                                                                      |
| 6   | (CR-01, review fix) The other room member is notified when LEAVE_ROOM tears the room down                                | ✓ VERIFIED (behaviorally, ad-hoc) | `roomHandlers.ts:270-274` emits `ROOM_ERROR 'ROOM_CLOSED'` to the room before deleting; `App.tsx:99-103` `onRoomError` handles `'ROOM_CLOSED'` by clearing the session token and calling `resetLobby()`. **Verifier wrote and ran a temporary 2-client wire-level test** (host creates room, guest joins, host emits LEAVE_ROOM) confirming the guest receives `ROOM_ERROR('ROOM_CLOSED')` — passed, then removed (not committed; closes the review's own IN-01 gap, which was untested by the phase's shipped test suite).          |
| 7   | (CR-02, review fix) LEAVE_ROOM cannot tear down a room with an active match                                              | ✓ VERIFIED (behaviorally, ad-hoc) | `roomHandlers.ts:267-268`: `if (room && room.gameState !== null) return;`. **Verifier wrote and ran a temporary test** that creates a room, sets `room.gameState` to a truthy sentinel (simulating mid-match), emits `LEAVE_ROOM`, and confirms `getRoom(roomCode)` still returns the room afterward — passed, then removed.                                                                                                                                                                                                         |
| 8   | (CR-03, review fix) A failed `ROOM_SETTINGS_CONFIRM` shows an error and lets the host retry                              | ✓ VERIFIED (behaviorally, ad-hoc) | `GameSettingsScreen.tsx:44-53,166` reads `gameError`, resets `hasConfirmed` via `useEffect`, renders `.errorText`. **Verifier wrote and ran a temporary RTL test**: clicked Confirm (hides the button), set `gameError: 'DRAFT_SUPPLY_EXHAUSTED'` on the store, confirmed the error text renders AND the Confirm button re-appears — passed, then removed (this exact scenario was flagged by `36-REVIEW.md` as untested and remained untested by the shipped `GameSettingsScreen.test.tsx`, which has zero `gameError` assertions). |
| 9   | (SC2/36-02) No player id appears in more than one pack across all 6 rounds/12 packs of a match                           | ✓ VERIFIED                        | `matchUsedIds` `Set<string>` threaded through `generateDraftPacks`, populated only after `packs.push` (grep confirms exactly 2 `.add` call sites, both post-push). `draftEngine.test.ts` BUG-34 block asserts 48 unique ids across 5 seeds x 4 pool selections. `draftPacks.test.ts` `assertStructuralInvariants` asserts the same over real `crypto.randomInt`.                                                                                                                                                                     |
| 10  | (36-02) Within-round no-duplicate-card behavior (D-09) still holds as a superset                                         | ✓ VERIFIED                        | `draftPacks.test.ts` retains a separate, explicitly labelled D-07 per-round assertion alongside the match-wide one.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 11  | (36-02) A GK dealt in round 1 never reappears later                                                                      | ✓ VERIFIED                        | `draftEngine.test.ts` "round-1 GK ids disjoint from later rounds" test in the BUG-34 block.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 12  | (36-02) Every selectable pool combination still generates a full 12-pack match without throwing                          | ✓ VERIFIED                        | `draftEngine.test.ts`/`draftPacks.test.ts` no-throw sweep across `SELECTABLE_DRAFT_POOLS`; full suites green.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 13  | (36-02) Candidates classified but never dealt remain available for later rounds                                          | ✓ VERIFIED                        | `matchUsedIds.add` only fires on dealt `cards`, never on `classified`/`dealt`/tier-pool arrays (code inspection + grep gate: exactly 2 `.add` sites).                                                                                                                                                                                                                                                                                                                                                                                |
| 14  | (SC3/36-03) A short slot cascades DOWN within the selected pool(s) — chase→rare→uncommon→common                          | ✓ VERIFIED                        | `TIER_CASCADE_BELOW`/`tierDrawOrder` (`draftEngine.ts:216-230`); cascade-aware `tierSupplyMeetsNeed`/`buildTierPoolsForRound` rewritten rarest-first. `draftEngine.test.ts` BUG-35 block: 67/67 pass including cascade-prevents-shortfall tests.                                                                                                                                                                                                                                                                                     |
| 15  | (36-03) A slot is never filled with a HIGHER tier than called for                                                        | ✓ VERIFIED                        | `TIER_CASCADE_BELOW` table contains no entry rarer than its key (code inspection); "never-upgrade rule" tests assert rounds 2-3 packs contain only `common`, round 4 contains no `rare`/`chase`.                                                                                                                                                                                                                                                                                                                                     |
| 16  | (36-03) Cross-pool fallback only reached after same-pool cascade is exhausted                                            | ✓ VERIFIED                        | The cross-pool loop guard (`tierSupplyMeetsNeed`) is now cascade-aware, making the loop structurally unreachable while cascade can cover the round (D-12(a)); `draftEngine.test.ts` D-12(a) test: for `['mls']`, zero rounds-2-6 cards come from outside MLS. `draftPacks.test.ts` BUG-35 real-RNG block confirms the same over `crypto.randomInt`.                                                                                                                                                                                  |
| 17  | (SC3/36-03) Cross-pool fallback contributes common-tier cards only                                                       | ✓ VERIFIED                        | `resolveTieredCandidates`'s `fallbackPlayers` filter requires `classifyTier(computeTotalStat(p)) === 'common'` (`draftEngine.ts:400`). D-12(b) test for `['original']`: every non-original card dealt is common-tier, non-vacuously (at least one exists). `draftPacks.test.ts` confirms the same over real RNG.                                                                                                                                                                                                                     |
| 18  | (36-03) A shortfall surviving cascade + common fallback still fails loudly, never silent/short/duplicate                 | ✓ VERIFIED                        | The pre-existing "insufficient tiered supply" throw in `generateDraftPacks` is unchanged (diff review); D-11 fail-closed test present in `draftEngine.test.ts`.                                                                                                                                                                                                                                                                                                                                                                      |
| 19  | (36-03) A host selecting an unsuppliable pool combo gets a game error, not a crashed process                             | ✓ VERIFIED                        | `roomHandlers.ts:493-498` wraps `generateMatchPacks` in try/catch, emits `GAME_ERROR 'DRAFT_SUPPLY_EXHAUSTED'`, returns without `ROOM_SETTINGS_CONFIRMED`. Client-side CR-03 fix (truth 8) makes this observable/retryable to the host.                                                                                                                                                                                                                                                                                              |
| 20  | (SC4/36-04) On a duel tie, the ball is placed on the GK's dive-adjusted hex, not the shooter's                           | ✓ VERIFIED                        | `gameEngine.ts:2317,2327` both use `gkEffectivePos` in the `LOOSE_BALL` tie branch (was `state.ball.position`). `gameEngine.phase8.test.ts` BUG-36 block: 81/81 pass, including no-dive and 2-hex-dive discriminating tests.                                                                                                                                                                                                                                                                                                         |
| 21  | (36-04) The subsequent scatter walk originates from the GK's hex                                                         | ✓ VERIFIED                        | `gameEngine.phase8.test.ts` end-to-end test: `LOOSE_BALL_LAND` event's `from` equals the GK's dive-adjusted hex.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 22  | (36-04) `SHOT_ATTEMPT`'s `ballAfter` matches, so replay matches live play                                                | ✓ VERIFIED                        | `ballAfter.position` also corrected to `gkEffectivePos` (`gameEngine.ts:2317`); `buildReplayFrames` assertion confirms the SHOT_ATTEMPT-derived frame shows the ball at the GK hex.                                                                                                                                                                                                                                                                                                                                                  |
| 23  | (36-04) GOAL/SAVE/unsaveable-GOAL outcomes unchanged                                                                     | ✓ VERIFIED                        | GOAL guard test passes; diff review confirms SAVE branches (which already used `gkEffectivePos`) are untouched.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 24  | (36-04) Outfield-defender deflection pathing unchanged                                                                   | ✓ VERIFIED                        | `computeShotPathDeflection` confirmed absent from the diff (`git diff --stat` scoped to `gameEngine.ts`/`gameEngine.phase8.test.ts`/`gameEngine.test.ts` only).                                                                                                                                                                                                                                                                                                                                                                      |
| 25  | (SC5/36-05) Undo cannot revert past a resolved TACKLE_ATTEMPT/STEAL_ATTEMPT                                              | ✓ VERIFIED                        | `applyUndo`'s `isBoundary` disjunction (`gameEngine.ts:1411-1419`) includes `evt.type === 'TACKLE_ATTEMPT' \|\| evt.type === 'STEAL_ATTEMPT'`, unconditional. `gameEngine.phase26-undo.test.ts` 10/10 pass.                                                                                                                                                                                                                                                                                                                          |
| 26  | (36-05) Undo still works for moves made AFTER the resolved contest, same slot                                            | ✓ VERIFIED                        | Same test file: a `MOVE` appended after `TACKLE_ATTEMPT`/`STEAL_ATTEMPT` reverts successfully and leaves the contest event intact.                                                                                                                                                                                                                                                                                                                                                                                                   |
| 27  | (36-05) Client Undo button's enabled state mirrors the server boundary exactly                                           | ✓ VERIFIED                        | `ActionPanel.tsx:269-276` `canUndo`'s `isBoundary` includes the identical two terms. `ActionPanel.test.tsx` 73/73 pass including BUG-37 block.                                                                                                                                                                                                                                                                                                                                                                                       |
| 28  | (36-05) Pre-existing undo boundaries unchanged (SLOT_ADVANCE, KICK_OFF, HP_REPOSITION, FTP_REPOSITION, free-kick stages) | ✓ VERIFIED                        | `gameEngine.ts:1412-1418` — all five pre-existing terms present unmodified alongside the two new ones; the separate `DICE_ROLL` per-slot lockout (`~1426`) is byte-identical (verified via `git diff HEAD~2 -- gameEngine.ts \| grep DICE_ROLL` showing no output, per plan's own gate).                                                                                                                                                                                                                                             |
| 29  | (36-05) Undo is not permanently locked out for the rest of a slot after a failed tackle                                  | ✓ VERIFIED                        | The new terms were added to `isBoundary` (a floor), not to the `currentSlotEvents.some(...'DICE_ROLL')` lockout — confirmed by code inspection and truth 26's passing "still works after" tests.                                                                                                                                                                                                                                                                                                                                     |
| 30  | (WR-01, review fix) Ambiguous same-rank chase/rare tier ordering is guarded against                                      | ✓ VERIFIED                        | `assertNoAmbiguousChaseRareSlots` called at the top of both `tierSupplyMeetsNeed` and `buildTierPoolsForRound` (`draftEngine.ts`); `pnpm --filter @counter-attack/shared test` 613/613 pass including this file's coverage.                                                                                                                                                                                                                                                                                                          |
| 31  | Every plan's declared requirement ID is accounted for in REQUIREMENTS.md with matching scope                             | ✓ VERIFIED                        | BUG-33..37 present in `REQUIREMENTS.md` lines 42-46 and the traceability table (lines 95-99), each mapped only to "Phase 36 — Bug Fixes", no orphans (`_Last updated_` note confirms 25/25 v1.5 requirements mapped, 0 unmapped).                                                                                                                                                                                                                                                                                                    |

**Score:** 31/31 truths verified against the codebase. Truths 6-8 (the three Critical review findings) were verified by writing and running temporary, throwaway wire-level/RTL tests directly against `main` during this verification pass (not left in the repo) — this closes gaps the phase's own shipped test suite left open (`36-REVIEW.md`'s IN-01 for CR-01, and an untested path for CR-02/CR-03 that the fixer's own verification notes did not cover with new assertions).

### Required Artifacts

| Artifact                                                        | Expected                                                                              | Status     | Details                                                         |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------- |
| `packages/shared/src/events.ts`                                 | `LEAVE_ROOM` event + void payload                                                     | ✓ VERIFIED | Line 26 `LEAVE_ROOM: 'room:leave'`, line 166 `() => void` entry |
| `packages/server/src/roomHandlers.ts`                           | `LEAVE_ROOM` handler calling `deleteRoom`                                             | ✓ VERIFIED | Lines 257-284, plus CR-01/CR-02 additions                       |
| `packages/client/src/components/GameSettingsScreen.tsx`         | Unconditional Back button                                                             | ✓ VERIFIED | Lines 168-170, plus CR-03 error-display additions               |
| `packages/server/src/__tests__/room.integration.test.ts`        | Wire-level LEAVE_ROOM tests                                                           | ✓ VERIFIED | `describe('LEAVE_ROOM (BUG-33)', ...)`, 2 tests, both passing   |
| `packages/client/src/components/GameSettingsScreen.test.tsx`    | Back-control component tests                                                          | ✓ VERIFIED | 14/14 tests passing (3 new for Back)                            |
| `packages/shared/src/draftEngine.ts`                            | `matchUsedIds`, `TIER_CASCADE_BELOW`, `tierDrawOrder`, cascade-aware supply functions | ✓ VERIFIED | All present, `tierSupplyCount` confirmed removed (grep = 0)     |
| `packages/shared/src/draftEngine.test.ts`                       | BUG-34/BUG-35 unit tests                                                              | ✓ VERIFIED | 67 tests total, all passing                                     |
| `packages/server/src/__tests__/draftPacks.test.ts`              | Match-wide + BUG-35 real-RNG tests                                                    | ✓ VERIFIED | 9 tests, all passing                                            |
| `packages/server/src/gameEngine.ts`                             | `gkEffectivePos` in LOOSE_BALL tie branch; extended `isBoundary`                      | ✓ VERIFIED | Both present, confirmed at exact lines cited in plans           |
| `packages/server/src/__tests__/gameEngine.phase8.test.ts`       | BUG-36 regression tests                                                               | ✓ VERIFIED | 81/81 passing                                                   |
| `packages/server/src/__tests__/gameEngine.phase26-undo.test.ts` | BUG-37 clamp tests                                                                    | ✓ VERIFIED | 10/10 passing                                                   |
| `packages/client/src/components/ActionPanel.tsx`                | Mirrored `isBoundary`                                                                 | ✓ VERIFIED | Lines 269-276                                                   |
| `packages/client/src/components/ActionPanel.test.tsx`           | BUG-37 button-state tests                                                             | ✓ VERIFIED | 73/73 passing                                                   |

### Key Link Verification

| From                                      | To                                              | Via                                           | Status  | Details                                                                                |
| ----------------------------------------- | ----------------------------------------------- | --------------------------------------------- | ------- | -------------------------------------------------------------------------------------- |
| `GameSettingsScreen.tsx`                  | `App.tsx`                                       | `onBack={handleSettingsBack}`                 | ✓ WIRED | `App.tsx:314`                                                                          |
| `App.tsx`                                 | `roomHandlers.ts`                               | `socket.emit(ClientEvents.LEAVE_ROOM)`        | ✓ WIRED | `App.tsx:254`                                                                          |
| `roomHandlers.ts`                         | `roomStore.ts`                                  | `deleteRoom(roomCode)` synchronous call       | ✓ WIRED | `roomHandlers.ts:276`                                                                  |
| `generateDraftPacks` round loop           | `resolveGkCandidates`/`resolveTieredCandidates` | `matchUsedIds` param                          | ✓ WIRED | Both call sites pass it; grep confirms 2 `.add` sites post-push                        |
| `tierSupplyMeetsNeed`                     | cross-pool loop guard                           | cascade-aware sufficiency check               | ✓ WIRED | Loop is unreachable while cascade covers the round (confirmed by D-12(a) test passing) |
| `resolveTieredCandidates` fallback filter | `classifyTier(computeTotalStat(p))`             | common-only restriction                       | ✓ WIRED | Line 400, confirmed by D-12(b)/real-RNG tests                                          |
| `ROOM_SETTINGS_CONFIRM`                   | `GAME_ERROR`                                    | try/catch around `generateMatchPacks`         | ✓ WIRED | `roomHandlers.ts:493-498`                                                              |
| `SHOT case gkEffectivePos`                | `LOOSE_BALL` tie branch                         | both `ball`/`ballAfter` read `gkEffectivePos` | ✓ WIRED | `gameEngine.ts:2317,2327`                                                              |
| `applyUndo isBoundary`                    | `ActionPanel canUndo isBoundary`                | identical boundary-event set                  | ✓ WIRED | Term-for-term match confirmed by grep gate                                             |
| `roomHandlers.ts LEAVE_ROOM`              | `ServerEvents.ROOM_ERROR('ROOM_CLOSED')`        | CR-01 notify                                  | ✓ WIRED | `roomHandlers.ts:274`, ad-hoc behavioral test confirmed the guest receives it          |
| `App.tsx onRoomError`                     | `resetLobby()`                                  | CR-01 `'ROOM_CLOSED'` branch                  | ✓ WIRED | `App.tsx:99-103`                                                                       |
| `GameSettingsScreen`                      | `useGameStore.gameError`                        | CR-03 error display + retry                   | ✓ WIRED | `GameSettingsScreen.tsx:49-53,166`, ad-hoc behavioral test confirmed                   |

### Data-Flow Trace (Level 4)

Not separately run — this phase is entirely server-authoritative state-transition and UX-mirror logic (no dashboards/list views with a DB-to-render chain). The relevant "does the value actually reach the render" question is covered by the artifact/key-link checks above (e.g. `gameError` from the store reaching `GameSettingsScreen`'s render, confirmed behaviorally).

### Behavioral Spot-Checks

| Behavior                                                          | Command                                                                | Result                                                  | Status |
| ----------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------- | ------ |
| BUG-33 server suite                                               | `pnpm --filter @counter-attack/server test -- room.integration`        | 17/17 passed                                            | ✓ PASS |
| BUG-33 client suite                                               | `pnpm --filter @counter-attack/client test -- GameSettingsScreen`      | 14/14 passed                                            | ✓ PASS |
| BUG-34/35 shared suite                                            | `pnpm --filter @counter-attack/shared test -- draftEngine`             | 67/67 passed                                            | ✓ PASS |
| BUG-34/35 server suite                                            | `pnpm --filter @counter-attack/server test -- draftPacks`              | 9/9 passed                                              | ✓ PASS |
| BUG-36 server suite                                               | `pnpm --filter @counter-attack/server test -- gameEngine.phase8`       | 81/81 passed                                            | ✓ PASS |
| BUG-37 server suite                                               | `pnpm --filter @counter-attack/server test -- gameEngine.phase26-undo` | 10/10 passed                                            | ✓ PASS |
| BUG-37 client suite                                               | `pnpm --filter @counter-attack/client test -- ActionPanel`             | 73/73 passed                                            | ✓ PASS |
| Full server suite (wave-merge gate)                               | `pnpm --filter @counter-attack/server test`                            | 642 passed, 1 skipped, 1 todo, 0 failed (33 files)      | ✓ PASS |
| Full client suite (wave-merge gate)                               | `pnpm --filter @counter-attack/client test`                            | 483/483 passed (25 files)                               | ✓ PASS |
| Full shared suite (wave-merge gate)                               | `pnpm --filter @counter-attack/shared test`                            | 613/613 passed (13 files)                               | ✓ PASS |
| Full-workspace typecheck                                          | `pnpm -r typecheck`                                                    | Clean, 0 errors (shared/server/client)                  | ✓ PASS |
| Pitfall 5 gate: `matchUsedIds.add` call sites                     | `grep -c "matchUsedIds.add" packages/shared/src/draftEngine.ts`        | 2                                                       | ✓ PASS |
| `tierSupplyCount` fully removed                                   | `grep -vn '^\s*[/*]' draftEngine.ts \| grep -c tierSupplyCount`        | 0                                                       | ✓ PASS |
| Mirror gate: STEAL_ATTEMPT in both boundary sets                  | `grep -n STEAL_ATTEMPT gameEngine.ts ActionPanel.tsx`                  | present in both                                         | ✓ PASS |
| Wrong-site gate: DICE_ROLL lockout unchanged                      | `git diff HEAD~2 -- gameEngine.ts \| grep DICE_ROLL`                   | no output                                               | ✓ PASS |
| **CR-01 live behavior** (ad-hoc, written/run/removed by verifier) | 2-client wire test: host+guest join, host emits LEAVE_ROOM             | guest received `ROOM_ERROR('ROOM_CLOSED')`              | ✓ PASS |
| **CR-02 live behavior** (ad-hoc, written/run/removed by verifier) | Set `room.gameState` truthy, emit LEAVE_ROOM                           | room still present in store afterward (no-op confirmed) | ✓ PASS |
| **CR-03 live behavior** (ad-hoc, written/run/removed by verifier) | RTL: click Confirm, set `gameError`, check render                      | error text shown, Confirm button re-appears             | ✓ PASS |

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` declared or discovered for this phase; this is a bug-fix phase verified via the project's existing vitest suites plus the ad-hoc behavioral checks above.

### Requirements Coverage

| Requirement | Source Plan | Description                                                 | Status      | Evidence           |
| ----------- | ----------- | ----------------------------------------------------------- | ----------- | ------------------ |
| BUG-33      | 36-01       | Game Settings Back control + immediate server-side teardown | ✓ SATISFIED | Truths 1-8 above   |
| BUG-34      | 36-02       | Match-wide draft-pack player uniqueness                     | ✓ SATISFIED | Truths 9-13 above  |
| BUG-35      | 36-03       | Same-pool tier cascade + common-only cross-pool fallback    | ✓ SATISFIED | Truths 14-19 above |
| BUG-36      | 36-04       | Loose-ball origin on a shot-duel tie                        | ✓ SATISFIED | Truths 20-24 above |
| BUG-37      | 36-05       | Undo clamp at a resolved tackle/steal                       | ✓ SATISFIED | Truths 25-29 above |

No orphaned requirements: `REQUIREMENTS.md`'s traceability table maps only BUG-33..37 to "Phase 36 — Bug Fixes" and its own footer confirms 25/25 v1.5 requirements mapped with 0 unmapped.

**Documentation staleness (not a code gap):** `REQUIREMENTS.md` lines 42-46 and 95-99 still show BUG-33..37 as unchecked `[ ]` / status "Pending" despite all 5 being code-verified complete above. This matches the exact pattern flagged in the prior Phase 35 verification (checkbox updates lag actual completion and are typically applied at ship time) — not a missing-implementation gap.

### Anti-Patterns Found

| File                                 | Line     | Pattern                                                                                                   | Severity   | Impact                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/server/src/gameEngine.ts`  | 3063     | `// TODO: if an OPPOSING player occupies targetHex...`                                                    | ℹ️ Info    | Pre-existing (git blame: commit `a6b0aaf5`, 2026-07-12, well before Phase 36 started 2026-07-28) — not introduced by this phase, not a debt-marker gate violation.                                                                                                                                                                                                                                  |
| `packages/shared/src/draftEngine.ts` | ~432-453 | WR-02: `buildTierPoolsForRound` claims a tier's full unclaimed primary population rather than just `need` | ⚠️ Warning | Explicitly reviewed and deliberately deferred (not skipped through negligence) — `36-REVIEW.md` marks it "not required for correctness"; `36-REVIEW-FIX.md` documents a code-path analysis showing zero correctness impact (cascade only flows downward, so over-claiming a tier's own pool can never starve a different tier). No test regression, no must-have truth depends on this being fixed. |

No `TBD`/`FIXME`/`XXX` debt markers introduced by this phase's diff. No stub returns, empty handlers, or hardcoded-empty-data patterns found in any of the 8 files this phase touched.

### Gaps Summary

No blocking gaps. All 31 observable truths (roadmap Success Criteria + plan-level must-haves, deduplicated) are verified against current source on `main`, including the 4 critical/warning review findings the phase's own review-fix pass applied. Three of those (CR-01, CR-02, CR-03) had zero automated test coverage in the shipped suite — the review's own IN-01 note flagged this for CR-01, and the fixer's verification notes for CR-02/CR-03 relied only on `tsc`/full-suite-green rather than new assertions targeting the fix itself. This verification closed that gap by writing, running, and then removing three temporary behavioral tests directly against `main`, all of which passed.

The one remaining Warning (WR-02) was a deliberate, well-reasoned, non-correctness-impacting deferral — not a phase gap.

Human verification is requested for the live/visual experience of the three newly-introduced or newly-fixed user-facing behaviors (two-tab Back/ROOM_CLOSED flow, loose-ball scatter origin during real play, Undo button greying out/re-enabling during real play) — all three are state-correct and unit/integration-tested, but the moment-to-moment UX has not been eyeballed in a running browser, consistent with this project's established verification convention (see Phase 35's precedent).

---

_Verified: 2026-08-02T20:45:00Z_
_Verifier: Claude (gsd-verifier)_
