---
phase: 38-corner-kick
plan: 13
type: execute
wave: 10
depends_on: [38-11, 38-12]
gap_closure: true
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.cornerKick.test.ts
  - packages/server/src/__tests__/gameHandlers.cornerKick.test.ts
  - packages/client/src/components/CornerKickSetupPanel.tsx
  - packages/client/src/components/CornerKickSetupPanel.test.tsx
autonomous: true
requirements: [CORNER-04]
must_haves:
  truths:
    - 'An accurate Low Pass corner aimed at or past a defender in the pass path runs the interception loop'
    - 'A defender occupying the Low Pass corner target hex auto-intercepts and takes possession'
    - 'A ZoI defender adjacent to the Low Pass corner path rolls for interception on the existing die-6-or-combined-10 threshold'
    - 'An accurate High Pass corner is still never interceptable and still routes into the HEADER contest'
    - 'An intercepted corner tears down every cornerKick state field so the next Standard Pass is not accuracy-gated'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'High-only interception bypass for corners plus CORNER_KICK_TEARDOWN on both interception return paths'
      contains: 'CORNER_KICK_TEARDOWN'
    - path: 'packages/server/src/__tests__/gameEngine.cornerKick.test.ts'
      provides: 'Engine assertions for Low-interceptable / High-non-interceptable and post-interception teardown'
      contains: 'autoIntercept'
    - path: 'packages/client/src/components/CornerKickSetupPanel.tsx'
      provides: 'Low Pass button tooltip corrected to state the pass is interceptable'
      contains: 'Low Pass'
  key_links:
    - from: 'packages/server/src/gameEngine.ts applyRoll PASS case'
      to: 'packages/shared/src/passValidator.ts validatePass'
      via: 'the STANDARD passType branch already populating autoIntercepts/rollIntercepts for a corner Low pass'
      pattern: 'rollIntercepts'
    - from: 'packages/server/src/gameHandlers.ts GAME_ROLL'
      to: 'packages/server/src/gameEngine.ts applyRoll'
      via: 'preGeneratedInterceptionDice seeded for rollIntercepts before applyRoll consumes them'
      pattern: 'preGeneratedInterceptionDice'
---

<objective>
Correct rulebook Assumption A2: a Low Pass corner IS interceptable. A High Pass corner stays
non-interceptable and continues to resolve through the existing header contest.

Purpose: gap-closure item 1 from `38-09-SUMMARY.md`. The human verifier returned A2 as
**CORRECTED (partial)** — the implementation currently treats both corner options as
non-interceptable via a single `state.cornerKickTeam != null` bypass at `gameEngine.ts:2184`,
which is right for High (a corner flies into the box) and wrong for Low (a grounded pass a
defender can step in front of).

Output: the interception bypass narrowed to High-only, `CORNER_KICK_TEARDOWN` added to the two
interception return paths that corners can now reach, tests proving both halves of A2, and the
Low Pass button tooltip corrected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-corner-kick/38-CONTEXT.md
@.planning/phases/38-corner-kick/38-RESEARCH.md
@.planning/phases/38-corner-kick/38-UI-SPEC.md
@.planning/phases/38-corner-kick/38-09-SUMMARY.md
</context>

## Artifacts this phase produces

- `isCornerKick` in `applyRoll`'s PASS case is replaced by a High-only guard (renamed to `isCornerKickHighDelivery`) (`packages/server/src/gameEngine.ts`)
- `CORNER_KICK_TEARDOWN` spread added to the `autoIntercepts` return and the `rollIntercepts` return in `applyRoll`'s PASS case (`packages/server/src/gameEngine.ts`)
- New test names in `packages/server/src/__tests__/gameEngine.cornerKick.test.ts`:
  - `accurate Low corner: a defender on the target hex auto-intercepts and takes possession`
  - `accurate Low corner: a ZoI defender adjacent to the path rolls for interception`
  - `accurate Low corner: a failed interception roll still delivers the ball`
  - `accurate High corner: an adjacent defender never triggers an interception and the phase becomes HEADER`
  - `intercepted Low corner: every cornerKick state field is torn down`
- New test name in `packages/server/src/__tests__/gameHandlers.cornerKick.test.ts`:
  - `CORNER-04: a Low corner aimed past a ZoI defender seeds preGeneratedInterceptionDice`
- Corrected Low Pass button `title` text in `packages/client/src/components/CornerKickSetupPanel.tsx`

## Gap-closure source audit

| Source                       | Item                                                                               | Covered by |
| ---------------------------- | ---------------------------------------------------------------------------------- | ---------- |
| 38-09-SUMMARY item 1         | Low Pass corners must be interceptable (A2 correction)                             | Task 1     |
| 38-09-SUMMARY item 1         | High Pass corners stay non-interceptable via the header contest                    | Task 1     |
| 38-09-SUMMARY A2 verdict     | Record the corrected verdict at the `Assumption A2` source-comment site            | Task 1     |
| Newly-reachable code path    | Interception returns lacked `CORNER_KICK_TEARDOWN` (corners never hit them before) | Task 2     |
| 38-UI-SPEC / 38-CONTEXT D-09 | Panel copy must describe the real rule                                             | Task 3     |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow the corner interception bypass to High Pass only</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.cornerKick.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 1930-2110 (the PASS case up to the accurate path: `requiresAccuracyCheck`, the corner inaccurate branch with `CORNER_KICK_TEARDOWN`, `passTypeMap`/`validatePassType`, the `validatePass` call, `autoIntercepts`/`rollIntercepts`, and the `newLastActionType` derivation at lines 2103-2109)
    - packages/server/src/gameEngine.ts lines 2170-2270 (the three bypass flags `isHeaderPass` / `isFirstTimePass` / `isCornerKick`, the guard at line 2185, and both interception return blocks)
    - packages/server/src/gameEngine.ts lines 2371-2511 (the delivery returns, all of which already spread `CORNER_KICK_TEARDOWN`, and the HIGH_PASS-to-HEADER transition at 2422-2484)
    - packages/server/src/gameEngine.ts lines 652-670 (`CORNER_KICK_TEARDOWN` and its "MUST spread this" comment)
    - packages/shared/src/passValidator.ts lines 95-188 (the `STANDARD` path-blocking guard, `destDefender` auto-intercept population, and the `passType !== 'LONG' && passType !== 'HIGH'` gate on `rollIntercepts` — confirming a corner Low pass mapped to `STANDARD` already gets populated interception lists with no validator change needed)
    - packages/server/src/gameHandlers.ts lines 1846-1870 (the authoritative `validatePass` re-run and the `preGeneratedInterceptionDice` seeding, gated only on `!isHeaderPass` — confirming the dice are already seeded for a corner Low pass)
    - .planning/phases/38-corner-kick/38-09-SUMMARY.md (the A2 CORRECTED verdict text)
  </read_first>
  <behavior>
    - An accurate Low corner whose target hex is occupied by a defender auto-intercepts: possession transfers to that defender, phase becomes `PASS`, `lastActionType` becomes `SUCCESSFUL_TACKLE`
    - An accurate Low corner passing a ZoI defender rolls that defender's interception on the existing die-equals-6-or-combined-at-least-10 threshold, using `preGeneratedInterceptionDice`
    - A failed interception roll still delivers the Low corner to the target hex as today
    - An accurate High corner with a defender adjacent to the taker runs no interception at all and still transitions to `HEADER`
    - An inaccurate corner of either type still routes to `LOOSE_BALL` via the existing corner arm, unchanged
    - Non-corner `STANDARD_PASS`, `HIGH_PASS`, `LONG_BALL` and `FIRST_TIME_PASS` interception behaviour is unchanged
  </behavior>
  <action>
    In `packages/server/src/gameEngine.ts`, in `applyRoll`'s `PASS` case, replace the
    `const isCornerKick = state.cornerKickTeam != null;` flag (line ~2184) with a High-only guard.
    Rename it `isCornerKickHighDelivery` and define it as `state.cornerKickTeam != null` AND
    `newLastActionType === 'HIGH_PASS'`. Use `newLastActionType` (derived at lines ~2103-2109),
    not `state.lastActionType`, so the flag agrees with `deliveredPassType` used elsewhere in the
    same block. Update the `if (!isHeaderPass && !isFirstTimePass && !isCornerKick)` condition at
    line ~2185 to use the new name.

    Rewrite the adjacent `Assumption A2` source comment to record the corrected rule verbatim:
    a High Pass corner is not interceptable (it flies into the box, mirroring HIGH_PASS/LONG_BALL),
    a Low Pass corner IS interceptable (it is a grounded pass a defender can step in front of), and
    that this was corrected by the Phase 38 human-verification checkpoint (38-09-SUMMARY.md, A2
    verdict CORRECTED (partial)). Reference gap-closure plan 38-13.

    Make no change to `passTypeMap`, `validatePassType`, `validatePass`, or `passValidator.ts`.
    A corner Low pass already maps to `'STANDARD'`, which already populates `autoIntercepts` and
    `rollIntercepts`, and `gameHandlers.ts` already seeds `preGeneratedInterceptionDice` for it —
    verify both by reading, and state in the SUMMARY that no validator or handler change was
    needed. Make no change to the High-corner unlimited-distance override in `gameHandlers.ts`.

    Extend `packages/server/src/__tests__/gameEngine.cornerKick.test.ts` with the four
    interception-behaviour test names listed in "Artifacts this phase produces" (the teardown test
    belongs to Task 2). Build fixtures from the existing `cornerLowState` and `cornerHighState`
    consts (lines ~1372-1387), adding a defender on or adjacent to the pass path and seeding
    `preGeneratedInterceptionDice` for the roll cases. Inject `d1` deterministically the same way
    the existing `applyRoll PASS-case corner accuracy gate` describe block does, choosing a die
    that makes the accuracy check pass so the interception loop is actually reached. Include an
    explicit regression assertion that `cornerHighState` with an adjacent defender still ends in
    `phase === 'HEADER'`.

  </action>
  <acceptance_criteria>
    - `grep -c "isCornerKickHighDelivery" packages/server/src/gameEngine.ts` returns 2 (declaration plus the guard use)
    - `grep -c "const isCornerKick =" packages/server/src/gameEngine.ts` returns 0
    - The `Assumption A2` comment in `gameEngine.ts` contains both the word `High` and the word `Low` and cites `38-09` or `38-13`
    - `git diff --stat packages/shared/src/passValidator.ts` shows no change
    - A test asserts an accurate Low corner into a defender-occupied target hex ends with `lastActionType === 'SUCCESSFUL_TACKLE'` and the defender as `ball.carrierId`
    - A test asserts an accurate Low corner past a ZoI defender produces a `STEAL_ATTEMPT` event in `eventLog`
    - A test asserts a failed interception roll still delivers the Low corner to the target hex
    - A test asserts an accurate High corner with an adjacent defender ends in `phase === 'HEADER'` and appends no `STEAL_ATTEMPT` event
    - `pnpm --filter @counter-attack/server test -- gameEngine.cornerKick` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- gameEngine.cornerKick</automated>
  </verify>
  <done>Low corners run the interception loop, High corners do not, and the A2 correction is recorded at the source site.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tear down corner state on the two newly-reachable interception return paths</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.cornerKick.test.ts, packages/server/src/__tests__/gameHandlers.cornerKick.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 652-670 (`CORNER_KICK_TEARDOWN` and the comment stating every corner-resolving return MUST spread it so no stale `cornerKick*` field survives)
    - packages/server/src/gameEngine.ts lines 2186-2266 (the `autoIntercepts` return block at ~2199-2220 and the `rollIntercepts` intercepted return at ~2243-2264 — neither currently spreads `CORNER_KICK_TEARDOWN`, because before Task 1 a corner could never reach them)
    - packages/server/src/gameEngine.ts lines 2384-2510 (the delivery returns that DO spread it, for the exact placement and comment style to copy)
    - packages/server/src/gameEngine.ts lines 1943-1953 (`requiresAccuracyCheck` — this is what a surviving `cornerKickTeam` would wrongly re-trigger on the NEXT ordinary Standard Pass, which is the concrete bug being prevented)
    - packages/server/src/__tests__/gameEngine.cornerKick.test.ts lines 1730-1830 (the `Corner-context persistence and teardown audit` describe block, `runCornerSequenceToPass` and `expectAllCornerFieldsCleared` — reuse `expectAllCornerFieldsCleared` rather than restating the field list)
  </read_first>
  <behavior>
    - After a Low corner is auto-intercepted, every `cornerKick*` field is at its teardown value
    - After a Low corner is intercepted on a roll, every `cornerKick*` field is at its teardown value
    - The very next ordinary `STANDARD_PASS` after an intercepted corner is NOT accuracy-gated (`requiresAccuracyCheck` is false)
    - Possession transfer, `lastActionType`, `stealAttemptedByIds`/`tackleAttemptedByIds` resets and `preGeneratedInterceptionDice` clearing on those returns are unchanged
  </behavior>
  <action>
    In `packages/server/src/gameEngine.ts`, add `...CORNER_KICK_TEARDOWN` as the final spread in
    the state object of both interception returns inside `applyRoll`'s `PASS` case: the
    `autoIntercepts` loop return (~line 2199-2220) and the `rollIntercepts` intercepted return
    (~line 2243-2264). Match the placement and comment style of the existing delivery returns:
    a short comment noting it is an unconditional no-op for a non-corner pass and that it tears
    down an intercepted corner, citing 38-13.

    Add the `intercepted Low corner: every cornerKick state field is torn down` test to
    `packages/server/src/__tests__/gameEngine.cornerKick.test.ts`, placing it in the existing
    `Corner-context persistence and teardown audit` describe block and calling the existing
    `expectAllCornerFieldsCleared` helper on the post-interception state. Cover both interception
    paths (auto and roll) — either as two `it` blocks or one parameterised block, whichever matches
    the file's existing style.

    Add a follow-on assertion in the same block that feeding the post-interception state into a
    subsequent ordinary `STANDARD_PASS` does not trigger the accuracy gate — assert on the observed
    outcome (the pass delivers regardless of a low die) rather than by reaching into
    `requiresAccuracyCheck`, which is a local.

    Add the `CORNER-04: a Low corner aimed past a ZoI defender seeds preGeneratedInterceptionDice`
    test to `packages/server/src/__tests__/gameHandlers.cornerKick.test.ts`, in the existing
    `CORNER-04: GAME_ROLL High Pass corner range override` describe block or a sibling one. Seed a
    corner-kick PASS state with a defender in ZoI of the intended low-pass line, emit
    `ClientEvents.GAME_ROLL` with `'STANDARD_PASS'` and the target hex, and assert the room's game
    state shows a non-empty `preGeneratedInterceptionDice` at the moment `applyRoll` consumes them,
    or — if that intermediate state is not observable through the socket — assert the resulting
    broadcast contains a `STEAL_ATTEMPT` event with a non-zero `defenderDie`, which is only
    possible if the dice were seeded.

  </action>
  <acceptance_criteria>
    - `grep -c "CORNER_KICK_TEARDOWN" packages/server/src/gameEngine.ts` returns at least 7 (the definition plus the pre-existing spreads plus the two new ones)
    - Both interception return blocks in `applyRoll`'s PASS case contain a `...CORNER_KICK_TEARDOWN` spread
    - A test calls `expectAllCornerFieldsCleared` on the state produced by an auto-intercepted Low corner
    - A test calls `expectAllCornerFieldsCleared` on the state produced by a roll-intercepted Low corner
    - A test asserts an ordinary `STANDARD_PASS` immediately after an intercepted corner delivers regardless of the die
    - A socket-level test proves interception dice are seeded for a Low corner
    - `pnpm --filter @counter-attack/server test` (full server suite) exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- gameEngine.cornerKick &amp;&amp; pnpm --filter @counter-attack/server test</automated>
  </verify>
  <done>An intercepted corner cannot leak `cornerKick*` state into the next possession, closing the state-integrity hole that Task 1 would otherwise open.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the Low Pass button copy to describe the interception rule</name>
  <files>packages/client/src/components/CornerKickSetupPanel.tsx, packages/client/src/components/CornerKickSetupPanel.test.tsx</files>
  <read_first>
    - packages/client/src/components/CornerKickSetupPanel.tsx lines 283-314 (the High/Low choice branch and both button `title` strings — the Low one currently reads "Low Pass — needs a combined score of 8 or more; no header required")
    - packages/client/src/components/CornerKickSetupPanel.test.tsx (search for `Low Pass` and `High Pass` — confirm whether any test asserts the exact title string, which would need updating in lockstep)
    - .planning/phases/38-corner-kick/38-UI-SPEC.md (the panel copy conventions this string must stay consistent with)
    - packages/client/src/store/useGameStore.ts lines 526-598 (`selectPassType` — confirm the `STANDARD_PASS` branch already computes `interceptionRiskHexes`, so the orange interception-risk tint already appears for a Low corner with no store change required)
  </read_first>
  <behavior>
    - The Low Pass button's tooltip states that the pass can be intercepted
    - The High Pass button's tooltip is unchanged
    - The orange interception-risk hex tint appears for a Low corner (already produced by the existing `STANDARD_PASS` branch in `selectPassType`) with no store change
    - No new CSS class or hex tint type is introduced
  </behavior>
  <action>
    In `packages/client/src/components/CornerKickSetupPanel.tsx`, update the Low Pass button's
    `title` string to state the corrected rule: it needs a combined score of 8 or more, requires no
    header, and CAN be intercepted by a defender in the pass path. Keep the existing em-dash
    prefix format and sentence style of the sibling High Pass tooltip. Leave the High Pass tooltip
    exactly as it is — it is still correct.

    Verify by reading `useGameStore.ts`'s `selectPassType` that the `STANDARD_PASS` branch already
    populates `interceptionRiskHexes`, so a Low corner already renders the existing orange
    risk tint via `HexGrid`'s `interceptionRiskSet`. Make no store or `HexGrid` change; record the
    verification in the SUMMARY. If a test asserted the old Low Pass title verbatim, update it to
    the new string.

    Add no new test unless one is needed to keep an existing title assertion green — this task is
    copy-only.

  </action>
  <acceptance_criteria>
    - The Low Pass button `title` in `CornerKickSetupPanel.tsx` contains the word `intercept`
    - The High Pass button `title` in `CornerKickSetupPanel.tsx` is byte-identical to its pre-plan value
    - `git diff --stat packages/client/src/store/useGameStore.ts` shows no change
    - `git diff --stat packages/client/src/components/HexGrid.tsx` shows no change
    - `git diff --stat packages/client/src/components/CornerKickSetupPanel.module.css` shows no change
    - `pnpm --filter @counter-attack/client test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @counter-attack/client test &amp;&amp; pnpm --filter @counter-attack/client typecheck</automated>
  </verify>
  <done>The Low Pass control tells the manager the truth about interception risk, matching the corrected A2 rule.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                                                               |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client socket to GAME_ROLL      | The client picks the pass type and target; the server re-runs `validatePass` and owns the interception resolution, dice, and possession transfer.         |
| corner state to next possession | `cornerKickTeam` persisting past a corner's resolution silently accuracy-gates the next ordinary Standard Pass — a cross-possession state-integrity leak. |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                             |
| --------- | ----------------- | -------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-43   | Tampering         | Stale `cornerKick*` fields surviving an intercepted corner     | mitigate    | Task 2 spreads `CORNER_KICK_TEARDOWN` into both newly-reachable interception returns, and a test asserts the following ordinary `STANDARD_PASS` is not accuracy-gated. This is the concrete exploit Task 1 would otherwise open.                                                            |
| T-38-44   | Spoofing          | Client claiming `HIGH_PASS` to dodge the Low interception loop | mitigate    | The bypass keys off `newLastActionType`, which is derived from the same `state.lastActionType` the handler committed after re-running `validatePass` with the High-only unlimited-distance override — a spoofed High pass is subject to High's own range and PATH_BLOCKED rules, not Low's. |
| T-38-45   | Tampering         | Interception dice supplied or influenced by the client         | mitigate    | Unchanged: `preGeneratedInterceptionDice` is produced server-side by `rollDice()` in `gameHandlers.ts` before `applyRoll` runs; the engine only reads the array by index and never accepts a client-supplied die.                                                                           |
| T-38-46   | Denial of Service | A corner Low pass now failing `validatePass` and dead-ending   | accept      | The engine's own `validatePass` call already runs for every pass and returns empty interception lists on failure, so the delivery path is unchanged; a corner Low pass uses the ordinary Standard cap, which the handler validated.                                                         |
| T-38-SC   | Tampering         | npm/pnpm installs                                              | accept      | No package-manager installs in this plan.                                                                                                                                                                                                                                                   |

</threat_model>

<verification>

- `pnpm --filter @counter-attack/server test` (full server suite) green — this plan changes a shared pass path, so a whole-suite run is mandatory, not just the corner files.
- `pnpm --filter @counter-attack/client test` and `typecheck` green.
- `packages/shared/src/passValidator.ts`, `packages/client/src/store/useGameStore.ts` and `packages/client/src/components/HexGrid.tsx` are unchanged.
- 38-10's and 38-12's tests still green.
  </verification>

<success_criteria>

- A Low Pass corner is interceptable by both the auto-intercept and roll-intercept paths.
- A High Pass corner remains non-interceptable and still routes into `HEADER`.
- Every corner-resolving return path, including the two interception returns, tears down `cornerKick*` state.
- The Low Pass control's tooltip states the interception rule.
- Full server and client suites green.
  </success_criteria>

<output>
Create `.planning/phases/38-corner-kick/38-13-SUMMARY.md` when done, recording the A2 verdict as
CORRECTED with the exact new rule, and confirming that no change was required in
`passValidator.ts`, `useGameStore.ts` or `HexGrid.tsx`.
</output>
