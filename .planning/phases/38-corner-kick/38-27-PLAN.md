---
phase: 38-corner-kick
plan: 27
type: execute
wave: 20
depends_on: [38-25]
gap_closure: true
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/shared/src/offside.ts
  - packages/server/src/__tests__/gameEngine.cornerKick.test.ts
  - packages/server/src/__tests__/cornerKick.integration.test.ts
autonomous: true
requirements: [CORNER-03]
must_haves:
  truths:
    - 'A CORNER_KICK_REPOSITION move places the selected piece at any single legal destination hex in one action — adjacency is no longer required and no multi-click walk is possible'
    - 'The defending side can never be placed inside the 3-hex corner exclusion zone; the attacking side may be placed on any unoccupied on-pitch hex'
    - 'A piece is added to cornerKickActivatedIds when its placement completes, and is rejected with PIECE_LOCKED on any later attempt in the same reposition window — including within the same stage'
    - 'Each stage still accepts at most CORNER_KICK_STAGES[stageIndex].max distinct pieces, so each side can still reposition six distinct pieces across its three stages'
    - 'Undoing a reposition restores the piece position, releases its stage-cap slot and clears its activation, so the piece can be repositioned again'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applyCornerKickReposition rewritten as bounded single-destination placement'
      contains: 'applyCornerKickReposition'
  key_links:
    - from: 'applyCornerKickReposition'
      to: 'cornerKickActivatedIds'
      via: 'PIECE_LOCKED guard reads it, success path appends to it'
      pattern: 'cornerKickActivatedIds'
    - from: 'applyCornerKickReposition'
      to: 'isWithinCornerExclusionZone'
      via: 'defending-side destination filter'
      pattern: 'isWithinCornerExclusionZone'
---

<objective>
Replace the unbounded one-hex-per-click walk in `CORNER_KICK_REPOSITION` with the free-kick-style
interaction model: pick a piece, pick one destination hex inside a bounded allowed area, done — and
mark the piece activated at the moment that placement completes.

Purpose: `38-24-SUMMARY.md` bug 2, the corrected reading of D-GAP-03 — "unlimited repositioning should
be single hex selection within an allowed area like on a free kick. Player should be marked activated
after the move." The shipped mechanic (38-17) requires `hexDistance(from, to) === 1` per emission with
no cap on how many steps a piece may take, so repositioning a player across the box is an unbounded
sequence of clicks. `applyFreeKickMove` (`gameEngine.ts:6716`) is the established model this must
match: one destination click, no adjacency requirement, bounded by an allowed-area predicate, with a
per-stage distinct-piece budget and a cross-stage activation lock.

This plan is the SERVER half. Plan 38-29 mirrors it in the client.

Output: a rewritten `applyCornerKickReposition`, a corrected `applyUndo` refund arm, and refreshed
doc comments on `CORNER_KICK_STAGES`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38-corner-kick/38-CONTEXT.md
@.planning/phases/38-corner-kick/38-24-SUMMARY.md
@.planning/phases/38-corner-kick/38-15-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols introduced by THIS plan: none. This plan rewrites the body and result union of the
existing exported `applyCornerKickReposition` and edits existing doc comments.

Symbols this plan REMOVES from an existing union: `'NOT_ADJACENT'` is dropped from
`ApplyCornerKickRepositionResult['reason']` (it stays in `ApplyCornerKickFinalMoveResult`, which is
unchanged by this plan).

Pre-existing symbols this plan consumes unchanged: `CORNER_KICK_STAGES`, `cornerKickStageTeam`
(`packages/shared/src/offside.ts`); `isWithinCornerExclusionZone` (`packages/shared/src/outOfBounds.ts`);
`isPitchHex` (`packages/shared/src/pitch.ts`); `hexDistance` (`packages/shared/src/hex.ts`);
`cornerKickEligibleIds`, `cornerKickStagePlacedIds`, `cornerKickActivatedIds`, `cornerKickUsedPace`
(`GameState`).

## Gap-closure source audit

| Source                         | Item                                                                                   | Covered by                           |
| ------------------------------ | -------------------------------------------------------------------------------------- | ------------------------------------ |
| 38-24-SUMMARY.md bug 2         | Reposition must be single-destination selection within an allowed area, like free kick | Task 1                               |
| 38-24-SUMMARY.md bug 2         | "Player should be marked activated after the move"                                     | Task 1                               |
| 38-24-SUMMARY.md open question | Whether the reposition budget survives the interaction change                          | A-GAP3-BUDGET (locked below), Task 1 |
| CORNER-03                      | Each manager repositions up to 6 players, alternating 2 at a time, attacker first      | Tasks 1, 2                           |

## Locked assumptions

`38-24-SUMMARY.md` flags two points as open and states no human is available to re-confirm them
mid-round. Both are locked here as explicit assumptions, not silent guesses, and are re-surfaced for
confirmation in plan 38-30's checkpoint.

- **A-GAP3-BUDGET (reposition budget unchanged).** `CORNER_KICK_STAGES` keeps its existing shape —
  six stages, `max: 2` distinct pieces each, alternating attacking/defending — and
  `cornerKickActivatedIds` keeps locking a piece out of every LATER stage. Net effect is unchanged
  from what shipped: two distinct pieces per stage, six distinct pieces per side across its three
  stages. Only the interaction model and the activation timing change in this plan.
- **A-GAP3-AREA (what "allowed area" means).** Mirroring `applyFreeKickMove` /
  `computeFreeKickSetupValidHexes` exactly: the ATTACKING side's allowed area is every on-pitch hex
  not occupied by another piece (free kick imposes no further restriction on the kicking team); the
  DEFENDING side's allowed area is the same set minus every hex inside
  `isWithinCornerExclusionZone(hex, cornerKickHex)` — the corner analogue of free kick's
  ">2 hexes from `freeKickHex`" defending rule, reusing the exclusion radius this phase already
  enforces everywhere else rather than inventing a second radius.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite applyCornerKickReposition as bounded single-destination placement with activation on completion</name>
  <files>packages/server/src/gameEngine.ts, packages/shared/src/offside.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 4229-4375 — the whole `applyCornerKickReposition` block: its result union, its D-GAP-03 doc comment, the `NOT_ADJACENT` guard at 4317, the two-part `PIECE_LOCKED` guard at 4309-4315, the `CORNER_EXCLUSION_ZONE` guard at 4331-4337, the `STAGE_LIMIT_REACHED` guard at 4346-4351, and the success return at 4362-4374
    - packages/server/src/gameEngine.ts lines 6676-6868 — `applyFreeKickMove`, the model this rewrite mirrors: note that it performs NO adjacency check at all, that its destination legality is a bounded-area predicate (`>2 hexes from freeKickHex` for the defending side), and that its budget is a per-stage distinct-piece count
    - packages/server/src/gameEngine.ts lines 4377-4465 — `applyCornerKickStageEnd`, whose Pitfall-4 comment about the "per-piece 6-hex budget" is now stale and whose `cornerKickActivatedIds: null` reset on entry to `CORNER_KICK_FINAL_SETUP` must be PRESERVED
    - packages/server/src/gameEngine.ts lines 1854-1916 — `applyUndo`'s `CORNER_KICK_REPOSITION` arm, which reads `cornerKickUsedPace`, `cornerKickStagePlacedIds` and `cornerKickActivatedIds` and computes `remainingStageMovesForPiece`
    - packages/server/src/gameEngine.ts line 1787 — `const stepDistance = hexDistance(moveToUndo.from, moveToUndo.to)`, the value the refund arm subtracts
    - packages/shared/src/offside.ts lines 58-99 — `CORNER_KICK_STAGES` and `cornerKickStageTeam`, including the stale Pitfall-4 divergence paragraph in the table's JSDoc
    - packages/server/src/gameHandlers.ts lines 797-854 — the `GAME_MOVE` `CORNER_KICK_REPOSITION` branch, which owns `MOVE` event construction; confirm it needs no change (it already captures `from` before the engine call and emits a single `MOVE` event whatever the distance)
  </read_first>
  <action>
    Rewrite `applyCornerKickReposition`'s guard sequence to the following exact order, keeping every
    guard's existing rejection reason string:

    1. `WRONG_PHASE` — phase and the `cornerKickStageIndex`/`cornerKickTeam` context guard, unchanged.
    2. `PIECE_NOT_FOUND` — unchanged.
    3. `WRONG_TEAM` — acting team from `cornerKickStageTeam(state.cornerKickStageIndex, cornerKickTeam)`, unchanged. Never read `activeTeam` (T-38-10).
    4. `NOT_ELIGIBLE` — `cornerKickEligibleIds[side]` membership, unchanged.
    5. `PIECE_LOCKED` — CHANGED. Reject whenever `(state.cornerKickActivatedIds ?? []).includes(pieceId)`. Delete the `!stagePlacedIdsForLockCheck.includes(pieceId)` second half and the `stagePlacedIdsForLockCheck` local entirely. A piece is activated the instant its placement completes (bug 2: "marked activated after the move"), so re-touching it in the same stage is now correctly rejected; Undo is the mechanism for correcting a misplacement.
    6. DELETE the `hexDistance(piece.position, to) !== 1` / `NOT_ADJACENT` guard completely, and remove `'NOT_ADJACENT'` from `ApplyCornerKickRepositionResult`'s reason union. There is no distance constraint of any kind on a corner reposition destination.
    7. `INVALID_TARGET` — `!isPitchHex(to)`, unchanged.
    8. `INVALID_TARGET` — any other piece already standing on `to`, unchanged. Note this is stricter than `computeFreeKickSetupValidHexes`, which only filters own-team pieces; keep the engine's stricter any-piece rule, because that is what `applyCornerKickReposition` already enforces and what the client must mirror.
    9. `CORNER_EXCLUSION_ZONE` — defending side only, unchanged (A-GAP3-AREA).
    10. `STAGE_LIMIT_REACHED` — CHANGED to a straight `stagePlacedIds.length >= CORNER_KICK_STAGES[state.cornerKickStageIndex].max` check. The `alreadyCountedThisStage` re-touch exemption is now unreachable (guard 5 rejects those first), so remove the local and always append `pieceId` to the new stage-placed list. Keep reading the cap from `CORNER_KICK_STAGES` — never a literal `2` (WR-02/T-38-40).

    Success return: set `pieces` with the piece at `to`; append `pieceId` to both
    `cornerKickStagePlacedIds` and `cornerKickActivatedIds`; and set
    `cornerKickUsedPace[pieceId] = (existing ?? 0) + hexDistance(piece.position, to)` — CHANGED from
    `+ 1`. The distance-based increment is load-bearing: `applyUndo` subtracts
    `hexDistance(moveToUndo.from, moveToUndo.to)` from this ledger, so a `+1` increment against a
    multi-hex placement would leave the two out of step. Keep appending no `ActionEvent` here — the
    `GAME_MOVE` handler still owns the `MOVE` event for this phase.

    Rewrite the function's doc comment: state that it is single-destination placement modelled on
    `applyFreeKickMove` (naming that function), that adjacency is deliberately absent, that
    `cornerKickActivatedIds` is applied on placement completion and locks the piece for the remainder
    of the whole reposition window, that A-GAP3-AREA defines the allowed area per side, and that
    A-GAP3-BUDGET keeps the `CORNER_KICK_STAGES` budget unchanged. Delete the stale D-GAP-03
    paragraph about "a piece already in cornerKickActivatedIds may keep moving freely for the REST of
    the stage that activated it".

    In `applyUndo`'s `CORNER_KICK_REPOSITION` arm, keep the existing three-field return
    (`cornerKickUsedPace`, `cornerKickStagePlacedIds`, `cornerKickActivatedIds`) and the
    `remainingStageMovesForPiece` derivation exactly as they are — with one placement per piece per
    stage, `remainingStageMovesForPiece` is always 0 after an undo, so the arm correctly releases both
    the stage slot and the activation. Update its comment to say so instead of describing the old
    running-total-across-stages rationale.

    In `applyCornerKickStageEnd`, keep `cornerKickActivatedIds: null` on the terminal transition into
    `CORNER_KICK_FINAL_SETUP` and keep `cornerKickUsedPace` un-reset across stage advances. Replace
    the Pitfall-4 paragraph's "Corner's 6-hex budget is a per-piece TOTAL" wording with the current
    truth: there is no hex budget; `cornerKickUsedPace` survives only as an Undo-refund ledger.

    In `packages/shared/src/offside.ts`, rewrite the stale Pitfall-4 divergence paragraph in
    `CORNER_KICK_STAGES`' JSDoc: a piece touched in one corner stage IS now locked out of later
    stages via `cornerKickActivatedIds` (so the free-kick `movedPieceIds` analogy now holds), and the
    per-piece 6-hex budget no longer exists. Do not change the table's values.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NOT_ADJACENT" packages/server/src/gameEngine.ts` still matches only `applyCornerKickFinalMove`'s union and guard — `applyCornerKickReposition`'s block (between the `applyCornerKickReposition` and `applyCornerKickStageEnd` section banners) contains zero occurrences
    - `applyCornerKickReposition` contains no `hexDistance(piece.position, to) !== 1` expression
    - The `PIECE_LOCKED` guard in `applyCornerKickReposition` is a single-term condition on `cornerKickActivatedIds` with no `cornerKickStagePlacedIds` term
    - `applyCornerKickReposition`'s success return increments `cornerKickUsedPace[pieceId]` by `hexDistance(piece.position, to)`, not by `1`
    - `grep -v '^\s*[*/]' packages/server/src/gameEngine.ts | grep -c "CORNER_KICK_STAGES\[" ` is non-zero (the stage cap is still read from the shared table, not hardcoded)
    - `applyCornerKickStageEnd` still returns `cornerKickActivatedIds: null` on the transition to `CORNER_KICK_FINAL_SETUP`
    - `pnpm --filter @counter-attack/server typecheck` and `pnpm --filter @counter-attack/shared typecheck` both exit 0
  </acceptance_criteria>
  <done>A corner reposition is a single bounded placement that activates the piece on completion; no adjacency requirement and no stage-local re-touch exemption remain.</done>
</task>

<task type="auto">
  <name>Task 2: Re-test the reposition window against the placement model</name>
  <files>packages/server/src/__tests__/gameEngine.cornerKick.test.ts, packages/server/src/__tests__/cornerKick.integration.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.cornerKick.test.ts — every `applyCornerKickReposition` test, especially those asserting `NOT_ADJACENT`, those walking a piece hex by hex, and those asserting a piece touched this stage stays movable
    - packages/server/src/__tests__/cornerKick.integration.test.ts — the socket-level reposition walkthrough, which currently emits repeated adjacent `GAME_MOVE`s
    - packages/server/src/gameEngine.ts — `applyCornerKickReposition` as rewritten in Task 1, so the tests assert the implemented guard order
    - packages/shared/src/offside.ts lines 79-99 — `CORNER_KICK_STAGES` and `cornerKickStageTeam`, so stage indices and acting sides in fixtures are derived from the table rather than restated
  </read_first>
  <action>
    Delete or rewrite every test that depends on the removed adjacency rule or on the removed
    same-stage re-touch exemption. Add engine-level tests asserting:

    - A placement to a hex more than one away from the piece's current position succeeds and moves the piece exactly to that hex (assert with a destination at `hexDistance` 5 or greater, derived from the fixture's own positions rather than a restated literal pair).
    - The same piece attempted a second time in the SAME stage is rejected with `PIECE_LOCKED`.
    - The same piece attempted in a LATER stage of the same window is rejected with `PIECE_LOCKED`.
    - A third distinct piece in a stage that already placed `CORNER_KICK_STAGES[stageIndex].max` pieces is rejected with `STAGE_LIMIT_REACHED`.
    - Across a side's three stages, six distinct pieces can be placed and a seventh is rejected (A-GAP3-BUDGET).
    - A DEFENDING placement to a hex inside `isWithinCornerExclusionZone(hex, cornerKickHex)` is rejected with `CORNER_EXCLUSION_ZONE`, while an ATTACKING placement to the same hex succeeds (A-GAP3-AREA).
    - An off-pitch destination and an occupied destination are both rejected with `INVALID_TARGET`.
    - `cornerKickUsedPace[pieceId]` after one placement equals `hexDistance(from, to)`.
    - Undoing that placement restores the original position, removes the piece from both `cornerKickStagePlacedIds` and `cornerKickActivatedIds`, and a subsequent placement of the SAME piece then succeeds.

    In `cornerKick.integration.test.ts`, replace the multi-click walk in the reposition walkthrough
    with a single `GAME_MOVE` emission per piece to a distant legal hex, and assert the broadcast
    state shows the piece at that hex, present in `cornerKickActivatedIds`, and that a second
    `GAME_MOVE` for the same piece produces a `PIECE_LOCKED` `GAME_ERROR`.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/server test` passes with zero failing tests
    - `grep -c "NOT_ADJACENT" packages/server/src/__tests__/gameEngine.cornerKick.test.ts` returns 0 for reposition-window tests (any remaining occurrence must belong to a `applyCornerKickFinalMove` test)
    - A test asserts a successful reposition placement whose `hexDistance(from, to)` is 5 or greater
    - A test asserts `PIECE_LOCKED` for a second placement of the same piece within one stage
    - A test asserts six distinct pieces placeable per side across three stages and a seventh rejected
    - A test asserts an Undo re-enables the same piece for a fresh placement
    - `cornerKick.integration.test.ts` contains a socket-level `PIECE_LOCKED` assertion for a repeated `GAME_MOVE` on the same piece
  </acceptance_criteria>
  <done>The server test suite proves the placement model, the activation-on-completion lock, the preserved six-distinct-piece budget and the bounded defending area.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                                                           |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| client socket to game engine | Removing the adjacency guard widens the accepted destination set; the pitch, occupancy, eligibility and exclusion-zone guards are now the only bounds |
| engine to shared stage table | `CORNER_KICK_STAGES` remains the sole source of the per-stage distinct-piece cap for engine, panel and grid                                           |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                          | Disposition | Mitigation Plan                                                                                                                                     |
| --------- | ---------------------- | ------------------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-90   | Elevation of Privilege | A wider destination set letting a defender reach the corner        | mitigate    | The `CORNER_EXCLUSION_ZONE` guard is retained ahead of the success path and re-tested for the new long-distance placements                          |
| T-38-91   | Tampering              | Repositioning the same piece repeatedly to exceed the 6-piece rule | mitigate    | `PIECE_LOCKED` now fires on any `cornerKickActivatedIds` membership, and a test asserts the seventh distinct piece per side is rejected             |
| T-38-92   | Tampering              | Piece stacking via a non-adjacent destination                      | mitigate    | The any-piece occupancy guard is kept and re-tested with a long-distance destination                                                                |
| T-38-93   | Repudiation            | Undo desyncing from the pace ledger after the increment change     | mitigate    | `cornerKickUsedPace` now increments by `hexDistance`, exactly the value `applyUndo` subtracts; a test asserts the ledger value equals that distance |
| T-38-SC   | Tampering              | npm/pnpm installs                                                  | accept      | No new dependency is added by this plan                                                                                                             |

</threat_model>

<verification>

- `pnpm --filter @counter-attack/server test` and `pnpm --filter @counter-attack/shared test` pass.
- `pnpm --filter @counter-attack/server typecheck` exits 0.
- `applyCornerKickReposition` contains no adjacency guard and a single-term `PIECE_LOCKED` guard.
- Both locked assumptions (A-GAP3-BUDGET, A-GAP3-AREA) are restated verbatim in `38-27-SUMMARY.md` so plan 38-30's checkpoint can put them to the verifier.

</verification>

<success_criteria>

- Repositioning a player during the alternating window is one piece click plus one destination click, at any distance, inside a bounded allowed area.
- The player is marked activated when that placement completes and cannot be moved again in the window.
- The six-distinct-pieces-per-side budget is unchanged.
- Undo fully reverses a placement, including its activation.

</success_criteria>

<output>
Create `.planning/phases/38-corner-kick/38-27-SUMMARY.md` when done, restating A-GAP3-BUDGET and
A-GAP3-AREA verbatim for the closing checkpoint.
</output>
