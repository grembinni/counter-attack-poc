---
phase: 38-corner-kick
plan: 33
type: execute
wave: 24
depends_on: [38-31, 38-32]
gap_closure: true
files_modified: []
autonomous: false
requirements: [OOB-03, CORNER-01, CORNER-02, CORNER-03, CORNER-04, CORNER-05, CORNER-06]
must_haves:
  truths:
    - 'A human has observed the Undo control visually enable after a placement in the 2-2-2 corner reposition window'
    - 'A human has observed a single Undo revert exactly the most recent placement'
    - 'A human has stepped through two Undos in one round, reverting the second placement then the first, matching a normal Movement Phase'
    - 'A human has observed Undo working in the pre-kick 3-hex window and refusing to cross a completed round or un-place the corner-taker'
    - 'The two transitions edited this round (taker-select and stage-end) have been re-exercised live, so the adjacent-code carve-out in 38-30-SUMMARY.md is honoured'
    - 'The deferral of the offside-ring defect has been disclosed to the verifier and either accepted or overruled, with repro data captured either way'
    - 'A non-approval routes to a further gap-closure round rather than being patched inside this plan'
  artifacts: []
  key_links: []
---

<objective>
Re-verify, in a live two-browser session, that the Undo regression reported in `38-30-SUMMARY.md` bug 2 is
fixed in all three of its sub-findings, and obtain an explicit position on the deferral of bug 1.

Purpose: `38-30-SUMMARY.md`'s verdict was CORRECTED, not approved. Its routing section names three items for
this round: fix the Undo regression (highest priority, a direct regression in the third round's own scope and
a re-open of D-GAP-01), triage bug 1, and re-run the relevant portions of the ten-step walkthrough with
particular attention to steps 6, 7 and 9. This checkpoint is item 3.

Scope note carried from `38-30-SUMMARY.md`: the consolidated "rest of the changes seems fine" reply that
covered steps 1, 3, 4, 5, 8, the visual half of 9 and the banner half of 10 is to be treated as sufficient
and does NOT need re-verification unless this round's changes touch adjacent code. This round's changes DO
touch two transitions inside that set (the corner-taker-select transition from step 4, and the stage-end
handoff between steps 6 and 7), so step 7 below re-exercises exactly those two and nothing more.

Output: user approval, or a numbered list of corrections to fold into a fifth gap-closure round.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-corner-kick/38-CONTEXT.md
@.planning/phases/38-corner-kick/38-UI-SPEC.md
@.planning/phases/38-corner-kick/38-30-PLAN.md
@.planning/phases/38-corner-kick/38-30-SUMMARY.md
@.planning/phases/38-corner-kick/38-31-SUMMARY.md
@.planning/phases/38-corner-kick/38-32-SUMMARY.md
</context>

## Artifacts this phase produces

This plan produces no code artifacts — it is the round's verification gate. It consumes:

- `lastDiceRoll: null` on `applyCornerKickTakerSelect`'s `CORNER_KICK_REPOSITION` return literal (38-31)
- `lastDiceRoll: null` on `applyCornerKickStageEnd`'s terminal `CORNER_KICK_FINAL_SETUP` return literal (38-31)
- The refreshed LIFO multi-undo contract comment on `applyUndo`'s `CORNER_KICK_REPOSITION` arm (38-31)
- The four pinned `CornerKickSetupPanel` Undo tests (38-32)
- The DEFERRED verdict for `38-30-SUMMARY.md` bug 1 in `deferred-items.md` and
  `.planning/todos/pending/2026-08-09-bug-offside-ring-after-goal.md` (38-32)
- Decisions requiring a human position here: D-GAP-01 (re-opened) and the bug 1 deferral

## Gap-closure source audit

| Source                             | Item                                                                   | Covered by     |
| ---------------------------------- | ---------------------------------------------------------------------- | -------------- |
| GOAL (ROADMAP Phase 38 goal)       | Corner kick "fully playable" end to end                                | Steps 1-7      |
| REQ OOB-03                         | Corner awarded on a byline exit after a defending touch                | Step 1 (setup) |
| REQ CORNER-01                      | Both goalkeepers repositioned first                                    | Step 7         |
| REQ CORNER-02                      | Corner-taker placed at the fixed corner hex                            | Step 7         |
| REQ CORNER-03                      | Alternating 2-at-a-time reposition window, now correctable by Undo     | Steps 1, 2, 3  |
| REQ CORNER-04                      | High Pass / Low Pass accuracy check                                    | Step 7 (smoke) |
| REQ CORNER-05                      | High Pass corner requires a header                                     | Step 7 (smoke) |
| REQ CORNER-06                      | Pre-kick 3-hex window, now correctable by Undo                         | Step 4         |
| CONTEXT D-05                       | Strict-pairs rounds — Undo must not leak across a round boundary       | Step 3         |
| CONTEXT D-06                       | Confirm-with-0 pattern still intact after the Undo work                | Step 3         |
| CONTEXT D-09                       | Panel conventions — Undo affordance matches the Movement Phase         | Step 1         |
| 38-30-SUMMARY bug 2, sub-finding 1 | Undo control does not visually enable                                  | Step 1         |
| 38-30-SUMMARY bug 2, sub-finding 2 | Undo reverts the single most recent placement                          | Step 2         |
| 38-30-SUMMARY bug 2, sub-finding 3 | Steppable multi-undo stack matching a normal Movement Phase            | Step 2         |
| D-GAP-01 (re-opened)               | Undo releases the stage-cap slot and the activation                    | Step 2         |
| 38-30-SUMMARY bug 1                | Offside ring after goal — DEFERRED, disclosed for an explicit position | Step 6         |
| 38-30-SUMMARY adjacent-code caveat | Two edited transitions inside the consolidated-confirm set             | Step 7         |
| 38-31 targeted-fix constraint      | Throw-In / Goal Kick dice readout must be unchanged                    | Step 5         |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Two-browser re-verification of the corner-kick Undo fix (round 4)</name>
  <files>none — verification gate, no files are modified</files>
  <read_first>
    - `.planning/phases/38-corner-kick/38-30-SUMMARY.md` — bug 2's three sub-findings verbatim, bug 1
      verbatim, and the "Is Phase 38 Ready to be Marked Complete?" routing section that scopes this round
    - `.planning/phases/38-corner-kick/38-30-PLAN.md` — the ten-step walkthrough, so steps 6, 7 and 9 are
      re-run in the same language the verifier already knows, and so no step is silently re-scoped
    - `.planning/phases/38-corner-kick/38-31-SUMMARY.md` — where `lastDiceRoll` is actually nulled and what
      the five new server tests assert, so the framing presented below matches the code rather than this
      plan's prediction
    - `.planning/phases/38-corner-kick/38-32-SUMMARY.md` — the DEFERRED verdict for bug 1 and its
      justification, which must be disclosed in the verifier's own words in step 6
    - `.planning/phases/38-corner-kick/38-24-SUMMARY.md` — the original D-GAP-01 ruling ("keep Undo's
      stage-cap-slot refund as implemented"), which this round re-opens under the new placement model
  </read_first>
  <action>
    Do not write or modify any code in this task.

    Preparation the executor performs before pausing: run `pnpm install` if `node_modules` is absent; run
    `pnpm --filter @counter-attack/shared build`; run `pnpm typecheck` and `pnpm test` and report both
    results; check ports 3001 and 5173-5175 for stale dev-server processes from earlier sessions and kill
    any found (this cost time in both 38-24 and 38-30 — if the first server start hits `EADDRINUSE`, kill
    the specific lingering PID and start again rather than retrying blindly); start
    `pnpm --filter @counter-attack/server dev` and `pnpm --filter @counter-attack/client dev`, confirm both
    come up, and report the client URL. Re-read the five files in `read_first` so the framing below
    describes what was actually built.

    Then present the `what-built` and `how-to-verify` content below and BLOCK for the human's reply. Do not
    self-approve, do not infer a verdict from passing tests, and do not proceed on `workflow.auto_advance`.
    This gate exists because the automated suite was fully green in 38-30 while the Undo control was
    unusable in the browser — every client fixture seeded `lastDiceRoll` as null, so no test could see the
    defect. That is the fourth consecutive round in which a green suite missed a real defect.

    On reply, write `38-33-SUMMARY.md` recording: a CONFIRMED or CORRECTED verdict for every one of steps
    1-7 below; an explicit re-ruling on D-GAP-01 under the bounded placement model; an explicit accept or
    overrule on the bug 1 deferral, with any repro data the verifier supplied captured verbatim; and whether
    Phase 38 is ready to be marked Complete.

    If the reply is anything other than `approved`, do NOT patch anything in this plan. Record every
    reported issue verbatim, state that Phase 38 is not ready, and route to `/gsd-plan-phase 38 --gaps` for
    a further round — the same discipline 38-15, 38-24 and 38-30 followed.

  </action>
  <what-built>
    Two plans landed since the last checkpoint. Between them they close the Undo bug you reported and
    record a decision about the offside-ring bug.

    - **38-31 — the Undo bug is fixed, and the cause was not where it looked.** The Undo control was not
      broken: it was permanently switched off. Every Undo control in the game (Movement Phase included)
      refuses to light up while the last dice roll is still on screen, so you cannot undo a move back across
      a committed dice result. When a corner is awarded, the dice roll that sent the ball out of play was
      never being cleared — so for the whole corner sequence the game still believed a dice result was
      pending, and the Undo control stayed greyed out no matter how many players you had placed. The dice
      value is now cleared the moment the corner-taker is placed and the 2-2-2 window opens, and again when
      the pre-kick window opens. Nothing about Undo's own logic changed — it was already correct, including
      the step-through stack you asked for. The dice roll that caused the corner deliberately stays visible
      during the two goalkeeper windows, and only clears when the first window you can actually undo in
      opens.
    - **38-31 — the step-through stack is now covered by tests.** There was no test for two different
      players placed in the same round being undone one at a time. There is now, and it asserts the exact
      behaviour you described: the first Undo reverts the second placement, the second Undo reverts the
      first, each one handing the player back to you as selectable and unmarked, and a third Undo does
      nothing rather than un-placing the corner-taker.
    - **38-32 — the Undo control's look is pinned to the Movement Phase's.** You asked for it to behave
      "the same as a normal move phase". It already uses exactly the same button treatment as the Movement
      Phase's Undo — greyed out at half opacity when unavailable, full strength when available — and there
      is now a test that fails if anyone ever gives the corner version its own styling.
    - **38-32 — the offside-ring bug is recorded as deferred, not dropped.** See step 6; this needs your
      explicit yes or no.

  </what-built>
  <how-to-verify>
    Start the dev servers and open two browser windows joined to the same room, one per team. Create the
    game with the "Out-of-Bounds / Restarts" toggle ON. Keep the browser devtools console open for the
    whole session.

    This is a shorter walkthrough than last time on purpose. `38-30-SUMMARY.md` records your consolidated
    "rest of the changes seems fine" reply for steps 1, 3, 4, 5, 8, the visual half of 9 and the banner
    half of 10, and that stands — only the two transitions this round actually edited are re-checked
    (step 7 below). Give an explicit CONFIRMED or CORRECTED verdict for every numbered step.

    Play until a defending player last touches a ball that then crosses their own byline, so a corner is
    awarded. Advance through both goalkeeper windows and place a corner-taker to reach the 2-2-2
    repositioning window.

    **1. The Undo control lights up (bug 2, sub-finding 1).**
    - On entering the 2-2-2 window with nothing yet placed, expect `Undo` to be visibly greyed out and
      unclickable.
    - Place one player (pick the player, then pick a destination hex). **Expect `Undo` to immediately become
      full-strength and clickable** — this is the exact thing that never happened last round.
    - Compare it against the `Undo` in a normal Movement Phase: same button size, same grey-to-solid
      transition, no colour of its own. Say so if it does not match.

    **2. Single Undo and the step-through stack (bug 2, sub-findings 2 and 3; D-GAP-01).**
    - With one player placed, press `Undo` once. Expect that player back on their starting hex, the
      orange-ring/red-X activated marker gone, the player selectable again, and no error banner.
    - Now place TWO different players in the same round without pressing Confirm. Press `Undo`.
      **Expect only the SECOND player to revert** — the first must stay where you put them, still marked.
    - Press `Undo` again. **Expect the first player to revert too.** Both should now be back at their
      starting hexes, both unmarked and selectable, and the round should read as though nothing was placed.
    - Press `Undo` a third time. Expect nothing to happen and no crash — there is nothing left to undo.
    - **D-GAP-01 RE-RULING NEEDED.** With two players placed (the round's limit), undo one and confirm a
      THIRD, previously-blocked player becomes selectable. You ruled in 38-15 that undoing a placement
      should give the slot back. That ruling was made against the old one-hex-at-a-time model, which has
      since been replaced — please confirm it still reads correctly under the current single-click
      placement model, or state what you want instead.

    **3. Undo must not leak across a round boundary.**
    - Place a player, press Confirm to hand the round to your opponent, let them place a player and Confirm
      back to you. Expect `Undo` to be greyed out again at the start of your new round — it must not reach
      back into either your own completed round or your opponent's.
    - Also confirm `Confirm` still works with zero players placed, and still shows the "players left to
      reposition, are you sure" prompt when you have not used the whole round.

    **4. Undo in the pre-kick 3-hex window (CORNER-06).**
    - Reach the pre-kick window (`Reposition 1 player — up to 3 hexes.`). Expect `Undo` greyed out at first.
    - Move your one player. Expect `Undo` to light up, and pressing it to revert that move and let you pick
      a DIFFERENT player afterwards.
    - Expect still NO activated marker at any point in this window — that was fixed last round and must not
      have regressed.

    **5. Nothing was taken away from the dice display.** The fix works by clearing the last dice roll at a
    specific moment, so please check nothing you needed disappeared.
    - During the two goalkeeper windows, the dice roll that sent the ball out of play should still be
      visible wherever it normally shows.
    - It should clear when the 2-2-2 window opens. The action log still holds the full record either way.
    - Then, separately, take a **Throw In** and a **Goal Kick** and confirm their dice readouts behave
      exactly as they did before — the fix was deliberately kept off the shared restart path so those two
      are untouched. Say so if either looks different.

    **6. The offside-ring bug — DISCLOSURE, explicit yes or no needed.** You reported: "after goal is
    scored and player positions are reset for kick off players still showed offsides rings when they should
    be reset." **This has NOT been fixed this round, and that is a deliberate decision we need you to sign
    off on.**
    - What we found: the server code that runs when a goal is scored already clears every offside flag, on
      both of the only two paths that can score a goal, and both are covered by passing tests that have
      been in the suite since Phase 25. Replay frames never carry offside flags at all. Nothing in Phase 38
      touches offside. So the cause is somewhere we could not find by reading the code, and finding it
      needs a live investigation rather than an edit.
    - It has been written up in full — including everything already ruled out — as a tracked bug, and it is
      flagged as very likely the same underlying problem as a long-standing bug where stale shot-path
      shading also survives into the kick-off setup screen. That one has been open since Phase 25.
    - Proposed route: fix it as its own investigation (`/gsd-debug`) or as a Phase 39 bug-fix item, rather
      than holding Phase 38 open for a fifth round.
    - **If you accept the deferral**, please still help the next session by answering: was the goal scored
      by a normal shot, a snapshot, or a header at goal? Were you watching the live game or the replay? Do
      the rings clear once the kick-off is actually taken, or do they persist into the movement phase?
    - **If you do not accept the deferral**, say so and it becomes a fifth gap-closure round.

    **7. Adjacent-code check on the two transitions we edited.** Only these two, because they sit inside
    the set you previously confirmed in one consolidated reply.
    - **Corner-taker placement:** confirm selecting your player and pressing Confirm still teleports that
      player and the ball to the gold-ringed corner hex, and still opens the 2-2-2 window at the attacking
      side's first round. Confirm an opponent's player is still not clickable.
    - **Stage-end handoff:** confirm the final Confirm of the last defending round still moves the game
      into the pre-kick window with the attacking side acting first.
    - Then take the corner to completion once (High Pass or Low Pass, your choice) and confirm it still
      resolves normally — accuracy check in the log, header contest on an accurate High Pass, delivery with
      no header on an accurate Low Pass.

  </how-to-verify>
  <acceptance_criteria>
    - Every one of steps 1-7 has been performed in a live two-browser session and has a recorded CONFIRMED or CORRECTED verdict in `38-33-SUMMARY.md`
    - Step 1 produced an observed transition of the `Undo` control from greyed-out to full-strength immediately after a single placement in the 2-2-2 window
    - Step 2 produced an observed two-placement sequence in which the first `Undo` reverted ONLY the most recent placement and the second `Undo` reverted the earlier one, with both players ending selectable and unmarked
    - Step 2 produced an observed third `Undo` that changed nothing and did not un-place the corner-taker
    - Step 2 produced an explicit D-GAP-01 re-ruling under the bounded single-click placement model
    - Step 3 produced an observed greyed-out `Undo` at the start of a fresh round after a Confirm handoff
    - Step 4 produced an observed `Undo` in `CORNER_KICK_FINAL_SETUP` that reverted the move and released the one-player slot, with no activated marker seen at any pace value
    - Step 5 produced an explicit statement that the Throw In and Goal Kick dice readouts are unchanged
    - Step 6 produced an explicit accept or overrule on the bug 1 deferral, and — if accepted — the three repro answers captured verbatim in `38-33-SUMMARY.md`
    - Step 7 produced an observed corner-taker placement, an observed stage-end handoff into the pre-kick window, and one corner taken to resolution
    - `38-33-SUMMARY.md` states plainly whether Phase 38 is ready to be marked Complete
  </acceptance_criteria>
  <verify>
    <human-check>Two-browser walkthrough steps 1-7 above, with an explicit verdict per step, a D-GAP-01 re-ruling, and an explicit position on the bug 1 deferral.</human-check>
  </verify>
  <resume-signal>
    Reply with either:
    - `approved` — the Undo control now lights up after a placement, single and step-through Undo both
      behave like a normal Movement Phase, Undo does not leak across rounds, the pre-kick window's Undo
      works, nothing was lost from the dice display, D-GAP-01 still reads correctly under the new placement
      model, and deferring the offside-ring bug out of Phase 38 is acceptable; or
    - a numbered list of issues, explicitly stating your position on the D-GAP-01 re-ruling and on the
      offside-ring deferral even when everything else passes.
  </resume-signal>
  <done>All seven steps have a recorded verdict, D-GAP-01 has an explicit re-ruling, the bug 1 deferral has an explicit accept or overrule, and `38-33-SUMMARY.md` states whether Phase 38 can be marked Complete.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                                                                  |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| human verifier to phase gate  | This checkpoint is the only control that catches a defect a fully green suite cannot see — which is exactly what happened to the Undo control in 38-30.      |
| planner triage to phase close | The bug 1 deferral is a planner-authored judgement call; shipping it without an explicit human position would repeat the D-GAP-02 and D-GAP-03 failure mode. |
| two live clients to server    | The stepped Undo stack, the round handoff and the cross-round Undo clamp are only genuinely exercised with two real sockets alternating turns.               |
| browser runtime to verifier   | 38-30's other defect was console-only; devtools stays open for the whole session again.                                                                      |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                         | Disposition | Mitigation Plan                                                                                             |
| --------- | ---------------------- | ----------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------- |
| T-38-114  | Repudiation            | Closing Phase 38 with the offside-ring defect silently dropped    | mitigate    | Step 6 discloses the deferral in full and the resume-signal cannot be `approved` without a position on it   |
| T-38-115  | Repudiation            | D-GAP-01 carried forward on a ruling made against a deleted model | mitigate    | Step 2 demands an explicit re-ruling under the current bounded placement model                              |
| T-38-116  | Tampering              | A verdict inferred from a green test suite rather than observed   | mitigate    | The action forbids self-approval and names 38-30, where the suite was green and the control was unusable    |
| T-38-117  | Elevation of Privilege | Undo reaching across a round handoff into the opponent's work     | mitigate    | Step 3 exercises the clamp live with two real sockets, complementing 38-31's engine-level boundary tests    |
| T-38-118  | Information Disclosure | The dice-clear silently removing information the player needs     | mitigate    | Step 5 checks the goalkeeper-window dice readout and the untouched Throw In / Goal Kick readouts explicitly |
| T-38-SC   | Tampering              | npm/pnpm installs                                                 | accept      | `pnpm install` may be run only to restore an absent `node_modules`; no dependency is added by this round    |

</threat_model>

<verification>

- Human verifier replies `approved`, or supplies a numbered issue list.
- A CONFIRMED or CORRECTED verdict for each of steps 1-7 is recorded in `38-33-SUMMARY.md`.
- D-GAP-01 has an explicit re-ruling and the bug 1 deferral has an explicit accept or overrule.
- If any ruling changes behaviour, a fifth gap-closure round is created via `/gsd-plan-phase 38 --gaps`
  rather than patched ad hoc.
- If approved: the corner Undo contract and the "null `lastDiceRoll` at a reversible-window entry" rule are
  carried into STATE.md's "Decisions Locked" for v1.6; the offside-ring todo is carried forward as a tracked
  deferred item; ROADMAP.md and REQUIREMENTS.md are updated to mark Phase 38 and OOB-03 / CORNER-01..06
  complete.

</verification>

<success_criteria>

- The Undo control has been observed to enable after a placement in the 2-2-2 window.
- Single-step and multi-step Undo have both been observed behaving like a normal Movement Phase.
- The pre-kick window's Undo has been observed working, with no activated marker.
- The two transitions edited this round have been re-exercised, honouring the adjacent-code caveat.
- The offside-ring deferral has an explicit human position, with repro data captured either way.

</success_criteria>

<output>
Create `.planning/phases/38-corner-kick/38-33-SUMMARY.md` when done, recording the per-step verdicts, the
D-GAP-01 re-ruling verbatim, the position on the bug 1 deferral plus any repro data supplied, and whether
Phase 38 is now ready to be marked Complete.
</output>
