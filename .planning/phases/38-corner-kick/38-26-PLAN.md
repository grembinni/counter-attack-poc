---
phase: 38-corner-kick
plan: 26
type: execute
wave: 19
depends_on: []
gap_closure: true
files_modified:
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/ActionLog.test.tsx
autonomous: true
requirements: [CORNER-01, CORNER-03]
must_haves:
  truths:
    - 'A CORNER_KICK_CLEAR_OUT_MOVE event renders a normal action-log line instead of crashing the ActionLog component'
    - 'The client typecheck reports zero errors, so any future ActionEventType added without a formatEvent case fails the build rather than crashing at runtime'
    - 'Every member of the ActionEventType union has a formatEvent case — proven by the compiler, not by a grep'
  artifacts:
    - path: 'packages/client/src/components/ActionLog.tsx'
      provides: 'formatEvent case for CORNER_KICK_CLEAR_OUT_MOVE'
      contains: "case 'CORNER_KICK_CLEAR_OUT_MOVE'"
    - path: 'packages/client/src/components/ActionLog.test.tsx'
      provides: 'regression test rendering a CORNER_KICK_CLEAR_OUT_MOVE event'
      contains: 'CORNER_KICK_CLEAR_OUT_MOVE'
  key_links:
    - from: 'packages/client/src/components/ActionLog.tsx formatEvent'
      to: 'ActionEvent union in packages/shared/src/types.ts'
      via: 'exhaustive switch enforced by the declared Formatted return type'
      pattern: "case 'CORNER_KICK_CLEAR_OUT_MOVE'"
---

<objective>
Fix the runtime crash `TypeError: Cannot destructure property 'prefix' of 'formatEvent(...)' as it is
undefined` at `ActionLog.tsx:1070` by adding the missing `formatEvent` case for
`CORNER_KICK_CLEAR_OUT_MOVE`, and confirm by compiler evidence that no other `ActionEventType` member
is missing a case.

Purpose: `38-24-SUMMARY.md` bug 4. The root cause is already confirmed and must NOT be re-diagnosed —
`formatEvent`'s switch (`ActionLog.tsx:329`) has no `case 'CORNER_KICK_CLEAR_OUT_MOVE'` and no
`default`, so it falls off the end and returns `undefined`, which the destructure at line 1070 then
throws on. Because `formatEvent` declares a `: Formatted` return type, TypeScript already reports
this as `TS2366 Function lacks ending return statement` — the error is real and currently present in
the tree (`packages/client` typecheck fails today with exactly one error, on exactly this line).

Output: the missing case, a regression test, and a green `pnpm --filter @counter-attack/client typecheck`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38-corner-kick/38-24-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols introduced by THIS plan: none — this plan adds one `case` arm inside the existing
module-private `formatEvent` function and one test.

Pre-existing symbols this plan consumes unchanged: `Formatted` (type, `ActionLog.tsx:322`),
`pieceColorOf`, `P`, `PNamed` (`ActionLog.tsx`), the `CORNER_KICK_CLEAR_OUT_MOVE` `ActionEvent`
variant (`packages/shared/src/types.ts:563-575`), and `setEventLog` / `render(<ActionLog />)` (the
test file's existing harness).

## Gap-closure source audit

| Source                  | Item                                                              | Covered by |
| ----------------------- | ----------------------------------------------------------------- | ---------- |
| 38-24-SUMMARY.md bug 4  | ActionLog crashes on `CORNER_KICK_CLEAR_OUT_MOVE`                 | Task 1     |
| 38-24-SUMMARY.md item 4 | "grep for any OTHER ActionEventType union members missing a case" | Task 1     |
| STATE.md pitfall        | Every new ActionEventType needs per-event checklist treatment     | Task 1     |

<tasks>

<task type="auto">
  <name>Task 1: Add the CORNER_KICK_CLEAR_OUT_MOVE formatEvent case and prove the switch is exhaustive</name>
  <files>packages/client/src/components/ActionLog.tsx, packages/client/src/components/ActionLog.test.tsx</files>
  <read_first>
    - packages/client/src/components/ActionLog.tsx lines 320-340 — the `Formatted` type declaration and `formatEvent`'s signature, whose `: Formatted` return annotation is what makes the missing case a compile error
    - packages/client/src/components/ActionLog.tsx lines 941-1022 — the five existing `CORNER_KICK_*` cases; `CORNER_KICK_GK_PLACE` (941) and `CORNER_KICK_MOVE` (984) are the two shapes the new case must sit between stylistically
    - packages/client/src/components/ActionLog.tsx lines 1040-1075 — the `items.map` render loop and the destructure at 1070 that currently throws
    - packages/shared/src/types.ts lines 82-143 — the full `ActionEventType` union, to cross-check the case list
    - packages/shared/src/types.ts lines 563-575 — the `CORNER_KICK_CLEAR_OUT_MOVE` variant's exact fields: `slot: 'ATTACKER' | 'DEFENDER'`, `pieceId`, `from`, `to`, `timestamp`, and deliberately NO `ballAfter`
    - packages/client/src/components/ActionLog.test.tsx lines 926-1020 — the `describe('ActionLog — Phase 38 (38-07): Corner Kick event rendering')` block and its `setEventLog([...]) / render(<ActionLog />) / expect(container.textContent).toMatch(...)` idiom
  </read_first>
  <action>
    Add `case 'CORNER_KICK_CLEAR_OUT_MOVE': { ... }` to `formatEvent`'s switch, placed immediately
    after the `CORNER_KICK_ACCURACY` case (the last existing arm, ending around line 1020) so the
    corner family stays contiguous.

    The returned `Formatted` object must use exactly these four fields, matching the adjacent corner
    cases:
    - `prefix`: the literal string `[CORNER KICK]` — the same prefix `CORNER_KICK_GK_PLACE`,
      `CORNER_KICK_TAKER_PLACED` and `CORNER_KICK_STAGE_ADVANCE` already use. Do NOT invent a new
      bracket label.
    - `prefixColor`: `pieceColorOf(event.pieceId)` — same call as `CORNER_KICK_GK_PLACE`.
    - `content`: a JSX fragment beginning with a `{' '}` space, naming the piece with the `PNamed`
      component (`<PNamed pieceId={event.pieceId} />`, matching `CORNER_KICK_GK_PLACE` /
      `CORNER_KICK_TAKER_PLACED`, not the role-prefixed `P` used by `CORNER_KICK_MOVE`), followed by
      the words `cleared the corner` and then `{event.from.q},{event.from.r} → {event.to.q},{event.to.r}`.
      Derive a side label from `event.slot` the way `CORNER_KICK_GK_PLACE` derives one from
      `event.side`: `'ATTACKER'` renders `Attacking`, `'DEFENDER'` renders `Defending`.
    - `isGoal`: `false`.

    Write a short comment above the case in this file's established voice naming the requirement
    (`CORNER-01`), the source (`38-24-SUMMARY.md` bug 4, gap-closure round 3), and the fact that this
    event is now emitted automatically by `applyAutomaticCornerClearOut` at corner-award time rather
    than by an interactive phase.

    Do NOT add a `default:` arm. The absence of a default is what lets TypeScript prove exhaustiveness
    against the `ActionEvent` union — adding one would silently re-open this entire bug class.

    Exhaustiveness audit for the rest of the union (this replaces a manual grep): run
    `pnpm --filter @counter-attack/client typecheck` BEFORE the edit and record the output in the
    summary — it currently reports exactly one error,
    `src/components/ActionLog.tsx(329,74): error TS2366`. Run it again AFTER the edit; a clean exit
    is positive proof that every remaining `ActionEventType` member has a case, because TS2366 fires
    once for the whole function regardless of how many union members are unhandled. Record in the
    summary, explicitly, that the compiler audit found no other missing `formatEvent` case.

    Add one regression test to the existing Phase 38 describe block in `ActionLog.test.tsx`: seed a
    single `CORNER_KICK_CLEAR_OUT_MOVE` event via `setEventLog`, render `<ActionLog />`, and assert
    the render does not throw and that `container.textContent` matches `/\[CORNER KICK\]/` and the
    rendered `from`→`to` coordinates. Also add a test that renders a log containing the event
    alongside at least one other corner event, so the `items.map` loop at line 1045 is exercised with
    a mixed list.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client typecheck &amp;&amp; pnpm --filter @counter-attack/client test -- ActionLog</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case 'CORNER_KICK_CLEAR_OUT_MOVE'" packages/client/src/components/ActionLog.tsx` returns 1
    - `grep -c "default:" packages/client/src/components/ActionLog.tsx` returns 0 inside `formatEvent` (no default arm was added)
    - `pnpm --filter @counter-attack/client typecheck` exits 0 (it exits 1 with `ActionLog.tsx(329,74): error TS2366` before this task)
    - `pnpm --filter @counter-attack/client test -- ActionLog` passes, including a test whose event log contains a `CORNER_KICK_CLEAR_OUT_MOVE` event
    - The new test asserts `container.textContent` matches `/\[CORNER KICK\]/` for that event
    - The summary records the pre-edit and post-edit `typecheck` output verbatim as the exhaustiveness audit result
  </acceptance_criteria>
  <done>Rendering an event log containing a `CORNER_KICK_CLEAR_OUT_MOVE` event produces a normal log line, and `pnpm --filter @counter-attack/client typecheck` exits 0.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                                                            |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| server event log to client DOM | Every `ActionEvent` the server appends is rendered by `formatEvent`; an unhandled variant crashes the whole `ActionLog`/`SideLog` tree |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                    | Disposition | Mitigation Plan                                                                                                                              |
| --------- | ----------------- | ------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-88   | Denial of Service | `formatEvent` returning `undefined` for an unhandled variant | mitigate    | The missing case is added and no `default` arm is introduced, so the declared `: Formatted` return type keeps the switch compiler-exhaustive |
| T-38-89   | Repudiation       | A future `ActionEventType` shipping without a log formatter  | mitigate    | The typecheck gate in `<acceptance_criteria>` makes the omission a build failure rather than a runtime crash                                 |
| T-38-SC   | Tampering         | npm/pnpm installs                                            | accept      | No new dependency is added by this plan                                                                                                      |

</threat_model>

<verification>

- `pnpm --filter @counter-attack/client typecheck` exits 0.
- `pnpm --filter @counter-attack/client test -- ActionLog` passes.
- The summary records the before/after typecheck output proving exactly one missing case existed and none remain.

</verification>

<success_criteria>

- No corner-kick action can crash the ActionLog.
- The exhaustiveness of `formatEvent` is enforced by the compiler, not by convention.
- The audit for other missing cases is answered with compiler evidence and recorded in the summary.

</success_criteria>

<output>
Create `.planning/phases/38-corner-kick/38-26-SUMMARY.md` when done.
</output>
