---
phase: 38-corner-kick
plan: 28
type: execute
wave: 20
depends_on: [38-25]
gap_closure: true
files_modified:
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/store/useGameStore.test.ts
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/HexGrid.test.tsx
  - packages/client/src/components/CornerKickSetupPanel.tsx
  - packages/client/src/components/CornerKickSetupPanel.test.tsx
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/BallLocationRing.tsx
  - packages/client/src/components/BallLocationRing.test.tsx
  - packages/client/src/components/EventBanner.tsx
  - packages/client/src/components/EventBanner.test.tsx
  - packages/client/src/utils/restartErrorMessage.ts
  - packages/client/src/utils/restartErrorMessage.test.ts
  - packages/shared/src/types.ts
  - packages/shared/src/outOfBounds.ts
  - packages/shared/src/outOfBounds.test.ts
autonomous: true
requirements: [OOB-03, CORNER-01]
must_haves:
  truths:
    - 'No client surface can render, select into, or emit against a corner-kick clear-out phase — the phase value no longer exists in the GamePhase union'
    - 'The Corner Kick restart banner still fires exactly once per corner, now keyed on the attacking goalkeeper reposition phase'
    - 'The ball marker still renders on the corner hex for every remaining corner-kick phase'
    - 'Error copy for the deleted clear-out rejections is gone, while the still-live corner rejection codes keep their player-facing sentences'
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: 'GamePhase union without CORNER_KICK_CLEAR_OUT and GameState without cornerKickClearOutSlot'
    - path: 'packages/client/src/components/EventBanner.tsx'
      provides: 'RESTART_BANNERS re-keyed to the corner sequence entry phase'
      contains: "CORNER_KICK_GK_SETUP_ATTACKING: 'Corner Kick!'"
  key_links:
    - from: 'packages/client/src/components/EventBanner.tsx RESTART_BANNERS'
      to: 'CORNER_KICK_GK_SETUP_ATTACKING'
      via: 'phase-entry banner table, one row per restart family'
      pattern: "CORNER_KICK_GK_SETUP_ATTACKING: 'Corner Kick!'"
---

<objective>
Remove the client-side clear-out surface left stranded by plan 38-25, then delete
`CORNER_KICK_CLEAR_OUT` from the `GamePhase` union, `cornerKickClearOutSlot` from `GameState`, and
`isLegalClearOutStep` from the shared package.

Purpose: `38-24-SUMMARY.md` bug 1 requires the clear-out to be automatic with "no interactive
selection step, no Confirm click". Plan 38-25 removed the server half; this plan removes the client
panel, the piece-selection branches, the phase label, the ball-marker entry and the now-dead error
copy, and re-keys the Corner Kick restart banner so defect 4's banner (38-19) does not silently
disappear along with the phase it was keyed to.

Output: a client with no clear-out code path, a Corner Kick banner that still fires, and a shared
`GamePhase` union with five corner phases instead of six.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38-corner-kick/38-UI-SPEC.md
@.planning/phases/38-corner-kick/38-24-SUMMARY.md
@.planning/phases/38-corner-kick/38-19-SUMMARY.md
@.planning/phases/38-corner-kick/38-22-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols introduced by THIS plan: none — this plan is a deletion and one key rename inside the
existing `RESTART_BANNERS` table.

Symbols this plan DELETES:

- `'CORNER_KICK_CLEAR_OUT'` — member of the `GamePhase` union (`packages/shared/src/types.ts:621`)
- `cornerKickClearOutSlot` — optional `GameState` field (`packages/shared/src/types.ts:~1357`)
- `isLegalClearOutStep` — exported function (`packages/shared/src/outOfBounds.ts:267`)
- `canSelectCornerKickClearOut` and `cornerKickClearOutActingTeam` — locals in `HexGrid.tsx`
- `MUST_CLEAR_CORNER` and `NOT_TOWARD_GOAL` — keys of `RESTART_ERROR_MESSAGES`

Symbols this plan KEEPS (still live after the change, do not delete): `CORNER_EXCLUSION_RADIUS`,
`isWithinCornerExclusionZone`, `cornerClearOutGoalHex`, `cornerClearOutDestination`,
`CORNER_KICK_CLEAR_OUT_MOVE` (the `ActionEvent` variant — still emitted by the automatic clear-out),
and the `CORNER_EXCLUSION_ZONE`, `PIECE_LOCKED`, `NOT_ADJACENT`, `NOT_ELIGIBLE` and
`STAGE_LIMIT_REACHED` error-copy keys.

## Gap-closure source audit

| Source                 | Item                                                              | Covered by    |
| ---------------------- | ----------------------------------------------------------------- | ------------- |
| 38-24-SUMMARY.md bug 1 | No interactive selection step, no Confirm click for the clear-out | Tasks 1, 2    |
| 38-19 / 38-15 defect 4 | The Corner Kick restart banner must survive the phase deletion    | Task 2        |
| CORNER-01              | A corner opens on the goalkeeper reposition windows               | Tasks 2, 3    |
| OOB-03                 | The awarded corner is fully playable from the client              | Tasks 1, 2, 3 |

<tasks>

<task type="auto">
  <name>Task 1: Remove the clear-out selection and valid-hex branches from the store and the grid</name>
  <files>packages/client/src/store/useGameStore.ts, packages/client/src/store/useGameStore.test.ts, packages/client/src/components/HexGrid.tsx, packages/client/src/components/HexGrid.test.tsx</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts lines 918-971 — the whole `if (gameState.phase === 'CORNER_KICK_CLEAR_OUT')` branch inside `selectPiece`, including its `isLegalClearOutStep`/`cornerClearOutGoalHex`/`hexNeighbors` destination computation
    - packages/client/src/store/useGameStore.ts lines 1-60 — the import block, to identify which of `isLegalClearOutStep`, `cornerClearOutGoalHex`, `hexNeighbors`, `isWithinCornerExclusionZone` become unused after the deletion (`isWithinCornerExclusionZone` stays — it is used by the reposition, GK and final-setup branches)
    - packages/client/src/components/HexGrid.tsx lines 155-175 — the `cornerKickClearOutSlot` store selector
    - packages/client/src/components/HexGrid.tsx lines 856-882 — `cornerKickClearOutActingTeam` and `canSelectCornerKickClearOut`
    - packages/client/src/components/HexGrid.tsx lines 1036-1050 — the `isClickable` disjunction containing `canSelectCornerKickClearOut`
    - packages/client/src/components/HexGrid.tsx lines 1125-1140 — the `handleClick` ternary chain arm for `canSelectCornerKickClearOut`
    - packages/client/src/components/HexGrid.tsx lines 1120-1145 — the selection-ring / cursor ternary that also references `canSelectCornerKickClearOut`
    - packages/client/src/store/useGameStore.test.ts and packages/client/src/components/HexGrid.test.tsx — every `CORNER_KICK_CLEAR_OUT` test
  </read_first>
  <action>
    Delete the entire `CORNER_KICK_CLEAR_OUT` branch from `selectPiece` in `useGameStore.ts`,
    including its leading comment block. Remove any import that becomes unused as a result — check
    `isLegalClearOutStep`, `cornerClearOutGoalHex` and `hexNeighbors` specifically, and keep any of
    them that another branch still uses. Do NOT touch the `CORNER_KICK_GK_SETUP_*`,
    `CORNER_KICK_TAKER_SELECT`, `CORNER_KICK_REPOSITION` or `CORNER_KICK_FINAL_SETUP` branches in
    this plan — plan 38-29 owns the reposition branch.

    In `HexGrid.tsx`, delete the `cornerKickClearOutSlot` store selector, the
    `cornerKickClearOutActingTeam` const, the `canSelectCornerKickClearOut` const, the
    `canSelectCornerKickClearOut ||` term from the `isClickable` disjunction, the
    `canSelectCornerKickClearOut ? () => selectPiece(piece.id) :` arm from the `handleClick` chain,
    and the `canSelectCornerKickClearOut` arm from the cursor/ring ternary. Remove imports that
    become unused; `isWithinCornerExclusionZone` and `CORNER_KICK_STAGES` stay.

    Delete every `CORNER_KICK_CLEAR_OUT` test from `useGameStore.test.ts` and `HexGrid.test.tsx`.
    Do not replace them with new tests here — the automatic clear-out has no client behaviour to
    test, and plan 38-25 already covers it server-side.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- useGameStore HexGrid</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CORNER_KICK_CLEAR_OUT'" packages/client/src/store/useGameStore.ts` returns 0
    - `grep -c "CORNER_KICK_CLEAR_OUT'" packages/client/src/components/HexGrid.tsx` returns 0
    - `grep -c "canSelectCornerKickClearOut\|cornerKickClearOutActingTeam\|cornerKickClearOutSlot" packages/client/src/components/HexGrid.tsx` returns 0
    - `grep -c "isWithinCornerExclusionZone" packages/client/src/components/HexGrid.tsx` is still non-zero (the exclusion-zone rule survives)
    - `pnpm --filter @counter-attack/client test -- useGameStore HexGrid` passes
    - `pnpm lint` reports no unused-import errors in the two edited source files
  </acceptance_criteria>
  <done>No piece can be selected into a clear-out interaction and no clear-out destination set can be computed on the client.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the clear-out panel, phase label and ball-marker entry, and re-key the Corner Kick banner</name>
  <files>packages/client/src/components/CornerKickSetupPanel.tsx, packages/client/src/components/CornerKickSetupPanel.test.tsx, packages/client/src/components/GameBoard.tsx, packages/client/src/components/BallLocationRing.tsx, packages/client/src/components/BallLocationRing.test.tsx, packages/client/src/components/EventBanner.tsx, packages/client/src/components/EventBanner.test.tsx, packages/client/src/utils/restartErrorMessage.ts, packages/client/src/utils/restartErrorMessage.test.ts</files>
  <read_first>
    - packages/client/src/components/CornerKickSetupPanel.tsx lines 15-70 — the module doc comment listing the handled phases, the `cornerKickClearOutSlot` selector (~41) and the phase-guard disjunction (~60)
    - packages/client/src/components/CornerKickSetupPanel.tsx lines 124-180 — the whole `if (phase === 'CORNER_KICK_CLEAR_OUT')` branch this task deletes
    - packages/client/src/components/GameBoard.tsx lines 66-75 — the `PHASE_LABEL` corner entries, including `CORNER_KICK_CLEAR_OUT: 'CORNER KICK — CLEAR THE CORNER'` (69)
    - packages/client/src/components/GameBoard.tsx lines 355-375 — the panel dispatch chain whose first corner term is `phase === 'CORNER_KICK_CLEAR_OUT'`
    - packages/client/src/components/BallLocationRing.tsx lines 44-64 — `BALL_MARKER_PHASES` and its 38-22 comment
    - packages/client/src/components/EventBanner.tsx lines 6-29 — the `RESTART_BANNERS` doc comment (which states the table must stay one entry per restart family, T-38-66) and the table itself
    - packages/client/src/utils/restartErrorMessage.ts lines 60-95 — the corner-specific message block, to see exactly which keys belong to the deleted clear-out and which belong to still-live guards
    - The four colocated test files, to find every assertion that names the deleted phase or the deleted message keys
  </read_first>
  <action>
    `CornerKickSetupPanel.tsx`: delete the `if (phase === 'CORNER_KICK_CLEAR_OUT') { ... }` branch in
    full, delete the `cornerKickClearOutSlot` store selector, remove `'CORNER_KICK_CLEAR_OUT'` from
    the component's phase-guard disjunction, and update the module doc comment's phase list. Remove
    the `isWithinCornerExclusionZone` import ONLY if no other branch in the file still uses it.

    `GameBoard.tsx`: delete the `CORNER_KICK_CLEAR_OUT` row from `PHASE_LABEL` and its two-line
    comment, and remove `phase === 'CORNER_KICK_CLEAR_OUT' ||` from the `CornerKickSetupPanel`
    dispatch condition. The remaining five corner phases plus the `PASS`-with-`cornerKickTeam` term
    stay exactly as they are.

    `BallLocationRing.tsx`: remove `'CORNER_KICK_CLEAR_OUT'` from `BALL_MARKER_PHASES` and delete the
    two-line 38-22 comment above it. The five remaining corner phases stay.

    `EventBanner.tsx`: this is the load-bearing edit — change the `RESTART_BANNERS` key
    `CORNER_KICK_CLEAR_OUT` to `CORNER_KICK_GK_SETUP_ATTACKING`, keeping the value `'Corner Kick!'`
    unchanged. That phase is now the corner sequence's entry phase, so the banner still fires exactly
    once per corner and the table stays one-entry-per-restart-family (T-38-66). Update the doc
    comment's parenthetical example so it no longer names a phase that does not exist. Failing to
    make this change would silently delete the Corner Kick banner that 38-19 shipped for 38-15
    defect 4.

    `restartErrorMessage.ts`: delete the `MUST_CLEAR_CORNER` and `NOT_TOWARD_GOAL` entries and the
    38-22 comment that introduces them — both codes were emitted only by the deleted
    `applyCornerKickClearOut`/`applyCornerKickClearOutEnd`. KEEP `CORNER_EXCLUSION_ZONE`,
    `PIECE_LOCKED`, `NOT_ELIGIBLE`, `NOT_ADJACENT` and `STAGE_LIMIT_REACHED` — each is still emitted
    by a live corner guard (`applyCornerKickGkPlace`, `applyCornerKickReposition`,
    `applyCornerKickFinalMove`).

    Update the four colocated test files: delete assertions naming the removed phase or the two
    removed message keys, and add an `EventBanner` test asserting the `'Corner Kick!'` banner fires
    on a broadcast whose phase becomes `CORNER_KICK_GK_SETUP_ATTACKING` and does NOT fire again on
    the subsequent transition to `CORNER_KICK_GK_SETUP_DEFENDING`.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- CornerKickSetupPanel GameBoard BallLocationRing EventBanner restartErrorMessage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "CORNER_KICK_CLEAR_OUT'" packages/client/src/components/ packages/client/src/utils/` returns 0 across all listed files
    - `grep -c "CORNER_KICK_GK_SETUP_ATTACKING: 'Corner Kick!'" packages/client/src/components/EventBanner.tsx` returns 1
    - `grep -c "MUST_CLEAR_CORNER\|NOT_TOWARD_GOAL" packages/client/src/utils/restartErrorMessage.ts` returns 0
    - `grep -c "CORNER_EXCLUSION_ZONE" packages/client/src/utils/restartErrorMessage.ts` returns 1
    - An `EventBanner` test asserts the `Corner Kick!` banner appears once on entry to `CORNER_KICK_GK_SETUP_ATTACKING` and not again on the next corner phase transition
    - `pnpm --filter @counter-attack/client test -- CornerKickSetupPanel GameBoard BallLocationRing EventBanner restartErrorMessage` passes
  </acceptance_criteria>
  <done>The clear-out panel, label, ball-marker entry and dead error copy are gone, and the Corner Kick restart banner still fires exactly once per corner.</done>
</task>

<task type="auto">
  <name>Task 3: Delete CORNER_KICK_CLEAR_OUT, cornerKickClearOutSlot and isLegalClearOutStep from the shared package</name>
  <files>packages/shared/src/types.ts, packages/shared/src/outOfBounds.ts, packages/shared/src/outOfBounds.test.ts</files>
  <read_first>
    - packages/shared/src/types.ts lines 605-640 — the `GamePhase` union's corner block, including the comment above `'CORNER_KICK_CLEAR_OUT'` (620-621) that references `cornerKickClearOutSlot`
    - packages/shared/src/types.ts lines 1340-1360 — the `cornerKickActivatedIds` and `cornerKickClearOutSlot` field declarations and their JSDoc
    - packages/shared/src/outOfBounds.ts lines 250-280 — `isLegalClearOutStep` and its JSDoc, which names its two consumers (both now deleted)
    - packages/shared/src/outOfBounds.test.ts lines 260-305 — the `isLegalClearOutStep` describe block
    - The output of a repo-wide `grep -rn "CORNER_KICK_CLEAR_OUT'\|cornerKickClearOutSlot\|isLegalClearOutStep" packages --include=*.ts --include=*.tsx` run BEFORE editing, to confirm tasks 1 and 2 and plan 38-25 have already removed every consumer
  </read_first>
  <action>
    Run the repo-wide grep named in `<read_first>` first and confirm the only remaining hits are in
    the three files this task edits (plus `packages/shared/dist/`, which is build output and must be
    ignored — never hand-edit `dist/`). If any other source file still matches, fix that file here
    rather than leaving a broken build.

    Delete `'CORNER_KICK_CLEAR_OUT'` from the `GamePhase` union along with the comment paragraph
    above it that describes the clear-out and names `cornerKickClearOutSlot`. Delete the
    `cornerKickClearOutSlot` field declaration and its JSDoc from `GameState`.

    Delete `isLegalClearOutStep` and its JSDoc from `packages/shared/src/outOfBounds.ts`, and delete
    its `describe` block from `outOfBounds.test.ts`. Keep `CORNER_EXCLUSION_RADIUS`,
    `isWithinCornerExclusionZone`, `cornerClearOutGoalHex` and `cornerClearOutDestination` — all four
    are still consumed. Update `cornerClearOutGoalHex`'s JSDoc, which currently says
    "'Closer to goal' in the clear-out rule (`isLegalClearOutStep`) means …", to reference
    `cornerClearOutDestination` instead.

    Rebuild the shared package so the client and server typechecks read fresh declarations:
    `pnpm --filter @counter-attack/shared build`.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "CORNER_KICK_CLEAR_OUT'" packages --include=*.ts --include=*.tsx` produces no output (matches inside `packages/*/dist/` are excluded by rebuilding, not by hand-editing)
    - `grep -rn "cornerKickClearOutSlot\|isLegalClearOutStep" packages/shared/src packages/server/src packages/client/src` produces no output
    - `grep -c "CORNER_KICK_CLEAR_OUT_MOVE" packages/shared/src/types.ts` is still non-zero (the event variant survives the phase deletion)
    - `pnpm typecheck` exits 0 for all three packages
    - `pnpm test` passes for all three packages
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>The clear-out phase and its state field no longer exist anywhere in the type system, and the whole monorepo typechecks, tests and lints clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                                                                                   |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| server broadcast to client  | The client renders whatever `phase` the server sends; removing a phase value from the union makes an impossible phase a compile error rather than a silent render fallthrough |
| game-state change to player | `RESTART_BANNERS` is the only signal that a restart was awarded; re-keying it wrong would silently drop a shipped UX affordance                                               |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                         | Disposition | Mitigation Plan                                                                                                                              |
| --------- | ---------------------- | ----------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-94   | Repudiation            | The Corner Kick restart banner disappearing with its keyed phase  | mitigate    | Task 2 re-keys the row to `CORNER_KICK_GK_SETUP_ATTACKING` and adds a fires-once test; the acceptance criteria assert the literal key        |
| T-38-95   | Denial of Service      | A stranded client branch reading a phase that can no longer occur | mitigate    | Task 3's union deletion turns any missed reference into a compile error, gated by a full `pnpm typecheck`                                    |
| T-38-96   | Information Disclosure | Raw wire codes leaking to the DOM after message-key removal       | mitigate    | Only the two codes that can no longer be emitted are removed; `restartErrorMessage`'s unknown-code fallback still covers anything unexpected |
| T-38-SC   | Tampering              | npm/pnpm installs                                                 | accept      | No new dependency is added by this plan                                                                                                      |

</threat_model>

<verification>

- `pnpm typecheck` exits 0 for shared, server and client.
- `pnpm test` passes for all three packages; `pnpm lint` exits 0.
- `grep -rn "CORNER_KICK_CLEAR_OUT'" packages --include=*.ts --include=*.tsx` produces no output after a shared rebuild.
- An `EventBanner` test proves the Corner Kick banner still fires exactly once per corner.

</verification>

<success_criteria>

- The corner-kick clear-out exists nowhere on the client and nowhere in the type system.
- Every UX affordance the clear-out phase happened to carry (restart banner, ball marker, phase label) is either correctly re-homed or correctly removed.
- The monorepo builds, typechecks, tests and lints clean.

</success_criteria>

<output>
Create `.planning/phases/38-corner-kick/38-28-SUMMARY.md` when done.
</output>
