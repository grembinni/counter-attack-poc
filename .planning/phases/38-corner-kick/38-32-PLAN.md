---
phase: 38-corner-kick
plan: 32
type: execute
wave: 23
depends_on: [38-29, 38-30]
gap_closure: true
files_modified:
  - packages/client/src/components/CornerKickSetupPanel.tsx
  - packages/client/src/components/CornerKickSetupPanel.test.tsx
  - .planning/phases/38-corner-kick/deferred-items.md
  - .planning/todos/pending/2026-08-09-bug-offside-ring-after-goal.md
autonomous: true
requirements: [CORNER-03, CORNER-06]
must_haves:
  truths:
    - "The corner panel's Undo control keeps the exact same enablement contract and visual treatment as the Movement Phase's Undo in ActionPanel — no colour class, no bespoke affordance"
    - 'A test proves the Undo control is disabled while lastDiceRoll is non-null, pinning the coupling that caused the 38-30 regression'
    - 'A test proves the Undo control stays enabled while more than one undoable placement remains, and disables only when none remain'
    - 'The offside-ring-after-goal defect (38-30 bug 1) has an explicit, written, justified triage verdict that survives phase close — it is not silently dropped'
  artifacts:
    - path: 'packages/client/src/components/CornerKickSetupPanel.test.tsx'
      provides: 'Undo enablement and multi-step stack mirror coverage for both corner windows'
      contains: 'lastDiceRoll'
    - path: '.planning/phases/38-corner-kick/deferred-items.md'
      provides: 'The recorded Bug-1 triage verdict and its justification'
      contains: 'offside'
    - path: '.planning/todos/pending/2026-08-09-bug-offside-ring-after-goal.md'
      provides: 'A tracked pending todo so the deferred defect survives Phase 38 close'
  key_links:
    - from: 'packages/client/src/components/CornerKickSetupPanel.tsx'
      to: 'packages/client/src/components/ActionPanel.tsx'
      via: 'identical canUndo guard shape and identical bare styles.ctaButton plus disabled affordance'
      pattern: 'lastDiceRoll'
---

<objective>
Two things this round must record rather than rebuild.

First: lock the corner panel's Undo affordance to the Movement Phase's, with tests that would have caught
`38-30-SUMMARY.md` bug 2. The panel's production code is already correct — `canUndoReposition` and
`canUndoFinalSetup` mirror `ActionPanel`'s `canUndo` exactly, and the Undo button already uses the same bare
`styles.ctaButton` plus `disabled` affordance the Movement Phase uses. The regression was entirely server
state (fixed in 38-31); the test suite missed it because every client fixture seeds `lastDiceRoll` as null.

Second: record the triage verdict for `38-30-SUMMARY.md` bug 1 (offside rings still showing after a goal
resets positions for kick-off). It is deferred out of Phase 38, with the evidence written down so the
follow-up starts with a head start instead of from zero.

Purpose: close the client half of the Undo regression with regression-sensitive coverage, and make the
deferral of bug 1 an explicit, auditable decision rather than a silent omission.

Output: four new panel tests, one clarifying comment, one deferred-items entry, one pending todo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-corner-kick/38-UI-SPEC.md
@.planning/phases/38-corner-kick/38-30-SUMMARY.md
@.planning/phases/38-corner-kick/38-29-SUMMARY.md
@.planning/phases/38-corner-kick/deferred-items.md
</context>

## Artifacts this phase produces

- `packages/client/src/components/CornerKickSetupPanel.tsx` — comment-only change on the two `canUndo*`
  IIFEs naming the server-side contract they depend on. No behavioural change, no new props, no new exports.
- `packages/client/src/components/CornerKickSetupPanel.test.tsx` — four new test cases
- `.planning/phases/38-corner-kick/deferred-items.md` — new section `## From Plan 38-32 (round-4 triage of 38-30 bug 1)`
- `.planning/todos/pending/2026-08-09-bug-offside-ring-after-goal.md` — new file

No new symbols, no new CSS classes, no new phases, no new events, no new CLI flags.

## Gap-closure source audit

| Source                             | Item                                                                          | Covered by      |
| ---------------------------------- | ----------------------------------------------------------------------------- | --------------- |
| REQ CORNER-03                      | The 2-2-2 window's Undo affordance must match a normal Movement Phase         | Task 1          |
| REQ CORNER-06                      | The pre-kick window's Undo affordance must match a normal Movement Phase      | Task 1          |
| CONTEXT D-09                       | Panel conventions carried from Phase 35 — no bespoke Undo styling introduced  | Task 1          |
| 38-30-SUMMARY bug 2, sub-finding 1 | "Does not light up" — pin the affordance and the enablement coupling in tests | Task 1          |
| 38-30-SUMMARY bug 2, sub-finding 3 | Multi-step stack — client mirror stays enabled while placements remain        | Task 1          |
| 38-30-SUMMARY bug 1                | Offside ring not cleared after goal to kickoff reset                          | Task 2 (defer)  |
| GOAL (ROADMAP Phase 38 goal)       | No goal item is deferred; bug 1 falls outside the corner-kick goal statement  | Task 2 (stated) |

<tasks>

<task type="auto">
  <name>Task 1: Pin the corner Undo affordance and its enablement contract with regression-sensitive tests</name>
  <files>packages/client/src/components/CornerKickSetupPanel.tsx, packages/client/src/components/CornerKickSetupPanel.test.tsx</files>
  <read_first>
    - `packages/client/src/components/CornerKickSetupPanel.tsx` — the `CORNER_KICK_REPOSITION` branch
      (the `canUndoReposition` IIFE and its Undo button, rendered as
      `className={styles.ctaButton} disabled={!canUndoReposition} onClick={emitUndo}`) and the
      `CORNER_KICK_FINAL_SETUP` branch (`canUndoFinalSetup`, which scans for `CORNER_KICK_MOVE` rather than
      `MOVE`, with only `CORNER_KICK_STAGE_ADVANCE` as its boundary).
    - `packages/client/src/components/ActionPanel.tsx` — the shared `canUndo` IIFE, which opens with
      `if (lastDiceRoll) return false;`, and its Undo buttons in the `HIGH_PASS_MOVE`,
      `FIRST_TIME_PASS_MOVE` and MOVE branches. Confirm each uses the bare `styles.ctaButton` with
      `disabled={!canUndo}` and NO ready/pending colour class. This is the "same as a normal move phase"
      affordance the verifier asked for; corner already matches it.
    - `packages/client/src/components/CornerKickSetupPanel.module.css` — `.ctaButton`, the
      `.ctaButton:disabled` rule (`opacity: 0.5; cursor: default;`), `.ctaButtonReady`, `.ctaButtonPending`.
      The 0.5 to 1.0 opacity transition IS the "lights up" affordance, and it is identical to ActionPanel's.
    - `packages/client/src/components/CornerKickSetupPanel.test.tsx` — the `cornerKickState()` helper, which
      spreads `mockMovementState` and therefore inherits its null `lastDiceRoll`. That is the fixture blind
      spot that let the regression ship. Also read the existing Undo tests: "renders an Undo button for the
      acting manager", "Undo is disabled with no MOVE after the last stage boundary", "Undo is enabled and
      emits GAME_UNDO once a MOVE exists in the current stage", and the two `CORNER_KICK_FINAL_SETUP`
      equivalents.
    - `packages/server/src/gameEngine.ts` — `applyUndo`'s `CORNER_KICK_REPOSITION` boundary reduce and its
      `moveTypeForPhase` chain, to confirm the client mirror's boundary and move types still match exactly.
  </read_first>
  <action>
    Make no behavioural change to the panel. Specifically: do NOT delete or weaken
    `if (lastDiceRoll) return false;` in either IIFE, and do NOT add `ctaColorClass`, `ctaButtonReady` or
    `ctaButtonPending` to the Undo button. The verifier asked for parity with a normal Movement Phase, and
    `ActionPanel.tsx`'s Undo buttons deliberately use the bare `styles.ctaButton` with `disabled`. Adding a
    colour class here would diverge from that parity and from CONTEXT D-09.

    Comment change only, in `CornerKickSetupPanel.tsx`. Extend the existing "UX mirror only" comment above
    each of `canUndoReposition` and `canUndoFinalSetup` to name the server contract they now depend on:
    `lastDiceRoll` is nulled on entry to `CORNER_KICK_REPOSITION` by `applyCornerKickTakerSelect`, and on
    entry to `CORNER_KICK_FINAL_SETUP` by `applyCornerKickStageEnd`'s terminal branch, as of plan 38-31.
    Before that fix this guard short-circuited for the entire corner sequence and the control could never
    enable (`38-30-SUMMARY.md` bug 2). State that the guard itself is intentional and mirrors
    `ActionPanel.tsx`'s `canUndo`, so the correct fix for any future "Undo never enables" report in a new
    restart family is to null `lastDiceRoll` at that family's reversible-window entry, never to drop the
    guard.

    Add four tests to `CornerKickSetupPanel.test.tsx`, beside the existing Undo tests.

    Test (a) — `CORNER_KICK_REPOSITION` with a `CORNER_KICK_TAKER_PLACED` boundary AND a following `MOVE` in
    the event log, but `lastDiceRoll` overridden to `{ rolls: [3, 4], context: 'LOOSE_BALL' }`: the Undo
    button has `disabled === true`. Title it as the pinned coupling, referencing `38-30-SUMMARY.md` bug 2
    sub-finding 1 and plan 38-31 as the server-side fix that keeps this null in real play.

    Test (b) — `CORNER_KICK_REPOSITION` multi-step mirror. Event log
    `[CORNER_KICK_TAKER_PLACED, MOVE(away-1), MOVE(away-2)]` gives `disabled === false`. Re-seed the store
    with the same log minus the last `MOVE` (simulating the server's post-undo broadcast) and assert still
    `disabled === false`. Re-seed with the boundary only and assert `disabled === true`. Assert all three
    states in one test body so the stepped stack reads as a single behaviour; use whatever
    cleanup-and-re-render idiom the file's existing tests already use between re-seeds.

    Test (c) — `CORNER_KICK_FINAL_SETUP`: the same non-null `lastDiceRoll` disabled assertion as (a), but
    with a `CORNER_KICK_STAGE_ADVANCE` boundary and a following `CORNER_KICK_MOVE`.

    Test (d) — affordance parity. In `CORNER_KICK_REPOSITION` with one undoable `MOVE`, assert the Undo
    button's `className` contains the `ctaButton` class and contains NEITHER `ctaButtonReady` NOR
    `ctaButtonPending`, while the sibling Confirm button in the same render DOES carry one of them. This
    pins "same as a normal move phase" against a future well-meaning edit.

    Use the existing `cornerKickState(phase, overrides)` helper with an explicit `lastDiceRoll` override
    where needed. Do not change the helper's defaults — other tests depend on them.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- CornerKickSetupPanel</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- CornerKickSetupPanel` passes with 4 more tests than before this task
    - Test (a) fails if `if (lastDiceRoll) return false;` is deleted from `canUndoReposition` — record in the summary that this was checked by temporarily deleting the line and re-running
    - Test (b) asserts the button's `disabled` value at three distinct event-log states within one test body, in the order two-moves then one-move then zero-moves
    - Test (d) asserts the Undo button's `className` does not match `/ctaButtonReady|ctaButtonPending/` and the Confirm button's `className` does match it
    - `git diff packages/client/src/components/CornerKickSetupPanel.tsx` shows comment-only changes — no changed executable line and no changed JSX attribute
    - `pnpm --filter @counter-attack/client typecheck` exits 0
    - `pnpm --filter @counter-attack/client test` is green overall with no pre-existing test regressed
  </acceptance_criteria>
  <done>The corner Undo affordance and its `lastDiceRoll` coupling are pinned by tests that fail if either the guard or the Movement-Phase-parity styling is changed.</done>
</task>

<task type="auto">
  <name>Task 2: Record the DEFERRED triage verdict for the offside-ring-after-goal defect (38-30 bug 1)</name>
  <files>.planning/phases/38-corner-kick/deferred-items.md, .planning/todos/pending/2026-08-09-bug-offside-ring-after-goal.md</files>
  <read_first>
    - `.planning/phases/38-corner-kick/deferred-items.md` — the existing section headings (`## From Plan
      38-06 ...`, `## From Plan 38-14 ...`, `## From Plan 38-19 ...`) and their evidence-first house style:
      state the finding, cite the exact files and symbols checked, state why it is out of the discovering
      plan's scope, name the extension point for whoever picks it up.
    - `.planning/todos/pending/2026-07-02-bug-kickoff-setup-persistent-light-shading-on-shot-path-hexes.md`
      — copy this file's frontmatter shape (`created`, `title`, `area`, `resolves_phase`, `files`) and its
      `## Problem` / `## What We Know` structure. Read its "Server side — all paths clear lastShotPath null
      before KICK_OFF_SETUP" section closely: it is the SAME signature as this new defect (server provably
      clears the field, client still renders it for the whole KICK_OFF_SETUP phase). That is the strongest
      single piece of triage evidence and must be cited in the new todo.
    - `.planning/phases/38-corner-kick/38-30-SUMMARY.md` — the verbatim bug 1 text and the routing
      instruction requiring an explicit in-scope or out-of-scope decision.
  </read_first>
  <action>
    Write the triage verdict — DEFERRED out of Phase 38 — in two places. Do not attempt a fix in this plan,
    and do not modify any file under `packages/`.

    Add a new section to `deferred-items.md` headed `## From Plan 38-32 (round-4 triage of 38-30 bug 1)`,
    recording the verdict and this evidence. All of it was established by static analysis during planning
    and must be restated so the follow-up does not repeat it:

    - The verbatim report: "after goal is scored and player positions are reset for kick off players still
      showed offsides rings when they should be reset."
    - The rendering path is `HexGrid.tsx`'s `isOffside={(offsidePieceIds ?? []).includes(piece.id)}` feeding
      `PieceOverlay`'s independent red-ring layer, reading `gameState.offsidePieceIds` straight from the
      server broadcast. There is no client-side offside derivation that could go stale on its own.
    - Both live goal paths in `applyRoll`'s SHOT branch (the GK-out-of-range GOAL and the duel GOAL) already
      set `offsidePieceIds: []` on the `KICK_OFF_SETUP` transition, each carrying the `BUG-06 / D-47`
      comment, and both are covered by passing tests: the
      `BUG-06: offsidePieceIds reset on GOAL restart path (applyRoll SHOT branch)` describe block in
      `offside.test.ts`, and the D-47 both-ready reset test in `kickoffSetup.integration.test.ts`.
    - `grep -n "state.score\[" packages/server/src/gameEngine.ts` returns exactly those two scoring sites,
      so there is no third goal path that could bypass the reset.
    - `buildReplayFrames`' seed object does not include `offsidePieceIds` at all, so replay frames carry
      `undefined` and can never render an offside ring; and the client's `setGameState` replaces `gameState`
      wholesale rather than merging, so a replay frame cannot resurrect a stale live value either.
    - Conclusion: the root cause is NOT in the goal-to-kickoff reset code path as first assumed, and it is
      NOT in any file touched by 38-25 through 38-29 (`applyCornerKickReposition`,
      `applyAutomaticCornerClearOut`, `offside.ts`'s `CORNER_KICK_STAGES` docs, `ActionLog.tsx`,
      `useGameStore.ts`, `HexGrid.tsx`'s corner arms, `CornerKickSetupPanel.tsx`, `EventBanner.tsx`). No
      Phase 38 plan touches the offside lifecycle at all.
    - Strongest lead for the follow-up: this defect has the same signature as the long-pending BUG-23 todo
      (`2026-07-02-bug-kickoff-setup-persistent-light-shading-on-shot-path-hexes.md`) — server provably
      nulls the field, yet the client renders it for the entire `KICK_OFF_SETUP` phase. BUG-23 has been
      carried unresolved across four milestone closes. Treat a shared root cause as the leading hypothesis.
    - Why deferring rather than fixing here: root-causing needs live two-browser reproduction with the exact
      scoring action captured, not a code edit — an open-ended investigation. Phase 38's requirement set is
      OOB-03 and CORNER-01 through CORNER-06; none covers offside or kickoff. Bundling an open-ended
      investigation into the final gap-closure round would block a phase that is otherwise one surgical fix
      from closing.
    - Route: `/gsd-debug` against the tracked todo, or a Phase 39 bug-fix item. Explicitly NOT a further
      Phase 38 gap-closure round.

    Create the pending todo file at `.planning/todos/pending/2026-08-09-bug-offside-ring-after-goal.md`
    with frontmatter mirroring the BUG-23 todo: `created: 2026-08-09T00:00:00.000Z`, a `title` of
    `Bug — offside rings still rendered after a goal resets positions for kick-off`, `area: rendering`,
    `resolves_phase: null`, and a `files:` list naming `packages/client/src/components/HexGrid.tsx`,
    `packages/client/src/components/PieceOverlay.tsx`, `packages/client/src/store/useGameStore.ts` and
    `packages/server/src/gameEngine.ts`.

    Body sections, in this order:
    - `## Problem` — the verbatim report plus the observation context (found during Phase 38 UAT, plan
      38-30's step 9 "Visual check and replay", by the human two-browser verifier).
    - `## What We Know` — the same evidence bullets as the deferred-items entry above.
    - `## Leading Hypothesis` — the BUG-23 shared-root-cause link, stated as a hypothesis, not a conclusion.
    - `## Repro Data Needed` — exactly what the next session must capture: which action scored the goal
      (regular shot, snapshot, or header-at-goal); whether the observation was live or during replay;
      whether the rings clear after the first kickoff move or persist into MOVE; and a devtools read of
      `useGameStore.getState().gameState.offsidePieceIds` at the moment the rings are visible on screen.

    Cross-reference the two files in both directions: the deferred-items entry names the todo filename, and
    the todo names both `.planning/phases/38-corner-kick/deferred-items.md` and `38-30-SUMMARY.md`. Neither
    should be findable without the other.

  </action>
  <verify>
    <automated>grep -q "From Plan 38-32" .planning/phases/38-corner-kick/deferred-items.md && grep -q "offsidePieceIds" .planning/todos/pending/2026-08-09-bug-offside-ring-after-goal.md && echo TRIAGE_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `TRIAGE_RECORDED`
    - `deferred-items.md` contains a section headed `## From Plan 38-32 (round-4 triage of 38-30 bug 1)` that states the word DEFERRED and names the route (`/gsd-debug` or Phase 39, explicitly not a further Phase 38 gap-closure round)
    - That section cites all five evidence points by name: `HexGrid.tsx`'s `isOffside` prop, the two `offsidePieceIds: []` GOAL branches, the BUG-06 describe block in `offside.test.ts`, the D-47 test in `kickoffSetup.integration.test.ts`, and `buildReplayFrames`' seed omitting `offsidePieceIds`
    - The new todo file has frontmatter keys `created`, `title`, `area`, `resolves_phase` and `files`, and body sections `## Problem`, `## What We Know`, `## Leading Hypothesis` and `## Repro Data Needed`
    - The todo's `## Problem` section contains the verifier's verbatim sentence
    - `git status --porcelain packages/` reports no modified files from this task
  </acceptance_criteria>
  <done>The offside-ring defect has a written, justified DEFERRED verdict in `deferred-items.md` and a tracked pending todo carrying every piece of evidence already established, so it survives Phase 38 close and the follow-up does not restart the investigation.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                                                                                                                         |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client panel to server authority   | `canUndoReposition` / `canUndoFinalSetup` are UX mirrors only; `applyUndo` plus the `GAME_UNDO` handler's `validUndoPhases` and `isActivePlayer` checks remain the sole enforcement layer. This plan changes no client-side gating. |
| planning record to future sessions | A deferred defect that is not written down is indistinguishable from a dropped one; Task 2 exists so the deferral is auditable at phase close and at milestone close.                                                               |

## STRIDE Threat Register

| Threat ID | Category    | Component                                                                        | Disposition | Mitigation Plan                                                                                                                                                 |
| --------- | ----------- | -------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-111  | Tampering   | Client Undo enablement treated as an authorisation gate                          | accept      | No new attack surface. The panel change is comment-only; a client that forces the button enabled still hits `applyUndo`'s boundary and phase guards server-side |
| T-38-112  | Repudiation | A UAT-reported defect being silently dropped at close                            | mitigate    | Task 2 writes the verdict to two tracked locations, one of which (`.planning/todos/pending/`) is scanned by the milestone-close artifact audit                  |
| T-38-113  | Tampering   | A later edit restoring bespoke Undo styling or removing the `lastDiceRoll` guard | mitigate    | Tests (a) and (d) fail on either change                                                                                                                         |
| T-38-SC   | Tampering   | npm/pnpm installs                                                                | accept      | No dependency is added or changed; `pnpm install` may only be run to restore an absent `node_modules`                                                           |

No new attack surface: this plan adds no endpoint, no event type, no phase, no client-supplied input path,
and no production-code behaviour change.

</threat_model>

<verification>

- `pnpm --filter @counter-attack/client typecheck` exits 0.
- `pnpm --filter @counter-attack/client test` is green, with 4 new tests in `CornerKickSetupPanel.test.tsx`.
- `git diff packages/client/src/components/CornerKickSetupPanel.tsx` contains comment-only changes.
- `.planning/phases/38-corner-kick/deferred-items.md` gains the round-4 triage section.
- `.planning/todos/pending/2026-08-09-bug-offside-ring-after-goal.md` exists and is cross-referenced.

</verification>

<success_criteria>

- The corner Undo control's enablement contract and its Movement-Phase-parity styling are both covered by
  tests that fail if either is changed.
- The `lastDiceRoll` coupling that caused the regression is explicit in both the panel comments and a test.
- The offside-ring defect reported in `38-30-SUMMARY.md` is deferred by an explicit, justified, written
  decision with a tracked todo — never silently omitted.

</success_criteria>

<output>
Create `.planning/phases/38-corner-kick/38-32-SUMMARY.md` when done, recording the four new tests, the
confirmation that the panel diff is comment-only, and the DEFERRED verdict for 38-30 bug 1 with its
one-line justification and route.
</output>
