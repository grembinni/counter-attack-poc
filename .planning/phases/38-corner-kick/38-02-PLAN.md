---
phase: 38-corner-kick
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.cornerKick.test.ts
autonomous: true
requirements: [OOB-03, CORNER-01, CORNER-02]
must_haves:
  truths:
    - 'A byline exit after a defending touch awards a corner kick to the attacking team'
    - 'The corner restart resolves to the fixed hex on the corner nearest the exit, relocating if occupied'
    - "The attacking manager's goalkeeper is repositioned before the defending manager's"
    - 'The kicking manager can choose any of their own on-pitch pieces as corner-taker'
    - 'The chosen corner-taker and the ball both end up on the resolved corner hex'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'triggerOutOfBoundsRestart CORNER_KICK branch, applyCornerKickGkPlace, applyCornerKickGkWindowEnd, applyCornerKickTakerSelect'
      contains: 'applyCornerKickTakerSelect'
    - path: 'packages/server/src/__tests__/gameEngine.cornerKick.test.ts'
      provides: 'unit coverage for the corner trigger, GK reposition pair, and taker selection'
      contains: 'triggerOutOfBoundsRestart'
  key_links:
    - from: 'packages/server/src/gameEngine.ts'
      to: 'packages/shared/src/outOfBounds.ts'
      via: 'CORNER_KICK_HEX import + resolveThrowInHex occupancy resolution'
      pattern: 'CORNER_KICK_HEX'
    - from: 'triggerOutOfBoundsRestart'
      to: 'CORNER_KICK_GK_SETUP_ATTACKING'
      via: 'phase transition on a CORNER_KICK classification'
      pattern: 'CORNER_KICK_GK_SETUP_ATTACKING'
---

<objective>
Replace `triggerOutOfBoundsRestart`'s explicitly-marked `CORNER_KICK` dead-end with a real
corner-kick trigger, then build the first two steps of the chain: the turn-based goalkeeper
reposition pair (CORNER-01) and the corner-taker selection/placement (CORNER-02).

Per D-07 these are Corner Kick's own dedicated phase values (not `GK_RESTART`/Goal Kick reuse),
and the fixed hex geometry follows D-01/D-02.

Purpose: OOB-03's classification half already shipped in Phase 37 — `classifyOutOfBounds`
returns `'CORNER_KICK'` correctly today and must not be touched. This plan builds the consumer
branch that acts on it and the two setup steps that D-04 places before the reposition windows.

Output: 4 new pure engine functions plus the trigger branch, with unit coverage in a new
`gameEngine.cornerKick.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/38-corner-kick/38-CONTEXT.md
@.planning/phases/38-corner-kick/38-RESEARCH.md
@.planning/phases/38-corner-kick/38-PATTERNS.md
@.planning/phases/38-corner-kick/38-01-SUMMARY.md
</context>

## Artifacts this phase produces

See `.planning/phases/38-corner-kick/38-01-PLAN.md` § "Artifacts this phase produces" for the
full phase-wide symbol list. Symbols introduced by **this plan**:

- `applyCornerKickGkPlace` (function, `packages/server/src/gameEngine.ts`)
- `applyCornerKickGkWindowEnd` (function, `packages/server/src/gameEngine.ts`)
- `applyCornerKickTakerSelect` (function, `packages/server/src/gameEngine.ts`)
- `ApplyCornerKickGkPlaceResult`, `ApplyCornerKickGkWindowEndResult`,
  `ApplyCornerKickTakerSelectResult` (exported result types)
- `packages/server/src/__tests__/gameEngine.cornerKick.test.ts` (new file)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the CORNER_KICK dead-end in triggerOutOfBoundsRestart (OOB-03)</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.cornerKick.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 3183-3340 (`triggerOutOfBoundsRestart` in full: the `CORNER_KICK` dead-end at 3218, the `commonReset` block at 3220-3230, the `THROW_IN` branch at 3232-3269, and the `GOAL_KICK` branch at 3271-3339 which is the structural template)
    - packages/shared/src/outOfBounds.ts (`classifyExit`, `bylineOwner`, `classifyOutOfBounds`, `resolveThrowInHex`, `GOAL_KICK_RESTART_HEX`, and the new `CORNER_KICK_HEX`)
    - packages/server/src/__tests__/gameEngine.outOfBounds.test.ts (existing trigger-branch test setup helpers and assertion style — reuse its state fixtures rather than inventing new ones)
    - packages/shared/src/hex.ts (`hexDistance` signature)
    - .planning/phases/38-corner-kick/38-RESEARCH.md §1 (the team-inversion caveat)
  </read_first>
  <behavior>
    - A `BYLINE` exit over the home byline whose `ball.lastTouchedBy.teamId === 'home'` returns a state with `cornerKickTeam === 'away'` (team inversion: `owner` is the DEFENDING team)
    - The same exit over the away byline with a last touch by `'away'` returns `cornerKickTeam === 'home'`
    - The returned state's `phase` is `'CORNER_KICK_GK_SETUP_ATTACKING'`
    - `cornerKickHex` equals `CORNER_KICK_HEX[owner].top` when `lastInBoundsHex` is nearer the top corner, and `.bottom` when nearer the bottom corner; an exact tie resolves to `top`
    - When the resolved corner hex is already occupied by a piece, `cornerKickHex` is the nearest free on-pitch hex returned by `resolveThrowInHex` and no two pieces share a coordinate
    - `attackingTeam` and `activeTeam` are both set to `cornerKickTeam`
    - `ball.position` is the resolved corner hex, `ball.carrierId` is `null` (no taker chosen yet), and `ball.lastTouchedBy` is preserved unchanged from the incoming state
    - An `OUT_OF_BOUNDS` `ActionEvent` is appended with `restart: 'CORNER_KICK'` and `awardedTo: cornerKickTeam`
    - All `commonReset` fields are applied and every `cornerKick*` field is initialized (no stale value from a prior corner survives)
    - A `SIDELINE` exit still returns the `THROW_IN` state and a byline exit after an ATTACKING touch still returns the `GOAL_KICK` state — both unchanged
  </behavior>
  <action>
    In `packages/server/src/gameEngine.ts`, delete the line `if (restart === 'CORNER_KICK') return null;`
    (currently line 3218) together with its four-line Phase-38 hook comment, and add a real
    `if (restart === 'CORNER_KICK') { ... }` branch in its place, positioned after the
    `commonReset` declaration and before the `THROW_IN` branch so it can use `commonReset`.

    Critical team inversion (do NOT copy the `GOAL_KICK` branch's `const goalKickTeam = owner`):
    for a corner, `owner` (the byline's own team) is the team whose defender touched the ball
    last, so the corner is awarded to the OPPOSITE team. Compute
    `const cornerKickTeam = owner === 'home' ? 'away' : 'home'` after a
    `if (owner === null) return null` defensive guard (a BYLINE exit always has an owner).

    Corner selection: compare `hexDistance(lastInBoundsHex, CORNER_KICK_HEX[owner].top)` against
    `hexDistance(lastInBoundsHex, CORNER_KICK_HEX[owner].bottom)` and pick the nearer; on an
    exact tie pick `top` (deterministic default). Index `CORNER_KICK_HEX` by `owner` — the
    byline owner — never by `cornerKickTeam`. Resolve the chosen hex against occupancy with
    `resolveThrowInHex(preferredCornerHex, state.pieces)` exactly as the `GOAL_KICK` branch does
    with `GOAL_KICK_RESTART_HEX`. Pass the FULL piece list (unlike the goal-kick branch, which
    excludes the GK) because no piece has been designated the taker yet at trigger time.

    Return a state literal shaped like the `GOAL_KICK` branch's, setting:
    `phase: 'CORNER_KICK_GK_SETUP_ATTACKING'`, `cornerKickTeam`, `cornerKickHex: resolvedCornerHex`,
    `cornerKickTakerId: null`, `cornerKickEligibleIds: null`, `cornerKickStageIndex: null`,
    `cornerKickStagePlacedIds: null`, `cornerKickUsedPace: null`, `cornerKickMoveSlot: null`,
    `cornerKickMovedPieceId: null`, `cornerKickPaceUsed: 0`,
    `attackingTeam: cornerKickTeam`, `activeTeam: cornerKickTeam`,
    `ball: { position: resolvedCornerHex, carrierId: null, lastTouchedBy: state.ball.lastTouchedBy }`,
    the appended `OUT_OF_BOUNDS` event (`kind: exit`, `restart`, `awardedTo: cornerKickTeam`,
    `lastTouchedByPieceId: state.ball.lastTouchedBy?.pieceId ?? null`,
    `ballAfter: { position: resolvedCornerHex, carrierId: null }`), and `...commonReset`.
    Explicitly null every `cornerKick*` field even though `...state` spreads — Pitfall 3 requires
    no stale value from a prior corner to leak into this one.

    Do NOT modify `classifyOutOfBounds`, `classifyExit`, `bylineOwner`, or `resolveThrowInHex`.
    Do NOT modify the `THROW_IN` or `GOAL_KICK` branches. Update the function's JSDoc (3187-3202)
    to remove the "Returns null on a CORNER_KICK classification" claim, which is no longer true.

    Create `packages/server/src/__tests__/gameEngine.cornerKick.test.ts` and cover every behavior
    listed above, plus explicit regression assertions that a sideline exit still produces
    `THROW_IN_SETUP` and a byline exit after an attacking touch still produces
    `GOAL_KICK_SETUP_GK`. Reuse the fixture/state-builder helpers already present in
    `gameEngine.outOfBounds.test.ts` rather than duplicating them.

  </action>
  <acceptance_criteria>
    - `grep -c "if (restart === 'CORNER_KICK') return null;" packages/server/src/gameEngine.ts` returns 0
    - `grep -c "CORNER_KICK_GK_SETUP_ATTACKING" packages/server/src/gameEngine.ts` returns at least 1
    - `git diff packages/shared/src/outOfBounds.ts` is empty for this task
    - `pnpm --filter @counter-attack/server test -- gameEngine.cornerKick` exits 0
    - `pnpm --filter @counter-attack/server test -- gameEngine.outOfBounds` still exits 0 (no throw-in/goal-kick regression)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- gameEngine.cornerKick &amp;&amp; pnpm --filter @counter-attack/server test -- gameEngine.outOfBounds</automated>
  </verify>
  <done>A byline exit after a defending touch returns a `CORNER_KICK_GK_SETUP_ATTACKING` state awarded to the opposite team from the byline owner, with the correct occupancy-resolved corner hex; throw-in and goal-kick behaviour is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn-based goalkeeper reposition pair (CORNER-01, D-03/D-04)</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.cornerKick.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 3341-3430 (`applyThrowInPlace` and its `ApplyThrowInPlaceResult` discriminated union — the one-shot teleport shape and guard ordering to copy)
    - packages/server/src/gameEngine.ts lines 5383-5460 (`applyFreeKickMove`'s pick-up-and-place-anywhere branch — the uncapped placement model, which validates target legality without any `hexDistance` cap)
    - packages/server/src/gameEngine.ts lines 3621-3700 (`applyGoalKickWindowEnd` — the window-to-window advance shape and its event emission)
    - packages/shared/src/pitch.ts (`isPitchHex`)
    - .planning/phases/38-corner-kick/38-RESEARCH.md §3 and Assumptions Log A1 (uncapped placement is an ASSUMED reading of CORNER-01's silence on a hex cap; confirmed at the 38-09 checkpoint)
  </read_first>
  <behavior>
    - `applyCornerKickGkPlace` rejects with `WRONG_PHASE` outside `CORNER_KICK_GK_SETUP_ATTACKING`/`CORNER_KICK_GK_SETUP_DEFENDING`
    - It rejects with `PIECE_NOT_FOUND` for an unknown piece id and `NOT_GOALKEEPER` for a piece whose `role !== 'GK'`
    - It rejects with `WRONG_TEAM` when the piece's team is not the team whose window is active (attacking team during `_ATTACKING`, defending team during `_DEFENDING`)
    - It rejects with `INVALID_TARGET` for an off-pitch hex (`isPitchHex` false) or a hex occupied by any other piece
    - On success the goalkeeper's position becomes the target hex regardless of distance, and a `CORNER_KICK_GK_PLACE` event is appended with the correct `side`
    - Re-placing the same goalkeeper again within the same window is allowed and overwrites the previous position
    - `applyCornerKickGkWindowEnd` advances `CORNER_KICK_GK_SETUP_ATTACKING` to `CORNER_KICK_GK_SETUP_DEFENDING` with `activeTeam` flipped to the defending team
    - `applyCornerKickGkWindowEnd` advances `CORNER_KICK_GK_SETUP_DEFENDING` to `CORNER_KICK_TAKER_SELECT` with `activeTeam` set back to `cornerKickTeam`
    - Confirming with zero placements made is legal in both windows (D-06)
    - `ball.position`, `ball.carrierId` and `cornerKickHex` are unchanged by both functions
  </behavior>
  <action>
    Add `applyCornerKickGkPlace(state: GameState, pieceId: string, to: HexCoord)` and
    `applyCornerKickGkWindowEnd(state: GameState)` to `packages/server/src/gameEngine.ts`,
    placed immediately after the new corner trigger branch's containing function so the whole
    corner chain reads top-to-bottom in source order.

    Export a discriminated result union `ApplyCornerKickGkPlaceResult` with
    `{ ok: false; reason: 'WRONG_PHASE' | 'PIECE_NOT_FOUND' | 'NOT_GOALKEEPER' | 'WRONG_TEAM' | 'INVALID_TARGET' }`
    and `{ ok: true; state: GameState }`, and `ApplyCornerKickGkWindowEndResult` with
    `{ ok: false; reason: 'WRONG_PHASE' }` / `{ ok: true; state: GameState }`. Guard ordering
    mirrors `applyThrowInPlace`: phase guard first, then piece lookup, then role check, then team
    ownership, then target legality — so the caller always receives the most specific reason.

    D-03/D-04 turn order: the ATTACKING (kicking) manager's goalkeeper moves first. Derive the
    window's acting team from the phase — `CORNER_KICK_GK_SETUP_ATTACKING` acts as
    `state.cornerKickTeam`, `CORNER_KICK_GK_SETUP_DEFENDING` acts as its opposite. Never read
    `activeTeam` for this decision; set `activeTeam` from the derived value instead.

    Placement model (A1, uncapped): validate only that the target is on-pitch via `isPitchHex`
    and unoccupied by any piece other than the goalkeeper being placed. Deliberately do NOT apply
    any `hexDistance` cap — CORNER-01 states no cap where CORNER-03 and CORNER-06 both state one
    explicitly. Add a source comment marking this as RESEARCH.md Assumption A1, naming the exact
    single-line change needed if the rulebook turns out to cap it (add a `hexDistance(gk.position, to) > CAP`
    guard alongside the `isPitchHex` check).

    `applyCornerKickGkWindowEnd` performs no move validation of its own — it only advances the
    phase and `activeTeam`, appending nothing (no event is needed; the individual
    `CORNER_KICK_GK_PLACE` events already record the moves, and there is no reversible-move
    boundary to protect here since Undo does not reach these phases). Confirming with zero
    placements is explicitly legal (D-06).

    Extend `gameEngine.cornerKick.test.ts` with a `describe` block covering every behavior above,
    including the negative guards and the zero-placement confirm path.

  </action>
  <acceptance_criteria>
    - `grep -c "export function applyCornerKickGkPlace" packages/server/src/gameEngine.ts` returns 1
    - `grep -c "export function applyCornerKickGkWindowEnd" packages/server/src/gameEngine.ts` returns 1
    - Source contains a comment string `Assumption A1` adjacent to the placement legality check
    - Tests assert all five `ok: false` reasons are reachable and distinct
    - `pnpm --filter @counter-attack/server test -- gameEngine.cornerKick` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- gameEngine.cornerKick</automated>
  </verify>
  <done>Both goalkeeper windows run attacking-team-first, accept an uncapped legal on-pitch placement, reject wrong-team/wrong-role/illegal-target attempts, and hand off to `CORNER_KICK_TAKER_SELECT`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Corner-taker selection and placement at the fixed hex (CORNER-02, D-01)</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.cornerKick.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 3341-3430 (`applyThrowInPlace` in full — this is the exact shape to copy: phase guard, piece lookup, team-ownership check, unconditional teleport to a server-owned hex)
    - packages/server/src/gameEngine.ts (the new corner trigger branch from Task 1 — `cornerKickHex` is already occupancy-resolved there)
    - packages/shared/src/outOfBounds.ts (`resolveThrowInHex`)
    - .planning/phases/38-corner-kick/38-RESEARCH.md §4
    - .planning/phases/38-corner-kick/38-CONTEXT.md (D-01: no placement-choice UI; the hex is fixed and server-owned)
  </read_first>
  <behavior>
    - Rejects with `WRONG_PHASE` outside `CORNER_KICK_TAKER_SELECT`
    - Rejects with `PIECE_NOT_FOUND` for an unknown piece id
    - Rejects with `WRONG_TEAM` when the chosen piece does not belong to `cornerKickTeam`
    - Any own piece is selectable, including the goalkeeper
    - On success the chosen piece's position becomes `state.cornerKickHex`, re-resolved against the current piece list excluding the taker itself so it can never be blocked by its own prior position
    - On success `ball.position` equals the taker's new position, `ball.carrierId` equals the taker id, and `ball.lastTouchedBy` is `{ pieceId: takerId, teamId: cornerKickTeam }`
    - On success `cornerKickTakerId` is set and `phase` becomes `CORNER_KICK_REPOSITION` with `cornerKickStageIndex: 0`, `cornerKickStagePlacedIds: []`, `cornerKickUsedPace: {}` and `activeTeam` = the stage-0 team (`cornerKickTeam`)
    - A `CORNER_KICK_TAKER_PLACED` event is appended carrying `from`, `to` and `ballAfter`
    - The ball and taker never disagree: `pieces.find(p => p.id === ball.carrierId).position` deep-equals `ball.position`
  </behavior>
  <action>
    Add `applyCornerKickTakerSelect(state: GameState, pieceId: string)` to
    `packages/server/src/gameEngine.ts` after `applyCornerKickGkWindowEnd`, exporting an
    `ApplyCornerKickTakerSelectResult` union with
    `{ ok: false; reason: 'WRONG_PHASE' | 'PIECE_NOT_FOUND' | 'WRONG_TEAM' }` and
    `{ ok: true; state: GameState }`.

    Copy `applyThrowInPlace`'s body shape verbatim in structure: phase guard on
    `CORNER_KICK_TAKER_SELECT`, then `state.pieces.find`, then
    `piece.teamId !== state.cornerKickTeam` → `WRONG_TEAM`, then an unconditional teleport
    (distance is irrelevant — this is a placement, not a move).

    Destination: re-resolve `state.cornerKickHex` with
    `resolveThrowInHex(state.cornerKickHex, state.pieces.filter(p => p.id !== pieceId))` so the
    taker's own current position can never block its destination (the same
    exclude-the-moving-piece reasoning the `GOAL_KICK` trigger branch applies to the goalkeeper).
    A goalkeeper repositioned onto the corner hex during CORNER-01 is exactly the case this
    guards. Write the re-resolved hex back into `cornerKickHex` so downstream phases and the
    client read one consistent value.

    Set the ball and taker together in the same return literal — `ball.position` = the resolved
    hex, `ball.carrierId` = `pieceId`, `ball.lastTouchedBy` = `{ pieceId, teamId: state.cornerKickTeam }`
    — so carrier position and ball position can never diverge. Setting `lastTouchedBy` here is
    load-bearing: it is the field a re-exit during this corner would classify against.

    Transition to `CORNER_KICK_REPOSITION` and initialize the CORNER-03 window in the same
    return: `cornerKickStageIndex: 0`, `cornerKickStagePlacedIds: []`, `cornerKickUsedPace: {}`,
    `activeTeam: state.cornerKickTeam` (stage 0 is the attacking side per `CORNER_KICK_STAGES`),
    and `cornerKickTakerId: pieceId`. Leave `cornerKickEligibleIds` to plan 38-03's
    `computeCornerKickEligibleIds` — set it to `null` here with a comment pointing at that
    function, so 38-03 has exactly one place to wire it in.

    Append a `CORNER_KICK_TAKER_PLACED` event with `pieceId`, `from` (the piece's pre-placement
    position), `to`, `timestamp: Date.now()`, and
    `ballAfter: { position: resolvedHex, carrierId: pieceId }`.

    Extend `gameEngine.cornerKick.test.ts` with a describe block covering every behavior above,
    including a case where the corner hex is occupied by the just-repositioned goalkeeper and a
    case asserting the carrier/ball position invariant.

  </action>
  <acceptance_criteria>
    - `grep -c "export function applyCornerKickTakerSelect" packages/server/src/gameEngine.ts` returns 1
    - A test asserts `ball.carrierId === takerId` AND the taker piece's position deep-equals `ball.position`
    - A test asserts selecting an opponent piece returns `{ ok: false, reason: 'WRONG_TEAM' }`
    - A test asserts the resulting phase is `CORNER_KICK_REPOSITION` with `cornerKickStageIndex === 0`
    - `pnpm --filter @counter-attack/server test -- gameEngine.cornerKick` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- gameEngine.cornerKick</automated>
  </verify>
  <done>The kicking manager can designate any own piece as corner-taker; that piece and the ball land together on the server-owned corner hex and the state advances into the CORNER-03 window at stage 0.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                                                                                                                                                   |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| handler → engine   | `pieceId` and `to` arrive from a client socket payload. The engine functions in this plan are the authoritative validation layer; the handler (38-05) adds mutex/ownership pre-checks but never substitutes for these guards. |
| engine → GameState | Every returned state must be a fresh literal; in-place mutation of `state.pieces` would corrupt the room snapshot shared with both sockets.                                                                                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                               | Disposition | Mitigation Plan                                                                                                                                                                                                                                       |
| --------- | ---------------------- | ----------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-04   | Spoofing               | `applyCornerKickGkPlace` / `applyCornerKickTakerSelect` piece ownership | mitigate    | Explicit `piece.teamId !== <derived acting team>` → `WRONG_TEAM` guard in both functions, with the acting team derived from `phase` + `state.cornerKickTeam` and never from a client-supplied value or from `activeTeam`.                             |
| T-38-05   | Tampering              | `applyCornerKickGkPlace` target hex                                     | mitigate    | `isPitchHex(to)` plus an occupancy scan over `state.pieces` before accepting the placement; an off-pitch or occupied hex returns `INVALID_TARGET`.                                                                                                    |
| T-38-06   | Tampering              | Corner restart hex forgery                                              | mitigate    | The corner hex is derived server-side from `CORNER_KICK_HEX[bylineOwner]` + `lastInBoundsHex` and stored in `cornerKickHex`; `applyCornerKickTakerSelect` takes no hex parameter at all, so a client cannot influence where the corner is taken from. |
| T-38-07   | Elevation of Privilege | Wrong team awarded the corner                                           | mitigate    | Team inversion (`cornerKickTeam = owner === 'home' ? 'away' : 'home'`) is unit-tested in both directions; the `GOAL_KICK` branch's `owner`-is-the-awarded-team assumption is explicitly not reused.                                                   |
| T-38-SC   | Tampering              | npm/pnpm installs                                                       | accept      | No package-manager installs in this plan (RESEARCH.md Package Legitimacy Audit: zero new packages).                                                                                                                                                   |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test -- gameEngine.cornerKick` green.
- `pnpm --filter @counter-attack/server test -- gameEngine.outOfBounds` green (no Phase-37 regression).
- `pnpm --filter @counter-attack/server build` compiles.
- `git diff packages/shared/` empty for this plan.
</verification>

<success_criteria>

- `triggerOutOfBoundsRestart` returns a fully-formed `CORNER_KICK_GK_SETUP_ATTACKING` state for a
  byline exit after a defending touch, awarded to the correct (inverted) team.
- The two goalkeeper windows run attacking-first and hand off to taker selection.
- The corner-taker and the ball land together on the occupancy-resolved fixed corner hex, and the
  state enters `CORNER_KICK_REPOSITION` at stage 0.
- Phase 37's throw-in and goal-kick paths are byte-for-byte unaffected.
  </success_criteria>

<output>
Create `.planning/phases/38-corner-kick/38-02-SUMMARY.md` when done.
</output>
