---
phase: 38-corner-kick
plan: 21
type: execute
wave: 16
depends_on: [38-20]
gap_closure: true
files_modified:
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/gameHandlers.cornerKick.test.ts
  - packages/server/src/__tests__/cornerKick.integration.test.ts
autonomous: true
requirements: [OOB-03, CORNER-01, CORNER-02]
must_haves:
  truths:
    - 'A player can clear a piece out of the corner zone over the socket, one hex per click, and the opponent sees the result'
    - 'A player can confirm their clear-out over the socket, and the turn passes to the opponent and then to the goalkeeper window'
    - 'A socket that is not the acting manager cannot move a piece or confirm during the clear-out'
    - 'A clear-out rejection reaches the requesting client as a GAME_ERROR wire code and nothing else changes'
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'CORNER_KICK_CLEAR_OUT branches in the GAME_MOVE and GAME_END_TURN handlers'
      contains: 'applyCornerKickClearOut'
    - path: 'packages/server/src/__tests__/cornerKick.integration.test.ts'
      provides: 'socket-level walkthrough of the clear-out step into the goalkeeper window'
      contains: 'CORNER_KICK_CLEAR_OUT'
  key_links:
    - from: 'packages/server/src/gameHandlers.ts GAME_MOVE'
      to: 'applyCornerKickClearOut'
      via: 'a phase branch placed alongside the existing CORNER_KICK_REPOSITION and CORNER_KICK_FINAL_SETUP branches'
      pattern: 'applyCornerKickClearOut'
    - from: 'packages/server/src/gameHandlers.ts GAME_END_TURN'
      to: 'applyCornerKickClearOutEnd'
      via: 'a phase branch alongside the existing corner window/stage/final end branches'
      pattern: 'applyCornerKickClearOutEnd'
---

<objective>
Expose the clear-out step over the socket surface so it is actually playable, and prove the whole
corner opening — award, clear-out, goalkeeper windows, taker select — end to end at the socket
level.

Purpose: 38-20 built the engine transitions but deliberately left `gameHandlers.ts` untouched, so
the new phase is currently reachable only from unit tests. Without this plan a real corner would
deadlock at the clear-out.

Output: two new handler branches and socket-level coverage that drives a real two-client corner
from the award through to the goalkeeper window.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-corner-kick/38-15-SUMMARY.md
@.planning/phases/38-corner-kick/38-20-SUMMARY.md
@.planning/phases/38-corner-kick/38-PATTERNS.md
</context>

## Artifacts this phase produces

- `GAME_MOVE` handler branch for `CORNER_KICK_CLEAR_OUT` (`packages/server/src/gameHandlers.ts`)
- `GAME_END_TURN` handler branch for `CORNER_KICK_CLEAR_OUT` (`packages/server/src/gameHandlers.ts`)
- New describe blocks in `gameHandlers.cornerKick.test.ts` and `cornerKick.integration.test.ts`

## Gap-closure source audit

| Source         | Item                                                      | Covered by                      |
| -------------- | --------------------------------------------------------- | ------------------------------- |
| 38-15 defect 3 | The clear-out must be playable by a real client           | Task 1                          |
| 38-15 defect 3 | Turn order: attacking manager first, then defending       | Task 1 (guard) + Task 2 (proof) |
| 38-20 handoff  | Socket wiring explicitly deferred from 38-20 to this plan | Task 1                          |

<tasks>

<task type="auto">
  <name>Task 1: Wire the clear-out into the GAME_MOVE and GAME_END_TURN handlers</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts lines 745-845 (the `CORNER_KICK_REPOSITION` and `CORNER_KICK_FINAL_SETUP` branches of `GAME_MOVE` — the `isActivePlayer` pre-check with its T-38-17 rationale, the ASVS V5 payload-shape validation, and the crucial difference between the two branches over who appends the move event)
    - packages/server/src/gameHandlers.ts lines 1425-1500 (the corner branches of `GAME_END_TURN`: `applyCornerKickGkWindowEnd`, `applyCornerKickStageEnd` with its explicit `socketTeam(socket)` parameter, and `applyCornerKickFinalSetupEnd`)
    - packages/server/src/gameHandlers.ts lines 1550-1610 (`validUndoPhases` and the comment listing which corner phases are deliberately excluded from Undo — the clear-out joins that exclusion list and must NOT be added)
    - packages/server/src/gameEngine.ts (`applyCornerKickClearOut` and `applyCornerKickClearOutEnd` as built by 38-20, including which of them appends its own event)
    - .planning/phases/38-corner-kick/38-PATTERNS.md (the `isProcessing` mutex + phase-guard + pure-delegate handler shape every corner handler follows)
  </read_first>
  <action>
    In `packages/server/src/gameHandlers.ts`:

    1. Add the import for `applyCornerKickClearOut` and `applyCornerKickClearOutEnd` alongside the
       other `applyCornerKick*` imports.

    2. In the `GAME_MOVE` handler, add a `room.gameState.phase === 'CORNER_KICK_CLEAR_OUT'` branch
       placed immediately BEFORE the existing `CORNER_KICK_REPOSITION` branch. It must:
       - reject a non-acting socket with `GAME_ERROR 'WRONG_TEAM'` via `isActivePlayer`, which is
         correct here for the same reason recorded at the `CORNER_KICK_REPOSITION` branch:
         `activeTeam` is kept in sync with the clear-out slot by `triggerOutOfBoundsRestart` and by
         `applyCornerKickClearOutEnd`'s slot handoff. Copy that rationale comment, adapted.
       - run the same ASVS V5 payload-shape validation on `to` that the two sibling branches run,
         emitting `GAME_ERROR 'INVALID_TARGET'` on a malformed payload.
       - delegate to `applyCornerKickClearOut`, emit `GAME_ERROR` with the returned `reason` on
         failure, and assign the returned state on success.
       - append NO event of its own — `applyCornerKickClearOut` emits its own
         `CORNER_KICK_CLEAR_OUT_MOVE`, so this branch follows the `CORNER_KICK_FINAL_SETUP`
         pattern, not the `CORNER_KICK_REPOSITION` pattern. State that in a comment, because the
         two adjacent branches differ on exactly this point and the wrong choice double-logs.
       - `broadcastState(io, room)` and return, in every path.

    3. In the `GAME_END_TURN` handler, add a `CORNER_KICK_CLEAR_OUT` branch alongside the existing
       corner end branches. Delegate to `applyCornerKickClearOutEnd(room.gameState, socketTeam(socket))`,
       letting the engine — not the handler — own the acting-team comparison, exactly as the
       `applyCornerKickStageEnd` branch already does. Emit `GAME_ERROR` with the returned `reason`
       on failure (`MUST_CLEAR_CORNER` is the one a player will actually see), assign on success,
       broadcast, return.

    4. Do NOT add `'CORNER_KICK_CLEAR_OUT'` to `validUndoPhases`. Extend the existing comment at
       that list to name the clear-out among the deliberately-excluded corner phases, citing 38-20's
       recorded rationale.

    5. Re-run the wire-code enumeration the client's `restartErrorMessage.ts` header comment
       mandates (`grep -o "GAME_ERROR, '[A-Z_]*'" packages/server/src/gameHandlers.ts | sort -u`)
       and list in the SUMMARY every code this plan newly makes reachable, so 38-22 can map them.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "applyCornerKickClearOut\b" packages/server/src/gameHandlers.ts` is at least 2 (import and call)
    - `grep -c "applyCornerKickClearOutEnd" packages/server/src/gameHandlers.ts` is at least 2 (import and call)
    - The `GAME_MOVE` clear-out branch contains no `ActionEvent` literal — the engine owns event construction for this phase
    - `grep -n "'CORNER_KICK_CLEAR_OUT'" packages/server/src/gameHandlers.ts` does NOT match inside the `validUndoPhases` array literal
    - `pnpm --filter @counter-attack/server typecheck` exits 0
  </acceptance_criteria>
  <done>Both clear-out actions are reachable over the socket with the same mutex, guard, and broadcast discipline as every other corner handler.</done>
</task>

<task type="auto">
  <name>Task 2: Socket-level coverage of the clear-out step and its turn order</name>
  <files>packages/server/src/__tests__/gameHandlers.cornerKick.test.ts, packages/server/src/__tests__/cornerKick.integration.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/cornerKick.integration.test.ts (the whole file — its two-client harness, its corner-award driver, and every existing test that drives from the award straight into a goalkeeper placement; those drivers now have to pass through the clear-out and must be updated, not bypassed)
    - packages/server/src/__tests__/gameHandlers.cornerKick.test.ts (the handler-level harness and its `GAME_ERROR` assertion style)
    - packages/server/src/__tests__/testHelpers.ts
    - packages/server/src/gameHandlers.ts (the two branches added in Task 1)
  </read_first>
  <action>
    Add a describe block `CORNER_KICK_CLEAR_OUT over the socket (38-15 defect 3)` covering:

    In `gameHandlers.cornerKick.test.ts`:
    - "a non-acting socket's clear-out move is rejected with WRONG_TEAM and changes nothing".
    - "a malformed `to` payload is rejected with INVALID_TARGET".
    - "an engine rejection reason reaches the requesting socket verbatim as a GAME_ERROR" — drive a
      `NOT_TOWARD_GOAL` case and assert the emitted code.
    - "confirming with an in-zone movable piece emits MUST_CLEAR_CORNER and does not advance the
      slot".
    - "exactly one `CORNER_KICK_CLEAR_OUT_MOVE` is appended per accepted move" — this is the
      double-log guard; assert the event-log delta is 1, not 2.

    In `cornerKick.integration.test.ts`:
    - "a corner award opens the clear-out with the attacking manager acting" — assert the broadcast
      phase and slot on both clients.
    - "both managers clear their in-zone pieces and the sequence reaches
      CORNER_KICK_GK_SETUP_ATTACKING" — a full two-client walkthrough driving real emits, not
      seeded state.
    - "a defending reposition into the exclusion zone is rejected over the socket" — drive the
      sequence to `CORNER_KICK_REPOSITION` and attempt a defending move into the zone.

    Then update every pre-existing integration driver that jumped from the corner award directly
    into a goalkeeper placement so it now walks the clear-out first. Do not seed the goalkeeper
    phase directly to sidestep the new step — the point of this plan is that the step is on the
    real path. Do not delete any test.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/server test` passes with a total test count no lower than the pre-plan baseline (record both numbers in the SUMMARY)
    - At least 8 new tests exist across the two files
    - `grep -rn "phase: 'CORNER_KICK_GK_SETUP_ATTACKING'" packages/server/src/__tests__/cornerKick.integration.test.ts` shows no driver that seeds that phase to skip the clear-out
    - No test file was deleted and no test case was removed
  </acceptance_criteria>
  <done>A real two-client corner walks award, attacking clear-out, defending clear-out, and into the goalkeeper window, and every new rejection path is asserted at the socket boundary.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                                                                   |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| socket to handler    | The clear-out adds two new untrusted entry points; both need the same team guard, payload validation, and mutex discipline as their siblings. |
| handler to event log | The adjacent corner branches disagree on who appends the move event; copying the wrong neighbour double-logs every clear-out move.            |

## STRIDE Threat Register

| Threat ID | Category               | Component                                              | Disposition | Mitigation Plan                                                                                     |
| --------- | ---------------------- | ------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------- |
| T-38-71   | Elevation of Privilege | A non-acting socket moving pieces during the clear-out | mitigate    | `isActivePlayer` pre-check plus a dedicated handler test that asserts no state change on rejection. |
| T-38-72   | Tampering              | A malformed `to` payload reaching unguarded hex math   | mitigate    | The ASVS V5 shape check is copied verbatim from the sibling branches and asserted by a test.        |
| T-38-73   | Repudiation            | Double-logged clear-out moves corrupting the replay    | mitigate    | The branch appends no event and a test asserts an event-log delta of exactly 1 per accepted move.   |
| T-38-SC   | Tampering              | npm/pnpm installs                                      | accept      | No package-manager installs in this plan.                                                           |

</threat_model>

<verification>

- `pnpm --filter @counter-attack/server test` passes; count not lower than baseline.
- `pnpm --filter @counter-attack/server typecheck` exits 0.
- `packages/server/src/gameEngine.ts`, `packages/shared`, and every `packages/client` file are unchanged by this plan.
- `validUndoPhases` still excludes `CORNER_KICK_CLEAR_OUT`.

</verification>

<success_criteria>

- The clear-out step is fully playable over the socket by both managers in turn.
- Every rejection reason the engine can return is reachable and asserted at the socket boundary.
- Pre-existing corner integration coverage now drives through the new step rather than around it.

</success_criteria>

<output>
Create `.planning/phases/38-corner-kick/38-21-SUMMARY.md` when done, listing every newly reachable
`GAME_ERROR` wire code for 38-22 to map.
</output>
