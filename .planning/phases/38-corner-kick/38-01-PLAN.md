---
phase: 38-corner-kick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/outOfBounds.ts
  - packages/shared/src/outOfBounds.test.ts
  - packages/shared/src/offside.ts
  - packages/shared/src/offside.test.ts
  - packages/shared/src/types.ts
  - packages/shared/src/actionSequence.ts
  - packages/shared/src/actionSequence.test.ts
  - packages/shared/src/events.ts
autonomous: true
requirements: [OOB-03, CORNER-01, CORNER-02, CORNER-03, CORNER-04, CORNER-06]
must_haves:
  truths:
    - 'CORNER_KICK_HEX resolves a fixed restart hex for all four corners (home/away x top/bottom)'
    - 'CORNER_KICK_STAGES models 6 alternating stages, attacking side first, max 2 distinct pieces per stage'
    - 'cornerKickStageTeam maps a stage index plus the attacking team to the acting team'
    - 'Every new Corner Kick GamePhase value compiles into the GamePhase union'
    - 'ELIGIBLE_NEXT_ACTIONS has a CORNER_KICK_RESTART row allowing exactly STANDARD_PASS and HIGH_PASS'
  artifacts:
    - path: 'packages/shared/src/outOfBounds.ts'
      provides: 'CORNER_KICK_HEX fixed corner-taker geometry (D-01/D-02)'
      contains: 'CORNER_KICK_HEX'
    - path: 'packages/shared/src/offside.ts'
      provides: 'CORNER_KICK_STAGES + cornerKickStageTeam (CORNER-03 / D-05)'
      contains: 'CORNER_KICK_STAGES'
    - path: 'packages/shared/src/types.ts'
      provides: '5 new GamePhase values, cornerKick* GameState fields, CORNER_KICK_RESTART LastActionType, 5 new ActionEvent variants'
      contains: 'CORNER_KICK_REPOSITION'
    - path: 'packages/shared/src/actionSequence.ts'
      provides: 'CORNER_KICK_RESTART eligibility row'
      contains: 'CORNER_KICK_RESTART'
    - path: 'packages/shared/src/events.ts'
      provides: 'GAME_CORNER_KICK_GK_PLACE + GAME_CORNER_KICK_TAKER client events'
      contains: 'GAME_CORNER_KICK_TAKER'
  key_links:
    - from: 'packages/shared/src/types.ts'
      to: 'packages/shared/src/actionSequence.ts'
      via: 'LastActionType union exhaustiveness on Record<LastActionType, ...>'
      pattern: 'CORNER_KICK_RESTART'
    - from: 'packages/shared/src/offside.ts'
      to: 'packages/shared/src/types.ts'
      via: 'cornerKickStageIndex typed 0..5 matching CORNER_KICK_STAGES length'
      pattern: 'cornerKickStageIndex'
---

<objective>
Establish every shared-package contract the Corner Kick flow needs, so that the server-engine
plans (38-02..38-05) and client plans (38-06/38-07) build against fixed, compiler-enforced
types rather than discovering them.

Purpose: interface-first. Corner Kick gets its own dedicated GamePhase chain and GameState
field cluster (D-07), structurally independent of both `GK_RESTART` and Goal Kick, reusing
only pure helpers. Getting the contracts landed first is what lets the client and server
plans proceed in parallel waves.

Output: `CORNER_KICK_HEX` geometry, `CORNER_KICK_STAGES`/`cornerKickStageTeam`, 5 new
`GamePhase` values, the `cornerKick*` `GameState` field cluster, the `CORNER_KICK_RESTART`
`LastActionType` + `ELIGIBLE_NEXT_ACTIONS` row, 5 new `ActionEvent` variants, and 2 new
`ClientEvents` entries — all with shared-package unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-corner-kick/38-CONTEXT.md
@.planning/phases/38-corner-kick/38-RESEARCH.md
@.planning/phases/38-corner-kick/38-PATTERNS.md
@.planning/phases/38-corner-kick/38-VALIDATION.md
</context>

## Artifacts this phase produces

New symbols created by Phase 38 (these do not exist in the codebase before this phase —
do not flag them as drift):

**Shared package (this plan)**

- `CORNER_KICK_HEX` (const, `packages/shared/src/outOfBounds.ts`)
- `CORNER_KICK_STAGES` (const, `packages/shared/src/offside.ts`)
- `cornerKickStageTeam` (function, `packages/shared/src/offside.ts`)
- `GamePhase` members: `CORNER_KICK_GK_SETUP_ATTACKING`, `CORNER_KICK_GK_SETUP_DEFENDING`, `CORNER_KICK_TAKER_SELECT`, `CORNER_KICK_REPOSITION`, `CORNER_KICK_FINAL_SETUP`
- `LastActionType` member: `CORNER_KICK_RESTART`
- `GameState` fields: `cornerKickTeam`, `cornerKickHex`, `cornerKickTakerId`, `cornerKickEligibleIds`, `cornerKickStageIndex`, `cornerKickStagePlacedIds`, `cornerKickUsedPace`, `cornerKickMoveSlot`, `cornerKickMovedPieceId`, `cornerKickPaceUsed`
- `ActionEvent` variants: `CORNER_KICK_GK_PLACE`, `CORNER_KICK_TAKER_PLACED`, `CORNER_KICK_STAGE_ADVANCE`, `CORNER_KICK_MOVE`, `CORNER_KICK_ACCURACY`
- `ClientEvents` entries: `GAME_CORNER_KICK_GK_PLACE`, `GAME_CORNER_KICK_TAKER`
- `ELIGIBLE_NEXT_ACTIONS.CORNER_KICK_RESTART` row

**Server package (plans 38-02..38-05)**

- `applyCornerKickGkPlace`, `applyCornerKickGkWindowEnd`, `applyCornerKickTakerSelect`,
  `computeCornerKickEligibleIds`, `applyCornerKickReposition`, `applyCornerKickStageEnd`,
  `applyCornerKickFinalMove`, `applyCornerKickFinalSetupEnd` (all `packages/server/src/gameEngine.ts`)
- Result types: `ApplyCornerKickGkPlaceResult`, `ApplyCornerKickTakerSelectResult`,
  `ApplyCornerKickRepositionResult`, `ApplyCornerKickStageEndResult`,
  `ApplyCornerKickFinalSetupEndResult`
- `isCornerKickContext`, `CORNER_KICK_UNLIMITED_DISTANCE` (`packages/server/src/gameHandlers.ts`)

**Client package (plans 38-06/38-07)**

- `CornerKickSetupPanel` component + `CornerKickSetupPanel.module.css`
- `CORNER_KICK_FINAL_SETUP_CONFIG` (`packages/client/src/store/useGameStore.ts`)
- `emitCornerKickGkPlace`, `emitCornerKickTaker` (store actions)

**New file paths**

- `packages/client/src/components/CornerKickSetupPanel.tsx`
- `packages/client/src/components/CornerKickSetupPanel.module.css`
- `packages/client/src/components/CornerKickSetupPanel.test.tsx`
- `packages/server/src/__tests__/gameEngine.cornerKick.test.ts`
- `packages/server/src/__tests__/cornerKick.integration.test.ts`

## Multi-source coverage audit

| Source   | Item                                                                                                     | Covered by                                           |
| -------- | -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| GOAL     | ROADMAP Phase 38 goal — byline exit after defending touch is awarded and fully playable as a corner kick | 38-02 (trigger), 38-03/38-05 (windows), 38-04 (kick) |
| GOAL     | Success criterion 1 (award + both GKs repositioned first)                                                | 38-02, 38-05, 38-06, 38-07                           |
| GOAL     | Success criterion 2 (taker at fixed hex, then 6 players alternating 2 at a time, attacker first)         | 38-01, 38-02, 38-03, 38-05                           |
| GOAL     | Success criterion 3 (pre-kick second window, 1 player each, ≤3 hexes, attacker first)                    | 38-03, 38-05, 38-06                                  |
| GOAL     | Success criterion 4 (High/Low pass, in-box unlimited / ≤15 elsewhere, 8+ accuracy)                       | 38-04, 38-05, 38-07                                  |
| REQ      | OOB-03                                                                                                   | 38-01, 38-02, 38-08, 38-09                           |
| REQ      | CORNER-01                                                                                                | 38-01, 38-02, 38-05, 38-06, 38-07, 38-08, 38-09      |
| REQ      | CORNER-02                                                                                                | 38-01, 38-02, 38-05, 38-06, 38-07, 38-08, 38-09      |
| REQ      | CORNER-03                                                                                                | 38-01, 38-03, 38-05, 38-06, 38-07, 38-08, 38-09      |
| REQ      | CORNER-04                                                                                                | 38-01, 38-04, 38-05, 38-07, 38-08, 38-09             |
| REQ      | CORNER-05                                                                                                | 38-04, 38-05, 38-07, 38-08, 38-09                    |
| REQ      | CORNER-06                                                                                                | 38-01, 38-03, 38-05, 38-06, 38-07, 38-08, 38-09      |
| RESEARCH | §1 OOB-03 hook (replace dead-end, team inversion)                                                        | 38-02 Task 1                                         |
| RESEARCH | §2 fixed corner geometry + top/bottom nearest resolution                                                 | 38-01 Task 1, 38-02 Task 1                           |
| RESEARCH | §3 GK reposition phase pair (uncapped, A1)                                                               | 38-02 Task 2                                         |
| RESEARCH | §4 corner-taker select (applyThrowInPlace shape)                                                         | 38-02 Task 3                                         |
| RESEARCH | §5 CORNER-03 stage/pace fusion + Pitfall 4                                                               | 38-01 Task 2, 38-03 Tasks 1-2                        |
| RESEARCH | §5.4 pre-kick 3-hex slot alternation                                                                     | 38-03 Task 3                                         |
| RESEARCH | §6 High/Low accuracy gate + range override                                                               | 38-04 Task 1, 38-05 Task 3                           |
| RESEARCH | Pitfall 1 (GamePhase registration checklist)                                                             | 38-01, 38-05 Task 2, 38-07 Task 2                    |
| RESEARCH | Pitfall 2 (dedicated ActionEventType, never DICE_ROLL)                                                   | 38-01 Task 3, 38-04 Task 2                           |
| RESEARCH | Pitfall 3 (cornerKickTeam persistence audit)                                                             | 38-04 Task 3                                         |
| RESEARCH | Pitfall 5 (byline owner's own penalty area)                                                              | 38-05 Task 3                                         |
| RESEARCH | A1/A2/A4 rulebook assumptions                                                                            | 38-09 checkpoint                                     |
| CONTEXT  | D-01 single fixed hex per corner, no placement UI                                                        | 38-01 Task 1                                         |
| CONTEXT  | D-02 mirror-symmetric coordinates                                                                        | 38-01 Task 1                                         |
| CONTEXT  | D-03 turn-based GK reposition, attacking first                                                           | 38-02 Task 2                                         |
| CONTEXT  | D-04 GK reposition runs before taker placement                                                           | 38-02 Tasks 2-3                                      |
| CONTEXT  | D-05 strict-pairs alternating window                                                                     | 38-01 Task 2, 38-03 Tasks 1-2                        |
| CONTEXT  | D-06 early confirm with 0 moves allowed (Goal Kick pattern)                                              | 38-03 Task 2, 38-07 Task 1                           |
| CONTEXT  | D-07 own dedicated phase chain, pure-helper reuse only                                                   | 38-01 Task 3 (phase values), all engine plans        |
| CONTEXT  | D-08 GOAL*KICK*\*-style naming                                                                           | 38-01 Task 3                                         |
| CONTEXT  | D-09 Phase 35 panel conventions, reuse existing tints only                                               | 38-06, 38-07                                         |

No unplanned items. Deferred Ideas: none in CONTEXT.md. No item requires a phase split.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CORNER_KICK_HEX fixed corner-taker geometry (D-01/D-02)</name>
  <files>packages/shared/src/outOfBounds.ts, packages/shared/src/outOfBounds.test.ts</files>
  <read_first>
    - packages/shared/src/outOfBounds.ts (full file, 170 lines — `GOAL_KICK_RESTART_HEX` at 46-49 and its doc comment at 27-45 are the exact shape to copy; `classifyOutOfBounds` at 108-128 and `resolveThrowInHex` at 142-170)
    - packages/shared/src/outOfBounds.test.ts (existing mirror-symmetry assertion style for GOAL_KICK_RESTART_HEX — assert against derived geometry, not restated literals)
    - packages/shared/src/pitch.ts (lines 20-110 — `PITCH_HEXES` construction incl. the "r=0 excluded for even q" rule, `PITCH_REGIONS`, `isPitchHex`, and `DIFFICULT_ANGLE_HEXES` at 96-189 which must NOT be imported or aliased here)
    - .planning/phases/38-corner-kick/38-PATTERNS.md (section "packages/shared/src/outOfBounds.ts — add CORNER_KICK_HEX")
  </read_first>
  <behavior>
    - `CORNER_KICK_HEX.home.top` is `{ q: 0, r: 1 }` and `CORNER_KICK_HEX.home.bottom` is `{ q: 0, r: 25 }`
    - `CORNER_KICK_HEX.away.top` is `{ q: 36, r: 1 }` and `CORNER_KICK_HEX.away.bottom` is `{ q: 36, r: 25 }`
    - Mirror symmetry holds: for each of `top`/`bottom`, `home.q + away.q === 36` and `home.r === away.r`
    - Every one of the four hexes satisfies `isPitchHex`
    - The four hexes are pairwise distinct
    - `CORNER_KICK_HEX` is not referentially or structurally derived from `DIFFICULT_ANGLE_HEXES` (no import of that symbol appears in `outOfBounds.ts`)
  </behavior>
  <action>
    Add an exported `CORNER_KICK_HEX` constant to `packages/shared/src/outOfBounds.ts`, placed
    immediately after the existing `GOAL_KICK_RESTART_HEX` declaration, typed
    `Readonly<Record<'home' | 'away', Record<'top' | 'bottom', HexCoord>>>` with values
    home.top `{ q: 0, r: 1 }`, home.bottom `{ q: 0, r: 25 }`, away.top `{ q: 36, r: 1 }`,
    away.bottom `{ q: 36, r: 25 }`.

    Key the outer record by BYLINE OWNER (the defending team whose goal line the ball crossed),
    matching `GOAL_KICK_RESTART_HEX`'s indexing convention — NOT by the attacking/kicking team.
    State this explicitly in the JSDoc, because the corner is awarded to the opposite team from
    the key (the inversion that 38-02 depends on).

    Coordinate rationale to record in the JSDoc: `q=0` and `q=36` are `PITCH_REGIONS.homeGoal`
    /`awayGoal`'s columns (the bylines); `PITCH_HEXES` excludes `r=0` for even `q`, and both `0`
    and `36` are even, so `{q:0,r:0}`/`{q:36,r:0}` are off-pitch and `r=1` is the nearest valid
    top row; `r=25` is the pitch's last row and is never excluded. Mirror symmetry
    `home.q + away.q === 36` matches `GOAL_KICK_RESTART_HEX`.

    Per D-01, add an explicit JSDoc warning that `DIFFICULT_ANGLE_HEXES` (`pitch.ts:103`, the
    PITCH-03 shooting-penalty zone) is a different, much larger region that happens to share
    these four endpoint coordinates by independent derivation, and must never be imported,
    aliased, or kept in sync with this constant.

    Do NOT modify `classifyOutOfBounds` — its `'CORNER_KICK'` branch is already correct and its
    own doc comment declares it off-limits to this phase. Do NOT modify `resolveThrowInHex`.

    Extend `packages/shared/src/outOfBounds.test.ts` with a `CORNER_KICK_HEX` describe block
    covering the behaviors above, asserting mirror symmetry via derived arithmetic
    (`home.q + away.q === 36`) rather than restating literals, matching the existing
    `GOAL_KICK_RESTART_HEX` test convention in that file.

  </action>
  <acceptance_criteria>
    - `grep -c "export const CORNER_KICK_HEX" packages/shared/src/outOfBounds.ts` returns 1
    - `grep -v '^\s*\*' packages/shared/src/outOfBounds.ts | grep -c "DIFFICULT_ANGLE_HEXES"` returns 0
    - `git diff packages/shared/src/outOfBounds.ts` shows no changed line inside the `classifyOutOfBounds` function body
    - `pnpm --filter @counter-attack/shared test -- outOfBounds` exits 0 with the new CORNER_KICK_HEX describe block passing
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test -- outOfBounds</automated>
  </verify>
  <done>`CORNER_KICK_HEX` exists with the four verified coordinates, is keyed by byline owner, imports nothing from `DIFFICULT_ANGLE_HEXES`, and its unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CORNER_KICK_STAGES and cornerKickStageTeam (CORNER-03, D-05)</name>
  <files>packages/shared/src/offside.ts, packages/shared/src/offside.test.ts</files>
  <read_first>
    - packages/shared/src/offside.ts (lines 1-60 — `FREE_KICK_STAGES` at 38-43 and `freeKickStageTeam` at 49-56 are the exact structural template)
    - packages/shared/src/offside.test.ts (existing `FREE_KICK_STAGES`/`freeKickStageTeam` coverage to mirror)
    - .planning/phases/38-corner-kick/38-PATTERNS.md (section "packages/shared/src/offside.ts — add CORNER_KICK_STAGES")
    - .planning/phases/38-corner-kick/38-CONTEXT.md (D-05, D-06)
  </read_first>
  <behavior>
    - `CORNER_KICK_STAGES` has exactly 6 entries
    - Stages at even indices (0, 2, 4) have `side: 'attacking'`; odd indices (1, 3, 5) have `side: 'defending'`
    - Every stage has `max: 2`
    - `cornerKickStageTeam(0, 'home')` returns `'home'`; `cornerKickStageTeam(1, 'home')` returns `'away'`
    - `cornerKickStageTeam(4, 'away')` returns `'away'`; `cornerKickStageTeam(5, 'away')` returns `'home'`
    - Across all 6 stages for either `cornerKickTeam` value, each side acts exactly 3 times (3 rounds x 2 pieces = up to 6 pieces per side)
  </behavior>
  <action>
    Add an exported `CORNER_KICK_STAGES` constant to `packages/shared/src/offside.ts` immediately
    after `freeKickStageTeam`, declared `as const` in the same style as `FREE_KICK_STAGES`, with
    6 entries alternating `{ side: 'attacking', max: 2 }` and `{ side: 'defending', max: 2 }`
    starting with `'attacking'` (D-05: strict pairs, attacking manager first, 3 rounds per side).

    Note in the JSDoc that `side` uses the literals `'attacking'`/`'defending'` — deliberately
    distinct from `FREE_KICK_STAGES`'s `'kicking'`/`'defending'` — because Corner Kick's acting
    team is resolved from `state.cornerKickTeam` (the attacking/kicking team), and reusing the
    free-kick literal would invite accidental cross-use of `freeKickStageTeam`.

    Add an exported `cornerKickStageTeam(stageIndex: 0|1|2|3|4|5, cornerKickTeam: 'home'|'away'): 'home'|'away'`
    function that returns `cornerKickTeam` when the stage's `side` is `'attacking'`, otherwise the
    opposite team — byte-for-byte `freeKickStageTeam`'s body shape with the corner literals.

    Add a JSDoc block on `CORNER_KICK_STAGES` recording the Pitfall-4 divergence explicitly:
    `max` is the per-stage cap on DISTINCT pieces touched during that stage (re-touching an
    already-counted piece in the same stage is free), and — unlike `applyFreeKickReady`'s
    permanent `movedPieceIds` lock — a piece touched in one stage is NOT locked out of later
    stages; its per-piece 6-hex budget (`cornerKickUsedPace`) persists and stays spendable
    across all 6 stages.

    Extend `packages/shared/src/offside.test.ts` with a `CORNER_KICK_STAGES` describe block
    covering the behaviors above, mirroring the existing `FREE_KICK_STAGES` test structure.

  </action>
  <acceptance_criteria>
    - `grep -c "export const CORNER_KICK_STAGES" packages/shared/src/offside.ts` returns 1
    - `grep -c "export function cornerKickStageTeam" packages/shared/src/offside.ts` returns 1
    - `pnpm --filter @counter-attack/shared test -- offside` exits 0 with the new describe block passing
    - Test file asserts `CORNER_KICK_STAGES.length === 6` and asserts alternation by index parity, not by restating the literal array
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test -- offside</automated>
  </verify>
  <done>`CORNER_KICK_STAGES` and `cornerKickStageTeam` exist, alternate attacking-first across 6 stages with `max: 2`, and their unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add Corner Kick GamePhase values, GameState fields, LastActionType, ActionEvents and ClientEvents</name>
  <files>packages/shared/src/types.ts, packages/shared/src/actionSequence.ts, packages/shared/src/actionSequence.test.ts, packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/types.ts (lines 220-250 for the `STANDARD_PASS` ActionEvent shape incl. `passerId`/`accurate`; lines 435-490 for `THROW_IN_PLACE`/`GOAL_KICK_WINDOW_ADVANCE`/`GOAL_KICK_CHOICE`/`GOAL_KICK_MOVE`/`GOAL_KICK` ActionEvent variants; lines 488-528 for the `GamePhase` union; lines 530-560 for `LastActionType`; lines 1085-1172 for the throw-in/goal-kick `GameState` field cluster)
    - packages/shared/src/actionSequence.ts (full file, 115 lines — `NextActionType`, the `Record<LastActionType, ...>` exhaustive table, and the `THROW_IN_MOVEMENT_1`/`_2` + `GOAL_KICK_RESTART` rows at 103-114)
    - packages/shared/src/actionSequence.test.ts (existing row-coverage test style)
    - packages/shared/src/events.ts (lines 100-125 for `ClientEvents` throw-in/goal-kick entries and the comment explaining that goal-kick repositioning deliberately reuses `GAME_MOVE`/`GAME_END_TURN`; lines 280-292 for the `ClientToServerEvents` payload map)
    - .planning/phases/38-corner-kick/38-PATTERNS.md (section "packages/shared/src/types.ts")
  </read_first>
  <action>
    **types.ts — GamePhase union.** Add 5 new members after the existing `GOAL_KICK_MOVE` entry
    (D-07/D-08, own dedicated chain, `GOAL_KICK_*` naming convention):
    `CORNER_KICK_GK_SETUP_ATTACKING`, `CORNER_KICK_GK_SETUP_DEFENDING`,
    `CORNER_KICK_TAKER_SELECT`, `CORNER_KICK_REPOSITION`, `CORNER_KICK_FINAL_SETUP`.
    Give each an inline comment naming its requirement (CORNER-01, CORNER-01, CORNER-02,
    CORNER-03, CORNER-06 respectively) and a block comment stating these are genuinely new
    phase values, not aliases of `GK_RESTART` or the `GOAL_KICK_*` chain (D-07), mirroring the
    existing Phase 37 comment above `THROW_IN_SETUP`.

    **types.ts — GameState fields.** Append a `cornerKick*` cluster after the existing
    `goalKickPaceUsed` field, each with a JSDoc naming its requirement and its analog field:
    - `cornerKickTeam?: 'home' | 'away' | null` — the ATTACKING team awarded the corner (mirrors `throwInTeam`/`goalKickTeam`). Persistent through to kick resolution.
    - `cornerKickHex?: HexCoord | null` — the occupancy-resolved fixed corner hex, computed once at trigger time (mirrors `throwInHex`).
    - `cornerKickTakerId?: string | null` — the piece chosen as corner-taker (CORNER-02).
    - `cornerKickEligibleIds?: { attacking: readonly string[]; defending: readonly string[] } | null` — precomputed once, mirrors `goalKickEligibleIds`' two-key shape.
    - `cornerKickStageIndex?: 0 | 1 | 2 | 3 | 4 | 5 | null` — mirrors `freeKickStageIndex`.
    - `cornerKickStagePlacedIds?: readonly string[] | null` — distinct pieces touched THIS stage, cap 2, resets each stage (mirrors `freeKickPlacedPieceIds`).
    - `cornerKickUsedPace?: Readonly<Record<string, number>> | null` — cumulative hexes per piece, cap 6, PERSISTS across all 6 stages (mirrors `goalKickUsedPace`). JSDoc must state the Pitfall-4 non-reset rule explicitly.
    - `cornerKickMoveSlot?: 'ATTACKER' | 'DEFENDER' | null` — CORNER-06 slot alternation (mirrors `goalKickMoveSlot`).
    - `cornerKickMovedPieceId?: string | null` — mirrors `goalKickMovedPieceId`.
    - `cornerKickPaceUsed?: number` — cumulative hexes this CORNER-06 slot, cap 3 (mirrors `goalKickPaceUsed`).
    Use `?:` optional-with-null shapes exactly as the goal-kick cluster does; `exactOptionalPropertyTypes` is enforced project-wide.

    **types.ts — LastActionType.** Add `'CORNER_KICK_RESTART'` after `'GOAL_KICK_RESTART'` with
    a JSDoc noting it restricts the next action to `STANDARD_PASS`/`HIGH_PASS` (CORNER-04) and
    that the persistent `cornerKickTeam` field — not this value — is what gates the Low-option
    accuracy check, because the `GAME_ROLL` handler overwrites `lastActionType` with the chosen
    passType before `applyRoll` runs.

    **types.ts — ActionEvent variants.** Add 5 new members to the `ActionEvent` union after the
    existing `GOAL_KICK` variant. Never reuse `DICE_ROLL` (STATE.md pitfall):
    - `CORNER_KICK_GK_PLACE` — `pieceId`, `side: 'ATTACKING' | 'DEFENDING'`, `from`, `to`, `timestamp` (mirrors `THROW_IN_PLACE` minus `ballAfter`; the ball does not move).
    - `CORNER_KICK_TAKER_PLACED` — `pieceId`, `from`, `to`, `timestamp`, `ballAfter` (byte-for-byte `THROW_IN_PLACE`'s shape).
    - `CORNER_KICK_STAGE_ADVANCE` — `fromStageIndex: 0|1|2|3|4|5`, `timestamp` (mirrors `GOAL_KICK_WINDOW_ADVANCE`).
    - `CORNER_KICK_MOVE` — `slot: 'ATTACKER' | 'DEFENDER'`, `pieceId`, `from`, `to`, `timestamp` (byte-for-byte `GOAL_KICK_MOVE`'s shape).
    - `CORNER_KICK_ACCURACY` — `takerId`, `passType: 'HIGH' | 'LOW'`, `targetHex`, `accurate`, `kickDie`, `kickScore`, `timestamp`, `ballAfter` (mirrors the `GOAL_KICK` event shape with `gkId` renamed to `takerId` plus the High/Low discriminator).
    If `ActionEventType` is a separate exported union in this file, add all 5 literals there too.

    **actionSequence.ts.** Add the `CORNER_KICK_RESTART: new Set<NextActionType>(['STANDARD_PASS', 'HIGH_PASS'])`
    row (the `Record<LastActionType, ...>` exhaustiveness check will not compile without it).
    Comment it referencing CORNER-04/CORNER-05 and stating the Throw-In precedent: reuse the
    existing `STANDARD_PASS`/`HIGH_PASS` labels for Low/High — do NOT invent
    `CORNER_KICK_HIGH`/`CORNER_KICK_LOW` `NextActionType` members.
    Extend `actionSequence.test.ts` with a case asserting the row's exact 2-member contents.

    **events.ts.** Add two `ClientEvents` entries after `GAME_GOAL_KICK_TARGET`:
    `GAME_CORNER_KICK_GK_PLACE: 'game:corner-kick-gk-place'` (payload `(pieceId: string, to: HexCoord)`,
    mirroring `GAME_FREE_KICK_MOVE`'s pick-up-and-place shape) and
    `GAME_CORNER_KICK_TAKER: 'game:corner-kick-taker'` (payload `(pieceId: string)`, mirroring
    `GAME_THROW_IN_PLACE` — the destination is server-owned and deliberately not in the payload).
    Add matching entries to the `ClientToServerEvents` interface. Add a comment stating that the
    CORNER-03 and CORNER-06 reposition windows deliberately reuse the existing `GAME_MOVE` +
    `GAME_END_TURN` events (the goal-kick/FREE_MOVE precedent) and that no further corner events
    are to be added.

  </action>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/shared build` (or `tsc --noEmit` for the shared package) exits 0
    - `grep -c "CORNER_KICK_GK_SETUP_ATTACKING\|CORNER_KICK_GK_SETUP_DEFENDING\|CORNER_KICK_TAKER_SELECT\|CORNER_KICK_REPOSITION\|CORNER_KICK_FINAL_SETUP" packages/shared/src/types.ts` returns at least 5
    - `grep -c "cornerKickUsedPace" packages/shared/src/types.ts` returns at least 1 and its JSDoc contains the words "persists" and "6 stages"
    - `grep -c "CORNER_KICK_RESTART" packages/shared/src/actionSequence.ts` returns at least 1
    - `grep -c "CORNER_KICK_HIGH\|CORNER_KICK_LOW" packages/shared/src/actionSequence.ts` returns 0
    - `grep -c "GAME_CORNER_KICK_TAKER\|GAME_CORNER_KICK_GK_PLACE" packages/shared/src/events.ts` returns at least 4 (2 in ClientEvents, 2 in ClientToServerEvents)
    - `pnpm --filter @counter-attack/shared test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test &amp;&amp; pnpm --filter @counter-attack/shared build</automated>
  </verify>
  <done>Shared package compiles with all 5 new phases, 10 new GameState fields, the new LastActionType + eligibility row, 5 new ActionEvent variants and 2 new ClientEvents; shared test suite is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                                                                                                                                    |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client socket → server handler | All Corner Kick intents (GK placement, taker selection, reposition moves, confirms, kick target) originate as untrusted client payloads. This plan defines the payload contracts those handlers will validate. |
| shared package → both runtimes | `actionSequence.ts` is imported client-side for button disabling only; the server import is the authoritative enforcement point (existing T-08-01 note).                                                       |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                        | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                     |
| --------- | --------- | ---------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38-01   | Tampering | `ClientEvents.GAME_CORNER_KICK_TAKER` payload shape              | mitigate    | Payload is `(pieceId: string)` only — the destination hex is deliberately excluded from the contract so a client can never choose where the corner is taken from (mirrors `GAME_THROW_IN_PLACE`). Enforced by the typed `ClientToServerEvents` map in this plan; runtime validation lands in 38-05. |
| T-38-02   | Spoofing  | `ELIGIBLE_NEXT_ACTIONS.CORNER_KICK_RESTART` used client-side     | accept      | Documented pre-existing posture (T-08-01): the table is advisory on the client; the server re-checks it. A tampered client constant cannot bypass server validation.                                                                                                                                |
| T-38-03   | Tampering | `cornerKickUsedPace` / `cornerKickStagePlacedIds` field contract | mitigate    | Both are server-owned `GameState` fields with no client-writable path; the type declarations mark them `Readonly`/`readonly` so no engine code can mutate them in place. Budget enforcement lands in 38-03.                                                                                         |
| T-38-SC   | Tampering | npm/pnpm installs                                                | accept      | No package-manager installs occur in this phase — RESEARCH.md "Package Legitimacy Audit" records zero new external packages. No `[ASSUMED]`/`[SUS]`/`[SLOP]` entries exist to gate.                                                                                                                 |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test` is green (outOfBounds, offside, actionSequence suites all pass).
- `pnpm --filter @counter-attack/shared build` compiles with no TypeScript errors.
- `pnpm --filter @counter-attack/server build` still compiles (the new `LastActionType` member is exhaustiveness-checked at `actionSequence.ts:49`; if the server fails to compile, the eligibility row is missing).
- `classifyOutOfBounds` in `outOfBounds.ts` is byte-identical to its pre-plan state.
</verification>

<success_criteria>

- All 5 Corner Kick `GamePhase` values, 10 `cornerKick*` `GameState` fields, the
  `CORNER_KICK_RESTART` `LastActionType` and its `ELIGIBLE_NEXT_ACTIONS` row, 5 new
  `ActionEvent` variants, `CORNER_KICK_HEX`, `CORNER_KICK_STAGES`, `cornerKickStageTeam`,
  and 2 new `ClientEvents` all exist and compile.
- Shared unit tests cover `CORNER_KICK_HEX` geometry/mirror-symmetry, `CORNER_KICK_STAGES`
  alternation, and the `CORNER_KICK_RESTART` eligibility row.
- No file outside `packages/shared/src` is modified.
  </success_criteria>

<output>
Create `.planning/phases/38-corner-kick/38-01-SUMMARY.md` when done.
</output>
