---
phase: 14-kick-off-rules-replay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/teams.ts
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/kickoffSetup.integration.test.ts
autonomous: true
requirements: [MATCH-06, MATCH-07]
must_haves:
  truths:
    - 'Every midfielder and back in the hardcoded squads starts within hex columns 6 through 20 inclusive'
    - 'Attempting a non-Standard-Pass action from the kick off hex during the KICK_OFF phase is rejected by the server with the KICKOFF_STANDARD_PASS_ONLY error code'
    - 'A Standard Pass during the KICK_OFF phase is still accepted (the guard does not block the legal action)'
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'KICK_OFF non-Standard-Pass rejection guard in GAME_ROLL handler'
      contains: 'KICKOFF_STANDARD_PASS_ONLY'
    - path: 'packages/shared/src/teams.ts'
      provides: 'Squad starting positions with all DEF/MID pieces in q range [6,20]'
    - path: 'packages/server/src/__tests__/kickoffSetup.integration.test.ts'
      provides: 'Integration test asserting MATCH-07 rejection and Standard Pass acceptance'
  key_links:
    - from: 'packages/server/src/gameHandlers.ts GAME_ROLL handler'
      to: 'ServerEvents.GAME_ERROR emit + broadcastState'
      via: "phase === 'KICK_OFF' && passType !== 'STANDARD_PASS' guard"
      pattern: 'KICKOFF_STANDARD_PASS_ONLY'
---

<objective>
Enforce the two kick off rules: (1) MATCH-06 — midfielders and backs must start within hex columns 6–20 (a default-placement requirement per D-01, not a live movement guard), and (2) MATCH-07 — only a Standard Pass may be played as the opening action from the kick off hex during the KICK_OFF phase.

Purpose: Make the opening of a match faithful to the rulebook and server-authoritative, so neither player can play an illegal opening pass or rely on an illegal default formation.
Output: Adjusted squad positions in teams.ts (if needed), a new GAME_ROLL guard in gameHandlers.ts, and integration test coverage for the MATCH-07 rejection.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-kick-off-rules-replay/14-CONTEXT.md
@.planning/phases/14-kick-off-rules-replay/14-PATTERNS.md
@.planning/phases/14-kick-off-rules-replay/14-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols/identifiers introduced or modified by this plan:

- Error code string literal `'KICKOFF_STANDARD_PASS_ONLY'` (new GAME_ERROR payload value emitted from `gameHandlers.ts` GAME_ROLL handler)
- Modified starting `position` values on DEF/MID members of `HOME_SQUAD` / `AWAY_SQUAD` in `packages/shared/src/teams.ts` (only if any DEF/MID currently sits outside q∈[6,20])
- New `it(...)` test cases in `kickoffSetup.integration.test.ts` for MATCH-07 rejection and Standard Pass acceptance
  No new types, exports, functions, or fields are created.
  </artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify and adjust DEF/MID starting positions to q∈[6,20] (MATCH-06)</name>
  <files>packages/shared/src/teams.ts</files>
  <read_first>
    - packages/shared/src/teams.ts (current HOME_SQUAD/AWAY_SQUAD definitions and the position-convention comment at lines 25–30)
    - packages/server/src/gameEngine.ts lines 2745–2779 (applyHalfTimeStart resets pieces to HOME_SQUAD/AWAY_SQUAD — same source of truth, so adjusting teams.ts fixes both initial and half-time placement per D-01)
    - .planning/phases/14-kick-off-rules-replay/14-CONTEXT.md D-01
  </read_first>
  <action>
    Per D-01, MATCH-06 is a default-placement requirement: every piece with role 'DEF' or 'MID' in HOME_SQUAD and AWAY_SQUAD must start with position.q within the inclusive range [6, 20]. This is NOT a live movement guard — do not add rejection logic to any handler.

    Audit the current positions. Home DEF pieces (home-1, home-2, home-3) are at q=5 — one column below the minimum (6). Home MID pieces (home-5, home-6) are at q=10 — inside range. Away DEF pieces (away-1, away-2, away-3) are at q=31 and away MID pieces (away-5, away-6) are at q=26 — both above the maximum (20).

    Adjust ONLY the q coordinate of out-of-range DEF/MID pieces to bring them inside [6,20], preserving each piece's r coordinate and keeping the home/away mirror relationship as close as practical:
    - Home DEF (home-1, home-2, home-3): change q from 5 to 6 (minimum allowed column; keeps them as the deepest outfield line while satisfying the constraint).
    - Away DEF (away-1, away-2, away-3): change q from 31 to 20 (maximum allowed column; mirror of home DEF relative to the 6–20 band).
    - Away MID (away-5, away-6): change q from 26 to a value inside [6,20] that preserves the mirror of the home MID column. Home MID is at q=10; the mirror within the band is q=16 (since 6+20-10=16). Set away MID q to 16.
    Leave home MID (home-5, home-6) at q=10 (already in range). Do NOT change GK, FWD, or ST positions — MATCH-06 scopes only midfielders and backs. Do NOT change any r value. Do NOT change any attribute value (pace/shooting/etc.).

    Update the position-convention comment block (teams.ts lines 25–30) so the documented DEF/MID q-values match the new coordinates and note that DEF/MID must remain within q∈[6,20] per MATCH-06 / D-01.

  </action>
  <verify>
    <automated>node -e "const ts=require('fs').readFileSync('packages/shared/src/teams.ts','utf8'); const m=[...ts.matchAll(/role: '(DEF|MID)',\s*\n\s*position: \{ q: (\d+),/g)]; const bad=m.filter(x=>+x[2]<6||+x[2]>20); if(bad.length){console.error('OUT OF RANGE:',bad.map(x=>x[1]+'@q'+x[2]));process.exit(1)} console.log('all',m.length,'DEF/MID in [6,20]')"</automated>
  </verify>
  <acceptance_criteria>
    - Every HOME_SQUAD/AWAY_SQUAD member with role 'DEF' or 'MID' has position.q satisfying 6 ≤ q ≤ 20 (verified by the node assertion above counting all DEF/MID and failing on any out-of-range).
    - No GK, FWD, or ST position.q is changed from its prior value.
    - No r coordinate and no attribute value (pace, shooting, tackling, dribbling, heading, saving, handling, resilience, aerialAbility, highPass) is changed on any piece.
    - The teams.ts position-convention comment (lines ~25–30) documents the new DEF/MID q-columns and the q∈[6,20] MATCH-06 constraint.
    - `pnpm --filter @counter-attack/shared test` passes (teams.test.ts still green after position edits).
  </acceptance_criteria>
  <done>All midfielders and backs in both hardcoded squads start within hex columns 6–20; forwards, strikers, and goalkeepers are unchanged; shared package tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add KICK_OFF Standard-Pass-only guard in GAME_ROLL and cover it with an integration test (MATCH-07)</name>
  <files>packages/server/src/gameHandlers.ts, packages/server/src/__tests__/kickoffSetup.integration.test.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts lines 950–1060 (the GAME_ROLL handler, the PASS/KICK_OFF branch, the MISSING_PASS_TYPE and INVALID_SEQUENCE guards, and the surrounding isProcessing try/finally)
    - packages/server/src/__tests__/kickoffSetup.integration.test.ts lines 1–165 (server lifecycle, createClient, oncePromise, waitForConnect helpers, and existing GAME_ERROR/snap-back test shape)
    - .planning/phases/14-kick-off-rules-replay/14-PATTERNS.md (D-02 insertion point and rejection pattern; GAME_ERROR assertion pattern)
    - .planning/phases/14-kick-off-rules-replay/14-CONTEXT.md D-02
  </read_first>
  <action>
    In gameHandlers.ts, inside the GAME_ROLL handler's `if (room.gameState.phase === 'PASS' || room.gameState.phase === 'KICK_OFF')` block, insert a new guard immediately after the INVALID_SEQUENCE check (the `ELIGIBLE_NEXT_ACTIONS` block ending at line ~996) and before the MISSING_TARGET check at line ~999. The guard condition: when `room.gameState.phase === 'KICK_OFF'` and `passType !== 'STANDARD_PASS'`, emit `ServerEvents.GAME_ERROR` with the payload string `'KICKOFF_STANDARD_PASS_ONLY'`, call `broadcastState(io, room)`, then `return`. Use the three-line rejection (emit GAME_ERROR, broadcastState, return) exactly as every other guard in this handler does; the guard sits inside the existing isProcessing try block so the finally still runs and resets the mutex. Do not add a guard on the MOVEMENT/kickOffActive path — D-02 confirms that path is already constrained.

    In kickoffSetup.integration.test.ts, add a new describe/it block (reuse the existing createClient, waitForConnect, oncePromise helpers — do not redefine them). The test must: create a room with two clients, drive the room into the KICK_OFF phase (seed `room.gameState.phase = 'KICK_OFF'` directly via getRoom if reaching it through the full flow is not already done elsewhere in this file, mirroring the FULL_TIME seeding pattern used in replay.integration.test.ts), then emit `ClientEvents.GAME_ROLL` from the active player's socket with passType `'HIGH_PASS'` and a valid targetHex. Assert via oncePromise that the first GAME_ERROR payload equals `'KICKOFF_STANDARD_PASS_ONLY'`, and assert the subsequent GAME_STATE frame still has phase `'KICK_OFF'` (snap-back). Add a second it that emits GAME_ROLL with passType `'STANDARD_PASS'` and a valid targetHex from the same KICK_OFF state and asserts the response is NOT a `'KICKOFF_STANDARD_PASS_ONLY'` GAME_ERROR (it may proceed or fail for unrelated target reasons, but must not be blocked by the new guard).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- kickoffSetup</automated>
  </verify>
  <acceptance_criteria>
    - gameHandlers.ts contains the exact literal `'KICKOFF_STANDARD_PASS_ONLY'` emitted via `socket.emit(ServerEvents.GAME_ERROR, ...)` followed by `broadcastState(io, room)` and `return`, located inside the KICK_OFF/PASS branch of GAME_ROLL after the INVALID_SEQUENCE guard.
    - The guard condition fires only when `room.gameState.phase === 'KICK_OFF'` AND `passType !== 'STANDARD_PASS'` (a STANDARD_PASS during KICK_OFF does not hit the guard).
    - kickoffSetup.integration.test.ts has a test asserting GAME_ERROR payload `=== 'KICKOFF_STANDARD_PASS_ONLY'` for a HIGH_PASS during KICK_OFF and a snap-back GAME_STATE with phase `'KICK_OFF'`.
    - kickoffSetup.integration.test.ts has a test asserting a STANDARD_PASS during KICK_OFF is not rejected with `'KICKOFF_STANDARD_PASS_ONLY'`.
    - `pnpm --filter @counter-attack/server test -- kickoffSetup` passes including the new cases.
  </acceptance_criteria>
  <done>A non-Standard-Pass opening action from the KICK_OFF phase is server-rejected with KICKOFF_STANDARD_PASS_ONLY and snaps back to KICK_OFF; a Standard Pass is unaffected; both behaviours are covered by passing integration tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                     |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| client → GAME_ROLL handler | The client supplies `passType` and `targetHex`; both are untrusted and must be validated server-side before any state mutation. |

## STRIDE Threat Register

| Threat ID | Category               | Component                               | Disposition | Mitigation Plan                                                                                                                                                                |
| --------- | ---------------------- | --------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-14-01   | Tampering              | GAME_ROLL passType during KICK_OFF      | mitigate    | New guard rejects any passType other than STANDARD_PASS during the KICK_OFF phase server-side (MATCH-07); client cannot bypass by sending HIGH_PASS/LONG_BALL/FIRST_TIME_PASS. |
| T-14-02   | Elevation of Privilege | GAME_ROLL from non-active player        | accept      | Already mitigated upstream by the existing `isActivePlayer` team guard at line ~970; no change needed.                                                                         |
| T-14-03   | Tampering              | Default formation outside legal columns | mitigate    | DEF/MID start positions constrained to q∈[6,20] in teams.ts (MATCH-06); applied to both initial and half-time placement since both read the same squad source.                 |
| T-14-SC   | Tampering              | npm/pip/cargo installs                  | mitigate    | No package installs in this plan; no new dependencies added.                                                                                                                   |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test` passes (MATCH-06 position edits do not break teams.test.ts).
- `pnpm --filter @counter-attack/server test -- kickoffSetup` passes including the two new MATCH-07 cases.
- `pnpm typecheck` is clean (no type errors introduced by the guard or position edits).
- Node assertion confirms every DEF/MID start q is within [6,20].
</verification>

<success_criteria>

- MATCH-06: every midfielder and back starts within hex columns 6–20 in both squads (and therefore at half-time reset, which reuses the same squad source).
- MATCH-07: a non-Standard-Pass opening action from the kick off hex during KICK_OFF is rejected server-side with KICKOFF_STANDARD_PASS_ONLY and the state snaps back to KICK_OFF; a Standard Pass is not blocked.
  </success_criteria>

<output>
Create `.planning/phases/14-kick-off-rules-replay/14-01-SUMMARY.md` when done.
</output>
