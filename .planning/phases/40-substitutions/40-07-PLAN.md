---
phase: 40-substitutions
plan: 07
type: execute
wave: 4
depends_on: [40-04, 40-05, 40-06]
files_modified:
  - packages/server/src/__tests__/substitution.integration.test.ts
autonomous: false
requirements: [SUB-01, SUB-02, SUB-03, SUB-04, SUB-05, SUB-06, SUB-07, SETTINGS-04]
must_haves:
  truths:
    - 'Two connected managers can each complete a substitution during a stoppage and both see the same resulting squad'
    - "A substitute inherits the departing player's shirt number and hex, and appears in both clients' broadcast state"
    - 'The third substitution is the last one a team gets, across the half-time boundary'
    - 'Added time at the end of a half includes one minute per substitution made in that half'
    - 'A substituted-out player is never offered again and a red-carded player is never replaceable'
    - 'A red-carded player appears on their team bench as a permanently unfillable entry and can never be brought back on (D-13)'
    - 'A Standard-mode room (11-player squads) reaches kick-off with an EMPTY bench, the substitution screen is still reachable, and no substitution is possible (D-12)'
  artifacts:
    - path: 'packages/server/src/__tests__/substitution.integration.test.ts'
      provides: 'Two-client socket walkthrough of SUB-01..07 and SETTINGS-04'
      min_lines: 200
  key_links:
    - from: 'packages/server/src/__tests__/substitution.integration.test.ts'
      to: 'ClientEvents.GAME_SUBSTITUTION'
      via: 'socket emit from both clients'
      pattern: 'GAME_SUBSTITUTION'
---

<objective>
Prove the whole substitution feature works across the real socket boundary with two
connected clients, then have a human confirm the interaction feel in two browsers.

Purpose: plans 40-01..40-06 each verify their own layer. This plan is the only place the
seams are exercised together — bench seeding at `LINEUP_CONFIRM`, the stoppage gate, the
engine rules, the added-time fold-in, D-13's red-card bench relocation, and roster
continuity through a goal. It runs in the final wave, after every behaviour it checkpoints
exists, so the human walkthrough verifies the complete phase rather than a partial build.

Output: one integration test file and a signed-off human verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-substitutions/40-CONTEXT.md
@.planning/phases/40-substitutions/40-VALIDATION.md
@.planning/ROADMAP.md
</context>

## Artifacts this phase produces

This plan creates `packages/server/src/__tests__/substitution.integration.test.ts`. The
full phase-wide symbol inventory is in `40-01-PLAN.md` under the same heading.

<tasks>

<task type="auto">
  <name>Task 1: Two-client substitution integration test</name>
  <files>packages/server/src/__tests__/substitution.integration.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/goalKick.integration.test.ts (whole file — the canonical two-client lifecycle: server boot, `createClient`, `confirmDefaultRoomSettings`, room/team/uniform/formation/lineup progression to a live `GameState`)
    - packages/server/src/__tests__/draftSession.integration.test.ts (how to drive a DRAFT room to `draftComplete` so a non-empty bench exists — `driveDraftToCompletionBenchOnly`-style helper)
    - packages/server/src/__tests__/testHelpers.ts (`confirmDefaultRoomSettings` — note it defaults `teamType: 'standard'`; a draft room needs its own settings payload)
    - packages/shared/src/stoppagePhases.ts (`STOPPAGE_PHASES`, `MAX_SUBS_PER_TEAM`, `maxOnPitchFor`)
    - packages/shared/src/types.ts (`BenchEntry`/`BenchEntryStatus` — assertions read `status`, never a boolean flag)
    - packages/server/src/roomStore.ts (`getRoom` — for seeding phase/state between assertions)
    - packages/server/src/gameEngine.ts `relocateRedCardedToBench` and its `resolveFoulChain` call site (plan 40-04 Task 4) — the D-13 behaviour this file proves over the wire
  </read_first>
  <action>
    Create the integration file following `goalKick.integration.test.ts`'s structure
    (self-contained server lifecycle and client helpers — this codebase deliberately copies
    that boilerplate per file rather than sharing it).

    Drive a DRAFT room through to a live match so both teams have a real 6-player bench —
    the draft room is the only room type with substitutable players today.

    Record in a file-header comment the verified fact that a STANDARD room's squads hold
    exactly 11 players today, so D-02/D-12's derivation (bench = roster minus the starting
    11) yields an EMPTY bench there, and that Phase 40 deliberately does NOT generate or
    seed one (D-10 retracted). The standard-room case therefore asserts an empty bench and
    a rejected substitution attempt — this is working-as-intended behaviour, not a gap, and
    the assertion must not be "fixed" by adding bench generation.

    Cover, in order:
    1. After `LINEUP_CONFIRM`, both clients' broadcast `GameState` has `bench.home.length === 6`,
       `bench.away.length === 6`, every entry `status: 'available'`, `subsUsed`
       `{ home: 0, away: 0 }`, `addedTimeBonus: 0`, and every piece carries a `playerId`.
    2. During `KICK_OFF_SETUP`, the home client substitutes; both clients receive a state in
       which the slot's `playerId` changed, `number` and `position` did not, `subsUsed.home`
       is 1, the bench entry for the outgoing player has `status: 'subbedOut'`, and the tail
       `eventLog` entry is the `SUBSTITUTION` event.
    3. The away client substitutes in the same stoppage (proving substitution is not
       turn-bound) and `subsUsed.away` becomes 1 without touching `subsUsed.home`.
    4. Re-emitting with the just-substituted-out player as `inPlayerId` is rejected
       `ALREADY_SUBBED` (SUB-07).
    5. Three home substitutions succeed and the fourth is rejected `SUB_CAP_REACHED`; after
       seeding a `HALF_TIME` transition, `subsUsed.home` is still 3 and a further attempt is
       still rejected (SUB-04, cap never resets).
    6. Seed a state whose `actionCount` is one movement cycle short of 45 with a known
       `refereeCard.leniency`, make substitutions, drive the half end, and assert the
       resulting `addedTime` equals roll + leniency + the number of substitutions made in
       that half (SUB-05).
    7. Seed a `redCarded` piece and assert substituting it is rejected `CANNOT_SUB_RED_CARD`,
       and that a substitution of a different piece still succeeds while the team stays at 10
       on-pitch (SUB-06/D-08).
    8. D-13 over the wire: with `foulsEnabled`/`bookingEnabled` on, drive a real foul whose
       injected `bookingDie` produces a red card, then assert on BOTH clients' broadcast
       state that (a) the sent-off player's team bench gained an entry with that player's
       `playerId`, their shirt `number` and `status: 'redCarded'`; (b) the piece is STILL in
       `pieces` with `redCarded: true` and `onPitch: false`; (c)
       `maxOnPitchFor(pieces, team)` is 10; and (d) emitting `GAME_SUBSTITUTION` with that
       player's `playerId` as `inPlayerId` is rejected `CANNOT_SUB_IN_RED_CARDED`.
    9. Substitute, then drive a goal, and assert the substitute is still at the slot, the
       departed player has not reappeared, and the bench (including the D-13 red-card entry
       from case 8's setup shape) is unchanged (SUB-03/SUB-07 continuity through the reset).
    10. SETTINGS-04: repeat the basic success case in a room created with all four v1.6
        toggles off and again with all four on.
    11. D-12: in a STANDARD room driven to a live match, assert `bench.home.length === 0`
        and `bench.away.length === 0`, that the broadcast state is otherwise fully valid
        (`subsUsed` zeroed, `addedTimeBonus: 0`, every piece carrying a `playerId`), and that
        emitting `GAME_SUBSTITUTION` with any `inPlayerId` is rejected `INVALID_SUBSTITUTE`
        while leaving `pieces`, `bench` and `subsUsed` unchanged. Assert explicitly that the
        room does NOT error out or disconnect — an empty bench is a normal state.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- substitution.integration</automated>
  </verify>
  <acceptance_criteria>
    - File exists, is ≥200 lines, and passes with 0 failures
    - `grep -c "it(" packages/server/src/__tests__/substitution.integration.test.ts` ≥ 11, with each title naming its requirement id (case 8 names `D-13`, case 11 names `D-12`)
    - The added-time assertion computes the expected value from the injected roll and `refereeCard.leniency` read from state — no hard-coded total
    - The standard-room case asserts an EMPTY bench and an `INVALID_SUBSTITUTE` rejection — it must NOT assert any auto-filled or generated bench player
    - `grep -inE "free-agent|freeAgent|FREE_AGENT|seedEmptyBench" packages/server/src/__tests__/substitution.integration.test.ts` returns 0 matches (D-10 retracted)
    - `grep -c "CANNOT_SUB_IN_RED_CARDED" packages/server/src/__tests__/substitution.integration.test.ts` ≥ 1
    - Every substitution in the file is initiated by a client `GAME_SUBSTITUTION` emit — no test asserts a server-initiated or automatic substitution, because this phase builds none
    - `pnpm --filter @counter-attack/server test` green; if a worker crash appears, re-run with `--pool=forks` before investigating (known environment flake)
  </acceptance_criteria>
  <done>All eight SUB/SETTINGS requirements plus D-12 and D-13 are proven across the real socket boundary with two clients.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Two-browser substitution walkthrough</name>
  <what-built>
    Substitutions end to end: a persistent SUB strip on the right edge of the pitch row
    (enabled only at a stoppage), a modal roster screen reusing the pre-match
    lineup/drag-and-drop screen, 1-for-1 bench→pitch substitution with jersey-number and
    position inheritance, a 3-per-match cap that survives half-time, +1 added-time minute
    per substitution, OUT badges on substituted-out bench cards, RED CARD badges on
    sent-off players now shown on the bench (D-13), red-carded players unreplaceable, and
    card/injury chips on roster rows. Standard-mode rooms deliberately have an empty bench
    (D-12) — the screen still opens and simply offers no substitutes.
  </what-built>
  <how-to-verify>
    1. Start the dev servers (`pnpm dev` from the repo root) and open two browser windows
       side by side at the client URL.
    2. Create a room in window A with team type **Draft** and all four v1.6 toggles ON,
       join from window B, and complete the draft and lineup confirmation on both sides.
    3. At the `KICK_OFF_SETUP` screen, confirm the SUB strip is visible on the right edge of
       the pitch in BOTH windows and is interactive (not dimmed).
    4. Click SUB in window A. Expect a modal over the pitch showing only your own XI in
       GK/DEF/MID/FWD columns, a bench carousel with your 6 drafted subs, and a
       "0/3 SUBS USED" chip. Confirm the opponent's players are nowhere in it.
    5. Drag a bench card onto one of your outfield cards. Expect the pitch behind the modal
       to show the substitute at the same hex wearing the same shirt number, the chip to
       read "1/3 SUBS USED", the departed player's bench card to show an "OUT" badge and be
       un-draggable, and a `[SUB]` line in the match log.
    6. Close the modal (× top-right). Start the match and play until a non-stoppage phase
       (e.g. a Movement Phase). Confirm the SUB strip is now dimmed and hovering it shows
       "Substitutions are only available during a stoppage in play."
    7. Play on until a restart occurs (throw-in, goal kick, corner or free kick). Confirm
       the strip re-enables during that restart's setup and that window B can substitute at
       the same moment window A can, regardless of whose turn it is.
    8. Make two more substitutions in window A (3/3 total). Confirm the chip turns red and a
       fourth drag is rejected with "Substitution limit reached for this match."
    9. D-13 — red card to the bench: get a player sent off (a hard tackle with fouls and
       bookings enabled, repeated until a red is shown). Confirm that (a) the player
       disappears from the pitch as before, (b) opening the SUB modal now shows that player
       ON THE BENCH with a "RED CARD" badge, visibly different from an "OUT" badge, (c) that
       card is dimmed and cannot be dragged onto any slot, (d) the permanent slot-cap note
       appears naming the reduced headcount, and (e) their now-empty pitch slot cannot be
       filled by any other bench player.
    10. Play to half time. Confirm the added-time value shown on the HALF TIME overlay
        includes one extra minute per substitution you made in the first half, that after
        starting the second half the SUBS USED chip still reads 3/3, and that the RED CARD
        bench entry from step 9 is still present after the half-time formation reset.
    11. D-12 — Standard-room empty bench: create a SECOND room with team type **Standard**
        (not Draft), join from window B, confirm the lineup and start the match. Open the
        SUB modal at `KICK_OFF_SETUP`. Expect the modal to open normally, show your XI, show
        a "0/3 SUBS USED" chip, and display "No available substitutes on the bench." with no
        bench cards. Confirm this reads as a calm empty state, not an error — this is
        expected until team rosters are expanded in a future phase.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues found (step number + what you saw vs. expected)</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                        |
| ---------------------------------- | -------------------------------------------------------------------------------------------------- |
| two independent clients → one room | Both sockets act on the same authoritative `GameState`; cross-team interference must be impossible |

## STRIDE Threat Register

| Threat ID | Category               | Component                                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                |
| --------- | ---------------------- | ------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-40-22   | Elevation of Privilege | Cross-team substitution over the wire      | mitigate    | The integration test asserts that each client can only change its own `subsUsed`/squad, and that an opponent-piece emit is rejected — verifying 40-05's `socketTeam` guard end to end rather than in isolation.                                                                                |
| T-40-23   | Repudiation            | Divergent client views of the roster       | mitigate    | Every assertion checks BOTH clients' received broadcast, proving the full-snapshot model leaves no client with a stale or private roster view.                                                                                                                                                 |
| T-40-24   | Tampering              | Added-time inflation across a full half    | mitigate    | The half-end assertion derives the expected added time from the injected roll plus the state's own `refereeCard.leniency` plus the substitution count, so an off-by-one accumulation is caught.                                                                                                |
| T-40-33   | Elevation of Privilege | Red-card laundering across the wire (D-13) | mitigate    | Case 8 asserts over the socket that the sent-off player stays in `pieces`, that `maxOnPitchFor` reports the reduced cap, and that emitting their id as `inPlayerId` is rejected `CANNOT_SUB_IN_RED_CARDED` — the relocation is server-authoritative and cannot be faked or undone by a client. |
| T-40-SC   | Tampering              | npm installs                               | accept      | No new packages this phase.                                                                                                                                                                                                                                                                    |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test -- substitution.integration` green
- `pnpm test` (full monorepo) green — this is the phase gate before `/gsd-verify-work`
- Human walkthrough approved
</verification>

<success_criteria>

- All eight phase requirements plus D-12 and D-13 verified across the socket boundary with two clients
- Both clients always observe an identical roster after every substitution
- A sent-off player is visible on the bench, permanently unfillable, and unreturnable
- A Standard-mode room's empty bench is proven to be a calm, valid state
- The human walkthrough is approved, or its findings are captured as gap-closure input
  </success_criteria>

<output>
Create `.planning/phases/40-substitutions/40-07-SUMMARY.md` when done
</output>
