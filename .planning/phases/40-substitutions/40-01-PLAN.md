---
phase: 40-substitutions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/stoppagePhases.ts
  - packages/shared/src/stoppagePhases.test.ts
  - packages/shared/src/index.ts
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/client/src/components/ActionLog.tsx
  - packages/client/src/components/ActionPanel.tsx
  - packages/server/src/gameEngine.ts
autonomous: true
requirements: [SUB-01, SUB-02, SUB-04, SUB-05, SUB-06, SUB-07]
must_haves:
  truths:
    - 'isStoppagePhase() returns true for exactly the 15 stoppage GamePhase values and false for the other 29'
    - 'GameState carries bench, subsUsed and addedTimeBonus fields that survive the full-snapshot broadcast'
    - 'A SUBSTITUTION ActionEvent can be constructed and is rendered as a match-log line'
    - 'Undo can never cross a SUBSTITUTION event'
  artifacts:
    - path: 'packages/shared/src/stoppagePhases.ts'
      provides: 'STOPPAGE_PHASES allow-list, isStoppagePhase(), MAX_SUBS_PER_TEAM, maxOnPitchFor()'
      exports: ['STOPPAGE_PHASES', 'isStoppagePhase', 'MAX_SUBS_PER_TEAM', 'maxOnPitchFor']
    - path: 'packages/shared/src/stoppagePhases.test.ts'
      provides: 'Full-GamePhase-union coverage for isStoppagePhase'
    - path: 'packages/shared/src/types.ts'
      provides: 'BenchEntry type, GameState.bench/subsUsed/addedTimeBonus, PlayerPiece.playerId, SUBSTITUTION ActionEvent'
      contains: 'type BenchEntry'
  key_links:
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/stoppagePhases.js'
      via: 'barrel re-export'
      pattern: "export \\* from './stoppagePhases.js'"
    - from: 'packages/client/src/components/ActionLog.tsx'
      to: 'ActionEvent SUBSTITUTION member'
      via: 'exhaustive switch case'
      pattern: "case 'SUBSTITUTION'"
    - from: 'packages/server/src/gameEngine.ts'
      to: 'applyUndo isBoundary reduce'
      via: 'unconditional boundary term'
      pattern: "evt.type === 'SUBSTITUTION'"
---

<objective>
Establish every shared contract Phase 40 depends on, in one wave-1 plan, so all later
plans compile against a stable surface: the `STOPPAGE_PHASES` allow-list that answers
SUB-01's "any stoppage" question (RESEARCH.md Open Question 1 / Assumption A1 — resolved
explicitly here, not inferred later), the new `GameState` roster fields, the
`SUBSTITUTION` `ActionEvent`, the `GAME_SUBSTITUTION` client event, and registration of
the new event type in every per-event-type bookkeeping list.

Purpose: this codebase has shipped the "new ActionEvent invisible to Undo/Replay" bug
twice already (BUG-30/31, BUG-37 — STATE.md Key Pitfalls). Registering the event in the
same plan that declares it is the countermeasure. It also keeps the monorepo green:
`ActionLog.formatEvent` switches exhaustively over `ActionEvent` with no `default`, so
adding a union member without its log case breaks the client typecheck.

Output: shared types/predicates + event registration. No behavior change yet — no
substitution can be performed until plan 40-02/40-05 land.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-substitutions/40-CONTEXT.md
@.planning/phases/40-substitutions/40-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols introduced by Phase 40 (this plan creates all of the shared-layer ones; later
plans create the rest). The drift/source-grounding pass must treat every name below as
newly created by this phase, not as a pre-existing symbol:

| Symbol                                                                                                       | Kind                                                  | File                                                        | Plan  |
| ------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------- | ----------------------------------------------------------- | ----- |
| `STOPPAGE_PHASES`                                                                                            | const (`readonly GamePhase[]`)                        | `packages/shared/src/stoppagePhases.ts`                     | 40-01 |
| `isStoppagePhase`                                                                                            | function                                              | `packages/shared/src/stoppagePhases.ts`                     | 40-01 |
| `MAX_SUBS_PER_TEAM`                                                                                          | const                                                 | `packages/shared/src/stoppagePhases.ts`                     | 40-01 |
| `maxOnPitchFor`                                                                                              | function                                              | `packages/shared/src/stoppagePhases.ts`                     | 40-01 |
| `BenchEntry`                                                                                                 | type                                                  | `packages/shared/src/types.ts`                              | 40-01 |
| `GameState.bench`                                                                                            | field                                                 | `packages/shared/src/types.ts`                              | 40-01 |
| `GameState.subsUsed`                                                                                         | field                                                 | `packages/shared/src/types.ts`                              | 40-01 |
| `GameState.addedTimeBonus`                                                                                   | field                                                 | `packages/shared/src/types.ts`                              | 40-01 |
| `PlayerPiece.playerId`                                                                                       | field                                                 | `packages/shared/src/types.ts`                              | 40-01 |
| `'SUBSTITUTION'`                                                                                             | `ActionEventType` member + `ActionEvent` union member | `packages/shared/src/types.ts`                              | 40-01 |
| `ClientEvents.GAME_SUBSTITUTION`                                                                             | event constant (`'game:substitution'`)                | `packages/shared/src/events.ts`                             | 40-01 |
| `SubstitutionPayload`                                                                                        | type                                                  | `packages/shared/src/events.ts`                             | 40-01 |
| `applySubstitution`                                                                                          | function                                              | `packages/server/src/gameEngine.ts`                         | 40-02 |
| `SubstitutionRejection`                                                                                      | type                                                  | `packages/server/src/gameEngine.ts`                         | 40-02 |
| `applyRosterContinuity`                                                                                      | function                                              | `packages/server/src/gameEngine.ts`                         | 40-02 |
| `emitSubstitution`                                                                                           | store action                                          | `packages/client/src/store/useGameStore.ts`                 | 40-06 |
| `SubstitutionButton`                                                                                         | component                                             | `packages/client/src/components/GameBoard.tsx`              | 40-06 |
| `LineupAssignmentScreen` prop `mode` (+ `midmatchPieces`, `bench`, `subsUsed`, `maxOnPitch`, `onSubstitute`) | props                                                 | `packages/client/src/components/LineupAssignmentScreen.tsx` | 40-03 |
| `BenchCarousel` prop `unavailablePlayerIds`                                                                  | prop                                                  | `packages/client/src/components/BenchCarousel.tsx`          | 40-03 |
| `packages/server/src/__tests__/gameEngine.substitution.test.ts`                                              | new test file                                         | —                                                           | 40-02 |
| `packages/server/src/__tests__/gameHandlers.substitution.test.ts`                                            | new test file                                         | —                                                           | 40-05 |
| `packages/server/src/__tests__/substitution.integration.test.ts`                                             | new test file                                         | —                                                           | 40-07 |

<tasks>

<task type="auto">
  <name>Task 1: Create the shared substitution-eligibility module (STOPPAGE_PHASES + caps)</name>
  <files>packages/shared/src/stoppagePhases.ts, packages/shared/src/stoppagePhases.test.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/index.ts (barrel export list and its per-line comment convention)
    - packages/shared/src/types.ts lines 768-826 (the full 44-member `GamePhase` union — the list below must be checked against it verbatim)
    - packages/server/src/gameHandlers.ts lines 1626-1666 (`validUndoPhases` — the exact allow-list idiom, including its per-entry inclusion/exclusion comments)
    - packages/shared/src/offside.ts (module-doc-comment + named-export style for a shared pure-rule module)
  </read_first>
  <action>
    Create `packages/shared/src/stoppagePhases.ts`. Module doc comment states it owns
    substitution *eligibility* rules: the stoppage-phase allow-list (SUB-01), the
    whole-match cap constant (SUB-04) and the red-card headcount cap (SUB-06/D-08).

    Export `STOPPAGE_PHASES: readonly GamePhase[]` containing EXACTLY these 15 values and
    no others (this is the planner's explicit resolution of RESEARCH.md Open Question 1 /
    Assumption A1 — do not add or remove entries):
    `KICK_OFF_SETUP`, `HALF_TIME`, `FREE_KICK_SETUP`, `THROW_IN_SETUP`,
    `GOAL_KICK_SETUP_GK`, `GOAL_KICK_SETUP_OPPONENT`, `GOAL_KICK_CHOICE`,
    `CORNER_KICK_GK_SETUP_ATTACKING`, `CORNER_KICK_GK_SETUP_DEFENDING`,
    `CORNER_KICK_TAKER_SELECT`, `CORNER_KICK_REPOSITION`, `CORNER_KICK_FINAL_SETUP`,
    `PENALTY_KICK_SETUP_ATTACKING`, `PENALTY_KICK_SETUP_DEFENDING`,
    `PENALTY_KICK_TAKER_SELECT`.

    Rule the list encodes (put it in the doc comment): a phase is a stoppage when the ball
    is dead and a manager is placing/choosing, i.e. the setup/reposition/taker-select stages of
    each of SUB-01's seven named restarts. Deliberately EXCLUDED, each with a one-line
    inline comment mirroring `validUndoPhases`' commenting discipline: `KICK_OFF` (the
    kick itself), `GOAL_KICK_TARGET`/`GOAL_KICK_MOVE` and `PENALTY_KICK` (ball in flight or
    duel resolving), the whole `GK_RESTART`/`GK_QUICK_THROW`/`GK_KICK_TARGET`/`GK_KICK_MOVE`
    chain (a GK catch/save restart is not one of SUB-01's seven named stoppages —
    GOALKICK-01 already established that chain as structurally separate from Goal Kick),
    `FOUL_CHOICE`/`GK_DIVE_AT_FEET_PROMPT`/`GK_DIVE_AT_FEET_TARGET`/`GK_BOX_ENTRY_PROMPT`/
    `GK_BOX_ENTRY_MOVE` (mid-duel decision prompts, not restarts), and
    `LOBBY`/`FULL_TIME`/`REPLAY` (no live match).

    Also export: `isStoppagePhase(phase: GamePhase): boolean` (returns
    `STOPPAGE_PHASES.includes(phase)`); `MAX_SUBS_PER_TEAM = 3` (SUB-04); and
    `maxOnPitchFor(pieces: readonly PlayerPiece[], teamId: 'home' | 'away'): number`
    returning `11 - (count of that team's pieces with redCarded === true)` — D-08's
    `maxOnPitch = 11 - redCardCount`, DERIVED from `pieces` per RESEARCH.md Assumption A3,
    never a stored counter field.

    Create `packages/shared/src/stoppagePhases.test.ts` covering: every one of the 15
    allow-listed values returns true; a hard-coded literal array of the other 29 `GamePhase`
    values each returns false (spell them out — this is the regression guard that a future
    phase adding a `GamePhase` must consciously classify); `STOPPAGE_PHASES.length === 15`;
    `MAX_SUBS_PER_TEAM === 3`; `maxOnPitchFor` returns 11 with no red cards, 10 with one
    red-carded piece on that team, 9 with two, and ignores the opponent's red cards.

    Add `export * from './stoppagePhases.js';` to `packages/shared/src/index.ts` following
    the existing one-line-comment-per-export convention (comment: Phase 40 SUB-01/04/06).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test -- stoppagePhases</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/shared test -- stoppagePhases` passes with 0 failures
    - `grep -c "'" packages/shared/src/stoppagePhases.ts` confirms the array literal has 15 phase entries and `STOPPAGE_PHASES.length === 15` is asserted in the test
    - `grep "export \* from './stoppagePhases.js'" packages/shared/src/index.ts` returns a match
    - `isStoppagePhase('GK_RESTART')`, `isStoppagePhase('PENALTY_KICK')`, `isStoppagePhase('KICK_OFF')` and `isStoppagePhase('MOVE')` all assert false in the test file
    - `maxOnPitchFor` is exported from `@counter-attack/shared` (import in the test is from the module path, and the barrel export is present)
  </acceptance_criteria>
  <done>The shared eligibility module exists, is barrel-exported, and its test file passes with full 44-value GamePhase coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add BenchEntry, GameState roster fields, PlayerPiece.playerId, SUBSTITUTION event and GAME_SUBSTITUTION client event</name>
  <files>packages/shared/src/types.ts, packages/shared/src/events.ts</files>
  <read_first>
    - packages/shared/src/types.ts lines 14-99 (`PlayerPiece`, including the `onPitch` guardrail comment that forbids reusing it for substitution semantics)
    - packages/shared/src/types.ts lines 116-196 (`ActionEventType` union and its per-phase comment blocks)
    - packages/shared/src/types.ts lines 740-766 (tail of the `ActionEvent` union — `PENALTY_KICK_CLEAR_OUT_MOVE` and `SECOND_HALF_CONFIRM` member shapes)
    - packages/shared/src/types.ts lines 1100-1160 and 1550-1640 (GameState field conventions: required core fields vs. the `?:` optional convention used by every Phase 37/38/39 addition)
    - packages/shared/src/types.ts lines 1060-1098 (`DraftSession.homeBenchIds`/`homeBenchNumbers`, `DraftClientView.benchIds`/`benchNumbers` — the pre-match bench concept being carried forward)
    - packages/shared/src/events.ts (ClientEvents constant block plus the `ClientToServerEvents` typed-signature block, e.g. `GAME_FOUL_CHOICE` at both sites)
  </read_first>
  <action>
    In `packages/shared/src/types.ts`:

    1. Add `export type BenchEntry = { playerId: string; jerseyNumber: number; subbedOut: boolean }`
       near the `PlayerPiece` definition. Doc-comment each field: `playerId` is a
       `PLAYER_POOL` id (`p001`-style), NOT a `PlayerPiece.id`; `jerseyNumber` is the bench
       shirt number (draft rooms: `DraftSession.*BenchNumbers`; standard rooms: the pool
       player's own `number`); `subbedOut: true` marks a player who has LEFT the pitch and
       may never return (SUB-07) — the entry is replaced in place at substitution time
       (see plan 40-02), so bench length is constant for the whole match.

    2. Add `playerId?: string` to `PlayerPiece` with a doc comment: the `PLAYER_POOL` id of
       the human currently occupying this slot. `PlayerPiece.id` stays the slot identity
       (`home-3`), which a substitution deliberately preserves (SUB-03: the substitute
       inherits the departing player's slot, jersey number and position), so `playerId` is
       the only field that tells you WHO is in the slot. Optional so no existing
       construction site breaks; `buildSquadPieces` populates it in plan 40-04.
       Explicitly state it is NOT `onPitch` (see the existing `onPitch` guardrail comment).

    3. Add three optional `GameState` fields next to the Phase 39 toggle block, each with a
       doc comment naming its requirement and its reset semantics:
       `bench?: { home: readonly BenchEntry[]; away: readonly BenchEntry[] }` (SUB-02/07,
       seeded at kick-off from the pre-match lineup/bench split);
       `subsUsed?: { home: number; away: number }` (SUB-04 — whole match, NEVER reset at
       half-time); `addedTimeBonus?: number` (SUB-05/D-06/D-07 — per-half accumulator, reset
       to 0 at the half boundary; the comment must state explicitly that `subsUsed` and
       `addedTimeBonus` are independent counters and must never be reset together).
       All three optional; every read site uses `?? 0` / `?? []` defaults.

    4. Add `'SUBSTITUTION'` to `ActionEventType` with a Phase 40 comment block, and the
       matching `ActionEvent` union member:
       `{ type: 'SUBSTITUTION'; team: 'home' | 'away'; pieceId: string; offPlayerName: string; onPlayerName: string; jerseyNumber: number; subsUsed: number; timestamp: number }`.
       Comment: `pieceId` is the slot piece id (which now holds the incoming player, so both
       names are denormalised into the event for a stable log line); `subsUsed` is the
       POST-increment count used by the log's `n/3` suffix. Deliberately NOT a `DICE_ROLL`
       (STATE.md v1.6 pitfall) and carries no `ballAfter` (the ball does not move).

    In `packages/shared/src/events.ts`: add `GAME_SUBSTITUTION: 'game:substitution'` to
    `ClientEvents`, export
    `type SubstitutionPayload = { outPieceId: string; inPlayerId: string }`, and add the
    typed signature `[ClientEvents.GAME_SUBSTITUTION]: (payload: SubstitutionPayload) => void;`
    to `ClientToServerEvents`, both placed after the Phase 39 entries and following the exact
    comment style used by `GAME_FOUL_CHOICE`.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/shared test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/shared build` (or the package's typecheck script) exits 0
    - `grep -n "type BenchEntry" packages/shared/src/types.ts` returns a match
    - `grep -c "addedTimeBonus" packages/shared/src/types.ts` is at least 1 and the field's doc comment contains the words "never" and "subsUsed" (independence note)
    - `grep -n "'SUBSTITUTION'" packages/shared/src/types.ts` returns matches in BOTH the `ActionEventType` union and the `ActionEvent` union
    - `grep -n "GAME_SUBSTITUTION" packages/shared/src/events.ts` returns matches in both `ClientEvents` and `ClientToServerEvents`
    - `PlayerPiece.playerId` is declared optional (`playerId?: string`) — no existing PlayerPiece literal in the repo fails to typecheck
  </acceptance_criteria>
  <done>Shared types and events expose the full substitution contract; the shared package typechecks and its existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Register SUBSTITUTION in every per-event-type bookkeeping list</name>
  <files>packages/client/src/components/ActionLog.tsx, packages/client/src/components/ActionPanel.tsx, packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/client/src/components/ActionLog.tsx lines 330-360 and 1230-1270 (`formatEvent` signature, `Formatted` shape, the `SECOND_HALF_CONFIRM` case, and the `PNamed`/`pieceColorOf` helpers)
    - packages/client/src/components/ActionPanel.tsx lines 258-315 (the `canUndo` boundary mirror and the comment block explaining term-for-term parity with the server)
    - packages/server/src/gameEngine.ts lines 2900-2985 (`applyUndo`'s `isBoundary` reduce)
    - packages/server/src/gameEngine.ts lines 9180-9260 (`REPLAY_ELIGIBLE_TYPES` and the documented `SECOND_HALF_CONFIRM`/`GK_BOX_ENTRY_MOVE` exclusion rationale)
  </read_first>
  <action>
    Three registrations plus one documented non-registration (RESEARCH.md Pitfall 1 —
    all four decided here, none left implicit):

    1. `ActionLog.tsx`: add a `case 'SUBSTITUTION':` to `formatEvent` returning
       `prefix: '[SUB]'`, `prefixColor: pieceColorOf(event.pieceId)`, `isGoal: false`, and
       content ` #{jerseyNumber} {offPlayerName} → {onPlayerName} ({subsUsed}/3)` built the
       same way the neighbouring cases build their `content` (plain template string is
       fine — no `PNamed` lookup, because the outgoing player is no longer in `pieces`).
       This case is mandatory for the client to compile: `formatEvent` has an exhaustive
       switch with no `default` and a non-nullable return type.

    2. `gameEngine.ts` `applyUndo`: add `evt.type === 'SUBSTITUTION' ||` as an
       UNCONDITIONAL boundary term in the `isBoundary` reduce (same shape as the existing
       `evt.type === 'GK_DIVE_AT_FEET' ||` term), with a comment stating a substitution is a
       committed roster change that Undo must never cross (RESEARCH.md Assumption A4).

    3. `ActionPanel.tsx` `canUndo`: add the identical unconditional term to the client
       mirror, and extend the comment block above `canUndo` with the same one-line
       rationale so the mirror stays provably term-for-term.

    4. `REPLAY_ELIGIBLE_TYPES`: do NOT add `SUBSTITUTION`. Add a comment next to the
       existing `SECOND_HALF_CONFIRM`/`GK_BOX_ENTRY_MOVE` exclusion note recording the
       deliberate decision: a substitution moves no piece and changes no ball state, so it
       produces no replay frame; it is visible in the match log only. This is an explicit
       exclusion, not an omission.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- ActionPanel &amp;&amp; pnpm --filter @counter-attack/server test -- gameEngine.undoReplay39</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "case 'SUBSTITUTION'" packages/client/src/components/ActionLog.tsx` returns a match
    - `grep -n "SUBSTITUTION" packages/server/src/gameEngine.ts` returns a match inside the `applyUndo` isBoundary reduce AND a match in the REPLAY_ELIGIBLE_TYPES exclusion comment
    - `grep -n "SUBSTITUTION" packages/client/src/components/ActionPanel.tsx` returns a match inside the `canUndo` reduce
    - `grep -v '^\s*//' packages/server/src/gameEngine.ts | grep -c "REPLAY_ELIGIBLE_TYPES = new Set" ` still returns 1 and `SUBSTITUTION` does NOT appear inside that Set literal
    - Client and server test suites both run green: `pnpm --filter @counter-attack/client test` and `pnpm --filter @counter-attack/server test`
  </acceptance_criteria>
  <done>The new event type is registered in the log, both Undo boundary lists, and explicitly documented as replay-excluded; the whole monorepo typechecks and all existing tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                                                                              |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client socket → server handler | Any client can emit `game:substitution` with arbitrary ids; the payload type declared here is a compile-time convenience only, never a runtime guarantee |
| shared predicate → both tiers  | `isStoppagePhase` is imported by client (button enablement) and server (handler guard); divergence between them would create a client-trusted gate       |

## STRIDE Threat Register

| Threat ID | Category               | Component                                        | Disposition | Mitigation Plan                                                                                                                                                                                                    |
| --------- | ---------------------- | ------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-40-01   | Tampering              | `ClientEvents.GAME_SUBSTITUTION` payload type    | mitigate    | Payload typing is declarative only; runtime validation of `outPieceId`/`inPlayerId` is mandated in plans 40-02 (engine guards) and 40-05 (handler guards). No trust is placed in the declared type.                |
| T-40-02   | Elevation of Privilege | `isStoppagePhase` shared predicate               | mitigate    | Single shared source of truth exported from `packages/shared` and imported by both tiers — the server never re-declares a local copy, so a client-side edit cannot widen the server's gate.                        |
| T-40-03   | Information Disclosure | `GameState.bench` in the full-snapshot broadcast | accept      | Bench composition is public match information (both managers see the opponent's bench on the pitch anyway once subs are made); the pre-match draft privacy model (`DraftClientView`) does not apply post-kick-off. |
| T-40-SC   | Tampering              | npm installs                                     | accept      | This phase installs zero new packages (RESEARCH.md "Package Legitimacy Audit: not applicable"); no supply-chain surface is added.                                                                                  |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test` green
- `pnpm --filter @counter-attack/client test` green (proves the exhaustive `formatEvent` switch compiles)
- `pnpm --filter @counter-attack/server test` green (proves the isBoundary edit broke nothing)
- `pnpm test` (full monorepo) green before this plan is considered done
</verification>

<success_criteria>

- `isStoppagePhase` classifies all 44 `GamePhase` values, asserted by test
- `STOPPAGE_PHASES` contains exactly the 15 resolved values, with inline exclusion comments for the ambiguous families
- `GameState` exposes `bench`, `subsUsed`, `addedTimeBonus`; `PlayerPiece` exposes `playerId`
- `SUBSTITUTION` exists as an `ActionEvent`, renders in the match log, is an Undo boundary on both tiers, and is documented as replay-excluded
- No behavior change to any existing flow: every pre-existing test still passes
  </success_criteria>

<output>
Create `.planning/phases/40-substitutions/40-01-SUMMARY.md` when done
</output>
