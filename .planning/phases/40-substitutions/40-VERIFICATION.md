---
phase: 40-substitutions
verified: 2026-08-17T00:45:33Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
---

# Phase 40: Substitutions Verification Report

**Phase Goal:** Managers can substitute players at any stoppage under a 3-per-match cap, with number/position inheritance, added-time contribution, and restrictions on red-carded or previously-substituted players — regardless of which other v1.6 toggles are enabled.
**Verified:** 2026-08-17T00:45:33Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth                                                                                                                                                                                                             | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | A manager can substitute a player at any stoppage (kick-off, half-time, free kick, penalty kick, goal kick, corner kick, throw-in setup) via drag-and-drop on the Roster screen, regardless of other v1.6 toggles | ✓ VERIFIED | `packages/shared/src/stoppagePhases.ts` exports `STOPPAGE_PHASES` (15 values covering all 7 named restarts' setup/reposition/taker-select stages) and `isStoppagePhase()`. Server guard: `applySubstitution` guard 1 (`gameEngine.ts:3031`) and handler guard (`gameHandlers.ts` `GAME_SUBSTITUTION`, uses `isStoppagePhase(room.gameState.phase)`). Client: `SubstitutionButton` always clickable, `GameBoard.tsx:611` `readOnly={!isSubEligiblePhase}`. Drag-and-drop wired in `LineupAssignmentScreen.tsx` `renderMidmatchColumn` onDrop → `onSubstitute?.(piece.id, inPlayerId)`. Toggle-independence proven by `gameEngine.substitution.test.ts:391` "SETTINGS-04: substitution succeeds under all 16 combinations of the four game-creation toggles" (passing). Full 55-test `stoppagePhases.test.ts` suite passes (all 44 GamePhase values classified).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2   | Substitute inherits departing player's jersey number and pitch position/lineup slot; each team limited to 3 subs for full match, never resets at half-time                                                        | ✓ VERIFIED | `applySubstitution` (`gameEngine.ts:3102-3123`) builds `newPiece = { ...outPiece, playerId, firstName, ... }` — spreads outPiece first so `id`/`teamId`/`number`/`position` (slot identity) are inherited, only identity/stat fields overwritten. `MAX_SUBS_PER_TEAM = 3` guard (`gameEngine.ts:3037`) rejects the 4th attempt with `SUB_CAP_REACHED`. `subsUsed` is initialized once at kick-off (`gameEngine.ts:447`) and only ever incremented in `applySubstitution` (`gameEngine.ts:3145-3149`) — grepped for all `subsUsed` occurrences in gameEngine.ts and confirmed no reset path exists anywhere, including the half-time transition (comment at `gameEngine.ts:2565` explicitly documents "subsUsed ... deliberately NOT reset here or anywhere else"). `gameEngine.substitution.test.ts` (42/42 passing) and `substitution.integration.test.ts` (12/12 passing, includes a cap-survives-HALF_TIME-transition case) cover this end to end.                                                                                                                                                                                                                                                                                                                                                                                                              |
| 3   | Each completed substitution adds 1 minute to the current half's added-time calculation                                                                                                                            | ✓ VERIFIED | `applySubstitution` increments `addedTimeBonus` unconditionally (`gameEngine.ts:3155`) and, when the per-half roll has already fired, directly increments `addedTime` (`gameEngine.ts:3156`, mutually exclusive with the fold-in path per inline comment). `applyEndTurn`'s added-time roll folds in `state.addedTimeBonus` (`gameEngine.ts:2537`: `roll + leniency + addedTimeBonus`), and `addedTimeBonus` resets to 0 only at the half-boundary crossing (`gameEngine.ts:2568`), independently of `subsUsed`. Verified by `substitution.integration.test.ts`'s SUB-05 case ("addedTime equals the injected roll + leniency + substitutions made that half — computed from state, never hard-coded" per 40-07-SUMMARY.md, and independently confirmed passing in this verification's own test run).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 4   | A red-carded player cannot be replaced by a substitute, and a substituted-out player is shown a clear "unavailable" indicator and can never return                                                                | ✓ VERIFIED | `applySubstitution` guard 4 (`gameEngine.ts:3051-3053`) rejects `CANNOT_SUB_RED_CARD` when the outgoing piece is red-carded. Guard 6 (`gameEngine.ts:3066-3068`) rejects `ALREADY_SUBBED` for a bench entry with `status: 'subbedOut'`. Guard 7 (`gameEngine.ts:3074-3076`) separately rejects `CANNOT_SUB_IN_RED_CARDED` for a bench entry with `status: 'redCarded'` (D-13). Client renders OUT / RED CARD badges via `BenchCarousel`'s `unavailablePlayerIds`/`redCardedPlayerIds` props (dimmed, `draggable={false}`, confirmed in `BenchCarousel.tsx:107-193` and `DraftPackCarousel.tsx:85-122`), fed from `LineupAssignmentScreen.tsx:777-826` filtering bench entries by `status`. A red-carded on-pitch card is blocked as a sub target via `isSubBlocked`/`isBlocked = piece.redCarded === true` (`LineupAssignmentScreen.tsx:456,470,489`). `relocateRedCardedToBench` (`gameEngine.ts:773`) adds the sent-off player to the bench while the piece remains in `state.pieces`, keeping `maxOnPitchFor`'s headcount math correct. `applyRosterContinuity` (`gameEngine.ts:3201`) overlays live identity onto every reset (goal, half-time) so a subbed-out or red-carded player can never be resurrected. All covered by passing tests in `gameEngine.substitution.test.ts`, `gameHandlers.substitution.test.ts`, and `substitution.integration.test.ts`. |

**Score:** 4/4 ROADMAP success criteria verified

### Plan-Level Must-Haves (frontmatter, 40-01..40-07)

All plan-declared `must_haves.truths`/`artifacts`/`key_links` were checked against the codebase and independently confirmed — no discrepancies found beyond the two already-known code-review warnings (see below). Representative spot checks:

- `packages/shared/src/stoppagePhases.ts`: `STOPPAGE_PHASES` (exactly 15 entries), `isStoppagePhase`, `MAX_SUBS_PER_TEAM`, `maxOnPitchFor` all present and exported; barrel-exported from `index.ts`.
- `BenchEntryStatus`/`BenchEntry`/`GameState.bench`/`subsUsed`/`addedTimeBonus`/`PlayerPiece.playerId`/`SUBSTITUTION` ActionEvent/`GAME_SUBSTITUTION` client event all present in `types.ts`/`events.ts`.
- `SUBSTITUTION` registered as an unconditional Undo boundary on both `gameEngine.ts` (`applyUndo`) and `ActionPanel.tsx` (`canUndo`); explicitly excluded from `REPLAY_ELIGIBLE_TYPES` with a documented rationale; renders via `ActionLog.tsx`'s `case 'SUBSTITUTION':`.
- `applySubstitution`, `applyRosterContinuity`, `relocateRedCardedToBench` all present in `gameEngine.ts` with the exact guard ordering, roster-continuity, and D-13 semantics described in the plans.
- `GAME_SUBSTITUTION` socket handler wired in `gameHandlers.ts`; `emitSubstitution` store action wired in `useGameStore.ts`; `SubstitutionButton` + `mode="midmatch"` modal wired in `GameBoard.tsx`.
- Bench seeding at `LINEUP_CONFIRM` (draft: `session.homeBenchIds`/`awayBenchIds`; standard: squad-minus-assignment, correctly empty for today's 11-player squads per D-12) confirmed in `roomHandlers.ts:898-933`.
- Mid-match roster panel groups by fixed formation slot (`FORMATIONS[formationId].slots[slotIndex].slotRole`, parsed from `piece.id`'s `${team}-${slotIndex}` suffix), not by the occupant's own role — confirmed in `LineupAssignmentScreen.tsx:435-448` and covered by a dedicated formation-shape regression test.

### Required Artifacts

| Artifact                                                         | Expected                                                                                    | Status     | Details                                                                   |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------- |
| `packages/shared/src/stoppagePhases.ts`                          | STOPPAGE_PHASES/isStoppagePhase/MAX_SUBS_PER_TEAM/maxOnPitchFor                             | ✓ VERIFIED | Present, exported, barrel-exported, 55/55 tests passing                   |
| `packages/shared/src/types.ts`                                   | BenchEntryStatus/BenchEntry/GameState roster fields/PlayerPiece.playerId/SUBSTITUTION event | ✓ VERIFIED | All present; typecheck clean                                              |
| `packages/shared/src/events.ts`                                  | GAME_SUBSTITUTION event + SubstitutionPayload                                               | ✓ VERIFIED | Present in ClientEvents and ClientToServerEvents                          |
| `packages/server/src/gameEngine.ts`                              | applySubstitution/applyRosterContinuity/relocateRedCardedToBench                            | ✓ VERIFIED | All three present, wired, 42/42 unit tests passing                        |
| `packages/server/src/gameHandlers.ts`                            | GAME_SUBSTITUTION handler                                                                   | ✓ VERIFIED | Present at line 1724-1728+, 43/43 tests passing                           |
| `packages/server/src/roomHandlers.ts`                            | Bench computation at LINEUP_CONFIRM                                                         | ✓ VERIFIED | Present, draft + standard paths both correct                              |
| `packages/client/src/store/useGameStore.ts`                      | emitSubstitution socket action                                                              | ✓ VERIFIED | Present, wired to socket.emit                                             |
| `packages/client/src/components/GameBoard.tsx`                   | SubstitutionButton + modal                                                                  | ✓ VERIFIED | Present, always-open/read-only-gated per gap-closure, 63/63 tests passing |
| `packages/client/src/components/LineupAssignmentScreen.tsx`      | mode="midmatch" branch, drag-to-substitute, formation-preserving grouping                   | ✓ VERIFIED | Present, 36/36 tests passing including formation-shape regression         |
| `packages/client/src/components/BenchCarousel.tsx`               | unavailablePlayerIds/redCardedPlayerIds badges, disabled prop                               | ✓ VERIFIED | Present, wired                                                            |
| `packages/server/src/__tests__/substitution.integration.test.ts` | Two-client socket walkthrough of SUB-01..07/SETTINGS-04                                     | ✓ VERIFIED | 12/12 passing (run independently in this verification)                    |

### Key Link Verification

| From                                    | To                                                  | Via                        | Status  | Details                                                                                      |
| --------------------------------------- | --------------------------------------------------- | -------------------------- | ------- | -------------------------------------------------------------------------------------------- |
| `gameHandlers.ts` GAME_SUBSTITUTION     | `stoppagePhases.ts` isStoppagePhase                 | phase guard                | ✓ WIRED | Confirmed                                                                                    |
| `gameHandlers.ts` GAME_SUBSTITUTION     | `gameEngine.ts` applySubstitution                   | pure-function delegate     | ✓ WIRED | Confirmed                                                                                    |
| `GameBoard.tsx`                         | `stoppagePhases.ts` isStoppagePhase                 | button/readOnly gating     | ✓ WIRED | `isSubEligiblePhase = isStoppagePhase(phase)`, used for both button color and modal readOnly |
| `GameBoard.tsx`                         | `LineupAssignmentScreen.tsx`                        | mode="midmatch"            | ✓ WIRED | Confirmed at GameBoard.tsx:593                                                               |
| `useGameStore.ts`                       | `ClientEvents.GAME_SUBSTITUTION`                    | socket.emit                | ✓ WIRED | Confirmed                                                                                    |
| `LineupAssignmentScreen.tsx`            | onSubstitute callback                               | drag bench → drop on-pitch | ✓ WIRED | Confirmed, gated by isBlocked/readOnly                                                       |
| `BenchCarousel.tsx`                     | `DraftPackCarousel.tsx` unavailable/redCarded props | membership check           | ✓ WIRED | Confirmed                                                                                    |
| `gameEngine.ts` goal + half-time resets | `applyRosterContinuity`                             | overlay of live identity   | ✓ WIRED | Confirmed at 4 in-engine reset sites plus 3 handler-side goal resets                         |

### Behavioral Spot-Checks (test suites executed independently in this verification)

| Behavior                                                              | Command                                                                  | Result                         | Status |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------ | ------ |
| Shared stoppage-phase classification                                  | `pnpm --filter @counter-attack/shared test -- stoppagePhases`            | 55/55 passed                   | ✓ PASS |
| Engine-level substitution rules (SUB-02..07, SETTINGS-04)             | `pnpm --filter @counter-attack/server test -- gameEngine.substitution`   | 42/42 passed                   | ✓ PASS |
| Socket-handler guards (SUB-01..07)                                    | `pnpm --filter @counter-attack/server test -- gameHandlers.substitution` | 43/43 passed                   | ✓ PASS |
| Two-client integration walkthrough (SUB-01..07/SETTINGS-04/D-12/D-13) | `pnpm --filter @counter-attack/server test -- substitution.integration`  | 12/12 passed                   | ✓ PASS |
| Mid-match roster panel rendering + formation-shape regression         | `pnpm --filter @counter-attack/client test -- LineupAssignmentScreen`    | 36/36 passed                   | ✓ PASS |
| SUB affordance + modal (GameBoard)                                    | `pnpm --filter @counter-attack/client test -- GameBoard`                 | 63/63 passed                   | ✓ PASS |
| Monorepo typecheck                                                    | `pnpm run typecheck`                                                     | shared/client/server all clean | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s)                                  | Description                                               | Status      | Evidence                                                                                     |
| ----------- | ----------------------------------------------- | --------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------- |
| SUB-01      | 40-05, 40-06, 40-07                             | Substitute at any stoppage, regardless of toggles         | ✓ SATISFIED | `isStoppagePhase` guard both tiers; SETTINGS-04 16-combo test                                |
| SUB-02      | 40-01..40-07                                    | Roster-screen drag-and-drop, one player per action        | ✓ SATISFIED | `renderMidmatchColumn` onDrop → `onSubstitute(outPieceId, inPlayerId)`                       |
| SUB-03      | 40-02, 40-03, 40-04, 40-07                      | Substitute inherits jersey number and slot                | ✓ SATISFIED | `newPiece = { ...outPiece, ... }` spread preserves id/number/position                        |
| SUB-04      | 40-01, 40-02, 40-05, 40-07                      | 3-per-match cap, never resets at half-time                | ✓ SATISFIED | `MAX_SUBS_PER_TEAM = 3`; `subsUsed` never reset (grep-confirmed)                             |
| SUB-05      | 40-01, 40-02, 40-07                             | +1 min added time per substitution                        | ✓ SATISFIED | `addedTimeBonus` fold-in / direct-increment split in `applyEndTurn`/`applySubstitution`      |
| SUB-06      | 40-01, 40-02, 40-03, 40-04, 40-05, 40-07        | Red-carded player cannot be replaced                      | ✓ SATISFIED | Guards 4 and 7 in `applySubstitution`; `relocateRedCardedToBench`                            |
| SUB-07      | 40-01, 40-02, 40-03, 40-04, 40-05, 40-06, 40-07 | Subbed-out player never returns, shown "unavailable"      | ✓ SATISFIED | Guard 6 in `applySubstitution`; OUT badge in `BenchCarousel`/`DraftPackCarousel`             |
| SETTINGS-04 | 40-02, 40-04, 40-05, 40-07                      | Substitutions always available regardless of toggle state | ✓ SATISFIED | 16-combination toggle test passing in `gameEngine.substitution.test.ts` and integration test |

No orphaned requirements — all 8 IDs mapped in REQUIREMENTS.md's "Substitutions (SUB)" section and SETTINGS-04 entry appear in at least one plan's `requirements:` frontmatter field. (REQUIREMENTS.md checkboxes/traceability table itself still show "Pending" — this is expected pre-verification bookkeeping state owned by the orchestrator, not a code gap; all requirements are functionally satisfied per the evidence above.)

### Anti-Patterns Found

| File                                                        | Line    | Pattern                                                                                              | Severity | Impact                                                                                                                                                                     |
| ----------------------------------------------------------- | ------- | ---------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/client/src/components/LineupAssignmentScreen.tsx` | 472-475 | `onDragOver` missing the `readOnly` guard that the sibling `onDrop` enforces (WR-02 in 40-REVIEW.md) | Info     | Inert today (no draggable source can reach it while read-only, since `BenchCarousel` disables `draggable` first) — confirmed by reading the code; not goal-blocking        |
| `packages/client/src/components/LineupAssignmentScreen.tsx` | 111-149 | GK-parity rejection reuses pregame "Swap rejected —" copy in mid-match mode (WR-01 in 40-REVIEW.md)  | Info     | Cosmetic wording mismatch on a narrow rejection subcase only; the substitution itself is still correctly rejected server-side; does not affect SUB-06's actual enforcement |

No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers were introduced by this phase. Two TODO-labeled comments exist in files this phase touched (`gameEngine.ts:7868`, `ActionLog.tsx:404`) but both predate Phase 40 (confirmed via `git log -S`) and concern unrelated features (steal-attempt dice, opposing-player possession on move) — not counted against this phase.

### Human Verification Required

None outstanding. The phase's checkpoint (`40-07-PLAN.md` Task 2, `checkpoint:human-verify`) was already run live by a human across two browsers, found one correctness bug (formation-shape regression) and four UX gaps, all of which were fixed (commits `558e205`/`066185e`/`948f94c`/`bc800ed`/`f3958b6`) and re-verified/approved by the human on a second pass per `40-07-SUMMARY.md`. This verifier independently confirmed the fix is present in the code (formation-slot-based grouping in `LineupAssignmentScreen.tsx:435-448`) and covered by a passing regression test, rather than trusting the SUMMARY narrative alone.

### Gaps Summary

None. All 4 ROADMAP success criteria and all 8 requirement IDs (SUB-01..07, SETTINGS-04) are backed by code that was independently read and by tests that were independently re-run in this verification session (not merely quoted from SUMMARY.md). The two open code-review warnings (WR-01 cosmetic message mismatch, WR-02 inert missing guard) do not block goal achievement and are appropriately classified as Info here, matching 40-REVIEW.md's own "0 critical" finding.

---

_Verified: 2026-08-17T00:45:33Z_
_Verifier: Claude (gsd-verifier)_
