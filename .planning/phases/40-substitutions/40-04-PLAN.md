---
phase: 40-substitutions
plan: 04
type: execute
wave: 3
depends_on: [40-02]
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/roomHandlers.ts
  - packages/server/src/__tests__/gameEngine.substitution.test.ts
autonomous: true
requirements: [SUB-02, SUB-03, SUB-06, SUB-07]
must_haves:
  truths:
    - 'Every piece created at kick-off carries the pool id of the player occupying it'
    - 'A match starts with a bench holding every squad player who is not in the starting 11, and 0 substitutions used'
    - 'After a goal resets the formation, the substituted-in player is still on the pitch and the substituted-out player has not returned'
    - 'After the half-time formation reset, red cards, yellow cards and injury state are still attached to the right players'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'playerId on every constructed piece; bench/subsUsed/addedTimeBonus seeding; roster continuity at all four in-engine kick-off resets'
      contains: 'applyRosterContinuity('
    - path: 'packages/server/src/roomHandlers.ts'
      provides: 'bench computation at LINEUP_CONFIRM for draft and standard rooms'
      contains: 'BenchEntry'
  key_links:
    - from: 'packages/server/src/roomHandlers.ts LINEUP_CONFIRM'
      to: 'buildInitialGameState bench params'
      via: 'draft session bench ids / squad-minus-assignment'
      pattern: "buildInitialGameState\\("
    - from: 'packages/server/src/gameEngine.ts goal + half-time resets'
      to: 'applyRosterContinuity'
      via: 'overlay of live identity onto reset positions'
      pattern: "applyRosterContinuity\\(resetPieces"
---

<objective>
Give the live match a bench, and make the roster survive every formation reset.

Purpose: D-01/D-02 carry the pre-match lineup/bench split forward into live `GameState`
for the first time — today `benchIds` is discarded at `LINEUP_CONFIRM`. Separately,
`buildKickOffPieces` rebuilds all 22 pieces from the DEFAULT squad order after every goal
and at half-time, which would resurrect a substituted-out player (breaking SUB-03/SUB-07)
and wipe `redCarded` (making D-08's permanent headcount cap evaporate). Both are
prerequisites for substitutions being real rather than cosmetic.

Output: seeded bench state and a continuity overlay at the four in-engine reset sites.
Plan 40-05 applies the same overlay to the three reset sites that live in `gameHandlers.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-substitutions/40-CONTEXT.md
@.planning/phases/40-substitutions/40-RESEARCH.md
</context>

## Artifacts this phase produces

This plan adds the `homeBench`/`awayBench` parameters to `buildInitialGameState`, the
`playerId` assignment inside `buildSquadPieces`, and the `applyRosterContinuity` call
sites. The full phase-wide symbol inventory is in `40-01-PLAN.md` under the same heading.

<tasks>

<task type="auto">
  <name>Task 1: Stamp playerId on every piece and seed bench/subsUsed/addedTimeBonus at match start</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 262-341 (`buildSquadPieces` — the `homePlayers.map((p, i) => ({ ...p, teamId, id: 'home-'+i, position, number }))` construction that currently drops the pool id)
    - packages/server/src/gameEngine.ts lines 343-442 (`buildInitialGameState`'s full parameter list and returned state object; `buildKickOffPieces`'s three-argument passthrough)
    - packages/shared/src/types.ts (`BenchEntry`, `GameState.bench`/`subsUsed`/`addedTimeBonus`, `PlayerPiece.playerId` — plan 40-01)
    - packages/server/src/__tests__/gameEngine.teamselect.test.ts and gameEngine.phase23.test.ts (existing `buildInitialGameState`/`buildKickOffPieces` call sites in tests — confirm the new params are appended last with defaults so none of them break)
  </read_first>
  <action>
    In `buildSquadPieces`, add `playerId: p.id` to both the home and away `.map()`
    constructions, placed next to the existing `id:` override with a comment: `id` is the
    slot identity a substitution deliberately preserves, `playerId` is who currently
    occupies it (SUB-03). The spread `...p` already copies the pool player's `id` into
    `id`, which the explicit `id:` override then replaces — `playerId` is what retains it.

    In `buildInitialGameState`, append two new trailing parameters after `injuryEnabled`,
    both defaulting to an empty array so every existing caller and test keeps compiling:
    `homeBench: readonly BenchEntry[] = []`, `awayBench: readonly BenchEntry[] = []`.
    Add to the returned state object: `bench: { home: homeBench, away: awayBench }`,
    `subsUsed: { home: 0, away: 0 }`, `addedTimeBonus: 0`, each with a short comment naming
    its requirement (SUB-02/07, SUB-04, SUB-05).

    Leave `buildKickOffPieces`'s signature unchanged — post-goal resets keep taking the
    default squad order (STATE.md's Pitfall 8/A2 decision stands); Task 3's continuity
    overlay is what restores identity, not a signature change here.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- gameEngine.teamselect</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "playerId: p.id" packages/server/src/gameEngine.ts` returns 2 (home and away maps)
    - `grep -n "homeBench" packages/server/src/gameEngine.ts` shows the parameter declared with an `= []` default and used in the returned `bench` field
    - `grep -n "subsUsed: { home: 0, away: 0 }" packages/server/src/gameEngine.ts` returns a match
    - A test asserts every piece returned by `buildInitialGameState` has a non-empty `playerId` and that all 22 `playerId` values are distinct
    - `pnpm --filter @counter-attack/server test` green — no existing `buildInitialGameState` caller needed editing
  </acceptance_criteria>
  <done>Pieces carry their pool identity and a fresh match starts with a seeded bench and zeroed counters.</done>
</task>

<task type="auto">
  <name>Task 2: Compute each team's bench at LINEUP_CONFIRM</name>
  <files>packages/server/src/roomHandlers.ts</files>
  <read_first>
    - packages/server/src/roomHandlers.ts lines 790-930 (the whole LINEUP_CONFIRM handler: the draft vs. standard branch, `resolveDraftOrder`, and the exact `buildInitialGameState(...)` argument list this task extends)
    - packages/shared/src/types.ts lines 1060-1098 (`DraftSession.homeBenchIds`/`awayBenchIds`/`homeBenchNumbers`/`awayBenchNumbers`)
    - packages/shared/src/teamConfig.ts (`getSquadPlayers` — the standard-mode full-roster lookup)
    - packages/server/src/roomStore.ts lines 130-200 (`Room.homeAssignment`/`awayAssignment` and the draft-session fields)
    - packages/server/src/__tests__/lineupAssignment.integration.test.ts (the existing socket-level LINEUP_CONFIRM coverage this change must not break)
  </read_first>
  <action>
    Immediately before the `buildInitialGameState(...)` call, compute
    `confirmedHomeBench: BenchEntry[]` and `confirmedAwayBench: BenchEntry[]`, then pass
    them as the two new trailing arguments.

    Draft rooms (`isDraftRoom`): map `session.homeBenchIds` to
    `{ playerId: id, jerseyNumber: session.homeBenchNumbers[id] ?? 0, subbedOut: false }`,
    and the same for away from `awayBenchIds`/`awayBenchNumbers`. These are the 6 cards
    drafted outside the 11 lineup slots.

    Standard rooms: `getSquadPlayers(room.homePickedTeam!)` filtered to players whose id is
    NOT in `room.homeAssignment!`, mapped to
    `{ playerId: p.id, jerseyNumber: p.number, subbedOut: false }`; same for away. Add a
    comment recording the verified fact that every non-draft squad in `PLAYER_POOL` holds
    exactly 11 players today, so this list is empty in standard rooms — the code is written
    against the general rule (D-02: bench = roster minus the starting 11) rather than the
    current data, and the UI already has the "No available substitutes on the bench." empty
    state for it.

    Do not add any new pre-match step, screen, event or `Room` field (D-02 forbids a
    matchday-squad selection step) and do not change any existing guard ordering in the
    handler.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- lineupAssignment</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BenchEntry" packages/server/src/roomHandlers.ts` returns a match and both draft and standard branches produce a value
    - The `buildInitialGameState(` call in LINEUP_CONFIRM passes exactly two additional trailing arguments after `room.injuryEnabled ?? false`
    - `pnpm --filter @counter-attack/server test -- lineupAssignment` and `-- draftSession` both green
    - A test (extend `lineupAssignment.integration.test.ts` or the draft session test) asserts a draft room's first broadcast `GameState` has `bench.home.length === 6` with `subbedOut === false` on every entry, and `subsUsed` `{ home: 0, away: 0 }`
    - No new socket event or `Room` field was introduced: `git diff packages/server/src/roomStore.ts` is empty
  </acceptance_criteria>
  <done>The bench survives `LINEUP_CONFIRM` into live `GameState` for both room types.</done>
</task>

<task type="auto">
  <name>Task 3: Apply roster continuity at the four in-engine kick-off resets</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.substitution.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 3974-4035 (goal reset #1 — the hoisted `const resetPieces = buildKickOffPieces(...)` feeding both `state.pieces` and the GOAL event's `piecesAfter`, BUG-30's invariant)
    - packages/server/src/gameEngine.ts around line 4075 (goal reset #2) and around line 7185 (goal reset #3)
    - packages/server/src/gameEngine.ts lines 9042-9094 (`applyHalfTimeStart` — reset #4, feeding both `pieces` and the `HALF_TIME_KICKOFF_RESET` event's `piecesAfter`)
    - packages/server/src/gameEngine.ts around line 9284 (`buildReplayFrames`' seed — deliberately NOT changed by this task; see the action)
    - packages/server/src/gameEngine.ts `applyRosterContinuity` (plan 40-02) for the overlay contract
  </read_first>
  <action>
    At each of the four in-engine reset sites, wrap the hoisted reset array:
    `const resetPieces = applyRosterContinuity(buildKickOffPieces(...), state.pieces);`
    keeping the single-hoist shape so the identical array still feeds both `state.pieces`
    and the accompanying event's `piecesAfter` (BUG-30's invariant must not regress). Do
    not introduce a second `buildKickOffPieces` call at any site.

    Add a comment at the first site (and a one-line back-reference at the others) recording
    why: `buildKickOffPieces` rebuilds from `getSquadPlayers`' default order, so without the
    overlay a goal would undo every substitution (SUB-03/SUB-07) and clear `redCarded`,
    silently lifting D-08's permanent headcount cap.

    Deliberately do NOT apply the overlay to `buildReplayFrames`' seed at ~9284: the replay
    reconstructs the match from its starting XI forward, and `SUBSTITUTION` is intentionally
    excluded from `REPLAY_ELIGIBLE_TYPES` (plan 40-01), so the replay shows the starting
    lineup throughout. Record that as an explicit comment at the seed site.

    Extend `gameEngine.substitution.test.ts` with a continuity describe block:
    substitute a player in, run one goal-reset path and `applyHalfTimeStart`, and assert for
    each that the substituted-in player is still at the slot (matched by `playerId`), the
    substituted-out player is absent from `pieces`, the bench entry is still
    `subbedOut: true`, `subsUsed` is unchanged, and a `redCarded`/`yellowCards`/
    `injuryCount`-bearing piece keeps those values while its `position` equals the
    formation reset position.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- gameEngine.substitution</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "applyRosterContinuity(buildKickOffPieces" packages/server/src/gameEngine.ts` returns 4
    - `grep -c "buildKickOffPieces(" packages/server/src/gameEngine.ts` shows no increase in total call count versus before this task (each site was wrapped, not duplicated)
    - The `buildReplayFrames` seed site is NOT wrapped, and carries the explanatory comment
    - New continuity tests pass, including the assertion that a red card survives both a goal reset and the half-time reset
    - `pnpm --filter @counter-attack/server test` green — in particular `replay.integration.test.ts`, `gameEngine.cornerKick.test.ts` and `gameEngine.secondHalf.test.ts` are unchanged and passing
  </acceptance_criteria>
  <done>Substitutions, cards and injuries survive every in-engine formation reset, proven by test.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                |
| ------------------------------------- | ------------------------------------------------------------------------------------------ |
| pre-match room state → live GameState | `LINEUP_CONFIRM` promotes server-held draft/assignment data into the broadcast match state |

## STRIDE Threat Register

| Threat ID | Category    | Component                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                     |
| --------- | ----------- | ------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-40-11   | Tampering   | Bench composition source        | mitigate    | Bench entries are derived exclusively from server-held state (`room.draftSession.*BenchIds` / `room.*Assignment` + `getSquadPlayers`), never from the client's `confirmedOrder` payload — preserving the existing ASVS V5 stance the handler already documents for the starting XI. |
| T-40-12   | Tampering   | Roster continuity across resets | mitigate    | Identity and match state are overlaid from server `state.pieces` keyed by slot id; a client cannot influence which player reappears after a goal, and `redCarded` can no longer be laundered away by scoring.                                                                       |
| T-40-13   | Repudiation | Goal / half-time reset events   | mitigate    | The single-hoist invariant is preserved, so the `piecesAfter` recorded in the `GOAL` and `HALF_TIME_KICKOFF_RESET` events is byte-identical to the state actually applied.                                                                                                          |
| T-40-SC   | Tampering   | npm installs                    | accept      | No new packages this phase.                                                                                                                                                                                                                                                         |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` green (engine, room, replay and integration suites)
- `pnpm test` green before this plan is considered done
</verification>

<success_criteria>

- Every kick-off piece carries a distinct `playerId`
- A draft room's live `GameState` starts with a 6-entry bench per team, `subsUsed` zeroed and `addedTimeBonus` 0
- A substitution survives a goal reset and the half-time reset; the departed player never reappears
- Red/yellow cards and injury counts survive both resets
- The replay seed is deliberately left on the starting XI, documented in code
  </success_criteria>

<output>
Create `.planning/phases/40-substitutions/40-04-SUMMARY.md` when done
</output>
