---
phase: 40-substitutions
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.substitution.test.ts
autonomous: true
requirements: [SUB-02, SUB-03, SUB-04, SUB-05, SUB-06, SUB-07, SETTINGS-04]
must_haves:
  truths:
    - "A substitution replaces exactly one on-pitch player with one bench player, and the substitute inherits the departing player's slot id, jersey number and hex position"
    - "A team's 4th substitution attempt is rejected, and the count is unchanged by half-time"
    - 'A red-carded player cannot be replaced'
    - 'A red-carded player sitting on the bench can never be substituted back on (D-13)'
    - 'A player who has been substituted out can never be substituted back on'
    - 'Each completed substitution adds one minute of added time to the half it occurred in'
    - 'Substitution succeeds with every combination of foulsEnabled/bookingEnabled/injuryEnabled/outOfBoundsEnabled'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applySubstitution pure function, addedTime accumulator fold-in, applyRosterContinuity helper'
      exports: ['applySubstitution', 'applyRosterContinuity']
    - path: 'packages/server/src/__tests__/gameEngine.substitution.test.ts'
      provides: 'Engine-level coverage for SUB-02..07 and SETTINGS-04'
      min_lines: 200
  key_links:
    - from: 'packages/server/src/gameEngine.ts applySubstitution'
      to: 'packages/shared/src/stoppagePhases.ts'
      via: 'isStoppagePhase / MAX_SUBS_PER_TEAM import'
      pattern: 'isStoppagePhase'
    - from: 'packages/server/src/gameEngine.ts applyEndTurn'
      to: 'state.addedTimeBonus'
      via: 'newAddedTime fold-in'
      pattern: 'state.addedTimeBonus'
---

<objective>
Implement the substitution rules engine: the pure `applySubstitution` function that owns
every SUB-02/03/04/06/07 guard (including D-13's "a red-carded bench entry can never come
back on"), the SUB-05 added-time accumulator, and the `applyRosterContinuity` helper that
later plans use so a substitution survives the post-goal and half-time piece rebuilds.

Purpose: the server is the sole authority (STATE.md "Server-authoritative state"). Every
rule lives here as a pure function so it is unit-testable without sockets and so the
handler (40-05) stays a thin mutex + guard + delegate shell.

Output: three engine additions plus the Wave-0 test file the rest of the phase's
verification map points at.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-substitutions/40-CONTEXT.md
@.planning/phases/40-substitutions/40-PATTERNS.md
@.planning/phases/40-substitutions/40-VALIDATION.md
</context>

## Artifacts this phase produces

This plan creates `applySubstitution`, `SubstitutionRejection` (including D-13's
`'CANNOT_SUB_IN_RED_CARDED'` member), `applyRosterContinuity` and
`packages/server/src/__tests__/gameEngine.substitution.test.ts`. The full phase-wide symbol
inventory is in `40-01-PLAN.md` under the same heading — every symbol listed there is new
in Phase 40 and has no pre-existing definition to drift from.

D-10 is retracted: no free-agent seeding symbol (`seedEmptyBenchFromFreeAgentPool` and
friends) exists anywhere in this phase, and no guard in this plan may compensate for an
empty bench by generating one. An empty bench simply means no substitution is possible
(D-12).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create gameEngine.substitution.test.ts in the RED state</name>
  <files>packages/server/src/__tests__/gameEngine.substitution.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.penaltyKick.test.ts lines 1-120 (fixture conventions: inline `PlayerPiece` literals, real `PITCH_REGIONS`-derived hexes, never invented coordinates)
    - packages/server/src/__tests__/gameEngine.phase8.test.ts (the existing `applyEndTurn` + `addedTime` / `options.addedTimeRoll` injection conventions this file must reuse for SUB-05)
    - packages/shared/src/types.ts (`BenchEntryStatus`, `BenchEntry`, `GameState.bench`/`subsUsed`/`addedTimeBonus`, `PlayerPiece.playerId` — added in plan 40-01)
    - packages/shared/src/stoppagePhases.ts (`isStoppagePhase`, `MAX_SUBS_PER_TEAM`, `maxOnPitchFor`)
    - packages/shared/src/teams.ts (`PLAYER_POOL` shape — bench player ids used by fixtures must be real pool ids, e.g. members of `getSquadPlayers('city')`)
  </read_first>
  <behavior>
    - SUB-02/D-04 (1-for-1 only): `applySubstitution` on a valid stoppage state returns `ok: true`; `state.pieces.length` is unchanged (22) and exactly one piece object differs from the input — the signature takes a single `outPieceId`/`inPlayerId` pair and can never express a multi-player swap
    - SUB-03: the piece at `outPieceId` keeps its `id`, `number` and `position`, and its `playerId`/`firstName`/`lastName`/attributes are now the incoming player's
    - SUB-03: the incoming piece has NO inherited `redCarded`/`yellowCards`/`injuryCount` (a fresh player), even when the outgoing piece carried a yellow card and an injury
    - SUB-04: three successful substitutions succeed; the 4th returns `{ ok: false, reason: 'SUB_CAP_REACHED' }`; `subsUsed` for the other team is untouched
    - SUB-04: `subsUsed` survives an `applyEndTurn` HALF_TIME transition unchanged (whole-match cap never resets)
    - SUB-05: each successful substitution increments `addedTimeBonus` by 1; when `addedTime` is already non-null (roll already fired for this half) it also increments `addedTime` by 1
    - SUB-05: `applyEndTurn` with `options.addedTimeRoll` injected folds the accumulator in — `addedTime === roll + refereeCard.leniency + addedTimeBonus`
    - SUB-05/D-07: the `applyEndTurn` HALF_TIME/FULL_TIME return resets `addedTimeBonus` to 0 while leaving `subsUsed` unchanged
    - SUB-06: `outPiece.redCarded === true` returns `{ ok: false, reason: 'CANNOT_SUB_RED_CARD' }`; `maxOnPitchFor` reports 10 for that team afterwards
    - SUB-07: substituting a player out then attempting to bring the same player back returns `{ ok: false, reason: 'ALREADY_SUBBED' }` (that entry's `status` is `'subbedOut'`)
    - D-13: a bench entry whose `status` is `'redCarded'` returns `{ ok: false, reason: 'CANNOT_SUB_IN_RED_CARDED' }`, and the reason is DISTINCT from both `ALREADY_SUBBED` and `CANNOT_SUB_RED_CARD`
    - D-13: after that rejection, `state.pieces`, `state.bench` and `subsUsed` are all referentially/structurally unchanged
    - D-12 (empty bench): `applySubstitution` against a state whose `bench[team]` is `[]` returns `{ ok: false, reason: 'INVALID_SUBSTITUTE' }` for any `inPlayerId` — the engine never fabricates a substitute
    - SUB-01 defence-in-depth: calling with a non-stoppage phase (e.g. `MOVE`) returns `{ ok: false, reason: 'WRONG_PHASE' }`
    - Ownership: `outPieceId` belonging to the opposing team returns `INVALID_SUBSTITUTE`; `inPlayerId` absent from `bench[team]` returns `INVALID_SUBSTITUTE`
    - GK parity: a non-GK bench player onto a GK slot returns `GK_SLOT_REQUIRES_GK`; a GK bench player onto an outfield slot returns `NON_GK_SLOT_REJECTS_GK`
    - SETTINGS-04: a parametrized loop over all 16 combinations of `foulsEnabled`/`bookingEnabled`/`injuryEnabled`/`outOfBoundsEnabled` all return `ok: true`
    - Continuity: `applyRosterContinuity(resetPieces, currentPieces)` returns pieces whose `position` comes from `resetPieces` and whose identity (`playerId`, names, attributes) and match state (`redCarded`, `yellowCards`, `injuryCount`, `onPitch`) come from `currentPieces`, matched by `id`
  </behavior>
  <action>
    Create the test file with the full RED-state spec above. Build a local
    `makeSubState()` fixture factory returning a `GameState` in `KICK_OFF_SETUP` with 22
    pieces (11 per team, ids `home-0..home-10` / `away-0..away-10`, `home-0`/`away-0` role
    `GK`, each carrying a real `playerId` from `getSquadPlayers(...)`), a `bench` of 3
    `BenchEntry` values per team drawn from real `PLAYER_POOL` ids not used in the XI, all
    with `status: 'available'`, plus `subsUsed: { home: 0, away: 0 }`, `addedTimeBonus: 0`,
    `addedTime: null`. Give the factory an options bag so a test can override a single
    entry's `status` to `'subbedOut'` or `'redCarded'` without hand-rolling a second fixture.

    Every hex literal must come from `PITCH_REGIONS` or a `hexNeighbors()` step of one
    (STATE.md placeholder-coordinate pitfall). Import `applySubstitution`,
    `applyRosterContinuity`, `applyEndTurn` and `buildKickOffPieces` from `../gameEngine.js`.

    The suite is expected to FAIL at this point (the two new exports do not exist yet) —
    that is the intended RED state and must be recorded in the task's commit message.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- gameEngine.substitution</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/server/src/__tests__/gameEngine.substitution.test.ts` exists and is ≥200 lines
    - Running the scoped test command reports failures caused by missing `applySubstitution`/`applyRosterContinuity` exports (RED), not by fixture/type errors
    - Every `it(...)` title names its requirement id (`SUB-02` … `SETTINGS-04`) or its decision id (`D-12`, `D-13`) so the VALIDATION map can be traced back
    - `grep -c "it(" packages/server/src/__tests__/gameEngine.substitution.test.ts` is ≥ 18
    - `grep -c "CANNOT_SUB_IN_RED_CARDED" packages/server/src/__tests__/gameEngine.substitution.test.ts` is ≥ 1
    - `grep -c "subbedOut:" packages/server/src/__tests__/gameEngine.substitution.test.ts` returns 0 — bench fixtures use `status:`, never a boolean flag
    - No hard-coded hex object literals other than references to `PITCH_REGIONS` members or `hexNeighbors()` results
  </acceptance_criteria>
  <done>The Wave-0 engine test file exists, compiles against the shared types, and fails only because the implementation is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement applySubstitution</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 248-442 (`buildSquadPieces` / `buildInitialGameState` / `buildKickOffPieces` — how a `PlayerPiece` is constructed from a `PoolPlayer`, including the `id`/`number`/`position` overrides)
    - packages/server/src/gameEngine.ts lines 800-895 (Phase 39 foul chain: how `pieces` is rebuilt immutably with `.map()` and how `redCarded`/`onPitch: false` are set — the shape a substitution must NOT disturb, and the exact site plan 40-04 Task 4 extends for D-13)
    - packages/server/src/gameEngine.ts `applyUndo` (an existing `{ ok: true; state } | { ok: false; reason }` pure function — return-contract template)
    - packages/shared/src/types.ts lines 14-99 (`PlayerPiece`, `BenchEntryStatus`, `BenchEntry`, and the `onPitch` guardrail comment)
    - packages/shared/src/stoppagePhases.ts (`isStoppagePhase`, `MAX_SUBS_PER_TEAM`)
    - packages/shared/src/teams.ts (`PoolPlayer` field list — the exact attribute set the new piece copies)
    - packages/server/src/__tests__/gameEngine.substitution.test.ts (the spec written in Task 1)
  </read_first>
  <action>
    Export from `gameEngine.ts`:
    `type SubstitutionRejection = 'WRONG_PHASE' | 'SUB_CAP_REACHED' | 'CANNOT_SUB_RED_CARD' | 'CANNOT_SUB_IN_RED_CARDED' | 'INVALID_SUBSTITUTE' | 'ALREADY_SUBBED' | 'GK_SLOT_REQUIRES_GK' | 'NON_GK_SLOT_REJECTS_GK'`
    and
    `applySubstitution(state: GameState, team: 'home' | 'away', outPieceId: string, inPlayerId: string): { ok: true; state: GameState } | { ok: false; reason: SubstitutionRejection }`.

    Doc-comment the two red-card reasons side by side so they can never be confused:
    `CANNOT_SUB_RED_CARD` = the OUTGOING on-pitch piece is sent off, so its slot is
    permanently unfillable (SUB-06/D-09); `CANNOT_SUB_IN_RED_CARDED` = the INCOMING bench
    entry is a sent-off player who is displayed on the bench for clarity only (D-13) and
    may never return to the pitch.

    Validate everything BEFORE mutating anything, in this exact order (each returns the
    named reason):
    1. `!isStoppagePhase(state.phase)` → `WRONG_PHASE` (defence in depth; the handler checks too)
    2. `(state.subsUsed?.[team] ?? 0) >= MAX_SUBS_PER_TEAM` → `SUB_CAP_REACHED`
    3. outgoing piece not found in `state.pieces`, or its `teamId !== team`, or its
       `playerId` is undefined → `INVALID_SUBSTITUTE`
    4. `outPiece.redCarded === true` → `CANNOT_SUB_RED_CARD` (SUB-06/D-09: the vacated slot
       is permanently unfillable)
    5. bench entry for `inPlayerId` not present in `state.bench?.[team]` → `INVALID_SUBSTITUTE`.
       This is also the D-12 empty-bench path: `state.bench?.[team] ?? []` being empty makes
       every lookup miss, and the function must NOT compensate by generating a substitute
       from any pool — comment that explicitly so no future edit reintroduces the retracted
       D-10 auto-fill.
    6. that bench entry's `status === 'subbedOut'` → `ALREADY_SUBBED` (SUB-07)
    7. that bench entry's `status === 'redCarded'` → `CANNOT_SUB_IN_RED_CARDED` (D-13).
       Keep 6 and 7 as two structurally separate checks with two distinct reasons — never
       collapse them into a single `status !== 'available'` test, because the client renders
       a different message and a different bench badge for each.
    8. `PLAYER_POOL` lookup of `inPlayerId` fails → `INVALID_SUBSTITUTE`
    9. GK parity, kept as two structurally separate checks (RESEARCH.md Pitfall 6 — never
       merge with any other boolean): outgoing `role === 'GK'` and incoming `role !== 'GK'`
       → `GK_SLOT_REQUIRES_GK`; outgoing `role !== 'GK'` and incoming `role === 'GK'` →
       `NON_GK_SLOT_REJECTS_GK`. Reuse those two existing error strings verbatim (they are
       already emitted by the pre-match lineup flow).

    Then build the new state (all immutable spreads, no mutation of `state`):
    - New piece: every attribute from the incoming `PoolPlayer` (`firstName`, `lastName`,
      `nationality`, `role`, `pace`, `shooting`, `tackling`, `dribbling`, `saving`,
      `handling`, `resilience`, `aerialAbility`, `highPass`) plus `playerId: inPlayerId`,
      and INHERITED from the outgoing piece: `id`, `teamId`, `number`, `position` (SUB-03).
      Do NOT copy `redCarded`, `yellowCards`, `injuryCount` or `onPitch` — a substitute
      arrives clean. Write a comment stating that inheriting `id` is what makes
      `ball.carrierId`, `movedPieceIds`, `paceUsedByPieceId` and every restart-stage
      per-piece budget continue to point at the slot: the substitute deliberately inherits
      the slot's already-spent movement/reposition budget for the current cycle, so a
      substitution can never be used to refresh a budget. No per-cycle tracking field is
      scrubbed.
    - `pieces`: `state.pieces.map(p => p.id === outPieceId ? newPiece : p)` — length stays 22.
    - `bench[team]`: map the incoming player's entry in place to
      `{ playerId: <outgoing piece's playerId>, jerseyNumber: <that entry's jerseyNumber>, status: 'subbedOut' }`.
      The outgoing player takes over the vacated bench card, so bench length is constant and
      the incoming player disappears from the bench. Any entry whose `status` is
      `'redCarded'` is copied through untouched — a substitution never rewrites a red-card
      entry (D-13). The other team's bench array is re-used by reference.
    - `subsUsed`: `{ home, away }` with `team` incremented (SUB-04).
    - `addedTimeBonus`: `(state.addedTimeBonus ?? 0) + 1` (SUB-05/D-06).
    - `addedTime`: unchanged when `null`/absent; when already set (the roll already fired
      for this half) increment it by 1 so a late substitution still adds its minute — the
      fold-in path in Task 3 only runs while `addedTime === null`, so the two paths are
      mutually exclusive and cannot double-count.
    - `eventLog`: append the `SUBSTITUTION` `ActionEvent` with `team`, `pieceId: outPieceId`,
      `offPlayerName`/`onPlayerName` as `"First Last"`, `jerseyNumber: outPiece.number`,
      `subsUsed` = the post-increment count, `timestamp: Date.now()`.
    - Everything else spreads through untouched — in particular `phase`, `activeTeam` and
      `movementSlot` are NOT changed: a substitution never advances the FSM.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- gameEngine.substitution</automated>
  </verify>
  <acceptance_criteria>
    - All SUB-02/03/04/06/07, D-12, D-13, ownership, GK-parity and SETTINGS-04 tests in `gameEngine.substitution.test.ts` pass (SUB-05 fold-in and continuity tests may still fail until Task 3)
    - `grep -n "export function applySubstitution" packages/server/src/gameEngine.ts` returns a match
    - `grep -c "CANNOT_SUB_IN_RED_CARDED" packages/server/src/gameEngine.ts` is ≥ 2 (the union member and the guard-7 return)
    - `grep -c "onPitch" ` within the `applySubstitution` body is 0 — the function never reads or writes `PlayerPiece.onPitch` (Phase 39 guardrail)
    - `grep -inE "freeAgent|free-agent|FREE_AGENT|seedEmptyBench" packages/server/src/gameEngine.ts` returns 0 matches (D-10 retracted)
    - A test asserts `result.state.pieces.length === state.pieces.length` after a successful substitution
    - A test asserts the substitute's `id`, `number` and `position` are strictly equal to the outgoing piece's, and that `playerId` differs
    - `pnpm --filter @counter-attack/server test` shows no regression in any pre-existing engine test
  </acceptance_criteria>
  <done>`applySubstitution` enforces every SUB rule plus D-13's no-return-for-red-cards rule as a pure function, and its engine tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Added-time accumulator fold-in, half-boundary reset, and applyRosterContinuity</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts lines 2438-2476 (the single `newAddedTime` computation at ~2450 and the HALF_TIME/FULL_TIME return branch — the D-06/D-07 integration points)
    - packages/server/src/gameEngine.ts lines 2476-2610 (the three other `addedTime: newAddedTime` return sites at ~2498/2571/2602, to confirm they all read the same local)
    - packages/server/src/gameEngine.ts lines 9042-9094 (`applyHalfTimeStart` — where `addedTime: null` resets; this plan deliberately does NOT reset `addedTimeBonus` here)
    - packages/server/src/gameEngine.ts lines 429-442 (`buildKickOffPieces` — the reset array `applyRosterContinuity` overlays)
    - packages/server/src/__tests__/gameEngine.substitution.test.ts (the SUB-05 and continuity specs from Task 1)
  </read_first>
  <action>
    Two edits plus one new export.

    1. SUB-05 fold-in: change the single assignment
       `newAddedTime = roll + state.refereeCard.leniency` (one line, ~2450) to add
       `+ (state.addedTimeBonus ?? 0)`. Verify by grep that exactly one `newAddedTime =`
       assignment site exists — all four `addedTime: newAddedTime` return sites read the
       same local, so this is one edit, not four (40-PATTERNS.md corrects RESEARCH.md
       Pitfall 2 on this point). Do not touch the `state.addedTime === null` guard: the
       set-once-per-half invariant must survive.

    2. D-07 half-boundary reset: add `addedTimeBonus: 0,` to the `endPhase`
       (HALF_TIME/FULL_TIME) return object in `applyEndTurn`, next to the existing
       `movedPieceIds: []` / `paceUsedByPieceId: {}` / `...THROW_IN_TEARDOWN` resets, with
       a comment stating: (a) `subsUsed` is deliberately NOT reset here (SUB-04 is a
       whole-match cap), and (b) the reset belongs at the END of the half rather than in
       `applyHalfTimeStart`, because `HALF_TIME` is itself a stoppage — substitutions made
       during the half-time screen must accumulate toward the SECOND half's added time.
       Do not add any `addedTimeBonus` handling to `applyHalfTimeStart`.

    3. Export `applyRosterContinuity(resetPieces: PlayerPiece[], currentPieces: readonly PlayerPiece[]): PlayerPiece[]`.
       For each reset piece, look up the current piece with the same `id`; when found return
       `{ ...currentPiece, position: resetPiece.position }`, otherwise return the reset piece
       unchanged. Doc comment must state what it fixes: `buildKickOffPieces` rebuilds pieces
       from the DEFAULT squad order via `getSquadPlayers`, which discards slot identity plus
       every accumulated per-match mutation — so without this overlay a goal or the
       half-time reset would resurrect a substituted-out player (violating SUB-03/SUB-07)
       and wipe `redCarded`/`onPitch: false`, making D-08's permanent headcount cap
       evaporate and putting a sent-off player back on the pitch. Positions come from the
       reset array (kick-off formation, the `+4` shift and the jersey-#9 anchor are already
       applied there and jersey numbers are identical on both sides of the overlay);
       identity and match state come from the live pieces. It never touches `state.bench` —
       the bench (including D-13's red-card entries) is not rebuilt by any reset. This plan
       only exports the helper — wiring it into the reset call sites is plans 40-04 and 40-05.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server test -- gameEngine.substitution &amp;&amp; pnpm --filter @counter-attack/server test -- gameEngine.phase8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "newAddedTime = " packages/server/src/gameEngine.ts` returns 1, and that line contains `state.addedTimeBonus`
    - `grep -n "addedTimeBonus: 0" packages/server/src/gameEngine.ts` returns exactly one match, inside `applyEndTurn`'s HALF_TIME/FULL_TIME return
    - `grep -c "addedTimeBonus" packages/server/src/gameEngine.ts` shows no occurrence inside `applyHalfTimeStart`
    - `grep -n "export function applyRosterContinuity" packages/server/src/gameEngine.ts` returns a match
    - Every SUB-05 and continuity test in `gameEngine.substitution.test.ts` passes
    - `pnpm --filter @counter-attack/server test` green — in particular the pre-existing added-time tests in `gameEngine.phase8.test.ts` and `gameEngine.secondHalf.test.ts` still pass unchanged
  </acceptance_criteria>
  <done>Added time folds in the per-half substitution bonus, the bonus resets at the half boundary while the match cap does not, and the roster-continuity helper is exported and tested.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                                    |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| handler → engine             | `applySubstitution` receives ids that originated in an untrusted client payload; it must not assume the handler validated them |
| engine → GameState broadcast | Any state the engine returns is broadcast verbatim to both players                                                             |

## STRIDE Threat Register

| Threat ID | Category               | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                              |
| --------- | ---------------------- | ----------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-40-04   | Elevation of Privilege | `applySubstitution` `outPieceId`                | mitigate    | Re-validate `outPiece.teamId === team` inside the engine (guard 3) even though the handler also checks — an opponent's piece id can never be substituted regardless of caller.                                                                                               |
| T-40-05   | Tampering              | `applySubstitution` `inPlayerId`                | mitigate    | The id must be present in `state.bench[team]` (server-owned) AND resolve in `PLAYER_POOL`; a client-supplied arbitrary or already-used id is rejected with `INVALID_SUBSTITUTE`/`ALREADY_SUBBED`.                                                                            |
| T-40-06   | Tampering              | Substitution as a budget-refresh exploit        | mitigate    | The substitute inherits the slot id and therefore all per-cycle bookkeeping (`movedPieceIds`, `paceUsedByPieceId`, corner/goal-kick per-piece pace maps); no tracking field is scrubbed, so a sub cannot restore a spent movement or reposition allowance. Asserted by test. |
| T-40-07   | Tampering              | Added-time inflation                            | mitigate    | `addedTimeBonus` is incremented exactly once per accepted substitution, the fold-in path runs only while `addedTime === null`, and the direct-increment path only when it is non-null — mutually exclusive, so a substitution can add at most 1 minute. Asserted by test.    |
| T-40-08   | Repudiation            | Substitution audit trail                        | mitigate    | Every accepted substitution appends a `SUBSTITUTION` `ActionEvent` (both player names, jersey number, running count) to `eventLog`, which is broadcast to both managers.                                                                                                     |
| T-40-29   | Elevation of Privilege | Returning a sent-off player to the pitch (D-13) | mitigate    | Guard 7 rejects any bench entry with `status: 'redCarded'` with its own reason before any state is built; the check runs in the pure engine, so bypassing the client's non-draggable bench card by emitting the id directly changes nothing. Asserted by test.               |
| T-40-SC   | Tampering              | npm installs                                    | accept      | No new packages this phase.                                                                                                                                                                                                                                                  |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test -- gameEngine.substitution` fully green
- `pnpm --filter @counter-attack/server test` green (no regression in added-time, half-time, undo/replay suites)
- `pnpm test` green before this plan is considered done
</verification>

<success_criteria>

- `applySubstitution` rejects all eight invalid cases with the named reasons and accepts the valid case with piece-count, id, number and position invariants intact
- A red-carded bench entry is rejected with its own distinct reason and never returns to the pitch (D-13)
- An empty bench yields `INVALID_SUBSTITUTE` and nothing is generated to fill it (D-12)
- The 3-per-team cap holds across a half boundary; the added-time bonus does not
- Added time equals `roll + leniency + bonus` for the half in which the substitutions occurred
- `applyRosterContinuity` preserves identity and match state while taking positions from the reset array
- No pre-existing engine test changed its expected values
  </success_criteria>

<output>
Create `.planning/phases/40-substitutions/40-02-SUMMARY.md` when done
</output>
