---
phase: 11-rule-correctness
plan: 02
type: execute
wave: 2
depends_on: ['11-01']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.rule11.test.ts
autonomous: true
requirements: [RULE-03]
must_haves:
  truths:
    - 'After a shot or snapshot resolves into a Loose Ball that scatters to a Pass, no stale shot-path hexes persist into the next phase'
    - 'The applyRoll LOOSE_BALL scatter -> PASS transition explicitly sets lastShotPath: null'
    - 'The SHOT loose-ball (tie) and SHOT save-dropped branches that previously carried the path forward are cleared per the audit'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'lastShotPath: null added to the loose-ball scatter and confirmed-bug shot branches'
      contains: 'lastShotPath: null'
  key_links:
    - from: 'packages/server/src/gameEngine.ts applyRoll LOOSE_BALL scatter return'
      to: 'lastShotPath'
      via: 'explicit null on transition to PASS'
      pattern: 'lastShotPath: null'
---

<objective>
Fix RULE-03: stale shot-path highlights persisting after a snapshot/shot resolves into a loose ball
and scatters to the next Pass/Movement phase. Audit every shot and snapshot resolution branch in
gameEngine.ts and explicitly set lastShotPath: null on the branches that carry a non-null path forward
into a phase where the trajectory should no longer render.

Purpose: In v1.0 UAT, shot-path hexes remained tinted on the board into the Movement Phase after a
deflection. The root cause is the applyRoll LOOSE_BALL scatter return spreading the prior state's
non-null lastShotPath without clearing it.
Output: Targeted lastShotPath: null additions (NOT a defensive clear in applyStartMovement, per D-07)
plus a regression test proving the scattered PASS state has lastShotPath null.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rule-correctness/11-CONTEXT.md
@.planning/phases/11-rule-correctness/11-RESEARCH.md
@.planning/phases/11-rule-correctness/11-PATTERNS.md
</context>

## Artifacts this phase produces

No new exported symbols. This plan modifies existing return objects in `gameEngine.ts` and appends
test cases to `gameEngine.rule11.test.ts` (created by plan 11-01).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clear lastShotPath on loose-ball scatter and the confirmed shot bug branches</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.rule11.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (all lastShotPath assignments — confirmed at lines 1179, 1255, 1291, 1328, 1343, 1513, 1597, 2289, 2321; the applyRoll LOOSE_BALL scatter -> PASS return object, which spreads ...state and does NOT currently set lastShotPath; the applyRoll SHOT branch ~lines 1109-1355)
    - .planning/phases/11-rule-correctness/11-RESEARCH.md (Detailed Bug Analysis RULE-03 audit table; Pitfall 5 "RULE-03 fix too broad")
    - .planning/phases/11-rule-correctness/11-PATTERNS.md (gameEngine.ts RULE-03 section: confirmed-correct vs confirmed-bug branch lists)
    - .planning/phases/11-rule-correctness/11-CONTEXT.md (D-07: targeted fix, do NOT add clearing to applyStartMovement; D-08: branches to audit)
  </read_first>
  <behavior>
    - Test: applyRoll on a LOOSE_BALL scatter that transitions to PASS returns state with lastShotPath === null (the primary RULE-03 fix — the scatter previously inherited a non-null path via spread).
    - Test: applyRoll on the SHOT loose-ball (tie) branch (~line 1291) returns lastShotPath === null after the fix.
    - Test: applyRoll on the SHOT save-dropped -> LOOSE_BALL branch (~line 1343) returns lastShotPath === null after the fix.
    - Regression: the confirmed-correct branches (GOAL ~1179/1255, save-caught -> GK_RESTART ~1328) still return lastShotPath === null (unchanged).
  </behavior>
  <action>
    Make three targeted edits in gameEngine.ts. Per D-07, do NOT add any clearing logic to
    applyStartMovement and do NOT touch the SHOT->LOOSE_BALL trajectory display while the ball is still
    loose beyond these three branches; the fix is surgical.

    1. applyRoll LOOSE_BALL scatter -> PASS return object: this return spreads ...state and currently has
       NO lastShotPath key (grep confirms no lastShotPath assignment in the scatter return), so the prior
       phase's non-null lastShotPath persists. Add `lastShotPath: null,` to this return object. This is
       the primary fix — the transition into PASS (and onward to MOVEMENT) must not carry the shot path.
    2. SHOT loose-ball (tie) branch at line ~1291: change `lastShotPath: shotPath,` to `lastShotPath: null,`.
    3. SHOT save-dropped -> LOOSE_BALL branch at line ~1343: change `lastShotPath: shotPath,` to
       `lastShotPath: null,`.

    Leave the confirmed-correct branches unchanged (1179, 1255, 1328, and the gameHandlers SNAP_DEFLECT
    branches which RESEARCH confirms already set null). Leave lines 1513/1597/2289/2321 (header shot path
    and early snapshot trajectory) unchanged — those intentionally set the path for in-flight rendering
    and are not part of the stale-into-next-phase bug.

    Append the four behavior tests above to gameEngine.rule11.test.ts (the file plan 11-01 created).
    Build fixtures from real squad positions so the scatter/shot branches are reachable.

  </action>
  <verify>
    <automated>cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.rule11.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - gameEngine.ts applyRoll LOOSE_BALL scatter -> PASS return object contains `lastShotPath: null`
    - gameEngine.ts line ~1291 (SHOT loose-ball tie) reads `lastShotPath: null` (no longer `shotPath`)
    - gameEngine.ts line ~1343 (SHOT save-dropped -> LOOSE_BALL) reads `lastShotPath: null`
    - applyStartMovement is unchanged (no lastShotPath clearing added there — D-07)
    - gameEngine.rule11.test.ts asserts the scattered PASS state has lastShotPath === null
    - `pnpm exec vitest run src/__tests__/gameEngine.rule11.test.ts` exits 0
  </acceptance_criteria>
  <done>The loose-ball scatter and the two confirmed-bug SHOT branches clear lastShotPath; applyStartMovement is untouched; regression tests confirm no stale path enters PASS/MOVEMENT.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                                                                                                  |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| server state -> client render | lastShotPath is server-authoritative state broadcast to both clients; the client renders whatever path the server sends. No untrusted input crosses here — the fix is internal state hygiene |

## STRIDE Threat Register

| Threat ID | Category               | Component                 | Disposition | Mitigation Plan                                                                                                                                      |
| --------- | ---------------------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-11-06   | Information Disclosure | lastShotPath broadcast    | accept      | Shot path reveals only public board geometry already visible to both players; no sensitive data. Stale-path bug is a UX defect, not a security issue |
| T-11-07   | Tampering              | gameEngine return objects | mitigate    | Fix is confined to setting lastShotPath: null on specific branches; covered by regression tests asserting confirmed-correct branches are unchanged   |
| T-11-08   | Tampering              | npm/pnpm installs         | accept      | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A)                                                                            |

</threat_model>

<verification>
- `cd packages/server && pnpm exec tsc --noEmit` exits 0
- `pnpm --filter @counter-attack/server test` passes (full server suite, no regression)
- Manual trace: a snapshot/shot that deflects to LOOSE_BALL and scatters to PASS produces a PASS state with lastShotPath null; the client board shows no leftover shot-path tint entering MOVEMENT
</verification>

<success_criteria>

- RULE-03: the loose-ball scatter -> PASS transition and the two confirmed-bug SHOT branches set lastShotPath: null; no stale shot path persists into the next phase
- applyStartMovement is not modified (targeted fix per D-07)
- New regression tests pass; full server suite green; TypeScript clean
  </success_criteria>

<output>
Create `.planning/phases/11-rule-correctness/11-02-SUMMARY.md` when done
</output>
