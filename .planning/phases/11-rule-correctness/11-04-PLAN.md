---
phase: 11-rule-correctness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/__tests__/gameHandlers.rule11.test.ts
  - packages/server/src/__tests__/gameEngine.rule11.test.ts
  - packages/server/src/gameHandlers.ts
autonomous: true
requirements: [RULE-01, RULE-02, RULE-03, RULE-04, RULE-05]
gap_closure: true

must_haves:
  truths:
    - 'A header tie (equal attacker/defender duel scores) deterministically transitions the game to LOOSE_BALL with carrierId null instead of deadlocking in HEADER'
    - 'The distance-7 boundary assertion in gameEngine.rule11.test.ts fails the test if a hex >6 from the winning contestant is accepted (no longer vacuous)'
    - 'Both header handler finally-block comments cite Pitfall 5 (isProcessing released in finally), not Pitfall 2'
  artifacts:
    - path: 'packages/server/src/__tests__/gameHandlers.rule11.test.ts'
      provides: 'Deterministic header-tie LOOSE_BALL recovery test (CR-02 regression coverage)'
      contains: 'LOOSE_BALL'
    - path: 'packages/server/src/__tests__/gameEngine.rule11.test.ts'
      provides: 'Unconditional distance-7 INVALID_TARGET assertion'
      contains: 'expect(result7.ok).toBe(false)'
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'Corrected Pitfall 5 comments on both header handler finally blocks'
      contains: 'Pitfall 5'
  key_links:
    - from: 'gameHandlers.rule11.test.ts header-tie test'
      to: 'GAME_HEADER_CONTESTANT bothConfirmed null-winner branch'
      via: 'vi.mock of diceUtils rollDice forcing equal scores'
      pattern: "vi\\.mock"
---

<objective>
Close the three remaining verification/review items for Phase 11. The two production blockers
(CR-01 TESTING_PACE_OVERRIDE, CR-02 header tie deadlock) are already fixed and committed; this
plan does NOT re-implement them. It adds the deterministic regression test the verifier flagged
as "human verification required" (item A), and clears two info-level review items (B and C) that
were left unfixed.

Purpose: Turn the manually-classified header-tie recovery into an automated, deterministic test,
and remove a vacuous test assertion plus two misleading code comments so Phase 11 verifies clean.
Output: One new test in gameHandlers.rule11.test.ts, one corrected assertion in
gameEngine.rule11.test.ts, two corrected comments in gameHandlers.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-rule-correctness/11-VERIFICATION.md
@.planning/phases/11-rule-correctness/11-REVIEW.md
@.planning/phases/11-rule-correctness/11-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic header-tie LOOSE_BALL recovery test (gap A / CR-02 regression)</name>
  <files>packages/server/src/__tests__/gameHandlers.rule11.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameHandlers.rule11.test.ts (full file — reuse setupRoom, oncePromise, seedHeaderReadyForContestants, createClient harness; do NOT rewrite the harness)
    - packages/server/src/gameHandlers.ts lines 1908-1954 (the bothConfirmed null-winner branch that transitions to LOOSE_BALL — this is the code under test, already implemented; do NOT modify it)
    - packages/server/src/gameEngine.ts lines 2182-2242 (computeHeaderDuelWinner — returns null when attackerScore === defenderScore; raw score = computeCombinedScore(piece.heading, die, []) = heading + die)
    - packages/server/src/diceUtils.ts (rollDice is imported by gameHandlers.ts as `import { rollDice } from './diceUtils.js'`; this is the mock target)
    - packages/shared/src/scoreUtils.ts lines 27-36 (computeCombinedScore is deterministic: attribute + diceValue + clampedPenalty; empty penalties = 0)
  </read_first>
  <behavior>
    - Test: with one contestant per team having EQUAL heading and rollDice mocked to a constant, both raw scores tie → computeHeaderDuelWinner returns null → server transitions to LOOSE_BALL.
    - Assert: after both teams confirm contestants, the broadcast GAME_STATE has phase === 'LOOSE_BALL'.
    - Assert: state.ball.carrierId is null (loose ball has no carrier).
    - Assert: state.headerDuelWinner is null (cleared on tie) and phase is NOT 'HEADER' (no deadlock).
  </behavior>
  <action>
    Force a deterministic header-duel tie and assert the LOOSE_BALL recovery (the CR-02 fix already
    in gameHandlers.ts lines 1928-1940).

    Mocking rollDice: at the TOP of the test file (before the imports of buildServer etc. — vi.mock is
    hoisted), add `vi.mock('../diceUtils.js', () => ({ rollDice: () => 3 }))`. Import `vi` from vitest
    (the file already imports afterEach, beforeEach, describe, expect, it from vitest — add `vi`).
    This makes every duel die equal to 3, so two contestants with equal heading produce equal raw
    scores (heading + 3 each) → computeHeaderDuelWinner returns null. Note: mocking diceUtils does not
    affect the existing tests in this file because none of them assert on specific dice outcomes (they
    use `toContain(['home','away',null])`), so a constant die value keeps them green.

    Guaranteeing equal heading: add a new seeder OR extend the existing seedHeaderReadyForContestants
    usage. In the new test, after seeding with seedHeaderReadyForContestants(roomCode), fetch the room
    via getRoom, pick the home attacker piece and away defender piece (same selectors the seeder uses:
    `teamId === 'home' && role !== 'GK'` and `teamId === 'away' && role !== 'GK'`), and overwrite both
    pieces' `heading` to the SAME value (e.g. 3) by re-assigning room.gameState.pieces with a map that
    sets `heading: 3` on those two piece ids. This makes the tie structural: equal heading + equal die.

    Drive the handler: clientA (home) emits GAME_HEADER_CONTESTANT with [homeAttacker.id] and await one
    GAME_STATE; then clientB (away) emits GAME_HEADER_CONTESTANT with [awayDefender.id] and await the
    GAME_STATE that the bothConfirmed branch broadcasts. Assert on THAT second state: phase === 'LOOSE_BALL',
    ball.carrierId === null, headerDuelWinner == null. Add the test inside a new
    `describe('RULE-02: header tie → LOOSE_BALL recovery (CR-02)', ...)` block, or append an `it(...)`
    to the existing 'GAME_HEADER_CONTESTANT — both-confirmed auto-fires duel' describe.

    Do NOT modify gameHandlers.ts, gameEngine.ts, or computeHeaderDuelWinner — the fix is already
    committed. This task is test-only. Follow the existing mockSocket/mockIo/mockRoom-equivalent
    harness (real Socket.io server on port 0 via setupRoom); do not introduce a different harness.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server exec vitest run src/__tests__/gameHandlers.rule11.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test exists asserting phase === 'LOOSE_BALL' after a header tie.
    - New test asserts ball.carrierId === null and headerDuelWinner == null on the tie path.
    - vi.mock('../diceUtils.js', ...) is present and hoisted above other imports.
    - All pre-existing tests in gameHandlers.rule11.test.ts still pass (the file now has 10+ tests, all green).
    - No source files (gameHandlers.ts / gameEngine.ts) were modified by this task.
  </acceptance_criteria>
  <done>The header-tie recovery is covered by a deterministic automated test that fails if the game stays in HEADER instead of transitioning to LOOSE_BALL; full gameHandlers.rule11 suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix vacuous distance-7 assertion (gap B / IN-02)</name>
  <files>packages/server/src/__tests__/gameEngine.rule11.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.rule11.test.ts lines 313-329 (the 'accepts a hex exactly at distance 6 from winner position' test — the conditional at lines 320-324 wraps the distance-7 assertion so it is a no-op when result7.ok is false)
  </read_first>
  <action>
    In the test 'accepts a hex exactly at distance 6 from winner position' (~line 313), the distance-7
    check at lines 320-324 is currently:

    if (result7.ok) {
      expect(result7.ok).toBe(false);
    }

    This is vacuous: when result7.ok is false (the expected case), the inner expect is never reached
    and the test passes regardless. Replace the conditional block with an unconditional assertion that
    distance-7 fails with INVALID_TARGET:

    expect(result7.ok).toBe(false);
    if (!result7.ok) expect(result7.reason).toBe('INVALID_TARGET');

    Keep the surrounding distance-6 assertions (the `if (!result.ok) { expect(result.reason).not.toBe('DUEL_NOT_RESOLVED'); }`
    block at lines 326-328) unchanged. Do not alter applyResolveHeaderTarget or any other test.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server exec vitest run src/__tests__/gameEngine.rule11.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The `if (result7.ok) { expect(result7.ok).toBe(false); }` block no longer exists.
    - The test now contains an unconditional `expect(result7.ok).toBe(false)` followed by `if (!result7.ok) expect(result7.reason).toBe('INVALID_TARGET')`.
    - The full gameEngine.rule11.test.ts suite (20 tests) still passes.
  </acceptance_criteria>
  <done>Distance-7 boundary is unconditionally asserted to fail with INVALID_TARGET; the assertion is no longer vacuous and the engine test suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Fix Pitfall comment typo on both header handler finally blocks (gap C / IN-03)</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts line 1780 (GAME_HEADER_ACCURACY_ACK finally block — comment reads `// MUST be in finally — Pitfall 2`, should be Pitfall 5)
    - packages/server/src/gameHandlers.ts line 1952 (GAME_HEADER_CONTESTANT finally block — comment reads `// MUST be in finally — Pitfall 2`, should be Pitfall 5)
  </read_first>
  <action>
    Two finally-block comments incorrectly cite Pitfall 2 (the "never read socket.rooms" anti-pattern)
    when they should cite Pitfall 5 (isProcessing released in finally — never conditionally). Every
    OTHER finally block in the file already says Pitfall 5 (lines 253, 1132, 1332, 1388, 1452, 1495,
    1543, 1661, 1741, 1851).

    Change `room.isProcessing = false; // MUST be in finally — Pitfall 2` to
    `room.isProcessing = false; // MUST be in finally — Pitfall 5` at line 1780 (GAME_HEADER_ACCURACY_ACK
    handler) and at line 1952 (GAME_HEADER_CONTESTANT handler).

    Do NOT touch the legitimate Pitfall 2 references at lines 17 and 119 — those correctly describe the
    socket.rooms anti-pattern in doc comments. Only the two finally-block `isProcessing = false` lines change.
    This is a comment-only change; no behavior changes.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/server exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both `isProcessing = false` finally comments in the header handlers now read `Pitfall 5`.
    - No `room.isProcessing = false; // MUST be in finally — Pitfall 2` lines remain in the file.
    - The Pitfall 2 references at lines 17 and 119 (socket.rooms doc comments) are unchanged.
    - tsc --noEmit exits 0 (comment-only change compiles clean).
  </acceptance_criteria>
  <done>Both header handler finally blocks correctly cite Pitfall 5; no remaining isProcessing finally block misattributes Pitfall 2.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                 |
| ------------------------------------ | ------------------------------------------------------------------------------------------- |
| client→server GAME_HEADER_CONTESTANT | Untrusted contestant confirmations cross here; the tie path under test resolves server-side |

## STRIDE Threat Register

| Threat ID  | Category          | Component              | Disposition | Mitigation Plan                                                                                                                                              |
| ---------- | ----------------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-11-04-01 | Denial of Service | Header tie deadlock    | mitigate    | CR-02 fix (already committed) transitions to LOOSE_BALL on null winner; Task 1 adds a deterministic regression test so the deadlock cannot silently reappear |
| T-11-04-SC | Tampering         | npm/pip/cargo installs | accept      | No package installs in this plan; test-only + comment-only changes using existing vitest/socket.io-client deps                                               |

</threat_model>

<verification>
- Run the two affected server test files; both must be fully green:
  - `pnpm --filter @counter-attack/server exec vitest run src/__tests__/gameHandlers.rule11.test.ts`
  - `pnpm --filter @counter-attack/server exec vitest run src/__tests__/gameEngine.rule11.test.ts`
- Run the full server suite to confirm no regression from the diceUtils mock:
  - `pnpm --filter @counter-attack/server test`
- `pnpm --filter @counter-attack/server exec tsc --noEmit` exits 0.
</verification>

<success_criteria>

- Header tie → LOOSE_BALL recovery is covered by a deterministic automated test (no longer "human verification required").
- Distance-7 boundary assertion is unconditional and fails if a >6 hex is accepted.
- Both header handler finally blocks cite Pitfall 5.
- All server tests pass; tsc clean.
  </success_criteria>

<artifacts_this_phase_produces>

- New test (function): header-tie → LOOSE_BALL recovery `it(...)` in `packages/server/src/__tests__/gameHandlers.rule11.test.ts`, plus a hoisted `vi.mock('../diceUtils.js', ...)`.
- Corrected assertion: unconditional distance-7 INVALID_TARGET check in `packages/server/src/__tests__/gameEngine.rule11.test.ts`.
- Corrected comments: two `Pitfall 5` finally-block comments in `packages/server/src/gameHandlers.ts`.
- No new files; no new source behavior (CR-01 and CR-02 fixes were committed prior to this plan).
  </artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-rule-correctness/11-04-SUMMARY.md` when done
</output>
