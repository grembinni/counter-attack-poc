---
phase: 11-rule-correctness
plan: 03
type: execute
wave: 2
depends_on: ['11-01']
files_modified:
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/store/useGameStore.rule11.test.ts
autonomous: true
requirements: [RULE-01, RULE-04, RULE-05]
must_haves:
  truths:
    - 'On a High Pass, the client shows the accuracy roll result and an attacker-only Continue button before any contestant selection UI appears (gated on headerAccuracyRollPending)'
    - 'Clicking Continue emits GAME_HEADER_ACCURACY_ACK to the server'
    - 'During SNAP_DEFLECT, a deflecting piece that has used its 2-hex pace shows no selectable outline and no move highlights'
    - "After a shot deflects into Loose Ball and a normal Movement Phase begins, both teams' pieces are selectable in the correct 4-5-2 slot sequence"
  artifacts:
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'RULE-01 accuracy-roll acknowledgment gate at the top of the HEADER phase block'
      contains: 'headerAccuracyRollPending'
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'emitHeaderAccuracyAck action emitting GAME_HEADER_ACCURACY_ACK'
      contains: 'emitHeaderAccuracyAck'
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: 'snapDeflectPaceUsed subscription and pace guard on canSelectSnapDeflect (RULE-04); RULE-05 selectability fix'
      contains: 'snapDeflectPaceUsed'
    - path: 'packages/client/src/store/useGameStore.rule11.test.ts'
      provides: 'Client tests for RULE-04 pace suppression and RULE-05 post-deflect selectability'
  key_links:
    - from: 'packages/client/src/components/ActionPanel.tsx HEADER block'
      to: 'emitHeaderAccuracyAck'
      via: 'Continue button onClick'
      pattern: 'emitHeaderAccuracyAck'
    - from: 'packages/client/src/components/HexGrid.tsx canSelectSnapDeflect'
      to: 'snapDeflectPaceUsed'
      via: 'pace-exhaustion guard'
      pattern: "snapDeflectPaceUsed \\?\\? 0"
---

<objective>
Land the three client-side rule-correctness fixes. RULE-01: gate the header contestant selection UI
behind the accuracy-roll acknowledgment (show the roll result + an attacker-only Continue button that
emits GAME_HEADER_ACCURACY_ACK). RULE-04: suppress SNAP_DEFLECT move highlights and selectable outline
for a deflecting piece once it has used its 2-hex pace. RULE-05: diagnose and fix the bug where, after
a shot deflects into Loose Ball and a Movement Phase begins, no pieces are selectable for either team.

Purpose: These complete the user-visible side of the phase. RULE-01's server flag/event (plan 11-01)
needs the client gate to be observable; RULE-04 and RULE-05 are client-only highlight bugs from v1.0 UAT.
Output: ActionPanel accuracy gate + store emit action, HexGrid pace guard + RULE-05 selectability fix,
and client tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-rule-correctness/11-CONTEXT.md
@.planning/phases/11-rule-correctness/11-RESEARCH.md
@.planning/phases/11-rule-correctness/11-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by this plan (exclude from drift verification — intentionally new):

- `emitHeaderAccuracyAck` — store action on `useGameStore` (`packages/client/src/store/useGameStore.ts`)
- `snapDeflectPaceUsed` — new `useGameStore` selector subscription in HexGrid (the GameState field already exists; this is a new client subscription)
- `useGameStore.rule11.test.ts` — new client test file

Note: `ClientEvents.GAME_HEADER_ACCURACY_ACK`, `GameState.headerAccuracyRollPending`, and
`GameState.headerDuelWinner` are created by plan 11-01 (server) — they are consumed here, not created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RULE-01 client accuracy-roll gate (ActionPanel + store emit action)</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/store/useGameStore.ts</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (HEADER phase block at line 181; existing subscriptions: attackingTeam line 34, headerConfirmed line 40, gameError line 41, isActivePlayer line 61; the styles.panel / styles.phaseLabel / styles.ctaButton / styles.errorText class usage in adjacent phase blocks ~lines 101-170)
    - packages/client/src/store/useGameStore.ts (emitHeaderContestant action at line 279-281 — the emit pattern to copy; the action type declarations ~lines 84-144; how socket is obtained via get())
    - packages/shared/src/events.ts (ClientEvents.GAME_HEADER_ACCURACY_ACK — added by plan 11-01)
    - .planning/phases/11-rule-correctness/11-PATTERNS.md (ActionPanel.tsx RULE-01 section: gate placement, attacker-only vs waiting copy, emitHeaderAccuracyAck pattern)
  </read_first>
  <behavior>
    - Test (store): calling emitHeaderAccuracyAck() with a connected socket calls socket.emit with ClientEvents.GAME_HEADER_ACCURACY_ACK and no arguments; with no socket it is a no-op.
    - Behavior (UI): when phase==='HEADER' and gameState.headerAccuracyRollPending is true, the attacking team's panel shows the accuracy roll value and a Continue button; the non-attacking team's panel shows a "waiting for attacker" message with no button.
    - Behavior (UI): when headerAccuracyRollPending is false/null, the existing contestant selection / both-confirmed UI renders unchanged.
  </behavior>
  <action>
    In useGameStore.ts: add an `emitHeaderAccuracyAck: () => void` action. Declare it in the store's
    action type block (alongside emitHeaderContestant ~line 84) and implement it following the
    emitHeaderContestant pattern (line 279): obtain socket via get(), guard `if (!socket) return;`, then
    `socket.emit(ClientEvents.GAME_HEADER_ACCURACY_ACK)` (zero arguments).

    In ActionPanel.tsx: subscribe to the pending flag (add
    `const headerAccuracyRollPending = useGameStore((s) => s.gameState.headerAccuracyRollPending);`
    near the other gameState subscriptions ~line 40) and to the new action
    (`const emitHeaderAccuracyAck = useGameStore((s) => s.emitHeaderAccuracyAck);`). At the TOP of the
    `if (phase === 'HEADER')` block (line 181), before the existing myConfirmed/bothConfirmed logic,
    add an early-return gate: when `(headerAccuracyRollPending ?? false)` is true, read the roll value
    from `lastDiceRoll?.rolls[0]` and render — for the attacking team (isActivePlayer && myTeam ===
    attackingTeam) a panel with the roll result text and a `styles.ctaButton` Continue button whose
    onClick calls emitHeaderAccuracyAck; for the other team a `styles.phaseLabel` "waiting for
    attacker" panel with no button. Include the `{gameError && <span className={styles.errorText}>...}`
    line on the attacker panel, matching the adjacent phase blocks. Use the existing styles classes
    (styles.panel, styles.phaseLabel, styles.ctaButton, styles.errorText) — do not add new CSS.

  </action>
  <verify>
    <automated>cd packages/client && pnpm exec tsc --noEmit && pnpm exec vitest run src/components/ActionPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - useGameStore.ts contains `emitHeaderAccuracyAck` declared in the action types and implemented with `socket.emit(ClientEvents.GAME_HEADER_ACCURACY_ACK)`
    - ActionPanel.tsx subscribes to `headerAccuracyRollPending` and renders an attacker-only Continue button gated on it, before the contestant selection UI
    - The Continue button onClick calls emitHeaderAccuracyAck
    - When headerAccuracyRollPending is falsy the existing HEADER contestant UI is unchanged
    - `pnpm exec tsc --noEmit` exits 0 and ActionPanel.test.tsx passes
  </acceptance_criteria>
  <done>The accuracy roll result is shown with an attacker-only Continue button that emits the ack; contestant selection UI only appears after the flag clears. TypeScript clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RULE-04 SNAP_DEFLECT pace-exhaustion highlight suppression</name>
  <files>packages/client/src/components/HexGrid.tsx, packages/client/src/store/useGameStore.rule11.test.ts</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (subscription block ~lines 47-94, specifically snapDeflectMovedPieceId at line 92 and highPassPaceUsed at line 81 as the subscription pattern; canSelectSnapDeflect at lines 570-575; how canSelectSnapDeflect feeds the outline/selectable render ~lines 617-631)
    - packages/client/src/store/useGameStore.ts (selectPiece SNAP_DEFLECT branch ~line 408 which already computes `paceRemaining = 2 - (gameState.snapDeflectPaceUsed ?? 0)` and returns validMoveHexes: [] when exhausted — this is the existing correct guard the test asserts)
    - packages/shared/src/types.ts (snapDeflectPaceUsed field ~line 404)
    - .planning/phases/11-rule-correctness/11-PATTERNS.md (HexGrid RULE-04 section: subscription add + guard add; Pitfall 6 subscription)
  </read_first>
  <behavior>
    - Test (store): selectPiece on a deflecting piece during SNAP_DEFLECT with snapDeflectPaceUsed === 2 sets validMoveHexes to [] (asserts the existing exhaustion guard still holds and is the contract HexGrid mirrors).
    - Test (store): selectPiece on a deflecting piece with snapDeflectPaceUsed === 0 returns a non-empty validMoveHexes (sanity: suppression only at exhaustion).
    - Behavior (HexGrid): canSelectSnapDeflect evaluates false once snapDeflectPaceUsed >= 2, so the piece shows no selectable outline.
  </behavior>
  <action>
    In HexGrid.tsx, add a subscription for the pace field next to the existing snapDeflectMovedPieceId
    subscription (line 92):
    `const snapDeflectPaceUsed = useGameStore((s) => s.gameState.snapDeflectPaceUsed);`
    (it is NOT currently subscribed — Pitfall 6). Then add a final guard term to the canSelectSnapDeflect
    expression (lines 570-575): append `&& (snapDeflectPaceUsed ?? 0) < 2` so the piece is no longer
    selectable once pace is exhausted. Use the `?? 0` null-guard pattern (matches selectPiece line 408).
    Do not change the SNAP_DEFLECT branch in useGameStore.selectPiece — RESEARCH confirms it already
    returns validMoveHexes: [] when pace is exhausted; this task only aligns the HexGrid outline gate.

    Create useGameStore.rule11.test.ts with the two store-level selectPiece tests above. Build the
    GameState fixture in SNAP_DEFLECT phase with a defending piece adjacent so a non-exhausted selectPiece
    yields non-empty moves.

  </action>
  <verify>
    <automated>cd packages/client && pnpm exec tsc --noEmit && pnpm exec vitest run src/store/useGameStore.rule11.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - HexGrid.tsx subscribes `const snapDeflectPaceUsed = useGameStore((s) => s.gameState.snapDeflectPaceUsed);`
    - canSelectSnapDeflect expression ends with `&& (snapDeflectPaceUsed ?? 0) < 2`
    - useGameStore.rule11.test.ts asserts selectPiece returns validMoveHexes [] at snapDeflectPaceUsed === 2 and non-empty at 0
    - useGameStore.selectPiece SNAP_DEFLECT branch is unchanged
    - `pnpm exec tsc --noEmit` exits 0 and the rule11 client test passes
  </acceptance_criteria>
  <done>A pace-exhausted deflecting piece is neither selectable nor shows move highlights; tests confirm the exhaustion contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RULE-05 diagnose and fix post-deflect Movement Phase selectability</name>
  <files>packages/client/src/components/HexGrid.tsx, packages/client/src/store/useGameStore.rule11.test.ts</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (canSelect block lines 553-558; slotQuota/activatedCount/pieceAlreadyActivated/slotFull computation lines 546-558; isActivePlayer derivation ~line 98 (myTeam === activeTeam); subscriptions activeTeam line 52, attackingTeam line 53, movedPieceIds line 54, movementSlot line 55, paceUsedByPieceId line 56)
    - packages/client/src/store/useGameStore.ts (setGameState handler — confirm it replaces gameState wholesale and does not retain stale paceUsedByPieceId/movementSlot from a prior phase; selectPiece MOVEMENT branch)
    - packages/server/src/gameEngine.ts (applyStartMovement ~lines 187-225 — sets movementSlot ATTACKER_4, activeTeam = attackingTeam, resets paceUsedByPieceId {} and movedPieceIds []; SLOT_SEQUENCE ~line 47; applyEndTurn slot advance ATTACKER_4 -> DEFENDER_5 -> ATTACKER_2 toggling activeTeam) — read-only, to confirm the server contract per D-11
    - .planning/phases/11-rule-correctness/11-RESEARCH.md (RULE-05 Suspects A-E and the diagnosis-instrumentation guidance; Open Question 2)
    - .planning/phases/11-rule-correctness/11-PATTERNS.md (HexGrid RULE-05 canSelect section + diagnosis instrumentation pattern)
    - .planning/phases/11-rule-correctness/11-CONTEXT.md (D-10/D-11: client-side diagnosis, server FSM believed correct)
  </read_first>
  <behavior>
    - Test: starting from a GameState produced by the LOOSE_BALL -> PASS -> applyStartMovement chain (deflection origin), the client selectability for the active team's pieces in the ATTACKER_4 slot returns at least one selectable piece (the RULE-05 regression — previously zero).
    - Test: when the slot advances to DEFENDER_5 (activeTeam = defender), the defender's pieces become selectable and the attacker's do not.
    - Behavior: no piece is selectable for a player whose myTeam !== activeTeam in the current slot.
  </behavior>
  <action>
    Diagnose first, then fix. Add temporary instrumentation per the PATTERNS.md RULE-05 pattern: a
    NODE_ENV-guarded console.log printing movementSlot, activeTeam, attackingTeam, isActivePlayer,
    Object.keys(paceUsedByPieceId), movedPieceIds, slotQuota, activatedCount once per render (guard
    `piece.id === pieces[0]?.id`). Drive the post-deflect MOVEMENT state (build the fixture via the
    server chain or a hand-constructed equivalent state) and identify which canSelect condition is false
    for all pieces. Likely culprits per RESEARCH: (Suspect E) slotFull true because paceUsedByPieceId
    carries stale entries from SNAP_DEFLECT, or (Suspect A) isActivePlayer false because activeTeam did
    not update after the team-changing scatter.

    Apply the minimal fix at the identified condition:
    - If stale client state is the cause (setGameState not fully replacing gameState, leaving stale
      paceUsedByPieceId/movementSlot), fix setGameState to replace those fields from the incoming
      snapshot rather than merging — the server already resets them in applyStartMovement.
    - If isActivePlayer / activeTeam logic is the cause, correct the derivation so the active team for
      the current movementSlot is selectable on the right client.
    Remove the temporary console.log instrumentation before finalizing (acceptance criteria forbids it
    remaining). If diagnosis proves the root cause is server-side (movementSlot arriving null), STOP and
    record the finding in the SUMMARY for a follow-up server plan rather than editing the server here
    (this plan owns only client files; gameEngine is read-only context).

    Add the two/three RULE-05 selectability tests above to useGameStore.rule11.test.ts.

  </action>
  <verify>
    <automated>cd packages/client && pnpm exec tsc --noEmit && pnpm exec vitest run src/store/useGameStore.rule11.test.ts && pnpm exec vitest run src/components/ActionPanel.test.tsx src/store/useGameStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A post-deflect MOVEMENT GameState yields at least one selectable piece for the active team in ATTACKER_4 (asserted by test) — the zero-selectable bug is resolved
    - When the slot is DEFENDER_5, the defender's pieces are selectable and the attacker's are not
    - No temporary console.log / diagnosis instrumentation remains in HexGrid.tsx (grep for '[RULE-05 DIAGNOSIS]' returns no match)
    - The SUMMARY records the confirmed root cause (which canSelect condition was false and why)
    - `pnpm exec tsc --noEmit` exits 0; rule11 client tests and existing useGameStore.test.ts pass
  </acceptance_criteria>
  <done>After a deflection into a Movement Phase, both teams can select their pieces in the correct 4-5-2 slot sequence; the root cause is identified and fixed (or escalated to a server follow-up if server-side), with no leftover instrumentation and passing tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                                                                                            |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client UI -> server event       | The Continue button emits GAME_HEADER_ACCURACY_ACK; the server (plan 11-01) re-validates phase and team ownership. The client gate is convenience only — authority remains server-side |
| server snapshot -> client store | setGameState ingests the authoritative GameState broadcast; client render derives selectability from it                                                                                |

## STRIDE Threat Register

| Threat ID | Category               | Component                       | Disposition | Mitigation Plan                                                                                                                                                   |
| --------- | ---------------------- | ------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-11-09   | Spoofing               | GAME_HEADER_ACCURACY_ACK emit   | mitigate    | Client gate is advisory; server handler (11-01) enforces controlsAttackingTeam. A spoofed ack from the wrong client is rejected server-side                       |
| T-11-10   | Tampering              | client selectability derivation | accept      | canSelect only gates which pieces a player may click; the server re-validates every GAME_MOVE. A tampered client cannot move an illegal piece — server rejects it |
| T-11-11   | Information Disclosure | RULE-05 diagnosis console.log   | mitigate    | Instrumentation is NODE_ENV-guarded and removed before finalize (acceptance criteria greps for its absence); no state logged in production builds                 |
| T-11-12   | Tampering              | npm/pnpm installs               | accept      | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A)                                                                                         |

</threat_model>

<verification>
- `cd packages/client && pnpm exec tsc --noEmit` exits 0
- `pnpm --filter @counter-attack/client test` passes (ActionPanel, useGameStore, rule11 suites)
- No `[RULE-05 DIAGNOSIS]` string remains in HexGrid.tsx
- Manual trace: High Pass shows accuracy roll + Continue (attacker only) before contestants; a pace-2 deflecting piece is not selectable; post-deflect Movement Phase shows selectable pieces for both teams across slots
</verification>

<success_criteria>

- RULE-01 (client): contestant UI is gated behind the accuracy-roll acknowledgment; Continue emits GAME_HEADER_ACCURACY_ACK
- RULE-04: SNAP_DEFLECT highlights and selectable outline suppressed when snapDeflectPaceUsed >= 2
- RULE-05: post-deflect Movement Phase makes both teams' pieces selectable in the 4-5-2 sequence; root cause documented; no instrumentation left behind
- All client tests pass; TypeScript clean
  </success_criteria>

<output>
Create `.planning/phases/11-rule-correctness/11-03-SUMMARY.md` when done
</output>
