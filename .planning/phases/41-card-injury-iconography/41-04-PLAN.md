---
phase: 41-card-injury-iconography
plan: 04
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - packages/client/src/components/LineupAssignmentScreen.tsx
  - packages/client/src/components/LineupAssignmentScreen.module.css
  - packages/client/src/components/LineupAssignmentScreen.test.tsx
autonomous: true
requirements: [ICON-01, ICON-02]

must_haves:
  truths:
    - 'The mid-match roster/lineup card shows the same shape+colour glyph as the pitch token and scoreboard instead of YELLOW/RED/INJ text (D-01)'
    - 'The roster card glyph sits immediately after the jersey number in the flag-role-number row, ahead of any trailing status text badge (D-02)'
    - 'A booked-and-injured roster card shows both glyphs side by side (D-04)'
    - 'The last copy of the card-colour ternary outside the shared module is gone (ICON-01)'
    - 'Pregame and draft-mode lineup cards render exactly as before — they pass no card/injury data, so no glyph appears'
  artifacts:
    - path: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      provides: 'Roster/lineup card badge rendered via the standalone CardInjuryBadge, colour derived via the shared cardColorFor'
      contains: 'CardInjuryBadge'
    - path: 'packages/client/src/components/LineupAssignmentScreen.module.css'
      provides: 'Dead cardChip/injuryChip rules removed, stale cross-file comments corrected'
    - path: 'packages/client/src/components/LineupAssignmentScreen.test.tsx'
      provides: 'Roster-card assertions rewritten from text chips to glyphs, plus D-02 anchor and D-04 side-by-side coverage'
  key_links:
    - from: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      to: 'packages/client/src/components/CardInjuryBadge.tsx'
      via: 'import of CardInjuryBadge + cardColorFor + CardColor'
      pattern: "from './CardInjuryBadge.js'"
    - from: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      to: 'LineupStatCard cardMeta row'
      via: 'badge rendered immediately after styles.cardNum'
      pattern: 'styles.cardNum'
---

<objective>
Migrate the third of the four surfaces onto the shared badge: the roster/lineup card
(`LineupStatCard` inside `LineupAssignmentScreen.tsx`). It loses its `YELLOW`/`RED`/`INJ`/`INJ ×2`
text chips in favour of the glyph (D-01), and it loses the last hand-written copy of the card-colour
ternary (ICON-01).

Purpose: ICON-01's checkable definition of "single shared component" is that
`redCarded === true ?` appears nowhere in `packages/client/src/components/*.tsx` except
`CardInjuryBadge.tsx`. `LineupAssignmentScreen.tsx:454-455` is the last remaining copy after
plan 41-03. This plan removes it.
Output: a migrated `LineupStatCard`, dead CSS removed, and the roster-card test assertions rewritten
from text to glyph.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-card-injury-iconography/41-CONTEXT.md
@.planning/phases/41-card-injury-iconography/41-UI-SPEC.md
@.planning/phases/41-card-injury-iconography/41-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by Phase 41 (none of these exist in the codebase before this phase — do not
treat them as drift):

| Symbol                                                           | Kind                                              | Location                                       |
| ---------------------------------------------------------------- | ------------------------------------------------- | ---------------------------------------------- |
| `CardInjuryBadge.tsx` / `CardInjuryBadge.test.tsx`               | new files                                         | `packages/client/src/components/` (plan 41-01) |
| `CardColor`, `BenchCardStatus`                                   | exported type aliases                             | `CardInjuryBadge.tsx` (plan 41-01)             |
| `cardColorFor`, `cardColorForBenchEntry`, `cardInjuryLabel`      | exported functions                                | `CardInjuryBadge.tsx` (plan 41-01)             |
| `CardInjuryBadgeGroup`, `CardInjuryBadge`                        | exported React components                         | `CardInjuryBadge.tsx` (plan 41-01)             |
| `data-testid="card-injury-badge"`                                | new test hook                                     | standalone `<svg>` wrapper (plan 41-01)        |
| `BenchEntry.yellowCards`, `BenchEntry.injuryCount`               | new optional fields                               | `packages/shared/src/types.ts` (plan 41-02)    |
| `StatCardProps.cardColor` retyped to `CardColor`                 | narrowed existing prop type (not a new prop)      | `LineupAssignmentScreen.tsx` (this plan)       |
| `DraftCardBodyProps.cardColor`, `DraftCardBodyProps.injuryCount` | new optional props                                | `DraftPackCarousel.tsx` (plan 41-05)           |
| `BenchCarouselProps.benchCardStatus`                             | new optional prop                                 | `BenchCarousel.tsx` (plan 41-05)               |
| `CardInjuryBadge.audit.test.ts`                                  | new file (source-scanning ICON-01 invariant spec) | `packages/client/src/components/` (plan 41-06) |
| `CardInjuryBadge.crossSurface.test.tsx`                          | new file (four-surface Success-Criterion-3 spec)  | `packages/client/src/components/` (plan 41-06) |

<tasks>

<task type="auto">
  <name>Task 1: Replace the roster card's text chips with the shared glyph</name>
  <files>packages/client/src/components/LineupAssignmentScreen.tsx, packages/client/src/components/LineupAssignmentScreen.module.css</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx (lines 25-40 — the existing relative-import block; lines 145-158 — `StatCardProps.cardColor`/`injuryCount` prop declarations; lines 232-260 — `LineupStatCard`'s `cardHeader`/`cardMeta` row and the two chip blocks at 249-258; lines 448-470 — `renderMidmatchColumn`'s local `cardColor` ternary at 454-455 and the `LineupStatCard` call site)
    - packages/client/src/components/CardInjuryBadge.tsx (the shared module from plan 41-01 — exact prop names and defaults of `CardInjuryBadge`, plus the `cardColorFor` / `CardColor` signatures)
    - packages/client/src/components/PlayerStatsPanel.tsx (as migrated by plan 41-03 — this task is the same swap on the sibling surface the source comment at LineupAssignmentScreen.tsx:246-248 calls "identical classes/copy")
    - packages/client/src/components/LineupAssignmentScreen.module.css (lines 217-245 — `.cardMeta` `gap: 8px` and `.cardNum`/`.lockedBadge`; lines 483-494 — the `.redCardBadge` comment that cross-references a class plan 41-03 deletes; lines 503-542 — the `.cardChip`/`.injuryChip` block and the `.subCounterChip` comment that references `.injuryChip`)
    - packages/client/src/components/ActionPanel.tsx (line 1 — the in-codebase precedent for an inline `type` modifier inside a value import, which `verbatimModuleSyntax` permits)
  </read_first>
  <action>
    Swap the roster/lineup card's text chips for the shared glyph and delete the last copy of the
    card-colour ternary.

    In `LineupAssignmentScreen.tsx`:

    1. Add `import { CardInjuryBadge, cardColorFor, type CardColor } from './CardInjuryBadge.js';`
       to the existing relative-import block (after the `import { NationFlag } from './NationFlag.js';`
       line). The inline `type` modifier is permitted under `verbatimModuleSyntax` and already has
       in-codebase precedent (`ActionPanel.tsx:1`).

    2. Retype the `StatCardProps` prop at line 151 from
       `cardColor?: 'yellow' | 'red' | null;` to `cardColor?: CardColor;`. Keep the
       `injuryCount?: number;` declaration unchanged. Update both doc comments to say
       "Phase 41 (ICON-01): derived by the shared `cardColorFor`; the local Phase 40 ternary is gone."
       Do NOT make either prop required — pregame/draft call sites deliberately pass neither.

    3. In `LineupStatCard`'s `cardMeta` row, delete BOTH chip blocks (the `stats-card-chip` span at
       lines 249-253 and the `stats-injury-chip` span at lines 254-258) together with the stale
       "Phase 40 (D-05)" comment above them, and render in their place, unconditionally, immediately
       after `<span className={styles.cardNum}>#{displayNumber}</span>`:

       `<CardInjuryBadge cardColor={cardColor ?? null} injuryCount={injuryCount ?? 0} size={16} />`

       Placement is exact and load-bearing (D-02 / ICON-02): the badge goes BEFORE the
       `{isGK && !allowGKDrag && <span className={styles.lockedBadge}>LOCK</span>}` element, not
       after it. `LOCK` is a trailing status text badge in the same category as the bench card's
       `RED CARD`/`OUT` badge, and the UI-SPEC locks the render order for every name-row surface as
       `flag · role · #number · [glyph] · [status text badge]`. This moves the glyph one slot earlier
       than the old chips sat; that is deliberate. It is also invisible in practice — `isGK` is
       `!isMidmatch && slotIndex === 0` so `LOCK` only ever renders in pregame, where `cardColor`
       and `injuryCount` are always undefined — so the two never co-render. The ordering is a
       consistency contract, not a visible change.

       No conditional wrapper and no new class: `CardInjuryBadge` already returns `null` when there
       is nothing to draw, and `.cardMeta`'s existing `gap: 8px` supplies the spacing that the old
       chips added via `margin-left: 8px`.

       `size={16}` is the UI-SPEC default. Do not use `20` here: this row is built around 14px icons
       (`NationFlag size={14}`, 12px `.cardRole`/`.cardNum` text), unlike `PlayerStatsPanel`'s 20px
       row. At `size={16}` the glyph's drawn extent is `r * 2 = size * 0.8 = 12.8px` tall, which
       sits inside the 14px flag's optical height rather than towering over it.

    4. In `renderMidmatchColumn` (lines 453-456), delete the local `cardColor` declaration
       (`piece.redCarded === true ? 'red' : (piece.yellowCards ?? 0) > 0 ? 'yellow' : null`) and pass
       `cardColor={cardColorFor(piece)}` directly at the `LineupStatCard` call site (line 467).
       Keep `injuryCount={piece.injuryCount ?? 0}` exactly as it is. Keep
       `const isBlocked = piece.redCarded === true;` — that is a substitution-eligibility guard
       (SUB-06), NOT a glyph derivation, and it must not be routed through `cardColorFor`. It also
       does not contain the audited `redCarded === true ?` ternary substring.

    In `LineupAssignmentScreen.module.css`:

    5. Delete the whole dead block at lines 503-530: the `/* D-05: card/injury chips ... */` comment,
       the `.cardChip, .injuryChip` rule, `.cardChip[data-card='yellow']`, `.cardChip[data-card='red']`,
       and `.injuryChip`. Leave `.cardMeta`, `.cardRole`, `.cardNum`, `.lockedBadge`, `.outBadge`,
       `.redCardBadge`, `.cardUnavailable`, `.subCounterChip*` and everything else untouched.

    6. Fix the two now-stale comments that cross-reference deleted classes (both would otherwise
       point at rules that no longer exist anywhere in the repo after plan 41-03 + this task):
       - the `.redCardBadge` comment (lines 483-484) currently reads "...red-card colour language
         matching PlayerStatsPanel.module.css's .cardChip[data-card='red']" — replace that clause
         with "...red-card colour language via `var(--color-card-red)`, the same token the shared
         CardInjuryBadge uses (Phase 41)."
       - the `.subCounterChip` comment (line 532) currently reads "SUB-06: sub-counter chip — same
         pill geometry as .injuryChip." — replace with "SUB-06: sub-counter chip — 11px uppercase
         pill (the geometry the former .injuryChip shared, before Phase 41 replaced those chips with
         the CardInjuryBadge glyph)."

    Do not touch the draft-mode branch, `benchCards`, `resolveTieredCard`, the drag/drop handlers,
    `TIER_CARD_CLASS` composition, or the `BenchCarousel` call sites — the bench surface is plan
    41-05's scope.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client typecheck &amp;&amp; pnpm stylelint &amp;&amp; pnpm exec eslint packages/client/src/components/LineupAssignmentScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "redCarded === true ?" packages/client/src/components/LineupAssignmentScreen.tsx` is 0.
    - `grep -rn "redCarded === true ? 'red'" packages/client/src/components --include="*.tsx" | grep -v "CardInjuryBadge.tsx"` returns zero lines — the `41-UI-SPEC.md` ICON-01 audit now passes across the whole component tree.
    - Needle scope note (do NOT widen): the gate above is narrowed to the glyph-COLOUR derivation form (`? 'red'`). `41-UI-SPEC.md` writes it as the bare substring `redCarded === true ?`, which is over-broad — `DraftPackCarousel.tsx:115` legitimately keeps `{redCarded === true ? (` to choose between the `RED CARD` and `OUT` availability text badges (Phase 40 SUB-07/D-13, a different question, untouched by this phase). Plan 41-06 encodes that distinction as a permanent spec with an explicit allowlist. Do not "fix" the DraftPackCarousel ternary to satisfy the literal wording of the spec gate.
    - `grep -cE "stats-card-chip|stats-injury-chip" packages/client/src/components/LineupAssignmentScreen.tsx` is 0.
    - `grep -cE "'INJ'|INJ ×2|INJ x2" packages/client/src/components/LineupAssignmentScreen.tsx` is 0.
    - `grep -cE "^\.cardChip|^\.injuryChip|\.cardChip\[" packages/client/src/components/LineupAssignmentScreen.module.css` is 0.
    - `grep -vE "^\s*(/\*|\*)" packages/client/src/components/LineupAssignmentScreen.module.css | grep -cE "cardChip|injuryChip"` is 0 (comment lines excluded, so the check cannot be satisfied or broken by prose).
    - `grep -c "CardInjuryBadge" packages/client/src/components/LineupAssignmentScreen.tsx` is at least 2 (import + usage), and the usage passes `size={16}`.
    - `grep -c "cardColorFor(piece)" packages/client/src/components/LineupAssignmentScreen.tsx` is 1.
    - In `LineupStatCard`'s `cardMeta`, `<CardInjuryBadge` appears on a line between the `styles.cardNum` line and the `styles.lockedBadge` line (verify by reading the block; D-02 order).
    - `pnpm --filter @counter-attack/client typecheck` exits 0.
    - `pnpm stylelint` exits 0.
    - `pnpm exec eslint packages/client/src/components/LineupAssignmentScreen.tsx` exits 0.
  </acceptance_criteria>
  <done>The roster card draws the shared glyph immediately after the jersey number, its dead chip CSS and stale comments are gone, and no card-colour ternary copy remains anywhere outside the shared module.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the roster-card tests from text chips to glyphs</name>
  <files>packages/client/src/components/LineupAssignmentScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.test.tsx (lines 440-585 — `makePiece`, the `HOME_TEAM_PIECES` fixture with `DefRedCarded`/`MidYellow`/`MidInjuredOnce`/`FwdInjuredTwice`, the `BENCH_MIXED` fixture, the `MidmatchOverrides` type, and the `renderMidmatch` helper; lines 714-748 — the four chip assertions being rewritten)
    - packages/client/src/components/PlayerStatsPanel.test.tsx (as rewritten by plan 41-03 — reuse its glyph assertion vocabulary verbatim so the two sibling surfaces are asserted identically)
    - packages/client/src/components/CardInjuryBadge.tsx (the `data-testid` and `aria-label` contract this spec asserts against)
  </read_first>
  <action>
    Rewrite the roster-card assertions in the
    `LineupAssignmentScreen — mid-match substitution mode (SUB-02/03/06/07, D-12/D-13)` describe block
    against the glyph contract. The three `.textContent`/`stats-*-chip` tests at lines 721-747 assert a
    design D-01 deliberately removes, so they cannot be left passing unchanged.

    Keep `renderMidmatch`, `makePiece`, `HOME_TEAM_PIECES` and every other existing test in the file
    untouched — this task edits only the three chip tests and adds two new ones.

    Replacements (keep the same fixture pieces and the same `.closest('[class*="statCard"]')` lookup
    style already used in the file):
    - `renders a yellow card chip...` -> `ICON-02: renders a yellow card glyph for an on-pitch piece
      with yellowCards 1` — assert `within(card).getByTestId('piece-card-badge').getAttribute('data-card')`
      is `'yellow'` for `Home MidYellow`.
    - `renders a red chip...` -> same shape for `Home DefRedCarded`, expecting `'red'`.
    - `renders INJ for injuryCount 1 and INJ ×2 for injuryCount 2` -> `ICON-02: renders exactly one
      injury glyph for injuryCount 1 and for injuryCount 2, with the count preserved only in the
      accessible label`. For `Home MidInjuredOnce`: exactly one `[data-testid="piece-injury-badge"]`
      and the wrapping `[data-testid="card-injury-badge"]` has `aria-label` `'Injured'`. For
      `Home FwdInjuredTwice`: still exactly one injury glyph, `aria-label` `'Injured ×2'` (U+00D7
      multiplication sign, matching the UI-SPEC Copywriting Contract). Assert via
      `within(card).getByTestId('card-injury-badge').getAttribute('aria-label')`.

    Add two new tests to the same describe block:
    - `ICON-02/D-02: the roster-card glyph renders immediately after the jersey number` — render
      `renderMidmatch()`, take the `Home MidYellow` card, find its `[class*="cardNum"]` element, and
      assert `cardNum.nextElementSibling` has `data-testid` `'card-injury-badge'`. This pins the
      shared anchor point that makes ICON-02's "identical relative position" checkable.
    - `ICON-02/D-04: a booked AND injured roster card shows both glyphs side by side` — render a
      single-piece roster via
      `renderMidmatch({ midmatchPieces: [makePiece({ id: 'home-6', playerId: 'p007', role: 'MID', number: 7, firstName: 'Home', lastName: 'MidBookedInjured', yellowCards: 1, injuryCount: 1 })] })`
      (`home-6` parses to slot index 6, which the component groups into the MID column — do not
      invent an id shape the `/-(\d+)$/` parser cannot read). Assert both
      `[data-testid="piece-card-badge"]` and `[data-testid="piece-injury-badge"]` are present, that
      the card rect's `x + width` is less than or equal to the injury cross's first child rect's `x`
      (the D-04 no-overlap rule, read as numbers off the SVG attributes exactly as
      `PieceOverlay.test.tsx` does), and that the wrapper `aria-label` is `'Yellow card, Injured'`.

    Also add one non-regression test outside that block (or at the end of it), guarding the pregame
    path: `ICON-02: a pregame lineup card renders no badge markup (no card/injury data is passed)` —
    render the existing Standard-mode/pregame harness already used by the
    `LineupAssignmentScreen — Standard-mode non-regression` describe block and assert
    `container.querySelector('[data-testid="card-injury-badge"]')` is `null`.

    Note for the runner: if vitest reports a worker crash on Windows, re-run with `--pool=forks`
    (known environment flake, not a test failure).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- LineupAssignmentScreen</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- LineupAssignmentScreen` exits 0 with 0 failures.
    - `grep -cE "stats-card-chip|stats-injury-chip" packages/client/src/components/LineupAssignmentScreen.test.tsx` is 0.
    - `grep -cE "'INJ'|INJ ×2|INJ x2" packages/client/src/components/LineupAssignmentScreen.test.tsx` is 0.
    - `grep -c "ICON-02" packages/client/src/components/LineupAssignmentScreen.test.tsx` is at least 5.
    - The suite contains an assertion that the `cardNum` element's `nextElementSibling` carries `data-testid="card-injury-badge"` (D-02 anchor).
    - The suite contains an assertion that the booked-and-injured card's card rect right edge is `<=` the injury cross's left edge (D-04 no-overlap).
    - The suite contains an assertion that a pregame lineup card renders no `card-injury-badge`.
    - `pnpm --filter @counter-attack/client test` (full client suite) exits 0 — no regression in `PieceOverlay`, `PlayerStatsPanel`, `BenchCarousel`, `DraftPackCarousel`, `HexGrid` or `GameBoard` specs.
    - `pnpm --filter @counter-attack/client typecheck` exits 0.
  </acceptance_criteria>
  <done>The roster-card spec asserts glyphs, the D-02 anchor, and the D-04 side-by-side layout, with pregame non-regression covered and the whole client suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                                                                               |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| server -> client (`GameState` broadcast) | Pre-existing. `LineupAssignmentScreen` already reads `midmatchPieces[].redCarded/yellowCards/injuryCount` from the broadcast snapshot; this plan changes only how those values are drawn. |

## STRIDE Threat Register

| Threat ID | Category                 | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                              |
| --------- | ------------------------ | ----------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-41-08   | Information Disclosure   | `LineupAssignmentScreen.tsx` (`LineupStatCard`) | accept      | Identical data to today, redrawn. The roster card already displayed card/injury status as text chips for every on-pitch piece of the viewing player's own team; no field becomes newly visible, and the surface's own-team scoping is unchanged.                                                                                                                                                             |
| T-41-09   | Tampering                | migrated `LineupStatCard` render path           | accept      | Presentational swap only: no store write, no socket emit, no `dangerouslySetInnerHTML`. The removed chip rendered `cardColor.toUpperCase()` as a text node; the replacement renders a React-escaped `aria-label` built from an enum-ish `CardColor` plus a number. The `isBlocked` substitution guard (SUB-06) is deliberately left untouched, so no drag/drop or substitution-eligibility decision changes. |
| T-41-SC   | Tampering (supply chain) | package installs                                | n/a         | Zero packages installed this plan (`41-RESEARCH.md`: Package Legitimacy Audit not applicable). No `[ASSUMED]`/`[SUS]` checkpoint required.                                                                                                                                                                                                                                                                   |

**Phase-level statement (applies to every Phase 41 plan):** this phase introduces NO new attack
surface — no new user-controllable input, no new server endpoint or socket event, no auth/session
change, no new network or trust boundary. This plan is pure client rendering consolidation. Per
`41-RESEARCH.md` Security Domain, ASVS V2/V3/V4/V5/V6 are non-applicable at L1 for this scope. This
is the expected, complete threat model for this phase, not an omission.
</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test -- LineupAssignmentScreen` green.
- `pnpm --filter @counter-attack/client test` (full client suite) green.
- `pnpm --filter @counter-attack/client typecheck` green.
- `pnpm stylelint` green.
- `grep -rn "redCarded === true ? 'red'" packages/client/src/components --include="*.tsx" | grep -v CardInjuryBadge.tsx` returns nothing — the `41-UI-SPEC.md` "Post-implementation verification" gate for ICON-01 now passes (needle narrowed to the glyph-colour derivation form; see the scope note in Task 1's acceptance criteria).
- `git diff packages/client/src/components/LineupAssignmentScreen.tsx` shows no change to the draft-mode branch or any drag/drop handler.
</verification>

<success_criteria>

- Roster/lineup card: shape+colour glyph immediately after the jersey number and ahead of `LOCK`
  (D-01/D-02), both glyphs side by side when booked and injured (D-04), no `YELLOW`/`RED`/`INJ` text.
- The last hand-written card-colour ternary outside `CardInjuryBadge.tsx` is deleted (ICON-01).
- Pregame and draft-mode lineup cards are byte-identical in behaviour — they pass no card/injury data
  and render no badge markup.
  </success_criteria>

<output>
Create `.planning/phases/41-card-injury-iconography/41-04-SUMMARY.md` when done
</output>
