---
phase: 41-card-injury-iconography
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/CardInjuryBadge.tsx
  - packages/client/src/components/CardInjuryBadge.test.tsx
autonomous: true
requirements: [ICON-01, ICON-02]

must_haves:
  truths:
    - 'One module owns the card/injury glyph shape, colour, and derivation for every player-showing surface (ICON-01)'
    - "cardColorFor returns 'red' for a red-carded piece, 'yellow' for a booked-but-not-sent-off piece, null for a clean piece"
    - "cardColorForBenchEntry returns 'red' for a redCarded bench entry, 'yellow' for a bench entry carrying a yellow card, null otherwise"
    - 'The standalone badge renders card and injury glyphs side by side with no overlap (D-04)'
    - 'A clean player (no card, no injury) renders no badge markup at all'
    - 'Every rendered badge carries a words-based accessible label (Yellow card / Red card / Injured / Injured x2)'
  artifacts:
    - path: 'packages/client/src/components/CardInjuryBadge.tsx'
      provides: 'Shared glyph module: CardColor, cardColorFor, cardColorForBenchEntry, cardInjuryLabel, CardInjuryBadgeGroup, CardInjuryBadge, BenchCardStatus'
      exports:
        [
          'CardColor',
          'BenchCardStatus',
          'cardColorFor',
          'cardColorForBenchEntry',
          'cardInjuryLabel',
          'CardInjuryBadgeGroup',
          'CardInjuryBadge',
        ]
      min_lines: 90
    - path: 'packages/client/src/components/CardInjuryBadge.test.tsx'
      provides: 'Unit spec for derivations, glyph geometry, side-by-side layout, accessible labels'
      min_lines: 100
  key_links:
    - from: 'packages/client/src/components/CardInjuryBadge.tsx'
      to: 'packages/client/src/styles/tokens.css'
      via: 'CSS custom properties for every fill/stroke (no hex literals)'
      pattern: "var\\(--color-card-(red|yellow|badge-border)\\)"
---

<objective>
Create the single shared card/injury glyph module that ICON-01 requires: one file owning the
colored-rectangle card glyph, the white-cross injury glyph, and the derivation functions that
decide which glyph to draw. Nothing consumes it yet — plans 41-03/41-04/41-05 migrate the four
surfaces onto it.

Purpose: today the same glyph derivation ternary is hand-written in three components and the
bench has no glyph at all. This module becomes the one place that logic lives (D-01).
Output: `packages/client/src/components/CardInjuryBadge.tsx` + its unit spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-card-injury-iconography/41-CONTEXT.md
@.planning/phases/41-card-injury-iconography/41-UI-SPEC.md
@.planning/phases/41-card-injury-iconography/41-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by Phase 41 (none of these exist in the codebase before this phase — do not
treat them as drift):

| Symbol                               | Kind                     | Location                                                            |
| ------------------------------------ | ------------------------ | ------------------------------------------------------------------- |
| `CardInjuryBadge.tsx`                | new file                 | `packages/client/src/components/CardInjuryBadge.tsx`                |
| `CardInjuryBadge.test.tsx`           | new file                 | `packages/client/src/components/CardInjuryBadge.test.tsx`           |
| `CardColor`                          | exported type alias      | `CardInjuryBadge.tsx`                                               |
| `BenchCardStatus`                    | exported type alias      | `CardInjuryBadge.tsx`                                               |
| `cardColorFor`                       | exported function        | `CardInjuryBadge.tsx`                                               |
| `cardColorForBenchEntry`             | exported function        | `CardInjuryBadge.tsx`                                               |
| `cardInjuryLabel`                    | exported function        | `CardInjuryBadge.tsx`                                               |
| `CardInjuryBadgeGroup`               | exported React component | `CardInjuryBadge.tsx`                                               |
| `CardInjuryBadge`                    | exported React component | `CardInjuryBadge.tsx`                                               |
| `data-testid="card-injury-badge"`    | new test hook            | standalone `<svg>` wrapper                                          |
| `BenchEntry.yellowCards`             | new optional field       | `packages/shared/src/types.ts` (plan 41-02)                         |
| `BenchEntry.injuryCount`             | new optional field       | `packages/shared/src/types.ts` (plan 41-02)                         |
| `DraftCardBodyProps.cardColor`       | new optional prop        | `packages/client/src/components/DraftPackCarousel.tsx` (plan 41-05) |
| `DraftCardBodyProps.injuryCount`     | new optional prop        | `packages/client/src/components/DraftPackCarousel.tsx` (plan 41-05) |
| `BenchCarouselProps.benchCardStatus` | new optional prop        | `packages/client/src/components/BenchCarousel.tsx` (plan 41-05)     |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared CardInjuryBadge module</name>
  <files>packages/client/src/components/CardInjuryBadge.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.tsx (lines 212-292 — the badge IIFE whose geometry is the verbatim source of truth per D-01; note `dotOffsetX/dotOffsetY`, `PIECE_RADIUS * 0.59`, and the existing `data-testid`/`data-card` attributes)
    - packages/client/src/components/PlayerStatsPanel.tsx (lines 36-106 — `MiniTokenBadge`, the shipped self-contained-`<svg>`-in-DOM pattern the standalone export mirrors)
    - packages/shared/src/types.ts (lines 14-117 — `PlayerPiece.redCarded/yellowCards/injuryCount` optionality and `BenchEntry`/`BenchEntryStatus`)
    - packages/client/src/styles/tokens.css (lines 30-40 and 125-142 — `--color-text-inverse`, `--color-card-yellow`, `--color-card-red`, `--color-card-badge-border`)
    - .planning/phases/41-card-injury-iconography/41-UI-SPEC.md (Component &amp; Data Contract section)
  </read_first>
  <behavior>
    - `cardColorFor({ redCarded: true })` -> `'red'`; `{ yellowCards: 1 }` -> `'yellow'`; `{ yellowCards: 1, redCarded: true }` -> `'red'` (red wins); `{}` -> `null`
    - `cardColorForBenchEntry({ status: 'redCarded' })` -> `'red'`; `{ status: 'subbedOut', yellowCards: 1 }` -> `'yellow'`; `{ status: 'available' }` -> `null`
    - `cardInjuryLabel('yellow', 0)` -> `'Yellow card'`; `('red', 0)` -> `'Red card'`; `(null, 1)` -> `'Injured'`; `(null, 2)` -> `'Injured x2'` (multiplication sign U+00D7); `('yellow', 1)` -> `'Yellow card, Injured'`
    - `<CardInjuryBadge cardColor={null} injuryCount={0} />` renders nothing (null)
    - `<CardInjuryBadge cardColor="yellow" injuryCount={0} />` renders one `<svg data-testid="card-injury-badge">` containing exactly one `[data-testid="piece-card-badge"]` rect with `data-card="yellow"` and zero `[data-testid="piece-injury-badge"]`
    - `<CardInjuryBadge cardColor={null} injuryCount={2} />` renders exactly ONE injury glyph (binary treatment — no second cross, no numeric overlay)
    - `<CardInjuryBadge cardColor="red" injuryCount={1} />` renders both glyphs with non-overlapping horizontal extents: card rect `x + width` is less than the injury cross's leftmost rect `x`
    - `<CardInjuryBadgeGroup cx={100} cy={50} r={10} cardColor="red" injuryCount={1} />` renders the card rect and injury cross both centred on (100, 50) — the pitch token's existing layered treatment (D-03), not the side-by-side layout
  </behavior>
  <action>
    Create `packages/client/src/components/CardInjuryBadge.tsx` as the single owner of card/injury
    glyph drawing and derivation (ICON-01, D-01). Project conventions that apply: `verbatimModuleSyntax`
    (use `import type` for type-only imports), `.js` extensions on relative imports, `strict` +
    `exactOptionalPropertyTypes` + `noUncheckedIndexedAccess`.

    Export exactly these symbols:

    1. `export type CardColor = 'yellow' | 'red' | null;`
    2. `export type BenchCardStatus = { cardColor: CardColor; injuryCount: number };`
    3. `export function cardColorFor(piece: Pick&lt;PlayerPiece, 'redCarded' | 'yellowCards'&gt;): CardColor`
       — the derivation currently duplicated at `PieceOverlay.tsx:237-238`, `PlayerStatsPanel.tsx:150-151`
       and `LineupAssignmentScreen.tsx:454-455`: red wins over yellow.
    4. `export function cardColorForBenchEntry(entry: Pick&lt;BenchEntry, 'status' | 'yellowCards'&gt;): CardColor`
       — `status === 'redCarded'` implies red with zero extra data (a piece cannot reach that bench
       status without `redCarded === true`); otherwise fall back to the entry's `yellowCards`
       (the new optional field added by plan 41-02). Deliberately does NOT use the
       `redCarded === true ?` expression, so the ICON-01 audit in plan 41-06 stays meaningful.
    5. `export function cardInjuryLabel(cardColor: CardColor, injuryCount: number): string` —
       joins the present parts with `', '` per the UI-SPEC Copywriting Contract. Returns `''` when
       neither is present.
    6. `export function CardInjuryBadgeGroup({ cx, cy, r, cardColor, injuryCount })` — raw SVG
       primitives (no `<svg>` wrapper) that compose into a parent `<svg>` coordinate space.
       Geometry is lifted VERBATIM from `PieceOverlay.tsx:240-289`: `cardWidth = r * 1.5`,
       `cardHeight = r * 2`, `barLength = r * 1.8`, `barThickness = r * 0.6`; card rect at
       `x = cx - cardWidth / 2`, `y = cy - cardHeight / 2`, `rx={1.5}`, `strokeWidth={1}`; injury
       cross as two rects inside a `<g>`, `rx={1}`, `strokeWidth={0.75}`. Card rect keeps
       `data-testid="piece-card-badge"` and `data-card={cardColor}`; the cross `<g>` keeps
       `data-testid="piece-injury-badge"`; both keep `pointerEvents="none"`. The injury cross renders
       AFTER the card rect in DOM order (existing D-05 layering, asserted by `PieceOverlay.test.tsx:314-324`).
       Fills: `var(--color-card-red)` / `var(--color-card-yellow)` for the card, `var(--color-text-inverse)`
       for the cross. Strokes: `var(--color-card-badge-border)` (the token whose value IS the
       `rgba(0,0,0,0.5)` literal currently inline in `PieceOverlay.tsx` — same computed colour, token
       form per the UI-SPEC Color table; introduce no hex literal anywhere in this file).
       Accessibility: put `role="img"` + `aria-label={cardInjuryLabel(cardColor, 0)}` on the card rect
       and `role="img"` + `aria-label={cardInjuryLabel(null, injuryCount)}` on the cross `<g>`.
    7. `export function CardInjuryBadge({ cardColor, injuryCount, size = 16 })` — self-contained
       `<svg>` for plain-DOM surfaces, mirroring `MiniTokenBadge`'s shape. Returns `null` when there
       is nothing to draw. Wrapper carries `data-testid="card-injury-badge"`, `role="img"`,
       `aria-label={cardInjuryLabel(cardColor, injuryCount)}`, a `<title>` child with the same string
       (hover tooltip, mirroring the `title={fullLabel}` stat-chip precedent), and
       `style={{ flexShrink: 0 }}`. Layout constants (D-04 side-by-side, derived so the two glyphs
       never overlap): `r = size * 0.4`; when only one glyph is present `viewW = size` and that
       glyph's `cx = size * 0.5`; when both are present `viewW = size * 1.5`, card `cx = size * 0.34`,
       injury `cx = size * 1.10`; `cy = size / 2` always. Render by composing `CardInjuryBadgeGroup`
       twice (once with `injuryCount={0}`, once with `cardColor={null}`) so geometry has exactly one
       definition. `viewBox={`0 0 ${viewW} ${size}`}`, `width={viewW}`, `height={size}`.

    Header doc comment must state: (a) this module is the ICON-01 single source of truth and the
    `redCarded === true ?` ternary must never be re-introduced in a consumer; (b) the `piece-*`
    `data-testid` names are legacy-named but are now the shared cross-surface contract, kept
    unchanged so `PieceOverlay.test.tsx` stays green as a behaviour-preservation signal;
    (c) the injury glyph is binary by design (D-01 / UI-SPEC) — `injuryCount >= 2` differs only in
    the accessible label.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client typecheck &amp;&amp; pnpm exec eslint packages/client/src/components/CardInjuryBadge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/client/src/components/CardInjuryBadge.tsx` exists.
    - `pnpm --filter @counter-attack/client typecheck` exits 0.
    - `pnpm exec eslint packages/client/src/components/CardInjuryBadge.tsx` exits 0.
    - `grep -c "export" packages/client/src/components/CardInjuryBadge.tsx` finds all seven exports: `grep -E "export (type )?(function )?(CardColor|BenchCardStatus|cardColorFor|cardColorForBenchEntry|cardInjuryLabel|CardInjuryBadgeGroup|CardInjuryBadge)\b" packages/client/src/components/CardInjuryBadge.tsx` returns 7 lines.
    - `grep -nE "#[0-9a-fA-F]{3,8}" packages/client/src/components/CardInjuryBadge.tsx` returns zero lines (no hex literals — tokens only).
    - `grep -c "data-testid=\"piece-card-badge\"" packages/client/src/components/CardInjuryBadge.tsx` is 1 and `grep -c "data-testid=\"piece-injury-badge\"" ...` is 1.
    - `grep -c "data-testid=\"card-injury-badge\"" packages/client/src/components/CardInjuryBadge.tsx` is 1.
  </acceptance_criteria>
  <done>The shared module compiles, lints clean, exports all seven symbols, and uses only design tokens for colour.</done>
</task>

<task type="auto">
  <name>Task 2: Unit spec for the shared badge module</name>
  <files>packages/client/src/components/CardInjuryBadge.test.tsx</files>
  <read_first>
    - packages/client/src/components/CardInjuryBadge.tsx (the module written in Task 1)
    - packages/client/src/components/PieceOverlay.test.tsx (lines 271-347 — the existing badge assertion style: `container.querySelector('[data-testid=...]')`, `data-card` attribute checks, `compareDocumentPosition` for DOM order, numeric `x`/`width` reads)
    - packages/client/src/components/BenchCarousel.test.tsx (lines 1-55 — the sibling `describe`/`afterEach(cleanup)` + fixture-builder convention)
  </read_first>
  <action>
    Create `packages/client/src/components/CardInjuryBadge.test.tsx` covering every case in Task 1's
    `<behavior>` block. Use `vitest` + `@testing-library/react` exactly as the sibling component
    tests do (`import { describe, it, expect, afterEach } from 'vitest'`, `afterEach(() => cleanup())`).

    Organise as four `describe` blocks:
    1. `cardColorFor` / `cardColorForBenchEntry` precedence (pure-function cases, including
       `yellowCards: 2` with no red still returning `'yellow'`, and an `undefined`-field clean object
       returning `null`).
    2. `cardInjuryLabel` strings, including the `'Injured x2'` case using the U+00D7 multiplication
       sign and the two-part `'Yellow card, Injured'` join.
    3. Standalone `CardInjuryBadge` rendering: clean renders nothing (`container.firstChild` is
       `null`); yellow-only and red-only render one card rect with the right `data-card`;
       `injuryCount: 1` and `injuryCount: 2` each render exactly ONE `piece-injury-badge`;
       the both-present case asserts D-04 side-by-side by reading the card rect's `x` + `width` and
       the injury `<g>`'s first child rect `x`, expecting `cardRight <= injuryLeft`; wrapper carries
       `data-testid="card-injury-badge"` and the expected `aria-label`.
    4. `CardInjuryBadgeGroup` rendering inside a host `<svg>`: assert card rect centre equals the
       passed `cx`/`cy` (compute from `x + width / 2`, `y + height / 2`) and that the injury cross is
       centred on the same point — i.e. the pitch token's layered treatment (D-03) is preserved, in
       contrast to describe-block 3's side-by-side layout.

    Also assert the token contract: the card rect `fill` attribute is exactly
    `var(--color-card-red)` for red and `var(--color-card-yellow)` for yellow, and the cross rects'
    `fill` is `var(--color-text-inverse)`.

    Note for the runner: if vitest reports a worker crash on Windows, re-run with `--pool=forks`
    (known environment flake, not a test failure).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- CardInjuryBadge</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- CardInjuryBadge` exits 0 with at least 14 passing tests and 0 failures.
    - The suite includes an assertion that the both-present standalone badge's card rect right edge is less than or equal to the injury cross's left edge (D-04 no-overlap).
    - The suite includes an assertion that `injuryCount: 2` renders exactly one `piece-injury-badge` element.
    - The suite includes an assertion that `CardInjuryBadgeGroup` centres both glyphs on the passed `cx`/`cy`.
    - `pnpm --filter @counter-attack/client typecheck` exits 0.
  </acceptance_criteria>
  <done>The shared module has a green unit spec covering derivations, both render modes, the D-04 side-by-side layout, and the accessible labels.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                   |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| server -> client (`GameState` broadcast) | Pre-existing boundary. This plan adds no new crossing: it only renders fields already broadcast and already server-validated. |

## STRIDE Threat Register

| Threat ID | Category                 | Component             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                 |
| --------- | ------------------------ | --------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-41-01   | Information Disclosure   | `CardInjuryBadge.tsx` | accept      | The glyph renders `redCarded`/`yellowCards`/`injuryCount`, which are already broadcast in full to both clients on every action (full-snapshot architecture, no delta patching). No field becomes newly visible to a player who could not already read it from `GameState`.      |
| T-41-02   | Tampering                | `CardInjuryBadge.tsx` | accept      | Pure presentational component with no writes: no store mutation, no socket emit, no persistence, no `dangerouslySetInnerHTML`. Props are typed `CardColor`/`number` and are only used as SVG geometry and as an `aria-label` string rendered as a text node (React escapes it). |
| T-41-SC   | Tampering (supply chain) | package installs      | n/a         | This plan installs zero packages (`41-RESEARCH.md` Package Legitimacy Audit: not applicable). No `[ASSUMED]`/`[SUS]` package checkpoint is required.                                                                                                                            |

**Phase-level statement (applies to every Phase 41 plan):** this phase introduces NO new attack
surface. There is no new user-controllable input, no new server endpoint or socket event, no
auth/session change, and no new network or trust boundary. The only non-client change in the phase
(plan 41-02's two optional `BenchEntry` fields) copies already-validated server-side values into a
display record; it is data propagation, never new untrusted input. Per `41-RESEARCH.md` Security
Domain, ASVS V2/V3/V4/V5/V6 are all non-applicable at L1 for this scope. This is the expected and
complete threat model for this phase, not an omission.
</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test -- CardInjuryBadge` green.
- `pnpm --filter @counter-attack/client typecheck` green.
- `pnpm exec eslint packages/client/src/components/CardInjuryBadge.tsx` green.
- No consumer changes yet — `git status` shows exactly two new files and no modified files.
</verification>

<success_criteria>

- One module owns the glyph geometry, the colour tokens, the two derivation functions, and the
  accessible-label copy (ICON-01 foundation).
- The standalone export renders card + injury side by side without overlap (D-04); the group export
  preserves the pitch token's layered, corner-anchored treatment unchanged (D-03).
- Zero hex colour literals; every fill/stroke resolves through `tokens.css`.
  </success_criteria>

<output>
Create `.planning/phases/41-card-injury-iconography/41-01-SUMMARY.md` when done
</output>
