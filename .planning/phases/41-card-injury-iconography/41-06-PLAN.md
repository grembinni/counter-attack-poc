---
phase: 41-card-injury-iconography
plan: 06
type: execute
wave: 4
depends_on: [41-01, 41-02, 41-03, 41-04, 41-05]
files_modified:
  - packages/client/src/components/CardInjuryBadge.audit.test.ts
  - packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx
autonomous: true
requirements: [ICON-01, ICON-02, ICON-03]

must_haves:
  truths:
    - 'Exactly one module in packages/client/src/components draws the card/injury glyph and derives its colour — nothing else does (ICON-01)'
    - 'The removed text-chip treatment leaves no residue: no chip test-ids, no chip copy, no chip CSS rules anywhere'
    - 'All four surfaces import the shared module rather than reimplementing it'
    - 'A booking or injury applied mid-match makes the glyph appear on all four surfaces, with none left stale or missing (ROADMAP Success Criterion 3)'
    - 'The ICON-01 invariant is enforced by a permanent spec, so Phase 42 cannot silently reintroduce a fourth treatment'
  artifacts:
    - path: 'packages/client/src/components/CardInjuryBadge.audit.test.ts'
      provides: 'Source-scanning ICON-01 invariant spec: single derivation, single glyph markup, zero chip residue, four confirmed importers'
      min_lines: 90
    - path: 'packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx'
      provides: 'Four-surface consistency spec proving a mid-match booking/injury updates pitch token, scoreboard, roster card, and bench card identically'
      min_lines: 120
  key_links:
    - from: 'packages/client/src/components/CardInjuryBadge.audit.test.ts'
      to: 'packages/client/src/components/'
      via: 'readdirSync source scan of every .tsx sibling'
      pattern: 'readdirSync'
    - from: 'packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx'
      to: 'PieceOverlay / PlayerStatsPanel / LineupAssignmentScreen / BenchCarousel'
      via: 'one disciplinary state rendered through all four surfaces'
      pattern: 'piece-card-badge'
---

<objective>
Close the phase with the two checks that no single surface plan can make on its own: a repo-wide
ICON-01 consolidation audit, and a four-surface consistency test for ROADMAP Success Criterion 3.

Purpose: plans 41-03/41-04/41-05 each prove their own surface in isolation. ICON-01's real claim —
"a single shared component **replaces** the independently-implemented treatments" — and Success
Criterion 3's claim — "updates all four surfaces consistently, with no surface left showing a stale
or missing icon" — are both cross-surface properties. This plan turns each into an executable,
permanently-enforced spec rather than a one-shot manual grep, so Phase 42 (which consumes this
component for its bench red-card marker) cannot silently regress it.
Output: two new spec files. If either audit fails, remediating the offending source file is in scope
for this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-card-injury-iconography/41-UI-SPEC.md
@.planning/phases/41-card-injury-iconography/41-VALIDATION.md
@.planning/phases/41-card-injury-iconography/41-PATTERNS.md
</context>

## Artifacts this phase produces

New symbols created by Phase 41 (none of these exist in the codebase before this phase — do not
treat them as drift):

| Symbol                                                           | Kind                                              | Location                                       |
| ---------------------------------------------------------------- | ------------------------------------------------- | ---------------------------------------------- |
| `CardInjuryBadge.tsx` / `CardInjuryBadge.test.tsx`               | new files                                         | `packages/client/src/components/` (plan 41-01) |
| `CardColor`, `BenchCardStatus`                                   | exported type aliases                             | `CardInjuryBadge.tsx` (plan 41-01)             |
| `cardColorFor`, `cardColorForBenchEntry`, `cardInjuryLabel`      | exported functions                                | `CardInjuryBadge.tsx` (plan 41-01)             |
| `CardInjuryBadgeGroup`, `CardInjuryBadge`                        | exported React components                         | `CardInjuryBadge.tsx` (plan 41-01)             |
| `data-testid="card-injury-badge"`                                | new test hook                                     | standalone `<svg>` wrapper (plan 41-01)        |
| `BenchEntry.yellowCards`, `BenchEntry.injuryCount`               | new optional fields                               | `packages/shared/src/types.ts` (plan 41-02)    |
| `StatCardProps.cardColor` retyped to `CardColor`                 | narrowed existing prop type (not a new prop)      | `LineupAssignmentScreen.tsx` (plan 41-04)      |
| `DraftCardBodyProps.cardColor`, `DraftCardBodyProps.injuryCount` | new optional props                                | `DraftPackCarousel.tsx` (plan 41-05)           |
| `BenchCarouselProps.benchCardStatus`                             | new optional prop                                 | `BenchCarousel.tsx` (plan 41-05)               |
| `CardInjuryBadge.audit.test.ts`                                  | new file (source-scanning ICON-01 invariant spec) | `packages/client/src/components/` (this plan)  |
| `CardInjuryBadge.crossSurface.test.tsx`                          | new file (four-surface Success-Criterion-3 spec)  | `packages/client/src/components/` (this plan)  |

<tasks>

<task type="auto">
  <name>Task 1: ICON-01 consolidation audit as a permanent spec</name>
  <files>packages/client/src/components/CardInjuryBadge.audit.test.ts</files>
  <read_first>
    - packages/client/src/components/CardInjuryBadge.tsx (plan 41-01 — the one file legitimately containing the derivation and the glyph markup; note its header doc already states the ternary must never be re-introduced in a consumer)
    - packages/client/src/components/DraftPackCarousel.tsx (as migrated by plan 41-05 — line ~115 keeps a PRE-EXISTING `redCarded === true ? (` availability ternary that selects the `RED CARD` vs `OUT` text badge; it is NOT a glyph derivation and must be allowlisted, not "fixed")
    - packages/client/src/components/LineupAssignmentScreen.tsx (as migrated by plan 41-04 — `const isBlocked = piece.redCarded === true;` is a substitution-eligibility guard, also not a glyph derivation)
    - .planning/phases/41-card-injury-iconography/41-UI-SPEC.md ("Post-implementation verification (carried from RESEARCH.md, load-bearing for ICON-01)")
    - packages/client/vitest.config.ts (confirm the include glob picks up a plain `.ts` spec in the components directory, not only `.tsx`)
  </read_first>
  <action>
    Create `packages/client/src/components/CardInjuryBadge.audit.test.ts` — a source-scanning vitest
    spec that makes ICON-01's "single shared component" claim checkable and permanent.

    Deliberately a `.ts` file, not `.tsx`: every scan below globs `.tsx` siblings, so the audit file
    excludes itself by construction and its own needle literals can never satisfy or break their own
    assertions.

    Setup:
    - `const COMPONENTS_DIR = fileURLToPath(new URL('.', import.meta.url));` (portable ESM form —
      do NOT use `__dirname`, the client package is `"type": "module"`).
    - `const ALL_TSX = readdirSync(COMPONENTS_DIR).filter((f) => f.endsWith('.tsx'));`
    - `const SOURCE_TSX = ALL_TSX.filter((f) => !f.endsWith('.test.tsx'));`
    - A `read(file)` helper returning `readFileSync(join(COMPONENTS_DIR, file), 'utf8')` with
      whitespace normalized via `.replace(/\s+/g, ' ')`. Normalization is required, not cosmetic:
      Prettier is free to wrap a ternary across lines, and an un-normalized substring search would
      silently pass on a reformatted duplicate.
    - A `readCss(file)` helper that additionally strips block comments
      (`.replace(/\/\*[\s\S]*?\*\//g, '')`) BEFORE matching, so prose mentioning a deleted class name
      can never fail (or falsely satisfy) a selector assertion.
    - `const SURFACES = ['PieceOverlay.tsx', 'PlayerStatsPanel.tsx', 'LineupAssignmentScreen.tsx', 'DraftPackCarousel.tsx'];`

    Assertions, grouped into describe blocks:

    1. `ICON-01: the card-colour derivation exists in exactly one place`
       - For every file in `SOURCE_TSX` except `CardInjuryBadge.tsx`, the regex
         `/redCarded\s*===\s*true\s*\?\s*['"]red['"]/` must NOT match. Fail the assertion with the
         offending filename in the message so a future regression names itself.
       - `CardInjuryBadge.tsx` must match that regex exactly once (guards against the shared module
         itself sprouting a second copy, e.g. inside `cardColorForBenchEntry`, which plan 41-01
         deliberately wrote without it).
       - Broader belt-and-braces check with an explicit, documented allowlist: the bare substring
         `redCarded === true ?` may appear ONLY in `CardInjuryBadge.tsx` (the glyph derivation) and
         `DraftPackCarousel.tsx` (the pre-existing `RED CARD` vs `OUT` availability ternary, Phase 40
         SUB-07/D-13 — a different question, deliberately untouched by Phase 41). Encode that
         allowlist as a named const with an explanatory comment; do not silently widen it.

    2. `ICON-01: the glyph markup exists in exactly one place`
       - Among `SOURCE_TSX`, exactly one file contains `data-testid="piece-card-badge"` and exactly
         one contains `data-testid="piece-injury-badge"`, and in both cases that file is
         `CardInjuryBadge.tsx`. (Test files legitimately query these hooks, which is why this scans
         source files only.)

    3. `ICON-01/ICON-02: all four surfaces consume the shared module`
       - Every file in `SURFACES` contains `from './CardInjuryBadge.js'`.
       - No file in `SOURCE_TSX` other than `CardInjuryBadge.tsx` declares
         `function cardColorFor` or `function cardColorForBenchEntry`.

    4. `D-01: the removed text-chip treatment leaves no residue`
       - Across `ALL_TSX` (source AND test files — plans 41-03/41-04 rewrote the specs too), zero
         files contain `stats-card-chip`, `stats-injury-chip`, `cardColor.toUpperCase()`, `'INJ'`,
         `INJ ×2`, or `INJ x2`.
       - Across the two stylesheets `PlayerStatsPanel.module.css` and
         `LineupAssignmentScreen.module.css`, read via `readCss`, zero occurrences of `.cardChip` or
         `.injuryChip`.

    Add a file header doc comment stating: this spec is the executable form of the `41-UI-SPEC.md`
    "Post-implementation verification" gate for ICON-01; it is intentionally a source scan rather
    than a behavioural test because the property being protected is "nothing else implements this",
    which no render-based test can express; and Phase 42 must extend rather than delete it when it
    adds the bench red-card marker.

    If any assertion fails on first run, that is a real ICON-01 gap from an earlier plan — fix the
    offending source file in this plan (and note it in the SUMMARY) rather than relaxing the
    assertion or widening the allowlist.

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- CardInjuryBadge.audit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- CardInjuryBadge.audit` exits 0 with at least 8 passing assertions and 0 failures.
    - The file is named `CardInjuryBadge.audit.test.ts` (a `.ts`, not `.tsx`) and `grep -c "\.tsx" packages/client/src/components/CardInjuryBadge.audit.test.ts` is at least 2 (it globs `.tsx` siblings).
    - `grep -c "readdirSync" packages/client/src/components/CardInjuryBadge.audit.test.ts` is at least 1 — the scan is directory-driven, so a NEW component file is covered automatically without editing the spec.
    - `grep -c "replace(/\\\\s+/g, ' ')" packages/client/src/components/CardInjuryBadge.audit.test.ts` is at least 1 (whitespace normalization present).
    - `grep -c "DraftPackCarousel" packages/client/src/components/CardInjuryBadge.audit.test.ts` is at least 1 — the availability-ternary allowlist is explicit and commented, not implicit.
    - Manual falsification check, then revert: temporarily add the line `const x = piece.redCarded === true ? 'red' : null;` to `packages/client/src/components/PlayerStatsPanel.tsx`, confirm `pnpm --filter @counter-attack/client test -- CardInjuryBadge.audit` now FAILS and names `PlayerStatsPanel.tsx` in the failure message, then `git checkout -- packages/client/src/components/PlayerStatsPanel.tsx` and confirm the suite is green again. Record this check in the SUMMARY — an audit that cannot fail is not an audit.
    - `pnpm --filter @counter-attack/client typecheck` and `pnpm exec eslint packages/client/src/components/CardInjuryBadge.audit.test.ts` both exit 0.
  </acceptance_criteria>
  <done>ICON-01's "one shared component, nothing else draws this logic" is a permanently-enforced, provably-failing-when-violated spec covering derivation, glyph markup, importers, and chip residue.</done>
</task>

<task type="auto">
  <name>Task 2: Four-surface consistency spec (ROADMAP Success Criterion 3)</name>
  <files>packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx</files>
  <read_first>
    - packages/client/src/components/PieceOverlay.test.tsx (lines 1-125 — the `vi.mock('../socket.js')` stub, the minimal `PlayerPiece` literals, and the `renderPiece` helper that wraps the overlay in a host `<svg>` with `COLOR_SCHEME_REGISTRY` palettes)
    - packages/client/src/components/PlayerStatsPanel.test.tsx (lines 10-45 and 120-135 — the `useGameStore.setState` + `mockMovementState` harness and the `overridePiece` helper that patches one piece inside `gameState.pieces`)
    - packages/client/src/components/LineupAssignmentScreen.test.tsx (lines 554-585 — the full mid-match prop set `renderMidmatch` passes, which this spec must reproduce)
    - packages/client/src/components/BenchCarousel.test.tsx (lines 20-52 — the `makeCard` minimal `TieredPoolPlayer` builder; it is module-local and not exported, so reproduce it locally)
    - packages/client/src/components/CardInjuryBadge.tsx (the `piece-card-badge` / `piece-injury-badge` hooks that BOTH render modes carry — this is the only assertion vocabulary that spans the SVG-group surface and the three DOM surfaces)
  </read_first>
  <action>
    Create `packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx`, proving ROADMAP
    Phase 41 Success Criterion 3: "A booking or injury applied mid-match updates all four surfaces
    consistently, with no surface left showing a stale or missing icon."

    Standard preamble: `vi.mock('../socket.js', () => ({ socket: { emit: vi.fn(), on: vi.fn(), off: vi.fn() } }));`
    and `afterEach(() => cleanup());`, matching every sibling component spec.

    Shared vocabulary — define ONE helper used identically by all four surfaces:

    `function glyphContract(container: HTMLElement)` returning
    `{ cards: Array.from(container.querySelectorAll('[data-testid="piece-card-badge"]')).map((e) => e.getAttribute('data-card')), injuries: container.querySelectorAll('[data-testid="piece-injury-badge"]').length }`.

    Define two module-level expectations used by every assertion:
    `const CLEAN = { cards: [], injuries: 0 };` and `const BOOKED_AND_INJURED = { cards: ['yellow'], injuries: 1 };`.
    The whole point of the spec is that the SAME expected object satisfies all four surfaces — assert
    with `toEqual` against these constants, never with per-surface bespoke expectations.

    The disciplinary state under test is `{ yellowCards: 1, injuryCount: 1 }` applied mid-match to a
    previously clean player. Structure as four `it` blocks in a describe named
    `Card/injury iconography — ROADMAP Phase 41 Success Criterion 3: all four surfaces update consistently`,
    each following the same clean -> update -> assert shape (the "update" step is what proves nothing
    is stale — do not shortcut it by rendering the booked state directly):

    1. `pitch token (PieceOverlay) shows the glyph after a mid-match booking+injury` — render
       `<svg><PieceOverlay piece={cleanPiece} uniformStyle="pinstripes-vertical" palette={COLOR_SCHEME_REGISTRY.city.palette} selectionState="none" onClick={noop} onInspect={noop} carrierId={null} attackingTeam="home" /></svg>`;
       assert `glyphContract(container)` equals `CLEAN`; `rerender` the same tree with
       `piece={{ ...cleanPiece, yellowCards: 1, injuryCount: 1 }}`; assert it equals `BOOKED_AND_INJURED`.
       Build `cleanPiece` as a minimal `PlayerPiece` literal (id `home-6`, teamId `home`, role `MID`,
       number 7, a `position` coord, and the nine stat fields) exactly as `PieceOverlay.test.tsx` does.
    2. `scoreboard card (PlayerStatsPanel) shows the glyph after a mid-match booking+injury` — seed
       the store in a `beforeEach` with `gameState: { ...mockMovementState, selectedTeams: { home: 'city', away: 'crew' } }`
       plus `selectedPieceId: 'home-6'` and the other fields `PlayerStatsPanel.test.tsx`'s `beforeEach`
       sets; render `<PlayerStatsPanel />`; assert `CLEAN`; then inside
       `act(() => { ... })` (imported from `@testing-library/react`) patch the store with
       `pieces: pieces.map((p) => p.id === 'home-6' ? { ...p, yellowCards: 1, injuryCount: 1 } : p)`;
       assert `BOOKED_AND_INJURED` without re-rendering — the zustand subscription must do it, which
       is exactly the "no stale icon" property.
    3. `roster card (LineupAssignmentScreen mid-match) shows the glyph after a mid-match booking+injury` —
       render `<LineupAssignmentScreen assignment={[]} formationId="4-4-2" playerSlot={1} myTeamId="city" onSwap={noop} onConfirm={noop} lineupConfirmed={false} mode="midmatch" midmatchPieces={[cleanPiece]} bench={[]} subsUsed={0} maxOnPitch={11} onSubstitute={noop} readOnly={false} />`;
       assert `CLEAN`; `rerender` with `midmatchPieces={[{ ...cleanPiece, yellowCards: 1, injuryCount: 1 }]}`;
       assert `BOOKED_AND_INJURED`.
    4. `bench card (BenchCarousel) shows the glyph after a mid-match booking+injury` — render
       `<BenchCarousel cards={[makeCard('p013')]} teamId="city" benchNumbers={{ p013: 13 }} onCardDragStart={noop} onDropToBench={noop} />`
       with no `benchCardStatus`; assert `CLEAN`; `rerender` with
       `benchCardStatus={{ p013: { cardColor: 'yellow', injuryCount: 1 } }}`; assert
       `BOOKED_AND_INJURED`. Reproduce `makeCard` locally from `BenchCarousel.test.tsx`'s builder
       (it is module-local, not exported) — do not export it from that test file.

    Add a fifth `it` that closes ICON-02's "identical iconography" claim explicitly:
    `ICON-02: all four surfaces render the identical glyph contract for the identical state` —
    collect the four `glyphContract` results from the booked+injured render of each surface into an
    array and assert every element `toEqual(BOOKED_AND_INJURED)` in a single assertion, so a future
    divergence on any one surface fails here with all four values visible in the diff.

    Header doc comment must state: this spec exists because each surface plan (41-03/41-04/41-05)
    proves only its own surface; Success Criterion 3 is a cross-surface property; and the shared
    `piece-card-badge`/`piece-injury-badge` hooks are what let one assertion vocabulary span the SVG
    `CardInjuryBadgeGroup` surface and the three DOM `CardInjuryBadge` surfaces.

    Note for the runner: if vitest reports a worker crash on Windows, re-run with `--pool=forks`
    (known environment flake, not a test failure).

  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- CardInjuryBadge.crossSurface</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/client test -- CardInjuryBadge.crossSurface` exits 0 with exactly 5 passing tests and 0 failures.
    - `grep -c "glyphContract" packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx` is at least 6 (one definition + one use per surface + the combined assertion) — every surface is asserted through the SAME helper, not bespoke per-surface queries.
    - `grep -cE "PieceOverlay|PlayerStatsPanel|LineupAssignmentScreen|BenchCarousel" packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx` is at least 4 — all four surfaces are actually rendered.
    - `grep -c "BOOKED_AND_INJURED" packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx` is at least 5 — the same expectation constant satisfies every surface.
    - Each of the four surface tests asserts `CLEAN` BEFORE the update and `BOOKED_AND_INJURED` AFTER it (verify by reading; a test that renders the booked state directly does not prove Success Criterion 3).
    - `pnpm --filter @counter-attack/client test` (full client suite) exits 0.
    - `pnpm -r test` exits 0 — client and server suites both green at phase close.
    - `pnpm -r typecheck`, `pnpm stylelint`, `pnpm lint` and `pnpm knip` all exit 0 (knip confirms no orphaned module or dead export survived the consolidation).
  </acceptance_criteria>
  <done>One booking+injury applied mid-match provably lights up the identical glyph contract on the pitch token, scoreboard card, roster card, and bench card, with no surface stale or missing.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                                                                                                                                                                                      |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none newly crossed) | This plan adds only test files. It renders existing components against local fixtures and reads repository source text from disk inside the test runner. No production code path, network call, or trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category                 | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                     |
| --------- | ------------------------ | ----------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-41-13   | Tampering                | `CardInjuryBadge.audit.test.ts` filesystem read | accept      | The spec reads only its own sibling directory, resolved from `import.meta.url` (no env var, no CLI argument, no user-supplied path), with `readFileSync` in read-only mode and zero writes. It executes under vitest/jsdom in the dev and CI toolchain only and ships in no bundle. |
| T-41-SC   | Tampering (supply chain) | package installs                                | n/a         | Zero packages installed this plan (`41-RESEARCH.md`: Package Legitimacy Audit not applicable). Both new specs use only `vitest`, `@testing-library/react` and Node builtins (`node:fs`, `node:url`, `node:path`), all already present. No `[ASSUMED]`/`[SUS]` checkpoint required.  |

**Phase-level statement (applies to every Phase 41 plan):** this phase introduces NO new attack
surface — no new user-controllable input, no new server endpoint or socket event, no auth/session
change, no new network or trust boundary. This plan is test-only. Per `41-RESEARCH.md` Security
Domain, ASVS V2/V3/V4/V5/V6 are non-applicable at L1 for this scope. This is the expected, complete
threat model for this phase, not an omission.
</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test -- CardInjuryBadge.audit` green, and provably red when a
  duplicate derivation is temporarily injected (falsification check recorded in the SUMMARY).
- `pnpm --filter @counter-attack/client test -- CardInjuryBadge.crossSurface` green with 5 tests.
- `pnpm -r test` green (client + server).
- `pnpm -r typecheck`, `pnpm lint`, `pnpm stylelint`, `pnpm knip` all green.
- `grep -rn "redCarded === true ? 'red'" packages/client/src/components --include="*.tsx" | grep -v CardInjuryBadge.tsx` returns nothing.
- `git status` shows exactly two new files and no modified files — unless the audit surfaced a real
  ICON-01 gap, in which case the remediating file change is listed in the SUMMARY with its cause.
</verification>

<success_criteria>

- ICON-01 is enforced by an executable spec, not a one-time grep: single derivation, single glyph
  markup, four confirmed importers, zero chip residue in source, tests, and CSS.
- ROADMAP Success Criterion 3 is proven end to end: one mid-match booking+injury yields the identical
  glyph contract on all four surfaces, each verified to have been clean immediately beforehand.
- The whole monorepo is green (tests, typecheck, lint, stylelint, knip) at phase close.
  </success_criteria>

<output>
Create `.planning/phases/41-card-injury-iconography/41-06-SUMMARY.md` when done
</output>
