---
phase: 44-referee-leniency-advanced-settings-drawer
plan: 03
type: execute
wave: 2
depends_on: ['44-02']
files_modified:
  - packages/client/src/components/GameSettingsScreen.tsx
  - packages/client/src/components/GameSettingsScreen.module.css
  - packages/client/src/components/GameSettingsScreen.test.tsx
autonomous: true
requirements: [REFEREE-01, REFEREE-02, REFEREE-04, SETTINGS-06]
user_setup: []

must_haves:
  truths:
    - 'Opening the Advanced section shows a Referee Leniency row in the right column, between Out-of-Bounds and Tackle/Steal Decline'
    - 'The Referee Leniency override checkbox is unchecked by default'
    - 'The value stepper is always visible and is greyed out and non-interactive while the override checkbox is unchecked'
    - 'Switching the override on leaves the stepper showing 4, and the stepper accepts only integers 2 through 5'
    - 'A short inline note next to the stepper tells the host the setting also affects added time'
    - 'Confirming settings passes refereeLeniencyOverride and refereeLeniencyValue out through onConfirm alongside the existing six toggles'
  artifacts:
    - path: 'packages/client/src/components/GameSettingsScreen.tsx'
      provides: 'Referee Leniency override state, stepper row, coupling note, extended onConfirm contract'
      contains: 'refereeLeniencyOverride'
    - path: 'packages/client/src/components/GameSettingsScreen.module.css'
      provides: 'leniencyRow / leniencyControls / leniencyInput classes'
      contains: '.leniencyInput'
    - path: 'packages/client/src/components/GameSettingsScreen.test.tsx'
      provides: 'REFEREE-01/02/04 UI regression tests'
      contains: 'spinbutton'
  key_links:
    - from: 'GameSettingsScreen.tsx Referee Leniency checkbox'
      to: "the number input's disabled attribute"
      via: 'disabled={!refereeLeniencyOverride}'
      pattern: "disabled=\\{!refereeLeniencyOverride\\}"
    - from: 'GameSettingsScreen.tsx local Leniency state'
      to: 'onConfirm payload'
      via: 'handleConfirm pass-through, no parent-toggle normalisation'
      pattern: "refereeLeniencyOverride,[\\s\\S]{0,60}refereeLeniencyValue,"
---

<objective>
Add the Referee Leniency manual-override row to the Advanced drawer that plan 44-02 built, and widen `GameSettingsScreen`'s `onConfirm` contract to carry the two new values.

Purpose: REFEREE-01 (host-toggleable override, default off), REFEREE-02 (2–5 stepper defaulting to a mid value), REFEREE-04 (UI copy noting the added-time coupling), and the final third of SETTINGS-06 (the right column reaching its full three-row shape).

This plan is client-UI-only and deliberately stops at the component boundary. Extending `onConfirm`'s type here is typecheck-safe against the still-unmodified `App.tsx`: a handler declaring **fewer** properties stays assignable to a prop type declaring **more**, so the repository remains green at this plan's boundary. Plan 44-04 then widens `App.tsx` and the wire contract in the other direction.

Output: one new row, three CSS classes, two new `onConfirm` fields, and a UI regression suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-referee-leniency-advanced-settings-drawer/44-CONTEXT.md
@.planning/phases/44-referee-leniency-advanced-settings-drawer/44-PATTERNS.md
@.planning/phases/44-referee-leniency-advanced-settings-drawer/44-02-SUMMARY.md
</context>

## Artifacts this phase produces

Produced by **this plan** (44-03):

| Symbol                                                                                | Kind                                  | Location                         |
| ------------------------------------------------------------------------------------- | ------------------------------------- | -------------------------------- |
| `refereeLeniencyOverride` / `setRefereeLeniencyOverride`                              | `useState<boolean>` (default `false`) | `GameSettingsScreen.tsx`         |
| `refereeLeniencyValue` / `setRefereeLeniencyValue`                                    | `useState<number>` (default `4`)      | `GameSettingsScreen.tsx`         |
| `handleRefereeLeniencyValueChange`                                                    | change handler with 2–5 clamp         | `GameSettingsScreen.tsx`         |
| `onConfirm` fields `refereeLeniencyOverride: boolean`, `refereeLeniencyValue: number` | prop-type contract extension          | `GameSettingsScreen.tsx` `Props` |
| `.leniencyRow`, `.leniencyControls`, `.leniencyInput`                                 | CSS module classes                    | `GameSettingsScreen.module.css`  |
| Copy string `(also affects added time)`                                               | UI copy (REFEREE-04)                  | `GameSettingsScreen.tsx`         |

Produced by other plans in this phase (do **not** create them here): engine params (44-01); `advancedOpen` / `deriveFoulDependents` / `.advancedGrid` / `.advancedColumn` (44-02); the wire contract, `Room` fields and server validation guards (44-04).

<tasks>

<task type="auto">
  <name>Task 1: Add Referee Leniency state, row markup, and CSS</name>

<files>packages/client/src/components/GameSettingsScreen.tsx, packages/client/src/components/GameSettingsScreen.module.css</files>

<read_first> - `packages/client/src/components/GameSettingsScreen.tsx` post-44-02 — the `useState` declaration block, the `advancedGrid`/`advancedColumn` JSX, and specifically the `{/* 44-03 inserts the Referee Leniency row here (D-07) */}` marker plan 44-02 left in the right column between the Out-of-Bounds row and the Tackle/Steal Decline row. - `packages/client/src/components/GameSettingsScreen.module.css` post-44-02 — `.poolRow` (163-171), `.poolRowDisabled` (174-183), `.comingSoon` (185-188), `.ctaButton:disabled` (207-210, the existing `opacity: 0.5` disabled idiom this file already uses via the `:disabled` pseudo-class), `.advancedGrid` / `.advancedColumn`, and the file header's spacing (4/8/16/24/32 only) and `var(--token)` colour rules. - `.planning/phases/44-referee-leniency-advanced-settings-drawer/44-CONTEXT.md` decisions D-01 through D-05 and D-08 — all six are implemented by this task.
</read_first>

  <action>
    **CSS first.** Add three classes to `GameSettingsScreen.module.css` after `.advancedColumn`, tagged `REFEREE-01/02 (Phase 44)`:
    - `.leniencyRow`: `display: flex; flex-direction: column; gap: 4px;` — stacks the control line above the coupling note, which keeps the row readable inside a ~208px grid column.
    - `.leniencyControls`: `display: flex; align-items: center; gap: 8px;` — the checkbox + label + stepper line.
    - `.leniencyInput`: `width: 48px; font-size: 13px; color: var(--color-text-primary); background: var(--color-bg-surface); border: 1px solid var(--color-border-muted); border-radius: 4px; padding: 4px;` plus a `.leniencyInput:disabled { opacity: 0.4; cursor: not-allowed; }` rule. The `:disabled` pseudo-class is the correct mechanism here (matching this file's existing `.ctaButton:disabled` idiom) rather than a conditional `.leniencyInputDisabled` className — it means the disabled visual can never drift out of sync with the actual `disabled` attribute. `opacity: 0.4` + `cursor: not-allowed` are copied from `.poolRowDisabled` so the grey-out reads identically to the Booking/Injury-under-Fouls language (D-04). Use only `var(--token)` colours and the 4/8/16/24/32 spacing scale.

    **Component state.** Add two `useState` declarations alongside the existing toggles:
    - `const [refereeLeniencyOverride, setRefereeLeniencyOverride] = useState<boolean>(false);` — doc-comment `REFEREE-01 (Phase 44)`: default OFF. Note this deliberately breaks the default-ON convention of the other five toggles because REFEREE-01 specifies "default off"; the random 2–5 roll (REFEREE-03) stays the out-of-box behaviour.
    - `const [refereeLeniencyValue, setRefereeLeniencyValue] = useState<number>(4);` — doc-comment `REFEREE-02 (Phase 44) / D-01`: 4, explicitly chosen over the arithmetic midpoint floor of 3. The row never unmounts (D-04), so this initial 4 is what the host sees the first time they switch the override on. Toggling the override off and back on deliberately does NOT reset the value — preserving the host's last choice is less surprising, and D-01 only constrains the first switch-on.

    **Change handler.** Add `handleRefereeLeniencyValueChange(e: React.ChangeEvent<HTMLInputElement>)`: parse with `Number.parseInt(e.target.value, 10)`; if `Number.isNaN(parsed)` return without calling the setter (a cleared or non-numeric input snaps back to the last valid value rather than propagating `NaN`); otherwise `setRefereeLeniencyValue(Math.min(5, Math.max(2, parsed)))`. Doc-comment that the `min`/`max` attributes handle native spinner clamping (D-02/D-03) while this JS clamp covers typed and pasted input, and that neither is a security control — `roomHandlers.ts` re-validates the range server-side (plan 44-04).

    **Row markup.** Replace the `{/* 44-03 inserts the Referee Leniency row here (D-07) */}` marker in the right column with a `<div className={styles.leniencyRow}>` containing, in order:
    1. `<div className={styles.leniencyControls}>` holding:
       - `<label className={styles.poolRow}>` wrapping `<input type="checkbox" checked={refereeLeniencyOverride} onChange={() => setRefereeLeniencyOverride((v) => !v)} />` and the text `Referee Leniency`. Wrap only the checkbox in this label — a `<label>` associates with its first labelable descendant, so wrapping the number input too would leave the stepper with no accessible name.
       - `<input type="number" className={styles.leniencyInput} aria-label="Referee Leniency value" min={2} max={5} step={1} value={refereeLeniencyValue} disabled={!refereeLeniencyOverride} onChange={handleRefereeLeniencyValueChange} />`. The explicit `aria-label` is required (it is outside the label) and is the stable test hook. `min={2} max={5}` are what D-02's "disabled at bounds" resolves to under D-03's native-input decision.
    2. `<span className={styles.comingSoon}>(also affects added time)</span>` — the REFEREE-04 coupling note. Exact copy is `(also affects added time)`: terse, parenthesised, and in the same voice as the existing `(requires Fouls)` suffix (D-08). It is rendered unconditionally, not only when disabled, because it describes what Leniency does rather than why a control is unavailable.

    The row must sit **between** the Out-of-Bounds / Restarts row and the Tackle/Steal Decline Prompt row (D-05/D-07 right-column order: Out-of-Bounds, Referee Leniency, Tackle/Steal Decline). Do not apply `styles.poolRowDisabled` to the row or to the checkbox label — the override checkbox must stay fully interactive at all times; only the stepper greys out (D-04).

    Do not touch `deriveFoulDependents` — Referee Leniency has no parent-toggle dependency and must not be routed through the Fouls derivation.

  </action>

<acceptance_criteria> - Source assertion: `grep -n "Referee Leniency" packages/client/src/components/GameSettingsScreen.tsx` shows the label between the `Out-of-Bounds / Restarts` line number and the `Tackle/Steal Decline Prompt` line number. - Source assertion: `grep -c 'min={2}' packages/client/src/components/GameSettingsScreen.tsx` returns `1` and `grep -c 'max={5}' ...` returns `1`. - Source assertion: `grep -c "useState<number>(4)" packages/client/src/components/GameSettingsScreen.tsx` returns `1` (D-01). - Source assertion: `grep -c "useState<boolean>(false)" packages/client/src/components/GameSettingsScreen.tsx` returns `1` — Referee Leniency is the only default-off toggle. - Source assertion: `grep -c "also affects added time" packages/client/src/components/GameSettingsScreen.tsx` returns `1` (REFEREE-04). - Source assertion: `grep -c "poolRowDisabled" packages/client/src/components/GameSettingsScreen.tsx` returns `3` — the two Booking/Injury rows and the draft-pool row only; the Leniency row does not use it. - Test command: `pnpm --filter @counter-attack/client typecheck` exits 0. - Test command: `pnpm stylelint` exits 0.
</acceptance_criteria>

  <verify>
    <automated>pnpm --filter @counter-attack/client typecheck &amp;&amp; pnpm stylelint</automated>
  </verify>

<done>The Advanced drawer's right column reads Out-of-Bounds → Referee Leniency → Tackle/Steal Decline; the override checkbox defaults off, the stepper shows 4 and is disabled/greyed while the override is off, and the added-time note renders.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the onConfirm contract with the two Leniency fields</name>

<files>packages/client/src/components/GameSettingsScreen.tsx</files>

<read_first> - `packages/client/src/components/GameSettingsScreen.tsx` lines 20-42 — the `Props.onConfirm` settings object type and its per-field doc-comment convention (`/** TACKLE-01 (Phase 43): ... */`). - `packages/client/src/components/GameSettingsScreen.tsx` `handleConfirm` post-44-02 — now reading `foulDependents.booking` / `foulDependents.injury`. - `packages/client/src/App.tsx` lines 265-287 and 355 — `handleSettingsConfirm`'s current 8-field param type and the `<GameSettingsScreen onConfirm={handleSettingsConfirm} .../>` JSX site. Read only; **do not modify App.tsx in this plan** — plan 44-04 owns it. - `.planning/phases/44-referee-leniency-advanced-settings-drawer/44-PATTERNS.md` section "`onConfirm` prop signature to extend".
</read_first>

  <action>
    Append two fields to the `Props.onConfirm` settings object type, after `tackleStealDecline: boolean`, each with a doc-comment in the established style:
    - `/** REFEREE-01 (Phase 44): manual Referee Leniency override toggle — default off; when false the engine keeps the random 2–5 roll (REFEREE-03). */ refereeLeniencyOverride: boolean;`
    - `/** REFEREE-02 (Phase 44): host-selected Leniency value, integer 2–5. Always sent (the stepper never unmounts, D-04); only consulted server-side when refereeLeniencyOverride is true. */ refereeLeniencyValue: number;`

    In `handleConfirm`, add `refereeLeniencyOverride,` and `refereeLeniencyValue,` to the `onConfirm({...})` object as **raw pass-throughs**. Unlike `booking`/`injury` these get no parent-toggle normalisation: Referee Leniency has no dependency like Fouls, and the value is intentionally sent even when the override is off so the server never has to reason about a missing field. Add a short comment saying exactly that.

    **Do not modify `App.tsx`.** This widening is intentionally one-sided and is typecheck-safe: `handleSettingsConfirm: (s: S8) => void` remains assignable to `onConfirm: (s: S10) => void` under `strictFunctionTypes` because the contravariant check requires `S10` (10 fields) to be assignable to `S8` (8 fields), which holds. At runtime `App.tsx` receives the 10-field object and forwards it wholesale via `socket.emit`, so the two extra fields ride along harmlessly and are ignored by the not-yet-updated server. Plan 44-04 closes the loop by widening `App.tsx` and the wire contract. If the client typecheck unexpectedly fails at the JSX site, stop and report it rather than editing `App.tsx` — that would create a file conflict with 44-04.

  </action>

<acceptance_criteria> - Source assertion: `grep -c "refereeLeniencyOverride" packages/client/src/components/GameSettingsScreen.tsx` returns at least `5` (state declaration, setter call, checkbox `checked`, stepper `disabled`, prop type, handleConfirm). - Source assertion: `grep -n "refereeLeniencyValue" packages/client/src/components/GameSettingsScreen.tsx` shows a hit inside the `Props` type block (lines < 50) and a hit inside `handleConfirm`. - Source assertion: `git diff --name-only` does NOT list `packages/client/src/App.tsx`. - Test command: `pnpm --filter @counter-attack/client typecheck` exits 0 — proves the one-sided widening is assignable and the repo is green at this plan's boundary. - Test command: `pnpm --filter @counter-attack/server typecheck` exits 0 — proves no wire contract was touched.
</acceptance_criteria>

  <verify>
    <automated>pnpm --filter @counter-attack/client typecheck &amp;&amp; pnpm --filter @counter-attack/server typecheck</automated>
  </verify>

<done>`onConfirm` declares and receives `refereeLeniencyOverride` and `refereeLeniencyValue`; `App.tsx` is untouched and both package typechecks pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add REFEREE-01/02/04 UI tests and update the exact-payload assertions</name>

<files>packages/client/src/components/GameSettingsScreen.test.tsx</files>

  <behavior>
    - REFEREE-01: with the Advanced drawer open, the `Referee Leniency` checkbox is present and `checked === false` on first render.
    - REFEREE-01/D-04: the stepper (`getByRole('spinbutton', { name: 'Referee Leniency value' })`) is present on first render (always mounted, never conditionally rendered) and `disabled === true` while the override is off.
    - REFEREE-02/D-01: the stepper's `value` is `'4'` and it becomes enabled after clicking the override checkbox.
    - REFEREE-02/D-02/D-03: the stepper's `min` attribute is `'2'` and its `max` attribute is `'5'`.
    - REFEREE-02 clamp: typing a value above 5 (e.g. `9`) leaves state at 5, and typing below 2 (e.g. `1`) leaves state at 2 — asserted through the confirmed payload, not just the DOM.
    - REFEREE-04: the text `(also affects added time)` renders inside the drawer.
    - Confirm pass-through, override off: `onConfirm` receives `refereeLeniencyOverride: false, refereeLeniencyValue: 4`.
    - Confirm pass-through, override on with a changed value: `onConfirm` receives `refereeLeniencyOverride: true` and the selected value.
    - Independence: unchecking Fouls does not disable or alter the Referee Leniency row (it is not routed through `deriveFoulDependents`).
  </behavior>

<read_first> - `packages/client/src/components/GameSettingsScreen.test.tsx` post-44-02 — the `openAdvanced()` helper, and every `expect(onConfirm).toHaveBeenCalledWith({...})` exact-object assertion (there were nine before this phase; 44-02 must not have removed any). - `packages/client/src/components/GameSettingsScreen.tsx` post-Task-1/2 — the exact `aria-label` string and label text for the query selectors.
</read_first>

  <action>
    Every existing `expect(onConfirm).toHaveBeenCalledWith({...})` exact-object assertion now fails, because the payload gained two properties. Add `refereeLeniencyOverride: false,` and `refereeLeniencyValue: 4,` to **each** of them. Do not weaken any of these to `expect.objectContaining(...)` to make them pass — the exact-object shape is what proves no field silently disappeared, and it caught this change correctly.

    Add a new describe block `GameSettingsScreen — Referee Leniency override (REFEREE-01/02/04, Phase 44)` covering every case in `<behavior>`. Call the existing `openAdvanced()` helper after `render(...)` in each test.

    Query the stepper as `screen.getByRole<HTMLInputElement>('spinbutton', { name: 'Referee Leniency value' })` — `spinbutton` is the ARIA role of `<input type="number">`. Query the checkbox as `screen.getByRole<HTMLInputElement>('checkbox', { name: 'Referee Leniency' })`; use the exact string, not a regex, so it cannot accidentally match the stepper's `aria-label`.

    For the clamp tests, use `userEvent.clear(input)` then `userEvent.type(input, '9')`, then click Confirm and assert the payload carries `refereeLeniencyValue: 5`. Asserting through the payload rather than the DOM value is the meaningful check — the payload is what reaches the server. Enable the override first, since the input is disabled while it is off and `userEvent` will refuse to type into a disabled control.

    For the independence test, uncheck Fouls and assert the Referee Leniency checkbox's `disabled === false` and that `(requires Fouls)` renders exactly twice (Booking and Injury only, never the Leniency row).

  </action>

<acceptance_criteria> - Test command: `pnpm --filter @counter-attack/client test -- GameSettingsScreen.test.tsx` exits 0. - Source assertion: `grep -c "refereeLeniencyOverride: false" packages/client/src/components/GameSettingsScreen.test.tsx` returns at least `9` — every pre-existing exact-payload assertion was extended, none deleted. - Source assertion: `grep -c "expect.objectContaining" packages/client/src/components/GameSettingsScreen.test.tsx` returns `0` — no assertion was loosened. - Behavior assertion: temporarily changing the stepper's initial state from `4` to `3` in `GameSettingsScreen.tsx` fails at least 9 tests. Revert immediately after checking. - Test command: `pnpm --filter @counter-attack/client test` exits 0 (full client suite, including `App.test.tsx`). - Test command: `pnpm lint` exits 0.
</acceptance_criteria>

  <verify>
    <automated>pnpm --filter @counter-attack/client test &amp;&amp; pnpm --filter @counter-attack/client typecheck &amp;&amp; pnpm lint</automated>
  </verify>

<done>New tests pin default-off, always-mounted-but-disabled stepper, default 4, min/max 2–5, both clamp directions, the added-time note, payload pass-through, and Fouls-independence; all nine pre-existing exact-payload assertions were extended rather than loosened; the full client suite is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                                                                                                                                       |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| host user → `<input type="number">` value      | The first hop of the new untrusted-input path. Client-side `min`/`max` and the JS clamp are UX affordances only, never security controls — a host can bypass both with DevTools or a hand-crafted socket payload. |
| `GameSettingsScreen` → `onConfirm` → `App.tsx` | In-process; no validation boundary. The real boundary is `roomHandlers.ts`, hardened in plan 44-04.                                                                                                               |

## STRIDE Threat Register

| Threat ID | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --------- | ---------------------- | --------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-44-09   | Tampering              | Referee Leniency stepper value                | transfer    | Client-side `min={2} max={5}` and the `Math.min(5, Math.max(2, parsed))` clamp are deliberately treated as non-authoritative. Task 1's handler doc-comment records this explicitly so no future reader mistakes them for enforcement. Authoritative allow-list validation lands in plan 44-04 (threats T-44-04/T-44-05) and rejects out-of-range and non-integer values before any room mutation.                                           |
| T-44-10   | Denial of Service      | `handleRefereeLeniencyValueChange` `NaN` path | mitigate    | A cleared or non-numeric input yields `NaN` from `Number.parseInt`. Propagating `NaN` into state would render `value={NaN}` (a React warning plus an uncontrolled-input switch) and could emit `refereeLeniencyValue: NaN` over the socket, which `JSON.stringify` serialises as `null` and which would fail the server's `Number.isInteger` guard, blocking match start. The handler returns early on `NaN`, keeping the last valid value. |
| T-44-11   | Elevation of Privilege | override toggle availability                  | accept      | Only the host (socket slot 1) ever renders `GameSettingsScreen`, and `roomHandlers.ts` line 451 independently rejects `ROOM_SETTINGS_CONFIRM` from any non-slot-1 socket with `WRONG_TURN`. Leniency inherits that existing, unchanged guard; no new privilege surface.                                                                                                                                                                     |
| T-44-SC   | Tampering              | npm/pnpm installs                             | accept      | This plan installs no packages and adds no dependencies. Package Legitimacy Gate is not applicable.                                                                                                                                                                                                                                                                                                                                         |

</threat_model>

<verification>
1. `pnpm --filter @counter-attack/client typecheck` exits 0.
2. `pnpm --filter @counter-attack/server typecheck` exits 0 (proves no wire contract was touched).
3. `pnpm --filter @counter-attack/client test` exits 0 (full client suite).
4. `pnpm stylelint` exits 0.
5. `pnpm lint` exits 0.
6. `git diff --name-only` lists exactly the three files in `files_modified` — `App.tsx`, `events.ts`, and all server files are untouched.
</verification>

<success_criteria>

- The Referee Leniency row renders in the Advanced drawer's right column between Out-of-Bounds and Tackle/Steal Decline (D-05/D-07).
- The override checkbox defaults unchecked (REFEREE-01).
- The stepper is always mounted, shows `4`, is `disabled` while the override is off, and constrains input to integers 2–5 in both directions (REFEREE-02, D-01/D-02/D-03/D-04).
- `(also affects added time)` renders next to the stepper (REFEREE-04, D-08).
- `onConfirm` carries `refereeLeniencyOverride` and `refereeLeniencyValue` as raw pass-throughs.
- Client typecheck, server typecheck, full client suite, stylelint, and lint are all green with `App.tsx` unmodified.
  </success_criteria>

<output>
Create `.planning/phases/44-referee-leniency-advanced-settings-drawer/44-03-SUMMARY.md` when done.
</output>
