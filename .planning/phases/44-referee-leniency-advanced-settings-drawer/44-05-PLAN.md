---
phase: 44-referee-leniency-advanced-settings-drawer
plan: 05
type: execute
wave: 4
depends_on: ['44-04']
files_modified:
  - packages/server/src/__tests__/refereeLeniency.integration.test.ts
autonomous: false
requirements:
  [REFEREE-01, REFEREE-02, REFEREE-03, REFEREE-04, SETTINGS-05, SETTINGS-06, SETTINGS-07]
user_setup: []

must_haves:
  truths:
    - 'A host who enables the override and picks a value starts a match whose refereeCard.leniency equals exactly that value'
    - 'A host who leaves the override off starts a match whose refereeCard.leniency is a random integer 2..5'
    - 'The overridden value is the same number the booking threshold and the added-time calculation both consume'
    - 'A human confirms the Advanced drawer is collapsed by default, reads as two logical columns, and greys the stepper correctly'
  artifacts:
    - path: 'packages/server/src/__tests__/refereeLeniency.integration.test.ts'
      provides: "socket-level end-to-end proof that the host's override reaches GameState"
      min_lines: 120
  key_links:
    - from: 'ROOM_SETTINGS_CONFIRM payload over a real socket'
      to: 'broadcast GameState.refereeCard.leniency'
      via: 'roomHandlers store -> LINEUP_CONFIRM -> buildInitialGameState'
      pattern: "refereeCard\\.leniency"
---

<objective>
Prove the whole six-hop chain end to end over a real socket, then put the finished UI in front of the developer.

Plans 44-01 through 44-04 each verified their own hop in isolation: the engine conditional, the disclosure layout, the stepper row, and the validate/store/broadcast plumbing. Nothing has yet asserted that a host clicking a number in a browser produces a match with that Leniency. Task 1 closes that gap at the socket layer; Task 2 closes it at the human layer and collects the one open UI question CONTEXT.md flagged for flag-back.

Purpose: phase acceptance for all seven requirements. This plan writes no production code — if a task here fails, the fix belongs in the plan that owns the offending file, not here.

Output: one integration test file and a signed-off human verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-referee-leniency-advanced-settings-drawer/44-CONTEXT.md
@.planning/phases/44-referee-leniency-advanced-settings-drawer/44-01-SUMMARY.md
@.planning/phases/44-referee-leniency-advanced-settings-drawer/44-02-SUMMARY.md
@.planning/phases/44-referee-leniency-advanced-settings-drawer/44-03-SUMMARY.md
@.planning/phases/44-referee-leniency-advanced-settings-drawer/44-04-SUMMARY.md
</context>

## Artifacts this phase produces

Produced by **this plan** (44-05):

| Symbol                                                              | Kind              | Location                 |
| ------------------------------------------------------------------- | ----------------- | ------------------------ |
| `packages/server/src/__tests__/refereeLeniency.integration.test.ts` | new file          | server integration suite |
| `setupMatchWithLeniency(override, value)`                           | local test helper | same file                |

No production symbols. Every symbol this phase ships is listed in the "Artifacts this phase produces" tables of plans 44-01 through 44-04.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Socket-level end-to-end Referee Leniency integration test</name>

<files>packages/server/src/**tests**/refereeLeniency.integration.test.ts</files>

  <behavior>
    - Override ON with value 2, and again with value 5: the first broadcast `GameState` after `LINEUP_CONFIRM` has `refereeCard.leniency` exactly equal to the confirmed value (REFEREE-01/02).
    - Override OFF: `refereeCard.leniency` is an integer in 2..5 (REFEREE-03 end-to-end, proving the already-shipped random path survives the new plumbing).
    - Override ON with value 3: `getRoom(roomCode)!.refereeLeniencyOverrideEnabled === true` and `.refereeLeniencyValue === 3`, and the broadcast state's `refereeCard.leniency === 3` — Room storage and GameState agree.
    - Late joiner: after the host confirms with the override on, a second client joining receives `ROOM_SETTINGS_CONFIRMED` whose 9th and 10th positional args are the confirmed flag and value (covers threat T-44-14, the `ROOM_JOIN` replay emit site).
    - REFEREE-04 single-source coupling: with the override pinned to a known value, the broadcast `GameState` exposes that number only at `refereeCard.leniency` — assert no sibling `refereeLeniency*` key exists on the broadcast state, so booking and added time cannot read divergent values.
  </behavior>

<read_first> - `packages/server/src/__tests__/tackleStealPrompt.integration.test.ts` lines 80-230 — the closest structural analog: `createClient`, `oncePromise`, `waitForConnect`, `confirmRoomSettings(...)`, and `setupRoom(tackleStealDecline: boolean)`, which drives create → confirm settings → join → team pick → uniform confirm → `LINEUP_CONFIRM` and returns the first broadcast `GameState`. Clone this file's scaffolding shape (deliberately copied verbatim per file, per the convention documented in `testHelpers.ts` lines 4-12) rather than importing it. - `packages/server/src/__tests__/testHelpers.ts` — `confirmDefaultRoomSettings`, and its header comment explaining which scaffolding is shared vs. deliberately duplicated. - `packages/server/src/__tests__/room.integration.test.ts` lines 799-824 — the positional-destructure assertion idiom for `ROOM_SETTINGS_CONFIRMED`. - `packages/shared/src/events.ts` post-44-04 `ROOM_SETTINGS_CONFIRMED` — the exact positional order, so the destructure indices are right.
</read_first>

  <action>
    Create `packages/server/src/__tests__/refereeLeniency.integration.test.ts`. Header comment: `Phase 44 — REFEREE-01/02/03/04 end-to-end Leniency override integration tests. Covers the full six-hop chain: ROOM_SETTINGS_CONFIRM payload -> allow-list validation -> Room -> LINEUP_CONFIRM -> buildInitialGameState -> broadcast GameState.refereeCard.leniency.`

    Copy the server-lifecycle scaffolding (`buildServer`, `beforeAll`/`afterAll`, `clearAllRooms`, `createClient`, `oncePromise`, `waitForConnect`) verbatim from `tackleStealPrompt.integration.test.ts` — that file's per-file-self-contained convention is the established pattern here, and importing its internals would couple two suites.

    Write one local helper `setupMatchWithLeniency(refereeLeniencyOverride: boolean, refereeLeniencyValue: number)` modelled on `setupRoom`, returning `{ roomCode, clientA, clientB, state }` where `state` is the first broadcast `GameState` after both `LINEUP_CONFIRM` emits. Keep every other setting at the same defaults `setupRoom` uses so Leniency is the only variable.

    Cover every case in `<behavior>`. For the override-ON cases run each value at least 3 times with distinct rooms — a single pass could coincidentally match a random roll, and 3 passes at both range ends make that effectively impossible.

    For the late-joiner case, register the `ROOM_SETTINGS_CONFIRMED` listener on `clientB` **before** emitting `ROOM_JOIN`, and destructure positionally to index 8 and 9. This is the only automated coverage of the `ROOM_JOIN` replay emit site, which PATTERNS.md did not surface — call that out in a comment so the test is not later deleted as redundant with the `room.integration.test.ts` broadcast test (which only covers the confirm-time emit).

    Use per-test timeouts matching the neighbouring integration files (5000ms) and always disconnect both clients in a `finally` or `afterEach`, following the analog file. Do not modify any production file; if a test fails, report which plan owns the file rather than patching it here.

  </action>

<acceptance_criteria> - Test command: `pnpm --filter @counter-attack/server test -- refereeLeniency.integration.test.ts` exits 0 with at least 5 passing tests. - Behavior assertion: temporarily reverting `roomHandlers.ts`'s `buildInitialGameState` call to drop the two trailing override args makes the override-ON tests fail while the override-OFF test still passes — the suite distinguishes the two paths. - Behavior assertion: temporarily reverting the `ROOM_JOIN` replay emit (dropping its two trailing args) makes the late-joiner test fail. - Source assertion: `grep -c "refereeCard.leniency" packages/server/src/__tests__/refereeLeniency.integration.test.ts` returns at least `4`. - Source assertion: `git diff --name-only` lists only `packages/server/src/__tests__/refereeLeniency.integration.test.ts` — no production file was touched. - Test command: `pnpm test` exits 0 (full monorepo suite). - Test command: `pnpm lint` exits 0.
</acceptance_criteria>

  <verify>
    <automated>pnpm --filter @counter-attack/server test -- refereeLeniency.integration.test.ts &amp;&amp; pnpm test &amp;&amp; pnpm lint</automated>
  </verify>

<done>The new integration file proves override-on exactness at both range ends, override-off randomness, Room/GameState agreement, late-joiner broadcast replay, and the single-Leniency-field coupling — all over a real socket, with no production file modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the Advanced drawer and Leniency override</name>

<files>none — verification only; this task creates and modifies no file</files>

<read_first> - `.planning/phases/44-referee-leniency-advanced-settings-drawer/44-CONTEXT.md` decisions D-01 through D-08 — the walkthrough below maps one-to-one onto them, and D-03 carries an explicit flag-back instruction. - `.planning/phases/44-referee-leniency-advanced-settings-drawer/44-02-SUMMARY.md` and `44-03-SUMMARY.md` — the exact copy strings and layout the executors shipped, so the walkthrough describes what is actually on screen rather than what was planned.
</read_first>

  <action>
    Run the full automated gate first — `pnpm typecheck`, `pnpm test`, `pnpm lint`, `pnpm stylelint`, `pnpm knip`, `pnpm build` — and confirm all six exit 0. Do NOT present the checkpoint if any is red: a human should never be asked to eyeball a build already known to be broken. Fix or report first.

    Then start the dev stack and present the numbered walkthrough in `<how-to-verify>` verbatim. Wait for the developer's response. Do not self-approve; `workflow.auto_advance` does not apply to this checkpoint.

    Record the developer's verbatim answer to the D-03 flag-back question (step 16) in `44-05-SUMMARY.md`. If they ask for custom +/- buttons with per-direction disabled styling, that is a scope change — log it as a Phase 46 follow-up, do not implement it here. Route reported defects to the owning plan's files: steps 3-9 → plan 44-02, steps 10-16 → plan 44-03, steps 17-20 → plan 44-04.

  </action>

  <what-built>
    Everything in Phase 44 is now automated-green:
    - `GameSettingsScreen`'s five original match-rule toggles moved from an always-visible "Match Rules" stack into a collapsed-by-default `Advanced ▸` disclosure (SETTINGS-05), laid out as a two-column grid — Fouls/Booking/Injury left, Out-of-Bounds/Referee Leniency/Tackle-Steal-Decline right (SETTINGS-06).
    - The Fouls→Booking/Injury grey-out now runs through a single `deriveFoulDependents` derivation used at render time and confirm time (SETTINGS-07).
    - A new Referee Leniency row: an override checkbox defaulting off (REFEREE-01), an always-visible native number stepper showing 4, disabled and greyed while the override is off, constrained to integers 2–5 (REFEREE-02), and an inline `(also affects added time)` note (REFEREE-04).
    - The value travels client → socket → server allow-list validation → Room → `buildInitialGameState`, replacing the random 2–5 roll only when the override is on; REFEREE-03's random path is untouched and regression-pinned.
  </what-built>

  <how-to-verify>
    Start the stack in two terminals:
    1. `pnpm --filter @counter-attack/server dev`
    2. `pnpm --filter @counter-attack/client dev` — open the URL Vite prints (default `http://localhost:5173`).

    **A. Collapsed-by-default disclosure (SETTINGS-05, D-06)**
    3. Click **Create Room**. On the Game Settings screen, confirm you see an `Advanced ▸` text link with a chevron — underlined, secondary-text colour, matching the `← Back` link's weight — and that **no** match-rule checkboxes are visible.
    4. Confirm the link reads as a link, not a full-width bordered bar (D-06 explicitly rejected the bordered-bar treatment).

    **B. Two-column layout (SETTINGS-06, D-07)**
    5. Click `Advanced`. Confirm the chevron flips to `▾` and six rows appear in two columns: **left** = Fouls, Booking, Injury; **right** = Out-of-Bounds / Restarts, Referee Leniency, Tackle/Steal Decline Prompt.
    6. Check the long labels ("Out-of-Bounds / Restarts", "Tackle/Steal Decline Prompt") inside the ~480px card. If wrapping looks cramped or misaligned, say so — this is the layout's main visual risk.

    **C. Fouls dependency inside the new layout (SETTINGS-07)**
    7. Uncheck **Fouls**. Confirm Booking and Injury both grey out, become unclickable, and each show `(requires Fouls)`.
    8. Confirm the **Referee Leniency** row is completely unaffected — its checkbox stays interactive and shows no `(requires Fouls)` text.
    9. Re-check Fouls; confirm Booking/Injury return to normal.

    **D. Referee Leniency row (REFEREE-01/02/04, D-01..D-05, D-08)**
    10. Confirm the Referee Leniency checkbox is **unchecked** by default and the number stepper next to it is **visible but greyed out**, showing `4`.
    11. Confirm the note `(also affects added time)` renders near the stepper in small secondary text.
    12. Check the override checkbox. Confirm the stepper becomes editable and still shows `4`.
    13. Click the stepper's **up** arrow repeatedly. Confirm it stops at **5** and will not go to 6.
    14. Click the **down** arrow repeatedly. Confirm it stops at **2** and will not go to 1.
    15. Select the field and type `9`, then click elsewhere. Confirm it snaps back to a value within 2–5 rather than accepting 9.

    **E. FLAG-BACK QUESTION (D-03) — please answer explicitly**
    16. CONTEXT.md D-03 chose a native `<input type="number">` over custom +/- buttons and noted that native inputs **cannot** grey out an individual arrow at the bound the way a custom control could — the spinner simply stops responding. Look at the stepper sitting at 5 and at 2. **Is the native "just stops" behaviour acceptable, or do you want custom +/- buttons with per-direction disabled styling?** If you want custom buttons, say so — that is a scope change requiring a new plan, not a silent swap.

    **F. End-to-end effect (REFEREE-02/04)**
    17. With the override **on** and set to **5**, and with **Fouls + Booking on**, confirm settings, join from a second browser (or an incognito window) with the room code, and play through team/uniform/lineup into a live match.
    18. Trigger a tackle or steal until a foul with a booking roll occurs. Confirm bookings resolve without error (the exact card outcome is dice-dependent — you are checking that nothing breaks, not a specific card).
    19. Play until the clock passes 45 and added time is announced. Confirm the announced added time is consistent with a Leniency of 5 (it is `roll + 5 + substitution bonus`), not a lower random value.
    20. Start a **second** match with the override left **off**. Confirm play still works normally — this is REFEREE-03's already-shipped random 2–5 roll, which must be unchanged.

  </how-to-verify>

<acceptance_criteria> - CLI: `pnpm typecheck`, `pnpm test`, `pnpm lint`, `pnpm stylelint`, `pnpm knip`, and `pnpm build` each exit 0 before the checkpoint is presented. - Behavior assertion: the developer has responded with either "approved" or a specific defect list. - Behavior assertion: the developer's verbatim answer to the D-03 flag-back question (step 16) appears in `44-05-SUMMARY.md`. - Source assertion: `git status --porcelain` shows no file modification attributable to this task.
</acceptance_criteria>

  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm lint &amp;&amp; pnpm stylelint &amp;&amp; pnpm knip &amp;&amp; pnpm build</automated>
    <human-check>Developer completes the 20-step walkthrough, replies "approved" or lists defects, and explicitly answers the D-03 native-vs-custom-stepper question in step 16.</human-check>
  </verify>

<done>All six automated gates pass, the developer has completed the walkthrough and replied, and their D-03 answer is recorded verbatim in the summary.</done>

<resume-signal>Type "approved" to close Phase 44, or describe any issues — and please answer the D-03 flag-back question in step 16 either way.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                  | Description                                                                        |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| socket client → `roomHandlers.ts` `ROOM_SETTINGS_CONFIRM` | Exercised, not modified, by this plan. Hardened in plan 44-04 (T-44-04 / T-44-05). |
| developer → running application                           | Manual verification only; no code path.                                            |

## STRIDE Threat Register

| Threat ID | Category    | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------- | ----------- | --------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-44-15   | Tampering   | end-to-end override chain across all six hops | mitigate    | Every individual hop was verified in isolation by plans 44-01..44-04, which leaves a real gap: a correct-looking hop can still be mis-wired to its neighbour (e.g. the engine args appended in the wrong order, silently shifting `confirmedAwayBench`). Task 1 asserts the observable end state — the broadcast `GameState.refereeCard.leniency` — over a real socket, which is the only assertion that can catch a cross-hop wiring error. |
| T-44-14   | Tampering   | `ROOM_JOIN` late-joiner settings replay       | mitigate    | Task 1's late-joiner test is the only automated coverage of the second `ROOM_SETTINGS_CONFIRMED` emit site, which PATTERNS.md did not surface. Without it, a truncated replay would ship silently because the confirm-time emit is separately tested and would pass.                                                                                                                                                                         |
| T-44-16   | Repudiation | manual verification result                    | mitigate    | The checkpoint enumerates numbered, individually-answerable steps with explicit expected outcomes rather than "check it looks right", so the recorded approval maps to specific observed behaviour. The D-03 flag-back question is called out as requiring an explicit answer so a known-imperfect affordance cannot be implicitly accepted.                                                                                                 |
| T-44-SC   | Tampering   | npm/pnpm installs                             | accept      | This plan installs no packages and adds no dependencies. Package Legitimacy Gate is not applicable.                                                                                                                                                                                                                                                                                                                                          |

</threat_model>

<verification>
1. `pnpm typecheck` exits 0 across all three packages.
2. `pnpm test` exits 0 across the full monorepo.
3. `pnpm lint` exits 0.
4. `pnpm stylelint` exits 0.
5. `pnpm knip` exits 0.
6. `pnpm build` exits 0 (proves the client production bundle and both TS builds still compile).
7. Human checkpoint approved, including an explicit answer to the D-03 flag-back question.
</verification>

<success_criteria>

- A host-selected Leniency of 2 or 5 produces a match with exactly that `refereeCard.leniency`, proven over a real socket (REFEREE-01/02).
- The override left off produces a random integer 2..5 (REFEREE-03, unchanged).
- The overridden value is the single number both the booking threshold and the added-time calculation read (REFEREE-04).
- A late-joining player receives the confirmed override flag and value on the `ROOM_JOIN` settings replay.
- A human has confirmed the collapsed-by-default Advanced disclosure (SETTINGS-05), the two logical columns (SETTINGS-06), the Fouls grey-out inside the new layout (SETTINGS-07), the default-off override with a greyed always-visible 2–5 stepper defaulting to 4, and the added-time coupling note.
- The D-03 native-vs-custom-stepper question has an explicit developer answer recorded in the summary.
- Full monorepo typecheck, test, lint, stylelint, knip, and build are green.
  </success_criteria>

<output>
Create `.planning/phases/44-referee-leniency-advanced-settings-drawer/44-05-SUMMARY.md` when done. Record the developer's answer to the D-03 flag-back question verbatim in the summary — if they asked for custom +/- buttons, note it as a follow-up for Phase 46 rather than acting on it here.
</output>
