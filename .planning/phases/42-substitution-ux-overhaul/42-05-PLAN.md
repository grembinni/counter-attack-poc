---
phase: 42-substitution-ux-overhaul
plan: 05
type: execute
wave: 3
depends_on: ['42-01', '42-03']
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.redCardExclusion.test.ts
autonomous: true
requirements: [BUG-38]

must_haves:
  truths:
    - "A red-carded player's frozen hex never blocks a restart placement, a reposition destination, or a kick-off setup move."
    - 'The whole-file grep audit mandated by D-10 item 4 is complete for gameEngine.ts, with every skip documented.'
    - 'A red-carded piece cannot be produced anywhere in gameEngine.ts by a filter that forgot the flag.'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'red-card-aware occupancy across the whole file'
      contains: 'isActivePiece'
    - path: 'packages/server/src/__tests__/gameEngine.redCardExclusion.test.ts'
      provides: 'BUG-38 engine-level regression coverage for occupancy and eligibility'
      min_lines: 60
  key_links:
    - from: 'packages/server/src/gameEngine.ts'
      to: '@counter-attack/shared isActivePiece'
      via: 'named import added in 42-03'
      pattern: 'isActivePiece'
---

<objective>
Finish the BUG-38 occupancy sweep across the second half of `packages/server/src/gameEngine.ts` (lines ~5103–9315) and close out D-10 item 4's grep audit with an engine-level regression test file.

Purpose: 42-03 covered the file's first half. BUG-38's acceptance text is unconditional — "their frozen position must stop blocking movement/occupancy and stop appearing in any gameplay computation ... or any other pitch-occupancy check, plus any other site an audit finds". The restart/reposition apply-functions in this half of the file (free kick, corner kick, goal kick, throw-in, penalty kick, kick-off setup) are exactly where a dismissed player's frozen hex is most visible to a manager: a placement silently rejected as OCCUPIED with nothing rendered on that hex.

Output: every remaining pitch-occupancy predicate in `gameEngine.ts` red-card aware, a documented audit table covering the whole file, and a dedicated engine-level regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md
@.planning/phases/42-substitution-ux-overhaul/42-03-SUMMARY.md
@.planning/phases/42-substitution-ux-overhaul/42-01-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `42-01-PLAN.md` under "Artifacts this phase produces". This plan creates no new exported symbol; it consumes `isActivePiece` from 42-01.

<tasks>

<task type="auto">
  <name>Task 1: Occupancy sweep, gameEngine.ts lines 5103-6731</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts around each of these lines, reading the enclosing function before editing: 5103, 5528, 5843, 6077, 6415, 6731
    - packages/server/src/gameEngine.ts lines 5735-5760 and 6320-6340 (the corner-kick-taker and goal-kick eligible-ids filters already retrofitted in 42-03 — do not re-edit them; read only to avoid double-applying)
    - .planning/phases/42-substitution-ux-overhaul/42-03-SUMMARY.md (the classification table and the block-comment wording established for this sweep, which this plan must match)
  </read_first>
  <action>
Apply the same classification rule 42-03 established. For each of the six listed lines, read the enclosing function and classify the predicate as OCCUPANCY (answers "is a piece physically standing on this hex") or CONSTRUCTION (operates on a freshly-built array that cannot contain a dismissed piece).

For OCCUPANCY sites, add `isActivePiece(p) &&` as a leading conjunct, preserving every existing clause (`p.id !== pieceId`, `p.teamId === ...`, role filters) exactly. Line 5103 uses `.find(...)` to resolve the occupant of a hex rather than `.some(...)` — apply the conjunct there too, so the resolved occupant is never a dismissed piece.

For CONSTRUCTION sites (if any), leave unchanged and add a one-line audit comment naming BUG-38 plus the reason, exactly as 42-03 did for `buildSquadPieces`.

Do not touch `applyRosterContinuity`, `computeCornerKickEligibleIds`, `computeGoalKickEligibleIds`, or `computePenaltyKickEligibleIds` — those were retrofitted in 42-03 and re-editing them risks double-applying the predicate.

Record every site's classification and enclosing function name for the summary's audit table.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- --pool=forks</automated>
</verify>
<acceptance_criteria> - Each OCCUPANCY site in {5103, 5528, 5843, 6077, 6415, 6731} (located by enclosing function name) contains `isActivePiece`; each CONSTRUCTION site carries a BUG-38 audit comment. - `grep -c "isActivePiece" packages/server/src/gameEngine.ts` increased by at least 5 relative to the count at the end of 42-03. - `git diff packages/server/src/gameEngine.ts | grep -c "applyRosterContinuity"` returns 0. - `pnpm --filter @counter-attack/server test -- --pool=forks` passes the full suite with zero failures.
</acceptance_criteria>
<done>Restart and reposition occupancy checks in the 5103-6731 range no longer treat a dismissed piece's frozen hex as blocked.</done>
</task>

<task type="auto">
  <name>Task 2: Occupancy sweep, gameEngine.ts lines 7225-9315, and whole-file audit closeout</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts around each of these lines, reading the enclosing function before editing: 7225, 7422, 7885, 8326, 8747, 9064, 9126, 9315
    - packages/server/src/gameEngine.ts lines 9460-9545 (`REPLAY_ELIGIBLE_TYPES` and the replay-reconstruction comments — 9064/9126/9315 sit near replay/free-kick logic; read enough to keep the edits clear of the replay frame builder, which reconstructs from the starting XI and must not gain a red-card filter)
    - .planning/phases/42-substitution-ux-overhaul/42-03-SUMMARY.md (classification table format to continue)
  </read_first>
  <action>
Apply the same OCCUPANCY/CONSTRUCTION classification and conjunct treatment to the eight listed lines.

Special care at the replay boundary: `buildReplayFrames` reconstructs the match from the starting XI forward and deliberately does NOT apply `applyRosterContinuity` (documented at ~9635). Any predicate INSIDE the replay frame builder operates on reconstructed pieces that carry no live `redCarded` state — classify those as CONSTRUCTION and leave them unchanged with an audit comment, rather than adding a filter that would diverge replay from live behavior.

Then close out D-10 item 4's audit for this file. Run a whole-file sweep for any remaining predicate that builds a list of pieces or tests hex occupancy without the shared predicate:
`grep -n "pieces\.\(some\|find\|filter\)" packages/server/src/gameEngine.ts`
Classify every hit into one of: (a) already red-card aware via `isActivePiece`, (b) an id-based lookup (`p.id === someId`) where the flag is irrelevant because the caller has already validated the id, (c) CONSTRUCTION, or (d) a genuine gap. Fix every (d). Produce the full classification as a table in the summary — this table IS the deliverable for D-10 item 4, and Success Criterion 5's "plus any other site an audit finds" is satisfied by it.

If the audit finds a genuine gap outside the occupancy line list (for example an eligibility or defender-list filter not previously identified), fix it in this task and call it out prominently in the summary as an audit find.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- --pool=forks && pnpm --filter @counter-attack/server typecheck</automated>
</verify>
<acceptance_criteria> - Each OCCUPANCY site in {7225, 7422, 7885, 8326, 8747, 9064, 9126, 9315} (located by enclosing function name) contains `isActivePiece`; each CONSTRUCTION site carries a BUG-38 audit comment. - `grep -vn "^\s*[*/]" packages/server/src/gameEngine.ts | grep "p\.position\.q ===" | grep -vc "isActivePiece"` returns a number equal to the count of sites classified CONSTRUCTION in the summary table (i.e. every non-CONSTRUCTION positional predicate is narrowed). - The summary contains a complete classification table for every `pieces.some|find|filter` hit in the file, each row labelled (a)/(b)/(c)/(d). - `pnpm --filter @counter-attack/server test -- --pool=forks` passes the full suite with zero failures. - `npx eslint packages/server/src/gameEngine.ts` and `npx prettier --check packages/server/src/gameEngine.ts` both exit 0.
</acceptance_criteria>
<done>`gameEngine.ts` has no un-audited piece-list or occupancy predicate, and the audit table is written down.</done>
</task>

<task type="auto">
  <name>Task 3: Engine-level BUG-38 regression tests</name>
  <files>packages/server/src/__tests__/gameEngine.redCardExclusion.test.ts</files>
  <read_first>
    - packages/server/src/__tests__/gameEngine.cornerKick.test.ts and packages/server/src/__tests__/gameEngine.outOfBounds.test.ts (these already contain red-card exclusion tests added by the `red-card-bench-removal-scope` debug session — copy their fixture-construction style and assertion shape, and do not duplicate their coverage)
    - packages/server/src/__tests__/testHelpers.ts (shared state/piece fixture builders)
    - packages/server/src/gameEngine.ts (the occupancy sites edited in Tasks 1-2, to pick apply-functions that are directly callable from a unit test)
    - .planning/phases/42-substitution-ux-overhaul/42-03-SUMMARY.md and this plan's Task 2 audit table (choose test targets that cover the highest-risk sites found)
  </read_first>
  <action>
Create `packages/server/src/__tests__/gameEngine.redCardExclusion.test.ts` with a `describe('BUG-38 — red-carded pieces excluded from gameEngine occupancy and eligibility', ...)` block.

Cover at minimum:

1. `applyMove` rejection retrofit: moving a piece with `redCarded: true` still returns `{ ok: false, reason: 'MOVE_INVALID', detail: 'RED_CARDED' }` — proving 42-03's retrofit preserved the exact return shape.
2. `applyMove` occupancy: a destination hex occupied ONLY by a red-carded piece is now a legal move destination; the same hex occupied by a live piece still returns the occupancy rejection.
3. At least three restart/reposition apply-functions chosen from the occupancy sites narrowed in Tasks 1-2 (pick ones with direct unit-test reachability, e.g. free-kick setup placement, corner-kick reposition, kick-off setup placement): a red-carded piece on the target hex no longer blocks the placement; a live piece on the same hex still does.
4. Two-clause proof: one case where the blocking piece has `onPitch: false` but `redCarded` unset, asserting the same exclusion.
5. Eligibility retrofit spot-checks: `computePenaltyKickEligibleIds` and the corner-kick-taker filter still exclude a red-carded piece after the `isActivePiece` retrofit (guard against a retrofit that accidentally inverted the predicate).

Every dismissed-piece fixture must set BOTH `redCarded: true` and `onPitch: false` except case 4, which sets only `onPitch: false`.

Do not duplicate assertions that already exist in `gameEngine.cornerKick.test.ts`, `gameEngine.outOfBounds.test.ts`, `gameEngine.phase17.test.ts` or `gameEngine.booking.test.ts` — reference them in a comment instead.
</action>
<verify>
<automated>pnpm --filter @counter-attack/server test -- --pool=forks gameEngine.redCardExclusion</automated>
</verify>
<acceptance_criteria> - `packages/server/src/__tests__/gameEngine.redCardExclusion.test.ts` exists and runs >= 10 passing assertions with zero failures. - Every negative case (exclusion) has a paired positive control (live piece still blocks/is still eligible) in the same `describe`. - `pnpm --filter @counter-attack/server test -- --pool=forks` (whole suite) exits 0. - `pnpm -r typecheck` exits 0.
</acceptance_criteria>
<done>Engine-level red-card exclusion is proven by tests for movement, occupancy and eligibility, each with a positive control.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                                                     |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| handler → `gameEngine` apply-functions | The engine re-validates everything the handler already checked; it is the last line of defence before state mutation.           |
| live state → replay reconstruction     | `buildReplayFrames` operates on reconstructed starting-XI pieces, not live roster state — a boundary this sweep must not cross. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                  | Disposition | Mitigation Plan                                                                                                                                                                              |
| --------- | ---------------------- | ---------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-15   | Tampering              | Occupancy predicates in restart/reposition apply-functions | mitigate    | Conjunct-only edits with the full pre-existing suite required green; positive controls in Task 3 assert live pieces still block, so a narrowing cannot silently become a removal.            |
| T-42-16   | Tampering              | `buildReplayFrames` reconstruction path                    | mitigate    | Adding a red-card filter inside the replay builder would diverge replay from the live match. Explicitly classified CONSTRUCTION and left unchanged; the audit table must show this decision. |
| T-42-17   | Elevation of Privilege | `applyMove` red-card rejection after retrofit              | mitigate    | Task 3 case 1 asserts the exact `MOVE_INVALID`/`RED_CARDED` return shape survives, so a retrofit that inverted the predicate fails loudly.                                                   |
| T-42-18   | Repudiation            | Incomplete audit passed off as complete                    | mitigate    | D-10 item 4 requires a written audit. The acceptance criteria demand a full classification table for every `pieces.some\|find\|filter` hit, labelled per row, published in the summary.      |
| T-42-SC   | Tampering              | npm/pnpm installs                                          | accept      | No packages installed by this plan. No Package Legitimacy Gate applies.                                                                                                                      |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test -- --pool=forks` — full server suite green.
- `pnpm -r typecheck` — clean.
- `git diff packages/server/src/gameEngine.ts | grep -c "applyRosterContinuity"` returns 0.
- `npx eslint packages/server/src/gameEngine.ts packages/server/src/__tests__/gameEngine.redCardExclusion.test.ts` — clean.
- Whole-repo residual check: `grep -rvn "^\s*[*/]" --include=*.ts packages/server/src packages/shared/src | grep "redCarded !== true\|redCarded === true" | grep -v "\.test\.ts"` returns no live predicate outside `stoppagePhases.ts`.
</verification>

<success_criteria>

- Every pitch-occupancy predicate in `gameEngine.ts` is red-card aware or documented as CONSTRUCTION.
- A complete D-10-item-4 audit table for `gameEngine.ts` exists in the summary, with every `pieces.some|find|filter` hit classified.
- Any additional gap the audit finds is fixed in this plan and called out.
- Engine-level regression tests cover movement rejection, occupancy, three restart placements, the two-clause predicate, and two eligibility retrofits — each with a positive control.
  </success_criteria>

<output>
Create `.planning/phases/42-substitution-ux-overhaul/42-05-SUMMARY.md` when done. Record: the classification table for lines 5103-9315, the whole-file D-10-item-4 audit table, and any audit finds fixed outside the pre-identified line list.
</output>
