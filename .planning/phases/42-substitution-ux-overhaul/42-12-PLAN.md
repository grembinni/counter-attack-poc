---
phase: 42-substitution-ux-overhaul
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/GameBoard.module.css
  - packages/client/src/components/GameBoard.test.tsx
  - packages/client/src/components/LineupAssignmentScreen.tsx
  - packages/client/src/components/LineupAssignmentScreen.module.css
  - packages/client/src/components/LineupAssignmentScreen.test.tsx
autonomous: true
gap_closure: true
requirements: [SUB-11, SUB-12, SUB-16, SUB-17]

must_haves:
  truths:
    - 'The persistent side strip reads ROSTER, not SUB.'
    - 'When a substitution is actionable, the WHOLE side strip is solid green and the ROSTER label is white — there is no white strip, no small green patch, and no dark-green text.'
    - "The Resume button sits in the panel's own content flow directly under the bench/roster, not pinned to the bottom of the viewport."
    - 'The Substitute/Cancel button is the same standard button size as Resume and sits immediately next to it in one action row.'
    - "Both Cancel buttons (the substitution-mode Cancel and the confirm popup's Cancel) are orange."
    - "Closing the panel still works: a button named 'Resume match' with visible text 'Resume' dismisses the modal with no socket emit."
  artifacts:
    - path: 'packages/client/src/components/GameBoard.module.css'
      provides: 'solid-green editable strip; removed bottom-pinned Resume CTA row'
      contains: 'subButtonStripActive'
    - path: 'packages/client/src/components/LineupAssignmentScreen.module.css'
      provides: 'midmatchActionRow, rosterActionButton, rosterActionButtonCancel, resumeButton, subConfirmButtonCancel'
      contains: 'midmatchActionRow'
    - path: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      provides: 'onResume prop and the under-the-roster action row'
      contains: 'onResume'
  key_links:
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: 'LineupAssignmentScreen onResume'
      via: 'prop wired to setSubOpen(false)'
      pattern: 'onResume=\\{'
    - from: 'packages/client/src/components/LineupAssignmentScreen.tsx'
      to: '.midmatchActionRow'
      via: 'action row rendered after .benchSection'
      pattern: 'midmatchActionRow'
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: '.subButtonStripActive'
      via: 'actionable className composition on the outer strip'
      pattern: 'subButtonStripActive'
---

<objective>
Close gap items 2, 3, 4 and 5 from `42-10-SUMMARY.md` — the four chrome defects the live human verifier reported against the SUB-16/SUB-17 contract:

- item 2: the strip does not turn fully green with white text when subs are available (observed: white strip background, a small green patch behind the label, dark-green label text), and the label should read **ROSTER** instead of **SUB**.
- item 3: the Substitute button should be standard button size and sit next to the Resume button.
- item 4: the Resume button should sit under the roster, not pinned to the bottom of the screen.
- item 5: the Cancel button (substitution mode and the confirm popup) should be orange.

Purpose: 42-09 delivered SUB-16/SUB-17 against a translucent green token and a bottom-anchored CTA row that read wrong in live play. `--color-speed-standard-bg` is `rgba(34, 197, 94, 0.15)` — a 15%-opacity tint that shows the parent through instead of painting the strip green, which is exactly the reported "white strip with a small green patch". And `.resumeCtaRow` is the last flex child of `.substitutionModalCard`, OUTSIDE `LineupAssignmentScreen`'s own scrolling `.screen`, which is exactly why Resume reads as pinned to the bottom of the viewport rather than following the roster.

Output: a solid-green ROSTER strip with white text, and one standard-size action row (Substitute/Cancel + Resume) sitting in the panel's content flow directly under the bench.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md
@.planning/phases/42-substitution-ux-overhaul/42-UI-SPEC.md
@.planning/phases/42-substitution-ux-overhaul/42-PATTERNS.md
@.planning/phases/42-substitution-ux-overhaul/42-09-SUMMARY.md
@.planning/phases/42-substitution-ux-overhaul/42-10-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `42-01-PLAN.md` under "Artifacts this phase produces". This plan creates:

| Symbol                      | Kind             | File                                                               |
| --------------------------- | ---------------- | ------------------------------------------------------------------ |
| `onResume` prop             | component prop   | `packages/client/src/components/LineupAssignmentScreen.tsx`        |
| `.midmatchActionRow`        | CSS module class | `packages/client/src/components/LineupAssignmentScreen.module.css` |
| `.rosterActionButton`       | CSS module class | `packages/client/src/components/LineupAssignmentScreen.module.css` |
| `.rosterActionButtonCancel` | CSS module class | `packages/client/src/components/LineupAssignmentScreen.module.css` |
| `.resumeButton`             | CSS module class | `packages/client/src/components/LineupAssignmentScreen.module.css` |
| `.subConfirmButtonCancel`   | CSS module class | `packages/client/src/components/LineupAssignmentScreen.module.css` |

This plan **removes**: `.resumeCtaRow` and `.resumeCta` from `GameBoard.module.css` (created in 42-08/42-09), and their JSX from `GameBoard.tsx`.

## UI-SPEC deviations recorded (all sourced from the human verifier, `42-10-SUMMARY.md`)

| 42-UI-SPEC.md said                                                                   | This plan ships                                                              | Why                                                                                            |
| ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `.subButtonStrip` editable background = `--color-speed-standard-bg` (15% green tint) | `--color-cta-ready-bg` (#27ae60, solid) with `--color-text-inverse` label    | Verifier: "the whole SUB strip is expected to turn green with white text" (Section C, step 13) |
| Strip label copy "SUB"                                                               | "ROSTER"                                                                     | Verifier: explicit rename request bundled with the step-13 bug                                 |
| Substitution-mode entry button = small `.ctaButton` treatment (11px/700)             | standard CTA size, in a row beside Resume                                    | Verifier: "should be standard button size, positioned next to the Resume button" (Section B)   |
| Resume = full-width bottom CTA of the modal card                                     | standard-size button in the roster's own content flow, under the bench       | Verifier: "more neutrally under the roster" rather than pinned to the bottom (Section C, 14)   |
| Active-mode Cancel = "label swap only — no color change needed"                      | orange (`--color-cta-pending-bg`), both the mode Cancel and the popup Cancel | Verifier: "Cancel button ... should be styled orange" (Section B)                              |

No new colour literal and no new token is introduced — `--color-cta-ready-bg`, `--color-cta-ready-bg-hover`, `--color-cta-pending-bg`, `--color-cta-pending-bg-hover` and `--color-text-inverse` all already exist in `packages/client/src/styles/tokens.css`.

<tasks>

<task type="auto">
  <name>Task 1: ROSTER label + solid-green strip with white text (gap item 2)</name>
  <files>packages/client/src/components/GameBoard.tsx, packages/client/src/components/GameBoard.module.css, packages/client/src/components/GameBoard.test.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx lines 137-219 — `SideLog` (the structural template) and `SubstitutionButton`, including the `actionable` className composition on both the outer `.subButtonStrip` div (lines 196-202) and the inner `.sideLogChevron` button (lines 203-206), the `aria-label` / `title` ternaries, and `<span className={styles.subButtonLabel}>SUB</span>` at line 215.
    - packages/client/src/components/GameBoard.module.css lines 312-360 — `.sideLogCollapsed` and `.sideLogChevron` (which already declares `background: none; color: var(--color-text-secondary);` and a `:hover` colour) — the inner button inherits this base, which is why the inner button needs NO background of its own once the strip is solid.
    - packages/client/src/components/GameBoard.module.css lines 574-618 — `.subButtonStrip`, `.subButtonLabel`, `.subButtonActive` (+ `:hover`), `.subButtonStripActive`.
    - packages/client/src/styles/tokens.css lines 50-60 and 100-116 — confirm `--color-speed-standard-bg: rgba(34, 197, 94, 0.15)` (translucent, the root cause) versus `--color-cta-ready-bg: #27ae60` / `--color-cta-ready-bg-hover: #219a52` (solid) and `--color-text-inverse: #ffffff`.
    - packages/client/src/components/GameBoard.test.tsx lines 520-560 and 682-760 — the `substitution affordance (SUB-01/02)` and `roster panel server wiring` describes; specifically test 2 (`SUB-17: the SUB strip container carries the active class...`) which locates the button by `aria-label` and asserts `parentElement?.className` matches `/subButtonStripActive/`.
    - .planning/phases/42-substitution-ux-overhaul/42-10-SUMMARY.md § "Section C — Chrome, SUB-16/17 (steps 13-14)" — the verbatim step-13 bug description.
  </read_first>
  <action>
**A. Rename the visible label.** In `SubstitutionButton`, change `<span className={styles.subButtonLabel}>SUB</span>` to render `ROSTER`. Leave both `aria-label` values (`'Open substitutions'` / `'View roster'`) and both `title` strings EXACTLY as they are — existing `GameBoard.test.tsx` queries locate this button by `aria-label`, and changing them would break unrelated tests for no user-visible benefit.

**B. Make the strip solid green.** In `GameBoard.module.css`, change `.subButtonStripActive`'s declaration from `background: var(--color-speed-standard-bg);` to `background: var(--color-cta-ready-bg);`. Replace its comment with a note recording that the previous token is a 15%-alpha tint (`rgba(34, 197, 94, 0.15)`) that let the parent show through — the observed "white strip with a small green patch" in Phase 42's live verification — and that `--color-cta-ready-bg` is the app's existing solid green, the same token `.confirmButtonReady` already paints with.

**C. Make the label white and remove the inner green patch.** Change `.subButtonActive` to declare ONLY `color: var(--color-text-inverse);` — delete its `background: var(--color-speed-standard-bg);` declaration entirely. With no background declared, the inner button falls back to `.sideLogChevron`'s existing `background: none`, so the small green rectangle behind the label disappears and the solid strip shows through. Change `.subButtonActive:hover` from `color: var(--color-success-hover);` to `color: var(--color-text-inverse);` so the label stays white on hover instead of turning green.

**D. Keep the label legible at 11px on green.** In `.subButtonLabel`, change `font-weight: 400` to `font-weight: 700`. Add a comment recording that white-on-`--color-cta-ready-bg` measures below the 4.5:1 WCAG AA small-text threshold, that this is the app's pre-existing primary-CTA colour pairing (`.confirmButtonReady`/`.resumeCta` already ship it), and that the weight bump is the mitigation the verifier's explicit "green with white text" instruction allows. `700` is already a declared weight in `42-UI-SPEC.md`'s typography table — no new weight is introduced.

**E. Tests.** In `GameBoard.test.tsx`:

- Add an assertion to the existing SUB-17 test (or a new sibling test) that the strip's visible label text is `ROSTER`: locate the button by `aria-label`, then assert its `textContent` is `'ROSTER'` in BOTH the actionable and non-actionable states.
- Add an assertion that in the non-actionable state the inner button's `className` does NOT match `/subButtonActive/`, and in the actionable state it DOES — locking the two-class pairing (outer strip + inner label) that the verifier's report depends on.
- Grep the file first for any existing query on the literal string `'SUB'` used to find this control and retarget it to `'ROSTER'`; report in the SUMMARY whether any existed.
  </action>
  <verify>
  <automated>pnpm --filter @counter-attack/client test -- --pool=forks GameBoard</automated>
  </verify>
  <acceptance_criteria>
- `grep -c ">ROSTER<" packages/client/src/components/GameBoard.tsx` returns `1`.
- `grep -c "subButtonLabel}>SUB<" packages/client/src/components/GameBoard.tsx` returns `0`.
- `grep -A3 "^\.subButtonStripActive" packages/client/src/components/GameBoard.module.css | grep -c "var(--color-cta-ready-bg)"` returns `1`.
- `grep -A4 "^\.subButtonActive {" packages/client/src/components/GameBoard.module.css | grep -c "background"` returns `0` — the inner green patch declaration is gone.
- `grep -A4 "^\.subButtonActive {" packages/client/src/components/GameBoard.module.css | grep -c "var(--color-text-inverse)"` returns `1`.
- `grep -rc "color-speed-standard-bg" packages/client/src/components/GameBoard.module.css` returns `0`.
- `npx stylelint "packages/client/src/components/GameBoard.module.css"` exits 0.
- `pnpm --filter @counter-attack/client test -- --pool=forks GameBoard` passes, including the pre-existing SUB-17 `subButtonStripActive` test.
  </acceptance_criteria>
  <done>The persistent strip reads ROSTER, paints solid green when actionable, and shows a white label with no inner patch.</done>
  </task>

<task type="auto">
  <name>Task 2: Move Resume under the roster and pair it with a standard-size Substitute/Cancel button (gap items 3 and 4)</name>
  <files>packages/client/src/components/LineupAssignmentScreen.tsx, packages/client/src/components/LineupAssignmentScreen.module.css, packages/client/src/components/GameBoard.tsx, packages/client/src/components/GameBoard.module.css, packages/client/src/components/GameBoard.test.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx lines 58-116 (the `Props` type, ending at `actionPending`) and lines 1015-1167 (the ENTIRE `mode === 'midmatch'` return block: the `.screen` wrapper with its container-level `onDragEnd`, the heading, the helper `<p>`, the `.subCounterChip` span, the `subMode === 'reposition' ? Substitute : Cancel` button pair at lines 1050-1082, the `.slotCapNote`, `.formationColumns`, `.benchSection`, the `pendingSub` confirm popup, and the trailing `rejectionMessage`).
    - packages/client/src/components/LineupAssignmentScreen.module.css lines 9-19 (`.screen` — `height: 100%; overflow-y: auto; display:flex; flex-direction:column; align-items:center; gap:16px; padding:24px` — the flow the action row must join), lines 49-95 (`.confirmSection`, `.confirmButton`, `.confirmButtonReady` — the app's standard CTA geometry: 20px/800, `padding: 14px 48px`, `border-radius: 6px`, `min-width: 200px`, `1px solid var(--color-border-muted)`), lines 292-300 (`.benchSection` — `max-width: 1260px; width: 100%; border-top: 1px solid var(--color-border); padding-top: 16px`), lines 549-568 (`.subModeButton` and its `:hover`/`:disabled`), lines 634-651 (`.subConfirmActions`, `.subConfirmActions .subModeButton { flex: 1 }`, `.subConfirmButtonReady`).
    - packages/client/src/components/GameBoard.tsx lines 599-652 — the `subOpen` modal block: `.substitutionOverlay` > `.substitutionModalCard` > `<LineupAssignmentScreen mode="midmatch" ...>` followed by the `.resumeCtaRow`/`.resumeCta` block to be removed.
    - packages/client/src/components/GameBoard.module.css lines 632-678 — `.substitutionModalCard` (`display:flex; flex-direction:column` with the "so the Resume CTA row sits at the bottom of the panel's content flow" comment) and the `.resumeCtaRow`/`.resumeCta`/`.resumeCta:hover` blocks to be deleted.
    - packages/client/src/components/GameBoard.test.tsx lines 620-660 and 695-760 — every test that locates `getByRole('button', { name: 'Resume match' })` and asserts `textContent === 'Resume'` plus modal dismissal; these must keep passing after the button moves component.
    - .planning/phases/42-substitution-ux-overhaul/42-10-SUMMARY.md § "Section B" (Substitute button sizing/placement) and § "Section C, step 14" (Resume placement).
  </read_first>
  <action>
**A. Add the `onResume` prop.** In `LineupAssignmentScreen`'s `Props`, after `actionPending`, add:

`onResume?: () => void;`

with a doc comment: mid-match only; supplied by `GameBoard.tsx` to dismiss the substitution modal. Rendered inside this component (not by the parent) so the Resume button sits in the roster panel's own scrolling content flow directly under the bench, per gap item 4 — the previous bottom-of-`.substitutionModalCard` placement read as pinned to the viewport bottom. Destructure `onResume` in the component signature alongside `actionPending`.

**B. Build the action row under the bench.** In the `mode === 'midmatch'` return block:

1. DELETE the standalone `subMode === 'reposition' ? <button>Substitute</button> : <button>Cancel</button>` pair from its current position (immediately after the `.subCounterChip` span). Keep the `.subCounterChip` span itself exactly where it is.
2. Immediately AFTER the closing `</div>` of `.benchSection` and BEFORE the `pendingSub` confirm-popup block, render:

`<div className={styles.midmatchActionRow}>` containing, in this order:

- the moved Substitute/Cancel button pair, with all existing behaviour preserved verbatim — same `disabled`/`aria-disabled` expression (`readOnly === true || subsUsedVal >= MAX_SUBS_PER_TEAM`), same `aria-label`s (`'Enter substitution mode'` / `'Cancel substitution'`), same `onClick` bodies (including the three state resets), same labels `Substitute` / `Cancel`. Only the `className` changes: `styles.rosterActionButton` for the Substitute state, and `` `${styles.rosterActionButton} ${styles.rosterActionButtonCancel}` `` for the Cancel state.
- then, only when `onResume !== undefined`, a `<button type="button" className={styles.resumeButton} aria-label="Resume match" onClick={onResume}>Resume</button>`.

The `aria-label="Resume match"` and the visible text `Resume` are load-bearing — four existing `GameBoard.test.tsx` assertions query exactly those strings.

**C. New CSS in `LineupAssignmentScreen.module.css`.** Add, next to the existing `.subModeButton` block:

- `.midmatchActionRow` — `display: flex; gap: 8px; justify-content: center; align-items: center; width: 100%; max-width: 1260px; padding: 8px 0 4px;` (mirrors `.confirmSection`'s width/max-width/padding so it lines up with the roster columns above it; no `border-top`, since `.benchSection` already draws the divider above).
- `.rosterActionButton` — the app's standard CTA geometry copied from `.confirmButton`: `font-size: 20px; font-weight: 800; color: var(--color-text-inverse); background: var(--color-bg-surface-alt); border: 1px solid var(--color-border-muted); border-radius: 6px; padding: 14px 48px; min-width: 200px; cursor: pointer; transition: background 0.15s ease;` plus `:hover { background: var(--team-accent); }` (preserving `.subModeButton`'s existing hover semantic) and `:disabled { opacity: 0.5; cursor: default; }`.
- `.rosterActionButtonCancel` — `composes: rosterActionButton;` and `background: var(--color-cta-pending-bg);` with `:hover { background: var(--color-cta-pending-bg-hover); }` (gap item 5, applied here in the same pass because the Cancel state IS this button).
- `.resumeButton` — `composes: rosterActionButton;` and `background: var(--color-cta-ready-bg);` with `:hover { background: var(--color-cta-ready-bg-hover); }`.

Use `composes:` — this stylesheet already uses it (`.statCardSentOff { composes: statCardBase; }`).

Do NOT change `.subModeButton`: it is still used by the confirmation popup's two buttons, which must stay small inside the 280px `.subConfirmCard`.

**D. Remove the bottom-pinned CTA from `GameBoard`.** In `GameBoard.tsx`, delete the entire `<div className={styles.resumeCtaRow}>...</div>` block (including its comment) and instead pass `onResume={() => setSubOpen(false)}` to `<LineupAssignmentScreen mode="midmatch" ...>`. In `GameBoard.module.css`, delete the `.resumeCtaRow`, `.resumeCta` and `.resumeCta:hover` rules, and update `.substitutionModalCard`'s comment: it keeps `display: flex; flex-direction: column` (needed so the child `.screen` fills the card) but no longer hosts a bottom CTA row.

**E. Tests.** In `GameBoard.test.tsx`, keep every existing Resume assertion unchanged (they must pass unmodified — that is the proof the move preserved the contract). Add one new assertion to the SUB-16 test: the Resume button and the Substitute button share a common parent element whose `className` matches `/midmatchActionRow/`, proving gap item 3's "next to the Resume button" requirement structurally rather than visually.

In `LineupAssignmentScreen.test.tsx`, add a test that when `onResume` is NOT supplied (pre-match/draft call sites and any mid-match caller that omits it) no button named `Resume match` renders — proving the new prop is additive and cannot leak into the pre-match screens.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- --pool=forks GameBoard LineupAssignmentScreen</automated>
</verify>
<acceptance_criteria>

- `grep -c "onResume" packages/client/src/components/LineupAssignmentScreen.tsx` >= 3 (prop type, destructure, usage).
- `grep -c "onResume=" packages/client/src/components/GameBoard.tsx` returns `1`.
- `grep -c "resumeCtaRow\|resumeCta" packages/client/src/components/GameBoard.tsx` returns `0`.
- `grep -c "resumeCtaRow\|\.resumeCta" packages/client/src/components/GameBoard.module.css` returns `0`.
- `grep -c "midmatchActionRow" packages/client/src/components/LineupAssignmentScreen.module.css` returns `1` and in `LineupAssignmentScreen.tsx` returns `1`.
- `grep -c "composes: rosterActionButton" packages/client/src/components/LineupAssignmentScreen.module.css` returns `2` (Cancel + Resume).
- In `LineupAssignmentScreen.tsx`, the `.midmatchActionRow` element appears AFTER the `.benchSection` closing tag and BEFORE the `pendingSub !== null` popup block — verify by line numbers and record them in the SUMMARY.
- `pnpm --filter @counter-attack/client test -- --pool=forks GameBoard` passes with the four pre-existing `Resume match` assertions UNMODIFIED.
- `npx stylelint "packages/client/src/**/*.module.css"` exits 0.
- `pnpm knip` exits 0 — no orphaned CSS module class or unused export left by the removed `.resumeCta*` rules.
  </acceptance_criteria>
  <done>Substitute/Cancel and Resume render as two standard-size buttons in one row directly beneath the bench, inside the roster panel's own scroll flow.</done>
  </task>

<task type="auto">
  <name>Task 3: Orange Cancel in the confirmation popup (gap item 5, popup half)</name>
  <files>packages/client/src/components/LineupAssignmentScreen.tsx, packages/client/src/components/LineupAssignmentScreen.module.css, packages/client/src/components/LineupAssignmentScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.tsx lines 1125-1163 — the `pendingSub` confirmation popup: `.subConfirmOverlay` > `.subConfirmCard` > `.subConfirmText` > `.subConfirmActions` with the Cancel button (`className={styles.subModeButton}`, `aria-label="Cancel substitution selection"`) and the Confirm button (`` `${styles.subModeButton} ${styles.subConfirmButtonReady}` ``).
    - packages/client/src/components/LineupAssignmentScreen.module.css lines 595-651 — the whole popup block, especially `.subConfirmActions .subModeButton { flex: 1; max-width: none; }` and `.subConfirmButtonReady { background: var(--color-cta-ready-bg); }` + its `:hover` — the exact pattern the new Cancel class must mirror.
    - packages/client/src/styles/tokens.css lines 54-61 — `--color-cta-pending-bg: #f39c12` / `--color-cta-pending-bg-hover: #e67e22`, the app's existing orange CTA pair (used today by the ActionPanel's not-yet-ready CTA). No new token is needed.
    - packages/client/src/components/LineupAssignmentScreen.test.tsx — locate the existing SUB-14/SUB-15 popup tests (they query `role="dialog"` and the `Cancel substitution selection` / `Confirm substitution` aria-labels) so the new class assertion is added to the right describe.
    - .planning/phases/42-substitution-ux-overhaul/42-10-SUMMARY.md § "Section B" — "The Cancel button (shown when substitution mode is active / when the confirm popup is open) should be styled orange".
  </read_first>
  <action>
**A. Add the popup's orange Cancel class.** In `LineupAssignmentScreen.module.css`, directly above `.subConfirmButtonReady`, add:

`.subConfirmButtonCancel { background: var(--color-cta-pending-bg); }` and `.subConfirmButtonCancel:hover { background: var(--color-cta-pending-bg-hover); }`

with a comment recording that it mirrors `.subConfirmButtonReady`'s shape exactly, swapping the ready-green token pair for the app's existing pending-orange token pair, per gap item 5.

**B. Apply it.** In the popup JSX, change the Cancel button's `className` from `styles.subModeButton` to `` `${styles.subModeButton} ${styles.subConfirmButtonCancel}` ``. Leave `type`, `aria-label` and `onClick` untouched. Do not touch the Confirm button.

**C. Test.** In the popup's existing describe in `LineupAssignmentScreen.test.tsx`, add a test asserting that with a substitution staged, the button with `aria-label="Cancel substitution selection"` has a `className` matching `/subConfirmButtonCancel/`, and that the mode-level Cancel button (`aria-label="Cancel substitution"`, rendered by Task 2's action row) has a `className` matching `/rosterActionButtonCancel/`. One test, both Cancel surfaces — this is the assertion that fails if either half of gap item 5 regresses.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- --pool=forks LineupAssignmentScreen</automated>
</verify>
<acceptance_criteria>

- `grep -c "subConfirmButtonCancel" packages/client/src/components/LineupAssignmentScreen.module.css` returns `2` (rule + `:hover`).
- `grep -c "subConfirmButtonCancel" packages/client/src/components/LineupAssignmentScreen.tsx` returns `1`.
- `grep -c "var(--color-cta-pending-bg)" packages/client/src/components/LineupAssignmentScreen.module.css` returns `2` (popup Cancel + mode-level Cancel from Task 2).
- `grep -c "subConfirmButtonReady" packages/client/src/components/LineupAssignmentScreen.tsx` still returns `1` — the Confirm button is untouched.
- `npx stylelint "packages/client/src/components/LineupAssignmentScreen.module.css"` exits 0 (no raw colour literal introduced).
- `pnpm --filter @counter-attack/client test -- --pool=forks LineupAssignmentScreen` passes, including the new both-Cancels test.
  </acceptance_criteria>
  <done>Both Cancel affordances render orange; Confirm stays green; the popup's compact button geometry is unchanged.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                                                                       |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| roster panel → modal dismissal  | `onResume` is now a child-rendered control calling a parent-owned `setSubOpen(false)`. If it fails to render, a player is trapped in a full-viewport modal.       |
| `actionable` prop → visual only | The ROSTER strip's green state is purely cosmetic; click-ability is unconditional (40-07 gap closure). No authorisation decision rides on this class composition. |

## STRIDE Threat Register

| Threat ID | Category          | Component                                              | Disposition | Mitigation Plan                                                                                                                                                                                                  |
| --------- | ----------------- | ------------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-45   | Denial of Service | Resume button moved into a conditionally-rendered prop | mitigate    | If `onResume` were ever omitted by `GameBoard.tsx`, the panel would have no exit. Mitigated by keeping the four pre-existing `GameBoard.test.tsx` `Resume match` assertions UNMODIFIED as the contract proof.    |
| T-42-46   | Spoofing          | Green ROSTER strip implying an unavailable capability  | accept      | `actionable` is derived from `isStoppagePhase(phase)`, the same predicate the server enforces; a stale value can only mislead cosmetically. The server rejects any out-of-stoppage substitution regardless.      |
| T-42-47   | Tampering         | Deleted `.resumeCta*` rules leaving a dangling class   | mitigate    | `pnpm knip` and `npx stylelint` are both in the verification list; the grep acceptance criteria assert zero remaining references in both the TSX and the CSS.                                                    |
| T-42-48   | Repudiation       | Orange Cancel misread as a destructive action          | accept      | Cancel is fully reversible in both surfaces (`42-UI-SPEC.md` Copywriting Contract). Orange is the app's existing "pending/not-ready" CTA colour, not its destructive colour (`--color-danger`), which is unused. |
| T-42-SC   | Tampering         | npm/pnpm installs                                      | accept      | No packages installed by this plan. No Package Legitimacy Gate applies.                                                                                                                                          |

</threat_model>

<verification>

- `pnpm --filter @counter-attack/client test -- --pool=forks` — full client suite green.
- `pnpm -r typecheck` — clean (the new optional `onResume` prop must not break any existing call site).
- `npx stylelint "packages/client/src/**/*.module.css"` — clean.
- `npx eslint packages/client/src/components/GameBoard.tsx packages/client/src/components/LineupAssignmentScreen.tsx packages/client/src/components/GameBoard.test.tsx packages/client/src/components/LineupAssignmentScreen.test.tsx` — clean.
- `pnpm knip` — exits clean.
- `pnpm --filter @counter-attack/client check-contrast` — still passes (this gate validates team accents only; the white-on-green strip pairing is documented as an accepted deviation in Task 1D).
- `pnpm format:check` — clean.
  </verification>

<success_criteria>

- The side strip label reads `ROSTER`; when actionable the strip background is `--color-cta-ready-bg` and the label is `--color-text-inverse` with no inner background patch.
- `--color-speed-standard-bg` no longer appears anywhere in `GameBoard.module.css`.
- Substitute/Cancel and Resume are two standard-size buttons sharing one `.midmatchActionRow` rendered after `.benchSection` inside `LineupAssignmentScreen`.
- `.resumeCtaRow` / `.resumeCta` are fully removed from both `GameBoard.tsx` and `GameBoard.module.css`.
- Both Cancel buttons are orange via `--color-cta-pending-bg`; Confirm stays `--color-cta-ready-bg`.
- All four pre-existing `Resume match` tests in `GameBoard.test.tsx` pass unmodified.
  </success_criteria>

<output>
Create `.planning/phases/42-substitution-ux-overhaul/42-12-SUMMARY.md` when done. Record: the exact token swap for the strip, the final DOM order inside the mid-match `.screen` (heading → helper → chip → slot-cap note → formation columns → bench section → action row → popup → rejection), whether any test queried the literal `'SUB'` label, and the full list of removed `GameBoard.module.css` rules.
</output>
