---
phase: 42-substitution-ux-overhaul
plan: 10
type: execute
wave: 7
depends_on: ['42-04', '42-05', '42-08', '42-09']
files_modified:
  - packages/client/src/components/LineupAssignmentScreen.test.tsx
  - packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx
autonomous: false
requirements: [SUB-10, SUB-13, SUB-18, BUG-38]

must_haves:
  truths:
    - 'Every Phase 40 SUB-0X assertion still exercises the code path it originally covered, verified side by side rather than assumed from a green suite.'
    - 'A red-carded player still displays correctly on the bench through the shared CardInjuryBadge after the reposition and substitution rework.'
    - 'A human has confirmed in a live two-browser match that positioning mode, substitution mode, the Resume button, the green strip, and the red-card fix all behave as specified.'
  artifacts:
    - path: 'packages/client/src/components/LineupAssignmentScreen.test.tsx'
      provides: 'the SUB-0X side-by-side audit block and any coverage gaps it closes'
      contains: 'SUB-0'
    - path: '.planning/phases/42-substitution-ux-overhaul/42-10-SUMMARY.md'
      provides: 'the written SUB-0X audit table and the human-verification record'
  key_links:
    - from: '42-10-SUMMARY.md audit table'
      to: 'packages/client/src/components/LineupAssignmentScreen.test.tsx'
      via: 'one row per Phase 40 SUB-0X assertion naming the test and the guard it still reaches'
      pattern: 'SUB-0'
---

<objective>
Close the phase with the explicit regression gate research demands, and get a human to confirm the whole feature in a live two-browser match.

Purpose: research names the positioning/substitution mode coexistence as the single highest-regression-risk item of the milestone and is specific about what "done" means — "not merely 'tests still pass' but a deliberate side-by-side check that each SUB-0X assertion still exercises the code path it originally covered." A green suite does not prove that; a guard can be silently bypassed while its test still passes for an unrelated reason. This plan performs that audit in writing, closes any gap it finds, and then hands off to human verification because the drag-and-drop interaction, the green banner and the deflection fix are all things only a live match can confirm.

Output: a written SUB-0X audit table, a bench-badge regression check, a full-repo quality gate, and a signed-off human verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md
@.planning/phases/42-substitution-ux-overhaul/42-07-SUMMARY.md
@.planning/phases/42-substitution-ux-overhaul/42-08-SUMMARY.md
@.planning/phases/42-substitution-ux-overhaul/42-09-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

## Artifacts this phase produces

See the table in `42-01-PLAN.md` under "Artifacts this phase produces". This plan creates no new production symbol.

<tasks>

<task type="auto">
  <name>Task 1: Side-by-side SUB-0X assertion audit and bench-badge regression check</name>
  <files>packages/client/src/components/LineupAssignmentScreen.test.tsx, packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx</files>
  <read_first>
    - packages/client/src/components/LineupAssignmentScreen.test.tsx (the whole file — enumerate every `it(...)` that cites SUB-02, SUB-03, SUB-04, SUB-05, SUB-06, SUB-07, D-12 or D-13, including the block around the original lines 671-756)
    - packages/client/src/components/LineupAssignmentScreen.tsx (the component in its final post-42-08 state — the guard each audited assertion is supposed to reach)
    - packages/client/src/components/BenchCarousel.tsx (the bench-card draggability guard at ~line 111 and the `unavailablePlayerIds` / `redCardedPlayerIds` / `benchCardStatus` props)
    - packages/client/src/components/CardInjuryBadge.crossSurface.test.tsx (the existing four-surface parity test from Phase 41 — the bench surface is the one this phase's rework could disturb)
    - .planning/phases/41-card-injury-iconography/41-CONTEXT.md (the `CardInjuryBadge` contract this phase reuses as-is per D-07)
    - .planning/research/PITFALLS.md lines 76-88 (Pitfall 5's exact definition of the required gate)
    - .planning/phases/42-substitution-ux-overhaul/42-07-SUMMARY.md and 42-08-SUMMARY.md (the lists of pre-existing tests those plans modified — each modified test needs the closest scrutiny in this audit)
  </read_first>
  <action>
**A. SUB-0X side-by-side audit.** For every Phase 40 assertion enumerated in the read pass, produce one audit row recording:
- the `it(...)` title and the requirement it cites,
- the guard in `LineupAssignmentScreen.tsx` / `BenchCarousel.tsx` it was written to exercise (name the function and the condition),
- whether that guard is still reached by that test after the rework, established by evidence rather than assumption. Acceptable evidence, in order of preference: (i) the test drives a code path whose guard is provably still on it (cite the current line/condition), or (ii) a temporary mutation check — invert or delete the guard, confirm the test fails, restore the guard. Use (ii) for every guard whose reachability is not obvious from reading, and at minimum for the 3-sub cap, the red-card `isSubBlocked` exclusion, the `subbedOut` bench exclusion and the `readOnly` guard.
- the verdict: STILL-COVERED, or GAP.

For every GAP, add a test in this task that restores coverage, citing the original requirement id. Do not weaken or delete the original assertion.

**B. Bench red-card display regression check (D-07 / SUB-18).** D-07 states this phase adds no new bench-side component — the obligation is a regression check that Phase 41's `CardInjuryBadge` still renders correctly on the bench once the reposition/substitution rework and the BUG-38 fixes have landed. Verify and, where coverage is missing, add to `CardInjuryBadge.crossSurface.test.tsx`:

- a bench entry with `status: 'redCarded'` renders the red-card glyph and the `RED CARD` badge, in positioning mode and in substitution mode;
- a bench entry with `status: 'subbedOut'` still renders the `OUT` badge and the `.cardUnavailable` dimming in both modes;
- an injured bench entry still renders its injury glyph in both modes;
- bench glyph rendering is unaffected by `readOnly`, by `actionPending`, and by an open substitution-confirm popup.

If all of the above is already covered by existing tests, say so explicitly per bullet in the summary and add no duplicate test.

**C. Do not add new production code in this task.** If the audit uncovers a genuine behavioral defect rather than a coverage gap, stop, record it, and report it — do not fix it here, so the fix lands as a visible, reviewable change rather than buried in a test plan.
</action>
<verify>
<automated>pnpm --filter @counter-attack/client test -- --pool=forks LineupAssignmentScreen CardInjuryBadge</automated>
</verify>
<acceptance_criteria> - The summary contains an audit table with one row per Phase 40 SUB-0X assertion, each row naming the test title, the guard, the evidence type (i/ii), and a STILL-COVERED or GAP verdict. - At least four rows use evidence type (ii) — the 3-sub cap, the red-card `isSubBlocked` exclusion, the `subbedOut` bench exclusion and the `readOnly` guard — and each records the observed failing test name from its mutation check. - Every GAP row has a corresponding new test in `LineupAssignmentScreen.test.tsx` citing the original requirement id. - All four bench-badge bullets in part B are either covered by a named existing test or by a new one. - `git diff packages/client/src/components/LineupAssignmentScreen.tsx packages/client/src/components/BenchCarousel.tsx` is empty (no production code changed by this task). - `pnpm --filter @counter-attack/client test -- --pool=forks` exits 0.
</acceptance_criteria>
<done>Every Phase 40 substitution guard is proven still reachable by its original test, and the bench badge is proven unaffected by the rework.</done>
</task>

<task type="auto">
  <name>Task 2: Whole-repo quality gate and BUG-38 audit closeout</name>
  <files>packages/client/src/components/LineupAssignmentScreen.test.tsx</files>
  <read_first>
    - .planning/phases/42-substitution-ux-overhaul/42-05-SUMMARY.md (the D-10-item-4 whole-file audit table for `gameEngine.ts`)
    - .planning/phases/42-substitution-ux-overhaul/42-01-SUMMARY.md, 42-02-SUMMARY.md, 42-03-SUMMARY.md, 42-04-SUMMARY.md (each plan's list of narrowed predicates and documented skips)
    - package.json (the root `test`, `typecheck`, `lint`, `format:check`, `stylelint`, `knip` and `check-contrast` scripts)
    - .planning/REQUIREMENTS.md lines 31-42 (SUB-08..18 and BUG-38 acceptance text, to score each requirement against the delivered behavior)
  </read_first>
  <action>
Run the full repository gate and record every result verbatim in the summary. Run in this order, because the shared `dist/` must be rebuilt before the server and client typecheck against the new `isActivePiece` and `ROSTER_REPOSITION` exports:

1. `pnpm --filter @counter-attack/shared build`
2. `pnpm -r typecheck`
3. `pnpm --filter @counter-attack/shared test -- --pool=forks`
4. `pnpm --filter @counter-attack/server test -- --pool=forks`
5. `pnpm --filter @counter-attack/client test -- --pool=forks`
6. `pnpm lint`
7. `pnpm format:check`
8. `pnpm stylelint`
9. `pnpm knip`
10. `pnpm --filter @counter-attack/client check-contrast`

If a vitest run reports a worker crash rather than an assertion failure, re-run that same command once — `--pool=forks` is the known workaround for that environment flake. If `pnpm lint` hits a Node OOM (a known environment issue on this repo), fall back to running `npx eslint` scoped to the files this phase touched and say so in the summary.

Then close out BUG-38's audit in writing. Run a final whole-repo residual sweep and record the output:

- `grep -rvn "^\s*[*/]" --include=*.ts --include=*.tsx packages/shared/src packages/server/src packages/client/src | grep "redCarded !== true\|redCarded === true\|onPitch === false\|onPitch !== false" | grep -v "\.test\."`
  Every remaining hit must be inside `packages/shared/src/stoppagePhases.ts` (the helper itself) or `packages/shared/src/types.ts` (doc comments). Any other hit is a missed site — fix it in this task and call it out.

Finally, produce a requirement-by-requirement scorecard in the summary: one row per SUB-08..SUB-18 and BUG-38, naming the plan that delivered it and the specific automated test or grep assertion that proves it. Mark anything only provable by a human as `pending-human` and hand it to Task 3.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared build && pnpm -r typecheck && pnpm -r test</automated>
</verify>
<acceptance_criteria> - All ten commands are recorded in the summary with their exit status; every one exits 0 (or, for `pnpm lint`, the documented scoped-eslint fallback exits 0 with the fallback explicitly noted). - The residual `grep` sweep output is pasted in the summary and contains no hit outside `stoppagePhases.ts` and `types.ts`. - The summary contains a 12-row scorecard covering SUB-08..SUB-18 and BUG-38, each row naming a delivering plan and a proving test/assertion, with `pending-human` used only for items handed to Task 3. - `pnpm knip` exits 0.
</acceptance_criteria>
<done>The whole repository is green, no red-card flag check survives outside the shared helper, and every phase requirement has a named proof.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live two-browser verification of the substitution UX overhaul</name>
  <what-built>
Phase 42 in full, across nine prior plans:
- BUG-38: a red-carded player is excluded from deflection eligibility, Zone of Influence, movement-path/occupancy blocking, response-move selection and every other eligibility list, through one shared `isActivePiece` helper applied across `packages/shared`, `gameEngine.ts`, `gameHandlers.ts` and `HexGrid.tsx`.
- SUB-08/09/10: the mid-match roster panel now defaults to a positioning mode where on-field cards drag onto each other to swap positions instantly, gated off while a game action is selected and while the bench is inert.
- SUB-11/12/13/14/15: a Substitute button (disabled at 3/3) enters substitution mode and becomes Cancel; one bench-to-pitch drop stages one swap and opens a confirmation popup naming both players; Confirm applies it and returns to positioning mode, Cancel clears it and stays.
- SUB-16/17: a full-width green Resume button replaces the small × close control, and the whole SUB side-banner strip turns green while the roster is editable.
- SUB-18/D-05/D-06: a red-carded player's slot shows a SENT OFF placeholder that another on-field player can still be dropped into; the player appears on the bench through Phase 41's existing CardInjuryBadge.
  </what-built>
  <how-to-verify>
Run `pnpm --filter @counter-attack/shared build` then start the server and client dev servers. Open two browsers, create a room in one, join with the code in the other, and play into a live match.

**A. Positioning mode (SUB-08/09/10)**

1. Reach any stoppage (kick-off setup is easiest). Click the vertical SUB strip on the right edge to open the roster panel.
2. Confirm the helper text reads "Drag a player onto another to swap positions." and a `Substitute` button is visible.
3. Drag one outfield card onto another. Expect: the swap applies immediately with no popup, both cards visibly change places in the formation grid, and the change appears on BOTH browsers.
4. Confirm the match log shows a `[SWAP]` line naming both players with their numbers.
5. Try to drag the GK card. Expect: it does not drag.
6. Try to drag a bench card. Expect: it does not drag.
7. Close the panel, click a piece on the pitch to select it, reopen the panel. Expect: on-field cards no longer drag (SUB-09).

**B. Substitution mode (SUB-11..15)** 8. With fewer than 3 subs used, click `Substitute`. Expect: the button becomes `Cancel` and the helper text changes to the bench-drag copy. 9. Drag a bench card onto an on-field card. Expect: a popup reading "Substitute {player off} for {player on}?" with `Cancel` and `Confirm Substitution` buttons — and nothing applied yet. 10. Click the popup's `Cancel`. Expect: popup closes, no substitution applied, still in substitution mode. 11. Stage again and click `Confirm Substitution`. Expect: the substitution applies on both browsers, the counter increments, and the panel returns to positioning mode. 12. Use all 3 substitutions. Expect: the `Substitute` button renders disabled and the counter chip reads `3/3 SUBS USED` in red.

**C. Chrome (SUB-16/17)** 13. During a stoppage, confirm the entire 28px SUB strip (its background, not just the "SUB" text) is green. Leave the stoppage and confirm it returns to the neutral background. 14. Confirm there is no small × in the panel's top-right corner, and that a full-width green `Resume` button at the bottom of the panel closes it.

**D. Red card (SUB-18 / BUG-38) — the critical one** 15. Get a player sent off (second yellow, or a straight red from a Professional Foul). 16. Confirm the piece disappears from the pitch entirely on both browsers. 17. Open the roster panel. Expect: that player's formation slot shows a `SENT OFF` placeholder instead of a card, the column layout does not visibly reflow, and the player appears on the bench with the red-card badge. 18. Drag another on-field player into the SENT OFF slot. Expect: the swap applies and the empty slot moves to the vacated position (D-05). 19. **The original bug:** take a shot whose path passes through or beside the hex where the sent-off player was standing. Expect: NO deflection is rolled for the invisible player and the shot resolves normally. 20. Walk the ball carrier adjacent to the hex where the sent-off player was standing. Expect: NO steal attempt is triggered. 21. Move any player ONTO the hex where the sent-off player was standing. Expect: the move is allowed — the hex is no longer blocked. 22. Score a goal or reach half-time so the formation resets. Reopen the roster panel. Expect: the position swap you made in step 3 has persisted (the two players are still in each other's slots), and the sent-off player is still sent off.
</how-to-verify>
<verify>
<human-check>All 22 steps behave as described in a live two-browser match, with particular attention to steps 19-22.</human-check>
</verify>
<acceptance_criteria> - The user has confirmed each lettered section (A, B, C, D) as pass or fail. - Any failure is recorded verbatim with its step number in `42-10-SUMMARY.md` and routed to a gap-closure plan rather than fixed inline. - Steps 19, 20 and 21 — the three original BUG-38 symptoms — are explicitly confirmed, since they are the reason this phase carries the bug fix. - Step 22 is explicitly confirmed, since reposition survival across a reset is the property that justified the slot-bound swap model chosen in 42-06.
</acceptance_criteria>
<resume-signal>Type "approved" to close the phase, or describe the failing step numbers and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                                  |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| automated suite → shipped behavior | A green suite is not proof that a guard is still reached; this plan exists specifically to close that gap.                                   |
| two live clients → server          | Human verification is the only check that exercises the real socket round trip between two browsers, which no unit test in this repo covers. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                            |
| --------- | ---------------------- | -------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-42   | Tampering              | Silently bypassed SUB-0X guard                     | mitigate    | Pitfall 5's named failure mode: a mode conditional narrows a guard while its test keeps passing for an unrelated reason. Mitigated by requiring mutation-check evidence (invert the guard, observe the test fail, restore) for the four highest-risk guards, with the observed failing test name recorded. |
| T-42-43   | Repudiation            | An audit claimed but not performed                 | mitigate    | The audit's deliverable is a written table with per-row evidence type and, for mutation checks, the observed failing test name — not a pass/fail claim.                                                                                                                                                    |
| T-42-44   | Elevation of Privilege | A residual un-narrowed red-card site               | mitigate    | A whole-repo residual grep whose output must be pasted into the summary and must contain no hit outside `stoppagePhases.ts` and `types.ts`.                                                                                                                                                                |
| T-42-45   | Denial of Service      | Fixing a discovered defect inside a test-only plan | mitigate    | Task 1 explicitly forbids production edits and requires a `git diff` acceptance criterion proving none were made, so a real defect surfaces as a routed gap rather than an unreviewed inline patch.                                                                                                        |
| T-42-SC   | Tampering              | npm/pnpm installs                                  | accept      | No packages installed by this plan. No Package Legitimacy Gate applies.                                                                                                                                                                                                                                    |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared build && pnpm -r typecheck && pnpm -r test` — all green.
- `pnpm lint && pnpm format:check && pnpm stylelint && pnpm knip` — all clean.
- `pnpm --filter @counter-attack/client check-contrast` — no failures.
- Residual red-card flag sweep returns hits only in `packages/shared/src/stoppagePhases.ts` and `packages/shared/src/types.ts`.
- Human sign-off recorded for all four lettered sections of the live verification.
</verification>

<success_criteria>

- A written SUB-0X audit table exists with per-row evidence, including four mutation checks with observed failing test names.
- Every GAP found is closed by a new test citing its original requirement id, with no production code changed in the audit task.
- The bench red-card / subbed-out / injury badges are proven unaffected in both modes.
- The whole repository passes typecheck, all three test suites, lint, format, stylelint, knip and contrast.
- A 12-row requirement scorecard covers SUB-08..SUB-18 and BUG-38 with a named proof each.
- A human has confirmed the live two-browser walkthrough, with steps 19-22 explicitly called out.
  </success_criteria>

<output>
Create `.planning/phases/42-substitution-ux-overhaul/42-10-SUMMARY.md` when done. Record: the SUB-0X audit table, the bench-badge check results, the ten gate command results, the pasted residual grep output, the 12-row requirement scorecard, and the human verification outcome per lettered section.
</output>
