---
phase: 42-substitution-ux-overhaul
plan: 17
type: execute
wave: 2
depends_on: ['42-16']
files_modified: []
autonomous: true
gap_closure: true
requirements: [BUG-38]

must_haves:
  truths:
    - 'Every piece-list construction site in packages/shared is enumerated and classified as OCCUPANCY (must be isActivePiece-filtered), CONSTRUCTION (by-id lookup, exempt), or DELIBERATE (counts dismissed pieces on purpose) — with zero unclassified sites remaining.'
    - 'The D-10 item 4 audit mandate is closed for the WHOLE packages/shared package, explicitly including offside.ts, which the original audit scope never named.'
    - 'The server-side and client-side isActivePiece call-site counts recorded by the phase verifier have not regressed.'
    - 'The whole repository passes the same quality gate 42-10 and 42-15 ran, in the same order.'
  artifacts:
    - path: '.planning/phases/42-substitution-ux-overhaul/42-17-SUMMARY.md'
      provides: 'the closed D-10 item 4 audit table for packages/shared and the whole-repo quality-gate table'
      contains: 'Shared-Package Piece-List Audit'
  key_links:
    - from: '.planning/phases/42-substitution-ux-overhaul/42-VERIFICATION.md'
      to: '.planning/phases/42-substitution-ux-overhaul/42-17-SUMMARY.md'
      via: 'the three `missing:` items closed one-for-one'
      pattern: 'missing'
---

<objective>
Close the third and final item on `42-VERIFICATION.md`'s `missing:` list: re-run the D-10 item 4 grep audit across the WHOLE `packages/shared` package — with `offside.ts` explicitly in scope, which the original audit (per `42-CONTEXT.md` D-10 and `42-05-PLAN.md`) never named — and then run the whole-repository quality gate over 42-16's changes.

Purpose: BUG-38 was not missed because the fix was hard; it was missed because the audit's SCOPE was wrong. `42-05` closed the audit for `gameEngine.ts` and `42-02` for `gameHandlers.ts`, but `packages/shared` was scanned file-by-file for known suspects rather than enumerated exhaustively, so `offside.ts` — a file with six live `evaluateOffside` call sites in `gameEngine.ts` — was never looked at. This plan replaces "audit the files we suspect" with "enumerate every piece-list construction site in the package and classify each one", so the closure is provable rather than asserted. `offside.ts` also changes offside RULINGS in live play, so the repo-wide gate is a genuine regression check, not ceremony.

Output: a complete, classified audit table for `packages/shared`, a regression tripwire on the server/client call-site counts, and a green whole-repo gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md
@.planning/phases/42-substitution-ux-overhaul/42-VERIFICATION.md
@.planning/phases/42-substitution-ux-overhaul/42-05-SUMMARY.md
@.planning/phases/42-substitution-ux-overhaul/42-16-SUMMARY.md
@packages/shared/src/stoppagePhases.ts
</context>

## Artifacts this phase produces

See the table in `42-01-PLAN.md` under "Artifacts this phase produces". This plan creates **no source symbol**. Its only artifact is `42-17-SUMMARY.md`.

**Inline-fix policy.** This plan is an audit and a gate; it modifies no production file by default (`files_modified: []`). Exactly one narrow exception applies, and only if ALL of its conditions hold:

- the finding is a NEW site (not one of the sites already enumerated in Task 1's baseline table), AND
- it is inside `packages/shared/src`, AND
- it classifies as OCCUPANCY, AND
- the fix is adding `isActivePiece` as a conjunct to an existing filter predicate with no other change.

In that case, apply the conjunct AND add a regression test to that module's own `.test.ts` in the same shape 42-16 used, and record both in the SUMMARY (updating `files_modified` in this plan's frontmatter to name what was touched). Anything else — a finding in `packages/server`, `packages/client`, a structural change, or a site needing a design decision — is RECORDED VERBATIM and routed to a further gap-closure round, never patched inline. This is the same policy `42-15` operated under.

<tasks>

<task type="auto">
  <name>Task 1: Exhaustive piece-list audit of packages/shared (D-10 item 4 closeout)</name>
  <files>(none by default — read-only audit; see the inline-fix policy above for the single narrow exception)</files>
  <read_first>
    - `.planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md` § "BUG-38 Fix Shape" — D-08 (exclude-by-flag, never splice or null `position`), D-09 (one shared helper), and D-10 item 4's verbatim audit mandate. Note that item 4's written scope names `gameEngine.ts`, `gameHandlers.ts`, and "packages/shared's validators (`moveValidator.ts` explicitly)" — `offside.ts` is absent, which is the scope hole this plan closes.
    - `packages/shared/src/stoppagePhases.ts` lines 80-107 — the `isActivePiece` contract wording that defines what "must be built through this predicate" means, and lines 63-78 (`maxOnPitchFor`), which deliberately COUNTS `redCarded === true` pieces and must never be filtered.
    - `.planning/phases/42-substitution-ux-overhaul/42-05-SUMMARY.md` § the OCCUPANCY / CONSTRUCTION classification table and its block-comment wording — this task's table must use the same two class labels plus a third, DELIBERATE, for `maxOnPitchFor`.
    - `.planning/phases/42-substitution-ux-overhaul/42-16-SUMMARY.md` § "isActivePiece Occurrence Audit" and § "Classification Notes" — what 42-16 already changed and which two `offside.ts` sites it classified as CONSTRUCTION, so this audit confirms rather than re-derives them.
    - `packages/shared/src/offside.ts`, `packages/shared/src/passValidator.ts`, `packages/shared/src/moveValidator.ts`, `packages/shared/src/fouls.ts`, `packages/shared/src/outOfBounds.ts`, `packages/shared/src/shotValidator.ts` — read the enclosing function of every grep hit before classifying it. Do not classify from the grep line alone.
  </read_first>
  <action>
**A. Enumerate.** Run these two greps over `packages/shared/src` (excluding `*.test.ts`) and merge the results into one candidate list:

- `grep -rn "pieces\.filter\|pieces\.find\|pieces\.some\|pieces\.map" packages/shared/src --include=*.ts | grep -v "\.test\.ts"`
- `grep -rn "for (const .* of .*pieces\|\.pieces\b" packages/shared/src --include=*.ts | grep -v "\.test\.ts"` — catches loop-style and destructured iteration the first grep misses.

**B. Classify every hit** against this baseline (captured pre-42-16; LINE NUMBERS WILL HAVE SHIFTED after 42-16's edits — match by file plus enclosing function name, never by line number):

| File                | Enclosing function / expression                | Expected class | Expected state                                                                                                                 |
| ------------------- | ---------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `fouls.ts`          | `isProfessionalFoul` — `fouler` by-id lookup   | CONSTRUCTION   | unfiltered, correct                                                                                                            |
| `fouls.ts`          | `isProfessionalFoul` — `candidates` filter     | OCCUPANCY      | already filtered (pre-existing `isActivePiece(candidate)`)                                                                     |
| `moveValidator.ts`  | destination-occupancy `.some`                  | OCCUPANCY      | already filtered                                                                                                               |
| `moveValidator.ts`  | ZoI `opponents` filter                         | OCCUPANCY      | already filtered (Pitfall 7 fix)                                                                                               |
| `moveValidator.ts`  | ball `carrier` by-id lookup                    | CONSTRUCTION   | unfiltered, correct                                                                                                            |
| `offside.ts`        | `opposingPiecesEqualOrAhead`                   | OCCUPANCY      | filtered by 42-16                                                                                                              |
| `offside.ts`        | `evaluateOffside` — `piecesById` map           | CONSTRUCTION   | unfiltered, correct (by-id index)                                                                                              |
| `offside.ts`        | `evaluateOffside` — `stillFlagged` filter      | OCCUPANCY      | filtered by 42-16                                                                                                              |
| `offside.ts`        | `evaluateOffside` — `newlyFlagged` filter      | OCCUPANCY      | filtered by 42-16                                                                                                              |
| `offside.ts`        | `triggerOffsideFoul` — `offender` by-id lookup | CONSTRUCTION   | unfiltered, correct                                                                                                            |
| `outOfBounds.ts`    | `resolveThrowInHex` — `isOccupied` `.some`     | OCCUPANCY      | already filtered                                                                                                               |
| `passValidator.ts`  | STANDARD `opponentPieces` filter               | OCCUPANCY      | already filtered                                                                                                               |
| `passValidator.ts`  | HIGH/LONG adjacent-blocker `opponentPieces`    | OCCUPANCY      | already filtered                                                                                                               |
| `passValidator.ts`  | LONG `ownTeammates` filter                     | OCCUPANCY      | filtered by 42-16                                                                                                              |
| `passValidator.ts`  | LONG `opponents` filter                        | OCCUPANCY      | filtered by 42-16                                                                                                              |
| `passValidator.ts`  | `destDefender` lookup                          | OCCUPANCY      | already filtered                                                                                                               |
| `passValidator.ts`  | `rollIntercepts` `opponents` filter            | OCCUPANCY      | already filtered                                                                                                               |
| `shotValidator.ts`  | `gk` and `carrier` by-id lookups               | CONSTRUCTION   | unfiltered, correct                                                                                                            |
| `stoppagePhases.ts` | `maxOnPitchFor` — `redCardCount` filter        | DELIBERATE     | MUST stay unfiltered — SUB-06/D-08 counts dismissed pieces intentionally; filtering it would break the `11 - redCardCount` cap |

Reproduce this table in the SUMMARY with an ACTUAL column recording what you found, and an extra row for every hit not in the baseline. A hit not in the baseline is a NEW FINDING — classify it, and apply the inline-fix policy stated above.

**C. Cross-check the contract.** For each OCCUPANCY row, confirm the predicate is `isActivePiece(...)` and NOT a hand-written `redCarded !== true` / `onPitch !== false` clause — the exact bug class `stoppagePhases.ts:99-103` says BUG-38 exists to close. Run `grep -rn "redCarded\|onPitch" packages/shared/src --include=*.ts | grep -v "\.test\.ts" | grep -v "^\s*\*"` and confirm every remaining raw-flag reference is either inside `stoppagePhases.ts` itself (the predicate and `maxOnPitchFor`), inside `types.ts` (the field declarations and their doc comments), or an explanatory comment — never a live filter clause in another module.

**D. Server/client regression tripwire.** Confirm the earlier rounds' work has not been reverted:

- `grep -c "isActivePiece" packages/server/src/gameEngine.ts` — record the count; `42-VERIFICATION.md` line 69 recorded 60 call sites. Any number lower than 60 is a finding.
- `grep -c "isActivePiece" packages/server/src/gameHandlers.ts` — record the count; it must be non-zero and the seven sites named in `42-VERIFICATION.md` line 69 (1096, 1176, 1305, 2389, 2613, 2691, 2790) must still resolve to `isActivePiece` usage in their enclosing functions.
- `grep -c "isActivePiece" packages/client/src/components/HexGrid.tsx` — must be non-zero (the render-skip plus the `canSelect*` gates from 42-04).

Record every command and its actual output. Do not fix anything outside the inline-fix policy.
</action>
<verify>
<automated>node -e "const fs=require('fs');const strip=(f)=>fs.readFileSync(f,'utf8').split('\n').filter(l=>!/^\s\*(\*|\/\/|\/\*)/.test(l)).join('\n');const n=(s,f)=>(strip(f).match(new RegExp(s,'g'))||[]).length;const off=n('isActivePiece','packages/shared/src/offside.ts');const pv=n('isActivePiece','packages/shared/src/passValidator.ts');const mv=n('isActivePiece','packages/shared/src/moveValidator.ts');console.log({off,pv,mv});process.exit(off>=4&&pv>=7&&mv>=3?0:1)"</automated>
</verify>
<acceptance_criteria>

- The SUMMARY contains a `## Shared-Package Piece-List Audit` table reproducing all 19 baseline rows plus an ACTUAL column, with zero rows left unclassified and zero rows whose ACTUAL class contradicts the EXPECTED class without a written explanation.
- Every OCCUPANCY row's ACTUAL column names `isActivePiece`; no OCCUPANCY row is satisfied by a hand-written `redCarded`/`onPitch` clause.
- The step-C grep output is recorded in full, and every remaining raw `redCarded`/`onPitch` reference outside `stoppagePhases.ts` and `types.ts` is accounted for as a comment or a field access, not a live filter.
- `grep -c "isActivePiece" packages/server/src/gameEngine.ts` returns a value >= 60; the `gameHandlers.ts` and `HexGrid.tsx` counts are recorded and non-zero.
- The verify command exits 0, printing counts of at least 4 (`offside.ts`), 7 (`passValidator.ts`), and 3 (`moveValidator.ts`) with comment lines stripped.
- If any NEW FINDING exists, the SUMMARY states explicitly whether the inline-fix policy's four conditions were ALL met, and either shows the one-conjunct fix plus its regression test, or records the finding verbatim as a routed gap item.
  </acceptance_criteria>
  <done>Every piece-list site in packages/shared is enumerated and classified, and D-10 item 4 is closed for the whole package rather than for a hand-picked subset.</done>
  </task>

<task type="auto">
  <name>Task 2: Whole-repo quality gate over the 42-16 changes</name>
  <files>(none — read-only verification; no source file is modified by this task)</files>
  <read_first>
    - `.planning/phases/42-substitution-ux-overhaul/42-15-PLAN.md` Task 1 § A — the exact 10-command gate list and order, and the documented `pnpm lint` fallback (`npx eslint` scoped to the changed files) used when the pre-existing `packages/shared` typescript-eslint file-count-cap issue fires.
    - `.planning/phases/42-substitution-ux-overhaul/42-16-SUMMARY.md` — the four files 42-16 touched and its recorded test counts, so this gate's results can be checked against what actually shipped.
    - `CLAUDE.md` — project commands and conventions.
  </read_first>
  <action>
Run the gate in this exact order, recording pass/fail and any output for each:

1. `pnpm --filter @counter-attack/shared build`
2. `pnpm -r typecheck`
3. `pnpm --filter @counter-attack/shared test -- --pool=forks`
4. `pnpm --filter @counter-attack/server test -- --pool=forks`
5. `pnpm --filter @counter-attack/client test -- --pool=forks`
6. `pnpm lint` (on the documented pre-existing `packages/shared` cap failure, fall back to `npx eslint` scoped to `packages/shared/src/offside.ts packages/shared/src/offside.test.ts packages/shared/src/passValidator.ts packages/shared/src/passValidator.test.ts` and say so explicitly)
7. `pnpm format:check`
8. `pnpm stylelint`
9. `pnpm knip`
10. `pnpm --filter @counter-attack/client check-contrast`

Command 4 is the load-bearing one for this round: `evaluateOffside` is invoked from 6+ sites in `gameEngine.ts` on every movement and pass resolution, and 42-16 changes what it returns for any state containing a dismissed piece. Any server-suite failure must be diagnosed as either (a) a genuine regression introduced by 42-16, or (b) the known vitest worker-crash flake — for (b), rerun the failing file with `--pool=forks` and record both runs before concluding. Do not weaken or edit any test expectation to make the gate pass; a genuine regression is recorded and routed, per the inline-fix policy.

Also record the delta this round produced: total test counts for `@counter-attack/shared` and `@counter-attack/server` before (from `42-16-SUMMARY.md`) and after.

Finally, state in the SUMMARY whether all three `missing:` items from `42-VERIFICATION.md` lines 45-48 are now closed, item by item, quoting each one and naming the plan and evidence that closed it.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared build && pnpm -r typecheck && pnpm --filter @counter-attack/shared test -- --pool=forks && pnpm --filter @counter-attack/server test -- --pool=forks && pnpm --filter @counter-attack/client test -- --pool=forks</automated>
</verify>
<acceptance_criteria>

- All 10 gate commands appear in the SUMMARY as a table with a pass/fail verdict each; any fallback used is named and justified against the precedent in `42-15-SUMMARY.md`.
- Commands 1-5 and 7-10 exit 0.
- `git diff --stat` shows changes ONLY to the four files listed in 42-16's `files_modified` (plus any file the inline-fix policy legitimately added in Task 1) — no test expectation anywhere was edited to accommodate the change.
- The SUMMARY contains a `## Verification Gap Closure` section quoting all three `missing:` items from `42-VERIFICATION.md` lines 45-48 verbatim, each marked CLOSED with the plan number and the specific evidence (test name, grep output, or audit row) that closes it.
- The before/after test counts for the shared and server packages are recorded.
  </acceptance_criteria>
  <done>The repository is green with 42-16's changes in place, and all three recorded verification gaps are provably closed.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                                                                                                     |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| audit claim → phase closure          | This SUMMARY's audit table is the evidence a re-run of `/gsd-verify-phase` will check Success Criterion 5 against. An unverified claim here re-opens the same gap a third time. |
| gate result → "no regression" belief | `offside.ts` alters live rulings reached from 6+ `gameEngine.ts` call sites; the server suite is the only thing standing between the fix and a silent rule regression.          |

## STRIDE Threat Register

| Threat ID | Category    | Component                                                          | Disposition | Mitigation Plan                                                                                                                                                                                                       |
| --------- | ----------- | ------------------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-65   | Repudiation | Audit asserted as complete without enumeration                     | mitigate    | Task 1 requires a 19-row baseline table reproduced with an ACTUAL column and an explicit row for every unlisted hit; a claim of "no findings" is only acceptable alongside the recorded grep output that produced it. |
| T-42-66   | Tampering   | A red server suite made green by editing a test                    | mitigate    | The inline-fix policy forbids editing test expectations; an acceptance criterion requires `git diff --stat` to show only 42-16's four files, so an edited expectation elsewhere fails the gate visibly.               |
| T-42-67   | Tampering   | Earlier rounds' `isActivePiece` work silently reverted             | mitigate    | Task 1 step D is a regression tripwire pinning `gameEngine.ts` at >= 60 call sites (the count `42-VERIFICATION.md` recorded) plus non-zero counts in `gameHandlers.ts` and `HexGrid.tsx`.                             |
| T-42-68   | Tampering   | A flaky server run misread as a genuine regression, or the reverse | mitigate    | The known vitest worker-crash flake must be re-run per-file with `--pool=forks` and BOTH runs recorded before any conclusion; a genuine regression is routed, never patched inline.                                   |
| T-42-SC   | Tampering   | npm/pnpm installs                                                  | accept      | No packages are installed by this plan. No Package Legitimacy Gate applies.                                                                                                                                           |

</threat_model>

<verification>

- The 19-row shared-package audit table is complete with an ACTUAL column and zero unclassified rows.
- Step C's raw-flag grep confirms no live filter clause outside `stoppagePhases.ts` hand-writes `redCarded`/`onPitch`.
- The server/client tripwire counts meet or exceed the values recorded in `42-VERIFICATION.md`.
- All 10 gate commands are recorded with verdicts; 1-5 and 7-10 exit 0.
- All three `42-VERIFICATION.md` `missing:` items are quoted and marked CLOSED with named evidence.
  </verification>

<success_criteria>

- D-10 item 4's audit is closed for the WHOLE `packages/shared` package by enumeration, not by suspicion — with `offside.ts` explicitly in the table.
- No unfiltered OCCUPANCY-class piece list remains anywhere in `packages/shared`.
- The repository is green and no existing test expectation was altered to get there.
- The phase's three recorded verification gaps each have a named closing artifact, so a re-run of `/gsd-verify-phase 42` can confirm Success Criterion 5 from committed evidence.
  </success_criteria>

<output>
Create `.planning/phases/42-substitution-ux-overhaul/42-17-SUMMARY.md` when done. It MUST contain:

- a `## Shared-Package Piece-List Audit` table (19 baseline rows plus any new finding) with FILE, ENCLOSING FUNCTION, EXPECTED CLASS, ACTUAL CLASS, and EVIDENCE columns,
- a `## Raw-Flag Contract Check` section with step C's full grep output and a line-by-line accounting,
- a `## Server/Client Regression Tripwire` table with the three recorded counts and their thresholds,
- a `## Whole-Repo Quality Gate` table (10 rows) with a verdict per command and any fallback named,
- a `## Verification Gap Closure` section quoting all three `missing:` items from `42-VERIFICATION.md` lines 45-48 verbatim, each marked CLOSED with the closing plan and evidence, or OPEN with a routed gap item,
- a `## Next Step` line stating that `/gsd-verify-phase 42` should be re-run to re-score Success Criterion 5.
  </output>
