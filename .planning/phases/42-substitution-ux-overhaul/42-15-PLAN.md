---
phase: 42-substitution-ux-overhaul
plan: 15
type: execute
wave: 3
depends_on: ['42-11', '42-12', '42-13', '42-14']
files_modified: []
autonomous: false
gap_closure: true
requirements: [SUB-11, SUB-12, SUB-16, SUB-17, SUB-18, BUG-38]

must_haves:
  truths:
    - 'The whole repository passes the same 10-command quality gate 42-10 ran, in the same order.'
    - 'Each of the 7 gap items from 42-10-SUMMARY.md is re-verified by the human in a live two-browser session, item by item, with the result recorded verbatim.'
    - 'Phase 42 closes as passed only if every one of the 7 items is confirmed fixed; any remaining failure is recorded verbatim and routed to a further gap-closure round rather than fixed inline.'
  artifacts:
    - path: '.planning/phases/42-substitution-ux-overhaul/42-15-SUMMARY.md'
      provides: 'the quality-gate table and the verbatim human verification record closing (or re-opening) Phase 42'
      contains: 'Human Verification Outcome'
  key_links:
    - from: '.planning/phases/42-substitution-ux-overhaul/42-10-SUMMARY.md'
      to: '.planning/phases/42-substitution-ux-overhaul/42-15-SUMMARY.md'
      via: 'the 7 Gap Closure Items re-verified one-for-one'
      pattern: 'gap item'
---

<objective>
Close out Phase 42's gap-closure round: re-run the whole-repository quality gate, then put the four gap-closure plans' output in front of the human verifier in a live two-browser session that walks the SAME 7 items `42-10-SUMMARY.md` recorded as failures.

Purpose: `42-10` failed verification on Sections C and D and produced 7 discrete items. Plans 42-11 through 42-14 close all 7. This plan is the gate that decides whether Phase 42 finally closes as `passed` or goes round again — and, per the discipline 42-10 established, it records any remaining failure verbatim rather than patching it inline.

Output: a quality-gate table, a verbatim per-item human verification record, and a phase verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md
@.planning/phases/42-substitution-ux-overhaul/42-UI-SPEC.md
@.planning/phases/42-substitution-ux-overhaul/42-10-SUMMARY.md
@.planning/phases/42-substitution-ux-overhaul/42-11-SUMMARY.md
@.planning/phases/42-substitution-ux-overhaul/42-12-SUMMARY.md
@.planning/phases/42-substitution-ux-overhaul/42-13-SUMMARY.md
@.planning/phases/42-substitution-ux-overhaul/42-14-SUMMARY.md
</context>

## Artifacts this phase produces

See the table in `42-01-PLAN.md` under "Artifacts this phase produces". This plan creates no source symbol. Its only artifact is `42-15-SUMMARY.md`.

**Inline-fix policy:** this plan modifies no production file. If Task 1's gate or Task 2's verification surfaces a defect, it is recorded verbatim in the SUMMARY and routed to a further gap-closure round — the same rule 42-10 Task 3 operated under. The one exception is a gate command that fails for a reason wholly unrelated to Phase 42 (e.g. the pre-existing `packages/shared` typescript-eslint file-count-cap issue 42-10 hit): document the failure, apply the same documented scoped fallback 42-10 used, and move on.

<tasks>

<task type="auto">
  <name>Task 1: Whole-repo quality gate and gap-closure grep audit</name>
  <files>(none — read-only verification; no source file is modified by this task)</files>
  <read_first>
    - .planning/phases/42-substitution-ux-overhaul/42-10-SUMMARY.md § "Whole-Repo Quality Gate (Task 2)" — the exact 10-command table and order, and the documented `pnpm lint` fallback (`npx eslint` scoped to the phase's files) used when the pre-existing `packages/shared` typescript-eslint file-count-cap issue fires.
    - .planning/phases/42-substitution-ux-overhaul/42-11-SUMMARY.md, 42-12-SUMMARY.md, 42-13-SUMMARY.md, 42-14-SUMMARY.md — every file each plan touched, and each plan's own recorded acceptance evidence, so the audit greps below can be checked against what actually shipped.
    - CLAUDE.md — project commands and conventions.
  </read_first>
  <action>
**A. Run the gate in this exact order**, recording pass/fail and any output for each:

1. `pnpm --filter @counter-attack/shared build`
2. `pnpm -r typecheck`
3. `pnpm --filter @counter-attack/shared test -- --pool=forks`
4. `pnpm --filter @counter-attack/server test -- --pool=forks`
5. `pnpm --filter @counter-attack/client test -- --pool=forks`
6. `pnpm lint` (on the documented pre-existing `packages/shared` cap failure, fall back to `npx eslint` scoped to the files listed in the four gap-closure SUMMARYs and say so explicitly)
7. `pnpm format:check`
8. `pnpm stylelint`
9. `pnpm knip`
10. `pnpm --filter @counter-attack/client check-contrast`

**B. Gap-closure grep audit.** Run and record each result:

- `grep -rn "DEFENDER_TOO_CLOSE" packages/*/src` → expect zero matches (gap item 7 closed).
- `grep -rn "color-speed-standard-bg" packages/client/src/components/GameBoard.module.css` → expect zero matches (gap item 2 closed).
- `grep -rn "resumeCtaRow\|\.resumeCta" packages/client/src/components/GameBoard.tsx packages/client/src/components/GameBoard.module.css` → expect zero matches (gap item 4 closed).
- `grep -rn "REPOSITION_SLOT_OCCUPIED" packages/server/src/gameEngine.ts packages/client/src/components/LineupAssignmentScreen.tsx` → expect matches in both (gap item 6 closed).
- `grep -rn ">ROSTER<" packages/client/src/components/GameBoard.tsx` → expect exactly one match (gap item 2's rename).
- `grep -rn "var(--color-cta-pending-bg)" packages/client/src/components/LineupAssignmentScreen.module.css` → expect two matches (gap item 5, both Cancel surfaces).
- `grep -rn "glyphCardColor" packages/client/src/components/DraftPackCarousel.tsx` → expect two matches (gap item 1).

**C. Cross-plan consistency check.** Confirm no gap-closure plan reverted another's change: diff the current tree against the four SUMMARYs' recorded file lists and confirm every file appears in exactly the plans that claimed it, with no unclaimed modification. Record any discrepancy.

Do not fix anything found. Record it.
</action>
<verify>
<automated>pnpm -r typecheck && pnpm --filter @counter-attack/shared test -- --pool=forks && pnpm --filter @counter-attack/server test -- --pool=forks && pnpm --filter @counter-attack/client test -- --pool=forks</automated>
</verify>
<acceptance_criteria>

- All 10 gate commands are recorded in the SUMMARY as a table with a pass/fail verdict each; any fallback used is named and justified.
- All 7 grep audits return the expected counts stated above; any deviation is recorded as a finding with the actual output.
- Commands 1-5 and 7-10 exit 0.
- The cross-plan consistency check reports zero unclaimed file modifications, or names them.
  </acceptance_criteria>
  <done>The repository is green and every gap-closure change is grep-verifiable in the tree.</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live two-browser re-verification of all 7 gap items</name>
  <files>(none — verification only; no file is modified by this checkpoint)</files>
  <read_first>
    - .planning/phases/42-substitution-ux-overhaul/42-10-SUMMARY.md § "Task 3: Human Verification Outcome (Verbatim Record)" and § "Gap Closure Items" — the 7 items being re-verified and the record format the outcome must be written back in.
    - .planning/phases/42-substitution-ux-overhaul/42-11-SUMMARY.md through 42-14-SUMMARY.md — what each plan actually shipped, so the walkthrough below can be corrected if any plan deviated.
  </read_first>
  <action>
Present the walkthrough in `<how-to-verify>` to the user, wait for the resume signal, and record the outcome verbatim in `42-15-SUMMARY.md` — item by item, using the numbered item scheme from `42-10-SUMMARY.md` § "Gap Closure Items".

Do NOT fix any reported failure inline. Record it verbatim (including the step number and what the verifier actually observed), set the phase verdict to `gaps_found`, and emit a numbered `## Gap Closure Items` list in the same format `42-10-SUMMARY.md` used so the next round can be planned directly from it. Only if every one of the 7 items is confirmed does the verdict become `passed`.
</action>
<what-built>
Four gap-closure plans landed against the 7 items recorded in `42-10-SUMMARY.md` § "Gap Closure Items":

- **42-11** — a red-carded bench card now shows only the `RED CARD` badge; the duplicate card glyph is suppressed (injury glyph, yellow-card glyph, `OUT` badge and the draft-pack row all unchanged).
- **42-12** — the side strip label reads `ROSTER` and paints solid `--color-cta-ready-bg` green with a white label when a substitution is actionable; Substitute/Cancel and Resume are now two standard-size buttons in one row directly under the bench, inside the roster panel's own scroll flow; both Cancel affordances (mode-level and confirm popup) are orange.
- **42-13** — a defending piece inside the 2-hex free-kick bubble is auto-moved to the minimum legal distance (ring 3, toward its own goal) both on entering and on ending a defending stage; the silent `DEFENDER_TOO_CLOSE` dead-end is gone.
- **42-14** — a reposition that would land two active players on one hex is refused with a specific roster-panel message, and the SENT OFF slot stops advertising a drop it cannot accept — while a sent-off slot whose hex is free is still a legal reposition destination (D-05).
  </what-built>
  <how-to-verify>
  Run `pnpm --filter @counter-attack/shared build`, then start the server and client dev servers. Open two browsers, create a room in one, join with the code in the other, and play into a live match. Walk the items in this order and report each one by number.

**Item 2 + item 3 + item 4 + item 5 — chrome (roster panel):**

1. During normal play (not a stoppage), look at the right-edge vertical strip. Expect: label reads `ROSTER` (not `SUB`), neutral dark strip.
2. Reach a stoppage where a substitution is available. Expect: the ENTIRE strip is solid green with the `ROSTER` label in white. There must be no white strip, no small green rectangle behind the label, and no dark-green text. — **item 2**
3. Click the strip to open the roster panel. Scroll to the bottom of the panel content. Expect: one row containing a `Substitute` button and a green `Resume` button side by side, both the same standard size, sitting directly under the bench carousel and scrolling with the content rather than being pinned to the bottom of the window. — **items 3 and 4**
4. Click `Substitute`. Expect: the same button becomes an orange `Cancel`, still beside `Resume`. — **item 5**
5. Drag a bench card onto an on-pitch card to stage a substitution. Expect: the confirmation popup's `Cancel` is orange and its `Confirm Substitution` is green. Click `Cancel` — you stay in substitution mode with the selection reset.
6. Click `Resume`. Expect: the panel closes and play continues.

**Item 1 — bench badge:**

7. Get a player sent off (or use a state that already has one). Open the roster panel and look at that player's bench card. Expect: exactly ONE card indicator — the red `RED CARD` badge. There must be no second, plainer card icon beside or overlapping it. — **item 1**
8. If a bench player is both carded and injured, confirm the injury glyph is still visible.
9. Confirm a merely substituted-out player still shows the `OUT` badge, and a yellow-carded bench player still shows its yellow glyph.

**Item 6 — red-card slot swap:**

10. With a sent-off player on your team, move one of your active players onto the hex where the sent-off player was standing (this is legal now — BUG-38).
11. At the next stoppage, open the roster panel in positioning mode and try to drag another active player onto that sent-off player's SENT OFF slot. Expect: the slot does NOT light up as a drop target, the drop does nothing, and the two active players are never shown stacked in one slot. — **item 6**
12. Now move that active player OFF the sent-off player's hex, return to the roster panel, and drag an active player onto the SENT OFF slot again. Expect: this time it works — the slot is still a real, droppable slot (D-05).

**Item 7 — free-kick wall distance:**

13. Concede a foul that awards a free kick, with one of the defending team's players standing right next to the foul hex. Expect: as soon as the free kick is awarded, that defender has already been moved back to at least 3 hexes from the ball.
14. During the defending team's placement stage, move a defender back in close to the ball, then click Ready. Expect: the defender is automatically pushed back out to the minimum legal distance and the stage advances — the Ready button must NOT silently do nothing. — **item 7**
15. Confirm the free-kick taker standing on the ball hex is never moved by any of this, and that no two players end up on the same hex.

**Regression sweep (must still pass — these were CONFIRMED PASS in `42-10`):**

16. Positioning mode: drag one on-field player onto another to swap positions; it applies instantly with no popup.
17. Positioning-mode dragging is disabled while a game action is selected on the pitch.
18. Bench cards are inert in positioning mode.
19. The `Substitute` button is disabled at 3/3 subs used.
20. A sent-off player triggers no deflection and no steal attempt, their old hex no longer blocks movement, and their state survives a kick-off reset.
    </how-to-verify>
    <verify>
    <human-check>Every one of the 7 gap items receives an explicit verdict from the verifier, recorded verbatim by item number in 42-15-SUMMARY.md.</human-check>
    <automated>node -e "const t=require('fs').readFileSync('.planning/phases/42-substitution-ux-overhaul/42-15-SUMMARY.md','utf8'); const ok=/Human Verification Outcome/.test(t)&&/Phase Verdict/.test(t); process.exit(ok?0:1)"</automated>
    </verify>
    <acceptance_criteria>

- `42-15-SUMMARY.md` contains a `## Task 2: Human Verification Outcome (Verbatim Record)` section with a subsection per gap item 1-7 plus the regression sweep.
- It contains a `## Phase Verdict` section reading exactly `passed` or `gaps_found`.
- If `gaps_found`, it also contains a numbered `## Gap Closure Items` list in `42-10-SUMMARY.md`'s format.
- `git diff --stat packages/` is empty for this task — no production file was touched in response to the verification result.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 42, or report the failing item numbers with what you actually observed. Any failure is recorded verbatim in `42-15-SUMMARY.md` and routed to a further gap-closure round rather than fixed inline.</resume-signal>
  <done>Every gap item has an explicit human verdict and the phase verdict is recorded.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                                                                                             |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| human verdict → phase closure | The `approved` resume signal is the only thing that moves Phase 42 from `gaps_found` to `passed`. It is not inferred from a green gate. |

## STRIDE Threat Register

| Threat ID | Category    | Component                                         | Disposition | Mitigation Plan                                                                                                                                                                                    |
| --------- | ----------- | ------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-58   | Repudiation | Verification outcome recorded imprecisely         | mitigate    | The verifier reports by item NUMBER against the 7 items from `42-10-SUMMARY.md`; the SUMMARY records each one verbatim, matching 42-10's own record format.                                        |
| T-42-59   | Tampering   | A green automated gate mistaken for phase closure | mitigate    | Task 2 is `gate="blocking"`; the plan is `autonomous: false` and explicitly states the phase verdict comes from the human, not from Task 1's commands.                                             |
| T-42-60   | Tampering   | A reported failure quietly patched inline         | mitigate    | The inline-fix policy is stated in the plan body, restated in Task 2's `<action>`, and enforced by an acceptance criterion asserting `git diff --stat packages/` is empty for the checkpoint task. |
| T-42-SC   | Tampering   | npm/pnpm installs                                 | accept      | No packages installed by this plan. No Package Legitimacy Gate applies.                                                                                                                            |

</threat_model>

<verification>

- All 10 quality-gate commands recorded with verdicts.
- All 7 grep audits recorded with actual output.
- The human verification outcome recorded verbatim, item by item.
  </verification>

<success_criteria>

- The repository passes the full gate (or every deviation is documented with the same justification standard 42-10 used).
- Each of the 7 gap items has an explicit human verdict.
- The SUMMARY states an unambiguous phase verdict: `passed` (all 7 confirmed) or `gaps_found` (with the residual items listed for the next round).
  </success_criteria>

<output>
Create `.planning/phases/42-substitution-ux-overhaul/42-15-SUMMARY.md` when done. It MUST contain:

- a `## Whole-Repo Quality Gate` table (10 rows) and a `## Gap-Closure Grep Audit` table (7 rows),
- a `## Task 2: Human Verification Outcome (Verbatim Record)` section organised by gap item number 1-7 plus the regression sweep,
- a `## Phase Verdict` section stating `passed` or `gaps_found`, and — if `gaps_found` — a numbered `## Gap Closure Items` list in the same format `42-10-SUMMARY.md` used, so the next round can be planned from it directly.
  </output>
