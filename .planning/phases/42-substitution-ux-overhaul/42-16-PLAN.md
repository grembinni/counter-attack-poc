---
phase: 42-substitution-ux-overhaul
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/passValidator.ts
  - packages/shared/src/passValidator.test.ts
  - packages/shared/src/offside.ts
  - packages/shared/src/offside.test.ts
autonomous: true
gap_closure: true
requirements: [BUG-38]

must_haves:
  truths:
    - "A LONG pass may land within 5 hexes of a red-carded/benched teammate and adjacent to a red-carded/benched opponent — a sent-off player's frozen hex no longer produces LANDING_RESTRICTED."
    - 'A red-carded or benched opponent is never counted as a covering defender by opposingPiecesEqualOrAhead, so it can neither prevent an offside trigger nor clear an existing offside flag.'
    - 'A red-carded or benched piece is never itself newly flagged offside by evaluateOffside, and never retains a prior offside flag once dismissed.'
    - 'Both fixes are proven by committed regression tests that fail against the pre-fix code, not merely by reading the source.'
  artifacts:
    - path: 'packages/shared/src/passValidator.ts'
      provides: 'isActivePiece-filtered LONG landing-restriction ownTeammates/opponents lists (D-09/D-10)'
      contains: 'isActivePiece'
    - path: 'packages/shared/src/offside.ts'
      provides: 'isActivePiece-filtered opponent counting and offside-flag evaluation (D-09/D-10 item 4)'
      contains: 'isActivePiece'
    - path: 'packages/shared/src/passValidator.test.ts'
      provides: 'BUG-38 regression coverage for the LONG landing-restriction branch'
      contains: 'LANDING_RESTRICTED'
    - path: 'packages/shared/src/offside.test.ts'
      provides: 'BUG-38 regression coverage for offside opponent counting and flag evaluation'
      min_lines: 100
      contains: 'opposingPiecesEqualOrAhead'
  key_links:
    - from: 'packages/shared/src/offside.ts'
      to: 'packages/shared/src/stoppagePhases.ts isActivePiece'
      via: "named import from './stoppagePhases.js'"
      pattern: "import \\{ isActivePiece \\} from './stoppagePhases.js'"
    - from: 'packages/shared/src/passValidator.ts LONG branch'
      to: 'packages/shared/src/stoppagePhases.ts isActivePiece'
      via: 'existing named import, applied to the two landing-restriction filters'
      pattern: 'isActivePiece'
---

<objective>
Close the two residual BUG-38 unfiltered-piece-list sites `42-VERIFICATION.md` recorded as Success Criterion 5's remaining failure: `passValidator.ts`'s LONG-pass landing-restriction check and `offside.ts`'s opponent counting / offside-flag evaluation. Both are mechanical applications of the existing shared `isActivePiece` predicate (D-09), plus the regression coverage neither site has today.

Purpose: `stoppagePhases.ts:80-104` documents a hard contract — "eligibility/occupancy/ZoI/interceptor lists, anywhere in `packages/shared`, must be built through this predicate rather than a hand-written inline clause." These two sites violate it. `passValidator.ts:131-140` was independently flagged as code-review WARNING WR-01 in `42-REVIEW.md`. `offside.ts:134-141` was proven live by the verifier's executed probe: a `redCarded: true` opponent standing equal-or-ahead of an attacker returned an opposing count of 2 instead of 1, so a sent-off player is currently still playing opponents onside in every `evaluateOffside` call — reached from 6+ live call sites in `gameEngine.ts` on every movement and pass resolution.

Output: both sites filtered through `isActivePiece`, and two regression suites that fail against the pre-fix code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-substitution-ux-overhaul/42-CONTEXT.md
@.planning/phases/42-substitution-ux-overhaul/42-VERIFICATION.md
@.planning/phases/42-substitution-ux-overhaul/42-01-SUMMARY.md
@packages/shared/src/stoppagePhases.ts
</context>

## Artifacts this phase produces

See the table in `42-01-PLAN.md` under "Artifacts this phase produces". This plan creates **no new exported symbol**. It consumes the existing `isActivePiece` predicate (created in 42-01, `packages/shared/src/stoppagePhases.ts`) at two additional call sites and adds test-only `describe` blocks. Nothing here introduces a new public API, type, CSS class, or file.

**Scope fence:** this plan touches exactly the four files in `files_modified`. Do NOT touch `LineupAssignmentScreen.tsx`, `GameBoard.tsx`, any `.module.css`, `gameEngine.ts`, `gameHandlers.ts`, `moveValidator.ts`, `outOfBounds.ts`, or `fouls.ts` — Success Criteria 1-4 are fully verified and the other `packages/shared` sites are already correctly filtered. If a new unfiltered site is discovered while working, record it in the SUMMARY for plan 42-17's audit rather than fixing it here.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filter the LONG-pass landing restriction through isActivePiece (WR-01)</name>
  <files>packages/shared/src/passValidator.ts, packages/shared/src/passValidator.test.ts</files>
  <read_first>
    - `packages/shared/src/passValidator.ts` lines 96-175 — the whole guard sequence. Note the three sites ALREADY converted by the BUG-38 sweep and the exact inline-comment wording each carries: the STANDARD `opponentPieces` filter (`:101-106`, comment "BUG-38: isActivePiece applied at the array-construction site — the downstream `.some()` predicate at line ~104 is left untouched, opponentPieces is pre-filtered."), the HIGH/LONG adjacent-blocker `opponentPieces` filter (`:112-116`), the `destDefender` lookup (`:151-161`), and the `rollIntercepts` `opponents` list (`:169-171`). The new edits must match this comment discipline.
    - `packages/shared/src/passValidator.ts` line 20 — `import { isActivePiece } from './stoppagePhases.js';` is ALREADY present. Do not add a duplicate import.
    - `packages/shared/src/stoppagePhases.ts` lines 80-107 — the `isActivePiece` contract doc and the explicit "Do NOT collapse this to a single clause" note (both `redCarded !== true` and `onPitch !== false` are checked).
    - `packages/shared/src/passValidator.test.ts` lines 1-52 — the fixtures the new tests must reuse: `basePiece` (home, `id: 'p1'`, at `{q:0,r:0}`), `makeOpponent(id, q, r)` (away), `makeTeammate(id, q, r)` (home), and `baseState` (phase `'PASS'`, `pieces: [basePiece]`, ball carried by `'p1'`).
    - `packages/shared/src/passValidator.test.ts` lines 186-220 — the THREE existing LONG landing tests that are the active-piece control cases (teammate at `{6,5}` restricts a pass to `{10,5}`; opponent at `{11,5}` restricts it; passer self-exclusion). These must remain byte-for-byte unchanged.
    - `packages/shared/src/passValidator.test.ts` lines 260-328 — the existing `describe('BUG-38: red-carded/benched opponent exclusion', ...)` block, its comment header, and its naming style (`redCarded1`, `benched1`, "does NOT block ... when a red-carded opponent ..."). New cases go inside this same block.
    - `.planning/phases/42-substitution-ux-overhaul/42-REVIEW.md` § "WR-01" — the exact defect statement and the fix already proposed there.
  </read_first>
  <behavior>
    Add these cases inside the existing `describe('BUG-38: red-carded/benched opponent exclusion', ...)` block in `passValidator.test.ts`. Each mirrors an existing active-piece control test at `:186-202` with only the eligibility flag changed, so the pair proves the predicate rather than the geometry.

    - A LONG pass from `{q:0,r:0}` to `{q:10,r:5}` with a `redCarded: true` teammate at `{q:6,r:5}` (hexDistance 4, i.e. `<= 5`) returns `ok: true` — the pre-fix code returns `LANDING_RESTRICTED`.
    - The same pass with an `onPitch: false` (and NOT `redCarded`) teammate at `{q:6,r:5}` returns `ok: true` — proves the two-clause predicate, mirroring the existing `'excludes an onPitch: false opponent (without redCarded) from path blocking, proving the two-clause predicate'` test.
    - A LONG pass from `{q:0,r:0}` to `{q:10,r:5}` with a `redCarded: true` opponent at `{q:11,r:5}` (hexDistance 1) returns `ok: true` — the pre-fix code returns `LANDING_RESTRICTED`.
    - The same pass with an `onPitch: false` opponent at `{q:11,r:5}` returns `ok: true`.
    - A mixed case: an ACTIVE teammate at `{q:6,r:5}` plus a `redCarded: true` opponent at `{q:11,r:5}` still returns `LANDING_RESTRICTED` — proves the fix did not disable the restriction wholesale.

  </behavior>
  <action>
In `packages/shared/src/passValidator.ts`, inside the `if (passType === 'LONG')` block at `:131-140` (guard step 4, "PASS-04 landing constraints"):

- Add `isActivePiece(p) &&` as a leading conjunct to the `ownTeammates` filter, preserving both existing clauses (`p.teamId === piece.teamId` and `p.id !== piece.id`) exactly and in order.
- Add `isActivePiece(p) &&` as a leading conjunct to the `opponents` filter, preserving the existing `p.teamId !== piece.teamId` clause.
- Leave the two downstream `.some((p) => hexDistance(to, p.position) <= N)` predicates and both `<= 5` / `<= 1` thresholds untouched — the arrays are pre-filtered, matching the pattern the STANDARD branch's own comment describes.
- Add one inline `// BUG-38:` comment above the block in the same voice as `:102-103` and `:151`, stating that a sent-off/benched piece's frozen hex no longer restricts a Long Ball landing spot. Keep it to two lines maximum.
- Do not change the `PassResult` union, the guard-precedence ordering, the JSDoc block at `:51-72`, or the `options.maxDistance` behaviour.

Then add the five cases described in `<behavior>` to `passValidator.test.ts`, inside the existing `describe('BUG-38: red-carded/benched opponent exclusion', ...)` block, using `makeTeammate`/`makeOpponent` spread with the flag overridden (the block's established idiom: `{ ...makeOpponent('redCarded1', 5, 0), redCarded: true }`). Use fresh, descriptive ids that do not collide with existing ones in the block (e.g. `longTeammateRedCarded`, `longTeammateBenched`, `longOppRedCarded`, `longOppBenched`). Each new `it` title must name LONG and the flag, matching the block's existing title style.

Run the suite BEFORE applying the source change to confirm the new tests fail with `LANDING_RESTRICTED`, then apply the change and confirm they pass. Record both observations in the SUMMARY.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared test -- --pool=forks src/passValidator.test.ts</automated>
</verify>
<acceptance_criteria>

- `pnpm --filter @counter-attack/shared test -- --pool=forks src/passValidator.test.ts` exits 0 with at least 39 tests passing (34 pre-existing + 5 new).
- `grep -v '^\s*\*' packages/shared/src/passValidator.ts | grep -v '^\s*//' | grep -c 'isActivePiece'` returns exactly `7` (1 import + 6 call sites: STANDARD `opponentPieces`, HIGH/LONG `opponentPieces`, LONG `ownTeammates`, LONG `opponents`, `destDefender`, `rollIntercepts` `opponents`).
- `sed -n '128,150p' packages/shared/src/passValidator.ts` shows `isActivePiece(p) &&` as the FIRST conjunct in both the `ownTeammates` and the `opponents` filter predicates.
- The three pre-existing LONG landing tests (`'rejects LONG pass with LANDING_RESTRICTED when target is within 5 hexes of own teammate'`, `'rejects LONG pass with LANDING_RESTRICTED when target is adjacent to an opponent'`, `'LONG landing check does NOT count the passer as a blocking teammate (self-exclusion)'`) are unmodified and still pass — `git diff packages/shared/src/passValidator.test.ts` shows only additions inside the BUG-38 `describe` block.
- The SUMMARY records the pre-fix failing-test output (the five new cases returning `LANDING_RESTRICTED`) and the post-fix passing output.
  </acceptance_criteria>
  <done>A Long Ball can be played into space vacated by a sent-off player, and the restriction still fires for every active player.</done>
  </task>

<task type="auto" tdd="true">
  <name>Task 2: Filter offside opponent counting and flag evaluation through isActivePiece</name>
  <files>packages/shared/src/offside.ts, packages/shared/src/offside.test.ts</files>
  <read_first>
    - `packages/shared/src/offside.ts` lines 100-205 — `attackingDirection`, `isPastHalfway`, `isAheadOf`, `opposingPiecesEqualOrAhead` (`:134-141`, the confirmed defect), `isOffsideNow` (`:147-153`), `isClearedNow` (`:169-178`, D-40 possession gate), and `evaluateOffside` (`:186-201`, whose `stillFlagged` and `newlyFlagged` derivations are the two additional same-class sites in this file). Also note `OFFSIDE_HALFWAY_Q = 18` and that home attacks toward higher `q` (`+1`), away toward lower `q` (`-1`).
    - `packages/shared/src/offside.ts` lines 1-2 — the current imports (`./types.js`, `./pitch.js`). `isActivePiece` is NOT imported here yet; `stoppagePhases.ts` imports only `./types.js`, so adding `./stoppagePhases.js` creates no import cycle.
    - `packages/shared/src/stoppagePhases.ts` lines 80-107 — the `isActivePiece` contract doc, including the "anywhere in `packages/shared`" clause this file currently violates.
    - `packages/shared/src/offside.test.ts` (all 51 lines) — today it covers ONLY `CORNER_KICK_STAGES` and `cornerKickStageTeam`; it imports nothing from `./types.js` and has no `GameState` fixture.
    - `packages/shared/src/passValidator.test.ts` lines 1-52 — the `basePiece` / `baseState` fixture shape to copy into `offside.test.ts` (every `PlayerPiece` field and every `GameState` field the type requires, including `refereeCard`, `movedPieceIds`, `paceUsedByPieceId`, `movementSlot`, and `ball.lastTouchedBy`).
    - `packages/server/src/__tests__/offside.test.ts` lines 1-100 and lines 179-395 — the EXISTING geometry suite for these same functions (`opposingPiecesEqualOrAhead`, `isOffsideNow`, `isClearedNow`, `evaluateOffside`). No fixture in that file sets `redCarded` or `onPitch`, so every existing case must keep passing unchanged after this fix. Read it to confirm the assumption before editing, and to avoid duplicating its geometry cases.
    - `.planning/phases/42-substitution-ux-overhaul/42-VERIFICATION.md` lines 32-44 — the verifier's probe result: a `redCarded: true` opponent positioned equal-or-ahead returned a count of 2 instead of 1.
  </read_first>
  <behavior>
    Add a new `describe('BUG-38: red-carded/benched piece exclusion', ...)` block to `packages/shared/src/offside.test.ts`, built on a local `GameState` fixture modelled on `passValidator.test.ts`'s `basePiece`/`baseState`.

    Fixture geometry (home attacks toward higher `q`; `OFFSIDE_HALFWAY_Q` is 18):
    - `homeFwd` — home, `{q:25, r:10}` (past halfway, ahead of the ball).
    - `homeCarrier` — home, `{q:20, r:10}`, and `ball` = `{ position: {q:20,r:10}, carrierId: <homeCarrier id>, lastTouchedBy: null }` so the ball is possessed (required by `isClearedNow`'s D-40 gate).
    - `awayActive` — away, `{q:30, r:10}` (equal-or-ahead of `homeFwd`).
    - `awayDismissed` — away, `{q:26, r:10}` (also equal-or-ahead), flagged `redCarded: true` in one variant and `onPitch: false` in the other.

    Expected behaviour:
    - `opposingPiecesEqualOrAhead(state, homeFwd)` returns `1` with a `redCarded: true` `awayDismissed`, and `1` with an `onPitch: false` `awayDismissed` — the pre-fix code returns `2` in both cases (this is exactly the verifier's probe).
    - Control: with BOTH away pieces active, it returns `2` — the count logic itself is unchanged.
    - `isOffsideNow(state, homeFwd)` returns `true` when `awayDismissed` is dismissed (count drops to 1, satisfying D-21 condition 3) and `false` when both away pieces are active.
    - `isClearedNow(state, homeFwd)` returns `false` when `awayDismissed` is dismissed (count 1, ball possessed, `homeFwd` ahead of ball) and `true` when both are active (count >= 2 clears).
    - `evaluateOffside(state)` returns exactly `[homeFwd.id]` when `awayDismissed` is dismissed, and `[]` when both away pieces are active.
    - A home piece that is itself `redCarded: true`, placed at `{q:28, r:10}` (past halfway and ahead of the ball, with only one active away opponent equal-or-ahead), is NEVER present in `evaluateOffside`'s result.
    - A home piece already listed in `state.offsidePieceIds` that is now `redCarded: true` is DROPPED from `evaluateOffside`'s result — a dismissed player does not carry a sticky offside flag.

  </behavior>
  <action>
In `packages/shared/src/offside.ts`:

- Add `import { isActivePiece } from './stoppagePhases.js';` alongside the existing `./types.js` and `./pitch.js` imports.
- In `opposingPiecesEqualOrAhead` (`:134-141`), add `if (!isActivePiece(opp)) return false;` as the FIRST statement inside the `.filter` callback, above the existing `if (opp.teamId === piece.teamId) return false;` guard. Leave the `(opp.position.q - piece.position.q) * dir >= 0` equal-or-ahead arithmetic and the `.length` unchanged.
- In `evaluateOffside` (`:186-201`), add `if (!isActivePiece(piece)) return false;` to the `stillFlagged` filter immediately after the existing defensive `if (!piece) return false;` line, and add `isActivePiece(p) &&` as a leading conjunct to the `newlyFlagged` filter predicate (before the existing `!stillFlagged.includes(p.id)` and `isOffsideNow(state, p)` clauses).
- Leave `piecesById` (`:188`) and `triggerOffsideFoul`'s `state.pieces.find((p) => p.id === offenderId)` (`:248`) UNCHANGED — both are by-id lookups, not eligibility/occupancy lists. Add a one-line `// BUG-38:` audit comment at each explaining the classification, in the same voice `outOfBounds.ts:191-196` and `passValidator.ts:102-103` use, so plan 42-17's audit can classify them without re-deriving the reasoning.
- Extend `opposingPiecesEqualOrAhead`'s JSDoc (`:128-133`) with a sentence stating that dismissed/benched pieces are excluded via `isActivePiece` per BUG-38 (D-09), and note the behavioural consequence explicitly: excluding a dismissed opponent LOWERS the covering-defender count, which makes offside FIRE more readily (D-21 condition 3) and makes an existing flag CLEAR less readily (D-22 condition b). This direction is intended — a sent-off player cannot play an attacker onside.
- Do not change `isPastHalfway`, `isAheadOf`, `attackingDirection`, `OFFSIDE_HALFWAY_Q`, `FREE_KICK_STAGES`, `CORNER_KICK_STAGES`, either stage-team helper, `isClearedNow`'s D-40 possession gate, or any part of `triggerOffsideFoul`'s returned state.

Then add the fixture and the `describe` block specified in `<behavior>` to `packages/shared/src/offside.test.ts`, importing `opposingPiecesEqualOrAhead`, `isOffsideNow`, `isClearedNow`, and `evaluateOffside` from `./offside.js` and `GameState`/`PlayerPiece` types from `./types.js`. Keep the two existing `describe` blocks (`CORNER_KICK_STAGES`, `cornerKickStageTeam`) unchanged.

Run the shared suite BEFORE applying the source change to confirm the count assertions fail with `2` where `1` is expected, then apply the change and confirm they pass. Then run the server-side geometry suite to prove no existing offside case regressed. Record all three observations in the SUMMARY.
</action>
<verify>
<automated>pnpm --filter @counter-attack/shared build && pnpm --filter @counter-attack/shared test -- --pool=forks src/offside.test.ts && pnpm --filter @counter-attack/server test -- --pool=forks src/**tests**/offside.test.ts</automated>
</verify>
<acceptance_criteria>

- `pnpm --filter @counter-attack/shared test -- --pool=forks src/offside.test.ts` exits 0, and the file's test count grew by at least 9 over the current 5.
- `pnpm --filter @counter-attack/server test -- --pool=forks src/__tests__/offside.test.ts` exits 0 with ZERO changed expectations — `git diff --stat packages/server/src/__tests__/offside.test.ts` is empty.
- `grep -v '^\s*\*' packages/shared/src/offside.ts | grep -v '^\s*//' | grep -c 'isActivePiece'` returns exactly `4` (1 import + `opposingPiecesEqualOrAhead` + `evaluateOffside` `stillFlagged` + `evaluateOffside` `newlyFlagged`).
- `grep -n "from './stoppagePhases.js'" packages/shared/src/offside.ts` returns exactly one match, and `pnpm --filter @counter-attack/shared build` exits 0 (no import cycle).
- The SUMMARY reproduces the verifier's probe as a committed assertion: it states the pre-fix `opposingPiecesEqualOrAhead` result (`2`) and the post-fix result (`1`) for the fixture described in `<behavior>`.
- `pnpm -r typecheck` exits 0.
  </acceptance_criteria>
  <done>A sent-off opponent no longer plays an attacker onside, and a sent-off player is never flagged offside themselves.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                | Description                                                                                                                                                                                   |
| ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client socket event → server handler → shared validator | An untrusted client emits a pass/move action; `gameHandlers.ts` calls `validatePass` and `gameEngine.ts` calls `evaluateOffside`. Both edited functions sit on the server-authoritative side. |
| `packages/shared` module → both packages                | These are pure functions imported by BOTH the server (authoritative) and the client (UI hints). A change here alters server rulings, not just rendering.                                      |

## STRIDE Threat Register

| Threat ID | Category               | Component                                             | Disposition | Mitigation Plan                                                                                                                                                                                                                                                         |
| --------- | ---------------------- | ----------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-42-61   | Tampering              | `validatePass` LONG landing restriction               | mitigate    | The filter is loosened only for pieces the server itself flagged `redCarded`/`onPitch:false` during booking resolution. A client cannot set those flags — they are written server-side in `gameEngine.ts`'s booking branch and broadcast in the authoritative snapshot. |
| T-42-62   | Elevation of Privilege | `opposingPiecesEqualOrAhead` covering-defender count  | mitigate    | `evaluateOffside` is only ever invoked from `gameEngine.ts` on server-side state; no client input reaches the predicate. The change tightens rulings (offside fires more readily), so it cannot be abused to legitimise an otherwise-illegal attacking position.        |
| T-42-63   | Tampering              | Over-broad edit silently weakening an unrelated guard | mitigate    | The scope fence names the four editable files; acceptance criteria pin exact `isActivePiece` occurrence counts (7 in `passValidator.ts`, 4 in `offside.ts`) and require `git diff --stat` on `packages/server/src/__tests__/offside.test.ts` to be empty.               |
| T-42-64   | Repudiation            | Fix claimed without proof it changed behaviour        | mitigate    | Both tasks are `tdd="true"`: the SUMMARY must record the pre-fix failing output and the post-fix passing output, reproducing the verifier's probe (`2` → `1`) as a committed assertion.                                                                                 |
| T-42-SC   | Tampering              | npm/pnpm installs                                     | accept      | No packages are installed by this plan. No Package Legitimacy Gate applies.                                                                                                                                                                                             |

</threat_model>

<verification>

- `pnpm --filter @counter-attack/shared build` exits 0.
- `pnpm -r typecheck` exits 0.
- `pnpm --filter @counter-attack/shared test -- --pool=forks` exits 0 (whole shared suite, not just the two edited files).
- `pnpm --filter @counter-attack/server test -- --pool=forks src/__tests__/offside.test.ts` exits 0 with no changed expectations.
- The `isActivePiece` occurrence counts are exactly 7 (`passValidator.ts`) and 4 (`offside.ts`) with comment lines filtered out.
  </verification>

<success_criteria>

- `passValidator.ts`'s LONG landing-restriction `ownTeammates` and `opponents` filters both lead with `isActivePiece`, closing code-review WR-01.
- `offside.ts` imports `isActivePiece` and applies it at `opposingPiecesEqualOrAhead` and at both `evaluateOffside` derivations; the two by-id lookups are documented as CONSTRUCTION-class and deliberately unchanged.
- Both fixes carry committed regression tests that demonstrably failed before the source change.
- No existing test expectation anywhere in the repository was modified to accommodate the change.
  </success_criteria>

<output>
Create `.planning/phases/42-substitution-ux-overhaul/42-16-SUMMARY.md` when done. It MUST contain:

- a `## Pre-Fix / Post-Fix Evidence` section with the failing test output captured BEFORE each source edit and the passing output after, including the verifier's probe reproduced as `opposingPiecesEqualOrAhead` returning `2` pre-fix and `1` post-fix,
- a `## isActivePiece Occurrence Audit` table recording the comment-filtered grep counts for `passValidator.ts` and `offside.ts` with each match's enclosing function,
- a `## Classification Notes` list naming `offside.ts:188` (`piecesById`) and `offside.ts:248` (`triggerOffsideFoul` offender lookup) as CONSTRUCTION-class by-id lookups left unchanged, with the reason,
- a `## Test Counts` line giving before/after counts for `packages/shared/src/passValidator.test.ts`, `packages/shared/src/offside.test.ts`, and `packages/server/src/__tests__/offside.test.ts`.
  </output>
