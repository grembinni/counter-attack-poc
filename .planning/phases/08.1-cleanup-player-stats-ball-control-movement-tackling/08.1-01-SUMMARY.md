---
plan: 08.1-01
status: complete
phase: 08.1-cleanup-player-stats-ball-control-movement-tackling
completed: 2026-06-05
key-files:
  created: []
  modified:
    - packages/shared/src/teams.ts
    - packages/shared/src/teams.test.ts
self-check: PASSED
---

## Summary

Redesigned both hardcoded squads in `teams.ts` to the rulebook-correct 1-6 attribute scale, tier-balanced to the Counter Attack tier system (1/2/3/3/2 per squad), and updated `teams.test.ts` to enforce the 6 ceiling and tier distribution.

## What Was Built

- **teams.ts**: HOME_SQUAD and AWAY_SQUAD rewritten on the 1-6 attribute scale. Each squad has exactly the 1/2/3/3/2 tier distribution (Tier 1=34+, Tier 2=32-33, Tier 3=30-31, Tier 4=28-29, Tier 5=26-27). Outfielders have saving/handling/aerialAbility=0 (GK-only attributes per D-01); GKs have highPass=0 and shooting>=1. All id/teamId/name/role/position fields preserved. Header comment updated to reference 1-6 scale (D-01, D-03).
- **teams.test.ts**: Ceiling assertion updated from <=10 to <=6. `minForAttr` helper allows saving/handling/aerialAbility=0 for non-GK roles and highPass=0 for GKs. `getTier()` helper added. Tier distribution test asserts 1/2/3/3/2 for both squads (D-03). 11-player count guard added.

## Verification

- `pnpm --filter @counter-attack/shared test -- teams` — 14 tests GREEN
- `pnpm --filter @counter-attack/shared build` — compiles with no type errors

## Deviations

Minor: The `minForAttr` function in teams.test.ts was extended to include `saving: 0` for non-GK roles (originally only aerialAbility and handling were listed), aligning with D-01 which classifies saving as a GK-only attribute.
