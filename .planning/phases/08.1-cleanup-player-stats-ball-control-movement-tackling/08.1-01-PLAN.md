---
phase: 08.1-cleanup-player-stats-ball-control-movement-tackling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/teams.ts
  - packages/shared/src/teams.test.ts
autonomous: true
requirements: [TEAM-01, TEAM-02]
must_haves:
  truths:
    - 'Every attribute on every player in both squads is in the range 1-6 (GK-only attributes saving/handling/aerialAbility are 0 on outfielders; GK highPass is 0)'
    - 'Each squad of 11 players has exactly the tier distribution 1/2/3/3/2 (Tier 1=34+, Tier 2=32-33, Tier 3=30-31, Tier 4=28-29, Tier 5=26-27)'
    - 'Player cards still expose name, role, and all 9 attributes (no field removed)'
  artifacts:
    - path: 'packages/shared/src/teams.ts'
      provides: 'Rebalanced HOME_SQUAD and AWAY_SQUAD on the 1-6 scale'
      contains: 'HOME_SQUAD'
    - path: 'packages/shared/src/teams.test.ts'
      provides: 'Updated ceiling assertion (6) plus tier distribution tests'
      contains: 'getTier'
  key_links:
    - from: 'packages/shared/src/teams.ts'
      to: 'packages/shared/src/types.ts (PlayerPiece)'
      via: 'import type PlayerPiece'
      pattern: 'PlayerPiece'
---

<objective>
Redesign both hardcoded squads in `teams.ts` to the rulebook-correct 1-6 attribute scale, tier-balanced to the Counter Attack tier system (1/2/3/3/2 per squad), and update `teams.test.ts` so the test suite enforces the 6 ceiling and the tier distribution.

Purpose: The current squads use a 1-10 placeholder scale (Phase 4), which makes every attribute comparison (steal threshold, tackle duel, pass accuracy) wrong relative to the rulebook. This is the data foundation that all dice mechanics (Plan 02) compare against. Implements D-01 through D-04 and TEAM-01/TEAM-02.

Output: `teams.ts` rewritten with 22 players (2 squads × 11) all on the 1-6 scale and tier-balanced; `teams.test.ts` updated to catch any future value >6 and to assert the 1/2/3/3/2 distribution for both squads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-cleanup-player-stats-ball-control-movement-tackling/08.1-CONTEXT.md
@.planning/phases/08.1-cleanup-player-stats-ball-control-movement-tackling/08.1-RESEARCH.md
@.planning/phases/08.1-cleanup-player-stats-ball-control-movement-tackling/08.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update teams.test.ts ceiling and add tier distribution tests (Wave 0)</name>
  <files>packages/shared/src/teams.test.ts</files>
  <read_first>
    - packages/shared/src/teams.test.ts (current ceiling assertion at lines ~44-45 and ~91-92 asserting toBeLessThanOrEqual(10); fixture/iteration structure to mirror)
    - packages/shared/src/teams.ts (current squad export names HOME_SQUAD / AWAY_SQUAD and PlayerPiece field list)
    - packages/shared/src/types.ts (PlayerPiece attribute field names: pace, shooting, tackling, dribbling, heading, saving, handling, resilience, aerialAbility, highPass)
    - .planning/phases/08.1-cleanup-player-stats-ball-control-movement-tackling/08.1-RESEARCH.md (Code Example 4: getTier helper and tier distribution test)
  </read_first>
  <behavior>
    - Test: every attribute value for every player in HOME_SQUAD and AWAY_SQUAD is <= 6 (was <= 10)
    - Test: HOME_SQUAD tier distribution is exactly [Tier1=1, Tier2=2, Tier3=3, Tier4=3, Tier5=2]
    - Test: AWAY_SQUAD tier distribution is exactly [Tier1=1, Tier2=2, Tier3=3, Tier4=3, Tier5=2]
    - Test: each squad still has exactly 11 players (regression guard)
    - These tier-distribution tests MUST fail against the current 1-10 teams.ts (RED) before Task 2 rewrites the data
  </behavior>
  <action>
    Change the existing attribute-ceiling assertion in teams.test.ts from `toBeLessThanOrEqual(10)` to `toBeLessThanOrEqual(6)` in both occurrences (the HOME and AWAY iteration blocks). Update the assertion message string from "must be <= 10" to "must be <= 6". Keep the existing `toBeGreaterThanOrEqual(0)` lower bound (0 is valid for GK-only attributes on outfielders per D-01).

    Add a `getTier(total: number): number` helper at module scope returning 1 for total>=34, 2 for >=32, 3 for >=30, 4 for >=28, 5 for >=26, else 0. Add an `ATTRS` const array listing all 9 attribute keys: pace, shooting, tackling, dribbling, heading, saving, handling, resilience, aerialAbility. Add a reusable assertion that, for a given squad, sums all 9 attributes per player (0s count toward the total per D-02), buckets each player into a tier via getTier, and asserts the tier counts equal [1: 1, 2: 2, 3: 3, 4: 3, 5: 2]. Apply it once for HOME_SQUAD and once for AWAY_SQUAD. Cite D-03 in a test description string (e.g. "has the correct tier distribution 1/2/3/3/2 (D-03)").

    Do NOT modify teams.ts in this task — these tests are expected to be RED until Task 2.

  </action>
  <verify>
    <automated>cd packages/shared && pnpm test -- teams 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - teams.test.ts contains `toBeLessThanOrEqual(6)` and does NOT contain `toBeLessThanOrEqual(10)`
    - teams.test.ts contains a `getTier` function definition
    - teams.test.ts contains a test description string referencing "1/2/3/3/2" and "D-03"
    - Running the shared test suite shows the new tier-distribution tests FAILING (RED) while teams.ts still holds 1-10 values — confirming the test exercises real data
  </acceptance_criteria>
  <done>The ceiling assertion is 6, the tier helper and distribution tests exist for both squads, and the tier tests fail against the un-rewritten teams.ts (proving the test is meaningful).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite HOME_SQUAD and AWAY_SQUAD on the 1-6 scale, tier-balanced</name>
  <files>packages/shared/src/teams.ts</files>
  <read_first>
    - packages/shared/src/teams.ts (full current file — preserve id/name/role/position fields, teamId, and the away mirror positions; only rebalance attribute numbers)
    - packages/shared/src/teams.test.ts (the assertions Task 1 just wrote — these define the target: all attrs 1-6, tier distribution 1/2/3/3/2, 11 players each)
    - packages/shared/src/types.ts (PlayerPiece type to ensure no field is dropped)
    - .planning/phases/08.1-cleanup-player-stats-ball-control-movement-tackling/08.1-CONTEXT.md (D-01..D-04 scale and tier rules)
    - .planning/phases/08.1-cleanup-player-stats-ball-control-movement-tackling/08.1-PATTERNS.md (teams.ts section: preserved constraints — handling:0/aerialAbility:0 for outfielders, highPass:0 for GK, shooting:1 min for GK, positions unchanged)
  </read_first>
  <behavior>
    - All attribute values across both squads fall in 1-6, except GK-only attributes (saving, handling, aerialAbility) which are 0 on outfielders and highPass which is 0 on GKs
    - Each squad sums per the tier table to exactly the 1/2/3/3/2 distribution
    - teams.test.ts passes fully (GREEN) after this rewrite
  </behavior>
  <action>
    Rewrite the attribute numbers for all 11 players in HOME_SQUAD and all 11 in AWAY_SQUAD so that:
    - Every role-relevant attribute is in [1,6] (per D-01). Outfield players keep `saving: 0`, `handling: 0`, `aerialAbility: 0` (D-01 — GK-only attributes, 0 = not applicable). GKs keep `highPass: 0` (D-04 carried from Phase 7.1) and `shooting: 1` minimum.
    - Tier total = sum of all 9 attributes (pace+shooting+tackling+dribbling+heading+saving+handling+resilience+aerialAbility); for outfielders the three 0s mean the total equals the sum of the 6 role attributes (D-02).
    - Distribute the 11 players per squad into tiers exactly: 1 player at Tier 1 (total 34+), 2 at Tier 2 (32-33), 3 at Tier 3 (30-31), 3 at Tier 4 (28-29), 2 at Tier 5 (26-27) — per D-03.
    - GK tier reflects GK-relevant attributes (Saving, Handling, Aerial Ability, Pace, Tackling, Resilience) per D-04; place the GK at a realistic tier (high Saving/Handling).
    - On the 1-6 scale, set highPass within role-realistic caps: DEF 2-4, MID 3-5, FWD 3-5 (adjusted from the 1-10 prior D-07 ranges per RESEARCH Pitfall 7 — these are not test-enforced beyond the <=6 ceiling).
    - Make the two squads distinct (different names, different attribute profiles) but matched in overall tier balance so neither team is advantaged (D-03).
    - Preserve every id, teamId, name, role, and position field exactly as they are now (positions are locked from Phase 7.1 D-01; away positions remain q_away = 36 - q_home). Only the attribute numbers change.
    Update the file header comment that currently says "1-10 scale" to state the 1-6 scale and cite D-01/D-03.
  </action>
  <verify>
    <automated>cd packages/shared && pnpm test -- teams 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @counter-attack/shared test` passes with all teams.test.ts tests GREEN (ceiling <=6, tier distribution 1/2/3/3/2 for both squads, 11 players each)
    - teams.ts header comment references the 1-6 scale (no "1-10" remains)
    - HOME_SQUAD and AWAY_SQUAD each export 11 PlayerPiece objects with all original id/teamId/name/role/position fields intact
    - No attribute value exceeds 6 anywhere in teams.ts
  </acceptance_criteria>
  <done>Both squads are on the 1-6 scale, tier-balanced 1/2/3/3/2, positions and identities preserved, and the full shared test suite is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                                                                         |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------- |
| shared package consumers | teams.ts is static data imported by both server engine and client display; no runtime input crosses a boundary here |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                          | Disposition | Mitigation Plan                                                                                                       |
| --------- | --------- | ------------------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-08.1-01 | Tampering | Attribute values out of valid 1-6 range introduced by future edits | mitigate    | teams.test.ts asserts every attribute <= 6 and >= 0, and enforces tier distribution — regressions fail CI (D-01/D-03) |
| T-08.1-SC | Tampering | npm/pnpm installs                                                  | accept      | No new packages introduced this plan (RESEARCH Package Legitimacy Audit: none added); no install task present         |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared test` green (teams ceiling + tier distribution)
- `pnpm --filter @counter-attack/shared build` compiles teams.ts with no type errors
</verification>

<success_criteria>

- Both squads on the 1-6 scale with tier distribution 1/2/3/3/2 each
- teams.test.ts enforces the 6 ceiling and the distribution (would fail on any future violation)
- All original player identities and positions preserved
  </success_criteria>

<output>
Create `.planning/phases/08.1-cleanup-player-stats-ball-control-movement-tackling/08.1-01-SUMMARY.md` when done
</output>
