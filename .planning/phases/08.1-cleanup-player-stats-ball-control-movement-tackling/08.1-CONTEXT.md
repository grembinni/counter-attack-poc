# Phase 8.1: cleanup player stats, ball control, movement, tackling - Context

**Gathered:** 2026-06-05
**Status:** Ready for planning

<domain>
## Phase Boundary

Fix four foundational gameplay issues that make the game unplayable in its current state:

1. **Player stats** — All 9 attributes must use a 1–6 scale (not 1–10). Current values in teams.ts are wrong. Both squads must be rebalanced using the Counter Attack tier system.
2. **Ball control (steal mechanic)** — Steal attempt is currently a solo defender roll. The correct mechanic is a duel: defender rolls tackling + die, ball carrier rolls dribbling + die; higher wins.
3. **Movement UX** — After a step move, the piece must stay selected so players can chain moves without re-clicking. ZoI hexes (adjacent to opponents) must be visually distinct to warn of steal risk.

**In scope:**

- Redesign both HOME_SQUAD and AWAY_SQUAD in `packages/shared/src/teams.ts` — 1–6 scale, tier-balanced (11 players per squad)
- Fix `applyMove` in `packages/server/src/gameEngine.ts` — replace solo steal roll with dribbling-vs-tackling duel (2 dice)
- Fix `setGameState` in `packages/client/src/store/useGameStore.ts` — keep selected piece across state broadcasts during the same movement slot
- Add ZoI visual indicator in `packages/client/src/components/HexGrid.tsx` — different highlight color for ZoI-risk hexes

**Out of scope (deferred to later phases):**

- Passing cleanup (target selection, accuracy per pass type, ball movement on accurate pass, interception) → Phase 8.2
- Shot declaration flow, all remaining broken action flows → Phase 8.3
- Code review debt (CR-01 timer leak, WR-01..WR-04 warnings) → Phase 8.3

</domain>

<decisions>
## Implementation Decisions

### Player Stats — Scale and Validation

- **D-01:** All 9 player attributes use a **1–6 scale**. This is the Counter Attack rulebook constraint. Any value > 6 in the current teams.ts is invalid. Outfield-players retain `handling: 0`, `saving: 0`, and `aerialAbility: 0` (these are GK-only attributes — 0 means "not applicable", not a skill value). GK retains `shooting: 1` (minimum valid value — GKs can technically shoot). The `highPass: 0` on GKs is preserved (D-04 from Phase 7.1: GKs use a separate GK kick accuracy check, not the highPass attribute).

- **D-02:** The 1–6 range applies to all attributes that are relevant to the player's role. Tier totals count all 9 attributes (0s count as 0). For outfield players, their 6 role-relevant attributes (Pace, Shooting, Tackling, Dribbling, Heading, Resilience) drive the tier total; the 3 GK-only attributes (Saving, Handling, Aerial Ability) are 0 and still count.

### Player Stats — Squad Tier Distribution

- **D-03:** Each squad of 11 players must be balanced using the Counter Attack tier system:

  | Tier | Talent Level  | Total Pts | Count per squad |
  | ---- | ------------- | --------- | --------------- |
  | 1    | World Class   | 34+       | 1               |
  | 2    | International | 32–33     | 2               |
  | 3    | Top Flight    | 30–31     | 3               |
  | 4    | Respected Pro | 28–29     | 3               |
  | 5    | Journeyman    | 26–27     | 2               |
  |      | **Total**     |           | **11**          |

  Both home and away squads must each have this distribution. The two squads should be distinct — different names, different attribute profiles — but matched in overall tier balance so neither team has an unfair advantage.

- **D-04:** The GK's tier should be calculated on their GK-relevant attributes: Saving, Handling, Aerial Ability, Pace, Tackling, Resilience (Heading may be included). The GK's tier generally sets the "ceiling" for each team's defence. Claude determines the precise GK tier placement to make each squad feel realistic (e.g., a Tier 2 GK with high Saving and Handling).

### Steal Mechanic — Dribbling vs Tackling Duel

- **D-05:** The steal attempt when a ball carrier steps into a defender's ZoI is now a **two-sided duel**, not a solo defender roll:
  - **Defender** rolls 1 die: combined score = `defender.tackling + die`
  - **Ball carrier** rolls 1 die: combined score = `carrier.dribbling + die`
  - **Steal SUCCESS** if defender's combined > ball carrier's combined
  - **Steal FAIL** (play continues) if tied OR ball carrier wins
  - Both dice values must be pre-generated before `applyMove` is called (Pitfall 4 — no dice inside pure engine functions)

- **D-06:** The `STEAL_ATTEMPT` ActionEvent in `packages/shared/src/types.ts` must include both sides' dice and combined scores for the event log and replay. Extend the discriminant with: `defenderDie`, `carrierDie`, `defenderCombined`, `carrierCombined`.

- **D-07:** The `applyMove` function signature does NOT receive pre-generated dice today — it calls `rollDice()` internally for the steal. This must be refactored: the caller (`gameHandlers.ts` GAME_MOVE handler) pre-generates `defenderDie` and `carrierDie` before calling `applyMove`, and passes them in via an options parameter (mirrors the `addedTimeRoll` pattern in `applyEndTurn`). The pure `applyMove` engine function never calls `rollDice()` directly (Pitfall 1).

### Movement UX — Sticky Selection

- **D-08:** After a valid move is broadcast in MOVEMENT phase, the selected piece must remain selected and `validMoveHexes` must be recomputed from the piece's **new position** with its **remaining pace** (i.e., `piece.pace - paceUsedByPieceId[piece.id]`).

- **D-09:** Selection clears in `setGameState` when:
  - The `movementSlot` changed (slot advance → different team or fresh ATTACKER_2 slot)
  - The `phase` changed (e.g., MOVEMENT → PASS)
  - An existing `selectedPieceId` is no longer in `state.pieces` (piece removed — shouldn't happen in normal play)
  - Otherwise: keep `selectedPieceId`, recompute `validMoveHexes` using the updated `GameState`.

- **D-10:** The `selectPiece` logic in useGameStore already computes `hexesInRange(piece.position, piece.pace)` and filters by `validateMove`. After a state update with sticky selection, the recomputation uses the same logic but with `piece.pace - paceUsedByPieceId[piece.id]` as the effective range so only remaining moves are highlighted.

### Movement UX — ZoI Visual Indicator

- **D-11:** Valid move hexes that are in an opponent's ZoI (adjacent to ≥1 opponent piece) display a distinct visual style compared to safe move hexes:
  - Safe hexes: existing green/standard highlight
  - ZoI-risk hexes: amber or red-tinted outline/fill (specific colour/class: Claude's discretion, consistent with existing `HexGrid` CSS module)
  - Determination: for each `validMoveHex`, call `getZoIDefenders(hex, opponents).length > 0` (already exported from `packages/shared/src/hex.ts`)

- **D-12:** The ZoI visual indicator is **display-only on the client**. The server independently validates steal triggers — no server change needed for the visual.

### Claude's Discretion

- The exact tier placement of each individual player (e.g., which DEF is Tier 3 vs Tier 4) is Claude's call, provided the overall squad distribution follows D-03.
- Specific attribute values per player within the 1–6 range are Claude's call, provided they are role-realistic (GK: high Saving/Handling; DEF: high Tackling; FWD: high Pace/Shooting) and the tier totals satisfy D-03.
- CSS class name and colour for the ZoI-risk hex highlight — consistent with existing `HexGrid.module.css` conventions.
- Whether `applyMove` receives the two dice as separate named params or as a struct — mirrors the cleanest pattern in the existing codebase.

</decisions>

<canonical_refs>

## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase goal and scope

- `.planning/ROADMAP.md` §Phase 08.1 — phase goal and plan list

### Prior phase decisions relevant to this phase

- `.planning/phases/07.1-ui-cleanup/07.1-CONTEXT.md` — D-01 (4-5-2 formation positions in teams.ts; home half q≤18, away half q≥18; r-symmetry about r=12.5)
- `.planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md` — D-04 (GK highPass: 0 rule), D-06 (outfield handling:0), D-05 (outfield aerialAbility:0), D-07 (DEF highPass 3–5), D-07 (MID highPass 5–7), D-07 (FWD highPass 4–6). These must survive the rewrite.

### Files being changed

- `packages/shared/src/teams.ts` — full redesign: 1–6 scale, tier-balanced squads (D-01..D-04)
- `packages/shared/src/types.ts` — extend `STEAL_ATTEMPT` ActionEvent with `defenderDie`, `carrierDie`, `defenderCombined`, `carrierCombined` fields (D-06)
- `packages/server/src/gameEngine.ts` → `applyMove` — refactor steal to accept pre-generated dice and run dribbling-vs-tackling duel (D-05, D-07)
- `packages/server/src/gameHandlers.ts` → GAME_MOVE handler — pre-generate two steal dice before calling `applyMove` (D-07, Pitfall 1)
- `packages/client/src/store/useGameStore.ts` → `setGameState` — sticky selection logic (D-08, D-09, D-10)
- `packages/client/src/components/HexGrid.tsx` — ZoI-risk hex coloring (D-11, D-12)

### Shared utilities available (no changes needed)

- `packages/shared/src/hex.ts` → `getZoIDefenders(hex, opponents)` — already exported; use for ZoI coloring (D-11)
- `packages/shared/src/hex.ts` → `hexesInRange(position, range)` — already used by selectPiece; used again for sticky recomputation (D-10)
- `packages/shared/src/moveValidator.ts` → `validateMove` — unchanged; still used for filtering valid hexes (D-10)

### Test files (must stay green)

- `packages/shared/src/moveValidator.test.ts` — 14 tests for validateMove; steal duel changes must not break these (validateMove itself doesn't roll dice)
- `packages/server/src/__tests__/gameEngine.test.ts` — existing applyMove tests; steal-dice refactor may require test updates
- `packages/shared/src/teams.test.ts` — verify squad validity; may need tier assertion tests added

</canonical_refs>

<code_context>

## Existing Code Insights

### Reusable Assets

- `getZoIDefenders(hex, opponentPieces)` (`packages/shared/src/hex.ts:132`) — returns defenders adjacent to a hex; use directly in HexGrid to classify valid move hexes as ZoI-risk (D-11)
- `hexesInRange(position, pace)` (`packages/shared/src/hex.ts:72`) — returns all hexes within range; reuse in sticky selection recomputation with `pace - paceUsed` (D-10)
- `validateMove(state, piece, to)` (`packages/shared/src/moveValidator.ts:57`) — pure function; unchanged; filters candidates for valid move highlighting
- `rollDice()` in gameHandlers.ts — caller pre-generates dice before passing to engine; same pattern as `addedTimeRoll` in GAME_END_TURN handler

### Established Patterns

- **Pre-generated dice injected by caller (Pitfall 1)** — `applyEndTurn` receives `addedTimeRoll` via options; `applyMove` steal refactor follows the same pattern
- **Discriminated union results** — all engine functions return `{ ok: false, reason }` or `{ ok: true, state }`; STEAL_ATTEMPT event extension follows existing ActionEvent union pattern
- **Zustand per-slice selectors** — `setGameState` reads from `get()` to compare previous slot/phase; uses `set()` to write new state
- **Single SVG root, DOM-order z-ordering** — HexGrid ZoI highlight follows existing `isValidMove` / `isSelected` pattern with a new CSS class

### Integration Points

- `GAME_MOVE handler` (`packages/server/src/gameHandlers.ts`) → calls `applyMove`; this is where the two steal dice are pre-generated (D-07)
- `setGameState` (`packages/client/src/store/useGameStore.ts:161`) → currently clears selection on every call; D-08/D-09 refactor reads previous state before deciding
- `HexGrid.tsx` hex click handler → already uses `validMoveHexSet`; add `zoiRiskSet` computed the same way for coloring (D-11)

</code_context>

<specifics>
## Specific Ideas

- **Tier validation helper**: A `validateSquad(squad)` helper in `teams.test.ts` (or a new `teams.validate.ts`) that asserts: every attribute ∈ [0,6], every player's tier total falls within their declared tier range, and the squad distribution is exactly 1/2/3/3/2. Run as unit tests so future stat edits catch regressions automatically.
- **Sticky selection on state broadcast**: In `setGameState`, compare `newState.movementSlot !== prevState.movementSlot || newState.phase !== prevState.phase` to decide whether to clear. When keeping selection, compute `remainingPace = piece.pace - (newState.paceUsedByPieceId[selectedPieceId] ?? 0)` and call `hexesInRange(newPosition, remainingPace)` + `validateMove` filter.
- **ZoI hex class**: Name it `hexZoIRisk` in `HexGrid.module.css`; apply amber/orange tint (e.g., `rgba(255, 165, 0, 0.4)`) on top of the existing valid-move highlight, so it remains visually distinct from both selected and unselected hexes.

</specifics>

<deferred>
## Deferred Ideas

- **Passing cleanup** → **Phase 8.2**: target hex selection before rolling, correct accuracy attribute per pass type (dribbling for Standard/First-time/Long, highPass for High), ball movement on accurate pass, interception by adjacent defenders. The current `applyRoll` PASS branch always uses `validatePassAccuracy(carrier, 'HIGH', ...)` — this is incorrect but deferred to 8.2.
- **Shot declaration flow and all remaining broken actions** → **Phase 8.3**: adding a Shoot button that transitions from MOVEMENT/PASS to SHOT phase (current `game:shot` handler requires phase already === 'SHOT'); discrete action buttons in post-movement PASS phase; SNAP-02 (opponent deflection move before snapshot shot). Phase 8.3 also covers the code review debt below.
- **Code review debt** → **Phase 8.3**: Fix CR-01 (BLOCKER: `startReplayStream` room-reference leak during 3s setTimeout hold), WR-01 (dead `HIGH_PASS` in `passTypes` set), WR-02 (intermediate slot transitions don't reset lastActionType), WR-03 (passTrigger uses wrong condition), WR-04 (duplicate HEADER resolution buttons), IN-01 (passTypes Set allocated per-call).
- **Human UAT items** (from Phase 8): Snapshot full-path and Header button path still pending live two-player verification — carry into Phase 8.3.

</deferred>

---

_Phase: 08.1-cleanup-player-stats-ball-control-movement-tackling_
_Context gathered: 2026-06-05_
