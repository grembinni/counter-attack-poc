---
phase: 08.1-cleanup-player-stats-ball-control-movement-tackling
plan: 03
subsystem: ui
tags: [react, zustand, svg, hex-grid, zoi, ball-possession, sticky-selection]

# Dependency graph
requires:
  - phase: 08.1-02
    provides: extended shared types (TACKLE_ATTEMPT, paceUsedByPieceId) that compile cleanly when client imports from shared

provides:
  - Sticky piece selection in setGameState: retains selectedPieceId and recomputes validMoveHexes with remaining pace across step-moves within the same slot/phase
  - ZoI-risk amber hex tint overlay on valid move hexes adjacent to an opponent (getZoIDefenders)
  - Directional white ball possession dot on carrier piece (home: bottom-right, away: bottom-left)
  - .hexZoIRisk CSS class for amber overlay

affects:
  - 08.1-04 (if any further client UX)
  - any phase that modifies HexGrid.tsx or PieceOverlay.tsx

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sticky Zustand setGameState: read prev state via get(), compare slot/phase, recompute validMoveHexes with remainingPace = pace - paceUsedByPieceId[id]"
    - "ZoI risk classification: filter validMoveHexes by getZoIDefenders(hex, opponents).length > 0 → amber polygon overlay in PITCH_HEXES.map"
    - "SVG directional possession dot: cx offset keyed off piece.teamId (+6 home, -6 away), cy +6, r=2.5"

key-files:
  created: []
  modified:
    - packages/client/src/store/useGameStore.ts
    - packages/client/src/components/HexGrid.tsx
    - packages/client/src/components/PieceOverlay.tsx
    - packages/client/src/components/HexGrid.module.css

key-decisions:
  - "Sticky selection is restored by setGameState on broadcast, not prevented at emitMove — emitMove optimistic clear kept as Pitfall 2 avoidance"
  - "ZoI dot direction uses piece.teamId (home always attacks right), not attackingTeam — consistent regardless of which team currently has the ball (Open Question 3)"
  - "attackingTeam passed as prop to PieceOverlay for future directional overrides; unused in current implementation"

patterns-established:
  - "setGameState sticky: branches on slotChanged || phaseChanged || !pieceStillExists — clear path and sticky path with remaining-pace recomputation"
  - "ZoI overlay: secondary polygon element in PITCH_HEXES.map, gated on zoiRiskSet.has(hexId) && isValidMove, className from CSS module"

requirements-completed: [MOVE-05]

# Metrics
duration: 15min
completed: 2026-06-05
---

# Phase 08.1 Plan 03: Client UX — sticky selection, ZoI tint, ball possession dot Summary

**Sticky Zustand setGameState with remaining-pace recomputation, amber ZoI-risk hex tint via getZoIDefenders, and directional white ball carrier dot replacing the placeholder inner ring**

## Performance

- **Duration:** 15 min
- **Started:** 2026-06-05T00:00:00Z
- **Completed:** 2026-06-05T00:15:00Z
- **Tasks:** 2 (Task 3 is human-verify checkpoint, not yet approved)
- **Files modified:** 4

## Accomplishments

- setGameState retains selection and recomputes valid hexes with remaining pace when slot/phase unchanged (D-17, D-18, D-19)
- Valid move hexes adjacent to opponents show amber/orange tint via getZoIDefenders classification (D-20, D-21)
- Ball carrier shows small white directional dot (home: bottom-right, away: bottom-left); old inner ring removed (D-15)
- Client builds with zero type errors; all pre-commit hooks pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Sticky selection in setGameState** - `bd7886b` (feat)
2. **Task 2: ZoI-risk hex tint and directional ball possession dot** - `2c4225b` (feat)

_Task 3 is a human-verify checkpoint — no code commit._

## Files Created/Modified

- `packages/client/src/store/useGameStore.ts` - setGameState refactored: reads prev state via get(), branches on slot/phase/piece-exists, sticky path recomputes with remainingPace
- `packages/client/src/components/HexGrid.tsx` - imports getZoIDefenders; builds zoiRiskSet; renders amber polygon overlay for ZoI-risk hexes; passes attackingTeam to PieceOverlay
- `packages/client/src/components/PieceOverlay.tsx` - adds attackingTeam prop; replaces inner ring (r=5, f5f0dc) with directional white dot (cx±6, cy+6, r=2.5) keyed off piece.teamId
- `packages/client/src/components/HexGrid.module.css` - adds .hexZoIRisk { fill: rgba(255,165,0,0.4) }

## Decisions Made

- Dot direction keyed off `piece.teamId` (home always attacks right) rather than `attackingTeam` — avoids direction flip when possession changes (Open Question 3 resolution)
- `attackingTeam` received as prop and voided for now; kept in Props type for future directional overrides without breaking API
- emitMove optimistic clear kept as-is (Pitfall 2) — sticky selection is restored when server broadcast arrives via setGameState

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The file had been updated by a prior agent (Phase 8 additions) vs the version documented in PLAN.md read_first references, but the actual implementation pattern was identical and the edits applied cleanly.

## Known Stubs

None - all logic wired to live GameState from server broadcast.

## Threat Flags

No new threat surface introduced. All changes are client-side rendering/state with no network endpoints, auth paths, or schema changes. T-08.1-07 (sticky selection tampering) mitigated — server independently validates each GAME_MOVE regardless of client selection state.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Task 3 human-verify checkpoint pending: operator must confirm sticky selection, ZoI tint, and ball dot in a live 2-browser session
- Upon approval, MOVE-05 client-side playability surface is complete
- Phase 08.1 plan 03 code changes are complete and committed

---

_Phase: 08.1-cleanup-player-stats-ball-control-movement-tackling_
_Completed: 2026-06-05_
