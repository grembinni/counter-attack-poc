---
phase: 27-game-creation-settings
plan: 03
type: execute
wave: 2
depends_on: ['27-01']
files_modified:
  - packages/client/src/constants/speedOptions.ts
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/components/GameSettingsScreen.tsx
  - packages/client/src/components/GameSettingsScreen.module.css
  - packages/client/src/components/GameSettingsScreen.test.tsx
  - packages/client/src/App.tsx
autonomous: true
requirements: [DRAFT-01]
must_haves:
  truths:
    - 'After creating a room, the host (slot 1) lands on a GAME_SETTINGS screen, not directly on WAITING'
    - 'The settings screen shows a Match Speed picker, a Standard/Draft team-type toggle, and — only in Draft mode — five pool checkboxes with Legends/Icons disabled and marked coming soon'
    - 'In Draft mode, Original is pre-checked and the Confirm button is disabled whenever zero of the three enabled pools are checked'
    - 'Clicking Confirm Settings emits ROOM_SETTINGS_CONFIRM with { speed, teamType, draftPools }'
    - 'On ROOM_SETTINGS_CONFIRMED the host stores the settings locally and transitions from GAME_SETTINGS to WAITING'
    - 'The joining player never renders the GAME_SETTINGS screen'
  artifacts:
    - path: 'packages/client/src/components/GameSettingsScreen.tsx'
      provides: 'Host settings pre-step form (speed + team type + draft pools) with single bundled onConfirm'
      min_lines: 40
    - path: 'packages/client/src/constants/speedOptions.ts'
      provides: 'Shared SPEED_OPTIONS const (extracted, single source)'
      contains: 'SPEED_OPTIONS'
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: "Screen union member 'GAME_SETTINGS'"
      contains: 'GAME_SETTINGS'
    - path: 'packages/client/src/App.tsx'
      provides: 'GAME_SETTINGS routing branch, onRoomJoined host redirect, ROOM_SETTINGS_CONFIRMED handler, confirm emit, teamType/draftPools local state'
      contains: 'GAME_SETTINGS'
  key_links:
    - from: 'packages/client/src/App.tsx (onRoomJoined slot 1)'
      to: "setScreen('GAME_SETTINGS')"
      via: 'host redirect after ROOM_JOINED'
      pattern: 'GAME_SETTINGS'
    - from: 'packages/client/src/components/GameSettingsScreen.tsx'
      to: 'ClientEvents.ROOM_SETTINGS_CONFIRM (via App handleSettingsConfirm)'
      via: 'onConfirm callback → socket.emit'
      pattern: 'ROOM_SETTINGS_CONFIRM'
---

<objective>
Build the host-only Game Settings pre-step screen and wire it into the client screen state machine. A new `GameSettingsScreen` component (speed picker + Standard/Draft toggle + conditional draft-pool checkboxes) hands a single bundled settings object to `App.tsx`, which emits `ROOM_SETTINGS_CONFIRM` and routes the host onward. Extracts the duplicated `SPEED_OPTIONS` const into a shared client module (rule-of-three) so the screen's interactive picker and the two later read-only subheaders (27-04) share one source.

Purpose: DRAFT-01 — game creation gets a pre-step settings screen (D-01 host-only, immediately after Create Room). Implements D-04 (five checkboxes, Legends/Icons disabled + coming soon), D-05 (Original pre-checked), D-06 (Confirm disabled at zero pools), and the client half of D-02 (joiner never sees this screen; host transitions to WAITING on confirm).

Output: new `speedOptions.ts`, new `GameSettingsScreen.tsx`/`.module.css`/`.test.tsx`, a new `Screen` union member, and App.tsx routing/handler/state wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-game-creation-settings/27-RESEARCH.md
@.planning/phases/27-game-creation-settings/27-PATTERNS.md
@.planning/phases/27-game-creation-settings/27-UI-SPEC.md
@packages/shared/src/events.ts
@packages/client/src/components/UniformSelectionScreen.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract shared SPEED_OPTIONS + add GAME_SETTINGS Screen member</name>
  <files>packages/client/src/constants/speedOptions.ts, packages/client/src/store/useGameStore.ts</files>
  <read_first>
    - packages/client/src/components/TeamSelectionScreen.tsx (lines 54-59: the exact `SPEED_OPTIONS` array to copy verbatim — value/label/icon/colorClass fields)
    - packages/client/src/components/UniformSelectionScreen.tsx (lines 97-102: byte-identical `SPEED_OPTIONS` copy — confirm shape matches)
    - packages/client/src/store/useGameStore.ts (lines 22-32: the `Screen` union and its comment convention)
  </read_first>
  <action>
    Create `packages/client/src/constants/speedOptions.ts` exporting `SPEED_OPTIONS` with the exact shape currently declared in TeamSelectionScreen.tsx lines 55-59: an array of `{ value: GameSpeed; label: string; icon: string; colorClass: string }` with the three entries slow (🐢, 'speedColorSlow'), standard (⚽, 'speedColorStandard'), fast (⚡, 'speedColorFast'). Import `GameSpeed` type from `@counter-attack/shared`. In `useGameStore.ts`, add `'GAME_SETTINGS'` to the `Screen` union immediately after `'WAITING'` and before `'TEAM_SELECTION'`, tagged with a comment `// Phase 27: host-only pre-team-selection settings screen (DRAFT-01, D-01)`. Do NOT modify TeamSelectionScreen.tsx or UniformSelectionScreen.tsx in this plan — they keep their local `SPEED_OPTIONS` copies until 27-04 converts them (temporary rule-of-three duplication, resolved next wave). `setScreen` needs no change — it already accepts any union member.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `packages/client/src/constants/speedOptions.ts` exports `SPEED_OPTIONS` with three entries whose `value`s are `'slow'`, `'standard'`, `'fast'`.
    - Source assertion: `Screen` union in `useGameStore.ts` contains `'GAME_SETTINGS'`.
    - `pnpm --filter @counter-attack/client typecheck` exits 0.
  </acceptance_criteria>
  <done>Shared SPEED_OPTIONS module exists and the GAME_SETTINGS screen member is added; client typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build GameSettingsScreen component + tests</name>
  <files>packages/client/src/components/GameSettingsScreen.tsx, packages/client/src/components/GameSettingsScreen.module.css, packages/client/src/components/GameSettingsScreen.test.tsx</files>
  <read_first>
    - packages/client/src/components/UniformSelectionScreen.tsx (local-state + single bundled onConfirm pattern: `selectedTeam`/`selectedStyle`/`selectedFormation` useState + one confirm callback — mirror this structure)
    - packages/client/src/components/TeamSelectionScreen.tsx (lines 117-146: the interactive `.speedOptions` button block to MOVE here as the settings screen's speed picker, driven by the shared SPEED_OPTIONS)
    - packages/client/src/components/LobbyScreen.module.css (`.page`/`.card` centered-column shell + `.ctaButton`/`.ctaButton:disabled` treatment to reuse)
    - packages/client/src/components/TeamSelectionScreen.module.css (`.tab`/`.tabActive` pattern reused for the Standard/Draft toggle and pool checkboxes, and `.speedColor*` classes)
    - .planning/phases/27-game-creation-settings/27-UI-SPEC.md (Copywriting Contract + Color + Interaction Notes: heading "Game Settings", section labels, CTA "Confirm Settings", `.card:disabled` opacity 0.4 for Legends/Icons, "(coming soon)")
  </read_first>
  <behavior>
    - Renders heading "Game Settings", a "Match Speed" picker (3 options from shared SPEED_OPTIONS), a "Team Type" toggle with options "Standard" and "Draft", and a primary CTA labelled "Confirm Settings".
    - Team Type defaults to 'standard'; draft-pool section is hidden while Standard is selected.
    - Selecting Draft reveals five pool checkboxes labelled "Original", "MLS", "International", "Legends", "Icons"; Legends and Icons are disabled and show "(coming soon)"; Original is checked by default (D-05).
    - In Draft mode with all three enabled pools unchecked, the "Confirm Settings" button is disabled (D-06); checking at least one enables it.
    - Clicking Confirm calls `onConfirm` once with `{ speed, teamType, draftPools }` — draftPools is `[]` when teamType is 'standard', and the checked selectable pools when 'draft'.
    - A Draft-mode confirm can never include 'legends' or 'icons' (their checkboxes are non-interactive).
  </behavior>
  <action>
    Create `GameSettingsScreen.tsx` as a top-level sibling component (NOT nested in LobbyScreen). Props: `onConfirm: (settings: { speed: GameSpeed; teamType: TeamType; draftPools: DraftPoolId[] }) => void`. Hold all field state as local `useState`: `speed` (default 'standard'), `teamType` (default 'standard'), `draftPools: DraftPoolId[]` (default `['original']` per D-05). Import `SPEED_OPTIONS` from `../constants/speedOptions.js`; import `TeamType`, `DraftPoolId`, `SELECTABLE_DRAFT_POOLS`, `GameSpeed` from `@counter-attack/shared`. Render the pool checkbox list from a static labels map for all five `DraftPoolId`s, but derive `disabled` for any id NOT in `SELECTABLE_DRAFT_POOLS` (i.e. Legends/Icons) and append "(coming soon)" for those. Compute `confirmDisabled = teamType === 'draft' && draftPools.length === 0` (D-06) and set the CTA `disabled` attribute from it. `handleConfirm` calls `onConfirm({ speed, teamType, draftPools: teamType === 'draft' ? draftPools : [] })`. Use `.page`/`.card` shell classes and the `.ctaButton` treatment; reuse `.tab`/`.tabActive` for the team-type toggle. Create `GameSettingsScreen.module.css` duplicating the needed tokens from LobbyScreen/TeamSelection CSS (project convention — do not introduce a shared CSS partial); new spacing must use only 4/8/16/24/32 per UI-SPEC. Do NOT use gold `#f5c518` for interactive elements. Write `GameSettingsScreen.test.tsx` covering all behaviors above (render controls; Draft reveals checkboxes with Legends/Icons disabled; Original pre-checked; Confirm disabled when all enabled pools unchecked; onConfirm payload shape for both standard and draft). Follow the existing client component test conventions (Vitest + Testing Library, see UniformSelectionScreen.test.tsx / TeamSelectionScreen.test.tsx for render/query style).
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- GameSettingsScreen</automated>
  </verify>
  <acceptance_criteria>
    - Test-command assertion: `pnpm --filter @counter-attack/client test -- GameSettingsScreen` passes.
    - Behavior assertion: a test proves the Confirm button is `disabled` in Draft mode with zero enabled pools checked and enabled after checking Original.
    - Behavior assertion: a test proves Legends and Icons checkboxes are `disabled` and labelled with "(coming soon)".
    - Behavior assertion: a test proves `onConfirm` receives `draftPools: []` in Standard mode and the checked selectable pools in Draft mode.
    - Source assertion: pool disabled-state is derived from `SELECTABLE_DRAFT_POOLS`, not a hardcoded id check.
  </acceptance_criteria>
  <done>GameSettingsScreen renders and behaves per D-04/D-05/D-06 and its tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire GAME_SETTINGS into App routing, host redirect, confirm emit, and settings receipt</name>
  <files>packages/client/src/App.tsx</files>
  <read_first>
    - packages/client/src/App.tsx (lines 1-27 imports; line 34 `selectedSpeed` local state; lines 60-70 `onRoomJoined`; lines 90-102 `onTeamSelectionStart`/`onTeamSpeedChanged`; lines 139-170 socket on/off registration + cleanup; lines 173-204 emit helpers; lines 206-240 routing ternary)
    - packages/client/src/components/GameSettingsScreen.tsx (its `onConfirm` prop signature)
    - packages/shared/src/events.ts (ROOM_SETTINGS_CONFIRM / ROOM_SETTINGS_CONFIRMED signatures)
    - .planning/phases/27-game-creation-settings/27-RESEARCH.md (Recommended Screen Insertion Flow diagram — the exact host/joiner routing)
  </read_first>
  <action>
    Import `GameSettingsScreen` and the `TeamType`/`DraftPoolId` types. Add local state alongside `selectedSpeed` (line 34): `const [teamType, setTeamType] = useState<TeamType>('standard');` and `const [draftPools, setDraftPools] = useState<DraftPoolId[]>([]);`. In `onRoomJoined` (line 69), change the slot-1 branch from `setScreen('WAITING')` to `setScreen('GAME_SETTINGS')` — keep the same `(s === 'LANDING' || s === 'CREATE_ROOM')` guard so only the host's own join confirmation triggers it. Add a `handleSettingsConfirm(settings: { speed; teamType; draftPools })` function (near the other emit helpers ~line 179) that calls `setSelectedSpeed(settings.speed); setTeamType(settings.teamType); setDraftPools(settings.draftPools); socket.emit(ClientEvents.ROOM_SETTINGS_CONFIRM, settings);`. Add an `onRoomSettingsConfirmed(speed, teamType, draftPools)` handler in the effect that runs `setSelectedSpeed(speed); setTeamType(teamType); setDraftPools(draftPools); if (useGameStore.getState().screen === 'GAME_SETTINGS') setScreen('WAITING');` — this moves the host from the settings screen to WAITING (the joiner is never on GAME_SETTINGS so it is a no-op for them). Register it with `socket.on(ServerEvents.ROOM_SETTINGS_CONFIRMED, onRoomSettingsConfirmed)` and add the matching `socket.off(...)` in the cleanup return (Pitfall 9 — every on has an off). Add a routing branch `screen === 'GAME_SETTINGS' ? (<GameSettingsScreen onConfirm={handleSettingsConfirm} />) :` inserted before the `TEAM_SELECTION` branch (line 229), matching the structural style of the existing `LINEUP_ASSIGNMENT`/`UNIFORM_SELECTION` branches. Leave `handleSpeedChange`/`emitTeamSpeed` in place for now (they become unused once 27-04 drops the speed props from the two screens; do not delete here to avoid churn). The `onTeamSelectionStart` handler is unchanged — it still routes to UNIFORM_SELECTION.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- App</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `onRoomJoined` slot-1 branch calls `setScreen('GAME_SETTINGS')` (grep confirms no `setScreen('WAITING')` remains in that branch).
    - Source assertion: App registers `ServerEvents.ROOM_SETTINGS_CONFIRMED` with a matching `socket.off` in cleanup.
    - Source assertion: routing ternary has a `screen === 'GAME_SETTINGS'` branch rendering `<GameSettingsScreen onConfirm={handleSettingsConfirm} />` before the TEAM_SELECTION branch.
    - Source assertion: `handleSettingsConfirm` emits `ClientEvents.ROOM_SETTINGS_CONFIRM` with the bundled settings object.
    - `pnpm --filter @counter-attack/client typecheck` exits 0 and the client suite (`pnpm --filter @counter-attack/client test`) is green.
  </acceptance_criteria>
  <done>Host reaches GAME_SETTINGS after create, Confirm emits ROOM_SETTINGS_CONFIRM, and ROOM_SETTINGS_CONFIRMED moves the host to WAITING; App tests/typecheck pass.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (owned by this plan)

- Component `GameSettingsScreen` (+ `.module.css`, `.test.tsx`) — `packages/client/src/components/`
- Shared const `SPEED_OPTIONS` — `packages/client/src/constants/speedOptions.ts`
- `Screen` union member `'GAME_SETTINGS'` — `packages/client/src/store/useGameStore.ts`
- App local state `teamType`, `draftPools`; `handleSettingsConfirm` (emits `ROOM_SETTINGS_CONFIRM`); `onRoomSettingsConfirmed` handler (host GAME_SETTINGS→WAITING); `onRoomJoined` slot-1 redirect to GAME_SETTINGS; GAME_SETTINGS routing branch — `packages/client/src/App.tsx`

Consumes: 27-01 (TeamType, DraftPoolId, SELECTABLE_DRAFT_POOLS, ROOM_SETTINGS_CONFIRM/ROOM_SETTINGS_CONFIRMED).
Provides downstream: 27-04 imports the shared `SPEED_OPTIONS` and reads App's `teamType`/`draftPools`/`selectedSpeed` local state for the subheaders.
</artifacts_produced>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                                          |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| client UI → client emit                 | The settings form collects untrusted input; disabled Legends/Icons checkboxes (D-04) and the disabled Confirm button (D-06) are UX affordances only. |
| client → server (ROOM_SETTINGS_CONFIRM) | The emitted payload is fully re-validated server-side in 27-02 (host-only, allow-lists, lock).                                                       |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition              | Mitigation Plan                                                                                                                                                               |
| --------- | ---------------------- | ---------------------------------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-27-02   | Tampering              | Disabled Legends/Icons checkboxes  | mitigate (server, 27-02) | Client disables Legends/Icons for UX; the authoritative control is the server allow-list `SELECTABLE_DRAFT_POOLS` in 27-02. Client-side disabling is NOT a security boundary. |
| T-27-04   | Input Validation       | Disabled Confirm at zero pools     | mitigate (server, 27-02) | Client Confirm-disabled (D-06) is UX only; server rejects empty draft pools with `'DRAFT_POOL_REQUIRED'`.                                                                     |
| T-27-06   | Information Disclosure | Joiner never renders GAME_SETTINGS | mitigate                 | `onRoomJoined` only redirects slot 1 to GAME_SETTINGS; slot 2 never enters that screen (D-02) and receives confirmed settings via ROOM_SETTINGS_CONFIRMED.                    |
| T-27-SC   | Tampering              | npm/pip/cargo installs             | accept                   | No new dependencies introduced this phase (RESEARCH.md Package Legitimacy Audit: N/A). No install task exists.                                                                |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test -- GameSettingsScreen` green.
- `pnpm --filter @counter-attack/client typecheck` and full client suite green.
- Manual (deferred to 27-05 checkpoint): create a room → land on Game Settings → toggle Draft → confirm.
</verification>

<success_criteria>

- Host lands on GAME_SETTINGS after Create Room; joiner never does.
- Settings screen implements D-04/D-05/D-06; Confirm emits the bundled ROOM_SETTINGS_CONFIRM.
- ROOM_SETTINGS_CONFIRMED moves the host to WAITING; SPEED_OPTIONS extracted for reuse.
  </success_criteria>

<output>
Create `.planning/phases/27-game-creation-settings/27-03-SUMMARY.md` when done.
</output>
