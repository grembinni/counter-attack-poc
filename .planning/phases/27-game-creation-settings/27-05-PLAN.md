---
phase: 27-game-creation-settings
plan: 05
type: execute
wave: 4
depends_on: ['27-03', '27-04']
files_modified:
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/GameBoard.test.tsx
autonomous: false
requirements: [DRAFT-01, DRAFT-02, DRAFT-03]
user_setup: []
must_haves:
  truths:
    - 'During gameplay the scoreboard shows a read-only active match-speed segment inline in the phase summary (D-08), unless it cannot fit without layout rework'
    - 'The speed segment is derived from gameState.gameSpeed and the shared SPEED_OPTIONS label — no new plumbing'
    - 'A human confirms the full flow: settings pre-step, standard-mode read-only speed, draft-mode summary, and scoreboard speed reminder'
  artifacts:
    - path: 'packages/client/src/components/GameBoard.tsx'
      provides: 'Read-only active-speed segment in the scoreboard .phaseSummary row (D-08)'
      contains: 'gameSpeed'
  key_links:
    - from: 'packages/client/src/components/GameBoard.tsx'
      to: 'gameState.gameSpeed + shared SPEED_OPTIONS'
      via: 'per-slice Zustand selector + label lookup'
      pattern: 'gameSpeed'
---

<objective>
Add the soft D-08 scoreboard speed reminder and run the phase-level human verification. During a match the scoreboard's existing single-line `.phaseSummary` gains one additional `·`-separated read-only segment showing the active match speed, reusing the shared `SPEED_OPTIONS` label and the existing dim `.phaseLabel` styling — no CSS grid/row-height changes. Then a blocking human-verify checkpoint walks the complete Phase 27 flow end to end.

Purpose: DRAFT-01 D-08 (speed reminder on the in-game scoreboard, "if it fits"). This is the final plan; its checkpoint is the phase's holistic UAT gate covering the settings pre-step (DRAFT-01), the Standard-mode read-only speed (DRAFT-02), and the Draft-mode summary line (DRAFT-03), plus the manual-only "does the speed segment fit the topBand layout" check flagged in 27-VALIDATION.md.

Output: `GameBoard.tsx` scoreboard segment + a targeted test, followed by the human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-game-creation-settings/27-UI-SPEC.md
@.planning/phases/27-game-creation-settings/27-RESEARCH.md
@.planning/phases/27-game-creation-settings/27-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add read-only active-speed segment to the scoreboard (D-08)</name>
  <files>packages/client/src/components/GameBoard.tsx, packages/client/src/components/GameBoard.test.tsx</files>
  <read_first>
    - packages/client/src/components/GameBoard.tsx (lines 149-167 for the per-slice `useGameStore((s) => s.gameState.X)` selector pattern to mirror; lines 286-293 for the `.phaseSummary` block with the existing `&middot;`-separated `.phaseLabel` segment — add the speed segment here)
    - packages/client/src/constants/speedOptions.ts (shared `SPEED_OPTIONS` created in 27-03 — resolve label/icon by matching `value === gameSpeed`)
    - .planning/phases/27-game-creation-settings/27-RESEARCH.md (Code Examples — Scoreboard integration point for D-08: additive to the existing flex row, no new row)
    - packages/client/src/components/GameBoard.test.tsx (existing socket-mock/render harness — extend it with a targeted assertion)
  </read_first>
  <action>
    Add a per-slice selector `const gameSpeed = useGameStore((s) => s.gameState.gameSpeed);` alongside the existing selectors (~line 149-167). In the `.phaseSummary` div (lines 286-293), after the existing `phaseLabel` span, add one more `<span className={styles.phaseLabel}>&nbsp;&middot;&nbsp;{label}</span>` where `label` is the display label resolved from the shared `SPEED_OPTIONS` (find the entry whose `value === gameSpeed`, use its `label`; optionally prefix its `icon`). There is NO `GAME_SPEED_LABEL` export — derive the label from `SPEED_OPTIONS`, not from a nonexistent map. Do NOT add a new row to `.scoreboardCentreCell` and do NOT alter the CSS grid track sizing — this must stay additive to the one existing flex row (research confirms this fits without layout rework). Guard the segment the same way the phase label is guarded if needed (e.g. render only when `phase !== 'REPLAY'` to avoid crowding the replay heading, matching the adjacent `phaseLabel` guard). Extend GameBoard.test.tsx with one assertion that, for a rendered game state with a known `gameSpeed`, the corresponding speed label text appears in the scoreboard.
  </action>
  <verify>
    <automated>pnpm --filter @counter-attack/client test -- GameBoard</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `GameBoard.tsx` reads `gameState.gameSpeed` via a per-slice selector and renders a speed label resolved from `SPEED_OPTIONS` inside `.phaseSummary`.
    - Source assertion: no new CSS grid row/track is added to the scoreboard (change is confined to the existing `.phaseSummary` flex row).
    - Behavior assertion: a GameBoard test proves the active speed label renders in the scoreboard for a known `gameSpeed`.
    - `pnpm --filter @counter-attack/client test -- GameBoard` passes and `pnpm --filter @counter-attack/client typecheck` exits 0.
  </acceptance_criteria>
  <done>The scoreboard shows the active match speed inline; GameBoard test and client typecheck pass. If the segment cannot fit cleanly, omit it and record a follow-up note per CONTEXT.md D-08 discretion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full Phase 27 game-creation-settings flow</name>
  <what-built>
    The complete game-creation settings feature: a host-only Game Settings pre-step screen (speed + Standard/Draft team type + draft-pool checkboxes with Legends/Icons disabled), server-authoritative confirm + settings lock + late-joiner settings delivery, read-only speed subheader in Standard mode and a settings summary line in Draft mode on the team-selection (Uniform) screen, and a read-only active-speed reminder on the in-game scoreboard.
  </what-built>
  <how-to-verify>
    Run the app with two browser tabs (host + joiner). Recommended: `pnpm --filter @counter-attack/server dev` and `pnpm --filter @counter-attack/client dev`, then open the client URL in two tabs.

    1. Host tab: click Create Game. Confirm the host lands on a "Game Settings" screen (NOT directly on a waiting screen).
    2. On Game Settings, confirm: a Match Speed picker, a Standard/Draft team-type toggle, and (only after selecting Draft) five pool checkboxes where Original is pre-checked and Legends/Icons are disabled with a "(coming soon)" note. Uncheck all three enabled pools in Draft mode → the "Confirm Settings" button is disabled; re-check one → it enables.
    3. Standard-mode path: choose Standard + a speed (e.g. Fast), click Confirm Settings. In the joiner tab, join the room. Confirm BOTH tabs advance to the team/uniform selection screen, the speed shows as a read-only subheader (no clickable speed buttons), and it reflects Fast.
    4. Draft-mode path (fresh room): choose Draft + pools (e.g. Original + International), confirm, join from the second tab. Confirm the team-selection screen shows a single read-only summary line like "Speed: ⚽ Standard · Team Type: Draft · Draft Pool: Original, International" in place of the speed picker, with no edit-back link.
    5. Race check: create a room, and in the joiner tab join BEFORE clicking Confirm Settings in the host tab. Confirm the joiner does NOT jump to team selection prematurely; only after the host confirms do both tabs advance, with the joiner showing the correct settings.
    6. Scoreboard (D-08, soft): play into the match and confirm the active match speed appears inline in the scoreboard phase summary without crowding the team name / phase label or wrapping the top band. If it does not fit cleanly, note it as an acceptable omission.

  </how-to-verify>
  <acceptance_criteria>
    - All six steps behave as described; the joiner never sees the settings screen; settings are locked (no re-edit) after confirm.
    - The race check shows no premature team-selection transition for the joiner.
  </acceptance_criteria>
  <resume-signal>Type "approved" to complete Phase 27, or describe any issues (which step, expected vs actual) to spawn gap-closure.</resume-signal>
</task>

</tasks>

<artifacts_produced>

## Artifacts this phase produces (owned by this plan)

- Read-only active-speed segment in the scoreboard `.phaseSummary` (D-08) + per-slice `gameSpeed` selector — `packages/client/src/components/GameBoard.tsx`
- GameBoard test assertion for the speed segment — `packages/client/src/components/GameBoard.test.tsx`

Consumes: 27-03 (shared `SPEED_OPTIONS`), and the full flow delivered by 27-01…27-04. `gameState.gameSpeed` already exists on every GameState broadcast (no new server plumbing).
</artifacts_produced>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                                                       |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| server → client (scoreboard display) | Display-only; `gameState.gameSpeed` is already part of the authoritative GameState snapshot. No new untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category  | Component                | Disposition | Mitigation Plan                                                                                                   |
| --------- | --------- | ------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------- |
| T-27-08   | Tampering | Scoreboard speed segment | accept      | Pure read-only derivation from the server-authoritative `gameState.gameSpeed`; no client input, no mutation path. |
| T-27-SC   | Tampering | npm/pip/cargo installs   | accept      | No new dependencies introduced this phase (RESEARCH.md Package Legitimacy Audit: N/A). No install task exists.    |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client test -- GameBoard` green; `pnpm --filter @counter-attack/client typecheck` green.
- Full workspace suite `pnpm -r test` green before the human checkpoint.
- Human checkpoint approved.
</verification>

<success_criteria>

- Scoreboard shows the active match speed read-only during gameplay (D-08), or it is omitted with a recorded follow-up note.
- Human verification confirms the entire Phase 27 flow (DRAFT-01/02/03) works across two clients, including the race ordering.
  </success_criteria>

<output>
Create `.planning/phases/27-game-creation-settings/27-05-SUMMARY.md` when done.
</output>
