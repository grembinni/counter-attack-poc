---
phase: 08-match-lifecycle-post-game-replay
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/gameHandlers.ts
  - packages/server/src/gameEngine.ts
autonomous: true
requirements: [MATCH-01, REPLAY-01]
must_haves:
  truths:
    - 'Emitting game:snapshot over the wire invokes applySnapshot and broadcasts the resulting SHOT-phase state'
    - 'Emitting game:header over the wire resolves the HEADER duel (same resolution path as game:roll in HEADER phase)'
    - 'A GK save-and-spill transitions to LOOSE_BALL phase and defers landing to a fresh roll — no reuse of the shot-duel dice'
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'GAME_SNAPSHOT and GAME_HEADER socket handlers registered in registerGameHandlers'
      contains: 'ClientEvents.GAME_SNAPSHOT'
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'GK spill branch transitions to LOOSE_BALL instead of reusing duel dice'
      contains: "phase: 'LOOSE_BALL'"
  key_links:
    - from: 'packages/server/src/gameHandlers.ts'
      to: 'applySnapshot'
      via: 'socket.on(ClientEvents.GAME_SNAPSHOT) calling applySnapshot(room.gameState)'
      pattern: 'GAME_SNAPSHOT'
    - from: 'packages/server/src/gameHandlers.ts'
      to: 'applyRoll (HEADER resolution)'
      via: 'socket.on(ClientEvents.GAME_HEADER) delegating to the same resolution as GAME_ROLL'
      pattern: 'GAME_HEADER'
---

<objective>
Close the three server-side Phase 8 verification gaps (CR-01 server end, CR-02, CR-03) found in 08-VERIFICATION.md.

- CR-01 (server end): register `socket.on(ClientEvents.GAME_SNAPSHOT, ...)` in `registerGameHandlers()` so the client's `emitSnapshot` is no longer silently dropped. `applySnapshot` is already implemented and unit-tested; it just needs to be invoked over the wire.
- CR-02: register `socket.on(ClientEvents.GAME_HEADER, ...)` that resolves the HEADER duel via the same path the existing `GAME_ROLL` handler already uses (HEADER is in `DICE_PHASES`). This removes the divergence where the Header button emits `game:header` to a server with no listener.
- CR-03: in the `applyRoll` SHOT branch, the GK save-and-spill path calls `computeLooseBall(gk.position, d1, d2)` reusing the shot-duel dice. Change it to transition to `LOOSE_BALL` phase and defer landing to a fresh roll — exactly as the SHOT tie path (same branch, ~line 661-674) and every other loose-ball path already do.

Purpose: These are the server halves of the broken snapshot/header wiring plus a fairness/correctness bug in the loose-ball landing. They are surgical fixes — no new features.
Output: Two new socket handlers registered; one SHOT-branch spill path rewritten to defer to a fresh LOOSE_BALL roll; server tsc + existing test suite still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register GAME_SNAPSHOT and GAME_HEADER socket handlers (CR-01 server end, CR-02)</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (the GAME_ROLL handler ~line 339-428 for the canonical dice-phase resolution pattern: isProcessing mutex, phase guard, isActivePlayer team guard, sequence guards, pre-generated dice d1/d2/d3, applyRoll call, snap-back broadcastState; the GAME_START_MOVEMENT handler for the simpler no-dice applyX→broadcastState pattern; the existing import of applyRoll/applySnapshot from gameEngine.js and ClientEvents from @counter-attack/shared)
    - packages/server/src/gameEngine.ts (applySnapshot signature ~line 1139: takes only GameState, returns {ok,reason} | {ok,state}; the SHOT-phase resolution it produces has phase 'SHOT')
    - packages/shared/src/events.ts (ClientEvents.GAME_SNAPSHOT = 'game:snapshot', ClientEvents.GAME_HEADER = 'game:header', and their ClientToServerEvents signatures: both () => void)
  </read_first>
  <action>
    Import `applySnapshot` from `./gameEngine.js` alongside the existing `applyRoll` import (verify it is not already imported; add it to the import list if missing).

    Register `socket.on(ClientEvents.GAME_SNAPSHOT, () => { ... })` inside `registerGameHandlers`, following the GAME_START_MOVEMENT handler shape: read `socket.data.roomCode`, `getRoom`, `if (!room || room.isProcessing) return;`, set `room.isProcessing = true;` in a try with `finally { room.isProcessing = false; }`. Inside: gate on `isActivePlayer(socket, room)` returning `GAME_ERROR 'WRONG_TEAM'` + snap-back broadcastState on failure (the active/attacking player is the one who may snapshot). Then call `const result = applySnapshot(room.gameState);`. On `!result.ok` emit `GAME_ERROR` with `result.reason` and `broadcastState(io, room)` (snap-back). On success set `room.gameState = result.state;` and `broadcastState(io, room)`. Do NOT pre-generate dice here — applySnapshot only transitions MOVEMENT/PASS → SHOT; the actual shot is resolved by a subsequent game:roll. applySnapshot performs its own phase/sequence validation internally, so no phase guard beyond the null-state check inside applySnapshot is required, but keep the `room.gameState === null` guard before calling.

    Register `socket.on(ClientEvents.GAME_HEADER, () => { ... })` that resolves the HEADER duel via the SAME logic the existing GAME_ROLL handler uses for the HEADER phase. Rather than duplicate the GAME_ROLL body, the simplest correct fix is to make GAME_HEADER perform the dice-phase resolution restricted to the HEADER phase: phase guard `room.gameState.phase !== 'HEADER'` → `GAME_ERROR 'WRONG_PHASE'` + snap-back; team guard `isActivePlayer` → `GAME_ERROR 'WRONG_TEAM'` + snap-back; the existing HEADER sequence guard (`lastActionType !== null && !ELIGIBLE_NEXT_ACTIONS[lastActionType].has('HEADER')` → `INVALID_SEQUENCE` + snap-back); pre-generate `d1 = rollDice(); d2 = rollDice(); d3 = rollDice();`; `applyRoll(room.gameState, d1, d2, d3)`; on `!result.ok` emit reason + snap-back; on success `room.gameState = result.state; broadcastState(io, room);`. Reasoning to record in a code comment: GAME_ROLL already covers HEADER via DICE_PHASES, so GAME_HEADER is a dedicated alias that delegates to the identical applyRoll resolution; registering it (rather than removing it from ClientEvents) is the lower-risk fix because the client's emitHeader and the events.ts contract already exist and are shipped.

    Keep all existing handlers unchanged (additive registrations only).

  </action>
  <verify>
    <automated>cd packages/server && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `gameHandlers.ts` contains `socket.on(ClientEvents.GAME_SNAPSHOT` inside `registerGameHandlers`
    - `gameHandlers.ts` contains `socket.on(ClientEvents.GAME_HEADER` inside `registerGameHandlers`
    - GAME_SNAPSHOT handler calls `applySnapshot(room.gameState)` and broadcasts via `broadcastState(io, room)` on success
    - GAME_HEADER handler pre-generates three `rollDice()` values and calls `applyRoll`, guarded by `phase === 'HEADER'`
    - Both handlers use the `room.isProcessing` mutex with release in a `finally` block
    - `cd packages/server && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The client's game:snapshot and game:header emissions are received and resolved by the server: snapshot invokes applySnapshot and broadcasts the SHOT-phase state; header resolves the HEADER duel via applyRoll.</done>
</task>

<task type="auto">
  <name>Task 2: GK save-and-spill defers landing to a fresh LOOSE_BALL roll (CR-03)</name>
  <files>packages/server/src/gameEngine.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts SHOT branch (~line 608-733): the SAVE/spill block at ~line 708-728 that currently calls computeLooseBall(gk.position, d1, d2); the SHOT tie path at ~line 661-674 that ALREADY transitions to phase 'LOOSE_BALL' with ball { position: state.ball.position, carrierId: null } and defers landing; the LOOSE_BALL case at ~line 912-929 that consumes a fresh d1/d2 to compute the landing
    - packages/shared/src/scoreUtils.ts (computeLooseBall expects independent direction die d1 and distance die d2)
    - packages/server/src/__tests__/gameEngine.phase8.test.ts (~line 660-690: existing snapshot/shot tie tests asserting phase 'LOOSE_BALL' — the expected post-spill phase)
  </read_first>
  <action>
    In the `applyRoll` SHOT branch, locate the `SAVE` block where `handling.caught` is false (the spill `else` branch, ~line 708-728). It currently computes `const landing = computeLooseBall(gk.position, d1 as ..., d2 as ...)` and returns `phase: 'MOVEMENT'` with `ball: { position: landing, carrierId: null }`, reusing `d1`/`d2` which were already consumed as the shooter/GK duel dice.

    Replace this branch so it transitions to `LOOSE_BALL` phase WITHOUT computing the landing — mirroring the SHOT tie path at ~line 661-674. Return `ok: true` with state: `phase: 'LOOSE_BALL'`, `ball: { position: gk.position, carrierId: null }` (ball drops at the GK's position, the spill origin), `lastDiceRoll: { rolls: [shooterDice, gkDice, handlingDice], context: 'SHOT_DUEL' }`, and `snapshotPenalty: false`. The next `game:roll` will hit the existing LOOSE_BALL case (~line 912), which pre-generates a fresh d1/d2 and calls computeLooseBall on independent dice and sets `lastActionType: 'DEFLECTION'`. Remove the now-unused `computeLooseBall` call from this spill branch. Do not change the GK-caught branch (~line 696-707) or the MISS/GOAL/tie branches. Add a one-line comment noting the spill now defers landing to a fresh roll (consistent with D-13/D-19 and the tie path).

    Confirm `computeLooseBall` is still imported and used elsewhere (LOOSE_BALL case, line 913) — do NOT remove the import.

  </action>
  <verify>
    <automated>cd packages/server && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The GK spill branch in the SHOT case no longer calls `computeLooseBall` with `d1`/`d2`
    - The spill branch returns `phase: 'LOOSE_BALL'` with `ball: { position: gk.position, carrierId: null }`
    - `computeLooseBall` is still imported (used by the LOOSE_BALL case at ~line 912)
    - `cd packages/server && pnpm exec tsc --noEmit` exits 0
    - `cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts` exits 0 (53 tests pass)
  </acceptance_criteria>
  <done>A GK save-and-spill produces a LOOSE_BALL phase whose landing is resolved by a subsequent fresh roll, eliminating the biased reuse of shot-duel dice.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                                                                 |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| client UI → server | game:snapshot and game:header emits are now received; both are re-validated server-side (phase/team/sequence guards) before mutating state. |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                      | Disposition | Mitigation Plan                                                                                                                                       |
| --------- | --------- | -------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-21   | Tampering | client emits game:snapshot out of turn / wrong phase           | mitigate    | GAME_SNAPSHOT handler gates on isActivePlayer and applySnapshot performs phase/sequence/position validation (NOT_IN_PENALTY_AREA / INVALID_SEQUENCE). |
| T-08-22   | Tampering | client emits game:header out of phase or out of turn           | mitigate    | GAME_HEADER handler gates on phase === 'HEADER', isActivePlayer, and the HEADER sequence guard before resolving via applyRoll.                        |
| T-08-23   | Spoofing  | client tries to influence loose-ball landing by replaying dice | mitigate    | Spill now defers landing to a fresh server-generated LOOSE_BALL roll (crypto-backed rollDice); client supplies no dice.                               |
| T-08-SC   | Tampering | npm/pip/cargo installs                                         | accept      | No package installs in this plan or phase (RESEARCH.md §Package Legitimacy Audit: not applicable).                                                    |

</threat_model>

<verification>
- `cd packages/server && pnpm exec tsc --noEmit` exits 0.
- `cd packages/server && pnpm exec vitest run` — no NEW failures beyond the two pre-existing game.integration.test.ts failures documented in 08-02/08-03 SUMMARYs.
- `registerGameHandlers` contains GAME_SNAPSHOT and GAME_HEADER `socket.on` registrations.
- The SHOT spill branch transitions to LOOSE_BALL phase and no longer reuses d1/d2.
</verification>

<success_criteria>

- game:snapshot and game:header are no longer silently dropped — both have registered handlers.
- GK spill produces an independent loose-ball landing via a fresh roll.
- Server type-check and Phase 8 engine test suite pass.
  </success_criteria>

<output>
Create `.planning/phases/08-match-lifecycle-post-game-replay/08-07-SUMMARY.md` when done.
</output>
