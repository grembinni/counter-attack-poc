---
phase: 08-match-lifecycle-post-game-replay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/shared/src/actionSequence.ts
  - packages/shared/src/actionSequence.test.ts
  - packages/shared/src/index.ts
autonomous: true
requirements: [MATCH-01, MATCH-03, MATCH-04]
must_haves:
  truths:
    - 'The shared package exports a LastActionType type and an ELIGIBLE_NEXT_ACTIONS lookup constant'
    - 'ELIGIBLE_NEXT_ACTIONS encodes the D-08 eligibility table exactly (High Pass allows only Header; Shot only follows Movement)'
    - 'GameState carries addedTime, lastActionType, kickOffTeam, and kickOffActive fields'
    - 'GameState declares optional replayIndex and replayTotal fields carried on replay frames (D-31)'
    - 'kickOffActive flags that the first ball action after kick-off must be a Standard Pass from the centre hex (MATCH-03 / D-27)'
    - 'KICK_OFF_SETUP is a valid GamePhase value'
    - 'New Socket.io client events game:ready, game:kick-off-move, game:half-time-start are declared and typed'
  artifacts:
    - path: 'packages/shared/src/actionSequence.ts'
      provides: 'ELIGIBLE_NEXT_ACTIONS constant + NextActionType type'
      contains: 'ELIGIBLE_NEXT_ACTIONS'
    - path: 'packages/shared/src/actionSequence.test.ts'
      provides: 'Unit tests for the eligibility table'
      contains: 'HIGH_PASS'
    - path: 'packages/shared/src/types.ts'
      provides: 'LastActionType type, KICK_OFF_SETUP phase, new GameState fields, new ActionEvent subtypes'
      contains: 'LastActionType'
  key_links:
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/actionSequence.ts'
      via: 'barrel re-export'
      pattern: "export \\* from './actionSequence"
---

<objective>
Add all Phase 8 shared-package primitives that the server engine, server handlers, and client all depend on: the `LastActionType` type, the `ELIGIBLE_NEXT_ACTIONS` action-sequence lookup table (D-07/D-08), the new `GameState` fields (D-06, D-27, D-31), the `KICK_OFF_SETUP` phase value, the new `ActionEvent` subtypes for replay coverage, and the three new client Socket.io events.

Purpose: This is the contract layer. Every later plan imports these types and constants. Defining them first prevents the "scavenger hunt" anti-pattern in downstream plans — they receive the exact type shapes and the eligibility constant rather than inferring them.
Output: Extended `types.ts`, `events.ts`, new `actionSequence.ts` + test, updated `index.ts` barrel. `pnpm --filter @counter-attack/shared build` and `test` both pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: actionSequence.ts module + eligibility table tests</name>
  <files>packages/shared/src/actionSequence.ts, packages/shared/src/actionSequence.test.ts</files>
  <read_first>
    - packages/shared/src/types.ts (to import LastActionType — added in Task 2; if ordering forces it, define LastActionType in types.ts first then this file)
    - packages/shared/src/snapshotValidator.ts (file header + functional-module style analog per PATTERNS.md)
    - packages/shared/src/pitch.ts (ReadonlySet usage pattern for PITCH_REGIONS)
  </read_first>
  <behavior>
    - ELIGIBLE_NEXT_ACTIONS[MOVEMENT_PHASE] has exactly: MOVEMENT, STANDARD_PASS, HIGH_PASS, LONG_BALL, SNAPSHOT, SHOT
    - ELIGIBLE_NEXT_ACTIONS[SUCCESSFUL_TACKLE] has exactly: MOVEMENT, STANDARD_PASS, HIGH_PASS, LONG_BALL, SNAPSHOT (no SHOT)
    - ELIGIBLE_NEXT_ACTIONS[STANDARD_PASS] has exactly: MOVEMENT, FIRST_TIME_PASS, SNAPSHOT
    - ELIGIBLE_NEXT_ACTIONS[FIRST_TIME_PASS] has exactly: MOVEMENT, SNAPSHOT
    - ELIGIBLE_NEXT_ACTIONS[HIGH_PASS] has exactly: HEADER (and nothing else)
    - ELIGIBLE_NEXT_ACTIONS[LONG_BALL] has exactly: MOVEMENT, HEADER
    - ELIGIBLE_NEXT_ACTIONS[HEADER] has exactly: MOVEMENT, FIRST_TIME_PASS, SNAPSHOT
    - ELIGIBLE_NEXT_ACTIONS[DEFLECTION] has exactly: MOVEMENT, FIRST_TIME_PASS, LONG_BALL, SNAPSHOT
    - ELIGIBLE_NEXT_ACTIONS[SNAPSHOT] and [SHOT] are empty sets
    - Every LastActionType key is present (the Record is exhaustive — TypeScript compile enforces this)
  </behavior>
  <action>
    Create `packages/shared/src/actionSequence.ts` exporting a `NextActionType` union ('MOVEMENT' | 'STANDARD_PASS' | 'FIRST_TIME_PASS' | 'HIGH_PASS' | 'LONG_BALL' | 'HEADER' | 'SNAPSHOT' | 'SHOT') and a constant `ELIGIBLE_NEXT_ACTIONS: Record<LastActionType, ReadonlySet<NextActionType>>` populated EXACTLY per the D-08 table reproduced in 08-PATTERNS.md (the membership is enumerated in the behavior block above and is the canonical D-08 matrix). Import `LastActionType` from `./types.js`. Use `new Set([...])` per key; type the Record as `ReadonlySet` to match the PITCH_REGIONS pattern. No class, no default export — single named const + type, matching snapshotValidator.ts functional-module style. Then create `actionSequence.test.ts` using Vitest (`describe`/`it`/`expect`) asserting each key's membership exactly via `.has(...)` true/false checks and `.size` for the High Pass (size 1) and SNAPSHOT/SHOT (size 0) cases. Reference D-07/D-08 in a file-header comment.
  </action>
  <verify>
    <automated>cd packages/shared && pnpm exec vitest run src/actionSequence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/actionSequence.ts` contains `export const ELIGIBLE_NEXT_ACTIONS`
    - `actionSequence.ts` contains `Record<LastActionType, ReadonlySet<NextActionType>>`
    - `ELIGIBLE_NEXT_ACTIONS.HIGH_PASS.size === 1` and `ELIGIBLE_NEXT_ACTIONS.HIGH_PASS.has('HEADER')` is true (asserted in test)
    - `ELIGIBLE_NEXT_ACTIONS.SUCCESSFUL_TACKLE.has('SHOT')` is false (asserted in test)
    - `cd packages/shared && pnpm exec vitest run src/actionSequence.test.ts` exits 0
  </acceptance_criteria>
  <done>actionSequence.ts exports the eligibility table; its test file passes and asserts every key's membership against the D-08 matrix.</done>
</task>

<task type="auto">
  <name>Task 2: types.ts extensions — LastActionType, KICK_OFF_SETUP, GameState fields, ActionEvent subtypes</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>
    - packages/shared/src/types.ts (the full current file — extend in place, do not rewrite existing members)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md (types.ts section: exact new member shapes)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md (D-06 field definitions, D-06 LastActionType union, D-27 kick-off pass-from-centre constraint, D-31 replay frame metadata)
  </read_first>
  <action>
    Extend `packages/shared/src/types.ts` (do NOT remove or reorder existing members):
    1. Add `LastActionType` union type (place it before `GameState`) with exactly these members: 'MOVEMENT_PHASE' | 'SUCCESSFUL_TACKLE' | 'STANDARD_PASS' | 'FIRST_TIME_PASS' | 'HIGH_PASS' | 'LONG_BALL' | 'HEADER' | 'DEFLECTION' | 'SNAPSHOT' | 'SHOT' (per D-06).
    2. Add `'KICK_OFF_SETUP'` to the `GamePhase` union (it is currently absent — confirmed by inspection; place after 'KICK_OFF').
    3. Append to `ActionEventType` union the literals: 'HIGH_PASS', 'LONG_BALL', 'STANDARD_PASS', 'FIRST_TIME_PASS', 'SHOT_ATTEMPT', 'SNAPSHOT', 'HALF_TIME', 'FULL_TIME'.
    4. Append to the `ActionEvent` discriminated union these member shapes (matching the existing `type` + payload + `timestamp: number` style): HIGH_PASS/LONG_BALL/STANDARD_PASS/FIRST_TIME_PASS each `{ from: HexCoord; to: HexCoord; accurate: boolean }`; SHOT_ATTEMPT `{ shooterId: string; targetHex: HexCoord; outcome: 'GOAL' | 'MISS' | 'SAVE' | 'LOOSE_BALL' }`; SNAPSHOT `{ shooterId: string }`; HALF_TIME `{ half: 1; score: { home: number; away: number } }`; FULL_TIME `{ score: { home: number; away: number } }`. All include `timestamp: number`. (Claude's Discretion subtypes per CONTEXT.)
    5. Add these fields to the `GameState` type per D-06/D-27/D-31. Document each with a one-line comment citing the deciding decision:
       - `addedTime: number | null` (D-06 — null until actionCount first crosses 45)
       - `lastActionType: LastActionType | null` (D-06 — null at match start and after kick-off reset)
       - `kickOffTeam: 'home' | 'away'` (D-06 — team that kicked off first half)
       - `kickOffActive: boolean` (D-27 / MATCH-03 — true once the phase transitions KICK_OFF → MOVEMENT after kick-off confirmation; while true the first ball action MUST be a Standard Pass originating from the centre hex; cleared to false after the first accurate Standard Pass resolves)
       - `replayIndex?: number` (D-31 — optional; 1-based position carried on REPLAY-phase frames only; absent outside replay)
       - `replayTotal?: number` (D-31 — optional; total replay frame count carried on REPLAY-phase frames only; absent outside replay)
  </action>
  <verify>
    <automated>cd packages/shared && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` contains `export type LastActionType =`
    - `types.ts` GamePhase union contains `'KICK_OFF_SETUP'`
    - `types.ts` GameState contains `addedTime: number | null`, `lastActionType: LastActionType | null`, and `kickOffTeam: 'home' | 'away'`
    - `types.ts` GameState contains `kickOffActive: boolean` (D-27 / MATCH-03 enforcement flag)
    - `types.ts` GameState contains the optional fields `replayIndex?: number` and `replayTotal?: number` (D-31)
    - `types.ts` ActionEvent union contains a member with `type: 'SHOT_ATTEMPT'` and one with `type: 'HALF_TIME'`
    - `cd packages/shared && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>types.ts compiles with all new Phase 8 types, the KICK_OFF_SETUP phase, the GameState fields (addedTime, lastActionType, kickOffTeam, kickOffActive, optional replayIndex/replayTotal), and the new ActionEvent subtypes.</done>
</task>

<task type="auto">
  <name>Task 3: events.ts new client events + index.ts barrel export</name>
  <files>packages/shared/src/events.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/events.ts (current ClientEvents const + ClientToServerEvents interface — extend in place)
    - packages/shared/src/index.ts (current barrel exports — append only)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md (events.ts and index.ts sections)
  </read_first>
  <action>
    1. In `packages/shared/src/events.ts`, add three entries to the `ClientEvents` const object (keep `as const`): `GAME_READY: 'game:ready'` (D-24 kick-off ready confirmation), `GAME_KICK_OFF_MOVE: 'game:kick-off-move'` (piece repositioning during KICK_OFF_SETUP, distinct from game:move per Research OQ-2/Pitfall 2), `GAME_HALF_TIME_START: 'game:half-time-start'` (D-28 trigger 2nd half). Add matching typed signatures to the `ClientToServerEvents` interface: `[ClientEvents.GAME_READY]: () => void`, `[ClientEvents.GAME_KICK_OFF_MOVE]: (pieceId: string, to: HexCoord) => void`, `[ClientEvents.GAME_HALF_TIME_START]: () => void`. (HexCoord is already imported in events.ts.)
    2. In `packages/shared/src/index.ts`, append `export * from './actionSequence.js';` to the barrel (so `@counter-attack/shared` re-exports ELIGIBLE_NEXT_ACTIONS and NextActionType).
  </action>
  <verify>
    <automated>cd packages/shared && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `events.ts` ClientEvents contains `GAME_READY: 'game:ready'`, `GAME_KICK_OFF_MOVE: 'game:kick-off-move'`, `GAME_HALF_TIME_START: 'game:half-time-start'`
    - `events.ts` ClientToServerEvents declares signatures for all three new events
    - `index.ts` contains `export * from './actionSequence`
    - `cd packages/shared && pnpm build` exits 0 and emits dist with the new exports
  </acceptance_criteria>
  <done>events.ts declares the three new client events with typed signatures; the shared barrel re-exports actionSequence; the package builds clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                                                              |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| client → shared types | Type definitions only; no runtime trust boundary in this plan. Shared package is imported by both client and server; it contains no I/O. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                           | Disposition | Mitigation Plan                                                                                                                                                                                                                 |
| --------- | ---------------------- | --------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-01   | Tampering              | ELIGIBLE_NEXT_ACTIONS constant imported client-side | accept      | Constant is also imported server-side for authoritative enforcement (plan 08-04). Client use is UX-only (button disabling). A tampered client constant cannot bypass server validation. Documented in actionSequence.ts header. |
| T-08-02   | Information Disclosure | new ActionEvent subtypes in event log               | accept      | Event log already broadcast in full per ARCH-04; no new sensitive fields (shooterId/coords already player-visible). No PII.                                                                                                     |
| T-08-SC   | Tampering              | npm/pip/cargo installs                              | accept      | No package installs in this plan or phase (RESEARCH.md §Package Legitimacy Audit: not applicable — zero new dependencies).                                                                                                      |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/shared build` succeeds.
- `pnpm --filter @counter-attack/shared test` passes (includes new actionSequence.test.ts).
- `KICK_OFF_SETUP` is present in the GamePhase union; the GameState fields (addedTime, lastActionType, kickOffTeam, kickOffActive, optional replayIndex/replayTotal) exist; the three client events are typed.
</verification>

<success_criteria>

- ELIGIBLE_NEXT_ACTIONS encodes the D-08 table exactly and is covered by passing tests.
- GameState has addedTime, lastActionType, kickOffTeam, kickOffActive, and optional replayIndex/replayTotal.
- KICK_OFF_SETUP is a valid GamePhase.
- game:ready, game:kick-off-move, game:half-time-start are declared and typed.
- shared package builds and all shared tests pass.
  </success_criteria>

<output>
Create `.planning/phases/08-match-lifecycle-post-game-replay/08-01-SUMMARY.md` when done.
</output>
