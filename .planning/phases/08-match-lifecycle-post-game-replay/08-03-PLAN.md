---
phase: 08-match-lifecycle-post-game-replay
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/roomStore.ts
  - packages/server/src/__tests__/gameEngine.phase8.test.ts
autonomous: true
requirements: [MATCH-03, MATCH-04, REPLAY-02, REPLAY-03]
must_haves:
  truths:
    - 'applyKickOffReady rejects when the attacking team has no piece on the centre hex (q:18,r:13)'
    - "applyKickOffReady rejects defending-team pieces inside the centre circle and pieces outside the team's own half"
    - 'applyHalfTimeStart sets attackingTeam to the opposite of kickOffTeam, increments half to 2, resets actionCount to 0 and addedTime to null'
    - 'buildReplayFrames produces one GameState frame per replay-eligible event and skips SLOT_ADVANCE events'
    - 'Room carries a replayTimer handle and a readyPlayers set, and deleteRoom clears the replayTimer'
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applyKickOffReady, applyHalfTimeStart, buildReplayFrames'
      contains: 'buildReplayFrames'
    - path: 'packages/server/src/roomStore.ts'
      provides: 'Room.replayTimer + Room.readyPlayers fields; deleteRoom clears replayTimer'
      contains: 'replayTimer'
  key_links:
    - from: 'packages/server/src/gameEngine.ts buildReplayFrames'
      to: 'buildInitialGameState + eventLog transitions'
      via: 'deterministic state reconstruction from event log (REPLAY-03)'
      pattern: 'buildReplayFrames'
---

<objective>
Add the kick-off-setup placement validator (`applyKickOffReady`, MATCH-03), the second-half transition (`applyHalfTimeStart`, MATCH-04 — D-26/D-29), and the replay frame builder (`buildReplayFrames`, REPLAY-02/REPLAY-03 — D-31/D-32). Extend the `Room` type with `replayTimer` and `readyPlayers` and clear the timer in `deleteRoom` (Pitfall 4).

Purpose: These three pure engine functions plus the Room state plumbing are the remaining server-side primitives for the match lifecycle and replay. They depend on the clock/sequence work from 08-02 (same file, sequential) and feed the handlers in 08-04.
Output: Extended `gameEngine.ts` (+3 functions) and `roomStore.ts` (+2 Room fields, deleteRoom cleanup); new tests added to `gameEngine.phase8.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-RESEARCH.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: applyKickOffReady + applyHalfTimeStart engine functions</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.phase8.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (applyGKRestart + applyStartMovement for result-type/pure-function pattern; buildInitialGameState for default reset positions)
    - packages/shared/src/pitch.ts (PITCH_REGIONS.centreCircle, kickOffHex {q:18,r:13}, isInRegion)
    - packages/shared/src/teams.ts (HOME_SQUAD/AWAY_SQUAD 4-5-2 default positions for reset)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md (D-23, D-24, D-25, D-26, D-27, D-28, D-29)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-RESEARCH.md (Pattern 4, Pitfall 7 — both teams active during setup)
  </read_first>
  <behavior>
    - applyKickOffReady(state, team) rejects ('CENTRE_HEX_EMPTY') when team is the attacking team and no attacking piece sits on kickOffHex {q:18,r:13} (D-25)
    - applyKickOffReady rejects ('OUT_OF_ZONE') when any of that team's pieces is outside its own half (home q<=18, away q>=18) (D-23)
    - applyKickOffReady rejects ('IN_CENTRE_CIRCLE') when the defending team has a piece inside the centre circle (hexDistance > 3 from kickOffHex required for defenders) (D-23)
    - applyKickOffReady on valid placement returns ok with the team recorded as ready (the handler tracks both-ready transition; engine validates one team's placement)
    - applyHalfTimeStart sets attackingTeam = opposite of kickOffTeam, half = 2, actionCount = 0, addedTime = null, phase = 'KICK_OFF_SETUP', lastActionType = null (D-26, D-29, D-10)
    - applyHalfTimeStart rejects when phase is not HALF_TIME
  </behavior>
  <action>
    Add `ApplyKickOffReadyResult` (`{ ok: false; reason: 'WRONG_PHASE' | 'CENTRE_HEX_EMPTY' | 'OUT_OF_ZONE' | 'IN_CENTRE_CIRCLE' } | { ok: true; state: GameState }`) and a pure `applyKickOffReady(state: GameState, team: 'home' | 'away'): ApplyKickOffReadyResult`. Validate (phase must be KICK_OFF_SETUP; else WRONG_PHASE): for the given team's pieces, every piece must be in its own half (home → `piece.position.q <= PITCH_REGIONS.kickOffHex.q`; away → `>= kickOffHex.q`; kickOffHex.q is 18) else OUT_OF_ZONE; if the team is the DEFENDING team (team !== state.attackingTeam), no piece may be inside the centre circle (`!isInRegion(piece.position, 'centreCircle')` for all) else IN_CENTRE_CIRCLE; if the team is the ATTACKING team, exactly one of its pieces must occupy kickOffHex {q:18,r:13} else CENTRE_HEX_EMPTY (D-25). Return ok:true with state unchanged (the both-ready→KICK_OFF transition is owned by the handler via Room.readyPlayers per Pattern 4). Add `ApplyHalfTimeStartResult` and pure `applyHalfTimeStart(state: GameState): ApplyHalfTimeStartResult`: reject WRONG_PHASE unless `phase === 'HALF_TIME'`; on success return state with `attackingTeam: state.kickOffTeam === 'home' ? 'away' : 'home'`, `half: 2`, `actionCount: 0`, `addedTime: null`, `phase: 'KICK_OFF_SETUP'`, `lastActionType: null`, and pieces reset to the 4-5-2 default positions from teams.ts (so setup begins from formation). Add tests covering each rejection reason and the successful half-time transition field-by-field.
  </action>
  <verify>
    <automated>cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts -t "kick-off setup"</automated>
  </verify>
  <acceptance_criteria>
    - `gameEngine.ts` exports `applyKickOffReady` and `applyHalfTimeStart`
    - Test asserts applyKickOffReady returns `reason: 'CENTRE_HEX_EMPTY'` when attacking team has no piece on {q:18,r:13}
    - Test asserts applyKickOffReady returns `reason: 'IN_CENTRE_CIRCLE'` for a defending piece inside the centre circle
    - Test asserts applyHalfTimeStart sets `half: 2`, `actionCount: 0`, `addedTime: null`, and `attackingTeam` = opposite of kickOffTeam
    - `cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts -t "kick-off setup"` exits 0
  </acceptance_criteria>
  <done>applyKickOffReady enforces MATCH-03 placement rules with typed reasons; applyHalfTimeStart performs the MATCH-04 second-half flip and half reset; both are unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildReplayFrames deterministic reconstruction (REPLAY-02, REPLAY-03)</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.phase8.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (buildInitialGameState, applyMove, applyRoll — the transitions to replay; eventLog shape)
    - packages/shared/src/types.ts (ActionEvent union + ActionEventType — which events are replay-eligible)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md (D-31, D-32)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-RESEARCH.md (Pattern 3, Pitfall 6 reset, Don't Hand-Roll — replay reconstruction, A2 assumption re shotTarget UX-only)
  </read_first>
  <behavior>
    - buildReplayFrames(finalState) returns a GameState[] reconstructed by replaying finalState.eventLog from buildInitialGameState
    - SLOT_ADVANCE events produce NO frame (skipped, D-32)
    - MOVE, DICE_ROLL, STEAL_ATTEMPT, GOAL, KICK_OFF events (and the new pass/shot/snapshot event subtypes that change board state) each produce exactly one frame (D-32)
    - The number of returned frames equals the count of replay-eligible (non-SLOT_ADVANCE) events
    - Reconstruction is deterministic: the same eventLog always yields the same frame sequence (REPLAY-03 — no extra data capture; reads only eventLog)
  </behavior>
  <action>
    Add a pure `buildReplayFrames(finalState: GameState): GameState[]`. Start from `buildInitialGameState(finalState.roomCode)` (or a seeded equivalent that matches the original kick-off team — use finalState.kickOffTeam to seed attackingTeam so reconstruction matches; A2). Iterate `finalState.eventLog`; for each event, apply the corresponding board-state mutation (reusing existing engine helpers where they exist) and push a snapshot of the resulting GameState into the frames array — EXCEPT for `SLOT_ADVANCE` events, which are skipped and produce no frame (D-32). For DICE_ROLL/STEAL_ATTEMPT/GOAL/KICK_OFF and the pass/shot/snapshot subtypes, push a frame reflecting that event's state change. Each pushed frame must carry `phase: 'REPLAY'` so the client renders the replay screen, and the persistent final `score`. Keep the function pure (no setInterval here — the handler owns timing, plan 08-04). Add `describe('buildReplayFrames — REPLAY-02/03')` tests: a log with N non-SLOT_ADVANCE events yields N frames; SLOT_ADVANCE events are skipped (a log of only SLOT_ADVANCE yields 0 frames); reconstruction from the same log twice yields deep-equal frame arrays (determinism).
  </action>
  <verify>
    <automated>cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts -t "buildReplayFrames"</automated>
  </verify>
  <acceptance_criteria>
    - `gameEngine.ts` exports `buildReplayFrames`
    - Test asserts a SLOT_ADVANCE-only event log produces a zero-length frame array
    - Test asserts frame count equals non-SLOT_ADVANCE event count for a mixed log
    - Test asserts two calls with the same eventLog return deep-equal arrays (determinism)
    - Every returned frame has `phase === 'REPLAY'` (asserted)
    - `cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts -t "buildReplayFrames"` exits 0
  </acceptance_criteria>
  <done>buildReplayFrames deterministically reconstructs one frame per replay-eligible event, skips SLOT_ADVANCE, tags frames as REPLAY phase, and is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Room type replayTimer + readyPlayers fields and deleteRoom cleanup</name>
  <files>packages/server/src/roomStore.ts</files>
  <read_first>
    - packages/server/src/roomStore.ts (Room type, deleteRoom, disconnectTimers cleanup loop — extend in place)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md (roomStore.ts section)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-RESEARCH.md (Pitfall 4)
  </read_first>
  <action>
    Extend the `Room` type in `roomStore.ts` with two optional fields following the existing `shotTarget?: HexCoord | null` optional-field convention: `replayTimer?: ReturnType<typeof setInterval> | null` (D-31 replay loop handle) and `readyPlayers?: Set<1 | 2> | null` (D-24 KICK_OFF_SETUP ready tracking, Pattern 4). In `deleteRoom`, after the existing `disconnectTimers` cleanup loop, add `if (room.replayTimer) clearInterval(room.replayTimer);` to prevent the post-deletion emit leak (Pitfall 4). Do not change `broadcastState` or other functions.
  </action>
  <verify>
    <automated>cd packages/server && pnpm exec tsc --noEmit && pnpm exec vitest run src/__tests__/roomStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `roomStore.ts` Room type contains `replayTimer?: ReturnType<typeof setInterval> | null`
    - `roomStore.ts` Room type contains `readyPlayers?: Set<1 | 2> | null`
    - `roomStore.ts` deleteRoom contains `clearInterval(room.replayTimer)` guarded by `if (room.replayTimer)`
    - `cd packages/server && pnpm exec tsc --noEmit` exits 0 and existing roomStore tests still pass
  </acceptance_criteria>
  <done>Room carries replayTimer and readyPlayers; deleteRoom clears the replay interval; the server package type-checks and existing roomStore tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                                                                                      |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| handler → engine (kick-off placement) | applyKickOffReady receives the team derived from the socket's player slot (validated in handler, plan 08-04); the engine validates placement rules but trusts the team argument. |
| replay timer lifecycle                | setInterval handle stored on the in-memory Room; must be cleared on room deletion/disconnect to avoid emit-to-dead-room.                                                         |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                                               |
| --------- | ---------------------- | ---------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| T-08-06   | Tampering              | kick-off placement bypass          | mitigate    | applyKickOffReady validates own-half, centre-circle, and centre-hex rules server-side (MATCH-03); the handler (08-04) rejects with snap-back. Client zone tinting is UX-only. |
| T-08-07   | Denial of Service      | replay timer leak                  | mitigate    | deleteRoom clears replayTimer (Pitfall 4); plan 08-04 also clears it on disconnect during REPLAY. Verified by deleteRoom acceptance criterion.                                |
| T-08-08   | Elevation of Privilege | one team confirming ready for both | mitigate    | readyPlayers is a Set keyed by player slot 1                                                                                                                                  | 2; the handler adds only the confirming socket's slot, so a single socket cannot mark both ready (enforced in 08-04). |
| T-08-SC   | Tampering              | npm/pip/cargo installs             | accept      | No package installs in this plan or phase (RESEARCH.md §Package Legitimacy Audit: not applicable).                                                                            |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` passes including the new kick-off-setup, half-time, and buildReplayFrames tests.
- deleteRoom clears replayTimer.
</verification>

<success_criteria>

- applyKickOffReady enforces all MATCH-03 placement rules with typed rejection reasons.
- applyHalfTimeStart flips attackingTeam to the non-kick-off team and resets half/actionCount/addedTime.
- buildReplayFrames is deterministic, skips SLOT_ADVANCE, and tags frames as REPLAY.
- Room has replayTimer + readyPlayers; deleteRoom clears the timer.
  </success_criteria>

<output>
Create `.planning/phases/08-match-lifecycle-post-game-replay/08-03-SUMMARY.md` when done.
</output>
