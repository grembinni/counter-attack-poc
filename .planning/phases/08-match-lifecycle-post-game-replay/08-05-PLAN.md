---
phase: 08-match-lifecycle-post-game-replay
plan: 05
type: execute
wave: 5
depends_on: [08-01, 08-04]
files_modified:
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/App.tsx
  - packages/client/src/components/HalfTimeScreen.tsx
  - packages/client/src/components/HalfTimeScreen.module.css
  - packages/client/src/components/FullTimeScreen.tsx
  - packages/client/src/components/FullTimeScreen.module.css
  - packages/client/src/components/KickOffSetupPanel.tsx
  - packages/client/src/components/KickOffSetupPanel.module.css
autonomous: true
requirements: [MATCH-03, MATCH-04, MATCH-05]
must_haves:
  truths:
    - 'The store Screen type includes HALF_TIME, FULL_TIME, and REPLAY, and exposes emitReady, emitKickOffMove, emitHalfTimeStart emitters'
    - 'App.tsx routes GameState.phase to the correct screen (HALF_TIME/FULL_TIME/REPLAY screens; KICK_OFF_SETUP stays on GAME_BOARD)'
    - 'HalfTimeScreen shows the score and a Start 2nd Half button enabled only for the non-first-half kick-off team'
    - 'FullTimeScreen shows the final score and result line and displays a Replay starting notice (auto-transitions when REPLAY arrives)'
    - 'KickOffSetupPanel shows live placement constraint status and a Ready button disabled until constraints are met'
  artifacts:
    - path: 'packages/client/src/components/HalfTimeScreen.tsx'
      provides: 'Half-time screen with Start 2nd Half'
      contains: 'Start 2nd Half'
    - path: 'packages/client/src/components/FullTimeScreen.tsx'
      provides: 'Full-time screen with final score + result'
      contains: 'Full Time'
    - path: 'packages/client/src/components/KickOffSetupPanel.tsx'
      provides: 'Kick-off setup sidebar panel with Ready button'
      contains: 'Kick-Off Setup'
  key_links:
    - from: 'packages/client/src/App.tsx onGameState'
      to: 'store.setScreen by GameState.phase'
      via: 'phase-conditional screen routing'
      pattern: 'HALF_TIME'
---

<objective>
Add the client routing and lifecycle screens: extend the Zustand store `Screen` type and emitters (D-24/D-28), route `GameState.phase` to the new screens in `App.tsx`, and build the `HalfTimeScreen`, `FullTimeScreen`, and `KickOffSetupPanel` components per the UI-SPEC (frozen tokens, exact copy).

Purpose: This delivers the user-visible match lifecycle: kick-off setup confirmation, the half-time hand-off, and the full-time result. It depends on the new store emitters and the server handlers from 08-04.
Output: Extended `useGameStore.ts` and `App.tsx`; three new components (each with its CSS module). `pnpm --filter @counter-attack/client build` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-UI-SPEC.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: store Screen type + emitters; App.tsx phase routing</name>
  <files>packages/client/src/store/useGameStore.ts, packages/client/src/App.tsx</files>
  <read_first>
    - packages/client/src/store/useGameStore.ts (Screen type line 8, existing emitter pattern, selectedPieceId/validMoveHexes slices)
    - packages/client/src/App.tsx (onGameState handler ~line 21, render-return ternary ~line 76, socket listener useEffect + cleanup)
    - packages/shared/src/events.ts (ClientEvents.GAME_READY / GAME_KICK_OFF_MOVE / GAME_HALF_TIME_START)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md (useGameStore.ts + App.tsx sections — exact extension shapes)
  </read_first>
  <action>
    In `useGameStore.ts`: extend the `Screen` union to add `'HALF_TIME' | 'FULL_TIME' | 'REPLAY'` (KICK_OFF_SETUP intentionally NOT added — it reuses 'GAME_BOARD' so the pitch stays visible, per UI-SPEC). Add three emitters following the existing `socket.emit(ClientEvents.X)` pattern: `emitReady: () => { socket.emit(ClientEvents.GAME_READY); }`; `emitKickOffMove: (pieceId: string, to: HexCoord) => { socket.emit(ClientEvents.GAME_KICK_OFF_MOVE, pieceId, to); set({ selectedPieceId: null, validMoveHexes: [] }); }`; `emitHalfTimeStart: () => { socket.emit(ClientEvents.GAME_HALF_TIME_START); }`. Add their signatures to the `GameStore` type.
    In `App.tsx` `onGameState(state)`: after `setGameState(state)`, route by phase — `state.phase === 'HALF_TIME'` → `setScreen('HALF_TIME')`; `'FULL_TIME'` → `setScreen('FULL_TIME')`; `'REPLAY'` → `setScreen('REPLAY')`; else → ensure `'GAME_BOARD'` (covers KICK_OFF_SETUP, MOVEMENT, etc. — pitch visible). Extend the render-return ternary to render `<HalfTimeScreen />` when screen === 'HALF_TIME', `<FullTimeScreen />` when 'FULL_TIME', and (placeholder/import) `<ReplayScreen />` or `<GameBoard />` for 'REPLAY' — for this plan, route REPLAY to `<GameBoard />` (the ReplayPanel swap lands in 08-06; keep the import wiring minimal and note REPLAY uses the board layout). Import HalfTimeScreen and FullTimeScreen. Keep all existing socket-listener cleanup intact.
  </action>
  <verify>
    <automated>cd packages/client && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `useGameStore.ts` Screen type contains `'HALF_TIME'`, `'FULL_TIME'`, `'REPLAY'`
    - `useGameStore.ts` defines `emitReady`, `emitKickOffMove`, `emitHalfTimeStart`
    - `App.tsx` onGameState sets screen to 'HALF_TIME'/'FULL_TIME'/'REPLAY' by phase and falls back to 'GAME_BOARD'
    - `App.tsx` render imports and routes `HalfTimeScreen` and `FullTimeScreen`
    - `cd packages/client && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Store exposes the new screen values and emitters; App.tsx routes phases to the correct screens with cleanup preserved.</done>
</task>

<task type="auto">
  <name>Task 2: HalfTimeScreen + FullTimeScreen components</name>
  <files>packages/client/src/components/HalfTimeScreen.tsx, packages/client/src/components/HalfTimeScreen.module.css, packages/client/src/components/FullTimeScreen.tsx, packages/client/src/components/FullTimeScreen.module.css</files>
  <read_first>
    - packages/client/src/components/LobbyScreen.tsx (full-screen card layout analog; per-slice selector usage)
    - packages/client/src/components/LobbyScreen.module.css (.page / .card / .heading / .ctaButton token values to copy)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-UI-SPEC.md (Screen 2 HALF_TIME, Screen 3 FULL_TIME — exact copy, typography, colours; Copywriting Contract)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md (HalfTimeScreen / FullTimeScreen sections)
  </read_first>
  <action>
    Create `HalfTimeScreen.tsx`: full-screen centred card (`.page` min-height 100vh flex-centre bg #1a1a2e; `.card` max-width 440px bg #16213e border 1px #0f3460 radius 4px padding 24px gap 16px). Use per-slice selectors: `score`, `playerSlot`, `gameState.kickOffTeam`, `gameState.addedTime`, `emitHalfTimeStart`. Render (top→bottom): heading "Half Time" (20px/700/#e0e0e0); "End of 1st Half" (13px/400/#a0a0a0); score `{home} – {away}` at 28px/700/#e0e0e0 monospace ('Courier New', Courier, monospace) with "Home" (#1a56b0) / "Away" (#c0392b) labels at 13px/400; "Added time played: +{addedTime}'" shown only when `addedTime > 0`; "2nd half kick-off: {teamName}" (team that did NOT kick off first, coloured by team); a "Start 2nd Half" `.ctaButton` (bg #0f3460, padding 10px 20px, 13px/700) — compute `myTeam = playerSlot === 1 ? 'home' : playerSlot === 2 ? 'away' : null`, `canStart = myTeam !== null && myTeam !== kickOffTeam`; disable (opacity 0.5; cursor default) when `!canStart` with `title="Only the 2nd half kick-off team can start"`; onClick `emitHalfTimeStart()`. No auto-advance timer.
    Create `FullTimeScreen.tsx`: same `.page`/`.card` layout. Selectors: `score`. Render: heading "Full Time"; score display (same 28px monospace format + team labels); result line — `home > away` → "Home wins" (13px/700/#1a56b0), `away > home` → "Away wins" (#c0392b), else "Draw" (#e0e0e0); transition notice "Replay starting…" (13px/400/#a0a0a0) static. NO buttons (the server transitions to REPLAY after ~3s; App routes on the next game:state). Copy both CSS modules from LobbyScreen.module.css token values; introduce NO new colour/size tokens (UI-SPEC frozen inventory).
  </action>
  <verify>
    <automated>cd packages/client && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `HalfTimeScreen.tsx` contains the strings "Half Time" and "Start 2nd Half"
    - HalfTimeScreen Start button is disabled when `myTeam === kickOffTeam` (source: `disabled={!canStart}` with canStart derived from playerSlot vs kickOffTeam)
    - `FullTimeScreen.tsx` contains "Full Time", a result line covering wins/draw, and "Replay starting…"
    - Both CSS modules use only frozen tokens (#1a1a2e, #16213e, #0f3460, #e0e0e0, #a0a0a0, #1a56b0, #c0392b) — no new hex values
    - `cd packages/client && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>HalfTimeScreen and FullTimeScreen render per the UI-SPEC with exact copy, frozen tokens, and correct Start-2nd-Half gating.</done>
</task>

<task type="auto">
  <name>Task 3: KickOffSetupPanel component (constraint status + Ready)</name>
  <files>packages/client/src/components/KickOffSetupPanel.tsx, packages/client/src/components/KickOffSetupPanel.module.css</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (sidebar panel analog; per-slice selectors; ctaButton + disabled pattern; gameError display)
    - packages/client/src/components/ActionPanel.module.css (.panel / .ctaButton / disabled token values)
    - packages/shared/src/pitch.ts (PITCH_REGIONS.kickOffHex, centreCircle, isInRegion — for client-side constraint display only)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-UI-SPEC.md (Screen 1 KICK_OFF_SETUP — KickOffSetupPanel contents, constraint rows, Ready button states, copy)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md (KickOffSetupPanel section)
  </read_first>
  <action>
    Create `KickOffSetupPanel.tsx`. Selectors: `gameState.phase`, `playerSlot`, `gameState.pieces`, `gameState.attackingTeam`, `gameState.gameError` (or `gameError`), `emitReady`. Return null unless `phase === 'KICK_OFF_SETUP'`. Both players see it (no isActivePlayer gate). Compute `myTeam = playerSlot === 1 ? 'home' : 'away'`; `isAttacking = myTeam === attackingTeam`. Compute constraint display (client-side reflection only — server is authoritative): centreHexOccupied = some of my team's pieces at {q:18,r:13}; piecesOutOfZone = count of my pieces failing the own-half rule (home q<=18 / away q>=18) or, for defenders, inside the centre circle. Render: heading "Kick-Off Setup" (13px/700/#e0e0e0); instruction "Place your players in your permitted zone. One player must be on the centre hex." (13px/400/#a0a0a0, line-height 1.5); constraint rows per UI-SPEC — attacking team sees the centre-hex row ("Centre hex: occupied" #a0a0a0 / "Centre hex: EMPTY — required" #ef4444); both see placement row ("Placement: valid" / "Placement: {N} pieces out of zone" #ef4444); a server `gameError` line at 13px/400/#ef4444 (auto-clears on next game:state). "Ready" `.ctaButton`: disabled when any of my constraints unmet (opacity 0.5; cursor default); `title` per team when disabled (attacking: "Place a player on the centre hex first"; defending: "Move all players to your own half outside the centre circle"); onClick `emitReady()`. After the local player has confirmed ready (track via a local `useState` set on click) and opponent has not, change the label to "Waiting for opponent…" (13px/400/#a0a0a0) and disable. Copy `.panel`/`.ctaButton` from ActionPanel.module.css; no new tokens.
  </action>
  <verify>
    <automated>cd packages/client && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `KickOffSetupPanel.tsx` returns null when `phase !== 'KICK_OFF_SETUP'`
    - Contains the strings "Kick-Off Setup", "Ready", and the centre-hex constraint copy
    - Ready button uses `disabled={...}` tied to unmet constraints and switches to "Waiting for opponent…" after local confirm
    - `KickOffSetupPanel.module.css` uses only frozen tokens (#16213e, #0f3460, #e0e0e0, #a0a0a0, #ef4444)
    - `cd packages/client && pnpm build` exits 0
  </acceptance_criteria>
  <done>KickOffSetupPanel renders for both players with live constraint status, a properly gated Ready button, and the waiting-for-opponent state, using frozen tokens.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                                                                      |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| client UI → server | The new screens emit game:ready / game:half-time-start / game:kick-off-move; all are re-validated server-side (08-04). Client gating is UX-only. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                           | Disposition | Mitigation Plan                                                                                                                                                                          |
| --------- | ---------------------- | ------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-16   | Tampering              | client disabling Ready/Start buttons could be bypassed via devtools | accept      | Client button gating is UX reflection only; server (08-04) authoritatively validates game:ready placement and game:half-time-start team. A bypassed client emit is rejected server-side. |
| T-08-17   | Information Disclosure | constraint computation reads full piece list                        | accept      | Piece list is already fully broadcast per ARCH-04; no new data exposure.                                                                                                                 |
| T-08-SC   | Tampering              | npm/pip/cargo installs                                              | accept      | No package installs in this plan or phase (RESEARCH.md §Package Legitimacy Audit: not applicable).                                                                                       |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client build` succeeds.
- Store has new screen values + emitters; App routes phases; three screens render with frozen tokens and exact copy.
</verification>

<success_criteria>

- HALF_TIME/FULL_TIME/REPLAY screen values + emitReady/emitKickOffMove/emitHalfTimeStart in the store.
- App.tsx routes GameState.phase to the right screen.
- HalfTimeScreen, FullTimeScreen, KickOffSetupPanel match the UI-SPEC.
  </success_criteria>

<output>
Create `.planning/phases/08-match-lifecycle-post-game-replay/08-05-SUMMARY.md` when done.
</output>
