---
phase: 08-match-lifecycle-post-game-replay
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - packages/server/src/gameHandlers.ts
  - packages/server/src/__tests__/kickoffSetup.integration.test.ts
  - packages/server/src/__tests__/replay.integration.test.ts
autonomous: true
requirements: [MATCH-01, MATCH-02, MATCH-03, MATCH-04, REPLAY-01]
must_haves:
  truths:
    - 'A game:ready event from a player with invalid placement is rejected with a game:error and the state snaps back'
    - 'KICK_OFF_SETUP transitions to KICK_OFF only after BOTH players confirm ready'
    - 'When the KICK_OFF phase advances to MOVEMENT, kickOffActive is set true so the first ball action is forced to be a Standard Pass from the centre hex (MATCH-03 / D-27)'
    - 'While kickOffActive is true the server rejects any game:pass whose type is not STANDARD, and rejects a non-centre-hex origin (INVALID_KICK_OFF_PASS); kickOffActive clears after the first accurate Standard Pass resolves'
    - 'Every action handler rejects an action that is not eligible after the current lastActionType (INVALID_SEQUENCE)'
    - 'The end-turn handler injects a crypto.randomInt added-time roll into applyEndTurn'
    - 'After FULL_TIME the server streams game:state frames at 1-second intervals via setInterval and clears the timer when frames are exhausted'
  artifacts:
    - path: 'packages/server/src/gameHandlers.ts'
      provides: 'game:ready, game:kick-off-move, game:half-time-start handlers; sequence guards; kick-off pass-from-centre enforcement (D-27); replay setInterval'
      contains: 'GAME_READY'
    - path: 'packages/server/src/__tests__/replay.integration.test.ts'
      provides: 'Over-the-wire replay frame timing test'
      contains: 'GAME_STATE'
    - path: 'packages/server/src/__tests__/kickoffSetup.integration.test.ts'
      provides: 'Over-the-wire kick-off placement validation test'
      contains: 'game:ready'
  key_links:
    - from: 'packages/server/src/gameHandlers.ts'
      to: 'ELIGIBLE_NEXT_ACTIONS / applyKickOffReady / buildReplayFrames / gameState.kickOffActive'
      via: 'imports from @counter-attack/shared and gameEngine; reads/writes kickOffActive flag'
      pattern: 'ELIGIBLE_NEXT_ACTIONS'
---

<objective>
Wire the Phase 8 engine functions into Socket.io handlers: new `game:ready`, `game:kick-off-move`, and `game:half-time-start` handlers; `lastActionType` sequence validation (D-07) on every existing action handler; the kick-off "first pass must be a Standard Pass from the centre hex" enforcement (D-27 / MATCH-03) gated on `kickOffActive`; the injected added-time roll in the end-turn handler (D-05); and the replay `setInterval` streaming loop after FULL_TIME (D-31/REPLAY-01). All handlers follow the established isProcessing-mutex + snap-back-on-error + team-guard patterns.

Purpose: This is the request-response layer that turns the pure engine into live, validated, multiplayer behaviour. It is the security boundary for all Phase 8 untrusted client input (V4 Access Control, V5 Input Validation).
Output: Extended `gameHandlers.ts`; two new integration tests (kick-off placement over the wire, replay frame delivery over the wire).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-RESEARCH.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: game:ready, game:kick-off-move, game:half-time-start handlers</name>
  <files>packages/server/src/gameHandlers.ts, packages/server/src/__tests__/kickoffSetup.integration.test.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (GAME_START_MOVEMENT handler scaffold, GAME_MOVE handler, socketTeam helper, isActivePlayer, broadcastState usage, snap-back-on-error pattern)
    - packages/server/src/roomStore.ts (getRoom, Room.readyPlayers field, broadcastState)
    - packages/shared/src/events.ts (ClientEvents.GAME_READY / GAME_KICK_OFF_MOVE / GAME_HALF_TIME_START)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md (gameHandlers.ts section — exact 12-line handler scaffold)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md (D-24 ready transition, D-27 first-pass-from-centre constraint, D-28 second-half start)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-RESEARCH.md (Pitfall 2, Pitfall 7, Security Domain threat patterns)
  </read_first>
  <action>
    Add three handlers in `gameHandlers.ts`, each copying the GAME_START_MOVEMENT scaffold (roomCode guard → getRoom → `if (!room || room.isProcessing) return` → `room.isProcessing = true; try { ... } finally { room.isProcessing = false }`). Every `socket.emit(ServerEvents.GAME_ERROR, reason)` MUST be followed by `broadcastState(io, room)` (snap-back pattern).
    1. `GAME_KICK_OFF_MOVE` (pieceId, to): phase guard `phase === 'KICK_OFF_SETUP'` else WRONG_PHASE. Do NOT use isActivePlayer (Pitfall 7 — both teams active). Derive the moving team from `socketTeam(socket)`; reject 'NOT_YOUR_PIECE' if the piece's teamId !== that team (V4/V5 — Tampering mitigation). Reposition the piece to `to` (no pace/ZoI; this is free setup placement, D-23) and broadcast. Validate `to` is a valid HexCoord (numbers q,r) (V5).
    2. `GAME_READY`: phase guard KICK_OFF_SETUP. Call `applyKickOffReady(room.gameState, socketTeam(socket))`; if `!result.ok` emit GAME_ERROR(result.reason) + broadcastState + return. On ok, add the socket's player slot to `room.readyPlayers` (initialise the Set if null — only the confirming socket's own slot, never both, per T-08-08). When `room.readyPlayers.size === 2`, transition `room.gameState.phase` to 'KICK_OFF' and clear readyPlayers (D-24). Broadcast.
    3. `GAME_HALF_TIME_START`: phase guard HALF_TIME. Reject 'NOT_KICK_OFF_TEAM' unless `socketTeam(socket) !== room.gameState.kickOffTeam` (only the non-first-half kick-off team may start; Security Domain). Call `applyHalfTimeStart(room.gameState)`; on ok set `room.gameState = result.state` and broadcast (D-28).
    Kick-off flag wiring (D-27 / MATCH-03): in the handler that advances the phase from KICK_OFF to MOVEMENT (the GAME_START_MOVEMENT handler — the kick-off team starting play), set `room.gameState.kickOffActive = true` at the same point the phase becomes MOVEMENT. (The corresponding enforcement + clear-on-resolve happens in Task 2.) Do the same for the second-half kick-off transition path if KICK_OFF → MOVEMENT is reached there too.
    Create `kickoffSetup.integration.test.ts` (analog: existing game integration tests with a real Socket.io server + socket.io-client) asserting: a `game:ready` with the attacking team's centre hex empty yields a `game:error` and the broadcast state remains KICK_OFF_SETUP; placing a defending piece in the centre circle then `game:ready` is rejected; both teams ready transitions phase to KICK_OFF; and that once play starts (KICK_OFF → MOVEMENT) the broadcast state has `kickOffActive === true`.
  </action>
  <verify>
    <automated>cd packages/server && pnpm exec vitest run src/__tests__/kickoffSetup.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `gameHandlers.ts` registers `ClientEvents.GAME_READY`, `ClientEvents.GAME_KICK_OFF_MOVE`, `ClientEvents.GAME_HALF_TIME_START`
    - Each new handler wraps logic in `room.isProcessing = true; try { ... } finally { room.isProcessing = false }`
    - GAME_KICK_OFF_MOVE rejects a move of an opponent's piece (test asserts game:error / no reposition)
    - GAME_READY transitions to KICK_OFF only when readyPlayers.size === 2 (asserted in integration test)
    - GAME_HALF_TIME_START rejects when the requesting socket's team === kickOffTeam
    - When the phase advances KICK_OFF → MOVEMENT, `room.gameState.kickOffActive` is set to `true` (asserted in integration test)
    - `cd packages/server && pnpm exec vitest run src/__tests__/kickoffSetup.integration.test.ts` exits 0
  </acceptance_criteria>
  <done>Three new handlers enforce KICK_OFF_SETUP placement, both-ready transition, and second-half start with proper guards and snap-back; the KICK_OFF → MOVEMENT transition sets kickOffActive; covered by an over-the-wire integration test.</done>
</task>

<task type="auto">
  <name>Task 2: sequence validation + kick-off pass-from-centre enforcement + added-time roll injection</name>
  <files>packages/server/src/gameHandlers.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (GAME_MOVE, GAME_ROLL, GAME_SHOT, GAME_END_TURN, GAME_START_MOVEMENT, GAME_GK_RESTART, the pass handler; existing rollDice usage)
    - packages/shared/src/actionSequence.ts (ELIGIBLE_NEXT_ACTIONS, NextActionType)
    - packages/shared/src/pitch.ts (PITCH_REGIONS.kickOffHex / centre hex {q:18,r:13} for the centre-origin check)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md (D-07, D-11 movement-after-high-pass rejection, D-13 header-only-after-high-pass, D-19 shot-only-after-movement-or-snapshot, D-27 first-pass-must-be-standard-from-centre)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md (gameHandlers.ts sequence-validation + rollDice-injection patterns)
  </read_first>
  <action>
    Add a sequence guard to each action-initiating handler, placed before the engine call, following the team-guard placement pattern. For a handler initiating next-action `X` (a `NextActionType`): if `room.gameState.lastActionType !== null && !ELIGIBLE_NEXT_ACTIONS[room.gameState.lastActionType].has(X)`, emit `GAME_ERROR('INVALID_SEQUENCE')` + `broadcastState` + return (D-07). Map handlers to their NextActionType: `GAME_START_MOVEMENT` → 'MOVEMENT' (this also enforces D-11 — rejected when lastActionType === 'HIGH_PASS'); the pass handler → its pass type ('STANDARD_PASS'|'HIGH_PASS'|'LONG_BALL'|'FIRST_TIME_PASS' from payload); `game:snapshot` (if a handler exists or via applySnapshot path) → 'SNAPSHOT'; the header path (`game:roll` while phase HEADER, or a dedicated header handler) → 'HEADER' (D-17 — valid only after HIGH_PASS/LONG_BALL); the shot path (`game:shot`) → 'SHOT' (D-19 — only after MOVEMENT_PHASE or SNAPSHOT).

    Kick-off first-pass enforcement (D-27 / MATCH-03), in the pass handler ONLY, placed before the engine call (and before/independent of the sequence guard since lastActionType is null at this point): if `room.gameState.kickOffActive === true`, then (a) reject `GAME_ERROR('INVALID_KICK_OFF_PASS')` + broadcastState + return when the pass payload `type !== 'STANDARD'` (High Pass / Long Ball / First-Time Pass are all rejected at kick-off); and (b) reject `GAME_ERROR('INVALID_KICK_OFF_PASS')` + broadcastState + return when the pass origin hex is not the centre hex {q:18,r:13} (PITCH_REGIONS.kickOffHex). After a Standard Pass from the centre hex resolves successfully (the engine returns `result.ok` and the pass is accurate), clear the flag: `room.gameState.kickOffActive = false` (set it on the post-engine state before broadcast). A Standard Pass that misses still clears kickOffActive (kick-off is taken; the ball is in play) — clear it on any successful Standard-Pass resolution regardless of accuracy.

    In `GAME_END_TURN`, before calling `applyEndTurn`, generate `const addedTimeRoll = rollDice();` (existing crypto.randomInt-backed helper) and pass `applyEndTurn(room.gameState, { addedTimeRoll })` so the engine consumes it only when actionCount crosses 45 and addedTime is null (D-05). Set `room.gameState = result.state` and broadcast as today. Ensure the HIGH_PASS lockout: when `lastActionType === 'HIGH_PASS'`, every handler except the header path returns INVALID_SEQUENCE (this falls out of the ELIGIBLE_NEXT_ACTIONS[HIGH_PASS] = {HEADER} guard).

  </action>
  <verify>
    <automated>cd packages/server && pnpm exec vitest run src/__tests__/gameHandlers.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `gameHandlers.ts` imports `ELIGIBLE_NEXT_ACTIONS` from `@counter-attack/shared`
    - Each action handler contains an `INVALID_SEQUENCE` guard referencing `ELIGIBLE_NEXT_ACTIONS[...]has(...)`
    - When `kickOffActive === true`, the pass handler rejects a pass whose `type !== 'STANDARD'` (High Pass / Long Ball from the centre hex are rejected with `INVALID_KICK_OFF_PASS`)
    - When `kickOffActive === true`, the pass handler rejects a Standard Pass whose origin is not the centre hex {q:18,r:13}
    - `kickOffActive` is cleared to `false` after a Standard Pass from the centre hex resolves (asserted in test)
    - `GAME_END_TURN` handler calls `rollDice()` and passes `{ addedTimeRoll }` into `applyEndTurn`
    - The shot path guard rejects when `lastActionType` is not in the eligible set (e.g. after STANDARD_PASS)
    - `cd packages/server && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Every action handler validates the proposed next action against ELIGIBLE_NEXT_ACTIONS and rejects invalid sequences; while kickOffActive the first ball action is forced to be a Standard Pass from the centre hex and the flag clears on resolution; the end-turn handler injects a crypto-backed added-time roll.</done>
</task>

<task type="auto">
  <name>Task 3: FULL_TIME → REPLAY setInterval streaming loop + disconnect cleanup</name>
  <files>packages/server/src/gameHandlers.ts, packages/server/src/__tests__/replay.integration.test.ts</files>
  <read_first>
    - packages/server/src/gameHandlers.ts (where FULL_TIME is produced — the applyEndTurn result handling in GAME_END_TURN and applyRoll result handling; the disconnect handler)
    - packages/server/src/gameEngine.ts (buildReplayFrames signature/return)
    - packages/server/src/roomStore.ts (Room.replayTimer, broadcastState, io.to(roomCode).emit pattern)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md (D-30, D-31, D-32, D-33; Claude's Discretion — FULL_TIME shows ~3s before replay)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-RESEARCH.md (Pattern 3, Pitfall 4, Open Question 3 — FULL_TIME shown ~3s before frames)
  </read_first>
  <action>
    When a handler produces a state with `phase === 'FULL_TIME'` (end of second half), after broadcasting the FULL_TIME state: schedule the replay. Compute `const frames = buildReplayFrames(room.gameState);` and a `replayTotal = frames.length`. After a ~3 second delay (server-driven so the FULL_TIME screen shows first per Open Question 3 — use a setTimeout that stores nothing the client needs, or start the interval and let the first tick be the FULL_TIME hold; choose the setTimeout-then-setInterval approach), start `room.replayTimer = setInterval(() => { if (idx >= frames.length) { clearInterval(room.replayTimer!); room.replayTimer = null; return; } io.to(room.roomCode).emit(ServerEvents.GAME_STATE, { ...frames[idx], replayTotal, replayIndex: idx + 1 }); idx++; }, 1000)` (D-31, D-32; include the replay position metadata the UI-SPEC ReplayPanel reads — replayIndex/replayTotal carried on the frame, typed as the optional GameState fields added in 08-01). Ensure each frame has `phase: 'REPLAY'` (buildReplayFrames already tags it). In the existing `disconnect` handler, if the room is in REPLAY phase (or replayTimer is set), clear it: `if (room.replayTimer) { clearInterval(room.replayTimer); room.replayTimer = null; }` (Pitfall 4 / T-08-07). Create `replay.integration.test.ts` with a real Socket.io server + client: drive a short match to FULL_TIME (or seed a room with a small eventLog and trigger the FULL_TIME→replay path), collect emitted `game:state` events, and assert that REPLAY-phase frames arrive in sequence (use vitest fake timers or a short real interval) and that the timer is cleared after the last frame (no further emits). Assert at least one frame carries `phase === 'REPLAY'` and a `replayTotal` value.
  </action>
  <verify>
    <automated>cd packages/server && pnpm exec vitest run src/__tests__/replay.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `gameHandlers.ts` calls `buildReplayFrames` and assigns `room.replayTimer = setInterval(`
    - The interval callback calls `clearInterval` and sets `room.replayTimer = null` when frames are exhausted
    - The disconnect handler clears `room.replayTimer` when set
    - Emitted replay frames carry `phase: 'REPLAY'` and a `replayTotal` (asserted in integration test)
    - `cd packages/server && pnpm exec vitest run src/__tests__/replay.integration.test.ts` exits 0
  </acceptance_criteria>
  <done>After FULL_TIME the server streams replay frames once per second, includes replay position metadata, and cleans up the interval on exhaustion and on disconnect; covered by an integration test.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                                                                                                    |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client → server handlers            | All Phase 8 untrusted client input crosses here: game:ready, game:kick-off-move (pieceId + HexCoord), game:half-time-start, and all action events. This is the authoritative validation point. |
| server → clients (replay broadcast) | Replay frames are server-generated from the event log; no client input influences them.                                                                                                        |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                       | Disposition | Mitigation Plan                                                                                                                                                                             |
| --------- | ---------------------- | ------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-09   | Tampering              | game:kick-off-move claiming opponent pieces                                     | mitigate    | Handler validates `piece.teamId === socketTeam(socket)`; rejects 'NOT_YOUR_PIECE' (V4/V5). Test asserts opponent-piece move is rejected.                                                    |
| T-08-10   | Tampering              | game:ready with unmet placement constraints                                     | mitigate    | applyKickOffReady validates all placement rules server-side before recording ready (MATCH-03); handler snaps back on rejection.                                                             |
| T-08-11   | Tampering              | game:half-time-start by wrong team                                              | mitigate    | Handler rejects unless `socketTeam(socket) !== kickOffTeam` (only the non-kick-off team starts 2nd half).                                                                                   |
| T-08-12   | Tampering              | invalid action sequence injection                                               | mitigate    | Every action handler validates lastActionType against ELIGIBLE_NEXT_ACTIONS (D-07); rejects INVALID_SEQUENCE. Client button disabling is UX-only.                                           |
| T-08-13   | Tampering              | game:ready / actions during REPLAY phase                                        | mitigate    | Phase guards on all new handlers reject when `phase !== 'KICK_OFF_SETUP'` (or required phase); actions during REPLAY hit phase guards and are dropped.                                      |
| T-08-14   | Elevation of Privilege | missing isProcessing mutex enabling double-process                              | mitigate    | All new handlers check and set `room.isProcessing` in try/finally (established pattern); duplicate concurrent events dropped.                                                               |
| T-08-15   | Denial of Service      | replay timer not cleared on disconnect                                          | mitigate    | disconnect handler clears replayTimer; deleteRoom (08-03) also clears it (Pitfall 4).                                                                                                       |
| T-08-16   | Tampering              | illegal kick-off first action (e.g. High Pass / Long Ball / shot from kick-off) | mitigate    | While `kickOffActive`, the pass handler rejects any non-STANDARD pass and any non-centre origin with INVALID_KICK_OFF_PASS (D-27 / MATCH-03); flag clears only on a resolved Standard Pass. |
| T-08-SC   | Tampering              | npm/pip/cargo installs                                                          | accept      | No package installs in this plan or phase (RESEARCH.md §Package Legitimacy Audit: not applicable).                                                                                          |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` passes including kickoffSetup.integration.test.ts and replay.integration.test.ts.
- All new handlers use the isProcessing mutex and snap-back-on-error pattern.
- Server type-checks clean.
</verification>

<success_criteria>

- game:ready / game:kick-off-move / game:half-time-start handlers enforce placement, both-ready, and team guards with snap-back.
- The KICK_OFF → MOVEMENT transition sets kickOffActive; the first ball action is forced to a Standard Pass from the centre hex and the flag clears on resolution (MATCH-03 / D-27).
- Every action handler rejects ineligible sequences (INVALID_SEQUENCE).
- End-turn handler injects a crypto added-time roll.
- FULL_TIME streams replay frames at 1s intervals and cleans up on exhaustion/disconnect.
  </success_criteria>

<output>
Create `.planning/phases/08-match-lifecycle-post-game-replay/08-04-SUMMARY.md` when done.
</output>
