---
phase: 08-match-lifecycle-post-game-replay
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - packages/server/src/gameEngine.ts
  - packages/server/src/__tests__/gameEngine.phase8.test.ts
autonomous: true
requirements: [MATCH-01, MATCH-02, SNAP-01, SNAP-02, SNAP-03]
must_haves:
  truths:
    - 'Completing a 4-5-2 Movement Phase increments actionCount by 3 minutes'
    - 'First-time Pass, Shot, Header, Snapshot, Quick Throw, and Loose Ball cost 0 minutes; Standard/High/Long pass and GK kick cost 1 minute'
    - 'When actionCount first reaches 45, added time is rolled inline (injected die + referee leniency) and stored once, never re-rolled'
    - 'An accurate Standard Pass no longer transitions into SHOT; it sets lastActionType and returns to an action-choice state'
    - "applySnapshot rejects when the ball-carrier is not in the opponent's penalty area (mid-movement) and flags a -1 shooting penalty when valid"
  artifacts:
    - path: 'packages/server/src/gameEngine.ts'
      provides: 'applyEndTurn clock hook, lastActionType updates in applyRoll, applySnapshot'
      contains: 'applySnapshot'
    - path: 'packages/server/src/__tests__/gameEngine.phase8.test.ts'
      provides: 'Unit tests for clock, added time, sequence types, snapshot'
      contains: 'actionCount'
  key_links:
    - from: 'packages/server/src/gameEngine.ts'
      to: 'ELIGIBLE_NEXT_ACTIONS'
      via: 'import from @counter-attack/shared (sequence-aware lastActionType updates)'
      pattern: 'lastActionType'
---

<objective>
Add the match-clock and action-sequence state to the pure game engine: per-action time costs (D-03/D-04), inline added-time roll at 45 minutes (D-05), the half-time/full-time threshold branch inside `applyEndTurn`, `lastActionType` updates across `applyRoll` branches (D-12..D-21), the corrected PASS→action-choice restructure (D-09, Pitfall 8), and the new `applySnapshot` engine function (D-22, SNAP-01..03).

Purpose: All match-lifecycle behaviour is server-authoritative pure-function logic. Getting the clock, added-time guard, and sequence transitions correct and unit-tested here de-risks the highest-complexity area before handlers and UI consume it.
Output: Extended `gameEngine.ts` with clock hooks + `applySnapshot` + `lastActionType` updates; new `gameEngine.phase8.test.ts` covering MATCH-01, MATCH-02, SNAP-01..03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-RESEARCH.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: applyEndTurn clock hook + added-time roll + half/full-time branch</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.phase8.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (read applyEndTurn fully, plus buildInitialGameState and applyGKRestart for the injected-die pattern)
    - packages/server/src/__tests__/gameEngine.test.ts (fixture + describe/it patterns, base state objects)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md (D-03 time table, D-04, D-05)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-RESEARCH.md (Pattern 2, Pitfall 1, Pitfall 3, Pitfall 5)
  </read_first>
  <behavior>
    - applyEndTurn at the ATTACKER_2→null transition increments actionCount by 3 (MATCH-01)
    - When the post-increment actionCount >= 45 AND state.addedTime === null, addedTime is set to (injected addedTimeRoll + state.refereeCard.leniency); never re-rolled when addedTime is already non-null (MATCH-02, Pitfall 3)
    - When actionCount >= 45 + (addedTime ?? 0) and state.half === 1, the returned phase is 'HALF_TIME' (Pitfall 5)
    - When actionCount >= 45 + (addedTime ?? 0) and state.half === 2, the returned phase is 'FULL_TIME' (Pitfall 5)
    - When the threshold is not reached, behaviour is unchanged from current applyEndTurn (transitions to PASS/action-choice) with actionCount incremented and lastActionType set to 'MOVEMENT_PHASE'
    - buildInitialGameState returns addedTime: null, lastActionType: null, kickOffTeam: <coin-flip attackingTeam>
  </behavior>
  <action>
    Extend `buildInitialGameState` to include the three D-06 fields: `addedTime: null`, `lastActionType: null`, `kickOffTeam: attackingTeam` (the coin-flip winner already computed in that function). Then extend `applyEndTurn`: add an options parameter `options?: { addedTimeRoll?: number }` (mirroring the `rollDie` injection pattern of applyGKRestart — do NOT call `randomInt` inside applyEndTurn; Pitfall 1). At the ATTACKER_2 → null (MOVEMENT→PASS) transition where `nextSlot === null`: compute `newActionCount = state.actionCount + 3` (D-04); compute `newAddedTime = state.addedTime`; if `newActionCount >= 45 && state.addedTime === null` set `newAddedTime = (options?.addedTimeRoll ?? <default>) + state.refereeCard.leniency` (D-05, guard prevents re-roll per Pitfall 3); compute `halfEnd = 45 + (newAddedTime ?? 0)`; if `newActionCount >= halfEnd`, return state with `phase = state.half === 1 ? 'HALF_TIME' : 'FULL_TIME'` (Pitfall 5), carrying `actionCount: newActionCount`, `addedTime: newAddedTime`, and `lastActionType: 'MOVEMENT_PHASE'`; otherwise return the normal action-choice transition with `actionCount: newActionCount`, `addedTime: newAddedTime`, and `lastActionType: 'MOVEMENT_PHASE'`. Preserve the existing eventLog append behaviour and the Gap-1 invariants (movementSlot/movedPieceIds/paceUsedByPieceId reset). Create `gameEngine.phase8.test.ts` (analog: gameEngine.test.ts) with a `describe('applyEndTurn — Phase 8 clock')` group covering all six behaviors above using fixture states with `movementSlot: 'ATTACKER_2'` and varying actionCount (e.g. 41 → 44 no half-time; 44 → 47 triggers added time then continues; states forcing HALF_TIME vs FULL_TIME by half).
  </action>
  <verify>
    <automated>cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts -t "clock"</automated>
  </verify>
  <acceptance_criteria>
    - `gameEngine.ts` applyEndTurn signature includes an `addedTimeRoll` option (grep `addedTimeRoll`)
    - `gameEngine.ts` does NOT introduce a new `randomInt(` call inside applyEndTurn (added-time roll is injected)
    - `buildInitialGameState` returns object containing `addedTime: null`, `lastActionType: null`, `kickOffTeam`
    - Test asserts actionCount increments by 3 at ATTACKER_2 end; addedTime set once and not re-rolled; HALF_TIME for half 1 and FULL_TIME for half 2 at threshold
    - `cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts -t "clock"` exits 0
  </acceptance_criteria>
  <done>applyEndTurn applies the +3 clock, rolls added time once via injection, and branches to HALF_TIME/FULL_TIME by half; buildInitialGameState seeds the new fields; clock tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lastActionType updates + PASS→action-choice restructure across applyRoll branches</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.phase8.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (read applyRoll fully — PASS, SHOT, HEADER, LOOSE_BALL branches; applyGKRestart)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md (D-12 through D-21: per-action lastActionType values + time increments; D-09)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-RESEARCH.md (Pitfall 8 — PASS→SHOT must be removed)
  </read_first>
  <behavior>
    - Accurate STANDARD pass resolution sets lastActionType 'STANDARD_PASS', actionCount += 1, and returns to action-choice (phase 'PASS' as neutral ball-action state) — NOT phase 'SHOT' (Pitfall 8, D-12)
    - Accurate HIGH pass sets lastActionType 'HIGH_PASS', actionCount += 1, phase 'HEADER' (D-13: header is the only valid next)
    - Accurate LONG ball sets lastActionType 'LONG_BALL', actionCount += 1 (D-16)
    - Accurate FIRST_TIME pass sets lastActionType 'FIRST_TIME_PASS', actionCount += 0 (D-15)
    - Inaccurate pass routes to LOOSE_BALL and, when LOOSE_BALL landing resolves, sets lastActionType 'DEFLECTION' (D-20)
    - HEADER branch resolution sets lastActionType 'HEADER', actionCount += 0 (D-17)
    - SHOT branch: GOAL sets lastActionType null (D-19); MISS → MOVEMENT (next movement sets MOVEMENT_PHASE); time += 0
    - applyGKRestart: 'kick' += 1 min and lastActionType 'MOVEMENT_PHASE' on accurate / 'DEFLECTION' on spill; 'throw' += 0 and lastActionType 'STANDARD_PASS'; 'movement' += 0 and lastActionType null (D-21)
    - A successful STEAL_ATTEMPT in applyMove sets lastActionType 'SUCCESSFUL_TACKLE' and actionCount += 3 (D-14)
  </behavior>
  <action>
    Update `applyRoll` so every resolution branch sets the correct `lastActionType` and applies the correct per-action time increment per the D-12..D-21 table (values enumerated in the behavior block). CRITICAL (D-09, Pitfall 8): the accurate-pass branch must NOT transition to `phase: 'SHOT'`. For an accurate STANDARD pass, transition possession to the target hex and return to the neutral ball-action state (phase 'PASS' with the new carrier) setting `lastActionType: 'STANDARD_PASS'`; the SHOT phase becomes reachable only via the existing `game:shot` (from MOVEMENT) and via `applySnapshot`. Branch pass time costs per D-03 (STANDARD/HIGH/LONG +1, FIRST_TIME +0). In the LOOSE_BALL branch, set `lastActionType: 'DEFLECTION'` once landing is computed. In the HEADER branch set `lastActionType: 'HEADER'`, +0 min. In the SHOT branch set GOAL→`lastActionType: null`. Update `applyGKRestart` per D-21 ('kick' +1/MOVEMENT_PHASE or DEFLECTION; 'throw' +0/STANDARD_PASS; 'movement' +0/null). Update `applyMove`'s successful STEAL_ATTEMPT path to set `lastActionType: 'SUCCESSFUL_TACKLE'` and `actionCount += 3` (D-14 — the movement phase that contained the tackle counts in full). Add a `describe('applyRoll — Phase 8 lastActionType + time')` group to gameEngine.phase8.test.ts asserting each branch's lastActionType and actionCount delta, plus an explicit test that an accurate STANDARD pass does NOT yield phase 'SHOT'.
  </action>
  <verify>
    <automated>cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts -t "lastActionType"</automated>
  </verify>
  <acceptance_criteria>
    - `gameEngine.ts` applyRoll accurate-pass branch result phase is not 'SHOT' (asserted by a test that an accurate STANDARD pass produces phase !== 'SHOT')
    - Test asserts STANDARD_PASS → lastActionType 'STANDARD_PASS' & actionCount +1; HIGH → 'HIGH_PASS' & phase 'HEADER'; FIRST_TIME → +0; HEADER → 'HEADER' +0; SHOT GOAL → lastActionType null
    - Test asserts applyGKRestart sets lastActionType per choice ('kick'→MOVEMENT_PHASE accurate, 'throw'→STANDARD_PASS, 'movement'→null)
    - Test asserts a successful STEAL_ATTEMPT sets lastActionType 'SUCCESSFUL_TACKLE' and actionCount +3
    - `cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts -t "lastActionType"` exits 0
  </acceptance_criteria>
  <done>Every applyRoll branch, applyGKRestart choice, and the steal path set the correct lastActionType and time cost; the PASS→SHOT path is removed; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: applySnapshot engine function (SNAP-01, SNAP-02, SNAP-03)</name>
  <files>packages/server/src/gameEngine.ts, packages/server/src/__tests__/gameEngine.phase8.test.ts</files>
  <read_first>
    - packages/server/src/gameEngine.ts (applyGKRestart for result-type + pure-function pattern; existing SHOT branch in applyRoll for how the -1 penalty is applied)
    - packages/shared/src/pitch.ts (isInRegion, PITCH_REGIONS penalty-area keys)
    - packages/shared/src/actionSequence.ts (ELIGIBLE_NEXT_ACTIONS for the SNAPSHOT eligibility gate)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md (D-18, D-22; SNAP-01/02/03)
  </read_first>
  <behavior>
    - applySnapshot rejects with reason 'WRONG_PHASE' when phase is not MOVEMENT and not immediately-post-pass (the two SNAP-01 trigger conditions)
    - applySnapshot rejects with reason 'NOT_IN_PENALTY_AREA' when triggered during MOVEMENT but the ball-carrier is NOT in the opponent's penalty area (SNAP-01)
    - applySnapshot rejects with reason 'INVALID_SEQUENCE' when lastActionType is not in ELIGIBLE_NEXT_ACTIONS-permitted set for SNAPSHOT
    - On success, returns state with phase 'SHOT', lastActionType 'SNAPSHOT', and a flag/marker that the upcoming shot carries a -1 dice penalty (SNAP-02); actionCount unchanged (+0, D-18)
    - The opponent-deflection step (1 player up to 2 hexes) is represented as a server-resolvable pre-shot state per SNAP-02 — record that a deflection move is pending so the handler/UI can request it
  </behavior>
  <action>
    Add `ApplySnapshotResult` discriminated union (`{ ok: false; reason: 'WRONG_PHASE' | 'NOT_IN_PENALTY_AREA' | 'INVALID_SEQUENCE' } | { ok: true; state: GameState }`) and a pure `applySnapshot(state: GameState): ApplySnapshotResult`. Validate in order: (1) sequence guard — if `state.lastActionType !== null` and `!ELIGIBLE_NEXT_ACTIONS[state.lastActionType].has('SNAPSHOT')` return INVALID_SEQUENCE; (2) phase/position guard — valid when `phase === 'MOVEMENT'` AND the ball-carrier is in the opponent's penalty area (use `isInRegion(state.ball.position, state.attackingTeam === 'home' ? 'awayPenaltyArea' : 'homePenaltyArea')`), OR immediately after an accurate pass (lastActionType is a pass type) — otherwise return WRONG_PHASE or NOT_IN_PENALTY_AREA as appropriate. On success return `{ ok: true, state: { ...state, phase: 'SHOT', lastActionType: 'SNAPSHOT', <snapshot -1 penalty marker>, <pending deflection marker> } }` with actionCount unchanged (D-18). Apply the -1 penalty downstream by reading the snapshot marker in the SHOT branch of applyRoll (SNAP-02/SNAP-03 — all standard shot rules otherwise apply). Add `describe('applySnapshot — SNAP-01..03')` tests: rejects when not in penalty area mid-movement; rejects on invalid sequence; succeeds and sets phase 'SHOT' + lastActionType 'SNAPSHOT'; SHOT resolution from a snapshot applies the -1 penalty (assert combined-score reflects penalty, alongside existing outside-area -1 and auto-miss-on-1 rules).
  </action>
  <verify>
    <automated>cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts -t "applySnapshot"</automated>
  </verify>
  <acceptance_criteria>
    - `gameEngine.ts` exports `applySnapshot` and `ApplySnapshotResult`
    - `applySnapshot` returns `{ ok: false, reason: 'NOT_IN_PENALTY_AREA' }` when ball-carrier is outside opponent penalty area during MOVEMENT (asserted in test)
    - `applySnapshot` success result has `state.phase === 'SHOT'` and `state.lastActionType === 'SNAPSHOT'` (asserted)
    - A shot resolved after a snapshot applies a -1 shooting penalty (asserted via combined-score comparison)
    - `cd packages/server && pnpm exec vitest run src/__tests__/gameEngine.phase8.test.ts -t "applySnapshot"` exits 0
  </acceptance_criteria>
  <done>applySnapshot enforces SNAP-01 position/sequence rules, transitions to SHOT with the -1 penalty marker (SNAP-02/03), and is covered by passing tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                                                                                                        |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| handler → engine           | Pure functions receive already-validated, server-held state and injected dice. The boundary that matters (untrusted client input) is enforced in handlers (plan 08-04); engine functions assume validated callers. |
| crypto randomness → engine | Added-time and snapshot dice MUST be generated by the caller (crypto.randomInt) and injected; engine never generates randomness.                                                                                   |

## STRIDE Threat Register

| Threat ID | Category    | Component                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                              |
| --------- | ----------- | ------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-03   | Spoofing    | added-time roll source          | mitigate    | Roll injected as a parameter generated by the handler via existing server-side `rollDice` (crypto.randomInt). Engine never calls randomInt for added time (V6 Cryptography; Pitfall 1). Verified by acceptance criterion that no new randomInt call appears in applyEndTurn. |
| T-08-04   | Tampering   | snapshot -1 penalty bypass      | mitigate    | Penalty marker is set in applySnapshot (server pure function) and consumed in the SHOT branch server-side; the client cannot omit it.                                                                                                                                        |
| T-08-05   | Repudiation | sequence transitions not logged | accept      | lastActionType transitions are derived from the existing eventLog appends; no separate audit requirement for a 2-player POC.                                                                                                                                                 |
| T-08-SC   | Tampering   | npm/pip/cargo installs          | accept      | No package installs in this plan or phase (RESEARCH.md §Package Legitimacy Audit: not applicable).                                                                                                                                                                           |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/server test` passes including gameEngine.phase8.test.ts.
- No `randomInt(` call is added inside applyEndTurn (injection pattern preserved).
- Accurate Standard Pass no longer reaches phase 'SHOT'.
</verification>

<success_criteria>

- Movement Phase = +3 min; pass/GK-kick costs match D-03; added time rolled once at 45 and never re-rolled.
- HALF_TIME for half 1, FULL_TIME for half 2 at the threshold.
- lastActionType set correctly across all applyRoll branches, applyGKRestart, and the steal path.
- applySnapshot enforces SNAP-01..03 and is unit-tested.
  </success_criteria>

<output>
Create `.planning/phases/08-match-lifecycle-post-game-replay/08-02-SUMMARY.md` when done.
</output>
