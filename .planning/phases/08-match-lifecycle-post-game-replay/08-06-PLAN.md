---
phase: 08-match-lifecycle-post-game-replay
plan: 06
type: execute
wave: 6
depends_on: [08-05]
files_modified:
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/ActionPanel.tsx
  - packages/client/src/components/TurnIndicator.tsx
  - packages/client/src/components/GameBoard.tsx
  - packages/client/src/components/ReplayPanel.tsx
  - packages/client/src/components/ReplayPanel.module.css
autonomous: false
requirements: [MATCH-01, MATCH-02, MATCH-05, REPLAY-01, REPLAY-02, SNAP-01]
must_haves:
  truths:
    - "During KICK_OFF_SETUP the pitch tints the local team's valid placement zone and draws a gold ring on the centre hex"
    - 'During KICK_OFF_SETUP, clicking own piece then a valid zone hex emits game:kick-off-move; opponent pieces are not movable'
    - "The GameBoard header shows the match time in accent-gold, formatted as N' or 45+N' during added time"
    - 'ActionPanel shows a Snapshot button in SNAPSHOT phase and disables action buttons that are ineligible for the current lastActionType'
    - 'During REPLAY the sidebar shows ReplayPanel with the Action N of N counter and a Play Again button after the last frame'
  artifacts:
    - path: 'packages/client/src/components/ReplayPanel.tsx'
      provides: 'Replay sidebar panel (position counter + Play Again)'
      contains: 'Play Again'
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: 'Kick-off zone tint + centre-hex ring + kick-off-move click routing'
      contains: 'KICK_OFF_SETUP'
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'Snapshot button + eligibility-based disabling'
      contains: 'Snapshot'
  key_links:
    - from: 'packages/client/src/components/ActionPanel.tsx'
      to: 'ELIGIBLE_NEXT_ACTIONS'
      via: 'import from @counter-attack/shared to reflect lastActionType (UX disabling only)'
      pattern: 'ELIGIBLE_NEXT_ACTIONS'
---

<objective>
Complete the Phase 8 client by extending the in-board components: HexGrid kick-off zone tinting + centre-hex ring + kick-off-move click routing (MATCH-03 UX), the GameBoard header match-time display (MATCH-01/MATCH-02 UX, MATCH-05), ActionPanel Snapshot button + eligibility-based disabling (D-07/D-08 reflection, SNAP-01), and the ReplayPanel with Play Again (REPLAY-01/REPLAY-02). Then a human-verify checkpoint confirms the full match lifecycle end-to-end.

Purpose: This is the final integration layer that makes the lifecycle playable and observable. It depends on the store emitters/screens from 08-05.
Output: Extended HexGrid, ActionPanel, TurnIndicator, GameBoard; new ReplayPanel + CSS; passing client build; human-verified full-match run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-CONTEXT.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-UI-SPEC.md
@.planning/phases/08-match-lifecycle-post-game-replay/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HexGrid kick-off zone tint + centre-hex ring + kick-off-move click routing</name>
  <files>packages/client/src/components/HexGrid.tsx</files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (existing isHighlighted overlay pattern, HexCell rendering, click routing for MOVEMENT, single-SVG-root DOM-order z-layering)
    - packages/client/src/store/useGameStore.ts (selectPiece, validMoveHexes, emitKickOffMove from 08-05, playerSlot, attackingTeam)
    - packages/shared/src/pitch.ts (PITCH_REGIONS.kickOffHex {q:18,r:13}, centreCircle, isInRegion; half-boundary q=18)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-UI-SPEC.md (Hex Interaction States Phase 8 additions; Screen 1 zone tinting + click routing tables)
  </read_first>
  <action>
    Extend `HexGrid.tsx` to render three new SVG overlays ONLY when `phase === 'KICK_OFF_SETUP'`, each as an additional `<polygon>` after the base hex fill (DOM-order z-layer, existing isHighlighted pattern):
    1. Valid-zone tint for the LOCAL team's unoccupied valid hexes: home → fill `#1a56b0` opacity 0.25; away → fill `#c0392b` opacity 0.25. (Opponent zone gets no tint.) Determine "my valid zone" from playerSlot→team and the half rule (home q<=18, away q>=18); attacking team additionally includes centre-circle hexes, defending team excludes centre circle.
    2. Centre-hex required ring on {q:18,r:13}: a `<polygon>` with `fill: none; stroke: #f5c518; strokeWidth: 2` (always visible during setup, signals MATCH-03).
    3. Selected-piece valid-move highlight reuses the existing `validMoveHexes` highlight (gold fill 0.45 / stroke #d4a017 1.5) — same as MOVEMENT.
    Click routing during KICK_OFF_SETUP: clicking an own-team piece → `selectPiece(id)` (highlights valid zone hexes); clicking a valid zone hex while a piece is selected → `emitKickOffMove(pieceId, hex)`; clicking an opponent piece or any other hex → no-op (cannot move opponent pieces). Keep the existing MOVEMENT-phase routing untouched for non-setup phases. Introduce NO new colour tokens (all four overlay colours are in the frozen inventory).
  </action>
  <verify>
    <automated>cd packages/client && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `HexGrid.tsx` references `KICK_OFF_SETUP` and renders zone-tint overlays gated on that phase
    - Centre-hex ring polygon uses `stroke: #f5c518` / `strokeWidth` 2 and `fill: none`
    - KICK_OFF_SETUP click on a valid zone hex calls `emitKickOffMove`; opponent-piece clicks are no-ops
    - No new hex colour values introduced (only #1a56b0, #c0392b, #f5c518, #d4a017 — all frozen)
    - `cd packages/client && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>During KICK_OFF_SETUP the pitch tints the local valid zone, rings the centre hex in gold, and routes clicks to emitKickOffMove for own pieces only.</done>
</task>

<task type="auto">
  <name>Task 2: ActionPanel Snapshot button + eligibility disabling; GameBoard header time; TurnIndicator + KICK_OFF_SETUP panel swap</name>
  <files>packages/client/src/components/ActionPanel.tsx, packages/client/src/components/GameBoard.tsx, packages/client/src/components/TurnIndicator.tsx</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (existing buttons, isActivePlayer gate, emit pattern, disabled style, Roll button phase gating)
    - packages/client/src/components/GameBoard.tsx (header flex row: Title / Score / ConnectionStatus; sidebar panel slot)
    - packages/client/src/components/TurnIndicator.tsx (PHASE_LABEL map, score display, actionCount source)
    - packages/shared/src/actionSequence.ts (ELIGIBLE_NEXT_ACTIONS for button disabling)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-UI-SPEC.md (Action Button Eligibility section; Match Time Display Extension; Layout Spec header extensions; KICK_OFF_SETUP layout — TurnIndicator PHASE_LABEL)
  </read_first>
  <action>
    ActionPanel: add a "Snapshot" button visible when `phase === 'SNAPSHOT'` and active player (emits `game:snapshot` — add the emitter/event if not already present; if the event is not yet wired, emit via the store's snapshot emitter, otherwise add `emitSnapshot` calling `socket.emit('game:snapshot')`). Add a "Header" button visible when `phase === 'HEADER'` and active player (emits `game:header` similarly). Add eligibility-based DISABLING (not hiding): read `lastActionType` from the store; using `ELIGIBLE_NEXT_ACTIONS[lastActionType]` (import from @counter-attack/shared), disable any action button whose NextActionType is not in the eligible set when `lastActionType !== null` — this is UX reflection ONLY; the server independently validates (do NOT skip server emits). Apply the existing disabled style (`opacity: 0.5; cursor: default`) via the `disabled` attribute and set `title` per the UI-SPEC disabled-tooltip table (e.g. Start Movement → "Not available after a High Pass — must head the ball"). Keep all Phase 7 buttons as-is (additive).
    GameBoard header: insert a `<span className={styles.headerTime}>` between Score and ConnectionStatus showing match time in `#f5c518` at 13px/700: format `{actionCount}'` normally, and `45+{actionCount - 45}'` when `actionCount > 45 && addedTime !== null` (per UI-SPEC formula). Gap 16px (md). Only render during the GAME_BOARD board view (not during KICK_OFF_SETUP/HALF_TIME/FULL_TIME/REPLAY screens — though KICK_OFF_SETUP uses the board, the time is irrelevant there per UI-SPEC; hide it unless phase is a play phase). Swap the sidebar panel: render `KickOffSetupPanel` (from 08-05) when `phase === 'KICK_OFF_SETUP'`, `ReplayPanel` (Task 3) when `phase === 'REPLAY'`, else the existing ActionPanel.
    TurnIndicator: add `'KICK_OFF_SETUP': 'KICK OFF SETUP'` to the PHASE_LABEL map; add the actionCount/addedTime match-time format helper if TurnIndicator is the chosen home for it (UI-SPEC lists TurnIndicator EXTENDED for match time — keep the header `.headerTime` as the canonical display and ensure no duplicate/conflicting time render).
  </action>
  <verify>
    <automated>cd packages/client && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ActionPanel.tsx` imports `ELIGIBLE_NEXT_ACTIONS` and disables ineligible action buttons based on `lastActionType`
    - `ActionPanel.tsx` renders a "Snapshot" button gated on `phase === 'SNAPSHOT'`
    - `GameBoard.tsx` renders a `.headerTime` span; format is `45+N'` when actionCount > 45 and addedTime is set, else `N'`
    - `GameBoard.tsx` swaps the sidebar to KickOffSetupPanel during KICK_OFF_SETUP and ReplayPanel during REPLAY
    - `TurnIndicator.tsx` PHASE_LABEL contains `KICK_OFF_SETUP`
    - `cd packages/client && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ActionPanel reflects sequence eligibility and exposes Snapshot/Header buttons; the header shows match time with added-time formatting; the sidebar swaps panels by phase; TurnIndicator labels KICK_OFF_SETUP.</done>
</task>

<task type="auto">
  <name>Task 3: ReplayPanel component</name>
  <files>packages/client/src/components/ReplayPanel.tsx, packages/client/src/components/ReplayPanel.module.css</files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (.panel container analog, ctaButton)
    - packages/client/src/components/TurnIndicator.tsx (score row layout with team colours)
    - packages/client/src/store/useGameStore.ts (gameState.score, setScreen, and the replayIndex/replayTotal fields carried on replay frames from 08-04)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-UI-SPEC.md (Screen 4 REPLAY — ReplayPanel contents, position counter, Play Again, copy)
    - .planning/phases/08-match-lifecycle-post-game-replay/08-PATTERNS.md (ReplayPanel section)
  </read_first>
  <action>
    Create `ReplayPanel.tsx`. Selectors: `gameState.phase`, `gameState.score`, `setScreen`, and the replay position metadata (`replayIndex` / `replayTotal` carried on the broadcast frames per 08-04; read defensively — if `replayTotal` is undefined, render "Action {N}" without "of N"). Return null unless `phase === 'REPLAY'`. Render (`.panel` style copied from ActionPanel.module.css): heading "Replay" (13px/700/#e0e0e0); final score `{home} – {away}` (13px/700) with team-coloured labels (#1a56b0/#c0392b); position counter "Action {current} of {total}" (13px/400/#f5c518); running indicator "Playing…" (13px/400/#a0a0a0) while not at the last frame, switching to "Replay complete." when `replayIndex >= replayTotal`; a "Play Again" `.ctaButton` shown ONLY when replay is complete — onClick `setScreen('CREATE_ROOM')` (no socket emit; server cleans up via disconnect per D-33). No pause/scrub/step controls. Create `ReplayPanel.module.css` copying `.panel`/`.ctaButton` token values from ActionPanel.module.css; no new tokens.
  </action>
  <verify>
    <automated>cd packages/client && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `ReplayPanel.tsx` returns null when `phase !== 'REPLAY'`
    - Contains "Replay", an "Action {…} of {…}" counter using accent-gold #f5c518, and a "Play Again" button shown only when complete
    - Play Again onClick calls `setScreen('CREATE_ROOM')` with no socket emit
    - `ReplayPanel.module.css` uses only frozen tokens (#16213e, #0f3460, #e0e0e0, #a0a0a0, #f5c518, #1a56b0, #c0392b)
    - `cd packages/client && pnpm build` exits 0
  </acceptance_criteria>
  <done>ReplayPanel shows the persistent final score, the live Action N of N counter, the running/complete indicator, and a Play Again button that returns to the lobby.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Full match lifecycle human verification</name>
  <files>packages/client/src/components/GameBoard.tsx (and all Phase 8 client/server files — verification only, no edits)</files>
  <what-built>
    The complete Phase 8 match lifecycle: KICK_OFF_SETUP placement + Ready flow, the minute clock + added time, action-sequence enforcement, half-time hand-off, full-time result, and the auto-playing post-game replay — wired end-to-end across two browser tabs.
  </what-built>
  <action>
    This is a human-verification checkpoint — no code changes. Build and run the full stack, then walk a human through the verification steps below across two browser tabs to confirm the full match lifecycle behaves correctly. Pause for the resume signal before marking the plan complete.
  </action>
  <how-to-verify>
    1. From the repo root run `pnpm --recursive build` then start the server (`pnpm --filter @counter-attack/server dev`) and client (`pnpm --filter @counter-attack/client dev`).
    2. Open two browser tabs at the client dev URL. Create a room in tab 1, join with the code in tab 2.
    3. KICK_OFF_SETUP: confirm both tabs show the pitch with their own valid zone tinted (blue for home, red for away) and a gold ring on the centre hex {q:18,r:13}. Try clicking Ready in the attacking tab WITHOUT a piece on the centre hex — confirm the Ready button is disabled with the centre-hex tooltip, and the KickOffSetupPanel shows "Centre hex: EMPTY — required" in red. Place a player on the centre hex; confirm Ready enables. Click Ready in both tabs; confirm transition to KICK_OFF then play.
    4. Match clock: play a few Movement Phases; confirm the header time (accent-gold) increases by 3 each completed movement and pass actions add 1. Verify the action-sequence rules visually (e.g. after a High Pass only the Header path is available; Shot is unavailable directly after a Standard Pass — ineligible buttons are disabled with tooltips).
    5. (To reach half-time quickly you may temporarily lower the 45 threshold in gameEngine for testing, then revert.) Confirm at the threshold the HALF_TIME screen appears in both tabs with score + "Start 2nd Half" enabled only for the non-first-half kick-off team; clicking it starts the 2nd half with a fresh KICK_OFF_SETUP and the clock reset to 0.
    6. Reach full time: confirm the FULL_TIME screen shows the final score and the correct result line (Home wins / Away wins / Draw) and "Replay starting…", then after ~3s the REPLAY screen appears automatically.
    7. Replay: confirm the board re-renders frames roughly one per second, the sidebar shows "Action N of N" advancing, and when it finishes "Replay complete." appears with a "Play Again" button that returns both tabs to the create/join lobby.
  </how-to-verify>
  <verify>
    <automated>pnpm --recursive build</automated>
  </verify>
  <resume-signal>Type "approved" if the full lifecycle works, or describe any issues (e.g. clock miscount, sequence not enforced, replay not advancing, wrong half-time team).</resume-signal>
  <done>A human has run the full stack across two tabs and confirmed the KICK_OFF_SETUP → clock/added-time → sequence enforcement → half-time → full-time → replay → Play Again lifecycle works, or logged specific issues to address.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                                                                                |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client UI → server | ActionPanel/HexGrid emits (kick-off-move, snapshot, header, roll) are all re-validated server-side (08-04). Button disabling and zone tinting are UX-only. |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                        | Disposition | Mitigation Plan                                                                                                                                                                                   |
| --------- | ---------------------- | ---------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-18   | Tampering              | client could emit an ineligible action despite a disabled button | mitigate    | ELIGIBLE_NEXT_ACTIONS is the same constant the server enforces (08-04); a bypassed disabled button results in server INVALID_SEQUENCE rejection + snap-back. Client disabling is reflection only. |
| T-08-19   | Tampering              | client clicks opponent pieces during KICK_OFF_SETUP              | mitigate    | HexGrid routes only own-team piece clicks to emitKickOffMove; the server (08-04) additionally rejects NOT_YOUR_PIECE.                                                                             |
| T-08-20   | Information Disclosure | replay frames expose full board history                          | accept      | Event log / board state already fully broadcast per ARCH-04; replay shows only already-visible information.                                                                                       |
| T-08-SC   | Tampering              | npm/pip/cargo installs                                           | accept      | No package installs in this plan or phase (RESEARCH.md §Package Legitimacy Audit: not applicable).                                                                                                |

</threat_model>

<verification>
- `pnpm --filter @counter-attack/client build` succeeds.
- HexGrid kick-off overlays + click routing, ActionPanel eligibility disabling + Snapshot/Header buttons, header match-time, ReplayPanel all present.
- Human checkpoint confirms the full match lifecycle end-to-end.
</verification>

<success_criteria>

- KICK_OFF_SETUP zone tinting + centre-hex ring + kick-off-move routing work.
- Header shows match time with added-time formatting.
- ActionPanel reflects sequence eligibility and exposes Snapshot/Header.
- ReplayPanel shows the position counter and Play Again.
- Full lifecycle verified by a human across two tabs.
  </success_criteria>

<output>
Create `.planning/phases/08-match-lifecycle-post-game-replay/08-06-SUMMARY.md` when done.
</output>
