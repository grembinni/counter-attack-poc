---
phase: 27-response-activation-model
plan: 12
type: execute
wave: 8
depends_on: ['27-11']
files_modified: []
autonomous: true
requirements: [RESP-01, RESP-02, RESP-03, RESP-04, RESP-05, RESP-06, RESP-07, RESP-08, RESP-09]
must_haves:
  truths:
    - 'The full automated test suite passes with zero regressions across all 11 prior Phase 27 plans'
    - "Every response type's live behavior is confirmed against its CONTEXT.md decision and recorded for the phase's UAT batch (workflow.human_verify_mode=end-of-phase — no mid-flight halt)"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 27 with a full-suite automated regression run plus end-to-end human
verification steps for every response type this phase touched, folded into
`<verify><human-check>` blocks per `workflow.human_verify_mode=end-of-phase` (no mid-flight
checkpoint halts — the verifier harvests these into the phase's UAT batch at end-of-phase).

Purpose: RESP-02/RESP-09's visual requirements and the multi-mechanic sequencing built across
Plans 27-08/27-09 have no single automated assertion that proves the WHOLE flow is coherent
end-to-end — only a live playthrough does, and per-plan test suites (each scoped to its own
files) cannot see cross-plan integration issues (e.g. a ball crossing that should trigger
keeper ball-in-box but instead falls through to FINAL_THIRD_RESPONSE).
Output: phase sign-off, harvested UAT items.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-response-activation-model/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated regression</name>
  <files></files>
  <action>
    Run the complete automated suite across all three packages and confirm zero failures and
    zero new TypeScript errors introduced by any of Plans 27-01 through 27-11.
  </action>
  <verify>
    <automated>pnpm -r typecheck && pnpm -r test && pnpm -r build</automated>
  </verify>
  <done>pnpm -r typecheck, pnpm -r test, and pnpm -r build all exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Live verification — positioning-turn response types (HEADER, DEFLECT, FINAL THIRD)</name>
  <files></files>
  <action>
    Start a local 2-browser match (`pnpm dev` at the repo root, open two tabs, join the same
    room code) and drive it to each of HEADER, DEFLECT, and FINAL THIRD at least once.
  </action>
  <verify>
    <automated>pnpm -r test</automated>
    <human-check>
      <test>
        1. HEADER: after a High Pass lands with eligible players on both sides, position the
           attacking team's contestant first (white hexes, badge at distance 2 from ball),
           confirm hand-off to the defending team is automatic (no confirm click), position
           the defender, confirm the existing duel/target flow proceeds unchanged.
        2. DEFLECT: force a Snapshot shot; confirm only defenders near the shot path render
           as selectable, the defender's single move auto-resolves the deflect roll with no
           End Turn click, and movement still feels capped at 2 hexes.
        3. FINAL THIRD: advance the ball into a final third with pieces from both teams
           present; confirm the attacking team can move any number of eligible pieces within
           their OWN per-piece movement range (not a fixed ring around the ball), End Turn
           hands off to the defending team, and the action panel notes the keeper
           repositioned.
      </test>
      <expected>All three flows match CONTEXT.md D-02, D-03, D-04, D-08, D-10, D-12, D-13,
      D-18, D-20, D-21 as described above — no dead-end phases, no double-broadcast glitches,
      no stale highlight bleed between phases.</expected>
      <why_human>Multi-step, cross-plan real-time sequencing (server hand-off + client
      highlight timing) that per-plan unit/integration tests exercise in isolation but cannot
      prove coherent end-to-end in a live two-client session.</why_human>
    </human-check>
  </verify>
  <done>All three positioning-turn response types confirmed live, matching their CONTEXT.md decisions; any mismatch is recorded for phase UAT follow-up.</done>
</task>

<task type="auto">
  <name>Task 3: Live verification — implicit-single-piece types, ball ring, and auto-skip</name>
  <files></files>
  <action>
    Continue the match to exercise DIVE, keeper ball-in-box, the ball-hex ring, and (if
    reachable) the RESP-08 auto-skip path.
  </action>
  <verify>
    <automated>pnpm -r test</automated>
    <human-check>
      <test>
        1. DIVE: force a regular shot to GK_DIVE; confirm white hexes with a -1 badge only at
           the outer (3-hex) ring — visual-only check, mechanics unchanged.
        2. Keeper ball-in-box: get the ball into a penalty area via a route OTHER than a
           LOOSE_BALL landing (e.g. a completed pass); confirm the keeper's team sees up to 7
           highlighted hexes and one click resumes play immediately.
        3. Ball-hex ring: during at least 2 different response phases, confirm a gold RING
           (stroke only, not a filled overlay) is visible on the ball's hex, layered under
           the ball marker.
        4. Auto-skip (best-effort, only if reachable this session): engineer a state with
           zero eligible players for DEFLECT or HEADER (e.g. all defenders far from the shot
           path); confirm the phase is skipped without hanging and the skip is visible in the
           ActionLog.
      </test>
      <expected>All checks match CONTEXT.md D-05, D-06, D-07, D-08, D-09, D-11, D-14, D-19,
      RESP-08 as described above.</expected>
      <why_human>Visual layering (ring under marker, badge placement) and rare-state
      reachability are not practically assertable by the existing automated test harness.</why_human>
    </human-check>
  </verify>
  <done>DIVE, keeper ball-in-box, the ball-hex ring, and (if reachable) auto-skip are confirmed live; any mismatch is recorded for phase UAT follow-up.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                      |
| --------------- | -------------------------------- |
| N/A (this plan) | Verification only — no new code. |

## STRIDE Threat Register

| Threat ID | Category  | Component    | Disposition | Mitigation Plan                   |
| --------- | --------- | ------------ | ----------- | --------------------------------- |
| T-27-SC   | Tampering | npm installs | accept      | No package installs in this plan. |

</threat_model>

<verification>
- `pnpm -r typecheck && pnpm -r test && pnpm -r build` exits 0.
- Both `<human-check>` blocks harvested into the phase's UAT batch at end-of-phase.
</verification>

<success_criteria>
All 9 RESP requirements are demonstrably true in a live match, not just in isolated unit
tests. Phase 27 is ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/27-response-activation-model/27-12-SUMMARY.md` when done.
</output>
