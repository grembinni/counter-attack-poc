---
phase: 27-response-activation-model
plan: 10
type: execute
wave: 6
depends_on: ['27-06', '27-08']
files_modified:
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/ActionPanel.tsx
autonomous: true
requirements: [RESP-01, RESP-02, RESP-05, RESP-06]
must_haves:
  truths:
    - 'During FINAL_THIRD_RESPONSE, selecting an eligible piece (either team, currently active sub-phase) shows white response hexes around THAT piece (its own per-piece movement range), not a fixed ring around the ball'
    - 'The active side can move any number of their eligible pieces, End Turn when done, and Undo a move made this response phase'
    - "The action panel notes that the keeper was auto-repositioned when that side's turn begins (D-18)"
  artifacts:
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'selectPiece FINAL_THIRD_RESPONSE branch (per-piece range, MOVEMENT-style) + sticky-selection support'
      contains: 'FINAL_THIRD_RESPONSE'
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'FINAL_THIRD_RESPONSE panel: eligible-count helper text, keeper-repositioned note, End Turn button'
      contains: 'FINAL_THIRD_RESPONSE'
  key_links:
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'packages/client/src/store/useGameStore.ts emitMove / emitEndTurn'
      via: 'generic isValidMove && selectedPieceId onClick fallback + existing End Turn button wiring'
      pattern: 'FINAL_THIRD_RESPONSE'
---

<objective>
Wire the client side of Plan 27-07/27-08's FINAL_THIRD_RESPONSE engine: per-piece selection
and movement range (D-13's "own movement range, not a fixed ring around the ball"), attacker-
then-defender turn UI (D-21), and the D-18 keeper-repositioned helper text.

Purpose: complete RESP-05/RESP-06's visible half.
Output: playable FINAL_THIRD_RESPONSE UI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-response-activation-model/27-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useGameStore.ts — FINAL_THIRD_RESPONSE per-piece selection</name>
  <files>
    packages/client/src/store/useGameStore.ts
  </files>
  <read_first>
    - packages/client/src/store/useGameStore.ts (lines 531-562 — the `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` `selectPiece` branch, the direct structural template for per-piece-range MOVEMENT-style selection with a `finalThirdMovementSlot`-keyed eligible list instead of a `phase`-keyed one; lines 808-835 — the matching `setGameState` sticky-selection block)
  </read_first>
  <behavior>
    - Selecting an eligible piece (`finalThirdEligibleIds[side].includes(id)` where `side` derives from `finalThirdMovementSlot`, piece belongs to `myTeam`, not already in `movedPieceIds`): `validMoveHexes` becomes adjacent, on-pitch, unoccupied hexes within the piece's own `pace` attribute minus `finalThirdUsedPace[id]` — a per-piece range around the SELECTED PIECE, never a fixed ring around the ball (D-13).
    - Selecting a piece not in the current sub-phase's eligible list, or belonging to the inactive side: rejected.
    - `setGameState` clears selection on `finalThirdMovementSlot` change (attacker→defender hand-off) — extend the existing `responseMoveStateChanged` boolean.
  </behavior>
  <action>
    Add a `selectPiece` branch: `if (gameState.phase === 'FINAL_THIRD_RESPONSE') { ... }` mirroring the `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` block's shape exactly, but keying eligibility off `gameState.finalThirdEligibleIds?.[gameState.finalThirdMovementSlot === 'ATTACKER' ? 'attack' : 'defense']` and pace off `piece.pace - (gameState.finalThirdUsedPace?.[id] ?? 0)` instead of a flat `6`. Add the matching `setGameState` sticky-selection block (mirrors 808-835, same pace-remaining derivation) and extend `responseMoveStateChanged` to include `newState.finalThirdMovementSlot !== prevState.finalThirdMovementSlot`.
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- useGameStore</automated>
  </verify>
  <done>Client selection/highlight range for FINAL_THIRD_RESPONSE matches D-13's per-piece-range model exactly, for both sub-phases and both teams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HexGrid.tsx — 'response' classification for FINAL_THIRD_RESPONSE</name>
  <files>
    packages/client/src/components/HexGrid.tsx
  </files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (the `isResponseMoveTint` derivation extended in Plans 27-03/27-06 — this task adds the third contributor)
  </read_first>
  <behavior>
    - During `FINAL_THIRD_RESPONSE` with a piece selected, its valid destination hexes render `highlightType='response'` with no penalty badge (D-08: FINAL_THIRD has no penalty tiering).
  </behavior>
  <action>
    Extend `isResponseMoveTint` to also include `(phase === 'FINAL_THIRD_RESPONSE' && isValidMove)`. No penalty flag contribution needed from this phase (leave `penalty` computation from Plans 27-03/27-06 as-is — FINAL_THIRD_RESPONSE hexes simply never set either of the existing penalty flags, so `penalty` naturally evaluates falsy for them).
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- HexGrid</automated>
  </verify>
  <done>FINAL_THIRD_RESPONSE hexes render white, never badged.</done>
</task>

<task type="auto">
  <name>Task 3: ActionPanel.tsx — FINAL_THIRD_RESPONSE panel (D-04/D-18/D-21)</name>
  <files>
    packages/client/src/components/ActionPanel.tsx
  </files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (the `SNAPSHOT_DEFLECT`/`FIRST_TIME_PASS_MOVE` panel blocks — direct templates for an eligible-count-driven helper line + End Turn button; confirm `withEndTurnConfirm`'s eligible-remaining-count signature)
  </read_first>
  <action>
    Add an `if (phase === 'FINAL_THIRD_RESPONSE')` block. If `myTeam` is not the currently-active side (`finalThirdMovementSlot === 'ATTACKER' ? attackingTeam : <opposing team>`), return `waitingPanel`. Otherwise render a helper block with two lines: "Final Third Response" / a short note combining D-04's framing ("Move your eligible players, then End Turn.") with D-18's keeper note — exact wording is Claude's Discretion per CONTEXT.md, keep it short and consistent with existing helper-text style, e.g. "Your keeper has repositioned to their starting position and is not available to move." Render an End Turn button wired to `emitEndTurn` (same `withEndTurnConfirm` pattern as `SNAPSHOT_DEFLECT`/`FIRST_TIME_PASS_MOVE`, eligible-remaining derived from how many of `finalThirdEligibleIds[side]` are NOT yet in `movedPieceIds`). No special "no eligible players" case is needed here — Plan 27-08 only enters this phase when the list is non-empty.
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- ActionPanel</automated>
  </verify>
  <done>FINAL_THIRD_RESPONSE panel shows the correct active-side prompt, keeper-repositioned note, and a working End Turn button; the inactive side sees a waiting panel.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                               |
| --------------- | ----------------------------------------------------------------------------------------- |
| N/A (this plan) | Client-side UX only; all authoritative validation lives server-side in Plans 27-07/27-08. |

## STRIDE Threat Register

| Threat ID | Category  | Component    | Disposition | Mitigation Plan                   |
| --------- | --------- | ------------ | ----------- | --------------------------------- |
| T-27-SC   | Tampering | npm installs | accept      | No package installs in this plan. |

</threat_model>

<verification>
- `cd packages/client && pnpm test -- useGameStore HexGrid ActionPanel` exits 0.
- `pnpm -r typecheck` exits 0.
- Manual: force the ball into a final third with pieces from both teams present; confirm attacker moves first with per-piece ranges, End Turn hands off to defender, defender's End Turn resumes normal play, keeper note is visible (covered by the Plan 27-12 end-of-phase checkpoint).
</verification>

<success_criteria>
FINAL_THIRD_RESPONSE is fully playable end-to-end with the correct per-piece range model
(D-13), sequential turn order (D-21), and keeper-repositioned messaging (D-18).
</success_criteria>

<output>
Create `.planning/phases/27-response-activation-model/27-10-SUMMARY.md` when done.
</output>
