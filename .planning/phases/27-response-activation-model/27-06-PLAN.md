---
phase: 27-response-activation-model
plan: 06
type: execute
wave: 4
depends_on: ['27-03', '27-05']
files_modified:
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/ActionPanel.tsx
autonomous: true
requirements: [RESP-01, RESP-02, RESP-04]
must_haves:
  truths:
    - "During my team's header-positioning turn, only pieces within hex-distance 2 of the ball are selectable, and selecting one shows white response hexes (with a -1 badge at distance 2 from the ball) as click targets"
    - "Clicking a valid hex during header positioning moves the piece and the panel updates to show the other side's turn (or the post-duel screen) with no extra click"
    - 'The pre-existing multi-select contestant checkboxes/Decline button no longer appear during the new positioning pre-step'
  artifacts:
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'selectPiece HEADER-positioning branch + setGameState sticky-selection support for the new sub-state'
      contains: 'headerPositioningTurn'
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: "HEADER positioning hexes render as 'response' with a badge at distance 2 from ball"
      contains: 'headerPositioningTurn'
    - path: 'packages/client/src/components/ActionPanel.tsx'
      provides: 'New single-select positioning prompt ahead of the untouched post-duel target-selection block'
  key_links:
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'packages/client/src/store/useGameStore.ts emitMove'
      via: 'generic isValidMove && selectedPieceId onClick fallback (unchanged, no new click branch needed)'
      pattern: "emitMove\\(selectedPieceId"
---

<objective>
Wire the client side of Plan 27-05's server engine: selection eligibility, highlight
classification, and the ActionPanel prompt for HEADER's new sequential positioning turn.
Per research's HexGrid finding, the generic `isValidMove && selectedPieceId → emitMove`
click fallback (`HexGrid.tsx:528`) already handles the click-to-move mechanic for any phase
whose `validMoveHexes` the store populates correctly — no new onClick branch is needed here,
only correct `validMoveHexes` population (useGameStore) and correct highlight classification
(HexGrid ternary).

Purpose: complete RESP-01/RESP-02/RESP-04 for HEADER — the visible half of Plan 27-05's
server-side work.
Output: playable HEADER positioning turn UI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-response-activation-model/27-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useGameStore.ts — selectPiece + sticky-selection for HEADER positioning</name>
  <files>
    packages/client/src/store/useGameStore.ts
  </files>
  <read_first>
    - packages/client/src/store/useGameStore.ts (lines 200-253 — `ResponseMoveValidHexConfig`/`computeResponseMoveValidHexes`, widen the `lockedPieceIdField`/`paceUsedField` unions to include `'headerPositionMovedPieceId'`/`'headerPositionPaceUsed'`, mirroring Plan 27-05's server-side union widening; lines 622-648 — the SNAPSHOT_DEFLECT `selectPiece` branch as the direct structural template for a `clickDistanceMode: 'range'` phase; lines 736-806 — `setGameState`'s sticky-selection blocks, specifically the SNAPSHOT_DEFLECT block at 787-806 as the template)
  </read_first>
  <behavior>
    - Selecting a piece when `phase === 'HEADER'`, `headerPositioningTurn === 'attacker'`, my team is the attacking team, `!headerAccuracyRollPending`, and the piece is within hex-distance 2 of `ball.position`: `validMoveHexes` becomes the intersection of (a) `computeResponseMoveValidHexes` with `paceCap: 2, clickDistanceMode: 'range'` and (b) `hexDistance(hex, ball.position) <= 2` (D-10 destination range).
    - Selecting a piece outside distance 2 of the ball, or belonging to the wrong team, or during the wrong side's turn, or while `headerAccuracyRollPending`: `validMoveHexes` stays empty / selection is rejected.
    - After a server broadcast where `headerPositioningTurn` has advanced to the other side (or become `null`), any stale local selection is cleared (sticky-selection logic must treat a `headerPositioningTurn` change as a state-changed event, same as `movementSlot`/`highPassMovementSlot` changes already are).
  </behavior>
  <action>
    Widen `ResponseMoveValidHexConfig`'s `lockedPieceIdField`/`paceUsedField` unions to include `'headerPositionMovedPieceId'`/`'headerPositionPaceUsed'`. Add a new `selectPiece` branch: `if (gameState.phase === 'HEADER' && gameState.headerPositioningTurn !== null && !(gameState.headerAccuracyRollPending ?? false)) { ... }` placed before the existing generic MOVEMENT fallthrough at the bottom of `selectPiece`. Compute `actingTeam` from `headerPositioningTurn`/`attackingTeam` (mirrors Plan 27-05 Task 2's server derivation). Reject selection (empty `validMoveHexes`) when `piece.teamId !== myTeam`, `myTeam !== actingTeam`, `hexDistance(piece.position, gameState.ball.position) > 2`, or the piece is locked out by `headerPositionMovedPieceId`. Otherwise call `computeResponseMoveValidHexes(id, piece, gameState, { lockedPieceIdField: 'headerPositionMovedPieceId', paceUsedField: 'headerPositionPaceUsed', paceCap: 2, clickDistanceMode: 'range' })` and filter the result to `hexDistance(hex, gameState.ball.position) <= 2`.

    Extend `setGameState`'s `responseMoveStateChanged` boolean (~line 674-678) to also compare `newState.headerPositioningTurn !== prevState.headerPositioningTurn`, and add a sticky-selection branch (mirrors the SNAPSHOT_DEFLECT block at 787-806) recomputing the same filtered valid-hex set for the still-selected piece when `headerPositioningTurn` is unchanged across a broadcast.

  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- useGameStore</automated>
  </verify>
  <done>Client-side eligibility and destination filtering for HEADER positioning matches the server's D-10 rules; stale selections clear correctly on turn hand-off.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HexGrid.tsx — 'response' classification + -1 badge for HEADER positioning</name>
  <files>
    packages/client/src/components/HexGrid.tsx
  </files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (lines 480-496 — the priority ternary post-Plan-27-03, specifically where `isResponseMoveTint` was introduced; lines 168-177 — the existing `headerTargetStep` derivation, to make sure the NEW positioning-turn highlight and the EXISTING post-duel `header-target`/`isHeaderNonGoalTarget` highlight never both apply to `phase === 'HEADER'` at the same time — they are gated on disjoint sub-states (`headerPositioningTurn !== null` vs `headerDuelWinner === myTeam`))
  </read_first>
  <behavior>
    - During HEADER positioning (`headerPositioningTurn !== null`, my team's turn, `isValidMove` true for a hex), that hex renders `highlightType='response'`.
    - A HEADER-positioning hex at hex-distance exactly 2 from `ball.position` additionally renders `penalty={true}`; distance 1 renders `penalty={false}` (D-08).
    - The existing post-duel `header-target`/`isHeaderNonGoalTarget` highlight is completely unaffected — it only activates when `headerDuelWinner === myTeam`, which cannot be true while `headerPositioningTurn !== null` (mutually exclusive sub-states).
  </behavior>
  <action>
    Extend `isResponseMoveTint` (from Plan 27-03 Task 2) to also include `(phase === 'HEADER' && headerPositioningTurn !== null && isValidMove)`. Compute `isHeaderPositionPenalty = phase === 'HEADER' && headerPositioningTurn !== null && hexDistance(hex, ball.position) === 2`. Extend the `penalty` prop computation from Plan 27-03 to `penalty={isResponseMoveTint ? (isGKDivePenalty || isHeaderPositionPenalty) : undefined}` (both flags are mutually exclusive by phase, so `||` is safe — never both true for the same hex).
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- HexGrid</automated>
  </verify>
  <done>HEADER positioning hexes render white with the correct D-08 penalty badge; no collision with the pre-existing post-duel target highlight.</done>
</task>

<task type="auto">
  <name>Task 3: ActionPanel.tsx — new positioning prompt ahead of the untouched post-duel block</name>
  <files>
    packages/client/src/components/ActionPanel.tsx
  </files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (lines 405-479 — the full existing HEADER block: `headerAccuracyRollPending` waiting panel, `bothConfirmed` post-duel target-selection panel, and the multi-select contestant UI to be replaced)
  </read_first>
  <action>
    Inside the existing `if (phase === 'HEADER')` block, after the `headerAccuracyRollPending` waiting-panel early return (unchanged) and BEFORE the `bothConfirmed` post-duel block (unchanged), insert a new branch: `if (headerPositioningTurn !== null) { ... }`. If `myTeam` is the currently-acting side (derive the same way Task 1/Plan 27-05 Task 2 does), render a helper block: "Contest Header!" / "Select a player within range and move to position (or wait for it to auto-hand-off)." with no button (the move itself, via HexGrid click, IS the confirm — matches D-02's no-separate-confirm framing). If `myTeam` is NOT the acting side, return `waitingPanel`. Delete the old multi-select contestant `<>` block (`headerContestantIds`-driven checkboxes/"Confirm Selection"/"Decline (no contestant)" button) — it is dead code for this pre-step now that a single click-to-move replaces it. Leave `bothConfirmed`'s post-duel panel completely untouched (D-02: existing duel/target flow proceeds exactly as today).
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- ActionPanel</automated>
  </verify>
  <done>ActionPanel shows a single-select positioning prompt during headerPositioningTurn, a waiting panel for the non-acting side, and the unchanged post-duel target-selection panel once both sides have positioned.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                        |
| --------------- | ---------------------------------------------------------------------------------- |
| N/A (this plan) | Client-side UX only; all authoritative validation lives server-side in Plan 27-05. |

## STRIDE Threat Register

| Threat ID | Category  | Component    | Disposition | Mitigation Plan                   |
| --------- | --------- | ------------ | ----------- | --------------------------------- |
| T-27-SC   | Tampering | npm installs | accept      | No package installs in this plan. |

</threat_model>

<verification>
- `cd packages/client && pnpm test -- useGameStore HexGrid ActionPanel` exits 0.
- `pnpm -r typecheck` exits 0.
- Manual: play a match to a HIGH_PASS→HEADER transition with eligible pieces on both sides; confirm attacker positions first, hand-off is automatic, defender positions second, duel/target flow proceeds unchanged (covered by the Plan 27-12 end-of-phase checkpoint).
</verification>

<success_criteria>
HEADER positioning is fully playable end-to-end: select → white/badged highlight → click →
automatic hand-off → (repeat for defender) → unchanged duel/target flow.
</success_criteria>

<output>
Create `.planning/phases/27-response-activation-model/27-06-SUMMARY.md` when done.
</output>
