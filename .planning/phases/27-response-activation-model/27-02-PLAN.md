---
phase: 27-response-activation-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/components/HexCell.tsx
  - packages/client/src/components/HexCell.test.tsx
autonomous: true
requirements: [RESP-02]
must_haves:
  truths:
    - "A hex can render a plain white 'response' highlight for a valid response-move destination"
    - 'A hex flagged as incurring a challenge penalty additionally shows a small -1 text badge on top of the white fill'
  artifacts:
    - path: 'packages/client/src/components/HexCell.tsx'
      provides: "HexHighlightType includes 'response'; HIGHLIGHT_STYLES has a 'response' entry; a new optional penalty prop renders a -1 badge"
      contains: "'response'"
  key_links:
    - from: 'packages/client/src/components/HexCell.tsx'
      to: 'packages/client/src/components/HexGrid.tsx (consumed in Plan 27-03+)'
      via: "highlightType='response' + penalty prop"
      pattern: 'penalty'
---

<objective>
Add the two new client-only visual primitives D-06/D-07/D-09 require: a `'response'`
`HexHighlightType` (plain white fill, distinct styling weight from the existing
`shot-path`/`shot-path-action` types it will eventually replace for GK_DIVE and
SNAPSHOT_DEFLECT) and a `-1` penalty badge overlay. This plan touches only `HexCell.tsx` —
no phase/state wiring — so it can run independently of every other Wave 1/2 plan.

Purpose: give every later response-type plan (03, 05/06, 07/08, 09/10, 11) a single,
already-tested rendering primitive to point at instead of re-inventing hex-highlight styling
per response type.
Output: extended `HexCell.tsx` + test coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-response-activation-model/27-CONTEXT.md
@packages/client/src/components/PieceOverlay.tsx
</context>

<artifacts_produced>

- `HexHighlightType` gains `'response'`.
- `HIGHLIGHT_STYLES.response` — white fill, `restOpacity: 0.5`, `hoverOpacity: 0.65`, `stroke: '#dddddd'`, `strokeWidth: 1.5` (same visual family as `shot-path`/`shot-path-action`, since `'response'` replaces both of those roles for GK_DIVE/SNAPSHOT_DEFLECT in Plan 27-03).
- `HexCell` gains an optional `penalty?: boolean` prop: when `true` AND `highlightType === 'response'`, renders a small `-1` `<text>` badge centered on the hex, `pointerEvents="none"` (mirrors the jersey-number `<text>` pattern in `PieceOverlay.tsx:254-267`, not a new rendering approach — D-07/Claude's Discretion).
  </artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 'response' highlightType + -1 penalty badge</name>
  <files>
    packages/client/src/components/HexCell.tsx
    packages/client/src/components/HexCell.test.tsx
  </files>
  <read_first>
    - packages/client/src/components/HexCell.tsx (full file, 132 lines — `HexHighlightType` union at 6-13, `HIGHLIGHT_STYLES` table at 15-68, `Props` at 70-74, render body at 81-132; note the existing `isDifficultAngle` dot pattern at 126-129 as the closest precedent for "conditional small SVG overlay on top of the base hex")
    - packages/client/src/components/HexCell.test.tsx (full file — existing test shape: renders `<svg><HexCell .../></svg>`, queries `container.querySelectorAll('polygon')` / attribute checks)
    - packages/client/src/components/PieceOverlay.tsx (lines 253-267 — jersey-number `<text>` element: `x={cx} y={cy} textAnchor="middle" dominantBaseline="central" fontSize={15} fontWeight={700} pointerEvents="none"`)
  </read_first>
  <behavior>
    - `highlightType='response'` renders an overlay `<polygon>` with `fill="rgba(255,255,255,1)"` (same white as `shot-path`/`shot-path-action` — asserted via the existing `fills` array pattern in `HexCell.test.tsx`).
    - `highlightType='response'` with no `penalty` prop (or `penalty={false}`) renders NO `<text>` element containing `-1`.
    - `highlightType='response'` with `penalty={true}` renders exactly one `<text>` element whose text content is `-1`.
    - `penalty={true}` with a highlightType other than `'response'` (or `highlightType=undefined`) renders NO `-1` badge — the badge is only ever meaningful alongside the response highlight.
  </behavior>
  <action>
    Add `'response'` to the `HexHighlightType` union (after `'header-target'`). Add a `response` entry to `HIGHLIGHT_STYLES` per `<artifacts_produced>`'s exact values. Add `penalty?: boolean` to `Props`. In the render body, after the existing highlight-overlay `polygon` block, add a conditional block: `{highlightType === 'response' && penalty === true && (<text x={cx} y={cy} textAnchor="middle" dominantBaseline="central" fontSize={11} fontWeight={700} fill="#1a1a1a" pointerEvents="none">-1</text>)}` — small, dark text so it reads clearly against the white fill; do not reuse `PieceOverlay`'s `fontSize={15}`/light fill (that's sized/colored for a jersey number over a piece circle, not a hex overlay). Extend `HexCell.test.tsx` with the four behavior cases above, following the file's existing `renderHighlighted` helper pattern (extend it to accept an optional `penalty` arg).
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- HexCell</automated>
  </verify>
  <done>HexCell renders the new 'response' white highlight and, when penalty=true, a centered -1 text badge; all four test cases pass; no existing HexCell.test.tsx assertions regress.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                           |
| --------------- | ------------------------------------------------------------------------------------- |
| N/A (this plan) | Pure presentational component change — no data enters from the network at this layer. |

## STRIDE Threat Register

| Threat ID | Category  | Component    | Disposition | Mitigation Plan                   |
| --------- | --------- | ------------ | ----------- | --------------------------------- |
| T-27-SC   | Tampering | npm installs | accept      | No package installs in this plan. |

</threat_model>

<verification>
- `cd packages/client && pnpm test -- HexCell` exits 0.
- `pnpm -r typecheck` exits 0.
</verification>

<success_criteria>
HexCell.tsx exposes a ready-to-consume 'response' highlight + -1 badge primitive, fully unit
tested, with zero coupling to any specific game phase.
</success_criteria>

<output>
Create `.planning/phases/27-response-activation-model/27-02-SUMMARY.md` when done.
</output>
