---
phase: 27-response-activation-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/shared/src/events.ts
  - packages/shared/src/deflectValidator.ts
  - packages/shared/src/deflectValidator.test.ts
  - packages/shared/src/index.ts
autonomous: true
requirements: [RESP-01, RESP-03, RESP-04, RESP-05, RESP-06, RESP-07, RESP-08, RESP-09]
must_haves:
  truths:
    - 'All new GamePhase values, GameState fields, and ActionEvent variants this phase needs exist in packages/shared/src/types.ts before any server/client plan references them'
    - "A pure, testable function determines whether a defender's position is within the shot-path deflect eligibility radius (D-12/D-20), independent of movement-pace concerns"
  artifacts:
    - path: 'packages/shared/src/types.ts'
      provides: "GamePhase: 'FINAL_THIRD_RESPONSE' | 'GK_BALL_IN_BOX'; new GameState optional fields for header positioning, final third, keeper snap, and keeper ball-in-box; new ActionEventType/ActionEvent members"
      contains: 'FINAL_THIRD_RESPONSE'
    - path: 'packages/shared/src/deflectValidator.ts'
      provides: 'computeDeflectEligibility(defenderPosition, shotOrigin, shotTarget) -> boolean, per D-12 (distance <=3 from hexLine)'
      exports: ['computeDeflectEligibility']
    - path: 'packages/shared/src/deflectValidator.test.ts'
      provides: 'Unit coverage for computeDeflectEligibility boundary (dist 3 true, dist 4 false)'
  key_links:
    - from: 'packages/shared/src/deflectValidator.ts'
      to: 'packages/shared/src/hex.ts hexLine/hexDistance'
      via: 'import'
      pattern: 'hexLine|hexDistance'
    - from: 'packages/shared/src/index.ts'
      to: 'packages/shared/src/deflectValidator.ts'
      via: 'barrel export'
      pattern: 'deflectValidator'
---

<objective>
Establish the shared type contracts and one net-new pure validator that every subsequent
Phase 27 plan (server engine, server handlers, client store, client rendering) depends on.
This is interface-first work per D-01 through D-21: no behavior changes yet, only the
vocabulary (phase names, state fields, event types, socket events) that later plans wire up.

Purpose: prevent every downstream plan from inventing its own ad hoc field names or
guessing at shapes — they all read this file's additions as the single source of truth.
Output: extended `types.ts`/`events.ts`, new `deflectValidator.ts` + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-response-activation-model/27-CONTEXT.md
@.planning/phases/27-response-activation-model/RESEARCH.md
@packages/shared/src/headingValidator.ts
@packages/shared/src/hex.ts
</context>

<artifacts_produced>

## Artifacts this plan produces

New symbols:

- `packages/shared/src/deflectValidator.ts` — `computeDeflectEligibility(defenderPosition, shotOrigin, shotTarget): boolean`
- `packages/shared/src/deflectValidator.test.ts` — unit tests
- `GamePhase` members: `'FINAL_THIRD_RESPONSE'`, `'GK_BALL_IN_BOX'`
- `GameState` optional fields (all `?:`, matching the file's existing optional-field convention — see `types.ts:580-660`):
  - `headerPositioningTurn?: 'attacker' | 'defender' | null` — D-02
  - `headerPositionMovedPieceId?: string | null` — D-02
  - `headerPositionPaceUsed?: number` — D-02
  - `finalThirdEligibleIds?: { attack: readonly string[]; defense: readonly string[] } | null` — D-13/D-21 (mirrors `freeMoveEligibleIds` shape/naming for consistency)
  - `finalThirdUsedPace?: Readonly<Record<string, number>> | null` — D-04
  - `finalThirdMovementSlot?: 'ATTACKER' | 'DEFENDER' | null` — D-21
  - `finalThirdResume?: { phase: GamePhase; activeTeam: 'home' | 'away' } | null` — D-04 (mirrors `freeMoveResume`)
  - `keeperRepositionedForZone?: { home: boolean; away: boolean }` — D-15
  - `gkBallInBoxPosition?: HexCoord | null` — D-05/D-14 (mirrors `gkDivePosition`)
  - `gkBallInBoxResume?: { phase: GamePhase; activeTeam: 'home' | 'away' } | null` — D-05
  - `ballInPenaltyArea?: 'home' | 'away' | 'none'` — D-19 (mirrors `ballZone` shape, separate tracker since penalty area is a narrower nested zone)
- `ActionEventType` members: `'HEADER_POSITION_MOVE'`, `'FINAL_THIRD_MOVE'`, `'GK_BALL_IN_BOX_MOVE'`, `'KEEPER_REPOSITION'`, `'RESPONSE_SKIP'`
- `ActionEvent` union variants for each of the above (shapes below)
- `ClientEvents.GAME_GK_BALL_IN_BOX = 'game:gk-ball-in-box'`, `ClientToServerEvents[GAME_GK_BALL_IN_BOX]: (to: HexCoord) => void`

No new npm/pip/cargo packages installed by this plan.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 27 GamePhase/GameState/ActionEvent contracts to shared types</name>
  <files>
    packages/shared/src/types.ts
    packages/shared/src/events.ts
  </files>
  <read_first>
    - packages/shared/src/types.ts (lines 378-405 — `GamePhase` union, note the existing `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE` comment style to match; lines 445-759 — `GameState`, especially the `freeMoveEligibleIds`/`freeMoveUsedPace`/`freeMoveResume` block at 683-752 as the direct shape template for the new final-third fields, and `gkDivePosition`/`snapDeflectMovedPieceId` at 605-614 as the template for `gkBallInBoxPosition`)
    - packages/shared/src/events.ts (lines 10-82 `ClientEvents`, 41-53 `GAME_GK_DIVE`/`GAME_HEADER_ACCURACY_ACK` as the naming/comment template for `GAME_GK_BALL_IN_BOX`; lines 123-202 `ClientToServerEvents`)
  </read_first>
  <action>
    In `types.ts`:
    1. Add `'FINAL_THIRD_RESPONSE'` and `'GK_BALL_IN_BOX'` to the `GamePhase` union (after `'FREE_MOVE_DEFENSE'`), each with a one-line comment citing D-04/D-13 and D-05/D-14/D-19 respectively.
    2. Add the 5 new `ActionEventType` string literals listed in `<artifacts_produced>` to the union (after `'FK_STAGE_ADVANCE'`), each commented with its owning decision ID.
    3. Add matching `ActionEvent` discriminated-union members:
       - `HEADER_POSITION_MOVE`: `{ type; slot: 'attacker' | 'defender'; pieceId: string; from: HexCoord; to: HexCoord; timestamp: number }` (mirrors `SNAP_DEFLECT_MOVE`'s shape — no `ballAfter`, ball does not move during positioning)
       - `FINAL_THIRD_MOVE`: `{ type; slot: 'ATTACKER' | 'DEFENDER'; pieceId: string; from: HexCoord; to: HexCoord; timestamp: number }` (mirrors `GK_KICK_MOVE`'s shape)
       - `GK_BALL_IN_BOX_MOVE`: `{ type; pieceId: string; from: HexCoord; to: HexCoord; timestamp: number }` (mirrors `SNAP_DEFLECT_MOVE`)
       - `KEEPER_REPOSITION`: `{ type; pieceId: string; from: HexCoord; to: HexCoord; zone: 'home' | 'away'; timestamp: number }` — D-15/D-16, logs the instant auto-snap
       - `RESPONSE_SKIP`: `{ type; responseType: 'HEADER' | 'DEFLECT' | 'FINAL_THIRD' | 'GK_BALL_IN_BOX'; reason: 'NO_ELIGIBLE_PLAYERS'; timestamp: number }` — RESP-08 "logs the fact" requirement, uniform across all 4 response types that can have zero eligible players
    4. Add the 11 new optional `GameState` fields listed in `<artifacts_produced>`, placed near their thematic siblings (header fields near `headerTargetHex`/`headerWinnerId` ~line 601-602; final-third fields near `freeMoveResume` ~line 752; keeper/ball-in-box fields as a new block after `passerId` ~line 758). Use `exactOptionalPropertyTypes`-safe optional syntax (`?: X | null`, matching `headerDuelWinner?: 'home' | 'away' | null` at line 600) — do not use `| undefined`.

    In `events.ts`:
    5. Add `GAME_GK_BALL_IN_BOX: 'game:gk-ball-in-box'` to `ClientEvents` (after `GAME_GK_DIVE`), with a comment citing D-05/D-14/D-19 and noting it mirrors `GAME_GK_DIVE`'s single-click, no-`selectPiece` pattern.
    6. Add `[ClientEvents.GAME_GK_BALL_IN_BOX]: (to: HexCoord) => void;` to `ClientToServerEvents` (after the `GAME_GK_DIVE` entry).

    Do NOT modify `ELIGIBLE_NEXT_ACTIONS` or `LastActionType` — the new phases are overlay/interrupt phases (like `FREE_MOVE_ATTACK`/`FREE_MOVE_DEFENSE`), not `lastActionType`-driven "next action" choices.

  </action>
  <verify>
    <automated>cd packages/shared && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>types.ts and events.ts compile with all new GamePhase/GameState/ActionEvent/event contracts present; `pnpm -r typecheck` at the repo root still passes for server and client packages even though they don't yet consume the new fields (no breaking change to existing code).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeDeflectEligibility shared validator (D-12/D-20)</name>
  <files>
    packages/shared/src/deflectValidator.ts
    packages/shared/src/deflectValidator.test.ts
    packages/shared/src/index.ts
  </files>
  <read_first>
    - packages/shared/src/headingValidator.ts (full file — mirror this file's shape: JSDoc header citing the owning decision IDs, a single pure exported function, no GameState mutation)
    - packages/shared/src/hex.ts (hexLine, hexDistance signatures)
    - packages/shared/src/index.ts (barrel export list — confirm the exact re-export pattern used for headingValidator/shotValidator)
  </read_first>
  <behavior>
    - Given a shot path from `{q:20,r:13}` to `{q:30,r:13}` (a straight `hexLine`) and a defender exactly 3 hexes from the nearest point on that line, `computeDeflectEligibility` returns `true` (D-12: "within hex-distance 3 of any point on the line").
    - Given a defender exactly 4 hexes from the nearest point on the line, returns `false`.
    - Given a defender standing directly ON a path hex (distance 0), returns `true`.
    - Function does not mutate or read `GameState` — pure geometry only (D-20: this is an eligibility/highlight filter, NOT a movement-pace concern; the 2-hex pace cap stays a separate, unchanged concern owned by `gameHandlers.ts`/`useGameStore.ts`).
  </behavior>
  <action>
    Create `computeDeflectEligibility(defenderPosition: HexCoord, shotOrigin: HexCoord, shotTarget: HexCoord): boolean` in `deflectValidator.ts`. Implementation: compute `hexLine(shotOrigin, shotTarget)`, return `true` if `Math.min(...pathHexes.map(h => hexDistance(defenderPosition, h))) <= 3`, else `false`. JSDoc cites D-12 ("defenders within hex-distance 3 of any point on the shot path") and D-20 (clarifies this is an eligibility radius, not a movement budget — the existing SNAPSHOT_DEFLECT `paceCap: 2` in `gameHandlers.ts`/`useGameStore.ts` is unchanged and is a downstream concern for Plan 27-04). Add the barrel export to `index.ts` alongside the other validators.
  </action>
  <verify>
    <automated>cd packages/shared && pnpm test -- deflectValidator</automated>
  </verify>
  <done>computeDeflectEligibility exported from @counter-attack/shared, boundary-tested at distance 3 (true) and 4 (false), zero GameState coupling.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                                                                                                                                                    |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| N/A (this plan) | Pure type/contract additions and a pure geometry function — no new network input surface, no state mutation. Trust boundaries are established by the plans that consume these contracts (27-04 through 27-11). |

## STRIDE Threat Register

| Threat ID | Category  | Component    | Disposition | Mitigation Plan                                     |
| --------- | --------- | ------------ | ----------- | --------------------------------------------------- |
| T-27-SC   | Tampering | npm installs | accept      | No package installs in this plan; nothing to audit. |

</threat_model>

<verification>
- `pnpm -r typecheck` exits 0 (types.ts/events.ts changes don't break existing server/client compilation).
- `cd packages/shared && pnpm test` exits 0 (new deflectValidator test + all existing shared tests green).
- `pnpm -r build` exits 0.
</verification>

<success_criteria>
Every GamePhase value, GameState field, ActionEvent variant, and socket event that Phase 27's
remaining 11 plans reference already exists and typechecks. computeDeflectEligibility is
available, tested, and exported for reuse by both server (Plan 27-04) and any future client
mirror.
</success_criteria>

<output>
Create `.planning/phases/27-response-activation-model/27-01-SUMMARY.md` when done.
</output>
