---
phase: 27-response-activation-model
plan: 03
type: execute
wave: 2
depends_on: ['27-01', '27-02']
files_modified:
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/HexGrid.test.tsx
autonomous: true
requirements: [RESP-02, RESP-07, RESP-09]
must_haves:
  truths:
    - 'During any response phase (HEADER, SNAPSHOT_DEFLECT, GK_DIVE, FINAL_THIRD_RESPONSE, GK_BALL_IN_BOX) the hex containing the ball shows a gold ring, rendered under the ball marker'
    - "GK_DIVE valid dive hexes render with the new white 'response' highlight instead of 'shot-path-action', with a -1 badge at exactly distance 3 from the GK's reference position (D-08/D-11) and no badge at distance 1-2"
    - "SNAPSHOT_DEFLECT valid move hexes render with the new white 'response' highlight instead of 'shot-path-action'/'shot-path', with no badge (D-08)"
  artifacts:
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: "Generalized ball-hex ring overlay; 'response' inserted into the highlight priority ternary between goal and shot-path; GK_DIVE and SNAPSHOT_DEFLECT reclassified to 'response'"
      contains: 'RESPONSE_PHASES'
  key_links:
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'packages/client/src/components/HexCell.tsx HIGHLIGHT_STYLES.response'
      via: "highlightType='response' prop"
      pattern: 'highlightType: HexHighlightType | undefined'
    - from: 'packages/client/src/components/HexGrid.tsx ball-hex ring'
      to: 'packages/client/src/components/BallMarker.tsx'
      via: 'DOM order (ring drawn in Layer 1 hex loop, BallMarker in Layer 2)'
      pattern: 'BallMarker'
---

<objective>
Retrofit the two already-implemented response phases (GK_DIVE, SNAPSHOT_DEFLECT) onto the
new `'response'` highlight primitive from Plan 27-02, generalize the existing HEADER-only
gold ball overlay into a ring shown during every response phase (RESP-09), and insert
`'response'` into `HexGrid.tsx`'s highlight-priority ternary at the correct precedence. This
is the last shared-infrastructure plan before response-type-specific work (Plans 27-04+)
starts layering new phases on top.

Purpose: D-01 scopes this phase to positioning/selection UX only — GK_DIVE's and
SNAPSHOT_DEFLECT's underlying mechanics (`applyGKDive`, the SNAPSHOT_DEFLECT deflect roll)
are untouched here; only their hex-highlight classification changes.
Output: `HexGrid.tsx` renders the unified response-phase visual language for the two phases
that already exist; new phases plug into the same ternary in later plans.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-response-activation-model/27-CONTEXT.md
@.planning/phases/27-response-activation-model/RESEARCH.md
@packages/client/src/components/HexCell.tsx
</context>

<artifacts_produced>

- `RESPONSE_PHASES` — a `Set<GamePhase>` (or equivalent inline check) covering `'HEADER'`, `'SNAPSHOT_DEFLECT'`, `'GK_DIVE'`, `'FINAL_THIRD_RESPONSE'`, `'GK_BALL_IN_BOX'` — used by the generalized ball-hex ring condition. Later plans (27-06, 27-10, 27-11) do not need to touch this set again; it already lists every phase this feature covers, even before those phases have real behavior wired up.
- Ball-hex ring: replaces the existing gold filled-overlay block (`HexGrid.tsx:589-599`) with a stroke-only ring (`fill="none"`, `stroke="#f5c518"`, `strokeWidth={2}`), condition changed from `phase === 'HEADER'` to `RESPONSE_PHASES.has(phase)`.
- Priority ternary (`HexGrid.tsx:480-496`): `'response'` inserted between `isGoalTint` and the shot-path checks — precedence becomes `risk > goal > response > shot-path > kickoff > safe` (Claude's Discretion per D-06, resolved: response occupies the same precedence slot `shot-path-action` used to occupy, since it replaces that role).
- `isGKDiveTarget`/`isHpMoveTarget` (SNAPSHOT_DEFLECT) reclassified from `'shot-path-action'` to `'response'`; new `isGKDivePenalty` (distance === 3 from `gkDivePosition`) drives the `penalty` prop on `<HexCell>` for GK_DIVE hexes only — SNAPSHOT_DEFLECT never sets `penalty` (D-08: no penalty tiering for DEFLECT).
  </artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ball-hex ring generalization (RESP-09)</name>
  <files>
    packages/client/src/components/HexGrid.tsx
    packages/client/src/components/HexGrid.test.tsx
  </files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (lines 1-30 imports; lines 562-600 — the per-hex `<g>` return block including the existing HEADER-only gold overlay at 589-599; confirm this block sits inside the `PITCH_HEXES.map` loop, i.e. Layer 1, and `<BallMarker>` is Layer 2 at ~line 659 — DOM order alone satisfies "renders under BallMarker", no z-index needed)
    - packages/client/src/components/HexGrid.test.tsx (existing test structure/mocking pattern for `useGameStore`)
  </read_first>
  <behavior>
    - When `phase` is any of `HEADER`, `SNAPSHOT_DEFLECT`, `GK_DIVE`, `FINAL_THIRD_RESPONSE`, `GK_BALL_IN_BOX`, the hex at `ball.position` renders a `<polygon>` with `fill="none"` and `stroke="#f5c518"`.
    - When `phase` is any other value (e.g. `MOVE`, `PASS`), no such ring polygon is rendered at the ball's hex.
    - The ring polygon element appears before `<BallMarker>` in the rendered DOM (Layer 1 before Layer 2 — assert via `container.innerHTML` substring ordering or a DOM-position comparison).
  </behavior>
  <action>
    Define `const RESPONSE_PHASES = new Set<GamePhase>(['HEADER', 'SNAPSHOT_DEFLECT', 'GK_DIVE', 'FINAL_THIRD_RESPONSE', 'GK_BALL_IN_BOX']);` near the top of the component (or module scope). Replace the `phase === 'HEADER' && hex.q === ball.position.q && hex.r === ball.position.r` condition at `HexGrid.tsx:590` with `RESPONSE_PHASES.has(phase) && hex.q === ball.position.q && hex.r === ball.position.r`. Change the polygon from a filled overlay (`fill="#f5c518" fillOpacity={0.5}`) to a stroke-only ring (`fill="none" stroke="#f5c518" strokeWidth={2} pointerEvents="none"`) per D-09 ("ring/outline", not filled, static — no animation/pulse). Import `GamePhase` from `@counter-attack/shared` if not already imported.
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- HexGrid</automated>
  </verify>
  <done>Ball-hex ring renders (stroke-only, gold) during all 5 response phases and only those phases; existing HEADER-specific test coverage still passes with the new stroke-only assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GK_DIVE + SNAPSHOT_DEFLECT visual retrofit to 'response' (D-08/D-11, D-01/D-03 scope: visual only)</name>
  <files>
    packages/client/src/components/HexGrid.tsx
    packages/client/src/components/HexGrid.test.tsx
  </files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (lines 152-166 — `gkDiveTargetSet` build loop; lines 394-496 — `isGKDiveTarget`, `isHpMoveTarget`, `isShotPathActionTint`, `isShotPathTint`, and the priority ternary itself; lines 516-518 — GK_DIVE's dedicated onClick branch, must stay wired to the SAME `isGKDiveTarget` boolean, only the highlightType output changes)
  </read_first>
  <behavior>
    - A hex in `gkDiveTargetSet` at hex-distance exactly 3 from `gkDivePosition` (GK's reference position, per `applyGKDive`'s `hexDistance(gk.position, to)` semantics) renders `highlightType='response'` and `penalty={true}` (shows the -1 badge).
    - A hex in `gkDiveTargetSet` at hex-distance 1 or 2 from `gkDivePosition` renders `highlightType='response'` and `penalty={false}` (no badge).
    - During `SNAPSHOT_DEFLECT` with a piece selected, a valid move hex renders `highlightType='response'` with `penalty` absent/false (D-08: no penalty tiering for DEFLECT) — never `'shot-path-action'` or `'shot-path'`.
    - GK_DIVE's existing click behavior (`onClick = () => emitGKDive(hex)` for `isGKDiveTarget && isGKTeamPlayer`) and SNAPSHOT_DEFLECT's existing click behavior (generic `isValidMove && selectedPieceId → emitMove`) are unchanged — only the `highlightType`/`penalty` classification changes, per D-01 (no resolution-mechanic changes).
  </behavior>
  <action>
    In the priority ternary (`HexGrid.tsx:480-496`), remove `isShotPathActionTint` as a distinct branch and fold its two contributors (`isGKDiveTarget`, `highPassContestZoneSet.has(hexId) && isValidMove` — the latter is HIGH_PASS_MOVE's contest-zone preview and is OUT OF SCOPE for this phase, leave it mapped to its current tint) into the new `'response'` branch ONLY for the `isGKDiveTarget` and SNAPSHOT_DEFLECT (`isHpMoveTarget`'s SNAPSHOT_DEFLECT half) cases — do not change HIGH_PASS_MOVE's contest-zone tint or GK_KICK_MOVE/HIGH_PASS_MOVE/FIRST_TIME_PASS_MOVE's existing 'safe' classification (out of scope per D-01/CONTEXT boundary — only HEADER, DEFLECT, FINAL_THIRD, DIVE, keeper ball-in-box are in scope). Concretely: add `isResponseMoveTint = isGKDiveTarget || (phase === 'SNAPSHOT_DEFLECT' && selectedPieceId !== null && isValidMove)`, insert it into the ternary at `isGoalTint ? 'goal' : isResponseMoveTint ? 'response' : isShotPathActionTint ? ... (keep isShotPathActionTint for the remaining HIGH_PASS_MOVE-only case) : isShotPathTint ? 'shot-path' : ...`. Compute `isGKDivePenalty = isGKDiveTarget && gkDivePosition != null && hexDistance(gkDivePosition, hex) === 3`, pass `penalty={isResponseMoveTint ? isGKDivePenalty : undefined}` to `<HexCell>` (SNAPSHOT_DEFLECT hexes never set `isGKDivePenalty` true since that flag is gated on `isGKDiveTarget`). Update `HexGrid.test.tsx` with the four behavior assertions.
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- HexGrid</automated>
  </verify>
  <done>GK_DIVE and SNAPSHOT_DEFLECT hex highlights render via the new 'response' type; GK_DIVE shows the -1 badge only at distance 3; SNAPSHOT_DEFLECT never shows a badge; both phases' click-to-move behavior is unchanged; full client test suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                                        |
| --------------- | -------------------------------------------------------------------------------------------------- |
| N/A (this plan) | Presentational reclassification only; no new click targets, no change to which server events fire. |

## STRIDE Threat Register

| Threat ID | Category  | Component    | Disposition | Mitigation Plan                   |
| --------- | --------- | ------------ | ----------- | --------------------------------- |
| T-27-SC   | Tampering | npm installs | accept      | No package installs in this plan. |

</threat_model>

<verification>
- `cd packages/client && pnpm test -- HexGrid` exits 0.
- `pnpm -r typecheck` exits 0.
- Manual spot-check (covered by the Plan 27-12 end-of-phase checkpoint): start a match, force a shot to reach GK_DIVE, confirm white hexes + -1 badge at the outer ring; force a snapshot to reach SNAPSHOT_DEFLECT, confirm white hexes with no badge; confirm the gold ball ring appears in both phases.
</verification>

<success_criteria>
GK_DIVE and SNAPSHOT_DEFLECT visually match the unified response-move language (white
hexes, badge only where D-08 requires one) with zero mechanic changes. The ball-hex ring
generalizes correctly to all 5 response phases (2 real today, 3 not yet wired — later plans
rely on RESPONSE_PHASES already listing them).
</success_criteria>

<output>
Create `.planning/phases/27-response-activation-model/27-03-SUMMARY.md` when done.
</output>
