---
phase: 27-response-activation-model
plan: 11
type: execute
wave: 7
depends_on: ['27-09', '27-10']
files_modified:
  - packages/client/src/store/useGameStore.ts
  - packages/client/src/components/HexGrid.tsx
  - packages/client/src/components/ActionPanel.tsx
autonomous: true
requirements: [RESP-01, RESP-02]
must_haves:
  truths:
    - "When the ball enters a penalty area and the keeper has a legal move, exactly the keeper's team sees up to 7 white response hexes (own hex + up to 6 neighbors) with no piece-selection step required"
    - 'Clicking a highlighted hex repositions the keeper and immediately returns play to whatever phase preceded the interrupt'
  artifacts:
    - path: 'packages/client/src/store/useGameStore.ts'
      provides: 'emitGkBallInBox action; setGameState handles GK_BALL_IN_BOX phase entry/exit cleanly'
      contains: 'emitGkBallInBox'
    - path: 'packages/client/src/components/HexGrid.tsx'
      provides: 'gkBallInBoxTargetSet computed inline (mirrors gkDiveTargetSet) + dedicated onClick branch, no selectPiece involved'
      contains: 'gkBallInBoxTargetSet'
  key_links:
    - from: 'packages/client/src/components/HexGrid.tsx'
      to: 'packages/client/src/store/useGameStore.ts emitGkBallInBox'
      via: "dedicated onClick branch, mirrors GK_DIVE's isGKDiveTarget onClick branch"
      pattern: 'emitGkBallInBox'
---

<objective>
Wire the client side of Plan 27-09's `GK_BALL_IN_BOX` engine, mirroring `GK_DIVE`'s
implicit-single-piece pattern exactly (research Pattern 3): no `selectPiece` step, the
target-hex set is computed unconditionally the instant the phase is entered, and a click
directly emits the dedicated event.

Purpose: complete RESP-01/RESP-02 for the last new response type.
Output: playable keeper ball-in-box UI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-response-activation-model/27-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useGameStore.ts — emitGkBallInBox + GK_BALL_IN_BOX phase-entry cleanup</name>
  <files>
    packages/client/src/store/useGameStore.ts
  </files>
  <read_first>
    - packages/client/src/store/useGameStore.ts (the `emitGKDive` action — direct template: `socket.emit(ClientEvents.GAME_GK_DIVE, to)`; `setGameState`'s generic phase-changed clear-selection branch already handles `GK_BALL_IN_BOX` correctly since it is a NEW phase value distinct from whatever preceded it — no special-casing needed there, confirm by reading the `phaseChanged` derivation)
  </read_first>
  <behavior>
    - `emitGkBallInBox(to)` emits `GAME_GK_BALL_IN_BOX` with the target hex, mirroring `emitGKDive`'s shape exactly.
    - Entering `GK_BALL_IN_BOX` from any prior phase clears `selectedPieceId`/`validMoveHexes` via the existing generic `phaseChanged` branch in `setGameState` (no new logic needed — this behavior is a confirmation check, not new code).
  </behavior>
  <action>
    Add `emitGkBallInBox: (to: HexCoord) => void` to the `GameStore` type and its implementation (`socket.emit(ClientEvents.GAME_GK_BALL_IN_BOX, to)`), placed near `emitGKDive`. Add a unit test confirming the emit call shape (mirrors any existing `emitGKDive` test if one exists, otherwise a minimal socket-mock assertion).
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- useGameStore</automated>
  </verify>
  <done>emitGkBallInBox is available and tested; GK_BALL_IN_BOX phase entry correctly resets stale selection state via existing generic logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HexGrid.tsx — implicit target set + dedicated onClick (mirrors GK_DIVE)</name>
  <files>
    packages/client/src/components/HexGrid.tsx
  </files>
  <read_first>
    - packages/client/src/components/HexGrid.tsx (lines 152-166 — `gkDiveTargetSet` build loop; lines 379-383 — `isGKDiveTarget`/`isGKTeamPlayer` derivation; lines 516-518 — the dedicated `phase === 'GK_DIVE' && isGKDiveTarget && isGKTeamPlayer` onClick branch — all three are the direct structural templates)
  </read_first>
  <behavior>
    - During `GK_BALL_IN_BOX`, the keeper's team sees up to 7 hexes (own current position + up to 6 on-pitch, unoccupied neighbors) rendered with `highlightType='response'`, no penalty badge.
    - Only the keeper's own team can click these hexes (`isGKTeamPlayer`-equivalent gate, derived from `activeTeam` per Plan 27-09's handler, which is set to the defending team).
    - Clicking a valid hex calls `emitGkBallInBox(hex)`.
    - No other team sees these hexes as clickable (opponent sees them highlighted at most, per the existing GK_DIVE precedent of gating clickability but not necessarily visibility — mirror whatever GK_DIVE does exactly for consistency).
  </behavior>
  <action>
    Add `const gkBallInBoxTargetSet = new Set<string>();` populated when `phase === 'GK_BALL_IN_BOX' && gkBallInBoxPosition != null`: for each of the keeper's current position and its 6 neighbors (reuse `hexesInRange(gkBallInBoxPosition, 1)` from `@counter-attack/shared` if available, matching the pattern used elsewhere in this file, e.g. FREE_MOVE's adjacency loop), filter to `isPitchHex` and unoccupied, add to the set. Extend `isResponseMoveTint` to include `(phase === 'GK_BALL_IN_BOX' && gkBallInBoxTargetSet.has(hexId) && isGKBallInBoxTeamPlayer)` where `isGKBallInBoxTeamPlayer` mirrors `isGKTeamPlayer`'s derivation (team matches `activeTeam` during this phase). Add a dedicated onClick branch, placed alongside the existing `GK_DIVE` branch: `else if (phase === 'GK_BALL_IN_BOX' && gkBallInBoxTargetSet.has(hexId) && isGKBallInBoxTeamPlayer) { onClick = () => emitGkBallInBox(hex); }`.
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- HexGrid</automated>
  </verify>
  <done>GK_BALL_IN_BOX renders exactly like GK_DIVE structurally: implicit target set, no selectPiece, dedicated click handler.</done>
</task>

<task type="auto">
  <name>Task 3: ActionPanel.tsx — GK_BALL_IN_BOX panel (mirrors GK_DIVE)</name>
  <files>
    packages/client/src/components/ActionPanel.tsx
  </files>
  <read_first>
    - packages/client/src/components/ActionPanel.tsx (lines 355-369 — full `GK_DIVE` panel block, the direct structural template)
  </read_first>
  <action>
    Add `if (phase === 'GK_BALL_IN_BOX') { ... }` mirroring the `GK_DIVE` block exactly: if not the keeper's team, `return waitingPanel`; otherwise render a helper block, e.g. "Ball in the Box!" / "Reposition your keeper (max 1 hex)." with no button (single click resolves, matches GK_DIVE's no-End-Turn-needed framing).
  </action>
  <verify>
    <automated>cd packages/client && pnpm test -- ActionPanel</automated>
  </verify>
  <done>GK_BALL_IN_BOX shows the keeper's team a clear one-click prompt and a waiting panel for the opponent.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                 |
| --------------- | --------------------------------------------------------------------------- |
| N/A (this plan) | Client-side UX only; server-side validation is Plan 27-09's responsibility. |

## STRIDE Threat Register

| Threat ID | Category  | Component    | Disposition | Mitigation Plan                   |
| --------- | --------- | ------------ | ----------- | --------------------------------- |
| T-27-SC   | Tampering | npm installs | accept      | No package installs in this plan. |

</threat_model>

<verification>
- `cd packages/client && pnpm test -- useGameStore HexGrid ActionPanel` exits 0.
- `pnpm -r typecheck` exits 0.
- Manual: force a loose ball / pass / shot outcome to land inside a penalty area; confirm the keeper's team sees up to 7 highlighted hexes and one click resolves and resumes normal play (covered by the Plan 27-12 end-of-phase checkpoint).
</verification>

<success_criteria>
Keeper ball-in-box is fully playable end-to-end, structurally identical in UX weight to the
already-shipped GK_DIVE feature.
</success_criteria>

<output>
Create `.planning/phases/27-response-activation-model/27-11-SUMMARY.md` when done.
</output>
