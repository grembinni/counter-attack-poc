{
  "name": "@counter-attack/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "node --eval \"const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true})\"",
    "test": "echo \"No tests yet — added in Phase 3\" && exit 0"
  },
  "dependencies": {
    "@counter-attack/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.19.19"
  }
}
