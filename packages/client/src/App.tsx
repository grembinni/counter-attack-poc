import styles from './App.module.css';

/**
 * Root application component. Stub replaced in Plan 06-03.
 * Reads `screen` from Zustand store and renders the appropriate child screen.
 */
export function App() {
  return <div className={styles.app}>Counter Attack — Loading...</div>;
}
